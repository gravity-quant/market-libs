---
phase: 17-final-live-re-verification-4-live-03
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "REQUIREMENTS.md traceability table reads Complete for REFAC-05, SEC-01, ERG-01, and LIVE-03 (D-03 closure truth)"
    - "Integration audit reports 0 BLOCKER for v1.2 close (D-03 closure truth)"
    - "pytest final count is >= the Phase 15 baseline (989 collected) and the CI gate set is green at HEAD"
    - "The phase STOPS short of PR/merge ship (D-04): no /gsd-ship, no milestone archive performed"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.2 traceability table with REFAC-05/SEC-01/ERG-01/LIVE-03 flipped Open -> Complete"
      contains: "Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/17-final-live-re-verification-4-live-03/17-VALIDATION.md"
      via: "LIVE-03 Complete annotation cites the Phase 17 LIVE-03 gate / 17-VALIDATION.md"
      pattern: "17-VALIDATION"
---

<objective>
Land the v1.2 milestone-closure truths after the operator has approved the live gate in 17-02:
flip the REQUIREMENTS.md traceability rows for REFAC-05 / SEC-01 / ERG-01 / LIVE-03 from `Open`
to `Complete` (REFAC-06 stays `Deferred`), confirm a 0-BLOCKER integration audit, and attest the
final pytest + CI gate set is green at HEAD. Per D-04 this phase STOPS short of the actual PR/merge
ship: `/gsd-ship` and the milestone archive (`gsd-complete-milestone`) remain separate, downstream
of this phase, informed by `17-VALIDATION.md`.

Purpose: Close the v1.2 requirement traceability and produce a clean, audited landing state so the
operator can later run ship/complete-milestone with confidence.
Output: REQUIREMENTS.md with 4 rows flipped to Complete; an integration-audit + pytest/CI attestation
recorded in the SUMMARY; an explicit no-ship boundary note (D-04).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-final-live-re-verification-4-live-03/17-CONTEXT.md
@.planning/phases/17-final-live-re-verification-4-live-03/17-PATTERNS.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-final-live-re-verification-4-live-03/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip REQUIREMENTS.md traceability rows REFAC-05/SEC-01/ERG-01/LIVE-03 from Open to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (traceability table lines 141-147: REFAC-05 Open, REFAC-06 Deferred, SEC-01 Open, ERG-01 Open, LIVE-03 "Open (unblocked by Phase 16 DROP)")
    - .planning/phases/17-final-live-re-verification-4-live-03/17-PATTERNS.md (target annotation idiom: `Complete (Phase 17 LIVE-03 gate, 17-VALIDATION.md)`; preserve right-pad column-width spacing; REFAC-06 row stays Deferred)
    - .planning/phases/17-final-live-re-verification-4-live-03/17-VALIDATION.md (the approved gate doc this flip cites; operator_dispositions present, status approved)
  </read_first>
  <action>
    Edit ONLY the three status cells for REFAC-05, SEC-01, ERG-01 and the LIVE-03 status cell in the
    traceability table (REQUIREMENTS.md lines ~143-147). Flip each `Open` (and LIVE-03's
    `Open (unblocked by Phase 16 DROP)`) to `Complete`, annotated `Complete (Phase 17 LIVE-03 gate,
    17-VALIDATION.md)`. Leave the REFAC-06 row exactly as-is (`Deferred (Phase 12 NO-GO 2026-06-14)`).
    Use scoped Edit replacements per row — do NOT rewrite the whole file. Preserve the existing column
    alignment / right-pad spacing in the table. Also flip the inline requirement checkboxes if the file
    keeps a `- [ ] **LIVE-03**:` line to `- [x] **LIVE-03**:` for consistency (REFAC-05/SEC-01/ERG-01 are
    already `[x]`). Do NOT touch any other section. This flip is gated on the 17-02 operator approval
    (17-VALIDATION.md `status: approved`); if that doc is not approved, STOP and surface it.
  </action>
  <verify>
    <automated>test "$(grep -E 'REFAC-05|SEC-01|ERG-01|LIVE-03' .planning/REQUIREMENTS.md | grep -c Complete)" = 4</automated>
  </verify>
  <acceptance_criteria>
    - The traceability table rows for REFAC-05, SEC-01, ERG-01, and LIVE-03 each read `Complete` (exactly 4 Complete matches across those four REQ-IDs).
    - The REFAC-06 row still reads `Deferred (Phase 12 NO-GO 2026-06-14)` (unchanged).
    - LIVE-03's annotation cites `17-VALIDATION.md`: `grep -q "LIVE-03.*Complete.*17-VALIDATION" .planning/REQUIREMENTS.md` (or the annotation appears adjacent per the table layout).
    - Only the four status cells (and optional LIVE-03 checkbox) changed: `git diff .planning/REQUIREMENTS.md` shows edits confined to the traceability table + the LIVE-03 checklist line.
    - Column alignment / right-pad spacing preserved (table still renders).
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md traceability shows Complete for the four v1.2 requirements; REFAC-06 stays
    Deferred; the change is scoped to those cells.
  </done>
</task>

<task type="auto">
  <name>Task 2: Attest 0-BLOCKER integration audit + final pytest/CI green, and record the no-ship boundary (D-04)</name>
  <files>(read-only verification — no files modified; attestation recorded in 17-03-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/17-final-live-re-verification-4-live-03/17-CONTEXT.md (D-03 closure scope: 0-BLOCKER integration audit; D-04 STOP short of ship — no /gsd-ship, no milestone archive)
    - .planning/phases/17-final-live-re-verification-4-live-03/17-01-SUMMARY.md (deterministic-gate + pytest >= 989 attestation from Wave 1)
    - .planning/phases/17-final-live-re-verification-4-live-03/17-VALIDATION.md (operator-approved gate; the integration audit must reflect 0 BLOCKER for the live gate)
    - .planning/milestones/v1.1-phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VALIDATION.md (CI Green Final Matrix pattern lines 183-195: ruff check / ruff format / mypy strict / lint-imports / pytest 3.12 / pytest 3.13)
  </read_first>
  <action>
    Run the integration-audit query (`node .claude/gsd-core/bin/gsd-tools.cjs query audit-open`) and confirm
    it reports 0 items that constitute a v1.2 BLOCKER. Known non-BLOCKER open items are expected and must be
    enumerated, not treated as blockers: the Phase 15 15-HUMAN-UAT.md partial (operator-driven, carried per
    the v1.1 deferred pattern) and the spike-codegen-libcst-v1.3.md pending todo (explicitly deferred to v1.3
    per Phase 12 NO-GO, unrelated to LIVE-03). Record the final CI gate set at HEAD mirroring the
    11-VALIDATION.md "CI Green Final Matrix": `uv run ruff check .`, `uv run ruff format --check .`,
    `uv run mypy --strict`, `uv run lint-imports`, and `uv run pytest -q` (count must be >= the Phase 15
    baseline of 989 collected / 1 deselected; CI matrix Python 3.12 + 3.13 green is the operator-confirmed
    remote gate). Write an explicit boundary note in the SUMMARY: per D-04 this phase STOPS short of the
    actual PR/merge — `/gsd-ship` and `gsd-complete-milestone` (milestone archive) are deliberately NOT
    performed here and remain downstream, informed by 17-VALIDATION.md. Do NOT run `/gsd-ship`, do NOT
    archive the milestone, do NOT create a PR branch in this plan.
  </action>
  <verify>
    <automated>test "$(uv run pytest -q --collect-only 2>/dev/null | grep -oE '[0-9]+ tests collected' | grep -oE '^[0-9]+')" -ge 989 && uv run ruff check . >/dev/null 2>&1 && uv run mypy --strict >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `node .claude/gsd-core/bin/gsd-tools.cjs query audit-open` output is captured; every listed item is explicitly classified as a non-BLOCKER for v1.2 close (0 BLOCKER), with the 15-HUMAN-UAT partial and libcst-v1.3 todo named as the known carried items.
    - `uv run pytest -q --collect-only` reports >= 989 tests collected (Phase 15 baseline preserved; Success Criterion #5).
    - CI gate set green at HEAD: `uv run ruff check .` (passes), `uv run ruff format --check .` (passes), `uv run mypy --strict` (passes), `uv run lint-imports` (4 contracts kept, 0 broken).
    - The SUMMARY contains an explicit D-04 no-ship boundary note; no `/gsd-ship`, milestone-archive, or PR-branch action was taken (git log shows no ship/merge/tag commit from this plan).
  </acceptance_criteria>
  <done>
    Integration audit shows 0 BLOCKER (carried items enumerated); pytest >= 989 and the CI gate set
    are green at HEAD; the D-04 no-ship boundary is recorded and honored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved gate doc → traceability | the REQUIREMENTS.md flip is driven by the operator-approved 17-VALIDATION.md; no live data crosses here |
| audit/CI commands → SUMMARY | gate outputs are recorded as pass/fail attestations; no payloads involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | REQUIREMENTS.md traceability table | mitigate | scoped Edit of the four status cells only; acceptance asserts the diff is confined to the table + LIVE-03 checklist line; REFAC-06 row unchanged |
| T-17-09 | Elevation of Privilege | premature ship | mitigate | D-04 boundary enforced: no /gsd-ship, no milestone archive, no PR branch; acceptance asserts no ship/merge/tag commit from this plan |
| T-17-10 | Information Disclosure | SUMMARY / audit output | mitigate | audit + CI outputs carry no credentials or live payloads; only pass/fail + item names recorded |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NO packages; no package-manager invocation occurs |
</threat_model>

<verification>
- REQUIREMENTS.md traceability reads Complete for REFAC-05/SEC-01/ERG-01/LIVE-03; REFAC-06 stays Deferred.
- `audit-open` reviewed; 0 BLOCKER for v1.2 close; carried non-BLOCKER items enumerated.
- `uv run pytest -q --collect-only` >= 989 collected; ruff check / format / mypy strict / lint-imports green at HEAD.
- D-04 no-ship boundary recorded and honored (no ship/merge/tag/PR action).
</verification>

<success_criteria>
- All v1.2 requirements (REFAC-05, SEC-01, ERG-01, LIVE-03) reflected Complete in REQUIREMENTS.md (Success Criterion #4).
- Integration audit reports 0 BLOCKER (Success Criterion #4).
- pytest final count >= Phase 15 baseline; CI matrix green (Success Criterion #5).
- Phase stops short of ship (D-04); ship/complete-milestone remain downstream.
</success_criteria>

<output>
Create `.planning/phases/17-final-live-re-verification-4-live-03/17-03-SUMMARY.md` when done.
Record: the four flipped traceability cells, the audit-open result (0 BLOCKER + enumerated carried
items), the final pytest count + CI gate results, and the explicit D-04 no-ship boundary statement.
</output>
