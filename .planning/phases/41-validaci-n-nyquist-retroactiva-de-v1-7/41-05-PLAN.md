---
phase: 41
plan: 05
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "Las 9 filas de la Phase 38 (8 de mapa + 1 manual-only) llevan exactamente una disposición con evidencia nombrada"
    - "Las dos filas de revisión de documento se disponen contra la confirmación humana fechada que existe en disco, no contra un juicio nuevo"
    - "El script que regenera snapshots se re-corre sin ensuciar el árbol, preservando el invariante del criterio 1"
    - "38-VALIDATION.md declara el SHA de v1.7 y pasa a status validated sin que nyquist_compliant cambie"
  artifacts:
    - path: ".planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md"
      provides: "Sección Validation Audit 2026-08-31 con 9 disposiciones y front-matter transformado"
      contains: "## Validation Audit 2026-08-31"
  key_links:
    - from: ".planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md"
      to: ".planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VERIFICATION.md"
      via: "las dos filas históricas citan la confirmación humana fechada del front-matter de VERIFICATION"
      pattern: "human_verification"
    - from: ".planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md"
      to: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      via: "copia literal del SHA de v1.7, del HEAD de baseline y del esqueleto de sección"
      pattern: "37a83fe693a303a551f4374f48fe6fc5521804f7"
---

<!-- planner-discipline-allow: NOT-VERIFIABLE-RETROACTIVELY -->

<objective>
Auditar la Phase 38: 8 filas de mapa más 1 fila manual-only, con dos conductas que son revisión de
documento y una que muta el árbol de trabajo.

Purpose: esta fase es la primera del conjunto donde `VERIFIED-HISTORICALLY` es la disposición correcta
y no un atajo. Dos de sus nueve filas son revisión de un censo, y la evidencia que las sostiene no es
re-derivable: es una confirmación humana con timestamp registrada en el front-matter de
`38-VERIFICATION.md`. Además, una de sus filas ejecuta el script que regenera snapshots, que escribe
cuatro archivos al árbol de trabajo — si el resultado no fuera byte-idéntico, ensuciaría el árbol e
invalidaría el criterio 1 en silencio, en medio de una fase cuyo entregable entero depende de que ese
invariante se sostenga.

Output: `38-VALIDATION.md` con `## Validation Audit 2026-08-31`, 9 disposiciones y front-matter
transformado. Cero fuente de producto tocada.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Re-ejecutar las 7 filas automatizadas de la Phase 38 y reunir la evidencia histórica de las dos de revisión</name>
  <files>.planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md (el archivo objetivo, completo — mapa de 8 filas + 1 fila manual-only)
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VERIFICATION.md (front-matter: la confirmación humana fechada)
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md (el artefacto que las dos filas de revisión auditan)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 2, 3, 6, 7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Re-Execution Results → Phase 38, § Runtime State Inventory (fila de mutación del árbol de trabajo), § CI Enforcement Map
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Re-verificar el invariante de criterio 1 con `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`
    antes de ejecutar nada. Si sale distinto de 0, ABORTAR.

    Asignar claves ordinales: `38-r01`..`38-r08` a las 8 filas del mapa en su orden de aparición
    (`38-01-01`, `38-01-02`, `38-02-01`, `38-02-02`, `38-03-01`, `38-03-02`, `38-04-01`, `38-05-01`), y
    `38-m01` a la única fila de `## Manual-Only Verifications`. Denominador 9.

    Re-ejecutar las 7 filas automatizadas. Salidas de referencia medidas por el researcher esta sesión:
    el selector de campo de modelo opcional con 1 pasado; el de alias literal opcional con 1; el de
    puntas con 5; el de ida y vuelta con 4; mypy sobre el paquete de iol reportando ausencia de
    problemas sobre 31 archivos de fuente; y la suite roja de tipos de superficie de matriz con 19
    pasados. La evidencia que se escribe es la salida de esta corrida.

    Tratar con cuidado la fila `38-r06`, la que regenera snapshots: su comando corre el generador y
    después mira el diff. Ese generador **escribe cuatro archivos** bajo `verification/snapshots/`. La
    medición de esta sesión dio salida byte-idéntica con el árbol limpio, pero eso hay que re-confirmar,
    no asumir. Después de correrlo, comprobar que `git status --porcelain` está vacío y que
    `git diff --stat verification/snapshots/` no reporta cambios. Si el árbol quedara sucio, ABORTAR el
    plan y reportarlo: un árbol sucio invalida el criterio 1 de toda la fase 41, no sólo esta fila.

    Reunir la evidencia histórica de las dos filas de revisión de documento, `38-r08` y `38-m01`. Ambas
    apuntan a la completitud de disposiciones de `38-CENSUS.md`, y ninguna es re-derivable: lo que
    existe es una confirmación humana con timestamp en el front-matter de `38-VERIFICATION.md`, bajo la
    clave de verificación humana, fechada el 2026-08-29. Leer ese front-matter y transcribir la marca
    de tiempo exacta; confirmar además que `38-CENSUS.md` sigue en disco. Disponer ambas
    `VERIFIED-HISTORICALLY` por R-06. NO re-hacer la revisión del censo con criterio nuevo: eso
    reemplazaría la evidencia registrada por un juicio de esta sesión, que es una forma de re-definir el
    criterio en vez de auditarlo.

    Aplicar el guardia anti-vacuidad de la sección 6 del contrato a las 7 filas automatizadas.
    RESEARCH.md no midió ninguna fila de selección vacía en esta fase; si aparece una, escalarla.

    Recortar toda salida a su línea de resumen antes de escribirla al archivo.
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_models.py -k "puntas or round_trip" -q && uv run mypy packages/iol-client && uv run python verification/regen_snapshots.py && test -z "$(git status --porcelain)" && test -f .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md && grep -q 'human_verification' .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VERIFICATION.md && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - Las 9 filas tienen clave ordinal asignada (`38-r01`..`38-r08` más `38-m01`), únicas.
    - `uv run mypy packages/iol-client` sale 0 y su línea de resumen queda transcrita.
    - Después de correr el regenerador de snapshots, `git status --porcelain` devuelve cadena vacía y `git diff --stat verification/snapshots/` no reporta archivos cambiados.
    - `.planning/milestones/v1.7-phases/38-*/38-CENSUS.md` existe en disco.
    - La marca de tiempo exacta de la confirmación humana leída del front-matter de `38-VERIFICATION.md` queda transcrita literal (formato ISO con zona) para las filas `38-r08` y `38-m01`.
    - Cada una de las 7 filas automatizadas tiene una línea de resumen con conteo de tests pasados distinto de cero o, para las de tipo estático y snapshot, un código de salida 0 transcrito.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al terminar; `git status --porcelain verification/` vacío.
  </acceptance_criteria>
  <done>Las 7 filas automatizadas tienen evidencia de esta sesión, el regenerador de snapshots se corrió sin ensuciar el árbol, y las dos filas de revisión tienen su confirmación humana fechada transcrita literal.</done>
</task>

<task type="execute">
  <name>Task 2: Escribir la sección de auditoría de la Phase 38 y transformar su front-matter</name>
  <files>.planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 4, 5, 7, 8
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección, estilo de celda de evidencia, y el párrafo modelo que justifica por qué las filas manual-only siguen siendo manuales)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (par de encabezados en negrita, escalaciones, línea tildada de sign-off)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (prosa modelo para un resultado PARTIAL)
  </read_first>
  <action>
    Escribir `## Validation Audit 2026-08-31` al final de `38-VALIDATION.md`, precedida de una regla
    horizontal, después de `## Validation Sign-Off`, con los ocho bloques de la sección 5 del contrato.

    Valores concretos para esta fase: denominador **9**; disposición esperada 7 `VERIFIED-NOW`,
    2 `VERIFIED-HISTORICALLY`, 0 `NOT-VERIFIABLE-RETROACTIVELY`; correcciones de comando **0** (la
    sección correspondiente dice `Ninguna.`); archivos de test nuevos escritos **0**; filas cuya celda
    de enforcement dice que no corre en CI: **3** — la fila del regenerador de snapshots más su diff, y
    las dos filas de revisión de documento. Si la medición da otro reparto, escribir el medido y anotar
    la divergencia en el SUMMARY.

    Llenar la cuarta columna con la tabla de lookup de la sección 7 del contrato: las filas de pytest
    sobre `packages/iol-client/tests/` y `packages/matriz-client/tests/` mapean al job `test` con su
    rango de líneas; la fila de mypy sobre el paquete mapea al job `typecheck`; la fila del regenerador
    de snapshots y las dos de revisión de documento llevan la marca de no enforcement, con el motivo
    escrito en la celda (el script no aparece en el archivo de workflow; la revisión de documento no es
    automatizable por naturaleza).

    Incluir el párrafo que justifica por qué las filas de revisión siguen siendo manuales, copiando la
    forma del precedente de `09-VALIDATION.md`: nombrar la conducta, decir qué evidencia la sostiene
    (la confirmación humana con timestamp del front-matter de `38-VERIFICATION.md` más el censo en
    disco), y por qué no se re-deriva. Divergir del precedente en la línea de veredicto: nunca escribir
    que la fase "sigue siendo compliant"; usar la forma PARTIAL fijada en el contrato.

    Escribir la sección de hallazgos de bookkeeping: dos celdas de `File Exists` del mapa afirman que el
    archivo de test falta cuando hoy existe y corre verde; y el regenerador de snapshots es una fila que
    muta el árbol de trabajo, hecho que el mapa original no señala y que cualquier re-corrida futura
    debe tener presente.

    Actualizar la columna `Status` de las 9 filas según la regla de la sección 5 del contrato y extender
    la línea de leyenda. Las dos filas de revisión reciben el valor histórico, nunca la marca verde: el
    contrato lo prohíbe explícitamente para filas cuyo tipo de test es manual o revisión de documento.

    Transformar el front-matter según la sección 4 del contrato: `status` pasa de `draft` a `validated`;
    `nyquist_compliant` se queda en `false` porque R-09 falla por su condición (b) — la fase tiene dos
    filas históricas. Escribir ese rationale junto al veredicto, en la forma de prosa de
    `07-VALIDATION.md`, sin adoptar su valor de flag. `not_verifiable_retroactively` es `0`: esta fase
    no conserva ningún ítem de esa clase, y decirlo con un cero explícito es lo que hace uniforme al
    conjunto de cinco. Se agregan `audited_commit_sha` con el commit de v1.7, `audit_baseline_head`
    copiado literal del contrato, `frozen_tree_verified: true`, `updated: 2026-08-31` y
    `last_audited: 2026-08-31`. Quedan intactos `phase`, `slug`, `created`, `wave_0_complete` y los dos
    comentarios de lifecycle.

    Cerrar con la línea de veredicto en la forma fijada y una línea nueva tildada en el
    `## Validation Sign-Off`, dejando sin tildar las casillas preexistentes.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md; X() { awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 38-[rm][0-9]/' "$F"; }; test "$(X | wc -l | tr -d ' ')" -eq 9 && test "$(X | grep -c 'VERIFIED-NOW')" -eq 7 && test "$(X | grep -c 'VERIFIED-HISTORICALLY')" -eq 2 && test "$(X | grep -c 'NOT-VERIFIABLE-RETROACTIVELY')" -eq 0 && test "$(X | grep -c 'NOT ENFORCED')" -eq 3 && grep -q '^status: validated' "$F" && grep -q '^nyquist_compliant: false' "$F" && grep -q '^not_verifiable_retroactively: 0' "$F" && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$F" && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de disposición tiene exactamente 9 filas dentro del rango acotado entre `### Disposición por fila` y la línea de leyenda.
    - La suma de los tres conteos de token dentro de ese rango es exactamente `9`.
    - El reparto medido es 7 / 2 / 0: `VERIFIED-NOW` = `7`, `VERIFIED-HISTORICALLY` = `2`, `NOT-VERIFIABLE-RETROACTIVELY` = `0`.
    - `grep -c 'NOT ENFORCED'` dentro del rango acotado es `3`.
    - Las dos filas históricas citan la marca de tiempo ISO de la confirmación humana leída de `38-VERIFICATION.md`.
    - Ninguna fila de tipo revisión de documento lleva la marca verde en la columna `Status`.
    - La sección de correcciones de comando dice `Ninguna.`
    - Front-matter: `grep -c '^status: validated'` es `1`, `grep -c '^nyquist_compliant: false'` es `1`, `grep -c '^not_verifiable_retroactively: 0'` es `1`, `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7'` es `1`, `grep -cE '^audit_baseline_head: [0-9a-f]{40}'` es `1`.
    - `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 38-VALIDATION.md` es `1`.
    - `grep -c '^- \[x\] .*nyquist_compliant: true' 38-VALIDATION.md` es `0`.
    - `git status --porcelain` sólo lista `38-VALIDATION.md` bajo `.planning/milestones/v1.7-phases/`; `git status --porcelain verification/` vacío.
  </acceptance_criteria>
  <done>Las 9 filas de la Phase 38 están dispuestas: 7 con evidencia re-ejecutada, 2 con la confirmación humana fechada citada literal; 3 filas quedan marcadas como no cubiertas por CI; el flag no se mueve.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producido por este plan:
- `.planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md` — **modificado**. Sección `## Validation Audit 2026-08-31` (métricas, tabla de disposición de 9 filas y 4 columnas, `Ninguna.` en correcciones de comando, párrafo que justifica las filas manual-only, hallazgos de bookkeeping, escalaciones, veredicto); columna `Status` actualizada; front-matter con `status: validated`, `nyquist_compliant: false`, `not_verifiable_retroactively: 0`, `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified: true`, `updated`, `last_audited`.

Cero archivos nuevos, cero locks nuevos en `verification/`, cero ediciones a `.github/workflows/ci.yml`. Las 3 filas marcadas como no cubiertas por CI son superficies **preexistentes**, no locks producidos por esta auditoría: se reportan como hallazgo y su destino queda ruteado al edit consolidado de `ci.yml` de la Phase 45 por el plan 41-07, no aquí.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de pytest/mypy local → markdown committeado | Las líneas de resumen transcritas quedan en el repo |
| script generador de snapshots → árbol de trabajo | La fila `38-r06` escribe 4 archivos bajo `verification/snapshots/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-05-01 | Information Disclosure | transcripción de salida de suite a `38-VALIDATION.md` | mitigate | Sección 6 del contrato: sólo línea de resumen; ninguna fila lee `.env` ni abre red (ASVS V7) |
| T-41-05-02 | Tampering | `verification/regen_snapshots.py` muta el árbol de trabajo | mitigate | Criterio de aceptación explícito: `git status --porcelain` vacío y sin cambios en `verification/snapshots/` después de correrlo; abortar el plan si el árbol queda sucio |
| T-41-05-03 | Repudiation | re-hacer la revisión del censo con criterio nuevo y presentarlo como la verificación original | mitigate | R-06 exige citar la confirmación humana fechada existente; criterio de aceptación que exige la marca de tiempo ISO transcrita literal |
| T-41-05-04 | Repudiation | marcar verde una fila de revisión de documento | mitigate | Regla de la columna `Status` del contrato más criterio de aceptación explícito |
| T-41-05-SC | Tampering | instalaciones de paquetes | accept | Cero: ningún comando invoca `uv add`, `npm`, `pip` ni `cargo` |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V5/V6 — el plan edita un markdown y re-corre
comandos locales existentes.
</threat_model>

<verification>
1. Tabla de 9 filas, suma de tokens exactamente 9, reparto 7 / 2 / 0.
2. Tres celdas de enforcement marcadas como no cubiertas por CI, con motivo escrito.
3. Árbol limpio después del regenerador de snapshots.
4. Front-matter con los cinco campos nuevos y `nyquist_compliant` sin cambiar.
5. `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
</verification>

<success_criteria>
- Ninguna de las 9 filas queda sin disponer y ninguna lleva dos disposiciones.
- Las dos filas de revisión se sostienen en evidencia fechada preexistente, no en un juicio nuevo de esta sesión.
- El árbol de trabajo queda idéntico después de correr el regenerador de snapshots.
- `nyquist_compliant` sigue en `false` con rationale que cita la condición de R-09 que falla.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-05-SUMMARY.md` when done.
</output>
