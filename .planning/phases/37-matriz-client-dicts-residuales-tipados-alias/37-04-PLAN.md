---
phase: 37
plan: 04
type: execute
wave: 3
depends_on: [37-03]
files_modified:
  - tools/check_surface_types.py
  - packages/matriz-client/tests/test_surface_types_red.py
autonomous: true
requirements: [NOBJ-MTZ-01]

must_haves:
  truths:
    - "D-01a: the shared gate gains a SECOND scanning dimension — annotated field declarations inside exported classes — alongside the return-annotation dimension it already has."
    - "F-2: before this plan the gate reported zero violations with five untyped mapping fields in place, because _candidates_for only ever produced function definitions. That structural blind spot is closed."
    - "D-01b: the field predicate is NARROW — an untyped mapping annotation and a bare Any only, Optional stripped first. A list of Any must NOT match, or market-data's two exported warnings fields redden and this phase reaches into a package it declared out of scope."
    - "D-01c: UnknownFrame.raw is the ONE exemption, keyed on the qualified class-and-field name, never on the bare member name — a bare-name exemption would spare every member named raw in all six packages."
    - "D-01d: the gate is proven non-vacuous by a synthetic package injected at tmp_path, and the exemption is proven REACHABLE rather than dead code by counting it by name in exempted_by_reason."
    - "F-9: RequestSpec carries an optional untyped-mapping field in every package and is safe ONLY because RequestSpec appears in no package's __all__. This is recorded in the gate itself so a future export does not detonate it silently."
    - "Ratchet discipline holds: a red gate is never resolved by weakening the gate, by widening the exemption set, or by editing a foreign package."
  artifacts:
    - path: "tools/check_surface_types.py"
      provides: "Field-annotation scanning dimension, narrow predicate, qualified exemption table, and a field counter in the summary"
      contains: "_FIELD_EXEMPTIONS"
    - path: "packages/matriz-client/tests/test_surface_types_red.py"
      provides: "Non-vacuity proof for the field dimension plus exemption reachability and predicate-narrowness locks"
      contains: "_write_fake_package"
  key_links:
    - from: "tools/check_surface_types.py"
      to: "tools/check_surface_types.py"
      via: "scan_surface_types adjudicates field candidates alongside function candidates for every resolved class export"
      pattern: "_adjudicate_field\\("
    - from: "packages/matriz-client/tests/test_surface_types_red.py"
      to: "tools/check_surface_types.py"
      via: "the test injects a synthetic package root, exercising REPO_ROOT as a default argument rather than an inlined constant"
      pattern: "check_surface_types\\(root=tmp_path\\)"
  prohibitions:
    - statement: "Do not land this plan before the retypes of Plans 37-02 and 37-03. RESEARCH Pitfall 3: extending the gate while matriz still declares untyped mapping fields makes the lint job red between commits. Wave 3 exists for exactly this ordering; the reverse order is forbidden."
    - statement: "Do not add the exemption to _is_exempt. That helper takes the SIMPLE member name by its own documented design, so adding the field name there would exempt every member of that name across all six packages."
    - statement: "Do not widen the predicate to any mention of Any. A wide predicate reddens two exported fields in market-data-client, a package this phase declared disjoint and out of scope (D-01b). If something outside matriz reddens, NARROW the predicate — never exempt it and never edit the foreign package."
    - statement: "Do not commit a fake package under packages/. It would enter check_decode_intactness Check D's roster and owe check_uniform_structure a models module and a types module, reddening two unrelated gates. The synthetic package lives under tmp_path only."
    - statement: "Do not prove the gate by spawning a mypy or CLI subprocess. Call scan_surface_types / check_surface_types in-process with an injected root."
    - statement: "Do not touch models.py, _core.py or any package source in this plan."
---

<objective>
Close the gate's structural blind spot: `tools/check_surface_types.py` reports zero violations
today with five untyped mapping fields in place, because it has only ever looked at return
annotations. Give it a field dimension with a narrow predicate and exactly one qualified exemption,
and prove the new dimension is not vacuous.

Purpose: Success Criterion 2 is not "matriz has no untyped mapping fields" — Plans 37-02/03 already
delivered that. It is that the gate REPORTS zero with a single documented exemption "no obtenida
por omisión o por un hueco de resolución del gate". A green gate that structurally cannot see the
thing it claims to check is the exact failure mode this criterion names.

Output: a second candidate/adjudicate pair in the shared gate, a qualified exemption table, a field
counter in the summary line, and a new matriz-local RED fixture module.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-CONTEXT.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-RESEARCH.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates the rows marked **37-04**.

| Symbol | Kind | Home | Created by |
|--------|------|------|------------|
| `parse_get_detailed_positions_response` / `parse_get_account_report_response` | envelope unwrap | `matriz_client._core` | 37-01 |
| `_parse_risk_response` | REMOVED | `matriz_client._core` | 37-01 |
| `TickPriceRange` | `_SafeModel` dataclass, `baseline` provenance | `matriz_client.models` | 37-02 |
| `_mapping_value` / `_apply_mapping_policy` / `_convert` | element-typed, self-recursing axis | `matriz_client.models` | 37-02 |
| `InstrumentDetail.tickPriceRanges` | → `dict[str, TickPriceRange]` | `matriz_client.models` | 37-02 |
| `InstrumentPositionReport` / `DetailedAccountReport` | `_SafeModel` dataclasses, `vendor-documented, UNMEASURED` provenance | `matriz_client.models` | 37-03 |
| `DetailedPosition.report` / `AccountReport.detailedAccountReports` / `AccountReport.portfolio` | retypes (2-level / 1-level / scalar) | `matriz_client.models` | 37-03 |
| Two `SHAPE` ledger rows | unobserved-roster declarations | `.planning/verification/matriz-client-findings.md` | 37-03 |
| `_field_candidates_for` | gate helper — the annotated field declarations one resolved class export contributes | `tools/check_surface_types.py` | **37-04** |
| `_adjudicate_field` | gate helper — classifies one field candidate as exemption-or-violation | `tools/check_surface_types.py` | **37-04** |
| `_field_annotation_is_untyped_mapping` | gate helper — the NARROW field predicate | `tools/check_surface_types.py` | **37-04** |
| `_FIELD_EXEMPTIONS` | module-level table keyed on the qualified class-and-field name | `tools/check_surface_types.py` | **37-04** |
| `ScanResult.fields` | new counter field plus a new term in the summary line | `tools/check_surface_types.py` | **37-04** |
| `packages/matriz-client/tests/test_surface_types_red.py` | NEW test module — gate non-vacuity, exemption reachability, predicate narrowness | tests | **37-04** |
| `MarketDataSnapshot.bids` / `.offers` / `.last` / `.settlement` / `.close` / `.open_interest` | read-only `@property` aliases | `matriz_client.models` | 37-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the gate a field dimension with the one qualified exemption, proven non-vacuous</name>
  <files>tools/check_surface_types.py, packages/matriz-client/tests/test_surface_types_red.py</files>
  <read_first>
    - tools/check_surface_types.py:1-120 (the FULL module docstring, especially D-05 and D-12 — why this gate lives as a lint step and not under verification/, and the ratchet-discipline paragraph "a red gate is never resolved by weakening the gate")
    - tools/check_surface_types.py:196-222 (`ScanResult` and its docstring's contract for what `exempted` and `assignments` count and why)
    - tools/check_surface_types.py:386-414 (`_is_exempt` — read its docstring's statement that attribution is by the SIMPLE member name, never the qualified form; this is why the new exemption cannot go here — and `_annotation_mentions_any`, the WIDE return predicate the field predicate must NOT reuse)
    - tools/check_surface_types.py:495-540 (`_candidates_for` and `_adjudicate` — the exact triple shape, the `_module_level_statements` traversal, the two-slot return contract, and the four-space-indented violation message format the summary line depends on)
    - tools/check_surface_types.py:541-665 (`scan_surface_types`'s per-export loop, the anti-vacuity guard, the `ScanResult` construction, and `check_surface_types`'s summary string)
    - packages/iol-client/tests/test_surface_types_red.py:1-125 (the module docstring's anti-patterns, the `_REPO_ROOT` derivation independent of the gate's own constant, the `_write_fake_package` helper to copy, and the real-tree floor test)
    - packages/iol-client/tests/test_surface_types_red.py:153-195 (the exemption-reachability idiom: assert on `scan_surface_types`'s counters and on `exempted_by_reason` by name)
    - packages/matriz-client/src/matriz_client/models.py:527-561 (`UnknownFrame` — note it has NO base class, so it is invisible to `_safemodel_classes()`, and note its docstring already justifies the catch-all; the gate reaches it anyway because it is exported)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md § "tools/check_surface_types.py" (the mirror-the-existing-dimension instructions and the exemption-table shape)
  </read_first>
  <behavior>
    Non-vacuity (the property that did not hold before this plan):
    - A synthetic package under `tmp_path` exporting a dataclass whose body declares one field
      annotated as an untyped string-keyed mapping makes `check_surface_types(root=tmp_path)` raise
      `CheckFailure`, and the message names the qualified class-and-field pair.
    - The same synthetic package with the field annotated as a typed mapping does NOT raise.
    - A bare `Any` field annotation also raises.
    - An optional untyped mapping annotation also raises — the predicate strips the optional wrapper
      first (RESEARCH A2 / F-9 recommend closing this hole; nothing exported has that shape today,
      so the choice is unobservable on the real tree and only makes the ratchet stricter).

    Narrowness (the property that keeps this phase inside its declared blast radius):
    - A field annotated as a LIST of `Any` does NOT raise. This is D-01b's whole point: a wide
      predicate would redden two exported fields in `market-data-client`, a package this phase
      declared disjoint.
    - A field whose annotation merely mentions `Any` deeper inside a typed container does NOT raise.

    Exemption reachability (proving the exemption absorbs a real hit, not dead code):
    - A synthetic exported class named as the catch-all frame class, declaring the catch-all field
      with an untyped mapping annotation, does NOT raise; the scan result counts it, and its reason
      string appears BY NAME in `exempted_by_reason`.
    - The same field name on a DIFFERENT class name still raises — the exemption is qualified, not
      bare.

    Real tree, floors only (never equalities, so a new export cannot falsely redden):
    - `check_surface_types()` over the real repo root returns a summary string and does not raise.
    - `scan_surface_types(_REPO_ROOT)` reports no violations, at least six packages, at least three
      hundred definitions, at least one scanned field, and at least twenty absorbed exemptions.
    - The summary string names the new field counter.
  </behavior>
  <action>
    In `tools/check_surface_types.py`, add `_field_candidates_for` mirroring `_candidates_for`:
    keep the same qualified-name, simple-name, node triple shape and the same
    `_module_level_statements(node.body)` traversal so conditionally declared fields are included,
    but filter annotated assignment statements whose target is a plain name instead of function
    definitions. Only a class binding contributes field candidates; a function binding contributes
    none.

    Add `_field_annotation_is_untyped_mapping` as a NEW predicate — do not reuse
    `_annotation_mentions_any`, which is deliberately wide for return types. The new predicate
    strips an optional union wrapper first, then matches only two shapes: a bare `Any` name or
    attribute, and a string-keyed mapping subscript whose value parameter is `Any`. Everything else,
    including a list parameterised by `Any`, is not a match.

    Add `_FIELD_EXEMPTIONS` as a module-level mapping from the QUALIFIED class-and-field name to a
    short reason string, holding exactly one entry for the WebSocket catch-all frame's raw payload
    field. Give the entry an inline comment stating why it exists — it is the documented escape
    hatch for frames whose type is not modelled, already justified in that class's own docstring —
    and stating that this is the phase's single declared exemption. Consult this table from
    `_adjudicate_field` BEFORE `_is_exempt`, and flow its reason string into the same
    `exempt_counts` accumulator so the RED fixture can assert it by name.

    Add `_adjudicate_field` mirroring `_adjudicate`'s two-slot exemption-or-violation return
    contract and its four-space-indented violation message format — the summary line's formatting
    depends on that indent. Drop the "has no return annotation" arm: an annotated assignment always
    carries an annotation, so there is no field analogue and inventing one would be dead code. The
    violation detail should quote the unparsed annotation the way the return arm does.

    Wire both into `scan_surface_types`'s per-export loop next to the existing function-candidate
    loop, accumulating into a new `fields_total` counter. Add a `fields` field to `ScanResult` and
    extend its docstring to say what it counts, in the same voice the existing counter paragraphs
    use. Add the field count as a new term in `check_surface_types`'s summary string. Extend the
    anti-vacuity guard's reasoning in the module docstring: a scan that saw zero fields across six
    packages of dataclass-heavy model modules is now also a signal worth naming.

    Record RESEARCH F-9 in the gate itself, next to `_FIELD_EXEMPTIONS`: an AST scan across all six
    packages found that the internal request-spec dataclass declares an optional untyped mapping
    field in every one of them, and that this is unreachable ONLY because that class appears in no
    package's `__all__` — the gate resolves candidates from the exported surface. State that
    exporting it would make all six packages red, so anyone who exports it must re-check this gate
    first. This note exists so the fact is not rediscovered by a red CI run.

    Create `packages/matriz-client/tests/test_surface_types_red.py`. Mirror the iol module rather
    than importing from it — a shared test utility would be this repo's first cross-package test
    dependency and the repo has none by design. Copy the `_write_fake_package` helper verbatim, and
    derive the repo root independently of the gate's own constant the way the iol module does and
    for the same stated reason. Carry over the anti-pattern paragraph from that module's docstring:
    no subprocess, and no fake package committed under the real packages directory.

    Land the gate change and this test module in the SAME commit. The gate's real-tree floor test
    is the thing that proves the extension did not redden the tree, and a commit carrying only one
    half is a commit whose CI signal is meaningless.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/test_surface_types_red.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tools/check_surface_types.py` exits 0 and its printed summary contains a
      non-zero field count term.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_surface_types_red.py -q` exits 0.
    - `uv run --package iol-client pytest packages/iol-client/tests/test_surface_types_red.py -q` exits 0 —
      the pre-existing floors still hold after the extension.
    - `uv run python -c "import sys; sys.path.insert(0,'.'); from tools.check_surface_types import scan_surface_types; from pathlib import Path; r=scan_surface_types(Path('.')); print(r.fields, dict(r.exempted_by_reason))"`
      prints a non-zero field count and a reason map containing the new exemption reason with count 1.
    - Reverting only the field annotation of `InstrumentDetail.tickPriceRanges` to its pre-phase form
      in a scratch copy makes the gate raise — run this as a temporary check inside `tmp_path` via
      the synthetic package rather than by editing the real tree, and record the assertion name in
      the SUMMARY.
    - `uv run mypy packages/matriz-client/tests` reports success (importing the gate from a package
      test is what enrols `tools/*.py` in strict type checking).
    - `uv run ruff check tools packages/matriz-client` and
      `uv run ruff format --check tools packages/matriz-client` exit 0.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` exits 0 — the
      narrow predicate did not reach into the out-of-scope package.
  </acceptance_criteria>
  <done>The gate scans exported class field annotations, raises on a reintroduced untyped mapping
  field, spares a list of `Any`, absorbs exactly one qualified exemption that is counted by name,
  stays green on the real tree, and carries the `RequestSpec` blast-radius note in its own source.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the extension across all six packages and record the ratchet's new floor</name>
  <files>tools/check_surface_types.py, packages/matriz-client/tests/test_surface_types_red.py</files>
  <read_first>
    - tools/check_surface_types.py (the module as left by Task 1 — the summary string, the counters, and the exemption table)
    - .github/workflows/ci.yml:40-82 (the lint job's gate steps and the explicit verification-test list, so the phase-level claim "CI is green" is measured against what CI actually runs)
    - packages/market-data-client/src/market_data_client/models.py:935-950 and :1025-1040 (the two exported list-of-`Any` fields the narrow predicate must spare — confirm by running the gate, not by reading alone)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-RESEARCH.md § F-9 (the measured blast-radius table across all six packages)
  </read_first>
  <action>
    Run the gate and every package suite, and turn the results into recorded floors rather than a
    one-time observation.

    Add a test to the matriz RED module that pins the OUT-OF-SCOPE spare explicitly: a synthetic
    package declaring an exported field annotated as a list of `Any` scans clean, with a docstring
    naming D-01b and naming the two real market-data fields the narrowness protects. This is what
    stops a later contributor from "tightening" the predicate and silently pulling a disjoint
    package into this phase's blast radius.

    Add a floor assertion for the new counter to the real-tree test, using a lower bound taken from
    the measured value minus a margin — never an equality, for the same reason the existing
    definition and exemption floors are lower bounds: a new export must be able to raise the number
    without falsely reddening.

    Update the gate module's docstring header to name the field dimension as a first-class part of
    what this gate checks, with the phase and requirement id, in the same voice as the existing
    dimension's paragraph. Include the measured before-and-after: zero violations with five untyped
    mapping fields in place before, and the current counts after.
  </action>
  <verify>
    <automated>uv run python tools/check_surface_types.py && uv run --package matriz-client pytest packages/matriz-client/tests/test_surface_types_red.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python tools/check_surface_types.py` exits 0.
    - `uv run python tools/check_decode_intactness.py`, `uv run python tools/check_uniform_structure.py`
      and `uv run python tools/surface_parity.py` all exit 0.
    - All six package suites exit 0, run as
      `for p in ambito-financiero-client higyrus-client iol-client market-data-client matriz-client wallets-client; do uv run --package "$p" pytest "packages/$p/tests" -q || exit 1; done`.
    - `uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py` exits 0 — the two guards the lint job runs explicitly.
    - `uv run mypy packages/matriz-client/src packages/matriz-client/tests` reports success.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_surface_types_red.py -k "list or narrow" -q` exits 0 and collects at least 1 test.
    - The gate's printed summary line is quoted verbatim in the SUMMARY alongside the pre-phase
      baseline line recorded in 37-RESEARCH F-2.
  </acceptance_criteria>
  <done>The extended gate is green on all six packages, the narrowness that keeps market-data out of
  scope is pinned by its own test, the new counter has a floor, and the gate's docstring records
  what it now checks and what it measured before and after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor source → CI lint gate | The gate is the only automated barrier between a reintroduced untyped surface field and a merged commit |
| Gate exemption table → the ratchet's guarantee | Every exemption is a hole; an unqualified or unproven one silently disables the guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-17 | Repudiation | A gate that reports green without inspecting the field dimension | mitigate | `_field_candidates_for` plus `_adjudicate_field` close the measured blind spot (F-2), and the RED fixture proves non-vacuity by injecting a synthetic violation at `tmp_path` |
| T-37-18 | Tampering | An over-broad exemption disabling the ratchet across packages | mitigate | `_FIELD_EXEMPTIONS` is keyed on the QUALIFIED class-and-field name and holds exactly one entry; a test asserts the same field name on a different class still reddens |
| T-37-19 | Tampering | An exemption that is dead code and therefore unfalsifiable | mitigate | The exemption's reason string is counted in `exempted_by_reason` and asserted by name, so it is proven to absorb a real hit |
| T-37-20 | Denial of Service | A predicate widening that reddens an out-of-scope package | mitigate | D-01b's narrow predicate spares list-of-`Any`; a dedicated test pins the spare and names the two real market-data fields it protects; all six suites are run as acceptance |
| T-37-21 | Spoofing | An unexported internal dataclass later exported, detonating all six packages | mitigate | RESEARCH F-9's measurement is recorded in the gate source next to the exemption table, naming the class and the condition, so the trap is documented where it would be triggered |
| T-37-22 | Elevation of privilege | Resolving a red gate by weakening it | mitigate | The gate's own ratchet-discipline paragraph is reaffirmed and the plan prohibits widening the exemption set or editing a foreign package as a remedy |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `uv.lock` untouched. 37-RESEARCH's Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `uv run python tools/check_surface_types.py` exits 0 with a non-zero field count in the summary.
- All six package suites exit 0.
- `uv run mypy packages/matriz-client/src packages/matriz-client/tests` reports success.
- The three other repo gates exit 0.
- `git diff --name-only` for this plan lists only `tools/check_surface_types.py` and
  `packages/matriz-client/tests/test_surface_types_red.py`.
</verification>

<success_criteria>
- The gate reports zero untyped mapping field annotations on matriz's exported model surface with
  exactly one exemption, and that exemption is qualified, documented in the gate source, and proven
  reachable by a counted assertion.
- A reintroduced untyped mapping field on an exported class raises `CheckFailure` naming the
  qualified pair.
- A list of `Any` does not raise, so the out-of-scope package stays out of scope.
- No package source file was modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-04-SUMMARY.md` when done.
Quote the gate's new summary line verbatim next to the pre-phase baseline from 37-RESEARCH F-2 —
the delta is the evidence for Success Criterion 2.
</output>
