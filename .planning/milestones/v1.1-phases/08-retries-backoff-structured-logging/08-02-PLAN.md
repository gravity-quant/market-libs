---
phase: 08-retries-backoff-structured-logging
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/client.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
  - packages/ambito-financiero-client/tests/test_transport.py
  - packages/ambito-financiero-client/tests/test_logging.py
  - verification/snapshots/ambito_financiero_client-surface.txt
autonomous: true
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, ambito, canary, atomic-commit, rely, log]
user_setup: []

must_haves:
  truths:
    - "`ambito_financiero_client/_transport.py` define `RetryTransport(httpx.HTTPTransport)` con tenacity Retrying + full-jitter backoff + Retry-After cap 60s + mutation gate vía `request.extensions['idempotent']` (D-01, D-07, D-08, D-04)"
    - "`ambito_financiero_client/_atransport.py` define `AsyncRetryTransport(httpx.AsyncHTTPTransport)` mirror sync (D-32 — `await asyncio.sleep`, AsyncRetrying)"
    - "`ambito_financiero_client/_logging.py` define `RedactingFilter(logging.Filter)` + `attach()` factory; ámbito sin auth → patterns mínimos (sanity: no Bearer/password en URL, dado que ámbito no tiene credenciales — pero filter existe para forward-consistency cross-paquete + futura coverage)"
    - "`ambito_financiero_client/__init__.py` invoca `_logging.attach()` con idempotency check + `del _logging_attach` (no re-export; D-13 + LOG-01)"
    - "`_core.py RequestSpec` extendido con `endpoint_name: str = ''` (D-09); todos los GET builders (`build_dollar_*_request`, etc.) flippean `idempotent=True` + setean `endpoint_name=<fn name>` (D-03)"
    - "`Client.__init__` + `AsyncClient.__init__` + `configure()` extendidos con 2 nuevos kwargs `max_retries: int = 2` + `http_client: httpx.Client|None = None` (D-15, D-16, D-17 no log_level kwarg, D-18 only Client/configure level no per-call, D-19, D-20)"
    - "Shell `_request()` genera `request_id = uuid.uuid4().hex` pre-send; setea `request.extensions['idempotent'/'request_id'/'endpoint_name']` desde `spec`; emite DEBUG req/resp log; ámbito NO tiene 401 re-auth branch (no auth)"
    - "`_ensure_http_client()` wrappea `httpx.Client(transport=_transport.RetryTransport(max_attempts=self._max_retries+1))` por default (D-15 mapping `max_retries=N → max_attempts=N+1`); si `http_client=` provisto, lo usa AS-IS (D-16 contract)"
    - "`verification/snapshots/ambito_financiero_client-surface.txt` actualizado: `Client.__init__`, `AsyncClient.__init__`, `configure()` reflejan los 2 nuevos kwargs en su signature (D-28)"
    - "Existing ámbito tests (Phase 7 baseline ~XX tests) siguen GREEN post-cambios"
    - "Cross-cutting guard tests Plan 1: `test_retry_mutation_gate.py[ambito]` GREEN (ámbito sin mutating endpoints — branch GET retry verifica retry funciona); `test_retry_after_cap.py` GREEN (ámbito puede ser el target); `test_logging_root_unchanged.py` sigue GREEN (ámbito ahora attached NullHandler a `getLogger('ambito_financiero_client')`, NO a root); `test_logging_no_token_leak.py[ambito]` GREEN (ámbito sin token, but base_url se loguea — assertion verifica que el sentinel `SECRET-LITERAL-12345` en base_url SI aparece en url field, sanity inverso); `test_async_cancellation.py[ambito]` GREEN (AsyncRetryTransport respeta CancelledError)"
  artifacts:
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py"
      provides: "RetryTransport(httpx.HTTPTransport) sync con tenacity Retrying loop + mutation gate + Retry-After cap 60s + DEBUG/WARNING/ERROR logs"
      contains: "class RetryTransport"
      min_lines: 90
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py"
      provides: "AsyncRetryTransport(httpx.AsyncHTTPTransport) async mirror con AsyncRetrying"
      contains: "class AsyncRetryTransport"
      min_lines: 90
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py"
      provides: "RedactingFilter + attach() factory; ámbito-specific patterns (Bearer/password baseline aún sin cobertura real porque ámbito sin auth — pero present por forward-consistency)"
      contains: "class RedactingFilter"
      min_lines: 50
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py"
      provides: "_logging.attach() invocation con idempotency + del cleanup"
      contains: "_logging.attach()"
    - path: "packages/ambito-financiero-client/tests/test_transport.py"
      provides: "Unit tests for RetryTransport — mutation gate pass-through, retry on 503, Retry-After cap, jitter distribution, request_id persistence, GET retry"
      contains: "RetryTransport"
      min_lines: 80
    - path: "packages/ambito-financiero-client/tests/test_logging.py"
      provides: "Unit tests for RedactingFilter — attach idempotency + record.__dict__ scan"
      contains: "RedactingFilter"
      min_lines: 40
    - path: "verification/snapshots/ambito_financiero_client-surface.txt"
      provides: "Public surface snapshot ámbito con 2 nuevos kwargs en Client/AsyncClient/configure() signatures"
      contains: "max_retries"
  key_links:
    - from: "ambito_financiero_client.client.Client._request"
      to: "ambito_financiero_client._transport.RetryTransport (via state.http_client.send)"
      via: "request.extensions['idempotent','request_id','endpoint_name'] propagation"
      pattern: "request.extensions\\["
    - from: "ambito_financiero_client.__init__"
      to: "logging.getLogger('ambito_financiero_client')"
      via: "_logging.attach() — NullHandler + RedactingFilter"
      pattern: "_logging\\.attach"
    - from: "ambito_financiero_client._core.build_dollar_banco_nacion_request"
      to: "ambito_financiero_client._transport.RetryTransport.handle_request retry loop"
      via: "RequestSpec.idempotent=True triggers retry on 5xx/connect"
      pattern: "idempotent=True"
---

<objective>
ámbito canary del Phase 8 delivery pattern: agregar `_transport.py` (sync RetryTransport), `_atransport.py` (async AsyncRetryTransport), `_logging.py` (RedactingFilter + attach()), extender `_core.py` con `RequestSpec.endpoint_name`, flippear `idempotent=True` en todos los GET builders, modificar shell `_request()` para generar `request_id` UUID + setear `request.extensions`, extender `Client.__init__` + `AsyncClient.__init__` + `configure()` con 2 nuevos kwargs (`max_retries=2`, `http_client=None`), actualizar `__init__.py` con `_logging.attach()`, agregar unit tests `tests/test_transport.py` + `tests/test_logging.py`, y actualizar `verification/snapshots/ambito_financiero_client-surface.txt`. Per-package atomic commit per D-21.

Purpose: ámbito es el canary más simple (sin auth, sin token refresh, sin account_id, sin Risk API) — establece el mechanical pattern que iol/higyrus/matriz duplicarán 3× más. Si Plan 2 rompe, Plan 1 queda mergeable solo; iol/higyrus/matriz no se contaminan. Cumple Phase 8 ROADMAP Success Criteria #1 (retries transparentes), #4 (NullHandler + grep CI), #5 (structured fields) en su forma más simple. Cross-cutting guard tests turn GREEN para el slice ámbito.

Output: 1 commit atómico per D-21 con: 2 nuevos módulos transport (`_transport.py`, `_atransport.py`) + 1 nuevo módulo logging (`_logging.py`) + 4 extends (`_core.py`, `client.py`, `aio.py`, `__init__.py`) + 2 nuevos test files + 1 snapshot update. Commit message: `feat(ambito): retries + structured logging — RetryTransport, RedactingFilter (RELY-01..04, LOG-01..03)` per RESEARCH §"Commit message patterns".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md
@.planning/phases/08-retries-backoff-structured-logging/08-01-SUMMARY.md
@CLAUDE.md
@packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py
@packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py
@packages/ambito-financiero-client/tests/conftest.py
@verification/snapshots/ambito_financiero_client-surface.txt
@verification/test_retry_mutation_gate.py
@verification/test_retry_after_cap.py
@verification/test_logging_root_unchanged.py
@verification/test_logging_no_token_leak.py
@verification/test_async_cancellation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _transport.py + _atransport.py + _logging.py for ámbito (new private modules per RESEARCH §Pattern 1 + Pattern 4)</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py, packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py, packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py, packages/ambito-financiero-client/tests/test_transport.py, packages/ambito-financiero-client/tests/test_logging.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-01, D-04, D-06, D-07, D-08 — RetryTransport invariants; D-10 — RedactingFilter shape; D-19 — max_retries=0 bypass; D-31 — tenacity integration shape; D-32 — async cancellation via asyncio.sleep)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 1 lines 348-504 — CANONICAL `RetryTransport(httpx.HTTPTransport)` body verbatim para ámbito; Pattern 4 lines 605-682 — CANONICAL `RedactingFilter` body verbatim; §"Code Examples" lines 845-903 mutation gate; §"State of the Art" — old vs current approach)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/src/<pkg>/_transport.py`" lines 70-131 — sync transport assignments; §"`packages/<pkg>/src/<pkg>/_atransport.py`" lines 134-180 — async deltas; §"`packages/<pkg>/src/<pkg>/_logging.py`" lines 183-261 — RedactingFilter per-paquete recipes; "Per-package redaction recipes" line 254-261 — ámbito: "None (no auth, no credenciales en URL/headers)")
    - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py (canonical private module shape — module docstring + `from __future__ import annotations` + `__all__` list + section dividers `# ---...---`; analog para `_transport.py`/`_atransport.py`/`_logging.py` headers)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py (Phase 7 `_core.py` shape; verify `RequestSpec` already has `idempotent: bool = False` forward-declared per D-13 Phase 7)
    - packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py (read AmbitoAPIError/AmbitoRateLimitError class signatures — verify no AuthError since ámbito has no auth)
  </read_first>
  <behavior>
    Create 3 new modules + 2 new test files following the canonical patterns:

    **`packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py`** (~95 LOC):
    - Module docstring: `"""RetryTransport — sync. Phase 8 RELY-01..04 + D-01/D-07/D-08."""` + multi-paragraph explaining mutation gate, backoff full-jitter (base=1s, max=30s, exp=2), Retry-After cap 60s
    - `from __future__ import annotations`
    - Imports: `logging`, `time`, `uuid`, `email.utils.parsedate_to_datetime`, `typing.Any`, `httpx`, `tenacity.{Retrying, retry_if_exception_type, stop_after_attempt, wait_exponential_jitter}`
    - Module-level constants per RESEARCH §Pattern 1 line 377-383:
      - `_RETRYABLE_EXC: tuple[type[Exception], ...] = (httpx.ConnectError, httpx.ConnectTimeout, httpx.ReadTimeout)`
      - `_RETRYABLE_STATUS: frozenset[int] = frozenset({408, 409, 429, *range(500, 600)})`
      - `_RETRY_AFTER_CAP_S: float = 60.0`
      - `_LOGGER_NAME = "ambito_financiero_client"` (matches __init__.py attach target)
    - `class _RetryableStatus(Exception)` sentinel — NOT subclass of any `<Pkg>APIError` (D-07 invariant); holds `.response` attribute per RESEARCH §Pattern 1 lines 386-396
    - `def _parse_retry_after(value: str) -> float | None` — RFC 9110 §10.2.3 delta-seconds + HTTP-date fallback via `parsedate_to_datetime` per RESEARCH §Pattern 1 lines 402-411
    - `def _is_retryable_status(response: httpx.Response | None) -> bool` per RESEARCH §Pattern 1 lines 398-399
    - `class RetryTransport(httpx.HTTPTransport)`:
      - `__init__(self, *, max_attempts: int = 2, **kwargs: Any) -> None` per RESEARCH lines 427-430
      - `self._max_attempts = max(max_attempts, 1)`
      - `self._logger = logging.getLogger(_LOGGER_NAME)`
      - `def handle_request(self, request: httpx.Request) -> httpx.Response` per RESEARCH lines 432-503:
        - **Mutation gate**: `if not request.extensions.get("idempotent", False): return super().handle_request(request)` ← LINE 1 of method body (D-01 — CRITICAL)
        - **Bypass for max_attempts <= 1**: `if self._max_attempts <= 1: return super().handle_request(request)` (D-19 — max_retries=0 means no retry loop)
        - Extract `request_id`, `endpoint_name`, `account_id` (None for ámbito), `start = time.monotonic()`, `attempt_number = 0` from extensions
        - Outer `try` wraps `Retrying` loop:
          - `stop=stop_after_attempt(self._max_attempts)`
          - `wait=wait_exponential_jitter(initial=1.0, max=30.0, exp_base=2, jitter=1.0)` (D-08)
          - `retry=(retry_if_exception_type(_RETRYABLE_EXC) | retry_if_exception_type(_RetryableStatus))` (D-07)
          - `reraise=True`
        - Inside `for attempt in Retrying(...)`:
          - `with attempt:`
          - `attempt_number = attempt.retry_state.attempt_number`
          - `response = super().handle_request(request)`
          - `response.read()` (body-consume — Phase 7 D-06 alignment per RESEARCH line 459-460)
          - If `_is_retryable_status(response)`: read `Retry-After` header → `_parse_retry_after` → `time.sleep(min(delay, _RETRY_AFTER_CAP_S))` (D-04); emit `WARNING "retry attempt"` log with structured `extra={"package": _LOGGER_NAME, "method": request.method, "url": str(request.url), "status_code": response.status_code, "attempt": attempt_number, "request_id": request_id, "endpoint_name": endpoint_name, "retry_reason": f"status_{response.status_code}", **({"account_id": account_id} if account_id else {})}` (D-09 + D-12)
          - `raise _RetryableStatus(response)` (D-31 sentinel)
          - Else: `return response` (success)
        - Catch `_RetryableStatus`: `return exc.response` (retry exhausted — return last response unmolested per D-05)
        - Catch `_RETRYABLE_EXC`: log ERROR with structured fields + re-raise (terminal transport error per D-12)
      - `__slots__` NOT needed (httpx.HTTPTransport defines its own)
    - Module-level `__all__ = ["RetryTransport"]` (sentinel exception + parse helper internal)

    **`packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py`** (~95 LOC, async mirror):
    - Same module-level constants (DUPLICATED verbatim per "no shared internals" — per CONTEXT Claude's Discretion line 164 — recommendation: avoid one-way coupling) OR import from `_transport.py` (planner judgement). Recommendation: import `_RETRYABLE_EXC, _RETRYABLE_STATUS, _RETRY_AFTER_CAP_S, _RetryableStatus, _parse_retry_after, _is_retryable_status` from `ambito_financiero_client._transport` to avoid 2× duplication WITHIN the same package (still per-package — no cross-package shared) — this is the recommended path; planner picks at execute time.
    - `class AsyncRetryTransport(httpx.AsyncHTTPTransport)`:
      - `__init__(self, *, max_attempts: int = 2, **kwargs: Any) -> None` (same shape as sync)
      - `async def handle_async_request(self, request: httpx.Request) -> httpx.Response`:
        - Mutation gate identical to sync
        - Bypass for max_attempts ≤ 1
        - `async for attempt in AsyncRetrying(...)` (uses `await self.sleep` internally per D-32 — tenacity 9.1.4 confirmed in RESEARCH "Verification Notes" lines 1318-1349)
        - `async with attempt:`
        - `response = await super().handle_async_request(request)`
        - `await response.aread()` (body-consume async variant)
        - Retry-After: `await asyncio.sleep(min(delay, _RETRY_AFTER_CAP_S))` (D-32 — CancelledError propagates)
        - WARNING + ERROR logs identical structured shape
      - `__all__ = ["AsyncRetryTransport"]`

    **`packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py`** (~65 LOC):
    - Module docstring: `"""RedactingFilter + attach() — Phase 8 LOG-01/02/03. ámbito: no auth → minimal patterns. Forward-consistency con iol/higyrus/matriz."""`
    - `from __future__ import annotations`
    - Imports: `logging`, `re`
    - Module-level regex (ámbito-specific minimal set per PATTERNS line 254-261 — ámbito has no auth credentials, but baseline Bearer/password patterns preserved for forward-consistency):
      - `_BEARER_RE = re.compile(r"Bearer\s+[A-Za-z0-9._\-]+")`
      - `_PASSWORD_URLENC_RE = re.compile(r"(password=)[^&\s]+")`
      - `_PASSWORD_JSON_RE = re.compile(r'("password"\s*:\s*")[^"]+(")')`
    - `def _redact(text: str) -> str`: apply each regex sub in order (Bearer ***, `\1***`, `\1***\2`)
    - `class RedactingFilter(logging.Filter)`:
      - `def filter(self, record: logging.LogRecord) -> bool` per RESEARCH §Pattern 4 lines 653-672:
        - If `isinstance(record.msg, str)`: `record.msg = _redact(record.msg)`
        - If `record.args`: handle both dict and tuple via _redact per element
        - Scan `record.__dict__` for sentinel substrings and redact
        - Always `return True`
    - `def attach() -> None` per RESEARCH §Pattern 4 lines 675-682:
      - `logger = logging.getLogger("ambito_financiero_client")`
      - Idempotency: `if not any(isinstance(h, logging.NullHandler) for h in logger.handlers): logger.addHandler(logging.NullHandler())`
      - Idempotency: `if not any(isinstance(f, RedactingFilter) for f in logger.filters): logger.addFilter(RedactingFilter())`
    - `__all__ = ["RedactingFilter", "attach"]`

    **`packages/ambito-financiero-client/tests/test_transport.py`** (~85 LOC):
    - Unit tests for `RetryTransport` (no full Client — direct transport instantiation with httpx.MockTransport):
      - `test_non_idempotent_request_passes_through`: idempotent=False on request → super().handle_request called once → no Retrying loop (mutation gate verified)
      - `test_idempotent_get_retries_on_503`: 3 responses (503, 503, 200) → final 200 returned + 3 wire requests (Retrying loop active)
      - `test_idempotent_get_eventually_returns_5xx_on_exhaustion`: 3 responses (503, 503, 503) → 503 returned (no exception raised; APIError raised LATER by _core.raise_for_response)
      - `test_retry_after_cap_60s`: 429 + Retry-After:600 → time.monotonic delta < 65s + retry happens
      - `test_max_attempts_1_bypasses_loop`: max_attempts=1 → no Retrying iterator → 1 wire request
      - `test_request_id_in_log_records_persists_across_attempts`: caplog with DEBUG → record `extra['request_id']` same across N attempts (D-30)
      - `test_request_id_persists_in_extensions`: pre-set request.extensions['request_id'] → after retry, extensions unchanged (Pitfall 9)

    **`packages/ambito-financiero-client/tests/test_logging.py`** (~45 LOC):
    - Unit tests for `RedactingFilter` + `attach()`:
      - `test_attach_idempotent`: call attach() 3× → only 1 NullHandler + 1 RedactingFilter
      - `test_redact_bearer_token`: filter on record with `msg="Bearer abc123"` → after filter, msg="Bearer ***"
      - `test_redact_password_in_msg`: filter on `msg="password=secret"` → "password=***"
      - `test_filter_returns_true_always`: filter returns True (record always emitted)
      - `test_record_dict_scan`: extra={"weird": "Bearer xxx"} → record.__dict__["weird"] redacted
  </behavior>
  <action>
    1. Create `packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py` per RESEARCH §Pattern 1 (lines 348-504 verbatim with logger_name="ambito_financiero_client" + package=ámbito_financiero_client in extra). Follow PATTERNS §lines 70-131 + CONVENTIONS (from __future__ import annotations + __all__ + module docstring per PATTERNS §"Shared Patterns" lines 750-787).

    2. Create `packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py` per RESEARCH §Pattern 1 async variant + PATTERNS §lines 134-180. Recommendation: import `_RETRYABLE_EXC, _RETRYABLE_STATUS, _RETRY_AFTER_CAP_S, _RetryableStatus, _parse_retry_after, _is_retryable_status` from `ambito_financiero_client._transport` (intra-package import OK — same-package no-shared-internals; saves ~30 LOC duplication WITHIN ámbito). `AsyncRetrying` + `await asyncio.sleep` + body-consume via `await response.aread()`. Per D-32.

    3. Create `packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py` per RESEARCH §Pattern 4 lines 605-682 verbatim with logger_name="ambito_financiero_client". Per D-10 — ámbito-specific minimal patterns (Bearer + password baseline; no IOL refresh_token / Higyrus JSON / matriz auth_basic patterns needed). Note: ámbito has no real credentials, so the patterns are mostly forward-consistency — Plans 3-5 will EXTEND with iol/higyrus/matriz-specific patterns IN THEIR OWN per-package `_logging.py` (each duplicated).

    4. Create `packages/ambito-financiero-client/tests/test_transport.py` with the ~7 unit tests listed in <behavior>. Use `pytest_httpx.HTTPXMock` + `httpx.Client(transport=RetryTransport(max_attempts=N))` directly. Tests verify the transport in isolation BEFORE Client integration (Task 2). Pre-set request.extensions before calling transport.handle_request directly.

    5. Create `packages/ambito-financiero-client/tests/test_logging.py` with the ~5 unit tests listed in <behavior>. Use `logging.LogRecord` direct instantiation for filter tests (no real wire requests needed).

    6. Run `uv run pytest packages/ambito-financiero-client/tests/test_transport.py packages/ambito-financiero-client/tests/test_logging.py -v` — MUST all GREEN before proceeding to Task 2 (transport + logging modules work in isolation).
  </action>
  <verify>
    <automated>test -f packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py &amp;&amp; test -f packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py &amp;&amp; test -f packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py &amp;&amp; test -f packages/ambito-financiero-client/tests/test_transport.py &amp;&amp; test -f packages/ambito-financiero-client/tests/test_logging.py &amp;&amp; grep -c "class RetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py &gt;= 1 &amp;&amp; grep -c "class AsyncRetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py &gt;= 1 &amp;&amp; grep -c "class RedactingFilter" packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py &gt;= 1 &amp;&amp; grep -c "from __future__ import annotations" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py &gt;= 1 &amp;&amp; grep -c "wait_exponential_jitter" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py &gt;= 1 &amp;&amp; grep -c "AsyncRetrying" packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py &gt;= 1 &amp;&amp; uv run pytest packages/ambito-financiero-client/tests/test_transport.py packages/ambito-financiero-client/tests/test_logging.py -v 2&gt;&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py` exists with `class RetryTransport(httpx.HTTPTransport)` definition
    - Source: `_transport.py` contains `tenacity` imports: `Retrying`, `retry_if_exception_type`, `stop_after_attempt`, `wait_exponential_jitter`
    - Source: `_transport.py` contains mutation gate: `grep -c "extensions.get..idempotent" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py >= 1`
    - Source: `_transport.py` contains Retry-After cap: `grep -c "_RETRY_AFTER_CAP_S\|Retry-After" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py >= 1`
    - Source: `_transport.py` constants verified: `_RETRYABLE_STATUS = frozenset({408, 409, 429, *range(500, 600)})` per D-07
    - Source: `_atransport.py` contains `class AsyncRetryTransport(httpx.AsyncHTTPTransport)` + `async def handle_async_request` + `AsyncRetrying`
    - Source: `_atransport.py` contains `await asyncio.sleep` (NOT `time.sleep`) — D-32 verified
    - Source: `_logging.py` contains `class RedactingFilter(logging.Filter)` + `def attach()` + idempotency check `not any(isinstance(h, logging.NullHandler) for h in logger.handlers)`
    - Source: `_logging.py` attaches to `logging.getLogger("ambito_financiero_client")` (matches package import name)
    - Source: All 3 new files have `from __future__ import annotations` as line 1 of imports
    - Source: All 3 new files have `__all__` module-level export list
    - CLI: `uv run pytest packages/ambito-financiero-client/tests/test_transport.py -v` exits 0 with ≥6 tests passing
    - CLI: `uv run pytest packages/ambito-financiero-client/tests/test_logging.py -v` exits 0 with ≥4 tests passing
    - CLI: `uv run ruff check packages/ambito-financiero-client/` exits 0 (LOG ruleset doesn't fire on the new files — they don't use basicConfig)
    - CLI: `uv run mypy --strict packages/ambito-financiero-client/` exits 0 (tenacity py.typed verified; new modules type-correct)
  </acceptance_criteria>
  <done>
    3 new private modules (`_transport.py`, `_atransport.py`, `_logging.py`) exist and pass unit tests in isolation; ruff + mypy strict pass; transport handles mutation gate + retry + Retry-After cap; filter scrubs Bearer/password.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ámbito _core.py + client.py + aio.py + __init__.py shell with retry/log wiring + 2 new kwargs; update snapshot (atomic commit per D-21)</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/_core.py, packages/ambito-financiero-client/src/ambito_financiero_client/client.py, packages/ambito-financiero-client/src/ambito_financiero_client/aio.py, packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py, verification/snapshots/ambito_financiero_client-surface.txt</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-03 — login idempotent=True; D-09 — endpoint_name field; D-15 — 2 new kwargs; D-16 — http_client AS-IS; D-19 — max_retries=0 disable; D-28 — snapshot per-plan atomic; D-30 — request_id per business-call)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 3 lines 549-602 — 401 re-auth-once boundary (NOTE: ámbito sin auth → SKIP 401 branch; use as analog para iol/higyrus/matriz)); §"Architectural Responsibility Map" lines 141-152 — capability → tier mapping; §"Standard Stack" §"Code Examples" — log structured extra= fields)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/src/<pkg>/_core.py`" lines 264-289 — ámbito _core delta — flip idempotent=True + add endpoint_name; §"`packages/<pkg>/src/<pkg>/client.py` shell `_request()`" lines 342-374 — ámbito _request delta NO 401 branch; §"`packages/<pkg>/src/<pkg>/client.py` Client.__init__ + configure()" lines 466-518 — 2 new kwargs add + _ensure_http_client wraps RetryTransport; §"`packages/<pkg>/src/<pkg>/__init__.py`" lines 534-548 — `_logging.attach()` invocation; §"`verification/snapshots/<pkg>-surface.txt`" lines 712-723 — snapshot diff format)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py (current Phase 7 shape; verify `RequestSpec` has `idempotent: bool = False` forward-declared; verify all `build_X_request` functions exist for endpoints)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (current `Client.__init__` lines 58-68 + `_request()` lines ~106-114 + `_ensure_http_client()` + `configure()` body — Phase 7 baseline; READ FULL FILE since modifications span Client class + module-level configure())
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (current `AsyncClient.__init__` + `_request()` async path; mirror Client sync extension)
    - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py (current re-exports + `__all__`; locate insertion point for `_logging.attach()` — RIGHT AFTER imports, BEFORE `__all__`)
    - verification/snapshots/ambito_financiero_client-surface.txt (current Phase 7 snapshot; identify the 3 lines for Client.__init__, AsyncClient.__init__, configure() that need extension)
    - verification/regen_snapshots.py (Phase 6 script — invoke after changes to regen; or manual edit per D-28 — planner picks at execute time)
  </read_first>
  <behavior>
    Extend the existing Phase 7 shell to incorporate RetryTransport + RedactingFilter + 2 new kwargs:

    **`packages/ambito-financiero-client/src/ambito_financiero_client/_core.py`** (extend in place):
    - `RequestSpec` dataclass: add `endpoint_name: str = ""` after existing `idempotent: bool = False` (D-09)
    - For EVERY `build_<endpoint>_request(state, ...) -> RequestSpec` function: set `endpoint_name="<endpoint>"` + `idempotent=True` (since all ámbito endpoints are GETs — confirmed by `<context>` PROJECT.md "ámbito ... HTTP sync+async, no auth")
    - Example: `def build_dollar_banco_nacion_request(state, date)` → returns `RequestSpec(method="GET", path=..., params={...}, idempotent=True, endpoint_name="get_dollar_banco_nacion")`

    **`packages/ambito-financiero-client/src/ambito_financiero_client/client.py`** (extend in place):
    - `Client.__init__` signature delta — add `max_retries: int = 2` and `http_client: httpx.Client | None = None` keyword-only kwargs per D-15/D-16:
      ```python
      def __init__(
          self,
          *,
          base_url: str | None = None,
          user_agent: str | None = None,
          max_retries: int = 2,
          http_client: httpx.Client | None = None,
      ) -> None:
      ```
    - Body: existing logic + `self._max_retries = max_retries`. If `http_client is not None`: `self._state.http_client = http_client` (D-16 AS-IS, no auto-wrap).
    - `Client.__slots__` delta: add `"_max_retries"` to the slots tuple (currently `("_state",)` per PATTERNS line 520). Per project convention slots are mandatory.
    - `_ensure_http_client()` delta — wrap `httpx.Client(transport=RetryTransport(max_attempts=self._max_retries+1))`:
      ```python
      from ambito_financiero_client import _transport
      def _ensure_http_client(self) -> httpx.Client:
          if self._state.http_client is None:
              self._state.http_client = httpx.Client(
                  timeout=_REQUEST_TIMEOUT,
                  headers={"User-Agent": self._state.user_agent},
                  transport=_transport.RetryTransport(max_attempts=self._max_retries + 1),
              )
          assert isinstance(self._state.http_client, httpx.Client)
          return self._state.http_client
      ```
      NOTE: `max_retries=N` → `max_attempts=N+1` per D-19 (semantics: max_retries=0 → max_attempts=1 → 1 outgoing total = bypass). Anthropic/openai pattern (RESEARCH §"D-06 vs D-19" line 516).
    - `_request()` delta — migrate from `client.request(method, url, ...)` to `build_request + send` so `request.extensions` can be set BEFORE send (per RESEARCH §"Assumptions Log" A7):
      ```python
      def _request(self, spec: _core.RequestSpec) -> httpx.Response:
          http = self._ensure_http_client()
          request_id = uuid.uuid4().hex
          req = http.build_request(
              spec.method,
              f"{self._state.base_url}{spec.path}",
              params=spec.params,
              headers=spec.headers,
          )
          req.extensions["idempotent"] = spec.idempotent
          req.extensions["request_id"] = request_id
          req.extensions["endpoint_name"] = spec.endpoint_name
          return http.send(req)
      ```
      NOTE: ámbito has NO 401 re-auth branch (no auth surface — per PATTERNS line 359 "no 401 re-auth — ámbito no auth").
    - `configure()` signature delta — add `max_retries: int | None = None` and `http_client: httpx.Client | None = None` carry-forward params; pass through to `_default_client = Client(...)`. If `max_retries is None`, omit from kwargs (Client default takes over).
    - Imports: add `import uuid` at module top + `from ambito_financiero_client import _transport` for `_ensure_http_client`.

    **`packages/ambito-financiero-client/src/ambito_financiero_client/aio.py`** (extend in place — mirror sync deltas):
    - `AsyncClient.__init__` signature delta — same 2 kwargs (`max_retries`, `http_client: httpx.AsyncClient | None = None`)
    - `_ensure_http_client()` async delta — wrap `httpx.AsyncClient(transport=_atransport.AsyncRetryTransport(max_attempts=self._max_retries+1))`
    - `_request()` async delta — same `build_request + send` migration + `request.extensions` setup; NO 401 re-auth branch
    - Imports: add `import uuid` + `from ambito_financiero_client import _atransport`
    - `configure()` (if module-level exists in aio.py) — same kwargs added
    - `AsyncClient.__slots__` — add `_max_retries`

    **`packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py`** (extend in place):
    - INSERT after existing imports, BEFORE `__all__` per PATTERNS lines 539-547:
      ```python
      # Phase 8 LOG-01: attach NullHandler + RedactingFilter to package logger.
      # Library convention per Python Logging HOWTO. NEVER touches logging.root.
      from ambito_financiero_client import _logging as _logging_attach
      _logging_attach.attach()
      del _logging_attach  # NOT re-exported — internal wiring only
      ```
    - DO NOT add `_logging` to `__all__` (D-13 + LOG-01 — internal wiring).
    - Verify `__all__` block unchanged (Pitfall 8 prevention — snapshot diff explosion).

    **`verification/snapshots/ambito_financiero_client-surface.txt`** (update per D-28):
    - Identify the 3 lines per PATTERNS §lines 716-723:
      - `Client : class : (*, base_url: 'str | None' = None, user_agent: 'str | None' = None) -> 'None'`
      - Idem for `AsyncClient`
      - Idem for `configure : function : (...)`
    - REPLACE each with the new signature including 2 new kwargs:
      - `Client : class : (*, base_url: 'str | None' = None, user_agent: 'str | None' = None, max_retries: 'int' = 2, http_client: 'httpx.Client | None' = None) -> 'None'`
      - `AsyncClient : class : (*, base_url: 'str | None' = None, user_agent: 'str | None' = None, max_retries: 'int' = 2, http_client: 'httpx.AsyncClient | None' = None) -> 'None'`
      - `configure : function : (...same kwargs added with `Optional[int]` / `Optional[httpx.Client]` for the carry-forward None defaults...)`
    - Verify exact format by running `verification/regen_snapshots.py` IF available; OR manual edit + verification via `uv run pytest verification/test_public_surface.py -k ambito -v` (which compares signatures).
    - DO NOT add/remove any other entries (only Client/AsyncClient/configure() change). Public surface zero-drift principle (Phase 6 D-16 still enforced).

    **Atomic commit** — single commit message per RESEARCH §"Commit message patterns Phase 8" line 413:
    `feat(ambito): retries + structured logging — RetryTransport, RedactingFilter (RELY-01..04, LOG-01..03)`
  </behavior>
  <action>
    1. Edit `packages/ambito-financiero-client/src/ambito_financiero_client/_core.py`:
       - Add `endpoint_name: str = ""` to `RequestSpec` dataclass after `idempotent: bool = False`
       - For each `build_<endpoint>_request(...)` function: set `idempotent=True` + `endpoint_name="<fn name without 'build_' prefix and without '_request' suffix>"` in the returned `RequestSpec` literal. Confirm no `build_login_request` exists for ámbito (it has no auth — verify by reading current `_core.py`).

    2. Edit `packages/ambito-financiero-client/src/ambito_financiero_client/client.py`:
       - Add `import uuid` to top imports + `from ambito_financiero_client import _transport`
       - Add 2 kwargs to `Client.__init__` signature; handle them in body
       - Add `"_max_retries"` to `Client.__slots__` tuple
       - Modify `_ensure_http_client()` to instantiate `RetryTransport(max_attempts=self._max_retries+1)` and pass as `transport=` kwarg to `httpx.Client(...)`
       - Replace `_request()` body: migrate from `client.request(method, ...)` to `http.build_request(...)` + set `req.extensions["idempotent"/"request_id"/"endpoint_name"]` + `http.send(req)`
       - Modify `configure()` to accept the 2 carry-forward kwargs + pass them to the `Client(...)` constructor that replaces `_default_client`

    3. Edit `packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` — apply mirror sync deltas:
       - Add `import uuid` + `from ambito_financiero_client import _atransport`
       - Add 2 kwargs (`http_client: httpx.AsyncClient | None = None`) to `AsyncClient.__init__`; add `"_max_retries"` to slots
       - Modify `_ensure_http_client()` async to wrap `httpx.AsyncClient(transport=AsyncRetryTransport(max_attempts=self._max_retries+1))`
       - Modify `_request()` async to `await http.build_request(...)` (note: `build_request` is sync on both Client and AsyncClient; only `send` is async) + set extensions + `await http.send(req)`

    4. Edit `packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py`:
       - Insert `_logging.attach()` block per PATTERNS lines 539-547 between imports and `__all__`
       - Verify `__all__` content NOT changed
       - Verify `_logging` NOT appearing as module attribute via `del` cleanup

    5. Update `verification/snapshots/ambito_financiero_client-surface.txt`:
       - Replace the 3 lines for Client/AsyncClient/configure() with new signatures including `max_retries: 'int' = 2` and `http_client: 'httpx.Client | None' = None` (or `httpx.AsyncClient | None` for AsyncClient)
       - Preserve trailing newline + sort order
       - Recommendation: run `uv run python verification/regen_snapshots.py` IF it accepts a per-package filter; OR manual edit + verify via `pytest verification/test_public_surface.py -k ambito`

    6. Run verification:
       ```
       uv run pytest packages/ambito-financiero-client/ -v 2>&1 | tail -20  # ámbito unit tests including new test_transport.py + test_logging.py
       uv run pytest verification/test_public_surface.py -k ambito -v        # snapshot zero diff
       uv run pytest verification/test_retry_mutation_gate.py -k ambito -v   # mutation gate guard test (GET retry branch should PASS)
       uv run pytest verification/test_retry_after_cap.py -v                  # may use ámbito or iol — verify GREEN if uses ámbito
       uv run pytest verification/test_logging_root_unchanged.py -v          # GREEN — ámbito now uses NullHandler not root
       uv run pytest verification/test_logging_no_token_leak.py -k ambito -v # ámbito branch: SECRET in base_url DOES appear (inverse sanity) OR sentinel correctly handled
       uv run pytest verification/test_async_cancellation.py -k ambito -v    # GREEN — AsyncRetryTransport respects CancelledError
       uv run ruff check packages/ambito-financiero-client/ verification/    # LOG ruleset still GREEN
       uv run mypy --strict packages/ambito-financiero-client/                # tenacity py.typed compatible
       uv run lint-imports                                                    # Phase 7 D-09 contracts still GREEN (NEW: _logging.py / _transport.py may need new contracts — verify)
       ```

    7. Single atomic commit per D-21:
       ```
       git add packages/ambito-financiero-client/src/ packages/ambito-financiero-client/tests/ verification/snapshots/ambito_financiero_client-surface.txt
       git commit -m "feat(ambito): retries + structured logging — RetryTransport, RedactingFilter (RELY-01..04, LOG-01..03)

       Phase 8 Plan 2 — ámbito canary. Adds RetryTransport (sync+async) with tenacity Retrying loop, full-jitter backoff (base=1s, max=30s, exp=2), Retry-After cap 60s, mutation gate via request.extensions['idempotent']. Adds RedactingFilter + attach() to package logger (NullHandler). Extends Client/AsyncClient/configure() with max_retries=2 + http_client=None kwargs. ámbito has no auth so no 401 re-auth branch.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
       ```
  </action>
  <verify>
    <automated>grep -c "endpoint_name" packages/ambito-financiero-client/src/ambito_financiero_client/_core.py &gt;= 1 &amp;&amp; grep -c "idempotent=True" packages/ambito-financiero-client/src/ambito_financiero_client/_core.py &gt;= 1 &amp;&amp; grep -c "max_retries" packages/ambito-financiero-client/src/ambito_financiero_client/client.py &gt;= 2 &amp;&amp; grep -c "max_retries" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py &gt;= 2 &amp;&amp; grep -c "RetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/client.py &gt;= 1 &amp;&amp; grep -c "AsyncRetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py &gt;= 1 &amp;&amp; grep -c "request.extensions" packages/ambito-financiero-client/src/ambito_financiero_client/client.py &gt;= 1 &amp;&amp; grep -c "uuid.uuid4" packages/ambito-financiero-client/src/ambito_financiero_client/client.py &gt;= 1 &amp;&amp; grep -c "_logging.attach\|_logging_attach.attach" packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py &gt;= 1 &amp;&amp; grep -c "max_retries" verification/snapshots/ambito_financiero_client-surface.txt &gt;= 2 &amp;&amp; grep -c "http_client" verification/snapshots/ambito_financiero_client-surface.txt &gt;= 2 &amp;&amp; uv run pytest packages/ambito-financiero-client/ -v 2&gt;&amp;1 | tail -15 &amp;&amp; uv run pytest verification/test_public_surface.py -k ambito -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_retry_mutation_gate.py -k ambito -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_logging_root_unchanged.py -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_logging_no_token_leak.py -k ambito -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_async_cancellation.py -k ambito -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run ruff check packages/ambito-financiero-client/ verification/ &amp;&amp; uv run mypy --strict packages/ambito-financiero-client/ &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "endpoint_name" packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` >= 1 (RequestSpec extended)
    - Source: `grep -c "idempotent=True" packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` >= 1 (GET builders flipped)
    - Source: `grep -c "max_retries" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` >= 2 (signature + body)
    - Source: `grep -c "max_retries" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` >= 2 (signature + body)
    - Source: `grep -c "RetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` >= 1 (import + instantiation in _ensure_http_client)
    - Source: `grep -c "AsyncRetryTransport" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` >= 1
    - Source: `grep -c "request.extensions" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` >= 1 (idempotent/request_id/endpoint_name setup)
    - Source: `grep -c "uuid.uuid4" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` >= 1 (request_id generation per D-30)
    - Source: `grep -c "_logging.attach\|_logging_attach.attach" packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py` >= 1
    - Source: `grep -c "del _logging" packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py` >= 1 (Pitfall 8 prevention)
    - Source: `grep -c "max_retries" verification/snapshots/ambito_financiero_client-surface.txt` >= 2 (Client + AsyncClient + configure())
    - Source: `grep -c "http_client" verification/snapshots/ambito_financiero_client-surface.txt` >= 2
    - CLI: `uv run pytest packages/ambito-financiero-client/ -v` exits 0 (all existing tests + new test_transport.py + new test_logging.py)
    - CLI: `uv run pytest verification/test_public_surface.py -k ambito -v` exits 0 (snapshot matches updated file — zero diff)
    - CLI: `uv run pytest verification/test_logging_root_unchanged.py -v` exits 0 (ámbito import does not add to logging.root.handlers; NullHandler added to ambito_financiero_client logger only)
    - CLI: `uv run pytest verification/test_logging_no_token_leak.py -k ambito -v` exits 0 (ámbito-specific sentinel handling)
    - CLI: `uv run pytest verification/test_async_cancellation.py -k ambito -v` exits 0 (AsyncRetryTransport CancelledError propagation)
    - CLI: `uv run ruff check packages/ambito-financiero-client/ verification/` exits 0 (LOG ruleset doesn't fire)
    - CLI: `uv run mypy --strict packages/ambito-financiero-client/` exits 0 (strict type checks pass)
    - CLI: `uv run lint-imports` exits 0 (Phase 7 D-09 contracts + Phase 8 additions all pass — `_core.py` does NOT import from `_transport.py`/`_atransport.py`/`_logging.py`)
    - CLI: `git log --oneline -1 | grep -c "feat(ambito)"` >= 1 (atomic commit landed per D-21)
  </acceptance_criteria>
  <done>
    ámbito Plan 2 atomic commit landed: RetryTransport+AsyncRetryTransport+RedactingFilter+attach() wired; Client/AsyncClient/configure() have 2 new kwargs; shell `_request()` generates request_id + extensions; snapshot updated to include kwargs; all 5 cross-cutting guard tests GREEN for ámbito branch; existing ámbito tests preserved; ruff+mypy+lint-imports GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ambito_financiero_client.Client._request` → `state.http_client.send(request)` | Domain shell → transport layer; mutation gate set HERE pre-send via `request.extensions["idempotent"]` from `RequestSpec.idempotent` (D-01) |
| `ambito_financiero_client._transport.RetryTransport.handle_request` → `super().handle_request(request)` | Transport → wire; retry loop wraps; mutation gate top-of-method pass-through if `not extensions.get("idempotent", False)` |
| `ambito_financiero_client.__init__` → `logging.getLogger("ambito_financiero_client")` | Module side-effect → process-wide logging tree; only `NullHandler` + `RedactingFilter` attached; NEVER root logger (LOG-01) |
| `ambito_financiero_client.Client(http_client=user_supplied)` → `state.http_client = http_client` | Caller power-user path → uses transport AS-IS; documented contract (D-16) — no auto-wrap, caller responsible for own retry policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01-AMBITO | Tampering | Mutation gate (GET-only paquete; sanity check that idempotent=True is propagated correctly) | mitigate | `_core.py` builders flip `idempotent=True` for all 4 dollar endpoints; `_request()` propagates via `request.extensions`; verified by `test_retry_mutation_gate.py[ambito]` GET retry branch (turns GREEN — no mutating endpoints) |
| T-8-03-AMBITO | Tampering | Library hijacks root logger | mitigate | `__init__.py` `_logging.attach()` adds NullHandler to `getLogger("ambito_financiero_client")` only; `verification/test_logging_root_unchanged.py` covers (turns GREEN with ámbito's first attach landing); ruff LOG015 + CI grep covers static enforcement |
| T-8-04-AMBITO | Information Disclosure | DEBUG records may leak in base_url (no token, but base_url is user-supplied) | accept | ámbito has no auth → no token redaction surface; base_url-as-PII is out of scope (it's a public URL by design); `RedactingFilter` still present for forward-consistency with Bearer/password baseline patterns; `test_logging_no_token_leak.py[ambito]` uses sentinel in base_url to verify URL appears as expected (sanity inverse — confirms parametrize works correctly across all 4 packages) |
| T-8-05-AMBITO | Availability | async cancellation swallowed during retry | mitigate | `AsyncRetryTransport` uses `AsyncRetrying` (D-32) — `await self.sleep(do)` is `asyncio.sleep` natively; CancelledError propagates per RESEARCH "Verification Notes" line 1318-1349; `test_async_cancellation.py[ambito]` covers |
| T-8-09-AMBITO | Tampering | http_client= passed by caller bypasses RetryTransport silently | accept | D-16 LOCKED — power-user path; documented contract in docstring; caller responsible. v1.2 backlog: detect+warn if `http_client.transport` lacks RetryTransport. |
| T-8-IMPORT-LINTER | Tampering | New `_transport.py`/`_logging.py` modules accidentally import `client.py`/`aio.py` (creates circular dep + breaks Phase 7 dedup invariant) | mitigate | Recommend Plan 1 OR Plan 2 add new import-linter contracts: `ambito_financiero_client._transport ⊬ {client, aio}` and `ambito_financiero_client._logging ⊬ {client, aio}`. The current modules don't import client/aio (verified by RESEARCH); contract is defensive. Planner decides during execute time. |
</threat_model>

<verification>
- `_transport.py` + `_atransport.py` + `_logging.py` exist (4 packages will follow, ámbito is canary)
- ámbito `_core.RequestSpec.endpoint_name` field added; all GET builders set `idempotent=True` + `endpoint_name=<name>`
- `Client.__init__` + `AsyncClient.__init__` + `configure()` accept `max_retries: int = 2` + `http_client: ... = None` kwargs
- Shell `_request()` generates `request_id = uuid.uuid4().hex` + sets `request.extensions["idempotent"/"request_id"/"endpoint_name"]`
- `__init__.py` invokes `_logging.attach()` + `del` cleanup (Pitfall 8 prevention)
- Snapshot `verification/snapshots/ambito_financiero_client-surface.txt` updated with 2 new kwargs in 3 signatures
- Existing ámbito tests GREEN; cross-cutting guard tests for ámbito branch GREEN
- ruff + mypy strict + lint-imports GREEN
- Single atomic commit per D-21
</verification>

<success_criteria>
- ámbito canary delivers Phase 8 pattern end-to-end: RetryTransport + AsyncRetryTransport + RedactingFilter + Client surface extension + snapshot update
- Cross-cutting guard tests turn GREEN for ámbito parametrize branches: `test_retry_mutation_gate.py[ambito]` (GET retry), `test_retry_after_cap.py` (if uses ámbito), `test_logging_root_unchanged.py` (ámbito doesn't pollute root), `test_logging_no_token_leak.py[ambito]` (sanity inverse), `test_async_cancellation.py[ambito]` (CancelledError propagates)
- ámbito phase 7 baseline tests still GREEN (regression-free per Pitfall 18)
- Single commit `feat(ambito): retries + structured logging — RetryTransport, RedactingFilter (RELY-01..04, LOG-01..03)` lands per D-21
- Pattern established for Plan 3 (iol) to duplicate with auth-specific extensions
</success_criteria>

<output>
Create `.planning/phases/08-retries-backoff-structured-logging/08-02-SUMMARY.md` documenting:
- LOC delta per RESEARCH §"specifics" lines 418-426 format (e.g., `_transport.py: 0 → 95 (NEW)`, `_core.py: 180 → 195 (+8%)`, `client.py: 320 → 360 (+12%)`, `aio.py: 305 → 345 (+13%)`)
- Snapshot diff: exactly the 2 new kwargs lines for Client/AsyncClient/configure()
- Cross-cutting guard tests status per ámbito parametrize branch (which PASS now vs which still RED waiting for Plans 3-5)
- Atomic commit hash with message
- "ámbito canary established — pattern verified mechanically for Plan 3 (iol auth-flow) replication"
</output>
