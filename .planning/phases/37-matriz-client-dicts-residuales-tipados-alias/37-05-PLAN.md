---
phase: 37
plan: 05
type: execute
wave: 3
depends_on: [37-03]
files_modified:
  - packages/matriz-client/src/matriz_client/models.py
  - packages/matriz-client/tests/test_null_object.py
autonomous: true
requirements: [NOBJ-MTZ-02]

must_haves:
  truths:
    - "F-12: MarketDataSnapshot is simultaneously the REST return type and the payload of MarketDataFrame, so six properties on ONE class satisfy Success Criterion 3 on both surfaces — there is no WebSocket-side alias work and a ws_client.py edit here would be a no-op."
    - "The six aliases are bids over BI, offers over OF, last over LA, settlement over SE, close over CL, open_interest over OI — read-only views with no copy, no cache and no transformation."
    - "OP is a bare float and is deliberately NOT aliased, matching the Phase 36 template's exclusion of the scalar entries; matriz's extra scalars IV, EV, NV and ACP get no aliases either."
    - "F-15: properties are already proven invisible to get_type_hints and to the divergence count by the Phase 35 fixtures — this plan APPLIES that invariant and does not rewrite its proof."
    - "SC-4: the WebSocket daemon-thread paths stay green, including the per-connection and per-frame decode-mode propagation snapshotted at connect time — properties add no decode path, so this holds by construction and is asserted, not assumed."
    - "F-13: the roster floor is a lower bound and cannot break; raising it after three new classes is hygiene matching the Phase 36 precedent, never a blocking dependency."
    - "mypy --strict is clean over the package with the aliases in place, which is the typed half of Success Criterion 3."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/models.py"
      provides: "Six read-only alias properties on MarketDataSnapshot"
      contains: "def open_interest"
    - path: "packages/matriz-client/tests/test_null_object.py"
      provides: "Alias disjointness, identity on both surfaces, and the raised roster floor"
      contains: "open_interest"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "each alias returns the wire-named attribute unchanged"
      pattern: "return self\\.(BI|OF|LA|SE|CL|OI)"
    - from: "packages/matriz-client/tests/test_null_object.py"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "the identity test exercises a WS-frame-parsed snapshot reached through MarketDataFrame.marketData, proving one class serves both surfaces"
      pattern: "MarketDataFrame\\.from_api"
  prohibitions:
    - statement: "Do not write a separate WebSocket alias task and do not edit ws_client.py. RESEARCH F-12 measured that MarketDataFrame.marketData IS a MarketDataSnapshot, so the WS surface inherits the six properties for free. A WS-side task would be a no-op that fabricates the appearance of coverage."
    - statement: "Do not rewrite the property-invisibility proof. test_property_aliases_are_invisible_to_get_type_hints and test_adding_a_property_alias_does_not_change_the_divergence_count, with their _AliasShaped and _AliasFree fixtures, already prove the invariant generically — their own docstring says they exist for phases 36 through 38. Applying it is this plan's job; re-proving it is not."
    - statement: "Do not alias OP, HI, LO, TV, IV, EV, NV or ACP. The alias set is exactly the six the requirement names; adding more would diverge from the market-data template and from NOBJ-MTZ-02."
    - statement: "Do not give an alias a transformation, a default, a cache or a setter. A property that does anything other than return the wire field is no longer a view, and the invisibility argument stops being about naming."
    - statement: "Do not sequence anything behind the roster-floor bump. The assertion is a lower bound and cannot break; treating it as a dependency would invert F-13."
    - statement: "Do not touch _core.py, _decode.py, ws_client.py, client.py, aio.py or tools/ in this plan."
---

<objective>
Give `matriz_client.models.MarketDataSnapshot` the same six human-facing alias properties
`market-data-client` gained in Phase 36, so `snapshot.last.price`, `.bids`, `.offers`,
`.settlement`, `.close` and `.open_interest` read the same whether the instance arrived from the
REST surface or from a WebSocket frame.

Purpose: NOBJ-MTZ-02 and Success Criterion 3. The criterion's "es el mismo objeto y el mismo juego
de alias" is already true structurally — `MarketDataFrame.marketData` is a `MarketDataSnapshot` —
so the work is six properties on one class plus the test that makes the shared-object claim
falsifiable rather than assumed.

Output: six read-only properties, alias tests exercising both a REST-parsed and a WS-frame-parsed
instance, and the raised `_SafeModel` roster floor.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-CONTEXT.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-RESEARCH.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates the rows marked **37-05**.

| Symbol | Kind | Home | Created by |
|--------|------|------|------------|
| `parse_get_detailed_positions_response` / `parse_get_account_report_response` | envelope unwrap | `matriz_client._core` | 37-01 |
| `_parse_risk_response` | REMOVED | `matriz_client._core` | 37-01 |
| `TickPriceRange` | `_SafeModel` dataclass, `baseline` provenance | `matriz_client.models` | 37-02 |
| `_mapping_value` / `_apply_mapping_policy` / `_convert` | element-typed, self-recursing axis | `matriz_client.models` | 37-02 |
| `InstrumentDetail.tickPriceRanges` | → `dict[str, TickPriceRange]` | `matriz_client.models` | 37-02 |
| `InstrumentPositionReport` / `DetailedAccountReport` | `_SafeModel` dataclasses, `vendor-documented, UNMEASURED` provenance | `matriz_client.models` | 37-03 |
| `DetailedPosition.report` / `AccountReport.detailedAccountReports` / `AccountReport.portfolio` | retypes (2-level / 1-level / scalar) | `matriz_client.models` | 37-03 |
| Two `SHAPE` ledger rows | unobserved-roster declarations | `.planning/verification/matriz-client-findings.md` | 37-03 |
| `_field_candidates_for` / `_adjudicate_field` / `_field_annotation_is_untyped_mapping` / `_FIELD_EXEMPTIONS` / `ScanResult.fields` | new gate internals | `tools/check_surface_types.py` | 37-04 |
| `packages/matriz-client/tests/test_surface_types_red.py` | NEW test module | tests | 37-04 |
| `MarketDataSnapshot.bids` | read-only `@property` over `BI` → `list[MarketDataLevel]` | `matriz_client.models` | **37-05** |
| `MarketDataSnapshot.offers` | read-only `@property` over `OF` → `list[MarketDataLevel]` | `matriz_client.models` | **37-05** |
| `MarketDataSnapshot.last` | read-only `@property` over `LA` → `MarketDataEntryValue` | `matriz_client.models` | **37-05** |
| `MarketDataSnapshot.settlement` | read-only `@property` over `SE` → `MarketDataEntryValue` | `matriz_client.models` | **37-05** |
| `MarketDataSnapshot.close` | read-only `@property` over `CL` → `MarketDataEntryValue` | `matriz_client.models` | **37-05** |
| `MarketDataSnapshot.open_interest` | read-only `@property` over `OI` → `MarketDataEntryValue` | `matriz_client.models` | **37-05** |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the six alias properties and prove they hold on both surfaces</name>
  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/tests/test_null_object.py</files>
  <read_first>
    - packages/market-data-client/src/market_data_client/models.py:378-410 (the Phase 36 alias block — six properties, each a one-line docstring naming the wire field and the decision id, and a single return; this is verbatim-copyable apart from the return types and the decision id)
    - packages/matriz-client/src/matriz_client/models.py:400-441 (`MarketDataLevel`, `MarketDataEntryValue`, and `MarketDataSnapshot`'s fourteen wire fields — note the existing comment recording that `CL` arrives as an object like `LA`/`SE`/`OI` while `OP` is a bare scalar)
    - packages/matriz-client/src/matriz_client/models.py:508-516 (`MarketDataFrame` — its `marketData` field IS a `MarketDataSnapshot`; this is the whole reason there is no WS-side work)
    - packages/matriz-client/src/matriz_client/ws_client.py (read `_parse_frame` and how it builds `MarketDataFrame.from_api` — read only; this plan does not edit it)
    - packages/matriz-client/tests/test_null_object.py:195-215 (the `_AliasShaped` / `_AliasFree` fixtures whose docstring says they are "the exact shape phases 36-38 introduce")
    - packages/matriz-client/tests/test_null_object.py:286-331 (the two invisibility tests — read them so you can cite them instead of duplicating them)
    - packages/market-data-client/tests/test_market_data_chain.py:475-505 (the disjointness test and the identity test using `is` rather than `==`; the matriz variant must restate the disjointness rationale as name shadowing, because matriz's dataclasses carry no slots)
  </read_first>
  <behavior>
    The six aliases:
    - `bids` returns the `BI` list, `offers` returns the `OF` list, `last` returns `LA`,
      `settlement` returns `SE`, `close` returns `CL`, `open_interest` returns `OI`.
    - Each returns the IDENTICAL object the wire-named attribute returns — asserted with identity,
      not equality, which is what proves there is no copy, no cache and no transformation.

    Both surfaces, one object:
    - A snapshot parsed from a REST market-data body and a snapshot reached as
      `MarketDataFrame.from_api(<a frame payload>).marketData` are both `MarketDataSnapshot`
      instances and both answer all six aliases.
    - For a frame payload carrying book levels and a last-price entry,
      `frame.marketData.last.price` and `frame.marketData.bids[0].price` evaluate to the payload's
      values without raising.

    Null Object safety:
    - `MarketDataSnapshot.empty()` answers all six aliases without raising:
      `bids == []`, `offers == []`, and `last`, `settlement`, `close`, `open_interest` are each the
      empty entry-value instance, so `.price` on any of them is `None`.
    - The full chain `MarketDataFrame.empty().marketData.last.price` evaluates to `None`.

    Disjointness and exclusion:
    - The six alias names and the fourteen declared wire field names are disjoint sets, and the
      wire field set is exactly the fourteen currently declared.
    - `OP` is a bare float and has NO alias; `HI`, `LO`, `TV`, `IV`, `EV`, `NV` and `ACP` have none
      either. A test names this exclusion so it reads as a decision rather than an omission.

    Invariants inherited, not re-proven:
    - Adding the six properties changes neither `get_type_hints(MarketDataSnapshot)` nor
      `dataclasses.fields(MarketDataSnapshot)`, and changes no divergence count. The Phase 35
      fixtures already prove this generically; assert it once against the REAL class and cite those
      tests by name rather than duplicating their machinery.
  </behavior>
  <action>
    Append the six properties to `MarketDataSnapshot` in `models.py`, after the last declared field.
    Copy the Phase 36 block's form exactly: one property per alias, a single-line docstring naming
    the wire field it views, and a single return statement. Re-cite matriz's own decision id and the
    requirement `NOBJ-MTZ-02` rather than market-data's decision id. Annotate each return type with
    matriz's class names — the two list aliases return a list of the level model, the four scalar-
    entry aliases return the entry-value model.

    Add a short paragraph to `MarketDataSnapshot`'s class docstring recording three things a future
    reader needs: that these six are views and carry no behaviour; that `OP` is excluded because it
    arrives as a bare scalar rather than an entry object, which the adjacent existing comment
    already explains; and that this ONE class is both the REST return type and
    `MarketDataFrame.marketData`, so the aliases serve the WebSocket surface with no `ws_client.py`
    change — the fact that makes a separate WS task a no-op.

    Add the tests to `test_null_object.py` next to the existing alias fixtures. Write the
    disjointness test against the real class, restating the rationale as NAME SHADOWING rather than
    slot collision — matriz's dataclasses are frozen but carry no slots, so the market-data
    rationale does not transfer verbatim. Write the identity test to run over BOTH a REST-shaped
    snapshot built through `MarketDataSnapshot.from_api` and a WS-shaped one reached through
    `MarketDataFrame.from_api(...).marketData`, asserting identity for all six on each — that pair
    is the direct evidence for Success Criterion 3 and the reason no WS-side task exists. Write the
    empty-chain test. Write the exclusion test naming `OP` and the extra scalars.

    Do not touch the two invisibility tests or their fixtures. Reference them by name in the new
    tests' docstrings so the applied-versus-proven relationship is legible.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/test_null_object.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_null_object.py -k alias -q` exits 0 and collects strictly more tests than before this task.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_null_object.py::test_property_aliases_are_invisible_to_get_type_hints packages/matriz-client/tests/test_null_object.py::test_adding_a_property_alias_does_not_change_the_divergence_count -q` exits 0, and `git diff` shows those two tests and their fixtures unmodified.
    - `uv run python -c "from matriz_client.models import MarketDataSnapshot as S; s=S.empty(); print(s.bids, s.offers, s.last.price, s.settlement.price, s.close.price, s.open_interest.price)"`
      prints `[] [] None None None None` without raising.
    - `uv run python -c "from matriz_client.models import MarketDataFrame as F; print(F.empty().marketData.last.price)"` prints `None`.
    - `uv run python -c "import dataclasses, typing; from matriz_client.models import MarketDataSnapshot as S; f={x.name for x in dataclasses.fields(S)}; a={'bids','offers','last','settlement','close','open_interest'}; print(len(f), f & a, set(typing.get_type_hints(S)) & a)"`
      prints `14 set() set()`.
    - `uv run mypy packages/matriz-client/src` reports success.
    - `git diff --name-only` for this task lists only `models.py` and `test_null_object.py` —
      `ws_client.py` is untouched (F-12).
  </acceptance_criteria>
  <done>All six aliases exist as read-only views, return the identical wire objects on a REST-parsed
  and on a WS-frame-parsed snapshot, survive the empty chain, exclude `OP` by a named decision, and
  are invisible to the walker without any invisibility proof being rewritten.</done>
</task>

<task type="auto">
  <name>Task 2: Raise the roster floor and close the phase against the WebSocket and cross-package gates</name>
  <files>packages/matriz-client/tests/test_null_object.py</files>
  <read_first>
    - packages/matriz-client/tests/test_null_object.py:218-255 (the roster floor assertion, its docstring's rationale for a lower bound over an equality, and the parametrized roster tests that auto-extend to new classes)
    - packages/matriz-client/tests/test_null_object.py:105-150 (`_perturb` — confirm its `cur is None` branch covers the three new classes' all-optional-scalar shape; RESEARCH Pitfall 5 says a class whose fields default to something else would raise `AssertionError`)
    - packages/matriz-client/tests/test_null_object.py:250-275 (the comment explaining why `UnknownFrame` is deliberately invisible to `_safemodel_classes()` — the new floor must not accidentally count it)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-03-SUMMARY.md (the measured `_safemodel_classes()` count after the three new classes landed — use that number, do not guess)
    - packages/matriz-client/src/matriz_client/ws_client.py (the connect-time decode-mode snapshot and the per-frame bound context — read only, to confirm nothing in this phase perturbs it)
  </read_first>
  <action>
    Measure the roster first with
    `uv run --package matriz-client pytest packages/matriz-client/tests/test_null_object.py -q` plus a
    direct count, then raise the floor assertion from its current lower bound to the measured value.
    Keep it a lower bound, never an equality — the docstring already explains that phases may
    legitimately add classes, and the Phase 36 precedent in `market-data-client` does the same. Update
    the docstring's rationale to name the three classes this phase added and the phase that added
    them, so the number has a provenance.

    Confirm, and record in the SUMMARY, that the parametrized roster tests picked up all three new
    classes and that each satisfies falsy-when-empty, truthy-when-perturbed and silent-`empty()`. If
    `_perturb` raises on any of them, do NOT special-case it in the test — that means a new model
    carries a field shape outside the four the dispatcher handles, and the fix belongs in the model.

    Then run the phase-closing sweep and record every result verbatim: the matriz suite, the
    WebSocket suites including the per-connection decode-mode propagation, `mypy --strict` over the
    package, all four repo gates, the two `verification/` guards the lint job runs explicitly, and
    every one of the six package suites. Nothing in this task changes behaviour; its output is the
    evidence for Success Criteria 3 and 4.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_ws_client.py packages/matriz-client/tests/test_ws_decode_mode.py -q` exits 0.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_null_object.py -q` exits 0, and the floor assertion's bound equals the count recorded in `37-03-SUMMARY.md`.
    - `uv run python -c "import subprocess" ` is not used — the roster count is taken in-process:
      `uv run --package matriz-client python -c "import sys; sys.path.insert(0,'packages/matriz-client/tests'); from test_null_object import _safemodel_classes; print(len(_safemodel_classes()))"`
      prints the same number as the raised floor.
    - `uv run mypy packages/matriz-client/src` reports success — the typed half of Success Criterion 3.
    - `uv run python tools/check_surface_types.py`, `uv run python tools/check_decode_intactness.py`,
      `uv run python tools/check_uniform_structure.py` and `uv run python tools/surface_parity.py` all exit 0.
    - `uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py` exits 0.
    - All six package suites exit 0, run as
      `for p in ambito-financiero-client higyrus-client iol-client market-data-client matriz-client wallets-client; do uv run --package "$p" pytest "packages/$p/tests" -q || exit 1; done`.
    - `git diff --numstat packages/matriz-client/src/matriz_client/ws_client.py packages/matriz-client/src/matriz_client/_decode.py packages/matriz-client/src/matriz_client/client.py packages/matriz-client/src/matriz_client/aio.py`
      produces no output across the whole phase.
  </acceptance_criteria>
  <done>The roster floor matches the measured count with a stated provenance, the three new classes
  pass the parametrized Null Object contract, the WebSocket daemon-thread and decode-mode suites are
  green, and the full cross-package sweep is recorded as the phase's closing evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket frame (daemon thread) → `MarketDataFrame.from_api` → snapshot attribute chain | Untrusted vendor frames decoded off the main thread, under a decode mode snapshotted at connect time |
| Public attribute surface → caller code | The alias names become part of a published package's API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-23 | Denial of Service | Alias chains over an empty or partial snapshot | mitigate | Every alias returns a field whose default is an empty list or an empty entry-value model, so `empty().last.price` answers `None` rather than raising; asserted directly, including through `MarketDataFrame.empty()` |
| T-37-24 | Tampering | A property silently shadowing a declared wire field | mitigate | A disjointness test asserts the six alias names and the fourteen wire field names share no member, and that the wire roster is exactly the fourteen currently declared |
| T-37-25 | Repudiation | Aliases perturbing the divergence record | mitigate | Properties are absent from `get_type_hints` and `dataclasses.fields`; the Phase 35 fixtures prove the invariant generically and this plan asserts it once against the real class without rewriting the proof |
| T-37-26 | Tampering | Per-connection decode-mode propagation drifting | mitigate | `ws_client.py` is not edited; the connect-time snapshot and per-frame bound context are untouched, and the WebSocket decode-mode suite is run as acceptance rather than assumed green |
| T-37-27 | Information disclosure | A transforming or caching alias exposing stale or derived data | mitigate | Each property is a single return of the wire attribute; identity assertions with `is` make any copy, cache or transformation fail the test |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `uv.lock` untouched. 37-RESEARCH's Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
- `uv run mypy packages/matriz-client/src` reports success.
- All four repo gates exit 0 and all six package suites exit 0.
- `ws_client.py`, `_decode.py`, `client.py` and `aio.py` are byte-unchanged across the whole phase.
</verification>

<success_criteria>
- `snapshot.last.price`, `.bids`, `.offers`, `.settlement`, `.close` and `.open_interest` work on a
  `MarketDataSnapshot` from the REST surface and on one from a WebSocket frame — the same object and
  the same alias set.
- `mypy --strict` is clean over the package.
- The matriz suite is green on the REST paths and on the WebSocket daemon-thread paths, including
  per-connection and per-frame decode-mode propagation.
- The roster floor equals the measured class count and carries a stated provenance.
</success_criteria>

<output>
Create `.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-05-SUMMARY.md` when done.
Record the full closing sweep verbatim — it is the evidence Success Criteria 3 and 4 are verified
against, and Phase 39 (`LIVE-NOBJ-01`) reads it to know what was NOT verifiable live under D-MATZ-33.
</output>
