---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 05
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/matriz-client/src/matriz_client/_core.py
  - packages/matriz-client/src/matriz_client/client.py
  - packages/matriz-client/tests/test_core.py
  - main_matriz.py
  - verification/test_matriz_sweep_snapshot.py
autonomous: true
requirements: [REFAC-03, CR-03, CR-05]
requirements_addressed: [REFAC-03, CR-03, CR-05]
tags: [phase-07, matriz, atomic, cr-03, cr-05, refac-03]
user_setup: []

must_haves:
  truths:
    - "`matriz_client._core` define `RequestSpec` con `auth_basic: tuple[str, str] | None` (D-01 matriz row) + `unwrap` envelope helper + `parse_envelope_response` (CR-03 cierre) + auth-flow primitives + endpoint builders/parsers per group (§4 Segments, §5 Instruments, §6 Orders, §7 MarketData, §9 Risk)"
    - "CR-03 cerrado: `_core.parse_envelope_response(resp, endpoint)` ejecuta `resp.read()` PRIMERO, luego `raise_for_response(resp)`, luego `resp.json()` y shape check"
    - "CR-05 cerrado: `_envelope_probe(name, path, *, envelope_key=None, model_from_api=None, ...)` helper en `main_matriz.py` dedupea probes; 2 risk probes preservan `envelope_key=None` (D-07); 3 probes con side-effect quedan custom (Pitfall 5 / A4 honesty flag)"
    - "`client.py` matriz colapsa a transport shell: aliases D-04 + `_request(spec) -> httpx.Response`"
    - "matriz `aio.py` PERMANECE STUB Phase 6 — Phase 7 NO TOCA (Phase 10 destapa REST async)"
    - "`verification/test_matriz_sweep_snapshot.py` ejecuta las 18 probes contra payloads canned y verifica que `ProbeResult` shape pre vs post refactor sea IDÉNTICO (D-08)"
    - "`packages/matriz-client/tests/test_core.py` incluye `test_parse_envelope_consumes_body_before_raise` (Pitfall 3 mitigation — D-06)"
    - "LOC drop ≥30% — measured vs Phase 6 baseline 754 LOC client.py (aio.py stub 103 NO entra al conteo)"
    - "Public surface snapshot zero diff (D-16)"
    - "Cross-leak guard matriz sync verde; matriz async skipea con reason"
    - "ATOMIC commit per D-08"
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_core.py"
      provides: "RequestSpec (auth_basic) + parse_envelope_response (CR-03 fix) + auth-flow + endpoint builders/parsers + unwrap"
      contains: "parse_envelope_response"
      min_lines: 200
    - path: "packages/matriz-client/src/matriz_client/client.py"
      provides: "Sync transport shell — Client class preserved; endpoint bodies collapsed; aliases"
      contains: "_raise_for_response = _core.raise_for_response"
    - path: "packages/matriz-client/tests/test_core.py"
      provides: "Unit tests for matriz _core: CR-03 body-consume-before-raise + envelope shape + risk probes + auth-flow"
      min_lines: 80
    - path: "main_matriz.py"
      provides: "_envelope_probe helper + 15 probes migradas + 2 risk probes (envelope_key=None) + 3 custom probes (side-effects)"
      contains: "_envelope_probe"
    - path: "verification/test_matriz_sweep_snapshot.py"
      provides: "Snapshot guard parametrized over 18 probes — payload canned via pytest-httpx"
      contains: "_envelope_probe"
      min_lines: 100
  key_links:
    - from: "matriz_client._core.parse_envelope_response"
      to: "resp.read() before raise_for_response (CR-03 fix)"
      via: "explicit body consume (D-06)"
      pattern: "resp\\.read"
    - from: "main_matriz.py _envelope_probe"
      to: "matriz_client.client _matriz_request or shell"
      via: "_envelope_probe(envelope_key=...) helper"
      pattern: "_envelope_probe"
    - from: "verification/test_matriz_sweep_snapshot.py"
      to: "main_matriz.py 18 probes"
      via: "pytest-httpx canned + ProbeResult assertion"
      pattern: "ProbeResult"
    - from: "matriz_client.client._raise_for_response"
      to: "matriz_client._core.raise_for_response"
      via: "alias D-04"
      pattern: "_raise_for_response = _core.raise_for_response"
---

<objective>
ATOMIC matriz refactor — extraer `matriz_client/_core.py` con RequestSpec (auth_basic) + auth-flow + per-endpoint builders/parsers + `parse_envelope_response` que cierra CR-03 (body-consume-then-raise, HTTP/2-safe), colapsar `client.py` a transport shell, refactorizar 18 sweep probes en `main_matriz.py` al helper `_envelope_probe(envelope_key=...)` que cierra CR-05 preservando 2 risk probes (envelope_key=None) y 3 probes con side-effects (custom), y crear `verification/test_matriz_sweep_snapshot.py` como guard pre/post-refactor (D-08). matriz `aio.py` PERMANECE STUB hasta Phase 10.

Purpose: Phase 7 REFAC-03 + CR-03 + CR-05 todos en un único commit atómico (CONTEXT D-08). matriz tiene el blast radius máximo del milestone. Splitting CR-03/CR-05 across plans introduciría un half-state intermedio — no aceptable per D-08.

Output: 1 nuevo `_core.py`; 1 archivo colapsado (client.py); 1 nuevo `tests/test_core.py`; 1 driver actualizado; 1 nuevo snapshot guard. Single commit atómico (D-08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md
@CLAUDE.md
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/aio.py
@packages/matriz-client/src/matriz_client/_state.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/exceptions.py
@packages/matriz-client/src/matriz_client/__init__.py
@packages/matriz-client/tests/conftest.py
@packages/matriz-client/tests/test_client.py
@packages/matriz-client/tests/test_fixture_reaches_production.py
@main_matriz.py
@verification/snapshots/matriz_client-surface.txt
@.planning/milestones/v1.0-phases/05-matriz-verification/05-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build matriz `_core.py` with RequestSpec, auth-flow, per-endpoint builders/parsers, and parse_envelope_response (CR-03 closure)</name>
  <files>packages/matriz-client/src/matriz_client/_core.py, packages/matriz-client/tests/test_core.py</files>
  <read_first>
    - packages/matriz-client/src/matriz_client/client.py (líneas 1-120 module shape; 91-114 `_raise_for_response` + `_unwrap` MOVE verbatim; 122-200 Client class skeleton; 249-294 `_request` actual con CR-03 issue líneas 278-294; 302-340 endpoint methods §4/§5; más §6/§7/§9 endpoints)
    - packages/matriz-client/src/matriz_client/aio.py (STUB Phase 6 — confirmar 103 LOC, NO TOCAR)
    - packages/matriz-client/src/matriz_client/_state.py (Phase 6 — `_ClientState` con `username`, `password`, `token`, `token_expires_at`; constant `_TOKEN_TTL`)
    - packages/matriz-client/src/matriz_client/models.py (SafeModel: Segment, Instrument, InstrumentDetail, MarketDataSnapshot, Order, NewOrderResponse, Trade, Position, DetailedPosition, AccountReport)
    - packages/matriz-client/src/matriz_client/types.py (Literal types: CFICode, MarketDataEntry, MarketId, OrderType, SegmentId, Side, TimeInForce)
    - packages/matriz-client/src/matriz_client/exceptions.py (`PrimaryAPIError`, `AuthenticationError`)
    - packages/matriz-client/tests/conftest.py (autouse `configure(token=..., base_url=...)`)
    - packages/matriz-client/tests/test_client.py (test idiom pytest-httpx + canned payloads)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 2 — parse_envelope_response CR-03 fix verbatim; § Code Examples Example 4 — matriz `_core.py` skeleton verbatim; § Common Pitfalls Pitfall 3 — false CR-03 fix; § Code Examples Pattern 7 — `_envelope_probe`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§3 parse_envelope_response CR-03 closure; §11 `_envelope_probe` migration map; §10 tests/test_core.py matriz shape)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-06 CR-03; D-07 envelope_key=None for risk probes; D-08 atomic)
    - .planning/milestones/v1.0-phases/05-matriz-verification/05-REVIEW.md (CR-03 + CR-05 origin con line numbers)
    - CLAUDE.md (`from __future__ import annotations`; double quotes; line-length 100)
  </read_first>
  <behavior>
    - `_core.py` define `@dataclass(frozen=True, slots=True) RequestSpec` matriz-shape: `method`, `path`, `params: dict[str, Any] | None = None`, `headers: dict[str, str] | None = None`, `auth_basic: tuple[str, str] | None = None`.
    - `raise_for_response(resp) -> None`: MOVE verbatim de `client.py:91-114` (mapeo de status → `PrimaryAPIError`/`AuthenticationError`).
    - `unwrap(data: dict[str, Any], key: str, endpoint: str) -> Any`: MOVE verbatim (envelope-key extractor; raise PrimaryAPIError si missing).
    - `parse_envelope_response(resp: httpx.Response, endpoint: str) -> dict[str, Any]` — CR-03 FIX (D-06): orden CRÍTICO:
      1. `resp.read()` PRIMERO (CRITICAL — antes de cualquier raise).
      2. `raise_for_response(resp)`.
      3. `raw = resp.json()`.
      4. Shape check: `if not isinstance(raw, dict): raise PrimaryAPIError(...)`.
      5. Status check: `if raw.get("status") == "ERROR": raise PrimaryAPIError(...)`.
      6. Return `raw`.
    - `token_is_fresh(state) -> bool`: pure helper.
    - `build_login_request(state) -> RequestSpec`: matriz auth via header — `RequestSpec(method="POST", path="/auth/getToken", headers={"X-Username": state.username, "X-Password": state.password})`. Raise `AuthenticationError("ERROR", "PRIMARY_USER and PRIMARY_PASSWORD must be set")` si vacíos.
    - `parse_login_response(resp) -> tuple[str, float]`: token viene en `resp.headers["X-Auth-Token"]` (NO en body — D-22 Phase 6). `resp.read()` + `raise_for_response(resp)` + extract token from header + compute `expires_at = time.time() + _TOKEN_TTL`. Raise si missing.
    - Per endpoint público: `build_<endpoint>_request` + `parse_<endpoint>_response`. Envelope endpoints usan `parse_envelope_response` + `unwrap` + `Model.from_api`. Risk endpoints §9 (`get_detailed_positions`, `get_account_report`): NO envelope — body raíz ES el resultado (D-07); builder pasa `auth_basic=(state.username, state.password)`.
    - `_core.py` NO importa `matriz_client.client` ni `matriz_client.aio`.
    - `tests/test_core.py` ≥8 tests:
      1. `test_parse_envelope_consumes_body_before_raise` — CRITICAL CR-03 guard (Pitfall 3).
      2. `test_parse_envelope_response_returns_dict_on_success`.
      3. `test_parse_envelope_response_raises_on_non_dict_root`.
      4. `test_parse_envelope_response_raises_on_status_error`.
      5. `test_unwrap_raises_on_missing_key`.
      6. `test_build_login_request_includes_username_password_headers`.
      7. `test_parse_login_response_extracts_x_auth_token_header`.
      8. `test_build_get_detailed_positions_request_uses_auth_basic` (Risk API D-07).
  </behavior>
  <action>
    1. Reemplazar el placeholder Plan 1 de `matriz_client/_core.py` por el módulo completo siguiendo RESEARCH Code Example 4 (verbatim shape).

    2. Header docstring (per CONVENTIONS.md): "Pure builders and parsers for the matriz REST client. Phase 7 REFAC-03 + D-06 (CR-03 body-consume-then-raise) + D-01 + D-02 + D-04. NO imports from matriz_client.client or matriz_client.aio (enforced by import-linter)." + `Example` con `::` code block.

    3. `from __future__ import annotations`. Imports: time, dataclasses, typing.Any, httpx; internos: `from matriz_client._state import _TOKEN_TTL, _ClientState`, `from matriz_client.exceptions import AuthenticationError, PrimaryAPIError`, models y types imports según necesidad.

    4. Section dividers per PATTERNS §1:
       - `# --- RequestSpec ---`
       - `# --- Stateless helpers (D-04) ---`
       - `# --- Auth flow primitives (D-02) ---`
       - `# --- §4 Segments ---`, `# --- §5 Instruments ---`, `# --- §6 Orders ---`, `# --- §7 MarketData ---`, `# --- §9 Risk (NO envelope — D-07) ---`

    5. `__all__` con todos los símbolos públicos.

    6. Implementar `RequestSpec` con `auth_basic` opcional.

    7. Implementar `raise_for_response` y `unwrap` MOVE verbatim — preservar mensajes Phase 6.

    8. Implementar `parse_envelope_response` con D-06 orden estricto (RESEARCH Pattern 2 / PATTERNS §3). Comentar inline el orden CRÍTICO.

    9. Implementar `token_is_fresh`, `build_login_request`, `parse_login_response`.

    10. Per endpoint: builder + parser. Risk endpoints sin envelope — payload raíz es el resultado.

    11. Crear `packages/matriz-client/tests/test_core.py` con ≥8 tests. `test_parse_envelope_consumes_body_before_raise`: mock `httpx_mock.add_response(status_code=200, json={"status": "ERROR", "description": "boom"})`; fire `httpx.Client().get(...)`; pass response a `_core.parse_envelope_response(resp, "/x")`; assert `PrimaryAPIError` raised; assert `resp.is_closed` True OR `resp.content` no None (body buffered antes del raise).
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_core.py -x -v &amp;&amp; uv run mypy packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py &amp;&amp; uv run ruff check packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py &amp;&amp; uv run ruff format --check packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'from __future__ import annotations' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'class RequestSpec' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'auth_basic' packages/matriz-client/src/matriz_client/_core.py` &gt;= 1
    - Source: `grep -c 'def parse_envelope_response' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'def raise_for_response' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'def unwrap' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'def build_login_request' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'def parse_login_response' packages/matriz-client/src/matriz_client/_core.py` == 1
    - Source: `grep -c 'def build_' packages/matriz-client/src/matriz_client/_core.py` &gt;= 8
    - Source: `grep -c 'def parse_' packages/matriz-client/src/matriz_client/_core.py` &gt;= 7
    - Source: CR-03 order — `awk '/def parse_envelope_response/,/^def /' packages/matriz-client/src/matriz_client/_core.py | grep -nE 'resp\.read|raise_for_response' | head -2` shows `resp.read` at lower line than `raise_for_response`
    - Source: `grep -E "from matriz_client\.(client|aio)" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns NOTHING
    - Source: `wc -l packages/matriz-client/src/matriz_client/_core.py | awk '{print $1}'` &gt;= 200
    - Test command: `uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -x -v` PASSES specifically
    - Test command: `uv run pytest packages/matriz-client/tests/test_core.py -x` exits 0 with ≥8 tests
    - CLI: `uv run mypy packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py` exits 0
    - CLI: `uv run ruff check packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py` exits 0
    - CLI: `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>
    `matriz_client/_core.py` completo con CR-03 fix; ≥8 unit tests verde; lint-imports verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse `matriz_client/client.py` to transport shell + back-compat wrapper for main_matriz.py (D-04 aliases; D-03 _request returns Response; LOC drop ≥30%)</name>
  <files>packages/matriz-client/src/matriz_client/client.py</files>
  <read_first>
    - packages/matriz-client/src/matriz_client/client.py (current Client class + login + _ensure_token + _request + endpoints; PEP 562 shim al final NO TOCAR — Phase 6 D-01..D-04 invariante)
    - packages/matriz-client/src/matriz_client/_core.py (Task 1 producto)
    - packages/matriz-client/src/matriz_client/aio.py (STUB Phase 6 — NO TOCAR)
    - packages/matriz-client/tests/test_fixture_reaches_production.py (sentinel guard Phase 6)
    - packages/matriz-client/tests/test_client.py (pytest-httpx tests existentes)
    - main_matriz.py (líneas 1-300 — header + `_matriz_request(method, path, params=..., auth_basic=...)` que usa `client._request(...)`; los 18 probes)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§4 D-04 alias matriz incluye `_unwrap`; §5 transport shell matriz; §6 endpoint 3-liner matriz `get_segments`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 3 transport shell matriz sync; § Common Pitfalls Pitfall 7 — back-compat wrapper `_matriz_request`; § Code Example 5 — matriz `client.py` post-refactor verbatim)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-03 + D-04 + Phase 7 NO TOCA matriz aio.py)
    - verification/snapshots/matriz_client-surface.txt (D-16)
    - CLAUDE.md (no wildcard; PEP 562 shim NO TOCAR; mandatory `from __future__ import annotations`)
  </read_first>
  <behavior>
    - `client.py` post-refactor:
      - Imports: agregar `from matriz_client import _core` + `from matriz_client._core import RequestSpec`.
      - REEMPLAZAR `_raise_for_response(...)` y `_unwrap(...)` por aliases module-level D-04: `_raise_for_response = _core.raise_for_response` y `_unwrap = _core.unwrap`. Comentar D-04 + B8 preservation note forward-compat con Phase 10.
      - Class `Client`:
        - `login()`: 3-liner orquestación con `_core.build_login_request` + `_core.parse_login_response`.
        - `_ensure_token()`: usar `_core.token_is_fresh`.
        - `_request(spec: RequestSpec) -> httpx.Response` (D-03):
          - Si `spec.auth_basic is not None`: NO `_ensure_token`; usar `auth=httpx.BasicAuth(*spec.auth_basic)`.
          - Else: `self._ensure_token()`; `assert self._state.token is not None`; headers con `X-Auth-Token`.
          - Retornar `httpx.Response` cruda.
        - Endpoint methods: 3-liner.
      - Back-compat wrapper para main_matriz.py (Pitfall 7): agregar method `_matriz_legacy_request(self, method, path, *, params=None, auth_basic=None) -> dict[str, Any]` que internally: `spec = RequestSpec(method=method, path=path, params=params, auth_basic=auth_basic)`; `resp = self._request(spec)`; `return _core.parse_envelope_response(resp, path)`. Anotar como `# DEPRECATED: back-compat for main_matriz.py probes (Phase 7 Pitfall 7). Migrar driver en v1.2.`
      - Top-level `_matriz_request(...) -> dict[str, Any]` si existe a module level: preservar shape + delegate al default client's `_matriz_legacy_request`.
      - PEP 562 shim: NO TOCAR.
      - `__all__`: IDÉNTICO Phase 6.
    - matriz `aio.py` NO TOCAR — sigue siendo el stub Phase 6.
    - LOC drop: ≥30% sobre 754 LOC client.py. Target ≤528.
    - Public surface snapshot zero diff.
    - Cross-leak guard: sync matriz sentinel verde (SYNC-sentinel-matriz_client llega a X-Auth-Token header); async matriz sigue skipeado.
  </behavior>
  <action>
    1. Identificar endpoints en `client.py`: `grep -nE "^    (def |async def )(get_|new_order|cancel_order|login|_request|_ensure_token|configure)" packages/matriz-client/src/matriz_client/client.py`.

    2. Identificar shape de `_matriz_request`: `grep -nE "^(def |    def )_matriz_request" main_matriz.py packages/matriz-client/src/matriz_client/client.py`.

    3. Refactor `client.py`:
       - Top: agregar imports `_core` + `RequestSpec`.
       - REEMPLAZAR `_raise_for_response` y `_unwrap` por aliases module-level D-04 con comentario forward-compat Phase 10.
       - `Client.login()`: 3-liner según `<behavior>`.
       - `Client._ensure_token()`: usar `_core.token_is_fresh`.
       - `Client._request(spec)`: body según `<behavior>`.
       - `Client._matriz_legacy_request(method, path, *, params=None, auth_basic=None) -> dict[str, Any]`: back-compat wrapper.
       - Endpoint methods: 3-liner.
       - Module-level `_matriz_request(...)`: si existe, delegate al default client's `_matriz_legacy_request`.
       - PEP 562 shim: NO TOCAR.

    4. NO TOCAR `aio.py`.

    5. Validar LOC drop: `wc -l packages/matriz-client/src/matriz_client/client.py`. Confirmar ≤528.

    6. Validar D-16 snapshot: `uv run pytest verification/test_public_surface.py -k matriz -x`.

    7. Validar tests existentes: `uv run pytest packages/matriz-client/ -x`.

    8. Validar cross-leak guard: `uv run pytest verification/test_sync_async_isolation.py -k matriz -x -v`.

    9. Validar lint-imports verde.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/ -x &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -k matriz -x -v &amp;&amp; uv run pytest verification/test_public_surface.py -k matriz -x &amp;&amp; uv run mypy packages/matriz-client/ &amp;&amp; uv run ruff check packages/matriz-client/ &amp;&amp; uv run ruff format --check packages/matriz-client/ &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '_raise_for_response = _core.raise_for_response' packages/matriz-client/src/matriz_client/client.py` == 1
    - Source: `grep -c '_unwrap = _core.unwrap' packages/matriz-client/src/matriz_client/client.py` == 1
    - Source: `grep -cE 'def _request\(self,\s*spec' packages/matriz-client/src/matriz_client/client.py` == 1
    - Source: `grep -c '_matriz_legacy_request\|_matriz_request' packages/matriz-client/src/matriz_client/client.py` &gt;= 1 (back-compat per Pitfall 7)
    - Source: `grep -c '_core.build_login_request' packages/matriz-client/src/matriz_client/client.py` == 1
    - Source: `grep -c '_core.build_' packages/matriz-client/src/matriz_client/client.py` &gt;= 4
    - Source: `wc -l packages/matriz-client/src/matriz_client/client.py | awk '{print $1}'` ≤ 528 (≥30% drop vs 754 baseline)
    - Source: matriz aio.py UNCHANGED: `wc -l packages/matriz-client/src/matriz_client/aio.py` == 103 (Phase 6 stub preserved)
    - Test command: `uv run pytest packages/matriz-client/ -x` exits 0
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -k matriz -x -v` shows sync matriz PASS + async matriz SKIPPED with reason
    - Test command: `uv run pytest verification/test_public_surface.py -k matriz -x` exits 0
    - CLI: `uv run lint-imports` exits 0
    - CLI: `uv run mypy packages/matriz-client/` exits 0
    - CLI: `uv run ruff check packages/matriz-client/` exits 0
    - CLI: `uv run ruff format --check packages/matriz-client/` exits 0
  </acceptance_criteria>
  <done>
    `client.py` matriz colapsado con CR-03 wiring; back-compat wrapper preserva `_matriz_request` contract; aio.py stub intacto; LOC drop ≥30%; snapshot zero diff; cross-leak guard verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor 18 sweep probes in `main_matriz.py` to `_envelope_probe` helper + create `verification/test_matriz_sweep_snapshot.py` (CR-05 closure + D-08 atomic guard)</name>
  <files>main_matriz.py, verification/test_matriz_sweep_snapshot.py</files>
  <read_first>
    - main_matriz.py (líneas 1-300 — header + globals + `_matriz_request` + `_auth_failed` flag; líneas 300-1170 — 16 envelope probes; líneas 1222-1402 — 2 risk probes en líneas 1277 (`probe_get_detailed_positions`) y 1342 (`probe_get_account_report`); identificar las 3 probes con side-effect: `probe_get_segments` setea `_resolved_segment`, `probe_get_all_instruments` setea `_resolved_symbol`, `probe_get_market_data` tiene market-hours guard)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 7 — `_envelope_probe` signature target + migration map de 18 probes + A4 honesty flag; § Common Pitfalls Pitfall 5 — side-effect preservation)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§11 — `_envelope_probe` helper detalle + migration map; §9 — `verification/test_matriz_sweep_snapshot.py` shape)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-07 risk probes envelope_key=None; D-08 atomic refactor)
    - .planning/milestones/v1.0-phases/05-matriz-verification/05-REVIEW.md (CR-05 origin text)
    - packages/matriz-client/tests/test_fixture_reaches_production.py (pytest-httpx canned payload idiom)
    - verification/test_public_surface.py (parametrize sweep idiom)
    - packages/matriz-client/src/matriz_client/_core.py (Task 1 producto)
    - packages/matriz-client/src/matriz_client/client.py (Task 2 producto — `_matriz_legacy_request` back-compat wrapper en el shell)
    - CLAUDE.md (`from __future__ import annotations`; double quotes; line-length 100)
  </read_first>
  <behavior>
    - `main_matriz.py` post-refactor:
      - Definir el helper `_envelope_probe` siguiendo Pattern 7 RESEARCH.md verbatim:
        - `_envelope_probe(name: str, path: str, *, envelope_key: str | None = None, model_from_api: Callable[[Any], Any] | None = None, request_params: dict[str, Any] | None = None, auth_basic_fn: Callable[[], tuple[str, str]] | None = None) -> tuple[ProbeResult, Any | None]`
        - Body: if `_auth_failed` return SKIPPED; usar `_matriz_request` (back-compat wrapper de Task 2) con `auth_basic=` kwargs si aplica; manejar PrimaryAPIError → FINDING; si `envelope_key is None` validar shape dict raíz; else unwrap + validate list.
      - Migrar 15 probes "limpias" (sin side-effect) a calls al helper:
        - `probe_get_instruments_details` → `_envelope_probe("get_instruments_details", "/rest/instruments/details", envelope_key="instruments")`
        - `probe_get_instrument_detail` → `_envelope_probe("get_instrument_detail", f"/rest/instruments/details?marketId=ROFX&symbol={_resolved_symbol}", envelope_key="instrument")` (SKIP si `_resolved_symbol is None`).
        - `probe_get_instruments_by_cfi_ESXXXX`, `probe_get_instruments_by_segment`, `probe_get_trades`, `probe_get_active_orders`, `probe_get_filled_orders`, `probe_get_all_orders`, `probe_get_order_status`, `probe_get_order_history`, `probe_get_order_by_exec_id`, `probe_get_positions` (Risk envelope), etc. — cada uno con su `envelope_key`.
      - PRESERVAR custom (no migrar al helper) las 3 probes con side-effect / lógica especial (Pitfall 5 / A4):
        - `probe_get_segments` (setea `_resolved_segment`)
        - `probe_get_all_instruments` (setea `_resolved_symbol`)
        - `probe_get_market_data` (market-hours guard)
        - `probe_get_instruments_by_cfi_sanity` (loop sobre 8 CFI codes — no encaja en plano)
      - PRESERVAR 2 risk probes con `envelope_key=None` (D-07):
        - `probe_get_detailed_positions` → `_envelope_probe("get_detailed_positions", f"/rest/risk/detailedPosition/{_PRIMARY_ACCOUNT}", envelope_key=None, auth_basic_fn=_risk_auth)`
        - `probe_get_account_report` → `_envelope_probe("get_account_report", f"/rest/risk/accountReport/{_PRIMARY_ACCOUNT}", envelope_key=None, auth_basic_fn=_risk_auth)`
      - El driver compila + corre `python main_matriz.py --help` sin error (smoke test del refactor).
    - `verification/test_matriz_sweep_snapshot.py` NUEVO (PATTERNS §9 shape):
      - Module docstring "Snapshot guard: 18 main_matriz.py probes (CR-05) preserve ProbeResult shape pre/post refactor. Phase 7 D-08 — registrado pre-refactor con payloads canned vía pytest-httpx. Las 2 risk probes (probe_get_detailed_positions, probe_get_account_report) preservan envelope_key=None (D-07)."
      - `from __future__ import annotations`.
      - Imports: `pytest`, `from pytest_httpx import HTTPXMock`, `import main_matriz`, `import matriz_client`.
      - `_PROBE_FIXTURES`: lista de tuplas `(probe_callable, mock_url, mock_response_json, expected_status, expected_detail_substring)` cubriendo las 18 probes. Inline canned payloads pequeños (per RESEARCH.md Open Q #2 preference; Phase 6 06-02 idiom).
      - `@pytest.mark.parametrize("probe, url, response, expected_status, detail_sub", _PROBE_FIXTURES)` función `test_matriz_probe_envelope_shape_preserved`:
        - `matriz_client.configure(token="sentinel", token_expires_at=9_999_999_999.0, base_url="https://api.test", username="u", password="p")`.
        - `httpx_mock.add_response(url=url, json=response)`.
        - `result, _payload = probe()`.
        - `assert result.status == expected_status`.
        - `assert detail_sub in result.detail`.
      - Per A7 — Snapshot tests pueden tener dependencias entre sí (e.g. probe 5 requiere `_resolved_symbol` de probe 3). Planner orderingsmit dependencies en `_PROBE_FIXTURES` o usa fixtures pytest-style con setup ordering — recomendación: usar fixture autouse que pre-setea `main_matriz._resolved_segment = "DDF"` y `main_matriz._resolved_symbol = "GGAL"` (valores canned) para que los probes 4, 5 etc. tengan input válido.
  </behavior>
  <action>
    1. Inspeccionar `main_matriz.py` para identificar todas las 18 sweep probes + sus líneas exactas + sus envelope keys + sus dependencias de globals (`_resolved_segment`, `_resolved_symbol`, `_PRIMARY_ACCOUNT`, `_auth_failed`):
       - `grep -nE "^def probe_get_" main_matriz.py`
       - `grep -nE "global _resolved_(segment|symbol)" main_matriz.py`
       - `grep -nE "envelope_key|raw\.get\(" main_matriz.py | head -40` (identificar envelope_key strings)

    2. Implementar `_envelope_probe` helper en `main_matriz.py` siguiendo RESEARCH §Pattern 7 shape. Ubicación: después del bloque actual de probes-helpers (e.g. `_first_dict`) y antes de las probes individuales — el executor encuentra el lugar canónico via `grep -nE "^def (_first_dict|_resolve_)" main_matriz.py`.

    3. Migrar 15 probes "limpias" a calls al helper. Cada migración:
       - REEMPLAZAR el body actual de `def probe_get_<X>() -> tuple[ProbeResult, ...]:` por: `return _envelope_probe("get_<X>", "<path>", envelope_key="<key>", ...)`.
       - Conservar la firma del probe (`def probe_get_<X>() -> ...`) — los drivers consumen la API actual.
       - Si el probe requiere `_PRIMARY_ACCOUNT` (orders, account_report), usar el global existente (no duplicar).

    4. Migrar 2 risk probes con `envelope_key=None`:
       - `probe_get_detailed_positions` → `return _envelope_probe("get_detailed_positions", f"/rest/risk/detailedPosition/{_PRIMARY_ACCOUNT}", envelope_key=None, auth_basic_fn=_risk_auth)`
       - `probe_get_account_report` → análogo.
       - Identificar la fuente de `_risk_auth` (function que retorna `(user, pass)` para Risk API) — search `grep -nE "_risk_auth|RISK_USER" main_matriz.py packages/matriz-client/src/matriz_client/`.

    5. PRESERVAR custom (NO migrar al helper) las 3 probes con side-effect:
       - `probe_get_segments` — necesita setear `global _resolved_segment` post-call.
       - `probe_get_all_instruments` — necesita setear `global _resolved_symbol` post-call.
       - `probe_get_market_data` — market-hours guard logic.
       - `probe_get_instruments_by_cfi_sanity` — loop sobre 8 CFI codes.
       - Estas 4 quedan en `main_matriz.py` con su body actual; el commit incluye comentario `# Custom probe — kept as-is per RESEARCH A4 honesty flag (side-effect / loop logic not fit for _envelope_probe).`

    6. Crear `verification/test_matriz_sweep_snapshot.py` siguiendo PATTERNS §9 + RESEARCH Open Q #2 inline canned payloads:
       - Header docstring + `from __future__ import annotations`.
       - Imports per `<behavior>`.
       - Fixture autouse pre-setea `_resolved_segment`, `_resolved_symbol`, `_PRIMARY_ACCOUNT` con valores canned (e.g. `"DDF"`, `"GGAL"`, `"REM2024"`).
       - `_PROBE_FIXTURES` — lista de tuplas con las 18 probes + sus mocks. Para las 3 custom + 1 cfi-sanity, el snapshot solo verifica ProbeResult.status == "PASS" sin assert detallado del helper.
       - Test parametrizado.

    7. Validar el refactor smoke-test:
       - `uv run python main_matriz.py --help` (si hay flag --help) exit 0.
       - `uv run python -c "import main_matriz; print(callable(main_matriz._envelope_probe))"` retorna `True`.

    8. Correr suite completo:
       - `uv run pytest verification/test_matriz_sweep_snapshot.py -x -v` — los 18 cases pasan.
       - `uv run pytest -q 2>&1 | tail -5` — sin regresiones.

    9. ATOMIC commit per D-08: este Task junto con Tasks 1+2 forman un único commit. Si algo rompe, revert atómico.
  </action>
  <verify>
    <automated>uv run pytest verification/test_matriz_sweep_snapshot.py -x -v &amp;&amp; uv run python -c "import main_matriz; assert callable(main_matriz._envelope_probe), 'helper missing'" &amp;&amp; uv run pytest packages/matriz-client/ -x &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -5 &amp;&amp; uv run mypy main_matriz.py verification/test_matriz_sweep_snapshot.py &amp;&amp; uv run ruff check main_matriz.py verification/test_matriz_sweep_snapshot.py &amp;&amp; uv run ruff format --check main_matriz.py verification/test_matriz_sweep_snapshot.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '^def _envelope_probe' main_matriz.py` == 1
    - Source: `grep -c 'envelope_key=None' main_matriz.py` &gt;= 2 (the 2 risk probes preserved per D-07)
    - Source: `grep -c '_envelope_probe(' main_matriz.py` &gt;= 15 (15 clean migrations + 2 risk probes call the helper = ≥17 minimum)
    - Source: `grep -c 'global _resolved_segment\|global _resolved_symbol' main_matriz.py` &gt;= 2 (3 custom side-effect probes preserved)
    - Source: `grep -c 'def probe_get_segments\|def probe_get_all_instruments\|def probe_get_market_data' main_matriz.py` == 3 (custom probes preserved as their own def)
    - Source: `grep -c 'from __future__ import annotations' verification/test_matriz_sweep_snapshot.py` == 1
    - Source: `grep -c '@pytest.mark.parametrize' verification/test_matriz_sweep_snapshot.py` &gt;= 1
    - Source: `wc -l verification/test_matriz_sweep_snapshot.py | awk '{print $1}'` &gt;= 100
    - Behavior: `uv run python -c "import main_matriz; assert callable(main_matriz._envelope_probe), 'helper missing'"` exits 0
    - Test command: `uv run pytest verification/test_matriz_sweep_snapshot.py -x` exits 0 with ≥18 parametrized cases passing
    - Test command: `uv run pytest packages/matriz-client/ -x` exits 0
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -5` shows no failures; total ≥ previous baseline + ≥18 new
    - CLI: `uv run mypy main_matriz.py verification/test_matriz_sweep_snapshot.py` exits 0 (strict)
    - CLI: `uv run ruff check main_matriz.py verification/test_matriz_sweep_snapshot.py` exits 0
    - CLI: `uv run ruff format --check main_matriz.py verification/test_matriz_sweep_snapshot.py` exits 0
    - CLI: `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>
    `_envelope_probe` helper + 15 clean migrations + 2 risk probes (envelope_key=None) + 3 custom probes preserved (Pitfall 5); snapshot test guard `verification/test_matriz_sweep_snapshot.py` parametrizado sobre 18 cases verde; lint-imports verde; suite completa verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_core.py` → `client.py`/`aio.py` | T-7-01 import-linter enforça |
| sync surface → async surface | T-7-02 cross-leak guard (matriz async stub — skipped) |
| HTTP/2 connection pool stream lifecycle | T-7-05 CR-03 — body MUST be consumed BEFORE raise |
| Risk probe envelope handling | T-7-06 CR-05 — risk probes must use envelope_key=None (NOT empty string) |
| Driver side-effect globals (`_resolved_segment`, `_resolved_symbol`) | T-7-07 Pitfall 5 — _envelope_probe must NOT swallow setter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | `_core.py` accidental transport import | mitigate | Task 1 source `grep` empty + Task 2 `uv run lint-imports` exit 0 |
| T-7-02 | Tampering | Sync/async token cross-contamination | mitigate | Task 2 verify includes `verification/test_sync_async_isolation.py -k matriz`; sync sentinel reaches X-Auth-Token header; async test SKIPS with reason hasta Phase 10 |
| T-7-03 | Tampering | B8 alias break | mitigate | Task 2 source `grep "_raise_for_response = _core.raise_for_response"` == 1 + `_unwrap = _core.unwrap` == 1 |
| T-7-05 | DoS (HTTP/2 stream leak) | `_core.parse_envelope_response` may raise before body consumed | mitigate | Task 1 `test_parse_envelope_consumes_body_before_raise` (Pitfall 3); Task 1 source CR-03 order verification (`awk` confirms `resp.read` line &lt; `raise_for_response` line within function) |
| T-7-06 | Tampering / Functional bug | Risk probe migrated with `envelope_key=""` instead of `None` (D-07 violation) | mitigate | Task 3 source assertion `grep -c "envelope_key=None"` &gt;= 2; Task 3 snapshot guard tests `verification/test_matriz_sweep_snapshot.py` includes the 2 risk probes specifically |
| T-7-07 | Tampering (driver state) | _envelope_probe swallows the side-effect setter for `_resolved_segment`/`_resolved_symbol` (Pitfall 5) | mitigate | Task 3 PRESERVES the 3 side-effect probes as custom; source assertion `grep -c "global _resolved_segment\|global _resolved_symbol"` &gt;= 2 |
</threat_model>

<verification>
- B8 D-04 aliases verde (`_raise_for_response` + `_unwrap`).
- CR-03 body-consume-before-raise: test `test_parse_envelope_consumes_body_before_raise` PASSES + source order verified.
- CR-05 `_envelope_probe` helper + 15 clean migrations + 2 risk probes (envelope_key=None) + 3 custom preserved.
- `verification/test_matriz_sweep_snapshot.py` 18 parametrized cases verde.
- `uv run lint-imports` exit 0.
- Public surface snapshot zero diff (D-16).
- Cross-leak guard sync matriz verde; async matriz skipea.
- LOC drop ≥30% client.py; aio.py UNCHANGED (stub Phase 6).
- ATOMIC commit per D-08; revert atómico posible si algo rompe.
- Full suite verde post-refactor; new tests add ≥26 (8 _core + 18 snapshot).
</verification>

<success_criteria>
- `matriz_client/_core.py` con RequestSpec (auth_basic), auth-flow, builders/parsers, `parse_envelope_response` con D-06 (CR-03 cierre), unwrap; PURO.
- `client.py` colapsado a transport shell con back-compat wrapper `_matriz_legacy_request` (Pitfall 7); LOC drop ≥30% vs 754 baseline.
- matriz `aio.py` NO TOCADO (stub Phase 6 intacto).
- `main_matriz.py` con `_envelope_probe` + 15 clean migrations + 2 risk probes (envelope_key=None) + 3 custom preserved.
- `verification/test_matriz_sweep_snapshot.py` parametrizado verde sobre 18 probes.
- B8 aliases D-04 verde; tests verde.
- CR-03 fix con test guard verde + source order verification.
- CR-05 closure con snapshot guard verde + risk probes preservation source assertions.
- Public surface snapshot Phase 6 unchanged.
- Cross-leak guard verde (sync) + skipped (async).
- lint-imports verde con 4 contracts.
- ≥8 nuevos tests `_core` + ≥18 nuevos snapshot cases agregados.
- Single atómico commit (D-08).
</success_criteria>

<output>
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-05-SUMMARY.md`:
- LOC drop matriz:
  ```
  LOC drop vs Phase 6 baseline:
  - client.py: 754 → <X> (-<Y>%)
  - aio.py:    103 → 103 (UNCHANGED — stub Phase 6 → Phase 10)
  - _core.py:  0 → <N> (NEW)
  - client.py drop: <X>%   <PASS/FAIL ≥30%>
  ```
- CR-03 confirmed: `test_parse_envelope_consumes_body_before_raise` passes; source CR-03 order verified.
- CR-05 confirmed: `_envelope_probe` helper count migrations = 15 clean + 2 risk + 3 custom = 20 (≥18 expected; 2 extras for full coverage).
- B8 aliases verde.
- Public surface snapshot zero diff.
- Cross-leak guard: sync matriz PASS; async matriz SKIPPED with reason.
- Snapshot guard 18 cases parametrized verde.
- N new `_core` tests + 18 new snapshot tests agregados.
- ATOMIC commit hash recorded.
</output>
