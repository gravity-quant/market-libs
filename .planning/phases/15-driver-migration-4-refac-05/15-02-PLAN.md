---
phase: 15-driver-migration-4-refac-05
plan: 02
type: tdd
wave: 2
depends_on: ["15-01"]
files_modified:
  - main_iol.py
  - verification/test_main_iol_uses_single_client_instance.py
autonomous: true
requirements: [REFAC-05]
user_setup:
  - service: iol-client (live)
    why: "Per-package LIVE smoke at end of migration (D-11, Criterion #4) — exercises OAuth + forced-refresh write-site"
    env_vars:
      - name: IOL_USERNAME
        source: ".env in packages/iol-client/ (never committed)"
      - name: IOL_PASSWORD
        source: ".env in packages/iol-client/ (never committed)"
    dashboard_config:
      - task: "Operator runs `uv run --package iol-client python main_iol.py` and confirms exit 0 + forced-refresh probe shows a real token refresh (not a silenced no-op)"
        location: "Local terminal"

must_haves:
  truths:
    - "main_iol.py main() constructs exactly one sync Client(); _async_main() constructs exactly one AsyncClient() — D-01/D-02"
    - "The forced-refresh WRITE at :1294 and the get_instruments READ at :1296 operate on the SAME threaded client instance — D-03 critical write-site"
    - "Every probe_* receives client/aclient as a parameter; no probe reaches _get_default() — D-01"
    - "AST-guard test fails RED on un-migrated driver, passes GREEN after migration — Criterion #1"
    - "Finding titles/fids/probe-names in iol-client-findings.md are byte-stable — Criterion #2"
  artifacts:
    - path: "verification/test_main_iol_uses_single_client_instance.py"
      provides: "AST-walker asserting <=2 Client/AsyncClient ctors in main_iol.py"
      contains: "test_main_iol_uses_single_client_instance"
      min_lines: 25
    - path: "main_iol.py"
      provides: "Migrated driver — single sync + single async client; forced-refresh write on threaded instance"
      contains: "Client()"
  key_links:
    - from: "main_iol.py probe_refresh_token"
      to: "the threaded client instance"
      via: "client._state.token_expires_at = 0.0 then client.get_instruments('argentina') on the SAME object"
      pattern: "client\\._state\\.token_expires_at = 0\\.0"
    - from: "verification/test_main_iol_uses_single_client_instance.py"
      to: "main_iol.py"
      via: "ast.parse + ast.walk counting ctor Calls (ast.Name AND ast.Attribute)"
      pattern: "ast\\.walk"
---

<objective>
Migrate `main_iol.py` (15 probes, 1675 LOC) so `main()` builds one sync `Client()` and `_async_main()` builds one `AsyncClient()`, threaded into every probe. This driver contains the phase's MOST SENSITIVE line: the forced-refresh write-site at `:1294` (`token_expires_at = 0.0`) must write to the SAME threaded instance that `:1296` (`get_instruments`) reads — otherwise the forced-refresh regression test silently becomes a no-op. Author the AST-guard test FIRST (RED→GREEN).

Purpose: Wave 2 — second in the serial order (D-11), after ámbito establishes the test idiom. Guards OAuth-churn (anti-Pitfall 1).
Output: Migrated `main_iol.py` + its AST-guard test.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-driver-migration-4-refac-05/15-CONTEXT.md
@.planning/phases/15-driver-migration-4-refac-05/15-PATTERNS.md
@.planning/phases/15-driver-migration-4-refac-05/15-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — AST-guard test for main_iol.py (Criterion #1)</name>
  <files>verification/test_main_iol_uses_single_client_instance.py</files>
  <read_first>
    - verification/test_main_ambito_financiero_uses_single_client_instance.py (the Wave-1 sibling just created — copy its idiom verbatim, retarget to main_iol.py)
    - verification/test_main_drivers_bare_except.py (original analog)
    - main_iol.py:74-78 (current import block — `import iol_client` + `from iol_client import ... aio`)
  </read_first>
  <behavior>
    - Test asserts `1 <= count <= 2` Client/AsyncClient ctor Calls in main_iol.py.
    - RED: un-migrated driver constructs ZERO classes → count 0 < 1 → FAILS.
    - GREEN: post-migration count==2 → PASSES.
    - Walker matches both ast.Name and ast.Attribute ctor forms (D-05).
  </behavior>
  <action>
    Create `verification/test_main_iol_uses_single_client_instance.py` as a copy of the Wave-1 ámbito AST test, retargeted to `main_iol.py` with test function `test_main_iol_uses_single_client_instance`. Keep the same dual ast.Name/ast.Attribute ctor matching and the `1 <= len(ctor_sites) <= 2` assertion. Do not embed the literal class-name strings inside any "what NOT to count" comment; reference `with_options` by concept only. Run RED first against the un-migrated main_iol.py (must FAIL count 0).
    <!-- planner-discipline-allow: with_options -->
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_iol_uses_single_client_instance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED before Task 2 (count 0 violates lower bound).
    - GREEN after Task 2 (count==2).
    - Both ctor styles covered (grep file for `ast.Name` and `ast.Attribute`).
  </acceptance_criteria>
  <done>Test exists, fails RED, passes GREEN; matches both ctor styles.</done>
</task>

<task type="execute">
  <name>Task 2: Migrate main_iol.py — thread client/aclient, fix the forced-refresh write-site (D-01/D-02/D-03)</name>
  <files>main_iol.py</files>
  <read_first>
    - main_iol.py (full driver — 1675 LOC; the file being modified)
    - 15-PATTERNS.md lines 192-251 (iol migration recipe: import block, 17 _state read sites, the CRITICAL write-site block :1261-1296, module-attr allowlist :1271/:1287, _async_main batch :1511-1548, main() :1556-1584)
    - packages/iol-client/src/iol_client/__init__.py:55-56 (Client/AsyncClient exports)
    - packages/iol-client/src/iol_client/client.py:725 (allowlist mapping `_refresh_token`→`refresh_token`)
  </read_first>
  <action>
    PIN constructor style: add `from iol_client import Client, AsyncClient` (bare → ast.Name). In `main()` (:1556-1584) construct `client = Client()` once; thread it into every sync probe call and add a `client` param to each sync `def probe_*`. In `_async_main()` (:1511-1548) construct `aclient = AsyncClient()` at the top; thread into all 5 async probes; change `await aio.aclose()` (:1537) → `await aclient.aclose()` inside the existing `finally`/suppress.
    Mechanical 1:1 `_state` swaps (D-03) at the 17 sites — sync sites → `client._state.<attr>`, async sites → `aclient._state.<attr>`. Module-attr reads: `iol_client.client._refresh_token` (:1271) → `client._state.refresh_token`; `iol_client.client._token` (:1287) → `client._state.token` (per allowlist client.py:725).
    CRITICAL write-site (probe_refresh_token, :1261-1296): `iol_client.client._get_default()._state.token_expires_at = 0.0` (:1294) → `client._state.token_expires_at = 0.0`, and `iol_client.get_instruments("argentina")` (:1296) → `client.get_instruments("argentina")` — both MUST reference the SAME threaded `client` passed into probe_refresh_token, so the forced-refresh write is observed by the subsequent read (not a no-op against the wrong object). Add `client` to probe_refresh_token's signature.
    Swap public/raw calls: `iol_client.get_X(...)` → `client.get_X(...)` (login :193, get_quote :267, get_historical_quotes :413, get_instruments :561/:757, get_instruments_by_type :697); `await aio.get_X(...)` → `await aclient.get_X(...)`; `iol_client.client._request(...)` (:972) → `client._request(...)`.
    DO NOT touch `title=`/`fid=`/`class_=`/probe-name literals in `append_finding(...)` (D-06). Sync and async clients are SEPARATE (D-02).
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_iol_uses_single_client_instance.py -x && uv run --frozen ruff check main_iol.py && uv run --frozen mypy main_iol.py</automated>
  </verify>
  <acceptance_criteria>
    - AST-guard test GREEN (count==2).
    - `grep -c 'client\._state\.token_expires_at = 0\.0' main_iol.py` == 1 (write-site migrated to threaded instance).
    - probe_refresh_token signature has a `client` param AND both its write (:1294 migrated) and read (:1296 migrated) use that same `client`.
    - `grep -n '_get_default' main_iol.py` returns no CODE sites (only any remaining docstring prose, if present).
    - ruff check + mypy strict green.
    - No `append_finding` title/fid/class_ literal changed (`git diff main_iol.py`).
  </acceptance_criteria>
  <done>Single sync + single async client threaded into all 15 probes; forced-refresh write and read share one instance; finding literals untouched; CI-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → IOL live OAuth API | driver authenticates with IOL credentials from packages/iol-client/.env |
| driver process → per-instance token state | forced-refresh write mutates `client._state.token_expires_at` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | IOL credentials in logs/findings/commits | mitigate | Migration changes only client acquisition; `base_url=`/`_state` reads never surface IOL_USERNAME/IOL_PASSWORD into findings or logs. `.env` stays uncommitted. No credential literal touched. |
| T-15-02 | Tampering | multiple Client() → OAuth refresh churn | mitigate | ≤2-ctor AST gate (Task 1) caps construction at 1 sync + 1 async, preventing repeated OAuth login/refresh storms (anti-Pitfall 1). |
| T-15-04 | Repudiation | silenced forced-refresh regression | mitigate | The critical write-site fix ensures `:1294` write and `:1296` read share one instance — a real forced-refresh regression cannot pass unseen (no-op silencing eliminated). |
| T-15-03 | Denial of Service | IOL API rate limits / OAuth throttle | accept | Single operator-driven smoke; below `high` severity. |
| T-15-SC | Tampering | uv/pip installs | mitigate | No new packages installed (drivers + tests only); no package legitimacy audit required. |
</threat_model>

<verification>
- AST-guard test GREEN (count==2).
- Forced-refresh write/read share the threaded instance (single grep proves the write-site migrated).
- `_get_default()` eliminated from CODE lines of main_iol.py.
- ruff + mypy strict green.
- Operator confirms LIVE smoke exit 0 with a real token refresh observed.
</verification>

<success_criteria>
- main_iol.py constructs exactly 1 sync + 1 async client threaded into every probe.
- Forced-refresh write-site operates on the same threaded instance the next read uses.
- Finding titles/fids/probe-names byte-stable.
</success_criteria>

<artifacts_this_phase_produces>
**New test file + test function:**
- `verification/test_main_iol_uses_single_client_instance.py` → `test_main_iol_uses_single_client_instance()`

**New driver import line:**
- `main_iol.py`: `from iol_client import Client, AsyncClient` (bare, pinned per D-05)

**New helper signatures:**
- Every sync `def probe_*` gains `client`; every async `def probe_*` gains `aclient`.
- `probe_refresh_token(client)` — the critical-write-site probe now takes the threaded instance.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-driver-migration-4-refac-05/15-02-SUMMARY.md` when done
</output>
