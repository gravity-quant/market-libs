---
phase: 35-fundaci-n-null-object-bool-pol-tica-del-walker
plan: 03
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - packages/iol-client/src/iol_client/models.py
  - packages/iol-client/tests/test_null_object.py
  - packages/iol-client/tests/test_decode.py
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/tests/test_null_object.py
  - packages/market-data-client/tests/test_decode.py
autonomous: true
requirements: [NOBJ-01, NOBJ-02]

must_haves:
  truths:
    - "iol-client's 4 shipped SafeModel subclasses and market-data-client's 16 are each falsy when empty and truthy when perturbed, proven by introspecting the real models module (D-15)."
    - "market-data's empty() carries the mapping pass its from_api carries, so the two constructors cannot disagree on a dict-typed field — this is D-07's second form, and it is a per-package policy delta, not an inconsistency to harmonize away."
    - "empty() emits zero divergence records in both packages (T-29-33)."
    - "@property aliases stay invisible to get_type_hints() in both packages, and an alias-carrying class produces the same divergence record list as its alias-free twin (ROADMAP criterio 5)."
    - "Wrong-typed values on a non-optional list field still emit the six-key record and still raise under strict_decode in both packages — pinned before the walker edit lands."
    - "market-data's MarketDataSnapshot truthiness is documented as structurally, not semantically, meaningful: its from_api stamps received_at post-walk, so a live snapshot is always truthy even when the wire carried nothing (D-09)."
  artifacts:
    - path: "packages/iol-client/src/iol_client/models.py"
      provides: "SafeModel.empty() (form A) + SafeModel.__bool__()"
      contains: "def __bool__"
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "SafeModel.empty() (form B, with mapping pass) + SafeModel.__bool__()"
      contains: "_apply_mapping_policy"
    - path: "packages/iol-client/tests/test_null_object.py"
      provides: "iol enumeration + truthiness + alias-invisibility suite (roster >= 4)"
      min_lines: 90
    - path: "packages/market-data-client/tests/test_null_object.py"
      provides: "market-data enumeration + truthiness + alias-invisibility suite (roster >= 16)"
      min_lines: 90
  key_links:
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/_decode.py"
      via: "empty() walks an empty dict with SILENT_SINK and then applies the package's mapping pass with the same silent sink"
      pattern: "_apply_mapping_policy\\(cls, kwargs, sink=_decode\\.SILENT_SINK\\)"
    - from: "packages/iol-client/src/iol_client/models.py"
      to: "packages/iol-client/src/iol_client/_decode.py"
      via: "empty() synthesizes kwargs through walk_model with SILENT_SINK (form A — no mapping pass in this package)"
      pattern: "walk_model\\(cls, \\{\\}, policy=_decode\\.POLICY, sink=_decode\\.SILENT_SINK\\)"
  prohibitions:
    - statement: "empty() is never implemented as cls.from_api(None) in either package — it emits a terminal non_dict record, violating T-29-33."
      status: flagged-unverified
      verification: "Read both added classmethod bodies; each routes through walk_model with SILENT_SINK."
    - statement: "No shared helper is created for the two empty() forms. The mapping-pass delta is a declared per-package policy axis (29-SEMANTICS-MATRIX 'Never harmonize'); inventing a shared no-op to force byte-identity would be dead code Phase 36 deletes."
      status: flagged-unverified
      verification: "No new module or function is introduced to unify the two forms; each base carries its own body with a docstring declaring the delta."
    - statement: "empty() is never memoized in either package."
      status: flagged-unverified
      verification: "Behavioural, not textual — two successive empty() calls on the same shipped class must return distinct objects in both packages."
    - statement: "No pre-existing test in either package is edited. The only test_decode.py changes are appended functions."
      status: flagged-unverified
      verification: "git diff --numstat on both test_decode.py files shows 0 deletions."
---

<objective>
Fan the proven tracer out to `iol-client` and `market-data-client`: both bases gain `empty()` and `__bool__`, both get their own copy of the enumeration/truthiness/alias-invisibility suite, and both get the wrong-type falsification tests the walker edit must preserve.

Purpose: iol is higyrus's byte-twin base (form A of `empty()`, no mapping pass) and market-data is the mapping-pass case (form B) — together they cover both shapes D-07 admits. Doing them after the 35-01 tracer means the shape of the suite, the perturbation helper's branch order, and the alias-invisibility formulation are already settled and this plan is a mechanical port with two deliberate deltas.

Output: `empty()` + `__bool__` on both bases, `tests/test_null_object.py` ×2, and 2 new wrong-type tests inside each package's `tests/test_decode.py`.

No `_decode.py` byte moves. The canonical digest is untouched; all 4 v1.6 gates must be green at plan end.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-CONTEXT.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-PATTERNS.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol / file | Owner plan |
|---|---|
| `iol_client.models.SafeModel.empty()` (form A) + `.__bool__()` | **35-03 [this plan]** |
| `market_data_client.models.SafeModel.empty()` (form B, mapping pass) + `.__bool__()` | **35-03 [this plan]** |
| `packages/iol-client/tests/test_null_object.py` | **35-03 [this plan]** |
| `packages/market-data-client/tests/test_null_object.py` | **35-03 [this plan]** |
| `test_wrong_typed_list_field_still_reports_type` ×2 (iol, market-data) | **35-03 [this plan]** |
| `test_strict_mode_still_raises_on_a_wrong_typed_list` ×2 (iol, market-data) | **35-03 [this plan]** |
| higyrus base + suite + wrong-type tests | 35-01 (tracer, already landed) |
| D-17 ledger `35-RETIRED-TRIPLES.md` | 35-02 |
| matriz `_SafeModel.__bool__`, `UnknownFrame.__bool__`, matriz/ambito/wallets suites, ambito wrong-type tests | 35-04 |
| `_decode.py` EDIT 1 + EDIT 2 ×5, rewritten `# Phase 35, NOBJ-02:` comment ×5, the 11 inverted assertions, recomputed `CANONICAL_DIGEST` | 35-05 |
</artifacts_this_phase_produces>

<flagged_assumptions>
| Requirement | Probe verdict | Resolved by | Residual risk |
|---|---|---|---|
| NOBJ-01 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 1 + CONTEXT D-05, D-06, D-07, D-09, D-15 → the `must_haves.truths` rows above. | market-data's `MarketDataSnapshot` passes the falsy-when-empty assertion only because the `received_at` parameter defaults to `0.0` in the test path; in production the client always passes `time.time()`. The class-level green is STRUCTURAL. Mitigated by an in-test comment and a `__bool__` docstring sentence (D-09), not by weakening the assertion. |
| NOBJ-02 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 2 + D-01..D-04, D-13, D-14. This plan owns only the falsification half for these two packages. | Same as 35-01: these two tests are green day-0 and their value is as a tripwire for 35-05's edit. |
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the Null Object suite and base methods to iol-client (form A, 4-class roster)</name>

  <files>packages/iol-client/src/iol_client/models.py, packages/iol-client/tests/test_null_object.py, packages/iol-client/tests/test_decode.py</files>

  <read_first>
    - `packages/higyrus-client/tests/test_null_object.py` (the file 35-01 created — this is the source of the port; read it whole)
    - `packages/higyrus-client/src/higyrus_client/models.py:52-100` (the base as 35-01 left it: `from_api`, `empty()`, `__bool__`, `to_dict` in that order)
    - `packages/iol-client/src/iol_client/models.py` (whole file — imports at `:56-62` already include `Self`; the base at `:65-94` with `from_api` at `:73` and `to_dict` at `:80`; the 4 shipped classes `Punta` `:98`, `Cotizacion` `:114`, `Instrumento` `:162`, `Titulo` `:189`)
    - `packages/iol-client/tests/test_decode.py:1-160` (module header, autouse decode-context fixture, `_walk` / `_divergences` / `_tuples` helpers, module-local fixture dataclasses) and `:300-330` (the `test_missing_list_field_returns_empty_list_and_reports` at `:308` that 35-05 will invert — do not touch it) and `:1215-1235` (the model-site wrong-type test at `:1220` that stays green — the structural model for the new list-site test)
  </read_first>

  <behavior>
    - `test_every_shipped_model_is_falsy_when_empty` / `..._truthy_when_populated` / `test_empty_emits_nothing` parametrize over exactly the 4 iol classes.
    - `test_the_model_roster_is_not_vacuous` asserts `>= 4` (RESEARCH F-1 measured iol at 4).
    - `test_property_aliases_are_invisible_to_get_type_hints` and `test_adding_a_property_alias_does_not_change_the_divergence_count` behave as in higyrus.
    - `test_wrong_typed_list_field_still_reports_type` and `test_strict_mode_still_raises_on_a_wrong_typed_list` are appended to `tests/test_decode.py` and are green against the unedited walker.
  </behavior>

  <action>
Copy `packages/higyrus-client/tests/test_null_object.py` to `packages/iol-client/tests/test_null_object.py` and adapt it. The copy is deliberate and is repo policy: there is no shared code between packages (CLAUDE.md; `verification/safemodel_diff.py` documents the same constraint in its own docstring). Do not create a shared helper module and do not import anything from another package.

Adaptations, all of them mechanical: swap the package import to `iol_client`, swap the logger name string used by the `caplog.at_level(...)` call to `iol_client`, change the non-vacuity floor to `>= 4` with the inline comment citing RESEARCH F-1's measured count for iol, and rebuild the two module-local alias fixtures against `iol_client.models.SafeModel`. Everything else — the `_safemodel_classes()` four-condition filter including the `__module__` identity check, the `_perturb` branch order with `cur is None` first, the parametrize/ids idiom — is copied unchanged.

Then add `empty()` and `__bool__` to `class SafeModel` in `packages/iol-client/src/iol_client/models.py`. iol takes **form A** of D-07, identical in body to higyrus: `empty()` is a `@classmethod` returning `Self` whose two statements bind `kwargs` to `_decode.walk_model(cls, {}, policy=_decode.POLICY, sink=_decode.SILENT_SINK)` and then `return cls(**kwargs)`; `__bool__` returns `bool` and its single expression is `return self != type(self).empty()` (the D-06 body, verbatim). Placement mirrors higyrus: after `from_api`, before `to_dict`, with `__bool__` immediately after `empty()`. No `cast`, no `# type: ignore` — the adjacent `from_api` already proves `cls(**kwargs)` type-checks on this non-dataclass base. No memoization of any kind.

Docstrings mirror higyrus's, adapted to iol only where a fact differs. Keep the `T-29-33` citation on `empty()`, keep the paragraph recording that this constructor must not be expressed via the tolerant payload entry point, and keep the two recorded facts on `__bool__` (per-call cost proportional to the empty subtree; truthiness is a field-level emptiness test).

Finally append the two wrong-type tests to `packages/iol-client/tests/test_decode.py`, ported from the pair 35-01 added to higyrus. Place them adjacent to iol's existing model-site wrong-type test at `:1220`. Drive the module's own `list[Model]`-carrying fixture through the module's `_walk` helper, assert the full three-tuple against a one-element list (equality, not membership — membership would tolerate a spurious extra record), assert the returned value is `[]`, and in the strict-mode test assert on the raised exception's field-path and kind attributes rather than on the exception type alone. Do not modify any existing test in this file; 35-05 owns the inversion at `:308` and `:1202`.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client -q && uv run mypy packages/iol-client/src && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_null_object.py -q` exits 0.
    - `uv run pytest packages/iol-client/tests/test_null_object.py --collect-only -q | grep -c "test_every_shipped_model_is_falsy_when_empty"` returns `4`.
    - `uv run pytest packages/iol-client/tests/test_decode.py -q -k "wrong_typed_list"` exits 0 and reports 2 passing.
    - `grep -c "def __bool__" packages/iol-client/src/iol_client/models.py` returns `1`; `grep -c "def empty" packages/iol-client/src/iol_client/models.py` returns `1`.
    - `grep -c "return self != type(self).empty()" packages/iol-client/src/iol_client/models.py` returns `1`.
    - `grep -c "_apply_mapping_policy" packages/iol-client/src/iol_client/models.py` returns `0` — iol takes form A; the mapping pass does not exist in this package and must not be invented.
    - Non-memoization is proven behaviourally, not by grepping for a decorator name: `uv run python -c "from iol_client import models as m; assert m.Punta.empty() is not m.Punta.empty()"` exits 0.
    - `grep -c "higyrus\|matriz_client\|market_data_client\|ambito_financiero_client\|wallets_client" packages/iol-client/tests/test_null_object.py` returns `0` — no cross-package coupling survived the port.
    - `git diff --numstat packages/iol-client/tests/test_decode.py` shows `0` deletions.
    - `uv run pytest packages/iol-client -q` exits 0 with no pre-existing test reddened.
  </acceptance_criteria>

  <done>iol's 4 shipped models are falsy-when-empty and truthy-when-perturbed by enumeration, the alias invariant holds, both wrong-type tests pass against the unedited walker, and the iol suite plus mypy strict are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port to market-data-client with the form-B empty() (mapping pass) and the received_at caveat</name>

  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/tests/test_null_object.py, packages/market-data-client/tests/test_decode.py</files>

  <read_first>
    - `packages/iol-client/tests/test_null_object.py` (as this plan's Task 1 left it — the immediate source of the port)
    - `packages/market-data-client/src/market_data_client/models.py:150-270` — `_apply_mapping_policy` at `:159`, the base at `:204`, its `from_api` at `:212-223` including the Lock-8 conditional-sink comment at `:215-219`, and `MarketDataSnapshot` at `:267`
    - `packages/market-data-client/src/market_data_client/models.py:305-360` — `MarketDataSnapshot.from_api` at `:312` with its keyword-only `received_at` parameter and the post-walk stamp at `:344-353`; this is the D-09 fact the docstring must record
    - `packages/matriz-client/src/matriz_client/models.py:238-251` — the reference form-B `empty()`: in `empty()` BOTH sinks are unconditionally silent, so the `isinstance(payload, dict)` ternary that appears in `from_api` collapses away
    - `packages/market-data-client/tests/test_decode.py:230-260` (the `test_missing_list_field_returns_empty_list_and_reports` at `:240` — 35-05 inverts it, do not touch) and `:1480-1500` (the model-site wrong-type test at `:1484` — structural model for the new list-site test)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Pitfall 6 (market-data is outside the root mypy `files` list — run the scoped invocation too) and §Pitfall 8 (`MarketDataSnapshot` truthiness is structural, not semantic)
  </read_first>

  <behavior>
    - The three parametrized model-level tests cover exactly the 16 `SafeModel` subclasses. The 7 request dataclasses (`LatestRequest`, `NewSymbol`, `NewSymbols`, `SymbolPatch`, `MarketHoursIn`, `HolidayIn`, `HolidaysIn`) are excluded automatically because they do not subclass the base — they are input, not output (D-08).
    - `test_the_model_roster_is_not_vacuous` asserts `>= 16`.
    - `empty()` on a mapping-carrying class produces the same value as `from_api` on a non-dict payload for every dict-typed field — the two constructors agree.
    - `test_empty_emits_nothing` holds for all 16 despite the extra mapping pass, because that pass also receives the silent sink.
    - The two wrong-type list tests are appended to `tests/test_decode.py` and are green against the unedited walker.
  </behavior>

  <action>
Port `tests/test_null_object.py` from iol to `packages/market-data-client/tests/test_null_object.py`: swap the package import and logger-name string to `market_data_client`, raise the non-vacuity floor to `>= 16` citing RESEARCH F-1's measured count, and rebuild the two alias fixtures against `market_data_client.models.SafeModel`. Again: copy, never import cross-package.

Add one extra comment to this package's copy that the other five do not need, attached to the `MarketDataSnapshot` parametrize case or to the falsy test's body: the class passes `bool(from_api(None)) is False` only because `received_at` defaults to `0.0` on that code path, while the shipped client always supplies a wall-clock stamp — so the green here is structural, and the semantic question ("is this snapshot empty?") is asked of the FIELD, not of the envelope. Do not weaken or skip the assertion to accommodate this; state it (D-09, RESEARCH Pitfall 8).

Add `empty()` and `__bool__` to `class SafeModel` in `packages/market-data-client/src/market_data_client/models.py`. This package takes **form B** of D-07 because its `from_api` carries the mapping pass: `empty()` binds `kwargs` to `_decode.walk_model(cls, {}, policy=_decode.POLICY, sink=_decode.SILENT_SINK)`, then calls `_apply_mapping_policy(cls, kwargs, sink=_decode.SILENT_SINK)`, then `return cls(**kwargs)`. The conditional sink expression used in `from_api` at `:220-222` does NOT carry over — inside `empty()` there is no payload to be non-dict, so both sinks are unconditionally silent and the ternary collapses. `__bool__` is the same single-expression body as everywhere else: `return self != type(self).empty()`.

The `empty()` docstring must declare the delta explicitly, per D-07: keep the shared summary and the `T-29-33` citation, then add a paragraph stating that this package's tolerant constructor carries a mapping pass and that `empty()` must carry it too, or the two constructors would disagree on any dict-typed field. Do NOT introduce a shared no-op to make the four bases byte-identical — `29-SEMANTICS-MATRIX.md`'s "Never harmonize" rule governs here, and Phase 36 removes this mapping machinery from this package anyway, so a unifying shim would be born dead.

`__bool__`'s docstring records the same two facts as the other bases, plus the D-09 sentence for this package specifically: a model whose tolerant constructor stamps a client-side value after the walk is truthy even when the wire carried nothing, so callers test the field.

Finally append the two wrong-type list tests to `packages/market-data-client/tests/test_decode.py`, ported from iol's, placed next to the existing model-site wrong-type test at `:1484`. Same assertion discipline: full three-tuple equality against a one-element list, returned value asserted `[]`, strict-mode test asserting the raised exception's field-path and kind attributes. Modify no existing test — 35-05 owns the inversions at `:240` and `:1466` here, and the eleventh inversion in this package's `tests/test_core.py:1052` is also 35-05's, not yours.
  </action>

  <verify>
    <automated>uv run pytest packages/market-data-client -q && uv run mypy packages/market-data-client/src && uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_null_object.py -q` exits 0.
    - `uv run pytest packages/market-data-client/tests/test_null_object.py --collect-only -q | grep -c "test_every_shipped_model_is_falsy_when_empty"` returns `16`.
    - The same collect-only output contains none of `LatestRequest`, `NewSymbol`, `NewSymbols`, `SymbolPatch`, `MarketHoursIn`, `HolidayIn`, `HolidaysIn` — request dataclasses are out of scope (D-08) and the roster filter excludes them without a hand-maintained deny-list.
    - `uv run pytest packages/market-data-client/tests/test_decode.py -q -k "wrong_typed_list"` exits 0 and reports 2 passing.
    - `grep -c "def empty" packages/market-data-client/src/market_data_client/models.py` returns `1` and `grep -c "def __bool__" packages/market-data-client/src/market_data_client/models.py` returns `1`.
    - `grep -c "_apply_mapping_policy(cls, kwargs, sink=_decode.SILENT_SINK)" packages/market-data-client/src/market_data_client/models.py` returns `1` — the form-B pass is present and unconditional.
    - Non-memoization is proven behaviourally: `uv run python -c "from market_data_client import models as m; assert m.Health.empty() is not m.Health.empty()"` exits 0.
    - `uv run mypy packages/market-data-client/src` prints `Success: no issues found` — this invocation is not redundant with the global one; market-data is outside the root mypy `files` list (RESEARCH Pitfall 6).
    - `git diff --numstat packages/market-data-client/tests/test_decode.py` shows `0` deletions, and `git diff --numstat packages/market-data-client/tests/test_core.py` shows the file unchanged (`test_core.py:1052` belongs to 35-05).
    - `uv run pytest packages/market-data-client -q` exits 0 with no pre-existing test reddened.
  </acceptance_criteria>

  <done>market-data's 16 shipped models are falsy-when-empty and truthy-when-perturbed, `empty()` carries the mapping pass silently, the `received_at` caveat is recorded in both the test and the `__bool__` docstring, and both scoped and global mypy are clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party JSON payload → `walk_model` / `walk_field` | The workspace's input-validation layer (ASVS V5). Untouched by this plan — no `_decode.py` byte moves. |
| divergence record → package logger | ASVS V7 log sink. `_safe_key` neutralisation and the 64-char key cap are unchanged; this plan adds no new path into the sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-06 | Tampering | market-data `empty()` diverging from `from_api` on dict-typed fields | mitigate | Form B carries `_apply_mapping_policy` with the silent sink, enforced by an exact-string acceptance grep. Without it, `empty()` and the nested-model default would disagree and a Phase 36 alias chain would read a different object than the walker built. |
| T-35-07 | Tampering | Cached empty instance becoming shared mutable state | mitigate | Memoization prohibited by a `must_haves.prohibitions` row and enforced behaviourally in both packages: two successive `empty()` calls on the same class must return distinct objects. |
| T-35-08 | Repudiation | `MarketDataSnapshot`'s structural green being read as a semantic claim | mitigate | The caveat is written into the test body AND the `__bool__` docstring (D-09). The assertion is not weakened to hide the nuance — the nuance is stated. |
| T-35-SC | Tampering | pip / uv dependency installs | accept | No installs. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` clean at plan end. |
</threat_model>

<verification>
Never run bare `uv run pytest` (RESEARCH Pitfall 5 / F-9: `verification/` hangs past 10 min and is
red at baseline under `HARN-VERIF-01`).

1. `uv run pytest packages/iol-client -q` and `uv run pytest packages/market-data-client -q` — green.
2. `uv run pytest packages -q` — green (higyrus from 35-01 still green too).
3. `uv run ruff check packages/ && uv run ruff format --check packages/` — clean.
4. `uv run mypy` AND `uv run mypy packages/market-data-client/src` — both `Success`.
5. The 4 v1.6 gates, all still green (no `_decode.py` byte moved):
   `uv run python tools/check_decode_intactness.py`,
   `uv run python tools/check_uniform_structure.py`,
   `uv run python tools/check_surface_types.py`,
   `uv run python tools/surface_parity.py`.
6. `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` — byte-identical (D-12). Note market-data has no file under `verification/snapshots/`; its surface net is the in-package `tests/test_public_surface_market_data.py`, which runs in step 1.
7. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` — clean (T-35-SC).
</verification>

<success_criteria>
- Both bases expose `empty() -> Self` and `__bool__() -> bool`; both `__bool__` bodies are `return self != type(self).empty()` verbatim.
- iol's `empty()` is form A (no mapping pass, and none invented); market-data's is form B (mapping pass present, unconditional silent sink) with the delta declared in its docstring.
- iol's roster collects 4 parametrized cases, market-data's 16, and market-data's excludes all 7 request dataclasses without a hand-maintained list.
- The alias-invisibility pair passes in both packages.
- Four new wrong-type tests (2 per package) pass against the unedited walker.
- `pytest packages`, ruff, both mypy invocations, the 4 gates and the snapshot byte-identity check are green; both `test_decode.py` diffs show zero deletions and `test_core.py` is untouched.
</success_criteria>

<output>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-03-SUMMARY.md` when done.
Record the two measured roster counts, the exact `empty()` form used per package with its
justification, and a one-line statement that the canonical digest was NOT touched.
</output>
