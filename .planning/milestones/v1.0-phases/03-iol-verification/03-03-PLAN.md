---
phase: 03-iol-verification
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
  - .planning/verification/schemas/iol-client/get-quote.json
  - .planning/verification/schemas/iol-client/get-historical-quotes.json
  - .planning/verification/schemas/iol-client/get-instruments.json
  - .planning/verification/schemas/iol-client/get-instruments-by-type.json
  - .planning/verification/iol-client-findings.md
autonomous: false
requirements: [IOL-01, IOL-02, IOL-03, IOL-04, IOL-05, IOL-06]
tags: [verification, tests, invariants, live-run, schema-commit, checkpoint, drift-01-mirror]
must_haves:
  truths:
    - "test_client.py contiene la sección `# ------ Verified live (Phase 3) ------` con los invariantes mockeados de D-IOL-21"
    - "test_async_client.py contiene la sección `# ------ Verified live (Phase 3) ------` espejo async (D-IOL-21)"
    - "Test mockeado sync verifica URL exacta de get_quote (`/api/v2/bcba/Titulos/GGAL/Cotizacion?model.mercado=bcba&model.simbolo=GGAL&model.plazo=t2`) — IOL-02"
    - "Test mockeado sync verifica que get_instruments_by_type devuelve la lista interna de `data['titulos']` (IOL-04 envelope unwrap)"
    - "Test mockeado sync verifica que `ultimoPrecio` llega como `int | float` (IOL-04 numeric type)"
    - "Test mockeado sync verifica el formato del path histórico `YYYY-MM-DD/YYYY-MM-DD/sinAjustar` con day > 12 (IOL-04 date format)"
    - "Test mockeado async espeja los 4 invariantes sync via `await aio.<func>(...)` (D-IOL-21 dual surface)"
    - "Sección `# ------ Regressions ------` ya existente (Plan 03-01) preservada intacta"
    - "El driver corrió exitosamente al menos una vez contra api.invertironline.com (live verification de IOL-01..06 + IOL-07 in-vivo)"
    - "`.planning/verification/schemas/iol-client/get-quote.json` committeado al repo (DRIFT-01 mirror, 1 de 4)"
    - "`.planning/verification/schemas/iol-client/get-historical-quotes.json` committeado al repo (DRIFT-01 mirror, 2 de 4)"
    - "`.planning/verification/schemas/iol-client/get-instruments.json` committeado al repo (DRIFT-01 mirror, 3 de 4)"
    - "`.planning/verification/schemas/iol-client/get-instruments-by-type.json` committeado al repo con envelope `{titulos: [...]}` D-IOL-17/Pitfall 2 (DRIFT-01 mirror, 4 de 4)"
    - "`.planning/verification/iol-client-findings.md` committeado al repo con esqueleto + cualquier finding del primer live run"
  artifacts:
    - path: "packages/iol-client/tests/test_client.py"
      provides: "Sync Verified-live (Phase 3) section with IOL-04 invariants"
      contains: "# ------ Verified live (Phase 3) ------"
    - path: "packages/iol-client/tests/test_async_client.py"
      provides: "Async Verified-live (Phase 3) section mirror"
      contains: "# ------ Verified live (Phase 3) ------"
    - path: ".planning/verification/schemas/iol-client/get-quote.json"
      provides: "DRIFT-01 mirror baseline schema for get_quote"
      contains: "\"schema\":"
      contains_also: "\"endpoint\":"
    - path: ".planning/verification/schemas/iol-client/get-historical-quotes.json"
      provides: "DRIFT-01 mirror baseline schema for get_historical_quotes"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/iol-client/get-instruments.json"
      provides: "DRIFT-01 mirror baseline schema for get_instruments"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/iol-client/get-instruments-by-type.json"
      provides: "DRIFT-01 mirror baseline schema with envelope titulos (Pitfall 2)"
      contains: "titulos"
    - path: ".planning/verification/iol-client-findings.md"
      provides: "Phase 3 findings file (skeleton + first live run findings)"
      contains: "# Findings: iol-client"
  key_links:
    - from: "packages/iol-client/tests/test_client.py"
      to: "iol_client.get_instruments_by_type (envelope unwrap behavior)"
      via: "httpx_mock.add_response(url='https://api.test/api/v2/Cotizaciones/acciones/argentina/Todos', json={'titulos': [...]})"
      pattern: "titulos"
    - from: ".planning/verification/schemas/iol-client/*.json"
      to: "main_iol.py::probe_schema_snapshot"
      via: "first live driver run generates these files"
      pattern: "get-.*\\.json"
    - from: ".planning/verification/iol-client-findings.md"
      to: "main_iol.py::probe_*"
      via: "first live run via append_finding + write_findings"
      pattern: "iol-client-findings.md"
---

<objective>
Cerrar Phase 3 ejecutando el driver de IOL al menos una vez contra `api.invertironline.com` (genera findings file + 4 schema snapshots DRIFT-01 mirror), agregando los **invariantes IOL-04 verificados en vivo** como tests mockeados en `test_client.py` + `test_async_client.py` (sección `# ------ Verified live (Phase 3) ------`), y commiteando los 5 artefactos. Este plan tiene un checkpoint humano (Task 3.2) para revisar los artefactos generados antes del commit (D-04 mirror + nota MVP: opportunistic — fixes ad-hoc de findings encontrados durante el live run se hacen en plans adicionales 03-NN, no en este plan ejecutable).

La sección `# ------ Regressions ------` ya fue agregada en Plan 03-01 con los 4 tests de IOL-07 por surface (8 total); este plan agrega la sección `# ------ Verified live (Phase 3) ------` (D-IOL-21 cinco invariantes: URL exacta IOL-02 + envelope titulos IOL-04 + numeric type IOL-04 + path histórico day>12 IOL-04). La estructura de Wave 3 es idéntica a Plan 02-03 (test append + live checkpoint + commit baseline), con un schema snapshot extra (4 endpoints vs 1).

Purpose: IOL-01..06 quedan cumplidos (IOL-01/02/06 via driver run, IOL-03 via field_type_map findings, IOL-04 via field_type_map envelope check + Verified-live mocked tests, IOL-05 via opt-in probe_auth_401). IOL-07 ya quedó cubierto in-cycle por Plans 03-01 + 03-02 (fix + in-vivo verification). DRIFT-01 mirror cumplido con 4 schema snapshots committeados. La regla MVP: la fase pasa aun sin findings; cualquier finding OPEN que aparezca y sea promovido a CONFIRMED/FIXED por el humano queda fuera del plan ejecutable.

Output:
1. `test_client.py` + `test_async_client.py` con sección `# ------ Verified live (Phase 3) ------` (D-IOL-21 invariantes IOL-04) y la sección `# ------ Regressions ------` (de Plan 03-01) preservada
2. `.planning/verification/schemas/iol-client/{get-quote,get-historical-quotes,get-instruments,get-instruments-by-type}.json` committeados (4 archivos, DRIFT-01 mirror)
3. `.planning/verification/iol-client-findings.md` committeado (esqueleto + cualquier finding OPEN/EXPECTED del primer live run)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-iol-verification/03-CONTEXT.md
@.planning/phases/03-iol-verification/03-RESEARCH.md
@.planning/phases/03-iol-verification/03-PATTERNS.md
@.planning/phases/03-iol-verification/03-VALIDATION.md
@.planning/phases/03-iol-verification/03-01-PLAN.md
@.planning/phases/03-iol-verification/03-02-PLAN.md
@.planning/phases/02-mbito-verification/02-03-PLAN.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_iol.py
@verification/findings.py
@packages/iol-client/tests/test_client.py
@packages/iol-client/tests/test_async_client.py
@packages/iol-client/tests/conftest.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/__init__.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Append Verified-live (Phase 3) invariants to test_client.py + test_async_client.py (D-IOL-21, IOL-02 + IOL-04)</name>
  <files>packages/iol-client/tests/test_client.py, packages/iol-client/tests/test_async_client.py</files>
  <read_first>
    - packages/iol-client/tests/test_client.py (estado actual DESPUÉS de Plan 03-01: 8 tests sync pre-existentes + 4 tests sync de la sección `# ------ Regressions ------` agregada en Plan 03-01 Task 1.3; agregar la sección `# ------ Verified live (Phase 3) ------` ENTRE los pre-existentes y la sección Regressions, o ANTES de la sección Regressions — Discretion del implementador, recomendado ANTES del divider de Regressions para que el orden divider→tests sea consistente)
    - packages/iol-client/tests/test_async_client.py (estado actual DESPUÉS de Plan 03-01: 6 tests async pre-existentes + 4 tests async de Regressions; mismo pattern)
    - packages/iol-client/tests/conftest.py (autouse fixtures precargan `_token = "test-token"` y `_token_expires_at = 9_999_999_999.0` — los tests Verified-live heredan el setup sin modificación)
    - packages/iol-client/src/iol_client/client.py (DESPUÉS de Plan 03-01: comportamiento del cliente que estos invariantes lockean)
    - packages/iol-client/src/iol_client/aio.py (DESPUÉS de Plan 03-01: comportamiento async espejo)
    - packages/iol-client/src/iol_client/__init__.py (re-exports: `iol_client.get_quote`, `iol_client.get_historical_quotes`, `iol_client.get_instruments`, `iol_client.get_instruments_by_type`)
    - .planning/phases/03-iol-verification/03-CONTEXT.md (D-IOL-21 lista verbatim los 4 invariantes mínimos: URL exactas IOL-02, envelope titulos IOL-04, ultimoPrecio numeric IOL-04, path histórico day>12 IOL-04)
    - .planning/phases/03-iol-verification/03-RESEARCH.md (Code Examples sección "pytest-httpx refresh-token test pattern" — patrón general de `httpx_mock.add_response(url=...)` con URL full incluyendo query string; tests Verified-live siguen el mismo pattern)
    - .planning/phases/03-iol-verification/03-PATTERNS.md (sección 5 "test_client.py — APPEND (Verified live + Regressions)" tiene los tests Verified-live con código casi verbatim — copiar las firmas de los 3 tests sync + adaptar a async)
    - .planning/phases/02-mbito-verification/02-03-PLAN.md (analog directo del Phase 2 Task 3.1: mismo divider pattern + tests Verified-live mockeados con URL exacta + docstring Phase X: ...)
    - .planning/codebase/TESTING.md (pytest-httpx con URL full string + query string; `async def test_async_*` sin decorador por `asyncio_mode=auto`; convención de docstring `"Phase N: locking de ..."`)
  </read_first>
  <behavior>
    - Test 1 sync (IOL-02 URL exacta de get_quote): `test_get_quote_url_exacta_con_query_string` mockea `https://api.test/api/v2/bcba/Titulos/GGAL/Cotizacion?model.mercado=bcba&model.simbolo=GGAL&model.plazo=t2` con response `{"ultimoPrecio": 1234.5, "simbolo": "GGAL"}`; asserts `quote["ultimoPrecio"] == 1234.5` AND `isinstance(quote["ultimoPrecio"], (int, float))` (IOL-04 numeric type lock)
    - Test 2 sync (IOL-04 envelope titulos unwrap): `test_get_instruments_by_type_unwraps_titulos` mockea `https://api.test/api/v2/Cotizaciones/acciones/argentina/Todos` con `{"titulos": [{"simbolo": "GGAL"}, {"simbolo": "PAMP"}]}`; asserts el cliente devuelve la lista interna: `isinstance(titulos, list)`, `all(isinstance(t, dict) for t in titulos)`, `[t["simbolo"] for t in titulos] == ["GGAL", "PAMP"]`
    - Test 3 sync (IOL-04 path histórico day>12): `test_get_historical_quotes_url_dia_gt_12` mockea `https://api.test/api/v2/bcba/Titulos/GGAL/Cotizacion/seriehistorica/2026-04-15/2026-04-20/sinAjustar` (day=15 y 20 ambos > 12) con response `[{"fechaHora": "2026-04-18T17:00:00", "ultimoPrecio": 999.9}]`; llama `iol_client.get_historical_quotes("GGAL", dt.date(2026, 4, 15), dt.date(2026, 4, 20))`; asserts `serie[-1]["ultimoPrecio"] == 999.9`
    - Tests 4-6 async: espejos exactos vía `await aio.get_quote(...)`, `await aio.get_instruments_by_type(...)`, `await aio.get_historical_quotes(...)` — mismos URLs, mismos mocks, mismos asserts; nombres prefijados `test_async_*`
    - Las secciones quedan ordenadas (de top a bottom dentro del archivo):
      1. Tests pre-existentes (8 sync / 6 async) — sin tocar
      2. `# ------ Verified live (Phase 3) ------` (NUEVO en este plan) + 3 tests
      3. `# ------ Regressions ------` (ya existía de Plan 03-01) + 4 tests de IOL-07
  </behavior>
  <action>
    Append a `packages/iol-client/tests/test_client.py` la sección `# ------ Verified live (Phase 3) ------` (divider verbatim — D-IOL-21) ANTES del divider `# ------ Regressions ------` que Plan 03-01 ya colocó. NO mover el divider Regressions ni los 4 tests IOL-07 que ya están abajo — solo insertar el nuevo divider + 3 tests Verified-live en posición intermedia.

    Estructura final del archivo (verificable por orden de aparición):
    ```
    # (8 tests sync pre-existentes)

    # ------ Verified live (Phase 3) ------

    def test_get_quote_url_exacta_con_query_string(...):
        ...

    def test_get_instruments_by_type_unwraps_titulos(...):
        ...

    def test_get_historical_quotes_url_dia_gt_12(...):
        ...

    # ------ Regressions ------

    # (4 tests de IOL-07 que Plan 03-01 agregó)
    ```

    Mismo append a `packages/iol-client/tests/test_async_client.py`: divider Verified live + 3 tests async espejo ANTES del divider Regressions de Plan 03-01.

    **Convenciones obligatorias (CONVENTIONS.md / TESTING.md):**
    - Mantener `from __future__ import annotations` línea 3 (ya presente)
    - Imports ya existentes son suficientes (no se requieren imports nuevos para estos tests sync)
    - pytest-httpx con URL completa incluyendo query string para get_quote (el cliente actual emite `?model.mercado=...&model.simbolo=...&model.plazo=...` en ese orden — verificar inspeccionando el wrapper `get_quote` actual al implementar)
    - Para los tests con `dt.date(2026, 4, 21)` o similar: importar `datetime as dt` si no está ya (test_client.py:5 ya tiene `import datetime as dt`)
    - Async tests sin decorador (asyncio_mode=auto)
    - Docstring corto en cada test con formato `"""Phase 3: locking de URL emitida con día > 12 (IOL-04)."""` referenciando el requirement ID
    - ruff line-length=100, double quotes, 4 espacios
    - mypy --strict pasa

    **Tests sync (test_client.py) — 3 nuevos en posición intermedia:**

    1. `def test_get_quote_url_exacta_con_query_string(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 3: locking URL exacta de get_quote + ultimoPrecio numeric (IOL-02 + IOL-04)."""`
       - Mock: `httpx_mock.add_response(url="https://api.test/api/v2/bcba/Titulos/GGAL/Cotizacion?model.mercado=bcba&model.simbolo=GGAL&model.plazo=t2", json={"ultimoPrecio": 1234.5, "simbolo": "GGAL"})`
       - Call: `quote = iol_client.get_quote("GGAL")`
       - Asserts:
         - `quote["ultimoPrecio"] == 1234.5`
         - `isinstance(quote["ultimoPrecio"], (int, float))` (IOL-04: numeric type lock — NO string)
         - `quote["simbolo"] == "GGAL"`

    2. `def test_get_instruments_by_type_unwraps_titulos(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 3: locking del unwrap data['titulos'] (IOL-04 envelope). Si el wire deja de emitir 'titulos', el cliente devuelve [] silenciosamente — drift detectado por probe_field_type_map in-vivo (Pitfall 2), no por este test."""`
       - Mock: `httpx_mock.add_response(url="https://api.test/api/v2/Cotizaciones/acciones/argentina/Todos", json={"titulos": [{"simbolo": "GGAL"}, {"simbolo": "PAMP"}]})`
       - Call: `titulos = iol_client.get_instruments_by_type("acciones")`
       - Asserts:
         - `isinstance(titulos, list)`
         - `len(titulos) == 2`
         - `all(isinstance(t, dict) for t in titulos)`
         - `[t["simbolo"] for t in titulos] == ["GGAL", "PAMP"]`

    3. `def test_get_historical_quotes_url_dia_gt_12(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 3: locking del formato YYYY-MM-DD del path histórico (IOL-04). Día > 12 descarta ambigüedad DD/MM vs MM/DD estructuralmente."""`
       - Date setup: `desde = dt.date(2026, 4, 15)`, `hasta = dt.date(2026, 4, 20)` (ambos day > 12)
       - Mock: `httpx_mock.add_response(url="https://api.test/api/v2/bcba/Titulos/GGAL/Cotizacion/seriehistorica/2026-04-15/2026-04-20/sinAjustar", json=[{"fechaHora": "2026-04-18T17:00:00", "ultimoPrecio": 999.9}])`
       - Call: `serie = iol_client.get_historical_quotes("GGAL", desde, hasta)`
       - Asserts:
         - `isinstance(serie, list)`
         - `len(serie) >= 1`
         - `serie[-1]["ultimoPrecio"] == 999.9`

    **Tests async (test_async_client.py) — 3 espejos:**

    Mismos tests con prefijo `test_async_*`, firma `async def`, mocks idénticos, calls `await aio.<func>(...)`:

    4. `async def test_async_get_quote_url_exacta_con_query_string(httpx_mock: HTTPXMock) -> None:` — mirror exacto de Test 1 con `await aio.get_quote("GGAL")`
    5. `async def test_async_get_instruments_by_type_unwraps_titulos(httpx_mock: HTTPXMock) -> None:` — mirror de Test 2 con `await aio.get_instruments_by_type("acciones")`
    6. `async def test_async_get_historical_quotes_url_dia_gt_12(httpx_mock: HTTPXMock) -> None:` — mirror de Test 3 con `await aio.get_historical_quotes("GGAL", desde, hasta)`

    **Restricciones:**
    - NO modificar tests pre-existentes (8 sync + 6 async + sección Regressions de Plan 03-01 — todos quedan intactos)
    - NO mover ni alterar el divider `# ------ Regressions ------` ni los 4 tests de IOL-07 que están debajo (de Plan 03-01)
    - NO agregar tests para IOL-07 (esos ya están en sección Regressions de Plan 03-01)
    - NO ejecutar el driver en este task (eso es Task 3.2)
    - NO commitear ningún archivo de `.planning/verification/` en este task (eso es Task 3.3)
    - Verificar al final que el orden en cada archivo es: pre-existentes → Verified live (Phase 3) → Regressions

    Commit sugerido: `test(03-03): add Verified live (Phase 3) invariants for IOL-02 + IOL-04 (D-IOL-21)`
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/iol-client/tests/test_async_client.py -v</automated>
    <automated>uv run pytest packages/iol-client -q</automated>
    <automated>uv run mypy packages/iol-client</automated>
    <automated>uv run ruff check packages/iol-client/tests/test_client.py packages/iol-client/tests/test_async_client.py</automated>
    <automated>uv run ruff format --check packages/iol-client/tests/test_client.py packages/iol-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 3) ------" packages/iol-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 3) ------" packages/iol-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/iol-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/iol-client/tests/test_async_client.py</automated>
    <automated>uv run python -c "src = open('packages/iol-client/tests/test_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 3) ------'); idx_re = src.find('# ------ Regressions ------'); assert idx_vl > 0 and idx_re > 0, 'both dividers must exist'; assert idx_vl < idx_re, f'Verified live divider (at {idx_vl}) must precede Regressions divider (at {idx_re})'; print('order OK')"</automated>
    <automated>uv run python -c "src = open('packages/iol-client/tests/test_async_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 3) ------'); idx_re = src.find('# ------ Regressions ------'); assert idx_vl > 0 and idx_re > 0; assert idx_vl < idx_re, f'Verified live must precede Regressions'; print('order OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/iol-client/tests/test_client.py contains # ------ Verified live (Phase 3) ------`
    - Source: `packages/iol-client/tests/test_client.py contains # ------ Regressions ------` (de Plan 03-01, preservado)
    - Source: `packages/iol-client/tests/test_async_client.py contains # ------ Verified live (Phase 3) ------`
    - Source: `packages/iol-client/tests/test_async_client.py contains # ------ Regressions ------` (de Plan 03-01, preservado)
    - Source: `packages/iol-client/tests/test_client.py contains def test_get_quote_url_exacta_con_query_string(`
    - Source: `packages/iol-client/tests/test_client.py contains def test_get_instruments_by_type_unwraps_titulos(`
    - Source: `packages/iol-client/tests/test_client.py contains def test_get_historical_quotes_url_dia_gt_12(`
    - Source: `packages/iol-client/tests/test_async_client.py contains async def test_async_get_quote_url_exacta_con_query_string(`
    - Source: `packages/iol-client/tests/test_async_client.py contains async def test_async_get_instruments_by_type_unwraps_titulos(`
    - Source: `packages/iol-client/tests/test_async_client.py contains async def test_async_get_historical_quotes_url_dia_gt_12(`
    - Source: ambos archivos contienen `isinstance(` con `(int, float)` (IOL-04 numeric type lock)
    - Source: ambos archivos contienen `"2026-04-15"` literal y `"2026-04-20"` literal (IOL-04 fechas day > 12) — o la fecha equivalente que el implementador haya elegido siempre con day > 12
    - Source: ambos archivos contienen `{"titulos":` (IOL-04 envelope literal en mock)
    - Source: ambos archivos contienen `model.mercado=bcba&model.simbolo=GGAL&model.plazo=t2` (URL exacta con query string ordenado — IOL-02)
    - Source: ambos archivos contienen `seriehistorica/2026-04-15/2026-04-20/sinAjustar` (path histórico verbatim — IOL-04)
    - Source: el divider `# ------ Verified live (Phase 3) ------` aparece ANTES del divider `# ------ Regressions ------` en ambos archivos (verificable por byte offset comparison; el script python inline en `<verify>` lo chequea)
    - Source: ambos archivos preservan `from __future__ import annotations` línea 3
    - Source: tests pre-existentes (8 sync + 4 Regressions sync + 6 async + 4 Regressions async) NO modificados
    - Behavior: `uv run pytest packages/iol-client/tests/test_client.py -v` reporta >= 15 PASSED (8 viejos + 3 Verified live + 4 Regressions)
    - Behavior: `uv run pytest packages/iol-client/tests/test_async_client.py -v` reporta >= 13 PASSED (6 viejos + 3 Verified live + 4 Regressions)
    - Behavior: `uv run pytest packages/iol-client -q` exits 0
    - Static: `uv run mypy packages/iol-client` exits 0
    - Static: `uv run ruff check packages/iol-client/tests` exits 0
    - Static: `uv run ruff format --check packages/iol-client/tests` exits 0
    - Non-regression: `uv run pytest -q` (suite completa) exits 0
  </acceptance_criteria>
  <done>Sección `# ------ Verified live (Phase 3) ------` agregada en ambos test files con 3 tests por surface (6 total) que lockean los invariantes IOL-02 (URL exacta) + IOL-04 (envelope titulos, numeric type, path histórico day>12); orden divider preservado (Verified live ANTES de Regressions). Tests pre-existentes y sección Regressions de Plan 03-01 sin modificar. Suite completa verde, mypy + ruff limpios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.2: Live driver run (one-shot, baseline 4 schemas + findings file)</name>
  <files>main_iol.py, .planning/verification/schemas/iol-client/get-quote.json, .planning/verification/schemas/iol-client/get-historical-quotes.json, .planning/verification/schemas/iol-client/get-instruments.json, .planning/verification/schemas/iol-client/get-instruments-by-type.json, .planning/verification/iol-client-findings.md</files>
  <read_first>
    - main_iol.py (reescrito en Plan 03-02; entry point del live run con 15 probes en orden D-IOL-5)
    - .planning/phases/03-iol-verification/03-CONTEXT.md (D-04 mirror Phase 2 — exit 0 siempre salvo crash inesperado; D-IOL-1/2/4 — auth_401 opt-in single-shot last; D-21 — envelope JSON del schema; D-IOL-3 cascade SKIPPED)
    - .planning/phases/03-iol-verification/03-RESEARCH.md (Pitfall 9 — lockout policy unknown; documentar explícitamente "1 run por hora máx con VERIFY_IOL_BAD_CREDS=1")
    - .planning/verification/FINDINGS-TEMPLATE.md (formato esperado del archivo generado por write_findings + append_finding)
    - packages/iol-client/.env.example (verificar que el usuario tenga IOL_USER + IOL_PASSWORD en su `.env` antes de correr — driver lo skip-and-continue si faltan, HARN-01)
  </read_first>
  <what-built>
    El driver `main_iol.py` (reescrito en Plan 03-02) y los tests Verified-live + Regressions (Task 3.1 + Plan 03-01) están listos. Ahora ejecutamos el driver una vez contra `api.invertironline.com` para:

    1. **Generar los 4 primeros schema snapshots DRIFT-01 mirror:** `probe_schema_snapshot` escribirá los 4 archivos en `.planning/verification/schemas/iol-client/` con envelope D-21 (endpoint + client_function + captured_at + base_url + sample_params + schema). Para `get-instruments-by-type.json`: el schema captura el envelope CRUDO `{"titulos": [...]}` (Pitfall 2 — NO el unwrapped list).

    2. **Generar el findings file inicial:** `write_findings("iol-client")` crea el esqueleto y cualquier finding OPEN/EXPECTED detectado por los 15 probes se append-ea vía `append_finding` (idempotente).

    3. **Verificar IOL-01..06 + IOL-07 in-vivo:** 15 líneas `PROBE <name>: <status>` + `SUMMARY: ...` en stdout; exit 0 (D-04: salvo crash inesperado). El probe_refresh_token (14) verifica que el fix de Plan 03-01 funciona contra el server real (token rotates via refresh path sin re-disparar password grant).

    El comando exacto es:
    ```
    uv run --package iol-client python main_iol.py
    ```

    Para IOL-05 (probe 401 opt-in), el run adicional es:
    ```
    VERIFY_IOL_BAD_CREDS=1 uv run --package iol-client python main_iol.py
    ```

    **CRÍTICO Pitfall 9:** la corrida con `VERIFY_IOL_BAD_CREDS=1` consume 1 intento de auth fallido por run contra la cuenta real. Máximo 1 corrida por hora discrecional (industria usa 3-10 attempts como lockout threshold; IOL no documenta el suyo). NO re-correr en loop. Si la cuenta queda lockeada, esperar ~15-60 min y reintentar SIN el flag para confirmar normal operation.

    NOTA: la corrida default (sin VERIFY_IOL_BAD_CREDS) emite `PROBE auth_401: SKIPPED (opt-in via VERIFY_IOL_BAD_CREDS=1)`. La corrida con el flag es opcional para esta sesión de checkpoint; el commit del baseline en Task 3.3 puede hacerse con solo el run default y luego el humano corre opt-in deliberadamente para registrar el finding EXPECTED de IOL-05.

    **Importante (D-04 mirror):** los hallazgos OPEN son output esperado, NO errores. El driver exit 0 igual.
  </what-built>
  <action>
    Acción operativa (a ejecutarse en checkpoint humano antes del commit del Task 3.3):

    1. Asistente (o usuario) verifica que `packages/iol-client/.env` existe con `IOL_USER` e `IOL_PASSWORD` válidos (sino driver imprime `SKIPPED iol-client: missing IOL_USER, IOL_PASSWORD` y sale — HARN-01).

    2. Usuario corre el driver default contra `api.invertironline.com`:
       `uv run --package iol-client python main_iol.py`
       Verifica que stdout emite 15 líneas `PROBE <name>: <status>` (verbatim D-02 Phase 2 mirror) en el orden D-IOL-5 (auth_401 último) seguidas de una línea `SUMMARY: ...` con conteos por estado. Exit code = 0 (D-04 mirror).

    3. Se generan 5 artefactos en disco (output del driver):
       - 4 schemas en `.planning/verification/schemas/iol-client/`:
         - `get-quote.json` con envelope D-21 + schema observado para GGAL
         - `get-historical-quotes.json` con envelope D-21 + schema observado para GGAL últimos 5 días hábiles
         - `get-instruments.json` con envelope D-21 + schema observado para país=argentina
         - `get-instruments-by-type.json` con envelope D-21 + schema CRUDO `{"titulos": [...]}` (Pitfall 2 — NO el unwrapped list)
       - `.planning/verification/iol-client-findings.md` con esqueleto generado por `write_findings` + findings OPEN/EXPECTED del run (vía `append_finding`)

    4. Usuario inspecciona los artefactos:
       - Para cada JSON: keys = {endpoint, client_function, captured_at, base_url, sample_params, schema}; `base_url == "https://api.invertironline.com"` (asumiendo no se overrideó con IOL_BASE_URL).
       - Para `get-instruments-by-type.json`: el campo `schema` DEBE ser dict-wrapping-list `{"titulos": [...]}`, NO solo lista — si aparece como lista pura, el probe está usando el wrapper (BUG Pitfall 2; vuelve a Plan 03-02 a corregir).
       - Para el markdown: header `# Findings: iol-client`, sección `## Run Context (ART)` con Timestamp ISO-8601 reciente + `base_url=https://api.invertironline.com`, sección `## Index` (vacía o con rows), sección detalle por finding.

    5. Usuario clasifica findings OPEN encontrados según D-08 lifecycle Phase 1:
       - CONFIRMED: bug real reproducido → editar archivo manualmente
       - EXPECTED: comportamiento del server esperado terminal (ej. el 401 del opt-in si se corrió)
       - NO-FIX: dato puntual o ciclo no aplica
       - OPEN se mantiene si requiere más investigación

    6. **(OPCIONAL)** Usuario corre IOL-05 opt-in probe deliberadamente:
       `VERIFY_IOL_BAD_CREDS=1 uv run --package iol-client python main_iol.py`
       UNA SOLA VEZ — Pitfall 9 prohíbe loops por riesgo de lockout. Espera ver `PROBE auth_401: FINDING F-NN (EXPECTED)` y un finding nuevo con clase AUTH status EXPECTED.

    7. Usuario emite resume signal:
       - `"approved"` para que Task 3.3 commitee los 5 artefactos al repo
       - `"holds-finding-X"` + descripción para insertar Plan 03-04 opportunistic con fix dual sync/async + 2 regression tests ANTES del commit (uso del patrón Phase 2 opportunistic)
       - `"reject"` + razón si el driver crashea o output no respeta D-02/D-04 (vuelve al Plan 03-02 a corregir)

    No se escribe código en este task. No se edita ningún archivo (los artefactos son OUTPUT del driver, no INPUT humano). El task solo bloquea la ejecución hasta que el humano apruebe el commit.
  </action>
  <how-to-verify>
    1. **Verificá `.env`:** asegurate de que `packages/iol-client/.env` tiene `IOL_USER` e `IOL_PASSWORD` válidos. Si no existe, copialo de `.env.example` y completá los valores:
       ```
       cp packages/iol-client/.env.example packages/iol-client/.env
       # Editá packages/iol-client/.env con tus credenciales reales
       ```

    2. **Corré el driver default** (sin VERIFY_IOL_BAD_CREDS — el opt-in queda para el paso 6):
       ```
       uv run --package iol-client python main_iol.py
       ```

    3. **Verificá la salida stdout:** esperás ver 15 líneas `PROBE <name>: <status>` seguidas de `SUMMARY: ...`. Por ejemplo:
       ```
       PROBE login_sync: PASS _token cached, _refresh_token=<cached>
       PROBE login_async: PASS ok
       PROBE get_quote_sync: PASS ultimoPrecio=8765.5
       PROBE get_quote_async: PASS ultimoPrecio=8765.5
       PROBE get_historical_quotes_sync: PASS len=5
       PROBE get_historical_quotes_async: PASS len=5
       PROBE get_instruments_sync: PASS type=dict
       PROBE get_instruments_async: PASS type=dict
       PROBE get_instruments_by_type_sync: PASS 6 types ok
       PROBE get_instruments_by_type_async: PASS ok
       PROBE parity_sync_async: PASS shape sync == async en los 4 endpoints
       PROBE field_type_map: PASS sin drift detectado
       PROBE schema_snapshot: PASS los 4 escritos
       PROBE refresh_token: PASS refresh path verified — token rotated, _refresh_token=preserved
       PROBE auth_401: SKIPPED (opt-in via VERIFY_IOL_BAD_CREDS=1)
       SUMMARY: PASS=14 FAIL=0 SKIPPED=1 FINDING=0
       ```
       (los nombres exactos de status y detalles pueden variar según Discretion del implementador del driver, pero el orden y los nombres de probes deben coincidir con D-IOL-5).

    4. **Verificá los 5 artefactos en disco:**
       ```
       ls -la .planning/verification/schemas/iol-client/
       cat .planning/verification/schemas/iol-client/get-quote.json
       cat .planning/verification/schemas/iol-client/get-historical-quotes.json
       cat .planning/verification/schemas/iol-client/get-instruments.json
       cat .planning/verification/schemas/iol-client/get-instruments-by-type.json
       cat .planning/verification/iol-client-findings.md
       ```
       - Cada JSON debe ser un dict con keys `endpoint`, `client_function`, `captured_at`, `base_url`, `sample_params`, `schema` (envelope D-21).
       - **CRÍTICO — `get-instruments-by-type.json`:** el campo `schema` debe ser un dict que CONTIENE la key `"titulos"` (envelope crudo, Pitfall 2). NO debe ser una lista pura. Si ves `"schema": [{...}]` en lugar de `"schema": {"titulos": [{...}]}`, el driver está usando el wrapper en lugar de `_request` directo — STOP y reportar como "reject" para volver al Plan 03-02.
       - El markdown debe contener header `# Findings: iol-client`, sección `## Run Context (ART)` con Timestamp + base_url + (opcional) market hours.

    5. **Inspeccioná findings (si los hay):**
       - Cualquier finding OPEN: leer `**Expected:**` vs `**Actual:**` y decidir si es bug real (→ promover a CONFIRMED), comportamiento del server (→ EXPECTED), o info puntual (→ NO-FIX o dejar OPEN).
       - Posibles findings del primer run según D-IOL-15 (field_type_map):
         - `SHAPE` missing assumed key — algún campo en `_ASSUMED_QUOTE_FIELDS` no aparece en el payload real → ajustar `_ASSUMED_*` en main_iol.py o promover a CONFIRMED si es realmente un bug del cliente
         - `SHAPE` type drift — un campo numérico llega como string → investigar
         - `SHAPE` missing envelope key `'titulos'` — Pitfall 2 ya detectó que el wire cambió (raro pero el driver lo cubre)
       - Posibles findings AUTH:
         - `AUTH` "login() no capturó refresh_token del payload" — el server no rota; documentar como EXPECTED o investigar
       - Posibles findings SYNC-ASYNC-DRIFT — schema sync diff de async → investigar (no debería pasar; ambos hacen el mismo HTTP request modulo lock pattern)

    6. **(OPCIONAL) Corré el IOL-05 opt-in probe** una sola vez:
       ```
       VERIFY_IOL_BAD_CREDS=1 uv run --package iol-client python main_iol.py
       ```
       - Espera ver `PROBE auth_401: FINDING F-NN (EXPECTED)` y un finding nuevo con clase `AUTH` status `EXPECTED`, status_code = 401.
       - Si aparece `OPEN` con `actual=<otra cosa>` (D-IOL-3 catch-all): registrar el hallazgo y NO re-correr el probe (Pitfall 9 — riesgo de lockout).
       - Si después de este run la corrida default falla con `IOLAuthError`: la cuenta puede estar lockeada, esperar 15-60 min antes de continuar.

    7. **Confirmá al asistente:**
       - **"approved"** si los 5 artefactos lucen bien (especialmente `get-instruments-by-type.json` con envelope `titulos`) y querés que Task 3.3 los commitee al repo
       - **"holds-finding-X"** + descripción si querés que se cree un Plan 03-04 opportunistic para promover un finding específico a CONFIRMED → fix dual sync+async → 2 regression tests (D-07 mirror) **antes** de commitear el findings file
       - **"reject"** + razón si el driver crashea o el envelope de `get-instruments-by-type.json` sale unwrapped (= Pitfall 2 violado): vuelve al Plan 03-02 a corregir
  </how-to-verify>
  <verify>
    <human-check>Driver ejecutado al menos una vez con exit 0; output stdout respeta D-02 (15 líneas PROBE + SUMMARY); 5 artefactos generados (4 JSONs + 1 markdown) inspeccionados y aprobados por el humano; envelope `titulos` presente en get-instruments-by-type.json (Pitfall 2 verified)</human-check>
    <automated>test -f .planning/verification/schemas/iol-client/get-quote.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-historical-quotes.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-instruments.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-instruments-by-type.json</automated>
    <automated>test -f .planning/verification/iol-client-findings.md</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/verification/schemas/iol-client/get-instruments-by-type.json')); schema = data.get('schema'); assert isinstance(schema, dict) and 'titulos' in schema, f'Pitfall 2 violation: get-instruments-by-type.json schema must be a dict with titulos key, got {type(schema).__name__}'; print('Pitfall 2 envelope check OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/verification/schemas/iol-client/get-quote.json` (generado por el driver run)
    - File exists: `.planning/verification/schemas/iol-client/get-historical-quotes.json`
    - File exists: `.planning/verification/schemas/iol-client/get-instruments.json`
    - File exists: `.planning/verification/schemas/iol-client/get-instruments-by-type.json`
    - File exists: `.planning/verification/iol-client-findings.md`
    - File content (4 JSONs): cada uno contiene las 6 keys del envelope D-21 (`endpoint`, `client_function`, `captured_at`, `base_url`, `sample_params`, `schema`)
    - File content (Pitfall 2 critical): `get-instruments-by-type.json["schema"]` es un dict (no list) y contiene la key `"titulos"` (envelope crudo)
    - File content (markdown): contiene `# Findings: iol-client` (header generado por new_findings con _PKG="iol-client")
    - File content (markdown): contiene sección `## Run Context (ART)` con líneas `- Timestamp:` y `- Resolved base URL / env:`
    - Behavior: driver exit code = 0 (D-04 mirror)
    - Behavior: stdout contiene 15 líneas que matchean el regex `^PROBE [a-z_0-9]+: (PASS|FAIL|SKIPPED|FINDING)` (D-02 verbatim Phase 2 mirror)
    - Behavior: stdout contiene una línea que empieza con `SUMMARY: ` (D-02 summary final)
    - Behavior: si el opt-in IOL-05 corre, el findings file contiene una row con `Class: AUTH | Status: EXPECTED` (o similar formato del template)
    - Human approval recibida (resume signal = "approved" o equivalente)
  </acceptance_criteria>
  <resume-signal>
    Type **"approved"** to proceed to Task 3.3 (commit baseline artifacts: 4 schemas + findings file). Or describe issues / request opportunistic fix-plan via "holds-finding-X" / "reject".
  </resume-signal>
  <done>Driver ejecutado en vivo al menos una vez; 4 schema snapshots + findings markdown generados en disco; envelope `titulos` confirmado en get-instruments-by-type.json (Pitfall 2 verified); humano inspeccionó y aprobó los artefactos para commit. Optional: opt-in IOL-05 probe corrido una sola vez para registrar finding EXPECTED.</done>
</task>

<task type="auto">
  <name>Task 3.3: Commit baseline artifacts (4 DRIFT-01 schemas + Phase 3 findings file)</name>
  <files>.planning/verification/schemas/iol-client/get-quote.json, .planning/verification/schemas/iol-client/get-historical-quotes.json, .planning/verification/schemas/iol-client/get-instruments.json, .planning/verification/schemas/iol-client/get-instruments-by-type.json, .planning/verification/iol-client-findings.md</files>
  <read_first>
    - .planning/verification/schemas/iol-client/get-quote.json (recién generado por Task 3.2)
    - .planning/verification/schemas/iol-client/get-historical-quotes.json (recién generado por Task 3.2)
    - .planning/verification/schemas/iol-client/get-instruments.json (recién generado por Task 3.2)
    - .planning/verification/schemas/iol-client/get-instruments-by-type.json (recién generado por Task 3.2)
    - .planning/verification/iol-client-findings.md (recién generado por Task 3.2)
    - .gitignore (verificar que `.planning/verification/captures/` está ignored pero NO `.planning/verification/schemas/iol-client/` ni `.planning/verification/iol-client-findings.md`)
    - .planning/phases/02-mbito-verification/02-03-PLAN.md Task 3.3 (analog directo: mismo pattern de commit atómico — comando + mensaje + Co-Authored-By)
  </read_first>
  <action>
    Confirmar que los 5 artefactos existen en disco y NO están gitignored:

    1. `test -f .planning/verification/schemas/iol-client/get-quote.json` exits 0
    2. `test -f .planning/verification/schemas/iol-client/get-historical-quotes.json` exits 0
    3. `test -f .planning/verification/schemas/iol-client/get-instruments.json` exits 0
    4. `test -f .planning/verification/schemas/iol-client/get-instruments-by-type.json` exits 0
    5. `test -f .planning/verification/iol-client-findings.md` exits 0
    6. Para cada archivo: `git check-ignore <path>` exits 1 (NO ignored — committeable)

    Si alguno reporta ignored: STOP y arreglar `.gitignore` antes de continuar (esperamos que solo `.planning/verification/captures/` esté ignored, no `schemas/` ni `<pkg>-findings.md`).

    Verificar Pitfall 2 una vez más antes del commit (defense-in-depth en caso de que el humano no haya inspeccionado el JSON):
    ```
    uv run python -c "import json; data = json.load(open('.planning/verification/schemas/iol-client/get-instruments-by-type.json')); schema = data.get('schema'); assert isinstance(schema, dict) and 'titulos' in schema, 'Pitfall 2 violation — STOP commit'; print('OK')"
    ```
    Si falla: STOP commit, reportar al humano (la corrida no respetó el envelope crudo y debe re-correrse después de fix en Plan 03-02).

    Stageá los 5 archivos:
    ```bash
    git add .planning/verification/schemas/iol-client/get-quote.json
    git add .planning/verification/schemas/iol-client/get-historical-quotes.json
    git add .planning/verification/schemas/iol-client/get-instruments.json
    git add .planning/verification/schemas/iol-client/get-instruments-by-type.json
    git add .planning/verification/iol-client-findings.md
    ```

    Commit atómico (DRIFT-01 mirror + Phase 3 lifecycle):
    ```bash
    git commit -m "feat(03-03): commit DRIFT-01 mirror baseline (4 IOL schemas + Phase 3 findings)

    First live run of main_iol.py against api.invertironline.com:
    - Schema snapshots for get_quote, get_historical_quotes, get_instruments,
      and get_instruments_by_type (envelope D-21, Pitfall 2 envelope preserved).
    - Findings file populated by the 15 probes (D-IOL-1..22).

    Verifies IOL-01..06 in vivo + IOL-07 in-vivo via probe_refresh_token.
    IOL-05 opt-in via VERIFY_IOL_BAD_CREDS=1 (single-shot per Pitfall 9).
    Drift detection re-runs use the 4 schema snapshots (D-25) without overwriting.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    Verificar el commit en git log:
    ```bash
    git log -1 --oneline
    git diff HEAD~1 HEAD --name-only
    ```

    NO ejecutar el driver de nuevo en este task. NO commitear nada más en este task (test files de Task 3.1 ya tienen su propio commit; no mezclar concerns).

    **Importante:** si Task 3.2 generó un findings file con findings OPEN que el humano marcó como "holds-finding-X" en el checkpoint, este Task NO debe ejecutarse — el orquestador insertará Plan 03-04 opportunistic antes de cerrar Phase 3. En el flujo normal "approved", este task corre directo.
  </action>
  <verify>
    <automated>test -f .planning/verification/schemas/iol-client/get-quote.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-historical-quotes.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-instruments.json</automated>
    <automated>test -f .planning/verification/schemas/iol-client/get-instruments-by-type.json</automated>
    <automated>test -f .planning/verification/iol-client-findings.md</automated>
    <automated>! git check-ignore .planning/verification/schemas/iol-client/get-quote.json 2>&1 | grep -q schemas</automated>
    <automated>! git check-ignore .planning/verification/iol-client-findings.md 2>&1 | grep -q findings</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/verification/schemas/iol-client/get-instruments-by-type.json')); schema = data.get('schema'); assert isinstance(schema, dict) and 'titulos' in schema; print('Pitfall 2 envelope OK')"</automated>
    <automated>git log -1 --pretty=%s | grep -F "03-03"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "get-quote.json"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "get-historical-quotes.json"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "get-instruments.json"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "get-instruments-by-type.json"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "iol-client-findings.md"</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run mypy .</automated>
    <automated>uv run ruff check .</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/verification/schemas/iol-client/get-quote.json`
    - File exists: `.planning/verification/schemas/iol-client/get-historical-quotes.json`
    - File exists: `.planning/verification/schemas/iol-client/get-instruments.json`
    - File exists: `.planning/verification/schemas/iol-client/get-instruments-by-type.json`
    - File exists: `.planning/verification/iol-client-findings.md`
    - `.gitignore`: la ruta `.planning/verification/captures/` permanece ignored (T-3-CC preservado del threat model — captures con payload crudo se ignoran; schemas con type-names committeables)
    - `.gitignore`: NO añade `.planning/verification/schemas/iol-client/` ni `.planning/verification/iol-client-findings.md` (los nuevos artefactos son committeables)
    - Behavior: para cada archivo, `git check-ignore <path>` exits 1 (NO ignored)
    - Behavior: `git log -1` muestra un commit con prefijo `feat(03-03):` que incluye los 5 archivos
    - Behavior: `git diff HEAD~1 HEAD --name-only` lista los 5 archivos como agregados
    - File content (4 JSONs): cada uno contiene las 6 keys del envelope D-21
    - File content (Pitfall 2): `get-instruments-by-type.json["schema"]` es dict con key `"titulos"`
    - File content (markdown): contiene `# Findings: iol-client` header
    - File content (markdown): contiene sección `## Run Context (ART)` con `- Timestamp:` y `- Resolved base URL / env:`
    - File content (markdown): contiene sección `## Index` con tabla `| ID | Class | Surface | Status |`
    - Source: ningún test file modificado en este task (solo commit de artefactos generados por el driver)
    - Behavior: `uv run pytest -q` exits 0 (suite completa verde)
    - Behavior: `uv run mypy .` exits 0 (mypy strict whole-repo)
    - Behavior: `uv run ruff check .` exits 0 (ruff whole-repo)
  </acceptance_criteria>
  <done>4 schema snapshots DRIFT-01 mirror + Phase 3 findings file committeados al repo en un commit atómico `feat(03-03)`. .gitignore preserva captures/ ignored; schemas/iol-client/ y iol-client-findings.md committeables. Pitfall 2 confirmed (envelope `titulos` preservado). Próxima corrida del driver compara contra esta baseline. Suite completa verde + mypy strict + ruff sin errores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live driver → api.invertironline.com | Llamadas HTTP autenticadas (Bearer) durante el checkpoint Task 3.2 |
| driver → .planning/verification/schemas/iol-client/ (committeable) | 4 JSON artefactos que entran a git en Task 3.3 — sensibles a accidental disclosure de datos del payload real |
| driver → .planning/verification/iol-client-findings.md (committeable) | Markdown que entra a git — los campos `expected`/`actual`/`diff` reciben strings de los probes; si contienen valor crudo de payload, queda en git history |
| mocked tests → iol_client | Locking de comportamiento esperado del cliente, derivado del payload real observado |
| Pitfall 2 verification (Task 3.3 pre-commit) | Defense in depth: el commit no procede si el envelope `titulos` está unwrapped (= driver bug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-19 | Information Disclosure | findings file accidentalmente committeado con un valor crudo de la respuesta IOL (ej. ultimoPrecio, simbolo) | mitigate | El driver (Plan 03-02) usa `schema_of(payload)` para schema-related findings (PII-free por construcción — solo type-names). Findings AUTH usan `repr(exc)` que typically es el text de IOL (`invalid_grant`, etc., sin credenciales). Findings PARAM/SHAPE usan rangos numéricos sin valores específicos. Para defense in depth: humano inspecciona el markdown manualmente en checkpoint Task 3.2 antes de aprobar el commit. NO hay PII en payloads de IOL (precios públicos de mercado, símbolos, no usernames ni balances). |
| T-3-20 | Tampering | Schema snapshot committeado con valores reales en lugar de tipos | mitigate | `schema_of` produce solo type-names (`"str"`, `"int"`, `"float"`, `"bool"`, etc.) por construcción — verificado en Phase 1 con `test_harness_schema.py`. Acceptance criterion del Task 3.3 verifica que el content del envelope es estructural (schema es dict/list de strings, no valores). |
| T-3-21 | Tampering | Pitfall 2 violation: `get-instruments-by-type.json` se commitea con schema unwrapped (lista pura) | mitigate | Task 3.2 how-to-verify instrye al humano a inspeccionar el JSON; Task 3.3 acceptance criterion + automated verify ejecutan `assert isinstance(schema, dict) and 'titulos' in schema` antes del commit. Si falla, STOP commit. Defense in depth contra el caso "implementador olvidó usar `_request` directo". |
| T-3-22 | Repudiation | Schema snapshot sin metadata trazable | mitigate | Envelope D-21 incluye `captured_at` (ISO-8601 UTC), `base_url`, `sample_params` — la corrida que produjo cada baseline queda registrada en el archivo y en git history. |
| T-3-23 | DoS-against-self (lockout) | Re-correr el opt-in IOL-05 probe en loop durante el checkpoint | mitigate | Checkpoint 3.2 instruye explícitamente "una sola vez" y Pitfall 9 documentado en el `<what-built>`. La instrucción dice "NO re-correr el probe" si aparece OPEN. Acceptance criterion implícito: el humano respeta la cadencia de máximo 1 corrida por hora. |
| T-3-24 | Tampering | Tests mockeados Verified-live que pasan pero el cliente fallaría en vivo (mocks ficticios) | mitigate | D-IOL-21: invariantes derivados de los probes que LACONFIRMARÁN al correr Task 3.2 — si el live run pasa, los mocks de Task 3.1 reflejan la realidad. Si el live run revela un wire diferente al asumido, el findings file lo captura y los mocks se ajustan (vía Plan 03-04 opportunistic) antes de commitear. |
| T-3-25 | Tampering | Drift silencioso si los schemas se commitean con data ad-hoc o sin pasar por el driver | mitigate | Task 3.3 commits SOLO los archivos generados por Task 3.2 (live driver run). No hay paso intermedio donde el humano edite el JSON. La baseline inicial viene de una corrida única + checkpoint humano. |
| T-3-26 | Tampering | Pitfall 2 falsely passing because `schema_of` recursive walks the dict and the `"titulos"` key happens to be in a sub-payload | mitigate | El test del envelope verifica `"titulos" in schema` AT TOP LEVEL (no recursivo) — porque el envelope se captura como `resp.json()` que es `{"titulos": [...]}` y `schema_of(envelope)` retorna `{"titulos": [...types...]}` con `titulos` como key top-level. La verificación del acceptance criterion es `isinstance(schema, dict) and 'titulos' in schema` que solo pasa si la key está EN EL TOP LEVEL. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes externos. Solo dependencias ya bloqueadas en `uv.lock`. Package Legitimacy Audit de 03-RESEARCH.md sección "Package Legitimacy Audit" — vacía intencional. |
</threat_model>

<verification>
Whole-plan integration (Task 3.1 + 3.2 + 3.3):

```bash
# Task 3.1 — Tests verdes después del append
uv run pytest packages/iol-client -q
uv run mypy packages/iol-client
uv run ruff check packages/iol-client/tests
uv run ruff format --check packages/iol-client/tests

# Verificación de orden de los dividers
uv run python -c "src = open('packages/iol-client/tests/test_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 3) ------'); idx_re = src.find('# ------ Regressions ------'); assert 0 < idx_vl < idx_re; print('OK')"
uv run python -c "src = open('packages/iol-client/tests/test_async_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 3) ------'); idx_re = src.find('# ------ Regressions ------'); assert 0 < idx_vl < idx_re; print('OK')"

# Task 3.2 — Driver corrido manualmente con checkpoint humano (no se automatiza)

# Task 3.3 — Artefactos committeados
for f in get-quote get-historical-quotes get-instruments get-instruments-by-type; do
    test -f ".planning/verification/schemas/iol-client/${f}.json" || exit 1
    ! git check-ignore ".planning/verification/schemas/iol-client/${f}.json" || exit 1
done
test -f .planning/verification/iol-client-findings.md
! git check-ignore .planning/verification/iol-client-findings.md

# Pitfall 2 envelope check (defense-in-depth en Task 3.3 pre-commit)
uv run python -c "import json; data = json.load(open('.planning/verification/schemas/iol-client/get-instruments-by-type.json')); schema = data.get('schema'); assert isinstance(schema, dict) and 'titulos' in schema"

# Wave 3 full gate (todos los success criteria de Phase 3)
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `test_client.py` y `test_async_client.py` con sección `# ------ Verified live (Phase 3) ------` con 3 invariantes mockeados por surface (6 total): IOL-02 URL exacta, IOL-04 envelope titulos, IOL-04 numeric type, IOL-04 path histórico day>12
- La sección `# ------ Regressions ------` de Plan 03-01 (4 tests IOL-07 por surface) preservada intacta debajo del divider Verified live
- Orden de dividers verificable: pre-existentes → Verified live (Phase 3) → Regressions
- Driver `main_iol.py` ejecutado al menos una vez contra `api.invertironline.com` con exit 0 (D-04 mirror) y output respetando D-02 mirror Phase 2 (15 líneas PROBE + SUMMARY)
- 4 schema snapshots committeados al repo en `.planning/verification/schemas/iol-client/`: `get-quote.json`, `get-historical-quotes.json`, `get-instruments.json`, `get-instruments-by-type.json` (DRIFT-01 mirror)
- **Pitfall 2 verificado:** `get-instruments-by-type.json["schema"]` es dict con key `"titulos"` (envelope crudo preservado por `_request` directo en probe 12 — NO el unwrapped list del wrapper)
- `.planning/verification/iol-client-findings.md` committeado con esqueleto + cualquier finding OPEN/EXPECTED del primer live run (D-IOL-3, D-IOL-11, D-IOL-15)
- Checkpoint humano completado: el humano revisó los 5 artefactos y aprobó el commit (Task 3.2 resume signal = "approved")
- Suite completa verde después del commit (`uv run pytest -q && uv run mypy . && uv run ruff check .`)
- `.gitignore` preservado: `.planning/verification/captures/` sigue ignored; los nuevos artefactos son committeable
- IOL-01..04 + IOL-06 verificados en vivo (driver run, probes 1-13)
- IOL-04 lockeado en mockeados (tests Verified live de Task 3.1)
- IOL-05 opt-in verificado opcionalmente (no es bloqueante para el commit; humano puede correr `VERIFY_IOL_BAD_CREDS=1` deliberadamente una sola vez)
- IOL-07 in-vivo verificado por probe 14 (D-IOL-11); el fix de Plan 03-01 confirmed funcional contra el server real
- DRIFT-01 mirror cumplido (4 schema snapshots committeados para los 4 endpoints públicos del cliente IOL)
- Re-corridas futuras del driver comparan contra estas 4 baselines (drift detection D-25 lista para ejercitarse)
</success_criteria>

<output>
Crear `.planning/phases/03-iol-verification/03-03-SUMMARY.md` cuando done, registrando:
- Output verbatim del primer live run del driver (las 15 líneas + summary) — sirve como ground-truth para futuras re-corridas
- Contenido de los 4 schema snapshots (resumen por archivo: top-level keys observados + tipo) — registro de las baselines
- Confirmación de Pitfall 2 (envelope `titulos` presente en `get-instruments-by-type.json["schema"]`)
- Findings detectados (si los hay) y su clasificación inicial (OPEN/EXPECTED) — registro del estado al cierre de Phase 3
- Decisión sobre IOL-05 opt-in run: corrido o pospuesto
- Resultado del checkpoint humano (approved / opportunistic fix-plan 03-04 inserted)
- Resumen del fix IOL-07 verificado in-vivo: ¿el server rota refresh_token o no? (informa Plans 04 y 05 sobre el patrón a esperar de Higyrus/Matriz)
- Métrica de duración del plan
- Estado del findings file al cierre de Phase 3: cuántos OPEN, cuántos EXPECTED, cuántos CONFIRMED/FIXED (humano), cuántos NO-FIX
</output>
</content>
</invoke>
