---
phase: 08-retries-backoff-structured-logging
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - packages/iol-client/src/iol_client/_transport.py
  - packages/iol-client/src/iol_client/_atransport.py
  - packages/iol-client/src/iol_client/_logging.py
  - packages/iol-client/src/iol_client/__init__.py
  - packages/iol-client/src/iol_client/_core.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_transport.py
  - packages/iol-client/tests/test_logging.py
  - verification/snapshots/iol_client-surface.txt
autonomous: true
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, iol, atomic-commit, rely, log, oauth-401-reauth]
user_setup: []

must_haves:
  truths:
    - "`iol_client/_transport.py` define `RetryTransport(httpx.HTTPTransport)` mirror ámbito (Plan 2 pattern duplicated verbatim with `_LOGGER_NAME='iol_client'`); D-01/D-04/D-07/D-08 invariants preserved"
    - "`iol_client/_atransport.py` define `AsyncRetryTransport(httpx.AsyncHTTPTransport)` mirror ámbito async (D-32)"
    - "`iol_client/_logging.py` define `RedactingFilter` con patterns adicionales por D-10: Bearer/X-Auth-Token/password baseline + IOL refresh_token URL-encoded + IOL refresh_token JSON (refresh response includes both formats per `_core.py:175-178`)"
    - "`iol_client/__init__.py` invoca `_logging.attach()` con idempotency + `del` cleanup (LOG-01)"
    - "`_core.py RequestSpec` extendido con `endpoint_name: str = ''` (D-09); todos los GET builders (`build_get_quote_request`, `build_get_historical_quotes_request`, `build_get_instruments_request`, `build_get_instruments_by_type_request`) flippean `idempotent=True` + setean `endpoint_name=<fn name>` (D-03)"
    - "Auth-flow `build_login_request` (`_core.py:133-149`) Y `build_refresh_request` (`_core.py:181-194`) marcados `idempotent=True` (D-03) — transient 5xx en login/refresh es retry-eable; 401 es NEVER retry (D-02, D-07)"
    - "`Client.__init__` + `AsyncClient.__init__` + `configure()` extendidos con 2 kwargs `max_retries=2` + `http_client=None` (D-15)"
    - "Shell `_request()` (sync + async) genera `request_id`, setea `extensions`, AND implementa 401 re-auth-once vía pattern (D-02): `try _raise_for_response → except IOLAuthError → state.token=None; _ensure_token(); req.headers['Authorization']=f'Bearer {state.token}'; resp=http.send(req); _raise_for_response(resp); return resp`"
    - "`_ensure_token()` (login/refresh path) atraviesa el mismo `state.http_client` con RetryTransport instalado (D-29) — login retry-eable en 5xx; OAuth `refresh_token` redaction cubierta por `_logging.py` regex"
    - "`verification/snapshots/iol_client-surface.txt` actualizado con 2 nuevos kwargs en Client/AsyncClient/configure() signatures (D-28)"
    - "Cross-cutting guard tests: `test_retry_401_reauth.py[iol]` GREEN (mock 401→login→200 chain → 3 wire requests + FRESH-TOKEN en header del último); `test_logging_no_token_leak.py[iol]` GREEN (SECRET-LITERAL-12345 NO aparece en ningún caplog record)"
    - "iol existing tests (Phase 7 baseline) GREEN post-cambios; CR-01 conditional refresh_token rotation preservado (Phase 7 D-XX)"
  artifacts:
    - path: "packages/iol-client/src/iol_client/_transport.py"
      provides: "RetryTransport sync (mirror ámbito; only logger_name + package literal differ)"
      contains: "class RetryTransport"
      min_lines: 90
    - path: "packages/iol-client/src/iol_client/_atransport.py"
      provides: "AsyncRetryTransport async (mirror ámbito)"
      contains: "class AsyncRetryTransport"
      min_lines: 90
    - path: "packages/iol-client/src/iol_client/_logging.py"
      provides: "RedactingFilter con IOL-specific patterns (Bearer + X-Auth-Token + password + IOL refresh_token URL + JSON)"
      contains: "refresh_token"
      min_lines: 70
    - path: "packages/iol-client/src/iol_client/__init__.py"
      provides: "_logging.attach() invocation con idempotency + del cleanup"
      contains: "_logging.attach()"
    - path: "packages/iol-client/tests/test_transport.py"
      provides: "Unit tests for iol RetryTransport — mutation gate, 503 retry, max_retries=0 bypass"
      contains: "RetryTransport"
      min_lines: 70
    - path: "packages/iol-client/tests/test_logging.py"
      provides: "Unit tests for iol RedactingFilter — Bearer + refresh_token URL + refresh_token JSON redacted"
      contains: "refresh_token"
      min_lines: 50
    - path: "verification/snapshots/iol_client-surface.txt"
      provides: "Snapshot iol con 2 nuevos kwargs"
      contains: "max_retries"
  key_links:
    - from: "iol_client.client.Client._request"
      to: "iol_client._transport.RetryTransport.handle_request"
      via: "request.extensions propagation + http.send"
      pattern: "request.extensions\\["
    - from: "iol_client.client.Client._request 401 path"
      to: "iol_client.client.Client._ensure_token (re-auth then retry)"
      via: "except IOLAuthError → state.token=None → _ensure_token()"
      pattern: "IOLAuthError"
    - from: "iol_client._core.build_login_request"
      to: "iol_client._transport.RetryTransport (login transient retry)"
      via: "RequestSpec.idempotent=True"
      pattern: "idempotent=True"
    - from: "iol_client._logging.RedactingFilter"
      to: "OAuth refresh_token URL-encoded + JSON patterns"
      via: "_REFRESH_TOKEN_URLENC_RE + _REFRESH_TOKEN_JSON_RE substitution"
      pattern: "refresh_token"
---

<objective>
iol Plan 8-03: replicar el patrón ámbito (Plan 2) en `iol-client` con los extras OAuth-specific: `_logging.py` con `RedactingFilter` que cubre IOL refresh_token (URL-encoded + JSON formats per `_core.py:175-178`), shell `_request()` con 401 re-auth-once flow (D-02 — pattern canónico per RESEARCH §Pattern 3 lines 549-602), y auth-flow builders (`build_login_request`, `build_refresh_request`) marcados `idempotent=True` (D-03). Plus el delivery sync+async cross-package estándar: `_transport.py`, `_atransport.py`, `_core.py` RequestSpec extensions, Client/AsyncClient/configure() 2 new kwargs, snapshot update. Per-package atomic commit per D-21.

Purpose: iol es el primer paquete con auth — valida el pattern 401 re-auth-once en shell (no en transport's `retry_on=`) sin colisionar con OAuth refresh_token flow. CR-01 conditional refresh_token rotation (Phase 7 baseline) MUST be preserved — Plan 8-03 NO toca esa lógica, solo agrega retry wrapping + log wiring sobre top. Plan 8-03 establece el "auth paquete pattern" que Plans 8-04 (higyrus) y 8-05 (matriz) replicarán con sus tweaks (account_id, Risk API auth_basic). Cumple Phase 8 ROADMAP Success Criteria #3 (401 re-auth en shell), #5 (RedactingFilter con IOL refresh_token coverage).

Output: 1 commit atómico per D-21 con 2 nuevos módulos transport (`_transport.py`, `_atransport.py`) + 1 nuevo módulo logging (`_logging.py`) + 4 extends (`_core.py`, `client.py`, `aio.py`, `__init__.py`) + 2 nuevos test files + 1 snapshot update. Commit message: `feat(iol): retries + structured logging — RetryTransport, RedactingFilter, 401 re-auth-once, OAuth refresh_token redaction (RELY-01..04, LOG-01..03)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md
@.planning/phases/08-retries-backoff-structured-logging/08-02-SUMMARY.md
@CLAUDE.md
@packages/iol-client/src/iol_client/_state.py
@packages/iol-client/src/iol_client/_core.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/__init__.py
@packages/iol-client/src/iol_client/exceptions.py
@packages/iol-client/tests/conftest.py
@verification/snapshots/iol_client-surface.txt
@packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py
@verification/test_retry_mutation_gate.py
@verification/test_retry_401_reauth.py
@verification/test_retry_after_cap.py
@verification/test_logging_root_unchanged.py
@verification/test_logging_no_token_leak.py
@verification/test_async_cancellation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create iol _transport.py + _atransport.py + _logging.py (mirror ámbito Plan 2 + IOL refresh_token patterns)</name>
  <files>packages/iol-client/src/iol_client/_transport.py, packages/iol-client/src/iol_client/_atransport.py, packages/iol-client/src/iol_client/_logging.py, packages/iol-client/tests/test_transport.py, packages/iol-client/tests/test_logging.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-01, D-04, D-07, D-08 — RetryTransport invariants; D-10 — RedactingFilter shape; D-29 — login goes through RetryTransport; D-31 — tenacity integration shape)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 4 lines 605-682 — CANONICAL `_logging.py` for iol with IOL-specific patterns; §"Code Examples" lines 980-1050 — caplog token leak; §"Common Pitfalls" Pitfall 3 lines 755-763 — credentials in DEBUG)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"Per-package redaction recipes" line 254-260 — iol: OAuth `refresh_token=...` URL-encoded + JSON `"refresh_token":"..."`; §"`packages/<pkg>/src/<pkg>/_logging.py`" lines 188-251 — RedactingFilter generic template + per-paquete additions)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py (Plan 8-02 canary — verbatim template; iol _transport.py is mechanical duplicate with `_LOGGER_NAME = "iol_client"`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py (Plan 8-02 — mirror; iol async is verbatim duplicate)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_logging.py (Plan 8-02 baseline — extend with IOL refresh_token regex)
    - packages/iol-client/src/iol_client/_core.py (lines 175-178 — login response payload includes refresh_token in both URL form-encoded AND JSON formats; this is the exact pattern the RedactingFilter must cover per PATTERNS line 258-259)
    - packages/iol-client/src/iol_client/exceptions.py (read IOLAuthError signature — used in 401 re-auth catch in Task 2)
  </read_first>
  <behavior>
    Mirror the ámbito Plan 2 modules with these IOL-specific deltas:

    **`packages/iol-client/src/iol_client/_transport.py`** (~95 LOC — mechanical duplicate of ámbito with logger_name change):
    - Identical to `packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py` body
    - Only delta: `_LOGGER_NAME = "iol_client"` + `extra["package"] = "iol_client"` literal in log records
    - Module docstring updated: `"""RetryTransport — sync. Phase 8 iol. RELY-01..04 + D-01/D-07/D-08."""`
    - All constants (`_RETRYABLE_EXC`, `_RETRYABLE_STATUS`, `_RETRY_AFTER_CAP_S`), sentinel exception (`_RetryableStatus`), parse helper (`_parse_retry_after`), `RetryTransport(httpx.HTTPTransport)` class verbatim per RESEARCH §Pattern 1 lines 348-504

    **`packages/iol-client/src/iol_client/_atransport.py`** (~95 LOC — mechanical duplicate of ámbito):
    - Identical async mirror; logger_name + extra package change only
    - `AsyncRetryTransport(httpx.AsyncHTTPTransport)` with `AsyncRetrying` + `await asyncio.sleep`
    - Recommendation (planner's call per CONTEXT discretion line 164): import shared constants from `iol_client._transport` to avoid duplication WITHIN the iol package

    **`packages/iol-client/src/iol_client/_logging.py`** (~75 LOC — extends ámbito's baseline with IOL refresh_token patterns):
    - Module docstring: `"""RedactingFilter + attach() — iol_client Phase 8 LOG-01/02/03 + IOL OAuth refresh_token redaction (D-10)."""`
    - `from __future__ import annotations`
    - Imports: `logging`, `re`
    - Module-level regex per RESEARCH §Pattern 4 lines 629-634 (CANONICAL IOL set verbatim):
      ```python
      _BEARER_RE = re.compile(r"Bearer\s+[A-Za-z0-9._\-]+")
      _X_AUTH_TOKEN_RE = re.compile(r"(X-Auth-Token\s*:\s*)[A-Za-z0-9._\-]+", re.IGNORECASE)
      _PASSWORD_URLENC_RE = re.compile(r"(password=)[^&\s]+")
      _PASSWORD_JSON_RE = re.compile(r'("password"\s*:\s*")[^"]+(")')
      _REFRESH_TOKEN_URLENC_RE = re.compile(r"(refresh_token=)[^&\s]+")
      _REFRESH_TOKEN_JSON_RE = re.compile(r'("refresh_token"\s*:\s*")[^"]+(")')
      ```
      The 2 refresh_token regex are CRITICAL — iol login response per `_core.py:175-178` returns refresh_token in BOTH formats (form-encoded for `data=` body of token endpoint, JSON for response body).
    - `def _redact(text: str) -> str` per RESEARCH §Pattern 4 lines 637-644 (verbatim — applies all 6 substitutions in order)
    - `class RedactingFilter(logging.Filter)` per RESEARCH §Pattern 4 lines 647-672 verbatim — record.msg + record.args (dict/tuple) + record.__dict__ scan
    - `def attach() -> None` per RESEARCH §Pattern 4 lines 675-682 — `getLogger("iol_client")` + NullHandler + RedactingFilter idempotency

    **`packages/iol-client/tests/test_transport.py`** (~75 LOC):
    - Same 6-7 unit tests as ámbito Plan 2 with iol-specific imports
    - Plus: `test_login_request_retries_on_503_when_idempotent_true` — verifies login (idempotent=True per D-03) retries on transient 5xx (D-29 — login goes through RetryTransport)

    **`packages/iol-client/tests/test_logging.py`** (~55 LOC):
    - Same 4-5 baseline tests as ámbito Plan 2
    - PLUS IOL-specific:
      - `test_redact_refresh_token_urlenc`: `msg="refresh_token=abc.def-ghi_123"` → `msg="refresh_token=***"`
      - `test_redact_refresh_token_json`: `msg='{"refresh_token":"abc.def-ghi_123"}'` → `msg='{"refresh_token":"***"}'`
      - `test_login_response_payload_full_redaction`: simulate the iol login response JSON `{"access_token": "X", "refresh_token": "Y", "expires_in": 900}` → after filter, both tokens are `***`
  </behavior>
  <action>
    1. Create `packages/iol-client/src/iol_client/_transport.py` by reading Plan 2's `_transport.py` and substituting `ambito_financiero_client` → `iol_client` for `_LOGGER_NAME` constant + module docstring header. ALL other code is identical (RESEARCH §Pattern 1 line 348-504 is paquete-agnostic — the only per-package value is the logger name).

    2. Create `packages/iol-client/src/iol_client/_atransport.py` by analogous substitution from Plan 2's `_atransport.py`. Import shared constants from `iol_client._transport` (intra-package).

    3. Create `packages/iol-client/src/iol_client/_logging.py` per RESEARCH §Pattern 4 lines 605-682 verbatim with logger name `"iol_client"` AND the 6 redaction regex (2 baseline Bearer/X-Auth-Token, 2 password URL/JSON, 2 refresh_token URL/JSON). Per D-10 + PATTERNS line 254-260.

    4. Create `packages/iol-client/tests/test_transport.py` — copy Plan 2's `test_transport.py` shape with iol imports; add login retry test.

    5. Create `packages/iol-client/tests/test_logging.py` — copy Plan 2's shape; add 3 IOL-specific tests (refresh_token URL, refresh_token JSON, full login response payload redaction).

    6. Run `uv run pytest packages/iol-client/tests/test_transport.py packages/iol-client/tests/test_logging.py -v` — MUST GREEN.
  </action>
  <verify>
    <automated>test -f packages/iol-client/src/iol_client/_transport.py &amp;&amp; test -f packages/iol-client/src/iol_client/_atransport.py &amp;&amp; test -f packages/iol-client/src/iol_client/_logging.py &amp;&amp; test -f packages/iol-client/tests/test_transport.py &amp;&amp; test -f packages/iol-client/tests/test_logging.py &amp;&amp; grep -c "class RetryTransport" packages/iol-client/src/iol_client/_transport.py &gt;= 1 &amp;&amp; grep -c "class AsyncRetryTransport" packages/iol-client/src/iol_client/_atransport.py &gt;= 1 &amp;&amp; grep -c "class RedactingFilter" packages/iol-client/src/iol_client/_logging.py &gt;= 1 &amp;&amp; grep -c "iol_client" packages/iol-client/src/iol_client/_transport.py &gt;= 1 &amp;&amp; grep -c "refresh_token" packages/iol-client/src/iol_client/_logging.py &gt;= 2 &amp;&amp; grep -c "_REFRESH_TOKEN" packages/iol-client/src/iol_client/_logging.py &gt;= 2 &amp;&amp; uv run pytest packages/iol-client/tests/test_transport.py packages/iol-client/tests/test_logging.py -v 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_transport.py` exists with `RetryTransport` and `_LOGGER_NAME = "iol_client"`
    - Source: `_atransport.py` exists with `AsyncRetryTransport` using `AsyncRetrying`
    - Source: `_logging.py` exists with `RedactingFilter` AND `_REFRESH_TOKEN_URLENC_RE` AND `_REFRESH_TOKEN_JSON_RE` (per RESEARCH §Pattern 4 — IOL-specific patterns)
    - Source: `grep -c "iol_client" packages/iol-client/src/iol_client/_transport.py` >= 1 (logger name correct)
    - Source: `grep -c "iol_client" packages/iol-client/src/iol_client/_logging.py` >= 1 (attach to correct logger)
    - CLI: `uv run pytest packages/iol-client/tests/test_transport.py -v` exits 0 with ≥7 tests passing
    - CLI: `uv run pytest packages/iol-client/tests/test_logging.py -v` exits 0 with ≥7 tests passing (including 3 IOL refresh_token specific)
    - CLI: `uv run ruff check packages/iol-client/` exits 0
    - CLI: `uv run mypy --strict packages/iol-client/` exits 0
  </acceptance_criteria>
  <done>
    iol's 3 new private modules (`_transport.py`, `_atransport.py`, `_logging.py`) exist + pass unit tests; refresh_token URL + JSON patterns work; ruff + mypy strict GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend iol _core.py + client.py + aio.py + __init__.py shell with 401 re-auth-once flow + 2 new kwargs; update snapshot (atomic commit per D-21)</name>
  <files>packages/iol-client/src/iol_client/_core.py, packages/iol-client/src/iol_client/client.py, packages/iol-client/src/iol_client/aio.py, packages/iol-client/src/iol_client/__init__.py, verification/snapshots/iol_client-surface.txt</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-02 — 401 re-auth-once in shell; D-03 — login/refresh idempotent=True; D-29 — login goes through RetryTransport; D-30 — request_id per business-call; D-15 — 2 new kwargs)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 3 lines 549-602 CANONICAL iol _request() with 401 re-auth — VERBATIM template; §"Architectural Responsibility Map" lines 144-146 — 401 re-auth-once at Shell tier; §"Common Pitfalls" Pitfall 1 lines 735-743 — retry includes AuthError → infinite loop)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/src/<pkg>/_core.py`" lines 291-308 — iol _core delta — flip idempotent=True on 4 GET builders + login + refresh; §"`packages/<pkg>/src/<pkg>/client.py` shell `_request()`" lines 376-422 — iol _request delta WITH 401 re-auth branch (RESEARCH §Pattern 3 verbatim); §"`packages/<pkg>/src/<pkg>/client.py` Client.__init__ + configure()" lines 466-518 — 2 new kwargs add; §"`packages/<pkg>/src/<pkg>/aio.py`" lines 522-530 — async mirror with `self._client_lock` preservation)
    - packages/iol-client/src/iol_client/_core.py (Phase 7 baseline — verify RequestSpec has `idempotent: bool = False` already; locate 4 GET builders + login + refresh builders at lines 133-149, 181-194, 211-228, 231-251, 254-263, 266-277)
    - packages/iol-client/src/iol_client/client.py (Phase 7 baseline — read FULL FILE; locate Client class, __slots__, __init__, _ensure_token, _ensure_http_client, _request, configure())
    - packages/iol-client/src/iol_client/aio.py (Phase 7 baseline — read async lock pattern at lines 154-218; preserve `_client_lock` + `_token_lock` double-checked locking when adding RetryTransport)
    - packages/iol-client/src/iol_client/exceptions.py (IOLAuthError class — used in 401 re-auth catch)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (Plan 8-02 — read `_ensure_http_client()` + `_request()` deltas as canonical template; iol differs only in adding 401 re-auth branch)
    - packages/iol-client/tests/conftest.py (existing `configure(token=..., token_expires_at=9_999_999_999.0)` autouse fixture; verify Phase 6 baseline still in place)
  </read_first>
  <behavior>
    Extend iol shell with same 2-new-kwargs pattern as ámbito + 401 re-auth-once flow:

    **`packages/iol-client/src/iol_client/_core.py`** (extend):
    - `RequestSpec` dataclass: add `endpoint_name: str = ""` (D-09)
    - Flip `idempotent=True` + set `endpoint_name=<fn>` on:
      - `build_login_request` (line 133-149) — `endpoint_name="login"` (D-03)
      - `build_refresh_request` (line 181-194) — `endpoint_name="refresh"` (D-03)
      - `build_get_quote_request` (line 211-228) — `endpoint_name="get_quote"`
      - `build_get_historical_quotes_request` (line 231-251) — `endpoint_name="get_historical_quotes"`
      - `build_get_instruments_request` (line 254-263) — `endpoint_name="get_instruments"`
      - `build_get_instruments_by_type_request` (line 266-277) — `endpoint_name="get_instruments_by_type"`
    - DO NOT add `account_id` field to iol's RequestSpec (D-11 says only higyrus + matriz)

    **`packages/iol-client/src/iol_client/client.py`** (extend):
    - `Client.__init__` signature delta: add `max_retries: int = 2` and `http_client: httpx.Client | None = None` (D-15)
    - `Client.__slots__`: add `"_max_retries"` to the slots tuple (currently includes `_state` + Phase 6 baseline)
    - `_ensure_http_client()` delta: wrap `httpx.Client(transport=_transport.RetryTransport(max_attempts=self._max_retries+1))` (same shape as Plan 2 ámbito)
    - `_request()` delta — implement RESEARCH §Pattern 3 lines 564-602 VERBATIM (the canonical 401 re-auth pattern):
      ```python
      def _request(self, spec: _core.RequestSpec) -> httpx.Response:
          self._ensure_token()
          assert self._state.token is not None
          request_id = uuid.uuid4().hex
          http = self._ensure_http_client()
          url = f"{self._state.base_url}{spec.path}"
          headers = {"Authorization": f"Bearer {self._state.token}", **(spec.headers or {})}
          req = http.build_request(
              spec.method, url,
              params=spec.params, json=spec.json_body, headers=headers,
          )
          req.extensions["idempotent"] = spec.idempotent
          req.extensions["request_id"] = request_id
          req.extensions["endpoint_name"] = spec.endpoint_name
          resp = http.send(req)
          try:
              _raise_for_response(resp)
          except IOLAuthError:
              # D-02 exactly-one re-auth (iol has no Risk API → no auth_basic branch).
              self._state.token = None
              self._ensure_token()
              assert self._state.token is not None
              req.headers["Authorization"] = f"Bearer {self._state.token}"
              resp = http.send(req)
              _raise_for_response(resp)
          return resp
      ```
      KEY INVARIANTS per D-02 + D-07:
      - The `except IOLAuthError` catches AFTER `handle_request` returns (transport NEVER sees AuthError)
      - `state.token = None` → forces `_ensure_token()` to re-login
      - Updated `req.headers["Authorization"]` for the retry
      - Second `_raise_for_response(resp)` — if STILL 401, the exception propagates (no second re-auth attempt — Pitfall 1 prevention)
    - `configure()` delta: add `max_retries: int | None = None` + `http_client: httpx.Client | None = None` carry-forward params

    **`packages/iol-client/src/iol_client/aio.py`** (extend — mirror sync + async lock preservation):
    - `AsyncClient.__init__` signature delta: same 2 kwargs (`http_client: httpx.AsyncClient | None = None`)
    - `AsyncClient.__slots__`: add `"_max_retries"`
    - `_ensure_http_client()` async delta: wrap `httpx.AsyncClient(transport=_atransport.AsyncRetryTransport(max_attempts=self._max_retries+1))` WHILE preserving the existing `self._client_lock` double-checked locking pattern from Phase 6 (read lines 154-218 of current aio.py — DO NOT remove the lock; just instantiate AsyncClient with the new transport inside the lock-protected block)
    - `_request()` async delta — mirror sync with `async`/`await`:
      ```python
      async def _request(self, spec: _core.RequestSpec) -> httpx.Response:
          await self._ensure_token()
          assert self._state.token is not None
          request_id = uuid.uuid4().hex
          http = await self._ensure_http_client()
          url = f"{self._state.base_url}{spec.path}"
          headers = {"Authorization": f"Bearer {self._state.token}", **(spec.headers or {})}
          req = http.build_request(spec.method, url, params=spec.params, json=spec.json_body, headers=headers)
          req.extensions["idempotent"] = spec.idempotent
          req.extensions["request_id"] = request_id
          req.extensions["endpoint_name"] = spec.endpoint_name
          resp = await http.send(req)
          try:
              _raise_for_response(resp)
          except IOLAuthError:
              self._state.token = None
              await self._ensure_token()
              assert self._state.token is not None
              req.headers["Authorization"] = f"Bearer {self._state.token}"
              resp = await http.send(req)
              _raise_for_response(resp)
          return resp
      ```
      Preserve existing `_token_lock` double-checked locking inside `_ensure_token()` (DO NOT touch)
    - `configure()` (module-level if exists): same 2 kwargs

    **`packages/iol-client/src/iol_client/__init__.py`** (extend):
    - INSERT after imports, BEFORE `__all__` (mirror Plan 2 ámbito + PATTERNS lines 539-547):
      ```python
      # Phase 8 LOG-01: attach NullHandler + RedactingFilter to package logger.
      from iol_client import _logging as _logging_attach
      _logging_attach.attach()
      del _logging_attach
      ```

    **`verification/snapshots/iol_client-surface.txt`** (update per D-28):
    - Update Client.__init__, AsyncClient.__init__, configure() signatures to include `max_retries: 'int' = 2` + `http_client: 'httpx.Client | None' = None` (or `httpx.AsyncClient | None` for AsyncClient)
    - DO NOT touch other entries

    **Atomic commit** per D-21:
    `feat(iol): retries + structured logging — RetryTransport, RedactingFilter, 401 re-auth-once, OAuth refresh_token redaction (RELY-01..04, LOG-01..03)`

    **CR-01 conditional refresh_token rotation preservation:** Phase 7 D-XX preserved the conditional refresh logic — Plan 8-03 NEITHER touches `_ensure_token` body NOR modifies the conditional rotation; the 401 re-auth flow operates AT the shell level AFTER `_ensure_token()` was already invoked. The refresh path inside `_ensure_token()` benefits from RetryTransport for transient 5xx (D-29) but the 401-during-refresh case raises IOLAuthError immediately (NOT retried — D-02).
  </behavior>
  <action>
    1. Edit `packages/iol-client/src/iol_client/_core.py`:
       - Add `endpoint_name: str = ""` to `RequestSpec` dataclass
       - For each of 6 builders (login, refresh, get_quote, get_historical_quotes, get_instruments, get_instruments_by_type): set `idempotent=True` + `endpoint_name="<name>"` in returned RequestSpec literal

    2. Edit `packages/iol-client/src/iol_client/client.py`:
       - Add `import uuid` + `from iol_client import _transport`
       - Add 2 kwargs to `Client.__init__` signature; handle them in body
       - Add `"_max_retries"` to `Client.__slots__`
       - Modify `_ensure_http_client()` to wrap RetryTransport
       - REPLACE `_request()` body with RESEARCH §Pattern 3 lines 564-602 verbatim (401 re-auth-once flow)
       - Modify `configure()` to accept + pass through the 2 carry-forward kwargs

    3. Edit `packages/iol-client/src/iol_client/aio.py`:
       - Add `import uuid` + `from iol_client import _atransport`
       - Add 2 kwargs (`http_client: httpx.AsyncClient | None = None`) to `AsyncClient.__init__`; add `"_max_retries"` to slots
       - Modify `_ensure_http_client()` async to wrap `httpx.AsyncClient(transport=AsyncRetryTransport(max_attempts=self._max_retries+1))` INSIDE the existing `self._client_lock` block (DO NOT remove or weaken the lock)
       - REPLACE `_request()` async body with the async mirror of Pattern 3 (`async def _request` with `await http.send` and `IOLAuthError` re-auth-once)
       - Preserve existing `_token_lock` double-checked locking in `_ensure_token()` (DO NOT modify that fn)
       - Modify `configure()` to accept + pass through the 2 carry-forward kwargs

    4. Edit `packages/iol-client/src/iol_client/__init__.py`:
       - Insert `_logging.attach()` block between imports and `__all__` per Plan 2 pattern
       - Do NOT add `_logging` to `__all__`; verify `del _logging_attach` cleanup

    5. Update `verification/snapshots/iol_client-surface.txt`:
       - Replace 3 lines for Client.__init__, AsyncClient.__init__, configure()
       - Add `max_retries: 'int' = 2` + `http_client: 'httpx.Client | None' = None` (Client/configure) / `httpx.AsyncClient | None` (AsyncClient)

    6. Run verification:
       ```
       uv run pytest packages/iol-client/ -v 2>&1 | tail -25
       uv run pytest verification/test_public_surface.py -k iol -v 2>&1 | tail -5
       uv run pytest verification/test_retry_mutation_gate.py -k iol -v 2>&1 | tail -10
       uv run pytest verification/test_retry_401_reauth.py -k iol -v 2>&1 | tail -10
       uv run pytest verification/test_logging_root_unchanged.py -v 2>&1 | tail -5
       uv run pytest verification/test_logging_no_token_leak.py -k iol -v 2>&1 | tail -10
       uv run pytest verification/test_async_cancellation.py -k iol -v 2>&1 | tail -5
       uv run pytest verification/test_sync_async_isolation.py -v 2>&1 | tail -10   # CR-01 preservation
       uv run ruff check packages/iol-client/ verification/
       uv run mypy --strict packages/iol-client/
       uv run lint-imports
       ```

    7. Single atomic commit per D-21:
       ```
       git add packages/iol-client/src/ packages/iol-client/tests/ verification/snapshots/iol_client-surface.txt
       git commit -m "feat(iol): retries + structured logging — RetryTransport, RedactingFilter, 401 re-auth-once, OAuth refresh_token redaction (RELY-01..04, LOG-01..03)

       Phase 8 Plan 3 — iol. Adds RetryTransport+AsyncRetryTransport+RedactingFilter mirroring ámbito (Plan 2). Adds 401 re-auth-once flow in shell _request() per D-02 (try _raise_for_response → except IOLAuthError → state.token=None → _ensure_token() → retry once). Marks build_login_request + build_refresh_request idempotent=True (D-03). RedactingFilter covers IOL OAuth refresh_token URL-encoded + JSON formats. Extends Client/AsyncClient/configure() with max_retries=2 + http_client=None kwargs. CR-01 conditional refresh_token rotation preserved (Phase 7 baseline untouched).

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
       ```
  </action>
  <verify>
    <automated>grep -c "endpoint_name" packages/iol-client/src/iol_client/_core.py &gt;= 1 &amp;&amp; grep -c "idempotent=True" packages/iol-client/src/iol_client/_core.py &gt;= 4 &amp;&amp; grep -c "max_retries" packages/iol-client/src/iol_client/client.py &gt;= 2 &amp;&amp; grep -c "max_retries" packages/iol-client/src/iol_client/aio.py &gt;= 2 &amp;&amp; grep -c "RetryTransport" packages/iol-client/src/iol_client/client.py &gt;= 1 &amp;&amp; grep -c "AsyncRetryTransport" packages/iol-client/src/iol_client/aio.py &gt;= 1 &amp;&amp; grep -c "IOLAuthError" packages/iol-client/src/iol_client/client.py &gt;= 2 &amp;&amp; grep -c "IOLAuthError" packages/iol-client/src/iol_client/aio.py &gt;= 2 &amp;&amp; grep -c "uuid.uuid4" packages/iol-client/src/iol_client/client.py &gt;= 1 &amp;&amp; grep -c "request.extensions" packages/iol-client/src/iol_client/client.py &gt;= 1 &amp;&amp; grep -c "_logging_attach.attach\|_logging.attach" packages/iol-client/src/iol_client/__init__.py &gt;= 1 &amp;&amp; grep -c "max_retries" verification/snapshots/iol_client-surface.txt &gt;= 2 &amp;&amp; uv run pytest packages/iol-client/ -v 2&gt;&amp;1 | tail -15 &amp;&amp; uv run pytest verification/test_public_surface.py -k iol -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_retry_401_reauth.py -k iol -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_logging_no_token_leak.py -k iol -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_async_cancellation.py -k iol -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run ruff check packages/iol-client/ verification/ &amp;&amp; uv run mypy --strict packages/iol-client/ &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_core.py` has `endpoint_name` field AND `>= 4` instances of `idempotent=True` (login + refresh + 4 GETs ≥ 6 actually; `>= 4` minimum)
    - Source: `client.py` has `max_retries` (signature + body), `RetryTransport`, `IOLAuthError` (try + raise paths), `uuid.uuid4`, `request.extensions`
    - Source: `aio.py` has same: `max_retries`, `AsyncRetryTransport`, `IOLAuthError` async catch, `uuid.uuid4`
    - Source: `__init__.py` has `_logging.attach()` invocation + `del _logging_attach`
    - Source: snapshot has `max_retries: 'int' = 2` (Client + AsyncClient + configure() — 3 places); `http_client: 'httpx.Client \| None' = None` (Client/configure) and `httpx.AsyncClient \| None` (AsyncClient)
    - CLI: `uv run pytest packages/iol-client/ -v` exits 0 (existing 70+ tests + new test_transport + new test_logging all pass)
    - CLI: `uv run pytest verification/test_public_surface.py -k iol -v` exits 0 (snapshot zero diff)
    - CLI: `uv run pytest verification/test_retry_401_reauth.py -k iol -v` exits 0 (RELY-04 GREEN for iol — 401→login→200 chain = 3 wire requests + FRESH-TOKEN in last Authorization)
    - CLI: `uv run pytest verification/test_logging_no_token_leak.py -k iol -v` exits 0 (SECRET-LITERAL-12345 not in any record — Bearer / refresh_token URL / refresh_token JSON all redacted)
    - CLI: `uv run pytest verification/test_async_cancellation.py -k iol -v` exits 0 (D-32 — AsyncRetryTransport CancelledError propagation)
    - CLI: `uv run pytest verification/test_sync_async_isolation.py -v` exits 0 (Phase 7 cross-leak sentinel GREEN — CR-01 still preserved)
    - CLI: `uv run ruff check` + `uv run mypy --strict` + `uv run lint-imports` all exit 0
    - CLI: `git log --oneline -1 | grep -c "feat(iol)"` >= 1 (atomic commit per D-21)
  </acceptance_criteria>
  <done>
    iol Plan 3 atomic commit landed: RetryTransport+AsyncRetryTransport+RedactingFilter+401-re-auth-once flow wired in shell `_request()` (sync + async). OAuth refresh_token URL + JSON formats redacted. Login + refresh marked idempotent=True. 2 new kwargs in Client/AsyncClient/configure(). Snapshot updated. CR-01 preserved. Cross-cutting guard tests GREEN for iol parametrize branches.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `iol_client.Client._request` → 401 re-auth boundary | Shell catches `IOLAuthError` AFTER transport returns; transport sees ONLY HTTP-level signals + network errors (D-02, D-07 invariant) |
| `iol_client._ensure_token()` → `state.http_client.send(login_request)` | Login goes through SAME RetryTransport — transient 5xx retried (D-29); 401 → IOLAuthError immediate (no recursive re-auth — Pitfall 1 prevention) |
| `iol_client._logging.RedactingFilter` ← record.msg / record.args / record.__dict__ with refresh_token | Filter runs BEFORE consumer's handlers emit — even at DEBUG level, sentinel literals scrubbed via 6 regex substitutions |
| `iol_client.Client(http_client=user)` → state.http_client = user AS-IS | Caller-supplied http_client used without wrap (D-16) — caller responsible for own retry policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01-IOL | Tampering | Mutation gate — POST against 503 retry (iol has no public mutating endpoint surface but `_request()` is the central path) | mitigate | `_core.py` builders default `idempotent=False` (mutation gate honored); GET builders explicit `idempotent=True`; mutation gate via `request.extensions["idempotent"]` (D-01) verified by `test_retry_mutation_gate.py[iol]` GET retry branch |
| T-8-02-IOL | Denial-of-Service (self-induced) | 401 retry storm — IOLAuthError caught at transport instead of shell, infinite loop with stale Bearer | mitigate | D-07 LOCKED: `retry_on=(httpx.ConnectError, httpx.ConnectTimeout, httpx.ReadTimeout)` ONLY — IOLAuthError NEVER. Shell `_request()` D-02 pattern: try → except IOLAuthError → state.token=None → _ensure_token() → retry ONCE. Second 401 propagates without retry. `test_retry_401_reauth.py[iol]` covers (3 wire requests + FRESH-TOKEN) |
| T-8-03-IOL | Tampering | Library hijacks root logger | mitigate | `_logging.attach()` invokes `getLogger("iol_client")` + NullHandler — NEVER root; `test_logging_root_unchanged.py` covers (now turns GREEN with iol attach landed atop ámbito's Plan 2 attach); LOG015 + CI grep cover static |
| T-8-04-IOL | Information Disclosure | OAuth refresh_token leaks in DEBUG (login response payload contains refresh_token in both URL + JSON formats per `_core.py:175-178`) | mitigate | `_logging.py` includes `_REFRESH_TOKEN_URLENC_RE` AND `_REFRESH_TOKEN_JSON_RE` — both formats scrubbed; `test_logging_no_token_leak.py[iol]` mock login response with sentinel → assertion verifies sentinel NOT in any record (D-10) |
| T-8-05-IOL | Availability | async cancellation swallowed during retry | mitigate | `AsyncRetryTransport` uses tenacity AsyncRetrying + `await self.sleep(do)` natively (D-32); `test_async_cancellation.py[iol]` covers |
| T-8-CR01-PRESERVE | Tampering (regression) | Phase 7 CR-01 conditional refresh_token rotation accidentally lost during Plan 3 edits | mitigate | Plan 3 NEITHER touches `_ensure_token()` body NOR `_refresh_token_unlocked` logic; ONLY wraps the transport + adds 401 re-auth-once in `_request()`. Existing iol tests (Phase 7 baseline) GREEN post-Plan-3 = CR-01 preserved. `test_sync_async_isolation.py` Phase 7 sentinel test re-verified |
| T-8-IOL-EXT-LEAK | Information Disclosure | `request.extensions` dict mutated across retries → request_id/endpoint_name leaks between business calls | mitigate | Pitfall 9: extensions are READ-ONLY inside transport (per D-01 + RESEARCH §"Common Pitfalls" Pitfall 9). Shell `_request()` writes them once pre-send; transport never writes. Verified by `test_retry_401_reauth.py[iol]` — same request_id across attempts |
</threat_model>

<verification>
- iol `_transport.py` + `_atransport.py` + `_logging.py` created (mirror ámbito Plan 2 + refresh_token regex per D-10)
- iol `_core.py` RequestSpec extended + 6 builders flipped to idempotent=True (login + refresh + 4 GETs)
- iol shell `_request()` (sync + async) implements 401 re-auth-once pattern per RESEARCH §Pattern 3 verbatim
- iol Client/AsyncClient/configure() extended with max_retries=2 + http_client=None kwargs
- iol `__init__.py` invokes `_logging.attach()` with `del` cleanup
- iol snapshot updated atomically
- iol existing tests preserved; CR-01 conditional refresh rotation untouched
- Cross-cutting guard tests turn GREEN for iol parametrize branches
- ruff + mypy strict + lint-imports GREEN
- Single atomic commit per D-21
</verification>

<success_criteria>
- iol Phase 8 delivery establishes auth-paquete pattern (Plan 4 higyrus + Plan 5 matriz replicate)
- `test_retry_401_reauth.py[iol]` parametrize branch GREEN: 401 → re-login → 200 = 3 wire requests with FRESH-TOKEN in last Authorization header
- `test_logging_no_token_leak.py[iol]` GREEN: OAuth refresh_token literal scrubbed in URL + JSON formats
- iol existing 70+ tests + Phase 7 baselines preserved
- Single commit `feat(iol): retries + structured logging — RetryTransport, RedactingFilter, 401 re-auth-once, OAuth refresh_token redaction (RELY-01..04, LOG-01..03)` per D-21
</success_criteria>

<output>
Create `.planning/phases/08-retries-backoff-structured-logging/08-03-SUMMARY.md` documenting:
- LOC delta per Phase 8 specifics format: `_transport.py: 0 → ~95 (NEW; mirror Plan 2)`, `_atransport.py: 0 → ~95 (NEW)`, `_logging.py: 0 → ~75 (NEW; +2 refresh_token regex vs Plan 2)`, `_core.py: ~Phase 7 baseline → +endpoint_name field + 6 builders flipped idempotent=True`, `client.py: +~40 LOC (401 re-auth pattern + RetryTransport wiring + 2 kwargs)`, `aio.py: +~40 LOC (mirror + async lock preservation)`, `__init__.py: +3 lines (_logging.attach())`
- Snapshot diff: 2 new kwargs per Client/AsyncClient/configure() signatures
- Cross-cutting guard test status: test_retry_401_reauth.py[iol] now GREEN; test_logging_no_token_leak.py[iol] now GREEN; test_async_cancellation.py[iol] now GREEN
- CR-01 preservation evidence: existing iol tests pass, _ensure_token() body unchanged
- Atomic commit hash
- "iol Plan 3 establishes auth-paquete pattern — Plans 4 (higyrus account_id) and 5 (matriz auth_basic Risk API) replicate with their tweaks"
</output>
