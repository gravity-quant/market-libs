---
phase: 31-endpoints-de-ops-estructura-uniforme
plan: 05
type: tdd
wave: 3
depends_on: ["31-01", "31-04"]
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/src/market_data_client/_core.py
  - packages/market-data-client/src/market_data_client/client.py
  - packages/market-data-client/src/market_data_client/aio.py
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/tests/test_calendar_write.py
  - packages/market-data-client/tests/test_calendar_write_async.py
  - packages/market-data-client/tests/test_core.py
  - packages/market-data-client/tests/test_public_surface_market_data.py
  - main_market_data.py
autonomous: true
requirements: [TYP-02]

must_haves:
  truths:
    - "`market_data_client.add_holidays()` returns `AddHolidaysResult` and `delete_holiday()` returns `DeleteHolidayResult`, on the method AND the module shim, sync AND async — 8 signature sites, none returning an untyped mapping (criterion 1)."
    - "`AddHolidaysResult.days` is `list[CalendarDay]`, REUSING the shipped `CalendarDay` model — the live schema matches it field for field including both `str | None` hour fields. No parallel element model is declared (D-01)."
    - "`DeleteHolidayResult` declares `day: str` and `deleted: bool` — `bool`, not `int`. The committed test mocking an integer contradicts the live capture and is corrected on the TEST side (D-06)."
    - "`parse_calendar_write_response` is SPLIT into `parse_add_holidays_response` → `AddHolidaysResult` and `parse_delete_holiday_response` → `DeleteHolidayResult`; each method names its own parser and both carry `@_decode._response_parser` (D-05). `_core.__all__` swaps one name for two."
    - "Both new parsers PRESERVE the deliberate T-26-13 tolerance of the function they replace — an absent body, a `null`, a list or a scalar all collapse to `Model.from_api(None)`, never a raise. Choosing a raise instead would be a behavior change on a published mutation, which criterion 2's response-only framing does not authorize (G-4, resolved here; direct in-package precedent in `parse_calendar_config_response`)."
    - "The emitted requests for both mutations are UNCHANGED — plan 31-01's raw-bytes v0.4.0 pin is re-run after every task in this plan and stays green. That is criterion 2's proof (D-08)."
    - "The mutating gate is untouched — plan 31-01's AST guard is re-run after every task and stays green, proving `_ensure_mutation_allowed()` is still the first executable statement of all 8 methods on both shells, and that both holiday builders still emit `idempotent=True` (criterion 3, D-20)."
    - "`client.py`'s `add_holidays` docstring no longer claims the builder is marked non-idempotent — a stale claim contradicted by the shipped flag and by plan 31-01's test, and an active trap for criterion 3 (G-6). The `aio.py` counterpart is checked for the same drift."
    - "Both mutation results are re-exported from `__init__.py`, appear in `__all__`, and are added to the hand-maintained `_NEW_PUBLIC_NAMES` tuple, which asserts \"every listed name is importable\" and never \"every exported name is listed\" (G-2)."
    - statement: "`probe_delete_holiday_sync` / `_async` snapshot the PUBLIC result rather than a raw refire, because a refire of a DELETE is legitimately a 404 and would capture the wrong baseline shape — the probe's own docstring says so, and the second DELETE is the D-19 idempotency measurement that this probe exists to make. After typing, that snapshot becomes a declaration echo rather than wire evidence. Resolved as Phase 30's ratified option: accept the blindness, record an FA-09-style carry-forward in the probe docstring, and make the divergence census (not the schema-snapshot diff) the authoritative drift signal for this endpoint in Phase 33. Confirm at execution that the carry-forward note is present and that `add_holidays`, which already refires raw, is unaffected."
      verification: backstop
  prohibitions:
    - "MUST NOT relax, reorder, or conditionally skip `_ensure_mutation_allowed()` — including moving it below the builder call — to make a response-shape assertion pass. Criterion 3 IS the ASVS V4 access-control regression gate for this package, and it is verified by re-running plan 31-01's AST guard after every task in this plan."
    - "MUST NOT change any emitted request byte for either mutation — method, URL, query string, header set, body bytes, or body key order — while presenting the change as response-only. Verified by re-running plan 31-01's raw-bytes pin after every task."
    - "MUST NOT widen, narrow, or otherwise touch `idempotent=` on either holiday builder. Both are `idempotent=True` today, ratified on live row-count measurement (D-20). A reader of the stale `client.py` docstring would conclude the opposite; that docstring is corrected in this plan precisely so the trap is removed."
    - "MUST NOT convert either parser's `{}` tolerance branch into a raise. The tolerance is deliberate (T-26-13) and documented at length in the function being replaced; turning it into a raise is a behavior change on a mutation already published in v0.4.0."
    - "MUST NOT declare `DeleteHolidayResult.deleted` as `int` (or as a union absorbing both) to accommodate the committed mock. The live capture says `bool`; the mock is wrong and is corrected on the test side."
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "AddHolidaysResult (reusing CalendarDay) + DeleteHolidayResult"
      contains: "class AddHolidaysResult"
    - path: "packages/market-data-client/src/market_data_client/_core.py"
      provides: "parse_add_holidays_response + parse_delete_holiday_response, decorated, tolerance preserved"
      contains: "parse_delete_holiday_response"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "AddHolidaysResult.days reuses the shipped CalendarDay rather than a parallel element model"
      pattern: "list\\[CalendarDay\\]"
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "each mutation names its own parser after the split; the gate line and builder call are untouched"
      pattern: "parse_(add_holidays|delete_holiday)_response"
    - from: "packages/market-data-client/tests/test_v040_request_pin.py"
      to: "packages/market-data-client/src/market_data_client/client.py"
      via: "the v0.4.0 request pin re-run after every task is criterion 2's proof"
      pattern: "calendar/holidays"
---

<objective>
Close criterion 1 on the last two endpoints — the two that are already published as mutations
in `market-data-client` v0.4.0 — without moving a single request byte and without perturbing
the mutating gate.

This is the highest-risk plan in the phase and the reason plan 31-01 exists. Everything here is
a RESPONSE-side edit: two models, one shared parser split into two, eight annotations, and
roughly ninety-six lines of committed mocks that pin shapes the live wire contradicts
(`{"created": 1}` against a real `{days, note, saved}`; `{"deleted": 1}` as an integer against
a real `{day: str, deleted: bool}`).

Three guardrails run continuously: plan 31-01's raw-bytes v0.4.0 request pin (criterion 2), its
non-vacuous AST first-statement guard, and its direct builder idempotent-flag assertion
(criterion 3). All three are re-run after every task in this plan, not just at the end.

Purpose: a response-only change on a published mutation is only credible if the request side is
mechanically proven not to have moved. It is, four times over, on both surfaces.

Output: `AddHolidaysResult`, `DeleteHolidayResult`, two decorated parsers preserving their
deliberate tolerance, eight retyped sites, and a corrected `add_holidays` docstring.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-CONTEXT.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-PATTERNS.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-01-SUMMARY.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-04-SUMMARY.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Location |
|---|---|---|
| `market_data_client.models.AddHolidaysResult` | new frozen `SafeModel` — `days: list[CalendarDay]`, `note: str`, `saved: int` | market-data `models.py` |
| `market_data_client.models.DeleteHolidayResult` | new frozen `SafeModel` — `day: str`, `deleted: bool` | market-data `models.py` |
| `parse_add_holidays_response` → `AddHolidaysResult` | **new** parser split off the shared calendar-write one, decorated, tolerance preserved | market-data `_core.py` |
| `parse_delete_holiday_response` → `DeleteHolidayResult` | **new** parser, decorated, tolerance preserved | market-data `_core.py` |
| `parse_calendar_write_response` | **removed** — its `__all__` entry is replaced by the two new names | market-data `_core.py` |
| 8 retyped signatures | `Client`/`AsyncClient` `add_holidays`/`delete_holiday` + their 4 module shims | `client.py`, `aio.py` |
| corrected `add_holidays` docstring | G-6 — the stale non-idempotent claim removed on both shells | `client.py`, `aio.py` |
| 2 new `__all__` entries + import rows | public surface growth | market-data `__init__.py` |
| 2 new `_NEW_PUBLIC_NAMES` entries | hand-maintained surface roster | `tests/test_public_surface_market_data.py` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AddHolidaysResult (reusing CalendarDay) and DeleteHolidayResult</name>

  <reversibility rating="costly">Two models enter `market_data_client`'s published `__all__`
  and become the declared response shape of two mutations already released in v0.4.0.</reversibility>

  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/tests/test_core.py</files>

  <behavior>
    - `AddHolidaysResult.from_api({"days": [{"day": "2099-12-29", "closed": True, "description": "probe", "open_time": None, "close_time": None}], "note": "n", "saved": 1})` populates `.days[0]` as a `CalendarDay` with both hour fields `None`, plus `.note` and `.saved`.
    - `AddHolidaysResult.from_api(None)` yields a zero-valued instance with `.days == []`, and emits a `non_dict` divergence.
    - `AddHolidaysResult.from_api({"days": "not-a-list", ...})` degrades `.days` to `[]` and emits a divergence — the collection guard behaves as the walker defines it, with no model-side override.
    - `DeleteHolidayResult.from_api({"day": "2099-12-29", "deleted": True})` yields `.day == "2099-12-29"` and `.deleted is True`.
    - `DeleteHolidayResult.from_api({"day": "2099-12-29", "deleted": 1})` — the shape the committed mock currently asserts — is decoded per the walker's declared-type policy and its outcome is PINNED by a test, so the integer-versus-boolean divergence is recorded rather than absorbed silently.
    - `.to_dict()` on both round-trips to plain nested dicts.
  </behavior>

  <read_first>
    - `.planning/verification/schemas/market-data-client/add-holidays-sync-response.json` and `add-holidays-async-response.json` (identical — that identity is itself surface-parity evidence), plus `delete-holiday-sync-response.json` and `delete-holiday-async-response.json`. These four captures are the only source of truth for the two field sets (D-01).
    - `packages/market-data-client/src/market_data_client/models.py` lines 608-640 — the shipped `CalendarDay`, which `AddHolidaysResult.days` REUSES. Confirm field-for-field against the capture before declaring anything: `day: str`, `closed: bool`, `description: str`, `open_time: str | None = None`, `close_time: str | None = None`.
    - `packages/market-data-client/src/market_data_client/models.py` — the `SafeModel` base as it stands after plan 31-04 added `to_dict()`. Do not re-add it.
    - `packages/market-data-client/tests/test_decode.py` around lines 1195-1245 — the mapping-carrying-nested-field-type ban and the `from_api`-overriding set equality. `CalendarDay` becomes a nested field type here for the first time; confirm it declares no mapping field and no override, so both preconditions hold.
    - `packages/market-data-client/src/market_data_client/_core.py` around lines 1029-1063 (`parse_calendar_response`) — the existing `days` unwrap and double collection guard, for reference on how a `days` list is handled today.
  </read_first>

  <action>
Edit `packages/market-data-client/src/market_data_client/models.py`. Two model declarations,
nothing else.

`AddHolidaysResult` — `@dataclass(frozen=True, slots=True)` on `SafeModel`, with `days:
list[CalendarDay]`, `note: str`, `saved: int`. **Reuse `CalendarDay`; never declare a parallel
element model.** The live capture's `days[]` items match the shipped `CalendarDay` field for
field, including both `str | None` hour fields — a parallel class would drift from it and would
add a second nested field type to police under the two committed structural tests. Declare it
AFTER `CalendarDay` in the module so the reference resolves.

`DeleteHolidayResult` — `day: str`, `deleted: bool`. `bool`, per the capture. The committed
mock at the assertion site in `test_calendar_write.py` uses an integer; that mock is wrong and
is corrected in Task 3, not accommodated here.

Both get `CalendarDay`'s three-part docstring: (1) the endpoint and the decision reference, and
the fact that this is a MUTATION response typed response-only under criterion 2; (2) the
live-capture provenance sentence naming the committed schema JSON by path, and recording that
the sync and async captures are byte-identical; (3) for `AddHolidaysResult`, a sentence stating
that `days[]` reuses the shipped `CalendarDay` and why (field-for-field match, and the
no-parallel-model rule). Neither model carries `received_at` — a mutation acknowledgement is
not a snapshot and has no staleness dimension. Neither declares a mapping-typed field. Neither
overrides `from_api`; a shape carve-out belongs in the parser, and on a nested model an override
is silently skipped anyway because the walker's nested branch builds with `hint(**walk_model(...))`.

Add the behavior tests listed above to `packages/market-data-client/tests/test_core.py`, built
from the ACTUAL captured payloads rather than hand-written mocks — the point of this phase is
that the declaration answers to the wire.

Give particular attention to the integer-versus-boolean case. Do not assume what the walker does
with an integer arriving for a `bool`-declared field; run it, observe it, and pin the observed
outcome with an assertion plus a one-line comment recording the measurement. Phase 29 already
found one non-obvious widening in this area (an integer arriving for a float-declared matriz
field now widens, pinned by a test rather than prevented), so this is exactly the class of thing
to measure rather than reason about.

After the models land, re-run plan 31-01's two gates before moving on.
  </action>

  <acceptance_criteria>
    - Both classes exist: `grep -cE '^class (AddHolidaysResult|DeleteHolidayResult)\(SafeModel\):' packages/market-data-client/src/market_data_client/models.py` returns 2.
    - `CalendarDay` is reused, not duplicated: `grep -c 'days: list\[CalendarDay\]' packages/market-data-client/src/market_data_client/models.py` returns 1, and the module declares no additional class whose field set matches `CalendarDay`'s (inspect to confirm).
    - `DeleteHolidayResult.deleted` is boolean-declared: `grep -c 'deleted: bool' packages/market-data-client/src/market_data_client/models.py` returns 1.
    - Structural decode invariants hold: `uv run pytest packages/market-data-client/tests/test_decode.py -q` exits 0 (covers both the nested-field-type ban and the overriding-set equality).
    - The integer-versus-boolean outcome is measured and pinned: `uv run pytest packages/market-data-client/tests/test_core.py -k "delete_holiday" -q` exits 0 and the test file carries a comment recording the observed behavior.
    - `uv run pytest packages/market-data-client/tests/test_mutation_gate_ast.py packages/market-data-client/tests/test_v040_request_pin.py -q` exits 0 — plan 31-01's gates still green.
    - `uv run mypy packages/market-data-client/src` exits 0 (mandatory local step, D-13).
    - `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_core.py packages/market-data-client/tests/test_decode.py packages/market-data-client/tests/test_mutation_gate_ast.py packages/market-data-client/tests/test_v040_request_pin.py -q</automated>
  </verify>

  <done>Both models declared from the live captures; `CalendarDay` reused rather than duplicated; `deleted` is boolean; the integer arrival case is measured and pinned; both structural decode assertions and both plan-31-01 gates are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split the calendar-write parser, retype eight sites, correct the stale idempotency docstring</name>

  <reversibility rating="one-way">`add_holidays` and `delete_holiday` shipped in
  `market-data-client` v0.4.0; changing their returns from a mapping to a model breaks any
  external consumer's subscript access at source level. The door is chosen by the ROADMAP phase
  goal and CONTEXT D-01, so no checkpoint re-asks it — the rating is recorded as input to Phase
  34's bump decision, alongside the identical rating from plan 31-04.</reversibility>

  <files>packages/market-data-client/src/market_data_client/_core.py, packages/market-data-client/src/market_data_client/client.py, packages/market-data-client/src/market_data_client/aio.py, packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/tests/test_public_surface_market_data.py</files>

  <behavior>
    - `parse_add_holidays_response` on the captured 200 body returns a populated `AddHolidaysResult`; `parse_delete_holiday_response` returns a populated `DeleteHolidayResult`.
    - Both parsers return the zero-valued model for an absent body, a `null` body, a JSON list, and a JSON scalar — four tolerance branches, none of which raises.
    - Transport errors still flow through `raise_for_response` before any decoding: a 422 raises `MarketDataAPIError` from both parsers.
    - `market_data_client.add_holidays(...)` returns `AddHolidaysResult`; `delete_holiday(...)` returns `DeleteHolidayResult`; both `aio` mirrors likewise.
    - Both model names are importable from `market_data_client` and present in `__all__`.
    - Plan 31-01's AST guard and request pin remain green after every edit in this task.
  </behavior>

  <read_first>
    - `packages/market-data-client/src/market_data_client/_core.py` lines 1083-1114 — the current `parse_calendar_write_response` IN FULL, including its long docstring. That docstring argues at length that the tolerance is deliberate (T-26-13) and that this is why it is a separate function rather than a reuse of the health parser. Read it before replacing it; the argument carries forward into both replacements.
    - `packages/market-data-client/src/market_data_client/_core.py` lines 1066-1080 — `parse_calendar_config_response`, the in-package precedent for resolving an empty body to `Model.from_api(None)`. This is the shape both new parsers take.
    - `packages/market-data-client/src/market_data_client/_core.py` lines 88-100 — the `__all__` block where `parse_calendar_write_response` sits.
    - `packages/market-data-client/src/market_data_client/client.py` lines 676-730 — both gated mutation methods in full. Note the four-line body shape: gate call, builder call, `self._request(spec)`, parser call. Only the LAST line and the return annotation change.
    - `packages/market-data-client/src/market_data_client/client.py` line 684 area — the `add_holidays` docstring's claim about the builder's retry flag, which contradicts the shipped value and is corrected here (G-6).
    - `packages/market-data-client/src/market_data_client/aio.py` lines 685-740 — the async mirrors, and the docstring around line 690 which must be checked for the same drift.
    - `packages/market-data-client/src/market_data_client/client.py` lines 945-960 and `aio.py` lines 951-966 — the four module shims.
    - `packages/market-data-client/tests/test_public_surface_market_data.py` lines 30-75 — `_NEW_PUBLIC_NAMES` and `_MUTATION_METHODS`.
    - `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-01-SUMMARY.md` — what the AST guard and the request pin actually assert, so you can predict what a stray edit would trip.
  </read_first>

  <action>
**(a) `_core.py` — split into two decorated parsers, tolerance preserved (D-05, G-4 resolved).**

Replace `parse_calendar_write_response` with two functions, `parse_add_holidays_response`
returning `AddHolidaysResult` and `parse_delete_holiday_response` returning
`DeleteHolidayResult`. The live shapes of the two endpoints are unrelated, which is why one
function can no longer serve both.

Each follows `parse_calendar_config_response`'s shape:

- `@_decode._response_parser` on the `def` line — leading underscore intact, never aliased,
  never the inline scope-context form.
- `resp.read()` then `raise_for_response(resp)` then decode. Never reorder — Phase 7 D-06
  HTTP/2 safety, honoured by every parser in this file.
- **Both `{}` branches of the replaced function become `Model.from_api(None)`.** This is the
  G-4 decision, resolved here in favour of preserving the tolerance: an absent body, a `null`,
  a list and a scalar all collapse to the zero-valued instance, never a raise. The alternative
  — raising on an unexpected shape, as the health parsers now do — would be a BEHAVIOR CHANGE
  on a mutation already published in v0.4.0, which criterion 2's response-only framing does not
  authorize. There is direct in-package precedent one function above. Record this reasoning in
  each new docstring, carrying forward the T-26-13 tolerance argument from the function being
  replaced and noting explicitly that the replacements' contract is the same tolerance, now
  expressed as a typed zero-valued model instead of an empty mapping.
- Otherwise `Model.from_api(raw)`.

Import both models into `_core.py` from `market_data_client.models`. In `_core.__all__`, remove
`"parse_calendar_write_response"` and insert `"parse_add_holidays_response"` and
`"parse_delete_holiday_response"` in sort order. Confirm no other caller of the removed name
remains anywhere: grep the whole repo including `verification/`, `tests/`, and `main_*.py`.

**(b) `client.py` and `aio.py` — eight sites, and the gate untouched by construction.**

For each of the four methods (`add_holidays` and `delete_holiday` on both shells), exactly two
things change: the return annotation, and the parser name on the final line. The
`self._ensure_mutation_allowed()` first statement, the builder call, and `self._request(spec)`
are byte-unchanged. Do not reflow those lines, do not reorder them, do not "tidy" the method.
Then the four module shims get the matching annotations; their bodies do not change.

**(c) G-6 — correct the stale idempotency claim.** `client.py`'s `add_holidays` docstring
asserts that its builder is marked non-idempotent and is therefore not retried, and calls it the
only such spec in the package. Both holiday builders carry the opposite flag today, corrected in
Phase 27 on live row-count measurement (two identical POSTs left exactly one row; the endpoint
upserts by date), and `build_add_holidays_request`'s own docstring says no builder in the package
carries the old flag any more. Rewrite the paragraph to state the current, measured behavior and
cite the phase that established it. Check `aio.py`'s counterpart for the same drift and correct
it identically. Leave the 1-500 batch-bound sentence and the path-safety sentence intact — both
still describe real, live guards on the request path.

Do NOT edit either builder. Criterion 3 forbids it, and plan 31-01's direct flag assertion would
fail if you did — which is exactly the trap this docstring was setting.

While reading `main_market_data.py` in Task 3, you will find the same stale flag name inside the
Phase-27 live-measurement verdict text (`_emit_add_holidays_idempotency_verdict` and the finding
strings it emits). That prose is a RECORD of the measurement that produced the correction, not a
claim about the current client contract, and editing it would change the driver's emitted
findings. Scope it OUT of this phase deliberately and record that decision in the SUMMARY — do
not leave it silently unmentioned, and do not let it seed a builder edit.

**(d) Exports and the hand-maintained roster.** Add `AddHolidaysResult` and
`DeleteHolidayResult` to the models import block and to `__all__` in `__init__.py`, in ASCII
sort position among the capitalized names. Add the same two to `_NEW_PUBLIC_NAMES` in
`packages/market-data-client/tests/test_public_surface_market_data.py`, under the same
`# Phase 31 — ops endpoints (TYP-02)` comment plan 31-04 introduced. Do not touch
`_MUTATION_METHODS` — the method roster is unchanged by this phase and plan 31-01's AST guard
asserts it by equality.

Re-run plan 31-01's two gates after this task. If the request pin fails, a request byte moved
and the change is no longer response-only — stop and diagnose rather than adjusting the pin.
  </action>

  <acceptance_criteria>
    - Two decorated parsers exist: `grep -B1 -E '^def parse_(add_holidays|delete_holiday)_response' packages/market-data-client/src/market_data_client/_core.py` shows `@_decode._response_parser` above BOTH.
    - The shared parser is gone from every caller — `grep -rn 'parse_calendar_write_response' --include='*.py' . | grep -v __pycache__` returns no results.
    - `__all__` swapped one name for two: `grep -c '"parse_add_holidays_response"' packages/market-data-client/src/market_data_client/_core.py` returns 1 and the same for `"parse_delete_holiday_response"`.
    - Tolerance preserved on both: `uv run pytest packages/market-data-client/tests/test_core.py -k "add_holidays or delete_holiday" -q` exits 0 with assertions covering all four tolerance branches (absent body, null, list, scalar) on each parser, none of which raises.
    - All eight sites retyped: `grep -cE 'def (add_holidays|delete_holiday)\(self, .*\) -> (AddHolidaysResult|DeleteHolidayResult):' packages/market-data-client/src/market_data_client/client.py` returns 2, `grep -cE '^def (add_holidays|delete_holiday)\(.*\) -> (AddHolidaysResult|DeleteHolidayResult):' packages/market-data-client/src/market_data_client/client.py` returns 2, and the two `async def` equivalents in `aio.py` return 2 each.
    - The gate lines are byte-unchanged: `git diff packages/market-data-client/src/market_data_client/client.py packages/market-data-client/src/market_data_client/aio.py` shows no changed line containing `_ensure_mutation_allowed`, and no changed line containing `build_add_holidays_request` or `build_delete_holiday_request`.
    - Exports landed: `uv run python -c "import market_data_client as m; [m.__all__.index(n) for n in ('AddHolidaysResult','DeleteHolidayResult')]"` exits 0, and `grep -cE '"(AddHolidaysResult|DeleteHolidayResult)"' packages/market-data-client/tests/test_public_surface_market_data.py` returns 2.
    - The stale claim is gone from both shells: read the `add_holidays` docstring in `client.py` and in `aio.py` and confirm neither states that the builder is excluded from retries, and that each names the phase whose live measurement established the current flag.
    - **Criterion 2 and 3 proof:** `uv run pytest packages/market-data-client/tests/test_v040_request_pin.py packages/market-data-client/tests/test_mutation_gate_ast.py -q` exits 0 with all tests passing, AND `git diff packages/market-data-client/tests/test_v040_request_pin.py packages/market-data-client/tests/test_mutation_gate_ast.py` is empty — the gates passed without being adjusted.
    - `uv run mypy packages/market-data-client/src` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_v040_request_pin.py packages/market-data-client/tests/test_mutation_gate_ast.py packages/market-data-client/tests/test_core.py -q &amp;&amp; uv run mypy packages/market-data-client/src</automated>
  </verify>

  <done>One shared parser became two decorated ones with the T-26-13 tolerance preserved as `from_api(None)`; eight sites retyped with the gate lines and builder calls byte-unchanged; the stale idempotency claim corrected on both shells and the driver's Phase-27 verdict prose explicitly scoped out; both exports and both roster entries landed; plan 31-01's gates passed unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-mock the two calendar-write test files, fix the driver result reads, run the phase gate</name>

  <reversibility rating="reversible">Test re-mocks and two driver call-site edits; both follow
  mechanically from the source change and revert with it.</reversibility>

  <files>packages/market-data-client/tests/test_calendar_write.py, packages/market-data-client/tests/test_calendar_write_async.py, main_market_data.py</files>

  <behavior>
    - Every calendar-write mock for the two holiday endpoints uses the live shapes: `{"days": [...], "note": ..., "saved": ...}` for the add, `{"day": ..., "deleted": true}` for the delete.
    - Every assertion on a holiday result is attribute access on a model, not a mapping comparison.
    - The refusal matrix, the host-mismatch test, and the path-safety test are semantically unchanged and still assert zero HTTP requests on refusal.
    - The three config-trio tests in the same files are untouched — they parse through `parse_calendar_config_response`, which this phase does not modify.
    - `main_market_data.py`'s two `add_holidays` probes report a key count derived from the model's wire projection rather than from the model object.
  </behavior>

  <read_first>
    - `packages/market-data-client/tests/test_calendar_write.py` IN FULL, and `test_calendar_write_async.py` IN FULL. RESEARCH counted 49 and 47 matching lines respectively; the assertion sites that contradict the live wire are around lines 258-272 (the add, mocking a `created` count) and 345-359 (the delete, mocking an integer and asserting a mapping equality).
    - `packages/market-data-client/tests/test_calendar_write.py` lines 400-470 and `test_calendar_write_async.py` lines 380-425 — the refusal matrix asserting zero requests on refusal for all five gated methods including the dry-run preview. These are semantically load-bearing; re-mock around them, never through them.
    - `.planning/verification/schemas/market-data-client/add-holidays-sync-response.json` and `delete-holiday-sync-response.json` — the shapes the re-mocks must adopt.
    - `main_market_data.py` lines 2347-2395 (`probe_add_holidays_sync`) and 2559-2600 (`probe_add_holidays_async`) — the two probes whose PASS detail counts keys on the public result. Note the probe ALREADY refires the raw wire for the schema snapshot, so its snapshot path is correct and needs no change.
    - `main_market_data.py` lines 2474-2545 (`probe_delete_holiday_sync`) and 2659-2720 (`probe_delete_holiday_async`) — read the docstring explaining why the snapshot uses the PUBLIC result of the first delete rather than a refire (a refire is legitimately a 404 and would capture an error shape as the baseline; the second delete is the D-19 idempotency measurement).
    - `main_iol.py` around line 336 (`_capture_raw_wire`) and STATE.md's Phase 30 decision that the divergence census, not the schema-snapshot diff, is the authoritative drift signal for modelled endpoints — the precedent for the carry-forward note this task adds.
  </read_first>

  <action>
**(a) Re-mock, mechanically, following Phase 30-03's ratified precedent.** The mismatch is
corrected on the TEST side; no parser guard and no tolerance branch is loosened to accommodate a
mock.

In both calendar-write files, replace every holiday-endpoint response body with the live shape
from the committed captures, and convert every assertion on the returned value from a mapping
comparison to attribute access on the corresponding model — `.saved`, `.note`, `.days[0].day`,
`.days[0].closed`, `.deleted`, `.day`. The delete assertion that currently compares against a
mapping containing an integer is the sharpest case: the live wire sends a boolean, so both the
mock and the assertion change.

Leave the three config-trio tests alone — they route through `parse_calendar_config_response`,
which this phase does not touch. Leave the refusal matrix, the host-mismatch test and the
path-safety test semantically intact; if a mock body needs updating inside them, update the body
only and never the zero-request assertion. Those are the behavioral half of the ASVS V4 gate.

Add, in both files, at least one assertion that an empty 200 body degrades to the zero-valued
model rather than raising — the T-26-13 tolerance, now expressed through the type.

**(b) Driver result reads.** `probe_add_holidays_sync` and `probe_add_holidays_async` build
their PASS detail with a key count taken from the public result. After typing, that result is a
frozen slots dataclass and the count call fails. Use the `to_dict()` escape hatch — the reason
D-03 put it on the base — so the reported number stays a key count of the wire projection.
Change nothing else in those probes: their raw refire for the schema snapshot is already
correct and stays as-is.

Then check both `delete_holiday` probes. Their snapshot feeds the PUBLIC result, so after typing
it becomes a model projection — a snapshot that is a function of the declaration rather than the
wire, and therefore blind to a type change, an added key and a removed key. Unlike the add case
this cannot be fixed with a refire: a second DELETE is legitimately a 404 and would capture an
error shape as the baseline, and it is also the D-19 idempotency measurement the probe exists to
make. Resolve it as Phase 30 ultimately resolved the equivalent fork for iol: keep the projection,
and add an explicit FA-09-style carry-forward note to each probe docstring stating that for this
endpoint the authoritative drift signal in Phase 33 is the divergence census, NOT the
schema-snapshot diff, and naming `main_iol.py`'s `_capture_raw_wire` rationale as the precedent.
Feed `to_dict()` at that site so the snapshot at least remains a mapping of the declared shape,
and make the note unmissable — a future reader who sees a clean snapshot must know why that is
not evidence.

Scan the rest of `main_market_data.py` for any other read of a holiday result and treat each on
its own merits; plan 31-03 found the higyrus driver needed no change at all, so symmetry is not a
safe assumption in either direction.

**(c) Phase gate.** Run the full local suite INCLUDING `verification/`, both mypy invocations,
both `tools/` gates, and ruff. The full-suite run is not optional here: CI never executes
`verification/`, so the surface goldens are only ever checked locally.
  </action>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q` exits 0, and the combined market-data + higyrus count is at least the measured pre-change baseline of 699.
    - No holiday assertion compares against a mapping literal any more: `grep -cE '(add_holidays|delete_holiday)\([^)]*\)\s*==\s*\{' packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py` returns 0.
    - The live shapes are in the mocks: `grep -c '"saved"' packages/market-data-client/tests/test_calendar_write.py` returns at least 1, and `grep -c '"deleted": True' packages/market-data-client/tests/test_calendar_write.py` returns at least 1. Same for the async file.
    - The refusal matrix is semantically intact: `grep -c 'get_requests() == \[\]' packages/market-data-client/tests/test_calendar_write.py` returns the same count as before this task (record the before-count first).
    - The driver's add probes use the wire projection: `awk '/def probe_add_holidays_sync/,/def probe_calendar_after_holiday_sync/' main_market_data.py | grep -c 'to_dict()'` returns at least 1, and the async probe block likewise.
    - Both delete probes carry the carry-forward note: `awk '/def probe_delete_holiday_sync/,/^def /' main_market_data.py | grep -ci 'censo\|census'` returns at least 1, and the same for the async probe.
    - **Criterion 2 and 3 final proof:** `uv run pytest packages/market-data-client/tests/test_v040_request_pin.py packages/market-data-client/tests/test_mutation_gate_ast.py -q` exits 0 AND `git diff packages/market-data-client/tests/test_v040_request_pin.py packages/market-data-client/tests/test_mutation_gate_ast.py` is empty across this entire plan.
    - **Phase gate:** `uv run pytest -q` (full local suite including `verification/`) exits 0; `uv run mypy` exits 0; `uv run mypy packages/market-data-client/src` exits 0; `uv run python tools/check_decode_intactness.py` exits 0; `uv run python tools/check_uniform_structure.py` exits 0; `uv run ruff check . && uv run ruff format --check .` exits 0.
    - No untyped-mapping return remains on any of the five endpoints: inspect the five method definitions and their five module shims across both packages and confirm each names a model class.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run mypy &amp;&amp; uv run mypy packages/market-data-client/src &amp;&amp; uv run python tools/check_uniform_structure.py &amp;&amp; uv run python tools/check_decode_intactness.py</automated>
  </verify>

  <done>Roughly ninety-six lines of contradicted mocks are re-mocked to the live shapes with attribute-access assertions; the refusal matrix is semantically untouched; both add probes use the wire projection and both delete probes carry an explicit drift-blindness carry-forward; the v0.4.0 request pin and the AST guard passed unmodified across the whole plan; the full phase gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `add_holidays` / `delete_holiday` | A mutation crosses from in-process intent to a real calendar write. `_ensure_mutation_allowed()` is the sole authorization point and must remain the first executable statement. |
| client → market-data API (develop vs prod) | The `expected_host` exact-match leg decides which environment receives a write. |
| `day` argument → URL path segment | Caller-supplied string interpolated into a path; guarded by an allow-list regex plus all-dots rejection in the builder. |
| published v0.4.0 wire contract → this plan's diff | The request side is a released contract. This plan is response-only, and that claim is mechanical rather than asserted. |

## STRIDE Threat Register (ASVS L1, block_on = high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-24 | Tampering / EoP | `_ensure_mutation_allowed()` ordering, while editing the very methods it guards | mitigate | ASVS V4. Plan 31-01's non-vacuous AST guard is re-run after EVERY task in this plan, and an acceptance criterion additionally requires that no changed line in the `client.py`/`aio.py` diff contains the gate call. The guard must pass WITHOUT being modified — its own `git diff` is required to be empty. |
| T-31-25 | Tampering | Request emission drifting under a "response-only" change | mitigate | ASVS V13. Plan 31-01's raw-bytes v0.4.0 pin is re-run after every task, covering method, URL, query string, full header set, and body bytes including key order. Its `git diff` must be empty — a green pin that was adjusted proves nothing. |
| T-31-26 | DoS / Tampering | Retry amplification via a changed idempotency flag | mitigate | ASVS V4. Both builders stay `idempotent=True` (D-20, ratified on live row-count measurement). Enforced three ways: plan 31-01's direct flag assertion, an acceptance criterion forbidding any changed diff line naming either builder, and the G-6 docstring correction that removes the stale claim which would otherwise seed the wrong edit. |
| T-31-27 | Tampering / EoP | Path-segment retargeting via `day` | transfer | ASVS V5. The builder's allow-list regex and all-dots rejection are untouched by a response-only change, and the request pin's URL literal transitively proves the emitted path. The behavioral path-safety test in the calendar-write files is required to stay semantically intact. |
| T-31-28 | Repudiation | Tolerance silently becoming a raise on a published mutation | mitigate | ASVS V13. G-4 is resolved explicitly toward preserving the T-26-13 tolerance as `Model.from_api(None)`, with in-package precedent; named as a prohibition; and covered by an acceptance criterion requiring all four tolerance branches asserted per parser. |
| T-31-29 | Repudiation | Drift-blind schema snapshot at the two delete probes | accept | ASVS V8. A raw refire is impossible here — a second DELETE is legitimately a 404 and is also the D-19 measurement. Accepted with a mandatory, explicit carry-forward note in both probe docstrings routing Phase 33 to the divergence census as the authoritative signal, mirroring Phase 30's ratified resolution for iol. `schema_of` remains value-blind either way, so no data-protection property is lost. |
| T-31-30 | Info Disclosure | Credentials or wire values in test literals and findings | accept | ASVS V7 / CLAUDE.md C-4. Re-mocks use the conftest-seeded token and the committed capture shapes; the driver's finding text carries counts and statuses, never payload values, and `RedactingFilter` remains attached per package. |
| T-31-SC | Tampering | package-manager installs | mitigate | Not applicable — zero installs; `uv.lock` unchanged; `uv lock --check` is the first `lint` step. RESEARCH § Package Legitimacy Audit records the empty roster. |

**Load-bearing note:** criterion 3 IS this package's ASVS V4 regression gate. It is never
relaxed to make a response-shape test pass — the test is re-mocked instead. If the AST guard or
the request pin goes red in this plan, the correct response is to diagnose the source edit, not
to adjust the gate.
</threat_model>

## Flagged Assumptions (probe-surfaced, unresolved — not dismissed)

- **TYP-02 / concurrency / unresolved** — probe: *"If interrupted or run in parallel, what is
  guaranteed?"* Not dismissed, not fabricated. Manual reading applied while planning: this plan
  is the one place in the phase where the question has real teeth, because it touches two
  mutations. What is actually guaranteed today is a property of the SERVER and of the builders,
  neither of which this plan changes — `POST /calendar/holidays` upserts by date (measured live
  in Phase 27: two identical POSTs left exactly one row, which is why both builders carry
  `idempotent=True`), and a repeated DELETE is idempotent in state though possibly not in status
  (a second DELETE may return 404, recorded by the driver's D-19 verdict). An interrupted call
  therefore leaves server state governed by those semantics regardless of the response type.
  That is context, not resolution — the probe row asks a broader question than this evidence
  answers. **The probe row remains open.**

<verification>
```
uv run pytest packages/market-data-client -q
uv run pytest packages/market-data-client/tests/test_v040_request_pin.py \
              packages/market-data-client/tests/test_mutation_gate_ast.py -q
git diff packages/market-data-client/tests/test_v040_request_pin.py \
         packages/market-data-client/tests/test_mutation_gate_ast.py   # MUST be empty

# Phase gate
uv run pytest -q                                   # full local suite incl. verification/
uv run mypy
uv run mypy packages/market-data-client/src        # local-only, D-13
uv run python tools/check_decode_intactness.py
uv run python tools/check_uniform_structure.py
uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `AddHolidaysResult` reuses the shipped `CalendarDay`; `DeleteHolidayResult.deleted` is boolean.
- One shared parser became two decorated ones with the T-26-13 tolerance preserved as `from_api(None)`; the removed name has no remaining caller anywhere in the repo.
- All 8 signature sites retyped with the gate lines and builder calls byte-unchanged.
- The stale idempotency claim is corrected on both shells; the driver's Phase-27 verdict prose is explicitly scoped out and recorded, not silently left.
- Both re-mocked calendar-write files are green with attribute-access assertions and the refusal matrix semantically intact.
- Both add probes use the wire projection; both delete probes carry an explicit drift-blindness carry-forward.
- Plan 31-01's request pin and AST guard passed across the whole plan WITHOUT being modified.
- The full phase gate is green: full local suite including `verification/`, both mypy runs, both `tools/` gates, ruff check and format.
- Across both packages, all five endpoints and all five module shims name a model class in their return annotation — criterion 1 closed.
</success_criteria>

<output>
Create `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-05-SUMMARY.md` when done.
Record, prominently:
1. Confirmation that plan 31-01's request pin and AST guard passed with an EMPTY `git diff` across all three tasks — this is criterion 2 and criterion 3's evidence.
2. The measured behavior of an integer arriving for the boolean-declared `deleted` field.
3. The G-4 resolution and its rationale, plus the explicit out-of-scope decision on the driver's Phase-27 idempotency verdict prose.
4. The delete-probe drift-blindness carry-forward, phrased so Phase 33 picks it up.
5. A closing table of all five endpoints × two surfaces × (method, shim) with the model each now returns — the criterion-1 completion record.
</output>
