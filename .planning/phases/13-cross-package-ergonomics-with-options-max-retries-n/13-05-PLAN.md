---
phase: 13-cross-package-ergonomics-with-options-max-retries-n
plan: 05
type: execute
wave: 5
depends_on: [13-04]
files_modified:
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
  - verification/snapshots/iol-client-surface.txt
autonomous: true
requirements: [ERG-01]
requirements_addressed: [ERG-01]

must_haves:
  truths:
    - "iol_client.Client.with_options(max_retries=N) returns view sharing _state (token + refresh_token + token_expires_at)"
    - "iol_client.AsyncClient.with_options async mirror"
    - "view.close() / view.aclose() no-op — parent's http_client + cached OAuth token intact (no re-auth)"
    - "401 re-auth path in Client._request() shell remains UNCHANGED (B5: refresh_token flow on 401)"
    - "view's max_attempts override propagates through _request shell extensions + _send_auth_request"
    - "All 4 cross-cutting test_with_options_*[iol_client] rows GREEN at end of plan"
    - "Phase 13 final green gate PASSES: uv run pytest full monorepo ≥ 907 passing (v1.1 baseline preserved per SC#5 ROADMAP)"
    - "Phase 13 final green gate PASSES: uv run ruff check + ruff format --check + mypy --strict + pre-commit run --all-files all exit 0"
    - "iol Plan 5 last in serial because Phase 14 SEC-01 (disk persistence) coexists with view via shared _state"
  artifacts:
    - path: "packages/iol-client/src/iol_client/client.py"
      provides: "Client.with_options + _is_view + close() no-op + _request + _send_auth_request extensions['max_attempts']"
      contains: "def with_options"
    - path: "packages/iol-client/src/iol_client/aio.py"
      provides: "AsyncClient.with_options + async mirrors"
      contains: "def with_options"
    - path: "packages/iol-client/tests/test_client.py"
      provides: "Per-package mocked tests + 401-re-auth-with-view test (iol-specific)"
    - path: "packages/iol-client/tests/test_async_client.py"
      provides: "Async mirror tests"
    - path: "verification/snapshots/iol-client-surface.txt"
      provides: "Snapshot regenerated (D-P4 atomicity)"
  key_links:
    - from: "view.get_quote(...) (idempotent GET)"
      to: "RetryTransport.handle_request (reads extensions['max_attempts']=view._max_retries+1)"
      via: "_request shell extension propagation"
      pattern: "extensions\\[\"max_attempts\"\\] = self\\._max_retries \\+ 1"
    - from: "Client._request 401 branch"
      to: "_ensure_token (re-auth using parent's _state.refresh_token)"
      via: "shared _state — view's 401 triggers parent's refresh_token flow"
      pattern: "_ensure_token"
---

<objective>
Land `with_options(max_retries=N)` on `iol_client.Client` and `iol_client.AsyncClient` — the LAST package in the per-package serial roll-out, AND run the Phase 13 consolidated green gate (full monorepo pytest, ruff, mypy strict, pre-commit).

Purpose: iol is last in the serial because Phase 14 SEC-01 (IOL refresh_token disk persistence) will coexist with the view via shared `_state` — the view shares `state.refresh_token` and `state.token`, and Phase 14 will introduce `state.token_cache_path` (or similar) that ALSO needs to be shared with views. iol-last positioning lets Phase 14 planning see iol's view shape in concrete form before designing the disk-cache integration. Per STATE.md decisions: "ERG-01 (Phase 13) uses ámbito → higyrus → matriz → iol (iol LAST because it interacts with SEC-01 disk cache in Phase 14)".

Additional Plan 5 responsibility: **Phase 13 final green gate** (D-P1 consolidates Plan 5 final gate; no Plan 6 separate). On HEAD post-Plan-5:
- `uv run pytest` (full monorepo) reports ≥ 907 passing (SC#5 ROADMAP — v1.1 baseline preserved).
- `uv run ruff check` exits 0.
- `uv run ruff format --check` exits 0.
- `uv run mypy --strict` exits 0 across all 4 packages.
- `pre-commit run --all-files` exits 0 (idempotent).
- (If applicable) `uv run lint-imports` exits 0.

Output: iol sync + async `with_options` + per-package mocked tests + 401-re-auth-with-view iol-specific test + snapshot regen + consolidated green gate PASSING.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-03-SUMMARY.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-04-SUMMARY.md
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/_state.py
@verification/test_with_options.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Client.with_options + AsyncClient.with_options to iol (sync + async)</name>
  <files>
    packages/iol-client/src/iol_client/client.py,
    packages/iol-client/src/iol_client/aio.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md (ámbito reference)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-03-SUMMARY.md (higyrus reference — _send_auth_request extension write)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-04-SUMMARY.md (matriz reference — TokenStore isolation does NOT apply to iol per D-T4)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-V1..V5; D-T4 explicitly EXCLUDES iol from `client_max_retries` field — iol does not have a parametrized TokenStore)
    packages/iol-client/src/iol_client/client.py (current Client shape: __slots__, __init__, _request 291-326 — note `idempotent`/`request_id`/`endpoint_name` extensions at lines 321-323; 401 re-auth branch around lines 280-345 — view's 401 path needs no change because view shares `_state` and `_ensure_token()` already uses `self._state.refresh_token`)
    packages/iol-client/src/iol_client/aio.py (current AsyncClient shape)
  </read_first>
  <behavior>
    - `c = iol_client.Client(); view = c.with_options(max_retries=5)` returns fresh `Client`.
    - `view._state is c._state` AND `view._state.token is c._state.token` AND `view._state.refresh_token is c._state.refresh_token`.
    - `view.close()` is no-op.
    - `view.get_quote("GGAL")` (idempotent GET) works; under 503 with view's `max_retries=10`, produces 11 outgoing requests.
    - `view.get_quote(...)` returning 401: triggers `_ensure_token()` re-auth via parent's `state.refresh_token` (shared) → retry call ONCE with new token (Phase 6/8/9 401-re-auth path UNCHANGED).
    - `view.<mutating_endpoint>(...)` (if any) under 503: mutation gate forces 1 outgoing request (Phase 8 D-01).
    - Async mirror identical.
    - D-T4: iol does NOT get `client_max_retries` field. No `_state.py` change.
  </behavior>
  <action>
    Mirror Plans 2-3 exactly. No matriz-specific TokenStore field (per D-T4: "YAGNI: solo matriz tiene `build_token_store(state, max_retries=...)` parametrizado").

    **`packages/iol-client/src/iol_client/client.py`**:

    1. Add `_is_view` to `Client.__slots__` (alphabetically sorted).

    2. Set `self._is_view = False` in `__init__` after `self._max_retries = max_retries`.

    3. Add `Client.with_options` method:

       - Signature: `def with_options(self, *, max_retries: int) -> Self:`
       - Body: standard 6-line view constructor pattern (per `<specifics>` View constructor pattern).
       - Docstring: ~15 lines covering standard semantics + iol-specific note: "On iol, calling `view.get_quote(...)` shares the cached OAuth `_state.token` and `_state.refresh_token` with the parent. A 401 from the view triggers `_ensure_token()` which uses the parent's `_state.refresh_token` to re-auth — there is NO per-view refresh-token tracking. This means view's behavior under OAuth refresh is identical to the parent's." Mention forward reference to Phase 14 SEC-01: when disk persistence lands, the view will continue sharing `_state` (and therefore the disk-cached refresh_token) without modification.

    4. Modify `Client.close()` to no-op when `_is_view=True`.

    5. Modify `Client._request()` shell (lines 291-326): add `req.extensions["max_attempts"] = self._max_retries + 1` after `req.extensions["endpoint_name"] = spec.endpoint_name` (line 323). The existing 401 re-auth branch (around lines 280-345 per grep evidence) needs NO change — view's `_request` path is identical to parent's. The 401 retry happens INSIDE the same `_request` method using the shared `_state.refresh_token`.

    6. Modify `Client._send_auth_request()` (around lines 227-248 per grep): add `req.extensions["max_attempts"] = self._max_retries + 1` after `req.extensions["endpoint_name"]` (line 248). Per D-T6: auth requests are `idempotent=True` so override is honored.

    7. Modify `__repr__` to prefix `"view of "` when `_is_view=True`. iol repr redacts username/password/token (Phase 6 D-18) — preserve.

    8. DO NOT change `__all__`. DO NOT add new imports.

    **`packages/iol-client/src/iol_client/aio.py`** (mirror sync):

    1-7. Same shape as sync. Async `_request` + async `_send_auth_request` get the extension write.

    8. DO NOT change `__all__`. DO NOT add new imports.

    Constraint: total diff to `client.py` ≤ +35 LOC, `aio.py` ≤ +30 LOC.

    Run `uv run --package iol-client pytest packages/iol-client/tests/ -x -q` to confirm Phase 6/8/9 iol tests stay GREEN — especially the 401-re-auth tests (these prove view's 401 path is identical to parent's via shared `_state.refresh_token`).
  </action>
  <verify>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/iol-client/</automated>
    <automated>uv run mypy --strict packages/iol-client/src</automated>
    <automated>uv run pytest verification/test_with_options.py -k "iol_client" -x -q</automated>
    <automated>uv run pytest verification/test_retry_401_reauth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def with_options" packages/iol-client/src/iol_client/client.py` returns 1.
    - Source: `grep -c "def with_options" packages/iol-client/src/iol_client/aio.py` returns 1.
    - Source: `grep -c "_is_view" packages/iol-client/src/iol_client/client.py` returns ≥ 4.
    - Source: `grep -c "_is_view" packages/iol-client/src/iol_client/aio.py` returns ≥ 4.
    - Source (extension set in _request + _send_auth_request sync): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/iol-client/src/iol_client/client.py` returns ≥ 2.
    - Source (extension set in _request + _send_auth_request async): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/iol-client/src/iol_client/aio.py` returns ≥ 2.
    - Slot correctness: `python -c "from iol_client import Client, AsyncClient; assert '_is_view' in Client.__slots__ and '_is_view' in AsyncClient.__slots__"` exits 0.
    - D-T4 negative (iol does NOT get client_max_retries field): `grep -c "client_max_retries" packages/iol-client/src/iol_client/_state.py` returns 0.
    - D-T4 negative: `grep -c "client_max_retries" packages/iol-client/src/iol_client/client.py packages/iol-client/src/iol_client/aio.py` returns 0.
    - Behavior (Phase 6/8/9 iol tests still GREEN): `uv run --package iol-client pytest packages/iol-client/tests/ -x -q exits 0`.
    - Behavior (401 re-auth path UNCHANGED): `uv run pytest verification/test_retry_401_reauth.py -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py -k "iol_client" -x -q exits 0` (3 of 3 iol parametrize rows GREEN).
    - Quality: `uv run ruff check packages/iol-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/iol-client/src exits 0`.
  </acceptance_criteria>
  <done>
    iol `Client.with_options` + `AsyncClient.with_options` added. `_is_view` slot, lifecycle no-op, `_request` + `_send_auth_request` shells (sync + async) set `extensions["max_attempts"]`. D-T4 confirmed: NO `client_max_retries` field in iol `_state.py`/`client.py`/`aio.py`. 401 re-auth path unchanged. Phase 6/8/9 iol tests GREEN. All 3 cross-cutting tests for iol row GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-package mocked tests + iol-specific 401-re-auth-with-view test + snapshot regen</name>
  <files>
    packages/iol-client/tests/test_client.py,
    packages/iol-client/tests/test_async_client.py,
    verification/snapshots/iol-client-surface.txt
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P3 + D-P4)
    packages/higyrus-client/tests/test_client.py (Plan 3 reference for mocked-test patterns)
    packages/iol-client/tests/test_client.py (existing 401-re-auth test patterns — analog for new test)
    packages/iol-client/tests/test_async_client.py (existing async patterns)
    verification/snapshots/iol-client-surface.txt (current snapshot)
    verification/test_retry_401_reauth.py (Phase 6/8 401-re-auth cross-cutting — analog)
  </read_first>
  <behavior>
    - Per-package mocked tests in `test_client.py` / `test_async_client.py` (5 sync + 5 async standard).
    - One NEW iol-specific test: `test_with_options_view_401_triggers_reauth_via_shared_refresh_token` — under view's GET that returns 401, `_ensure_token()` is called and uses parent's `state.refresh_token` (mock httpx for both the initial 401 + the refresh response + the retry).
    - Snapshot regenerated; body diff zero.
  </behavior>
  <action>
    1. **Append mocked tests to `packages/iol-client/tests/test_client.py`** (mirror Plan 3 set + 1 iol-specific):

       - `test_with_options_close_is_noop`
       - `test_with_options_exit_is_noop`
       - `test_with_options_chaining_inner_wins_local`
       - `test_with_options_repr_shows_view_prefix`
       - `test_with_options_invalid_max_retries_raises_value_error`
       - **NEW (iol-specific)** `test_with_options_view_401_triggers_reauth_via_shared_refresh_token(httpx_mock)`:
         - Construct `Client(client_id="u", client_secret="p", token="stale-token", refresh_token="fresh-refresh", token_expires_at=9_999_999_999.0)`.
         - `view = client.with_options(max_retries=5)`.
         - Mock httpx sequence: (1) GET → 401 (stale token), (2) POST /token/refresh → 200 with `{"access_token": "new-token", "refresh_token": "new-refresh", ...}`, (3) GET → 200 with the actual quote.
         - Call `view.get_quote("GGAL")`.
         - Assert: returned quote is the success body; `client._state.token == "new-token"` (parent's state updated by view's re-auth path — shared `_state` semantics); `len(httpx_mock.get_requests())` is 3.
         - Assert: `view._state is client._state` (shared throughout).

         This validates that the view's 401 path uses the parent's shared `refresh_token` and that the re-auth result is visible to the parent (anti-Pitfall 13 extended to OAuth refresh flow).

       ~75 LOC for the 6 tests.

    2. **Append async-mirror tests to `packages/iol-client/tests/test_async_client.py`**:

       - 5 standard async-mirror tests.
       - `test_with_options_async_view_401_triggers_reauth_via_shared_refresh_token`: async mirror.

       ~75 LOC.

    3. **Regenerate snapshot for iol-client**:

       Run `uv run python verification/regen_snapshots.py`. Confirm iol snapshot body diff is zero (same reasoning as Plans 2-4). Commit the snapshot to maintain D-P4 atomicity.

    4. Run `uv run pytest verification/test_public_surface.py -x -q` to confirm GREEN.

    5. Run cross-cutting + full iol suite; all 3 of 4 cross-cutting tests for iol GREEN (matriz mutation-gate test is matriz-only, not parametrized for iol).
  </action>
  <verify>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/ -x -q</automated>
    <automated>uv run pytest verification/test_public_surface.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "iol_client" -x -q</automated>
    <automated>uv run ruff check packages/iol-client/ verification/</automated>
    <automated>uv run mypy --strict packages/iol-client/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def test_with_options_close_is_noop" packages/iol-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_view_401_triggers_reauth_via_shared_refresh_token" packages/iol-client/tests/test_client.py` returns 1.
    - Source (async mirror): `grep -c "def test_with_options_async_view_401_triggers_reauth_via_shared_refresh_token" packages/iol-client/tests/test_async_client.py` returns 1.
    - Behavior: `uv run --package iol-client pytest packages/iol-client/tests/ -x -q exits 0`.
    - Behavior: `uv run pytest verification/test_public_surface.py -x -q exits 0`.
    - Cross-cutting (3 of 4 GREEN for iol): `uv run pytest verification/test_with_options.py -k "iol_client" -x -q exits 0`.
    - Snapshot integrity: `git diff verification/snapshots/iol-client-surface.txt` reports empty.
    - Negative (other packages untouched): `git diff verification/snapshots/ambito-financiero-client-surface.txt verification/snapshots/higyrus-client-surface.txt verification/snapshots/matriz-client-surface.txt` reports empty.
    - Quality: `uv run ruff check packages/iol-client/ verification/ exits 0`.
    - Quality: `uv run mypy --strict packages/iol-client/ exits 0`.
  </acceptance_criteria>
  <done>
    Per-package iol mocked tests added (6 sync + 6 async), including the iol-specific 401-re-auth-with-view test. Snapshot regenerated (zero body diff). All 3 cross-cutting iol-parametrized tests GREEN. Phase 6/8/9 iol tests stay GREEN.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 13 final consolidated green gate (D-P1)</name>
  <files></files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P1 final paragraph — Plan 5 consolidated green gate)
    .planning/STATE.md (v1.1 baseline 907 tests; Quick tasks 260614-r1x: pre-commit run --all-files exits 0 idempotently)
    .planning/ROADMAP.md Phase 13 SC#5 (v1.1's 907-test baseline preserved)
    pyproject.toml (workspace-level config; check if `lint-imports` configured)
    .pre-commit-config.yaml (hooks list)
  </read_first>
  <behavior>
    Run the full Phase 13 consolidated green gate:

    1. `uv run pytest` (full monorepo) — reports ≥ 907 passing tests (v1.1 baseline preserved, SC#5 ROADMAP). The Phase 13 net-additive tests bump this up; record actual count in 13-05-SUMMARY.md.
    2. `uv run ruff check` (root, all packages, all verification) — exits 0.
    3. `uv run ruff format --check` — exits 0 (no unformatted code).
    4. `uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src verification/` — exits 0.
    5. If `lint-imports` is configured at workspace level (`tool.importlinter` block in pyproject.toml), run `uv run lint-imports` — exits 0.
    6. `pre-commit run --all-files` — exits 0 idempotently. Per Quick task 260614-r1x: this gate was made GREEN before Phase 13 planning started; Plan 5 confirms it stays GREEN.

    All gates must PASS before Plan 5 can complete.
  </behavior>
  <action>
    Execute the gate commands in sequence. For each:

    1. `uv run pytest` (full monorepo, no `-x` to see total count):
       - Capture exit code and total passing count.
       - Assert exit code == 0.
       - Assert passing count ≥ 907.
       - Document the actual count in `13-05-SUMMARY.md`.

    2. `uv run ruff check`:
       - Run from repo root.
       - Assert exit code == 0.

    3. `uv run ruff format --check`:
       - Run from repo root.
       - Assert exit code == 0.

    4. `uv run mypy --strict`:
       - Run against all 4 packages' `src/` AND `verification/`.
       - Assert exit code == 0.

    5. (Conditional) `uv run lint-imports`:
       - First check: `grep -c "importlinter" pyproject.toml`. If returns 0, SKIP this gate (not configured).
       - Otherwise run and assert exit code == 0.

    6. `pre-commit run --all-files`:
       - Run from repo root.
       - Assert exit code == 0.
       - Re-run a second time to confirm idempotency (per Quick task 260614-r1x lesson).
       - Assert second run exit code == 0.

    If ANY gate fails:
       - DO NOT commit Plan 5.
       - Document the failure in `13-05-SUMMARY.md` with full output.
       - Operator must fix before Plan 5 completes.

    If ALL gates pass:
       - Document the green status in `13-05-SUMMARY.md` with exact test count + gate outputs.
       - Plan 5 commit message can use the locked commit pattern from CONTEXT.md `<specifics>`: `feat(iol-client): Client.with_options(*, max_retries) + AsyncClient.with_options + _is_view lifecycle + green gate (ERG-01)`.

    NOTE: Plan 5 is the LAST plan in Phase 13. After all gates GREEN, Phase 13 is complete. Phase 14 (SEC-01 disk persistence) can begin.
  </action>
  <verify>
    <automated>uv run pytest</automated>
    <automated>uv run ruff check</automated>
    <automated>uv run ruff format --check</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src verification/</automated>
    <automated>pre-commit run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest` exits 0 AND reports ≥ 907 passing (SC#5 ROADMAP). Capture exact count.
    - `uv run ruff check` exits 0.
    - `uv run ruff format --check` exits 0.
    - `uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src verification/` exits 0.
    - Conditional: if `grep -c "importlinter" pyproject.toml` returns ≥ 1, then `uv run lint-imports` exits 0.
    - `pre-commit run --all-files` exits 0 on first run.
    - `pre-commit run --all-files` exits 0 on second run (idempotent — Quick task 260614-r1x invariant).
    - All 4 cross-cutting tests in `verification/test_with_options.py` GREEN: `uv run pytest verification/test_with_options.py -x -q exits 0`.
    - The CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz` GREEN (anti-Pitfall 14 / SC#2 ROADMAP merge gate confirmed).
    - All per-package `test_with_options_*` tests GREEN across 4 packages × 2 surfaces.
    - `13-05-SUMMARY.md` documents: exact pytest count; all 6 gate outputs; Phase 13 complete; forward reference to Phase 14 (SEC-01 disk persistence next).
  </acceptance_criteria>
  <done>
    All 6 green-gate commands PASS. pytest count ≥ 907 (v1.1 baseline preserved + Phase 13 net-additive). ruff check + format --check + mypy strict + lint-imports (if applicable) + pre-commit (idempotent) all exit 0. Cross-cutting `test_with_options.py` GREEN. CRITICAL mutation-gate test GREEN. Phase 13 complete. Forward reference: Phase 14 SEC-01 can begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.with_options` | Caller-controlled `max_retries` validated via `_validate_max_retries` |
| view → `_state.token` + `_state.refresh_token` (shared) | View shares OAuth credentials with parent; 401 re-auth flows through parent's `_ensure_token` |
| view 401 re-auth → `_ensure_token` | View's 401 path uses parent's shared `_state.refresh_token`; new token written to parent's `_state.token` (visible to all views) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-01 | Tampering | `Client.with_options` max_retries arg | mitigate | `_validate_max_retries` Phase 8 WR-06. Test `pytest.raises(ValueError)` for -1, True, 1.5. |
| T-13-05-02 | Information Disclosure | view's 401 re-auth log leaks token | mitigate | Logger namespace `iol_client` unchanged by view; RedactingFilter (Phase 8 LOG-02) still applies. iol existing tests `test_logging_no_token_leak.py` (cross-cutting) plus per-package retry-log tests cover. |
| T-13-05-03 | Denial of Service | View closes parent's http_client / cached OAuth token | mitigate | `_is_view` flag + no-op `close()` / `aclose()` guard. Per-package `test_with_options_close_is_noop` asserts parent's `http_client` AND `_state.token` survive `view.close()`. |
| T-13-05-04 | Tampering | View's 401 re-auth could corrupt parent's refresh_token | mitigate | View shares `_state.refresh_token` reference; `_ensure_token` writes new token back to `_state.token` (shared) AND new refresh_token to `_state.refresh_token` (shared). Parent sees the new tokens — INTENDED (shared `_state` semantics, Phase 6 D-13). No locking change needed; iol has no async REST yet so no concurrency surface (Phase 7 D-15: B7 ámbito divergence, but iol has token_lock per Phase 8 — view shares same lock via shared `_state`). |
| T-13-05-05 | Spoofing | View bypasses mutation gate (if iol gains POST endpoints) | mitigate | Phase 8 D-01 mutation gate via `extensions["idempotent"]` is FIRST gate. iol currently has no non-idempotent endpoints in v1.2 scope (`get_quote`, `get_instruments`, `get_historical_quotes` are all GETs). If future iol endpoints add POST, the gate still applies. Cross-cutting `test_with_options_does_not_bypass_mutation_gate_matriz` covers the contract for the package most likely to hit this (matriz). |
| T-13-05-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies in Plan 5. |

Phase 14 SEC-01 forward-reference (informative only — NOT implemented in Phase 13): when disk-cached `refresh_token` lands, the view's shared `_state` will include the disk-cache state (`token_cache_path` or similar). Views continue sharing the cache without per-view duplication. iol-last in Phase 13 serial means the view shape is fully concrete before Phase 14 designs the disk integration.

Credential surface unchanged in Phase 13: view shares `_state.token`, `_state.refresh_token`, `_state.client_id`, `_state.client_secret`. No new credential storage path. Parent's `_state` is single source.
</threat_model>

<verification>
- `Client.with_options` + `AsyncClient.with_options` exist on iol.
- `_is_view` slot added; `close()` / `aclose()` no-op.
- `_request` + `_send_auth_request` (sync + async) set `req.extensions["max_attempts"]`.
- D-T4 confirmed: NO `client_max_retries` field added to iol.
- 401 re-auth path UNCHANGED; view's 401 uses parent's shared `_state.refresh_token`.
- `__repr__` prefix; existing iol credential redaction preserved.
- Per-package mocked tests added (6 sync + 6 async), including iol-specific 401-re-auth-with-view test.
- Snapshot regenerated; body diff zero.
- All 3 cross-cutting iol-parametrized tests GREEN.
- **PHASE 13 GREEN GATE**:
  - `uv run pytest` ≥ 907 passing.
  - `uv run ruff check` exits 0.
  - `uv run ruff format --check` exits 0.
  - `uv run mypy --strict packages/*/src verification/` exits 0.
  - `uv run lint-imports` exits 0 (if configured).
  - `pre-commit run --all-files` exits 0 idempotently.
- The CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz` GREEN (Anti-Pitfall 14 / SC#2 ROADMAP merge gate confirmed).
- Phase 13 COMPLETE.
</verification>

<success_criteria>
- iol sync + async `with_options` lands (per-package serial complete: ámbito → higyrus → matriz → iol).
- iol-specific 401-re-auth-with-view test GREEN (shared `_state.refresh_token` semantics confirmed).
- All cross-cutting tests for `[iol_client]` parametrize row GREEN (3 of 4 — matriz mutation-gate is matriz-only).
- **All Phase 13 SC met**:
  - SC#1: cross-cutting `test_with_options_shares_http_client_and_token` GREEN × 4 packages.
  - SC#2: CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz` GREEN (anti-Pitfall 14).
  - SC#3: `RetryTransport.handle_request` reads `max_attempts` extension (Plan 1) — verified GREEN.
  - SC#4: per-package serial roll-out complete (ámbito → higyrus → matriz → iol).
  - SC#5: v1.1 907-test baseline preserved (`pytest` ≥ 907 passing).
- Phase 13 final green gate PASSES (all 6 commands exit 0).
- Phase 14 (SEC-01 IOL disk persistence) can begin from Phase 13 HEAD with stable iol view shape.
</success_criteria>

<output>
Create `.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-05-SUMMARY.md` when done. Document:
- iol sync + async `with_options` shape.
- D-T4 confirmed (no `client_max_retries` in iol).
- iol-specific 401-re-auth-with-view test (sync + async).
- Snapshot regen (zero body diff).
- **Phase 13 final green gate**: exact pytest count (≥ 907 passing), ruff check + format --check + mypy strict + (lint-imports if applicable) + pre-commit (idempotent) all GREEN.
- All 5 ROADMAP Phase 13 success criteria met.
- Anti-Pitfall 13 (resource leak) + Anti-Pitfall 14 (mutation gate) both confirmed mitigated.
- D-D2 forward-reference for Phase 15: 3 example usage patterns documented (per CONTEXT.md D-D2):
  - `client.with_options(max_retries=5).get_quote("RARE")` — idempotent GET with bumped cap.
  - `client.with_options(max_retries=0).get_movimientos(...)` — debug iteration, retry disabled.
  - `client.with_options(max_retries=10).new_order(...)` — matriz; mutation gate prevails; 10 attempts only for idempotent calls.
- Phase 13 complete; Phase 14 (SEC-01) can begin.
</output>
