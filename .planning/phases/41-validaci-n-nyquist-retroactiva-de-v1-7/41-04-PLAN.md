---
phase: 41
plan: 04
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - .planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "Las 14 filas de la Phase 37 llevan exactamente una disposición pese a que sus Task IDs originales no son únicos"
    - "La fila cuyo selector no selecciona ningún test queda re-apuntada a los tests que sí cubren la conducta, con ambos comandos escritos"
    - "37-VALIDATION.md declara el SHA de v1.7 y pasa a status validated sin que nyquist_compliant cambie"
  artifacts:
    - path: ".planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md"
      provides: "Sección Validation Audit 2026-08-31 con 14 disposiciones y front-matter transformado"
      contains: "## Validation Audit 2026-08-31"
  key_links:
    - from: ".planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md"
      to: "packages/matriz-client/tests/test_null_object.py"
      via: "la corrección de comando cita los tests con nombre distinto que cubren la conducta de alias sobre ambas superficies"
      pattern: "parsed_snapshot"
    - from: ".planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md"
      to: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      via: "copia literal del SHA de v1.7, del HEAD de baseline y del esqueleto de sección"
      pattern: "37a83fe693a303a551f4374f48fe6fc5521804f7"
---

<objective>
Auditar la Phase 37: 14 filas de mapa, cero filas manual-only, y dos problemas de identidad que la
hacen la más frágil del conjunto para el criterio 2.

Purpose: primero, los Task ID de este mapa **no son únicos** — la forma con sufijo genérico se repite
tres veces en el primer plan y cinco veces en el tercero, de modo que "exactamente una disposición por
fila" no es demostrable sin una clave ordinal. Segundo, una de sus filas usa un selector que no
selecciona ningún test: pytest sale con código 5 y su salida no contiene ninguna falla, así que un
transcripto pegado se lee limpio mientras nada se ejecutó. La conducta sí está cubierta, por dos tests
con otro nombre.

Output: `37-VALIDATION.md` con `## Validation Audit 2026-08-31`, 14 disposiciones bajo clave ordinal y
front-matter transformado. Cero fuente de producto tocada.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Re-ejecutar las 14 filas de la Phase 37 y re-apuntar el selector que no selecciona nada</name>
  <files>.planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md (el archivo objetivo, completo)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 2, 3, 6, 7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Re-Execution Results → Phase 37, § Architecture Patterns → Pattern 3, § Common Pitfalls 2, § Assumptions Log A1
    - packages/matriz-client/tests/test_null_object.py (cuerpo de los tests sustitutos, no sólo sus nombres)
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Re-verificar el invariante de criterio 1 con `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`
    antes de ejecutar nada. Si sale distinto de 0, ABORTAR.

    Asignar claves ordinales `37-r01`..`37-r14` a las 14 filas del mapa en su orden de aparición. Esto
    es obligatorio en esta fase: los Task ID del mapa se repiten, y sin clave ordinal el gate de
    "exactamente una disposición por fila" no es computable. La celda `Row` de la tabla de disposición
    lleva la clave ordinal seguida del Task ID original.

    Re-ejecutar las 13 filas cuyo comando funciona, transcribiendo el comando literal del mapa. Las
    salidas de referencia medidas por el researcher esta sesión, por si sirven de contraste: la suite
    roja de tipos de superficie de matriz con 19 pasados; su selector de exención con 3; el test de
    gate verde sobre el árbol real en iol con 1; el selector de envelope en el core de matriz con 10;
    el selector de rangos de precio con 3; el de portfolio con 3; el de extras con 9; el de mapping con
    24; el de conversión con 4; el selector de alias en la suite de Null Object con 11; el test nominal
    de que agregar un alias de propiedad no mueve el conteo de divergencias con 1; las dos suites de WS
    con 35 entre ambas; y mypy sobre el src del paquete, limpio. La evidencia que se escribe es la
    salida de **esta** corrida, no esos valores.

    Tratar la fila `37-r11` por la regla R-02. Su selector de superficies de alias selecciona 0 de 74
    tests y pytest sale con código 5. La conducta —que los alias funcionan sobre una instantánea
    parseada desde REST y sobre una parseada desde un frame de WS— está cubierta por dos tests de
    `packages/matriz-client/tests/test_null_object.py` cuyos nombres terminan en las variantes de
    instantánea parseada por REST y por frame de WS. Antes de re-apuntar, **leer el cuerpo de ambos
    tests** y confirmar que assertan esa conducta y no otra: RESEARCH.md registra esto como su
    asunción A1 y pide explícitamente que se verifique el cuerpo, no el nombre. Construir el comando
    corregido con un selector que capture ambos tests, ejecutarlo, y confirmar que reporta 2 tests
    pasados. Disponer `VERIFIED-NOW (comando corregido)` y transcribir en la celda de evidencia **los
    dos** comandos: el original del mapa con su resultado de selección vacía, y el corregido con su
    conteo de pasados.

    Aplicar el guardia anti-vacuidad de la sección 6 del contrato a las 14 filas. RESEARCH.md midió
    exactamente una fila de selección vacía en esta fase. Si aparece una **segunda**, es un hallazgo
    real: escalarlo en el SUMMARY, no re-apuntarla en silencio — R-02 autoriza exactamente dos
    correcciones en toda la fase 41, una aquí y otra en la Phase 39.

    Recortar toda salida a su línea de resumen antes de escribirla al archivo.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_surface_types_red.py -q && uv run pytest packages/matriz-client/tests/test_null_object.py -k "rest_parsed_snapshot or ws_frame_parsed_snapshot" -q && uv run mypy packages/matriz-client/src && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - Las 14 filas tienen clave ordinal `37-r01`..`37-r14` asignada, únicas: la lista ordenada y deduplicada tiene 14 elementos.
    - `uv run pytest packages/matriz-client/tests/test_null_object.py -k "rest_parsed_snapshot or ws_frame_parsed_snapshot" -q` sale 0 y reporta exactamente 2 tests pasados.
    - El comando original de `37-r11` queda documentado con su resultado real: su ejecución sale con código 5 y no reporta ningún test pasado.
    - El cuerpo de los dos tests sustitutos fue leído y su aserción está resumida en la celda de evidencia (no sólo sus nombres).
    - `uv run mypy packages/matriz-client/src` sale 0.
    - Ninguna otra fila de la fase presenta selección vacía; si alguna lo hiciera, queda escalada en el SUMMARY en vez de re-apuntada.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al terminar; `git status --porcelain verification/` vacío.
  </acceptance_criteria>
  <done>Las 14 filas tienen clave ordinal y evidencia de esta sesión; la fila de selección vacía está re-apuntada con ambos comandos escritos y el cuerpo de los sustitutos leído.</done>
</task>

<task type="execute">
  <name>Task 2: Escribir la sección de auditoría de la Phase 37 y transformar su front-matter</name>
  <files>.planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 4, 5, 7, 8
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección + estilo de celda de evidencia)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (par de encabezados en negrita, escalaciones, línea tildada de sign-off)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (prosa modelo para un resultado PARTIAL)
  </read_first>
  <action>
    Escribir `## Validation Audit 2026-08-31` al final de `37-VALIDATION.md`, precedida de una regla
    horizontal, después de `## Validation Sign-Off`, con los ocho bloques de la sección 5 del contrato.

    Valores concretos para esta fase: denominador **14**; disposición esperada 14 `VERIFIED-NOW` (de los
    cuales 1 con comando corregido), 0 `VERIFIED-HISTORICALLY`, 0 `NOT-VERIFIABLE-RETROACTIVELY`;
    archivos de test nuevos escritos **0**; filas cuya celda de enforcement dice que no corre en CI:
    **0** — las 14 filas son suites de paquete o mypy, ambas cubiertas por CI. Si la medición da otro
    reparto, escribir el medido y anotar la divergencia en el SUMMARY.

    Llenar la cuarta columna con la tabla de lookup de la sección 7 del contrato: las 13 filas de pytest
    sobre `packages/matriz-client/tests/` y `packages/iol-client/tests/` mapean al job `test` con su
    rango de líneas; la fila de mypy sobre el src del paquete mapea al tramo global del job `typecheck`.

    La sección `### Correcciones de comando` de esta fase tiene exactamente una entrada: la fila
    `37-r11`, con el comando original, el resultado de selección vacía, los dos tests sustitutos
    nombrados con lo que assertan, el comando corregido y su conteo de pasados. Escribir explícitamente
    que esto es una corrección de bookkeeping y no un gap de cobertura: la conducta estaba cubierta
    desde el principio, el selector del mapa era el que estaba mal.

    Escribir la sección de hallazgos de bookkeeping con dos: primero, que los Task ID de este mapa no
    son únicos —la forma con sufijo genérico se repite— y que por eso la auditoría introdujo la clave
    ordinal; ese es un defecto de forma del artefacto de plan-time que vale la pena dejar registrado.
    Segundo, que ocho celdas de `File Exists` del mapa afirman que el archivo de test falta o necesita
    extensión cuando hoy todas las suites citadas existen y corren verde.

    Actualizar la columna `Status` de las 14 filas según la regla de la sección 5 del contrato y
    extender la línea de leyenda. No tocar las celdas de `File Exists`: su contradicción con la realidad
    medida es el hallazgo.

    Transformar el front-matter según la sección 4 del contrato: `status` pasa de `draft` a `validated`;
    `nyquist_compliant` se queda en `false` porque R-09 falla por su condición (c) — la fase tiene una
    fila con calificador de corrección. Escribir ese rationale junto al veredicto, en la forma de prosa
    de `07-VALIDATION.md`, sin adoptar su valor de flag; decir de forma explícita que las 14 filas
    corrieron verde y que aun así el flag no se mueve, porque una de ellas sólo corre verde después de
    que la auditoría corrigiera su comando, y certificar cumplimiento total sobre un contrato que la
    propia auditoría reescribió sería grading contra lo que shipeó en vez de contra el criterio.
    `not_verifiable_retroactively` es `0`; se agregan `audited_commit_sha` con el commit de v1.7,
    `audit_baseline_head` copiado literal del contrato, `frozen_tree_verified: true`,
    `updated: 2026-08-31` y `last_audited: 2026-08-31`. Quedan intactos `phase`, `slug`, `created`,
    `wave_0_complete` y los comentarios de lifecycle si el archivo los tiene.

    Cerrar con la línea de veredicto en la forma fijada y una línea nueva tildada en el
    `## Validation Sign-Off`, dejando sin tildar las casillas preexistentes.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md; X() { awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 37-[rm][0-9]/' "$F"; }; test "$(X | wc -l | tr -d ' ')" -eq 14 && test "$(( $(X | grep -c 'VERIFIED-NOW') + $(X | grep -c 'VERIFIED-HISTORICALLY') + $(X | grep -c 'NOT-VERIFIABLE-RETROACTIVELY') ))" -eq 14 && grep -q '^status: validated' "$F" && grep -q '^nyquist_compliant: false' "$F" && grep -q '^not_verifiable_retroactively: 0' "$F" && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$F" && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de disposición tiene exactamente 14 filas dentro del rango acotado entre `### Disposición por fila` y la línea de leyenda.
    - La suma de los tres conteos de token dentro de ese rango es exactamente `14`.
    - El reparto medido es 14 / 0 / 0: `VERIFIED-NOW` = `14`, `VERIFIED-HISTORICALLY` = `0`, `NOT-VERIFIABLE-RETROACTIVELY` = `0`.
    - Exactamente una fila lleva el calificador de comando corregido, y es `37-r11`.
    - La sección `### Correcciones de comando` tiene exactamente una entrada y nombra los dos tests sustitutos.
    - Cero evidencia vacua: dentro del rango acotado, las líneas que mencionan descartes y no mencionan tests pasados son `0`.
    - `grep -c 'NOT ENFORCED'` dentro del rango acotado es `0`.
    - Front-matter: `grep -c '^status: validated'` es `1`, `grep -c '^nyquist_compliant: false'` es `1`, `grep -c '^not_verifiable_retroactively: 0'` es `1`, `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7'` es `1`, `grep -cE '^audit_baseline_head: [0-9a-f]{40}'` es `1`.
    - `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 37-VALIDATION.md` es `1`.
    - `grep -c '^- \[x\] .*nyquist_compliant: true' 37-VALIDATION.md` es `0`.
    - `git status --porcelain` sólo lista `37-VALIDATION.md` bajo `.planning/milestones/v1.7-phases/`.
  </acceptance_criteria>
  <done>Las 14 filas de la Phase 37 están dispuestas bajo clave ordinal, con la corrección de comando documentada con ambos comandos y el flag sin mover, con rationale escrito.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producido por este plan:
- `.planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md` — **modificado**. Sección `## Validation Audit 2026-08-31` (métricas, tabla de disposición de 14 filas y 4 columnas bajo clave ordinal `37-r01`..`37-r14`, una corrección de comando documentada, hallazgos de bookkeeping, escalaciones, veredicto); columna `Status` actualizada; front-matter con `status: validated`, `nyquist_compliant: false`, `not_verifiable_retroactively: 0`, `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified: true`, `updated`, `last_audited`.

Cero archivos nuevos, cero locks nuevos en `verification/`, cero ediciones a `.github/workflows/ci.yml`, cero ediciones a `packages/`. Ninguna declaración inerte ruteada a Phase 45 se origina en este plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de pytest/mypy local → markdown committeado | Único cruce: las líneas de resumen transcritas quedan en el repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04-01 | Information Disclosure | transcripción de salida de suite a `37-VALIDATION.md` | mitigate | Sección 6 del contrato: sólo línea de resumen; ninguna de las 14 filas lee `.env` ni abre red (ASVS V7) |
| T-41-04-02 | Repudiation | falsa garantía por un selector que no selecciona nada y cuya salida no contiene ninguna falla | mitigate | Guardia anti-vacuidad de la sección 6 más la regla R-02, que exige transcribir el comando original con su resultado y el corregido con su conteo de pasados |
| T-41-04-03 | Repudiation | re-apuntar un selector a un test que en realidad asserta otra conducta | mitigate | Criterio de aceptación que exige leer el cuerpo de los dos tests sustitutos y resumir su aserción en la evidencia (RESEARCH.md A1) |
| T-41-04-04 | Repudiation | disposición ambigua por Task ID repetidos en el mapa original | mitigate | Clave ordinal `37-rNN` obligatoria; criterio de aceptación que verifica 14 claves únicas |
| T-41-04-SC | Tampering | instalaciones de paquetes | accept | Cero: ningún comando invoca `uv add`, `npm`, `pip` ni `cargo` |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V5/V6 — el plan edita un markdown y re-corre
suites locales existentes.
</threat_model>

<verification>
1. Tabla de 14 filas bajo clave ordinal única, suma de tokens exactamente 14, reparto 14 / 0 / 0.
2. Una y sólo una corrección de comando, documentada con original y corregido.
3. Front-matter con los cinco campos nuevos y `nyquist_compliant` sin cambiar.
4. `git status --porcelain verification/` vacío; `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
</verification>

<success_criteria>
- Ninguna de las 14 filas queda sin disponer y ninguna lleva dos disposiciones, pese a los Task ID repetidos del mapa original.
- La fila de selección vacía está re-apuntada con evidencia de que los sustitutos cubren la misma conducta, y ambos comandos quedan escritos.
- `nyquist_compliant` sigue en `false` con rationale que dice explícitamente por qué 14 verdes no bastan.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-04-SUMMARY.md` when done.
</output>
