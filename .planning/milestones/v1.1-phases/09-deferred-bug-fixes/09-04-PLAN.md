---
phase: 09-deferred-bug-fixes
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified:
  - .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
autonomous: false
requirements: [BUG-01, BUG-02, BUG-03, BUG-04]
requirements_addressed: [BUG-01, BUG-02, BUG-03, BUG-04]
tags: [phase-09, green-gate, ci-evidence, snapshot-zero-diff, validation-only, wave-3]
user_setup: []

must_haves:
  truths:
    - "Plan 09-04 es VALIDATION-ONLY: NO modifica código de paquetes, NO crea nuevos archivos de test, NO actualiza findings. Solo actualiza `.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` con evidence de CI run + operator checkpoint."
    - "Full pytest matrix Python 3.12 + 3.13 GREEN (CI matrix runs)."
    - "Ruff check + ruff format --check clean."
    - "Mypy --strict clean para todos los paquetes (`packages/*/src/`)."
    - "Import-linter contract (Phase 7 D-09) holds: `uv run lint-imports` returns 0."
    - "Cross-leak sentinel (Phase 7 D-10) GREEN: `verification/test_sync_async_isolation.py` returns 0 (matriz skip preserved per Phase 7 D-13)."
    - "CI grep step `lint-logging` (Phase 8 D-27) clean: no `logging.basicConfig` or `logging.root` in `packages/*/src/`."
    - "Public surface snapshot zero-diff (Phase 6 D-09): los 4 snapshots `verification/snapshots/*-surface.txt` permanecen idénticos (BUG-01..04 no cambian signatures públicas — `_state` privado, `_core.py` privado, parametric test no afecta surface; `_state.account_id` field removal NO leakea a snapshot porque `_state` es privado)."
    - "Plan 09-01 nuevos tests (`test_refresh_token_lifecycle.py` + `..._async.py`) GREEN: 8 tests pasados."
    - "Plan 09-02 nuevos tests (`test_multi_account.py` + opcional `test_listado_cuentas_regression.py` si bucket c) GREEN."
    - "Plan 09-03 nuevo test paramétrico (`test_get_instruments_by_cfi_validates_cfi_code`) GREEN: 10 casos pasados."
    - "Test count delta total: ~760 baseline (Phase 8) + ~18-22 new = ~778-782 tests verde (8 iol + N higyrus 1-3 + 10 matriz; rango depende de bucket BUG-02 outcome)."
    - "matriz aio.py LOC == 103 (Phase 8 D-25 preserved); matriz `_atransport.py` absent (Phase 8 D-25 preserved)."
    - "F-09 (matriz findings) Status = FIXED; F-02 (higyrus findings) Status = FIXED|NO-FIX per bucket. Ambos con `Regression:` lines linkadas a los nuevos tests."
    - "`09-VALIDATION.md` frontmatter actualizado a `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true` post-operator-approval."
    - "Operator checkpoint final cierra Phase 9; ready para `/gsd-verify-work 9`."
  artifacts:
    - path: ".planning/phases/09-deferred-bug-fixes/09-VALIDATION.md"
      provides: "Green gate evidence + CI run output + frontmatter status=approved + per-task verify map updated"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/09-deferred-bug-fixes/09-VALIDATION.md"
      to: "Phase 9 commits (09-01, 09-02, 09-03)"
      via: "Per-Task Verification Map table + Wave 0 Requirements checkboxes"
      pattern: "✅"
---

<objective>
Plan 09-04 es el **green-gate consolidation** de Phase 9. Validation-only:
no toca código, no crea tests, no actualiza findings. Solo corre la suite
completa + lint + typecheck + snapshot + cross-leak + lint-imports +
lint-logging y consolida la evidence en `09-VALIDATION.md` con operator
checkpoint final.

Purpose:
1. Confirmar que los 4 bugs (BUG-01..04) están cerrados sin regresiones.
2. Validar Phase 6/7/8 invariantes preservados (snapshot, lint-imports,
   cross-leak, lint-logging, matriz aio.py LOC, `_atransport.py` absent).
3. Documentar el test count delta + outcome del BUG-02 triage en
   `09-VALIDATION.md`.
4. Operator checkpoint cierra Phase 9 para `/gsd-verify-work 9`.

Output:
- `.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` UPDATED con:
  - `status: approved` + `nyquist_compliant: true` + `wave_0_complete: true` en frontmatter.
  - Per-Task Verification Map filas marcadas ✅.
  - Wave 0 Requirements checkboxes marcadas.
  - Validation Sign-Off section con operator approval.
  - Evidence excerpts (pytest summary line, mypy clean, ruff clean, lint-imports
    clean, snapshot zero-diff, finding status post-Plan 09-02 + 09-03).
- 1 commit atómico per D-12: `ci(phase-09): green gate — full pytest + ruff + mypy + snapshot zero-diff + cross-leak (BUG-01..04)`.

NO toca código de paquetes (autonomous: false porque hay operator checkpoint
al final, pero el pre-checkpoint runs son automatizables).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-deferred-bug-fixes/09-CONTEXT.md
@.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
@.planning/phases/09-deferred-bug-fixes/09-01-PLAN.md
@.planning/phases/09-deferred-bug-fixes/09-02-PLAN.md
@.planning/phases/09-deferred-bug-fixes/09-03-PLAN.md
@.planning/verification/matriz-client-findings.md
@.planning/verification/higyrus-client-findings.md
@verification/test_public_surface.py
@verification/test_sync_async_isolation.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full green-gate suite + capture evidence</name>
  <files>.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md (estado actual: status draft, Wave 0 checkboxes unchecked)
    - .planning/phases/09-deferred-bug-fixes/09-01-PLAN.md, 09-02-PLAN.md, 09-03-PLAN.md (per-task SUMMARYs si existen — Plan 09-04 consolida lo que ya pasó)
    - .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md (analog estructura del green-gate previo)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (otro analog)
    - .planning/verification/matriz-client-findings.md (verificar F-09 Status post-Plan 09-03)
    - .planning/verification/higyrus-client-findings.md (verificar F-02 Status post-Plan 09-02)
    - pyproject.toml (confirmar comandos ruff/mypy/pytest configurados)
  </read_first>
  <action>
    Ejecutar la batería de green-gate commands secuencialmente y capturar
    los outputs condensados (summary lines, no full logs) para insertar
    en `09-VALIDATION.md`.

    **Step 1 — Full pytest matrix (Python 3.12 default; CI matrix corre 3.13 también):**
    ```bash
    uv run pytest --cov 2>&1 | tail -20 | tee /tmp/09-04-pytest.txt
    ```
    Expected: `N passed in <time>` donde N = ~778-782 (depende del bucket
    BUG-02 outcome). Si fail: investigation block — abortar el green gate.

    **Step 2 — Ruff check:**
    ```bash
    uv run ruff check 2>&1 | tee /tmp/09-04-ruff-check.txt
    ```
    Expected: `All checks passed!` o equivalente exit 0.

    **Step 3 — Ruff format check:**
    ```bash
    uv run ruff format --check 2>&1 | tee /tmp/09-04-ruff-format.txt
    ```
    Expected: `N files already formatted` exit 0.

    **Step 4 — Mypy strict:**
    ```bash
    uv run mypy --strict packages/ 2>&1 | tail -5 | tee /tmp/09-04-mypy.txt
    ```
    Expected: `Success: no issues found in N source files` exit 0.

    **Step 5 — Import-linter (Phase 7 D-09):**
    ```bash
    uv run lint-imports 2>&1 | tee /tmp/09-04-lint-imports.txt
    ```
    Expected: contracts pass exit 0.

    **Step 6 — Cross-leak sentinel (Phase 7 D-10):**
    ```bash
    uv run pytest verification/test_sync_async_isolation.py -x --no-header -q 2>&1 | tee /tmp/09-04-cross-leak.txt
    ```
    Expected: all pass exit 0 (matriz skip preserved per Phase 7 D-13 — async
    no existe aún).

    **Step 7 — CI lint-logging step (Phase 8 D-27):**
    ```bash
    ! grep -rn "logging.basicConfig\|logging.root" packages/*/src/ 2>&1 | tee /tmp/09-04-lint-logging.txt
    ```
    Expected: 0 hits exit 0.

    **Step 8 — Public surface snapshot zero-diff (Phase 6 D-09):**
    ```bash
    uv run pytest verification/test_public_surface.py -x --no-header -q 2>&1 | tee /tmp/09-04-snapshot.txt
    ```
    Expected: all 4 snapshot tests pass exit 0.

    **Step 9 — matriz aio.py LOC + `_atransport.py` absent (Phase 8 D-25):**
    ```bash
    wc -l packages/matriz-client/src/matriz_client/aio.py | tee /tmp/09-04-matriz-aio-loc.txt
    test ! -e packages/matriz-client/src/matriz_client/_atransport.py && echo "atransport ABSENT OK" | tee /tmp/09-04-matriz-atransport.txt
    ```
    Expected: `aio.py` = 103 lines (± minor whitespace); `_atransport.py` ABSENT OK.

    **Step 10 — Findings status post-Plan 09-02/09-03:**
    ```bash
    grep -A 1 "F-09" .planning/verification/matriz-client-findings.md | head -3 | tee /tmp/09-04-f09.txt
    grep -A 1 "F-02" .planning/verification/higyrus-client-findings.md | head -3 | tee /tmp/09-04-f02.txt
    ```
    Expected:
    - F-09: `Status: FIXED`.
    - F-02: `Status: FIXED` (bucket b/c) o `NO-FIX` (bucket a).

    **Step 11 — Test count delta:**
    ```bash
    uv run pytest --collect-only -q 2>&1 | tail -3 | tee /tmp/09-04-collect.txt
    ```
    Expected: `~780 tests collected` (760 baseline + ~20 deltas).

    **Step 12 — Editar `09-VALIDATION.md`:**
    1. Frontmatter:
       - `status: draft` → `status: approved`.
       - `nyquist_compliant: false` → `nyquist_compliant: true`.
       - `wave_0_complete: false` → `wave_0_complete: true`.
       - Agregar `phase_status: ready_for_verify` (alineado con Phase 8 D-VALIDATION convention).
    2. Per-Task Verification Map: cambiar `⬜ pending` → `✅ green` para
       cada fila (excepto las manual-only que dependen de operator paste
       evidence — esas quedan `✅ green` si Task 2 las aprobó).
    3. Wave 0 Requirements section: marcar todas las checkboxes `[ ]` →
       `[x]` post-completion (excepto bucket c que es conditional).
    4. Agregar nueva section `## Green-Gate Evidence` con los outputs
       condensados de los Steps 1-11.
    5. Validation Sign-Off section: marcar todas las checkboxes `[ ]` →
       `[x]`. Cambiar `**Approval:** pending` → `**Approval:** approved
       by operator on 2026-MM-DD` (timestamp del operator checkpoint).

    NO ejecutar los live re-runs de Higyrus/Matriz (esos son Plan 09-02 /
    09-03 territory; Plan 09-04 confía en que ya pasaron y consolida).
  </action>
  <verify>
    <automated>uv run pytest --no-header -q 2>&1 | tail -3 && uv run ruff check && uv run mypy --strict packages/ 2>&1 | tail -2 && uv run lint-imports && uv run pytest verification/test_public_surface.py verification/test_sync_async_isolation.py -x --no-header -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Full pytest GREEN: `uv run pytest --no-header -q` returns 0; output muestra `N passed` con N entre 776-790 (rango dependiendo del bucket BUG-02).
    - Ruff check clean: `uv run ruff check` returns 0.
    - Ruff format check clean: `uv run ruff format --check` returns 0.
    - Mypy strict clean: `uv run mypy --strict packages/` returns 0 con `Success: no issues found in N source files`.
    - Import-linter clean: `uv run lint-imports` returns 0.
    - Cross-leak sentinel GREEN: `uv run pytest verification/test_sync_async_isolation.py -x --no-header -q` returns 0.
    - Lint-logging CI step clean: `grep -c "logging.basicConfig\|logging.root" packages/*/src/` returns 0 (sin matches en packages/*/src/, OK si hay matches en `verification/` u otros lugares fuera de scope).
    - Snapshot zero-diff: `uv run pytest verification/test_public_surface.py -x --no-header -q` returns 0 con todos los snapshot tests pasados.
    - matriz aio.py LOC == 103: `wc -l packages/matriz-client/src/matriz_client/aio.py | grep -E "^\s*103\s"` returns 0 (line count = 103).
    - matriz `_atransport.py` ABSENT: `test ! -e packages/matriz-client/src/matriz_client/_atransport.py` returns 0.
    - F-09 Status FIXED: `grep -A 1 "^### F-09" .planning/verification/matriz-client-findings.md | grep -c "FIXED"` returns ≥ 1.
    - F-02 Status flipped from OPEN: `grep -A 1 "^### F-02" .planning/verification/higyrus-client-findings.md | grep -cE "FIXED|NO-FIX"` returns ≥ 1.
    - `09-VALIDATION.md` frontmatter contains `status: approved`, `nyquist_compliant: true`, `wave_0_complete: true`. Verified via `grep -c "status: approved" .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` returns 1 AND `grep -c "nyquist_compliant: true" .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` returns 1.
    - Per-Task Verification Map: 0 `⬜ pending` filas en estado pending; todas `✅ green`. Verified via `grep -c "⬜ pending" .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` returns 0.
    - Wave 0 Requirements: 0 unchecked `- [ ]` boxes (excepto el conditional bucket c que puede quedar opcional).
    - Green-Gate Evidence section presente con outputs de los 11 Steps.
  </acceptance_criteria>
  <done>
    All green-gate checks pass; `09-VALIDATION.md` updated con evidence +
    status approved + checkboxes marcadas; ready para operator final
    checkpoint Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final operator checkpoint — Phase 9 closure approval</name>
  <files>(no file modification — operator-driven checkpoint)</files>
  <action>
    Pause for operator. Operator performs the steps described in `<how-to-verify>` and returns one of the signals in `<resume-signal>`. No file modification within this task — Plans 09-01..03 implementation tasks own the writes.
  </action>
  <verify>
    <automated>echo "checkpoint pending operator approval — no automated verify"</automated>
  </verify>
  <done>
    Operator returned an approval signal per `<resume-signal>`; the orchestrator records the response in chat / SUMMARY.
  </done>
  <what-built>
    Plan 09-04 Task 1 ran the full green-gate suite + updated
    `09-VALIDATION.md` con evidence + frontmatter status approved. Los 4
    bugs (BUG-01..04) están closed con regression tests; F-02 + F-09
    findings updated; snapshot zero-diff; lint/typecheck/import-linter
    clean; matriz aio.py + `_atransport.py` Phase 8 D-25 invariants
    preserved.
  </what-built>
  <how-to-verify>
    Este checkpoint cierra Phase 9 antes de `/gsd-verify-work 9`. El
    operator confirma manualmente:

    **Step 1 — Validar `09-VALIDATION.md`:**
    ```bash
    cat .planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
    ```
    Confirmar:
    1. Frontmatter `status: approved`, `nyquist_compliant: true`,
       `wave_0_complete: true`.
    2. Per-Task Verification Map: 0 `⬜ pending`.
    3. Wave 0 Requirements: todas las checkboxes marcadas (excepto
       conditional bucket c si no aplica).
    4. Green-Gate Evidence section con outputs condensados que muestran
       todos los gates GREEN.
    5. Validation Sign-Off section con `**Approval:** approved by operator
       on 2026-MM-DD`.

    **Step 2 — Validar findings:**
    ```bash
    grep -A 5 "^### F-09" .planning/verification/matriz-client-findings.md
    grep -A 5 "^### F-02" .planning/verification/higyrus-client-findings.md
    ```
    Confirmar:
    - F-09: Status FIXED + Resolution + Regression line linkando a
      `tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code`.
    - F-02: Status FIXED|NO-FIX (per bucket) + Resolution con bucket
      marker + Regression line.

    **Step 3 — Validar test count delta:**
    ```bash
    uv run pytest --collect-only -q 2>&1 | tail -3
    ```
    Expected: ~778-782 tests collected (760 baseline + 18-22 new).

    **Step 4 — Validar commits Phase 9:**
    ```bash
    git log --oneline -10 | head -5
    ```
    Expected: 4 atomic commits visible:
    1. `test(iol): BUG-03 refresh_token lifecycle regression tests sync + async (BUG-03)`
    2. `fix(higyrus): BUG-02 triage + BUG-04 multi-account regression + remove _state.account_id (BUG-02, BUG-04)`
    3. `fix(matriz): BUG-01 hybrid Literal+regex CFI validation + cycle_closure FAIL→PASS (BUG-01)`
    4. `ci(phase-09): green gate — full pytest + ruff + mypy + snapshot zero-diff + cross-leak (BUG-01..04)`

    **Step 5 — Validar STATE.md (post-commit, opcional):**
    `STATE.md` actualizado por el orchestrator post-`/gsd-verify-work 9`;
    no requiere validation manual ahora.

    **Step 6 — Resume signal:**
    - `approved`: Phase 9 cerrada; proceder a `/gsd-verify-work 9`.
    - `green-gate FAIL: <gate>`: investigation block — abortar approval;
      reportar qué gate falla y por qué.
    - `findings mismatch: <detail>`: F-09 o F-02 no tienen el status
      esperado; re-validar Plan 09-02 / 09-03 outputs.
    - `commit log mismatch`: Phase 9 no tiene los 4 commits esperados;
      revisar Plan 09-01..03 commits.
  </how-to-verify>
  <resume-signal>
    Responder con `approved` (Phase 9 closed, proceed a /gsd-verify-work
    9); OR `green-gate FAIL: <gate>` para investigation block; OR
    `findings mismatch: <detail>`; OR `commit log mismatch`.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 09-04 → green-gate commands | Validation-only; no source modification. Confianza en que Plans 09-01..03 ya están aterrizados sin regresiones. |
| `09-VALIDATION.md` ↔ Phase 9 finding files | Plan 09-04 lee findings; NO los modifica (Plan 09-02 + 09-03 ya hicieron los updates). |
| operator approval → STATE.md | Post-checkpoint el orchestrator actualiza STATE.md vía `/gsd-verify-work 9`. Plan 09-04 NO toca STATE.md. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Spoofing | `09-VALIDATION.md` status approved sin verificación | mitigate | Task 1 corre la batería completa y captura evidence antes del operator checkpoint Task 2. El operator valida findings + commits + collect-only count antes de approve. |
| T-09-04-02 | Tampering | `_state.account_id` field cleanup ripple-effect a snapshot público | mitigate | Snapshot zero-diff es uno de los gates explícitos del Task 1 (Step 8). Si diff aparece, investigation block per Pitfall 10 RESEARCH.md. |
| T-09-04-03 | Tampering | matriz `_atransport.py` accidentalmente creado durante Phase 9 | mitigate | Task 1 Step 9 explicit check (Phase 8 D-25 invariant). Si presente, abort + investigation. |
| T-09-04-04 | Information Disclosure | Green-gate evidence section leakea credentials | accept | Evidence captura output de pytest/ruff/mypy — NO incluye `.env` reads. RedactingFilter (Phase 8 D-10) activo si pytest run trigger DEBUG. |
| T-09-04-SC | Tampering | npm/pip/cargo installs | accept | NO instala paquetes. |

Severity overall: **low** — validation-only; no new code surface; existing
gates (snapshot, lint-imports, cross-leak, lint-logging) catch issues.
</threat_model>

<verification>
## Phase-Level Checks (Plan 09-04)

- **All gates GREEN:** pytest + ruff + mypy + lint-imports + cross-leak +
  lint-logging + snapshot zero-diff.
- **Phase 6/7/8 invariants preserved:** matriz aio.py LOC = 103;
  `_atransport.py` absent; snapshot zero-diff; import-linter contract holds;
  cross-leak sentinel green; lint-logging clean.
- **Findings closed:** F-09 (matriz) = FIXED + Regression; F-02 (higyrus)
  = FIXED|NO-FIX + Regression (per bucket from Plan 09-02).
- **Test count delta documented:** ~+20 tests vs Phase 8 baseline.

## Manual Checks

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| `09-VALIDATION.md` final review | Operator confirms green-gate evidence consolidation + status approval | Task 2 step-by-step |
| Phase 9 commit log validation | 4 atomic commits expected per D-12 | Task 2 Step 4 |
</verification>

<success_criteria>
1. Task 1 verified: all 11 green-gate Steps PASS; `09-VALIDATION.md` updated con frontmatter approved + checkboxes + Green-Gate Evidence section.
2. Task 2 operator checkpoint closed: operator approves Phase 9 closure.
3. 1 atomic commit per D-12: `ci(phase-09): green gate — full pytest + ruff + mypy + snapshot zero-diff + cross-leak (BUG-01..04)`.
4. Ready para `/gsd-verify-work 9`.
</success_criteria>

<output>
Create `.planning/phases/09-deferred-bug-fixes/09-04-SUMMARY.md` when done.
Summary must document:
- Files modified: 1 (`09-VALIDATION.md`).
- Green-gate evidence outputs (pytest summary, ruff clean, mypy success, lint-imports, cross-leak, snapshot, lint-logging, matriz aio.py LOC, atransport absent).
- F-09 + F-02 status confirmation post-Plan 09-02/03.
- Test count delta from Phase 8 baseline.
- Phase 9 commit list (4 atomic commits).
- Operator approval timestamp.
- Next steps: `/gsd-verify-work 9`.
</output>
</content>
</invoke>
