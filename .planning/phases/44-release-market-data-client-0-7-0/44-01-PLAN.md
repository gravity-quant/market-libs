---
phase: 44-release-market-data-client-0-7-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/pyproject.toml
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/README.md
  - uv.lock
autonomous: true
requirements: [PUB-01]
user_setup:
  - service: github
    why: "Task 3 creates and pushes a NEW public branch carrying every pending commit of milestone v1.8; the push is outward-facing even though it is reversible"
    dashboard_config:
      - task: "Ensure the `gh` CLI / git remote is authenticated for the branch push (asserted with `gh auth status`; never echo the token)"
        location: "local shell — `gh auth status`, `git remote -v`"

must_haves:
  truths:
    - "`market-data-client` reads `0.7.0` at ALL FOUR version sites per D-01: `packages/market-data-client/pyproject.toml` line 3, `packages/market-data-client/src/market_data_client/__init__.py` `__version__`, and the TWO version-pinned install lines in `packages/market-data-client/README.md` (the git install tag pin, and the wheel-URL line which carries the version TWICE — once in the tag path, once in the wheel filename)"
    - "`FeedSubscription` is importable from the package root and listed in `market_data_client.__all__`, added in BOTH the `from market_data_client.models import (...)` block and the `__all__` list, in the alphabetical slot after `FeedPipeline` — per D-05 and RESEARCH § The `FeedSubscription` Fold (a `__all__`-only edit would name an unbound attribute and is a defect, not a fix)"
    - "`uv run python tools/check_surface_types.py` exits 0 and reports 187 `__all__` names — exactly one more than the 186 measured at research baseline — asserted by positive COUNT, not merely by `0 violations`, which would also hold if the edit silently did nothing (D-11 doctrine)"
    - "`packages/market-data-client/README.md` carries a `### v0.7.0` section inserted DIRECTLY ABOVE the existing `### v0.6.0`, as the FIRST `###` child of `## Changelog`, with no staging section introduced — per D-03"
    - "That section carries TWO SEPARATE migration tables — one for `Instrument` (`GET /instruments`), one for `Segment` (`GET /instruments/segments`) — never one merged table, per D-04, because `Instrument.marketId` (preserved as an additive alias) and `Segment.marketId` (removed with no replacement) are the most confusable pair in the whole change"
    - "The `Segment` table documents the measured truthiness flip: every `Segment` a released consumer ever decoded was FALSY (three empty strings, equal to its own `empty()`), and post-0.7.0 a populated row is TRUTHY — measured direction, which is the inverse of the direction PITFALLS § SHAPE-01 states"
    - "`uv.lock` was refreshed by exactly ONE `uv lock` run executed AFTER all four version sites were bumped, registers `market-data-client` at `0.7.0`, passes `uv lock --check`, and churned by exactly one insertion and one deletion — per D-02"
    - "`uv sync --all-packages --all-extras --dev` ran after `uv lock` and before the local test mirror, so `test_dunder_version_matches_installed_distribution_metadata` reads the bumped version from a regenerated `dist-info` instead of a stale one (RESEARCH Pitfall 3)"
    - "`.github/workflows/release.yml` and `.github/workflows/ci.yml` are untouched by every commit of this plan, and `release.yml` is byte-identical by sha256 at `HEAD`, at `origin/main` and at `market-data-client-v0.6.0` — per D-02, seventh reuse without an edit"
    - "`main_market_data.py` is untouched — `DRV-MD-SEG-43` is deferred to Phase 45 per D-06, not silently folded in"
    - "No other package's version site moved: `iol-client`, `higyrus-client`, `matriz-client`, `ambito-financiero-client` and `wallets-client` all read the same version in `pyproject.toml` on `HEAD` as on `origin/main` — ROADMAP criterion 5"
    - "The full `origin/main...HEAD` diff about to become public carries no JWT-shaped token, no `client_secret`-style assignment and no tracked `.env` file"
    - "A NEW branch matching `milestone/v1.8-*` was created from local `main` with `git checkout -b`, then pushed by name as a plain fast-forward; `origin/<branch>` resolves to the same commit as local HEAD, with no force flag, no rebase, and no `git push origin main` anywhere"
    - "No PR was opened, no tag was created and no tag was pushed in this plan — those belong to 44-02 and 44-03, each behind its own blocking human gate"
  prohibitions:
    - "Never run `uv lock` more than once in this plan (D-02). It must run AFTER all four version sites are bumped; an earlier run regenerates the lock at `0.6.0` and hard-fails CI check 1 of 15 (`ci.yml:32-33`, `uv lock --check`)."
    - "Never hand-edit `uv.lock`, and never accept a lock diff wider than the single member version line — a wider diff means uv re-resolved third-party dependencies and would smuggle an unreviewed dependency change into a public release."
    - "Never edit `.github/workflows/release.yml` or `.github/workflows/ci.yml` to make a gate pass. Surface the failing job and step instead."
    - "Never touch `main_market_data.py` in this phase — `DRV-MD-SEG-43` is deferred to Phase 45 by D-06, and the phase's success criteria are 100% release mechanics."
    - "Never run a bare `uv run pytest`. The root `testpaths` collects `verification/`, which carries known pre-existing failures that no CI job executes (backlog `HARN-VERIF-01`); a bare run would block this release on out-of-scope failures."
    - "Never `git push origin main`. HEAD sits on local `main`; the first git write operation of this plan is `git checkout -b milestone/v1.8-...`. A push to `main` would publish every pending commit to the public default branch with no PR, no CI gate and no human approval."
    - "Never rewrite history: no `--force`, no `--force-with-lease`, no rebase, no interactive history edit. Dozens of SUMMARY files across Phases 35-43 cite these SHAs by value."
    - "Never hardcode a commit count, a tag count or a PR number measured at planning time. CONTEXT.md D-07 measured 84 pending commits, RESEARCH measured 86, and the value at plan-write time was 88 — re-derive every count in-line at execution time (D-10)."
    - "Never echo the `gh` OAuth token, an SSH key or any secret value into output, a commit message or the SUMMARY. Report file and line only."
  artifacts:
    - path: "packages/market-data-client/pyproject.toml"
      provides: "the version string release.yml's awk gate validates against the tag market-data-client-v0.7.0"
      contains: "version = \"0.7.0\""
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "runtime version mirror at 0.7.0 plus the FeedSubscription re-export (import + __all__)"
      contains: "__version__ = \"0.7.0\""
    - path: "packages/market-data-client/README.md"
      provides: "the consumer-facing v0.7.0 changelog with two separate migration tables, plus both re-pinned install commands"
      contains: "### v0.7.0"
    - path: "uv.lock"
      provides: "single-refresh workspace-member registration of market-data-client at 0.7.0"
      contains: "name = \"market-data-client\""
  key_links:
    - from: "packages/market-data-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "release.yml:47 awk-reads the FIRST ^version line of the TAGGED tree and hard-fails the build unless it equals the version captured from the tag market-data-client-v0.7.0"
      pattern: "version = \"0.7.0\""
    - from: "packages/market-data-client/pyproject.toml"
      to: "uv.lock"
      via: "the single `uv lock` run re-registers the workspace member; `uv lock --check` is CI check 1 of 15 (ci.yml:32-33)"
      pattern: "name = \"market-data-client\""
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/tests/test_version_metadata.py"
      via: "test_dunder_version_matches_installed_distribution_metadata compares __version__ against importlib.metadata.version; only `uv sync` after `uv lock` regenerates the dist-info and keeps it green locally"
      pattern: "__version__ = \"0.7.0\""
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "tools/check_surface_types.py"
      via: "the FeedSubscription fold adds one name to the package __all__; the gate counts __all__ names across all six packages and must report 187"
      pattern: "FeedSubscription"
    - from: "packages/market-data-client/README.md"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "the v0.7.0 migration tables document the shipped Instrument (11 fields, marketId additive alias mirrored in from_api) and Segment (segment/live_instruments only) declared by Phase 43"
      pattern: "live_instruments"
---

<!--
  Comment-text discipline allowlist. Each literal below is negative-grepped by an
  acceptance criterion against a REPO file (never against this PLAN file). They must
  appear verbatim in the actions so the executor can find and replace the exact text.
-->
<!-- planner-discipline-allow: 0.6.0 -->
<!-- planner-discipline-allow: v0.6.0 -->
<!-- planner-discipline-allow: market-data-client-v0.6.0 -->
<!-- planner-discipline-allow: market_data_client-0.6.0-py3-none-any.whl -->
<!-- planner-discipline-allow: Unreleased -->

<objective>
Prepare the `market-data-client` 0.7.0 release entirely on a local branch: bump all four version
sites (D-01), fold the two-line `FeedSubscription` export fix (D-05), author the `### v0.7.0`
changelog with two separate migration tables (D-03, D-04), refresh `uv.lock` exactly once (D-02),
mirror every CI gate locally, scan the diff for credentials, and publish a new `milestone/v1.8-*`
branch by fast-forward push.

Purpose: everything in this plan is REVERSIBLE with a commit. Nothing here merges, tags or publishes.
Isolating all reversible preparation into one autonomous plan is what lets the two irreversible
operations each sit alone behind their own blocking human gate in plans 44-02 and 44-03 — the
structural rule `.planning/research/PITFALLS.md` states as "do not co-locate the release with
autonomous plans", and the reason ROADMAP.md:55 keeps the release phase separate from the fix phase.

Output: a pushed `milestone/v1.8-*` branch whose tip carries the four bumped version sites, the
folded export, the changelog, and a single-refresh `uv.lock`, with every local CI gate green.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-release-market-data-client-0-7-0/44-CONTEXT.md
@.planning/phases/44-release-market-data-client-0-7-0/44-RESEARCH.md
@.planning/phases/44-release-market-data-client-0-7-0/44-PATTERNS.md
@.planning/phases/44-release-market-data-client-0-7-0/44-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase publishes a surface that Phase 43 already built. It creates exactly ONE new public
Python symbol binding — the `FeedSubscription` re-export at the package root — plus a set of git and
GitHub artifacts. Stated explicitly rather than omitted.

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| **`market_data_client.FeedSubscription`** (re-export: import binding + `__all__` entry; the class itself already exists at `models.py:1372` and in `models.__all__:106`) | `packages/market-data-client/src/market_data_client/__init__.py` | **44-01 Task 1** | `from market_data_client import FeedSubscription`; `"FeedSubscription" in market_data_client.__all__`; surface gate reports 187 names |
| Four bumped version sites at `0.7.0` | `pyproject.toml`, `__init__.py`, `README.md` ×2 lines | **44-01 Task 1** | `release.yml`'s own awk expression; `grep -qx` on `__version__`; install-pin counts |
| `### v0.7.0` changelog + two migration tables | `packages/market-data-client/README.md` | **44-01 Task 2** | first-`###`-after-`## Changelog` assertion + token greps inside the sliced section |
| Single-refresh `uv.lock` | `uv.lock` | **44-01 Task 3** | one lock-touching commit, `1 1` churn, `uv lock --check` |
| **New public branch `milestone/v1.8-cierre-deuda-post-v1.7`** | **`git-ref:origin/milestone/v1.8-...`** | **44-01 Task 3** | `git rev-parse HEAD` == `git rev-parse origin/<branch>` |
| Release PR against `main` | `gh-pr:<n>` (expected #16; re-derive) | 44-02 Task 1 | 15/15 by positive count |
| Two-parent merge commit on `main` | `git-ref:origin/main` | 44-02 Task 3 | `git rev-list --parents -n1` → 3 fields |
| Annotated tag `market-data-client-v0.7.0` on the re-resolved merge SHA | `git-tag:market-data-client-v0.7.0` | 44-03 Task 2 | `git cat-file -t` == `tag`; anchor == re-resolved merge SHA |
| Public GitHub Release with wheel + sdist | `gh-release:market-data-client-v0.7.0` | 44-03 Task 2 | `gh release view --json assets` exact filenames |
| Post-publish install proof from the PUBLIC wheel | throwaway venv outside the repo | 44-03 Task 3 | deep chain against the INSTALLED distribution |
</artifacts_this_phase_produces>

<reversibility_gates>
| Task | Operation | Reversibility | Gate |
|---|---|---|---|
| Task 1 | edit 4 version sites + 2 export lines | fully reversible — local commits | none needed |
| Task 2 | author the `### v0.7.0` changelog section | fully reversible — local commit | none needed |
| Task 3 | `uv lock` / `uv sync` / local CI mirror | fully reversible | none needed |
| Task 3 | `git checkout -b` + `git push origin <branch>` | reversible — a branch can be deleted or amended forward; it merges nothing and publishes no artifact | none needed |

This plan carries ZERO of the phase's two blocking gates on irreversible operations. Plan 44-02
carries the merge gate; plan 44-03 carries the tag-push gate. Adding a gate here, or performing a
merge or a tag here, violates D-08.
</reversibility_gates>

<scope_decisions>
- **Exactly four version sites (D-01).** `uv.lock` is NOT one of them — it is a separate artifact
  refreshed once in Task 3. `packages/market-data-client/tests/test_version_metadata.py` is NOT one
  either — it parses `pyproject.toml` at runtime and hardcodes nothing. `models.py`'s
  `BREAKING since 0.6.0` docstring line and the `### v0.6.0` changelog body are historical records
  and MUST NOT change. `43-03-SUMMARY.md:264` says "tres sitios"; that count is stale — trust D-01.
- **`SURF-MD-FEEDSUB-43` is folded in (D-05), `DRV-MD-SEG-43` is not (D-06).** The fold is two edits
  in one file this plan already opens. The deferral is explicit and named: `main_market_data.py` is
  Phase 45's subject, and this plan asserts it was not touched.
- **No staging section (D-03).** No README in this monorepo has ever carried one; this phase does not
  introduce the pattern. The new entry goes straight in as `### v0.7.0` above `### v0.6.0`.
- **Two separate migration tables (D-04), never merged.** The confusable pair is
  `Segment.marketId` (removed, no replacement) versus `Instrument.marketId` (preserved as an additive
  alias under the Phase-43 D-22 precedent). Merging invites exactly that confusion.
- **`release.yml` is READ-ONLY (D-02).** Seventh reuse without an edit. Immutability is proven by
  sha256 digest identity across refs, NOT by the Phase-34 tag-baseline diff form, which is
  stale-by-construction and failed three times in that phase.
- **TDD framing does not apply.** `workflow.tdd_mode` is enabled project-wide, but every task in this
  plan performs version-string edits, documentation authoring, lockfile refresh and git operations.
  There is no function whose input/output contract could be pinned by a failing test first, and
  `44-VALIDATION.md` § Wave 0 Requirements records zero new test scaffolding: `test_version_metadata.py`
  already asserts the three-way version identity and the four repo gate tools already exist and are
  green at baseline. Every task is `type: auto` with shell assertions in an automated verify block.
- **Every automated verify block is wrapped in `bash -c` (D-10)** because zsh is this machine's shell and
  Phase 40 measured word-splitting failures under it. Inside the wrapper only double quotes are used,
  with `\"` and `\$` escapes, so nothing is re-expanded by the outer shell.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Bump all four version sites to 0.7.0 and fold the two-line FeedSubscription export fix (D-01, D-05)</name>
  <files>packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/README.md</files>
  <read_first>
    - `packages/market-data-client/pyproject.toml` line 3 — currently `version = "0.6.0"`. This is
      the ONLY site `.github/workflows/release.yml` validates, using
      `awk -F\" '/^version[[:space:]]*=/{print $2; exit}'`.
    - `packages/market-data-client/src/market_data_client/__init__.py` — the
      `from market_data_client.models import (...)` block (currently `FeedIngestor`, `FeedMarket`,
      `FeedPipeline`, `Health`, … with NO `FeedSubscription`), the `__all__` list (currently
      `"FeedPipeline"`, `"Health"`, `"HealthAuth"`, `"HealthFeed"`, … with NO `"FeedSubscription"`),
      and `__version__ = "0.6.0"` on the last line of the file.
    - `packages/market-data-client/src/market_data_client/models.py` — `models.__all__` around
      line 106 (which ALREADY exports `FeedSubscription`) and the `FeedSubscription` class
      definition around line 1372. Nothing in `models.py` changes in this phase.
    - `packages/market-data-client/README.md` lines 13-25 — the two version-pinned install commands:
      the `uv add "market-data-client @ git+…@market-data-client-v0.6.0#subdirectory=…"` line, and
      the `pip install "https://github.com/…/releases/download/market-data-client-v0.6.0/market_data_client-0.6.0-py3-none-any.whl"`
      line, which carries the version TWICE.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-RESEARCH.md` § Version Sites — Measured
      (the four-row table plus the explicit non-sites list) and § The `FeedSubscription` Fold — It Is
      a Two-Line Fix (the two edit locations plus the 186→187 surface-count baseline).
    - `.planning/phases/44-release-market-data-client-0-7-0/44-PATTERNS.md` § `44-01-PLAN.md` — the
      version-site assertion pattern, the stale-survivor negative assertion and its Phase-44
      correction (region-scope the survivor grep above `## Changelog`).
    - `./CLAUDE.md` § Conventions → Import Organization and Code Style — the `__init__.py` import
      list and `__all__` are ASCII-ordered and ruff rule `I` enforces it; `FeedSubscription` goes
      after `FeedPipeline` and before `Health` in BOTH blocks.
  </read_first>
  <action>
    Four version-site edits and one two-part export fold. No file outside the three named in
    `<files>` may change.

    (a) `packages/market-data-client/pyproject.toml` line 3: `version = "0.6.0"` becomes
    `version = "0.7.0"`. This is the site `release.yml` reads with its own awk expression at line 47;
    if it drifts from the tag version the release build hard-fails.

    (b) `packages/market-data-client/src/market_data_client/__init__.py`: the trailing
    `__version__ = "0.6.0"` becomes `__version__ = "0.7.0"`. The release pipeline does NOT validate
    this site — `packages/market-data-client/tests/test_version_metadata.py` does, via
    `test_dunder_version_matches_pyproject` and
    `test_dunder_version_matches_installed_distribution_metadata`. Do not touch the
    `_ = _get_default` line above it, and do not disturb the module-level docstring or the
    `from __future__ import annotations` line.

    (c) `packages/market-data-client/README.md`, TWO lines, THREE substitutions total:
      - the git install line: `market-data-client-v0.6.0` becomes `market-data-client-v0.7.0` inside
        the `git+https://github.com/gravity-quant/market-libs.git@…#subdirectory=packages/market-data-client`
        URL;
      - the wheel URL line: BOTH occurrences move — `market-data-client-v0.6.0` in the
        `releases/download/` path segment becomes `market-data-client-v0.7.0`, AND the filename
        `market_data_client-0.6.0-py3-none-any.whl` becomes
        `market_data_client-0.7.0-py3-none-any.whl`.
      A single-substitution edit on the wheel line fixes one and leaves the other. Phase 34 shipped
      exactly that defect — a README whose changelog announced v0.5.0 while the install command still
      pinned v0.4.0 — which became a code-review Critical fixed in follow-up PR #13. Do NOT touch the
      `## Changelog` body: `### v0.6.0` and everything under it are a historical record and must
      survive this task byte-identical. Task 2 owns the changelog.

    (d) The `FeedSubscription` fold (D-05), which is TWO edits, not one. D-05 describes it as a
    one-line fix; measured, the name is absent from the import block as well as from `__all__`.
      - In the `from market_data_client.models import (` block, insert `FeedSubscription,` on its own
        line immediately after `FeedPipeline,` and before `Health,` — the ASCII-sorted slot that ruff
        rule `I` requires.
      - In the package `__all__` list, insert `"FeedSubscription",` immediately after
        `"FeedPipeline",` and before `"Health",`.
      Adding the name to `__all__` alone would be a DEFECT, not a fix: `__all__` would name an
      unbound module attribute, `from market_data_client import *` would raise, and surface tooling
      that resolves names would break. Both edits are required.

    (e) Immediately after the edit, before any commit and long before any push, run
    `uv run python tools/check_surface_types.py` (D-05 requires this gate right here). It must exit 0
    AND its summary line must read `187 \`__all__\` names`. The research baseline measured 186 names
    with 0 violations, so 187 is exactly one more. Assert the COUNT, not merely `0 violations` — a
    zero-violation result would also hold if the edit silently did nothing (this is the same
    positive-assertion doctrine D-11 imposes on the CI check count). If the number is anything other
    than 187, do NOT edit the assertion: re-derive the baseline by stashing only this file
    (`git stash push -- packages/market-data-client/src/market_data_client/__init__.py`), re-running
    the gate, restoring with `git stash pop`, and confirming the delta is exactly +1. Surface any
    unexplained discrepancy rather than absorbing it.

    (f) Also run `uv run python tools/check_uniform_structure.py` and
    `uv run python tools/surface_parity.py` — both were green at research baseline
    (`surface_parity.py` is silent on success) and both are CI lint steps.

    (g) Commit shape is Claude's discretion (D-11 of CONTEXT § Claude's Discretion covers commit
    grouping). Two coherent commits are recommended:
    `chore(market-data-client): bump to v0.7.0 en los 4 sitios de versión (D-01)` covering the
    pyproject, the `__init__.py` version line and the two README install lines; and
    `fix(market-data-client): exportar FeedSubscription desde la raíz del paquete (SURF-MD-FEEDSUB-43, D-05)`
    covering the import block and `__all__`. Do NOT run `uv lock` in this task — Task 3 owns the
    single refresh (D-02). Do NOT touch `main_market_data.py` (D-06), any file under
    `.github/workflows/`, or any other package.
  </action>
  <verify>
    <automated>bash -c 'set -e; MV=$(awk -F"\"" "/^version[[:space:]]*=/{print \$2; exit}" packages/market-data-client/pyproject.toml); test "$MV" = "0.7.0"; grep -qx "__version__ = \"0.7.0\"" packages/market-data-client/src/market_data_client/__init__.py; test "$(grep -c "market-data-client-v0.7.0" packages/market-data-client/README.md)" = "2"; test "$(grep -c "market_data_client-0.7.0-py3-none-any.whl" packages/market-data-client/README.md)" = "1"; test "$(sed -n "1,/^## Changelog/p" packages/market-data-client/README.md | grep -c "0\.6\.0")" = "0"; grep -qx "### v0.6.0" packages/market-data-client/README.md; grep -q "^    FeedSubscription,$" packages/market-data-client/src/market_data_client/__init__.py; grep -q "^    \"FeedSubscription\",$" packages/market-data-client/src/market_data_client/__init__.py; uv run python -c "import market_data_client as m; assert \"FeedSubscription\" in m.__all__; assert m.FeedSubscription is not None; assert m.__all__ == sorted(m.__all__); assert m.__version__ == \"0.7.0\""; uv run python tools/check_surface_types.py | grep -qE "187 .__all__. names"; uv run python tools/check_surface_types.py; uv run python tools/check_uniform_structure.py; uv run python tools/surface_parity.py; uv run ruff check packages/market-data-client; uv run ruff format --check packages/market-data-client; test -z "$(git diff --name-only origin/main...HEAD -- main_market_data.py .github/workflows uv.lock)"; for P in iol-client higyrus-client matriz-client ambito-financiero-client wallets-client; do test -z "$(git diff --name-only origin/main...HEAD -- packages/$P/pyproject.toml)" || { echo "OTHER PACKAGE BUMPED: $P"; exit 1; }; done; echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml` outputs `0.7.0` — read with `release.yml`'s own expression, never a TOML parser and never `importlib.metadata`, because any other reader can disagree with the gate that decides whether the release publishes
    - `grep -qx '__version__ = "0.7.0"' packages/market-data-client/src/market_data_client/__init__.py` exits 0
    - `grep -c 'market-data-client-v0.7.0' packages/market-data-client/README.md` outputs exactly `2` (the git-install tag pin and the wheel-URL path segment) and `grep -c 'market_data_client-0.7.0-py3-none-any.whl'` outputs exactly `1` — all THREE substitutions across the two install lines landed
    - `sed -n '1,/^## Changelog/p' packages/market-data-client/README.md | grep -c '0\.6\.0'` outputs `0` — the survivor grep is region-scoped above the `## Changelog` heading, because the new changelog section legitimately names the prior version as its "Antes" column label (RESEARCH OQ-3); an unscoped zero-survivor grep would be self-invalidating
    - `grep -qx '### v0.6.0' packages/market-data-client/README.md` still exits 0 — the historical changelog body survived this task untouched
    - `packages/market-data-client/src/market_data_client/__init__.py` contains `FeedSubscription,` inside the `from market_data_client.models import (` block AND `"FeedSubscription",` inside `__all__`, each in the ASCII slot after `FeedPipeline` — the `__all__`-only edit that would leave an unbound attribute was NOT made
    - `python -c "import market_data_client as m; assert 'FeedSubscription' in m.__all__ and m.FeedSubscription is not None and m.__all__ == sorted(m.__all__)"` exits 0 — the name is both exported and bound, and the list stayed sorted so ruff rule `I` cannot redden
    - `uv run python tools/check_surface_types.py` exits 0 and its summary line reports exactly `187 \`__all__\` names` — up by exactly one from the 186 measured at research baseline. The assertion is a positive count, not `0 violations`
    - `uv run python tools/check_uniform_structure.py` and `uv run python tools/surface_parity.py` both exit 0 (`surface_parity.py` prints nothing on success)
    - `uv run ruff check packages/market-data-client` and `uv run ruff format --check packages/market-data-client` both exit 0
    - `git diff --name-only origin/main...HEAD -- main_market_data.py` is empty — `DRV-MD-SEG-43` is deferred to Phase 45 (D-06) and was not silently folded in
    - `git diff --name-only origin/main...HEAD -- .github/workflows` is empty and `git diff --name-only origin/main...HEAD -- uv.lock` is empty at the end of this task — no workflow file was touched and `uv lock` has not run yet (D-02 gives Task 3 the single refresh)
    - No other package's `pyproject.toml` differs from `origin/main` — `iol-client`, `higyrus-client`, `matriz-client`, `ambito-financiero-client` and `wallets-client` are all untouched (ROADMAP criterion 5)
    - No tag was created, no branch was created, no PR was opened and nothing was pushed in this task
  </acceptance_criteria>
  <done>All four version sites read `0.7.0`, both README install lines carry all three substitutions with zero prior-version survivors above `## Changelog`, `FeedSubscription` is both imported and listed in the package `__all__`, and the surface gate reports 187 names with zero violations.</done>
</task>

<task type="auto">
  <name>Task 2: Author the `### v0.7.0` changelog with two separate Instrument/Segment migration tables (D-03, D-04)</name>
  <files>packages/market-data-client/README.md</files>
  <read_first>
    - `packages/market-data-client/README.md` lines 123-152 — the `## Changelog` H2 at 123, the
      `### v0.6.0` heading at 125, and the whole v0.6.0 entry as the EXACT shape to mirror: bold lead
      paragraph carrying the breaking callout, then a two-column table headed
      `| Antes (0.5.0 publicado) | Ahora (0.6.0) |`, then explanatory prose. Also line 153 (`### v0.5.0`)
      to confirm section boundaries.
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md`
      § 1.1 (`Instrument`, 12 rows) and § 1.2 (`Segment`, 5 rows) — the locked source content
      (D-04), in the 6-column engineering format that this task converts to the README's 2-column
      consumer format. Read the two prose paragraphs under each table too: "La fila del alias,
      explícitamente" (why `marketId` is additive, never a rename, removal scheduled for the next
      MAJOR) and "Remoción no-breaking (argumento D-13)".
    - `packages/market-data-client/src/market_data_client/models.py` — the shipped `Instrument`
      declaration (around line 788: `symbol`, `marketId`, `segment`, `expired`, `market_id`,
      `currency`, `days_to_maturity`, `maturity`, `outright`, `subscribed`, `active: bool | None = None`,
      plus the `from_api` override that mirrors wire `market_id` onto `marketId` fill-never-overwrite)
      and the shipped `Segment` declaration (around line 870: exactly `segment: str` and
      `live_instruments: int`, no `from_api` override). Write the tables from the SHIPPED shape.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-RESEARCH.md` § Migration Table Source —
      the rows to emit, and the measured truthiness-flip finding with its explicit note that
      `.planning/research/PITFALLS.md` § SHAPE-01 states the flip in the WRONG direction.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-CONTEXT.md` D-03 and D-04.
  </read_first>
  <action>
    Author one new changelog section. Wording is Claude's discretion within the established voice:
    Spanish, bold lead line carrying the breaking callout FIRST, before/after tables, then prose.

    (a) Insert a `### v0.7.0` heading on its own line DIRECTLY ABOVE the existing `### v0.6.0`
    heading, so it becomes the FIRST `###` child of `## Changelog` (D-03). Introduce NO staging
    section: no README in this monorepo has ever carried one and this phase does not introduce the
    pattern. Do not renumber, reword, reorder or reindent any existing entry — `### v0.6.0` and
    `### v0.5.0` and everything under them are historical records.

    (b) Open the section with a bold lead paragraph naming the rupture, mirroring the shape of the
    v0.6.0 entry: `Instrument` and `Segment` are reconciled against a fresh read of the live wire —
    `Segment` is replaced whole (disjoint key sets) while `Instrument` gains seven fields and loses
    one — breaking, minor bump on the 0.x line. State in that same paragraph that
    `Instrument.marketId` is NOT renamed: it is preserved as an additive alias over the new
    `market_id`, with removal scheduled for the next MAJOR.

    (c) Emit TWO SEPARATE two-column tables (D-04), never one merged table, each introduced by a bold
    label naming its model and its endpoint — `Instrument` for `GET /instruments`, `Segment` for
    `GET /instruments/segments`. Use bold labels rather than a new heading level so the section keeps
    the exact shape of the v0.6.0 entry. Column headers follow the established form:
    `| Antes (0.6.0 publicado) | Ahora (0.7.0) |`. The reason not to merge is load-bearing and belongs
    in the prose: `Segment.marketId` is removed with no replacement while `Instrument.marketId`
    survives as an additive alias — the single most confusable pair in the change.

    Rows for the `Instrument` table:
      - `inst.instrumentType` → removed, no replacement; the key never travelled on the wire, so
        every released consumer always decoded the empty string.
      - `inst.marketId` → still works, as an ADDITIVE ALIAS over `inst.market_id`; against a real
        payload it used to sit permanently at the empty string and now carries the real value,
        because `Instrument.from_api` mirrors wire `market_id` onto it before the walker sees the
        payload — fill-never-overwrite, on a copy of the caller's dict. Prefer `inst.market_id`;
        removal is scheduled for the next MAJOR.
      - new fields with no prior expression: `inst.market_id` (`str`), `inst.currency` (`str`),
        `inst.days_to_maturity` (`int`), `inst.maturity` (`str`), `inst.outright` (`bool`),
        `inst.subscribed` (`bool`).
      - `inst.active` is new and is `bool | None` — check `is None`; do not assume `bool`. Say why in
        the prose: the `bool` member of that union was never observed (the wire sent null in 50/50
        rows), so it is a declared assumption that a future divergence census would surface, and
        declaring it plain `bool` would have turned a measured `extra` into a permanent `missing` on
        every catalogue read.
      - `symbol`, `segment` and `expired` are unchanged — do not claim otherwise.

    Rows for the `Segment` table:
      - `seg.marketSegmentId` → removed, no replacement; always decoded the empty string.
      - `seg.marketId` → removed, no replacement; always decoded the empty string. This is the row
        that must not be confused with `Instrument.marketId`.
      - `seg.description` → removed, no replacement; always decoded the empty string.
      - `seg.segment` (`str`) is new; `seg.live_instruments` (`int`) is new.
      - The TRUTHINESS row, in the MEASURED direction: `if seg:` was always false before, because the
        old and new key sets are disjoint so every decoded row equalled its own `empty()` and
        `SafeModel.__bool__` reports a model carrying nothing as falsy; from 0.7.0 `if seg:` is true
        for any populated row. A consumer holding `if seg:` or `[s for s in segs if s]` silently
        received an empty result before and receives every row now. Write this from the measurement:
        `.planning/research/PITFALLS.md` § SHAPE-01 states this flip backwards
        ("always-truthy-with-empty-strings to falsy-when-empty"); the executed measurement is the
        inverse and the changelog follows the measurement.

    (d) Close with prose covering: that `Segment` is a whole-model replacement rather than an
    extension and therefore carries no `from_api` override; that the three removed `Segment` fields
    and `Instrument.instrumentType` are non-breaking in practice under the Phase-43 D-13 argument,
    since their keys never existed on the wire so no released consumer can ever have held a populated
    value; and that `Segment` is deliberately NOT alias-mapped under the D-22 precedent, because that
    precedent's precondition is one key with a camelCase/snake_case spelling variant, whereas
    `marketSegmentId` and `segment` are simply different names.

    (e) Do not change any version site in this task — Task 1 owns them. Do not run `uv lock`. Commit
    the README alone; suggested subject:
    `docs(market-data-client): changelog v0.7.0 con las dos tablas de migración Instrument/Segment (D-03, D-04)`.
  </action>
  <verify>
    <automated>bash -c 'set -e; F=packages/market-data-client/README.md; test "$(awk "/^## Changelog\$/{f=1;next} f&&/^### /{print;exit}" "$F")" = "### v0.7.0"; grep -qx "### v0.6.0" "$F"; grep -qx "### v0.5.0" "$F"; test "$(grep -c "^### v0.7.0\$" "$F")" = "1"; SEC=$(awk "/^### v0\.7\.0\$/{f=1;next} /^### v/{f=0} f" "$F"); test -n "$SEC"; for T in "instrumentType" "marketId" "market_id" "currency" "days_to_maturity" "maturity" "outright" "subscribed" "active" "marketSegmentId" "description" "live_instruments" "if seg:" "Antes (0.6.0 publicado)" "Ahora (0.7.0)"; do printf "%s\n" "$SEC" | grep -qF -- "$T" || { echo "V0.7.0 SECTION MISSING TOKEN: $T"; exit 1; }; done; test "$(printf "%s\n" "$SEC" | grep -c "^| Antes (0.6.0 publicado) | Ahora (0.7.0) |\$")" = "2"; OLD=$(awk "/^### v0\.6\.0\$/{f=1;next} /^### v/{f=0} f" "$F"); for T in "market_data.last.price" "market_data.bids" "not snapshot.market_data" "Antes (0.5.0 publicado)"; do printf "%s\n" "$OLD" | grep -qF -- "$T" || { echo "HISTORICAL v0.6.0 BODY REGRESSED: $T"; exit 1; }; done; test "$(grep -c "^## Unreleased" "$F")" = "0"; test "$(sed -n "1,/^## Changelog/p" "$F" | grep -c "0\.6\.0")" = "0"; test -z "$(git diff --name-only origin/main...HEAD -- main_market_data.py .github/workflows uv.lock)"; echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Changelog$/{f=1;next} f&&/^### /{print;exit}' packages/market-data-client/README.md` outputs `### v0.7.0` — the new entry is the FIRST `###` child of `## Changelog`, directly above `### v0.6.0` (D-03)
    - `grep -c '^### v0.7.0$'` outputs `1`, and `grep -qx '### v0.6.0'` and `grep -qx '### v0.5.0'` both still exit 0 — nothing historical was renamed or dropped
    - `grep -c '^## Unreleased' packages/market-data-client/README.md` outputs `0` — no staging section was introduced (D-03)
    - Slicing the file on `^### v0.7.0$` up to the next `^### v` yields a non-empty section containing every one of these tokens: `instrumentType`, `marketId`, `market_id`, `currency`, `days_to_maturity`, `maturity`, `outright`, `subscribed`, `active`, `marketSegmentId`, `description`, `live_instruments`, and the literal `if seg:`
    - That same section contains EXACTLY TWO header rows reading `| Antes (0.6.0 publicado) | Ahora (0.7.0) |` — two separate tables, one per model, never one merged table (D-04)
    - The `Instrument` table documents `marketId` as an additive alias over `market_id` that now carries the real value, explicitly not a rename, with removal scheduled for the next MAJOR; and documents `active` as `bool | None` requiring an `is None` check
    - The `Segment` table documents `marketSegmentId`, `marketId` and `description` as removed with no replacement, `segment` and `live_instruments` as new, and carries the truthiness row in the MEASURED direction — `if seg:` was always false before and is true for a populated row now. The direction stated in `.planning/research/PITFALLS.md` § SHAPE-01 is the inverse and was NOT copied
    - Slicing the file on `^### v0.6.0$` still yields a section containing `market_data.last.price`, `market_data.bids`, `not snapshot.market_data` and `Antes (0.5.0 publicado)` — the historical entry survived byte-equivalent
    - `sed -n '1,/^## Changelog/p' packages/market-data-client/README.md | grep -c '0\.6\.0'` still outputs `0` — Task 1's install-pin work was not regressed by this edit
    - `git diff --name-only origin/main...HEAD -- main_market_data.py .github/workflows uv.lock` is empty
    - The commit touches `packages/market-data-client/README.md` and no other file
  </acceptance_criteria>
  <done>`packages/market-data-client/README.md` carries a `### v0.7.0` entry as the first child of `## Changelog`, opening with a bold breaking callout and containing two separate two-column migration tables — `Instrument` and `Segment` — that a consumer can follow field by field, including the measured truthiness flip, without opening the diff.</done>
</task>

<task type="auto">
  <name>Task 3: Single `uv lock` + `uv sync`, full local CI mirror, workflow digest identity, credential scan, and fast-forward push of the NEW milestone branch (D-02, D-07, D-10)</name>
  <files>uv.lock</files>
  <read_first>
    - `uv.lock` — the `[[package]]` member block for `market-data-client` (around lines 487-489:
      `name = "market-data-client"` / `version = "0.6.0"` / `source = { editable = … }`). Only that
      member's version line may move.
    - `.github/workflows/ci.yml` lines 22-167 — READ-ONLY. All four job bodies. The `lint` job runs
      `uv lock --check`, `uv sync --frozen`, `ruff check`, `ruff format --check`, `lint-imports`, the
      `logging.basicConfig` / `logging.root` grep over `packages/*/src/`,
      `tools/check_decode_intactness.py`, `tools/check_uniform_structure.py`,
      `tools/check_surface_types.py`, and the EXPLICIT `verification/` file allowlist at lines 80-93
      — re-derive that list from the file at execution time rather than trusting any literal
      elsewhere, because Phase 42 added a thirteenth entry to it. The `typecheck` job runs
      `uv run mypy` plus `uv run mypy packages/<pkg>/tests` for all six packages. The `test` job runs
      `pytest packages/<pkg>` per matrix cell, 6 packages × 2 Python versions.
    - `.github/workflows/release.yml` — READ-ONLY, DO NOT EDIT (D-02). Needed only to take its
      sha256 for the digest-identity assertion.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-RESEARCH.md` § Common Pitfalls 2
      (unrefreshed `uv.lock` fails CI check 1 of 15), 3 (stale `dist-info` produces a LOCAL-ONLY
      false red on `test_dunder_version_matches_installed_distribution_metadata`; `uv lock` does not
      regenerate it, only `uv sync` does), 4 (`bash -c` wrapping), 5 (re-derive counts), and
      § Git / GitHub State — Measured.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-PATTERNS.md` § Shared Patterns →
      "Credential scan before the public push", "Local CI mirror, never a bare `uv run pytest`",
      "Workflow immutability by sha256 digest identity — DO NOT copy the Phase-34 form", and
      § `44-01-PLAN.md` → the branch create-then-push block and the single-`uv lock` churn assertion.
    - `.planning/phases/44-release-market-data-client-0-7-0/44-CONTEXT.md` D-02, D-07, D-10.
  </read_first>
  <action>
    Refresh the lockfile once, re-sync the environment, run every gate CI will run, prove the diff
    about to become public carries no credential, then create and publish the release branch.

    (a) Run `uv lock` — exactly once, and only now (D-02). It MUST run AFTER Task 1's
    `pyproject.toml` edit: `uv lock` reads workspace-member versions from `pyproject.toml`, so an
    earlier run would regenerate the lock at `0.6.0` and fail CI check 1 of 15
    (`ci.yml:32-33`, `uv lock --check`). Never hand-edit `uv.lock`.

    Then assert the churn. Exactly one workspace member bumped in this phase, so
    `git show --numstat --format= <the lock commit> -- uv.lock` must report exactly `1 1` — one
    insertion, one deletion. Phase 40 measured `N N` for N bumped members, confirming one line per
    member. Anything wider means uv re-resolved third-party dependencies and would smuggle an
    unreviewed dependency change into a public release: STOP and surface it rather than committing.
    Run `uv lock --check` (exit 0), then confirm the line following `name = "market-data-client"` in
    `uv.lock` reads `version = "0.7.0"`.

    Commit `uv.lock` alone. Suggested subject:
    `chore(deps): refrescar uv.lock para market-data-client v0.7.0 (D-02)`.

    (b) Run `uv sync --all-packages --all-extras --dev` immediately after the lock commit and BEFORE
    the mirror. This is mandatory, not optional (RESEARCH Pitfall 3): the editable install's metadata
    directory on disk is literally `market_data_client-0.6.0.dist-info` with the old version inside
    it, `importlib.metadata.version("market-data-client")` reads that directory, and `uv lock` does
    NOT regenerate it. Without the sync,
    `packages/market-data-client/tests/test_version_metadata.py::test_dunder_version_matches_installed_distribution_metadata`
    goes red locally while CI stays green (CI does a fresh `uv sync --frozen` on a clean runner) —
    a local-only false red that invites a spurious "fix" to a file that is already correct. After the
    sync, assert the three-way identity directly:
    `uv run python -c "from importlib.metadata import version; import market_data_client; assert version('market-data-client') == market_data_client.__version__ == '0.7.0'"`.

    (c) Mirror EVERY CI job body locally, in this order, each must succeed:
    `uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`;
    the logging gate — grep `packages/*/src/` with the same extended regex `ci.yml:47` runs
    (`logging\.basicConfig\s*\(|logging\.root\.\w`) and require NO match;
    `uv run python tools/check_decode_intactness.py`;
    `uv run python tools/check_uniform_structure.py`;
    `uv run python tools/check_surface_types.py`;
    the `verification/` allowlist EXACTLY as `ci.yml:80-93` spells it — re-read the file and pass the
    same explicit file list, which currently runs thirteen files ending with
    `verification/test_literal_census_venue_gate.py`;
    `uv run pre-commit run --all-files`;
    `uv run mypy`;
    `uv run mypy packages/<pkg>/tests` for all six packages;
    `uv run pytest packages/<pkg> -q` for all six packages.

    NEVER run a bare `uv run pytest`: the root `testpaths` collects `verification/`, which carries
    known pre-existing failures that no CI job executes (backlog `HARN-VERIF-01`); a bare run would
    block this release indefinitely on out-of-scope failures. If `pre-commit` rewrites any file,
    amend the owning commit with the fix and re-run the whole mirror from the top. Never patch
    `ci.yml` or `release.yml` to make a gate pass.

    (d) Assert the workflow files are untouched, using the sha256 digest-identity form and NOT the
    Phase-34 tag-baseline diff form. Phase 34's assertion
    (`git diff --name-only <prior-release-tag>..HEAD -- .github/workflows` must be empty) is
    stale-by-construction — `ci.yml` legitimately changes between releases, and that assertion failed
    three times during that phase. The two assertions that are true and meaningful here:
      - `release.yml` is byte-identical everywhere: take its sha256 at `HEAD`, at `origin/main` and
        at `market-data-client-v0.6.0`; `sort -u` over those digests must yield exactly one line.
        Plan 44-03 extends the same check to the new tag, closing the D-02 "seventh reuse without an
        edit" claim.
      - This phase touched no workflow file: `git diff --name-only origin/main...HEAD -- .github/workflows`
        must produce zero lines.

    (e) Re-run the credential scan over the FULL diff this branch will publish. `git fetch origin main`
    first, then check `git diff origin/main...HEAD` for a JWT-shaped token
    (`eyJ[A-Za-z0-9_-]{20,}`) and for a `client_secret`-style assignment of a 20+ character value;
    separately assert that no TRACKED file has a path component equal to `.env` — per CLAUDE.md
    per-package `.env` files exist on disk, so confirm none is tracked and only the `.env.example`
    templates are. If ANY of these matches, STOP: do not push, do not proceed to plan 44-02, and
    surface the exact file and line. Never echo a token, a secret value, the `gh` OAuth token or the
    SSH key into output — report file and line only. This scan runs here, immediately before the
    push, because the diff spans every pending commit of milestone v1.8 and becomes permanently
    public on merge.

    (f) Create the branch, THEN push it. HEAD currently sits on local `main`, which is where all of
    this milestone's pending commits live. Re-derive the pending-commit count in-line with
    `git rev-list --count origin/main..HEAD` and record the value — do NOT trust any literal: D-07
    measured 84, RESEARCH measured 86 and the value at plan-write time was 88, which is exactly the
    drift D-10 exists to prevent. Run `git checkout -b milestone/v1.8-cierre-deuda-post-v1.7`
    (branch name is Claude's discretion within the `milestone/vX.Y-slug` pattern that PRs #12 and #15
    both followed); local `main` stays at the same SHA. Then
    `git push origin milestone/v1.8-cierre-deuda-post-v1.7` — a plain fast-forward, by name.

    A `git push origin main` here would publish every pending commit directly to the public default
    branch with no PR, no CI gate and no human approval. Do not run it. `--force` and
    `--force-with-lease` are FORBIDDEN. If the push is rejected as non-fast-forward, STOP and surface
    it — do not escalate to a force flag, do not rebase, and do not merge `origin/main` into the
    branch.

    (g) Do NOT run `/gsd-pr-branch` and do NOT filter `.planning/` out of the branch — the planning
    artifacts ship with the release, as they have in every prior release of this repository. Do NOT
    open, edit or merge any PR in this plan; plan 44-02 owns the PR behind its own human gate. Do NOT
    create or push any tag; plan 44-03 owns the tag behind a second, independent human gate.
  </action>
  <verify>
    <automated>bash -c 'set -e; git fetch origin main --quiet; uv lock --check; grep -A1 "^name = \"market-data-client\"\$" uv.lock | grep -qx "version = \"0.7.0\""; LOCKC=$(git log --format=%H origin/main..HEAD -- uv.lock | wc -l | tr -d " "); test "$LOCKC" = "1"; test "$(git show --numstat --format= "$(git log --format=%H origin/main..HEAD -- uv.lock)" -- uv.lock | awk "{print \$1, \$2}")" = "1 1"; uv sync --all-packages --all-extras --dev; uv run python -c "from importlib.metadata import version; import market_data_client; assert version(\"market-data-client\") == market_data_client.__version__ == \"0.7.0\""; uv run ruff check .; uv run ruff format --check .; uv run lint-imports; ! grep -rnE --include="*.py" "logging\.basicConfig\s*\(|logging\.root\.\w" packages/*/src/; uv run python tools/check_decode_intactness.py; uv run python tools/check_uniform_structure.py; uv run python tools/check_surface_types.py; VFILES=$(awk "/^          uv run pytest -q/{f=1;next} f&&/verification\//{gsub(/[ \\\\]/,\"\"); print} f&&/^\$/{f=0}" .github/workflows/ci.yml); test "$(printf "%s\n" "$VFILES" | wc -l | tr -d " ")" -ge 13; uv run pytest -q $VFILES; uv run pre-commit run --all-files; uv run mypy; for P in higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client; do uv run mypy "packages/$P/tests" || exit 1; done; for P in higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client; do uv run pytest "packages/$P" -q || exit 1; done; test "$(for R in HEAD origin/main market-data-client-v0.6.0; do git show "$R:.github/workflows/release.yml" | shasum -a 256 | cut -d" " -f1; done | sort -u | wc -l | tr -d " ")" = "1"; test "$(git diff --name-only origin/main...HEAD -- .github/workflows | wc -l | tr -d " ")" = "0"; ! git diff origin/main...HEAD | grep -qE "eyJ[A-Za-z0-9_-]{20,}"; ! git diff origin/main...HEAD | grep -qiE "client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}"; test -z "$(git ls-files | grep -E "(^|/)\.env\$")"; B=$(git rev-parse --abbrev-ref HEAD); case "$B" in milestone/v1.8-*) : ;; *) echo "NOT ON A v1.8 MILESTONE BRANCH: $B"; exit 1 ;; esac; git fetch origin "$B" --quiet; test "$(git rev-parse HEAD)" = "$(git rev-parse "origin/$B")"; test -z "$(git status --porcelain)"; AHEAD=$(git rev-list --count origin/main..HEAD); test "$AHEAD" -gt 0; echo "pending_commits=$AHEAD"; test -z "$(git tag -l "market-data-client-v0.7.0")"; test "$(gh pr list --state open --base main --json number --jq "length")" = "0"; echo PASS'</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` exits 0, and the line following `^name = "market-data-client"$` in `uv.lock` is `version = "0.7.0"`
    - Exactly ONE commit in `origin/main..HEAD` touches `uv.lock` (`git log --format=%H origin/main..HEAD -- uv.lock | wc -l` outputs `1`) — D-02's single-refresh rule is proven, not assumed
    - `git show --numstat --format= <that commit> -- uv.lock | awk '{print $1, $2}'` outputs exactly `1 1` — one insertion, one deletion, i.e. the single member version line. A wider churn means uv re-resolved third-party dependencies and the task halted instead of committing
    - `uv sync --all-packages --all-extras --dev` ran after `uv lock` and before the test mirror, and `python -c "from importlib.metadata import version; import market_data_client; assert version('market-data-client') == market_data_client.__version__ == '0.7.0'"` exits 0 — the stale `dist-info` false-red (RESEARCH Pitfall 3) was eliminated rather than misdiagnosed
    - All lint gates exit 0: `uv lock --check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`, the `packages/*/src/` logging grep finding no match, `tools/check_decode_intactness.py`, `tools/check_uniform_structure.py`, `tools/check_surface_types.py`, and the `verification/` allowlist re-derived from `ci.yml:80-93` (at least 13 files, ending with `test_literal_census_venue_gate.py`)
    - `uv run pre-commit run --all-files` passes with zero file rewrites; `uv run mypy` succeeds; `uv run mypy packages/<pkg>/tests` succeeds for all six packages; `uv run pytest packages/<pkg> -q` passes for all six packages
    - No bare `uv run pytest` was executed at any point in this plan
    - The sha256 of `.github/workflows/release.yml` is identical at `HEAD`, `origin/main` and `market-data-client-v0.6.0` — `sort -u` over those digests yields exactly one line. Phase 34's `git diff --name-only <prior-release-tag>..HEAD -- .github/workflows` form was NOT used; it is stale-by-construction and failed three times in that phase
    - `git diff --name-only origin/main...HEAD -- .github/workflows | wc -l` outputs `0`
    - The credential scan over `git diff origin/main...HEAD` finds no JWT-shaped token and no `client_secret`-style assignment, and `git ls-files | grep -E '(^|/)\.env$'` is empty. Any finding reports file and line only, never the matched value
    - `git rev-parse --abbrev-ref HEAD` matches `milestone/v1.8-*` and the command history shows `git checkout -b` before any push — the branch was CREATED in this task (D-07: no v1.8 branch existed)
    - `git rev-parse HEAD` equals `git rev-parse origin/<branch>`, and the command history shows `git push origin <branch>` by name with no `--force`, no `--force-with-lease`, no `git rebase`, no `git merge origin/main`, and **no `git push origin main` anywhere in the phase**
    - The pending-commit count was RE-DERIVED in-line with `git rev-list --count origin/main..HEAD` and recorded in the SUMMARY; no literal from CONTEXT.md (84), RESEARCH.md (86) or the plan text (88) was trusted (D-10)
    - `git status --porcelain` is empty — a clean tree is a precondition for plan 44-02
    - `git tag -l 'market-data-client-v0.7.0'` is empty and `gh pr list --state open --base main --jq 'length'` outputs `0` — no tag and no PR was created in this plan
  </acceptance_criteria>
  <done>`uv.lock` was refreshed by a single `uv lock` run with an exact `1 1` churn, `uv sync` regenerated the dist-metadata so the three-way version identity holds locally, every local CI-mirror gate across all four job bodies is green, `release.yml` is provably byte-identical across every relevant ref, the credential scan over `origin/main...HEAD` is clean, and a newly created `milestone/v1.8-*` branch exists on `origin` at local HEAD via a plain fast-forward push — with no PR, no tag and no merge anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → public branch on `origin` | The push publishes every pending commit of milestone v1.8 to a public repository. Reversible (the branch can be deleted or amended forward) but permanently visible in the interim. |
| `pyproject.toml` version → `release.yml`'s awk gate | The pipeline reads this ONE site and hard-fails on mismatch. It does NOT read `__init__.py` or `README.md`, so those two sites can drift silently and ship green. |
| `__all__` → the published import surface | `__all__` is the consumer-visible surface baked into the wheel; naming an unbound attribute breaks star-imports for every consumer. |
| workspace `pyproject.toml` → `uv.lock` | `uv lock --check` is the FIRST CI step; a stale lock reddens the whole gate. A wide lock diff can smuggle an unreviewed third-party dependency change into a public release. |
| repo diff → `gh` OAuth token / `.env` credentials | CLAUDE.md forbids committing `.env` or exposing credentials; the diff about to become public spans every pending commit of the milestone. |
| `.github/workflows/` → the release supply chain | A workflow edit inside a release branch is a supply-chain change, not a cosmetic one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | version drift between the four sites — `release.yml` validates only `pyproject.toml` | mitigate | Task 1 asserts `pyproject.toml` with `release.yml`'s OWN awk expression, `__version__` with `grep -qx`, and both README install lines by exact count (`2` tag pins, `1` wheel filename) with a region-scoped zero-survivor grep above `## Changelog`. Task 3 adds the three-way `importlib.metadata` identity after `uv sync`. ASVS L1 V14. |
| T-44-02 | Information Disclosure | `gh` OAuth token, SSH key or a tracked `.env` entering a public branch | mitigate | Task 3 (e) scans the full `origin/main...HEAD` diff for JWT-shaped tokens and `client_secret`-style assignments and asserts no tracked path component equals `.env`, immediately before the push; findings report file and line only, never the matched value. `gh auth status` is used instead of ever echoing a token. ASVS L1 V7. |
| T-44-03 | Tampering | workflow-file tampering smuggled inside a release branch | mitigate | Task 3 (d) proves `.github/workflows/release.yml` is byte-identical by sha256 at `HEAD`, `origin/main` and `market-data-client-v0.6.0`, and asserts a zero-line workflow diff over `origin/main...HEAD`. The Phase-34 tag-baseline diff form is explicitly NOT reused (it failed three times). ASVS L1 V14. |
| T-44-06 | Tampering / Repudiation | history rewrite invalidating SHAs cited across Phases 35-43 | mitigate | `must_haves.prohibitions` forbid `--force`, `--force-with-lease`, rebase and interactive history edits; Task 3 (f) mandates create-then-fast-forward-push and instructs a hard STOP on a non-fast-forward rejection rather than escalation to a force flag. |
| T-44-08 | Tampering | an unreviewed third-party dependency change riding into a public release via a wide `uv.lock` diff | mitigate | Task 3 (a) asserts exactly one lock-touching commit with an exact `1 1` numstat churn and halts instead of committing on anything wider; `uv lock --check` is re-asserted locally before the push. |
| T-44-09 | Tampering | `__all__` naming an unbound attribute after a one-sided export fold | mitigate | Task 1 (d) requires BOTH edits (import block and `__all__`) and Task 1's verify resolves the name at runtime (`m.FeedSubscription is not None`) in addition to membership, plus a positive surface-gate count of 187 rather than a bare `0 violations`. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | No third-party package is installed by this plan. `44-RESEARCH.md` § Package Legitimacy Audit records zero `[ASSUMED]`, zero `[SUS]` and zero `[SLOP]` packages for this phase, so no legitimacy checkpoint applies. The only install-shaped operation in the phase is 44-03 Task 3, which fetches this repository's own wheel from its public Release URL. |
</threat_model>

<verification>
1. All four version sites read `0.7.0`, each asserted with the reader that matters for it (`release.yml`'s awk for `pyproject.toml`, `grep -qx` for `__version__`, exact counts for the two README install lines) — D-01.
2. `sed -n '1,/^## Changelog/p'` over the README finds zero prior-version survivors; the `### v0.6.0` and `### v0.5.0` historical entries are intact — D-01 + OQ-3.
3. `FeedSubscription` is imported AND in `__all__`, resolves at runtime, and the surface gate reports 187 names with zero violations — D-05.
4. `### v0.7.0` is the first `###` under `## Changelog` and carries exactly two `| Antes (0.6.0 publicado) | Ahora (0.7.0) |` header rows — D-03 + D-04.
5. `uv.lock` has exactly one touching commit with a `1 1` churn and passes `uv lock --check`; `uv sync` ran after it — D-02 + RESEARCH Pitfall 3.
6. Every CI job body is mirrored green locally, with no bare `uv run pytest`.
7. `release.yml` sha256 is identical at `HEAD`, `origin/main` and `market-data-client-v0.6.0`; the workflow diff over `origin/main...HEAD` is zero lines — D-02.
8. The credential scan over the full public-bound diff is clean.
9. `main_market_data.py` is untouched and no other package's version site moved — D-06 + ROADMAP criterion 5.
10. `origin/milestone/v1.8-*` resolves to local HEAD; no PR, no tag, no merge, no push to `main`.
</verification>

<success_criteria>
- Every version site a consumer can read says `0.7.0`, including the two the release pipeline cannot check — which is precisely the hole that shipped the Phase-34 README defect.
- The changelog lets a consumer migrate `Instrument` and `Segment` field by field without opening the diff, with the two `marketId` rows kept in separate tables so the additive alias cannot be mistaken for the removal.
- The lockfile moved exactly once, by exactly one line, and the environment was re-synced so the local gate tells the truth instead of a version-shaped lie.
- Everything in this plan is still reversible with a commit: nothing merged, nothing tagged, nothing published — the two irreversible operations remain fully gated in 44-02 and 44-03.
</success_criteria>

<output>
Create `.planning/phases/44-release-market-data-client-0-7-0/44-01-SUMMARY.md` when done.

Record in the SUMMARY: every commit SHA with its subject; the exact version transitions at all four
sites; the pre- and post-fold surface-gate name counts as literal tool output; the measured `uv.lock`
churn (`git show --numstat` output verbatim); confirmation that `uv sync` ran after `uv lock` and the
three-way version identity assertion passed; the `release.yml` sha256 and the refs it was compared
across; the credential-scan result (clean / findings by file and line only, never a matched value);
the exact branch name created; the RE-DERIVED pending-commit count from
`git rev-list --count origin/main..HEAD` at execution time, with an explicit note that the literals
84 (CONTEXT.md D-07), 86 (RESEARCH.md) and 88 (plan-write time) were all superseded; and an explicit
statement that no PR was opened, no tag was created and nothing irreversible happened in this plan.
</output>
