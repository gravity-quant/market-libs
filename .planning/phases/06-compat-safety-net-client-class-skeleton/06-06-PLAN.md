---
phase: 06-compat-safety-net-client-class-skeleton
plan: 06
type: execute
wave: 1
depends_on: [06-01, 06-02, 06-05]
files_modified:
  - packages/matriz-client/src/matriz_client/_state.py
  - packages/matriz-client/src/matriz_client/client.py
  - packages/matriz-client/src/matriz_client/aio.py
  - packages/matriz-client/src/matriz_client/ws_client.py
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/matriz-client/tests/conftest.py
  - packages/matriz-client/tests/test_client.py
  - packages/matriz-client/tests/test_ws_client.py
  - packages/matriz-client/tests/test_client_class.py
  - packages/ambito-financiero-client/tests/test_harness_mutation_gate.py
  - verification/mutation_gate.py
  - verification/snapshots/matriz-client-surface.txt
autonomous: true
requirements: [REFAC-02]

user_setup: []

must_haves:
  truths:
    - "matriz_client.Client (sync) exists with __enter__/__exit__/close() mirroring current REST surface; login parses X-Auth-Token from response headers (not body) per D-22 and stores into self._state.token."
    - "matriz_client.AsyncClient STUB (per RESEARCH.md Open Q #1) exists in new packages/matriz-client/src/matriz_client/aio.py — contains only __init__/__aenter__/__aexit__/aclose()/__repr__/__reduce__/__deepcopy__. No REST methods. No top-level configure/get_X shims, no _default_async_client. Phase 10 REFAC-04 grows it."
    - "_ClientState fields: base_url, username, password, token, token_expires_at (rename from _token_ts), http_client (renamed from _session per Pitfall #5; absorbed). NO refresh_token (matriz has no OAuth refresh)."
    - "PEP 562 shim in client.py forwards _token → state.token, _token_ts → state.token_expires_at, _session → state.http_client (preserve matriz naming for any callers still reading `_session`), AND _base_url → state.base_url (Open Q #4 — extend D-02 because verification/mutation_gate.py reads `matriz_client.client._base_url`)."
    - "ws_client.py migrated: replaces `_rest._base_url` with `_rest._get_default()._state.base_url` and `_rest._ensure_token()` with `_rest._get_default()._ensure_token()` and `_rest._token` with `_rest._get_default()._state.token` (or rely on the extended shim)."
    - "verification/mutation_gate.py reads via the new shim (still uses `matriz_client.client._base_url` syntactically — works because the shim forwards `_base_url` for matriz only, per Open Q #4 mitigation)."
    - "packages/ambito-financiero-client/tests/test_harness_mutation_gate.py migrated: replaces `monkeypatch.setattr(matriz_client.client, '_base_url', url)` (lines 99, 110) with `matriz_client.configure(base_url=url)` (Pitfall #4)."
    - "verification/snapshots/matriz-client-surface.txt regenerated with Client and AsyncClient entries; baseline preserved."
    - "matriz fixture-reaches-production guard (sync, X-Auth-Token header) MIGRATED to matriz_client.configure(token=..., token_expires_at=...) and passes; matriz async guard REMAINS as pytest.skip (Phase 10 REFAC-04)."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_state.py"
      provides: "@dataclass(slots=True) _ClientState"
      min_lines: 30
    - path: "packages/matriz-client/src/matriz_client/aio.py"
      provides: "NEW file containing only stub AsyncClient class"
      min_lines: 50
    - path: "packages/matriz-client/tests/test_client_class.py"
      provides: "Skeleton tests for sync Client + stub AsyncClient lifecycle + ws_client integration + base_url shim test"
      min_lines: 80
  key_links:
    - from: "matriz_client.ws_client (post-refactor)"
      to: "_rest._get_default()._state.base_url + _rest._get_default()._ensure_token()"
      via: "Module-private access through the lazy default Client"
      pattern: "_rest\\._get_default\\(\\)"
    - from: "verification/mutation_gate.py (post-refactor)"
      to: "matriz_client.client._base_url (forwarded via PEP 562 shim)"
      via: "PEP 562 read forwarding to _get_default()._state.base_url"
      pattern: "matriz_client\\.client\\._base_url"
---

<objective>
Land the matriz skeleton with the unique Phase 6 wrinkles: (1) matriz `Client.login()` reads the bearer from `response.headers["X-Auth-Token"]` not from the JSON body (D-22); (2) matriz has NO `aio.py` today — ship a STUB `AsyncClient` per RESEARCH.md Open Q #1 with lifecycle only; (3) `matriz_client.client._session` becomes `_state.http_client` (Pitfall #5 rename); (4) `_base_url` MUST be added to the PEP 562 shim allowlist for matriz only because `verification/mutation_gate.py:55` reads it (Open Q #4 — addendum to D-02); (5) `ws_client.py` reads `_rest._base_url`, `_rest._token`, calls `_rest._ensure_token()` — migrate the cross-module call sites; (6) `packages/ambito-financiero-client/tests/test_harness_mutation_gate.py:99,110` writes `monkeypatch.setattr(matriz_client.client, "_base_url", url)` — migrate to `matriz_client.configure(base_url=url)`.

Purpose: Matriz is the trickiest package because it has the WebSocket cross-module dependency AND the cross-package test (`test_harness_mutation_gate.py` lives under ambito-financiero but writes to matriz). The matriz async stub satisfies CONTEXT.md success criterion 3 ("Los 4 paquetes exponen Client y AsyncClient") without crossing into Phase 10 REFAC-04 scope.

Output:
- 1 NEW source module: `_state.py`
- 1 NEW source module: `aio.py` (stub AsyncClient only — no REST surface)
- 3 MODIFIED source modules: `client.py`, `ws_client.py`, `__init__.py`
- 1 MODIFIED conftest, 1 MODIFIED test file (`test_client.py`), 1 MODIFIED test file (`test_ws_client.py`), 1 NEW test file (`test_client_class.py`)
- 1 MODIFIED cross-package test: `packages/ambito-financiero-client/tests/test_harness_mutation_gate.py`
- 1 REGENERATED snapshot: `verification/snapshots/matriz-client-surface.txt`
- `verification/mutation_gate.py` — UNCHANGED syntactically (still reads `matriz_client.client._base_url`); works because the matriz shim now forwards `_base_url`. Listed in `files_modified` because Plan 07 may further audit it; this plan does not edit it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-05-PLAN.md
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/ws_client.py
@packages/matriz-client/src/matriz_client/__init__.py
@packages/matriz-client/tests/conftest.py
@packages/matriz-client/tests/test_client.py
@packages/matriz-client/tests/test_ws_client.py
@packages/ambito-financiero-client/tests/test_harness_mutation_gate.py
@verification/mutation_gate.py
@packages/higyrus-client/src/higyrus_client/_state.py
@packages/higyrus-client/src/higyrus_client/client.py
@verification/test_fixture_reaches_production.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _state.py + Client (sync, X-Auth-Token from headers) + sync conftest + sync test migration + ws_client migration + mutation_gate test migration</name>
  <files>
    packages/matriz-client/src/matriz_client/_state.py,
    packages/matriz-client/src/matriz_client/client.py,
    packages/matriz-client/src/matriz_client/ws_client.py,
    packages/matriz-client/tests/conftest.py,
    packages/matriz-client/tests/test_client.py,
    packages/matriz-client/tests/test_ws_client.py,
    packages/matriz-client/tests/test_client_class.py,
    packages/ambito-financiero-client/tests/test_harness_mutation_gate.py
  </files>
  <read_first>
    - packages/matriz-client/src/matriz_client/client.py (496 LOC — globals `_base_url`, `_user`, `_password`, `_token`, `_token_ts`, `_session` (renamed httpx.Client); `_TOKEN_TTL = 23*60*60`; `login()` POSTs `/auth/getToken` and reads `response.headers["X-Auth-Token"]`; `_unwrap` envelope helper; `_risk_auth()` HTTP Basic fallback; `_request` raises on `data.status == "ERROR"` (CR-03 deferred to Phase 7))
    - packages/matriz-client/src/matriz_client/ws_client.py (reads `_rest._base_url` line 65; calls `_rest._ensure_token()` line 139; reads `_rest._token` lines 140, 150)
    - packages/matriz-client/tests/conftest.py (`monkeypatch.setattr(_client, "_token"/"_token_ts", ...)` — `_client` alias to `matriz_client.client`)
    - packages/matriz-client/tests/test_client.py (inline monkeypatch sites — `monkeypatch.setattr(_client, "_user", "")` and `_password`, `_token`, `_ensure_token`, `login` patches per RESEARCH.md Pitfall #4)
    - packages/matriz-client/tests/test_ws_client.py (any direct reads of `_rest._base_url` / `_token` / `_ensure_token` that need parity)
    - packages/ambito-financiero-client/tests/test_harness_mutation_gate.py (lines 99, 110 — `monkeypatch.setattr(matriz_client.client, "_base_url", url)` writes from an AMBITO test to MATRIZ module per Pitfall #4 / Open Q #4)
    - verification/mutation_gate.py (line 55 — `base = matriz_client.client._base_url`; the planner confirmed this stays as-is and relies on the extended shim)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 5 matriz row; Section 6 matriz adaptation X-Auth-Token + _risk_auth; Section 7 matriz STUB AsyncClient note; Section 8 matriz client.py shim row including `_session` rename)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md (Pitfall #5 _session rename; Open Q #1 stub AsyncClient; Open Q #4 _base_url shim extension + mutation_gate)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-13 matriz kwargs; D-22 matriz login parses X-Auth-Token from headers)
    - packages/higyrus-client/src/higyrus_client/client.py (Plan 05 analog for higyrus-style 23h TTL semantic + Bearer header)
  </read_first>
  <behavior>
    - `_state.py` defines `@dataclass(slots=True) _ClientState` with fields: `base_url: str` (env factory `"https://api.remarkets.primary.com.ar"`), `username: str` (env factory `PRIMARY_USER`), `password: str` (env factory `PRIMARY_PASSWORD`), `token: str | None = None`, `token_expires_at: float = 0.0`, `http_client: httpx.Client | None = None`, `account_id: str | None = None` (forward-declared). NO `token_lock` (matriz has no aio.py REST surface — Phase 10).
    - `Client.__init__(*, base_url=None, username=None, password=None, token=None, token_expires_at=None)` per D-13.
    - `Client.login()` POSTs `f"{self._state.base_url}/auth/getToken"` with HTTP Basic auth headers (Primary API spec); reads `response.headers["X-Auth-Token"]` per D-22; stores `self._state.token = header`; `self._state.token_expires_at = time.time() + _TOKEN_TTL`. Preserve current behavior verbatim — read `client.py:117-121`.
    - `Client._risk_auth()` HTTP Basic fallback (Risk API §9) preserved as instance method.
    - `Client._request(method, path, ...)`: `self._ensure_token()`; request with header `X-Auth-Token: {self._state.token}` (NOT `Authorization: Bearer`); on error call `_raise_for_response`; on `data.status == "ERROR"` raise `PrimaryAPIError` (CR-03 fix deferred — leave current logic). Preserve `_unwrap` envelope helper at module level (stateless).
    - PEP 562 shim with the EXTENDED allowlist for matriz (Open Q #4 mitigation): `_FORWARDED_TO_STATE = {"_token": "token", "_token_ts": "token_expires_at", "_base_url": "base_url"}`. `_FORWARDED_HTTP_CLIENT_NAMES = {"_session", "_client"}` (BOTH forwarded — preserve `_session` name for any direct readers; `_client` for consistency with other packages even though matriz didn't use it pre-refactor). Reads of `_user`/`_password` raise `AttributeError`. NO `_token_lock` (no aio REST).
    - `ws_client.py` migrated: replace `_rest._base_url` → `_rest._get_default()._state.base_url` AND `_rest._ensure_token()` → `_rest._get_default()._ensure_token()` AND `_rest._token` → `_rest._get_default()._state.token`. Use explicit `_get_default()` calls (not the shim) at the ws_client cross-module boundary — clearer ownership.
    - Conftest migrated: drop monkeypatch param; `matriz_client.configure(base_url="https://api.test", username="test-user", password="test-pass", token="test-token", token_expires_at=9_999_999_999.0)`.
    - Inline matriz test_client.py monkeypatch sites migrated mechanically.
    - `packages/ambito-financiero-client/tests/test_harness_mutation_gate.py:99,110`: `monkeypatch.setattr(matriz_client.client, "_base_url", url)` → `matriz_client.configure(base_url=url)`. Lines 30-31, 49-50, 70-71 already use monkeypatch to set/restore via the parametrized URL; same migration pattern.
    - `verification/mutation_gate.py:55` `base = matriz_client.client._base_url` REMAINS UNCHANGED — works because the matriz shim now forwards `_base_url` (Open Q #4 extension to D-02).
    - All matriz tests + the cross-package ambito test pass.
  </behavior>
  <action>
    Create `packages/matriz-client/src/matriz_client/_state.py` per PATTERNS.md Section 5 matriz row. Module top: `from __future__ import annotations`, `os`, `dataclasses`, `httpx`. Env helpers for `PRIMARY_BASE_URL`, `PRIMARY_USER`, `PRIMARY_PASSWORD` (read current client.py:58-60 for the env var names). Constants: `_TOKEN_TTL = 23 * 60 * 60`, `_REQUEST_TIMEOUT = 30.0`. Define `@dataclass(slots=True) class _ClientState` with the 7 fields listed in <behavior>.

    Restructure `packages/matriz-client/src/matriz_client/client.py`:
    1. Module top: `from __future__ import annotations`, stdlib, `httpx`, `from dotenv import load_dotenv`, `from matriz_client._state import _ClientState, _REQUEST_TIMEOUT, _TOKEN_TTL`, exception imports. `load_dotenv()` at module top per D-19.
    2. Keep module-level helpers stateless: `_unwrap(payload, key) -> Any` (envelope unwrap), `_raise_for_response(resp)` (HTTP error mapping; CR-03 deferred). Preserve current logic.
    3. Define `class Client` with `__slots__ = ("_state",)`. `__init__` per D-13. `__enter__/__exit__/close()` idempotent. `__repr__` redacted (`password='***'`, `token='***'`). `__reduce__/__deepcopy__` raise.
    4. `_ensure_http_client(self) -> httpx.Client` lazy.
    5. `login(self) -> str`: POST `f"{self._state.base_url}/auth/getToken"`; HTTP Basic auth header from `self._state.username:self._state.password`; on 200 read `response.headers["X-Auth-Token"]`; on missing header raise `AuthenticationError`; `self._state.token = header_value; self._state.token_expires_at = time.time() + _TOKEN_TTL`. Read current `client.py:98-121` for the verbatim logic.
    6. `_risk_auth(self)` — HTTP Basic Auth fallback for Risk API §9 (preserve current logic; instance method).
    7. `_ensure_token(self)`: same TTL semantic as higyrus — `if self._state.token and time.time() < self._state.token_expires_at: return`; else `self.login()`.
    8. `_request(self, method, path, ...)`: `self._ensure_token()`; request with `X-Auth-Token: {self._state.token}` header; `_raise_for_response(resp)`; check `data.status == "ERROR"` and raise `PrimaryAPIError` (preserve CR-03 deferred behavior).
    9. Public methods: each top-level function (sweep probes, instrument lookups, etc.) → instance method.
    10. Module-level `_default_client: Client | None = None`, `_get_default()`. Top-level `configure(...)` carry-forward including `client_id`-less matriz kwargs. Top-level `login()`, delegators.
    11. PEP 562 shim (Open Q #4 extension): `_FORWARDED_TO_STATE = {"_token": "token", "_token_ts": "token_expires_at", "_base_url": "base_url"}` (note `_base_url` ONLY for matriz — verification/mutation_gate.py:55 reads it). `_FORWARDED_HTTP_CLIENT_NAMES = {"_session", "_client"}` — `__getattr__` checks set membership and returns `_get_default()._state.http_client`. Reads of `_user`, `_password` raise `AttributeError`.
    12. ALSO expose a module-level shim function for `_ensure_token`: define `def _ensure_token() -> None: return _get_default()._ensure_token()` so `ws_client.py` callers that still want a function-call form work — but the planner's primary mitigation is to migrate `ws_client.py` to call `_rest._get_default()._ensure_token()` directly. Keep the function as a belt-and-suspenders shim.
    13. REMOVE module-level globals: `_base_url`, `_user`, `_password`, `_token`, `_token_ts`, `_session`.

    Migrate `packages/matriz-client/src/matriz_client/ws_client.py`:
    - Line 65: `url = _rest._base_url` → `url = _rest._get_default()._state.base_url`. Use the explicit accessor at the cross-module boundary for clarity (the shim would also work; the explicit form is more obvious in code review).
    - Line 139: `_rest._ensure_token()` → `_rest._get_default()._ensure_token()`.
    - Lines 140, 150: `_rest._token` → `_rest._get_default()._state.token`.
    - Confirm `ws_client.py` still imports `from matriz_client import client as _rest` at the top — preserve that alias.

    Migrate `packages/matriz-client/tests/conftest.py` `_configure_sync`: drop monkeypatch param; replace with `matriz_client.configure(base_url="https://api.test", username="test-user", password="test-pass", token="test-token", token_expires_at=9_999_999_999.0)`. Teardown unchanged.

    Migrate `packages/matriz-client/tests/test_client.py` inline write sites. Use `grep -n "monkeypatch.setattr(_client" packages/matriz-client/tests/test_client.py` + `grep -n "monkeypatch.setattr(matriz_client.client" packages/matriz-client/tests/test_client.py` to enumerate. Mechanical rewrites:
    - `monkeypatch.setattr(_client, "_user", "")` → `matriz_client._get_default()._state.username = ""` (or `configure(username="")` — choose direct state write to mirror test semantics; the test wants to wipe creds without rebuilding the default).
    - `monkeypatch.setattr(_client, "_password", "")` → analogous.
    - `monkeypatch.setattr(_client, "_token", X)` → `matriz_client._get_default()._state.token = X`.
    - `monkeypatch.setattr(_client, "_token_ts", X)` → `matriz_client._get_default()._state.token_expires_at = X`.
    - `monkeypatch.setattr(_client, "_ensure_token", fn)` / `"login"` patches — these monkeypatch FUNCTIONS, not state. Rewrite to `monkeypatch.setattr(matriz_client._get_default(), "_ensure_token", fn)` (mock the instance method).

    Audit `packages/matriz-client/tests/test_ws_client.py` for any direct module-state reads/writes that need similar migration; apply mechanical rewrites.

    Migrate `packages/ambito-financiero-client/tests/test_harness_mutation_gate.py` lines 99, 110: `monkeypatch.setattr(matriz_client.client, "_base_url", url)` → `matriz_client.configure(base_url=url)`. The teardown logic the test expects works because `configure()` re-reads env defaults if `base_url=None`. The other parametrize fixture lines (30-31, 49-50, 70-71) — read the file to confirm which are write sites vs assertion strings; migrate writes only.

    Create `packages/matriz-client/tests/test_client_class.py` with sync tests:
    - Standard skeleton tests (lifecycle, repr redaction, pickle/deepcopy raise, configure carry-forward, explicit-instance isolation).
    - `test_login_stores_token_from_x_auth_token_header()`: `httpx_mock.add_response` with `headers={"X-Auth-Token": "MATR-TOK"}`, call `Client().login()`, assert `c._state.token == "MATR-TOK"` (D-22).
    - `test_request_sends_x_auth_token_header()`: configure token, fire any GET via instance, assert `req.headers["X-Auth-Token"] == "..."` (not `Authorization`).
    - `test_pep_562_shim_forwards_base_url()` (Open Q #4 enforcement): set `_get_default()._state.base_url = "https://x"`; assert `matriz_client.client._base_url == "https://x"`.
    - `test_pep_562_shim_forwards_session_and_client()` (Pitfall #5): post-call, `matriz_client.client._session is matriz_client._get_default()._state.http_client` AND `matriz_client.client._client is matriz_client._get_default()._state.http_client`.
    - `test_pep_562_shim_raises_for_user_password()`.
    - `test_mutation_gate_reads_via_shim()`: simulate the mutation_gate flow — `matriz_client.configure(base_url="https://api.remarkets.primary.com.ar")`; import `verification.mutation_gate`; set `VERIFY_MUTATING=1`; assert `mutating_allowed() is True`. (Smoke test that the existing `verification/mutation_gate.py:55` read still works through the shim.)
    - `test_ws_client_uses_default_state_base_url()`: smoke — import `matriz_client.ws_client as ws`; configure base_url; trigger a code path that reads `_rest._base_url` (via the shim or directly through `_get_default()._state`) and assert correctness. May require mocking websocket-client; if too intrusive, settle for a unit-level check of the migrated lines in `ws_client.py`.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests packages/ambito-financiero-client/tests/test_harness_mutation_gate.py packages/matriz-client/tests/test_client_class.py -q</automated>
  </verify>
  <done>
    `uv run pytest packages/matriz-client/tests packages/ambito-financiero-client/tests/test_harness_mutation_gate.py packages/matriz-client/tests/test_client_class.py -q` exits 0. matriz tests pass with new instance pattern; the cross-package mutation gate test (under ambito) passes with `configure(base_url=url)`; `verification/mutation_gate.py` still works via the matriz `_base_url` shim extension.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stub AsyncClient in NEW aio.py + __init__.py + snapshot + guard re-run</name>
  <files>
    packages/matriz-client/src/matriz_client/aio.py,
    packages/matriz-client/src/matriz_client/__init__.py,
    packages/matriz-client/tests/test_client_class.py,
    verification/snapshots/matriz-client-surface.txt,
    verification/test_fixture_reaches_production.py
  </files>
  <read_first>
    - packages/matriz-client/src/matriz_client/_state.py (Task 1 output)
    - packages/matriz-client/src/matriz_client/client.py (Task 1 output for Client mirror)
    - packages/matriz-client/src/matriz_client/__init__.py (current `__all__`)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 7 STUB matriz adaptation note at line 495)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md (Open Q #1 resolution: stub with only lifecycle methods)
    - verification/snapshots/matriz-client-surface.txt (Plan 01 baseline)
    - verification/test_fixture_reaches_production.py (matriz sync guard from Plan 02 to migrate; matriz async stays skipped)
  </read_first>
  <behavior>
    - NEW file `packages/matriz-client/src/matriz_client/aio.py`. Module docstring: "Stub async surface for matriz-client. Phase 6 ships only `AsyncClient` lifecycle; full REST surface lands in Phase 10 REFAC-04. Calling any REST method on this stub raises `NotImplementedError`." Module top: `from __future__ import annotations`, `httpx`, `from matriz_client._state import _ClientState`.
    - `class AsyncClient` with `__slots__ = ("_state",)`. `__init__(*, base_url=None, username=None, password=None, token=None, token_expires_at=None) -> None`: same body as sync (copy non-None kwargs to `_state`). `__aenter__ -> Self: return self`. `__aexit__(self, exc_type, exc, tb) -> None: await self.aclose()`. `aclose(self) -> None`: idempotent — if `self._state.http_client is not None: assert isinstance(self._state.http_client, httpx.AsyncClient); await self._state.http_client.aclose(); self._state.http_client = None`. `__repr__` redacted. `__reduce__`/`__deepcopy__` raise. NO `_ensure_token`, NO `_request`, NO REST methods, NO top-level `configure()` / `get_X()` / `login()` shims. Just the class.
    - NO module-level `_default_async_client`. NO PEP 562 `__getattr__`. The module exposes `AsyncClient` and nothing else.
    - `__init__.py` re-exports `Client` AND `AsyncClient`; both visible at `matriz_client.Client` and `matriz_client.AsyncClient`.
    - Snapshot regenerated: matriz baseline + `Client` + `AsyncClient` lines added; all baseline entries preserved.
    - matriz sync guard in `verification/test_fixture_reaches_production.py` MIGRATED: replace `monkeypatch.setattr(matriz_client.client, "_token", "SYNC-sentinel-matriz", raising=False)` with `matriz_client.configure(base_url="https://api.test", username="test-user", password="test-pass", token="SYNC-sentinel-matriz", token_expires_at=9_999_999_999.0)`. Drop `monkeypatch` parameter.
    - matriz async guard REMAINS `pytest.skip("matriz aio.py is Phase 10 REFAC-04; stub AsyncClient ships in Plan 06 with no REST methods")` — the stub AsyncClient is now confirmed present, but its REST surface is not. No assertion against the wire.
  </behavior>
  <action>
    Create `packages/matriz-client/src/matriz_client/aio.py` per PATTERNS.md Section 7 STUB note (line 495):
    - Module docstring explicitly says "stub — Phase 10 REFAC-04 grows it".
    - `from __future__ import annotations`, imports `httpx`, `from typing import Self`, `from matriz_client._state import _ClientState`, exception imports.
    - `__all__ = ["AsyncClient"]`.
    - Define `class AsyncClient` with the body in <behavior>. Class docstring documents Phase 10 plan.

    Edit `packages/matriz-client/src/matriz_client/__init__.py`: add `Client` AND `AsyncClient` to `__all__`; `from matriz_client.client import Client` and `from matriz_client.aio import AsyncClient`.

    Append async stub tests to `packages/matriz-client/tests/test_client_class.py`:
    - `async def test_async_stub_context_manager()`: `async with AsyncClient() as c: pass`; `c._state.http_client is None` after.
    - `async def test_async_stub_aclose_idempotent()`.
    - `test_async_stub_repr_redacted()`.
    - `test_async_stub_pickle_raises()`, `test_async_stub_deepcopy_raises()`.
    - `def test_matriz_module_aio_exists_and_exports_async_client_only()`: `import matriz_client.aio as aio_mod`; `assert aio_mod.__all__ == ["AsyncClient"]`; `assert not hasattr(aio_mod, "configure")` AND `not hasattr(aio_mod, "login")` AND `not hasattr(aio_mod, "get_quote")` — proving the stub is a stub.

    Regenerate snapshot: `uv run python verification/regen_snapshots.py`. Verify D-06: every pre-refactor entry in `matriz-client-surface.txt` preserved; new `Client` and `AsyncClient` lines added.

    Migrate matriz sync guard in `verification/test_fixture_reaches_production.py`:
    - `test_matriz_sync_sentinel_token_reaches_x_auth_token_header`: replace `monkeypatch.setattr(matriz_client.client, "_token", "SYNC-sentinel-matriz", raising=False)` + `monkeypatch.setattr(matriz_client.client, "_token_ts", 9_999_999_999.0, raising=False)` with `matriz_client.configure(base_url="https://api.test", username="test-user", password="test-pass", token="SYNC-sentinel-matriz", token_expires_at=9_999_999_999.0)`. Drop `monkeypatch` parameter. Assertion `req.headers["X-Auth-Token"] == "SYNC-sentinel-matriz"` unchanged.
    - `test_matriz_async_skipped_until_phase_10` — keep the `pytest.skip(...)` call; the stub AsyncClient existing does not unlock REST-level assertions.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests verification/test_public_surface.py verification/test_fixture_reaches_production.py -q -k "matriz or public_surface"</automated>
  </verify>
  <done>
    `uv run pytest packages/matriz-client/tests verification/test_public_surface.py verification/test_fixture_reaches_production.py -q -k "matriz or public_surface"` exits 0. Stub AsyncClient exported; snapshot updated; matriz sync guard passes with new pattern; matriz async guard remains skipped. Full sweep `uv run pytest -q` is green; 277 baseline maintained.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ws_client.py → matriz_client.client default singleton | `ws_client.py` runs in a daemon thread; it reads token + base_url + calls `_ensure_token` on the sync default Client. The boundary is preserved post-refactor; explicit `_get_default()` calls make ownership clearer. Phase 10 will introduce `TokenStore` to harden this. |
| verification/mutation_gate.py → matriz `_base_url` | Read-only check; gates destructive operations (alta/cancelación de órdenes) to remarkets sandbox host only. The shim extension forwards `_base_url` so the gate logic is unchanged syntactically. |
| Cross-package test (ambito tests writing to matriz) | `test_harness_mutation_gate.py` lives under ambito-financiero but exercises matriz state. Migrating to `configure(base_url=url)` keeps the test working without resurrecting Pitfall #4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PEP 562 shim matriz allowlist + Open Q #4 `_base_url` extension | mitigate | Allowlist documented in code dict literal. `test_pep_562_shim_forwards_base_url` and `test_mutation_gate_reads_via_shim` enforce both the read path and the security gate. |
| T-06-02 | Information Disclosure | `Client.__repr__` token/password leak | mitigate | Redact pattern (password='***', token='***'); test enforces. |
| T-06-03 | Tampering | conftest migration + inline sites + cross-package test | mitigate | Plan's purpose. The matriz fixture-reaches-production guard PROVES the new path reaches the wire after migration. |
| T-06-04 | Denial of Service | `Client.close()`/`AsyncClient.aclose()` lifecycle | mitigate | Idempotent on both classes. |
| T-06-07 | Elevation of Privilege | mutation gate bypass via `_base_url` shim | mitigate | The gate logic in `verification/mutation_gate.py` reads through the shim; the gate compares against the exact sandbox hostname (`api.remarkets.primary.com.ar`). The shim extension does not change the gate semantics — it only changes the storage. Smoke test confirms the gate still returns `False` for non-sandbox hosts. |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed. |
</threat_model>

<verification>
- `uv run pytest packages/matriz-client/tests -q` → all matriz tests green.
- `uv run pytest packages/ambito-financiero-client/tests/test_harness_mutation_gate.py -q` → cross-package mutation gate test green.
- `uv run pytest verification/ -q -k "matriz or public_surface"` → matriz snapshot + matriz sync guard pass; matriz async stays skipped.
- `uv run pytest -q` → 277 baseline + new tests; full suite green.
- `uv run ruff check packages/matriz-client/` and `uv run mypy --strict packages/matriz-client/src` → exit 0.
- Manual: `grep -n "_base_url" verification/mutation_gate.py` returns the existing line 55 unchanged (`base = matriz_client.client._base_url`).
- Manual: `grep -n "monkeypatch.setattr(matriz_client.client, \"_base_url\"" packages/ambito-financiero-client/tests/test_harness_mutation_gate.py` returns 0 hits (all migrated).
</verification>

<success_criteria>
- REFAC-02 (matriz coverage) success criteria 3, 4 satisfied for matriz-client.
- `Client` (sync, full REST surface) + `AsyncClient` (stub, lifecycle only) exposed.
- Pitfall #5 `_session → http_client` rename absorbed.
- Open Q #4 `_base_url` shim extension landed; `verification/mutation_gate.py` works unchanged.
- `ws_client.py` cross-module reads migrated to `_get_default()._state.*`.
- Cross-package `test_harness_mutation_gate.py` migrated to `configure(base_url=url)`.
- Public-surface snapshot updated; baseline preserved.
- Fixture-reaches-production guard migrated and green.
</success_criteria>

<output>
Create `.planning/phases/06-compat-safety-net-client-class-skeleton/06-06-SUMMARY.md` when done.
</output>
