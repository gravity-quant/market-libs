---
phase: 18-libcst-codegen-tool-choice-spike-spike-006
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/README.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/DECISION.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/evidence-checklist.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit-run.log
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/client_with_marker.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/sha256_before.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/sha256_after.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
autonomous: false
requirements: [CODEGEN-01]
user_setup: []

must_haves:
  truths:
    - "The SPIKE-006 spike directory exists, paralleling SPIKE-005's shape (README + DECISION.md + evidence-checklist.txt + 001b/001c/001d)."
    - "Item 10a: matriz construct audit re-run on CURRENT matriz aio.py yields 0 unresolved rows (MERGE GATE PASS sentinel), not a fixed row count."
    - "Item 8: the @generated marker inserted via libcst Module.header is grammar-neutral with `from __future__ import annotations` (ruff check + ruff format --check + mypy --strict + ast.parse all clean)."
    - "Item 10b: the 4 matriz deny-list files are sha256-byte-identical pre/post, and the transformed aio.py sandbox copy is sha256-different (proves scope, not no-op)."
    - "No production packages/ source is mutated; uv.lock is unchanged after ephemeral libcst runs."
  artifacts:
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit.py"
      provides: "Verbatim copy of SPIKE-005 001c/audit.py, SOURCE re-pointed at current matriz aio.py"
      contains: "MERGE GATE PASS"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py"
      provides: "Per-module libcst deny-list sha256 harness (adapted from SPIKE-005 001d, unasync dropped)"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py"
      provides: "libcst Module.header marker insertion + 4-command marker-neutrality transcript"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/DECISION.md"
      provides: "Signed-decision skeleton with 10-item verdict frontmatter map (TBD placeholders)"
      contains: "decision: TBD"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/evidence-checklist.txt"
      provides: "10-item PASS/FAIL transcript skeleton mirroring SPIKE-005"
  key_links:
    - from: "001c-matriz-construct-audit/audit.py"
      to: "packages/matriz-client/src/matriz_client/aio.py"
      via: "SOURCE constant points at current matriz aio.py (959 LOC)"
      pattern: "matriz.*aio\\.py"
    - from: "001d-matriz-deny-list-config/experiment.py"
      to: "packages/matriz-client/src/matriz_client (sandbox copy)"
      via: "shutil.copytree → sha256 pre/post; only aio.py handed to libcst"
      pattern: "sha256"
---

<objective>
Stand up the SPIKE-006 spike directory and land the ~60% of the D-RIGOR-02 harness that is
inherited verbatim or near-verbatim from SPIKE-005 (per CONTEXT D-01): item 8 (`@generated`
marker × future-compat, `001b`), item 10a (matriz construct audit, `001c`), item 10b (matriz
deny-list sha256 intactness, `001d`), plus the `DECISION.md` / `evidence-checklist.txt` /
`README.md` skeletons that Plan 03 fills. This plan also clears the one-time libcst supply-chain
legitimacy checkpoint (D-05) that gates every ephemeral `uv run --with libcst` in Plans 01–03.

Purpose: Get the cheap, high-confidence inherited evidence recorded first so Plan 02 (the
genuinely-new `001a` CSTTransformer suite) and Plan 03 (aggregation) have a stable scaffold and a
cleared install gate to build on. Confirms — does not renegotiate — the matriz deny-list (D-09).

Output: The spike tree under `.planning/spikes/SPIKE-006-libcst-codegen-tool-choice/` with items
8, 10a, 10b recorded, plus decision skeletons.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-CONTEXT.md
@.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md
</context>

<artifacts_this_phase_produces>
Full SPIKE-006 tree (this plan creates the scaffold + 001b/001c/001d; Plans 02–03 add 001a + fill decisions):

```
.planning/spikes/SPIKE-006-libcst-codegen-tool-choice/
├── README.md                       # [Plan 01] frontmatter spike: 006, verdict: TBD
├── DECISION.md                     # [Plan 01 skeleton → Plan 03 signed] 10-item verdict map (D-06)
├── evidence-checklist.txt          # [Plan 01 skeleton → Plan 03 filled] 10-item transcripts (D-06)
├── NO-GO.md OR GO-handoff.md       # [Plan 03] close-out branch (D-08)
├── 001a-ambito-round-trip/         # [Plan 02] driver + transformers/ + output/ + FINDING.md
├── 001b-ambito-marker-future-compat/   # [Plan 01] item 8
├── 001c-matriz-construct-audit/        # [Plan 01] item 10a (audit.py verbatim)
└── 001d-matriz-deny-list-config/       # [Plan 01] item 10b (sha256 pre/post)
```

CSTTransformer class names (authored in Plan 02, drafts handed off in Plan 03 on GO):
`AsyncToSync`, `ImportNormalizer`, `DocstringLocalizer`, `ImportDirectionNormalizer`, and the D-03
suppression transformer `Suppressors` (removes `aclose()` delegator, WR-07 `ResourceWarning` block,
`import warnings`).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: libcst supply-chain legitimacy gate (before first ephemeral install)</name>
  <read_first>
    - .planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md (§Package Legitimacy Audit — libcst tagged [SUS]→assessed legitimate/[ASSUMED]; §Security Domain)
  </read_first>
  <what-built>
    RESEARCH.md ran `gsd-tools query package-legitimacy check --ecosystem pypi libcst` → verdict `SUS`,
    reasons `["unknown-downloads","no-repository"]`, assessed as metadata-gap false positives (the tool
    is Meta/Instagram LibCST at github.com/Instagram/LibCST). Because it is discovered from requirement
    text (not Context7/official-docs-in-session) it is tagged `[ASSUMED]` — per the package legitimacy
    gate this REQUIRES a blocking human verification before the first ephemeral `uv run --with libcst`
    install (D-05). This checkpoint is NOT auto-approvable (auto_advance ignored). Clearing it here gates
    every ephemeral libcst run in Plans 01, 02, and 03.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/libcst — confirm publisher/provenance and that `1.8.6` is the latest release (published 2025-11-03), band `>=1.8.0,<2` resolves to real published versions.
    2. Confirm the source repository is https://github.com/Instagram/LibCST (Meta/Instagram).
    3. Confirm the install is ephemeral only (`uv run --with 'libcst>=1.8.0,<2'`) and NOT added to root `[dependency-groups] dev` during the spike (D-05).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the ephemeral libcst install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold spike tree + decision skeletons + item 10a matriz construct audit (001c)</name>
  <files>
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/README.md,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/DECISION.md,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/evidence-checklist.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit-run.log,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
  </files>
  <read_first>
    - .planning/spikes/SPIKE-005-codegen-tool-choice/README.md (frontmatter + sub-experiment table shape to parallel)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md (frontmatter `evidence_checklist` verdict-map shape)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt (per-item transcript format)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit.py (copy VERBATIM — pure stdlib ast, DENY_LIST at lines 46-53)
    - packages/matriz-client/src/matriz_client/aio.py (current SOURCE target, 959 LOC)
  </read_first>
  <action>
    Create the spike root `.planning/spikes/SPIKE-006-libcst-codegen-tool-choice/`. Write `README.md`
    with frontmatter `spike: 006`, `verdict: TBD`, and a sub-experiment table paralleling SPIKE-005's
    (001a round-trip / 001b marker / 001c audit / 001d deny-list) plus the ephemeral `uv run --with
    'libcst>=1.8.0,<2'` run instructions (D-05). Write the `DECISION.md` skeleton with frontmatter
    `spike: 006`, `decision: TBD`, and an `evidence_checklist` map with all 10 D-RIGOR-02 keys set to
    `TBD` (items 1-8 mirror SPIKE-005 key names; add `item_9_transformer_purity` and
    `item_10_matriz_audit_denylist`); include `matriz_audit_unresolved_rows: TBD`, `timebox_status: TBD`.
    Write the `evidence-checklist.txt` skeleton with a `=== D-RIGOR-02 item N: ... ===` section per
    item and the exact automated command from 18-RESEARCH.md §Phase Requirements → Test Map. For item
    10a: copy SPIKE-005 `001c/audit.py` VERBATIM into `001c-matriz-construct-audit/audit.py`, ensuring
    its `SOURCE` constant points at the CURRENT `packages/matriz-client/src/matriz_client/aio.py`. Run it
    via `uv run python .../001c/audit.py`, capturing `audit-run.log` and `matriz-aio-constructs.md`. Do
    NOT hardcode a `== 109` row check (Pitfall 2 — matriz aio.py grew to 959 LOC); the gate is the
    `**MERGE GATE PASS:** zero unresolved rows.` sentinel and the "zero bare `asyncio.*` in module body"
    structural property. Record the verdict in `001c/FINDING.md`. This task uses NO libcst (pure stdlib
    ast) and mutates NO `packages/` source (read-only walk). Per D-01, D-09.
  </action>
  <verify>
    <automated>cd /Users/admin/development/market-libs && uv run python .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001c-matriz-construct-audit/audit.py >/tmp/spike006-001c.log 2>&1; grep -q 'MERGE GATE PASS' /tmp/spike006-001c.log && test -f .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/DECISION.md && test -f .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/evidence-checklist.txt</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python .../001c/audit.py` exits 0 and its output contains the `MERGE GATE PASS` sentinel with zero unresolved (`REVIEW`/`TBD`/`DENY-LIST-VIOLATION`) rows against current matriz aio.py.
    - `git diff --exit-code packages/` shows no production source mutation (audit is read-only).
    - `README.md`, `DECISION.md` (with `decision: TBD` + 10-item verdict keys), and `evidence-checklist.txt` exist at the spike root.
  </acceptance_criteria>
  <done>Spike tree scaffolded; item 10a recorded PASS (0 unresolved) against current matriz aio.py; decision skeletons in place. (D-01, D-09)</done>
</task>

<task type="auto">
  <name>Task 3: Inherited libcst experiments — item 8 marker (001b) + item 10b deny-list sha256 (001d)</name>
  <files>
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/client_with_marker.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/sha256_before.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/sha256_after.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
  </files>
  <read_first>
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py (marker design, 3-line PEP 263/236 block)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py (shutil.copytree → sha256 before → transform → sha256 after skeleton)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (marker target; `from __future__ import annotations` at line 18)
    - packages/matriz-client/src/matriz_client/aio.py (the only file handed to libcst in 001d)
    - .planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md (§Pattern 4 Module.header; §Item-10b deny-list scope under libcst; §Common Pitfalls)
  </read_first>
  <action>
    001b (item 8): author `001b/experiment.py` to insert the inherited 3-line `@generated` marker block
    via libcst `Module.header` (a sequence of `cst.EmptyLine` carrying `cst.Comment`) — NEVER via
    `str.replace` (Anti-Pitfall 6). Emit `client_with_marker.py` and verify the marker is grammar-neutral
    ahead of `from __future__ import annotations` by running, and transcribing to
    `verification_transcripts.txt`: `uv run ruff check`, `uv run ruff format --check`, `uv run mypy
    --strict`, and `python -c "import ast; ast.parse(...)"` on the marked file — all must exit 0. Confirm
    the marker `Module.header` empirically (RESEARCH A2 flags exact `header` semantics MEDIUM). Record in
    `001b/FINDING.md`. 001d (item 10b): adapt `001d/experiment.py` from SPIKE-005 — `shutil.copytree`
    the matriz package to a sandbox, compute sha256 of the 4 deny-list files (`_token_store.py`,
    `_refresh_policy.py`, `_refresh.py`, `ws_client.py`) + `aio.py` BEFORE; drop the unasync call and
    instead `cst.parse_module` the sandbox `aio.py` ONLY, apply a per-module transform (e.g. the
    `AsyncToSync` pass or a trivial round-trip), write it back, then recompute sha256 AFTER. Assert all 4
    deny-list files are byte-identical pre/post AND `aio.py` differs (proves scope, not no-op). The 4
    deny-list files are NEVER handed to libcst (per-module scope = libcst's equivalent of unasync's
    `fpath_list`). Capture `sha256_before.txt`/`sha256_after.txt` and `001d/FINDING.md`. Both experiments
    run ephemerally via `uv run --with 'libcst>=1.8.0,<2'`; the deny-list files are read-only
    sha256 targets (D-09). After both runs, confirm `uv.lock` is unchanged.
  </action>
  <verify>
    <automated>cd /Users/admin/development/market-libs && uv run --with 'libcst>=1.8.0,<2' python .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py && uv run --with 'libcst>=1.8.0,<2' python .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py && git diff --exit-code uv.lock && git diff --exit-code packages/</automated>
  </verify>
  <acceptance_criteria>
    - 001b: `ruff check`, `ruff format --check`, `mypy --strict`, and `ast.parse` on `client_with_marker.py` each exit 0 (marker grammar-neutral × `from __future__ import annotations`) — item 8 PASS transcript captured.
    - 001d: the experiment asserts all 4 deny-list files' sha256 identical pre/post AND sandbox `aio.py` sha256 differs; `sha256_before.txt` and `sha256_after.txt` written — item 10b PASS.
    - `git diff --exit-code uv.lock` and `git diff --exit-code packages/` both pass (ephemeral install, no production mutation).
  </acceptance_criteria>
  <done>Item 8 recorded PASS (marker via Module.header, 4 commands neutral); item 10b recorded PASS (4/4 deny-list byte-identical, aio.py transformed). Deny-list CONFIRMED out of scope. (D-01, D-05, D-09)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local env | Ephemeral third-party `libcst` install crosses into the local interpreter |
| repo source → spike sandbox | matriz/ámbito source read; sandbox copies transformed under `/tmp` + spike dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-SC | Tampering/Elevation | ephemeral `uv run --with 'libcst>=1.8.0,<2'` install | mitigate | Legitimacy check run in RESEARCH (verdict SUS = metadata gap, assessed legitimate Meta/Instagram); blocking `checkpoint:human-verify` (Task 1) before first install; ephemeral only (D-05, no committed dep); `git diff --exit-code uv.lock` after runs |
| T-18-01 | Tampering | matriz deny-list files (`_token_store.py`/`_refresh_policy.py`/`_refresh.py`/`ws_client.py`) | mitigate | item 10b: 4-file sha256 byte-identity pre/post (Task 3); files never parsed by libcst (per-module scope) |
| T-18-02 | Tampering | production `packages/` source | mitigate | Anti-Pitfall 2: read-only audit walk + `shutil.copytree` sandbox; `git diff --exit-code packages/` in Tasks 2 & 3 |
| T-18-03 | Info Disclosure | `.env` / credentials | accept | Spike reads no `.env`; matriz secret-handling code is deny-listed and unmodified (no secret surface touched) |
</threat_model>

<verification>
- `uv run python .../001c/audit.py` → `MERGE GATE PASS`, 0 unresolved rows (item 10a).
- 001b marker file passes `ruff check` + `ruff format --check` + `mypy --strict` + `ast.parse` (item 8).
- 001d asserts 4/4 deny-list sha256 identical pre/post + aio.py changed (item 10b).
- `git diff --exit-code uv.lock` and `git diff --exit-code packages/` clean after all runs.
- Human legitimacy checkpoint (Task 1) approved before any ephemeral libcst install.
</verification>

<success_criteria>
Spike tree scaffolded paralleling SPIKE-005; items 8, 10a, 10b recorded with captured transcripts;
decision skeletons (DECISION.md + evidence-checklist.txt) staged for Plan 03; matriz deny-list
CONFIRMED byte-identical (D-09); no production mutation; libcst legitimacy gate cleared (D-05).
</success_criteria>

<output>
Create `.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-01-SUMMARY.md` when done.
</output>
