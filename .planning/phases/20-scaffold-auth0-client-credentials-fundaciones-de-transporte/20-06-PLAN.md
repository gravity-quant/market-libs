---
phase: 20-scaffold-auth0-client-credentials-fundaciones-de-transporte
plan: 06
type: execute
wave: 4
depends_on: ["20-03", "20-04", "20-05"]
files_modified:
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/tests/conftest.py
  - packages/market-data-client/tests/test_token_lifecycle.py
  - packages/market-data-client/tests/test_token_lifecycle_async.py
  - packages/market-data-client/tests/test_client.py
  - packages/market-data-client/tests/test_async_client.py
autonomous: true
requirements: [AUTH-MD-01, CORE-MD-01]
user_setup: []

must_haves:
  truths:
    - "import market_data_client and from market_data_client import aio both work; __version__ == '0.1.0' (SC1)"
    - "attach() runs BEFORE any other import in __init__.py so credential redaction is active package-wide (CORE-MD-01)"
    - "The client_credentials token is fetched, cached, and re-fetched on TTL expiry — sync AND async (SC2, AUTH-MD-01)"
    - "GET /health + /health/feed respond via the retry transport on an anonymous path (no token fetch); a health 401 does not trigger re-auth (SC3)"
    - "All 4 CI gates (ruff check, ruff format --check, mypy strict, pytest) are green for the package (SC5)"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "attach-first ordering + re-exports (Client, AsyncClient, 4 exceptions, configure, get_health, get_health_feed) + __all__ + __version__='0.1.0'"
      contains: "__version__ = \"0.1.0\""
    - path: "packages/market-data-client/tests/conftest.py"
      provides: "autouse sync+async configure() fixtures + NEVER_EXPIRES + teardown close/aclose"
      contains: "NEVER_EXPIRES"
    - path: "packages/market-data-client/tests/test_token_lifecycle.py"
      provides: "sync fetch→cache→TTL-expiry refetch (mocked Auth0 token URL)"
      contains: "def test_"
    - path: "packages/market-data-client/tests/test_token_lifecycle_async.py"
      provides: "async mirror with double-checked lock"
      contains: "def test_"
    - path: "packages/market-data-client/tests/test_client.py"
      provides: "sync health anonymous + 401 mapping + exactly-once re-auth"
      contains: "def test_"
    - path: "packages/market-data-client/tests/test_async_client.py"
      provides: "async health anonymous + dispatch"
      contains: "def test_"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/src/market_data_client/_logging.py"
      via: "attach() called before importing client/aio/exceptions"
      pattern: "attach\\(\\)"
    - from: "packages/market-data-client/tests/conftest.py"
      to: "market_data_client.configure / aio.configure"
      via: "autouse fixtures seed creds + non-expiring token; teardown closes transport (Pitfall 6)"
      pattern: "configure\\("
---

<objective>
Wire the package together and prove the phase's behavior end-to-end: `__init__.py` with the CRITICAL attach-first ordering (`_logging.attach()` BEFORE any other import) + re-exports + `__version__="0.1.0"` (SC1); `conftest.py` with autouse sync+async `configure()` fixtures (Pitfall 6 cross-test contamination guard); and the behavioral test modules that could not run until now — token lifecycle (sync + async, mocked Auth0 token URL), health (anonymous, no token fetch), and the 401 carve-out. Finish by running all 4 CI gates green for the package (SC5).

Purpose: This is the integration + verification plan. It closes SC1 (imports/version), SC2 (token lifecycle sync+async), SC3 (health + exception mapping end-to-end), and SC5 (4 gates). SC4 (redaction) is already proven in Plan 03; this plan re-confirms it in the full-suite run.
Output: `__init__.py`, `conftest.py`, 4 test modules; full package suite + ruff + ruff format + mypy strict all green.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-CONTEXT.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-RESEARCH.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-PATTERNS.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-VALIDATION.md
</context>

<artifacts_produced>
This plan creates: `__init__.py` (attach-first + re-exports + `__all__` + `__version__="0.1.0"`), `tests/conftest.py` (autouse sync+async fixtures, `NEVER_EXPIRES` sentinel), `tests/test_token_lifecycle.py`, `tests/test_token_lifecycle_async.py`, `tests/test_client.py`, `tests/test_async_client.py`. Completes the phase's public surface: `import market_data_client` + `from market_data_client import aio`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: __init__.py (attach-first + re-exports) + conftest.py fixtures</name>
  <files>packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/tests/conftest.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/__init__.py (analog — attach-first ordering ~23-30; re-exports; `__all__`; `__version__`)
    - packages/iol-client/tests/conftest.py (analog — autouse sync+async fixtures; `NEVER_EXPIRES`; teardown close/aclose)
    - packages/market-data-client/src/market_data_client/client.py (Plan 04 — the symbols to re-export)
    - packages/market-data-client/src/market_data_client/aio.py (Plan 05 — AsyncClient + async configure)
  </read_first>
  <action>
    Create `packages/market-data-client/src/market_data_client/__init__.py`. KEEP iol's CRITICAL ordering VERBATIM: `from market_data_client import _logging as _logging_attach` → `_logging_attach.attach()` → `del _logging_attach` MUST run BEFORE any other package import, so the redaction filter is attached before `client`/`aio` are imported. Then (with `# noqa: E402`) import `AsyncClient` from `.aio`; `Client, _get_default, configure, get_health, get_health_feed` from `.client`; the 4 exceptions from `.exceptions`. Module docstring shows sync + async usage + Auth0 client_credentials note. `__all__` = [`AsyncClient`, `Client`, `MarketDataAPIError`, `MarketDataAuthError`, `MarketDataError`, `MarketDataRateLimitError`, `configure`, `get_health`, `get_health_feed`]. Keep the private `_get_default` re-export idiom (not in `__all__`) for tests. Set `__version__ = "0.1.0"`. REMOVE iol's `InstrumentType`/endpoint shims/`login`.
    Create `packages/market-data-client/tests/conftest.py` mirroring iol's two autouse fixtures. Define `NEVER_EXPIRES = 9_999_999_999.0`. `_configure_sync` (autouse): `market_data_client.configure(base_url="https://market-data-develop.test/api", client_id="cid", client_secret="csec", audience="aud", auth0_token_url="https://auth.test/oauth/token", token="test-token", token_expires_at=NEVER_EXPIRES)`; teardown `market_data_client.client._get_default().close()` + reset creds. `_configure_async` (autouse): the same on `aio.configure(...)`; teardown `await aio._get_default().aclose()` + reset. This guards Pitfall 6 (module-singleton cross-test contamination) and gives httpx_mock a fresh transport per test.
  </action>
  <verify>
    <automated>uv run ruff check packages/market-data-client/src/market_data_client/__init__.py packages/market-data-client/tests/conftest.py && uv run mypy packages/market-data-client/src && uv run python -c "import market_data_client as m; from market_data_client import aio; assert m.__version__=='0.1.0'; assert hasattr(m,'Client') and hasattr(m,'AsyncClient') and hasattr(m,'get_health') and hasattr(m,'configure'); assert hasattr(m,'MarketDataAuthError')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import market_data_client; from market_data_client import aio; assert market_data_client.__version__ == '0.1.0'"` exits 0 (SC1)
    - `__init__.py` calls `attach()` before importing `.aio`/`.client`/`.exceptions` (grep shows `attach()` above the `# noqa: E402` imports)
    - `market_data_client.__all__` includes Client, AsyncClient, the 4 exceptions, configure, get_health, get_health_feed
    - `conftest.py` defines `NEVER_EXPIRES` and autouse sync+async `configure()` fixtures with teardown close/aclose
    - `mypy` strict over `src` exits 0
  </acceptance_criteria>
  <done>Package imports cleanly with redaction attached first and version 0.1.0; conftest fixtures isolate the module singleton per test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: token lifecycle tests (sync + async) — fetch/cache/TTL-refetch</name>
  <files>packages/market-data-client/tests/test_token_lifecycle.py, packages/market-data-client/tests/test_token_lifecycle_async.py</files>
  <read_first>
    - packages/iol-client/tests/test_refresh_token_lifecycle.py (analog — the `match_content` idiom ~40-75; simplify to single grant)
    - packages/iol-client/tests/test_refresh_token_lifecycle_async.py (async analog)
    - packages/market-data-client/tests/conftest.py (Task 1 — fixtures + NEVER_EXPIRES)
    - packages/market-data-client/src/market_data_client/_core.py (Plan 02 — grant body shape for match_content)
  </read_first>
  <behavior>
    - Sync: seed `state.token=None; state.token_expires_at=0.0` to force an initial fetch; mock the Auth0 token URL (`https://auth.test/oauth/token`) with `match_content` on the client_credentials form body (`grant_type=client_credentials&client_id=cid&client_secret=csec&audience=aud`), `is_reusable=True`, returning `{"access_token":"TOK-1","token_type":"Bearer","expires_in":3600}`. Drive `_ensure_token()` (or an authenticated request) → assert `state.token=="TOK-1"` and one token POST occurred. Then set `state.token_expires_at=0.0` (simulate TTL expiry) and drive again → assert a SECOND token POST occurred by asserting `len(httpx_mock.get_requests())` increased (Pitfall 3 — body identical, assert on COUNT not ordering). Health is anonymous and must NOT fetch a token — do not use it to drive the token path.
    - Async: mirror with the double-checked lock via `await client._aensure_token()`; assert the same fetch→cache→refetch behavior on the async default client.
  </behavior>
  <action>
    Create `packages/market-data-client/tests/test_token_lifecycle.py` and `tests/test_token_lifecycle_async.py` per the behavior block. Reach the sync/async default clients through `market_data_client.client._get_default()._state` / `market_data_client.aio._get_default()._state`. Use `pytest-httpx` `httpx_mock`. For the token path, drive `_ensure_token()`/`_aensure_token()` directly (or an authenticated request). Assert token value cached after first fetch and a second token POST after forcing `token_expires_at=0.0`. Use `is_reusable=True` for the token response. Keep each test self-contained (set `state.token`/`token_expires_at` explicitly at the top — Pitfall 6).
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_token_lifecycle.py packages/market-data-client/tests/test_token_lifecycle_async.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest -k token` exits 0
    - The sync test asserts the token is cached after the first fetch and a SECOND token POST fires after `token_expires_at` is forced to 0.0 (count-based assertion, not ordering)
    - The async test exercises `_aensure_token()` (double-checked lock path) and asserts the same fetch→cache→refetch behavior
    - The token mock matches the client_credentials form body via `match_content`
  </acceptance_criteria>
  <done>AUTH-MD-01 proven end-to-end: client_credentials token fetched, cached, and re-fetched on TTL expiry in both sync and async surfaces (SC2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: health + 401 tests (sync + async) and full 4-gate run</name>
  <files>packages/market-data-client/tests/test_client.py, packages/market-data-client/tests/test_async_client.py</files>
  <read_first>
    - packages/iol-client/tests/test_client.py (analog — dispatch + auth-error handling patterns)
    - packages/iol-client/tests/test_async_client.py (async analog)
    - packages/market-data-client/tests/conftest.py (Task 1)
    - packages/market-data-client/src/market_data_client/client.py (Plan 04 — health getters, `_request` 401 branch)
  </read_first>
  <behavior>
    - Sync health: mock `https://market-data-develop.test/api/health` and `/health/feed` returning `{"status":"ok"}`; `market_data_client.get_health()` / `get_health_feed()` return the dict. Assert the health request carried NO `Authorization` header and NO request hit the token URL (anonymous path — Pitfall 4).
    - Sync 401 mapping: a health endpoint returning 401 raises `MarketDataAuthError` WITHOUT any token fetch (health 401 is real, no re-auth). An authenticated endpoint returning 429 raises `MarketDataRateLimitError`; other error status raises `MarketDataAPIError` (mapping already unit-tested in Plan 02 — assert once here at the dispatch level).
    - Async health: mirror get_health/get_health_feed anonymously.
  </behavior>
  <action>
    Create `packages/market-data-client/tests/test_client.py` (sync health + anonymous-path assertion + health-401-no-reauth) and `tests/test_async_client.py` (async health). Use `httpx_mock`; assert no request was sent to `https://auth.test/oauth/token` during health calls (inspect `httpx_mock.get_requests()` URLs). For the health-401 case, register a 401 for the health URL and assert `MarketDataAuthError` is raised and zero token POSTs occurred.
    Then run the full package suite and all 4 gates. If `mypy`/`ruff` surface issues in any Wave 1-3 module, fix them here (this is the phase gate). Confirm the CI LOG-01 grep gate is not violated in package src.
  </action>
  <verify>
    <automated>uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client && uv run mypy packages/market-data-client/src && uv run --package market-data-client pytest packages/market-data-client/tests -q && ! grep -rnE --include='*.py' 'logging\.basicConfig\s*\(|logging\.root\.\w' packages/market-data-client/src/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` exits 0 (full suite green)
    - `uv run ruff check packages/market-data-client` exits 0
    - `uv run ruff format --check packages/market-data-client` exits 0
    - `uv run mypy packages/market-data-client/src` exits 0 (strict)
    - The health test asserts anonymous dispatch (no Authorization header, no token-URL request) and that a health 401 raises `MarketDataAuthError` with zero token POSTs
    - `grep -rnE 'logging\.basicConfig\s*\(|logging\.root\.\w' packages/market-data-client/src/` returns no matches (CI LOG-01 clean)
  </acceptance_criteria>
  <done>Health endpoints proven anonymous over the retry transport; exception mapping proven at dispatch; all 4 CI gates green for the package (SC3 + SC5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| import time | `attach()` must run first so no credential can be logged before redaction is installed |
| test singleton | module-level state shared across tests; must be reset per test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-06 | Information Disclosure | credential logged before filter attached | mitigate | `__init__.py` calls `attach()` BEFORE importing client/aio — HIGH severity, blocking; enforced by the attach-first grep |
| T-20-02 | Information Disclosure | token attached to a public health call | mitigate | health test asserts no Authorization header and no token-URL request during health calls |
| T-20-08 | Denial of Service | health 401 triggering a spurious token grant | mitigate | test asserts a health 401 raises `MarketDataAuthError` with zero token POSTs (Pitfall 4) |
| T-20-01 | Information Disclosure | real credentials in committed files | mitigate | fixtures use dummy `cid`/`csec`/`aud` + test hosts; no `.env` committed |
</threat_model>

<verification>
## Phase Gate (SC1–SC5)
- SC1: `uv run python -c "import market_data_client; from market_data_client import aio; assert market_data_client.__version__=='0.1.0'"`
- SC2: `uv run --package market-data-client pytest -k token` green (sync + async lifecycle)
- SC3: `uv run --package market-data-client pytest -k "health or client"` green (anonymous health + exception mapping)
- SC4: `uv run --package market-data-client pytest -k "redact or logging"` green (redaction — Plan 03 modules re-run in-suite)
- SC5: `uv run ruff check packages/market-data-client` && `uv run ruff format --check packages/market-data-client` && `uv run mypy packages/market-data-client/src` && `uv run --package market-data-client pytest packages/market-data-client/tests` all exit 0
- CI LOG-01 grep gate clean over `packages/market-data-client/src/`
</verification>

<success_criteria>
The package imports (sync + async) at `__version__=="0.1.0"`; token lifecycle proven sync + async; health anonymous + exception mapping proven; all 4 CI gates green. Closes SC1, SC2, SC3, SC5 (SC4 re-confirmed in-suite).
</success_criteria>

<output>
Create `.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-06-SUMMARY.md` when done.
</output>
