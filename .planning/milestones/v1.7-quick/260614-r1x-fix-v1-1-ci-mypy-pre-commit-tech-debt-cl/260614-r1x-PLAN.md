---
phase: 260614-r1x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/matriz-client/src/matriz_client/aio.py
  - packages/matriz-client/tests/test_core.py
  - packages/matriz-client/tests/test_async_auth.py
  - verification/test_retry_401_reauth.py
  - packages/matriz-client/tests/test_async_mutations.py
  - packages/matriz-client/tests/test_atransport.py
  - packages/matriz-client/tests/test_transport.py
  - verification/test_async_configure_resource_warning.py
  - verification/test_findings_append_only.py
  - verification/test_findings_dedupe_by_title.py
  - verification/test_logging_no_token_leak.py
  - verification/test_main_matriz_login_fail_uniformity.py
  - verification/test_main_matriz_schema_snapshot_alignment.py
  - verification/test_matriz_sweep_snapshot.py
  - verification/test_phase06_nyquist_gaps.py
  - .pre-commit-config.yaml
autonomous: true
requirements:
  - CI-CLEAN-BASELINE
must_haves:
  truths:
    - "`uv run pre-commit run --all-files` returns exit 0"
    - "`uv run ruff format --check .` reports 0 diffs"
    - "`uv run mypy --strict` on the two target test files reports no issues"
    - "`aio._raise_for_response is _sync_client._raise_for_response is _core.raise_for_response` (B8 identity) still holds"
  artifacts:
    - path: ".pre-commit-config.yaml"
      provides: "mypy hook with tenacity in additional_dependencies"
      contains: "tenacity"
    - path: "packages/matriz-client/src/matriz_client/aio.py"
      provides: "_raise_for_response listed in __all__"
      contains: "_raise_for_response"
  key_links:
    - from: "packages/matriz-client/tests/test_async_auth.py"
      to: "packages/matriz-client/src/matriz_client/aio.py"
      via: "from matriz_client import aio as _aio; _aio._raise_for_response"
      pattern: "_raise_for_response"
---

<objective>
Restore a CI-clean baseline before Phase 13 kickoff. Close 4 atomic tech-debt buckets surfaced at v1.1 close + Phase 08 hook gap, so `uv run pre-commit run --all-files` returns exit 0 from `main`.

Per operator decision (D-A+B+C+D in CONTEXT.md):
- Bucket A — 6 mypy errors in matriz-client tests (3 unused `[list-item]` + 2 `attr-defined` from `_raise_for_response` not exported + 1 unused `[attr-defined]`).
- Bucket B — 1 file with pending `ruff format`.
- Bucket C — 11 residual files with pending `ruff format`.
- Bucket D — `.pre-commit-config.yaml` mypy hook missing `tenacity` (Phase 08 tech debt).

Purpose: v1.2 phases must start from a CI-clean baseline (operator directive in `12-SUMMARY.md`); the `_raise_for_response` `__all__` fix lands as a real export (NOT `type: ignore` suppression).

Output: Three atomic, independently revertable commits — one per concern type (mypy fixes, formatting, pre-commit hook).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260614-r1x-fix-v1-1-ci-mypy-pre-commit-tech-debt-cl/260614-r1x-CONTEXT.md
@.pre-commit-config.yaml
@packages/matriz-client/src/matriz_client/aio.py
@packages/matriz-client/tests/test_core.py
@packages/matriz-client/tests/test_async_auth.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix matriz-client mypy strict errors (Bucket A) — `_raise_for_response` export + 4 unused ignores</name>
  <files>
    packages/matriz-client/src/matriz_client/aio.py,
    packages/matriz-client/tests/test_core.py,
    packages/matriz-client/tests/test_async_auth.py
  </files>
  <action>
    Apply the 3 surgical mypy-strict fixes in matriz-client (per CONTEXT.md Bucket A + Implementation Decisions §`_raise_for_response` re-export fix). Do NOT suppress with `type: ignore`; do NOT module-assign as an alias workaround. Use the locked export-via-`__all__` path.

    1. `packages/matriz-client/src/matriz_client/aio.py` — add `"_raise_for_response"` to the `__all__` list (currently at line 94). Insert it in alphabetically-sorted order between `"_get_default"`/preceding entries and `"AsyncClient"` if a leading-underscore convention is already present, otherwise prepend it at the top of the list (the existing `__all__` is alphabetic but case-folded — match the existing ordering style; if uncertain, put it as the FIRST entry since leading-underscore identifiers sort before letters in ASCII). The `from matriz_client._core import raise_for_response as _raise_for_response` at line 61 + the `_ = _raise_for_response` ruff-quiet line at 91 stay as-is. This is PEP-562-conforming explicit re-export; mypy strict treats names listed in `__all__` as part of the public API regardless of leading underscore.

    2. `packages/matriz-client/tests/test_core.py` lines 375–377 — remove the trailing `  # type: ignore[list-item]` from each of the three parametrize tuples (`(None, True)`, `(123, True)`, `([], True)`). These were unused per mypy drift (CONTEXT.md Bucket A). Keep the values themselves and the surrounding `# WR-02 fix` comment block intact (do not delete the block comment — only strip the inline ignores on lines 375, 376, 377).

    3. `packages/matriz-client/tests/test_async_auth.py` line 245 — remove the trailing `  # type: ignore[attr-defined]` from `_ = aio._does_not_exist`. This is unused now that PEP-562 shim raises `AttributeError` at the right type level. Keep the `with pytest.raises(AttributeError):` block intact.

    After the `__all__` edit in step 1, lines 223–224 of `test_async_auth.py` (`assert _aio._raise_for_response is _sync_client._raise_for_response` and `assert _aio._raise_for_response is _core.raise_for_response`) MUST type-check cleanly without any change to that file (the B8 identity test continues to work — only mypy stops complaining because the name is now explicitly exported).

    Critical constraints from CONTEXT.md:
    - Do NOT add `# type: ignore[attr-defined]` to lines 223–224 to suppress the error.
    - Do NOT module-assign `_raise_for_response = _core.raise_for_response` (the explicit `as`-import at line 61 stays).
    - Do NOT touch `packages/matriz-client/src/matriz_client/client.py` (sync surface already exports correctly via module assignment).
    - Mirror sync/async only applies to runtime logic — this is type-system metadata, no client.py changes needed.
  </action>
  <verify>
    <automated>uv run mypy --strict packages/matriz-client/tests/test_core.py packages/matriz-client/tests/test_async_auth.py && uv run mypy --strict packages/matriz-client/src/matriz_client/aio.py && uv run pytest packages/matriz-client/tests/test_async_auth.py::test_b8_aio_raise_for_response_lock_in -x</automated>
  </verify>
  <done>
    `mypy --strict` on the two test files + `aio.py` returns "Success: no issues found"; the B8 identity test `test_b8_aio_raise_for_response_lock_in` passes; `_raise_for_response` appears in `aio.py`'s `__all__`; no new `# type: ignore` comments added anywhere; the 4 listed unused ignores are gone.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply `ruff format` to 12 pending files (Buckets B + C)</name>
  <files>
    verification/test_retry_401_reauth.py,
    packages/matriz-client/tests/test_async_mutations.py,
    packages/matriz-client/tests/test_atransport.py,
    packages/matriz-client/tests/test_transport.py,
    verification/test_async_configure_resource_warning.py,
    verification/test_findings_append_only.py,
    verification/test_findings_dedupe_by_title.py,
    verification/test_logging_no_token_leak.py,
    verification/test_main_matriz_login_fail_uniformity.py,
    verification/test_main_matriz_schema_snapshot_alignment.py,
    verification/test_matriz_sweep_snapshot.py,
    verification/test_phase06_nyquist_gaps.py
  </files>
  <action>
    Run `uv run ruff format` on EXACTLY the 12 files listed in `files` (1 from Bucket B + 11 from Bucket C per CONTEXT.md). Do NOT run `ruff format .` on the whole tree — that would mask the explicit out-of-scope rule (CONTEXT.md: *"cualquier `Would reformat` o mypy error que aparezca en archivos no listados debe surfacearse, no auto-corregirse"*).

    Command (single invocation, exact file list):

    `uv run ruff format verification/test_retry_401_reauth.py packages/matriz-client/tests/test_async_mutations.py packages/matriz-client/tests/test_atransport.py packages/matriz-client/tests/test_transport.py verification/test_async_configure_resource_warning.py verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py verification/test_logging_no_token_leak.py verification/test_main_matriz_login_fail_uniformity.py verification/test_main_matriz_schema_snapshot_alignment.py verification/test_matriz_sweep_snapshot.py verification/test_phase06_nyquist_gaps.py`

    Then run `uv run ruff format --check .` on the whole tree to verify (a) the 12 files are now clean AND (b) no OTHER file is reported as `Would reformat`. If a file OUTSIDE the 12-file list also shows `Would reformat`, STOP and report it as an out-of-scope surfacing — do NOT auto-format it.

    Also confirm `uv run ruff check .` returns 0 errors after formatting (the formatter occasionally surfaces lint issues that were previously hidden by indentation).
  </action>
  <verify>
    <automated>uv run ruff format --check . && uv run ruff check .</automated>
  </verify>
  <done>
    `ruff format --check .` reports "X files already formatted" with 0 diffs; `ruff check .` returns 0 errors; the 12 listed files appear in the git diff with formatting-only changes; no out-of-list files appear in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add `tenacity` to mypy hook `additional_dependencies` (Bucket D)</name>
  <files>.pre-commit-config.yaml</files>
  <action>
    Edit `.pre-commit-config.yaml` to add `tenacity>=9.1.0,<10` to the `additional_dependencies` block of the `mypy` hook (currently at lines 23–28). Insert it preserving alphabetical order — between `pytest-httpx>=0.34` and any other dep, or at the bottom if no other ordering is implied (the current list orders by appearance, not alphabetic; append at the end to minimize diff noise, immediately after `pytest-httpx>=0.34`).

    Constraint from CONTEXT.md Bucket D: this fix is config-only. Do NOT touch `packages/*/pyproject.toml` (tenacity is already declared there since Phase 08 commit `273891b`). Do NOT touch `_transport.py` or `_atransport.py` runtime code — those files only surface as mypy errors because the hook's venv is missing the dep; the runtime code itself is correct.

    Version pin rationale (per CONTEXT.md): `tenacity>=9.1.0,<10` matches the package-level pyproject.toml constraints from Phase 08.

    After the edit, run `uv run pre-commit run mypy --all-files` (or `pre-commit run mypy --all-files` if `pre-commit` is on PATH directly) to confirm the 8 previously-failing `import-not-found` errors on `_transport.py` + `_atransport.py` across the 4 packages now resolve.

    Then run the global acceptance gate: `uv run pre-commit run --all-files` MUST return exit 0. If it does not, STOP and report which hook failed — do NOT keep patching out-of-scope errors.
  </action>
  <verify>
    <automated>uv run pre-commit run --all-files</automated>
  </verify>
  <done>
    `.pre-commit-config.yaml` contains `tenacity>=9.1.0,<10` under the mypy hook's `additional_dependencies`; `pre-commit run --all-files` returns exit 0 with all hooks PASS or "Skipped"; no `import-not-found` errors remain in `_transport.py` / `_atransport.py` mypy output.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks merge to main, the acceptance gate (CONTEXT.md "Verification gate") MUST all pass:

```bash
uv run pre-commit run --all-files       # exit 0
uv run ruff check .                      # 0 errors
uv run ruff format --check .             # 0 diffs
uv run mypy --strict packages/matriz-client/tests/test_core.py packages/matriz-client/tests/test_async_auth.py  # Success
```

If ANY return non-zero, the quick-task is NOT complete and the executor must surface the failure to the orchestrator.

Independent revertability: each task should land as a SEPARATE commit, so any one can be reverted in isolation:
- Commit 1 (Task 1): `fix(matriz-client): export _raise_for_response in aio __all__ + drop 4 unused type-ignores`
- Commit 2 (Task 2): `style: ruff format 12 pending files (matriz tests + verification harness)`
- Commit 3 (Task 3): `chore(pre-commit): add tenacity to mypy hook additional_dependencies`
</verification>

<success_criteria>
- All 3 tasks complete with their `<verify>` automated commands returning exit 0.
- `uv run pre-commit run --all-files` returns exit 0 from `main` after merge.
- `matriz_client.aio._raise_for_response` is importable AND mypy-strict-clean from external modules (B8 identity test passes).
- 12 files reformatted, NO others touched (out-of-scope rule honored).
- `.pre-commit-config.yaml` mypy hook has `tenacity>=9.1.0,<10` in `additional_dependencies`.
- No `# type: ignore` comments added anywhere; the 4 unused ignores from Bucket A are removed.
- No runtime code under `_transport.py` / `_atransport.py` / `client.py` / `aio.py` runtime sections modified beyond the `__all__` line in `aio.py`.
- 3 atomic commits in git history, each individually revertable.
</success_criteria>

<output>
Create `.planning/quick/260614-r1x-fix-v1-1-ci-mypy-pre-commit-tech-debt-cl/260614-r1x-SUMMARY.md` when done.
</output>
