---
phase: 33-verificaci-n-en-vivo-en-modo-estricto-fixes
plan: 06
type: execute
wave: 4
depends_on: ["33-05"]
files_modified:
  - scripts/literal_census_33.py
  - packages/iol-client/src/iol_client/types.py
  - .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md
autonomous: true
requirements: [LIVE-TYP-01]

must_haves:
  truths:
    - "`33-LITERALS.md` carries a populated observed-values table for matriz's seven RESPONSE `Literal`-aliased fields, gathered from raw wire and not from the divergence stream — which structurally cannot produce it (criterion 3, D-08)."
    - "Each of matriz's four aliases (`MarketId`, `CFICode`, `Currency`, `OrderType`) has an explicit disposition confirming it decodes without enforcement and recording the values observed — and no alias is widened, closed or enforced (criterion 3, D-09)."
    - "iol's `mercado` / `plazo` are closed as permanent `str` with the evidence stated: the RESPONSE-side value set observed on `Titulo`, plus the reason that set is insufficient to close an INPUT `Literal` (criterion 3, D-10, DT-07)."
    - "`packages/iol-client/src/iol_client/types.py` records DT-07 as CLOSED with its outcome, so the file stops pointing forward at a decision that has been made."
    - "No raw payload gathered for the census enters git — captures land only in the gitignored staging directory."
  artifacts:
    - path: ".planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md"
      provides: "the Literal census, per-alias dispositions, and the DT-07 closure evidence"
      contains: "DT-07"
    - path: "scripts/literal_census_33.py"
      provides: "read-only value collection over the raw wire of the matriz and iol endpoints that carry the target fields"
    - path: "packages/iol-client/src/iol_client/types.py"
      provides: "DT-07 recorded CLOSED (str permanent) with a pointer to 33-LITERALS.md"
      contains: "DT-07"
  key_links:
    - from: "scripts/literal_census_33.py"
      to: "verification/capture.py"
      via: "capture() writes raw payloads to the gitignored .planning/verification/captures/"
      pattern: "capture\\("
    - from: ".planning/phases/33-.../33-LITERALS.md"
      to: "packages/matriz-client/src/matriz_client/types.py"
      via: "each of the four aliases gets a disposition citing its declared member set"
      pattern: "MarketId|CFICode|Currency|OrderType"
  prohibitions:
    - id: P-01
      category: privacy
      statement: "MUST NOT widen the frozen six-key type-not-value divergence record, and MUST NOT compose any finding field from anything other than those six keys plus the endpoint and surface the harness itself set — a git-committed findings file must never carry a live wire value."
      status: resolved
      verification: judgment
    - id: P-02
      category: transparency
      statement: "MUST NOT report a census number, a phase criterion, or a cycle-closure PASS produced by a signal that inspected nothing — a green arising from a dropped log level, a swallowed handler exception, an unseeded fid allocator, or a zero-applicable closure filter is a false clean."
      status: resolved
      verification: judgment
    - id: P-03
      category: transparency
      statement: "MUST NOT shrink the census to fit the ratified floor — no narrowing the walker, no dropping a divergence kind, and no deferring a confirmed finding to an unnamed later."
      status: resolved
      verification: judgment
    - id: P-04
      category: transparency
      statement: "MUST NOT widen, close, or enforce matriz's four pre-existing RESPONSE Literal aliases, and MUST NOT promote iol's mercado/plazo to a Literal on RESPONSE-side evidence — the signed D-lock (D-09) and D-10 forbid both, and a Literal closed on insufficient evidence breaks legitimate caller input."
      status: resolved
      verification: judgment
    - id: P-05
      category: safety
      statement: "MUST NOT let the census generate deliberate 4xx traffic against a live brokerage account to enumerate accepted input values — that sweep is explicitly deferred (D-10) and is not part of this cycle."
      status: resolved
      verification: judgment
  flagged_assumptions:
    - "The deterministic edge-probe returned LIVE-TYP-01 as `unclassified` / `unresolved`; its acceptance surface is compound (five ROADMAP criteria), not a single classifiable edge. Carried as an explicit flagged assumption — the five ROADMAP Phase 33 success criteria are the real acceptance surface. Full statement in 33-01-PLAN.md."
    - "The census is RESPONSE-side by construction. The set a vendor EMITS is not provably the set it ACCEPTS as an input parameter — case variants, aliases and deprecated-but-still-accepted values are all possible and none is observable without the deliberate-4xx sweep D-10 rejects. This is a stated limit of the evidence, not a gap in the method, and 33-LITERALS.md must say so in the DT-07 section."
---

<objective>
Close criterion 3 with real evidence: build the value census the divergence stream structurally
cannot produce, record what matriz's four RESPONSE `Literal` aliases actually see on the wire without
widening any of them, and close iol's DT-07 as permanent `str` with the evidence attached rather than
with an absence.

Purpose: `_decode.walk_field`'s `Literal` branch returns early for an out-of-set value of the correct
runtime type when `policy.literal_enforced` is `False` — and it is `False` in all five `POLICY`
constants — so the sink is never called and no divergence record is emitted. `29-DLOCK-RESPONSE-LITERAL.md`
asserts the opposite; the shipped code is authoritative and CONTEXT D-08 already caught it. The census
therefore reads raw wire, from endpoints that are pure reads.

Output: a read-only census script, `33-LITERALS.md` with a populated observed-values table and one
disposition per alias, and the DT-07 outcome recorded in iol's `types.py`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CONTEXT.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-RESEARCH.md
@.planning/phases/29-decoder-observable/29-DLOCK-RESPONSE-LITERAL.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md
@packages/matriz-client/src/matriz_client/types.py
@packages/iol-client/src/iol_client/types.py
</context>

## Artifacts this phase produces

Created or modified by **this plan**: `scripts/literal_census_33.py` (may be uncommitted),
`.planning/phases/33-.../33-LITERALS.md`, and a scoped docstring update to
`packages/iol-client/src/iol_client/types.py` recording DT-07 as closed.

Everything else in the phase's artifact inventory is listed in `33-01-PLAN.md` under
"Artifacts this phase produces"; do not duplicate those symbols here.

<tasks>

<task type="auto">
  <name>Task 1: Collect the raw-wire value census for matriz's 7 Literal fields and iol's Titulo.mercado / Titulo.plazo</name>
  <files>scripts/literal_census_33.py</files>
  <read_first>
    - `packages/matriz-client/src/matriz_client/types.py` — the four target aliases and their declared member sets: `MarketId` (`:44`), `OrderType` (`:38`), `CFICode` (`:50`), `Currency` (`:95`)
    - `packages/matriz-client/src/matriz_client/models.py` — the seven RESPONSE fields carrying them: `marketId` (`:262`, `:283`), `cficode` (`:291`, `:303`), `currency` (`:315`), `orderTypes` (`:316`), `ordType` (`:352`), plus the surrounding model classes so the right endpoints are chosen
    - `packages/higyrus-client/src/higyrus_client/_decode.py:521-534` — the `Literal` branch showing the early `return value` that makes the divergence stream unusable for this census (identical in all five copies)
    - `packages/iol-client/src/iol_client/models.py:214-231` — `Titulo.mercado` / `Titulo.plazo` declared `str`, and `:151` `Cotizacion.plazo` (the echo-only, non-evidence source)
    - `packages/iol-client/src/iol_client/_core.py::parse_get_instruments_by_type_response` — the parser for `GET /api/v2/Cotizaciones/{instrument_type}/{pais}/Todos`, which returns every instrument of a type across the venue
    - `verification/capture.py` (lines 1-34) — the module-header pattern and `capture(pkg, name, payload)` writing into the gitignored `.planning/verification/captures/`
    - `verification/env_gate.py:32-41` — `require_env`'s SKIP-not-raise contract
    - `33-05-SUMMARY.md` — the pre-flight AUTH results; a package that did not authenticate cannot contribute a census row
    - `33-RESEARCH.md` Pattern 5 (the evidence-source table and the zero-risk `Titulo` source) and Pitfall P-6, P-10
  </read_first>
  <action>
    Create `scripts/literal_census_33.py` as a **real `.py` file** (never `python -c` — `find_dotenv()`
    falls back to `os.getcwd()` when `__main__` has no `__file__`, and there is no `.env` at the repo
    root, P-10). Follow the `verification/capture.py` module-header pattern: Spanish docstring naming
    LIVE-TYP-01 criterion 3 and D-08/D-09/D-10 in the first line, `from __future__ import annotations`,
    repo root via `Path(__file__).resolve().parent.parent`.

    For matriz, construct one `Client()` and call only read endpoints, collecting the distinct observed
    values per target field:
    - `marketId` — from `get_all_instruments` and `get_instruments_details` (the two models declaring
      it at `models.py:262` and `:283`)
    - `cficode` — from the two models declaring it at `models.py:291` and `:303`
    - `currency` and `orderTypes` — from the model declaring them at `models.py:315-316`
      (`get_segments` / instrument-detail surface, whichever the model belongs to)
    - `ordType` — from `get_active_orders`, `get_all_orders` and `get_order_status`

    Collect the values from the **raw wire**, not from the decoded model: a decoded model has already
    passed through the walker and, for these fields, the walker returns the value unchanged, so the
    two agree today — but reading the wire is what makes the census independent of the decoder it is
    auditing. Reuse each driver's existing raw-request path or issue the request through the client's
    own transport; do not add a new HTTP layer.

    For iol, construct one `Client()` and call `get_instruments_by_type` for each instrument type the
    driver already exercises, collecting the distinct `mercado` and `plazo` values from the `Titulo`
    rows. This endpoint returns every instrument of the requested type across the venue, so the value
    set is genuinely observed rather than echoed back. Separately note — but do NOT count as evidence —
    `Cotizacion.plazo` from `get_quote`, which only echoes the `"bcba"` / `"t2"` defaults the driver
    sent; record it in the artifact explicitly labelled non-evidence so a later reader cannot mistake
    it for a census.

    Write every raw payload the census touches through `verification.capture.capture(...)` so it lands
    in `.planning/verification/captures/` and nowhere else — that directory is gitignored at
    `.gitignore:51` and is the only legal home for raw wire (C-4). The script prints, per field, the
    endpoint, the number of rows inspected and the sorted distinct value set; the value sets are the
    only wire-derived content that will reach the committed artifact, and they are exactly the content
    criterion 3 asks for.

    The script is read-only. Issue no order-entry call, no mutation, and no deliberate 4xx sweep of
    candidate `mercado` / `plazo` values against the live brokerage account — that sweep is explicitly
    deferred by D-10 and rejected as a Deferred Idea in CONTEXT.

    The script may stay uncommitted (Claude's Discretion, matching `29-SIZING.md`'s throwaway
    `sizing.py`), but if uncommitted its full text must be reproduced in `33-06-SUMMARY.md` so the
    method is reproducible from the report.
  </action>
  <acceptance_criteria>
    - `uv run python scripts/literal_census_33.py` completes and prints, per target field, the endpoint, a non-zero row count and a sorted distinct value set — or an explicit `SKIPPED` line naming the package that did not authenticate.
    - The output covers all seven matriz fields (`marketId` ×2, `cficode` ×2, `currency`, `orderTypes`, `ordType`) and both iol fields (`Titulo.mercado`, `Titulo.plazo`).
    - `git status --porcelain .planning/verification/captures/` is empty — the staging directory is gitignored and nothing from it is staged.
    - `git diff --stat packages/matriz-client/src/matriz_client/types.py packages/matriz-client/src/matriz_client/models.py` is empty — no alias was widened, closed or enforced (D-09).
    - `uv run pytest packages/matriz-client/tests/test_decode.py packages/matriz-client/tests/test_types.py -q` passes — the aliases still decode without enforcement.
    - `uv run ruff check . && uv run ruff format --check .` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_decode.py packages/matriz-client/tests/test_types.py -q &amp;&amp; test -z "$(git diff --stat packages/matriz-client/src/matriz_client/types.py packages/matriz-client/src/matriz_client/models.py)" &amp;&amp; test -z "$(git status --porcelain .planning/verification/captures/)" &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>A read-only census script has collected the distinct observed value set for all seven matriz Literal-aliased fields and both iol `Titulo` fields; every raw payload landed in the gitignored staging directory; matriz's aliases and models are byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Write 33-LITERALS.md and record the DT-07 closure in iol's types.py</name>
  <files>.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md, packages/iol-client/src/iol_client/types.py</files>
  <read_first>
    - `33-06-SUMMARY.md` in progress — the Task 1 census output
    - `.planning/phases/29-decoder-observable/29-DLOCK-RESPONSE-LITERAL.md` — D-09 verbatim, including the assertion at lines 140-142 that the divergence stream is the census mechanism, which the shipped walker falsifies
    - `packages/matriz-client/src/matriz_client/types.py` — the declared member sets, to contrast against the observed sets
    - `packages/iol-client/src/iol_client/types.py` — the current placeholder docstring, which points forward at "deferred to Phase 33, pending a live census"; this is the file to update
    - `.planning/future-plans/tipado_homogeneo.md` — the DT-01..DT-09 table, DT-07 in particular
    - `33-RESEARCH.md` Pattern 5, including the "Why this does not overturn D-10" paragraph
  </read_first>
  <action>
    Write `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md` with these
    sections:

    `## Method` — state that the walker cannot supply this census and cite the source line: with
    `policy.literal_enforced` `False` in all five `POLICY` constants, `_decode.walk_field:521-534`
    takes the `return value` branch for an out-of-set value of the correct runtime type and never
    calls the sink. State that `29-DLOCK-RESPONSE-LITERAL.md:140-142` claims otherwise and is
    superseded by the shipped code, and that `verification.schema.schema_of` cannot serve either
    because it reduces every value to its type name. Name the endpoints and row counts the census
    actually read.

    `## matriz RESPONSE fields (D-09)` — a table with columns: Field | Model | Alias | Declared member
    set | Observed value set | Rows inspected | Endpoint | In-set? | Disposition. One row per each of
    the seven fields. The disposition for every row is **confirmed decoding without enforcement, values
    recorded** — never "widen", never "close", never "enforce". Add a paragraph stating in plain terms
    that D-09 and `29-DLOCK-RESPONSE-LITERAL.md` forbid widening or closing these aliases in this
    milestone, that any such change would be its own decision with its own signed artifact, and that
    this document therefore records evidence rather than acting on it. Where an observed value fell
    outside its declared member set, say so explicitly — that is a real finding about the alias's
    accuracy and belongs in the re-scope conversation in `33-CENSUS.md`, routed to a named destination,
    not silently normalised away here.

    `## iol mercado / plazo — DT-07 closure (D-10)` — record the observed RESPONSE-side value sets for
    `Titulo.mercado` and `Titulo.plazo` with their row counts and endpoint. Then state the disposition:
    **`str`, permanent, closed with evidence.** Give the reasoning in full — the set a vendor emits in
    responses is not provably the set it accepts as an input parameter, since case variants, aliases and
    deprecated-but-still-accepted values are all possible and none is observable without the deliberate
    4xx sweep D-10 rejects for generating intentional error traffic against a live brokerage; and a
    `Literal` closed on an incomplete set breaks legitimate caller input, which is strictly worse than
    `str`. Record `Cotizacion.plazo` from `get_quote` in a clearly-labelled non-evidence note: it echoes
    the `"bcba"` / `"t2"` defaults the driver sent and is not a census. Close the section by stating
    that this is "closed with evidence that the evidence is insufficient to close a `Literal`", which is
    a materially stronger position than closing for absence of evidence.

    `## Carry-forward` — anything the census surfaced that is not resolvable in this cycle, each with a
    named destination consistent with `33-CENSUS.md`'s re-scope table. If nothing carries forward, say
    so and state why.

    Then update `packages/iol-client/src/iol_client/types.py`'s module docstring with a scoped edit:
    replace the forward-looking sentence that defers the `mercado` / `plazo` promotion to Phase 33 with
    the recorded outcome — DT-07 **CLOSED**, `str` permanent, decided in Phase 33 on RESPONSE-side
    evidence, with a pointer to `33-LITERALS.md`. Keep the rest of the docstring, keep `__all__` empty,
    add no `Literal` alias, and change no annotation anywhere in the package. The file must remain a
    deliberate placeholder; the only change is that it stops pointing forward at a decision that has
    now been made.
  </action>
  <acceptance_criteria>
    - `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md` exists and contains the four headings `## Method`, `## matriz RESPONSE fields`, `## iol mercado / plazo`, `## Carry-forward`.
    - The matriz table has at least 7 data rows, each with a non-empty Observed value set (or an explicit `SKIPPED — credentials` where the package did not authenticate), and every Disposition cell records values without widening, closing or enforcing.
    - `grep -c 'DT-07' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md` is at least 1, and the document states `str` as the permanent outcome.
    - `grep -c 'DT-07' packages/iol-client/src/iol_client/types.py` is at least 1 and the file records the closure.
    - `uv run python -c "import ast,sys; t=ast.parse(open('packages/iol-client/src/iol_client/types.py').read()); sys.exit(0 if not [n for n in t.body if isinstance(n,ast.ClassDef)] and not [n for n in ast.walk(t) if isinstance(n,ast.Name) and n.id=='Literal'] else 1)"` exits 0 — no `Literal` alias was added.
    - `git diff --stat packages/matriz-client/src/matriz_client/types.py` is empty.
    - `uv run pytest packages/iol-client packages/matriz-client -q` passes.
    - `uv run mypy && uv run ruff check . && uv run ruff format --check .` pass.
    - `33-LITERALS.md` contains no account identifier, CUIT or token fragment — enum-like field values, model names, field paths, endpoint templates and counts only.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md &amp;&amp; grep -q 'DT-07' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md &amp;&amp; grep -q 'DT-07' packages/iol-client/src/iol_client/types.py &amp;&amp; uv run python -c "import ast,sys; t=ast.parse(open('packages/iol-client/src/iol_client/types.py').read()); sys.exit(0 if not [n for n in t.body if isinstance(n,ast.ClassDef)] and not [n for n in ast.walk(t) if isinstance(n,ast.Name) and n.id=='Literal'] else 1)" &amp;&amp; test -z "$(git diff --stat packages/matriz-client/src/matriz_client/types.py)" &amp;&amp; uv run pytest packages/iol-client packages/matriz-client -q &amp;&amp; uv run mypy &amp;&amp; uv run ruff check .</automated>
    <human-check>Read 33-LITERALS.md and confirm: (1) each of the seven matriz rows carries a real observed value set with its row count and endpoint, and none of the four aliases was widened, closed or enforced; (2) any observed value outside a declared member set is named and routed, not normalised away; (3) the DT-07 section states `str` permanent with the RESPONSE-vs-INPUT reasoning spelled out, and labels the `Cotizacion.plazo` echo as non-evidence; (4) iol's types.py now records DT-07 as closed instead of deferring it.</human-check>
  </verify>
  <done>Criterion 3 is closed with evidence: matriz's four aliases have per-field observed value sets and a record-only disposition, iol's DT-07 is `str` permanent with the RESPONSE-side evidence and its stated limit, and iol's `types.py` records the outcome without gaining a single `Literal`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vendor APIs (MATBA ROFEX, IOL) -> census script | Untrusted third-party JSON, read-only. IOL is an account-scoped brokerage surface. |
| raw payload -> disk | The census is the one place in this phase that deliberately handles wire VALUES rather than type names. |
| observed value set -> `33-LITERALS.md` | A git-committed artifact; the value sets are the only wire-derived content that legitimately reaches it. |
| `.env` (iol 3 keys, matriz 8 keys) -> census script | Credentials in memory; never printed, never written. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-32 | Information Disclosure | a raw payload gathered for the census being committed to git | mitigate | Every payload is written through `verification.capture.capture(...)` into `.planning/verification/captures/`, gitignored at `.gitignore:51`. An acceptance criterion asserts `git status --porcelain` on that directory is empty. The report may be committed; the payloads may not (C-4). |
| T-33-33 | Information Disclosure | an account identifier riding into `33-LITERALS.md` alongside the enum-like values | mitigate | Only the sorted distinct value sets of the nine named fields reach the artifact, plus model names, field paths, endpoint templates and row counts. Those nine fields are enum-like vocabulary (`MarketId`, `CFICode`, `Currency`, `OrderType`, `mercado`, `plazo`), not identifiers. An acceptance criterion re-checks the document. |
| T-33-34 | Tampering | widening, closing or enforcing a matriz alias to make an out-of-set observation look clean | mitigate | Forbidden by the signed D-09 lock and by prohibition P-04. Enforced by an acceptance criterion asserting `git diff --stat` on matriz's `types.py` and `models.py` is empty, and by the existing decode/type tests staying green. An out-of-set observation is recorded and routed, never normalised. |
| T-33-35 | Tampering | closing iol's `mercado` / `plazo` as a `Literal` from RESPONSE-side evidence | mitigate | Forbidden by D-10 and prohibition P-04. Enforced by an AST acceptance criterion asserting no `Literal` name appears in iol's `types.py` after the edit. |
| T-33-36 | Safety / DoS on a live brokerage | a deliberate 4xx sweep of candidate `mercado` / `plazo` values to enumerate the accepted set | mitigate | Explicitly out of scope (D-10, Deferred Ideas). The script is read-only and issues no request expected to fail. Prohibition P-05 records this. |
| T-33-37 | Repudiation | an empty observed-values table being read as "no values observed" rather than "the census did not run" | mitigate | Every row carries a row count and an endpoint; a package that did not authenticate is recorded as `SKIPPED — credentials`, never as an empty set. An acceptance criterion requires either a non-empty set or an explicit SKIPPED marker per row. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages installed; `uv.lock` untouched (`33-RESEARCH.md` `## Package Legitimacy Audit` is empty by construction). No install task, therefore no legitimacy checkpoint. |
</threat_model>

<verification>
- `uv run python scripts/literal_census_33.py` — per-field endpoint, row count and sorted distinct value set for all nine target fields
- `git status --porcelain .planning/verification/captures/` empty
- `git diff --stat packages/matriz-client/src/matriz_client/types.py packages/matriz-client/src/matriz_client/models.py` empty
- `uv run pytest packages/matriz-client/tests/test_decode.py packages/matriz-client/tests/test_types.py -q`
- `uv run pytest packages/iol-client packages/matriz-client -q`
- AST check: no `Literal` alias added to `packages/iol-client/src/iol_client/types.py`
- `uv run mypy && uv run ruff check . && uv run ruff format --check .`
- `33-LITERALS.md` present with all four sections and a populated matriz table

</verification>

<success_criteria>
- `33-LITERALS.md` has a populated observed-values table for all seven matriz RESPONSE fields with row counts and endpoints, sourced from raw wire and not from the divergence stream
- Every matriz alias disposition records values and nothing else — no widening, no closing, no enforcement; matriz's `types.py` and `models.py` are byte-unchanged
- Any observed value outside a declared member set is named in the artifact and routed to a named destination consistent with `33-CENSUS.md`
- iol's DT-07 is closed as permanent `str` with the RESPONSE-side evidence recorded and its insufficiency for an INPUT `Literal` stated
- `Cotizacion.plazo` is recorded as explicitly non-evidence
- `packages/iol-client/src/iol_client/types.py` records DT-07 as closed, gains no `Literal` alias, and keeps `__all__` empty
- Every raw payload the census read stayed inside the gitignored staging directory
</success_criteria>

<output>
Create `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-06-SUMMARY.md` when done.
Record: the full census output per field (endpoint, row count, sorted distinct value set); any observed
value falling outside its declared member set, with the destination it was routed to; the full text of
`scripts/literal_census_33.py` if it was left uncommitted; and the exact DT-07 wording added to iol's
`types.py`.
</output>
