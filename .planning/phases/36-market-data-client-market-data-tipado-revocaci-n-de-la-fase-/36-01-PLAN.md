---
phase: 36
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/tests/test_core.py
  - packages/market-data-client/tests/test_decode.py
autonomous: false
requirements: [NOBJ-MD-02]

must_haves:
  truths:
    - "No test module under packages/market-data-client/tests/ reaches the mapping machinery through the models module — the suite would stay green if those four helpers vanished from models.py tomorrow."
    - "The three locks that merely BORROWED the Optional detector still assert exactly what they asserted before: T-31-17 (the health models declare exactly two Optionals), the mutation-result no-Optional lock, and WR-03 (a from_api-override model is never a nested field type)."
    - "The two health/mutation-result tests still assert that received_at is absent from their hints — only the mapping half of each assertion is gone."
    - "The package suite is green and the test count is exactly 660 under the retire verdict (662 under the preserve verdict) — any other number means a deletion overshot the census (RESEARCH Pitfall 1)."
    - "The exempt roster inside test_no_call_site_exempt_safemodel_appears_as_a_nested_field_type is still non-empty after the mapping disjunct is dropped: the two from_api overrides (MarketDataSnapshot, Symbol) keep the guard from going vacuous."
    - "models.py is byte-identical to its pre-phase state — this plan is test-side only."
  artifacts:
    - path: "packages/market-data-client/tests/test_core.py"
      provides: "Module-local Optional detector plus the two renamed health/mutation-result locks"
      contains: "def _strip_optional"
    - path: "packages/market-data-client/tests/test_decode.py"
      provides: "Module-local Optional detector, the narrowed call-site-exemption guard, and the mapping axis removed"
      contains: "def _strip_optional"
  key_links:
    - from: "packages/market-data-client/tests/test_core.py"
      to: "packages/market-data-client/tests/test_core.py"
      via: "the T-31-17 and mutation-result locks read the module-local detector, not the models one"
      pattern: "_strip_optional\\(hint\\)"
    - from: "packages/market-data-client/tests/test_decode.py"
      to: "packages/market-data-client/tests/test_decode.py"
      via: "the WR-03 nested-type lock reads the module-local detector"
      pattern: "_strip_optional\\(hint\\)"
  prohibitions:
    - statement: "Do not resolve a red gate by weakening or deleting the gate. A test that reddens because its subject changed gets MIGRATED to the property it still protects; a test that reddens because the code is wrong gets a code fix. Deleting a whole line range because some of it is stale silently retires the live invariants that share the range."
    - statement: "Do not reach outside the two test files this plan names. Any edit to models.py, _decode.py, _core.py, client.py, aio.py, pyproject.toml, __version__ or uv.lock in this plan is out of scope and changes the blast radius the phase declared."
---

<objective>
Retire the mapping axis from the `market-data-client` TEST suite so that Plan 36-02 can delete
the machinery from `models.py` without reddening anything, and do it by the per-call-site census
rather than by the line ranges CONTEXT D-05 named — those ranges over-cover and would carry off
three live invariants that have nothing to do with mapping.

Purpose: D-05 orders the mapping machinery deleted "junto con sus tests de precondición".
RESEARCH F-4 measured the actual blast radius: 13 call sites, of which **1 is a clean deletion**
and the rest are surgical edits, because `_strip_optional` is used by three surviving locks as a
generic Optional detector and not as mapping code at all. This plan performs the surgery first,
while `models.py` is still intact, so every step is independently green.

Output: `test_core.py` and `test_decode.py` with zero references to the mapping machinery, the
three borrowed-detector locks repointed to module-local copies, and the CR-03 axis disposed of by
an explicit operator verdict.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-CONTEXT.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan produces no new public symbols. It is the prep half of the phase. The phase-wide
artifact roster (created by Plan 36-02 and 36-03) is:

| Symbol | Kind | Home | Created by |
|--------|------|------|------------|
| `BookLevel` | `SafeModel` dataclass (`price: float \| None`, `size: int \| None`) | `market_data_client.models` | 36-02 |
| `EntryValue` | `SafeModel` dataclass (`price: float \| None`, `size: int \| None`, `date: int \| None`) | `market_data_client.models` | 36-02 |
| `MarketDataEntries` | `SafeModel` container (`BI`, `CL`, `HI`, `LA`, `LO`, `OF`, `OI`, `OP`, `SE`, `TV`) | `market_data_client.models` | 36-02 |
| `MarketDataEntries.bids` / `.offers` | read-only `@property` aliases over `BI` / `OF` → `list[BookLevel]` | `market_data_client.models` | 36-02 |
| `MarketDataEntries.last` / `.settlement` / `.close` / `.open_interest` | read-only `@property` aliases over `LA` / `SE` / `CL` / `OI` → `EntryValue` | `market_data_client.models` | 36-02 |
| `MarketDataSnapshot.entries` | annotation change `list[str] \| None` → `list[str]` | `market_data_client.models` | 36-02 |
| `MarketDataSnapshot.market_data` | annotation change `dict[str, Any] \| None` → `MarketDataEntries` | `market_data_client.models` | 36-02 |
| `LatestRequest.entries` | annotation change `list[str] \| None = None` → `list[str] = field(default_factory=list)` | `market_data_client.models` | 36-02 |
| `packages/market-data-client/tests/test_market_data_chain.py` | NEW test module — 4 payloads × 2 surfaces | tests | 36-02 |
| `verification/test_main_market_data_deep_chain.py` | NEW AST lock — the driver dereferences the chain | verification | 36-03 |

Symbols this phase REMOVES from `market_data_client.models`: `_mapping_value`,
`_apply_mapping_policy`, `_is_mapping`, `_strip_optional` (36-02).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the disposition of the CR-03 required-mapping contract</name>
  <decision>
    What happens to `test_decode.py`'s `_RequiredMapping` fixture and its two CR-03 tests
    (`test_absent_required_mapping_field_reports_missing_and_substitutes_the_empty_dict` and
    `test_strict_mode_raises_on_an_absent_required_mapping_field`) once the mapping machinery
    leaves this package?
  </decision>
  <context>
    CONTEXT D-05 says the machinery goes "junto con sus tests de precondición" but never names
    these two rows, and RESEARCH raised it as Open Question 1 rather than answering it.

    The fixture exists BECAUSE of Phase 33: when SC-2 widened `MarketDataSnapshot.market_data`
    to `dict[str, Any] | None`, no shipped model declared a required mapping any more, so 33-07
    minted a module-local carrier to keep the contract testable. Its own docstring records the
    reasoning: "la alternativa, borrar las filas, habría retirado un contrato vivo porque su
    ejemplo cambió."

    After Phase 36 the situation is different in kind, not in degree: the contract's
    IMPLEMENTATION (`_mapping_value`) is being retired, not just its example. Keeping the two
    tests would require keeping `_mapping_value` alive purely to have something to assert
    against — which contradicts D-05 directly and leaves dead code in a shipped module.

    This is the one genuinely undecided item in the phase. Everything else (the breaking
    `market_data` annotation change, the `entries` revocation) was already decided by the
    operator at the 33-07 Task 1 checkpoint (`fix-shape-now`) and re-confirmed in 36-CONTEXT —
    those are locked and are NOT reopened here.
  </context>
  <options>
    <option id="retire">
      <name>Retire the CR-03 axis with its machinery (RESEARCH recommendation)</name>
      <pros>
        Honours D-05 literally; leaves no dead code in a shipped module; the surviving half of
        the property — lock 8, "a non-`dict` payload emits exactly ONE terminal record" — is
        preserved and keeps its measured record set, it is only retitled in Plan 36-02
        (`test_mapping_pass_is_silent_under_a_non_dict_payload` becomes a name about the
        terminal record rather than about the pass). The retirement is recorded by name in the
        plan SUMMARY so the contract is discoverable, not forgotten.
      </pros>
      <cons>
        `market-data-client` stops asserting that a required `dict[...]` field reports and
        substitutes. If a future phase re-declares a mapping field in this package, nothing here
        catches the regression until someone re-mints the axis.
      </cons>
    </option>
    <option id="preserve">
      <name>Keep the two tests alive by keeping the machinery</name>
      <pros>
        The CR-03 contract keeps an executable assertion in this package.
      </pros>
      <cons>
        Contradicts D-05, which this phase exists to execute; leaves `_mapping_value` and
        `_apply_mapping_policy` in `models.py` with no shipped caller (the walker's `_is_model`
        branch takes over `market_data` in Plan 36-02); keeps `market-data-client` on form B of
        D-07 for a reason that is now purely test-facing; makes ROADMAP SC-5 unachievable as
        written.
      </cons>
    </option>
  </options>
  <read_first>
    - packages/market-data-client/tests/test_decode.py:1181-1275 (the `_RequiredMapping` fixture and the two CR-03 tests, verbatim)
    - packages/market-data-client/tests/test_decode.py:1295-1309 (`test_mapping_pass_is_silent_under_a_non_dict_payload` — the lock-8 half that survives either way)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Open Questions" item 1
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-CONTEXT.md § D-05
  </read_first>
  <files>.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-01-SUMMARY.md</files>
  <action>
    Present the two options above verbatim, with the RESEARCH recommendation (`retire`) named as a
    recommendation rather than as a default, and wait for the operator's verdict. Write the verdict
    string into the plan SUMMARY under a heading that names CR-03, together with one sentence of
    rationale, so the retired-or-retained contract is discoverable by name later rather than only
    inferable from a diff. Do not edit any test file in this task — Task 3 executes the verdict.
  </action>
  <verify>
    <automated>grep -c 'CR-03' .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - A verdict string of exactly `retire` or `preserve` is recorded in the plan SUMMARY under a
      heading naming CR-03.
    - If `preserve` is selected, the SUMMARY also records that ROADMAP SC-5 cannot be satisfied
      as written and names the follow-up needed.
    - No file under `packages/` changed during this task.
  </acceptance_criteria>
  <done>The operator's verdict is recorded verbatim in the SUMMARY under a CR-03 heading, and no code has moved.</done>
  <resume-signal>Select: retire, or preserve</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Give test_core.py and test_decode.py their own Optional detector and drop the mapping assertions</name>
  <files>packages/market-data-client/tests/test_core.py, packages/market-data-client/tests/test_decode.py</files>
  <read_first>
    - packages/market-data-client/src/market_data_client/models.py:118-129 (the two helper bodies to copy — the six-line Optional detector, and the mapping predicate that is NOT copied anywhere)
    - packages/market-data-client/tests/test_null_object.py:33-38 (the module docstring paragraph that declares module-local helper copies DELIBERATE in this monorepo — the pattern being followed here)
    - packages/market-data-client/tests/test_core.py:1148-1177 (the two health locks — the mapping half and the T-31-17 Optional half)
    - packages/market-data-client/tests/test_core.py:1440-1455 (the mutation-result lock — three assertions, only one is about mapping)
    - packages/market-data-client/tests/test_decode.py:667-719 (the call-site-exemption guard and its Phase 31 narrowing docstring)
    - packages/market-data-client/tests/test_decode.py:1347-1383 (the WR-03 lock that borrows the Optional detector)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Censo de la maquinaria de mapping" (the authoritative 13-row table) and § "Pitfall 1"
  </read_first>
  <action>
    Execute the RESEARCH census row by row. Six sites, two files, zero line-range deletions.
    D-05 is what this discharges; Pitfall 1 is what it avoids.

    In `test_core.py`:

    1. Add a module-local `_strip_optional` helper near the top of the file, copied from the
       six-line body in `models.py:118-124` — signature `(tp: Any) -> Any`, returns `T` from
       `T | None` by testing `get_origin(tp)` against `Union` and `types.UnionType` and keeping
       the single non-`NoneType` argument, passing everything else through. Add the imports it
       needs (`types`, and `Union` / `get_args` / `get_origin` from `typing`) if the file does
       not already carry them. Give it a docstring that states WHY it is a local copy: this
       monorepo has no shared internal package by design, the same rationale
       `test_null_object.py` states for its own local helpers.
    2. In `test_health_models_declare_no_mapping_field_and_no_received_at` (~line 1151): delete
       the assertion that reads the mapping predicate off the models module, keep the
       `"received_at" not in hints` assertion, rename the function to
       `test_health_models_declare_no_received_at`, and rewrite the one-line docstring so it
       describes only what survives. The `cast(Any, ...)` comment two lines above cites the
       retired call-site pass as the precedent for that discipline — repoint it at the walker
       (`_decode.hints_for`) instead, so the comment does not outlive its referent.
    3. In `test_health_models_declare_exactly_the_two_locked_optionals` (~line 1162, lock
       T-31-17): repoint the detector call at the module-local copy. **Do not touch anything
       else in this test** — it is not a mapping test, it is the "a seventh Optional cannot be
       added" lock, and its expected set stays `{"FeedIngestor.last_error",
       "FeedPipeline.last_write_error"}`.
    4. In `test_mutation_result_models_declare_no_mapping_field_no_received_at_no_optional`
       (~line 1441): delete the mapping assertion, keep the `received_at` assertion and the
       no-Optional assertion, repoint the latter at the module-local detector, rename the
       function to `test_mutation_result_models_declare_no_received_at_and_no_optional`, and
       trim the docstring's mapping clause.

    In `test_decode.py`:

    5. Add the same module-local `_strip_optional` helper with the same docstring rationale, and
       the imports it needs.
    6. In `test_no_call_site_exempt_safemodel_appears_as_a_nested_field_type` (~line 667): drop
       the mapping disjunct from the `exempt` comprehension so exemption is defined solely as
       "the class declares its own `from_api`". The set becomes `{MarketDataSnapshot, Symbol}`,
       so the `assert exempt` non-vacuity guard on the next line still holds — verify that, do
       not assume it. Rewrite the docstring paragraph that describes the retired call-site pass
       as a second source of exemption, and rewrite the closing paragraph that promises "the two
       companion tests below" pin the same two axes: after this plan there is one companion, the
       WR-03 lock.
    7. In `test_models_with_a_from_api_override_are_never_a_nested_field_type` (~line 1347, lock
       WR-03): repoint the detector call at the module-local copy and change nothing else. Its
       expected set stays `{"MarketDataSnapshot", "Symbol"}`. Record in the SUMMARY that Plan
       36-02 will make this test's `nested_types` value GROW (by `MarketDataEntries`,
       `EntryValue`, `BookLevel`) while its verdict stays identical — RESEARCH Open Question 3
       resolved it as a non-event, and an executor who sees the computed set change should not
       treat it as a regression.

    Leave `models.py` alone. It still defines all four helpers after this task; nothing imports
    them from a test any more, which is the whole point.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client -q 2>&1 | tail -3 &amp;&amp; uv run ruff check packages/market-data-client &amp;&amp; uv run mypy packages/market-data-client/tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q` reports `663 passed` — this task deletes no test.
    - `grep -c 'models\._is_mapping(' packages/market-data-client/tests/test_core.py packages/market-data-client/tests/test_decode.py` reports `0` for both files.
    - `grep -c 'models\._strip_optional(' packages/market-data-client/tests/test_core.py packages/market-data-client/tests/test_decode.py` reports `0` for both files.
    - `grep -c 'def _strip_optional' packages/market-data-client/tests/test_core.py packages/market-data-client/tests/test_decode.py` reports `1` for both files.
    - `uv run pytest packages/market-data-client/tests/test_core.py -k 'locked_optionals' -q` passes — T-31-17 survives the repointing.
    - `uv run pytest packages/market-data-client/tests/test_decode.py -k 'from_api_override_are_never' -q` passes — WR-03 survives the repointing.
    - `git rev-parse HEAD:packages/market-data-client/src/market_data_client/models.py` is unchanged from its value at the start of this task.
    - `uv run mypy packages/market-data-client/tests` prints `Success: no issues found`.
  </acceptance_criteria>
  <done>Both test modules carry their own Optional detector, the four mapping assertions are gone, the two renamed locks still assert their non-mapping halves, and 663 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the CR-03 verdict and delete the mapping-carrier precondition test</name>
  <files>packages/market-data-client/tests/test_decode.py</files>
  <read_first>
    - packages/market-data-client/tests/test_decode.py:1311-1344 (`test_no_mapping_carrying_model_is_ever_a_nested_field_type` — the one clean deletion in the census)
    - packages/market-data-client/tests/test_decode.py:1181-1275 (the `_RequiredMapping` fixture and the two CR-03 tests, subject to the Task 1 verdict)
    - packages/market-data-client/tests/test_decode.py:1176-1184 (the section banner that introduces the CR-03 block — it goes with the block)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Runtime State Inventory" (the ⚠ note: CONTEXT's `~1328-1373` range covers TWO tests, one to delete and one to keep)
  </read_first>
  <action>
    Delete `test_no_mapping_carrying_model_is_ever_a_nested_field_type` in full — it is the only
    unconditional deletion in the census (D-05). Its subject is the mapping axis end to end: it
    asserts the carrier set equals `{"MarketDataSnapshot"}`, which stops being true the moment
    Plan 36-02 flips that field to a model. **Stop at its closing line.** The very next function
    in the file is the WR-03 lock, which survives — CONTEXT's `~1328-1373` range covers both, and
    taking the range would silently retire WR-03.

    Then apply the Task 1 verdict:

    - Verdict `retire`: delete the `_RequiredMapping` fixture, its two CR-03 tests
      (`test_absent_required_mapping_field_reports_missing_and_substitutes_the_empty_dict`,
      `test_strict_mode_raises_on_an_absent_required_mapping_field`) and the section banner that
      introduces them. Do NOT touch the four remaining tests in that block — they assert on
      `MarketDataSnapshot.market_data` and Plan 36-02 migrates them.
    - Verdict `preserve`: leave the fixture and its two tests in place, and record in the SUMMARY
      that Plan 36-02 must therefore keep the machinery alive and that ROADMAP SC-5 is not
      satisfiable as written.

    Whichever verdict landed, write the disposition into the plan SUMMARY naming CR-03
    explicitly, so the retired contract is discoverable by name rather than inferable from a diff.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client -q 2>&1 | tail -3 &amp;&amp; uv run ruff check packages/market-data-client &amp;&amp; uv run mypy packages/market-data-client/tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q` reports exactly `660 passed` under the `retire` verdict, or exactly `662 passed` under `preserve`. Any other count means a deletion overshot the census — RESEARCH Pitfall 1's warning sign.
    - `uv run pytest packages/market-data-client/tests/test_decode.py -k 'from_api_override_are_never' -q` passes — WR-03 is still present and still asserts `{"MarketDataSnapshot", "Symbol"}`.
    - `uv run pytest packages/market-data-client/tests/test_decode.py -k 'silent_under_a_non_dict' -q` passes — the lock-8 half of CR-03 survives under both verdicts.
    - `grep -c 'def test_no_mapping_carrying_model_is_ever_a_nested_field_type' packages/market-data-client/tests/test_decode.py` reports `0`.
    - `uv run mypy packages/market-data-client/tests` prints `Success: no issues found`.
    - `git rev-parse HEAD:packages/market-data-client/src/market_data_client/models.py` is unchanged from its value at the start of Task 2.
  </acceptance_criteria>
  <done>The mapping-carrier precondition test is gone, the CR-03 verdict is executed and recorded by name, WR-03 and lock 8 are intact, and the suite is green at the exact count the verdict predicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor HTTP response → `_decode` walker | Untrusted JSON crosses here. This plan does not move the boundary; it only removes test-side references to a call-site pass that sat just inside it. |
| divergence record → `market_data_client` logger | Structured records leave the process here. Unchanged by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01-01 | Tampering | The test suite's own verification power — a range deletion silently retiring T-31-17 or WR-03 | mitigate | Edit by the RESEARCH per-call-site census, never by line range; acceptance criteria run both locks by name (`-k locked_optionals`, `-k from_api_override_are_never`) and pin the exact post-task test count so an overshoot reddens. |
| T-36-01-02 | Elevation of Privilege | The mutating gate (`_ensure_mutation_allowed`) being loosened as collateral | accept | This plan touches two test modules only; the gate's AST guard (`test_mutation_gate_ast.py`) and `verification/test_main_market_data_no_gate_bypass.py` run inside the package suite, which is a per-task gate. |
| T-36-01-03 | Information Disclosure | Divergence records leaking symbol / account identifiers | accept | No record shape changes here. The existing mitigation stands: records carry field path and TYPE names only, never values (T-29-22), and undeclared keys are normalised by `_safe_key` (lock 11). |
| T-36-01-SC | Tampering | npm / pip / cargo supply chain | accept | This plan installs nothing. RESEARCH § Package Legitimacy Audit records "no aplica"; `uv.lock` and `packages/market-data-client/pyproject.toml` are not in `files_modified` and are pinned by blob hash in Plan 36-03's phase gate. |
</threat_model>

<verification>
- `uv run pytest packages/market-data-client -q` — green, count matches the Task 1 verdict.
- `uv run ruff check packages/market-data-client` — clean (the new local helpers bring imports with them).
- `uv run mypy packages/market-data-client/tests` — `Success: no issues found`.
- `git rev-parse HEAD:packages/market-data-client/src/market_data_client/models.py` returns the same blob hash it returned before this plan's first commit — `models.py` is untouched by this plan.
- `uv run python tools/check_decode_intactness.py` — the `_decode.py` digest is untouched (SC-5's "sin mover el hash").
</verification>

<success_criteria>
- Zero references to the mapping machinery remain in `packages/market-data-client/tests/`.
- T-31-17, the mutation-result no-Optional lock and WR-03 all still run and still pass.
- The CR-03 disposition is decided by the operator and recorded by name in the SUMMARY.
- `models.py` is untouched — Plan 36-02 owns every source-side edit of this phase.
- The package suite is green at exactly the count the verdict predicts.
</success_criteria>

<output>
Create `.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-01-SUMMARY.md` when done.
Record: the CR-03 verdict verbatim, the exact post-plan test count, and the note that Plan 36-02
will grow WR-03's computed `nested_types` set without changing its verdict (RESEARCH Open
Question 3 — a non-event, not a regression).
</output>
