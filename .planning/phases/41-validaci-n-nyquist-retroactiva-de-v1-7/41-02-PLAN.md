---
phase: 41
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "La fila placeholder de la Phase 35 deja de ser la unidad de disposición: sus 12 bloques de verificación reales quedan reconstruidos y disponibles individualmente"
    - "Las 13 filas de la Phase 35 llevan exactamente una disposición con evidencia nombrada y superficie de enforcement de CI"
    - "El único paso RED de TDD del conjunto queda dispuesto históricamente en vez de reportado como rojo"
    - "35-VALIDATION.md declara el SHA de v1.7 y pasa a status validated sin que nyquist_compliant cambie"
  artifacts:
    - path: ".planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md"
      provides: "Mapa reconstruido de 12 filas + sección Validation Audit 2026-08-31 con 13 disposiciones"
      contains: "## Validation Audit 2026-08-31"
  key_links:
    - from: ".planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md"
      to: ".planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-PLAN.md"
      via: "cada fila reconstruida cita su plan y tarea de origen"
      pattern: "35-0[1-5]-0[1-3]"
    - from: ".planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md"
      to: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      via: "copia literal del SHA de v1.7, del HEAD de baseline y del esqueleto de sección"
      pattern: "37a83fe693a303a551f4374f48fe6fc5521804f7"
---

<!-- planner-discipline-allow: (filled by planner) -->

<objective>
Auditar la Phase 35, la única de las cinco cuyo `## Per-Task Verification Map` shipeó vacío: una sola
fila placeholder que, dispuesta como unidad, certificaría "1/1, 100%" sin auditar nada (D-05).

Purpose: la Phase 35 es la excepción estructural del conjunto. Antes de disponer hay que reconstruir
sus 12 bloques de verificación reales desde `35-01..05-PLAN.md`, y tres de esos 12 comandos NO son
re-ejecutables tal como fueron escritos: uno es un paso RED de TDD que se invierte contra el árbol
post-implementación, y dos assertan sobre una ruta de `.planning/` que la mudanza al archivo del
milestone invalidó. Sin ese tratamiento explícito la auditoría reporta rojo falso.

Output: `35-VALIDATION.md` con el mapa reconstruido (12 filas + la placeholder marcada como superada),
la sección `## Validation Audit 2026-08-31` con 13 disposiciones, y el front-matter transformado.
Cero fuente de producto tocada; cero ediciones a los archivos de plan históricos.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Reconstruir el mapa de verificación de la Phase 35 — 12 filas desde los cinco archivos de plan</name>
  <files>.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md (el archivo objetivo, completo)
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-PLAN.md
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-02-PLAN.md
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-03-PLAN.md
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-04-PLAN.md
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-05-PLAN.md
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md (mapa de 10 columnas mejor poblado — analog de forma de fila)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md § 2 (claves de fila) y § 5 (regla de la columna Status)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Row Inventory (tabla de 12 filas con su forma de comando y su re-ejecutabilidad)
  </read_first>
  <action>
    Re-verificar primero el invariante de criterio 1 con `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`.
    Si sale distinto de 0, ABORTAR: la atribución de la auditoría depende de que la fuente de v1.7 siga
    intacta hasta que los cinco artefactos estén escritos.

    Extraer los bloques de verificación reales de los cinco archivos de plan. El conteo medido es
    **12**: `35-01-PLAN.md` tiene 3 tareas, `35-02` tiene 2, `35-03` tiene 2, `35-04` tiene 3 y `35-05`
    tiene 2; cada tarea tiene exactamente un bloque `<verify><automated>` y ninguna es un checkpoint.
    D-05 escribió "~14" — el número medido es 12; si la extracción da otra cosa, no ajustar al número
    esperado: reportar la divergencia. Verificar el conteo con `grep -c` sobre los cinco archivos antes
    de escribir la tabla.

    Reescribir `## Per-Task Verification Map` en `35-VALIDATION.md` conservando las mismas 10 columnas
    y el vocabulario de relleno del analog `38-VALIDATION.md` (guion medio y `N/A` en `Threat Ref` y
    `Secure Behavior`, comandos entre backticks, `✅` / `❌ W0` / `❌ manual` en `File Exists`, línea de
    leyenda de Status al pie).

    Conservar la fila placeholder existente — la que dice que la llenaría el planner — en su lugar, con
    una nota adjunta en su celda de Status que la marque como superada por las 12 filas reconstruidas y
    remita a `## Validation Audit 2026-08-31`. Borrarla eliminaría la evidencia de que la Phase 35
    shipeó con un mapa sin llenar, que es en sí un hallazgo de la auditoría.

    Agregar debajo las 12 filas, con `Task ID` en la forma `{plan}-{tarea}` en el orden de los planes:
    `35-01-01`, `35-01-02`, `35-01-03`, `35-02-01`, `35-02-02`, `35-03-01`, `35-03-02`, `35-04-01`,
    `35-04-02`, `35-04-03`, `35-05-01`, `35-05-02`. La columna `Plan` lleva el número de plan, la
    columna `Wave` el valor real leído del front-matter de cada plan, la columna `Requirement` el ID que
    ese plan declara en su front-matter (`NOBJ-01` / `NOBJ-02`), y `Automated Command` el comando
    transcrito **literal** del bloque `<verify><automated>` de esa tarea — sin corregirlo todavía. Las
    correcciones son parte de la disposición y se documentan en la sección de auditoría, no borrando el
    comando original del mapa.

    Marcar en `Test Type` el vocabulario ya usado en el repo: `unit`, `static`, `snapshot`,
    `unit (RED fixture)`, `doc review`. La tarea 1 del plan `35-01` es la única `unit (RED fixture)` del
    conjunto — su comando contiene una negación sobre un selector que hoy pasa. No ejecutarla en esta
    tarea.

    Dejar la columna `Status` de las 12 filas nuevas en el valor pendiente por ahora: se actualiza en la
    tarea 2, después de re-ejecutar, según la regla de la sección 5 del contrato.

    No tocar ninguna otra sección del archivo en esta tarea. No editar los cinco `35-0N-PLAN.md`: son
    registro histórico y sus rutas stale se corrigen en el comando ejecutado, no en el archivo.
  </action>
  <verify>
    <automated>D=.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker; test "$(grep -h -c '<automated>' $D/35-0*-PLAN.md | paste -sd+ - | bc)" -eq 12 && test "$(grep -c '^| 35-0[1-5]-0[1-3] ' $D/35-VALIDATION.md)" -eq 12 && grep -q 'filled by planner' $D/35-VALIDATION.md && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -h -c '<automated>' 35-0*-PLAN.md` suma exactamente `12` (3+2+2+3+2).
    - `grep -c '^| 35-0[1-5]-0[1-3] ' 35-VALIDATION.md` es exactamente `12`.
    - Los 12 Task IDs son únicos: `grep -oE '^\| 35-0[1-5]-0[1-3]' 35-VALIDATION.md | sort -u | wc -l` es `12`.
    - La fila placeholder sobrevive: `grep -c 'filled by planner' 35-VALIDATION.md` es `1`, y su celda de Status contiene la palabra `superada`.
    - Exactamente una fila lleva `unit (RED fixture)` en `Test Type`: `grep -c 'unit (RED fixture)' 35-VALIDATION.md` es `1`.
    - Los cinco `35-0N-PLAN.md` quedan sin modificar: `git status --porcelain .planning/milestones/v1.7-phases/35-*/35-0*-PLAN.md` está vacío.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al terminar.
  </acceptance_criteria>
  <done>El mapa de la Phase 35 tiene 12 filas reales con Task ID único, comando literal de origen y la placeholder conservada y marcada como superada. Ninguna fila fue ejecutada todavía.</done>
</task>

<task type="execute">
  <name>Task 2: Re-ejecutar las 10 filas re-ejecutables, disponer las 13, y escribir la sección de auditoría con el front-matter transformado</name>
  <files>.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md (con el mapa ya reconstruido en la tarea 1)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md (secciones 1 a 8, completas)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Row Inventory, § Re-Execution Results → Cross-cutting, § Disposition Decision Rules, § CI Enforcement Map, § Common Pitfalls 2 y 3, § Runtime State Inventory
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-SUMMARY.md (evidencia histórica del paso RED)
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VERIFICATION.md (verdad #2 — el set rojo medido)
    - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md (la ruta correcta post-archivo)
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección de auditoría + estilo de celda de evidencia)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (par de encabezados en negrita, sección de escalaciones, línea tildada de sign-off)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (prosa modelo para explicar un resultado PARTIAL)
  </read_first>
  <action>
    Escanear los 12 comandos reconstruidos buscando negaciones antes de ejecutar nada
    (RESEARCH.md Pitfall 3). Sólo la tarea 1 del plan `35-01` la tiene: su cadena corre un selector
    positivo y después asserta que tres tests fallan. Esos tres hoy pasan (medido: 45 pasaron, 3
    descartados), así que la cadena sale 1 **por diseño**. Esa fila NO se ejecuta y NO se "arregla":
    se dispone `VERIFIED-HISTORICALLY` por R-05, citando `35-01-SUMMARY.md`, el commit `ece3a3c` con el
    set rojo de 11 fallas medido, y la verdad #2 de `35-VERIFICATION.md` como corroboración
    independiente.

    Corregir la ruta de los comandos de las dos tareas del plan `35-02`. Ambas assertan sobre
    `35-RETIRED-TRIPLES.md` bajo `.planning/phases/35-.../`, ruta que la mudanza al archivo del
    milestone invalidó; el archivo vive hoy en
    `.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`
    y tiene 58 filas de tabla, por encima del piso de 35 que la aserción exige. Ejecutar el comando con
    la ruta corregida y disponer `VERIFIED-NOW (ruta corregida)` por R-03, transcribiendo en la celda de
    evidencia **ambos** comandos: el original del mapa y el corregido. No editar `35-02-PLAN.md`.

    Re-ejecutar las 10 filas restantes tal cual quedaron transcritas en el mapa. Seis de ellas son
    cadenas de suite de paquete más mypy más ruff (higyrus, iol, market-data, matriz, ámbito, wallets);
    dos son suites dirigidas; y las dos del plan `35-05` son las más caras — una corre los cuatro gates
    de `tools/` más la suite completa de `packages` (la corrida completa medida es de 2152 tests en
    ~93 s), y la otra es el gate de fase con los cuatro gates encadenados. Presupuestar ese tiempo; no
    reemplazarlas por una versión reducida.

    Aplicar el guardia anti-vacuidad de la sección 6 del contrato a cada corrida: la línea de resumen
    capturada debe reportar un conteo de tests que pasaron distinto de cero. Una salida que sólo
    reporte tests descartados sin ninguno ejecutado no es evidencia — pytest sale con código 5 en ese
    caso, que un `&&` sí corta pero que en un transcripto pegado se lee limpio. RESEARCH.md no encontró
    ninguna fila de la Phase 35 con esa forma; si aparece alguna, es un hallazgo real: escalarlo en el
    SUMMARY, no re-apuntar el selector en silencio.

    Disponer la fila manual-only existente, la de identidad byte-a-byte de snapshots: sus instrucciones
    de test son un comando ejecutable (regenerar y comprobar que el diff queda vacío), y RESEARCH.md lo
    re-corrió esta sesión con resultado byte-idéntico y árbol limpio. Se dispone `VERIFIED-NOW` por
    R-01 con la salida de esta sesión. Después de correr el regenerador, comprobar que
    `git status --porcelain` sigue vacío: ese script escribe cuatro archivos bajo
    `verification/snapshots/` y un árbol sucio invalidaría el criterio 1 en silencio.

    Recortar toda salida capturada a su línea de resumen antes de escribirla al archivo. La salida
    verbosa completa no se pega: CLAUDE.md prohíbe exponer credenciales en reportes y esta sección se
    commitea.

    Escribir `## Validation Audit 2026-08-31` al final de `35-VALIDATION.md`, precedida de una regla
    horizontal, después de `## Validation Sign-Off`, con el esqueleto de ocho bloques de la sección 5
    del contrato. Valores concretos para esta fase: el denominador es **13**; la disposición esperada es
    12 `VERIFIED-NOW` (de los cuales 2 con ruta corregida) y 1 `VERIFIED-HISTORICALLY`, con
    0 `NOT-VERIFIABLE-RETROACTIVELY`; los archivos de test nuevos escritos son **0**; el conteo esperado
    de filas cuya celda de enforcement dice que no corre en CI es **5** — el paso RED, las dos filas
    que assertan sobre markdown de `.planning/`, la fila de snapshots, y el gate de fase, cuya cuarta
    aserción es un script que no aparece en `ci.yml` (los tres gates de `tools/` de esa misma fila sí
    corren en el job `lint`; la celda debe decirlo con esa precisión en vez de colapsar la fila a un
    solo veredicto). Si la medición da un conteo distinto al esperado, escribir el medido y anotar la
    divergencia en el SUMMARY — nunca ajustar la tabla para que calce con el número esperado.

    Llenar la cuarta columna con la tabla de lookup de la sección 7 del contrato. Las cadenas de suite
    de paquete mapean al job `test`; sus tramos de mypy al job `typecheck`; sus tramos de ruff al job
    `lint`; los tres gates de `tools/` a sus líneas exactas del job `lint`.

    Escribir la sección de hallazgos de bookkeeping con los dos que RESEARCH.md midió y que no cambian
    ninguna disposición: la tabla de infraestructura de test de este archivo declara una versión de
    pytest que ya no es la real (declara 8.3; la medida es 9.0.3, con mypy 1.20.2 y ruff 0.15.12), y las
    dos rutas stale de `.planning/phases/35-...` siguen embebidas en `35-02-PLAN.md` a propósito.
    Nombrarlos; no corregirlos en silencio.

    Actualizar la columna `Status` de las 13 filas según la regla de la sección 5 del contrato, y
    extender la línea de leyenda con los valores nuevos.

    Transformar el front-matter según la sección 4 del contrato: `status` pasa de `draft` a `validated`;
    `nyquist_compliant` se queda en `false` porque R-09 no se satisface (esta fase tiene una fila
    histórica y dos correcciones de ruta); `not_verifiable_retroactively` es `0`; se agregan
    `audited_commit_sha` con el commit de v1.7, `audit_baseline_head` copiado literal del contrato,
    `frozen_tree_verified: true`, `updated: 2026-08-31` y `last_audited: 2026-08-31`. Se dejan intactos
    `phase`, `slug`, `created`, `wave_0_complete` y los dos comentarios de lifecycle. No copiar la forma
    de `40-VALIDATION.md`: su flag lo puso plan-check y no tiene sección de auditoría.

    Cerrar con la línea de veredicto en la forma fijada y con una línea nueva tildada agregada al
    `## Validation Sign-Off`, dejando sin tildar las casillas preexistentes — en particular la de
    `nyquist_compliant: true`, cuyo tildado sería exactamente la violación del criterio 3.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md; N=$(awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 35-[rm][0-9]/' "$F" | wc -l | tr -d ' '); test "$N" -eq 13 && S=$(awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 35-[rm][0-9]/' "$F" | grep -c 'VERIFIED-NOW'); H=$(awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 35-[rm][0-9]/' "$F" | grep -c 'VERIFIED-HISTORICALLY'); R=$(awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 35-[rm][0-9]/' "$F" | grep -c 'NOT-VERIFIABLE-RETROACTIVELY'); test $((S+H+R)) -eq 13 && grep -q '^status: validated' "$F" && grep -q '^nyquist_compliant: false' "$F" && grep -q '^not_verifiable_retroactively: 0' "$F" && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$F" && git status --porcelain verification/ | grep -q . && exit 1 || git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de disposición tiene exactamente 13 filas: el awk acotado entre `### Disposición por fila` y la línea de leyenda cuenta `13` líneas que empiezan con `| 35-r` o `| 35-m`.
    - Cada fila lleva exactamente una disposición: la suma de los tres conteos de token dentro de ese rango acotado es exactamente `13` (si una fila llevara dos tokens la suma daría más de 13).
    - El reparto medido es 12 / 1 / 0: `VERIFIED-NOW` = `12`, `VERIFIED-HISTORICALLY` = `1`, `NOT-VERIFIABLE-RETROACTIVELY` = `0`.
    - Cero evidencia vacua: `awk` acotado a la tabla, filtrado a líneas que mencionan tests descartados y que no mencionan tests pasados, devuelve `0` líneas.
    - Front-matter: `grep -c '^status: validated'` es `1`, `grep -c '^nyquist_compliant: false'` es `1`, `grep -c '^not_verifiable_retroactively: 0'` es `1`, `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7'` es `1`, y `grep -cE '^audit_baseline_head: [0-9a-f]{40}'` es `1`.
    - El literal de cierre de D-08 está presente: `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 35-VALIDATION.md` es `1`.
    - Ninguna casilla preexistente del sign-off fue tildada: `grep -c '^- \[x\] .*nyquist_compliant: true' 35-VALIDATION.md` es `0`.
    - Cero locks nuevos y árbol limpio fuera de `.planning/`: `git status --porcelain verification/` vacío y `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
    - `git status --porcelain` sólo lista `35-VALIDATION.md` bajo `.planning/milestones/v1.7-phases/`.
  </acceptance_criteria>
  <done>Las 13 filas de la Phase 35 están dispuestas con evidencia nombrada y superficie de enforcement; el front-matter declara ambos SHA y pasa a validated sin que nyquist_compliant cambie.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producido por este plan:
- `.planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md` — **modificado**. Mapa de verificación reconstruido (12 filas reales con Task ID `35-01-01`..`35-05-02`, más la fila placeholder conservada y marcada como superada); sección `## Validation Audit 2026-08-31` con bloque de métricas, tabla de disposición de 13 filas y 4 columnas, correcciones de ruta, hallazgos de bookkeeping, escalaciones y veredicto; front-matter con `status: validated`, `nyquist_compliant: false`, `not_verifiable_retroactively: 0`, `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified: true`, `updated`, `last_audited`.

Cero archivos nuevos. Cero locks nuevos en `verification/`. Cero ediciones a `.github/workflows/ci.yml`. Cero ediciones a `35-0N-PLAN.md`. Ninguna declaración inerte ruteada a Phase 45 se origina en este plan (el conteo esperado de locks nuevos es 0 por D-08).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de pytest/mypy/ruff local → markdown committeado | Único cruce: las líneas de resumen que este plan transcribe quedan en el repo |
| script generador de snapshots → árbol de trabajo | `verification/regen_snapshots.py` escribe 4 archivos; si el resultado no fuera byte-idéntico ensuciaría el árbol e invalidaría el criterio 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-02-01 | Information Disclosure | transcripción de salida de suite a `35-VALIDATION.md` | mitigate | Sección 6 del contrato: sólo línea de resumen, nunca salida verbosa; ninguna de las 12 filas lee `.env` ni abre red (ASVS V7) |
| T-41-02-02 | Tampering | `verification/regen_snapshots.py` escribe en el árbol de trabajo | mitigate | Criterio de aceptación explícito: `git status --porcelain verification/` vacío y el diff contra `v1.7` sigue vacío después de correrlo |
| T-41-02-03 | Repudiation | falsa garantía por marcar verde una fila que en realidad no ejecutó ningún test | mitigate | Guardia anti-vacuidad de la sección 6: la evidencia debe reportar un conteo de tests pasados distinto de cero; gate de aceptación que busca líneas con descarte y sin ejecución |
| T-41-02-04 | Repudiation | disponer el paso RED de TDD como rojo y "arreglar" un test para que la cadena pase | mitigate | Regla R-05 más la instrucción explícita de escanear negaciones antes de ejecutar; el archivo de plan histórico queda intacto por criterio de aceptación |
| T-41-02-SC | Tampering | instalaciones de paquetes | accept | Cero: ningún comando de este plan invoca `uv add`, `npm`, `pip` ni `cargo`; `uv.lock` queda byte-idéntico |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V5/V6 — el plan edita un markdown y re-corre
suites locales ya existentes; no hay auth, sesión, control de acceso, input no confiable ni cripto.
</threat_model>

<verification>
1. Tabla de disposición de 13 filas, suma de tokens exactamente 13, reparto 12 / 1 / 0.
2. Front-matter con los cinco campos nuevos y `nyquist_compliant` sin cambiar.
3. `git status --porcelain verification/` vacío; `ls verification/test_*.py | wc -l` sigue en 52.
4. `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
5. Sólo `35-VALIDATION.md` cambió bajo `.planning/milestones/`.
</verification>

<success_criteria>
- Ninguna de las 13 filas queda sin disponer y ninguna lleva dos disposiciones.
- La fila placeholder no fue usada como unidad de disposición ni borrada.
- Los tres comandos no re-ejecutables tal como fueron escritos están tratados por regla explícita (uno histórico, dos con ruta corregida), no reportados como rojo.
- Toda celda `VERIFIED-NOW` cita comando y línea de resumen de esta sesión con conteo de tests pasados distinto de cero.
- `nyquist_compliant` sigue en `false` con rationale escrito.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-02-SUMMARY.md` when done.
</output>
