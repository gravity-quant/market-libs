---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .github/workflows/ci.yml
  - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
  - packages/iol-client/src/iol_client/_core.py
  - packages/higyrus-client/src/higyrus_client/_core.py
  - packages/matriz-client/src/matriz_client/_core.py
  - verification/test_sync_async_isolation.py
autonomous: false
requirements: [REFAC-03]
requirements_addressed: [REFAC-03]
tags: [phase-07, import-linter, ci-gate, cross-leak]
user_setup: []

must_haves:
  truths:
    - "import-linter v2.11+ instalado y configurado en pyproject.toml; uv run lint-imports exits 0 al final del plan"
    - ".github/workflows/ci.yml ejecuta `uv run lint-imports` como gate en el job lint"
    - "Los 4 paquetes tienen `_core.py` placeholder con docstring + `from __future__ import annotations` (Pitfall 6 mitigation per D-09/A2)"
    - "`verification/test_sync_async_isolation.py` existe parametrizado sobre 4 paquetes; matriz skipea con reason explícito (D-11)"
    - "Test sync+async sentinel reaches wire request por paquete (3 pkgs no-skip): sync usa `SYNC-sentinel-<pkg>` y async usa `ASYNC-sentinel-<pkg>` con literales D-12 Phase 6 / D-10"
    - "Los 393 tests baseline siguen verde post-plan (zero regression — Pitfall 4 / A5)"
    - "Plan-1 NO bloquea Plan 2-5 (D-12): los contracts forbidden pasan vacíos contra placeholder _core.py"
  artifacts:
    - path: "pyproject.toml"
      provides: "`import-linter>=2.11,<3` in `[dependency-groups] dev` + `[tool.importlinter]` con 4 contracts forbidden"
      contains: "import-linter>=2.11"
    - path: ".github/workflows/ci.yml"
      provides: "CI step `uv run lint-imports` en job lint"
      contains: "lint-imports"
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_core.py"
      provides: "Placeholder for ámbito _core module (Pitfall 6 mitigation)"
      contains: "from __future__ import annotations"
      min_lines: 10
    - path: "packages/iol-client/src/iol_client/_core.py"
      provides: "Placeholder for iol _core module"
      contains: "from __future__ import annotations"
      min_lines: 10
    - path: "packages/higyrus-client/src/higyrus_client/_core.py"
      provides: "Placeholder for higyrus _core module"
      contains: "from __future__ import annotations"
      min_lines: 10
    - path: "packages/matriz-client/src/matriz_client/_core.py"
      provides: "Placeholder for matriz _core module"
      contains: "from __future__ import annotations"
      min_lines: 10
    - path: "verification/test_sync_async_isolation.py"
      provides: "Cross-leak sentinel test parametrizado sobre 4 paquetes (matriz skip)"
      contains: "SYNC-sentinel"
      min_lines: 80
  key_links:
    - from: "pyproject.toml [tool.importlinter]"
      to: "<pkg>._core (4 contracts)"
      via: "source_modules / forbidden_modules"
      pattern: "source_modules.*_core"
    - from: ".github/workflows/ci.yml"
      to: "uv run lint-imports"
      via: "CI step in lint job"
      pattern: "lint-imports"
    - from: "verification/test_sync_async_isolation.py"
      to: "SYNC-sentinel-<pkg> / ASYNC-sentinel-<pkg>"
      via: "configure(token=...) + httpx_mock wire assertion"
      pattern: "SYNC-sentinel|ASYNC-sentinel"
---

<objective>
Construir la infraestructura CI gates de Phase 7 ANTES de tocar ningún paquete: instalar y configurar `import-linter` v2.11 en `pyproject.toml` con 4 contracts `forbidden` (uno por paquete) que bloqueen el re-coupling de `_core.py → client.py/aio.py`, crear el step `uv run lint-imports` en `.github/workflows/ci.yml`, sembrar 4 `_core.py` placeholder (mitiga Pitfall 6: contracts vacíos contra módulo inexistente reportan WARN silente — A2/Open Q #4), y crear el guard cross-leak `verification/test_sync_async_isolation.py` parametrizado sobre 4 paquetes (matriz `pytest.skip` con reason D-11) que detecta a runtime cualquier bypass.

Purpose: Phase 7 D-09 + D-10 + D-11 — establecer las CI gates antes del primer refactor mecánico para que Plans 2-5 se verifiquen incrementalmente. Sin estas gates, una violación de boundary podría pasar a producción inadvertida (Pitfall 1/3).

Output: pyproject.toml con import-linter config + 4 contracts; .github/workflows/ci.yml con lint-imports step; 4 _core.py placeholders; verification/test_sync_async_isolation.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md
@CLAUDE.md
@pyproject.toml
@.github/workflows/ci.yml
@packages/iol-client/tests/test_fixture_reaches_production.py
@packages/matriz-client/tests/test_fixture_reaches_production.py
@packages/higyrus-client/tests/test_fixture_reaches_production.py
@packages/ambito-financiero-client/tests/test_fixture_reaches_production.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
@verification/test_public_surface.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificación human-blocking de `import-linter` package legitimacy</name>
  <files>pyproject.toml</files>
  <action>
    Pause for operator. Operator runs the 3 verification steps in `<how-to-verify>` and types "approved" in chat. No file modification — this is a gate BEFORE Task 2 modifies pyproject.toml.
  </action>
  <what-built>
    Antes de agregar `import-linter>=2.11,<3` a `pyproject.toml` `[dependency-groups] dev`, el operator humano DEBE verificar el package legitimacy (RESEARCH.md Package Legitimacy Audit — slopcheck unavailable, manual verification compensatoria).
  </what-built>
  <how-to-verify>
    Ejecutar los 3 comandos siguientes y confirmar:
    1. `pip index versions import-linter` → muestra versión `2.11` o superior (de 2.x).
    2. Visitar `https://github.com/seddonym/import-linter` → repo público con LICENSE Apache-2.0 + commits recientes.
    3. Visitar `https://pypi.org/project/import-linter/` → mantenedor `seddonym`, releases regulares desde 2017.

    Marca de approval esperada: "approved — import-linter 2.11+ verificado en PyPI + GitHub repo público; legítimo."
  </how-to-verify>
  <verify>
    <human-check>Operator confirms approval after running the 3 verification steps above.</human-check>
  </verify>
  <done>
    Operator types "approved" in chat after verifying import-linter is legitimate on PyPI + GitHub.
  </done>
  <resume-signal>Type "approved" si la verificación pasa; describir issues encontrados si no.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import-linter to dev deps + configure 4 forbidden contracts in pyproject.toml + create 4 `_core.py` placeholders</name>
  <files>pyproject.toml, packages/ambito-financiero-client/src/ambito_financiero_client/_core.py, packages/iol-client/src/iol_client/_core.py, packages/higyrus-client/src/higyrus_client/_core.py, packages/matriz-client/src/matriz_client/_core.py</files>
  <read_first>
    - pyproject.toml (lines 23-32 `[dependency-groups]` y 37-66 `[tool.ruff]`/`[tool.mypy]` shape — analog TOML section idiom per PATTERNS.md §12)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§12 `[tool.importlinter]` block exactly)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ "Code Examples" Example 1 — import-linter config con 4 contracts verbatim; § "Common Pitfalls" Pitfall 6 — placeholder rationale; § "Open Questions" Q4 — placeholder mitiga "Could not find package")
    - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py (Phase 6 module shape — docstring + `from __future__ import annotations` patrón para los placeholders, PATTERNS.md §1)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-09 con `import-linter` v2.11 verificada; D-12 — plan 1 no bloquea plan 2)
    - CLAUDE.md (mandatory `from __future__ import annotations`; sin wildcard; double quotes; ruff line-length=100)
  </read_first>
  <behavior>
    - Tras Task 2 ejecutado, `grep -c 'import-linter>=2.11' pyproject.toml` == 1.
    - Tras Task 2 ejecutado, `grep -c '\[\[tool.importlinter.contracts\]\]' pyproject.toml` == 4.
    - Tras Task 2 ejecutado, los 4 archivos `_core.py` placeholder existen, son válidos Python, y contienen exactamente:
      1. Docstring de propósito (per CLAUDE.md "module-level docstring obligatorio") — referencia D-01 (RequestSpec per-package) y D-04 (raise_for_response moved here);
      2. `from __future__ import annotations`;
      3. Listado vacío de `__all__: list[str] = []` (Plan 2-5 lo extiende).
    - Tras Task 2, `uv sync --all-packages --all-extras --dev` corre sin error; `import-linter` queda instalado.
    - Tras Task 2, `uv run lint-imports` exit 0 (4 contracts pasan vacíos contra placeholders).
    - mypy strict pasa contra los 4 placeholders (`uv run mypy packages/<pkg>/src/<pkg>/_core.py` exit 0).
    - ruff check + ruff format pasan (`uv run ruff check packages/`, `uv run ruff format --check packages/`).
  </behavior>
  <action>
    Editar `pyproject.toml`:

    1. En `[dependency-groups] dev` (líneas ~23-32), agregar la línea `"import-linter>=2.11,<3",  # Phase 7 REFAC-03 enforcement` al final del array (preservar trailing comma del último elemento; ruff/mypy/pytest/pre-commit conservan orden actual).

    2. Agregar al final del archivo (post `[tool.pytest.ini_options]` o donde corresponda topológicamente) la sección `[tool.importlinter]` literal de RESEARCH.md Code Examples Example 1:

       - `root_packages = ["ambito_financiero_client", "iol_client", "higyrus_client", "matriz_client"]`
       - 4 bloques `[[tool.importlinter.contracts]]` (uno por paquete) con:
         - `name = "<pkg>._core does not depend on transport modules"`
         - `type = "forbidden"`
         - `source_modules = ["<pkg>._core"]`
         - `forbidden_modules = ["<pkg>.client", "<pkg>.aio"]`

       Orden alfabético por paquete (ambito_financiero_client, higyrus_client, iol_client, matriz_client) para consistencia con `root_packages`.

    3. Crear los 4 archivos `_core.py` placeholder usando el module-shape de `_state.py` de Phase 6 (PATTERNS.md §1):

       - Path: `packages/<pkg>/src/<pkg>/_core.py` para los 4 paquetes (`ambito-financiero-client`/`ambito_financiero_client`, `iol-client`/`iol_client`, `higyrus-client`/`higyrus_client`, `matriz-client`/`matriz_client`).
       - Cabecera de cada uno: docstring multi-linea explicando:
         - "Pure builders and parsers for the <pkg> REST client. Phase 7 REFAC-03 + D-01..D-09 — NO imports from `<pkg>.client` or `<pkg>.aio` (enforced by import-linter contract in pyproject.toml)."
         - "Placeholder en Plan 1 — Plans 2-5 lo extienden con RequestSpec, builders, parsers, auth-flow primitives."
       - Inmediatamente después: `from __future__ import annotations`.
       - Después de un blank line, `__all__: list[str] = []` (Plan 2-5 lo poblará).

    4. Correr `uv lock` para actualizar `uv.lock` (A9 — el agregado de import-linter requiere relock); incluir `uv.lock` en `files_modified` si cambia.

    5. Correr `uv sync --all-packages --all-extras --dev` para instalar `import-linter`.

    6. Validar empíricamente Pitfall 6 / Open Q #4: `uv run lint-imports` con los 4 placeholders presentes debe imprimir "Contracts: 4 kept, 0 broken" (texto exacto de import-linter v2.11) y exit 0. Si imprime "Could not find package: <pkg>._core", los placeholders no son detectados — escalar al usuario.
  </action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv sync --all-packages --all-extras --dev --frozen &amp;&amp; uv run lint-imports &amp;&amp; uv run mypy packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/iol-client/src/iol_client/_core.py packages/higyrus-client/src/higyrus_client/_core.py packages/matriz-client/src/matriz_client/_core.py &amp;&amp; uv run ruff check packages/ &amp;&amp; uv run ruff format --check packages/</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "import-linter>=2.11" pyproject.toml` == 1
    - Source: `grep -c "\[\[tool.importlinter.contracts\]\]" pyproject.toml` == 4
    - Source: `grep -v '^#' pyproject.toml | grep -c 'source_modules = \["ambito_financiero_client._core"\]'` == 1
    - Source: `grep -v '^#' pyproject.toml | grep -c 'source_modules = \["iol_client._core"\]'` == 1
    - Source: `grep -v '^#' pyproject.toml | grep -c 'source_modules = \["higyrus_client._core"\]'` == 1
    - Source: `grep -v '^#' pyproject.toml | grep -c 'source_modules = \["matriz_client._core"\]'` == 1
    - Source: `grep -l 'from __future__ import annotations' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/iol-client/src/iol_client/_core.py packages/higyrus-client/src/higyrus_client/_core.py packages/matriz-client/src/matriz_client/_core.py | wc -l` == 4
    - CLI: `uv run lint-imports` prints "Contracts: 4 kept, 0 broken" (or equivalent v2.11 success text) and exits 0
    - CLI: `uv run mypy packages/<pkg>/src/<pkg>/_core.py` para los 4 paquetes exits 0
    - CLI: `uv run ruff check packages/` exits 0
    - CLI: `uv run ruff format --check packages/` exits 0
  </acceptance_criteria>
  <done>
    pyproject.toml tiene la dev dep + 4 contracts; los 4 `_core.py` placeholder existen; `lint-imports` exit 0; mypy strict + ruff check + ruff format pasan.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-leak sentinel test parametrizado en `verification/test_sync_async_isolation.py` + CI step `lint-imports` en `.github/workflows/ci.yml`</name>
  <files>verification/test_sync_async_isolation.py, .github/workflows/ci.yml</files>
  <read_first>
    - verification/test_public_surface.py (cross-package parametrize sweep idiom — `_PACKAGES` constant — PATTERNS.md §8/§9)
    - packages/iol-client/tests/test_fixture_reaches_production.py (líneas 31-49 — sync sentinel guard idiom: `configure(token=..., token_expires_at=...)` + `httpx_mock.add_response(url=..., json=...)` + `iol_client.get_X(...)` + `[req] = httpx_mock.get_requests()` + `assert req.headers["Authorization"] == "Bearer SYNC-sentinel-iol"`)
    - packages/matriz-client/tests/test_fixture_reaches_production.py (líneas 31-51 — X-Auth-Token header variant: `assert req.headers["X-Auth-Token"] == "SYNC-sentinel-matriz"`)
    - packages/higyrus-client/tests/test_fixture_reaches_production.py (sync sentinel + async sentinel idiom)
    - packages/ambito-financiero-client/tests/test_fixture_reaches_production.py (no-auth variant — `configure(base_url=...)` con sentinel base_url; `httpx_mock` assertion sobre `req.url`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Code Examples Example 2 — el shape verbatim; § Architecture Patterns "Cross-leak sentinel test"; D-10 + D-11)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§8 — analog completo)
    - .github/workflows/ci.yml (líneas 23-39 — `lint` job: `Sync workspace` + `ruff check` + `ruff format --check` steps — analog para append; PATTERNS.md §13)
    - CLAUDE.md (mandatory `from __future__ import annotations`; no relative imports; double quotes; line-length=100)
  </read_first>
  <behavior>
    - El test `verification/test_sync_async_isolation.py` parametriza sobre 4 paquetes en una sola lista `_PACKAGES`.
    - Para cada paquete con auth (iol_client, higyrus_client, matriz_client), test_sync_token_isolation_in_wire_request: configure sync con `SYNC-sentinel-<pkg_name>`, fire 1 request, capture via httpx_mock, assert header (Authorization=Bearer prefix o X-Auth-Token sin prefix) iguala `<value_prefix><sync_sentinel>`.
    - Para ámbito (no-auth): configure con `base_url="https://sync-sentinel-ambito_financiero_client.test"`, fire 1 request, assert URL contiene `"sync-sentinel-ambito_financiero_client.test"`.
    - test_async_token_isolation_in_wire_request: idéntico shape pero usando `pkg.aio.configure(...)` con `ASYNC-sentinel-<pkg_name>`; ejecuta el endpoint async (`await pkg.aio.get_X(...)`).
    - matriz: para test_async_*, `pytest.skip` con reason exacta: `"matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore"` (literal de D-11). matriz sync test NO se skipea — corre normalmente.
    - Sentinel naming locked: `f"SYNC-sentinel-{pkg_name}"` y `f"ASYNC-sentinel-{pkg_name}"` (D-12 Phase 6 / D-10 Phase 7 — PATTERNS.md §"Sentinel naming convention").
    - El test debe correr verde EN HEAD ANTES de cualquier refactor (Plans 2-5 no han modificado nada).
    - Tras Task 3 ejecutado, `.github/workflows/ci.yml` job `lint` tiene un step adicional `- name: import-linter (boundary enforcement, Phase 7 REFAC-03)` con `run: uv run lint-imports` (PATTERNS.md §13).
  </behavior>
  <action>
    1. Crear `verification/test_sync_async_isolation.py`. Header:
       - Module docstring (per CONVENTIONS.md): "Cross-leak sentinel test: sync token MUST NOT bleed into async surface and viceversa. Phase 7 D-10/D-11 — parametrizado sobre 4 paquetes. matriz `pytest.skip` con reason explícito hasta Phase 10 REFAC-04 + TokenStore (D-11 / forward-track Pitfall 1)." Citar D-10/D-11.
       - `from __future__ import annotations`
       - Imports: `import importlib`, `import datetime as dt`, `import pytest`, `from pytest_httpx import HTTPXMock`.

    2. Definir `_PACKAGES` constant — lista de tuplas. Estructura locked:
       ```
       _PACKAGES: list[tuple[str, str | None, str]] = [
           ("ambito_financiero_client", None, ""),     # no-auth — base_url proxy
           ("iol_client", "Authorization", "Bearer "),
           ("higyrus_client", "Authorization", "Bearer "),
           ("matriz_client", "X-Auth-Token", ""),
       ]
       ```
       (planner: si el shape de configure de cada paquete requiere kwargs extra como `base_url`, `username`, `password` en sync, factoriza un helper `_configure_sync(pkg, sentinel)` interno al archivo.)

    3. Función `test_sync_token_isolation_in_wire_request(pkg_name, header_name, value_prefix, httpx_mock)`:
       - `pkg = importlib.import_module(pkg_name)`
       - `sentinel = f"SYNC-sentinel-{pkg_name}"`
       - Caso ámbito (header_name is None): `pkg.configure(base_url=f"https://{sentinel}.test")`; `httpx_mock.add_response(json=[])`; fire `pkg.get_dollar_banco_nacion(dt.date(2026, 1, 2))` (endpoint público existente — verificar en `ambito_financiero_client/__init__.py`/`client.py` el nombre exacto y el path que llama); `req = httpx_mock.get_requests()[-1]`; `assert f"{sentinel}.test" in str(req.url)`.
       - Caso con auth: `pkg.configure(token=sentinel, token_expires_at=9_999_999_999.0, base_url="https://api.test", username="u", password="p")` (los kwargs específicos por paquete — operator confirma).
         Per paquete: setup mínimo y endpoint a llamar:
         - iol_client: `httpx_mock.add_response(url=lambda u: True, json={"instrumentos": []})`; `pkg.get_instruments("argentina")`.
         - higyrus_client: `httpx_mock.add_response(url=lambda u: True, json={"status": "ok"})`; usar el endpoint sin args (e.g. `pkg.get_health()` si existe; sino el endpoint más simple con args fijos).
         - matriz_client: `httpx_mock.add_response(url=lambda u: True, json={"status": "OK", "segments": []})`; `pkg.get_segments()`.
       - Validación: `req = httpx_mock.get_requests()[-1]`; `assert req.headers.get(header_name) == f"{value_prefix}{sentinel}"`.
       - Decorar con `@pytest.mark.parametrize("pkg_name, header_name, value_prefix", _PACKAGES)` (forma matiz-no-skip; el sync no skipea matriz porque sync REST está vivo).

    4. Función `test_async_token_isolation_in_wire_request(pkg_name, header_name, value_prefix, httpx_mock)`:
       - Decorar con `@pytest.mark.parametrize(...)` idéntica.
       - Declarar `async def`.
       - PRIMERO: `if pkg_name == "matriz_client": pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")` (D-11 — string verbatim).
       - `aio = pkg.aio  # type: ignore[attr-defined]`
       - `sentinel = f"ASYNC-sentinel-{pkg_name}"`
       - Caso ámbito: `aio.configure(base_url=f"https://{sentinel}.test")`; `httpx_mock.add_response(json=[])`; `await aio.get_dollar_banco_nacion(dt.date(2026, 1, 2))`; assert URL contiene sentinel.
       - Caso iol/higyrus: paralelo a sync pero con `await aio.get_X(...)`.
       - Validación: `req.headers.get(header_name) == f"{value_prefix}{sentinel}"`.

    5. (Caveat por A3 RESEARCH.md): los endpoints concretos llamados deben existir tanto sync como async para los 3 paquetes con auth. El executor verifica con `grep -rn 'def get_' packages/<pkg>/src/<pkg>/{client,aio}.py` antes de elegir.

    6. Editar `.github/workflows/ci.yml`. En el job `lint` (líneas ~23-39), APPEND un nuevo step después de `ruff format --check`:
       ```yaml
             - name: import-linter (boundary enforcement, Phase 7 REFAC-03)
               run: uv run lint-imports
       ```
       Mantener el job name `Lint y formato (ruff)` actual (D-09 honesty flag — opcional renombrar; preferencia: dejar). Si el executor encuentra que el indent del YAML es 2 spaces (verificar visualmente), respetar; ruff format check no aplica a yml.

    7. NO incluir aún tests específicos del `_core.py` de cada paquete — eso pertenece a Plans 2-5.
  </action>
  <verify>
    <automated>uv run pytest verification/test_sync_async_isolation.py -x -v &amp;&amp; uv run mypy verification/test_sync_async_isolation.py &amp;&amp; uv run ruff check verification/test_sync_async_isolation.py &amp;&amp; uv run ruff format --check verification/test_sync_async_isolation.py &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'from __future__ import annotations' verification/test_sync_async_isolation.py` == 1
    - Source: `grep -c 'SYNC-sentinel-' verification/test_sync_async_isolation.py` &gt;= 2 (constructed string used in both sync and async tests)
    - Source: `grep -c 'ASYNC-sentinel-' verification/test_sync_async_isolation.py` &gt;= 1
    - Source: `grep -c 'matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore' verification/test_sync_async_isolation.py` == 1 (D-11 reason verbatim)
    - Source: `grep -c 'pytest.skip' verification/test_sync_async_isolation.py` == 1
    - Source: `grep -c '@pytest.mark.parametrize' verification/test_sync_async_isolation.py` == 2
    - Source: `grep -c '_PACKAGES' verification/test_sync_async_isolation.py` &gt;= 2
    - Source: `grep -c 'name: import-linter' .github/workflows/ci.yml` == 1
    - Source: `grep -c 'run: uv run lint-imports' .github/workflows/ci.yml` == 1
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -x -v` exits 0 with all parametrize cases (sync: 4 incl. matriz; async: 3 + 1 skipped)
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -5` muestra que el conteo total subió en ≥7 tests respecto al baseline 393 (4 sync + 3 async + 1 skipped) y NO hay regresiones (no `FAILED`)
    - CLI: `uv run mypy verification/test_sync_async_isolation.py` exits 0 (strict)
    - CLI: `uv run ruff check verification/test_sync_async_isolation.py` exits 0
  </acceptance_criteria>
  <done>
    El cross-leak guard corre verde en HEAD; CI step `lint-imports` agregado; suite completa verde (393 + ≥7 nuevos = ≥400 collected).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→repo (`pyproject.toml`) | Operator agrega nueva dev dep `import-linter`; supply-chain risk per package_legitimacy_protocol |
| `_core.py` → transport (`client.py`/`aio.py`) | Boundary que import-linter enforça; bypassable solo via import dinámico |
| test fixture → wire request | Cross-leak sentinel verifica que sync token NO aparezca en async wire (y vice versa); tampering boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-SC | Tampering (supply chain) | `import-linter` PyPI package | mitigate | Task 1 — blocking human checkpoint verifica PyPI metadata + GitHub repo + version (slopcheck unavailable → manual compensation per RESEARCH.md Package Legitimacy Audit) |
| T-7-01 | Tampering | `_core.py` accidental import of transport state | mitigate | `[tool.importlinter]` con 4 contracts forbidden + CI step `lint-imports` (Task 2 + Task 3). Plan 1 establece la gate; Plans 2-5 la mantienen verde |
| T-7-02 | Tampering | Sync/async token cross-contamination | mitigate | `verification/test_sync_async_isolation.py` parametrizado en Task 3 — fires sync + async con sentinels distintos, asserta wire header per surface |
| T-7-06 | Tampering (CI test integrity) | Plan 1 contracts pasan vacíos contra módulo inexistente (Pitfall 6) | mitigate | Task 2 crea 4 `_core.py` placeholder; Task 2 verify-step empíricamente valida que `uv run lint-imports` reporta "4 kept, 0 broken" (no "Could not find package") |
</threat_model>

<verification>
- `uv run lint-imports` exit 0 con 4 contracts pasando contra placeholders.
- `uv run pytest verification/test_sync_async_isolation.py -x -v` exit 0; matriz async case skipea con reason visible.
- `uv run pytest -q` full suite verde (393 baseline + ≥7 nuevos = ≥400); no regresiones.
- `uv run ruff check .` + `uv run ruff format --check .` + `uv run mypy` pasan.
- `.github/workflows/ci.yml` muestra el nuevo step `lint-imports` en el job `lint`.
</verification>

<success_criteria>
- pyproject.toml contiene `import-linter>=2.11,<3` + `[tool.importlinter]` con 4 contracts forbidden (uno por paquete).
- 4 `_core.py` placeholder existen con header docstring + `from __future__ import annotations` + `__all__: list[str] = []`.
- `verification/test_sync_async_isolation.py` exists con 4 parametrize cases sync + 4 async (3 corren, 1 skip matriz async con D-11 reason).
- `.github/workflows/ci.yml` invoca `lint-imports` en CI.
- Phase 6 baseline 393 tests siguen verde; nuevos tests también verde.
- import-linter detecta empíricamente los 4 paquetes (no "Could not find package" warning) — Pitfall 6 cleared.
</success_criteria>

<output>
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md` al finalizar, documentando:
- Versión de `import-linter` instalada (resultado de `uv run lint-imports --version` o `uv pip show import-linter`).
- Output literal de `uv run lint-imports` (debe mostrar "4 kept, 0 broken").
- Test count baseline observado vía `uv run pytest --collect-only -q | tail -1` (verificación de A5 — número real vs 393 documentado en RESEARCH.md).
- Confirmación A2/Pitfall 6: contracts vacíos contra placeholders pasan (no warn).
</output>
