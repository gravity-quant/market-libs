---
phase: 38-iol-client-auditor-a-de-higyrus-mbito-wallets
plan: 04
type: execute
wave: 3
depends_on: ["38-02", "38-03"]
files_modified:
  - .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md
autonomous: true
requirements: [NOBJ-AUD-01]

must_haves:
  truths:
    - "Every model/list/mapping-typed field and every public return in higyrus-client, ambito-financiero-client and wallets-client appears in the census with a disposition — zero rows without one (ROADMAP SC-2)"
    - "The census enumerates the full candidate population, not only violations: with 0 violations measured in all three packages, a violations-only table would be three empty tables, which SC-2/SC-4 forbid as a vacuous green"
    - "The two zero-model packages are reported as absent by enumeration, not as clean: ambito-financiero-client and wallets-client each declare zero response models, and the wallets rows additionally carry its documented stub condition (no domain function in `__all__`, Phase 29 decoder exemption)"
    - "ROADMAP SC-3 is reported as an executed command plus its verbatim output, never as an assertion — the closing grep returns exactly 10 lines, all matriz `Literal`-alias leaves permitted by D-NO-03"
    - "Rows describing module-level `_request` helpers state that they are out of the gate's candidate set (unreachable from any `__all__`), not that they are exempted — the gate's real exemption taxonomy is dunder 13, private-helper 1, serialize-out 9, ws-catch-all 1"
    - "No number in the census is an estimate: each is quoted from a cited artifact line or derived by counting the rows of a table that transcribes an executed run"
  artifacts:
    - path: ".planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md"
      provides: "The NOBJ-AUD-01 field-by-field census with dispositions, the SC-3 closing-grep evidence, the enumerated zeros, and the cross-reference to the Phase-38 accounting addendum"
      contains: "zero by enumeration"
      min_lines: 80
  key_links:
    - from: ".planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md"
      to: ".planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md"
      via: "the census cites the Phase-38 addendum for the retired-triples accounting rather than restating it, and cites lines 184-197 for the enumerated absence of ambito/wallets rather than re-deriving it"
      pattern: "35-RETIRED-TRIPLES.md"
    - from: ".planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md"
      to: "tools/check_surface_types.py"
      via: "every disposition cell cites the widened gate's executed summary line as its evidence, so the census is a transcription of a run rather than a reading of source"
      pattern: "0 violations"
  prohibitions:
    - statement: "MUST NOT report a green for a package with no measured population as if it were a cleanliness result — a zero that comes from having no models at all must be labelled as absence by enumeration, with the stub or empty-module condition named"
      status: resolved
      verification: judgment
    - statement: "MUST NOT describe a member that the gate never reached as exempted — an unreachable member and an exempted one are different findings, and conflating them overstates how much the gate actually adjudicated"
      status: resolved
      verification: judgment
    - statement: "MUST NOT write the census into any auto-generated ledger under `.planning/verification/` — those files are machine-written between markers with a run-scoped OPEN/FIXED schema, and a static annotation census has no run context and would corrupt them"
      status: resolved
      verification: test
    - statement: "MUST NOT carry an unsourced count into an SC-3 claim — where a prior artifact's number and a measured number disagree, report the measured one with the command that produced it and name the discrepancy"
      status: resolved
      verification: test
  edge_coverage:
    - requirement_id: NOBJ-AUD-01
      category: unclassified
      status: unresolved
      reason: "The deterministic edge-probe returned `unclassified` for this requirement and the fallback protocol never auto-resolves that state. Surfaced here as an explicit flagged assumption. The operative acceptance mechanism for NOBJ-AUD-01 is not this probe but the census itself: CONTEXT D-08/D-09 and RESEARCH F-8/F-9 supply the concrete methodology (full candidate population by AST introspection) and the concrete numbers (higyrus 15 classes / 142 fields / 0 / 0; ambito 0; wallets 0), and ROADMAP SC-2's zero-rows-without-disposition rule is what is actually checked."
---

<objective>
Publish the NOBJ-AUD-01 audit as `38-CENSUS.md`: a field-by-field census with a disposition on
every row for higyrus-client, ambito-financiero-client and wallets-client, plus the ROADMAP
SC-3 closing-grep evidence reported as an executed command and its verbatim output.

Purpose: all three packages measure 0 violations. That is exactly the situation in which a
report is most likely to be worthless — three empty tables reading as a clean bill of health
nobody earned. SC-2 and SC-4 forbid that specifically, which is why the census enumerates the
whole candidate population and why the two zero-model packages are reported as absent by
enumeration rather than as clean.

Output: one new planning artifact under this phase's directory, following the form of
`35-RETIRED-TRIPLES.md` (D-07).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CONTEXT.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-RESEARCH.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-PATTERNS.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-02-SUMMARY.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-03-SUMMARY.md
</context>

## Artifacts this phase produces (plan 38-04)

Newly-created artifacts introduced by this plan. Drift verification must treat these as
net-new, not as references to pre-existing files:

| Artifact | Path | Kind |
|---|---|---|
| `38-CENSUS.md` | `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md` | new planning artifact (markdown) |

No code symbols and no committed tooling are created by this plan. The introspection used in
task 1 is a throwaway one-liner, deliberately not committed — this repo already has three
`tools/` gates and does not need a fourth for a one-time count.

<reversibility>
| Task | Rating | Rationale |
|---|---|---|
| Task 1 (evidence capture) | two-way | Read-only. Runs existing commands and records their output; changes no file. |
| Task 2 (write the census) | two-way | A new additive planning artifact under this phase's own directory. |
</reversibility>

<tasks>

<task type="auto">
  <name>Task 1: Capture the measured evidence — the two closing greps, the gate summary, the higyrus field inventory, and the four suite baselines</name>

  <files>(no file written — this task produces the transcript task 2 transcribes)</files>

  <read_first>
    - `packages/higyrus-client/src/higyrus_client/models.py` — the 15 exported classes and
      their 142 fields; this is the population the census enumerates. Read the whole file.
    - `packages/higyrus-client/src/higyrus_client/client.py` and `aio.py` — the public domain
      functions and their return annotations (both surfaces).
    - `packages/ambito-financiero-client/src/ambito_financiero_client/models.py` — the
      deliberately empty module (27 lines of docstring, 0 classes; the two `class` tokens in
      it are prose). Confirm rather than assume.
    - `packages/wallets-client/src/wallets_client/models.py` and `__init__.py` lines 20-30 —
      the stub condition: `__all__` carries 4 exception classes plus `configure` and no domain
      function.
    - `tools/check_surface_types.py` lines 252-302 — `_MAPPING_BASES` and the single
      `_FIELD_EXEMPTIONS` entry, plus the blast-radius note explaining why the request-spec
      dataclass's optional untyped mapping fields are unreachable rather than exempt.
    - `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-RESEARCH.md` F-8
      and F-9 — the numbers this task must reproduce, and F-9's correction to CONTEXT D-09's
      exemption taxonomy.
  </read_first>

  <action>
Run each command below and keep its verbatim output; task 2 transcribes it. Change no file.

1. **The SC-3 closing grep** over model fields, exactly as RESEARCH F-9 states it: match
   indented `name: annotation` lines in `packages/*/src/*/models.py` whose annotation carries
   an optional wrapper in any of its three spellings, then subtract the scalar leaves
   (`str`/`int`/`float`/`bool` followed by a `| None` arm), which D-NO-03 permits. At
   `HEAD = cf79e65` this returned 12 lines. After plan 38-01 it must return **10**, all in
   `packages/matriz-client/src/matriz_client/models.py`. Record the command AND its full
   output — SC-3 requires the result be reported as an executed command with its output, not
   as an assertion.

2. **The companion grep** for the `dict[...]`-on-fields half: match indented `name:
   annotation` lines in the same files whose annotation mentions a `dict[` subscript. Then
   classify each hit by hand into three buckets, because the raw grep over-reports: (a) fields
   with a fully typed value parameter, (b) the one dunder `ClassVar` mapping, (c) the single
   declared exemption. Anything else the grep returns is a **local variable inside a
   `to_dict` method body**, not a field — the indentation pattern cannot tell the difference,
   and a census that does not separate them over-reports.

3. **The widened gate**: `uv run python tools/check_surface_types.py`. Record the entire
   summary line — package count, `__all__` names, definitions scanned, fields scanned, alias
   exports, the exemption taxonomy with its four named reasons and their counts, and the
   violation count.

4. **The higyrus field inventory.** Enumerate, per exported class, the field count and the
   count of model-typed / list-of-model-typed / mapping-typed fields. Use a throwaway
   `uv run python -c` snippet built on stdlib `ast` over
   `packages/higyrus-client/src/higyrus_client/models.py` — **not** an import of the package
   and **not** `get_type_hints`. Importing `higyrus_client` runs `load_dotenv()` and builds an
   HTTP client at import time, which is exactly the discipline the gate enforces on itself.
   Do not commit the snippet; this is a one-time count. Expected: 15 classes carrying fields,
   142 fields total, 0 optional model/list-of-model fields, 0 untyped mappings. The 15 class
   names in declaration order are `Health`, `PosicionValuada`, `Parking`, `Movimiento`,
   `Posicion`, `DisposicionesGenerales`, `Domicilio`, `PersonaRelacionada`,
   `MedioComunicacion`, `CuentaBancaria`, `Agente`, `Operador`, `Sucursal`, `Administrador`,
   `Cuenta`; `SafeModel` is exported too but declares no fields, so it contributes 0 to the
   142. Cross-check your per-class sum against the gate's own `fields scanned` figure — if the
   two disagree, the disagreement is the finding and must be reported, not reconciled by
   adjusting a number.

5. **The public-return inventory** for the three packages, both surfaces where they exist:
   higyrus exposes `get_health`, `get_listado_cuentas`, `get_movimientos`,
   `get_posicion_valuada`, `get_posiciones`, `login` and `configure`; ambito exposes
   `get_dollar_banco_nacion` and `configure`; wallets exposes no domain function at all.
   Record each return annotation as written.

6. **The four suite baselines**: iol, higyrus, ambito-financiero, wallets. Record the counts.

7. Confirm the two zero-model files are what they are claimed to be: count the lines and the
   real `class` statements in each `models.py` for ambito and wallets, so the census's "zero
   by enumeration" claim is a measurement rather than a restatement of a prior phase.

Where any number you measure disagrees with a number in CONTEXT.md, record BOTH and name the
discrepancy — task 2 must write the measured one and cite the command. RESEARCH already found
two such cases: CONTEXT D-11 says 11 matriz `Literal | None` leaves where the grep and the AST
scan both find 10, and CONTEXT D-09 describes the exemption taxonomy in terms that do not
match the gate's actual four reasons.
  </action>

  <verify>
    <automated>grep -nE '^\s+[A-Za-z_][A-Za-z0-9_]*\s*:\s*.*(\| None|Optional\[|Union\[)' packages/*/src/*/models.py | grep -vE ':\s*(str|int|float|bool)\s*\| None' | tee /tmp/38-sc3-optional.txt | wc -l</automated>
    <automated>grep -nE '^\s+[A-Za-z_][A-Za-z0-9_]*\s*:\s*.*dict\[' packages/*/src/*/models.py | tee /tmp/38-sc3-mapping.txt | wc -l</automated>
    <automated>uv run python tools/check_surface_types.py</automated>
    <automated>uv run --package iol-client pytest packages/iol-client -q 2>&1 | tail -2 &amp;&amp; uv run --package higyrus-client pytest packages/higyrus-client -q 2>&1 | tail -2 &amp;&amp; uv run --package ambito-financiero-client pytest packages/ambito-financiero-client -q 2>&1 | tail -2 &amp;&amp; uv run --package wallets-client pytest packages/wallets-client -q 2>&1 | tail -2</automated>
    <automated>grep -c '^class ' packages/ambito-financiero-client/src/ambito_financiero_client/models.py packages/wallets-client/src/wallets_client/models.py; wc -l packages/ambito-financiero-client/src/ambito_financiero_client/models.py packages/wallets-client/src/wallets_client/models.py</automated>
  </verify>

  <acceptance_criteria>
    - The SC-3 optional grep returns exactly `10` lines and every line is in
      `packages/matriz-client/src/matriz_client/models.py`. If it returns 12, plan 38-01 did
      not land and this plan must not proceed.
    - `uv run python tools/check_surface_types.py` exits 0, and its summary line reports
      `0 violations`, at least `442 fields scanned`, and the exemption taxonomy
      `24 exempted (dunder 13, private-helper 1, serialize-out 9, ws-catch-all 1)`.
    - `grep -c '^class '` returns `0` for both `ambito_financiero_client/models.py` and
      `wallets_client/models.py`, and `wc -l` reports each at roughly 27 lines — the
      "zero by enumeration" claim is measured here, not inherited.
    - The higyrus per-class inventory sums to `142` fields across `15` field-carrying classes,
      with `0` optional model / list-of-model fields and `0` untyped-mapping fields; the sum
      is consistent with the gate's `fields scanned` figure.
    - The four suites report `292 passed` (iol, post-38-02), `289 passed`,
      `208 passed, 1 deselected`, and `10 passed`.
    - The higyrus inventory was produced by a stdlib-`ast` snippet, not by importing the
      package: `git status --porcelain` shows no new committed file, and no `.env` was read.
    - `git status --porcelain` is otherwise clean — this task writes nothing.
  </acceptance_criteria>

  <done>
    Every number the census needs has been produced by a command whose output is recorded
    verbatim, including the two greps, the gate summary, the per-class higyrus inventory, the
    public-return inventory, and the four suite counts. Any disagreement with CONTEXT.md is
    written down with both values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 38-CENSUS.md — full candidate population, a disposition on every row, and the enumerated zeros</name>

  <files>.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md</files>

  <read_first>
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`
      — the D-07 form template. Read it in full and copy four structural elements: (1) a main
      table where every row carries a disposition column, (2) a provenance paragraph stating
      that no number in the file is an estimate, naming where each came from, (3) zeros
      declared by enumeration under bolded sub-headings rather than by silence, (4) a
      closing "how the next phase should use this" section stating the failure mode the
      artifact prevents. Its lines 184-197 already record the absence of ambito/wallets model
      classes — cite that paragraph rather than re-deriving it.
    - The `## Phase 38 addendum` section that plan 38-03 appended to that same file — the
      census cross-references it for the retired-triples accounting instead of duplicating it.
    - `.planning/REQUIREMENTS.md` line 33 (NOBJ-AUD-01) and the D-NO-03 scalar-leaf allowance
      it cites.
    - `.planning/ROADMAP.md` Phase 38 success criteria 2, 3 and 4 — the exact contract this
      artifact discharges.
    - `packages/iol-client/README.md` around lines 150-165 — the documented `to_dict()`
      escape hatch the serialize-out exemption rows cite; do not re-open it.
    - The transcript produced by task 1.
  </read_first>

  <action>
Create `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md`. Follow
the form of `35-RETIRED-TRIPLES.md`; internal section layout is free (CONTEXT lists this as
Claude's discretion), but all of the following content must be present.

**1. Scope statement.** NOBJ-AUD-01 covers `higyrus-client`, `ambito-financiero-client` and
`wallets-client` only. Not iol (NOBJ-IOL-01, plans 38-01/38-03 of this same phase), not
market-data-client (Phase 36, closed), not matriz-client (Phase 37, closed). Say so, so a
reader does not mistake the omission for a gap.

**2. The main table — the full candidate population, not only violations (D-08).** With 0
violations measured in all three packages, a violations-only table is three empty tables,
which ROADMAP SC-2 and SC-4 forbid as a vacuous green. Use the seven-column row shape:
package, class, field, annotation, category, disposition, evidence. Every row carries a
disposition; there is no row without one. Following RESEARCH Open Question 3's
recommendation, mix two granularities and label which is which:
   - **Per field** for every link and mapping candidate — every model-typed, list-of-model-
     typed and mapping-typed field across the 15 higyrus classes, each with its annotation as
     written and a disposition (already non-Optional, no action required).
   - **Per class** aggregate rows for the scalar leaves, giving the count per class and a
     single disposition citing D-NO-03 as the policy that permits `T | None` on a scalar. The
     per-class counts must sum, with the per-field rows, to the 142 total.

**3. The public-return half.** A second table covering every public domain function in the
three packages, both surfaces where both exist, with its return annotation and a disposition.
higyrus's seven, ambito's two, and wallets' zero. Two rules for the disposition cells:
   - Rows for the `to_dict()` serialize-out members cite the gate's existing exemption
     (9 hits, reason `serialize-out`) and the documented escape hatch in the iol README. They
     propose no fix and do not re-open that decision (D-09).
   - Rows for module-level `_request` helpers say **out of the gate's candidate set** — they
     appear in no package's `__all__`, so the gate's resolve-from-the-exported-surface-outward
     design never reaches them. They are NOT exemptions. RESEARCH F-9 corrects CONTEXT D-09
     on this point: the gate's real taxonomy is `dunder 13, private-helper 1, serialize-out 9,
     ws-catch-all 1`, and the single `private-helper` hit is a matriz method, not a
     module-level function. Reproduce the taxonomy verbatim from the run.

**4. The enumerated zeros (Pitfalls 5 and 6).** A bolded sub-heading per zero-model package,
each stating the zero as a measurement:
   - `ambito-financiero-client`: `models.py` declares zero classes and is ~27 lines of module
     docstring; the two `class` tokens in it occur in prose. The absence is by enumeration,
     not by cleanliness. Cite `35-RETIRED-TRIPLES.md` lines 184-197.
   - `wallets-client`: same, PLUS the stub qualification SC-4 requires explicitly. Its
     `__all__` (`__init__.py` lines 22-28) carries four exception classes and `configure` and
     no domain function; it holds the Phase 29 decoder exemption
     (`29-WALLETS-EXEMPTION.md`) and has no `_decode.py`; its 10 passing tests exercise
     config and exception plumbing only and prove nothing about model cleanliness, because
     there are no models. Report the green WITH that qualification, never bare.

**5. The SC-3 closing evidence.** Both greps from task 1, each as the literal command
followed by its verbatim output — SC-3 requires "el resultado se reporta con el comando
ejecutado y su salida, no como afirmación". State that the optional-field grep returns 10
lines, all matriz `Literal`-alias leaves permitted by D-NO-03, and note the discrepancy
explicitly: CONTEXT D-11 says 11, the grep and the AST scan both find 10, and the census
writes the measured number with the command that produced it. For the mapping grep, separate
the typed-value fields, the dunder `ClassVar`, the one declared exemption, and the
`to_dict`-local variables the raw grep sweeps in — an unseparated transcript over-reports.

**6. The provenance paragraph.** Copy the analog's "no number in this file is an estimate"
paragraph shape: state that every figure is either quoted from a cited line of an artifact or
derived by counting rows of a table that transcribes an executed run, and name the runs (the
gate invocation, the two greps, the four suites, the stdlib-`ast` inventory).

**7. The forward section.** A "how Phase 39 should use this" section naming the failure mode
the artifact prevents: without a written candidate population, a live census that comes back
smaller cannot be split into "disappeared because the Null Object policy stopped recording it"
and "disappeared because it was fixed", and the drop reads as quality work nobody did. Cross-
reference the `## Phase 38 addendum` section in `35-RETIRED-TRIPLES.md` for the retired-triples
arithmetic — 2 field rows added, 0 triples retired, middle term unchanged — rather than
restating it here.

Do **not** write any of this into `.planning/verification/<pkg>-findings.md` (D-07): those
files are auto-generated between `BEGIN AUTO-GENERATED` / `END AUTO-GENERATED` markers with a
run-scoped ID/Class/Surface/Status schema and an OPEN-to-FIXED lifecycle. A static annotation
census has no run context and would corrupt the format.
  </action>

  <verify>
    <automated>test -f .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md &amp;&amp; wc -l .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md</automated>
    <automated>awk -F'|' '/^\|/ &amp;&amp; !/^\|[- :]*\|/ {print NF}' .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md | sort -u</automated>
    <automated>grep -c 'zero by enumeration' .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md; grep -c 'out of the gate' .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md; grep -c '29-WALLETS-EXEMPTION.md' .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md; grep -c '35-RETIRED-TRIPLES.md' .planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md</automated>
    <automated>git status --porcelain .planning/verification/</automated>
    <automated>uv run python tools/check_surface_types.py &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run python tools/check_uniform_structure.py</automated>
    <human-check>
      <test>Open `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md` and read every table row top to bottom.</test>
      <expected>Every row in both tables has a non-empty disposition cell and a non-empty evidence cell. The three packages are all represented — higyrus with real per-field and per-class rows, ambito and wallets with explicit zero-by-enumeration sub-headings rather than empty tables. The wallets section states the stub condition (no domain function in `__all__`, Phase 29 decoder exemption, 10 tests that exercise plumbing only). The SC-3 section shows both commands and their literal output rather than a claim about them. The 10-vs-11 discrepancy against CONTEXT D-11 is named, not silently absorbed.</expected>
      <why_human>SC-2's "zero rows without disposition" and SC-4's "no vacuous green" are documentation-completeness contracts. A grep can confirm the phrases are present; only a reader can confirm the dispositions are real rather than filler.</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md` exists
      and is at least 80 lines.
    - Every markdown table row in the file has the same column count as its header (the `awk`
      check above prints a single distinct value per table shape) and no data row has an empty
      disposition cell.
    - `grep -c 'zero by enumeration'` returns at least 2 (one per zero-model package).
    - `grep -c 'out of the gate'` returns at least 1 — the module-level `_request` rows use
      the unreachable-not-exempt wording (RESEARCH F-9's correction to CONTEXT D-09).
    - `grep -c '29-WALLETS-EXEMPTION.md'` returns at least 1 and the wallets section names its
      stub condition (Pitfall 5).
    - `grep -c '35-RETIRED-TRIPLES.md'` returns at least 2 — the citation for the enumerated
      absence (lines 184-197) and the cross-reference to the Phase-38 addendum.
    - The file reproduces the gate's summary line verbatim, including
      `24 exempted (dunder 13, private-helper 1, serialize-out 9, ws-catch-all 1)` and
      `0 violations`.
    - The file contains both SC-3 grep commands as literal text AND their output; the optional
      grep's output shows 10 lines, all in `matriz_client/models.py`.
    - The file explicitly names the 10-vs-11 discrepancy against CONTEXT D-11 and states which
      number is measured.
    - The higyrus rows account for all 142 fields across 15 classes: per-field rows for the
      link/mapping candidates plus per-class scalar-leaf aggregate counts that sum to the
      remainder.
    - `git status --porcelain .planning/verification/` produces no output — no auto-generated
      ledger was touched (D-07).
    - The three `tools/` gates still exit 0.
  </acceptance_criteria>

  <done>
    `38-CENSUS.md` exists with a disposition on every row, the full candidate population for
    higyrus, enumerated zeros with the wallets stub qualification for the other two, the SC-3
    closing evidence as executed commands with verbatim output, a no-estimates provenance
    paragraph, and the forward cross-reference to the Phase-38 accounting addendum.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured evidence → published census | Numbers cross from an executed run into a durable artifact that later phases treat as authoritative. Fabricated or unsourced numbers here become false facts downstream. |
| this phase's artifacts → Phase 39's live census subtraction | `38-CENSUS.md` and the Phase-38 addendum together are how Phase 39 tells policy-driven disappearances apart from fixes. |
| introspection tooling → the developer's `.env` | Enumerating higyrus's fields by importing the package would run `load_dotenv()` and construct an HTTP client. |

**No attack surface for the shipped libraries.** This plan writes one markdown file and runs
read-only commands. ASVS L1 assessment below is scoped to the integrity of the evidence rather
than to a runtime surface, because there is no runtime surface here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-15 | Repudiation | `38-CENSUS.md` as an audit record | mitigate | SC-3's own rule: results are reported as an executed command plus verbatim output, never as an assertion. Enforced by acceptance criteria requiring both grep commands and their literal output in the file, plus the no-estimates provenance paragraph copied from the Phase-35 analog. |
| T-38-16 | Repudiation | the vacuous-green failure mode | mitigate | The census enumerates the full candidate population, and every zero is declared by enumeration with its cause named (empty module / stub package). The wallets green carries its qualification, per SC-4's explicit requirement. Checked by the human-check block and by the `zero by enumeration` grep. |
| T-38-17 | Tampering | `.planning/verification/<pkg>-findings.md` auto-generated ledgers | mitigate | D-07: writing a static census into a run-scoped auto-generated ledger would corrupt its schema and its OPEN-to-FIXED lifecycle. Enforced by the `git status --porcelain .planning/verification/` criterion. |
| T-38-18 | Information Disclosure | per-package `.env` credentials (ASVS V14) | mitigate | The higyrus field inventory is produced by a stdlib-`ast` snippet, never by importing the package. Importing any client module runs `load_dotenv()` and constructs an HTTP client; the same discipline the gate enforces on itself applies to the one-off introspection. No credential value is recorded in the census. |
| T-38-19 | Tampering | unsourced counts propagating from prior artifacts | mitigate | Where a measured number disagrees with CONTEXT.md, the census writes the measured one with the command that produced it and names the discrepancy — specifically the 10-vs-11 matriz leaf count and the exemption taxonomy. |
| T-38-SC | Tampering | `uv`/PyPI supply chain | accept | Zero package-manager installs; no committed tooling is added (the introspection is a throwaway one-liner). `uv.lock` does not move. |
</threat_model>

<verification>
Whole-plan checks:

1. `test -f .planning/phases/38-.../38-CENSUS.md` and at least 80 lines
2. Every table row carries a disposition and an evidence cell (human-check, harvested at
   end of phase per `workflow.human_verify_mode = end-of-phase`)
3. Both SC-3 greps present as literal commands with verbatim output; the optional grep shows
   10 lines, all matriz
4. `uv run python tools/check_surface_types.py` → `0 violations`, taxonomy unchanged
5. `git status --porcelain .planning/verification/` → empty
6. `git diff --name-only HEAD~..HEAD` names only
   `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md`

**Validation-map crosswalk** — this plan covers row `38-05-01` of the Per-Task Verification
Map in `38-VALIDATION.md`. That row specifies a `checkpoint:human-verify`; because
`.planning/config.json` sets `workflow.human_verify_mode` to `end-of-phase`, it is emitted
here as a `<verify><human-check>` block instead, which the verifier harvests into
`38-UAT.md` at end of phase. The manual-only row in `38-VALIDATION.md`'s "Manual-Only
Verifications" table maps to the same block.
</verification>

<success_criteria>
- higyrus, ámbito and wallets are audited and published as a census with a disposition on
  every row; zero rows lack one (ROADMAP SC-2)
- The census enumerates the full candidate population, so no package produces an empty table
  and no green is vacuous (ROADMAP SC-2 and SC-4, D-08)
- The closing grep over `packages/*/src/*/models.py` is reported as an executed command with
  its verbatim output and returns only scalar and `Literal` leaves across all six packages
  (ROADMAP SC-3)
- `wallets-client`'s stub condition is recorded explicitly rather than reported as a green
  (ROADMAP SC-4)
- Rows for the `to_dict()` serialize-out members cite the gate's existing exemption without
  proposing a fix; rows for module-level `_request` helpers are described as out of the
  gate's candidate set rather than exempted (D-09 as corrected by RESEARCH F-9)
- Nothing was written into any auto-generated ledger under `.planning/verification/` (D-07)
</success_criteria>

<output>
Create `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-04-SUMMARY.md` when done
</output>
