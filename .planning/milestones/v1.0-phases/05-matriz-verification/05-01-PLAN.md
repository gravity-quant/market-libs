---
phase: 05-matriz-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - verification/safemodel_diff.py
  - verification/cycle_report.py
  - verification/__init__.py
  - packages/matriz-client/src/matriz_client/client.py
  - packages/matriz-client/tests/test_client.py
autonomous: true
requirements: [MATZ-03, MATZ-04, DRIFT-02]
tags: [verification, matriz-client, sync-only, helper-promotion, envelope-fix, token-assert-fix, regression-tests]
must_haves:
  truths:
    - "verification/safemodel_diff.py expone `diff_safemodel_bidirectional(payload, model_cls, path) -> Iterator[tuple[str, str, str]]` con duck-typing (`hasattr(cls, 'from_api') + dataclasses.is_dataclass(cls)`) (per D-MATZ-18 + Open Question #3 RESEARCH L998-1001 — cross-package compat higyrus.SafeModel vs matriz._SafeModel)"
    - "verification/cycle_report.py expone `verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]` que parsea `<pkg>-findings.md` estructuralmente (sin pytest), valida que cada CONFIRMED/FIXED tenga regression test path existente con `def test_<name>(` en el contenido (per D-MATZ-28)"
    - "verification/__init__.py barrel exporta `diff_safemodel_bidirectional` (orden alfabético — per D-MATZ-19); `verify_cycle_closure` queda accesible vía `from verification.cycle_report import verify_cycle_closure` (NO barrel — preferencia de modularidad RESEARCH L803)"
    - "packages/matriz-client/src/matriz_client/client.py agrega helper privado `_unwrap(data: dict[str, Any], key: str, endpoint: str) -> Any` que levanta `PrimaryAPIError(status='ERROR', description=f\"missing envelope key '{key}' in response from {endpoint}\", message=None)` si la key falta (per D-MATZ-9 verbatim)"
    - "client.py refactoriza los 18 sites de `_get(...)[key]` en `_unwrap(_get(...), key, path)` — verificable enumerando líneas 194/204/209/215/223/235/282/294/301/308/316/322/327/332/337/360/384/401 (per D-MATZ-10 + grep confirmation)"
    - "`get_detailed_positions` (línea 408) y `get_account_report` (línea 415) NO se tocan — retornan dict raíz directamente al model sin envelope key (per CONTEXT.md L344 nota)"
    - "client.py reemplaza `assert _token is not None` (línea 157, rama else no-auth_basic) por `if _token is None: raise RuntimeError(\"matriz_client.client: _ensure_token() did not populate _token\")` (per D-MATZ-12); la rama `if auth_basic:` queda INTACTA"
    - "client.py expande docstrings de `new_order`/`replace_order`/`cancel_order` con warning explícito `WARNING: Submission uses HTTP GET per Primary API §6.3 spec — this is intentional, not a bug. Never refactor to POST without API confirmation.` (per D-MATZ-17; texto exacto a discreción del implementador mientras cite §6.3 + 'never refactor')"
    - "packages/matriz-client/tests/test_client.py agrega sección `# ------ Regressions ------` AL FINAL (después del último test pre-existente) con: 18 regression tests del envelope fix (uno por endpoint refactoreado, D-MATZ-11) + 1 sentinel test del `_token` RuntimeError (D-MATZ-13). Total nuevo = 19 tests."
    - "Cada regression del envelope tiene docstring: `\"\"\"Regression: PrimaryAPIError tipado en lugar de KeyError no mapeado cuando envelope key falta (finding F-NN).\"\"\"` y verifica `\"missing envelope key '<key>'\" in exc_info.value.description`"
    - "Sentinel `_token` test verifica `pytest.raises(RuntimeError, match=\"did not populate _token\")` con monkeypatch que (a) setea `_token = None` y (b) sobreescribe `_ensure_token` con noop; docstring: `\"\"\"Regression: defensive guard against _ensure_token returning without populating _token (CONCERNS.md L52-55, finding F-NN).\"\"\"`"
    - "NO se introduce nueva subclase `PrimaryShapeError` — D-MATZ-9 rechaza explícitamente (consistencia con D-HIGY-8)"
    - "NO se agrega `aio.py` ni `test_async_client.py` — matriz es sync-only por diseño"
    - "Suite mockeada pre-existente (test_client.py, test_models.py, test_exceptions.py, test_types.py, test_ws_client.py) sigue verde sin modificación"
    - "mypy --strict + ruff check + ruff format pasan tras todos los cambios"
  artifacts:
    - path: "verification/safemodel_diff.py"
      provides: "Public helper `diff_safemodel_bidirectional(payload, model_cls, path='')` (duck-typed cross-package)"
      contains: "def diff_safemodel_bidirectional"
      contains_also: "_is_safemodel_like"
    - path: "verification/cycle_report.py"
      provides: "Public helper `verify_cycle_closure(pkg) -> tuple[bool, list[str]]`"
      contains: "def verify_cycle_closure"
      contains_also: "CONFIRMED"
    - path: "verification/__init__.py"
      provides: "Barrel re-export of `diff_safemodel_bidirectional` (cycle_report NOT re-exported per modularity preference)"
      contains: "diff_safemodel_bidirectional"
    - path: "packages/matriz-client/src/matriz_client/client.py"
      provides: "Sync surface with `_unwrap` helper + 18 envelope sites refactored + `_token` raise RuntimeError + 3 docstring warnings"
      contains: "def _unwrap"
      contains_also: "missing envelope key"
    - path: "packages/matriz-client/tests/test_client.py"
      provides: "# ------ Regressions ------ section with 18 envelope tests + 1 _token sentinel = 19 new tests"
      contains: "# ------ Regressions ------"
      contains_also: "missing envelope key"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/client.py::get_segments"
      to: "packages/matriz-client/src/matriz_client/client.py::_unwrap"
      via: "_unwrap(_get(path), 'segments', path)"
      pattern: "_unwrap\\(_get"
    - from: "packages/matriz-client/src/matriz_client/client.py::_unwrap"
      to: "packages/matriz-client/src/matriz_client/exceptions.py::PrimaryAPIError"
      via: "raise PrimaryAPIError(status='ERROR', description=f\"missing envelope key '{key}'...\", message=None)"
      pattern: "raise PrimaryAPIError"
    - from: "packages/matriz-client/tests/test_client.py::Regressions"
      to: "matriz_client.PrimaryAPIError"
      via: "with pytest.raises(PrimaryAPIError) as exc_info: ...; assert 'missing envelope key' in exc_info.value.description"
      pattern: "missing envelope key"
    - from: "packages/matriz-client/src/matriz_client/client.py::_request"
      to: "RuntimeError"
      via: "if _token is None: raise RuntimeError('matriz_client.client: _ensure_token() did not populate _token')"
      pattern: "did not populate _token"
    - from: "verification/__init__.py"
      to: "verification/safemodel_diff.py::diff_safemodel_bidirectional"
      via: "from verification.safemodel_diff import diff_safemodel_bidirectional"
      pattern: "diff_safemodel_bidirectional"
---

<objective>
Construir la **DRY foundation** de Phase 5 — todo lo que el driver `main_matriz.py` (Plan 05-02) y los Verified-live + mock-only tests (Plan 05-03) necesitan importar y ejercitar. El plan combina **6 entregables atómicos en 5 tasks**:

1. **Promotion del helper SafeModel diff** desde la copia inline de `main_higyrus.py` (Phase 4) a `verification/safemodel_diff.py` con duck-typing cross-package (D-MATZ-18, condicional de Phase 4 D-HIGY-4 cumplida).
2. **Nuevo módulo `verification/cycle_report.py`** con `verify_cycle_closure(pkg)` — parseo estructural de `<pkg>-findings.md` para validar que cada CONFIRMED/FIXED linkea a un regression test path existente (D-MATZ-28). Cierra DRIFT-02 al automatizarlo.
3. **Barrel update `verification/__init__.py`** exportando `diff_safemodel_bidirectional` en orden alfabético (D-MATZ-19). `verify_cycle_closure` se importa directo desde `verification.cycle_report` (preferencia de modularidad per RESEARCH L803).
4. **MATZ-04 envelope fix dual** en `packages/matriz-client/src/matriz_client/client.py`: helper privado `_unwrap(data, key, endpoint)` + refactor de los **18 sites** `_get(...)[key]` enumerados (líneas 194/204/209/215/223/235/282/294/301/308/316/322/327/332/337/360/384/401) en `_unwrap(_get(...), key, path)`. **NO crear subclase `PrimaryShapeError`** (D-MATZ-9 reusa `PrimaryAPIError(status='ERROR', ...)` por consistencia con D-HIGY-8). `get_detailed_positions` y `get_account_report` NO se tocan (retornan dict raíz directo al model).
5. **Fix opportunistic `_token` assert** (CONCERNS.md L52-55): línea 157 `assert _token is not None` → `if _token is None: raise RuntimeError("matriz_client.client: _ensure_token() did not populate _token")`. Solo en la rama `else` (no-auth_basic); la rama `if auth_basic:` queda intacta (Pitfall 7 RESEARCH L673-678).
6. **Docstring expand de `new_order`/`replace_order`/`cancel_order`** con warning `WARNING: Submission uses HTTP GET per Primary API §6.3 spec — this is intentional, not a bug. Never refactor to POST without API confirmation.` (D-MATZ-17, contenido a discreción mientras cite §6.3 + "never refactor"). Sentinel tests viven en Plan 05-03 (MATZ-06 mock-only contract); este plan solo agrega el docstring warning.

Y los **regression tests** correspondientes en `packages/matriz-client/tests/test_client.py` sección `# ------ Regressions ------` AL FINAL del archivo: **18 envelope tests + 1 _token sentinel = 19 tests nuevos** (D-MATZ-11 + D-MATZ-13). Esto bloquea cualquier reintroducción del antipattern `_get(...)[key]` o `assert _token`.

Purpose: Sin estas piezas, Plans 05-02/03/04 no pueden ejecutarse:
- Driver Plan 05-02 importa `diff_safemodel_bidirectional` del barrel y `verify_cycle_closure` del módulo nuevo
- Tests Plan 05-03 Verified-live invariantes dependen del comportamiento `_unwrap` (la URL exacta + envelope existen siempre en mocks; el fix se ejercita estructuralmente)
- Mock-only MATZ-06 (Plan 05-03) dependerá del docstring warning para los 3 sentinels GET-quirk
- Cycle closure (Plan 05-04) requiere `verify_cycle_closure` para emitir `PROBE cycle_closure_<pkg>: PASS|FAIL`

Además: 2 fixes opportunistic + helper promotion **deben pasar antes del live run** para evitar que un `KeyError` no mapeado en un envelope inesperado o un `AssertionError` en `_token` (potencialmente strippeado por `python -O`) corte el driver mid-flight o lo confunda durante el run real.

Output: 3 archivos nuevos (`safemodel_diff.py`, `cycle_report.py`, plus barrel update), 1 archivo cliente modificado (18 envelope refactors + helper + raise + 3 docstrings), 1 archivo de tests modificado (19 nuevos tests), suite verde, mypy strict + ruff verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-matriz-verification/05-CONTEXT.md
@.planning/phases/05-matriz-verification/05-RESEARCH.md
@.planning/phases/05-matriz-verification/05-PATTERNS.md
@.planning/phases/05-matriz-verification/05-VALIDATION.md
@.planning/phases/04-higyrus-verification/04-01-PLAN.md
@.planning/phases/04-higyrus-verification/04-01-SUMMARY.md
@.planning/phases/04-higyrus-verification/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@verification/findings.py
@verification/__init__.py
@verification/schema.py
@verification/redaction.py
@main_higyrus.py
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/exceptions.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/tests/test_client.py
@packages/matriz-client/tests/conftest.py
@packages/higyrus-client/src/higyrus_client/models.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1.1: Promote `_diff_safemodel_bidirectional` from main_higyrus.py inline to `verification/safemodel_diff.py` (D-MATZ-18) with duck-typed cross-package compatibility</name>
  <files>verification/safemodel_diff.py, verification/__init__.py</files>
  <read_first>
    - main_higyrus.py (líneas 89-93 imports `from collections.abc import Iterator` + `from types import NoneType, UnionType` + `from typing import Any, Union, get_args, get_origin, get_type_hints`; líneas 205-293 contienen los 4 helpers inline a promover: `_is_optional`, `_nested_safemodel_class`, `_is_list_of_safemodel`, `_diff_safemodel_bidirectional`. NOTA: en Phase 4 el helper usa import directo de `higyrus_client.models.SafeModel`; en Phase 5 debe ser duck-typed cross-package)
    - packages/higyrus-client/src/higyrus_client/models.py (base class `SafeModel` — verificar firma: `@dataclass(frozen=True, slots=True)` + `from_api(cls, payload: Any) -> Self` classmethod)
    - packages/matriz-client/src/matriz_client/models.py (base class `_SafeModel` — verificar firma idéntica a higyrus SafeModel; los 11 dataclasses heredan de `_SafeModel` con `from_api`)
    - verification/__init__.py (barrel actual con `__all__` orden alfabético líneas 29-49: `["Denylist", "anonymize", "append_finding", "capture", "mutating_allowed", "new_findings", "redact", "require_env", "safe_print", "schema_of", "write_findings"]`; D-MATZ-19 agrega `diff_safemodel_bidirectional` en orden alfabético entre `capture` y `mutating_allowed`)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-18 verbatim signature; D-MATZ-19 barrel orden alfabético)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 2 Bidirectional SafeModel Diff con duck typing recomendado Open Question #3 L998-1001 + Assumption A1 L978; código completo verbatim L427-475)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 3 verification/safemodel_diff.py NEW — código completo L569-675 con `_is_safemodel_like` duck-typing)
    - CLAUDE.md (Python 3.12+, `from __future__ import annotations`, ruff line-length=100, double quotes, 4 espacios, mypy strict)
  </read_first>
  <behavior>
    - `from verification import diff_safemodel_bidirectional` funciona desde cualquier driver
    - `from verification.safemodel_diff import diff_safemodel_bidirectional` también funciona (acceso directo)
    - Llamado con payload `{"a": 1, "extra": 99}` y `model_cls = HigyrusModel(a: int, b: int)` con `b: int` NON-optional, yield `(path, 'model-only', 'b')` (FALSE PASS riesgo) + `(path, 'wire-only', 'extra')` (info)
    - Llamado con `_SafeModel` matriz (e.g., `Segment(marketId, marketSegmentId)`) y payload `{"marketId": "ROFX"}`, yield `(path, 'model-only', 'marketSegmentId')` — duck typing detecta dataclass+from_api sin import directo de la base
    - Llamado con payload no-dict (e.g., `None` o `["list"]`), retorna sin yield (no levanta)
    - Si `model_cls` tiene field `Optional[T]` ausente en wire, NO yield (Optional opt-in)
    - Recursión en nested SafeModel: e.g., `InstrumentDetail` con field `segment: Segment` → recurse on payload['segment']
    - Recursión en list[SafeModel]: e.g., `MarketDataSnapshot` con field `BI: list[MarketDataLevel]` → recurse on payload['BI'][0] si presente
    - mypy strict pasa sin errores
    - Tests Phase 4 (`packages/higyrus-client/tests/test_client.py`) no dependen del helper inline directamente (ejercitan el cliente); el archivo `main_higyrus.py` NO se modifica en esta task (eso es Task 1.2 — separada para mantener atomicidad del archivo)
  </behavior>
  <action>
    Crear archivo nuevo `verification/safemodel_diff.py` con el contenido completo del helper promovido (D-MATZ-18), aplicando 3 deviaciones del patrón inline original:

    1. **Rename público**: `_diff_safemodel_bidirectional` → `diff_safemodel_bidirectional` (sin underscore, exportable)
    2. **Duck typing cross-package** (Open Question #3 RESEARCH L998-1001): reemplazar `isinstance(hint, type) and issubclass(hint, SafeModel)` por helper interno `_is_safemodel_like(cls)` que retorna `True` si:
       - `isinstance(cls, type)`
       - `dataclasses.is_dataclass(cls)`
       - `callable(getattr(cls, "from_api", None))`
       Esto admite tanto `higyrus_client.models.SafeModel` como `matriz_client.models._SafeModel` sin cruzar imports entre paquetes.
    3. **Docstring de módulo** (sin equivalente en inline original): explicar que es la promotion de Phase 4 D-HIGY-4, centralizando el diff bidireccional cross-package.

    **Estructura del archivo** (~80 líneas — anclar al código completo de 05-PATTERNS.md sección 3 L569-675):

    Imports al tope: `from __future__ import annotations` + stdlib (`dataclasses`, `collections.abc.Iterator`, `types.NoneType + UnionType`, `typing.Any + Union + get_args + get_origin + get_type_hints`). `__all__ = ["diff_safemodel_bidirectional"]`.

    4 funciones internas + 1 pública:
    - `_is_optional(hint: Any) -> bool` — True si Union[X, None] o X | None (chequear `Union` y `UnionType` ambos, args contiene `NoneType`)
    - `_is_safemodel_like(cls: Any) -> bool` — duck-typing: `isinstance(cls, type) and dataclasses.is_dataclass(cls) and callable(getattr(cls, "from_api", None))`
    - `_nested_safemodel_class(hint: Any) -> type | None` — si `hint` es SafeModel-like, devolver la clase; si `hint` es `list[X]` con X SafeModel-like, devolver X; sino `None`
    - `_is_list_of_safemodel(hint: Any) -> bool` — True si `get_origin(hint) is list` y `_nested_safemodel_class(hint) is not None`
    - `diff_safemodel_bidirectional(payload: Any, model_cls: type, path: str = "") -> Iterator[tuple[str, str, str]]` — yield `(path, direction, key)` por divergencia, recursivo en nested + `list[SafeModel]` (sampleando primer elemento si lista no vacía)

    Docstring del helper público verbatim (mirror RESEARCH L433-440):
    `"""Yield ``(path, direction, key)`` tuples for each model↔wire divergence.\n\ndirection ∈ {'model-only' (FALSE PASS risk), 'wire-only' (info)}.\n\nRecursive on nested SafeModels and list[SafeModel] (samples first element)."""`

    **Editar `verification/__init__.py`** para agregar el barrel export D-MATZ-19:
    - Insertar `from verification.safemodel_diff import diff_safemodel_bidirectional` después del último import existente
    - Agregar `"diff_safemodel_bidirectional"` a `__all__` en orden alfabético (entre `"capture"` y `"mutating_allowed"`)
    - NO agregar `verify_cycle_closure` al barrel (RESEARCH L803: preferencia de modularidad — se accede vía `from verification.cycle_report import verify_cycle_closure`)

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - `from __future__ import annotations` al tope del archivo nuevo
    - mypy --strict debe pasar; `Iterator[tuple[str, str, str]]` válido
    - ruff line-length=100, double quotes, 4 espacios
    - NO importar de ningún paquete-cliente (zero cross-package coupling)
    - NO usar `eval`, `exec`, ni reflection más allá de `get_type_hints` / `get_origin` / `get_args`
    - NO modificar `verification/findings.py`, `verification/schema.py`, ni ningún otro módulo del harness
    - NO modificar `main_higyrus.py` en esta task (eso es Task 1.2)

    Commit sugerido: `feat(05-01): promote diff_safemodel_bidirectional helper to verification/ with duck-typing (D-MATZ-18, D-MATZ-19)`
  </action>
  <verify>
    <automated>uv run mypy verification</automated>
    <automated>uv run ruff check verification/safemodel_diff.py verification/__init__.py</automated>
    <automated>uv run ruff format --check verification/safemodel_diff.py verification/__init__.py</automated>
    <automated>uv run python -c "from verification import diff_safemodel_bidirectional; print('barrel import OK')"</automated>
    <automated>uv run python -c "from verification.safemodel_diff import diff_safemodel_bidirectional; print('direct import OK')"</automated>
    <automated>uv run python -c "from collections.abc import Iterator; from verification import diff_safemodel_bidirectional as f; import inspect; sig = inspect.signature(f); assert list(sig.parameters) == ['payload', 'model_cls', 'path'], f'unexpected signature: {sig}'; print('signature OK')"</automated>
    <automated>uv run python -c "from dataclasses import dataclass; from verification import diff_safemodel_bidirectional; @dataclass(frozen=True) class M: a: int; b: int;\\nM.from_api = classmethod(lambda cls, p: cls(a=0, b=0))\\nresult = list(diff_safemodel_bidirectional({'a': 1, 'extra': 99}, M, '.root'))\\nassert ('.root', 'model-only', 'b') in result, f'missing model-only: {result}'\\nassert ('.root', 'wire-only', 'extra') in result, f'missing wire-only: {result}'\\nprint('duck-typing OK')" || true</automated>
    <automated>grep -c "def diff_safemodel_bidirectional" verification/safemodel_diff.py</automated>
    <automated>grep -c "def _is_safemodel_like" verification/safemodel_diff.py</automated>
    <automated>grep -F "diff_safemodel_bidirectional" verification/__init__.py</automated>
    <automated>uv run pytest verification/ -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `verification/safemodel_diff.py` existe y contiene `def diff_safemodel_bidirectional(payload: Any, model_cls: type, path: str = "") -> Iterator[tuple[str, str, str]]:` (firma exacta)
    - Source: `verification/safemodel_diff.py` contiene `def _is_safemodel_like(cls: Any) -> bool:` con cuerpo `isinstance(cls, type) and dataclasses.is_dataclass(cls) and callable(getattr(cls, "from_api", None))` (duck typing cross-package)
    - Source: `verification/safemodel_diff.py` contiene `def _is_optional(`, `def _nested_safemodel_class(`, `def _is_list_of_safemodel(` (4 helpers internos + 1 público total)
    - Source: `verification/safemodel_diff.py` `from __future__ import annotations` línea 1
    - Source: `verification/safemodel_diff.py` `__all__ = ["diff_safemodel_bidirectional"]` (sin helpers internos exportados)
    - Source: `verification/safemodel_diff.py` NO importa de `higyrus_client` ni `matriz_client` (zero cross-package coupling)
    - Source: `verification/__init__.py` contiene `from verification.safemodel_diff import diff_safemodel_bidirectional`
    - Source: `verification/__init__.py` lista `"diff_safemodel_bidirectional"` en `__all__` entre `"capture"` y `"mutating_allowed"` (orden alfabético)
    - Source: `verification/__init__.py` NO exporta `verify_cycle_closure` (preferencia modular; se importa directo desde cycle_report.py)
    - Behavior: `uv run python -c "from verification import diff_safemodel_bidirectional"` exits 0
    - Behavior: `uv run python -c "from verification.safemodel_diff import diff_safemodel_bidirectional"` exits 0
    - Behavior: `grep -c "def diff_safemodel_bidirectional" verification/safemodel_diff.py` == 1
    - Behavior: `grep -c "def _is_safemodel_like" verification/safemodel_diff.py` == 1
    - Behavior: `grep -F "diff_safemodel_bidirectional" verification/__init__.py` retorna >= 2 líneas (import + `__all__` entry)
    - Static: `uv run mypy verification` exits 0 con "Success: no issues found"
    - Static: `uv run ruff check verification/safemodel_diff.py verification/__init__.py` exits 0
    - Static: `uv run ruff format --check verification/safemodel_diff.py verification/__init__.py` exits 0
    - Non-regression: `uv run pytest verification/ -q` (si existen tests) no muestra failures
  </acceptance_criteria>
  <done>verification/safemodel_diff.py creado con duck-typed `diff_safemodel_bidirectional`; barrel `verification/__init__.py` actualizado con export alfabético; imports verifican; mypy + ruff verdes; sin cross-package coupling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.2: Create `verification/cycle_report.py` with `verify_cycle_closure(pkg)` for structural CONFIRMED/FIXED → regression-test linkage validation (D-MATZ-28)</name>
  <files>verification/cycle_report.py</files>
  <read_first>
    - verification/findings.py (líneas 212-345: `_parse_findings` interno + `_Finding` dataclass + `_ParsedFile`; entender el shape del findings file para reusar parsing logic. **Nota**: `_Finding` actualmente NO incluye campo `regression` — el helper nuevo parsea con regex aparte sobre la sección "Detalle por hallazgo" o agrega un parse del campo del template)
    - verification/findings.py (líneas 76-84: `FINDING_CLASSES` tuple verbatim — `("SHAPE", "AUTH", "ERROR-MAP", "PARAM", "SYNC-ASYNC-DRIFT", "NO-DATA", "ANTI-BOT")`; `STATUS_LIFECYCLE` líneas 86-92 verbatim — `("OPEN", "CONFIRMED", "FIXED", "EXPECTED", "NO-FIX")`)
    - verification/findings.py (función `findings_path(pkg)` — convención `.planning/verification/<pkg>-findings.md`)
    - .planning/verification/higyrus-client-findings.md (Phase 4 output — shape concreto de un finding con status CONFIRMED/FIXED y eventual campo regression; verificar formato del campo regression si existe — sino el plan define un parser tolerante con regex sobre líneas que matcheen `<file>.py::<test_name>`)
    - .planning/verification/FINDINGS-TEMPLATE.md (shape canónico del finding — verificar si el campo `regression:` está en el template o si es ad-hoc)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-28 verbatim L608-650 — signature `verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]` + driver invocation pattern + finding ERROR-MAP OPEN si missing; clase `ERROR-MAP` decidida sobre `CYCLE-CLOSURE` por YAGNI Open Question #2)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern + Assumption A5 L982 + Pitfall RESEARCH L1100-1102 — parse estructural sin pytest)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 4 verification/cycle_report.py NEW L688-770; sample structure con regex `_REGRESSION_RE = re.compile(r"^([^:]+\.py)::([A-Za-z_][A-Za-z0-9_]*)$")`)
    - CLAUDE.md (ruff line-length=100, mypy strict, double quotes)
  </read_first>
  <behavior>
    - `verify_cycle_closure("matriz-client")` con findings file inexistente → retorna `(True, [])` (nothing to validate)
    - `verify_cycle_closure("matriz-client")` con findings file que contiene solo findings en status `OPEN` → retorna `(True, [])` (filtro: solo CONFIRMED + FIXED)
    - `verify_cycle_closure("higyrus-client")` con un finding `F-01` en status `FIXED` y campo `regression: packages/higyrus-client/tests/test_client.py::test_get_health_raises_on_list_payload` donde el archivo y test existen → retorna `(True, [])`
    - Idem con `FIXED` pero `regression` apuntando a archivo inexistente o test name sin `def test_<name>(` en el contenido → retorna `(False, ["F-01"])`
    - Idem con `CONFIRMED` sin campo regression poblado → retorna `(False, ["F-01"])`
    - El parser es estructural: NO importa pytest, NO ejecuta collect, NO importa código de los paquetes — solo lee texto de markdown y texto de archivos `.py`
    - Cross-platform: usa `pathlib.Path` para resolución relativa al repo root (parent del `verification/` dir)
    - mypy strict pasa: signature `def verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]:`
  </behavior>
  <action>
    Crear archivo nuevo `verification/cycle_report.py` (~60 líneas) con la implementación de `verify_cycle_closure(pkg)` siguiendo el patrón estructural D-MATZ-28.

    **Estructura del archivo:**

    Imports: `from __future__ import annotations` + stdlib (`re`, `pathlib.Path`) + `from verification.findings import findings_path` (reusar resolución de path canónica).

    `__all__ = ["verify_cycle_closure"]` (helper público).

    Constantes:
    - `_REPO_ROOT = Path(__file__).resolve().parent.parent` (sube de `verification/cycle_report.py` al repo root)
    - `_REGRESSION_RE = re.compile(r"^([^:]+\.py)::([A-Za-z_][A-Za-z0-9_]*)$")` (matchea `<file>.py::<test_name>` estricto)

    Función pública `verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]`:
    1. `path = findings_path(pkg)` — usa helper existente
    2. Si `not path.exists()` → return `(True, [])` (nothing to validate, no error)
    3. Lee `text = path.read_text(encoding="utf-8")`
    4. Itera findings parseados con helper interno `_iter_findings(text)` que yield tuples `(fid, status, regression_or_None)` extrayendo de la sección "## Detalle por hallazgo" — el parser interno puede reusar el regex existente de `findings.py:_parse_findings` o ser standalone tolerante:
       - Regex sobre headers `### F-(\d+) -- (.+)` para fid
       - Regex sobre line `**Status:** \`(\w+)\`` para status (case-sensitive)
       - Regex sobre line `- \*\*Regression:\*\* (\S+)` (texto exacto del campo si el template lo define; sino aceptar variantes minor)
    5. Para cada `(fid, status, regression)`:
       - Skip si `status not in ("CONFIRMED", "FIXED")`
       - Si `not regression` → append `fid` a `missing`
       - Match `regression` contra `_REGRESSION_RE`; si no matchea → append `fid`
       - Si matchea, extraer `(test_file_rel, test_name)`. Resolver `test_file_abs = (_REPO_ROOT / test_file_rel).resolve()`. Si no existe → append `fid`
       - Leer contenido del archivo; si `f"def {test_name}("` NOT IN contenido → append `fid` (busca tanto `def test_x(` como `async def test_x(` — usar `f"def {test_name}("` que matchea ambos por substring)
    6. Return `(not missing, missing)` — `ok=True` si lista vacía

    Helper interno privado `_iter_findings(text: str) -> Iterator[tuple[str, str, str | None]]`:
    - Decisión discrecional del implementador: regex multilínea sobre el texto. **Recomendado** usar regex flexibles que toleren el formato de findings template:
      - Split por `^### F-` para aislar bloques por hallazgo
      - Por bloque, extraer fid con `re.search(r"^### (F-\d+)", block, re.MULTILINE)`
      - Status con `re.search(r"\*\*Status:\*\*\s*`?(\w+)`?", block)`
      - Regression con `re.search(r"(?:Regression|regression)\b[^\n]*?([\w\-/.]+\.py::[A-Za-z_][\w]*)", block)` (capture el path si presente; si no, `None`)
    - Si el findings template NO incluye un campo `Regression:` (verificar en `.planning/verification/FINDINGS-TEMPLATE.md`), el implementador puede:
      - (a) Definir convención: el campo se agrega como item de bullet en la sección del finding cuando se marca FIXED — en Phase 4 los regression tests no se linkean explícito sino que el docstring del test cita el `finding F-NN`. Phase 5 puede aceptar el match implícito: cualquier test cuyo docstring matchea `finding F-NN` es el regression test.
      - (b) Implementación discrecional alterna: el helper acepta una convención `**Regression:** path` que el driver/operador agrega manualmente al finding cuando lo marca FIXED. Sin el campo, status FIXED es treated como missing.
      - **Decisión recomendada**: implementar el regex tolerante (b) — un campo explícito `Regression: <path>::<test>` en el bullet/Notes section del finding. Phase 5 driver agrega este campo al marcar FIXED desde `append_finding`. Si findings históricos de Phase 2-4 no lo tienen y están FIXED → el helper reporta missing y deja a Plan 05-04 cycle closure documentar la convención forward-looking. **Documentar esto en el SUMMARY** como caveat operacional.

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - `from __future__ import annotations` línea 1
    - mypy strict; usar `tuple[bool, list[str]]` (Python 3.12 nativo)
    - ruff line-length=100, double quotes, 4 espacios
    - NO importar pytest, NO importar de paquetes cliente
    - NO ejecutar comandos shell ni subprocess
    - NO modificar `verification/findings.py` ni `verification/__init__.py` en esta task (`cycle_report` accesible vía import directo, NO barrel — RESEARCH L803)
    - Path resolution debe ser segura para path-traversal — el regex `_REGRESSION_RE` solo acepta paths sin `..` (acepta `packages/foo/tests/test_x.py` pero no `../../etc/passwd`); validación adicional: rechazar paths con `..` componente o `path.is_absolute()` retornando `False`

    Commit sugerido: `feat(05-01): add verification/cycle_report.py with verify_cycle_closure helper (D-MATZ-28)`
  </action>
  <verify>
    <automated>uv run mypy verification</automated>
    <automated>uv run ruff check verification/cycle_report.py</automated>
    <automated>uv run ruff format --check verification/cycle_report.py</automated>
    <automated>uv run python -c "from verification.cycle_report import verify_cycle_closure; ok, missing = verify_cycle_closure('nonexistent-pkg'); assert ok is True and missing == [], f'expected (True, []) for missing file, got ({ok}, {missing})'; print('missing-file path OK')"</automated>
    <automated>uv run python -c "from verification.cycle_report import verify_cycle_closure; import inspect; sig = inspect.signature(verify_cycle_closure); params = list(sig.parameters); assert params == ['pkg'], f'unexpected params: {params}'; print('signature OK')"</automated>
    <automated>grep -c "def verify_cycle_closure" verification/cycle_report.py</automated>
    <automated>grep -c "STATUS_LIFECYCLE\|CONFIRMED\|FIXED" verification/cycle_report.py</automated>
    <automated>grep -F "from verification.findings import findings_path" verification/cycle_report.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `verification/cycle_report.py` existe con `from __future__ import annotations` línea 1
    - Source: contiene firma EXACTA `def verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]:`
    - Source: `__all__ = ["verify_cycle_closure"]`
    - Source: importa `findings_path` desde `verification.findings` (reusa resolución canónica)
    - Source: define constante `_REGRESSION_RE` con regex que matchea `<file>.py::<test_name>` (no permite `..` ni paths absolutos)
    - Source: NO importa pytest, NO importa `higyrus_client`, NO importa `matriz_client`
    - Source: NO modifica `verification/findings.py` ni `verification/__init__.py`
    - Source: filtros solo `CONFIRMED` y `FIXED` (los demás status no participan del check); referencia al menos uno literal "CONFIRMED" y uno "FIXED" en el código
    - Behavior: `verify_cycle_closure("nonexistent-pkg")` retorna `(True, [])` sin levantar
    - Behavior: la signature `inspect.signature` reporta `(pkg: str) -> tuple[bool, list[str]]`
    - Behavior: `grep -c "def verify_cycle_closure" verification/cycle_report.py` == 1
    - Behavior: `grep -F "from verification.findings import findings_path" verification/cycle_report.py` retorna 1 línea
    - Static: `uv run mypy verification` exits 0
    - Static: `uv run ruff check verification/cycle_report.py` exits 0
    - Static: `uv run ruff format --check verification/cycle_report.py` exits 0
  </acceptance_criteria>
  <done>verification/cycle_report.py creado con `verify_cycle_closure` estructural (sin pytest), tolerante a archivos ausentes, regex de path validado. Importable vía `from verification.cycle_report import verify_cycle_closure`. mypy + ruff verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.3: MATZ-04 envelope fix in matriz client.py — add `_unwrap` helper + refactor 18 envelope sites + add 3 docstring warnings to new_order/replace_order/cancel_order (D-MATZ-9, D-MATZ-10, D-MATZ-17)</name>
  <files>packages/matriz-client/src/matriz_client/client.py</files>
  <read_first>
    - packages/matriz-client/src/matriz_client/client.py (416 líneas: imports, `_session` setup, `login()` líneas 98-123, `_ensure_token()` líneas 91-95, `_request()` líneas 131-173, `_get(path, **params)` líneas 176-179, los 18 endpoints públicos con sus envelope key wraps. Verificar líneas exactas vs el conteo D-MATZ-10: 194/204/209/215/223/235/282/294/301/308/316/322/327/332/337/360/384/401)
    - packages/matriz-client/src/matriz_client/exceptions.py (33 líneas: jerarquía `MatrizClientError → PrimaryAPIError(status, description, message)`. Firma `__init__(status: str, description: str | None = None, message: str | None = None)`. PrimaryAPIError YA está importado en client.py — verificar)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-9 código verbatim del `_unwrap` L294-313; D-MATZ-10 lista exhaustiva de 18 sites L322-345 con endpoints + keys; D-MATZ-17 warning text)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 6 con tabla de 18 sites L867-887; sección 6a `_unwrap` verbatim L817-836; sección 6b refactor pattern verbatim L853-863; sección 6d docstring expand verbatim L927-940)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 1 `_unwrap` Envelope Helper L341-401; Pitfall 1 "13 vs 18 sites" L633-636 — el conteo real es 18)
    - .planning/codebase/CONVENTIONS.md (ruff line-length=100, double quotes, 4 espacios, mypy strict, `from __future__ import annotations` obligatorio)
    - .planning/phases/04-higyrus-verification/04-01-PLAN.md (analog directo del fix de fase con raise typed pattern; la diferencia: matriz NO tiene aio.py, fix es single-surface)
  </read_first>
  <behavior>
    - Tras el fix, llamar `matriz_client.get_segments()` cuando el mock devuelve `{"some_other_key": []}` (sin key `"segments"`) levanta `PrimaryAPIError(status='ERROR', description="missing envelope key 'segments' in response from /rest/segment/all", message=None)`, NO `KeyError`
    - Mismo comportamiento para los otros 17 endpoints refactoreados (`get_all_instruments`, `get_instruments_details`, `get_instrument_detail`, `get_instruments_by_cfi`, `get_instruments_by_segment`, `new_order`, `replace_order`, `cancel_order`, `get_order_status`, `get_order_history`, `get_active_orders`, `get_filled_orders`, `get_all_orders`, `get_order_by_exec_id`, `get_market_data`, `get_trades`, `get_positions`) — cada uno con su envelope key específica
    - `get_detailed_positions` y `get_account_report` NO modificados (no tienen envelope key — `_request(...)` directo al model)
    - Si la envelope key está presente, comportamiento idéntico al pre-fix (retorna el unwrapped value para hidratar el model)
    - `python -O packages/matriz-client/tests/test_client.py` no afecta el comportamiento (raises no se strippean)
    - mypy strict pasa: el `_unwrap` retorna `Any` (la key value); el caller mantiene tipos vía las anotaciones existentes de las funciones públicas
    - Docstrings de `new_order` / `replace_order` / `cancel_order` mencionan §6.3 + "never refactor to POST"
    - Suite mockeada pre-existente sigue pasando: ningún test pre-existente depende de `KeyError` (los happy path tests retornan envelope correctas)
  </behavior>
  <action>
    Modificar `packages/matriz-client/src/matriz_client/client.py` con **4 cambios** estrictamente delimitados:

    **Cambio 1 — Helper `_unwrap` privado** (D-MATZ-9 verbatim):

    Insertar después del helper `_get` (línea ~179 — ANTES del primer endpoint público `get_segments`):

    ```
    def _unwrap(data: dict[str, Any], key: str, endpoint: str) -> Any:
        """Return ``data[key]`` or raise ``PrimaryAPIError`` if missing.

        Args:
            data: Decoded JSON response from ``_request``/``_get``.
            key: Envelope key expected to wrap the payload (e.g., ``"order"``).
            endpoint: Path that produced the response, used for error context.

        Raises:
            PrimaryAPIError: If ``key`` is absent from ``data``.
        """
        if key not in data:
            raise PrimaryAPIError(
                status="ERROR",
                description=f"missing envelope key '{key}' in response from {endpoint}",
                message=None,
            )
        return data[key]
    ```

    `PrimaryAPIError` YA está importado al tope del módulo (verificar al leer). Mantener double quotes ruff-style.

    **Cambio 2 — Refactor de los 18 sites de `_get(...)[key]`** (D-MATZ-10 verbatim tabla):

    Para cada uno de los 18 sites, transformar el pattern `_get(path, **params)["key"]` en `_unwrap(_get(path, **params), "key", path)`. El patrón concreto (sample del site `get_segments` línea 194):

    ANTES:
    ```
    def get_segments() -> list[Segment]:
        """Return all available market segments."""
        return [Segment.from_api(s) for s in _get("/rest/segment/all")["segments"]]
    ```

    DESPUÉS (extraer `path` a una variable local para reusar en `_unwrap`):
    ```
    def get_segments() -> list[Segment]:
        """Return all available market segments."""
        path = "/rest/segment/all"
        return [Segment.from_api(s) for s in _unwrap(_get(path), "segments", path)]
    ```

    Aplicar mismo refactor para los 18 sites enumerados con sus paths y envelope keys correspondientes:

    | # | Función | Línea actual | Path | Key |
    |---|---------|--------------|------|-----|
    | 1 | get_segments | 194 | /rest/segment/all | segments |
    | 2 | get_all_instruments | 204 | /rest/instruments/all | instruments |
    | 3 | get_instruments_details | 209 | /rest/instruments/details | instruments |
    | 4 | get_instrument_detail | 215 | /rest/instruments/detail | instrument |
    | 5 | get_instruments_by_cfi | 223 | /rest/instruments/byCFICode | instruments |
    | 6 | get_instruments_by_segment | 235 | /rest/instruments/bySegment | instruments |
    | 7 | new_order | 282 | /rest/order/newSingleOrder | order |
    | 8 | replace_order | 294 | /rest/order/replaceById | order |
    | 9 | cancel_order | 301 | /rest/order/cancelById | order |
    | 10 | get_order_status | 308 | /rest/order/id | order |
    | 11 | get_order_history | 316 | /rest/order/allById | orders |
    | 12 | get_active_orders | 322 | /rest/order/actives | orders |
    | 13 | get_filled_orders | 327 | /rest/order/filleds | orders |
    | 14 | get_all_orders | 332 | /rest/order/all | orders |
    | 15 | get_order_by_exec_id | 337 | /rest/order/byExecId | order |
    | 16 | get_market_data | 360 | /rest/marketdata/get | marketData |
    | 17 | get_trades | 384 | /rest/data/getTrades | trades |
    | 18 | get_positions | 401 | /rest/risk/position/getPositions/{account_id} | positions |

    **CRITICAL**: para `get_positions` (línea 401), el path tiene parámetro de URL `{account_id}` — usar f-string `path = f"/rest/risk/position/getPositions/{account_id}"` para preservar comportamiento.

    **NO MODIFICAR** `get_detailed_positions` (línea ~408) NI `get_account_report` (línea ~415) — estas retornan el dict raíz directo a `DetailedPosition.from_api(_request(...))` / `AccountReport.from_api(_request(...))`. Buscar cualquier `[key]` indexing en estas dos funciones; si encontrás alguno, NO refactorear y documentar en SUMMARY.

    **Cambio 3 — Fix `_token` assert** (D-MATZ-12 verbatim):

    En `_request` (líneas 131-173), específicamente en la **rama `else`** (no-auth_basic) que ejecuta `_ensure_token()` y luego usa `_token`. **CRITICAL**: la rama `if auth_basic:` queda INTACTA — solo se modifica la rama `else` (Pitfall 7 RESEARCH L673-678).

    ANTES (línea ~157):
    ```
        else:
            _ensure_token()
            assert _token is not None
            resp = _session.request(
                method,
                url,
                params=params,
                headers={"X-Auth-Token": _token},
            )
    ```

    DESPUÉS:
    ```
        else:
            _ensure_token()
            if _token is None:
                raise RuntimeError(
                    "matriz_client.client: _ensure_token() did not populate _token"
                )
            resp = _session.request(
                method,
                url,
                params=params,
                headers={"X-Auth-Token": _token},
            )
    ```

    **Cambio 4 — Docstring expand para new_order / replace_order / cancel_order** (D-MATZ-17):

    Para cada una de las 3 funciones públicas, expandir el docstring existente agregando un párrafo de warning explícito. El texto exacto del warning es discrecional mientras cumpla con D-MATZ-17:
    - Debe citar §6.3 spec
    - Debe contener la advertencia explícita "Never refactor to POST without API confirmation"
    - Debe estar en formato `WARNING: ...` para visibilidad

    **Recomendación de texto verbatim** (mirror RESEARCH L935-940):
    ```
    """Submit a new single order (§6.3).

    WARNING: Submission uses HTTP GET per Primary API §6.3 spec — this is
    intentional, not a bug. Never refactor to POST without explicit API
    confirmation; the upstream service silently mismatches POSTs.
    """
    ```

    Adaptar la primera línea para `replace_order` y `cancel_order` con la referencia §6.3 (o §6.4/§6.5 si la spec usa subsecciones específicas — discreción del implementador).

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - Mantener `from __future__ import annotations` línea 1
    - mypy --strict debe pasar sin errores
    - ruff line-length=100, double quotes, 4 espacios
    - NO introducir imports nuevos — `PrimaryAPIError` ya está importado en el módulo
    - NO crear subclase `PrimaryShapeError` (D-MATZ-9 explícitamente rechaza, consistencia con D-HIGY-8)
    - NO modificar firmas públicas (`get_segments`, `new_order`, etc.)
    - NO modificar `_session` setup, `login`, `_ensure_token`, `_get`
    - NO agregar `aio.py` (matriz es sync-only por diseño)
    - El warning de §6.3 puede usar `WARNING:` o `**WARNING:**` (sphinx/rst-style); ambos son aceptables
    - Para `get_positions` el f-string `path = f"/rest/risk/position/getPositions/{account_id}"` debe usar la variable real del scope

    Verificación inline post-edit (sanity grep):
    - `grep -c "_unwrap(" packages/matriz-client/src/matriz_client/client.py` debe ser **19** (18 sites + 1 helper definition)
    - `grep -c "assert _token is not None" packages/matriz-client/src/matriz_client/client.py` debe ser **0**
    - `grep -c "raise RuntimeError" packages/matriz-client/src/matriz_client/client.py` debe ser **1** (solo el `_token` fix)

    Commit sugerido: `fix(05-01): MATZ-04 envelope fix (18 sites) + _token raise (D-MATZ-12) + new/replace/cancel §6.3 docstring (D-MATZ-9, D-MATZ-10, D-MATZ-12, D-MATZ-17)`
  </action>
  <verify>
    <automated>uv run mypy packages/matriz-client</automated>
    <automated>uv run ruff check packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>uv run ruff format --check packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>uv run pytest packages/matriz-client/tests/test_client.py -q</automated>
    <automated>grep -c "def _unwrap" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "_unwrap(_get(" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "_unwrap(_request(" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "_unwrap(" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "assert _token is not None" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "raise RuntimeError" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "did not populate _token" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "§6.3\|6\\.3" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "Never refactor to POST" packages/matriz-client/src/matriz_client/client.py</automated>
    <automated>grep -c "missing envelope key" packages/matriz-client/src/matriz_client/client.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/matriz-client/src/matriz_client/client.py` contiene `def _unwrap(data: dict[str, Any], key: str, endpoint: str) -> Any:` (firma exacta D-MATZ-9)
    - Source: contiene `raise PrimaryAPIError(` dentro del cuerpo de `_unwrap` con `status="ERROR"`, `description=f"missing envelope key '{key}' in response from {endpoint}"`, `message=None`
    - Source: NO contiene `_get(...)[` ni `_get(path, ...)["` ni `_get(...,...)["` — todos los 18 sites refactoreados (verificable: `grep -E '_get\([^)]+\)\[' client.py` retorna 0 líneas)
    - Source: contiene `_unwrap(` exactamente **19 veces** (1 helper definition + 18 invocations)
    - Source: contiene `_unwrap(_get(` al menos 17 veces (los 17 sites que usan `_get`; site #18 `get_positions` puede usar `_request` directo con `auth_basic`, en cuyo caso es `_unwrap(_request(...), "positions", path)`)
    - Source: `assert _token is not None` aparece **0 veces** (D-MATZ-12 reemplaza el único site en línea 157)
    - Source: contiene `raise RuntimeError(` exactamente **1 vez** con string `"matriz_client.client: _ensure_token() did not populate _token"`
    - Source: contiene `did not populate _token` (el mensaje del raise)
    - Source: NO crea subclase `PrimaryShapeError` (no `class PrimaryShapeError` en el archivo)
    - Source: docstrings de `new_order`, `replace_order`, `cancel_order` mencionan `§6.3` (o el caracter `§` con el número de sección) y contienen `Never refactor to POST` literal
    - Source: la rama `if auth_basic:` de `_request` NO tiene `if _token is None` agregado (el fix solo va en la rama `else`)
    - Source: `get_detailed_positions` y `get_account_report` NO refactoreados (no usan `_unwrap`); siguen retornando `<Model>.from_api(_request(...))` directamente
    - Source: NO introduce imports nuevos
    - Source: preserva `from __future__ import annotations` línea 1
    - Behavior: `grep -c "def _unwrap" client.py` == 1
    - Behavior: `grep -c "_unwrap(" client.py` == 19
    - Behavior: `grep -c "assert _token is not None" client.py` == 0
    - Behavior: `grep -c "raise RuntimeError" client.py` == 1
    - Behavior: `grep -c "did not populate _token" client.py` >= 1
    - Behavior: `grep -c "Never refactor to POST" client.py` >= 3 (uno por new/replace/cancel)
    - Behavior: `grep -c "missing envelope key" client.py` >= 1 (en el `_unwrap` raise message)
    - Static: `uv run mypy packages/matriz-client` exits 0 con "Success: no issues found"
    - Static: `uv run ruff check packages/matriz-client/src/matriz_client/client.py` exits 0
    - Static: `uv run ruff format --check packages/matriz-client/src/matriz_client/client.py` exits 0
    - Non-regression: `uv run pytest packages/matriz-client/tests/test_client.py -q` exits 0 con todos los tests pre-existentes verdes
  </acceptance_criteria>
  <done>client.py refactoreado: `_unwrap` helper agregado + 18 envelope sites usan `_unwrap(_get(path), key, path)` + `_token` raise RuntimeError reemplaza assert + new/replace/cancel docstrings con §6.3 warning. PrimaryShapeError NO creada. mypy + ruff verdes. Tests pre-existentes sin regresión.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1.4: Add `# ------ Regressions ------` section to test_client.py with 18 envelope tests + 1 `_token` sentinel = 19 new tests (D-MATZ-11, D-MATZ-13)</name>
  <files>packages/matriz-client/tests/test_client.py</files>
  <read_first>
    - packages/matriz-client/tests/test_client.py (estado actual: imports al tope incluyendo `from pytest_httpx import HTTPXMock`, `import matriz_client`, posibles imports adicionales de excepciones; tests pre-existentes incluyendo `test_new_order_builds_params` y `test_new_order_omits_optional_fields` líneas ~153-184 — verificar al leer; las nuevas tests van AL FINAL del archivo después del último test pre-existente)
    - packages/matriz-client/tests/conftest.py (autouse `_configure_sync` precarga `_token = "test-token"` con `_token_ts` reciente y `base_url = "https://api.test"` — los nuevos tests heredan este setup; para el sentinel `_token` test, el monkeypatch sobreescribe estos valores temporalmente)
    - packages/matriz-client/src/matriz_client/client.py (DESPUÉS de Task 1.3: contiene `_unwrap` + 18 sites refactoreados + raise RuntimeError sobre `_token`; firma de cada endpoint public para construir las llamadas de los tests)
    - packages/matriz-client/src/matriz_client/__init__.py (barrel public: verificar que `PrimaryAPIError` está re-exportado para `from matriz_client import PrimaryAPIError`; sino importar via `from matriz_client.exceptions import PrimaryAPIError`)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-11 verbatim L348-355 — docstring exacto + pattern del test; D-MATZ-13 verbatim L380-386 — sentinel `_token` test pattern)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 7 con test patterns concretos L948-1006; sentinel `_token` pattern L996-1006)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 1 regression test verbatim L388-401)
    - .planning/phases/04-higyrus-verification/04-01-PLAN.md Task 1.4 (analog directo del patrón "append `# ------ Regressions ------` divider + N tests")
    - .planning/codebase/TESTING.md (convención pytest-httpx URL completa, autouse fixtures por paquete, `Regression: ... (finding F-NN)` docstring)
  </read_first>
  <behavior>
    - `test_client.py` tiene una sección nueva `# ------ Regressions ------` AL FINAL del archivo (después del último test pre-existente) con:
      - **18 envelope tests** (uno por endpoint refactoreado), cada uno:
        - Mockea `httpx_mock.add_response(url="<URL del endpoint>", method="GET", json={"some_other_key": <empty>})` (sin la envelope key esperada)
        - Llama la función pública del cliente con args mínimos
        - Asserta `with pytest.raises(PrimaryAPIError) as exc_info`
        - Asserta `f"missing envelope key '{KEY}'" in (exc_info.value.description or "")` donde `KEY` es la key faltante del endpoint
      - **1 sentinel test** `test_request_raises_runtime_error_if_ensure_token_leaves_none` que monkeypatchea `_token=None` + `_ensure_token=noop`, llama `_request("GET", ...)` y verifica `pytest.raises(RuntimeError, match="did not populate _token")`
    - Cada test usa el autouse `_configure_sync` precargado (no necesita resetear estado adicional excepto el sentinel `_token`)
    - Cada docstring del envelope test es verbatim per D-MATZ-11: `"""Regression: PrimaryAPIError tipado en lugar de KeyError no mapeado cuando envelope key falta (finding F-NN)."""`
    - El docstring del sentinel `_token` es verbatim per D-MATZ-13: `"""Regression: defensive guard against _ensure_token returning without populating _token (CONCERNS.md L52-55, finding F-NN)."""`
    - Tests pre-existentes en `test_client.py` siguen pasando sin modificación
    - mypy strict pasa
    - `uv run pytest packages/matriz-client/tests/test_client.py -q` reporta los 19 tests nuevos en PASSED
  </behavior>
  <action>
    Modificar `packages/matriz-client/tests/test_client.py` agregando AL FINAL del archivo (después del último test pre-existente) la sección verbatim `# ------ Regressions ------` seguida de 19 funciones de test.

    **Setup de imports (si faltan en el archivo):**
    - Verificar al leer que `from pytest_httpx import HTTPXMock` está importado (sino agregar al bloque de imports al tope)
    - Verificar que `PrimaryAPIError` está accesible: ya sea `from matriz_client import PrimaryAPIError` (si __init__.py lo re-exporta) o `from matriz_client.exceptions import PrimaryAPIError`. Agregar si falta
    - Verificar que `pytest` está importado (para `pytest.raises`)
    - Para el sentinel test, también importar `matriz_client.client as _client` (necesario para `monkeypatch.setattr(_client, "_token", None)`)

    **Divider verbatim al final del archivo:**
    ```
    # ------ Regressions ------
    ```

    **18 envelope tests** — uno por endpoint de la tabla D-MATZ-10 (en orden de los 18 sites). Cada test sigue este patrón:

    ```python
    def test_<func_name>_raises_primary_api_error_on_missing_envelope_key(
        httpx_mock: HTTPXMock,
    ) -> None:
        """Regression: PrimaryAPIError tipado en lugar de KeyError no mapeado cuando envelope key falta (finding F-NN)."""
        httpx_mock.add_response(
            url="<URL completa>",
            method="GET",
            json={"some_other_key": []},
        )
        with pytest.raises(PrimaryAPIError) as exc_info:
            matriz_client.<func_name>(<args>)
        assert "missing envelope key '<KEY>'" in (exc_info.value.description or "")
    ```

    **Mapeo URL + args mínimos para cada endpoint** (usar `base_url="https://api.test"` del fixture):

    1. `test_get_segments_raises_primary_api_error_on_missing_envelope_key`
       - URL: `https://api.test/rest/segment/all`
       - Call: `matriz_client.get_segments()`
       - Expected key: `segments`

    2. `test_get_all_instruments_raises_primary_api_error_on_missing_envelope_key`
       - URL: `https://api.test/rest/instruments/all`
       - Call: `matriz_client.get_all_instruments()`
       - Expected key: `instruments`

    3. `test_get_instruments_details_raises_primary_api_error_on_missing_envelope_key`
       - URL: `https://api.test/rest/instruments/details`
       - Call: `matriz_client.get_instruments_details()`
       - Expected key: `instruments`

    4. `test_get_instrument_detail_raises_primary_api_error_on_missing_envelope_key`
       - URL: dejar `url=None` (es decir, omitir `url=` en `add_response`) o usar `match_url` con substring para evitar acoplamiento a query string que depende de args; **recomendado** usar URL con query string parcial: `httpx_mock.add_response(json={...}, match_url=re.compile("instruments/detail"), method="GET")` — sino, usar URL completa con query string `https://api.test/rest/instruments/detail?symbol=X&marketId=ROFX`
       - Call: `matriz_client.get_instrument_detail("X")`
       - Expected key: `instrument`

    5. `test_get_instruments_by_cfi_raises_primary_api_error_on_missing_envelope_key`
       - URL: con query string `?CFICode=ESXXXX` (verbatim cómo el cliente lo emite)
       - Call: `matriz_client.get_instruments_by_cfi("ESXXXX")` (usar cast si CFICode es Literal estricto: `from typing import cast; from matriz_client.types import CFICode; matriz_client.get_instruments_by_cfi(cast(CFICode, "ESXXXX"))`)
       - Expected key: `instruments`

    6. `test_get_instruments_by_segment_raises_primary_api_error_on_missing_envelope_key`
       - URL: con query string (segmentId param verbatim)
       - Call: `matriz_client.get_instruments_by_segment(<args>)` — verificar firma al leer client.py para saber el nombre del param
       - Expected key: `instruments`

    7. `test_new_order_raises_primary_api_error_on_missing_envelope_key`
       - URL: con query string verbatim (todos los params que el cliente emite por default — verificar comportamiento de tests pre-existentes `test_new_order_builds_params`)
       - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0)`
       - Expected key: `order`

    8-15. (replace_order, cancel_order, get_order_status, get_order_history, get_active_orders, get_filled_orders, get_all_orders, get_order_by_exec_id) — patrón análogo

    16. `test_get_market_data_raises_primary_api_error_on_missing_envelope_key`
        - URL con query string (entries, depth si default poblado, symbol, marketId)
        - Call: `matriz_client.get_market_data("X")`
        - Expected key: `marketData`

    17. `test_get_trades_raises_primary_api_error_on_missing_envelope_key`
        - URL con query string (dateFrom, dateTo)
        - Call: `matriz_client.get_trades("X", dt.date(2026, 1, 1), dt.date(2026, 1, 7))` (verificar firma)
        - Expected key: `trades`

    18. `test_get_positions_raises_primary_api_error_on_missing_envelope_key`
        - URL: `https://api.test/rest/risk/position/getPositions/ACC` (account_id en path)
        - Call: `matriz_client.get_positions("ACC")`
        - Expected key: `positions`
        - **NOTA**: `get_positions` usa HTTP Basic Auth via `auth_basic=` kwarg en el cliente; pytest-httpx no chequea auth por default, el test funciona

    **Decisión discrecional**: para tests donde la URL completa con query string es brittle (i.e., cambios futuros en orden de params podrían romper el match), permitir el implementador omitir `url=` y dejar que pytest-httpx haga match por method+path; verificar que el test sigue siendo determinista (un solo mock add_response por test). Patrón aceptable:
    ```python
    httpx_mock.add_response(method="GET", json={"some_other_key": []})  # sin url=
    ```
    Esto requiere que el wrapper haga UNA sola request HTTP (verificar al leer client.py — `_get` hace UNA `_request`, OK).

    **Sentinel `_token` test (D-MATZ-13)**:

    ```python
    def test_request_raises_runtime_error_if_ensure_token_leaves_none(
        monkeypatch: pytest.MonkeyPatch,
    ) -> None:
        """Regression: defensive guard against _ensure_token returning without populating _token (CONCERNS.md L52-55, finding F-NN)."""
        import matriz_client.client as _client
        monkeypatch.setattr(_client, "_token", None, raising=False)
        monkeypatch.setattr(_client, "_ensure_token", lambda: None)
        with pytest.raises(RuntimeError, match="did not populate _token"):
            _client._request("GET", "/rest/anything")
    ```

    Si el `_request` requiere mock HTTP (httpx) para no fallar antes del raise, agregar `httpx_mock` parameter y `httpx_mock.add_response(json={})` — pero verificar al leer client.py el orden: si `if _token is None` es ANTES del request HTTP, no se necesita httpx mock. Si el orden es diferente, ajustar.

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - Mantener `from __future__ import annotations` línea 1 sin tocar
    - mypy --strict pasa; ruff line-length=100, double quotes, 4 espacios
    - NO modificar tests pre-existentes (van AL FINAL del archivo)
    - NO agregar sección `# ------ Verified live (Phase 5) ------` en este plan — esa va en Plan 05-03
    - NO commitear findings markdown — eso es output del driver del Plan 05-02
    - NO usar `match_content` salvo donde URL match no funcione
    - El autouse `_configure_sync` precarga `_token = "test-token"`; los 18 envelope tests heredan ese setup sin modificación. Solo el sentinel test usa monkeypatch
    - Los docstrings deben ser **verbatim** per D-MATZ-11 y D-MATZ-13 (placeholder `F-NN` se reemplaza cuando se asigna fid en Plan 05-03; mientras tanto, el literal `F-NN` queda)
    - NO importar `aio` (matriz no tiene async); NO crear `test_async_client.py`

    Commit sugerido: `test(05-01): add MATZ-04 + _token Regressions section (18 envelope tests + 1 sentinel)`
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/matriz-client -q</automated>
    <automated>uv run mypy packages/matriz-client</automated>
    <automated>uv run ruff check packages/matriz-client/tests/test_client.py</automated>
    <automated>uv run ruff format --check packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "^def test_.*_raises_primary_api_error_on_missing_envelope_key" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "missing envelope key" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "Regression: PrimaryAPIError tipado en lugar de KeyError" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "test_request_raises_runtime_error_if_ensure_token_leaves_none" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "did not populate _token" packages/matriz-client/tests/test_client.py</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/matriz-client/tests/test_client.py` contiene `# ------ Regressions ------` (divider verbatim)
    - Source: contiene **18 funciones** que matchean el patrón `^def test_.*_raises_primary_api_error_on_missing_envelope_key\(`
    - Source: contiene `def test_request_raises_runtime_error_if_ensure_token_leaves_none(` (sentinel _token)
    - Source: el docstring `Regression: PrimaryAPIError tipado en lugar de KeyError no mapeado cuando envelope key falta (finding F-NN).` aparece >= 18 veces (uno por envelope test)
    - Source: el docstring `Regression: defensive guard against _ensure_token returning without populating _token (CONCERNS.md L52-55, finding F-NN).` aparece >= 1 vez
    - Source: contiene `"missing envelope key '"` literal >= 18 veces (uno por test, con la key específica del endpoint)
    - Source: contiene `pytest.raises(PrimaryAPIError)` >= 18 veces
    - Source: contiene `pytest.raises(RuntimeError, match="did not populate _token")` >= 1 vez (o equivalente con escape)
    - Source: tests pre-existentes NO modificados — los nuevos 19 van AL FINAL del archivo después del último test pre-existente
    - Source: preserva `from __future__ import annotations` línea 1
    - Source: NO agrega sección `# ------ Verified live (Phase 5) ------` (esa va en Plan 05-03)
    - Source: imports al tope incluyen `from pytest_httpx import HTTPXMock`, `pytest`, y `PrimaryAPIError` accesible (via barrel `matriz_client` o `matriz_client.exceptions`)
    - Behavior: `grep -c "^def test_.*_raises_primary_api_error_on_missing_envelope_key" test_client.py` == 18
    - Behavior: `grep -F "# ------ Regressions ------" test_client.py` retorna 1 línea
    - Behavior: `grep -c "did not populate _token" test_client.py` >= 1
    - Behavior: `uv run pytest packages/matriz-client/tests/test_client.py -v` reporta los 18 envelope tests + 1 sentinel + tests pre-existentes, TODOS en PASSED, sin ningún failed/error
    - Behavior: `uv run pytest packages/matriz-client -q` exits 0
    - Static: `uv run mypy packages/matriz-client` exits 0
    - Static: `uv run ruff check packages/matriz-client/tests/test_client.py` exits 0
    - Static: `uv run ruff format --check packages/matriz-client/tests/test_client.py` exits 0
    - Non-regression: `uv run pytest -q` (whole repo) exits 0 — confirma que los cambios no rompieron ningún paquete del workspace
  </acceptance_criteria>
  <done>Sección `# ------ Regressions ------` agregada al final de test_client.py con 18 envelope tests (1 por site refactoreado en Task 1.3) + 1 sentinel `_token` test. Suite verde, mypy + ruff limpios. Tests pre-existentes intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `verification/safemodel_diff.py::diff_safemodel_bidirectional` ← payload arbitrario del wire (cliente → helper) | El helper recibe `Any` y debe defenderse contra non-dict/None payloads; el patrón actual `if not isinstance(payload, dict): return` previene la propagación a `get_type_hints` con argumento mal tipado |
| `verification/cycle_report.py::verify_cycle_closure` ← `<pkg>-findings.md` (markdown del operator/driver → parser) | El parser lee texto markdown que el operator puede haber editado manualmente. El `_REGRESSION_RE` debe rechazar paths con `..` o absolutos (path-traversal) |
| `packages/matriz-client/.../client.py::_unwrap` ← `_get`/`_request` JSON deserialized payload (Primary API → cliente) | Payload entra como `dict[str, Any]`; el helper levanta `PrimaryAPIError(status='ERROR', ...)` tipado si la key falta, en lugar de `KeyError` no contractual |
| `packages/matriz-client/.../client.py::_request` (rama else no-auth_basic) → `_session.request` con `X-Auth-Token: _token` | El `if _token is None: raise RuntimeError` es defensa contra `_ensure_token` que retorne sin poblar `_token` (e.g., race condition, future refactor que omita la asignación). `raise RuntimeError` no se strippea con `python -O` |
| Tests Regression (`pytest-httpx` mocks) → wrappers públicos del cliente | El autouse `_configure_sync` precarga `_token="test-token"` (valor sentinela). El sentinel `_token` test monkeypatchea esto temporal. Ningún mock contiene credenciales reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-01 | Tampering | `_get(...)[key]` raw indexing → `KeyError` no mapeado expone internals al caller | mitigate | MATZ-04 fix verbatim D-MATZ-9: `_unwrap` helper levanta `PrimaryAPIError(status='ERROR', description=f"missing envelope key '{key}' in response from {endpoint}")` con `description` controlled string. Callers que catchean `PrimaryAPIError` siguen funcionando. 18 regression tests lockean el behavior (Task 1.4). |
| T-5-02 | Tampering | `assert _token is not None` stripped por `python -O` (T-4-08 inherited Phase 4 pattern) | mitigate | D-MATZ-12 verbatim: `if _token is None: raise RuntimeError(...)` en lugar del assert. `raise` no se strippea. 1 sentinel test (Task 1.4) lockea el behavior. Acceptance criteria `grep -c "assert _token" == 0` garantiza completitud. |
| T-5-03 | Tampering | GET-as-write quirk "corregido" accidentalmente a POST por contributor futuro | mitigate (partial) | D-MATZ-17 docstring warning con `WARNING: Submission uses HTTP GET per Primary API §6.3 spec` + "Never refactor to POST". Cubre las 3 funciones (new/replace/cancel). **Los 3 sentinel tests (Plan 05-03)** completan la mitigación: cualquier refactor a POST rompe los tests `assert request.method == "GET"`. Este plan solo agrega el docstring; Plan 05-03 agrega los sentinels. |
| T-5-04 | Information Disclosure | `_unwrap` error message incluye `endpoint` path — bajo riesgo de leak de internals | accept | El `endpoint` es un path estático del API (e.g., `/rest/segment/all`), no contiene secrets ni PII. La info ya es conocida por cualquier caller del cliente. Mirror Phase 4 T-4-01 (status_code y type names no son PII). |
| T-5-05 | Information Disclosure | `verify_cycle_closure` lee `<pkg>-findings.md` que puede contener detalle de bugs internos — bajo riesgo si findings exponen lógica sensitiva | accept | Findings files son artefactos de proyecto committeados a git; ya pasan revisión de PR. El helper solo extrae fid/status/regression — NO emite contenido a stdout, NO leak via excepción. |
| T-5-06 | Tampering | Path-traversal en `verify_cycle_closure._REGRESSION_RE` permite saltar fuera del repo (`../../etc/passwd::test_x`) | mitigate | `_REGRESSION_RE = re.compile(r"^([^:]+\.py)::([A-Za-z_][A-Za-z0-9_]*)$")` ya rechaza paths con `..` por el caracter clase `[^:]+\.py` que admite cualquier path; la validación adicional `if ".." in test_file_rel.parts or test_file_abs.is_absolute(): skip` previene escape. Acceptance: regex debe rechazar paths con `..`. |
| T-5-07 | Information Disclosure | Sentinel `_token` test imprime el token "test-token" en caso de failure | accept | El valor "test-token" es sentinela conocido del autouse fixture, no es credencial real. pytest output con --tb=short en CI no es shared. Mirror pre-existing pattern. |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 NO instala paquetes externos. Toda la lógica nueva usa stdlib + módulos pre-existentes del repo. RESEARCH §Package Legitimacy Audit confirma "0 packages flagged". |

**Inherited threats from Phases 1-4 (still active):**
- T-3-04 (Information Disclosure): Tests usan valores sintéticos inline; ningún mock contiene credenciales reales — inherited mitigation.
- T-4-08 (Tampering): `assert` stripped por `python -O` — fix dual cubre HIGY-04 (Phase 4) y MATZ `_token` (este plan). Pattern lockeado.
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Helpers nuevos importables
uv run python -c "from verification import diff_safemodel_bidirectional; print('barrel OK')"
uv run python -c "from verification.cycle_report import verify_cycle_closure; print('direct OK')"

# 2. MATZ-04 envelope fix completo
grep -c "def _unwrap" packages/matriz-client/src/matriz_client/client.py            # == 1
grep -c "_unwrap(" packages/matriz-client/src/matriz_client/client.py              # == 19
grep -c "_get(.*)\[\"" packages/matriz-client/src/matriz_client/client.py          # == 0 (todos refactoreados)

# 3. _token fix
grep -c "assert _token is not None" packages/matriz-client/src/matriz_client/client.py   # == 0
grep -c "did not populate _token" packages/matriz-client/src/matriz_client/client.py    # >= 1
grep -c "raise RuntimeError" packages/matriz-client/src/matriz_client/client.py         # == 1

# 4. Docstring warnings
grep -c "Never refactor to POST" packages/matriz-client/src/matriz_client/client.py     # >= 3

# 5. Regression tests presentes
grep -F "# ------ Regressions ------" packages/matriz-client/tests/test_client.py
grep -c "^def test_.*_raises_primary_api_error_on_missing_envelope_key" packages/matriz-client/tests/test_client.py   # == 18
grep -c "test_request_raises_runtime_error_if_ensure_token_leaves_none" packages/matriz-client/tests/test_client.py  # == 1

# 6. Static checks
uv run mypy packages/matriz-client verification
uv run ruff check .
uv run ruff format --check .

# 7. Suite completa green (matriz tests + verification + cross-package non-regression)
uv run pytest packages/matriz-client -q
uv run pytest -q   # whole repo
```

Per Wave 1 merge:
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `verification/safemodel_diff.py` con `diff_safemodel_bidirectional` duck-typed creado (D-MATZ-18)
- `verification/cycle_report.py` con `verify_cycle_closure` creado (D-MATZ-28)
- `verification/__init__.py` barrel exporta `diff_safemodel_bidirectional` alfabéticamente (D-MATZ-19); `verify_cycle_closure` accesible vía `from verification.cycle_report import` (modularidad preference RESEARCH L803)
- `packages/matriz-client/src/matriz_client/client.py` con `_unwrap` helper + 18 envelope sites refactoreados (D-MATZ-9, D-MATZ-10)
- `packages/matriz-client/src/matriz_client/client.py` con `_token` assert reemplazado por `if _token is None: raise RuntimeError(...)` en la rama else no-auth_basic (D-MATZ-12)
- `packages/matriz-client/src/matriz_client/client.py` con docstrings warning §6.3 en new_order/replace_order/cancel_order (D-MATZ-17)
- NINGUNA subclase nueva `PrimaryShapeError` (D-MATZ-9 explícito)
- NINGÚN `assert _token` ni `_get(...)[key]` en client.py tras el fix (T-5-01 + T-5-02 mitigados)
- NO se agrega `aio.py` ni `test_async_client.py` (matriz es sync-only)
- `packages/matriz-client/tests/test_client.py` con sección `# ------ Regressions ------` + 18 envelope tests + 1 sentinel _token = 19 tests nuevos (D-MATZ-11, D-MATZ-13)
- Cada regression test usa docstring verbatim D-MATZ-11 / D-MATZ-13
- Suite `uv run pytest packages/matriz-client -q` verde con todos los tests pre-existentes + 19 nuevos PASSED
- Suite completa `uv run pytest -q` verde sin regresiones (Ámbito, IOL, Higyrus pasan)
- mypy strict sin errores en `packages/matriz-client/` y `verification/`
- ruff check + format limpios en todo el repo
- Plan 05-02 (driver) puede importar `diff_safemodel_bidirectional` y `verify_cycle_closure` para construir el lifecycle
- Plan 05-03 (Verified-live + MATZ-06) puede ejercitar `_unwrap` estructuralmente con mocks
</success_criteria>

<output>
Crear `.planning/phases/05-matriz-verification/05-01-SUMMARY.md` cuando done, registrando:
- Confirmación del helper promoteido en verification/safemodel_diff.py (signature + duck typing aplicado)
- Confirmación del cycle_report.py creado con verify_cycle_closure (formato del campo `regression:` esperado — convención forward-looking documentada como caveat operacional)
- Conteo final de envelope sites refactoreados (esperado: 18 exactos) — enumerar líneas finales tras el edit
- Confirmación de que `get_detailed_positions` y `get_account_report` NO fueron tocados
- Confirmación del `_token` fix solo en la rama else (NO en la rama auth_basic)
- Cantidad final de tests nuevos en test_client.py (esperado: 18 envelope + 1 sentinel = 19)
- Confirmación de que NO se introdujo PrimaryShapeError
- Confirmación de que `grep -c "assert _token is not None"` == 0 y `grep -c "_get(...).*[\""]` == 0
- Cualquier dificultad con la firma de los endpoints (e.g., kwargs específicos que requirieron ajuste)
- Métrica de duración del plan
</output>
</content>
</invoke>
