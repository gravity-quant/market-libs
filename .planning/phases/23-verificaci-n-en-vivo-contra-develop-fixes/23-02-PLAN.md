---
phase: 23-verificaci-n-en-vivo-contra-develop-fixes
plan: 02
type: tdd
wave: 2
depends_on: ["23-01"]
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/src/market_data_client/_core.py
  - packages/market-data-client/tests/
  - .planning/verification/market-data-client-findings.md
  - .planning/verification/schemas/market-data-client/
autonomous: true
requirements: [LIVE-MD-01]

must_haves:
  truths:
    - "Every client-vs-server divergence surfaced by the live driver and promoted to CONFIRMED is fixed in-cycle in models.py and/or the _core.py parsers, mirrored sync↔async (D-06)"
    - "Each CONFIRMED/FIXED finding links a mocked pytest-httpx regression via the Regression: field pointing at packages/market-data-client/tests/test_*.py::test_name (D-07)"
    - "verify_cycle_closure('market-data-client') returns PASS — the phase exit gate (success-criterion 4)"
    - "Any shell-level (_request) change that is not mirrored across client.py and aio.py opens a SYNC-ASYNC-DRIFT finding (D-06 policy)"
    - "New model fields added to reconcile live payloads are additive (Optional, default None) — received_at is never repurposed; reference models stay unstamped unless live payloads prove otherwise (D-05/D-06)"
  artifacts:
    - path: "packages/market-data-client/tests/"
      provides: "Mocked pytest-httpx regression tests, one per FIXED/CONFIRMED finding"
    - path: ".planning/verification/market-data-client-findings.md"
      provides: "Findings promoted OPEN→CONFIRMED→FIXED with regression links"
      contains: "market-data-client"
  key_links:
    - from: ".planning/verification/market-data-client-findings.md"
      to: "packages/market-data-client/tests/"
      via: "Regression: <file>.py::<test_name> field on FIXED/CONFIRMED findings"
      pattern: "Regression:"
    - from: "packages/market-data-client/src/market_data_client/_core.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "parse_*_response builds Model.from_api(...) — fixes auto-mirror both surfaces"
      pattern: "from_api"
---

<objective>
Close the verification cycle for `market-data-client`: run the Plan 01 driver against develop,
triage each surfaced divergence, and for every CONFIRMED divergence apply the in-cycle,
sync↔async-mirrored fix (in `models.py` and/or the `_core.py` parsers) guarded by a mocked
pytest-httpx regression test, then link that regression to the finding so
`verify_cycle_closure("market-data-client")` returns PASS.

Because live divergences are DISCOVERED at execution time (not pre-enumerable), the fix work is
structured as a repeatable TDD loop: on each CONFIRMED finding → write the failing mocked
regression (RED) → apply the mirrored fix (GREEN) → link `regression=` → re-run cycle closure.
If develop is unreachable or creds are absent (require_env SKIP / D-09 NO-DATA), no CONFIRMED
findings are produced and cycle closure passes vacuously — the apparatus still ships.

Purpose: Deliver LIVE-MD-01's "document-and-fix, mirrored sync/async, cycle-closure PASS" gate.
Output: reconciled `models.py`/`_core.py`, mocked regressions under
`packages/market-data-client/tests/`, promoted findings with regression links, cycle-closure PASS.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-verificaci-n-en-vivo-contra-develop-fixes/23-CONTEXT.md
@.planning/phases/23-verificaci-n-en-vivo-contra-develop-fixes/23-PATTERNS.md
@.planning/phases/23-verificaci-n-en-vivo-contra-develop-fixes/23-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 scope)

- `packages/market-data-client/tests/test_*.py` — new/extended mocked pytest-httpx regression
  functions (one per FIXED/CONFIRMED finding); new model-field additions are conditional on live payloads.
- `packages/market-data-client/src/market_data_client/models.py` — conditional in-cycle field
  reconciliations on the 7 SafeModels (additive Optional fields; `received_at` never repurposed; D-05/D-06).
- `packages/market-data-client/src/market_data_client/_core.py` — conditional parser fixes to
  `parse_market_data_response` / `parse_latest_response` (and reference parsers), preserving
  `resp.read()` → `time.time()` → `raise_for_response` ordering and the `[]` collection guard.
- `.planning/verification/market-data-client-findings.md` — findings promoted with `Regression:` links.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Live run + in-cycle divergence fix loop (RED mocked regression → GREEN mirrored fix)</name>
  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/src/market_data_client/_core.py, packages/market-data-client/tests/</files>
  <behavior>
    For each divergence surfaced by the live driver run and promoted to CONFIRMED:
    - RED: a new mocked pytest-httpx test reproducing the REAL server behavior fails against the
      current client (asserts the observed wire shape/field/param encoding the client does not yet handle).
    - GREEN: after the mirrored fix in models.py and/or _core.py, that same test passes, and the
      whole existing suite stays green (no regression on the already-passing shape tests).
    - Test cases follow the two analog patterns: shell-level Bearer+param-encoding via httpx_mock
      (analog tests/test_reference_client.py — asserts httpx-native bool `true`/`false`, offset=0
      preserved, None dropped) and pure _core parser/received_at behavior (analog tests/test_market_data.py).
  </behavior>
  <read_first>
    - packages/market-data-client/tests/test_reference_client.py (shell-level httpx_mock analog @1-53) and tests/test_reference_async_client.py (async mirror)
    - packages/market-data-client/tests/test_market_data.py (pure _core parser analog @39-190)
    - packages/market-data-client/tests/conftest.py (autouse seeding — NEVER_EXPIRES token + dummy base_url; do NOT re-seed @29-72)
    - packages/market-data-client/src/market_data_client/models.py (7 SafeModels; MarketDataSnapshot.from_api received_at injection @146-164; camelCase N815-exempt @22-23)
    - packages/market-data-client/src/market_data_client/_core.py (parsers @516-555 market-data with received_at stamp; @570-615 reference parsers without stamp)
    - packages/market-data-client/src/market_data_client/client.py (_request @289-346) and aio.py _request (the shell layer that must be hand-mirrored)
    - .planning/verification/market-data-client-findings.md (the findings emitted by the Plan 01 driver run)
    - verification/findings.py (append_finding + status lifecycle OPEN→CONFIRMED→FIXED; human-status preservation @517-612)
  </read_first>
  <action>
    Run the driver live via `uv run --package market-data-client python main_market_data.py` (or
    the aggregate `uv run python main_verify.py`). Triage the findings file: for each real
    client-vs-server divergence, promote the finding OPEN→CONFIRMED. Then run the repeatable loop
    per CONFIRMED finding:
    (1) RED — add a mocked pytest-httpx regression under `packages/market-data-client/tests/`
    (extend the matching existing `test_*.py` or add a dedicated file per existing conventions —
    your discretion) that reproduces the observed server behavior and currently FAILS against the
    client. Use the conftest autouse seeding (do not re-seed). For a param/encoding divergence use
    the `httpx_mock` shell analog; for a parse/shape/`received_at` divergence use the pure `_core`
    analog.
    (2) GREEN — apply the fix. SHAPE/field/type reconciliations go in `models.py` on the affected
    SafeModel (additive Optional fields default `None`; wire names stay camelCase verbatim;
    NEVER repurpose `received_at`, and keep reference models unstamped unless live payloads prove
    a consumer need — D-05). Parser fixes go in the `_core.py` `parse_*_response` functions,
    preserving the `resp.read()` → `time.time()` (market-data only) → `raise_for_response` ordering
    and the `[]` collection guard. Because `_core.py` and `models.py` are single-source shared by
    both `client.py` and `aio.py`, these fixes auto-mirror; if a divergence requires a shell-level
    change in `_request`, hand-mirror it in BOTH `client.py` and `aio.py` — an unmirrored shell
    change MUST open a `SYNC-ASYNC-DRIFT` finding (D-06 policy).
    (3) LINK — re-emit the finding via `append_finding(...)` with status `FIXED` and
    `regression="packages/market-data-client/tests/<file>.py::<test_name>"` (repo-relative path,
    file exists, contains `def <test_name>(` — the structural checks `verify_cycle_closure`
    enforces). Map classes per D-05: `received_at`/staleness/event-time → SHAPE, bool/param
    mis-encoding → PARAM, token/401 → AUTH; never invent a class.
    Repeat until every CONFIRMED finding is FIXED-with-regression. If the run yields NO CONFIRMED
    divergences (creds absent → require_env SKIP, or develop unreachable → NO-DATA per D-09),
    record that outcome in the summary; no model/parser change is fabricated and the loop is a
    no-op — cycle closure passes vacuously.
  </action>
  <verify>
    <automated>uv run --package market-data-client python -m pytest packages/market-data-client/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - The full `market-data-client` test suite passes green: `uv run --package market-data-client python -m pytest packages/market-data-client/tests/ -q` exits 0.
    - For every finding in status CONFIRMED or FIXED in `.planning/verification/market-data-client-findings.md`, a matching test function exists at the path named in its `Regression:` field and that file contains `def <test_name>(`.
    - No SafeModel edit removes or repurposes the `received_at` field on `MarketDataSnapshot`; any added field is Optional with default `None`.
    - `_core.py` market-data parsers still stamp `received_at = time.time()` between `resp.read()` and `raise_for_response`; reference parsers still carry NO stamp.
    - If a shell `_request` change was made, it appears in BOTH `client.py` and `aio.py` (or a `SYNC-ASYNC-DRIFT` finding exists explaining the gap).
    - If no divergences were confirmed, the SUMMARY documents the require_env-SKIP / NO-DATA outcome and no `models.py`/`_core.py` diff was fabricated.
  </acceptance_criteria>
  <done>Every confirmed divergence is fixed in-cycle, mirrored sync↔async, and locked by a passing mocked regression linked from its finding — or the run is documented as SKIP/NO-DATA with a vacuous loop.</done>
</task>

<task type="auto">
  <name>Task 2: Cycle-closure gate + green CI + runner classification</name>
  <files>.planning/verification/market-data-client-findings.md, packages/market-data-client/tests/</files>
  <read_first>
    - verification/cycle_report.py (verify_cycle_closure structural link check @123-176)
    - .planning/verification/market-data-client-findings.md (current promoted findings)
    - main_verify.py (aggregate runner classification RAN/SKIPPED/FAILED @44-96)
    - .planning/phases/23-verificaci-n-en-vivo-contra-develop-fixes/23-CONTEXT.md (D-07 exit gate; D-09 never-FAILED contract)
  </read_first>
  <action>
    Assert the phase exit gate: call `verify_cycle_closure("market-data-client")` and confirm it
    returns `(True, [])` (PASS) — every CONFIRMED/FIXED finding has a valid, existing, repo-relative
    regression link (criterion 4). Run the full quality gate: `uv run --package market-data-client
    python -m pytest packages/market-data-client/tests/`, `uv run ruff check`, `uv run ruff format
    --check`, and `uv run mypy` — all clean (strict). Run `uv run python main_verify.py` and confirm
    `market-data-client` is classified `RAN` (creds present) or `SKIPPED` (creds absent) — NEVER
    `FAILED` (D-09). Ensure committable artifacts are clean: findings file, schema snapshots, and
    regression tests contain no credentials or PII (raw payloads remain only in gitignored
    `.planning/verification/captures/`).
  </action>
  <verify>
    <automated>uv run python -c "from verification.cycle_report import verify_cycle_closure; ok,missing=verify_cycle_closure('market-data-client'); assert ok, f'cycle closure FAILED: {missing}'; print('cycle closure PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `verify_cycle_closure("market-data-client")` returns `(True, [])`.
    - `uv run --package market-data-client python -m pytest packages/market-data-client/tests/` exits 0.
    - `uv run ruff check` and `uv run ruff format --check` on changed files are clean; `uv run mypy` passes strict.
    - `uv run python main_verify.py` classifies `market-data-client` as `RAN` or `SKIPPED`, never `FAILED`.
    - `grep` of the committed findings file + schema snapshots finds no Bearer token, client_secret, or raw credential value.
  </acceptance_criteria>
  <done>Cycle closure PASS, full CI green, and the aggregate runner classifies the driver RAN/SKIPPED (never FAILED); all committed artifacts are credential-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → develop API / Auth0 | live probes carry the Bearer; server payloads are untrusted and may carry real symbols/prices |
| findings + regression fixtures + schema snapshots → committed repo | redaction boundary — committed artifacts must be credential- and PII-free |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Information Disclosure | committed mocked-regression fixtures / findings from real develop payloads | mitigate | Regression fixtures use synthetic or `anonymize`d values, never verbatim live credentials; raw captured payloads stay in gitignored `.planning/verification/captures/`; the findings ART block is written by `append_finding` (no token echo). Task 2 greps committed artifacts for credential leakage. |
| T-23-05 | Information Disclosure | Auth0 token / client_secret during live triage run | mitigate | Driver output already redacted (Plan 01 T-23-01); triage reads the findings file, not raw stdout; no token is copied into a test or the findings file. |
| T-23-SC | Tampering | package-manager installs | accept | No new npm/pip/cargo installs — regression tests use the already-locked `pytest-httpx`; no supply-chain surface introduced. |
</threat_model>

<verification>
- `uv run --package market-data-client python -m pytest packages/market-data-client/tests/` green.
- `verify_cycle_closure("market-data-client")` returns `(True, [])`.
- `uv run ruff check` / `uv run ruff format --check` / `uv run mypy` clean (strict).
- `uv run python main_verify.py` classifies market-data-client RAN or SKIPPED, never FAILED.
- Committed findings + schema snapshots contain no credential/PII.
</verification>

<success_criteria>
- Every CONFIRMED divergence fixed in-cycle, mirrored sync↔async, linked to a passing mocked regression.
- Cycle closure PASS (criterion 4).
- Full CI green; runner never FAILED (D-09).
- Model fixes additive and PROVISIONAL-safe (received_at never repurposed; reference models unstamped unless proven).
</success_criteria>

<output>
Create `.planning/phases/23-verificaci-n-en-vivo-contra-develop-fixes/23-02-SUMMARY.md` when done.
</output>
