---
phase: 30-iol-client-tipado
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - packages/iol-client/src/iol_client/models.py
  - packages/iol-client/src/iol_client/__init__.py
  - packages/iol-client/src/iol_client/_core.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_models.py
  - packages/iol-client/tests/test_core.py
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
autonomous: true
requirements: [TYP-01]

must_haves:
  truths:
    - "`get_historical_quotes` devuelve una lista de `Cotizacion`; cada fila se lee por atributo (criterio 2)."
    - "`get_instruments_by_type` devuelve una lista de `Titulo`; el envelope `titulos` se desenvuelve como paso raw-dict y no se modela (D-06)."
    - "`Titulo.from_api()` sobre el schema capturado `get-instruments-by-type.json` produce cero divergencias (criterio 1)."
    - "`Titulo.puntas` es un `Punta` singular, no una lista — la única forma observada en ese endpoint (D-02)."
    - "Una respuesta de serie histórica que no es una lista top-level levanta `IOLAPIError` en vez de degradar a lista vacía (D-06, ASVS V5)."
    - "Un `list[Cotizacion]` de N filas produce como máximo 1 registro de divergencia por campo, no N — el parser posee un solo `DecodeScope` por respuesta HTTP (D-05)."
    - "Las 8 firmas de `get_historical_quotes` y `get_instruments_by_type` (método/shim × sync/async) devuelven listas de modelos y mypy strict sigue limpio."
  artifacts:
    - path: "packages/iol-client/src/iol_client/models.py"
      provides: "Titulo — 20 campos del envelope titulos"
      contains: "class Titulo(SafeModel)"
    - path: "packages/iol-client/src/iol_client/_core.py"
      provides: "_parse_list_or_raise + los 2 parsers list-shaped devolviendo modelos"
      contains: "_parse_list_or_raise"
  key_links:
    - from: "packages/iol-client/src/iol_client/_core.py"
      to: "packages/iol-client/src/iol_client/models.py"
      via: "los parsers list-shaped construyen Cotizacion y Titulo fila por fila"
      pattern: "_parse_list_or_raise\\(resp, Cotizacion\\)"
    - from: "packages/iol-client/src/iol_client/client.py"
      to: "packages/iol-client/src/iol_client/_core.py"
      via: "método y shim de get_instruments_by_type delegan al parser y re-anotan el retorno"
      pattern: "-> list\\[Titulo\\]:"
  prohibitions:
    - statement: "Ningún campo de RESPONSE gana un tipo de conjunto cerrado; `mercado` y `plazo` en `Titulo` se declaran como texto libre y su promoción se decide en F33 con censo vivo (D-09/F29, criterio 4)."
      status: unverified
      flagged: true
    - statement: "Un guard de forma que falla **levanta**; jamás degrada a lista vacía — ésa es exactamente la regresión del vacío silencioso que este milestone existe para eliminar (D-06)."
      status: unverified
      flagged: true
    - statement: "El campo `puntas` no se declara como colección de diccionarios opacos: esquivaría el gate de homogeneidad de F32 por tecnicismo en vez de modelar la forma (D-02)."
      status: unverified
      flagged: true
    - statement: "La deduplicación de divergencias nunca se implementa con un conjunto de módulo de vida-proceso; el scope per-response es el único mecanismo permitido (Phase 29, aggregation lock 6)."
      status: unverified
      flagged: true
---

<objective>
Expandir el patrón validado por el tracer a las dos funciones **list-shaped**: `get_historical_quotes` (reusa `Cotizacion`, ya probado) y `get_instruments_by_type` (modelo `Titulo` nuevo, con desenvoltura de envelope). Introduce el helper compartido `_parse_list_or_raise` que también consumirá el plan 30-03.

Purpose: `Titulo` es el segundo modelo de superficie grande y el único con `Punta` singular; el helper de lista es el punto donde vive el guard de forma que convierte una respuesta inesperada en un error ruidoso en vez de una lista vacía. Con este plan quedan migradas 12 de las 16 firmas.

Output: `Titulo` en `models.py` y en `__all__`; `_parse_list_or_raise` decorado con el scope per-response; 2 parsers reescritos in place; 8 firmas re-anotadas en sync y async; tests de modelo y de parser.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-iol-client-tipado/30-CONTEXT.md
@.planning/phases/30-iol-client-tipado/30-RESEARCH.md
@.planning/phases/30-iol-client-tipado/30-PATTERNS.md
@.planning/phases/30-iol-client-tipado/30-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Ver la tabla completa en `30-01-PLAN.md` § "Artifacts this phase produces". Símbolos **creados por este plan**: `iol_client.models.Titulo`, `iol_client._core._parse_list_or_raise`. Todos los demás símbolos que este plan consume (`SafeModel`, `Punta`, `Cotizacion`, `to_dict`, `from_api`) los creó el plan 30-01 dentro de esta misma fase y **no** son drift.

## Flagged Assumptions (no resueltas — no borrar)

| # | Assumption | Estado | Por qué queda abierta |
|---|---|---|---|
| FA-01 | La fila de cobertura de edges para **TYP-01** llegó `unclassified` → **unresolved**; los edges concretos se derivan de los 5 criterios del ROADMAP § Phase 30 y de D-01..D-12. | unresolved | Sin SPEC file para la fase. Se declara en vez de descartarse. |
| FA-02 (RESEARCH A1) | `Titulo.fechaVencimiento`, `precioEjercicio` y `tipoOpcion`: el corpus **sólo** registra ausencia de valor; el tipo no-nulo está inferido del nombre del campo (texto para las dos fechas/opción, decimal para el precio), nunca observado. | abierta → F33 | Auto-correctiva: un tipo mal inferido produce un registro de divergencia con el tipo real nombrado. **Debe** quedar marcada en el docstring de `Titulo` para que Phase 33 la reconozca. |
| FA-03 (RESEARCH A2 / D-02) | El elemento de `Cotizacion.puntas` se asume con la forma de 4 decimales de `Titulo.puntas`. | abierta → F33 | Propiedad de D-02 por decisión. |
| FA-05 | D-01 y RESEARCH dicen "21 claves" para `Titulo`; el schema committeado tiene **20**. Este plan usa el schema, que CONTEXT declara fuente de verdad. | resuelta a favor del schema | Registrada para que 20 campos no se lea como una omisión. |

## Reversibility Ratings (REVERSIBILITY_GATES)

| Decisión de este plan | Rating | Gate |
|---|---|---|
| Guard de forma que levanta en vez de degradar | two-way (una línea) — pero **no se abre**: D-06 la fija y revertirla reintroduce un bug conocido | ninguno |
| `Titulo.puntas` singular en vez de lista | two-way — corregible con evidencia de F33 | ninguno |
| 8 firmas más migradas a modelos | two-way en el repo; la publicación es de F34 | ninguno |

Ninguna decisión one-way sin autorización previa → **no se inserta ningún `checkpoint:decision`**.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: modelo `Titulo` — 20 campos del envelope, con `Punta` singular</name>

  <files>
    packages/iol-client/src/iol_client/models.py,
    packages/iol-client/src/iol_client/__init__.py,
    packages/iol-client/tests/test_models.py
  </files>

  <read_first>
    - `packages/iol-client/src/iol_client/models.py` — estado post-30-01: `SafeModel`, `Punta`, `Cotizacion`. `Titulo` se agrega siguiendo exactamente el mismo patrón de declaración.
    - `.planning/verification/schemas/iol-client/get-instruments-by-type.json` — las claves y tipos de `titulos[]`. Fuente de verdad.
    - `packages/higyrus-client/src/higyrus_client/models.py` líneas 74-116 — el patrón de docstring con notas verificadas-contra-wire.
    - `packages/iol-client/src/iol_client/_decode.py` líneas 415-530 — `walk_field`: rama de opcional (:433-442, sin registro), rama de modelo anidado (:475-490, construye con `hint(**walk_model(...))` y **nunca** con `from_api`), rama decimal (:517-522, acepta entero y ensancha).
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Model Field Tables (tabla de `Titulo`), Pitfall 4, Assumptions Log A1.
    - `packages/iol-client/tests/test_models.py` — estado post-30-01; los tests nuevos siguen sus convenciones.
  </read_first>

  <behavior>
    Tests nuevos en `test_models.py`, escritos y en rojo antes de declarar la clase:
    - `Titulo.from_api(fila)` con una fila sintética construida desde `get-instruments-by-type.json` (las 20 claves, tipos del schema) construye los 20 campos y produce **cero** registros de divergencia.
    - `Titulo.from_api({})` devuelve typed zeros para los campos no opcionales (`descripcion == ""`, `apertura == 0.0`, `lote == 0`) y ausencia de valor para `fechaVencimiento`, `precioEjercicio`, `tipoOpcion` y `puntas`.
    - `Titulo.from_api({"puntas": {4 decimales}})` produce una instancia **singular** de `Punta` (no una lista) con los 4 valores.
    - `Titulo.from_api({"puntas": None}).puntas` no tiene valor y **no** emite registro de divergencia (rama Optional del walker).
    - Asimetría D-04 en el otro sentido: `Titulo.from_api({"cantidadOperaciones": 7})` produce `7.0` (ensanchado) y **cero** registros — contraste explícito con el test equivalente de `Cotizacion`, que sustituye typed-zero y sí reporta.
    - Round-trip: `schema_of(Titulo.from_api(fila).to_dict())` reproduce la forma de `titulos[0]` del schema committeado.
  </behavior>

  <action>
    RED → GREEN → REFACTOR.

    **RED.** Agregar los casos de `<behavior>` a `packages/iol-client/tests/test_models.py`. Correr y verlos fallar por símbolo inexistente.

    **GREEN.** Agregar a `models.py`, después de `Cotizacion`:

    `@dataclass(frozen=True, slots=True) class Titulo(SafeModel)` con exactamente estos 20 campos, tipos verbatim del corpus: `apertura: float`, `cantidadOperaciones: float`, `descripcion: str`, `fecha: str`, `fechaVencimiento: str | None`, `laminaMinima: int`, `lote: int`, `maximo: float`, `mercado: str`, `minimo: float`, `moneda: str`, `plazo: str`, `precioEjercicio: float | None`, `puntas: Punta | None`, `simbolo: str`, `tipoOpcion: str | None`, `ultimoCierre: float`, `ultimoPrecio: float`, `variacionPorcentual: float`, `volumen: float`. Sin valores por defecto.

    Los tres campos que el corpus registra sin valor se declaran opcionales por D-03 — el walker trata lo opcional como opt-in explícito y no emite divergencia, mientras que declararlos no-opcionales pre-cargaría el censo de Phase 33 con divergencias garantizadas que no son defectos.

    Nótese contra `Cotizacion`: acá `plazo` **no** es opcional (el corpus lo trae poblado en este endpoint), `cantidadOperaciones` es decimal (D-04) y `puntas` es singular (D-02). `mercado` y `plazo` quedan como texto libre — la promoción a conjunto cerrado es de F33 con censo vivo (criterio 4, DT-07).

    Docstring de `Titulo` — debe registrar, con la fecha de captura 2026-06-06: (a) que modela cada fila del envelope `titulos` y que el envelope en sí **no** se modela (D-06); (b) que `fechaVencimiento`, `precioEjercicio` y `tipoOpcion` llegaron **sin valor** en toda la captura, así que su tipo no-nulo está **inferido del nombre del campo y nunca observado** — FA-02/A1, a confirmar en Phase 33, y una divergencia de tipo en cualquiera de los tres es esta asunción corrigiéndose, no un defecto; (c) que `puntas` acá es singular mientras que en `Cotizacion` es una colección — dos formas del mismo nombre de clave en el mismo corpus (D-02); (d) que `cantidadOperaciones` es decimal acá y entero en `Cotizacion`, y que la rama decimal del walker **ensancha en silencio** un entero del wire mientras que la entera sustituye typed-zero y reporta — asimetría deliberada de D-04 que sigue la evidencia (Pitfall 4).

    Exportar `Titulo` en `packages/iol-client/src/iol_client/__init__.py`: agregarlo al bloque `from iol_client.models import (...)` y al `__all__`, ambos en orden alfabético (Pitfall 3 — sin esto ni el snapshot de superficie ni el gate AST de F32 lo ven).

    **No** agregar ningún override de `from_api` en `Titulo` ni en ningún modelo: `slots=True` reemplaza el objeto clase y un `super()` de cero argumentos capturaría la clase pre-slots (Pitfall 7); además un override resolvería el sink por `current_sink()` y saldría del scope envolvente, rompiendo la deduplicación.

    **REFACTOR.** `ruff format` + `ruff check --fix`; re-correr tests.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_models.py -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_models.py -q` sale 0 con ≥ 14 tests.
    - `uv run python -c "from iol_client.models import Titulo; import dataclasses; f=[x.name for x in dataclasses.fields(Titulo)]; assert len(f)==20, f"` sale 0.
    - `uv run python -c "from iol_client.models import Titulo, Punta; import typing; h=typing.get_type_hints(Titulo); assert h['puntas'] == Punta | None, h['puntas']; assert h['cantidadOperaciones'] is float"` sale 0.
    - `uv run python -c "from iol_client.models import Cotizacion; import typing; h=typing.get_type_hints(Cotizacion); assert h['cantidadOperaciones'] is int"` sale 0 (la asimetría D-04 sigue en pie tras el cambio).
    - `uv run python -c "import iol_client; assert 'Titulo' in iol_client.__all__"` sale 0.
    - `grep -c 'class Titulo(SafeModel)' packages/iol-client/src/iol_client/models.py` imprime `1`.
    - `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('packages/iol-client/src/iol_client/models.py').read_text()); assert not [n for n in ast.walk(t) if isinstance(n,ast.FunctionDef) and n.name=='from_api' and getattr(n,'col_offset',0)>4 or False][1:], 'un solo from_api'"` — verificación de que `from_api` sigue definido una única vez en el módulo.
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py uv.lock` sale 0.
  </acceptance_criteria>

  <done>
    `Titulo` existe con 20 campos verbatim del schema committeado, `Punta` singular, `cantidadOperaciones` decimal y los 3 campos de tipo inferido marcados en el docstring; está exportado en `__all__`; los tests cubren construcción, typed zeros, las dos formas de `puntas`, la asimetría D-04 en ambos sentidos y el round-trip.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `_parse_list_or_raise` + los 2 parsers list-shaped + las 8 firmas sync/async</name>

  <files>
    packages/iol-client/src/iol_client/_core.py,
    packages/iol-client/src/iol_client/client.py,
    packages/iol-client/src/iol_client/aio.py,
    packages/iol-client/tests/test_core.py
  </files>

  <read_first>
    - `packages/iol-client/src/iol_client/_core.py` líneas 320-360 — estado post-30-01: `parse_get_quote_response` ya devuelve modelo; `parse_get_historical_quotes_response` (:333-339) y `parse_get_instruments_by_type_response` (:353-360) son los objetivos de este plan. Los builders :234-319 quedan intactos.
    - `packages/higyrus-client/src/higyrus_client/_core.py` líneas 457-500 — el helper con decorador y los parsers de una línea con local anotado. El local anotado es load-bearing: el helper devuelve una lista genérica y la anotación es lo que estrecha el tipo para mypy strict.
    - `packages/iol-client/src/iol_client/exceptions.py` línea 13 — `IOLAPIError(status_code, message)`, **dos posicionales**.
    - `packages/iol-client/src/iol_client/_decode.py` línea 312 — `_response_parser`.
    - `packages/iol-client/src/iol_client/client.py` líneas 529-547 y 558-571 (métodos), 683-694 y 702-708 (shims).
    - `packages/iol-client/src/iol_client/aio.py` líneas 548-562 y 570-579 (métodos), 702-713 y 719-725 (shims).
    - `packages/iol-client/tests/test_core.py` líneas 345-375 — tests actuales de los parsers de lista, incluido el que afirma lista vacía ante envelope sin la clave.
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Pattern 3 (con las 3 adaptaciones iol), § The 16 Signatures, Pitfall 6.
  </read_first>

  <behavior>
    Tests en `test_core.py`, en rojo antes del cambio:
    - `parse_get_historical_quotes_response(resp)` con un body de lista de 2 filas devuelve una lista de 2 `Cotizacion` y `resultado[0].ultimoPrecio` tiene el valor mockeado.
    - `parse_get_historical_quotes_response(resp)` con un body que **no** es una lista top-level levanta `IOLAPIError` cuyo mensaje nombra el tipo recibido. No devuelve lista vacía.
    - `parse_get_instruments_by_type_response(resp)` con `{"titulos": [fila, fila]}` devuelve 2 `Titulo`.
    - `parse_get_instruments_by_type_response(resp)` con un envelope **sin** la clave `titulos` sigue devolviendo lista vacía — comportamiento preexistente que D-06 preserva a propósito (el test actual sobrevive sin cambios).
    - Un body de lista de 3 filas con la **misma** clave faltante en las 3 produce **1** registro de divergencia para ese campo, no 3 — prueba de que el parser posee un único `DecodeScope` por respuesta. Correr bajo el fixture de contexto de decode limpio de `test_decode.py:49-75`.
  </behavior>

  <action>
    RED → GREEN → REFACTOR. Las tres ediciones de código van en **un solo commit** (Pitfall 6: sync y async nunca se separan).

    **RED.** Escribir los casos de `<behavior>` en `test_core.py`.

    **GREEN.**

    1. `_core.py` — agregar el helper compartido `_parse_list_or_raise(resp: httpx.Response, model_cls: type[Any]) -> list[Any]`, decorado con `@_decode._response_parser`. Cuerpo: `resp.read()`, `raise_for_response(resp)`, `raw = resp.json()`, y si `raw` no es una lista **levantar** `IOLAPIError(0, ...)` con un mensaje que nombre el tipo recibido; si lo es, construir `[model_cls.from_api(item) for item in raw]`. Tres adaptaciones respecto del original de higyrus, todas obligatorias: (a) iol **no** tiene `_consume_and_check` — usar el par `resp.read()` + `raise_for_response(resp)` que ya usan sus parsers; copiar el helper de higyrus agregaría tolerancia a 204/cuerpo vacío que iol no tiene hoy, un cambio de comportamiento fuera de scope; (b) `IOLAPIError` toma **dos posicionales**, no la firma con lista de errores de higyrus; (c) el decorador va **sólo** en el helper y en los parsers de nivel superior que no lo usan — los parsers que delegan quedan como funciones de una línea sin decorar, para que el anidamiento de scopes siga siendo seguro.
    2. `_core.py` — reescribir `parse_get_historical_quotes_response` **in place**: retorno anotado como lista de `Cotizacion`, cuerpo de una línea con un local anotado que recibe `_parse_list_or_raise(resp, Cotizacion)` y lo retorna. Sin decorador propio (lo aporta el helper).
    3. `_core.py` — reescribir `parse_get_instruments_by_type_response` **in place**: conserva su decorador `@_decode._response_parser` y su cuerpo propio, porque no consume una lista top-level. Secuencia: `resp.read()`, `raise_for_response(resp)`, `data = resp.json()`, desenvolver `titulos = data.get("titulos", [])` como **paso raw-dict antes de construir modelos** — el envelope es forma de transporte, no de dominio, y no se modela (D-06) — y devolver la lista por comprensión con `Titulo.from_api(fila)`. La ausencia de la clave sigue produciendo lista vacía: es el comportamiento actual y D-06 no lo cambia.
    4. `client.py` — re-anotar el retorno del método (:529-547) y el shim (:683-694) de `get_historical_quotes` a lista de `Cotizacion`, y del método (:558-571) y shim (:702-708) de `get_instruments_by_type` a lista de `Titulo`. Importar los modelos. **Cero lógica** se mueve a los shells. El parámetro `ajustada` conserva su tipo de conjunto cerrado: es un literal de **INPUT** anterior a DT-07 (`_core.py:265`) y el lock de D-09 no lo alcanza porque no es un campo de respuesta. `mercado` sigue siendo texto libre.
    5. `aio.py` — idéntico en métodos (:548-562, :570-579) y shims (:702-713, :719-725).

    **REFACTOR.** `ruff format` + `ruff check --fix`; suite del paquete.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_core.py packages/iol-client/tests/test_models.py -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run lint-imports</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_core.py -q` sale 0.
    - `grep -c -- '-> list\[Cotizacion\]:' packages/iol-client/src/iol_client/client.py` imprime `2`; ídem en `aio.py`.
    - `grep -c -- '-> list\[Titulo\]:' packages/iol-client/src/iol_client/client.py` imprime `2`; ídem en `aio.py`.
    - `grep -c '_parse_list_or_raise' packages/iol-client/src/iol_client/_core.py` imprime ≥ `2` (definición + al menos un uso).
    - `grep -B3 -n 'def _parse_list_or_raise' packages/iol-client/src/iol_client/_core.py | grep -c '_response_parser'` imprime `1`.
    - `grep -c 'Titulo.from_api' packages/iol-client/src/iol_client/_core.py` imprime `1`.
    - `grep -c 'titulos' packages/iol-client/src/iol_client/_core.py` imprime ≥ `1` (el desenvolvimiento del envelope sigue vivo).
    - `uv run python -c "import inspect, iol_client; s=str(inspect.signature(iol_client.get_instruments_by_type)); assert 'Titulo' in s, s"` sale 0.
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `uv run lint-imports` sale 0 (el sentido `_core` → `models` → `_decode` no viola ningún contrato; el único contrato de iol prohíbe `_core` → `client`/`aio`).
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py uv.lock` sale 0.
    - Un módulo de dedupe de vida-proceso no se introdujo: `uv run python -c "import ast,pathlib; src=pathlib.Path('packages/iol-client/src/iol_client/_core.py').read_text(); t=ast.parse(src); assigns=[n for n in t.body if isinstance(n,(ast.Assign,ast.AnnAssign))]; assert not any(isinstance(getattr(a,'value',None),(ast.Set,ast.SetComp)) for a in assigns), 'set de modulo prohibido'"` sale 0.
  </acceptance_criteria>

  <done>
    `_parse_list_or_raise` existe, es dueño del scope per-response y levanta ante una forma inesperada; los dos parsers list-shaped devuelven modelos; las 8 firmas correspondientes están re-anotadas en sync y async en el mismo commit; el desenvolvimiento del envelope sobrevive como paso raw-dict; mypy e import-linter verdes.
  </done>
</task>

<task type="auto">
  <name>Task 3: migrar las aserciones de serie histórica y de instrumentos-por-tipo</name>

  <files>
    packages/iol-client/tests/test_client.py,
    packages/iol-client/tests/test_async_client.py,
    packages/iol-client/tests/test_core.py
  </files>

  <read_first>
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Blast Radius → "Attribute-access migration": inventario de 23 sitios y los 4 no-mecánicos. De ellos, este plan cubre los que consumen serie histórica y `titulos`.
    - `packages/iol-client/tests/test_client.py` líneas 55-160 — forma actual de los tests de estas dos funciones.
    - `packages/iol-client/tests/test_async_client.py` líneas 35-120 — espejos async.
    - `packages/iol-client/tests/test_core.py` líneas 360-375 — el test de envelope sin la clave `titulos`, que **sobrevive sin cambios**.
    - `packages/iol-client/tests/test_decode.py` líneas 49-75 — fixture `_pristine_decode_context`.
    - `packages/iol-client/tests/test_logging.py` — construye registros sintéticos y nunca decodifica un payload real: **inmune**, no tocar.
  </read_first>

  <action>
    Localizar los sitios con `grep -rn 'get_historical_quotes\|get_instruments_by_type' packages/iol-client/tests/` y, sobre esos tests, `grep -n '\[0\]\[\|\]\["' del archivo correspondiente.

    Reglas de migración:
    - Lectura por clave sobre una fila de serie histórica → lectura por atributo del mismo nombre sobre la `Cotizacion` de esa fila.
    - Lectura por clave sobre una fila de `titulos` → atributo del mismo nombre sobre el `Titulo`.
    - Igualdad contra un dict o contra una lista de dicts → aserciones de atributo más, cuando el test verifica cardinalidad, una aserción sobre el largo de la lista.
    - Antes de reescribir un sitio, **verificar que la clave existe en el modelo**. Si el test afirma una clave que no está entre los 20 campos del modelo correspondiente, esa aserción es inválida contra el corpus vivo: eliminarla y registrar la eliminación y su razón en el SUMMARY. No inventar un atributo equivalente.
    - El test de `test_core.py` que afirma lista vacía ante un envelope sin la clave `titulos` **no cambia**: la desenvoltura sigue produciendo lista vacía y ese comportamiento es deliberado.
    - Los mocks parciales ahora emiten registros de campo faltante por llamada; es inocuo (el modo estricto es `False` por defecto y ningún test de iol lo activa). **No** activar modo estricto: es entregable de Phase 33. Confirmar que ningún test afirma sobre conteos de registros capturados del logger del paquete.

    No debilitar cobertura: cada aserción migrada debe seguir probando lo mismo que probaba (valor, cardinalidad, o forma), sólo que por atributo.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client -q` sale 0 y reporta ≥ 205 tests.
    - `uv run pytest packages/iol-client -q -p no:randomly` sale 0 (los tests de divergencia no dependen del orden gracias al fixture de contexto limpio).
    - `grep -rn 'historical\[0\]\["' packages/iol-client/tests/` no devuelve resultados (salida vacía, exit 1).
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` sale 0.
    - `git diff --exit-code packages/iol-client/tests/test_logging.py` sale 0 (archivo inmune, no debe haber sido tocado).
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py uv.lock` sale 0.
  </acceptance_criteria>

  <done>
    Toda aserción que consume serie histórica o filas de `titulos` lee por atributo; las aserciones sobre claves inexistentes en el corpus fueron eliminadas con su razón registrada; el test de envelope vacío sobrevive intacto; suite del paquete verde con al menos la cardinalidad de línea base.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API IOL en vivo → `_parse_list_or_raise` | Entra una respuesta de forma arbitraria; el guard decide si se procesa o se rechaza. |
| Envelope `titulos` → construcción de modelos | La desenvoltura opera sobre un dict crudo no confiable antes de que exista cualquier modelo. |
| Registros de divergencia → handlers del consumidor | Cruce hacia código de terceros; sólo tipos y rutas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering | `_parse_list_or_raise` | mitigate | Degradación silenciosa de forma enmascara un upstream cambiado o comprometido. El guard **levanta** `IOLAPIError(0, ...)` nombrando el tipo recibido; nunca devuelve lista vacía (D-06, ASVS V5 — validación de entrada). Cubierto por un test dedicado en la tarea 2. |
| T-30-07 | Denial of service | dedupe de divergencias | mitigate | Una lista de N filas con un campo faltante podría emitir N registros por campo y saturar el pipeline de salida del consumidor. El `DecodeScope` per-response colapsa a 1 registro por campo; probado con una lista de 3 filas en la tarea 2. Un conjunto de módulo de vida-proceso está prohibido (aggregation lock 6) y verificado por AST. |
| T-30-01 | Information disclosure | `models.py` | mitigate | `Titulo` no agrega ningún canal de salida; sigue vigente la allowlist de imports verificada en 30-01. Los registros del decoder son tipo-y-ruta, nunca valor. |
| T-30-03 | Tampering | integridad del walker | mitigate | `_decode.py` sin cambios, verificado con `git diff --exit-code` en las 3 tareas. |
| T-30-SC | Tampering | instalaciones vía gestor de paquetes | mitigate | Cero paquetes externos. `git diff --exit-code uv.lock` es criterio de aceptación en las 3 tareas. Sin `[ASSUMED]`/`[SUS]`/`[SLOP]` que gatear. |
</threat_model>

<verification>
1. `uv run pytest packages/iol-client -q` → verde, ≥ 205 tests.
2. `uv run mypy packages/iol-client/src packages/iol-client/tests` → 0.
3. `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` → 0.
4. `uv run lint-imports` → 0.
5. `uv run python tools/check_decode_intactness.py` → 0.
6. `git diff --exit-code uv.lock packages/iol-client/src/iol_client/_decode.py` → 0.
7. 12 de las 16 firmas migradas: `grep -c -- '-> Cotizacion:\|-> list\[Cotizacion\]:\|-> list\[Titulo\]:' packages/iol-client/src/iol_client/client.py` imprime `6`; ídem `aio.py`.
</verification>

<success_criteria>
- `Titulo` decodifica el schema committeado de instrumentos-por-tipo con cero divergencias y su `to_dict()` reproduce esa forma.
- Serie histórica e instrumentos-por-tipo devuelven listas de modelos en sync y async, en el mismo commit.
- Una respuesta de forma inesperada levanta en vez de degradar.
- Una lista de N filas produce a lo sumo 1 registro de divergencia por campo.
</success_criteria>

<output>
Crear `.planning/phases/30-iol-client-tipado/30-02-SUMMARY.md`. Debe registrar: (a) las aserciones eliminadas por afirmar claves inexistentes en el corpus, con su razón; (b) confirmación de que FA-02/A1 quedó marcada en el docstring de `Titulo`; (c) la corrección de inventario 21 → 20 campos de `Titulo`; (d) conteo de tests antes/después.
</output>
