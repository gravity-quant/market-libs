---
phase: 09-deferred-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/iol-client/tests/test_refresh_token_lifecycle.py
  - packages/iol-client/tests/test_refresh_token_lifecycle_async.py
autonomous: true
requirements: [BUG-03]
requirements_addressed: [BUG-03]
tags: [phase-09, iol, bug-fixes, regression-tests, oauth-refresh, wave-1]
user_setup: []

must_haves:
  truths:
    - "BUG-03 (D-06): Regression tests cubren los 4 paths críticos del lifecycle de `_state.refresh_token` per instancia — el código ya está en producción desde Phase 6 D-IOL-10 + Phase 7 D-04 alias; este plan NO modifica `_core.py`/`client.py`/`aio.py`/`_state.py`."
    - "Path 1 (refresh→success): `state.refresh_token=seed-X` + `state.token_expires_at=0.0` → POST /token con `grant_type=refresh_token` → 200 con nuevo access_token + rotated refresh_token; assert `state.token` actualizado, `state.refresh_token` rotated, exactly 2 outgoing requests (refresh + endpoint)."
    - "Path 2 (refresh→401→password fallback): refresh_token revocado → 401 → catch interno → password grant → 200; assert `state.token`/`state.refresh_token` updated desde el password grant, exactly 3 outgoing requests (refresh + password + endpoint), NO `IOLAuthError` raised al caller."
    - "Path 3 (CR-01 conditional rotation, server NO rota): server responde sin `refresh_token` field → `parse_login_response` retorna `None` → shell guard `if refresh is not None:` NO se ejecuta → `state.refresh_token` preserved (NOT cleared, NOT replaced con None)."
    - "Path 4 (CR-01 conditional rotation, server SÍ rota): server responde con nuevo `refresh_token` → parser retorna el nuevo valor → shell guard se ejecuta → `state.refresh_token` actualizado al nuevo valor."
    - "Sync tests en `packages/iol-client/tests/test_refresh_token_lifecycle.py` (4 tests sync); Async mirror en `tests/test_refresh_token_lifecycle_async.py` (4 tests async). Total: 8 tests nuevos."
    - "Existing 760 tests baseline (Phase 8 final) sigue GREEN tras este plan (no regression; solo adds — quality_gate manual: tests count delta +8 esperado)."
    - "Snapshot público (Phase 6 D-09) NO cambia — este plan NO modifica signatures públicas; Plan 09-04 valida zero-diff."
  artifacts:
    - path: "packages/iol-client/tests/test_refresh_token_lifecycle.py"
      provides: "Sync regression tests para los 4 paths del refresh_token lifecycle (BUG-03)"
      contains: "match_content"
      min_lines: 120
    - path: "packages/iol-client/tests/test_refresh_token_lifecycle_async.py"
      provides: "Async mirror de los 4 paths con `aio` API + `await` + `aclose()` teardown (BUG-03)"
      contains: "match_content"
      min_lines: 120
  key_links:
    - from: "packages/iol-client/tests/test_refresh_token_lifecycle.py"
      to: "packages/iol-client/src/iol_client/client.py:277-289 (_ensure_token + _refresh)"
      via: "iol_client.get_instruments('argentina') triggers _ensure_token → _refresh path"
      pattern: "iol_client\\.get_instruments"
    - from: "packages/iol-client/tests/test_refresh_token_lifecycle_async.py"
      to: "packages/iol-client/src/iol_client/aio.py:259-273 (async _ensure_token + _refresh con token_lock)"
      via: "await aio.get_instruments('argentina') triggers async refresh path"
      pattern: "await aio\\.get_instruments"
    - from: "Both test files"
      to: "packages/iol-client/src/iol_client/_state.py:80 (refresh_token field)"
      via: "_get_default()._state mutation pattern (Phase 6 Pitfall #1)"
      pattern: "_get_default\\(\\)\\._state"
---

<objective>
Plan 09-01 entrega **solo regression tests mockeados** que ejercitan los 4 paths
críticos del lifecycle de `_state.refresh_token` en una sola instancia
`Client` (sync) + `AsyncClient` (async). Cierra el contrato observable de
BUG-03 (D-06 locked): el código ya está implementado en Phase 6 D-IOL-10 y
validado por la conftest migration; este plan **NO modifica** `_core.py`,
`client.py`, `aio.py`, ni `_state.py`. Solo crea 2 archivos de test.

Purpose: Locking de los 4 paths del refresh_token lifecycle previene futuras
regresiones del CR-01 conditional rotation guard, del refresh→password
fallback (Phase 6 D-IOL-10), y del double-checked locking async (D-IOL-09).
Si alguien edita `if refresh is not None:` por error a `state.refresh_token =
refresh`, el path 3 falla inmediatamente con un assert localizable.

Output:
- `packages/iol-client/tests/test_refresh_token_lifecycle.py` (NEW, 4 sync tests).
- `packages/iol-client/tests/test_refresh_token_lifecycle_async.py` (NEW, 4 async tests).
- Tests delta: +8 (sync 4 + async 4); total esperado ~768 (760 baseline + 8).
- 1 commit atómico per D-12: `test(iol): BUG-03 refresh_token lifecycle regression tests sync + async (BUG-03)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-deferred-bug-fixes/09-CONTEXT.md
@.planning/phases/09-deferred-bug-fixes/09-RESEARCH.md
@.planning/phases/09-deferred-bug-fixes/09-PATTERNS.md
@.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
@packages/iol-client/tests/conftest.py
@packages/iol-client/tests/test_client.py
@packages/iol-client/tests/test_async_client.py
@packages/iol-client/src/iol_client/_state.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/_core.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear test_refresh_token_lifecycle.py (sync, 4 paths)</name>
  <files>packages/iol-client/tests/test_refresh_token_lifecycle.py</files>
  <read_first>
    - packages/iol-client/tests/test_client.py:140-260 (analog exacto: `test_refresh_token_success_path`, `test_refresh_fails_falls_back_to_password`, `test_refresh_and_password_both_fail`, `test_login_captures_refresh_token` — copy literal del idiom para paths 1 y 2)
    - packages/iol-client/tests/conftest.py:1-52 (autouse `_configure_sync` fixture; doc Pitfall #1 PEP 562 read-only shim)
    - packages/iol-client/src/iol_client/_state.py:80 (`refresh_token: str | None = None` — el field bajo test)
    - packages/iol-client/src/iol_client/client.py:251-289 (login + _refresh + _ensure_token con `if refresh is not None:` guard CR-01)
    - packages/iol-client/src/iol_client/_core.py:142-226 (parse_login_response / parse_refresh_response retornando `tuple[str, float, str | None]`; CR-01 conditional rotation source)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-06, D-07 (4 paths spec con seeds + assertions explicitas)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"test_refresh_token_lifecycle.py" + §"Shared Patterns S1, S2, S3" (mandatory `from __future__ import annotations`, `_get_default()._state` access, `match_content` distingue 2 grants)
    - .planning/phases/09-deferred-bug-fixes/09-RESEARCH.md §"Pattern 2 pytest-httpx Mock for OAuth Refresh Flow" + §"Common Operation 2 CR-01 conditional rotation" + §"Common Pitfalls" 2, 3, 6 (autouse fixture leakage, FIFO mock order, refresh_token cleanup)
  </read_first>
  <behavior>
    - Test 1 `test_refresh_token_success_path_rotates`: seed `state.refresh_token="seed-refresh-XYZ"`, `state.token=None`, `state.token_expires_at=0.0`. Mock POST /token con `match_content=b"refresh_token=seed-refresh-XYZ&grant_type=refresh_token"` → 200 `{access_token: "NEW-TOK", expires_in: 900, refresh_token: "ROTATED-REFRESH"}`. Mock endpoint downstream (`/api/v2/argentina/Titulos/Cotizacion/Instrumentos` → `{"instrumentos": []}`). Invocar `iol_client.get_instruments("argentina")`. Assert: `state.token == "NEW-TOK"`, `state.refresh_token == "ROTATED-REFRESH"`, `state.token_expires_at > time.time()`, `len(httpx_mock.get_requests()) == 2` (refresh + endpoint, NO password fallback).
    - Test 2 `test_refresh_401_falls_back_to_password`: seed `state.refresh_token="REVOKED-REFRESH"`. Mock POST /token con `match_content=b"refresh_token=REVOKED-REFRESH&grant_type=refresh_token"` → 401 `text="invalid_grant"`. Mock POST /token con `match_content=b"username=u&password=p&grant_type=password"` → 200 `{access_token: "NEW-TOK", expires_in: 900, refresh_token: "FRESH-REFRESH"}`. Mock endpoint downstream. Invocar `iol_client.get_instruments("argentina")`. Assert: `state.token == "NEW-TOK"`, `state.refresh_token == "FRESH-REFRESH"`, `len(httpx_mock.get_requests()) == 3` (refresh + password + endpoint), NO `IOLAuthError` raised. CRÍTICO: usar `match_content` literal para DISTINGUIR los 2 mocks sobre el mismo URL — sin `match_content` colisionan FIFO (Pitfall 3).
    - Test 3 `test_refresh_preserves_token_when_server_omits_refresh_field` (CR-01 path FALSE): seed `state.refresh_token="STABLE-REFRESH"`. Mock POST /token con `match_content=b"refresh_token=STABLE-REFRESH&grant_type=refresh_token"` → 200 `{access_token: "NEW-TOK", expires_in: 900}` (SIN `refresh_token` field). Mock endpoint. Invocar `iol_client.get_instruments("argentina")`. Assert: `state.token == "NEW-TOK"`, `state.refresh_token == "STABLE-REFRESH"` (PRESERVED — CR-01 conditional rotation rama FALSE — `parse_login_response` retorna `None` → shell `if refresh is not None:` no entra).
    - Test 4 `test_refresh_rotates_when_server_provides_new_field` (CR-01 path TRUE): seed `state.refresh_token="OLD-REFRESH"`. Mock POST /token con `match_content=b"refresh_token=OLD-REFRESH&grant_type=refresh_token"` → 200 `{access_token: "NEW-TOK", expires_in: 900, refresh_token: "NEW-ROTATED-REFRESH"}`. Mock endpoint. Invocar. Assert: `state.token == "NEW-TOK"`, `state.refresh_token == "NEW-ROTATED-REFRESH"` (ROTATED — CR-01 rama TRUE).
  </behavior>
  <action>
    Crear `packages/iol-client/tests/test_refresh_token_lifecycle.py` con docstring que describa los 4 paths cubiertos. Header: `from __future__ import annotations` mandatory (Pattern S1), imports `import time`, `import pytest`, `from pytest_httpx import HTTPXMock`, `import iol_client`, `from iol_client.exceptions import IOLAuthError` (último por si el autocheck del read_first lo requiere para futuro path 5).

    El autouse `_configure_sync` (conftest.py:25-38) precarga `token="test-token"`, `token_expires_at=9_999_999_999.0`. Cada test mutar `state.token=None; state.token_expires_at=0.0; state.refresh_token="<seed-literal>"` explícitamente al inicio via `state = iol_client.client._get_default()._state` (Pattern S2 — el shim PEP 562 es read-only; NO usar `monkeypatch.setattr(pkg.client, "_token", X)`).

    Cada test sigue el shape canónico copy de `test_client.py:154-186` (Path 1 analog literal) y `test_client.py:189-223` (Path 2 analog literal):
    - Setup state explícito (token=None, expires_at=0.0, refresh_token=seed).
    - `httpx_mock.add_response(url="https://api.test/token", method="POST", match_content=b"<form-encoded>", status_code=..., json=...)` para el refresh grant.
    - Para Path 2: agregar segundo mock con `match_content=b"username=u&password=p&grant_type=password"` distinto (Pattern S3 — sin `match_content` los 2 mocks colisionan FIFO).
    - Mock endpoint `https://api.test/api/v2/argentina/Titulos/Cotizacion/Instrumentos` → `{"instrumentos": []}` (un único response — no se consume body especial).
    - Invocar `iol_client.get_instruments("argentina")`.
    - Asserts sobre `state.token`, `state.refresh_token`, `state.token_expires_at > time.time()` cuando aplica, `len(httpx_mock.get_requests()) == N`.

    Path 1 espera 2 outgoing (refresh + endpoint); Path 2 espera 3 outgoing (refresh + password + endpoint); Paths 3-4 esperan 2 outgoing (refresh + endpoint). NO incluir `IOLAuthError` en assertion (Path 2 success path NO raise — el fallback es silencioso, ya catched internamente).

    Test 3 CRÍTICO (CR-01 conditional rotation rama FALSE): respuesta JSON SIN la key `refresh_token` (NOT `refresh_token: null`, NOT `refresh_token: ""`, sino key ausente). Esto hace que `parse_login_response` retorne `None` en la 3ra posición de la tupla, y el shell guard `if refresh is not None:` en `client.py:265 o similar` (NO modificar — leer el file completo para confirmar líneas exactas) NO se ejecuta — el `state.refresh_token` cacheado se preserva.

    Test 4 CRÍTICO (CR-01 conditional rotation rama TRUE): respuesta JSON INCLUYE `refresh_token: "NEW-ROTATED-REFRESH"`. El parser retorna el nuevo valor; el shell guard se ejecuta y `state.refresh_token` es asignado al nuevo valor.

    NO incluir TDD `@pytest.fixture` adicional ni helper functions — los 4 tests son standalone para forensic-localizable. NO usar `should_be_used=False` per-response flag (NO existe — Pitfall 7). El autouse fixture maneja teardown del default client (`close()` + reconfigure); si se detecta contaminación cross-test de `state.refresh_token` (Pitfall 6), agregar comentario flagging-the-issue al final del archivo, pero NO modificar `conftest.py` en este plan (modificación cross-cutting; defer si emerge).
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/iol-client/tests/test_refresh_token_lifecycle.py` exists.
    - File begins with `"""<module docstring describing 4 paths>"""` + blank line + `from __future__ import annotations`.
    - 4 test functions present: `test_refresh_token_success_path_rotates`, `test_refresh_401_falls_back_to_password`, `test_refresh_preserves_token_when_server_omits_refresh_field`, `test_refresh_rotates_when_server_provides_new_field`. Verified via `grep -c "^def test_" packages/iol-client/tests/test_refresh_token_lifecycle.py | grep -v '^#'` returns 4.
    - Each test uses `state = iol_client.client._get_default()._state` access pattern (Pattern S2). Verified via `grep -c "_get_default()._state" packages/iol-client/tests/test_refresh_token_lifecycle.py | grep -v '^#'` returns ≥ 4.
    - Test 2 uses TWO `match_content=b"..."` mocks differing by body (refresh grant vs password grant). Verified via `grep -c 'match_content=b"' packages/iol-client/tests/test_refresh_token_lifecycle.py | grep -v '^#'` returns ≥ 5 (paths 1+2+3+4 use ≥1 each; path 2 uses 2).
    - Test 3 (CR-01 FALSE) JSON response body does NOT include the key `refresh_token` — verified semánticamente al leer el test: el JSON literal pasado a `add_response(..., json={...})` solo contiene `access_token` y `expires_in`.
    - Test 4 (CR-01 TRUE) JSON response body contains `"refresh_token": "NEW-ROTATED-REFRESH"`. Asserted via `grep -c "NEW-ROTATED-REFRESH" packages/iol-client/tests/test_refresh_token_lifecycle.py` returns ≥ 2 (literal y assertion).
    - Each test asserts `len(httpx_mock.get_requests()) == N` exactly (no `>=`, no `>`). Verified via `grep -c "len(httpx_mock.get_requests())" packages/iol-client/tests/test_refresh_token_lifecycle.py` returns ≥ 4.
    - Test command exit 0: `uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle.py -x` returns 0 con 4 tests passed.
    - Full iol suite still GREEN: `uv run pytest packages/iol-client/tests/ -x --no-header -q` returns 0 (no regression on existing tests).
    - Ruff clean: `uv run ruff check packages/iol-client/tests/test_refresh_token_lifecycle.py` returns 0 errors.
    - Mypy strict clean: `uv run mypy --strict packages/iol-client/tests/test_refresh_token_lifecycle.py` returns 0 errors.
  </acceptance_criteria>
  <done>
    File exists with 4 standalone tests covering the 4 lifecycle paths; all 4
    pass; ruff + mypy clean; existing iol suite intact (no regressions on
    `test_client.py` or other files).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test_refresh_token_lifecycle_async.py (async mirror, 4 paths)</name>
  <files>packages/iol-client/tests/test_refresh_token_lifecycle_async.py</files>
  <read_first>
    - packages/iol-client/tests/test_async_client.py:116-200 (analog exacto: `test_async_refresh_token_success_path` y siguientes — copy literal del idiom async)
    - packages/iol-client/tests/conftest.py:41-52 (autouse `_configure_async` fixture con `await aclose()` teardown)
    - packages/iol-client/src/iol_client/aio.py:240-273 (`_ensure_token()` async con `state.token_lock` double-checked locking + refresh→password fallback)
    - packages/iol-client/src/iol_client/_state.py:88 (`token_lock: asyncio.Lock | None = None` — lazy creation Phase 6 D-IOL-09)
    - packages/iol-client/tests/test_refresh_token_lifecycle.py (Task 1 output — el mirror sync de este archivo)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-07 (async mirror spec)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"test_refresh_token_lifecycle_async.py" (diferencias clave vs sync: `async def`, `await aio.<func>()`, `aio._get_default()._state`, `aio._token`/`aio._refresh_token` reads PEP 562)
    - .planning/phases/09-deferred-bug-fixes/09-RESEARCH.md §"Common Operation 1 Async test mirror with token_lock awareness" (template literal)
  </read_first>
  <behavior>
    - Tests 1-4 mirror del Task 1 sync con diferencias mecánicas: `async def test_...`, `state = aio._get_default()._state`, `await aio.get_instruments("argentina")`.
    - El `state.token_lock` (asyncio.Lock | None) se crea lazy en el primer `_ensure_token()` async; los tests NO ejercitan concurrencia (single coroutine basta) pero el flow del refresh dentro del lock es exactamente lo que valida la regression del double-checked locking (Phase 6 D-IOL-09).
    - El teardown del autouse async fixture (`conftest.py:51`) llama `await aio._get_default().aclose()` — NO bloquear con sync `close()`.
    - `pytest-asyncio` mode auto: NO requiere `@pytest.mark.asyncio` decorator (config en root pyproject.toml: `asyncio_mode = "auto"`).
  </behavior>
  <action>
    Crear `packages/iol-client/tests/test_refresh_token_lifecycle_async.py` mirror del Task 1. Header: `from __future__ import annotations`, imports `import time`, `import pytest`, `from pytest_httpx import HTTPXMock`, `from iol_client import IOLAuthError, aio` (idiom existente en `test_async_client.py:1-12`).

    Cada test mirror del sync con sustituciones mecánicas:
    1. `def test_<name>` → `async def test_async_<name>` (renombrar prefix a `test_async_` para forensic-distinct vs sync).
    2. `state = iol_client.client._get_default()._state` → `state = aio._get_default()._state`.
    3. `iol_client.get_instruments("argentina")` → `await aio.get_instruments("argentina")`.
    4. Asserts del state son idénticos: `state.token == "NEW-TOK"`, `state.refresh_token == "..."`.
    5. Las URLs y `match_content` son IDÉNTICOS al sync (mismo Bearer/grant_type protocol).

    Los 4 paths espejan exactamente: success-rotates / 401-fallback / preserve-on-omit / rotate-on-provide. Naming: `test_async_refresh_token_success_path_rotates`, `test_async_refresh_401_falls_back_to_password`, `test_async_refresh_preserves_token_when_server_omits_refresh_field`, `test_async_refresh_rotates_when_server_provides_new_field`.

    NO usar `@pytest.mark.asyncio` (mode auto — `asyncio_mode = "auto"` configurado en root `pyproject.toml`). NO modificar `conftest.py`. NO usar `monkeypatch.setattr(aio, "_token", X)` (PEP 562 shim read-only — Pattern S2).

    Token_lock: NO se ejercita explícitamente (single coroutine, mode auto). El flow del refresh dentro del lock es el behaviour bajo test — el regression check es que el path 1/2/3/4 funciona end-to-end en async sin race ni bloqueo permanente. Si el async refresh se "comió" la rotación CR-01 por race condition, el path 3 fallaría con `state.refresh_token == None` o `"NEW-TOK"` en vez de `"STABLE-REFRESH"`.
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle_async.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/iol-client/tests/test_refresh_token_lifecycle_async.py` exists.
    - File begins with `"""<module docstring describing async mirror of 4 paths>"""` + blank line + `from __future__ import annotations`.
    - Imports include `from iol_client import IOLAuthError, aio` (verified via `grep -c "from iol_client import.*aio" packages/iol-client/tests/test_refresh_token_lifecycle_async.py | grep -v '^#'` returns ≥ 1).
    - 4 async test functions present (verified via `grep -c "^async def test_async_" packages/iol-client/tests/test_refresh_token_lifecycle_async.py | grep -v '^#'` returns 4).
    - Each test uses `state = aio._get_default()._state` access (Pattern S2 async). Verified via `grep -c "aio._get_default()._state" packages/iol-client/tests/test_refresh_token_lifecycle_async.py | grep -v '^#'` returns ≥ 4.
    - Each test uses `await aio.get_instruments(...)` to trigger the refresh flow. Verified via `grep -c "await aio\.get_instruments" packages/iol-client/tests/test_refresh_token_lifecycle_async.py | grep -v '^#'` returns ≥ 4.
    - Test 2 async uses two `match_content=b"..."` mocks distinguishing refresh vs password grants. Verified semánticamente al leer el archivo: dos `httpx_mock.add_response` con `match_content=b"refresh_token=..."` y `match_content=b"username=u&password=p&..."`.
    - NO usa `@pytest.mark.asyncio` (mode auto). Verified via `grep -c "@pytest.mark.asyncio" packages/iol-client/tests/test_refresh_token_lifecycle_async.py` returns 0.
    - Test command exit 0: `uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle_async.py -x` returns 0 con 4 tests passed.
    - Full iol suite still GREEN incluyendo sync + async: `uv run pytest packages/iol-client/tests/ -x --no-header -q` returns 0.
    - Ruff clean: `uv run ruff check packages/iol-client/tests/test_refresh_token_lifecycle_async.py` returns 0 errors.
    - Mypy strict clean: `uv run mypy --strict packages/iol-client/tests/test_refresh_token_lifecycle_async.py` returns 0 errors.
  </acceptance_criteria>
  <done>
    Async mirror file exists con 4 tests pasados; ruff + mypy clean; full iol
    test suite GREEN (sync + async); tests count delta verified +4 (async) +4
    (sync from Task 1) = +8 total for Plan 09-01.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → mocked transport | Mocked credentials (`"u"`/`"p"`/`"seed-refresh-XYZ"`) used in tests are literal seeds, NEVER real tokens. RedactingFilter (Phase 8 D-10) ya activo no leakea aunque DEBUG. |
| pytest fixtures → `_ClientState.refresh_token` | Autouse `_configure_sync`/`_configure_async` precarga state.token; tests mutan refresh_token explícito. Cross-test contamination posible si teardown no resetea (Pitfall 6 RESEARCH.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | refresh_token literal en logs DEBUG | mitigate | Phase 8 RedactingFilter activo; los tests usan `"seed-refresh-XYZ"`, `"STABLE-REFRESH"`, `"OLD-REFRESH"` — literales sintéticos. Si DEBUG se activa accidentalmente durante pytest, RedactingFilter redacta. CI grep rule (Phase 8 D-27) bloquea `logging.basicConfig` en `packages/*/src/`. |
| T-09-01-02 | Tampering | Cross-test state contamination de `_state.refresh_token` | mitigate | Cada test muta `state.refresh_token` explícitamente al inicio. Autouse fixture cleanup llama `close()/aclose()` + reconfigure. Si Pitfall 6 emerge (test pasa en isolation, falla en suite), agregar `state.refresh_token = None` al teardown del conftest en defer-plan. |
| T-09-01-03 | Authentication Bypass | Tests asumen `_ensure_token()` refresh path se ejecuta cuando `state.token_expires_at == 0.0` | accept | Comportamiento documentado en `client.py:277` (Phase 6 D-IOL-10) + `aio.py:259`. Si futura edición rompe este invariante, los 4 tests fallan inmediatamente (path 1: 0 outgoing requests; path 2: solo refresh; paths 3/4: state.token sigue `None`). |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | Plan 09-01 NO instala paquetes nuevos. Toda dependencia (`pytest`, `pytest-httpx`, `pytest-asyncio`, `httpx`) ya en `uv.lock` post-Phase-8 con audit. |

Severity overall: **low** — testing-only changes; no new production surface;
existing security controls (RedactingFilter, NullHandler, CI grep rule)
intactos.
</threat_model>

<verification>
## Phase-Level Checks (Plan 09-01)

- **Test creation:** 2 new files, 8 new tests (4 sync + 4 async), all passing.
- **No source modification:** `git diff packages/iol-client/src/` returns empty
  for this commit — Plan 09-01 NO modifica `_core.py`, `client.py`, `aio.py`,
  ni `_state.py`. Verified via post-commit grep.
- **CR-01 conditional rotation preserved:** Path 3 (server omits refresh_token)
  validates the `if refresh is not None:` shell guard works correctly.
  Negative test: if anyone edits the guard accidentally (e.g., removing the
  conditional and always assigning `state.refresh_token = refresh`), path 3
  fails with `state.refresh_token is None` instead of `"STABLE-REFRESH"`.
- **Refresh→password fallback preserved (Phase 6 D-IOL-10):** Path 2 validates
  the silent fallback. If the catch block in `_ensure_token` is removed, path
  2 fails with `IOLAuthError` raised to caller.
- **Public surface zero-diff:** Phase 6 D-09 snapshot (`verification/snapshots/iol-client-surface.txt`)
  remains unchanged. Plan 09-04 valida via `verification/test_public_surface.py`.
- **Existing 760 tests baseline (Phase 8) intact:** No regression in
  `test_client.py` or `test_async_client.py` after this plan.

## Manual Checks (None)

Plan 09-01 NO requiere operator-driven verification — los tests mockeados son
completamente automatizados. Live re-verification es Plan 09-02 / 09-03
territory per D-13.
</verification>

<success_criteria>
1. Both files exist (`test_refresh_token_lifecycle.py` + `test_refresh_token_lifecycle_async.py`) with `from __future__ import annotations` headers.
2. 8 tests pass on first run: `uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle.py packages/iol-client/tests/test_refresh_token_lifecycle_async.py -x` returns 0.
3. Full iol package test suite GREEN: `uv run pytest packages/iol-client/tests/ -x --no-header -q` returns 0 (no regression vs pre-plan baseline).
4. Ruff + mypy strict clean for both new files.
5. No file modified in `packages/iol-client/src/` for this commit.
6. 1 atomic commit per D-12 with message format: `test(iol): BUG-03 refresh_token lifecycle regression tests sync + async (BUG-03)`.
</success_criteria>

<output>
Create `.planning/phases/09-deferred-bug-fixes/09-01-SUMMARY.md` when done.
Summary must document:
- Files created (2) with line counts.
- Tests added (8 = 4 sync + 4 async) with test names.
- Confirmed: no source modification (`git diff packages/iol-client/src/` empty).
- Test command output (paste pytest summary line).
- Tests count delta evidence: previous full suite count vs new.
</output>
</content>
</invoke>
