---
phase: 37
plan: 03
type: execute
wave: 2
depends_on: [37-01, 37-02]
files_modified:
  - packages/matriz-client/src/matriz_client/models.py
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/matriz-client/tests/test_models.py
  - packages/matriz-client/tests/test_decode.py
  - verification/snapshots/matriz-client-surface.txt
  - .planning/verification/matriz-client-findings.md
autonomous: true
requirements: [NOBJ-MTZ-01]

must_haves:
  truths:
    - "D-07 + F-7: DetailedPosition.report is TWO levels of open keys (contractType then symbol) and AccountReport.detailedAccountReports is ONE level — they are typed at different depths and never forced to a shared container shape."
    - "D-02: AccountReport.portfolio is float | None, a scalar leaf per D-NO-03 — it leaves the mapping axis entirely and is no longer touched by _apply_mapping_policy."
    - "REQUIREMENTS.md and ROADMAP say 'AccountReport.report'; the field is DetailedPosition.report. The plan uses the real field name so verification does not chase a field that never existed."
    - "D-07: the inner models declare ONLY scalars with direct evidence in the vendor sample; the deferred subtrees arrive as non-fatal extra divergences rather than as an invented model presented as observed (SC-1)."
    - "D-04a/D-04b: both new inner models carry the third provenance class, 'vendor-documented, UNMEASURED', naming the vendor doc line range, naming D-MATZ-33 as the reason no capture exists, and naming Phase 39 LIVE-NOBJ-01 as the destination."
    - "F-11: neither new inner model declares a mapping field, so the single-level __args__ walk in test_no_mapping_carrying_model_is_ever_a_nested_field_type stays sufficient — this is a stated constraint, not an unstated assumption."
    - "After this plan, exactly one dict-of-Any field annotation remains in matriz's models module: UnknownFrame.raw, the single documented exemption."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/models.py"
      provides: "InstrumentPositionReport and DetailedAccountReport plus the three Risk retypes"
      contains: "class InstrumentPositionReport"
    - path: "packages/matriz-client/src/matriz_client/__init__.py"
      provides: "Both new inner models on the exported surface"
      contains: "DetailedAccountReport"
    - path: ".planning/verification/matriz-client-findings.md"
      provides: "Ledger rows declaring the two rosters as unobserved, in the existing format"
      contains: "SHAPE"
    - path: "verification/snapshots/matriz-client-surface.txt"
      provides: "Regenerated surface reflecting the three retypes and the two new classes"
      contains: "InstrumentPositionReport"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "DetailedPosition.report declares a two-level mapping whose leaf the axis recursion decodes"
      pattern: "dict\\[str, dict\\[str, InstrumentPositionReport\\]\\]"
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/_core.py"
      via: "the retyped Risk fields only populate because Plan 37-01 unwrapped the vendor envelope"
      pattern: "detailedAccountReports: dict\\[str, DetailedAccountReport\\]"
  prohibitions:
    - statement: "Do not model the full two-level tree the vendor doc suggests. D-07 locks a MINIMAL disposition: the nested detailedPositions array (about twenty fields) and its detailedDailyDiff object (eight) are explicitly deferred until a real capture exists post-LIVE-MATZ-33. Shipping them would present an unobserved model as observed and falsify SC-1."
    - statement: "Do not present Primary-API.md as a live capture. It is the third provenance class, vendor-documented and UNMEASURED, and every docstring must say so in those terms."
    - statement: "Do not give the new inner models a mapping-typed field. F-11 measured that the precondition guard walks only one level of __args__, so a mapping at depth 2 would be invisible to it. Keeping the models mapping-free is the phase's answer; deepening the guard is not in scope."
    - statement: "Do not hand-edit the AUTO-GENERATED region of matriz-client-findings.md. It is append-only and round-tripped by verification/findings.py; a manual edit destroys neighbouring operator prose (the exact regression D-23 exists to prevent). Use append_finding."
    - statement: "Do not bypass the D-MATZ-33 hostname assert in main_matriz.py to obtain a real capture. The absence of one is the declared condition of this plan."
    - statement: "Do not touch tools/check_surface_types.py in this plan. The gate extension is Plan 37-04 and must land AFTER these retypes or the lint job is red between commits (RESEARCH Pitfall 3)."
---

<objective>
Type the three remaining Risk-endpoint mappings — `DetailedPosition.report` (two levels of open
keys), `AccountReport.detailedAccountReports` (one level) and `AccountReport.portfolio` (a scalar,
not a mapping at all) — at the minimal, honest depth D-07 locks, with the provenance of each
declared as vendor-documented and unmeasured.

Purpose: these three are the fields the requirement names and the ones with NO live capture. They
are the phase's test of SC-1: a model derived from a vendor document must be labelled as such,
must declare only what the document actually shows, and must let everything else arrive as a
reported divergence rather than disappear.

Output: two minimal inner models with unmeasured provenance, three retyped fields, ledger rows
declaring the unobserved rosters, and a regenerated surface snapshot.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-CONTEXT.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-RESEARCH.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-01-SUMMARY.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates the rows marked **37-03**.

| Symbol | Kind | Home | Created by |
|--------|------|------|------------|
| `parse_get_detailed_positions_response` / `parse_get_account_report_response` | envelope unwrap | `matriz_client._core` | 37-01 |
| `_parse_risk_response` | REMOVED | `matriz_client._core` | 37-01 |
| `TickPriceRange` | `_SafeModel` dataclass, `baseline` provenance | `matriz_client.models` | 37-02 |
| `_mapping_value` / `_apply_mapping_policy` / `_convert` | element-typed, self-recursing axis | `matriz_client.models` | 37-02 |
| `InstrumentDetail.tickPriceRanges` | → `dict[str, TickPriceRange]` | `matriz_client.models` | 37-02 |
| `InstrumentPositionReport` | `_SafeModel` dataclass — `instrumentInitialSize`, `instrumentFilledSize`, `instrumentCurrentSize`, all `float \| None`; `vendor-documented, UNMEASURED` provenance | `matriz_client.models` | **37-03** |
| `DetailedAccountReport` | `_SafeModel` dataclass — `settlementDate: int \| None`; `vendor-documented, UNMEASURED` provenance | `matriz_client.models` | **37-03** |
| `DetailedPosition.report` | annotation change → `dict[str, dict[str, InstrumentPositionReport]]` (TWO levels) | `matriz_client.models` | **37-03** |
| `AccountReport.detailedAccountReports` | annotation change → `dict[str, DetailedAccountReport]` (ONE level) | `matriz_client.models` | **37-03** |
| `AccountReport.portfolio` | annotation change → `float \| None`, leaves the mapping axis | `matriz_client.models` | **37-03** |
| Two `SHAPE` rows in `matriz-client-findings.md` | ledger declarations of the unobserved rosters | `.planning/verification/` | **37-03** |
| `_field_candidates_for` / `_adjudicate_field` / `_field_annotation_is_untyped_mapping` / `_FIELD_EXEMPTIONS` / `ScanResult.fields` | new gate internals | `tools/check_surface_types.py` | 37-04 |
| `packages/matriz-client/tests/test_surface_types_red.py` | NEW test module | tests | 37-04 |
| `MarketDataSnapshot.bids` / `.offers` / `.last` / `.settlement` / `.close` / `.open_interest` | read-only `@property` aliases | `matriz_client.models` | 37-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Type DetailedPosition.report at two levels from the derived minimal roster</name>
  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/src/matriz_client/__init__.py, packages/matriz-client/tests/test_models.py, packages/matriz-client/tests/test_decode.py</files>
  <read_first>
    - packages/matriz-client/documentation/Primary-API.md:1700-1795 (the enveloped `detailedPosition` sample — read the WHOLE region and DERIVE the roster from what is actually there rather than trusting any prior summary of it; note the shape is `report[contractType][symbol]` and that the innermost object carries both a `detailedPositions` array and sibling scalars)
    - packages/matriz-client/src/matriz_client/models.py:474-500 (`DetailedPosition` and `AccountReport` as they stand)
    - packages/matriz-client/src/matriz_client/models.py:94-197 (the axis AS REWRITTEN by Plan 37-02 — read the new signature; do not guess the parameter names)
    - packages/market-data-client/src/market_data_client/models.py:310-352 (`MarketDataEntries` — the closed-roster / extra-divergence DISCLOSURE paragraph; it records the COST of closing a roster and names the artifact where detection lands. Copy the form, never the content)
    - packages/matriz-client/tests/test_decode.py:460-475 (the committed `report == {}` assertion at :468)
    - packages/matriz-client/tests/test_models.py:245-255 (the committed `parsed.report == {}` assertion at :250)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-01-SUMMARY.md (which envelope disposition was ratified — it decides whether the test payloads are enveloped, flat, or both)
  </read_first>
  <behavior>
    Roster derivation (do this first, in the vendor doc, and record the result in the SUMMARY):
    - The innermost `report[contractType][symbol]` object in the sample carries three sibling
      scalars alongside the deferred `detailedPositions` array. Those three scalars — and only
      those — are the model's declared roster. Verify the names in the doc before writing them; do
      not carry them over from any summary.

    The model:
    - `InstrumentPositionReport` is a frozen `_SafeModel` dataclass whose fields are exactly those
      three scalars, each `float | None = None`. No mapping field, no list field, no nested model.
    - It inherits `from_api` / `empty` / `__bool__`; `empty()` is falsy and emits nothing.

    Two-level decoding:
    - `DetailedPosition.from_api(<the sample's detailedPosition object>)` yields a `report` whose
      outer key is the contract-type string, whose inner key is the symbol string, and whose leaf
      is an `InstrumentPositionReport` carrying the three sizes from the sample — not a raw dict at
      either level.
    - The deferred `detailedPositions` key inside that leaf emits exactly ONE non-fatal `extra`
      divergence record attributed to `InstrumentPositionReport`, and does NOT raise under
      `strict_decode=True`.
    - The divergence path locates the leaf through both open keys, so a reader can find it in the
      payload.

    Absence and malformation:
    - `DetailedPosition.from_api({"account": "x"}).report == {}` stays TRUE — the empty-mapping
      default survives the retype, so the committed assertion at `test_decode.py:468` and
      `test_models.py:250` does not flip.
    - A non-dict value on `report` still substitutes `{}`, still emits one record, and is still
      fatal under strict mode.
    - Chain safety: iterating `report` and reaching `.instrumentCurrentSize` on any leaf never
      raises for any of the payloads above.
  </behavior>
  <action>
    Derive the roster from `Primary-API.md:1700-1795` by reading it, then add
    `InstrumentPositionReport` to `models.py` immediately before `DetailedPosition`. Use matriz's
    local house style: `@dataclass(frozen=True)` with NO `slots=True`, base `_SafeModel`,
    wire-verbatim camelCase names, `float | None = None` leaves. Keep every field a scalar — F-11
    measured that the precondition guard walks a single level of `__args__`, so a mapping field on
    a model nested at depth 2 would be invisible to it, and this phase's answer is to keep the
    model mapping-free rather than deepen the guard.

    Write the provenance docstring in the THIRD provenance class D-04a establishes. State that the
    field set is vendor-documented and UNMEASURED; cite the exact `Primary-API.md` line range; state
    that no live capture exists because `matriz-client` is blocked from live runs by D-MATZ-33
    (`LIVE-MATZ-33`); state that this roster has never been observed on the wire; and state that
    undeclared keys arrive as non-fatal `extra` divergences. Add the closed-roster disclosure
    paragraph in the form `MarketDataEntries` uses: name what closing the roster COSTS (an
    undeclared key is discarded from the typed surface) and name the artifact where detection lands
    (`.planning/verification/matriz-client-findings.md`). Name the deferred subtree explicitly —
    the `detailedPositions` array and its `detailedDailyDiff` object — and name Phase 39
    `LIVE-NOBJ-01` as its destination, so the deferral is discoverable rather than forgotten.

    Retype `DetailedPosition.report` (`models.py:481`) to the two-level container
    `dict[str, dict[str, InstrumentPositionReport]]`, keeping `field(default_factory=dict)`. This is
    the shape that requires the axis recursion Plan 37-02 built; do NOT flatten it to one level and
    do NOT force it to share a container shape with `detailedAccountReports`, which is genuinely one
    level (F-7).

    Export `InstrumentPositionReport` from `__init__.py` in the alphabetical position of both the
    models import block and `__all__`.

    Add the decode tests to `test_models.py` and `test_decode.py`, deriving the payload from the
    vendor doc line range and carrying a one-line provenance comment above it naming that range and
    labelling it vendor-documented. Shape the payload to match the envelope disposition Plan 37-01
    ratified, reading `37-01-SUMMARY.md` for the option id rather than assuming.

    Add a short comment where `DetailedPosition.report` is declared recording the REQUIREMENTS.md /
    ROADMAP naming slip: both documents say `AccountReport.report`, but `AccountReport` has no such
    field and never did — the field is `DetailedPosition.report`. This keeps a later verification
    pass from hunting a field that does not exist.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/test_models.py packages/matriz-client/tests/test_decode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
    - `uv run mypy packages/matriz-client/src` reports success.
    - `grep -c 'InstrumentPositionReport' packages/matriz-client/src/matriz_client/models.py` returns
      at least 3 (class statement, the `report` annotation, and the docstring cross-reference).
    - `uv run python -c "import matriz_client, dataclasses; print(sorted(f.name for f in dataclasses.fields(matriz_client.InstrumentPositionReport)))"`
      prints exactly the three scalar names derived from the vendor doc, and that list is quoted in
      the SUMMARY next to the `Primary-API.md` line range it came from.
    - `uv run python -c "import matriz_client, dataclasses, typing; h=typing.get_type_hints(matriz_client.InstrumentPositionReport); print([n for n,t in h.items() if typing.get_origin(t) is dict])"`
      prints an empty list — the F-11 mapping-free constraint holds.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_decode.py -k extra -q` exits 0.
    - `uv run python tools/check_decode_intactness.py` exits 0.
  </acceptance_criteria>
  <done>`DetailedPosition.report` decodes the vendor sample into models at depth two, the deferred
  subtree arrives as one non-fatal `extra` record, the roster is the one actually present in the
  cited line range, and the docstring says in plain terms that nobody has ever seen this on the wire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type detailedAccountReports at one level and demote portfolio to a scalar</name>
  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/src/matriz_client/__init__.py, packages/matriz-client/tests/test_models.py, packages/matriz-client/tests/test_decode.py, verification/snapshots/matriz-client-surface.txt</files>
  <read_first>
    - packages/matriz-client/documentation/Primary-API.md:1815-1900 (the enveloped `accountData` sample — derive the `detailedAccountReports` inner roster by reading it; note the entry carries two nested open-keyed objects plus one scalar, and note `"portfolio":60240` sits at the `accountData` level, not inside the reports)
    - packages/matriz-client/documentation/Primary-API.md:1700-1710 (the `"totalMarketValue":60240` for the SAME account — the corroboration that `portfolio` is a market value, not an object)
    - packages/matriz-client/src/matriz_client/models.py:485-501 (`AccountReport` as it stands, with the two untyped mapping fields adjacent)
    - packages/matriz-client/tests/test_decode.py:465-475 (the `portfolio == {}` and `detailedAccountReports == {}` assertions at :470-471 — these DO flip for portfolio)
    - packages/matriz-client/tests/test_models.py:253-262 (the `parsed.portfolio == {}` and `parsed.detailedAccountReports == {}` assertions at :258-259)
    - verification/snapshots/matriz-client-surface.txt (the `AccountReport` and `DetailedPosition` rows — read the header: the file forbids hand-editing and names the regen script)
  </read_first>
  <behavior>
    `DetailedAccountReport` (one level):
    - A frozen `_SafeModel` dataclass whose declared roster is the scalar(s) with direct evidence
      in the cited sample. The two nested open-keyed objects the sample also shows are DEFERRED per
      D-07 and must not be modelled.
    - `AccountReport.from_api(<the sample's accountData object>).detailedAccountReports` is a dict
      whose single key maps to a `DetailedAccountReport`, not a raw dict — ONE level of open keys,
      not two.
    - The deferred sibling keys inside that entry emit non-fatal `extra` divergence records
      attributed to `DetailedAccountReport` and do not raise under `strict_decode=True`.
    - `AccountReport.from_api({"accountName": "x"}).detailedAccountReports == {}` stays TRUE.

    `portfolio` (scalar, D-02):
    - `AccountReport.portfolio` decodes the sample value `60240` to `60240.0`, silently widened by
      the walker's float arm with no divergence record.
    - `AccountReport.from_api({"accountName": "x"}).portfolio is None` — NOT `{}`. This inverts the
      committed assertions at `test_decode.py:470` and `test_models.py:258`; both flip.
    - `portfolio` no longer passes through the mapping axis at all: after this change,
      `_apply_mapping_policy` visiting an `AccountReport` touches exactly one field,
      `detailedAccountReports`.
    - A non-numeric `portfolio` value emits one `type` record expecting `float`, and is fatal under
      strict mode — the same treatment every other scalar leaf gets.

    Surface:
    - The regenerated snapshot's `AccountReport` row shows `detailedAccountReports` as the typed
      one-level mapping and `portfolio` as `float | None = None`; the `DetailedPosition` row shows
      the two-level `report`; and rows exist for both new classes.
  </behavior>
  <action>
    Derive the `detailedAccountReports` inner roster by reading `Primary-API.md:1815-1900`, then add
    `DetailedAccountReport` to `models.py` immediately before `AccountReport`, in matriz's local
    house style. Keep it mapping-free and list-free for the same F-11 reason as Task 1, and keep
    every field a `| None = None` scalar so `test_null_object.py`'s `_perturb` dispatcher handles it
    on its `cur is None` branch (RESEARCH Pitfall 5).

    Give it the same `vendor-documented, UNMEASURED` provenance docstring form as Task 1 — the line
    range for this sample, the D-MATZ-33 reason no capture exists, the never-observed statement, the
    non-fatal `extra` behaviour, and the closed-roster cost disclosure pointing at
    `.planning/verification/matriz-client-findings.md`. Name the deferred nested objects explicitly
    and name Phase 39 `LIVE-NOBJ-01` as their destination.

    Retype `AccountReport.detailedAccountReports` (`models.py:495`) to `dict[str, DetailedAccountReport]`
    — ONE level. Do not mirror `report`'s two-level shape here; F-7 measured the asymmetry and
    forcing a shared depth would fabricate a level of keys the vendor sample does not show.

    Retype `AccountReport.portfolio` (`models.py:496`) to `float | None = None`, replacing the
    `field(default_factory=dict)` with a plain `None` default. Record the D-02 evidence in a short
    comment: the vendor sample shows a number at `Primary-API.md:1894`, and the same value appears
    as `totalMarketValue` for the same account in the detailed-position sample, which is what makes
    "account market value" the reading rather than "an object we failed to model".

    Export `DetailedAccountReport` from `__init__.py` in both alphabetical positions.

    Flip only the assertions whose truth actually changed. The two `portfolio == {}` assertions
    become `is None`. The `detailedAccountReports == {}` assertions stay true and stay as written.
    Add the sample-driven decode tests with provenance comments naming the vendor line range and
    labelling it vendor-documented, shaped to the envelope disposition Plan 37-01 ratified.

    Add a test asserting that `_apply_mapping_policy` now touches exactly one field on
    `AccountReport` — that is the executable form of "portfolio left the mapping axis" and it is
    what stops a future reader from re-adding it.

    Regenerate the surface snapshot with `uv run python verification/regen_snapshots.py` and commit
    the diff in the same commit as the source change. Never hand-edit that file.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
    - `uv run mypy packages/matriz-client/src` reports success.
    - `uv run python -c "import matriz_client; r=matriz_client.AccountReport.from_api({'accountName':'x'}); print(r.portfolio, r.detailedAccountReports)"`
      prints `None {}`.
    - `uv run python -c "import matriz_client, typing; print(typing.get_type_hints(matriz_client.AccountReport)['portfolio'], typing.get_type_hints(matriz_client.DetailedPosition)['report'])"`
      shows `portfolio` as an optional float and `report` as the two-level mapping.
    - `grep -nE '^\s+\w+: dict\[str, Any\]' packages/matriz-client/src/matriz_client/models.py`
      returns exactly one line, and that line is `UnknownFrame`'s `raw` field.
    - `grep -c 'DetailedAccountReport' verification/snapshots/matriz-client-surface.txt` returns at
      least 2, and `grep -c 'InstrumentPositionReport' verification/snapshots/matriz-client-surface.txt`
      returns at least 2.
    - `uv run python tools/check_decode_intactness.py`, `uv run python tools/check_uniform_structure.py`,
      `uv run python tools/surface_parity.py` and `uv run python tools/check_surface_types.py` all exit 0.
    - `uv run ruff check packages/matriz-client` and `uv run ruff format --check packages/matriz-client` exit 0.
  </acceptance_criteria>
  <done>Both remaining Risk mappings are typed at their real depths, `portfolio` is a scalar that no
  longer reaches the mapping axis, the committed `{}` assertions that became false are inverted, and
  the only untyped mapping field left in matriz's models module is `UnknownFrame.raw`.</done>
</task>

<task type="auto">
  <name>Task 3: Declare the two unobserved rosters in the findings ledger</name>
  <files>.planning/verification/matriz-client-findings.md</files>
  <read_first>
    - .planning/verification/matriz-client-findings.md:1-40 (the ART block, the class vocabulary comment listing SHAPE / AUTH / ERROR-MAP / PARAM / SYNC-ASYNC-DRIFT / NO-DATA / ANTI-BOT, the status lifecycle comment, the Index table, and the AUTO-GENERATED markers)
    - .planning/verification/matriz-client-findings.md § "Detalle por hallazgo" for an existing SHAPE row with terminal status (the section shape a new row must match)
    - verification/findings.py:583-650 (`append_finding`'s full signature and its invariants — idempotent by `fid`, human-status preservation, the single-line `title` guard, and the `class_` / `status` membership validation)
    - verification/test_findings_append_only.py:1-40 (why a hand edit is forbidden: a new-fid append re-serialises the whole file, and before D-23 that silently destroyed every neighbour's operator prose)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-CONTEXT.md § D-04b (the two-artifact provenance format: docstring plus ledger row, and the instruction NOT to invent a new record format)
  </read_first>
  <action>
    Read the current maximum finding id with
    `uv run python -c "from verification.findings import max_existing_fid; print(max_existing_fid('matriz-client'))"`
    and allocate the next two sequential ids from it. Do not guess.

    Append two rows by calling `verification.findings.append_finding` — never by editing the
    AUTO-GENERATED region. Use `class_="SHAPE"` and a terminal `status` from the existing lifecycle
    (`NO-FIX` for a roster that is declared-but-unobserved and has no in-cycle fix available),
    `surface="sync"` to match the existing rows, and `idempotent_by_title=True` so a re-run does not
    duplicate the row.

    Row one covers `DetailedPosition.report` / `InstrumentPositionReport`. Row two covers
    `AccountReport.detailedAccountReports` / `DetailedAccountReport`. For each, the `expected` field
    states the declared roster and cites the `Primary-API.md` line range it came from; the `actual`
    field states plainly that no live observation exists; and the `diff` field names the blocking
    cause `LIVE-MATZ-33` (the D-MATZ-33 remarkets hostname assert at `main_matriz.py:2547-2551`, not
    bypassed) and the named destination Phase 39 `LIVE-NOBJ-01`. Keep every `title` on one line —
    `append_finding` raises on an embedded newline by design (CR-02).

    After appending, diff the file and confirm that every pre-existing finding's operator prose is
    byte-identical apart from the ART timestamp refresh, which the contract permits. If any
    neighbour lost a `Classification:` / `Rationale:` / `Resolution:` bullet, revert the file and
    report it — that is the D-23 regression, not an acceptable cost.
  </action>
  <verify>
    <automated>uv run pytest -q verification/test_findings_append_only.py verification/test_finding_count_consistency.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q verification/test_findings_append_only.py verification/test_finding_count_consistency.py verification/test_findings_dedupe_by_title.py` exits 0.
    - The Index table of `.planning/verification/matriz-client-findings.md` has exactly two more
      rows than before this task, both `SHAPE` with a terminal status.
    - `grep -c 'LIVE-MATZ-33' .planning/verification/matriz-client-findings.md` returns at least 2.
    - `grep -c 'LIVE-NOBJ-01' .planning/verification/matriz-client-findings.md` returns at least 2.
    - `git diff .planning/verification/matriz-client-findings.md` shows additions plus the ART
      timestamp line only — no pre-existing finding body is modified or removed.
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` still exits 0.
  </acceptance_criteria>
  <done>Both unobserved rosters are declared in the existing ledger, in the existing vocabulary,
  through the programmatic writer, with the blocking cause and the named destination recorded — and
  no neighbouring finding lost a byte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Primary API Risk endpoints → `from_api` → the two-level mapping axis | Untrusted vendor payload with attacker- or vendor-controlled key names at two nesting levels |
| Vendor documentation → the declared model roster | A document is being used as the source of a type contract that no measurement has confirmed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11 | Spoofing | Vendor doc presented as observed evidence | mitigate | The third provenance class, `vendor-documented, UNMEASURED`, is stated in each docstring together with the line range, the D-MATZ-33 blocking reason, and the explicit sentence that this roster has never been seen on the wire (SC-1) |
| T-37-12 | Information disclosure (loss) | Closing rosters derived from a document | mitigate | Undeclared keys are DISCARDED from the typed surface but reported as non-fatal `extra` divergences; the cost is disclosed in each docstring and the detection artifact is named |
| T-37-13 | Denial of Service | Attribute chains over two levels of vendor-controlled keys | mitigate | Every leaf is a `_SafeModel` with `| None` scalars; a non-dict container substitutes `{}` at either level; no chain raises regardless of what the vendor sends |
| T-37-14 | Tampering | A malformed `portfolio` value silently reading as an amount | mitigate | As a `float` leaf it now goes through the walker's float arm, which reports a `type` divergence and is fatal under strict mode — previously any non-dict value silently became `{}` |
| T-37-15 | Repudiation | Unobserved rosters vanishing from the record | mitigate | Two `SHAPE` ledger rows appended through `verification.findings.append_finding`, naming `LIVE-MATZ-33` as cause and Phase 39 `LIVE-NOBJ-01` as destination; the append-only guard is re-run to prove no neighbour was destroyed |
| T-37-16 | Elevation of privilege | Pressure to bypass D-MATZ-33 to obtain a capture | mitigate | The hostname assert is never bypassed; the absence of a capture is the declared condition of the plan and is recorded rather than worked around |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `uv.lock` untouched. 37-RESEARCH's Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
- `uv run mypy packages/matriz-client/src` reports success.
- All four repo gates exit 0.
- `uv run pytest -q verification/test_findings_append_only.py verification/test_finding_count_consistency.py verification/test_findings_dedupe_by_title.py` exits 0.
- `grep -nE '^\s+\w+: dict\[str, Any\]' packages/matriz-client/src/matriz_client/models.py` returns
  exactly one line and it is `UnknownFrame`'s field.
- `git diff --numstat packages/matriz-client/src/matriz_client/_decode.py` produces no output.
</verification>

<success_criteria>
- `DetailedPosition.report` is a two-level typed mapping; `AccountReport.detailedAccountReports` is
  a one-level typed mapping; `AccountReport.portfolio` is `float | None`.
- Both new inner models declare only scalars evidenced in the cited vendor line ranges and label
  their provenance as vendor-documented and unmeasured.
- Deferred subtrees arrive as non-fatal `extra` divergences, not as invented models.
- Two ledger rows record the unobserved rosters with the blocking cause and named destination.
- Exactly one untyped mapping field annotation remains in `models.py`: `UnknownFrame.raw`.
</success_criteria>

<output>
Create `.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-03-SUMMARY.md` when done.
Record the two derived rosters verbatim next to the `Primary-API.md` line ranges they came from,
and record the new `_safemodel_classes()` count — Plan 37-05 raises the roster floor to it.
</output>
