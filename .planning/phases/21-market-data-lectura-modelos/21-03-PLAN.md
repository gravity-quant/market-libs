---
phase: 21-market-data-lectura-modelos
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - packages/market-data-client/src/market_data_client/client.py
  - packages/market-data-client/tests/test_with_options.py
  - packages/market-data-client/tests/test_client.py
autonomous: true
requirements: [MD-01]

must_haves:
  truths:
    - "Client.get_market_data / get_latest / get_latest_batch dispatch authenticated GET/POST reads and return SafeModel results"
    - "with_options(max_retries=N) returns a shared-view clone (shares _state) whose calls use max_attempts = N+1"
    - "A view's close() is a no-op (does not tear down the parent transport)"
    - "Authenticated 401 → clear token → re-auth exactly once → retry → succeed; persistent 401 re-raises"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/client.py"
      provides: "with_options + _validate_max_retries + _max_retries/_is_view slots + 3 read methods + max_attempts threading + module shims"
      contains: "def with_options"
    - path: "packages/market-data-client/tests/test_with_options.py"
      provides: "sync retry-propagation-by-request-count test (Pitfall 1 regression)"
      contains: "def test_"
    - path: "packages/market-data-client/tests/test_client.py"
      provides: "D-10 sync 401 sequences + end-to-end param/body/Bearer serialization"
      contains: "def test_"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/_transport.py"
      via: "req.extensions['max_attempts'] = self._max_retries + 1 consumed at _transport.py:169"
      pattern: "extensions\\[.max_attempts.\\]"
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "read methods call build_*_request + parse_*_response"
      pattern: "_core\\.build_"
---

<objective>
Extend the sync `Client` (`client.py`) with the D-08 `with_options(max_retries=N)`
shared-view-clone (iol Phase-13 pattern), the three public read methods (`get_market_data`,
`get_latest`, `get_latest_batch`) + their module-level shims, and — THE LOAD-BEARING STEP —
thread `req.extensions["max_attempts"] = self._max_retries + 1` into BOTH `_request` and
`_send_auth_request` (D-08). Add permanent D-10 regression tests for the authenticated
`401 → clear token → re-auth once → retry → succeed` path and the persistent-401 re-raise, plus
the Pitfall-1 retry-propagation-by-count regression. The sync D-09 header precedence is ALREADY
CORRECT (`client.py:234-238` sets Authorization AFTER spreading spec.headers) — this plan uses it
as the reference the async surface (Plan 04) aligns to; do NOT change it.

Purpose: Make `with_options` real on the sync surface (without the extension threading it is a
silent no-op) and surface the read API. Output: extended `client.py`, `tests/test_with_options.py`,
extended `tests/test_client.py`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-market-data-lectura-modelos/21-CONTEXT.md
@.planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md
@.planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md
@.planning/phases/21-market-data-lectura-modelos/21-02-SUMMARY.md
</context>

## Artifacts this phase produces

See `21-01-PLAN.md` "Artifacts this phase produces" for the full symbol list. This plan
creates/modifies (sync surface): `with_options`, `_validate_max_retries`, `_max_retries` +
`_is_view` slots, `get_market_data`/`get_latest`/`get_latest_batch` (method + module shim), and
`req.extensions["max_attempts"]` threading in `client.py`; `tests/test_with_options.py`;
extends `tests/test_client.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing sync with_options + D-10 401 tests (RED)</name>
  <read_first>
    - packages/market-data-client/tests/test_client.py:32-52 (_BASE/_TOKEN_URL constants + _token_posts helper + anonymous-vs-authenticated assertion shape)
    - packages/market-data-client/tests/conftest.py (autouse fixtures + NEVER_EXPIRES; force token_expires_at=0.0 to exercise the grant/401 path)
    - .planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md (§Pitfall 1 retry-count, §Pitfall 5 assert-by-count, Code Examples 401 pattern)
    - .planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md ("tests/" section — D-10/D-11)
  </read_first>
  <behavior>
    - test_with_options.py (sync): a Client (or default) with with_options(max_retries=5) whose GET /marketdata is met by 6 transient 503 responses issues exactly 6 outgoing marketdata requests (max_retries+1); a max_retries=0 view issues exactly 1. Assert by COUNT of marketdata requests, not ordering.
    - test_with_options.py: view shares parent _state (view._state is parent._state) and view.close() does NOT close the parent transport (parent still usable afterward).
    - test_with_options.py: with_options(max_retries=-1) and with_options(max_retries=True) raise ValueError.
    - test_client.py (extend): authenticated get_market_data with token_expires_at fresh, a 401 then a 200 queued → returns the 200 result AND exactly ONE token POST fires after the 401 (re-auth once).
    - test_client.py (extend): two consecutive 401s → raises MarketDataAuthError with exactly ONE re-auth token POST (persistent-401 re-raise, no infinite loop).
    - test_client.py (extend): a successful authenticated get_market_data sends Authorization: Bearer <token> and encodes bool filters as "true"/"false" and drops None optionals in req.url.params.
  </behavior>
  <action>
    Create `packages/market-data-client/tests/test_with_options.py` and add cases to
    `packages/market-data-client/tests/test_client.py`. Import `Client` and the new methods
    (RED: methods/with_options not yet present). Reuse the existing `_BASE`/`_TOKEN_URL`
    constants and the `_token_posts(httpx_mock)` helper pattern. For retry-count tests queue
    N+1 `httpx_mock.add_response(status_code=503, ...)` for `{_BASE}/marketdata` and count
    `[r for r in httpx_mock.get_requests() if r.url.path == "/api/marketdata"]`. For the 401
    sequence, seed a token grant response at `_TOKEN_URL` and assert `len(_token_posts(...)) == 1`
    after the 401 (assert by COUNT — Pitfall 5). Rely on conftest autouse seeding; force
    `token_expires_at=0.0` via `market_data_client.configure(...)` only where the grant path must
    fire. Do NOT construct un-torn-down default clients (Pitfall 4).
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options.py packages/market-data-client/tests/test_client.py -q 2>&1 | grep -Eiq "error|failed|attributeerror|no attribute" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/market-data-client/tests/test_with_options.py` exists with a retry-count test asserting `== max_retries + 1` outgoing marketdata requests.
    - `test_client.py` contains new D-10 cases asserting exactly one token POST after a 401 and a persistent-401 `pytest.raises(MarketDataAuthError)`.
    - The suite fails now because `with_options` / `get_market_data` do not yet exist — confirms RED.
  </acceptance_criteria>
  <done>Sync with_options + D-10 401 + end-to-end serialization tests exist and fail because the client surface is not yet extended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add with_options plumbing + max_attempts threading to client.py (GREEN)</name>
  <read_first>
    - packages/iol-client/src/iol_client/client.py:86-102 (_validate_max_retries — COPY verbatim), :128 (__slots__), :140-177 (__init__ kwarg + set _max_retries/_is_view), :202 (view-aware close guard), :257-259 (_ensure_http_client uses self._max_retries+1), :264-329 (with_options), :356 + :465 (max_attempts threading in _send_auth_request + _request)
    - packages/market-data-client/src/market_data_client/client.py (current — full file; note _DEFAULT_MAX_ATTEMPTS at :71 to remove, __slots__ at :91, _ensure_http_client :148-165, _send_auth_request :167-188, _request :216-271)
    - packages/market-data-client/src/market_data_client/_transport.py:169 (consumes extensions["max_attempts"] — DO NOT change)
  </read_first>
  <action>
    In `client.py`: add a module-level `_validate_max_retries(value)` copied VERBATIM from
    `iol_client/client.py:86-102` (rejects bool, non-int, and < 0 with ValueError — duplicated
    per the no-shared-internals constraint). Change `Client.__slots__` from `("_state",)` to
    `("_is_view", "_max_retries", "_state")`. Add `max_retries: int = 2` to `__init__`, call
    `_validate_max_retries(max_retries)` FIRST, then set `self._max_retries = max_retries` and
    `self._is_view = False`. Add `with_options(self, *, max_retries: int) -> Self`: validate,
    `view = type(self).__new__(type(self))`, `view._state = self._state` (SHARE — no re-auth, no
    2nd pool), `view._max_retries = max_retries` (OVERRIDE), `view._is_view = True` (FLAG); return
    view. Prepend to `close()` the guard `if getattr(self, "_is_view", False): return`. Replace
    the `_DEFAULT_MAX_ATTEMPTS` usage in `_ensure_http_client` with
    `transport=_transport.RetryTransport(max_attempts=self._max_retries + 1)` and REMOVE the now-
    unused `_DEFAULT_MAX_ATTEMPTS` module constant + its stale Phase-21-deferral comment. THE
    LOAD-BEARING STEP (D-08, Pitfall 1): in BOTH `_request` AND `_send_auth_request`, after the
    existing `req.extensions["endpoint_name"] = spec.endpoint_name` line, add
    `req.extensions["max_attempts"] = self._max_retries + 1`. Do NOT touch the sync D-09 header
    build (`headers["Authorization"]` set AFTER spreading spec.headers is already correct).
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `Client.__slots__` equals `("_is_view", "_max_retries", "_state")` and `_validate_max_retries` is defined module-level in `client.py`.
    - Both `_request` and `_send_auth_request` set `request.extensions["max_attempts"]` (grep: `extensions\["max_attempts"\]` appears at least twice in `client.py`).
    - `_DEFAULT_MAX_ATTEMPTS` no longer appears in `client.py`.
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options.py -q` exits 0 (retry count == max_retries+1; view close no-op; ValueError on bad input).
  </acceptance_criteria>
  <done>with_options shared-view clone, view-aware close, _validate_max_retries, and max_attempts threading in both dispatch methods are present; with_options tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add sync read methods + module shims (GREEN)</name>
  <read_first>
    - packages/market-data-client/src/market_data_client/client.py:277-287 (get_health method shape) and :364-371 (module-level shims delegating to _get_default())
    - packages/market-data-client/src/market_data_client/_core.py (build_market_data_request / build_latest_request / build_latest_batch_request / parse_market_data_response / parse_latest_response — Plan 02)
    - packages/market-data-client/src/market_data_client/models.py (MarketDataSnapshot, LatestRequest — Plan 01)
    - packages/market-data-client/tests/test_client.py (the RED end-to-end + D-10 cases this task must satisfy)
  </read_first>
  <action>
    Add three public methods on `Client` mirroring the `get_health` shape (build spec →
    `self._request(spec)` → `_core.parse_*_response(resp)`): `get_market_data(self, *,
    market_id=None, prefix=None, active=None, entries=None, max_staleness_seconds=None,
    with_data=None, order=None, limit=None, offset=None)` → `_core.build_market_data_request` +
    `parse_market_data_response`; `get_latest(self, *, symbol=None, market_id=None, entries=None)`
    → `_core.build_latest_request` + `parse_latest_response`; `get_latest_batch(self,
    latest_request)` (accepts a `LatestRequest`) → `_core.build_latest_batch_request` +
    `parse_latest_response`. Type the return values with the models from Plan 01. Add matching
    module-level shims (`get_market_data`, `get_latest`, `get_latest_batch`) delegating to
    `_get_default()` (mirror the `get_health` shim at :364-371). These read methods flow through
    `_request`, which already injects the Bearer (authenticated=True specs) and threads
    max_attempts (Task 2), so `with_options(...).get_market_data(...)` inherits the per-call cap.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_client.py packages/market-data-client/tests/test_with_options.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def get_market_data" packages/market-data-client/src/market_data_client/client.py` (method) and a module-level `get_market_data` shim delegating to `_get_default()` also exists.
    - The D-10 sync 401 tests and the end-to-end Bearer/param-encoding tests in `test_client.py` pass.
    - `uv run --package market-data-client pytest packages/market-data-client -q` exits 0 (full package suite).
    - `uv run mypy packages/market-data-client/src` exits 0 and `uv run ruff check packages/market-data-client` exits 0.
  </acceptance_criteria>
  <done>Three sync read methods + module shims exist and dispatch authenticated reads; test_client.py (D-10 + end-to-end) green; full package suite + mypy + ruff green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (authenticated) | Bearer token injected into every authenticated read request |
| caller max_retries kwarg → transport | untrusted retry cap validated before use |

## STRIDE Threat Register (ASVS L1, block-on-high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Denial of Service | unbounded retries via bad max_retries | mitigate | `_validate_max_retries` rejects bool/non-int/negative with ValueError; max_attempts = N+1 is bounded and idempotent-gated. Severity: low. |
| T-21-03-02 | Elevation of Privilege | view sharing parent _state / token | accept | with_options intentionally shares _state (cached token + transport) per D-08; a view is the same trust principal as its parent. Severity: low. |
| T-21-03-03 | Information Disclosure | Bearer token in logs on the new read path | mitigate | Existing RedactingFilter + `__repr__` redaction (Phase 20); add no raw-header/token log statements. Severity: low. |
| T-21-03-04 | Spoofing | stale token replayed after 401 | mitigate | Exactly-once re-auth carve-out clears the cached token and re-grants before retry; persistent 401 re-raises (no loop). Severity: low. |
| T-21-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (zero new deps). No checkpoint required. |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client -q` green (full package suite incl. new test_with_options.py + extended test_client.py).
- `uv run mypy packages/market-data-client/src` clean (explicit package path).
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` green.
- Manual grep: `req.extensions["max_attempts"]` present in both `_request` and `_send_auth_request`.
</verification>

<success_criteria>
Sync `with_options(max_retries=N)` is a real shared-view clone whose calls use max_attempts=N+1
(retry count verified by request count), view close is a no-op, the three read methods + shims
dispatch authenticated reads returning SafeModel results, and the D-10 401 sequences pass; all
four gates green against the package path.
</success_criteria>

<output>
Create `.planning/phases/21-market-data-lectura-modelos/21-03-SUMMARY.md` when done.
</output>
