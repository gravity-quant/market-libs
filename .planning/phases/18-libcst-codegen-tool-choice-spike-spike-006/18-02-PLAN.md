---
phase: 18-libcst-codegen-tool-choice-spike-spike-006
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/README.md
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/experiment.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/async_to_sync.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/import_normalizer.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/docstring_localizer.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/import_direction_normalizer.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/suppressors.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/diff_vs_current_client.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/run_log.txt
  - .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
autonomous: true
requirements: [CODEGEN-01]
user_setup: []

must_haves:
  truths:
    - "Item 1 (GO-det): the generated sync output is diffed against the CURRENT (v1.2-head) hand-written client.py, and the residual divergence is captured honestly — PASS iff byte-identical, FAIL otherwise."
    - "Item 4 (GO-det): `ruff check` on the generated output is clean, including single-line import-order (I001) and ASYNC1xx."
    - "Item 6 (GO-det): the ámbito mocked pytest-httpx suite runs green against the generated client.py in a sandbox copy, with no circular self-import."
    - "Item 2: B8 identity holds on the generated module (`mod._raise_for_response is aio._raise_for_response is _core.raise_for_response`)."
    - "Items 3/5/7: `ruff format --check`, `mypy --strict`, and `lint-imports` are clean/intact against the generated output."
    - "Item 9 (new): each CSTTransformer subclass is a pure CSTNode→CSTNode function (no self-accumulation, no I/O); cross-module orchestration lives only in the impure driver."
    - "The content-absent-from-source obstacle (Q1: _validate_max_retries def + load_dotenv absent from aio.py) is instrumented and recorded as the decisive item-1/item-6 evidence — never bypassed by editing aio.py or reading client.py as a donor."
  artifacts:
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/experiment.py"
      provides: "Impure driver: parse aio.py → apply pure transformers → emit → item-1 diff → item-2 B8 → item-9 purity assertion"
      min_lines: 40
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/suppressors.py"
      provides: "D-03 suppression: remove aclose() delegator, WR-07 ResourceWarning block, import warnings"
      contains: "RemovalSentinel"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py"
      provides: "The generated sync transport shell (item-1 subject)"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/diff_vs_current_client.txt"
      provides: "Freshly regenerated item-1 baseline vs CURRENT client.py (SPIKE-005 diff is stale — Pitfall 1)"
    - path: ".planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/FINDING.md"
      provides: "Per-item 1/2/3/4/5/6/7/9 verdicts + Q1 content-absence instrumentation + item-9 purity scope note"
  key_links:
    - from: "001a/experiment.py"
      to: "packages/ambito-financiero-client/src/ambito_financiero_client/aio.py"
      via: "cst.parse_module reads aio.py as the SOLE source (aio.py NOT edited — D-03)"
      pattern: "parse_module"
    - from: "001a/output/client_generated.py"
      to: "packages/ambito-financiero-client/src/ambito_financiero_client/client.py"
      via: "diff -u item-1 gate vs current hand-written client.py"
      pattern: "diff"
    - from: "001a/transformers/suppressors.py"
      to: "aio.py aclose()/WR-07 block/import warnings"
      via: "cst.RemovalSentinel.REMOVE during sync emission (D-03)"
      pattern: "RemovalSentinel"
---

<objective>
Author the genuinely-new work of the spike: the pure libcst `CSTTransformer` suite + impure driver
that transforms the un-migrated ámbito `aio.py` into a candidate sync `client.py`, and run the 3
GO-determining D-RIGOR-02 items (1 byte-identical, 4 `ruff check` import-order, 6 mocked-suite-green)
plus items 2/3/5/7 and the new item 9 (transformer purity) against the generated output.

This is where the spike's answer lives. Per D-02 the item-1 bar is STRICT byte-identity to the
EXISTING hand-written `client.py`; per D-03 all normalization happens at generation time and `aio.py`
source is NOT edited — the transformers must additionally SUPPRESS aio-only constructs (no sync
`close()`/`aclose()` delegator, OMIT the WR-07 `ResourceWarning` block in `configure()`,
`_REQUEST_TIMEOUT` placement matching `client.py`). RESEARCH.md Q1 flags a likely-decisive obstacle:
`_validate_max_retries` (def, client.py:41-62) and `load_dotenv` (client.py:25,30) exist in the
target but are ABSENT from the `aio.py` source — libcst can move/rewrite existing nodes but cannot
synthesize source-absent content under the un-migrated constraint. Instrument this honestly.

Purpose: Produce the captured item-1/4/6 PASS-or-FAIL transcripts — a signed NO-GO for the same
source-shape root cause SPIKE-005 found is an explicitly valid, guaranteed deliverable (D-04, D-08).
A FAIL is NOT a red test to force green: do NOT edit `aio.py`, do NOT read `client.py` as a donor to
manufacture a GO. The honest gate transcript IS the deliverable.

Output: `001a/` transformer suite, generated output, regenerated item-1 diff, and per-item FINDING.md.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-CONTEXT.md
@.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
</context>

<artifacts_this_phase_produces>
This plan adds the `001a-ambito-round-trip/` sub-experiment: the driver `experiment.py`, the
`transformers/` package (`AsyncToSync`, `ImportNormalizer`, `DocstringLocalizer`,
`ImportDirectionNormalizer`, `Suppressors`), `output/client_generated.py`, the regenerated
`diff_vs_current_client.txt` (item-1 baseline), `run_log.txt`, and `FINDING.md`. On GO these
transformer classes are promoted (Plan 03) as the Phase 19 handoff drafts (D-08).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the pure CSTTransformer suite + impure driver; emit generated client.py</name>
  <files>
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/README.md,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/experiment.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/async_to_sync.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/import_normalizer.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/docstring_localizer.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/import_direction_normalizer.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/transformers/suppressors.py,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py
  </files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (SOLE source; refs: `_validate_max_retries` import line 34, `_atransport,_core` import line 32, `_raise_for_response` alias line 38, `_REQUEST_TIMEOUT` line 49, `aclose()` line 266, WR-07 ResourceWarning block lines ~225-240, `import warnings` line 27, "asincrónico" docstrings)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (STRICT byte-identity target; refs: `_validate_max_retries` def lines 41-62, `load_dotenv` import line 25 + call line 30, `_REQUEST_TIMEOUT` line 32, `_raise_for_response` alias line 36)
    - .planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md (§Architecture Patterns 2-5; §Pattern 3 caveat; §Open Questions Q1/Q2/Q3; §Common Pitfalls 3/4/5)
    - .claude/skills/spike-findings-codegen-market-libs/SKILL.md (§integration_blueprint — B8 preservation, marker design, Anti-Pitfalls)
  </read_first>
  <behavior>
    - `AsyncToSync` strips `async`/`await`, renames `AsyncClient→Client`, `_atransport→_transport`, `aclose→close` context on nodes it visits; leaves the `_raise_for_response = _core.raise_for_response` Assign node UNTOUCHED (Pitfall 4 / B8).
    - `ImportNormalizer` reorders the non-alphabetical single-line `from ambito_financiero_client import _atransport, _core` alias list to close item-4 I001.
    - `DocstringLocalizer` rewrites `cst.SimpleString.value` for `asincrónico→sincrónico`, `AsyncClient→Client`, strips `await ` inside docstring example blocks — preserving triple-quote style + indentation.
    - `Suppressors` returns `cst.RemovalSentinel.REMOVE` for the module-level `aclose()` FunctionDef and `import warnings`; rewrites `configure()` body to OMIT the WR-07 `ResourceWarning` block (D-03).
    - Each transformer subclass is PURE: no `self.<mutable>` accumulation across nodes, no file I/O, no global reads. Cross-module orchestration (relocating `_validate_max_retries`, handling the source-absent `load_dotenv` bootstrap) lives ONLY in the impure driver `experiment.py`, never in a transformer (item 9 / Q2 class-level reading).
  </behavior>
  <action>
    Under `001a-ambito-round-trip/transformers/`, author the five pure `CSTTransformer` subclasses named
    above using libcst node rewrites (`.with_changes()`, `RemovalSentinel`/`FlattenSentinel`,
    `Module.header`) — NEVER `str.replace` (Anti-Pitfall 6). Confirm the exact node-API surface
    empirically on first run (RESEARCH A2: `RemovalSentinel.REMOVE`, `Module.header`,
    `SimpleString.value` are MEDIUM-confidence). Author the impure driver `experiment.py` that:
    `cst.parse_module`s the ámbito `aio.py` (SOLE source; do NOT edit aio.py — D-03), applies the
    transformer passes, prepends the `@generated` marker via `Module.header`, and writes
    `output/client_generated.py`. For the `ImportDirectionNormalizer` + the content-absent regions
    (`_validate_max_retries` def, `load_dotenv` import+call — Q1), the driver attempts a pure-transform
    emission and records the residual. Do NOT smuggle a `client.py`-donor read or an `aio.py` edit to
    force byte-identity — if the source-absent content cannot be produced, that residual divergence is
    the honest item-1 evidence (D-02, D-04). Keep the `_raise_for_response = _core.raise_for_response`
    Assign node verbatim (B8 / Pitfall 4). Add a smoke assertion that `cst.parse_module(aio_src).code ==
    aio_src` (lossless round-trip validates the tool — distinct from the item-1 target diff, Pitfall 3).
  </action>
  <verify>
    <automated>cd /Users/admin/development/market-libs && uv run --with 'libcst>=1.8.0,<2' python .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/experiment.py >/tmp/spike006-001a.log 2>&1; test -f .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py && git diff --exit-code packages/ambito-financiero-client/src/ambito_financiero_client/aio.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --with 'libcst>=1.8.0,<2' python .../001a/experiment.py` runs to completion and writes `output/client_generated.py`.
    - `git diff --exit-code packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` passes (aio.py source NOT edited — D-03).
    - The lossless round-trip smoke assertion (`parse_module(aio_src).code == aio_src`) passes in the run log.
    - The generated output preserves the literal `_raise_for_response = _core.raise_for_response` assignment (no thunk wrapper — B8 / Pitfall 4).
    - No transformer subclass reads a second file or accumulates node state across visits (item-9 purity, asserted in the driver).
  </acceptance_criteria>
  <done>Transformer suite + driver authored; `client_generated.py` emitted from aio.py alone (aio.py unedited); B8 alias preserved; item-9 purity holds at the class level. (D-02, D-03)</done>
</task>

<task type="auto">
  <name>Task 2: Run the item 1/2/3/4/5/6/7/9 gate on the generated output; instrument Q1; write FINDING.md</name>
  <files>
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/diff_vs_current_client.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/run_log.txt,
    .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
  </files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (item-1 diff target — CURRENT v1.2-head, NOT the stale SPIKE-005 baseline)
    - .planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-RESEARCH.md (§Validation Architecture — the 10-item automated commands; §Code Examples item-6 sandbox recipe; §Pitfall 1 stale baseline; §Open Questions Q1/Q3)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt (item-2 B8 harness + item-6 sandbox recipe to reuse)
  </read_first>
  <action>
    Run the D-RIGOR-02 items this sub-experiment owns and transcribe each into `run_log.txt`:
    (item 1, GO-det) `diff -u packages/ambito-financiero-client/src/ambito_financiero_client/client.py
    001a/output/client_generated.py` → write to `diff_vs_current_client.txt`; regenerate against the
    CURRENT client.py (Pitfall 1 — the SPIKE-005 `diff_vs_v1.1_client.txt` is stale; expect MORE than
    SPIKE-005's 10 hunks, concentrated in the `with_options` docstring 43-vs-19-line divergence).
    (item 2) `importlib`-load the generated module and assert `mod._raise_for_response is
    aio._raise_for_response is _core.raise_for_response`. (item 3) `uv run ruff format --check` on the
    output. (item 4, GO-det) `uv run ruff check` on the output (I001 import-order + ASYNC1xx). (item 5)
    `uv run mypy --strict` on the output. (item 6, GO-det) the sandbox recipe: `cp -r` ámbito to `/tmp`,
    overwrite `client.py` with the generated file, `uv run --package ambito-financiero-client pytest -q`
    — watch for the SPIKE-005 self-import failure (`ImportError: cannot import name
    _validate_max_retries from partially initialized module`); never mutate `packages/` (Anti-Pitfall 2).
    (item 7) `uv run lint-imports` (runs against `packages/`, 4 contracts). (item 9) assert transformer
    purity (no I/O, no cross-node state) as a runtime check or documented property. In `FINDING.md`,
    record each item's PASS/FAIL verdict AND explicitly instrument Q1: state whether the residual item-1
    divergence is the `_validate_max_retries` def + `load_dotenv` bootstrap (content absent from aio.py)
    and whether item-6 dies on a self-import — if so, that is the signed same-root-cause evidence
    (honest FAIL, valid per D-04/D-08). Note the item-9 purity interpretation (class-level, Q2/A3) and
    each divergent docstring region (Q3) for the operator to ratify in Plan 03's DECISION.md. Report
    verdicts truthfully; do NOT soften the gate.
  </action>
  <verify>
    <automated>cd /Users/admin/development/market-libs && test -s .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/diff_vs_current_client.txt || diff -u packages/ambito-financiero-client/src/ambito_financiero_client/client.py .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py > .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/diff_vs_current_client.txt; grep -qiE 'item 1.*(PASS|FAIL)' .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/FINDING.md && grep -qiE 'item 6.*(PASS|FAIL)' .planning/spikes/SPIKE-006-libcst-codegen-tool-choice/001a-ambito-round-trip/FINDING.md && git diff --exit-code packages/</automated>
  </verify>
  <acceptance_criteria>
    - `diff_vs_current_client.txt` is regenerated against the CURRENT `packages/.../client.py` (not the stale SPIKE-005 v1.1 baseline); its hunk count and regions are recorded — item 1 verdict is PASS iff the diff is empty (exit 0), FAIL otherwise, recorded truthfully.
    - Items 2/3/4/5/6/7/9 each have a captured command + exit-code transcript in `run_log.txt` and a PASS/FAIL verdict in `FINDING.md`.
    - Item 6 runs in a `/tmp` sandbox copy; `git diff --exit-code packages/` passes (no production mutation).
    - `FINDING.md` explicitly instruments Q1 (whether residual item-1 divergence is the source-absent `_validate_max_retries` def + `load_dotenv` bootstrap, and whether item-6 fails on a self-import) and flags the item-9 purity scope (Q2) + docstring divergences (Q3) for operator ratification.
  </acceptance_criteria>
  <done>Items 1/2/3/4/5/6/7/9 verdicts captured against generated output; item-1 baseline regenerated vs current client.py; Q1 content-absence instrumented honestly; no `aio.py` edit and no `client.py`-donor smuggling. (D-02, D-03, D-04, D-08)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local env | Ephemeral `libcst` install (gate cleared in Plan 01) drives the 001a transform |
| repo source → sandbox | ámbito source read; generated client.py exercised in a `/tmp` sandbox copy for item 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | production `packages/` source (esp. `aio.py`) | mitigate | aio.py is read-only source (D-03); item-6 uses a `/tmp` `cp -r` sandbox (Anti-Pitfall 2); `git diff --exit-code packages/` + `git diff --exit-code .../aio.py` in both tasks |
| T-18-05 | Tampering | B8 identity (`_raise_for_response` alias) | mitigate | Transformers never touch the alias Assign node; item-2 `is`-triple assertion catches a thunk-wrapper regression (Pitfall 4) |
| T-18-06 | Spoofing (false GO) | item-1/item-6 verdict integrity | mitigate | Guardrail: no `aio.py` edit, no `client.py`-donor read; honest FAIL recorded per D-04; residual divergence attributed to Q1 content-absence in FINDING.md |
| T-18-SC | Tampering/Elevation | ephemeral `libcst` install | mitigate | Inherited from Plan 01's cleared blocking legitimacy checkpoint (D-05); no dep committed |
</threat_model>

<verification>
- `001a/experiment.py` emits `client_generated.py` from `aio.py` alone; `aio.py` unedited; lossless round-trip smoke passes.
- item 1 diff regenerated vs CURRENT client.py → `diff_vs_current_client.txt`; verdict recorded truthfully.
- items 2 (B8), 3 (ruff format), 4 (ruff check I001), 5 (mypy strict), 6 (sandbox pytest), 7 (lint-imports), 9 (purity) each transcribed with PASS/FAIL.
- `git diff --exit-code packages/` clean after item-6 sandbox run.
- Q1 content-absence + Q2 purity scope + Q3 docstring divergence instrumented in FINDING.md.
</verification>

<success_criteria>
The `001a` transformer suite + driver exist and are canary-proven; the 3 GO-determining items (1/4/6)
plus items 2/3/5/7/9 have captured PASS-or-FAIL transcripts against the un-migrated ámbito canary; a
FAIL (if it occurs) is honestly attributed to the source-shape root cause, never bypassed — a valid,
guaranteed deliverable per D-04/D-08.
</success_criteria>

<output>
Create `.planning/phases/18-libcst-codegen-tool-choice-spike-spike-006/18-02-SUMMARY.md` when done.
</output>