---
phase: 40-releases-breaking-coordinados
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - packages/matriz-client/README.md
  - packages/matriz-client/pyproject.toml
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/iol-client/README.md
  - packages/iol-client/pyproject.toml
  - packages/iol-client/src/iol_client/__init__.py
  - packages/market-data-client/README.md
  - packages/market-data-client/pyproject.toml
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/tests/test_snapshot_no_data_row.py
  - packages/higyrus-client/README.md
  - packages/higyrus-client/pyproject.toml
  - packages/higyrus-client/src/higyrus_client/__init__.py
  - uv.lock
autonomous: false
requirements: [PUB-NOBJ-01]
user_setup:
  - service: github
    why: "Task 1 is a blocking scope decision only the operator can make (D-02, D-12), and Task 4 pushes a new public branch carrying 186 commits"
    dashboard_config:
      - task: "Be available to answer the blocking scope gate in Task 1 (D-02 higyrus fold, D-12 market_id/active widening) BEFORE any pyproject.toml is edited"
        location: "the executing terminal session"
      - task: "Ensure the `gh` CLI / SSH remote is authenticated for the branch push (never echo the token)"
        location: "local shell — `gh auth status`, `git remote -v`"

must_haves:
  truths:
    - "The D-02 (higyrus fold-in) and D-12 (market_id/active widening) scope questions were answered by a literal operator reply at a blocking `checkpoint:decision` that ran BEFORE the first `pyproject.toml` edit and BEFORE the single `uv lock` — per D-02, D-07 and D-12 as corrected by RESEARCH OQ-1"
    - "`packages/matriz-client/README.md` carries a `## Changelog` section that did not exist before this phase, whose first and only entry is `### v0.3.0`, documenting the four field retypes plus the envelope-unwrap behavior fix, with the six alias properties documented separately as additive — per D-03"
    - "`packages/market-data-client` reads 0.6.0 at both version sites and its `## Unreleased — BREAKING` section has been MOVED into `## Changelog` as the first `### v0.6.0` entry, with both version-pinned install commands re-pinned — per D-01"
    - "`packages/iol-client` reads 0.4.0 at both version sites and its `## Unreleased — BREAKING` section has been MOVED into `## Changelog` as the first `### v0.4.0` entry — per D-01"
    - "`packages/matriz-client` reads 0.3.0 at `pyproject.toml` and at the newly added `matriz_client.__version__` — per D-01 and D-04/OQ-4"
    - "The 15-line `# NO BUMPEAR` comment block above `market_data_client.__version__` is gone — it named this phase as the future executor and became false at bump time"
    - "No package README still claims a future phase will do the bump: the literal `Fase 40` appears in no `packages/*/README.md`, and `packages/higyrus-client/README.md` no longer names Phase 34 as its executor — per D-02 (both branches)"
    - "`uv.lock` was regenerated by a single `uv lock` run executed AFTER every `pyproject.toml` edit, registers each bumped member at its new version, passes `uv lock --check`, and churned by exactly N insertions + N deletions where N is the number of bumped packages — per D-10"
    - "`uv sync --all-packages --all-extras --dev` ran after `uv lock` and before the local CI mirror, so `market-data-client`'s dist-metadata version test reads the bumped version — RESEARCH P3"
    - "`ambito-financiero-client` and `wallets-client` have a changed version at no site — their surfaces moved additively only, which is measured-and-classified, not unchanged — per D-01 and RESEARCH P11"
    - "No file under `.github/workflows/` is modified by any commit of this phase, and `release.yml` is byte-identical between `origin/main` and `HEAD` — per the read-only workflow contract"
    - "The full `origin/main...HEAD` diff about to become public carries no JWT-shaped token, no client_secret-style assignment and no tracked `.env` file"
    - "`origin/milestone/v1.7-nobj-null-objects` resolves to the same commit as local HEAD via a plain fast-forward push of a NEWLY CREATED branch, with no force flag, no rebase and no push to `main` — per D-05 and RESEARCH P6"
  prohibitions:
    - "Never resolve D-02 or D-12 in silence, in either direction. Both are real operator decisions with a measured blast radius; the agent may not pick a branch, may not infer one from `auto_advance` or yolo mode, and may not defer either a third time without naming a concrete destination."
    - "Never run `uv lock` more than once in this phase (D-10). If the Task 1 answer changes the bump set, that answer arrives BEFORE any bump, which is the entire reason the gate is hoisted here."
    - "Never rewrite history reachable from this branch: no `--force`, no `--force-with-lease`, no rebase and no interactive history edit. Dozens of SUMMARY files across Phases 35-39 cite these SHAs by value."
    - "Never `git push origin main`. HEAD sits on local `main` today; the phase's first git operation must be `git checkout -b milestone/v1.7-nobj-null-objects`. A push to `main` would publish 186 commits to the public default branch with no PR, no CI gate and no human approval (RESEARCH P6)."
    - "Never run a bare `uv run pytest`. The root `testpaths` collects `verification/`, which carries known pre-existing failures no CI job executes; a bare run blocks the release on out-of-scope failures."
    - "Never hand-edit `uv.lock`, and never accept a lock diff wider than one version line per bumped member — a wider diff means uv re-resolved third-party dependencies."
    - "Never patch `.github/workflows/ci.yml` or `release.yml` to make a gate pass. Surface the failing step instead."
  artifacts:
    - path: "packages/matriz-client/README.md"
      provides: "the consumer-facing v0.3.0 changelog authored from scratch (D-03)"
      contains: "## Changelog"
    - path: "packages/matriz-client/pyproject.toml"
      provides: "the version string release.yml validates against the matriz tag"
      contains: "version = \"0.3.0\""
    - path: "packages/matriz-client/src/matriz_client/__init__.py"
      provides: "runtime version mirror added for convention parity (OQ-4)"
      contains: "__version__ = \"0.3.0\""
    - path: "packages/iol-client/pyproject.toml"
      provides: "the version string release.yml validates against the iol tag"
      contains: "version = \"0.4.0\""
    - path: "packages/iol-client/src/iol_client/__init__.py"
      provides: "runtime version mirror for iol-client (NOT validated by the pipeline)"
      contains: "__version__ = \"0.4.0\""
    - path: "packages/market-data-client/pyproject.toml"
      provides: "the version string release.yml validates against the market-data tag"
      contains: "version = \"0.6.0\""
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "runtime version mirror, with the stale do-not-bump comment block removed"
      contains: "__version__ = \"0.6.0\""
    - path: "packages/market-data-client/README.md"
      provides: "de-provisionalized v0.6.0 changelog entry plus re-pinned install commands"
      contains: "### v0.6.0"
    - path: "packages/iol-client/README.md"
      provides: "de-provisionalized v0.4.0 changelog entry"
      contains: "### v0.4.0"
    - path: "uv.lock"
      provides: "workspace-member version registration for every bumped member, single refresh"
      contains: "name = \"matriz-client\""
  key_links:
    - from: "packages/matriz-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "release.yml:47 awk-reads the FIRST ^version line and hard-fails the build unless it equals the version captured from the tag matriz-client-v0.3.0"
      pattern: "version = \"0.3.0\""
    - from: "packages/iol-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "same awk version-match gate, run against the tag iol-client-v0.4.0"
      pattern: "version = \"0.4.0\""
    - from: "packages/market-data-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "same awk version-match gate, run against the tag market-data-client-v0.6.0"
      pattern: "version = \"0.6.0\""
    - from: "packages/market-data-client/pyproject.toml"
      to: "uv.lock"
      via: "the single `uv lock` run re-registers every workspace member; `uv lock --check` is CI check 1 of 15 (ci.yml:32-33)"
      pattern: "name = \"market-data-client\""
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/tests/test_version_metadata.py"
      via: "test_dunder_version_matches_installed_distribution_metadata compares __version__ against importlib.metadata; only `uv sync` after `uv lock` keeps it green locally (RESEARCH P3)"
      pattern: "__version__ = \"0.6.0\""
    - from: "packages/matriz-client/README.md"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "the v0.3.0 changelog documents the four retypes actually implemented in models.py (AccountReport.portfolio, InstrumentDetail.tickPriceRanges, DetailedPosition.report, AccountReport.detailedAccountReports)"
      pattern: "tickPriceRanges"
---

<!--
  Comment-text discipline allowlist. Each literal below is negative-grepped by an
  acceptance criterion against a REPO file (never against this PLAN). They must appear
  verbatim in the actions so the executor can find and delete the exact stale text.
-->
<!-- planner-discipline-allow: NO BUMPEAR -->
<!-- planner-discipline-allow: Fase 40 -->
<!-- planner-discipline-allow: Unreleased -->
<!-- planner-discipline-allow: Phase 34 -->
<!-- planner-discipline-allow: sin publicar todavía -->
<!-- planner-discipline-allow: market-data-client-v0.5.0 -->
<!-- planner-discipline-allow: pendiente de bump -->
<!-- planner-discipline-allow: logging.basicConfig -->

<objective>
Fix the bump set with a blocking operator decision, then perform every REVERSIBLE step of the
coordinated v1.7 breaking release: author `matriz-client`'s changelog from scratch, promote
`iol-client`'s and `market-data-client`'s pre-written `## Unreleased — BREAKING` blocks into dated
changelog entries, bump every version site of the final package set, refresh `uv.lock` exactly once,
re-sync the environment, mirror all four CI job bodies locally, re-run the credential scan over the
diff that is about to become public, and fast-forward push a NEWLY CREATED release branch.

Purpose: everything in this plan can be amended or reverted with a commit. Isolating it from the
irreversible publish (plans 40-02 and 40-03) means a failed prep costs nothing. The scope gate sits
at the very top because an "approve" on either open question changes the bump set — and answering it
any later would force a second `uv lock`, contradicting D-10 (RESEARCH P1 / OQ-1).

Output: a pushed `milestone/v1.7-nobj-null-objects` whose tip carries the per-package bump commits
and the single lock-refresh commit, with every local CI gate green.

**Three packages locked, a fourth conditional.** `market-data-client` 0.5.0 → 0.6.0 (Phase 36),
`iol-client` 0.3.0 → 0.4.0 (Phase 38), `matriz-client` 0.2.0 → 0.3.0 (Phase 37) — all three
source-breaking, all three locked by D-01. `higyrus-client` 0.2.0 → 0.3.0 is conditional on the
Task 1 answer to D-02. `ambito-financiero-client` and `wallets-client` are NOT bumped (D-01).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md
@.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md
@.planning/phases/40-releases-breaking-coordinados/40-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase creates **no new public symbols, classes or functions**. The surface being published was
built and live-verified by Phases 35-39. The only source-file mutations are version literals, one
added `__version__` line in `matriz_client/__init__.py` (OQ-4), the deletion of a stale comment
block in `market_data_client/__init__.py`, and — only if the operator approves D-12 — two field
type widenings in `market_data_client/models.py` with their matching test updates.

The section is stated explicitly rather than omitted, because a downstream reader would otherwise
assume it was forgotten.

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| Recorded D-02 + D-12 dispositions with option ids | `40-01-SUMMARY.md` § scope-gate dispositions | 40-01 Task 1 | verbatim operator reply + timestamp in the SUMMARY |
| `## Changelog` + `### v0.3.0` authored from scratch | `packages/matriz-client/README.md` | 40-01 Task 2 | `grep -qx '## Changelog'` + `grep -qx '### v0.3.0'` + the four migration tokens |
| matriz-client version sites at `0.3.0` (pyproject + new `__version__`) | `packages/matriz-client/{pyproject.toml,src/matriz_client/__init__.py}` | 40-01 Task 2 | `awk` / `grep -qx` assertions |
| iol-client v0.4.0 changelog entry + two version sites | `packages/iol-client/{README.md,pyproject.toml,src/iol_client/__init__.py}` | 40-01 Task 3 | `grep -qx '### v0.4.0'` + `awk` / `grep -qx` |
| market-data-client v0.6.0 changelog entry, re-pinned installs, two version sites, comment block removed | `packages/market-data-client/{README.md,pyproject.toml,src/market_data_client/__init__.py}` | 40-01 Task 3 | `grep -qx '### v0.6.0'` + install-pin occurrence counts + negative grep on the stale comment |
| higyrus-client v0.3.0 (conditional) or a corrected, re-pointed README note | `packages/higyrus-client/*` | 40-01 Task 3 | branch-specific assertions in that task |
| market_id/active widening + updated regression assertions (only if D-12 approves) | `packages/market-data-client/{src/market_data_client/models.py,tests/test_snapshot_no_data_row.py}` | 40-01 Task 3 | `uv run pytest packages/market-data-client -q` green |
| Single-refresh `uv.lock` registering every bumped member | `uv.lock` | 40-01 Task 4 | `grep -A1` per member + `uv lock --check` + exact N/N churn |
| New release branch pushed fast-forward | `git-ref:origin/milestone/v1.7-nobj-null-objects` | 40-01 Task 4 | `git rev-parse HEAD` == `git rev-parse origin/milestone/v1.7-nobj-null-objects` |
| New release PR against `main` | `gh-pr:<n>` | 40-02 Task 1 | `gh pr view <n> --json number,state,baseRefName,headRefName` |
| Two-parent merge commit on `main` | `git-ref:origin/main` | 40-02 Task 3 | `git rev-list --parents -n1 origin/main` → 3 fields |
| N annotated release tags on the merge SHA | `git-tag:<pkg>-client-v<X.Y.Z>` | 40-03 Task 2 | `git cat-file -t <tag>` == `tag`; `git rev-list -n1 <tag>` == merge SHA |
| N public GitHub Releases with wheel + sdist | `gh-release:<pkg>-client-v<X.Y.Z>` | 40-03 Task 2 | `gh release view --json assets` exact filenames |
| Post-publish install proof from the public wheels | throwaway venv (no repo path) | 40-03 Task 3 | deep-chain assertions executed against the installed distributions |
</artifacts_this_phase_produces>

<source_coverage_audit>
Multi-source coverage audit. Every item below is COVERED by a plan; there are no unplanned items.

| Source | Item | Covered by |
|---|---|---|
| GOAL | ROADMAP § Phase 40 Goal — the packages whose public surface changed are published with the break declared and a consumer-followable migration table, and no irreversible op happens without human approval | 40-01 (changelogs + bumps), 40-02 (PR + merge gate), 40-03 (tag gate + publish + post-publish proof) |
| GOAL | SC-1 — only changed packages bump; breaking callout FIRST in the changelog; migration table vieja→nueva; unchanged packages NOT re-published | 40-01 Tasks 2-3; unchanged-package assertion in Task 3 verify; no-tag assertion in 40-03 Task 2 |
| GOAL | SC-2 — `uv.lock` refreshed exactly once; CI green asserted by explicit count; merge is a real merge commit | 40-01 Task 4 (lock), 40-02 Task 1 (count), 40-02 Task 3 (merge) |
| GOAL | SC-3 — annotated tag per package on the re-resolved merge SHA; unedited `release.yml` publishes wheel + sdist; publication verified post-publish by installing the public wheel and exercising a deep chain | 40-03 Tasks 2 and 3 |
| GOAL | SC-4 — merge and tag push behind two independent, never-collapsed, never-auto-approved human checkpoints | 40-02 Task 2, 40-03 Task 1 |
| REQ | PUB-NOBJ-01 | all three plans (`requirements: [PUB-NOBJ-01]`) |
| RESEARCH | Bump set + exact target versions, measured against each published tag | 40-01 Tasks 2-3 |
| RESEARCH | Migration Table Content — locked before/after rows for all four candidate packages | 40-01 Tasks 2-3 |
| RESEARCH | Edit-Site Inventory — every stale line range, including the invisible comment block and the two install pins | 40-01 Task 3 |
| RESEARCH | P1 / OQ-1 — hoist the two scope questions above the bump | 40-01 Task 1 |
| RESEARCH | P2 — corrected workflow-immutability assertion (sha256 identity, never the tag-baseline diff form) | 40-01 Task 4 verify, 40-03 Task 2 verify |
| RESEARCH | P3 — `uv sync` after `uv lock` | 40-01 Task 4 |
| RESEARCH | P4 — never bare `uv run pytest` | `prohibitions` in all three plans; scoped commands in every verify |
| RESEARCH | P5 — count-based CI gate, no push while checks are in flight | 40-02 Task 1 |
| RESEARCH | P6 — branch topology: create the branch, never push `main` | 40-01 Task 4 |
| RESEARCH | P7 — dirty `.planning/config.json` | 40-01 Task 2 (a) |
| RESEARCH | P8 — three stale "the bump happens later" sites | 40-01 Task 3 |
| RESEARCH | P9 — N tags on one merge commit, watch each run individually | 40-03 Task 2 |
| RESEARCH | P10 — post-publish install from the public URL in a fresh 3.12 interpreter | 40-03 Task 3 |
| RESEARCH | P11 — ambito/wallets classified additive, not unchanged | `<scope_decisions>` below |
| RESEARCH | Security domain — ASVS L1 V2/V4/V5/V14 and the eight STRIDE patterns | `<threat_model>` in all three plans |
| RESEARCH | OQ-2 — decline destination for the README notes | 40-01 Task 1 `<options>` + Task 3 |
| RESEARCH | OQ-3 — in-repo release-memory refresh | `<scope_decisions>` below: **no target exists**; non-task |
| RESEARCH | OQ-4 — add matriz `__version__` | 40-01 Task 2, disposition: add, no new test file |
| CONTEXT | D-01 exactly three packages bump (fourth conditional) | 40-01 Tasks 2-3 |
| CONTEXT | D-02 higyrus fold question, hoisted | 40-01 Task 1, resolved in Task 3 |
| CONTEXT | D-03 matriz changelog authored from scratch, six aliases documented separately as additive | 40-01 Task 2 |
| CONTEXT | D-04 matriz `__version__` discretionary | 40-01 Task 2 (`<scope_decisions>`: add it) |
| CONTEXT | D-05 new branch `milestone/v1.7-nobj-null-objects`, new PR | 40-01 Task 4, 40-02 Task 1 |
| CONTEXT | D-06 exactly 15 CI checks | 40-02 Task 1 |
| CONTEXT | D-07 two blocking gates on irreversible ops, plus the hoisted scope gate which is not one of them | 40-01 Task 1, 40-02 Task 2, 40-03 Task 1 |
| CONTEXT | D-08 `gh pr merge --merge`, never squash, never rebase | 40-02 Task 3 |
| CONTEXT | D-09 annotated tags on the re-resolved merge SHA; one approval covers all tags | 40-03 Tasks 1-2 |
| CONTEXT | D-10 single `uv lock` after all bumps | 40-01 Task 4 |
| CONTEXT | D-11 post-publish install from the public wheel + deep chain | 40-03 Task 3 |
| CONTEXT | D-12 market_id/active widening question, hoisted | 40-01 Task 1, resolved in Task 3 |
| CONTEXT | Discretion: matriz changelog wording | 40-01 Task 2 (format contract fixed, prose is the executor's) |
| CONTEXT | Discretion: PR title and body | 40-02 Task 1 (title convention fixed, body prose is the executor's) |
| CONTEXT | Discretion: whether to add matriz `__version__` | decided in `<scope_decisions>`: add |
| CONTEXT | Discretion: commit grouping and order | decided per task below |
| CONTEXT | Deferred: further market_id/active work beyond what the operator approves | honored — Task 1 forces a named destination on decline, never a silent carry-forward |
</source_coverage_audit>

<reversibility_gates>
| Task | Operation | Reversibility | Gate |
|---|---|---|---|
| Task 1 | operator scope decision (D-02, D-12) | n/a — modifies no file | **blocking `checkpoint:decision`**, NOT one of the two irreversible-op gates |
| Task 2 | matriz changelog + version sites | fully reversible (`git revert` / amend) | none needed |
| Task 3 | iol / market-data / higyrus changelog + version sites (+ optional widening) | fully reversible | none needed |
| Task 4 | `uv lock`, `uv sync`, local CI mirror | fully reversible | none needed |
| Task 4 | `git checkout -b` + `git push origin milestone/v1.7-nobj-null-objects` | reversible-in-principle only; a published commit on a public repo can be reverted forward but not unpublished | no human gate — this is an append-only fast-forward to a *feature* branch, not to `main`. Force flags are forbidden, which is what keeps it append-only. |

The phase's two blocking gates on **irreversible operations** live in 40-02 (before the merge) and
40-03 (before the tag push), per D-07. The Task 1 scope gate does not merge, tag or publish anything
— it only fixes the package set before the bump, and therefore does not count against D-07's
"exactly two" or against ROADMAP criterion 4 (CONTEXT D-07 states this explicitly).
</reversibility_gates>

<scope_decisions>
Conscious decisions and deferrals, recorded so the executor and verifier see a decision rather than
a gap. Do NOT reopen these unless the operator explicitly says so.

- **`.github/workflows/release.yml` and `ci.yml` — do NOT touch.** `release.yml` is generic by tag
  regex and already matches every tag this phase will push; this is its sixth reuse without an edit.
  `ci.yml`'s `matrix.package` lists all six packages unconditionally, so the 15-check count is
  invariant to how many packages bump. If a check fails, surface the failing job and step; never
  patch the workflow.
- **`ambito-financiero-client` and `wallets-client` are measured-and-classified ADDITIVE, not
  unchanged.** Both surfaces moved since their published tags: ambito gained
  `AmbitoFinancieroDecodeError` and a keyword-only `strict_decode` parameter; wallets gained an
  empty `models.py` + `types.py`. Under the project's own precedent (Phase 34 classified higyrus's
  diff as "aditivo, no bump-worthy") the no-bump disposition stands. A verifier who re-runs a
  surface diff and finds a non-empty result for either package is seeing the expected outcome, not
  a discovered defect (RESEARCH P11).
- **matriz `__version__` — decided: ADD it** (OQ-4, discretionary under D-04). One bare
  module-level line `__version__ = "0.3.0"` immediately after the closing `]` of `__all__`, matching
  `packages/iol-client/src/iol_client/__init__.py:87`. `release.yml:47` never reads it, so this is
  convention parity with `CLAUDE.md`'s Exports rule, nothing more. Decided: do **NOT** add a
  `test_version_metadata.py` for matriz — that would import `market-data-client`'s dist-metadata
  coupling (RESEARCH P3) into a fifth package for zero gate benefit.
- **In-repo release-memory refresh (OQ-3) — decided: there is no target.** Measured at plan time:
  `/Users/admin/.claude/projects/-Users-admin-development-market-libs/memory/` contains exactly
  `MEMORY.md` and `project_matriz_bbsa_sandbox.md`. `market-data-client-releases.md`, which Phase
  34-03 Task 3 refreshed, **does not exist in this checkout**. There is nothing to refresh, so this
  phase authors no memory task. Creating new memory files remains the deferred item from Phase 34;
  this phase does not open it.
- **The 186 pending commits sit on local `main`, not on a milestone branch** (measured at plan time;
  RESEARCH said 182 and CONTEXT said 180 — the number moves with every commit, so recompute it at
  run time and never trust a literal). This inverts Phase 34's topology and is the single most
  dangerous copy-paste in the phase: Task 4 creates the branch first, then pushes it by name.
- **TDD framing does not apply to any task in this phase.** `workflow.tdd_mode` is enabled
  project-wide, and this plan states the reasoning explicitly rather than silently omitting the
  framing. This phase ships **zero product code**: it edits changelog prose, version literals and a
  lockfile, then performs git/gh/registry operations. There is no function whose input/output
  contract could be pinned by a failing test first — a RED/GREEN cycle over a version-string edit
  would assert a literal that the same commit writes, which is a tautology, not a test. Every task
  is therefore `type: execute`/`type: auto` with shell and git-state assertions in `<verify>`
  `<automated>` instead of pytest gates. The one branch that *would* touch product code — an
  approved D-12 widening — is a type-annotation change whose behavior is already pinned by the six
  existing assertions in `test_snapshot_no_data_row.py`; that task updates those assertions in the
  same commit as the widening, which is a regression-preservation edit, not a new test-first cycle.
- **Vertical-slice / MVP framing does not apply.** `MVP_MODE` is false and the natural wave
  structure (reversible prep → gated merge → gated publish) already supplies both the ordering and
  the failure isolation.
</scope_decisions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: SCOPE GATE — fix the bump set before any version site moves (D-02 higyrus, D-12 market_id/active)</name>
  <files>none — this task modifies no file</files>
  <read_first>
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` § Decisions D-02, D-07 and D-12 — both questions, the correction that hoists them here, and the operator's discuss-phase confirmation (2026-08-30) that they do NOT live at the pre-merge gate.
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § Common Pitfalls P1 and § Open Questions OQ-1 and OQ-2 — the measured blast radius of each "approve", and the default decline wording.
    - `.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-01-PLAN.md` lines 99-207 — the literal element shape for a `checkpoint:decision` gate in this project: `<decision>` / `<context>` / `<options>` with named option ids, evidence commands run and quoted before the options are presented, and the verbatim answer recorded in the SUMMARY.
    - `packages/higyrus-client/README.md` lines 129-162 — the `### v0.3.0 — sin publicar todavía` heading and the paragraph at 131-137 naming Phase 34 as executor. The body below it (139-162) is already complete and correctly voiced.
    - `packages/higyrus-client/src/higyrus_client/client.py` around line 450 — `get_health()` already returns `Health` in production code, shipped by Phase 31 (commit `bf04b2f`) and never published.
    - `packages/market-data-client/README.md` lines 29-34 — the divergence note ending "espera checkpoint del operador".
    - `packages/market-data-client/tests/test_snapshot_no_data_row.py` lines 61-63 (the module docstring stating the fields are still over-declared), 155-156, 179-180 (`assert row.market_id == ""` / `assert row.active is False`), and 261, 282 (`assert exc.value.field_path == ".market_id"`) — the assertions a D-12 approval turns red.
    - `.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-DEFERRED-market-data-leaves.md` — the full divergence record.
  </read_first>
  <decision>
    Two independent scope questions must be answered before the first `pyproject.toml` edit,
    because each one changes the set of packages that get bumped and therefore changes the content
    of the single `uv lock` run that D-10 permits.

    **Question A (D-02):** Does `higyrus-client` fold into this round as a fourth bumped package
    (`0.2.0` → `0.3.0`)?

    **Question B (D-12):** Does `market-data-client` widen `MarketDataSnapshot.market_id`
    (`str` → `str | None`) and `.active` (`bool` → `bool | None`) now, inside this same breaking
    bump?
  </decision>
  <context>
    What is LOCKED and not up for discussion: D-01 fixes three packages —
    `market-data-client` 0.5.0 → 0.6.0, `iol-client` 0.3.0 → 0.4.0, `matriz-client` 0.2.0 → 0.3.0.
    Those three bump regardless of the answers below. `ambito-financiero-client` and
    `wallets-client` do not bump regardless.

    **Question A evidence (D-02, higyrus):**
    - `packages/higyrus-client/src/higyrus_client/client.py` — `get_health()` has returned a typed
      `Health` since Phase 31 (v1.5, commit `bf04b2f`). The change is shipped in code and has never
      been published.
    - `packages/higyrus-client/pyproject.toml` still reads `0.2.0`; the last published tag is
      `higyrus-client-v0.2.0`.
    - `packages/higyrus-client/README.md:131-137` says the bump "los hace la Phase 34" — an
      assertion Phase 34 itself invalidated: `34-CONTEXT.md` D-01 excluded higyrus explicitly, and
      `34-01-SUMMARY.md` deviation 1 classified the diff as additive, not bump-worthy.
    - No planning artifact anywhere reassigns this debt to any phase. Approving folds it in; this is
      the third time the question has come up and it does not get deferred silently again.
    - Cost of approving: one more `pyproject.toml` + `__version__` + README retitle, one more line
      in the single `uv lock`, one more tag, one more Release. Because this gate runs BEFORE any
      bump, approving costs no re-loop — that is precisely why it was hoisted here.

    **Question B evidence (D-12, market_id/active):**
    - `36-DEFERRED-market-data-leaves.md` records a measured, uncorrected divergence: on the no-data
      row of `GET /marketdata/latest`, `market_id` and `active` arrive `null` over non-`Optional`
      declarations, the walker substitutes `""` / `False`, and `strict_decode` raises on
      `.market_id`. That document names "el bump coordinado de Phase 40" as the natural checkpoint.
    - But `PUB-NOBJ-01`'s scope is publishing breaks already decided, not deciding new ones. Phase
      39 neither touched nor resolved this item.
    - Cost of approving, measured: it is a **code change** in `models.py` and it turns at least six
      currently-green assertions red — `assert row.market_id == ""` at
      `packages/market-data-client/tests/test_snapshot_no_data_row.py:155` and `:179`,
      `assert exc.value.field_path == ".market_id"` at `:261` and `:282`, each with an `_async`
      twin — plus the module docstring at `:61-63`, which states in prose that these fields are
      still over-declared. Those assertions exist specifically to pin the un-widened behavior
      (36-REVIEW CR-02). Approving means updating all of them inside this same cycle, not as a
      post-hoc regression.
    - Because this gate runs before the bump and before `uv lock`, approving still costs no second
      lock run and no re-presented CI gate.

    Evidence commands to RUN and QUOTE before presenting the options (do not assert these from the
    text above — re-measure them, the tree moves):

    ```
    grep -n 'def get_health' packages/higyrus-client/src/higyrus_client/client.py
    awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/higyrus-client/pyproject.toml
    sed -n '129,138p' packages/higyrus-client/README.md
    grep -cE 'market_id|\.active' packages/market-data-client/tests/test_snapshot_no_data_row.py
    grep -n 'assert row.market_id\|assert row.active\|field_path == "\.market_id"' packages/market-data-client/tests/test_snapshot_no_data_row.py
    sed -n '29,34p' packages/market-data-client/README.md
    ```
  </context>
  <options>
    <option id="A-fold-higyrus">
      <name>Question A — FOLD: bump `higyrus-client` 0.2.0 → 0.3.0 as a fourth package in this round</name>
      <pros>Clears a real, shipped, unpublished source break that has been unassigned for two milestones. Consumers stop getting a wheel whose metadata says `0.2.0` over an API incompatible with the published `0.2.0`. Costs no re-loop because the gate runs before the bump. The changelog body at `README.md:139-162` is already written and correctly voiced — only the heading and the stale paragraph change.</pros>
      <cons>Widens the round from three packages to four: one more tag, one more Release, one more `release.yml` run to watch, one more line of `uv.lock` churn (`4 4` instead of `3 3`), and one more per-package row pair to count in the CI gate.</cons>
    </option>
    <option id="A-decline-reassign">
      <name>Question A — DECLINE and reassign to a named phase</name>
      <pros>Keeps the round at exactly the three packages D-01 locks. The debt gets a real owner rather than an implicit one.</pros>
      <cons>Requires naming a concrete destination phase at this checkpoint. `packages/higyrus-client/README.md:133-137` is corrected either way — it must stop naming Phase 34 as executor.</cons>
    </option>
    <option id="A-decline-unassigned">
      <name>Question A — DECLINE and mark explicitly "pendiente, sin fase asignada"</name>
      <pros>Honest about the state: no destination exists (ROADMAP ends at Phase 40). Still not a silent re-deferral — the README says so in words and a `deferred-items.md` entry records it.</pros>
      <cons>The debt survives into v1.8 planning without an owner. Third deferral of the same item.</cons>
    </option>
    <option id="B-widen-now">
      <name>Question B — WIDEN `market_id` and `active` to Optional inside this bump</name>
      <pros>Resolves a measured divergence at the exact moment a breaking bump makes it cheap to ship. Adds one row to `market-data-client`'s migration table. `strict_decode` stops raising on a legitimate `null`.</pros>
      <cons>It is a code change inside a release-mechanics phase whose stated scope is publishing already-decided breaks. Requires updating the six-plus assertions and the module docstring in `test_snapshot_no_data_row.py` (sync + async) in the same commit. Those assertions exist deliberately to pin the un-widened behavior.</cons>
    </option>
    <option id="B-decline-reassign">
      <name>Question B — DECLINE and reassign the README note to a named phase</name>
      <pros>Holds this phase to its stated scope. The note stops pointing at Phase 40, which stops being a valid future destination the moment this phase merges.</pros>
      <cons>Requires naming a concrete destination phase at this checkpoint.</cons>
    </option>
    <option id="B-decline-unassigned">
      <name>Question B — DECLINE and mark explicitly "pendiente, sin fase asignada"</name>
      <pros>Holds the scope line and is honest that no destination phase exists yet. Recorded in the README and in a `deferred-items.md` entry, not carried forward silently.</pros>
      <cons>The divergence stays live in production for another milestone.</cons>
    </option>
  </options>
  <action>
    Before presenting anything, RUN the six evidence commands listed in `<context>` and quote their
    literal output in the checkpoint message, so the operator sees the state measured rather than
    asserted. Then present both questions with their option ids verbatim and STOP.

    Do NOT modify any file in this task. Do NOT edit any `pyproject.toml`, any `__init__.py`, any
    `README.md`, `models.py`, any test, or `uv.lock`. Do NOT run `uv lock`. Do NOT create a branch,
    open a PR, create a tag or push anything.

    This gate is a `checkpoint:decision`, not one of the phase's two irreversible-operation gates —
    it merges nothing and publishes nothing. It is nonetheless BLOCKING and is never
    auto-approvable: `auto_advance: true` and `mode: yolo` are both active in `.planning/config.json`
    and neither satisfies it, silence does not satisfy it, an ambiguous reply does not satisfy it,
    and the agent may not self-issue it or pick a default. Both questions are genuine operator
    decisions with a measured blast radius, consistent with the project's D-08/D-18 precedent of
    never auto-resolving a scope-adjacent decision.

    Record in the plan SUMMARY, under a heading `Scope-gate dispositions (D-02, D-12)`: the
    operator's verbatim reply with its timestamp, the two selected option ids, and — for any decline
    branch — the concrete destination named by the operator, or the literal string
    "pendiente, sin fase asignada" if none was named. Tasks 2, 3 and 4 read the recorded option ids
    to determine the final package set N (3 or 4), the expected `uv.lock` churn (`N N`), and whether
    `models.py` and `test_snapshot_no_data_row.py` are in scope.

    If the operator replies "abort", stop cleanly: nothing has been modified and the phase can be
    re-run from this task.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain -- packages/ uv.lock)" || { echo "SCOPE GATE MUST NOT MODIFY ANY PACKAGE FILE OR THE LOCKFILE"; exit 1; }; for P in ambito-financiero-client higyrus-client iol-client market-data-client matriz-client wallets-client; do V=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' "packages/$P/pyproject.toml"); case "$P:$V" in ambito-financiero-client:0.2.0|higyrus-client:0.2.0|iol-client:0.3.0|market-data-client:0.5.0|matriz-client:0.2.0|wallets-client:0.2.0) ;; *) echo "VERSION ALREADY MOVED BEFORE THE SCOPE GATE: $P=$V"; exit 1;; esac; done; test -z "$(git tag -l 'matriz-client-v0.3.0')" && test -z "$(git tag -l 'iol-client-v0.4.0')" && test -z "$(git tag -l 'market-data-client-v0.6.0')" && test "$(git rev-parse --abbrev-ref HEAD)" = "main" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All six evidence commands from `<context>` were executed and their literal output is quoted in the checkpoint message
    - Both questions were presented with all six option ids verbatim, and the measured blast radius of each "approve" (a fourth package in the single `uv lock` for A; six-plus red assertions in `test_snapshot_no_data_row.py` for B) was stated
    - No file was modified: `git status --porcelain -- packages/ uv.lock` is empty and every one of the six packages still reads its pre-phase version under `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'`
    - `git rev-parse --abbrev-ref HEAD` is still `main` — no branch was created in this task
    - No `uv lock`, no `gh pr create`, no `git tag` and no `git push` command was executed before the operator's reply
    - The operator's reply is recorded verbatim with its timestamp in the SUMMARY under `Scope-gate dispositions (D-02, D-12)`, together with both selected option ids
    - For each decline branch selected, the SUMMARY records either the concrete destination phase the operator named, or the literal `pendiente, sin fase asignada`
    - The approval came from a literal operator response — not from `auto_advance`, not from yolo mode, not inferred from silence, and not self-issued by the agent
    - Execution continues to Task 2 ONLY once both questions have an explicit answer; a partial answer halts the plan
  </acceptance_criteria>
  <resume-signal>Answer BOTH questions by option id, e.g. "A-fold-higyrus, B-decline-unassigned". Valid ids: A-fold-higyrus | A-decline-reassign | A-decline-unassigned, and B-widen-now | B-decline-reassign | B-decline-unassigned. For any decline-reassign, name the destination phase. Or type "abort" to stop with nothing modified.</resume-signal>
  <done>The operator answered both scope questions with explicit option ids, the answers are recorded verbatim with a timestamp in the SUMMARY under `Scope-gate dispositions (D-02, D-12)`, the final package set N is fixed, and not a single package file, test, lockfile or branch was touched in the process.</done>
</task>

<task type="auto">
  <name>Task 2: Author matriz-client's Changelog from scratch and bump it to v0.3.0 (D-01, D-03, D-04/OQ-4, SC-1)</name>
  <files>.planning/config.json, packages/matriz-client/README.md, packages/matriz-client/pyproject.toml, packages/matriz-client/src/matriz_client/__init__.py</files>
  <read_first>
    - `packages/matriz-client/README.md` — the whole file (137 lines). It ends at `## Desarrollo`
      (line 131) and has NO `## Changelog` section at all. The new section is appended after the
      existing content.
    - `packages/iol-client/README.md` lines 149-175 — `## Changelog` H2 followed by `### v0.3.0` as
      its first child, bold lead line with a parenthetical semver justification, then the
      `| Función | Antes | Ahora |` table. This is the literal voice/format contract to copy.
    - `packages/market-data-client/README.md` lines 7-34 — the field-access antes/después table
      shape (`| Antes (0.5.0 publicado) | Ahora … |`) plus the prose-paragraph-after-the-table
      pattern at 23-28. matriz's four changes are field accesses, so this column shape fits better
      than iol's `| Función |` shape.
    - `packages/iol-client/README.md` lines 29-38 — the truthiness-flip prose pattern ("sigue siendo
      falsy … pero ya no es el valor nulo del lenguaje … deja de tomar esa rama, en silencio").
      matriz's `AccountReport.portfolio` needs the same treatment.
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § Migration Table Content →
      `matriz-client 0.2.0 → 0.3.0` — the four locked before/after rows, the verified declared types,
      the envelope-unwrap behavior fix with its `strict-unwrap` disposition, and the explicit list of
      six alias properties that must be documented separately and must NOT enter the migration table.
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` § Decisions D-01, D-03, D-04
      and § Claude's Discretion.
    - `.planning/phases/40-releases-breaking-coordinados/40-PATTERNS.md` §
      `packages/matriz-client/README.md` — the exact markdown skeleton and the column-shape guidance.
    - `packages/matriz-client/pyproject.toml` lines 1-10 — the `[project]` block; `version = "0.2.0"`
      is line 3.
    - `packages/matriz-client/src/matriz_client/__init__.py` — the file ends at line 184 with the
      closing `]` of `__all__` (final entry `"ws_subscribe_order_reports"`). There is no
      `__version__` today.
    - `packages/iol-client/src/iol_client/__init__.py` line 87 — the placement analog: a single bare
      module-level `__version__` assignment after the closing `]` of `__all__`, not exported in
      `__all__`, no docstring.
    - `.github/workflows/release.yml` lines 22-51 — READ-ONLY. The tag regex at `:28` and the awk
      version-match gate at `:42-51` that hard-fails the build on any tag-vs-pyproject mismatch.
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-01-PLAN.md` lines 296-386 (Task 2)
      — the version-bump action shape, the ordering rule, and the `__version__` local-assertion
      defense. Do NOT copy its workflow-immutability assertion form; that form is broken (RESEARCH P2).
  </read_first>
  <action>
    (a) Clear the dirty tree first (RESEARCH P7). `.planning/config.json` is modified in the working
    tree (one line of GSD bookkeeping). Commit it alone before anything else so it does not ride
    silently into a release commit. Subject:
    `chore(planning): sincronizar .planning/config.json antes del ciclo de release`.
    Exactly one file. If GSD re-dirties this file later in the phase, re-commit it the same way —
    never `git checkout --` it away mid-phase and never bundle it into a package commit.

    Immediately after this commit, capture and record two values that later tasks and plans need and
    that must never be taken from a document literal: `PHASE_BASE=$(git rev-parse HEAD)` — the
    commit this phase's package work starts from — and
    `AHEAD=$(git rev-list --count origin/main..HEAD)`. Record both in the SUMMARY. CONTEXT.md says
    180 commits, RESEARCH.md says 182, and the plan-time measurement was 186; the number moves with
    every commit and no literal in any document is trustworthy.

    (b) `packages/matriz-client/README.md` — create the `## Changelog` section from scratch (D-03).
    matriz-client is the only bumped package with no changelog at all; iol-client and
    market-data-client already have theirs pre-written. Append the new section after the existing
    final content, following the same H2-then-`### vX.Y.Z` structure the other two packages use.

    Hold every convention the sibling READMEs establish: Spanish prose; a bold lead line naming the
    bump class followed by a parenthetical semver justification (breaking = minor on a 0.x line);
    every identifier, field name and type name in backticks; antes/después markdown table for the
    field-access changes; wrapped to roughly 95-100 columns; continuation lines indented exactly two
    spaces; no trailing whitespace; file ends with a newline. The `trailing-whitespace` and
    `end-of-file-fixer` pre-commit hooks run over this file in CI.

    Required content, all locked by D-03 — the exact before/after expressions are in RESEARCH
    § Migration Table Content and are transcribed, not re-derived:

      1. **Migration table** with exactly these four rows, column headers
         `| Antes (0.2.0 publicado) | Ahora (0.3.0) |`:
         - `report.portfolio` was `dict[str, Any]` (always `{}` in practice) → is now `float | None`;
           check `is None`, not `== {}`.
         - `detail.tickPriceRanges["0"]["tick"]` → `detail.tickPriceRanges["0"].tick`
           (`dict[str, TickPriceRange]`).
         - `pos.report["<acct>"]["<sym>"]["instrumentCurrentSize"]` →
           `pos.report["<acct>"]["<sym>"].instrumentCurrentSize`
           (`dict[str, dict[str, InstrumentPositionReport]]` — **two** levels).
         - `acct.detailedAccountReports["<k>"]["settlementDate"]` →
           `acct.detailedAccountReports["<k>"].settlementDate`
           (`dict[str, DetailedAccountReport]` — **one** level).
      2. **A prose paragraph after the table** for the envelope-unwrap fix, which is a BEHAVIOR
         change and does not fit a type table (D-03 requires it be documented as such):
         `get_detailed_positions` and `get_account_report` previously decoded from the wrong nesting
         level and returned models with every field at its default. Phase 37 fixed this by unwrapping
         the vendor envelope keys `detailedPosition` / `accountData`. Under the operator-ratified
         `strict-unwrap` disposition a flat (unenveloped) Risk body now RAISES rather than silently
         decoding. Consumers who were tolerating all-default Risk models will now see real data — or
         a `PrimaryAPIError` where they previously saw an empty model.
      3. **A truthiness-flip paragraph** for `AccountReport.portfolio`, in the same voice as
         `packages/iol-client/README.md:29-38`: an always-`{}` dict was falsy and is now a
         `float | None` scalar; a consumer branching on `== {}` silently stops taking that branch,
         does not raise, does not break the build, and mypy says nothing.
      4. **A separate "aditivo, no breaking" block** — explicitly NOT part of the migration table
         (D-03) — covering the six new alias properties on `MarketDataSnapshot`: `bids`/`offers` →
         `list[MarketDataLevel]` and `last`/`settlement`/`close`/`open_interest` →
         `MarketDataEntryValue`, over the wire keys `BI`/`OF`/`LA`/`SE`/`CL`/`OI`. Note that `OP` and
         matriz's seven extra scalars were deliberately excluded (37-05 named-exclusion decision).
         Also additive: `TickPriceRange`, `InstrumentPositionReport`, `DetailedAccountReport` and
         `MatrizDecodeError` joining `__all__`, and the keyword-only `strict_decode: bool | None`
         parameter on `Client`, `AsyncClient` and `configure`.

    The heading is exactly `### v0.3.0` on its own line — no trailing qualifier of any kind. Unlike
    iol and market-data, matriz has no provisional section to de-provisionalize, so the entry is
    written in its final published form from the first keystroke.

    (c) `packages/matriz-client/pyproject.toml` line 3 — set the `[project]` `version` value to
    `0.3.0`. Nothing else in the file moves. `release.yml:42-51` awk-reads the FIRST `^version` line
    in this file and hard-fails the build unless it equals the version captured from the tag
    `matriz-client-v0.3.0`, so this value and that tag must agree exactly. Bump class: minor on a
    0.x line, the project's established encoding of source-breaking.

    (d) `packages/matriz-client/src/matriz_client/__init__.py` — append the single line
    `__version__ = "0.3.0"` after the closing `]` of `__all__`, matching the placement at
    `packages/iol-client/src/iol_client/__init__.py:87`. This is OQ-4's disposition and D-04's
    discretion exercised: `release.yml:47` never reads this site, matriz published `v0.1.1` and
    `v0.2.0` without it, and adding it is convention parity only. Do NOT add the name to `__all__`,
    do NOT add a docstring, and do NOT create a `test_version_metadata.py` for matriz — that would
    import market-data-client's dist-metadata coupling into a fifth package for no gate benefit.

    (e) Commit (b), (c) and (d) together as the matriz-client release commit — the changelog entry
    asserting `### v0.3.0` is only true once the version sites say `0.3.0`, so they belong in one
    commit. Subject:
    `chore(matriz-client): bump to v0.3.0 (cuatro retipados de modelo + fix de envelope-unwrap)`.
    Body: one short paragraph naming the bump class and headline, then a line inventorying the three
    aligned sites. Exactly three files.

    Touch NO other package in this task. Do NOT run `uv lock` — D-10 mandates exactly one refresh
    and Task 4 owns it. Touch no file under `.github/workflows/`.
  </action>
  <verify>
    <automated>MV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/matriz-client/pyproject.toml) && test "$MV" = "0.3.0" && grep -qx '__version__ = "0.3.0"' packages/matriz-client/src/matriz_client/__init__.py && test "$(grep -c '^__version__' packages/matriz-client/src/matriz_client/__init__.py)" = "1" && ! grep -q '"__version__"' packages/matriz-client/src/matriz_client/__init__.py && test ! -f packages/matriz-client/tests/test_version_metadata.py && grep -qx '## Changelog' packages/matriz-client/README.md && grep -qx '### v0.3.0' packages/matriz-client/README.md && SEC=$(awk '/^### v0\.3\.0$/{f=1;next} /^### v/{f=0} f' packages/matriz-client/README.md) && test -n "$SEC" && for T in '`portfolio`' '`float | None`' '`tickPriceRanges`' '`TickPriceRange`' '`InstrumentPositionReport`' '`detailedAccountReports`' '`DetailedAccountReport`' '`get_detailed_positions`' '`get_account_report`' '`detailedPosition`' '`accountData`' '`PrimaryAPIError`' '`bids`' '`offers`' '`open_interest`' '`MatrizDecodeError`' '`strict_decode`'; do printf '%s\n' "$SEC" | grep -qF -- "$T" || { echo "CHANGELOG INCOMPLETE: $T"; exit 1; }; done && test "$(printf '%s\n' "$SEC" | grep -c '^| ')" -ge 6 && test "$(git show --name-only --format= HEAD | grep -v '^$' | wc -l | tr -d ' ')" = "3" && git log -1 --format=%s | grep -q '^chore(matriz-client): bump to v0\.3\.0' && test "$(git diff --name-only HEAD~2..HEAD -- .github/workflows | wc -l | tr -d ' ')" = "0" && test "$(git diff --name-only HEAD~2..HEAD -- uv.lock | wc -l | tr -d ' ')" = "0" && uv run pre-commit run --files packages/matriz-client/README.md packages/matriz-client/pyproject.toml packages/matriz-client/src/matriz_client/__init__.py && uv run pytest packages/matriz-client -q && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` was empty after the first commit of this task, and the `.planning/config.json` commit contains exactly one file with subject `chore(planning): sincronizar .planning/config.json antes del ciclo de release`
    - `PHASE_BASE` (the SHA of that first commit) and the run-time-measured `AHEAD` count are both recorded in the SUMMARY; no document literal (180 / 182 / 186) was reused
    - `grep -qx '## Changelog' packages/matriz-client/README.md` exits 0 — the section did not exist before this task
    - `grep -qx '### v0.3.0' packages/matriz-client/README.md` exits 0 — exact-line match, no trailing qualifier
    - Slicing the README from `^### v0\.3\.0$` to the next `^### v` yields a non-empty section containing all of these literal backticked tokens: `` `portfolio` ``, `` `float | None` ``, `` `tickPriceRanges` ``, `` `TickPriceRange` ``, `` `InstrumentPositionReport` ``, `` `detailedAccountReports` ``, `` `DetailedAccountReport` ``, `` `get_detailed_positions` ``, `` `get_account_report` ``, `` `detailedPosition` ``, `` `accountData` ``, `` `PrimaryAPIError` ``, `` `bids` ``, `` `offers` ``, `` `open_interest` ``, `` `MatrizDecodeError` ``, `` `strict_decode` ``
    - That section contains a markdown table of at least 6 rows (header + separator + four migration rows), and the four migration rows are the exact before/after pairs locked in RESEARCH § Migration Table Content
    - The six alias properties are documented in a block explicitly labelled additive/not-breaking and do NOT appear as rows of the migration table (D-03)
    - The envelope-unwrap change is documented as a behavior correction in prose after the table, naming both affected functions, both vendor envelope keys, and the fact that a flat body now raises
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/matriz-client/pyproject.toml` outputs exactly `0.3.0`
    - `grep -qx '__version__ = "0.3.0"' packages/matriz-client/src/matriz_client/__init__.py` exits 0, `grep -c '^__version__'` on that file outputs `1`, and `__version__` was NOT added to `__all__`
    - `packages/matriz-client/tests/test_version_metadata.py` does not exist (OQ-4 / RESEARCH P3)
    - `git show --name-only --format= HEAD` lists exactly three files, all under `packages/matriz-client/`, and `git log -1 --format=%s` starts with `chore(matriz-client): bump to v0.3.0`
    - `git diff --name-only HEAD~2..HEAD -- .github/workflows | wc -l` outputs `0` and `git diff --name-only HEAD~2..HEAD -- uv.lock | wc -l` outputs `0` — no workflow file and no lockfile in either commit of this task
    - `uv run pre-commit run --files <the three matriz files>` passes with zero file rewrites
    - `uv run pytest packages/matriz-client -q` passes. No bare `uv run pytest` was executed
  </acceptance_criteria>
  <done>`packages/matriz-client/README.md` carries a `## Changelog` section that did not exist before, whose only entry `### v0.3.0` documents the four retypes in a migration table, the envelope-unwrap behavior fix in prose, and the six alias properties separately as additive; matriz-client reads `0.3.0` at `pyproject.toml` and at a newly added `__version__`; two commits exist (the config sync and the three-file matriz bump).</done>
</task>

<task type="auto">
  <name>Task 3: De-provisionalize iol-client and market-data-client, bump their version sites, purge every stale "a future phase does the bump" claim, and resolve the two scope answers (D-01, D-02, D-12, SC-1)</name>
  <files>packages/iol-client/README.md, packages/iol-client/pyproject.toml, packages/iol-client/src/iol_client/__init__.py, packages/market-data-client/README.md, packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/tests/test_snapshot_no_data_row.py, packages/higyrus-client/README.md, packages/higyrus-client/pyproject.toml, packages/higyrus-client/src/higyrus_client/__init__.py</files>
  <read_first>
    - The Task 1 SUMMARY entry `Scope-gate dispositions (D-02, D-12)` — the two selected option ids
      decide whether higyrus is bumped and whether the widening happens. Read the recorded answer;
      do not re-ask and do not infer.
    - `packages/iol-client/README.md` lines 5-42 (`## Unreleased — BREAKING`) and lines 149-175
      (`## Changelog` H2 at 149 with `### v0.3.0` at 151). Line 10 carries the "lo hace la Fase 40"
      sentence. `README.md:47` is a bare `uv add iol-client` with no version pin — no edit needed there.
    - `packages/market-data-client/README.md` lines 7-34 (`## Unreleased — BREAKING`, with the
      blockquote preamble at 9-13 and the divergence note at 29-34), line 44 (the git install pinned
      to the prior tag), line 53 (the wheel URL and filename pinned to the prior version), and lines
      152-154 (`## Changelog` H2 at 152 with `### v0.5.0` at 154).
    - `packages/market-data-client/src/market_data_client/__init__.py` lines 161-178 — the 15-line
      comment block headed `# NO BUMPEAR ACÁ (Phase 36 code review, WR-04)` sitting directly above
      `__version__ = "0.5.0"` at line 178. It names this phase as the future owner and becomes false
      the moment the bump lands. A Markdown-scoped grep never sees it; RESEARCH P8 flags it as the
      single most likely miss in the phase.
    - `packages/iol-client/src/iol_client/__init__.py` line 87 — `__version__ = "0.3.0"`.
    - `packages/higyrus-client/README.md` lines 129-162 — the `## Changelog` H2 at 129, the
      `### v0.3.0 — sin publicar todavía` heading at 131, the stale paragraph at 133-137 naming
      Phase 34 as executor, and the already-correct body at 139-162 which needs no rewrite in either
      branch.
    - `packages/higyrus-client/pyproject.toml` line 3 (`0.2.0`) and
      `packages/higyrus-client/src/higyrus_client/__init__.py` line 109 (`__version__ = "0.2.0"`).
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § Edit-Site Inventory (every
      stale line range with its target state) and § Migration Table Content → the `market-data-client`,
      `iol-client` and `higyrus-client` subsections (the locked before/after rows and declared-type
      facts).
    - `.planning/phases/40-releases-breaking-coordinados/40-PATTERNS.md` §
      `packages/{iol,market-data}-client/README.md` — the de-provisionalization pattern, the
      Phase-40 shape delta (this is a MOVE of an H2 block into `## Changelog`, not an in-place
      retitle), the install-pin sub-pattern, and the negative assertions with no Phase 34 analog.
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-01-PLAN.md` lines 332-366 (Task 2
      (d)-(g)) — the only precedent for turning a written-in-advance provisional entry into a
      published one, and the per-package bump commit shape. Do NOT copy its workflow-immutability
      assertion form (RESEARCH P2).
    - Only if the recorded B answer is `B-widen-now`:
      `packages/market-data-client/src/market_data_client/models.py` — the `MarketDataSnapshot`
      declaration of `market_id: str` and `active: bool`; and
      `packages/market-data-client/tests/test_snapshot_no_data_row.py` lines 61-63, 155-156,
      179-180, 261, 282 and their `_async` twins.
  </read_first>
  <action>
    Three de-provisionalizations sharing one pattern, plus two branch-dependent resolutions. Every
    `pyproject.toml` edit in this task must land before the `uv lock` Task 4 owns.

    (a) **`packages/iol-client` — move, then bump.** This is a MOVE, not an in-place retitle: the
    provisional content lives in a top-of-file `## Unreleased — BREAKING` H2 (lines 5-42), while the
    published entries live under `## Changelog` (line 149).
      - Lift the body of `## Unreleased — BREAKING` out of the top of the file and delete the H2 and
        its blockquote preamble, including the sentence at line 10 asserting that a later phase does
        the bump. Do not soften or reword that sentence into a hedge; remove it.
      - Retitle the antes/después table's right-hand column from the `pendiente de bump` framing to
        `Ahora (0.4.0)`.
      - Insert the resulting body under the existing `## Changelog` H2 as the FIRST `###` child,
        headed exactly `### v0.4.0` on its own line, above the existing `### v0.3.0`. The breaking
        callout must be the first non-blank content of the entry — ROADMAP criterion 1 requires the
        source-breaking callout to come first.
      - The migration rows are locked: `quote.puntas is None` / `quote.puntas or []` →
        `quote.puntas == []` (the `or []` fallback is no longer needed); `titulo.puntas is None` →
        `not titulo.puntas` (or `titulo.puntas == Punta.empty()`). Declared-type facts:
        `Cotizacion.puntas` `list[Punta] | None` → `list[Punta]`; `Titulo.puntas` `Punta | None` →
        `Punta`. The existing prose correctly flags the asymmetry the typechecker cannot catch —
        `Titulo.puntas` stops being the language's null value, so `is None` branches go silently
        dead. Keep that paragraph verbatim.
      - `packages/iol-client/pyproject.toml` line 3 → `0.4.0`. Same awk gate as every other package,
        matched against the tag `iol-client-v0.4.0`.
      - `packages/iol-client/src/iol_client/__init__.py` line 87 → `__version__ = "0.4.0"`. The
        release pipeline does NOT validate this site; if it drifts, the Release ships green and every
        consumer reading `iol_client.__version__` gets the wrong answer, undetectable at release
        time. The local `grep -qx` assertion in this task's verify block is the only defense.
      - Commit all three files together. Subject:
        `chore(iol-client): bump to v0.4.0 (Cotizacion.puntas y Titulo.puntas pierden | None)`.
        `README.md:47` needs no edit — iol's install command carries no version pin.

    (b) **`packages/market-data-client` — move, bump, re-pin, and purge the comment block.**
      - Same move as (a): lift the body of `## Unreleased — BREAKING` (lines 7-34), delete the H2 and
        the blockquote preamble at 9-13 (including the "lo hace la Fase 40" sentence), retitle the
        right-hand column to `Ahora (0.6.0)`, and insert the result as the FIRST `###` under the
        existing `## Changelog` H2, headed exactly `### v0.6.0`, above the existing `### v0.5.0`.
      - The five migration rows are locked in RESEARCH § Migration Table Content: 
        `snapshot.market_data["LA"]["price"]` → `snapshot.market_data.last.price`;
        `snapshot.market_data["BI"]` → `snapshot.market_data.bids` (`list[BookLevel]`);
        `if snapshot.market_data is None:` → `if not snapshot.market_data:`;
        `snapshot.entries is None` → `snapshot.entries == []`;
        `LatestRequest(entries=None)` → `LatestRequest(entries=[])`. Note `staleness_seconds` is
        unchanged (`float | None` at both refs) — do not claim otherwise.
      - **Resolve the divergence note at lines 29-34 according to the recorded B answer.** This note
        currently ends "espera checkpoint del operador" and names this phase as the destination,
        which stops being valid the moment the release merges.
        - If `B-widen-now`: widen `MarketDataSnapshot.market_id` from `str` to `str | None` and
          `.active` from `bool` to `bool | None` in
          `packages/market-data-client/src/market_data_client/models.py`; replace the note with one
          additional row in the migration table documenting the widening; and update
          `packages/market-data-client/tests/test_snapshot_no_data_row.py` in the SAME commit — the
          `assert row.market_id == ""` / `assert row.active is False` pairs (lines 155-156 and
          179-180 plus their `_async` twins), the `assert exc.value.field_path == ".market_id"`
          assertions (lines 261 and 282 plus twins), and the module docstring at lines 61-63 which
          states in prose that the fields are still over-declared. These assertions exist
          specifically to pin the un-widened behavior; updating them is a regression-preservation
          edit, not a test deletion — every one of them must still assert the NEW behavior, and none
          may be removed or `xfail`ed. Scalar leaves typed `T | None` are permitted by D-NO-03, so
          `tools/check_surface_types.py` stays green. Give the code change its own commit,
          `fix(market-data-client): ensanchar market_id/active a Optional sobre la fila no-data (D-12)`,
          landing before the bump commit.
        - If `B-decline-reassign` or `B-decline-unassigned`: keep the divergence documented, but
          rewrite the closing sentence so it no longer names this phase as the destination. It must
          name the concrete phase the operator gave, or say `pendiente, sin fase asignada` in those
          words. Also add a `deferred-items.md` entry in this phase's directory recording the
          disposition. No code and no test changes in this branch.
      - Re-pin BOTH version-pinned install commands. `packages/market-data-client/README.md` is the
        only file in the repo with version-pinned install lines: the git install at line 44 pins the
        prior tag `market-data-client-v0.5.0`, and the wheel URL at line 53 pins both that tag and
        the filename `market_data_client-0.5.0-py3-none-any.whl`. Both move to
        `market-data-client-v0.6.0` and `market_data_client-0.6.0-py3-none-any.whl`. Leaving them
        stale reproduces Phase 34's CR-01 defect verbatim.
      - `packages/market-data-client/src/market_data_client/__init__.py`: DELETE the 15-line comment
        block at lines 163-177 in its entirety — the block headed `# NO BUMPEAR ACÁ (Phase 36 code
        review, WR-04)`, which instructs every future agent not to bump here and names this phase as
        the owner. Every sentence in it becomes false the moment this task lands. Remove it; do not
        soften it, do not rewrite it into a historical note, and do not leave a stub. Then set line
        178 to `__version__ = "0.6.0"`. Leave the `_ = _get_default` line above it untouched.
      - Commit the README, the pyproject and the `__init__.py` together. Subject:
        `chore(market-data-client): bump to v0.6.0 (market_data pasa a Null Object tipado)`.

    (c) **`packages/higyrus-client` — resolve according to the recorded A answer.**
      - If `A-fold-higyrus`: retitle the heading at line 131 to exactly `### v0.3.0` (drop the
        trailing "not yet published" qualifier), delete the stale paragraph at lines 133-137 in its
        entirety — it names Phase 34 as executor, an assertion Phase 34 itself invalidated — and
        leave the body at 139-162 verbatim; it is already complete and correctly voiced. Then bump
        `pyproject.toml` line 3 to `0.3.0` and
        `packages/higyrus-client/src/higyrus_client/__init__.py` line 109 to
        `__version__ = "0.3.0"`. The migration row is `health["status"]` → `health.status`, plus the
        truthiness flip (an empty dict is falsy; a dataclass instance is always truthy), the new
        exported `Health` model, the `health.to_dict()` escape hatch, and the fact that a `204` or
        empty body collapses to a zero-valued `Health` and raises `HigyrusDecodeError` under
        `strict_decode` — all of that is already written in the existing body. Commit all three files
        together. Subject:
        `chore(higyrus-client): bump to v0.3.0 (get_health devuelve Health tipado)`.
      - If `A-decline-reassign` or `A-decline-unassigned`: touch NO version site. Correct the
        paragraph at lines 133-137 anyway — it must stop naming Phase 34 as the executor and must
        name either the concrete destination phase the operator gave or say
        `pendiente, sin fase asignada` in those words. Keep the heading's "not yet published"
        qualifier, because it remains true. Add a `deferred-items.md` entry recording the
        disposition. Commit the README alone. Subject:
        `docs(higyrus-client): reasignar el bump pendiente y dejar de citar a Phase 34`.

    (d) After every branch is applied, sweep for any surviving stale claim across all six package
    READMEs. No `packages/*/README.md` may still contain the literal `Fase 40`, no
    `packages/higyrus-client/README.md` may still contain `Phase 34`, no iol or market-data README
    may still contain an `## Unreleased` H2, and
    `packages/market-data-client/src/market_data_client/__init__.py` may no longer contain
    `NO BUMPEAR`. These four negative checks are asserted mechanically in this task's verify block.

    Touch NO other package: `ambito-financiero-client` and `wallets-client` keep their current
    versions at every site (D-01). Touch no file under `.github/workflows/`. Do NOT run `uv lock` —
    Task 4 owns the single refresh (D-10). Do NOT create a branch, open a PR, create a tag or push
    anything in this task.
  </action>
  <verify>
    <automated>IV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/iol-client/pyproject.toml) && test "$IV" = "0.4.0" && grep -qx '__version__ = "0.4.0"' packages/iol-client/src/iol_client/__init__.py && MV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml) && test "$MV" = "0.6.0" && grep -qx '__version__ = "0.6.0"' packages/market-data-client/src/market_data_client/__init__.py && grep -qx '### v0.4.0' packages/iol-client/README.md && grep -qx '### v0.6.0' packages/market-data-client/README.md && test "$(awk '/^## Changelog$/{f=1;next} f&&/^### /{print;exit}' packages/iol-client/README.md)" = "### v0.4.0" && test "$(awk '/^## Changelog$/{f=1;next} f&&/^### /{print;exit}' packages/market-data-client/README.md)" = "### v0.6.0" && ISEC=$(awk '/^### v0\.4\.0$/{f=1;next} /^### v/{f=0} f' packages/iol-client/README.md) && test -n "$ISEC" && for T in '`puntas`' '`Cotizacion`' '`Titulo`' '`Punta`'; do printf '%s\n' "$ISEC" | grep -qF -- "$T" || { echo "IOL CALLOUT REGRESSED: $T"; exit 1; }; done && MSEC=$(awk '/^### v0\.6\.0$/{f=1;next} /^### v/{f=0} f' packages/market-data-client/README.md) && test -n "$MSEC" && for T in 'market_data.last.price' 'market_data.bids' 'not snapshot.market_data' '`MarketDataEntries`' '`BookLevel`' '`LatestRequest`'; do printf '%s\n' "$MSEC" | grep -qF -- "$T" || { echo "MD CALLOUT REGRESSED: $T"; exit 1; }; done && ! grep -rq 'Fase 40' packages/*/README.md && ! grep -q 'Phase 34' packages/higyrus-client/README.md && ! grep -q '^## Unreleased' packages/iol-client/README.md && ! grep -q '^## Unreleased' packages/market-data-client/README.md && ! grep -q 'NO BUMPEAR' packages/market-data-client/src/market_data_client/__init__.py && test "$(grep -c 'market-data-client-v0.6.0' packages/market-data-client/README.md)" = "2" && test "$(grep -c 'market_data_client-0.6.0-py3-none-any.whl' packages/market-data-client/README.md)" = "1" && ! grep -q 'market-data-client-v0.5.0' packages/market-data-client/README.md && ! grep -q 'market_data_client-0.5.0-py3-none-any.whl' packages/market-data-client/README.md && for P in ambito-financiero-client wallets-client; do V=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' "packages/$P/pyproject.toml"); test "$V" = "0.2.0" || { echo "UNCHANGED PACKAGE BUMPED: $P=$V"; exit 1; }; done && HV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/higyrus-client/pyproject.toml) && HI=$(grep -o '"[0-9.]*"' <<<"$(grep '^__version__' packages/higyrus-client/src/higyrus_client/__init__.py)" | tr -d '"') && test "$HV" = "$HI" && { { test "$HV" = "0.3.0" && grep -qx '### v0.3.0' packages/higyrus-client/README.md; } || { test "$HV" = "0.2.0" && grep -q 'sin fase asignada\|Phase 4\|Fase 4' packages/higyrus-client/README.md; }; } && test -z "$(git diff --name-only HEAD~4..HEAD -- .github/workflows uv.lock)" && uv run pre-commit run --all-files && uv run pytest packages/iol-client -q && uv run pytest packages/market-data-client -q && uv run pytest packages/higyrus-client -q && uv run python tools/check_surface_types.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/iol-client/pyproject.toml` outputs `0.4.0` and `grep -qx '__version__ = "0.4.0"' packages/iol-client/src/iol_client/__init__.py` exits 0
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml` outputs `0.6.0` and `grep -qx '__version__ = "0.6.0"' packages/market-data-client/src/market_data_client/__init__.py` exits 0
    - The FIRST `###` heading after `^## Changelog$` is `### v0.4.0` in `packages/iol-client/README.md` and `### v0.6.0` in `packages/market-data-client/README.md` — the new entry is first, above the prior version, as ROADMAP criterion 1 requires
    - Slicing each README on its new exact heading yields a non-empty section that still carries the pre-written callout tokens: iol keeps `` `puntas` ``, `` `Cotizacion` ``, `` `Titulo` ``, `` `Punta` ``; market-data keeps `market_data.last.price`, `market_data.bids`, `not snapshot.market_data`, `` `MarketDataEntries` ``, `` `BookLevel` ``, `` `LatestRequest` `` — the move did not silently drop the body
    - `grep -rq 'Fase 40' packages/*/README.md` finds nothing — no package README still claims a future phase does the bump
    - `grep -q 'Phase 34' packages/higyrus-client/README.md` finds nothing — the invalidated executor claim is gone in BOTH branches of the A answer
    - `grep -q '^## Unreleased' packages/iol-client/README.md` and the same on `packages/market-data-client/README.md` both find nothing — the provisional H2 sections were moved, not left behind
    - `grep -q 'NO BUMPEAR' packages/market-data-client/src/market_data_client/__init__.py` finds nothing — the 15-line stale comment block is deleted, not softened (RESEARCH P8)
    - `grep -c 'market-data-client-v0.6.0' packages/market-data-client/README.md` outputs `2` (the git-install pin and the wheel-URL pin) and `grep -c 'market_data_client-0.6.0-py3-none-any.whl'` outputs `1`; the prior tag string and the prior wheel filename appear zero times
    - `ambito-financiero-client` and `wallets-client` both still read `0.2.0` at `pyproject.toml` — neither unchanged package was bumped (D-01)
    - `higyrus-client`'s `pyproject.toml` version and its `__version__` agree with each other, and match the recorded A answer: `0.3.0` with an exact-line `### v0.3.0` heading if `A-fold-higyrus`, or `0.2.0` with a README that names a concrete destination phase or says `pendiente, sin fase asignada` otherwise
    - If the recorded B answer is `B-widen-now`: `MarketDataSnapshot.market_id` and `.active` are declared Optional in `models.py`; every assertion in `test_snapshot_no_data_row.py` that previously pinned the un-widened behavior now pins the new behavior; none was deleted, skipped or `xfail`ed; and the module docstring no longer states the fields are still over-declared. If the answer is either decline: `models.py` and `test_snapshot_no_data_row.py` are untouched and the README note names a concrete destination or says `pendiente, sin fase asignada`
    - For every decline branch selected in Task 1, a `deferred-items.md` entry exists in `.planning/phases/40-releases-breaking-coordinados/` recording the disposition
    - `git diff --name-only HEAD~4..HEAD -- .github/workflows uv.lock` is empty — no workflow file and no lockfile entered any commit of this task
    - `uv run pre-commit run --all-files` passes with zero file rewrites, `uv run python tools/check_surface_types.py` reports zero violations, and `uv run pytest packages/{iol,market-data,higyrus}-client -q` all pass. No bare `uv run pytest` was executed
    - Every commit in this task follows the `chore(<pkg>): bump to v<X.Y.Z> (<headline>)` or `docs(<pkg>): …` convention, one coherent unit per commit
  </acceptance_criteria>
  <done>iol-client reads 0.4.0 and market-data-client reads 0.6.0 at both version sites each, both `## Unreleased — BREAKING` blocks have been moved into their `## Changelog` sections as the first dated entry, market-data's two install pins point at the new version, the stale do-not-bump comment block is gone, no README still names a future phase as the bump executor, and both scope answers from Task 1 are fully resolved in the tree.</done>
</task>

<task type="auto">
  <name>Task 4: Single `uv lock` + `uv sync`, full local CI mirror, credential scan, and fast-forward push of the NEW release branch (D-05, D-10, RESEARCH P3/P6)</name>
  <files>uv.lock</files>
  <read_first>
    - `uv.lock` — the `[[package]]` member blocks whose `version` lines a correct refresh changes:
      `ambito-financiero-client` at 21/22, `higyrus-client` at 283/284, `iol-client` at 383/384,
      `market-data-client` at 487/488, `matriz-client` at 548/549, `wallets-client` at 906/907.
      Only the bumped members' version lines may move.
    - `.github/workflows/ci.yml` lines 22-146 — READ-ONLY. All four job bodies. The `lint` job now
      runs EIGHT repo gates beyond ruff: `uv lock --check`, `uv sync --frozen`, `ruff check`,
      `ruff format --check`, `lint-imports`, the `logging.basicConfig` / `logging.root` grep over
      `packages/*/src/`, `tools/check_decode_intactness.py`, `tools/check_uniform_structure.py`,
      `tools/check_surface_types.py`, and an explicit 12-file `pytest -q verification/…` allowlist at
      lines 80-92. The `typecheck` job runs `uv run mypy` AND `uv run mypy packages/<pkg>/tests` for
      all six packages. The `test` job runs `pytest packages/<pkg>` per matrix cell.
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § `uv.lock` member blocks,
      § Common Pitfalls P3 (the `uv sync` requirement), P4 (never bare pytest), P6 (the branch
      topology) and P7, § Code Examples → the `uv.lock` churn assertion, and § Security Domain → the
      credential-scan patterns.
    - `.planning/phases/40-releases-breaking-coordinados/40-PATTERNS.md` § Shared Patterns →
      "Credential scan before the public push" and "Workflow immutability — DO NOT copy the Phase 34
      form".
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-01-PLAN.md` lines 388-493 (Task 3)
      — the lock/mirror/scan/push shape. FOUR corrections apply here and are spelled out in the
      action below: the churn literal, the mandatory `uv sync`, the eight-gate lint mirror, and the
      replaced workflow-immutability assertion. Its `git push origin <existing-branch>` line must NOT
      be copy-pasted — this phase creates the branch first (P6).
    - The Task 2 SUMMARY entry recording `PHASE_BASE` and the run-time `AHEAD` count.
  </read_first>
  <action>
    Refresh the lockfile once, re-sync the environment, run every gate CI will run, prove the diff
    about to become public carries no credential, then create and publish the release branch.

    (a) Run `uv lock` — exactly once, and only now (D-10). It MUST run AFTER every `pyproject.toml`
    edit from Tasks 2 and 3: `uv lock` reads workspace-member versions from `pyproject.toml`, so an
    earlier run would regenerate the lock at the old versions and fail CI check 1 of 15
    (`ci.yml:32-33`). Never hand-edit `uv.lock`.

    Then assert the churn is exactly one line per bumped member. Compute N at run time as the number
    of packages whose `pyproject.toml` differs from `origin/main` — do not hard-code 3 or 4, because
    the Task 1 answer decides it. `git show --numstat --format= HEAD -- uv.lock` must report exactly
    `N N`. Phase 34 measured `2 2` for two members, confirming one line per member. Anything wider
    means uv re-resolved third-party dependencies and would smuggle an unreviewed dependency change
    into a public release: STOP and surface it rather than committing.

    Run `uv lock --check` (exit 0), then confirm the line following `^name = "<pkg>"$` is
    `version = "<new>"` for each bumped member.

    Commit `uv.lock` alone. Subject:
    `chore(deps): refrescar uv.lock para los N paquetes bumpeados de v1.7` — with N and the actual
    package/version list spelled out in the body.

    (b) Run `uv sync --all-packages --all-extras --dev` immediately after the lock commit and BEFORE
    the mirror. This has no Phase 34 analog and is mandatory here (RESEARCH P3):
    `packages/market-data-client/tests/test_version_metadata.py` has
    `test_dunder_version_matches_installed_distribution_metadata`, which compares
    `market_data_client.__version__` against `importlib.metadata.version("market-data-client")`.
    After the `__init__.py` edit, the editable install's `.dist-info` still reports the OLD version
    until the environment is re-synced. The local mirror goes red while CI stays green (CI does a
    fresh `uv sync --frozen`), and an agent seeing that red test after a version bump can easily
    misdiagnose it as a real break. Only `market-data-client` has this test.

    (c) Mirror EVERY CI job body locally, in this order, each must succeed. This is eight lint gates,
    not the four Phase 34 ran:
      `uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`;
      the logging gate — grep `packages/*/src/` for `logging.basicConfig(` calls and `logging.root.`
      attribute access and require NO match, using the same extended regex `ci.yml` runs;
      `uv run python tools/check_decode_intactness.py`;
      `uv run python tools/check_uniform_structure.py`;
      `uv run python tools/check_surface_types.py`;
      the 12-file verification allowlist exactly as `ci.yml:80-92` spells it —
      `uv run pytest -q verification/test_main_market_data_deep_chain.py
      verification/test_safemodel_diff_null_object_links.py
      verification/test_main_matriz_risk_envelope_keys.py
      verification/test_safemodel_diff_mapping_recursion.py
      verification/test_main_verify_classification.py
      verification/test_main_matriz_skip_line_shape.py
      verification/test_main_higyrus_skip_line_shape.py verification/test_run_evidence.py
      verification/test_main_iol_deep_chain.py verification/test_main_higyrus_deep_chain.py
      verification/test_main_matriz_deep_chain.py verification/test_cycle_closure_phase33.py`;
      `uv run pre-commit run --all-files`;
      `uv run mypy`;
      `uv run mypy packages/<pkg>/tests` for all six packages;
      `uv run pytest packages/<pkg> -q` for all six packages.

    NEVER run a bare `uv run pytest`: the root `testpaths` collects `verification/`, which carries
    known pre-existing failures that no CI job executes (backlog `HARN-VERIF-01`); a bare run would
    block this release indefinitely on out-of-scope failures. If `pre-commit` rewrites any file,
    amend the owning commit with the fix and re-run the whole mirror from the top. Never patch
    `ci.yml` or `release.yml` to make a gate pass.

    (d) Assert the workflow files are untouched — using the CORRECTED form, not Phase 34's. Phase
    34's assertion (`git diff --name-only <prior-release-tag>..HEAD -- .github/workflows` must be
    empty) is broken and failed three times during that phase, because `ci.yml` legitimately changes
    between releases: Phases 36, 37 and 39 all added lint steps. The two assertions that are actually
    true and actually meaningful:
      - `release.yml` is byte-identical everywhere. Take its sha256 at `HEAD`, at `origin/main` and
        at the last published tag of each bumped package; `sort -u` over those digests must yield
        exactly one line.
      - THIS PHASE touched no workflow file. Assert
        `git diff --name-only $PHASE_BASE..HEAD -- .github/workflows` produces zero lines, using the
        `PHASE_BASE` SHA that Task 2 recorded. Also assert the diff of `release.yml` between
        `origin/main` and `HEAD` is empty, which holds regardless of baseline.

    (e) Re-run the credential scan over the FULL diff this PR will publish to a PUBLIC default
    branch. `git fetch origin main --quiet` first, then check `git diff origin/main...HEAD` for a
    JWT-shaped token (`eyJ[A-Za-z0-9_-]{20,}`) and for a `client_secret`-style assignment of a 20+
    character value; separately assert that no TRACKED file has a path component equal to `.env`
    (per CLAUDE.md, per-package `.env` files exist on disk — confirm none is tracked; only the
    `.env.example` templates are legitimate). If ANY of these matches, STOP: do not push, do not
    proceed to plan 40-02, and surface the exact file and line. Never echo a token, a secret value,
    the `gh` OAuth token or the SSH key into output — report file and line only. This scan must run
    here, immediately before the push, because the diff spans every commit of Phases 35-40 and
    becomes permanently public on merge.

    (f) Create the branch, THEN push it. HEAD currently sits on local `main`, which is where all of
    this milestone's pending commits live — the inverse of Phase 34's topology, where the commits
    were already on a milestone branch (RESEARCH P6). Run
    `git checkout -b milestone/v1.7-nobj-null-objects` (D-05 locks this name, following the
    `milestone/v1.5-mutations` precedent); local `main` stays at the same SHA. Then
    `git push origin milestone/v1.7-nobj-null-objects` — a plain fast-forward, by name.

    A `git push origin main` here would publish every pending commit directly to the public default
    branch with no PR, no CI gate and no human approval. Do not run it. `--force` and
    `--force-with-lease` are FORBIDDEN: a force-push would rewrite the commits whose SHAs the Phase
    35-39 SUMMARYs cross-reference by value. If the push is rejected as non-fast-forward, STOP and
    surface it — do not escalate to a force flag, do not rebase, and do not merge `origin/main` into
    the branch.

    (g) Do NOT run `/gsd-pr-branch` and do NOT filter `.planning/` out of the branch — the planning
    artifacts ship with the release, as they have in every prior release of this repo. Do NOT open,
    edit or merge any PR in this plan; plan 40-02 owns the PR. Do NOT create or push any tag.
  </action>
  <verify>
    <automated>git fetch origin main --quiet && N=$(for P in ambito-financiero-client higyrus-client iol-client market-data-client matriz-client wallets-client; do git diff --name-only origin/main...HEAD -- "packages/$P/pyproject.toml"; done | wc -l | tr -d ' ') && test "$N" -ge 3 && uv lock --check && grep -A1 '^name = "matriz-client"$' uv.lock | grep -qx 'version = "0.3.0"' && grep -A1 '^name = "iol-client"$' uv.lock | grep -qx 'version = "0.4.0"' && grep -A1 '^name = "market-data-client"$' uv.lock | grep -qx 'version = "0.6.0"' && grep -A1 '^name = "ambito-financiero-client"$' uv.lock | grep -qx 'version = "0.2.0"' && grep -A1 '^name = "wallets-client"$' uv.lock | grep -qx 'version = "0.2.0"' && LOCKC=$(git log --format=%H origin/main..HEAD -- uv.lock | wc -l | tr -d ' ') && test "$LOCKC" = "1" && test "$(git show --numstat --format= "$(git log --format=%H origin/main..HEAD -- uv.lock)" -- uv.lock | awk '{print $1, $2}')" = "$N $N" && uv sync --all-packages --all-extras --dev && uv run ruff check . && uv run ruff format --check . && uv run lint-imports && ! grep -rnE --include='*.py' 'logging\.basicConfig\s*\(|logging\.root\.\w' packages/*/src/ && uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py && uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_run_evidence.py verification/test_main_iol_deep_chain.py verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py verification/test_cycle_closure_phase33.py && uv run pre-commit run --all-files && uv run mypy && for P in higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client; do uv run mypy "packages/$P/tests" || exit 1; done && for P in higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client; do uv run pytest "packages/$P" -q || exit 1; done && test "$(for R in HEAD origin/main iol-client-v0.3.0 market-data-client-v0.5.0 matriz-client-v0.2.0 higyrus-client-v0.2.0; do git show "$R:.github/workflows/release.yml" | shasum -a 256 | cut -d' ' -f1; done | sort -u | wc -l | tr -d ' ')" = "1" && test "$(git diff --name-only origin/main...HEAD -- .github/workflows/release.yml | wc -l | tr -d ' ')" = "0" && ! git diff origin/main...HEAD | grep -qE 'eyJ[A-Za-z0-9_-]{20,}' && ! git diff origin/main...HEAD | grep -qiE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}' && test -z "$(git ls-files | grep -E '(^|/)\.env$')" && test "$(git rev-parse --abbrev-ref HEAD)" = "milestone/v1.7-nobj-null-objects" && git fetch origin milestone/v1.7-nobj-null-objects --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/milestone/v1.7-nobj-null-objects)" && test -z "$(git status --porcelain)" && test -z "$(git tag -l 'matriz-client-v0.3.0')" && test -z "$(git tag -l 'iol-client-v0.4.0')" && test -z "$(git tag -l 'market-data-client-v0.6.0')" && test -z "$(git tag -l 'higyrus-client-v0.3.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` exits 0
    - Exactly ONE commit in `origin/main..HEAD` touches `uv.lock` (`git log --format=%H origin/main..HEAD -- uv.lock | wc -l` outputs `1`) — the single-refresh rule of D-10 is proven, not assumed
    - `git show --numstat --format= <that commit> -- uv.lock | awk '{print $1, $2}'` outputs `N N`, where N is the run-time-computed count of packages whose `pyproject.toml` differs from `origin/main`. A wider churn means uv re-resolved third-party dependencies and the task halted instead of committing
    - The line following `^name = "matriz-client"$` is `version = "0.3.0"`, after `^name = "iol-client"$` is `version = "0.4.0"`, after `^name = "market-data-client"$` is `version = "0.6.0"`; `ambito-financiero-client` and `wallets-client` both still read `version = "0.2.0"`; `higyrus-client` matches the recorded A answer
    - `git log -1 --format=%s` on the lock commit starts with `chore(deps): refrescar uv.lock` and `git show --name-only --format=` on it lists only `uv.lock`
    - `uv sync --all-packages --all-extras --dev` ran after `uv lock` and before the mirror (RESEARCH P3), and `uv run pytest packages/market-data-client -q` — including `test_dunder_version_matches_installed_distribution_metadata` — passes
    - All eight lint gates exit 0: `uv lock --check`, `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`, the `packages/*/src/` logging grep finding no match, `tools/check_decode_intactness.py`, `tools/check_uniform_structure.py`, `tools/check_surface_types.py`, plus the 12-file `verification/` allowlist exactly as `ci.yml:80-92` lists it
    - `uv run pre-commit run --all-files` passes with zero file rewrites; `uv run mypy` succeeds; `uv run mypy packages/<pkg>/tests` succeeds for all six packages; `uv run pytest packages/<pkg> -q` passes for all six packages
    - No bare `uv run pytest` was executed at any point in this plan
    - The sha256 of `.github/workflows/release.yml` is identical at `HEAD`, `origin/main` and every bumped package's prior release tag — `sort -u` over those digests yields exactly one line. Phase 34's `git diff --name-only <prior-release-tag>..HEAD -- .github/workflows` form was NOT used; it is stale-by-construction and failed three times in that phase (RESEARCH P2)
    - `git diff --name-only $PHASE_BASE..HEAD -- .github/workflows | wc -l` outputs `0`, using the `PHASE_BASE` SHA recorded in Task 2, and `git diff --name-only origin/main...HEAD -- .github/workflows/release.yml | wc -l` outputs `0`
    - `git diff origin/main...HEAD | grep -E 'eyJ[A-Za-z0-9_-]{20,}'` produces no match, `git diff origin/main...HEAD | grep -iE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}'` produces no match, and `git ls-files | grep -E '(^|/)\.env$'` is empty. Any finding reports file and line only, never the matched value
    - `git rev-parse --abbrev-ref HEAD` is `milestone/v1.7-nobj-null-objects` — the branch was CREATED in this task; the command history shows `git checkout -b milestone/v1.7-nobj-null-objects` before any push
    - `git rev-parse HEAD` equals `git rev-parse origin/milestone/v1.7-nobj-null-objects`, and the command history shows `git push origin milestone/v1.7-nobj-null-objects` with no `--force`, no `--force-with-lease`, no `git rebase`, no `git merge origin/main`, and **no `git push origin main` anywhere in the phase**
    - `git status --porcelain` is empty (clean tree — precondition for plan 40-02)
    - No tag exists for any bumped package at the end of this plan
  </acceptance_criteria>
  <done>`uv.lock` was refreshed by a single `uv lock` run registering every bumped member with an exact N-insertion/N-deletion churn, `uv sync` re-installed the workspace, every local CI-mirror gate across all four job bodies is green, `release.yml` is provably byte-identical across every relevant ref, the credential scan over `origin/main...HEAD` is clean, and the newly created `milestone/v1.7-nobj-null-objects` exists on `origin` at local HEAD via a plain fast-forward push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → public GitHub repo | `gravity-quant/market-libs` is a PUBLIC repository. This push publishes the whole Phase 35-40 diff, including the `.planning/` artifacts. Everything crossing this boundary becomes permanently public. |
| operator scope decision → the bump set | The Task 1 answer determines which packages get a version, a tag and a public Release. An agent-invented answer would publish (or withhold) a package the operator never authorized. |
| `pyproject.toml` version strings → `release.yml` build gate | The pipeline reads each package's version from the tagged tree with its own `awk` expression and refuses to build on mismatch; a wrong value produces a failed or mislabelled public artifact. Three or four independent instances of this boundary. |
| `uv lock` resolver → committed `uv.lock` | An unreviewed third-party dependency re-resolution can enter the repo through a lockfile refresh nobody reads. |
| local branch selection → `origin/main` | HEAD is on local `main` and 186 commits ahead of `origin/main`. A mistargeted push bypasses the PR, the CI gate and both human gates in one command. |
| agent shell → GitHub credentials (`gh` OAuth token, SSH key) | Credentials are ambient in the environment; any echo lands in transcripts and logs. |
| local branch history → published history | Dozens of SUMMARY files across Phases 35-39 cite commit SHAs by value; a history rewrite orphans every one of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information Disclosure | the `origin/main...HEAD` diff published to a PUBLIC default branch | mitigate | Task 4 (e) re-runs the credential scan immediately before the push — JWT pattern `eyJ[A-Za-z0-9_-]{20,}`, `client_secret`-style assignments, and any tracked `.env` — and STOPS the phase on any match. Findings report file and line only, never the matched value. ASVS L1 V14, V2. |
| T-40-02 | Tampering | version strings vs. the release tags, ×3 or ×4 packages | mitigate | Tasks 2-3 assert `pyproject` == `__version__` per package using `release.yml:47`'s own awk expression, and Task 4 asserts every `uv.lock` member agrees. The pipeline validates ONLY `pyproject.toml`, so a drifted `__version__` would ship a silently mislabelled artifact — the local `grep -qx` assertions are the sole defense. ASVS L1 V5. |
| T-40-03 | Tampering | wrong package set published | mitigate | Task 1 fixes the set by explicit operator decision before any edit; Task 3's verify asserts `ambito-financiero-client` and `wallets-client` still read `0.2.0`, and Task 4 recomputes N from the actual `pyproject.toml` diff against `origin/main` rather than trusting a literal. |
| T-40-04 | Supply Chain / Tampering | `uv.lock` refresh | mitigate | Task 4 (a) asserts exactly one lock-touching commit in `origin/main..HEAD` and a churn of exactly `N N`; any wider diff means a third-party re-resolution and halts the task before commit. |
| T-40-05 | Tampering / Repudiation | published git history (Phases 35-39 SHAs) | mitigate | Task 4 (f) forbids `--force` / `--force-with-lease`, forbids rebasing or merging `origin/main` into the branch, and the push is a plain fast-forward; a rejection halts the task instead of escalating. |
| T-40-06 | Elevation of Privilege | pushing 186 commits straight to the unprotected public default branch | mitigate | `main` has NO branch protection (`gh api …/branches/main/protection` → 404), so nothing server-side would stop `git push origin main`. Task 4 (f) mandates `git checkout -b` first and the acceptance criteria assert that no `git push origin main` appears anywhere in the phase. ASVS L1 V4. |
| T-40-07 | Elevation of Privilege / Repudiation | the agent silently resolving D-02 or D-12 | mitigate | Task 1 is a blocking `checkpoint:decision` that modifies no file; its verify asserts every version site is still at its pre-phase value and HEAD is still on `main`. The answer is recorded verbatim with a timestamp. `auto_advance: true` and `mode: yolo` are active and explicitly do not satisfy it. ASVS L1 V4. |
| T-40-08 | Information Disclosure | `gh` OAuth token / SSH key in agent output | mitigate | Never echo a token or secret value; `gh auth status` already redacts. Scan findings report file and line only. ASVS L1 V2. |
| T-40-09 | Tampering | patching a CI gate to make it pass | mitigate | `.github/workflows/` is read-only for this phase. Task 4 (d) proves it with sha256 identity of `release.yml` across refs plus a zero-line workflow diff since `PHASE_BASE`. Phase 34 hit a red mypy check and fixed the test, never the workflow — same rule here. ASVS L1 V14. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero external packages. `uv lock` re-resolves the existing dependency set with only local workspace members' version strings changed, and the `N N` churn assertion proves no third-party change entered. RESEARCH § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` packages, so no legitimacy checkpoint is applicable. The only install-shaped operation in the phase is 40-03 Task 3, which installs THIS repository's own wheels from their public Release asset URLs — never a bare package name, per the slopsquat guard already documented at `packages/market-data-client/README.md:38-40`. |
</threat_model>

<verification>
1. The Task 1 scope gate ran BEFORE any `pyproject.toml` edit and before `uv lock`; both answers are recorded verbatim with option ids and a timestamp.
2. `packages/matriz-client/README.md` has a `## Changelog` section that did not exist before, with `### v0.3.0` as its only entry, a four-row migration table, the envelope-unwrap behavior fix in prose, and the six aliases documented separately as additive.
3. iol-client reads 0.4.0, market-data-client reads 0.6.0 and matriz-client reads 0.3.0 at every version site each; higyrus-client matches the recorded A answer.
4. Both `## Unreleased — BREAKING` H2 blocks were MOVED into their `## Changelog` sections as the first dated entry, with their bodies intact.
5. `Fase 40` appears in no package README, `Phase 34` appears in no higyrus README, and the `NO BUMPEAR` comment block is gone from `market_data_client/__init__.py`.
6. market-data's two version-pinned install commands point at the new version; the prior pins appear zero times.
7. `ambito-financiero-client` and `wallets-client` have a changed version at no site.
8. Exactly one commit touches `uv.lock`, its churn is `N N`, and `uv lock --check` exits 0.
9. `uv sync` ran between `uv lock` and the mirror; every gate of all four CI job bodies is green locally.
10. `release.yml` is byte-identical across `HEAD`, `origin/main` and every prior release tag; no workflow file changed since `PHASE_BASE`.
11. The credential scan over `origin/main...HEAD` is clean and no `.env` is tracked.
12. `origin/milestone/v1.7-nobj-null-objects` == local HEAD, created with `git checkout -b` and pushed fast-forward; no `git push origin main` was ever run.
13. No tag exists and no PR was touched at the end of this plan.
</verification>

<success_criteria>
- Neither open scope question was resolved in silence, and neither was answered after a point where answering it would have forced a second `uv lock` (D-10) or a re-presented CI gate.
- Every edit in this plan is reversible with a commit; nothing outward-facing and irreversible has happened.
- Every bumped package is release-ready with a changelog whose breaking callout comes FIRST and whose migration table a consumer can execute line by line.
- The lockfile was refreshed exactly once for all bumps and provably introduced no third-party dependency change (D-10).
- The release branch exists on `origin` at local HEAD, unblocking `gh pr create` in plan 40-02 (D-05) — and `main` was never pushed to directly.
- The two blocking human gates on irreversible operations remain ahead, in plans 40-02 and 40-03; this plan added none of them (D-07).
</success_criteria>

<output>
Create `.planning/phases/40-releases-breaking-coordinados/40-01-SUMMARY.md` when done.

Record in the SUMMARY: the `Scope-gate dispositions (D-02, D-12)` heading with the operator's
verbatim reply, its timestamp, both selected option ids and any named destination phase; the final
package set N and the exact version transitions; `PHASE_BASE`; the run-time-measured ahead count
(never the 180 / 182 / 186 literals from CONTEXT, RESEARCH or this plan); every commit SHA with its
file count; the measured `uv.lock` churn; the full local-mirror results including all eight lint
gates, the six `mypy … /tests` runs and the six per-package pytest counts; the credential-scan
result; the `release.yml` sha256 and the refs it was compared across; and the explicit scope
decisions from `<scope_decisions>` (matriz `__version__` added with no new test file; no in-repo
release-memory file exists to refresh; ambito/wallets classified additive rather than unchanged).
</output>
