---
phase: 38-iol-client-auditor-a-de-higyrus-mbito-wallets
plan: 03
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - packages/iol-client/README.md
  - .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md
autonomous: true
requirements: [NOBJ-IOL-01]

must_haves:
  truths:
    - "A consumer installing `iol-client` from `main` can read, in the package README, exactly which expressions stop working and what replaces them — including that `Titulo.puntas` is no longer the language's null value, so an identity check against nothing silently stops firing"
    - "The README callout assumes no version number: it is headed `## Unreleased — BREAKING` and names the last published tag as the comparison anchor, leaving the bump to Phase 40"
    - "`35-RETIRED-TRIPLES.md` cites no stale source line: every `iol_client/models.py` and `_decode.py` reference in it resolves to the branch or declaration it names at the current HEAD"
    - "Phase 39 can read, from the ledger it already treats as the single source for the middle term, that Phase 38 added 2 field rows to the NOBJ-02 disposition and retired 0 triples — and that the invariance is itself the finding, not an absence of measurement"
  artifacts:
    - path: "packages/iol-client/README.md"
      provides: "The `## Unreleased — BREAKING` callout with the two-row migration table and the truthiness-asymmetry paragraph"
      contains: "## Unreleased — BREAKING"
    - path: ".planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md"
      provides: "Corrected source line references plus a Phase-38 accounting addendum"
      contains: "iol_client/models.py:213"
  key_links:
    - from: "packages/iol-client/README.md"
      to: "packages/market-data-client/README.md"
      via: "the four-part `## Unreleased — BREAKING` structure copied in form from the same-milestone Phase 36 precedent (D-10)"
      pattern: "Unreleased — BREAKING"
    - from: ".planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md"
      to: ".planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md"
      via: "the Phase-38 addendum is the durable record Phase 39 reads; the census cross-references it rather than duplicating it (RESEARCH Open Question 2)"
      pattern: "38-CENSUS.md"
  prohibitions:
    - statement: "MUST NOT describe the retype to consumers as a type-annotation change alone — the callout must name the runtime consequence that an identity check against nothing stops firing on Titulo.puntas, which is the half a typechecker does not catch"
      status: resolved
      verification: judgment
    - statement: "MUST NOT assume or assert a version number for the unreleased change — Phase 40 owns the bump, and a README that pre-names a version becomes a false claim if the bump lands differently"
      status: resolved
      verification: test
    - statement: "MUST NOT leave the Phase-35 ledger half-corrected — either every stale source reference in it is fixed or none is; a ledger that is authoritative in one paragraph and wrong in another is worse than an uncorrected one"
      status: resolved
      verification: test
---

<objective>
Publish the breaking change to consumers and settle the accounting Phase 38 owes Phase 39.
Two documentation artifacts: a `## Unreleased — BREAKING` callout in the iol README with the
old-to-new migration table (D-10), and a correction plus a Phase-38 addendum in
`35-RETIRED-TRIPLES.md` (D-12).

Purpose: the `Titulo.puntas` change is the dangerous kind — the value stays falsy, so nothing
crashes, but it is no longer the language's null value, so every identity check against
nothing silently stops firing. That is invisible to mypy and invisible to a test suite that
does not look for it. It has to be written down where a consumer reads it. Separately, Phase
39 computes its census as a set subtraction against `35-RETIRED-TRIPLES.md`; if this phase
leaves that file citing lines that no longer exist and says nothing about what it changed,
Phase 39 has to guess.

Output: one new README section, corrected line references throughout the Phase-35 ledger, and
a delimited Phase-38 accounting addendum at its end.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CONTEXT.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-RESEARCH.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-PATTERNS.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-01-SUMMARY.md
</context>

## Artifacts this phase produces (plan 38-03)

Newly-created content introduced by this plan. Drift verification must treat these as
net-new, not as references to pre-existing sections:

| Artifact | File | Kind |
|---|---|---|
| `## Unreleased — BREAKING` section | `packages/iol-client/README.md` | new top-level markdown section (heading + blockquote + 2-row table + prose paragraph) |
| `## Phase 38 addendum` section | `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md` | new delimited section appended at end of file |

No code symbols are created by this plan.

<!-- planner-discipline-allow: iol_client/models.py:154 -->
<!-- planner-discipline-allow: _decode.py:431-435 -->
<!-- planner-discipline-allow: _decode.py:443-445 -->
<!-- planner-discipline-allow: _decode.py:482-484 -->
<!-- planner-discipline-allow: _decode.py:363-367 -->
<!-- planner-discipline-allow: v0.4.0 -->

<reversibility>
| Task | Rating | Rationale |
|---|---|---|
| Task 1 (README callout) | two-way | Documentation. Nothing is published until Phase 40; a revert leaves no consumer-visible trace. |
| Task 2 (ledger correction + addendum) | two-way | A planning artifact. The corrections are factual fixes; the addendum is additive and delimited. |
</reversibility>

<tasks>

<task type="auto">
  <name>Task 1: Add the `## Unreleased — BREAKING` callout to the iol README</name>

  <files>packages/iol-client/README.md</files>

  <read_first>
    - `packages/iol-client/README.md` — the file being edited. Read lines 1-10 (the one-line
      intro and the `## Instalación` heading that bound the insertion point), lines 105-120
      (the `## Changelog` heading and the single `### v0.3.0` entry that is the anchor tag),
      and lines 140-165 (the existing "Flip de truthiness — la parte que el typechecker NO
      atrapa" block and the `to_dict()` lossiness caveat, which are the tone templates).
    - `packages/market-data-client/README.md` lines 1-35 — the D-10 template. Copy its
      four-part structure in form: heading, blockquote naming the last published tag and why
      `main` is not it, a two-column migration table, then a prose paragraph naming the exact
      runtime consequence.
    - `packages/iol-client/src/iol_client/models.py` — the post-38-01 annotations, so the
      migration table describes what actually shipped.
  </read_first>

  <action>
Insert a new `## Unreleased — BREAKING` section into `packages/iol-client/README.md`,
positioned immediately after the one-line intro paragraph (line 3) and immediately BEFORE
`## Instalación` (line 5). This is the identical position the market-data-client README uses.
Write the section in Spanish, matching the rest of this README.

Four parts, in this order:

1. The heading `## Unreleased — BREAKING`. Do NOT write a version-numbered heading (D-10):
   that spelling — the v1.6 higyrus format — pre-assigns the number Phase 40 assigns, and
   would become a false claim if Phase 40 lands a different one.

2. A blockquote stating that `main` is not the published `0.3.0`. The anchor tag is
   `iol-client-v0.3.0`, the only entry in this README's `## Changelog` (around line 112).
   Give the reason, mirroring the analog's wording: the coordinated breaking bump of the six
   packages is done by Phase 40 in a single pass, which refreshes `uv.lock` exactly once.
   Close with the analog's framing — if you install from `main`, this is what changed relative
   to the tagged release.

3. A two-column migration table whose header reads `Antes (0.3.0 publicado)` and
   `Ahora (main, pendiente de bump)`, with exactly two data rows:
   - Cotizacion row: the old null-identity check and the `or []` fallback in the left column;
     `quote.puntas == []` in the right, noting the fallback is no longer needed.
   - Titulo row: the old null-identity check in the left column; `not titulo.puntas` in the
     right, with `titulo.puntas == Punta.empty()` given as the stricter alternative.

4. A prose paragraph naming the runtime consequence, and specifically naming the ASYMMETRY
   between the two rows — this is the part the table alone does not convey and the part a
   typechecker will not catch:
   - `Cotizacion.puntas` goes from a null value to the empty list: falsy before, falsy after,
     so no truthiness branch changes behaviour. Only the declared type moved.
   - `Titulo.puntas` goes from a null value to `Punta.empty()`: still falsy, because
     `SafeModel.__bool__` reports emptiness — but it is no longer the language's null value,
     so a consumer branching on identity-with-nothing stops taking that branch, in silence,
     with no crash and no type error.
   Reuse the framing of the existing "Flip de truthiness — la parte que el typechecker NO
   atrapa" block at lines 140-149 rather than inventing a new voice. State the chained-access
   gain too: `titulo.puntas.precioCompra` is now always valid.

Do not touch the `## Changelog` section, do not add a version entry there, and do not modify
`packages/iol-client/pyproject.toml` or `__version__` — Phase 40 owns all three.
  </action>

  <verify>
    <automated>grep -n "Unreleased — BREAKING" packages/iol-client/README.md</automated>
    <automated>awk '/^## /{print NR": "$0}' packages/iol-client/README.md | head -5</automated>
    <automated>git diff --name-only -- packages/iol-client/ | sort</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^## Unreleased — BREAKING$' packages/iol-client/README.md` returns 1.
    - The first `## ` heading in the file is `## Unreleased — BREAKING` and the second is
      `## Instalación` — confirmed by the `awk` output above.
    - `grep -c 'iol-client-v0.3.0' packages/iol-client/README.md` returns at least 1 (the
      anchor tag is named).
    - `grep -c 'Fase 40' packages/iol-client/README.md` returns at least 1 (the reason `main`
      carries an unbumped break is stated).
    - `grep -v '^#' packages/iol-client/README.md | grep -c 'v0\.4\.0'` returns 0 — no version
      number is assumed for the unreleased change.
    - The new section contains a markdown table with exactly two data rows, and
      `grep -c 'Punta.empty()' packages/iol-client/README.md` returns at least 1.
    - The prose paragraph explicitly states that `Titulo.puntas` is no longer the language's
      null value and that an identity check against it stops firing silently — the
      typechecker-invisible half, not just the annotation change.
    - `git diff --name-only -- packages/iol-client/` lists exactly
      `packages/iol-client/README.md`.
    - `git diff --name-only -- packages/iol-client/pyproject.toml` produces no output.
  </acceptance_criteria>

  <done>
    The README opens with an unversioned breaking callout that names the anchor tag, gives the
    two-row migration table, and states the `Titulo.puntas` identity-check asymmetry in prose.
    No version string moved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Correct the stale source references in 35-RETIRED-TRIPLES.md and append the Phase-38 accounting addendum</name>

  <files>.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</files>

  <read_first>
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`
      — the file being edited. Read it in full; at minimum the section "Why every row's
      retired kind is `missing`" (around lines 28-40), the iol row of the main table (line
      68), the "Row accounting" paragraph (line 94), the subtraction table (lines 141-148),
      the paragraph headed "`iol-client` — the zero is a fact about `Optional`, not a fact
      about quality" (lines 150-160), the "Mechanics" paragraph carrying the middle term
      (lines 178-186), and the "Two limits with named destinations" paragraph (lines 232-240).
    - `packages/iol-client/src/iol_client/_decode.py` lines 360-380 and 430-510 — READ ONLY.
      This is where the corrected line numbers come from; verify each one against the file
      rather than transcribing from RESEARCH.
    - `packages/iol-client/src/iol_client/models.py` — READ ONLY, for the two current
      annotation line numbers.
    - `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-RESEARCH.md` F-10
      — the measured drift table and the two accounting numbers.
  </read_first>

  <action>
Edit `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`.

**Part A — correct every stale source reference, not just the two D-12 names.** CONTEXT D-12
names only the two `models.py` references; RESEARCH F-10 found four more, all the same drift
in the same file. Fixing two and leaving four would produce a ledger that is authoritative in
one paragraph and wrong in another (RESEARCH Pitfall 7). All six are in scope. Verify each
replacement against the current source file before writing it — do not transcribe blindly:

| What it names | Correct location at HEAD |
|---|---|
| `Cotizacion.puntas` declaration | `iol_client/models.py:213` |
| `Titulo.puntas` declaration | `iol_client/models.py:301` |
| the walker's `Union` early return | `_decode.py:440-446` |
| the non-`Optional` list branch | `_decode.py:448-452` |
| the WR-02 nested-model branch | `_decode.py:504-505` |
| `_kind_of` | `_decode.py:369` is its `def` line; pick the range that actually covers the body and confirm it before writing |

The two `models.py` references each appear twice — once in the iol row of the main table
(line 68) and once in the "the zero is a fact about `Optional`" paragraph (lines 150-160).
Fix both occurrences of each.

**Part B — do NOT rewrite the main table's row set.** Replacing the iol explicit-zero row with
two new field rows would change the table from 35 field rows to 37, which breaks the "Row
accounting" paragraph's stated equality with the D-17 count in `35-CONTEXT.md:112-116` — an
invariant this file asserts about Phase 35's own scope at commit `242b9f3`. The file's own
"Two limits with named destinations" paragraph already resolves this: Phases 36, 37 and 38
introduce new non-`Optional` links whose retired triples belong to their own phases'
accounting. Keep the Phase-35 iol row, keep the 35-row total, and record Phase 38's
contribution in Part C instead.

**Part C — append a delimited Phase-38 accounting addendum** at the end of the file, headed
`## Phase 38 addendum` and opening with one sentence stating that everything above scopes to
Phase 35 at `242b9f3` and is unchanged by this addendum. It must record, as measured facts
(RESEARCH F-10, not estimates):

1. **Field rows added to the NOBJ-02 disposition: 2** — one for `iol-client | Cotizacion |
   .puntas (list[Punta]) | missing` and one for `iol-client | Titulo | .puntas (Punta) |
   missing`. Present them in the same seven-column shape the main table uses, so Phase 39 can
   union them without translating.

2. **Triples retired: 0, in both the records and the distinct-triples columns.** With the
   reason spelled out, because a bare zero re-reads as "iol was already clean" — the exact
   misreading the Phase-35 paragraph was written to prevent. The reason: a field retires a
   triple only if that triple was actually being emitted. Under the pre-Phase-38 optional
   declaration these two fields took the walker's `Union` early return and emitted nothing;
   under the post-Phase-38 non-optional declaration they take the NOBJ-02 collapse arms and
   still emit nothing. **The invariance is the finding.**

3. That Phase 39's middle term is therefore unchanged — higyrus 2, iol 0, market-data 0,
   matriz 5 against the distinct-triples column, matriz 6 against the records column — and
   that Phase 38 changed the ROSTER, not the ARITHMETIC. Cross-reference the "Mechanics"
   paragraph rather than restating its formula.

4. A cross-reference to
   `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CENSUS.md`
   for the full higyrus/ámbito/wallets audit, so Phase 39 finds the phase-local artifact from
   the file it already treats as the single source for the middle term (RESEARCH Open
   Question 2 — the note lives in both places, asymmetrically).

Also amend the "the zero is a fact about `Optional`" paragraph (lines 150-160) with one added
sentence pointing forward to the addendum, so a reader who stops at that paragraph learns the
Phase-38 outcome is now known and where to read it. Do not delete or rewrite the existing
argument — it is still correct about Phase 35.

Do not touch any file under `.planning/verification/` (D-07: the per-package findings ledgers
are auto-generated between markers with a run-scoped schema, and a static annotation census
would corrupt them). Do not touch any other phase's artifacts.
  </action>

  <verify>
    <automated>grep -cE 'iol_client/models\.py:(213|301)' .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</automated>
    <automated>grep -cE '_decode\.py:(440-446|448-452|504-505|369)' .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</automated>
    <automated>grep -v '^\s*<!--' .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md | grep -cE 'models\.py:(154|242)|_decode\.py:(431-435|443-445|482-484|363-367)'</automated>
    <automated>grep -c "Phase 38 addendum" .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</automated>
    <automated>grep -c "38-CENSUS.md" .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</automated>
  </verify>

  <acceptance_criteria>
    - The first verify command returns 4 — both new `models.py` references, each appearing
      twice (main-table row and prose paragraph).
    - The second verify command returns at least 4 — all four corrected `_decode.py`
      references are present.
    - The third verify command returns 0 — no stale reference survives anywhere in the file
      body. This is the "all six or none" criterion; a non-zero result means the ledger is
      half-corrected.
    - `grep -c '35 field rows' .planning/phases/35-.../35-RETIRED-TRIPLES.md` returns 1 — the
      Phase-35 row accounting and its D-17 cross-reference are intact (Part B).
    - `grep -c 'Phase 38 addendum'` returns 1 and `grep -c '38-CENSUS.md'` returns at least 1.
    - The addendum contains a table with exactly 2 data rows, both `iol-client`, both with
      kind `missing`, in the main table's seven-column shape.
    - The addendum states `0` triples retired in BOTH columns AND carries the sentence
      explaining that the invariance is the finding — a bare zero with no explanation fails
      this criterion.
    - The addendum restates the middle term as higyrus 2, iol 0, market-data 0, matriz 5
      (distinct) / matriz 6 (records), unchanged from the "Mechanics" paragraph.
    - `git status --porcelain .planning/verification/` produces no output.
    - `git diff --name-only -- .planning/phases/` lists only files under
      `35-fundaci-n-null-object-bool-pol-tica-del-walker/` and
      `38-iol-client-auditor-a-de-higyrus-mbito-wallets/`.
  </acceptance_criteria>

  <done>
    Every source reference in the Phase-35 ledger resolves at HEAD, the Phase-35 row
    accounting is untouched, and a delimited Phase-38 addendum records 2 added field rows,
    0 retired triples with the reason, the unchanged middle term, and the pointer to the
    census.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository documentation → downstream consumers | The README is what a consumer installing from `main` reads to decide whether their code still works. An incomplete disclosure is a correctness hazard for them, not for this repo. |
| Phase-38 artifacts → Phase 39's census arithmetic | `35-RETIRED-TRIPLES.md` is the single source for the middle term of Phase 39's subtraction. A wrong number there produces a false clean bill of health downstream. |

**No attack surface at all.** This plan edits two markdown files. No code executes, no
dependency moves, no credential is read, no network call is made. ASVS L1 assessment below is
correspondingly narrow, and that narrowness is the honest finding rather than an omission.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-11 | Repudiation | `packages/iol-client/README.md` breaking disclosure | mitigate | The callout must name the runtime consequence, not just the type change. Enforced by an acceptance criterion requiring the paragraph to state that an identity check against nothing stops firing on `Titulo.puntas` — the half mypy does not catch. An under-disclosed break lets a consumer's silent behaviour change be attributed to their own code. |
| T-38-12 | Tampering | `35-RETIRED-TRIPLES.md` (Phase 39's subtraction input) | mitigate | Two mechanisms: the "all six or none" stale-reference criterion (the third verify command must return 0), and the Part-B rule that the Phase-35 row set and its 35-row / D-17 invariant are not rewritten — Phase 38's contribution goes in a delimited addendum instead. |
| T-38-13 | Repudiation | `.planning/verification/<pkg>-findings.md` auto-generated ledgers | mitigate | D-07: those files are machine-written between markers with a run-scoped schema. Enforced by the `git status --porcelain .planning/verification/` criterion. |
| T-38-14 | Information Disclosure | README content | accept | The README carries no credential, no endpoint secret, and no internal host name. It names public type signatures and a public tag. Nothing added here changes that. |
| T-38-SC | Tampering | `uv`/PyPI supply chain | accept | Zero package-manager installs and zero version strings moved; the `pyproject.toml` criterion in task 1 enforces it. Phase 40 owns every publishing action. |
</threat_model>

<verification>
Whole-plan checks:

1. `grep -c '^## Unreleased — BREAKING$' packages/iol-client/README.md` → 1
2. `awk '/^## /{print NR": "$0}' packages/iol-client/README.md | head -2` → the callout first,
   `## Instalación` second
3. The stale-reference grep over `35-RETIRED-TRIPLES.md` → 0
4. `grep -c 'Phase 38 addendum' .planning/phases/35-.../35-RETIRED-TRIPLES.md` → 1
5. `git diff --name-only HEAD~..HEAD` names only `packages/iol-client/README.md` and
   `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`
6. `uv run --package iol-client pytest packages/iol-client -q` unchanged from plan 38-01's
   result (documentation-only plan; a test-count change means something else moved)

**Validation-map crosswalk** — `38-VALIDATION.md`'s Per-Task Verification Map has no row for
the README callout or the ledger patch; both are documentation deliverables of ROADMAP SC-1
and CONTEXT D-10/D-12 that the draft map did not enumerate. Their acceptance is grep-based and
fully automated above, so no new manual-only row is introduced.
</verification>

<success_criteria>
- The breaking change is disclosed in the package README with the old-to-new migration table
  and the truthiness asymmetry stated in prose (ROADMAP SC-1, D-10)
- No version number is assumed anywhere — Phase 40's bump is untouched
- Every source reference in `35-RETIRED-TRIPLES.md` resolves at HEAD (D-12, RESEARCH F-10)
- Phase 39 can read Phase 38's contribution — 2 field rows, 0 triples retired, and why the
  zero is not a claim of prior cleanliness — from the file it already uses as the source of
  the middle term
- The Phase-35 ledger's own 35-row accounting and its D-17 cross-reference are intact
- No file under `.planning/verification/` was touched
</success_criteria>

<output>
Create `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-03-SUMMARY.md` when done
</output>
