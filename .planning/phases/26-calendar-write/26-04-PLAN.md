---
phase: 26-calendar-write
plan: 04
type: execute
wave: 3
depends_on: ["26-03"]
files_modified:
  - packages/market-data-client/tests/test_calendar_write.py
  - packages/market-data-client/tests/test_calendar_write_async.py
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/tests/test_public_surface_market_data.py
autonomous: true
requirements: [MUT-MD-02]
tags: [market-data-client, mutation-gate, no-retry, public-surface, parity, python]

must_haves:
  truths:
    - "D-14 / ROADMAP SC#5: con el gate cerrado por default y el token FORZADO-vencido (token_expires_at=0.0), los cinco métodos levantan MarketDataMutationNotAllowedError y httpx_mock.get_requests() == [] — cero HTTP y cero round-trip a Auth0 — en sync y en async."
    - "D-14: preview_calendar_config también es rechazado con el gate cerrado — la prueba de que no hay carve-out."
    - "Gate ON con base_url cuyo host != expected_host ⇒ rechazo y cero requests, en ambos shells."
    - "D-18: delete_holiday('../config') con el gate ABIERTO levanta ValueError y emite CERO requests HTTP — el reset de configuración de mercado nunca se dispara."
    - "D-15 / ROADMAP SC#4: add_holidays contra 503 repetido emite EXACTAMENTE 1 request saliente y 0 sleeps — el primer test dispatch-level de idempotent=False del paquete."
    - "D-15: el control positivo contrastante delete_holiday (idempotent=True) contra 503 repetido emite 3 requests (max_retries default 2 → max_attempts 3), con time.sleep monkeypatcheado."
    - "D-17 / ROADMAP SC#5: los 8 nombres nuevos (HolidayIn, HolidaysIn, MarketHoursIn, add_holidays, delete_calendar_config, delete_holiday, preview_calendar_config, set_calendar_config) son importables desde market_data_client y están en __init__.__all__; los shims async siguen viviendo sólo bajo aio."
    - "ROADMAP SC#5: los 4 gates verdes — ruff check, ruff format --check, mypy strict sobre packages/market-data-client/src, y la suite completa."
  prohibitions:
    - "Los métodos async NO se re-exportan al namespace plano de market_data_client — el test de superficie pública lo enforcea."
    - "NO agregar fixtures nuevas de reset del gate — el conftest.py autouse ya resetea mutating_allowed=False y expected_host en ambas superficies."
    - "NO arreglar los errores mypy pre-existentes de los archivos de test del paquete (test_reference_core.py, test_mutation_gate.py) — están fuera de scope y no bloquean CI; tampoco agregar errores nuevos."
    - "NO tocar __version__ (sigue en 0.3.1) ni pyproject.toml ni uv.lock — el bump y el release son Phase 28."
    - "NO arreglar el bug de lectura de get_calendar / parse_calendar_response (D-16) — se registra como finding de Phase 27, no se toca acá."
    - "El gate mypy NO puede correrse como `uv run mypy .` — [tool.mypy] files EXCLUYE este paquete; hay que apuntarlo explícitamente a packages/market-data-client/src."
  artifacts:
    - path: "packages/market-data-client/tests/test_calendar_write.py"
      provides: "matriz adversarial sync (refusal ×5, host mismatch, D-18 cero-request), no-retry D-15 + control positivo, dispatch de shims"
      contains: "MarketDataMutationNotAllowedError"
    - path: "packages/market-data-client/tests/test_calendar_write_async.py"
      provides: "espejo async de la matriz adversarial y del dispatch de shims"
      contains: "MarketDataMutationNotAllowedError"
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "re-export de los 3 modelos + los 5 shims sync, en los import blocks y en __all__"
      contains: "set_calendar_config"
    - path: "packages/market-data-client/tests/test_public_surface_market_data.py"
      provides: "_NEW_PUBLIC_NAMES +8 y _MUTATION_METHODS +5 — la red de exports y paridad in-package"
      contains: "MarketHoursIn"
  key_links:
    - from: "packages/market-data-client/tests/test_calendar_write.py"
      to: "packages/market-data-client/src/market_data_client/_transport.py"
      via: "add_holidays con idempotent=False atraviesa RetryTransport.handle_request, que corta el loop en su primera línea"
      pattern: "get_requests\\(\\)"
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "re-export de MarketHoursIn / HolidayIn / HolidaysIn"
      pattern: "MarketHoursIn"
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/src/market_data_client/client.py"
      via: "re-export de los 5 shims sync (los async quedan bajo aio)"
      pattern: "set_calendar_config"
---

<objective>
Cerrar MUT-MD-02 probando el contrato hostil y la superficie pública: la matriz adversarial de
refusals con cero efecto colateral para los cinco métodos en ambos shells, el guard D-18 verificado
end-to-end, el primer test dispatch-level de `idempotent=False` del paquete con su control positivo
contrastante, los re-exports de los ocho nombres nuevos, y los 4 gates verdes.

Purpose: es la evidencia de los criterios 4 y 5 del ROADMAP. Depende del Plan 03 (necesita los
métodos, los shims y los dos archivos de test ya creados) y corre solo en Wave 3.

Output: la matriz de tests adversariales completa en los dos archivos de calendar write, el test de
no-retry D-15 con control positivo, `__init__.py` re-exportando 8 nombres, la red in-package de
superficie pública extendida, y los 4 gates ejecutados y verdes.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-calendar-write/26-CONTEXT.md
@.planning/phases/26-calendar-write/26-RESEARCH.md
@.planning/phases/26-calendar-write/26-PATTERNS.md
@.planning/phases/26-calendar-write/26-VALIDATION.md
@.planning/phases/26-calendar-write/26-02-PLAN.md
@.planning/phases/26-calendar-write/26-03-PLAN.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces

> Roster completo de Phase 26 (los 4 planes) — ninguno de estos símbolos existe hoy en el repo
> (verificado por grep, 26-RESEARCH.md § Runtime State Inventory). El pass de source-grounding de
> `plan-review-convergence` debe excluirlos de la verificación de drift.

**Builders nuevos en `_core.py`** (Plan 02): `build_set_calendar_config_request`,
`build_delete_calendar_config_request`, `build_preview_calendar_config_request`,
`build_add_holidays_request`, `build_delete_holiday_request`.

**Parser nuevo en `_core.py`** (Plan 02): `parse_calendar_write_response`.

**Request models nuevos en `models.py`** (Plan 01): `MarketHoursIn`, `HolidayIn`, `HolidaysIn`.

**Métodos nuevos en `client.py`** (Plan 03): `Client.set_calendar_config`,
`Client.delete_calendar_config`, `Client.preview_calendar_config`, `Client.add_holidays`,
`Client.delete_holiday`.

**Métodos nuevos en `aio.py`** (Plan 03): `AsyncClient.set_calendar_config`,
`AsyncClient.delete_calendar_config`, `AsyncClient.preview_calendar_config`,
`AsyncClient.add_holidays`, `AsyncClient.delete_holiday`.

**Shims module-level nuevos** (Plan 03): los cinco nombres en `client.py` (sync) y los cinco en
`aio.py` (async) — 10 funciones.

**Entradas nuevas de `__all__`:** `_core.__all__` +6 (Plan 02); `models.__all__` +3 (Plan 01);
`__init__.__all__` +8 (este plan): `HolidayIn`, `HolidaysIn`, `MarketHoursIn`, `add_holidays`,
`delete_calendar_config`, `delete_holiday`, `preview_calendar_config`, `set_calendar_config`.

**Archivos de test nuevos** (Plan 03, extendidos por este plan):
`tests/test_calendar_write.py`, `tests/test_calendar_write_async.py`.

**Archivos de test extendidos:** `tests/test_models.py` (Plan 01), `tests/test_core.py` (Plan 02),
`tests/test_public_surface_market_data.py` (este plan — tuplas `_NEW_PUBLIC_NAMES` +8 y
`_MUTATION_METHODS` +5).

**Sin cambios (consumo puro):** `_state.py`, `exceptions.py`, `_transport.py`, `_atransport.py`,
`_params.py`, `_logging.py`, `tests/conftest.py`, `pyproject.toml`, `uv.lock`, `__version__`
(sigue en `"0.3.1"` — el bump es Phase 28).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Matriz adversarial del gate (refusal ×5 × 2 shells, host mismatch, D-18 end-to-end)</name>
  <files>packages/market-data-client/tests/test_calendar_write.py, packages/market-data-client/tests/test_calendar_write_async.py</files>
  <behavior>
    - Test (×5, sync): con el gate cerrado por default y market_data_client.configure(token_expires_at=0.0), cada uno de set_calendar_config / delete_calendar_config / preview_calendar_config / add_holidays / delete_holiday levanta MarketDataMutationNotAllowedError y httpx_mock.get_requests() == [] — el token forzado-vencido prueba que no hubo POST a Auth0.
    - Test (×5, async): idéntico sobre aio._get_default() con aio.configure(token_expires_at=0.0).
    - Test (host mismatch, sync): gate ON con expected_host distinto del host del base_url del conftest ⇒ MarketDataMutationNotAllowedError y 0 requests.
    - Test (host mismatch, async): espejo.
    - Test (D-18 end-to-end, sync): con el gate ABIERTO, delete_holiday("../config") levanta ValueError y httpx_mock.get_requests() == [] — no se emitió ningún DELETE, y en particular ninguno contra /api/calendar/config.
    - Test (D-18 end-to-end, async): espejo.
    - Test (D-18, sync): con el gate ABIERTO, delete_holiday("") y delete_holiday("2026-12-25?x=1") levantan ValueError y emiten 0 requests.
  </behavior>
  <read_first>
    - packages/market-data-client/tests/test_symbols_write.py:112-157 (los tres tests de refusal por default y el de host mismatch — el patrón adversarial EXACTO, incluido el comentario que explica por qué `token_expires_at=0.0` prueba el cero-round-trip a Auth0), :35-37 (`_open_gate()`).
    - packages/market-data-client/tests/test_symbols_write_async.py:101-144 (los espejos async de esos mismos tests).
    - packages/market-data-client/tests/test_mutation_gate.py (la batería adversarial de Phase 25 sobre el gate en sí — leer para NO duplicar lo que ya está cubierto ahí; acá sólo se cubren los cinco métodos NUEVOS).
    - packages/market-data-client/tests/conftest.py:29-80 (fixtures autouse — el `base_url` sembrado es `https://market-data-develop.test/api` y el teardown ya resetea `mutating_allowed=False` + `expected_host="market-data-develop.bbsa.com.ar"`; NO agregar fixtures nuevas).
    - packages/market-data-client/src/market_data_client/client.py:257-283 y aio.py:215-240 (`_ensure_mutation_allowed` — las dos patas: flag y host exacto).
    - packages/market-data-client/src/market_data_client/_core.py — `build_delete_holiday_request` con el guard D-18 creado por el Plan 02 (leer la condición real y el mensaje del `ValueError`).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:545-566 (template de refusal adversarial, con la nota de que hay que replicarlo ×5 incluido `preview_calendar_config`), :150-164 (bloque D-18 «SIN ANÁLOGO EN EL REPO»).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:867-878 (el test de gate end-to-end shipeado + la instrucción de replicarlo ×5 × 2 superficies), :1060-1080 (Open Question 1 — la verificación con httpx de que `"../config"` normaliza a `DELETE /api/calendar/config`), :755-763 (Pitfall 7 — contaminación cross-test ya mitigada; usar `_open_gate()`, no fixtures nuevas).
  </read_first>
  <action>
    Escribí los tests directamente (el código de producción ya existe tras el Plan 03) y ejecutalos:
    los de refusal y host mismatch deben pasar en verde de entrada — si alguno FALLA, es un defecto
    real del Plan 03 (el gate no quedó como primera sentencia) y hay que corregir el método en
    `client.py`/`aio.py` antes de continuar.

    En `tests/test_calendar_write.py` agregá cinco tests de refusal-by-default, uno por método,
    siguiendo el shape de `test_symbols_write.py:112-121`: llamar
    `market_data_client.configure(token_expires_at=0.0)` para FORZAR el token vencido (si el gate no
    cortara primero, `_ensure_token` dispararía un POST a Auth0 y la ausencia de ese POST es lo que
    prueba el short-circuit), envolver la llamada en `pytest.raises(MarketDataMutationNotAllowedError)`
    y assertar `httpx_mock.get_requests() == []`. El de `preview_calendar_config` es la prueba de que
    D-14 no tiene carve-out — anotalo en el docstring del test.

    Agregá un test de host mismatch siguiendo `test_symbols_write.py:144-156`: abrir el gate con
    `expected_host` distinto al host del `base_url` sembrado por el conftest
    (`market-data-develop.test`) y assertar rechazo con 0 requests.

    Agregá los tests end-to-end del guard D-18: con el gate ABIERTO vía `_open_gate()`, llamar
    `delete_holiday("../config")` dentro de `pytest.raises(ValueError)` y assertar
    `httpx_mock.get_requests() == []`. El docstring del test debe explicar la amenaza concreta: sin
    el guard, ese input normaliza a `DELETE /api/calendar/config` — un reset de la configuración de
    mercado, otro endpoint de esta misma fase — y el `422` del servidor nunca se ejerce porque el
    request nunca llega al endpoint que lo validaría. Sumá los casos `""` y `"2026-12-25?x=1"` con
    los mismos dos asserts.

    Espejá TODA la matriz en `tests/test_calendar_write_async.py` sobre `aio._get_default()`, usando
    `aio.configure(token_expires_at=0.0)` para el token forzado-vencido y tests `async def` planos
    (`asyncio_mode="auto"`, sin decorador).

    No agregues fixtures nuevas: el `conftest.py` autouse ya resetea el gate en ambas superficies.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q` sale 0 con los ~14 casos de `<behavior>` en verde.
    - Existen exactamente 5 tests de refusal-by-default en el archivo sync y 5 en el async — uno por método público, incluido `preview_calendar_config`.
    - Cada test de refusal asserta `httpx_mock.get_requests() == []` DESPUÉS de haber llamado `configure(token_expires_at=0.0)` (evidencia de cero round-trip a Auth0).
    - El test D-18 sync asserta a la vez `pytest.raises(ValueError)` y `httpx_mock.get_requests() == []` para el input `"../config"`; el async hace lo mismo.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -k "refused or mismatch or path_safety or guard" -q` sale 0 y reporta al menos 16 tests seleccionados.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` sale 0.
  </acceptance_criteria>
  <done>Los cinco métodos rechazan por default sin emitir un solo byte a la red ni al servidor de auth, en ambos shells, y ningún `day` capaz de retargetear el request llega a producir un DELETE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No-retry dispatch-level de add_holidays (D-15) + control positivo + shims</name>
  <files>packages/market-data-client/tests/test_calendar_write.py, packages/market-data-client/tests/test_calendar_write_async.py</files>
  <behavior>
    - Test (D-15, sync): con el gate abierto y tres respuestas 503 encoladas, add_holidays(HolidaysIn([HolidayIn("2026-12-25")])) levanta MarketDataAPIError, httpx_mock.get_requests() tiene longitud EXACTAMENTE 1, y la lista de sleeps capturados está vacía.
    - Test (control positivo, sync): con tres respuestas 503 encoladas, delete_holiday("2026-12-25") (idempotent=True) levanta MarketDataAPIError y httpx_mock.get_requests() tiene longitud 3 (max_retries default 2 → max_attempts 3), con time.sleep monkeypatcheado a un no-op.
    - Test (shim sync): el shim module-level market_data_client.set_calendar_config(MarketHoursIn(...)) despacha por el default Client y emite el PUT esperado.
    - Test (shim sync): el shim module-level market_data_client.client.add_holidays(HolidaysIn([...])) despacha el POST esperado.
    - Test (shim async): el shim aio.set_calendar_config(...) despacha por el default AsyncClient.
    - Test (shim async): el shim aio.delete_holiday("2026-12-25") despacha el DELETE esperado.
  </behavior>
  <read_first>
    - packages/iol-client/tests/test_transport.py:41-53 (el ÚNICO test dispatch-level de `idempotent=False` del monorepo — se copia la FORMA del test, no código importado; nótese el marker `@pytest.mark.httpx_mock(assert_all_responses_were_requested=False)`, obligatorio: sin él pytest-httpx falla en teardown por respuestas no consumidas).
    - packages/market-data-client/tests/test_transport.py:88-107 (el patrón in-package de `monkeypatch.setattr(time, "sleep", lambda s: sleeps.append(s))` — sin esto el control positivo duerme ~4,4 s reales de jitter y multiplicaría por ~20 la latencia de la suite).
    - packages/market-data-client/src/market_data_client/_transport.py:157-163 (el gate `if not request.extensions.get("idempotent", False): return super().handle_request(request)` — código shipeado, NO tocar).
    - packages/market-data-client/tests/test_symbols_write.py:159-168 (template del test de shim module-level).
    - packages/market-data-client/tests/test_symbols_write_async.py:145-154 (espejo async del test de shim).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:811-847 (los dos tests de D-15 ya redactados y medidos esta sesión: 1 request / 0 sleeps vs. 3 requests / 2 sleeps), :725-743 (Pitfalls 4 y 5 — el marker y el monkeypatch de sleep).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:569-613 (sección «Test no-retry a nivel dispatch (D-15) — dos análogos parciales»).
  </read_first>
  <action>
    Escribí los tests y ejecutalos. El de no-retry es el PRIMER test dispatch-level de
    `idempotent=False` del paquete: si falla, es un defecto real del `idempotent=False` del builder
    (Plan 02) o del cableado del shell (Plan 03).

    En `tests/test_calendar_write.py` agregá el test de no-retry de `add_holidays`, decorado con
    `@pytest.mark.httpx_mock(assert_all_responses_were_requested=False)` (obligatorio: sin el marker
    el test falla en teardown por respuestas no consumidas, con un error que no tiene nada que ver
    con el retry). Capturá los sleeps con `monkeypatch.setattr(time, "sleep", lambda s: sleeps.append(s))`
    sobre una lista local, abrí el gate con `_open_gate()`, encolá tres respuestas `503` para
    `method="POST"`, envolvé la llamada en `pytest.raises(MarketDataAPIError)` y asertá tanto
    `len(httpx_mock.get_requests()) == 1` como que la lista de sleeps quedó vacía.

    Agregá el control positivo contrastante con `delete_holiday` (`idempotent=True`): mismo marker,
    `monkeypatch.setattr(time, "sleep", ...)` a un no-op para evitar el jitter real, tres respuestas
    `503` para `method="DELETE"`, y assert `len(httpx_mock.get_requests()) == 3`.

    Agregá dos tests de shim module-level sync siguiendo `test_symbols_write.py:159-168` (uno del
    trío de config, uno del par de feriados), y sus dos espejos async en
    `tests/test_calendar_write_async.py` siguiendo `test_symbols_write_async.py:145-154`.

    Importá `time` y `pytest` en el archivo sync si aún no están; mantené el orden de imports que
    exige ruff `I001`.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q` sale 0.
    - El test de no-retry asserta a la vez `len(httpx_mock.get_requests()) == 1` y que la lista de sleeps capturados está vacía.
    - El test de no-retry lleva el decorador `@pytest.mark.httpx_mock(assert_all_responses_were_requested=False)`.
    - El control positivo asserta `len(httpx_mock.get_requests()) == 3` y monkeypatchea `time.sleep`.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q --durations=5` sale 0 y ningún test individual supera 1 s (evidencia de que no hay jitter real dormido).
    - La suite completa del paquete corre en menos de 20 s de wall-clock (baseline medido: 191 tests en 0,25 s).
    - Existen 2 tests de shim sync y 2 de shim async para los métodos nuevos.
  </acceptance_criteria>
  <done>Queda probado a nivel dispatch que `POST /calendar/holidays` no se reintenta nunca (1 request, 0 sleeps) mientras un endpoint idempotente hermano sí reintenta 3 veces, y los shims module-level despachan por los singletons default en ambas superficies.</done>
</task>

<task type="auto">
  <name>Task 3: Re-exports en __init__.py + red de superficie pública + los 4 gates verdes</name>
  <files>packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/tests/test_public_surface_market_data.py</files>
  <read_first>
    - packages/market-data-client/src/market_data_client/__init__.py:39-57 (import block desde `client`, ordenado con `# noqa: E402`), :65-76 (import block desde `models`), :81-113 (`__all__` — PascalCase antes que snake_case por orden ASCII), :118 (`__version__ = "0.3.1"` — NO tocar).
    - packages/market-data-client/tests/test_public_surface_market_data.py:1-85 (archivo completo: docstring que explica por qué esta red es necesaria —los nets cross-package EXCLUYEN este paquete—, las tuplas `_NEW_PUBLIC_NAMES` / `_MUTATION_METHODS` y las cuatro pruebas existentes: importabilidad, membresía en `__all__`, paridad de métodos de clase, ubicación de shims).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:441-471 (sección `__init__.py` — los 8 nombres a agregar y el orden dentro de cada grupo), :683-705 (sección del test de superficie pública: SÓLO hay que extender las dos tuplas; las cuatro pruebas existentes cubren automáticamente las entradas nuevas — NO escribir pruebas nuevas ahí).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:880-891 (las tuplas actuales y las 8 + 5 entradas a sumar), :765-771 (Pitfall 8 — `RUF022`), :929-943 (§ Los 4 gates con los comandos exactos y la advertencia X6 sobre el scope de mypy).
    - .planning/phases/26-calendar-write/26-VALIDATION.md:29-37 (Sampling Rate — el comando compuesto de los 4 gates).
    - .planning/phases/25-mutating-gate-symbols-write/deferred-items.md (los errores mypy PRE-EXISTENTES en `test_reference_core.py` y `test_mutation_gate.py` — no arreglarlos, están fuera de scope y no bloquean CI).
  </read_first>
  <action>
    En `__init__.py`, agregá al import block de `market_data_client.client` los cinco shims sync
    `add_holidays`, `delete_calendar_config`, `delete_holiday`, `preview_calendar_config` y
    `set_calendar_config`, respetando el orden que ya tiene el bloque (clases primero, después los
    nombres en minúscula en orden ASCII: `_get_default`, `add_holidays`, `configure`,
    `create_symbol`, `create_symbols`, `delete_calendar_config`, `delete_holiday`, `get_calendar`,
    …, `preview_calendar_config`, `set_calendar_config`, `update_symbol`). Agregá al import block de
    `market_data_client.models` los tres modelos `HolidayIn`, `HolidaysIn` y `MarketHoursIn` en
    orden ASCII. Sumá los ocho nombres a `__all__` insertándolos en orden — los tres PascalCase en
    el grupo de clases (`HolidayIn` y `HolidaysIn` entre `Client` e `Instrument`; `MarketHoursIn`
    entre `MarketDataSnapshot` y `NewSymbol`) y los cinco snake_case en el grupo de funciones. NO
    re-exportes las variantes async: viven bajo `aio` y el test de superficie pública lo enforcea.
    NO toques `__version__`.

    En `tests/test_public_surface_market_data.py` extendé SÓLO las dos tuplas: `_NEW_PUBLIC_NAMES`
    suma los ocho nombres nuevos y `_MUTATION_METHODS` suma los cinco nombres de método. Las cuatro
    pruebas existentes cubren automáticamente las entradas nuevas — no escribas pruebas nuevas en
    ese archivo. Actualizá el docstring del módulo para que mencione que la red cubre ahora también
    la superficie de calendar write.

    Después corré los 4 gates y dejá los cuatro en verde. El de mypy DEBE apuntarse explícitamente
    al paquete: `[tool.mypy] files` en el `pyproject.toml` raíz EXCLUYE `packages/market-data-client/src`,
    así que `uv run mypy .` NO lo chequea (la cobertura real viene del hook `mypy` de pre-commit).
    Si aparecen errores mypy en archivos de test PRE-EXISTENTES (`test_reference_core.py`,
    `test_mutation_gate.py`), no los arregles — están documentados como deferred de Phase 25 y no
    bloquean CI; sí asegurate de no agregar ninguno nuevo.
  </action>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run mypy packages/market-data-client/src && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check .` sale 0 (sin `RUF022` ni `I001`).
    - `uv run ruff format --check .` sale 0.
    - `uv run mypy packages/market-data-client/src` sale 0 — comando explícito, NO `uv run mypy .`.
    - `uv run pytest -q` sale 0 (suite completa: todos los paquetes + `tests` + `verification/`).
    - `python -c "import market_data_client as m; names=('HolidayIn','HolidaysIn','MarketHoursIn','add_holidays','delete_calendar_config','delete_holiday','preview_calendar_config','set_calendar_config'); [getattr(m,n) for n in names]; assert all(n in m.__all__ for n in names)"` sale 0.
    - `python -c "import market_data_client as m; assert list(m.__all__) == sorted(m.__all__)"` sale 0.
    - `python -c "import market_data_client as m; from market_data_client import aio; assert all(hasattr(aio, n) for n in ('set_calendar_config','delete_calendar_config','preview_calendar_config','add_holidays','delete_holiday'))"` sale 0.
    - `python -c "import market_data_client as m; assert m.__version__ == '0.3.1'"` sale 0 (sin bump en esta fase).
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_public_surface_market_data.py -q` sale 0 con las tuplas extendidas.
    - `uv lock --check` sale 0 (sin dependencias nuevas).
  </acceptance_criteria>
  <done>Los ocho nombres nuevos son importables desde el namespace plano y están en `__all__` en orden; la red in-package enforcea exports y paridad sync/async para los cinco métodos; y los cuatro gates —con mypy apuntado explícitamente al paquete— están verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llamada del consumidor → gate | El punto donde se decide si una intención de mutar llega a la red o muere en el proceso. |
| `day` provisto por el consumidor → path del request (verificación end-to-end) | Acá se prueba, con el gate abierto, que un `day` hostil no produce ningún request. |
| builder → `RetryTransport` | `spec.idempotent` decide si una operación mutante puede repetirse en el cable. |
| superficie pública → consumidores del paquete | `__all__` y la paridad sync/async son el contrato que la red in-package protege. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | mutación de calendario disparada sin opt-in | mitigate | `_ensure_mutation_allowed()` como primera sentencia de los cinco métodos (D-14). Testeable acá: los 10 tests de refusal-by-default (5 sync + 5 async) que assertan `get_requests() == []`, incluido `preview_calendar_config` como prueba de que no hay carve-out. |
| T-26-05 | Spoofing / Tampering | mutación despachada contra un host inesperado o de producción | mitigate | Pata de host exacto del gate heredado. Testeable acá: host mismatch ⇒ rechazo y 0 requests, sync y async. |
| T-26-06 | Information Disclosure | el rechazo filtra el intento vía un round-trip a Auth0 | mitigate | El gate corre estrictamente antes de `_ensure_token`. Testeable acá: los tests de refusal fuerzan `token_expires_at=0.0` y assertan 0 requests — la ausencia del POST a Auth0 ES la prueba. |
| **T-26-01** | **Tampering** | **`delete_holiday` — retargeting de request vía `day` interpolado raw** | **mitigate** | **Guard D-18 en `build_delete_holiday_request` (owner: Plan 02, donde vive el `ValueError` y su test unitario). Acá se cierra la verificación end-to-end exigida: con el gate ABIERTO, `delete_holiday("../config")` levanta `ValueError` y `httpx_mock.get_requests() == []` — CERO requests HTTP, y en particular ningún `DELETE /api/calendar/config` (reset de configuración de mercado). Verificado contra httpx 0.28.1 en 26-RESEARCH.md Open Question 1.** |
| T-26-07 | Tampering | feriado duplicado por reintento de un POST no idempotente | mitigate | `idempotent=False` en `build_add_holidays_request` ⇒ `RetryTransport` no reintenta. Testeable acá a nivel dispatch (D-15): 3×503 ⇒ exactamente 1 request y 0 sleeps, con el control positivo contrastante `idempotent=True` ⇒ 3 requests. |
| T-26-10 | Spoofing | una superficie async rota o un export faltante shippean sin ser detectados | mitigate | Red in-package de superficie pública: los nets cross-package EXCLUYEN este paquete. `_NEW_PUBLIC_NAMES` +8 y `_MUTATION_METHODS` +5 hacen que las cuatro pruebas existentes (importabilidad, `__all__`, paridad de métodos de clase, ubicación de shims) cubran la superficie nueva. |
| T-26-09 | Information Disclosure | credencial o `updated_by` filtrado en logs de mutación | accept | `RedactingFilter` + `__repr__` redactor existentes sin cambios; los tests nuevos no imprimen payloads ni credenciales y el `.env` no se toca. |
| T-26-16 | Tampering | el bug de lectura pre-existente de `get_calendar` se propaga a la superficie de escritura | mitigate | D-06/D-16: el par de feriados retorna `dict[str, Any]` y no toca el modelo/parser de lectura rotos. El bug queda registrado como finding para Phase 27, explícitamente NO arreglado acá. |
| T-26-SC | Tampering | instalaciones npm/pip/cargo | accept | Phase 26 no instala ningún paquete; `uv lock --check` verde sin cambios (26-RESEARCH.md § Package Legitimacy Audit: no aplica). |
</threat_model>

<verification>
- Los 4 gates, en este orden y todos en verde:
  `uv run ruff check .` · `uv run ruff format --check .` ·
  `uv run mypy packages/market-data-client/src` (explícito — el global excluye el paquete, X6) ·
  `uv run pytest -q`.
- `uv lock --check` sale 0 (sin dependencias nuevas).
- `uv run lint-imports` sale 0 (no cubre este paquete, X8 — sólo se verifica que no rompa nada).
- Grep de disciplina de logging: los archivos nuevos no contienen `logging.basicConfig` ni `logging.root.`.
- Grep de paridad D-17: `market_data_client.__all__` contiene los 8 nombres nuevos y ninguno de los métodos async (que viven sólo bajo `aio`).
- `python -c "import market_data_client as m; assert m.__version__ == '0.3.1'"` — sin bump en Phase 26.
</verification>

<success_criteria>
MUT-MD-02 queda cerrado: los cinco métodos rechazan por default con cero HTTP y cero round-trip a
Auth0 en ambos shells (ROADMAP SC#5), el guard D-18 impide end-to-end el retargeting a
`DELETE /api/calendar/config`, `POST /calendar/holidays` queda probado como no reintentable a nivel
dispatch contra un control positivo idempotente (SC#4), los ocho nombres nuevos están re-exportados
con paridad sync/async enforceada por la red in-package, y los 4 gates —con mypy apuntado
explícitamente a `packages/market-data-client/src`— están verdes.
</success_criteria>

<output>
Create `.planning/phases/26-calendar-write/26-04-SUMMARY.md` when done.
</output>
