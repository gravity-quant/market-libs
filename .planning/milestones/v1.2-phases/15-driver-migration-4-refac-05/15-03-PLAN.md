---
phase: 15-driver-migration-4-refac-05
plan: 03
type: tdd
wave: 3
depends_on: ["15-02"]
files_modified:
  - main_higyrus.py
  - verification/test_main_higyrus_uses_single_client_instance.py
autonomous: true
requirements: [REFAC-05]
user_setup:
  - service: higyrus-client (live)
    why: "Per-package LIVE smoke at end of migration (D-11, Criterion #4)"
    env_vars:
      - name: HIGYRUS_USERNAME
        source: ".env in packages/higyrus-client/ (never committed)"
      - name: HIGYRUS_PASSWORD
        source: ".env in packages/higyrus-client/ (never committed)"
    dashboard_config:
      - task: "Operator runs `uv run --package higyrus-client python main_higyrus.py` and confirms exit 0 + findings written"
        location: "Local terminal"

must_haves:
  truths:
    - "main_higyrus.py main() constructs exactly one sync Client(); _async_main() constructs exactly one AsyncClient() — D-01/D-02"
    - "The async method-call on default `await aio._get_default()._ensure_http_client()` (:343) migrates to `await aclient._ensure_http_client()` — D-03 (method-call, not just _state read)"
    - "Every probe_* receives client/aclient as a parameter; no probe reaches _get_default() — D-01"
    - "AST-guard test fails RED, passes GREEN — Criterion #1"
    - "Finding titles/fids/probe-names in higyrus-client-findings.md are byte-stable — Criterion #2"
  artifacts:
    - path: "verification/test_main_higyrus_uses_single_client_instance.py"
      provides: "AST-walker asserting <=2 Client/AsyncClient ctors in main_higyrus.py"
      contains: "test_main_higyrus_uses_single_client_instance"
      min_lines: 25
    - path: "main_higyrus.py"
      provides: "Migrated driver — single sync + single async client threaded into ~19 probes"
      contains: "Client()"
  key_links:
    - from: "main_higyrus.py"
      to: "the threaded aclient"
      via: "await aclient._ensure_http_client() replaces await aio._get_default()._ensure_http_client()"
      pattern: "aclient\\._ensure_http_client\\(\\)"
    - from: "verification/test_main_higyrus_uses_single_client_instance.py"
      to: "main_higyrus.py"
      via: "ast.parse + ast.walk counting ctor Calls (ast.Name AND ast.Attribute)"
      pattern: "ast\\.walk"
---

<objective>
Migrate `main_higyrus.py` (largest driver — ~19 probes, 2458 LOC, 19 `_state` read sites) so `main()` builds one sync `Client()` and `_async_main()` builds one `AsyncClient()`, threaded into every probe. Includes the one method-call-on-default site (`:343`, `_ensure_http_client()`) beyond the plain `_state` reads. Author the AST-guard test FIRST (RED→GREEN).

Purpose: Wave 3 — third in the serial order (D-11), after iol.
Output: Migrated `main_higyrus.py` + its AST-guard test.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-driver-migration-4-refac-05/15-CONTEXT.md
@.planning/phases/15-driver-migration-4-refac-05/15-PATTERNS.md
@.planning/phases/15-driver-migration-4-refac-05/15-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — AST-guard test for main_higyrus.py (Criterion #1)</name>
  <files>verification/test_main_higyrus_uses_single_client_instance.py</files>
  <read_first>
    - verification/test_main_iol_uses_single_client_instance.py (Wave-2 sibling — copy idiom, retarget)
    - main_higyrus.py:107-110 (current import block)
  </read_first>
  <behavior>
    - Test asserts `1 <= count <= 2` Client/AsyncClient ctor Calls in main_higyrus.py.
    - RED: un-migrated driver count 0 → FAILS. GREEN: post-migration count==2 → PASSES.
    - Walker matches both ast.Name and ast.Attribute (D-05).
  </behavior>
  <action>
    Create `verification/test_main_higyrus_uses_single_client_instance.py` as a copy of the iol AST test retargeted to `main_higyrus.py` with test function `test_main_higyrus_uses_single_client_instance`. Same dual ctor matching and `1 <= len(ctor_sites) <= 2` assertion. Reference `with_options` by concept only in any comment. Run RED first against the un-migrated driver.
    <!-- planner-discipline-allow: with_options -->
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_higyrus_uses_single_client_instance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED before Task 2 (count 0). GREEN after (count==2).
    - Both ctor styles covered (grep `ast.Name` and `ast.Attribute`).
  </acceptance_criteria>
  <done>Test exists, RED→GREEN, both ctor styles matched.</done>
</task>

<task type="execute">
  <name>Task 2: Migrate main_higyrus.py — thread client/aclient across ~19 probes + the _ensure_http_client method-call (D-01/D-02/D-03)</name>
  <files>main_higyrus.py</files>
  <read_first>
    - main_higyrus.py (full driver — 2458 LOC; the file being modified)
    - 15-PATTERNS.md lines 254-289 (higyrus recipe: import block, 19 _state sites list, the method-on-default site :343, _async_main batch :2214-2275 / asyncio.run :2394, configure()/_token-reset docstring caveat at :2225)
    - packages/higyrus-client/src/higyrus_client/__init__.py:76-77 (Client/AsyncClient exports)
  </read_first>
  <action>
    PIN constructor style: add `from higyrus_client import Client, AsyncClient` (bare → ast.Name). In `main()` (:2296 region / asyncio.run :2394) construct `client = Client()` once; thread into every sync probe; add `client` param to each sync `def probe_*`. In `_async_main()` (:2214-2275) construct `aclient = AsyncClient()` at the top; thread into all async probes; change `await aio.aclose()` (:2275) → `await aclient.aclose()` in the existing finally/suppress.
    Mechanical 1:1 `_state` swaps (D-03) at all 19 sites (:462,:515,:574,:647,:741,:887,:986,:1090,:1204,:1318,:1451,:1558,:1666,:1713,:1879,:1950,:2036,:2137,:2421) — sync → `client._state.<attr>`, async → `aclient._state.<attr>`.
    Method-call-on-default (D-03, NOT a plain read): `await aio._get_default()._ensure_http_client()` (:343) → `await aclient._ensure_http_client()`.
    Swap public/raw calls: `higyrus_client.login()` (:464) → `client.login()`; `higyrus_client.get_movimientos(...)` (:318) → `client.get_movimientos(...)`; `higyrus_client.client._request(...)` (:576,:743,:990) → `client._request(...)`; `await aio.login()` (:517)/`await aio.get_movimientos(...)` (:371)/`await aio._request(...)` (:649,:889) → `aclient.<...>`.
    DO NOT touch the `configure()` call referenced in the :2225 docstring caveat — D-03 is a mechanical `_state`-access swap only; do not alter `configure()`/`_token`-reset behavior. DO NOT touch `title=`/`fid=`/`class_=`/probe-name literals in `append_finding(...)` (D-06). Sync and async clients SEPARATE (D-02).
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_higyrus_uses_single_client_instance.py -x && uv run --frozen ruff check main_higyrus.py && uv run --frozen mypy main_higyrus.py</automated>
  </verify>
  <acceptance_criteria>
    - AST-guard test GREEN (count==2).
    - `grep -c 'aclient\._ensure_http_client()' main_higyrus.py` == 1 (method-call migrated).
    - `grep -n '_get_default' main_higyrus.py` returns no CODE sites.
    - Every `def probe_*` signature carries `client` or `aclient`.
    - ruff check + mypy strict green.
    - No `append_finding` title/fid/class_ literal changed (`git diff main_higyrus.py`).
  </acceptance_criteria>
  <done>Single sync + single async client threaded into all ~19 probes; method-call-on-default migrated; configure() untouched; finding literals untouched; CI-green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Higyrus live API | driver authenticates with HIGYRUS credentials from packages/higyrus-client/.env |
| driver process → per-instance token state | `_state` reads + `_ensure_http_client()` operate on the threaded instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | HIGYRUS credentials in logs/findings/commits | mitigate | Migration changes only client acquisition; no credential surfaces into findings/logs; `.env` stays uncommitted; no credential literal touched. |
| T-15-02 | Tampering | multiple Client() → token/state churn | mitigate | ≤2-ctor AST gate (Task 1) caps construction at 1 sync + 1 async. |
| T-15-05 | Tampering | accidental configure()/_token-reset edit | mitigate | D-03 scope is `_state`-access swaps only; the :2225 configure() caveat is explicitly out of scope — verified by `git diff` showing no configure() edits. |
| T-15-03 | Denial of Service | Higyrus API rate limits | accept | Single operator-driven smoke; below `high` severity. |
| T-15-SC | Tampering | uv/pip installs | mitigate | No new packages installed; no package legitimacy audit required. |
</threat_model>

<verification>
- AST-guard test GREEN (count==2).
- `_ensure_http_client()` method-call migrated to threaded aclient.
- `_get_default()` eliminated from CODE lines.
- configure()/_token-reset behavior unchanged.
- ruff + mypy strict green.
- Operator confirms LIVE smoke exit 0.
</verification>

<success_criteria>
- main_higyrus.py constructs exactly 1 sync + 1 async client threaded into every probe.
- Method-call-on-default and all 19 _state sites migrated.
- Finding titles/fids/probe-names byte-stable.
</success_criteria>

<artifacts_this_phase_produces>
**New test file + test function:**
- `verification/test_main_higyrus_uses_single_client_instance.py` → `test_main_higyrus_uses_single_client_instance()`

**New driver import line:**
- `main_higyrus.py`: `from higyrus_client import Client, AsyncClient` (bare, pinned per D-05)

**New helper signatures:**
- Every sync `def probe_*` gains `client`; every async `def probe_*` gains `aclient`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-driver-migration-4-refac-05/15-03-SUMMARY.md` when done
</output>
