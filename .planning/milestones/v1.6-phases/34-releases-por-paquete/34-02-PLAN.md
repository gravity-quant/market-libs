---
phase: 34-releases-por-paquete
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified: []
autonomous: false
requirements: [PUB-TYP-01]
user_setup:
  - service: github
    why: "Updating the existing release PR and merging it into the public default branch are outward-facing GitHub operations; the merge is irreversible in practice"
    dashboard_config:
      - task: "Ensure the `gh` CLI is authenticated (asserted in Task 1 via `gh auth status`; never echo the token)"
        location: "local shell — `gh auth status`"
      - task: "Be available to answer the first blocking go/no-go checkpoint before the merge (D-08a)"
        location: "the executing terminal session"

must_haves:
  truths:
    - "PR #12 is UPDATED in place — retitled and its body rewritten to cover Phases 29-34 and both released versions — never closed, never replaced by a new PR, per D-05"
    - "PR #12 still targets base `main` from head `milestone/v1.5-mutations`, and its head now contains every commit of Phase 33 and of plan 34-01 — per D-05"
    - "The PR diff still carries the `.planning/` artifacts; they were not filtered out — per D-06"
    - "The PR reports exactly 15 checks and all 15 have status `pass`, of which exactly 2 are `Tests · iol-client · py3.1x` and exactly 2 are `Tests · market-data-client · py3.1x` — asserted by COUNT, never by absence of the word fail — per SC#3"
    - "The PR is merged into `main` only after the operator has explicitly replied approved at a blocking checkpoint that ran before any merge command — per D-08(a)"
    - "`origin/main` advances to a real merge commit with TWO parents, produced by `gh pr merge 12 --merge` — never squash, never rebase — per D-09"
    - "The merged tree reads version 0.3.0 for iol-client and 0.5.0 for market-data-client, so `release.yml`'s version-match gate will pass for both tags — per D-10"
    - "No tag is created or pushed in this plan; the tag push is gated by a SECOND independent checkpoint in plan 34-03 — per D-08"
  prohibitions:
    - "Never rewrite history reachable from this branch or from `main`: `--squash` and `--rebase` are forbidden merge strategies, and no force flag is permitted on any push. Dozens of SUMMARY files across Phases 29-33 cite these SHAs by value."
    - "Never report a gate as green from anything except an exact positive count. `pending`, `skipping`, `cancelled` and a zero-check run all read as green under an absence-of-failure check, and `cancel-in-progress: true` (ci.yml:20) makes `cancelled` genuinely reachable."
    - "Neither of this phase's two blocking human checkpoints may be satisfied by the agent itself, by `auto_advance`, by yolo mode, or by inferring approval from silence or from an ambiguous reply. Only a literal operator response counts, and it is recorded verbatim."
    - "Never close PR #12 or open a replacement PR. The vehicle is updated in place; closing it would orphan the review history already attached to it."
  artifacts:
    - path: "gh-pr:12"
      provides: "the single release PR, updated in place to cover Phases 29-34 and both versions"
      contains: "release: iol-client v0.3.0 + market-data-client v0.5.0"
    - path: "git-ref:origin/main"
      provides: "the two-parent merge commit that becomes the shared tag anchor for BOTH tags in plan 34-03"
      contains: "Merge pull request"
  key_links:
    - from: "gh-pr:12"
      to: ".github/workflows/ci.yml"
      via: "the PR triggers ci.yml; the `test` job fans out over 6 packages (ci.yml:113-119) × 2 Python versions (ci.yml:120) plus 3 non-matrix jobs, producing exactly 15 checks"
      pattern: "Tests · iol-client · py3.1[23]"
    - from: "git-ref:origin/main"
      to: "git-tag:iol-client-v0.3.0"
      via: "plan 34-03 creates the annotated tag ON this merge commit so release.yml's version-match gate runs against the merged tree"
      pattern: "iol-client-v0.3.0"
    - from: "git-ref:origin/main"
      to: "git-tag:market-data-client-v0.5.0"
      via: "plan 34-03 creates the second annotated tag on the SAME merge commit; two tags, one commit, one approval"
      pattern: "market-data-client-v0.5.0"
---

<objective>
Update the existing release PR #12 in place, prove the CI gate is genuinely green by counting
passing checks, and — only after an explicit human go/no-go — merge it into `main` with a real
merge commit.

Purpose: `main` has no branch protection in this repository. GitHub will merge this PR with red,
pending or cancelled checks and will not stop anything. The count-based assertion plus the D-08(a)
checkpoint are therefore the only enforcement standing between an unverified build and a public
default branch.
Output: A merge commit on `origin/main` with two parents, carrying both version bumps, ready to
anchor BOTH tags in plan 34-03.

This plan is `autonomous: false`. The merge is irreversible in practice and is gated by a blocking
`checkpoint:human-verify`. It requires a clean working tree and an authenticated `gh`. The tag
pushes and the public Releases are NOT part of this plan — they are gated behind a SECOND
checkpoint in plan 34-03. D-08 requires two separate gates; do not collapse them and do not add a
third.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-releases-por-paquete/34-CONTEXT.md
@.planning/phases/34-releases-por-paquete/34-PATTERNS.md
@.planning/phases/34-releases-por-paquete/34-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This phase creates **no new public symbols, classes or functions** — the surface it publishes was
built and verified by Phases 29-33. This plan produces the middle group.

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| Completed + de-provisionalized `### v0.5.0` changelog | `packages/market-data-client/README.md` | 34-01 Tasks 1 & 2 | `grep -qx '### v0.5.0'` + ten backticked identifier tokens |
| Four aligned version sites (iol 0.3.0, market-data 0.5.0) | two `pyproject.toml` + two `__init__.py` | 34-01 Task 2 | `awk` / `grep -qx` assertions |
| Single-refresh `uv.lock` registering both members | `uv.lock` | 34-01 Task 3 | `grep -A1` per member + `uv lock --check` + 2/2 churn |
| Pushed release branch (fast-forward) | `git-ref:origin/milestone/v1.5-mutations` | 34-01 Task 3 | `git rev-parse HEAD` == `git rev-parse origin/...` |
| **The updated release PR (#12, edited not replaced)** | **`gh-pr:12`** | **34-02 Task 1** | `gh pr view 12 --json number,title,state,baseRefName,headRefName` |
| **Merge commit on `main` (two parents)** | **`git-ref:origin/main`** | **34-02 Task 3** | `git rev-list --parents -n1 origin/main` → 3 fields |
| Release tag for iol-client | `git-tag:iol-client-v0.3.0` | 34-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Release tag for market-data-client | `git-tag:market-data-client-v0.5.0` | 34-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Public GitHub Release + wheel + sdist (iol) | `gh-release:iol-client-v0.3.0` | 34-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
| Public GitHub Release + wheel + sdist (market-data) | `gh-release:market-data-client-v0.5.0` | 34-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
| Refreshed release memory | `.claude/projects/-Users-admin-development-market-libs/memory/market-data-client-releases.md` | 34-03 Task 3 | region-by-region greps in that task |
</artifacts_this_phase_produces>

<flagged_assumptions>
No SPEC.md exists for this phase, so the deterministic edge-probe engine ran in the planner's place
against PUB-TYP-01. All three applicable probe categories came back **unresolved** and none maps
onto a release-publish requirement. Per the no-silent-drop rule they are surfaced here as explicit
flagged assumptions rather than omitted or forced into `must_haves`.

| Probe category | Probe question | Status | Flagged assumption |
|---|---|---|---|
| adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | unresolved — not applicable | PUB-TYP-01 defines no comparison or merge semantics over a data structure. The nearest real analogue is the two tags landing on the *same* merge commit this plan produces — an intended, non-degenerate configuration locked by D-09/D-10 and asserted in 34-03 Task 2. No collection or comparison logic exists to probe. |
| empty | What is the result for empty, single-element, or null input? | unresolved — not applicable | PUB-TYP-01 takes no input collection. The nearest real analogue is a CI run reporting **zero** checks, which `ci.yml`'s `paths-ignore: ["**.md", ".gitignore"]` makes genuinely reachable for a Markdown-only diff; that degenerate case is covered by the exact `TOTAL = 15` gate in Task 1 of this plan, not by an edge-probe truth. |
| ordering | When elements compare equal, is output order specified and stable? | unresolved — not applicable | PUB-TYP-01 produces no ordered output. The nearest real analogue is the *operational* ordering that IS load-bearing and IS specified: merge (this plan) strictly before tag (plan 34-03). That is a sequencing constraint in the plan text, not sortable output with a stability question. |

If a future revision of PUB-TYP-01 introduces collection or comparison semantics, re-run the probe
rather than inheriting this dismissal.
</flagged_assumptions>

<reversibility_gates>
| Task | Operation | Reversibility | Gate |
|---|---|---|---|
| Task 1 | `gh pr edit 12` (title + body) | fully reversible — a PR title and body can be edited again at any time | none needed |
| Task 1 | `gh pr checks 12 --watch` | read-only | none needed |
| Task 2 | operator go/no-go | n/a — this IS the gate | **blocking human checkpoint, D-08(a)** |
| Task 3 | `gh pr merge 12 --merge` | **irreversible in practice** — publishes ~the whole Phase 29-34 diff to the public default branch; a revert is a forward commit, not an unpublish | gated by Task 2 |

This plan carries exactly ONE of the phase's two blocking gates. Plan 34-03 carries the other.
Adding a gate here, or collapsing this one into 34-03's, violates D-08.
</reversibility_gates>

<scope_decisions>
- **PR #12 is edited, never replaced (D-05).** It is currently titled
  `milestone v1.6: tipado homogéneo de la superficie pública (phases 29-32)`, state `OPEN`,
  `MERGEABLE`, base `main`, head `milestone/v1.5-mutations`. Do not run `gh pr close`, do not run
  `gh pr create`, and do not open a second PR. This is a delta from the Phase 28 precedent, whose
  28-02 Task 1 ran `gh pr create`.
- **`.planning/` stays in the diff (D-06).** Do not run `/gsd-pr-branch` and do not filter the
  planning artifacts out.
- **No rebase and no merge of `origin/main` into the branch (D-07).** `HEAD..main` consists only of
  the four merge commits of PRs #8/#9/#10/#11, already contained in this branch's history; the
  divergence is cosmetic.
- **`.github/workflows/` is read-only (D-11).** If a check fails, surface the failing job and step;
  never patch `ci.yml` or `release.yml` to make a gate pass.
- **No tag in this plan.** Both tags belong to 34-03, behind the second gate.
- **TDD framing does not apply.** This plan performs only git/gh operations and produces no code.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Update PR #12 in place and assert 15/15 checks pass BY COUNT, with 2 iol rows and 2 market-data rows (D-05, D-06, SC#3)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/34-releases-por-paquete/34-PATTERNS.md` § PR #12 update — the three-assertion precondition block, the `gh pr edit 12` substitute for `gh pr create`, the count-based CI gate reproduced verbatim, and the Phase-34 delta requiring two per-package grep counts instead of one
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` § Decisions D-05, D-06, D-07, D-08, D-09
    - `.github/workflows/ci.yml` — READ-ONLY. Trigger block and `paths-ignore` (`:6`, `:11`), `concurrency: cancel-in-progress: true` (`:20`), the three non-matrix job names (`Lint y formato (ruff)` `:24`, `pre-commit hooks` `:69`, `Type check (mypy)` `:87`), the test job name template (`:108`), `matrix.package` (`:113-119`) and `matrix.python-version` (`:120`)
    - `.planning/phases/34-releases-por-paquete/34-01-SUMMARY.md` — the four commit SHAs, the recomputed ahead count at push time, and the confirmation that `origin/milestone/v1.5-mutations` equals local `HEAD`
    - `.planning/milestones/v1.5-phases/28-release-prep-publish-v0-3-0/28-02-PLAN.md` Task 1 — the literal count-based gate and the reasoning for why an absence-of-failure check is inadequate. Ignore its `28-RESEARCH.md` line citations; that file is Phase-28-scoped and has no Phase-34 equivalent.
  </read_first>
  <action>
    (a) Preconditions, all three must hold before anything else: `gh auth status` exits 0 (never echo
    the token — the command already redacts it); `git status --porcelain` is empty; and
    `git rev-parse HEAD` equals `git rev-parse origin/milestone/v1.5-mutations`. That third assertion
    was FALSE before plan 34-01 Task 3 — local was 47+ commits ahead of origin — and 34-01's push is
    what makes it true. If it is still false, STOP: updating PR #12 would describe a head that does
    not contain the release. Recompute rather than trusting any literal count.

    (b) Update PR **#12** in place with `gh pr edit 12`. Do NOT run `gh pr close` and do NOT run
    `gh pr create` — D-05 mandates updating the existing vehicle. This is the one structural delta
    from the Phase 28 precedent.

    New title, following the `release: <scope>` convention established by PRs #8-#11 and extended to
    two packages:
    `release: iol-client v0.3.0 + market-data-client v0.5.0 (tipado homogéneo de la superficie pública, fases 29-34)`

    The current title (`milestone v1.6: tipado homogéneo de la superficie pública (phases 29-32)`) is
    stale on two counts: it names phases 29-32 when the branch now carries 29-34, and it names no
    version at all.

    Body wording is Claude's discretion; supply it with `--body-file` rather than an inline argument
    so multi-line Markdown survives shell quoting. It must cover, concretely:
      - the two packages and their exact version transitions: `iol-client` 0.2.0 → 0.3.0 and
        `market-data-client` 0.4.0 → 0.5.0;
      - that BOTH are source-breaking, and that both carry a README changelog callout — iol's
        `### v0.3.0` (dict→modelo across 16 signatures, including the truthiness flip and the
        `to_dict()` escape hatch) and market-data's `### v0.5.0` (four Phase 31 ops endpoints typed,
        plus the three Phase 33 shape fixes SC-1/SC-2/SC-3);
      - the phase span actually contained in the head: 29 through 34;
      - that `.planning/` artifacts are intentionally included (D-06);
      - that the four unchanged packages are NOT bumped and NOT published.
    The body must contain no credential and no secret value. The GitHub Releases themselves use
    `--generate-notes`, so the body does not need to be release-notes-shaped.

    (c) Wait for the checks to settle with `gh pr checks 12 --watch`, then take a final snapshot with
    `gh pr checks 12`.

    (d) Assert the gate BY COUNT, never by the absence of the word "fail". Require all four of: the
    total row count is exactly 15; the number of rows whose status field is `pass` is exactly 15;
    exactly 2 rows match `Tests · iol-client · py3.1[23]`; and exactly 2 rows match
    `Tests · market-data-client · py3.1[23]`.

    Statuses other than `pass` include `fail`, `pending`, `skipping` and `cancelled` — an
    absence-of-failure check reads all of those as green, and `cancelled` is genuinely reachable
    because `ci.yml:20` sets `concurrency: cancel-in-progress: true`. A Markdown-only diff would
    trigger ZERO checks (`paths-ignore: ["**.md", ".gitignore"]`), and "no checks" is not green —
    which is exactly why the total must equal 15 rather than merely being non-zero.

    The count is still 15 even though two packages changed instead of one: `matrix.package`
    (`ci.yml:113-119`) already lists all six packages unconditionally, so 6 × 2 = 12 test checks plus
    the three non-matrix jobs is invariant to how many packages a PR touches.

    (e) Do NOT push any further commit to the branch while checks are running — it cancels the
    in-flight run. If a fix is genuinely required, land it, then re-run `gh pr checks 12 --watch` to
    completion and re-assert the full 15/15 count from scratch. Never patch `ci.yml` to make a check
    pass (D-11).

    (f) Do NOT merge in this task. Do NOT create or push any tag in this task or anywhere in this
    plan. The merge is gated by Task 2; both tags are gated by a separate checkpoint in plan 34-03.
  </action>
  <verify>
    <automated>gh auth status >/dev/null 2>&1 && test -z "$(git status --porcelain)" && git fetch origin milestone/v1.5-mutations --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/milestone/v1.5-mutations)" && PR=12 && test "$(gh pr view "$PR" --json state --jq .state)" = "OPEN" && test "$(gh pr view "$PR" --json baseRefName --jq .baseRefName)" = "main" && test "$(gh pr view "$PR" --json headRefName --jq .headRefName)" = "milestone/v1.5-mutations" && gh pr view "$PR" --json title --jq .title | grep -q '^release: iol-client v0\.3\.0 + market-data-client v0\.5\.0' && gh pr view "$PR" --json files --jq '.files[].path' | grep -q '^\.planning/' && TOTAL=$(gh pr checks "$PR" | wc -l | tr -d ' ') && PASSED=$(gh pr checks "$PR" | awk -F'\t' '$2=="pass"' | wc -l | tr -d ' ') && IOL=$(gh pr checks "$PR" | grep -c 'Tests · iol-client · py3\.1[23]') && MD=$(gh pr checks "$PR" | grep -c 'Tests · market-data-client · py3\.1[23]') && test "$TOTAL" = "15" && test "$PASSED" = "15" && test "$IOL" = "2" && test "$MD" = "2" && test "$(gh pr list --state open --base main --json number --jq 'length')" = "1" && test -z "$(git tag -l 'iol-client-v0.3.0')" && test -z "$(git tag -l 'market-data-client-v0.5.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --state open --base main --json number --jq 'length'` outputs `1`, and that PR's number is `12` — no replacement PR was opened and #12 was not closed (D-05)
    - `gh pr view 12 --json title --jq .title` starts with `release: iol-client v0.3.0 + market-data-client v0.5.0 (`
    - `gh pr view 12 --json state --jq .state` is `OPEN`, `baseRefName` is `main`, `headRefName` is `milestone/v1.5-mutations`
    - The PR body names both version transitions (`0.2.0` → `0.3.0` and `0.4.0` → `0.5.0`), states that both packages are source-breaking, and names the phase span 29-34
    - `gh pr view 12 --json files --jq '.files[].path'` includes at least one path under `.planning/` (D-06 — the artifacts were not filtered out)
    - `gh pr checks 12 | wc -l` outputs exactly `15`
    - `gh pr checks 12 | awk -F'\t' '$2=="pass"' | wc -l` outputs exactly `15` — no row is `fail`, `pending`, `skipping` or `cancelled`
    - `gh pr checks 12 | grep -c 'Tests · iol-client · py3\.1[23]'` outputs exactly `2`
    - `gh pr checks 12 | grep -c 'Tests · market-data-client · py3\.1[23]'` outputs exactly `2`
    - `git rev-parse HEAD` still equals `git rev-parse origin/milestone/v1.5-mutations` — no commit was pushed mid-run
    - `git tag -l 'iol-client-v0.3.0'` and `git tag -l 'market-data-client-v0.5.0'` are both empty
    - No `gh pr merge`, no `gh pr close` and no `gh pr create` command was executed in this task
  </acceptance_criteria>
  <done>PR #12 is updated in place with the two-package release title and a body covering Phases 29-34 and both versions, it still carries the `.planning/` artifacts, and `gh pr checks 12` reports 15 rows of which 15 are `pass`, including exactly 2 iol-client and 2 market-data-client matrix jobs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FIRST blocking go/no-go gate — before the IRREVERSIBLE merge to the public default branch (D-08a)</name>
  <read_first>
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` D-08 — exactly two blocking checkpoints, both explicit and independent, never collapsed. This is gate (a).
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` D-09 — merge commit real, never squash, never rebase, and why.
    - The 34-02 Task 1 output already in context: the PR URL and the counted check totals
    - `.planning/milestones/v1.5-phases/28-release-prep-publish-v0-3-0/28-02-PLAN.md` Task 2 — the literal shape of this checkpoint in the direct precedent. Ignore its `28-RESEARCH.md` line citations.
  </read_first>
  <acceptance_criteria>
    - The operator was shown: the PR URL and number (12), the literal `gh pr checks 12` output with the counted totals (15 rows / 15 `pass` / 2 iol-client rows / 2 market-data-client rows), the diff stat, both exact version transitions, and an explicit statement that `main` has no branch protection so this approval is the only gate
    - No `gh pr merge`, no `git tag` and no `git push` command was executed before the operator's reply
    - The operator's reply is recorded verbatim in the SUMMARY together with its timestamp
    - The approval came from a literal operator response — not from `auto_advance`, not from yolo mode, not inferred from silence, and not self-issued by the agent
    - Execution continues to Task 3 ONLY if the reply is an explicit "approved"; any other reply (including silence, ambiguity, or "abort") halts the plan with PR #12 still open and `origin/main` unchanged
  </acceptance_criteria>
  <action>PAUSE. This is the first of exactly two blocking human checkpoints for this phase (`autonomous: false`). Do NOT merge the PR, do NOT create either tag, and do NOT push anything until the operator explicitly replies "approved". This gate is never auto-approvable: `auto_advance` and yolo mode do not satisfy it, silence does not satisfy it, and the agent may not self-issue it. If the operator replies "abort", stop cleanly with no irreversible action taken — PR #12 stays open and everything so far is revertible with a commit. Present: the PR URL and number (12), the literal `gh pr checks 12` output with the counted totals (15 rows / 15 pass / 2 iol-client rows / 2 market-data-client rows), the diff stat, the two exact version transitions being published, and an explicit statement that `main` has NO branch protection so this approval is the only gate.</action>
  <what-built>
    PR #12 (`milestone/v1.5-mutations` → `main`) has been UPDATED in place — not replaced — and now
    reads `release: iol-client v0.3.0 + market-data-client v0.5.0 (tipado homogéneo de la superficie
    pública, fases 29-34)`. All 15 CI checks report `pass`, counted explicitly: 15 total rows, 15
    with status `pass`, 2 of them `Tests · iol-client · py3.12`/`· py3.13` and 2 of them
    `Tests · market-data-client · py3.12`/`· py3.13`. The working tree is clean and `gh` is
    authenticated.

    What this release publishes, both source-breaking:
      - `iol-client` 0.2.0 → **0.3.0** — the dict→modelo change across all 16 signatures, with the
        `### v0.3.0` changelog callout authored back in Phase 30 (including the truthiness flip and
        the `to_dict()` escape hatch).
      - `market-data-client` 0.4.0 → **0.5.0** — the four Phase 31 ops endpoints now returning typed
        models, PLUS the three Phase 33 shape fixes the operator locked `fix-shape-now` at the 33-07
        checkpoint: SC-1 `preview_calendar_config` now returns `CalendarConfigPreview` instead of
        `CalendarConfig`; SC-2 `MarketDataSnapshot.entries`/`.market_data`/`.staleness_seconds` are
        now `| None`; SC-3 `Symbol.created_at`/`.updated_at` are now `str | None`.

    The other four packages (`higyrus-client`, `ambito-financiero-client`, `matriz-client`,
    `wallets-client`) are NOT bumped and NOT published — their public surface did not change.

    The next task performs an operation that is irreversible in practice: merging this PR into
    `main`, the default branch of a PUBLIC repository. It will use `gh pr merge 12 --merge` — a real
    merge commit with two parents, never `--squash` and never `--rebase`, because squashing would
    orphan the SHAs that dozens of SUMMARY files across Phases 29-33 cross-reference by value.

    CRITICAL CONTEXT: `main` has no branch protection. GitHub will merge this PR regardless of check
    status. The 15/15 count above and YOUR approval are the only things preventing an unverified
    build from landing on a public default branch.

    A credential scan over the full `origin/main...HEAD` diff was re-run immediately before the push
    in plan 34-01 Task 3 and came back clean: no JWT-shaped token, no `client_secret` assignment, no
    tracked `.env`.

    The release TAGS and the public GitHub Releases are NOT part of this approval. Both tags are
    gated by a second, separate checkpoint in plan 34-03 (D-08 requires two independent gates, and
    that second gate covers both tags in one approval).
  </what-built>
  <how-to-verify>
    1. Open PR #12 on GitHub. Confirm the title is
       `release: iol-client v0.3.0 + market-data-client v0.5.0 (tipado homogéneo de la superficie pública, fases 29-34)`,
       base `main`, head `milestone/v1.5-mutations`, and that this is the SAME PR #12 that was
       already open — not a replacement (D-05).
    2. Confirm the diff scope looks right: the `iol-client` and `market-data-client` package sources
       and tests from Phases 29-33, the version bumps in both `pyproject.toml` files and both
       `__init__.py` files, the single `uv.lock` refresh, the `### v0.5.0` README changelog entry,
       and the `.planning/` artifacts (intentionally kept — D-06).
    3. Confirm the `### v0.5.0` changelog entry in `packages/market-data-client/README.md` names all
       three Phase 33 shape breaks — `preview_calendar_config` → `CalendarConfigPreview`;
       `MarketDataSnapshot.entries` / `.market_data` / `.staleness_seconds` → `| None`;
       `Symbol.created_at` / `.updated_at` → `str | None` — alongside the four Phase 31 ops-endpoint
       breaks. Seven items, not four.
    4. Confirm the `### v0.5.0` heading no longer carries the "not yet published" qualifier and that
       the paragraph deferring the bump to this phase is gone — it would be false once merged.
    5. Confirm no file under `.github/workflows/` appears in the diff (D-11).
    6. Confirm the check table shows 15 rows and all 15 are `pass` — not pending, not cancelled, and
       not an empty table.
    7. Confirm you authorize the irreversible merge into public `main` NOW, and that you understand
       the tag push is a separate approval you will be asked for again.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `gh pr merge 12 --merge`, or "abort" (optionally describing blockers) to stop before any irreversible action. Anything other than an explicit "approved" means do not merge.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge PR #12 with a real merge commit and verify `origin/main` advanced to a two-parent commit carrying both bumps (D-09, D-10)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/34-releases-por-paquete/34-PATTERNS.md` § Checkpoint gate (a) — merge — the merge command form, the two-parent assertion and the merged-tree version assertions generalized to both packages
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` D-09 (merge commit real, never squash, never rebase) and D-10 (tags go on the merge SHA, which is why the shape of this commit matters)
    - `.github/workflows/release.yml` lines 42-51 — READ-ONLY. The awk version-match gate that will run against the TAGGED tree in plan 34-03, which is why this task asserts the merged tree already carries both versions.
    - `.planning/milestones/v1.5-phases/28-release-prep-publish-v0-3-0/28-02-PLAN.md` Task 3 — the merge + verification command forms in the direct precedent. Ignore its `28-RESEARCH.md` line citations.
  </read_first>
  <action>
    Execute ONLY after Task 2 returned an explicit "approved" from the operator. If it returned
    anything else — including silence, ambiguity, or an auto-advance signal — stop.

    (a) Merge with `gh pr merge 12 --merge`. The `--merge` flag is mandatory (D-09): `--squash` would
    collapse the branch into a single commit and orphan every SHA that the Phase 29-33 SUMMARYs
    cross-reference by value; `--rebase` would rewrite published history reachable from the existing
    `iol-client-v0.2.0` and `market-data-client-v0.4.0` tags. The repository allows all three merge
    methods, so nothing but this instruction prevents the wrong one. `delete_branch_on_merge` is
    `false`, so `milestone/v1.5-mutations` survives the merge — plan 34-03 commits the release-memory
    update onto it afterwards. Do NOT pass a branch-delete flag.

    (b) Refresh local refs: `git fetch origin main --tags`. Then capture the merge commit as
    `MERGE_SHA=$(git rev-parse origin/main)` and inspect it with
    `git log -1 --format='%H %p %s' "$MERGE_SHA"` — the `%p` field MUST list two parent SHAs and the
    subject must be a `Merge pull request #12 …` line. Assert it mechanically:
    `git rev-list --parents -n1 origin/main` must yield exactly three whitespace-separated fields. A
    single-parent commit means the merge was squashed or rebased: STOP and surface it rather than
    proceeding, because plan 34-03 tags exactly this commit and `release.yml` validates each version
    against the tagged tree.

    (c) Assert the merged tree actually carries BOTH bumps, read with the same
    `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` expression `release.yml:47` uses:
    `git show origin/main:packages/iol-client/pyproject.toml` must report `0.3.0`, and
    `git show origin/main:packages/market-data-client/pyproject.toml` must report `0.5.0`. If either
    does not, the corresponding tag would be rejected by the pipeline's version-match gate — STOP.

    (d) Do NOT create or push any tag here. Do NOT push to `main` directly; the merge goes through
    the PR so attribution, PR closure and the merge-commit shape all match the prior releases. Never
    echo the `gh` token or any credential.

    Record `MERGE_SHA` and both parent SHAs in the SUMMARY — plan 34-03 recomputes `MERGE_SHA` from
    `git rev-parse origin/main` rather than trusting a literal, but the recorded value is the
    cross-check.
  </action>
  <verify>
    <automated>git fetch origin main --tags --quiet && test "$(git rev-list --parents -n1 origin/main | wc -w | tr -d ' ')" -eq 3 && git log -1 --format=%s origin/main | grep -q '^Merge pull request' && IV=$(git show origin/main:packages/iol-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$IV" = "0.3.0" && MV=$(git show origin/main:packages/market-data-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$MV" = "0.5.0" && test "$(gh pr view 12 --json state --jq .state)" = "MERGED" && git ls-remote --heads origin milestone/v1.5-mutations | grep -q . && test -z "$(git tag -l 'iol-client-v0.3.0')" && test -z "$(git tag -l 'market-data-client-v0.5.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --parents -n1 origin/main | wc -w` outputs `3` — the commit plus exactly two parents (D-09)
    - `git log -1 --format=%s origin/main` begins with `Merge pull request`
    - `git show origin/main:packages/iol-client/pyproject.toml` parsed with `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` outputs `0.3.0`
    - `git show origin/main:packages/market-data-client/pyproject.toml` parsed the same way outputs `0.5.0`
    - `gh pr view 12 --json state --jq .state` is `MERGED` (not `OPEN`, not `CLOSED`)
    - `git tag -l 'iol-client-v0.3.0'` and `git tag -l 'market-data-client-v0.5.0'` are both still empty — no tag was created in this plan
    - `git ls-remote --heads origin milestone/v1.5-mutations` still returns a ref — the branch survived the merge and is available for the plan 34-03 memory commit
    - The command history shows `gh pr merge 12 --merge` and contains no `--squash`, no `--rebase` and no branch-delete flag
    - `MERGE_SHA` and both parent SHAs are recorded in the SUMMARY
  </acceptance_criteria>
  <done>PR #12 is merged; `origin/main` points at a real merge commit with two parents whose tree carries `version = "0.3.0"` for iol-client and `version = "0.5.0"` for market-data-client; no tag exists yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release branch → public `main` | The merge publishes the whole Phase 29-34 diff to the default branch of a PUBLIC repository. This crossing is irreversible in practice. |
| CI check results → merge decision | `main` has no branch protection, so GitHub applies no gate of its own; the check status only influences the merge if the agent enforces it. |
| agent → irreversible GitHub operation | `gh pr merge` succeeds unconditionally; only the human checkpoint stands between an unverified build and `main`. |
| PR body / agent output → `gh` OAuth token | Ambient credential; any echo lands in transcripts and in a public PR body. |
| merge strategy → cross-referenced SHAs | Phases 29-33 SUMMARYs cite commit SHAs by value; squash or rebase orphans them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering | merging red, pending or cancelled code into an unprotected public `main` | mitigate | Task 1 (d) asserts the gate by COUNT — 15 total rows, 15 with status `pass`, 2 iol-client rows, 2 market-data-client rows — instead of an absence-of-failure check, which passes on pending, cancelled and empty output. `cancel-in-progress: true` (`ci.yml:20`) makes `cancelled` genuinely reachable, and a Markdown-only diff would produce zero checks. ASVS L1 V4. |
| T-34-06 | Elevation of Privilege / Repudiation | the irreversible merge itself | mitigate | Task 2 is a blocking `checkpoint:human-verify` in an `autonomous: false` plan; Task 3 executes only on an explicit literal "approved" and is not satisfiable by `auto_advance` or yolo mode; "abort" stops cleanly with PR #12 still open. With no branch protection on `main`, this checkpoint is the sole access control on the operation. ASVS L1 V4. |
| T-34-09 | Tampering | merge strategy (squash / rebase would rewrite history) | mitigate | Task 3 (a) mandates `gh pr merge 12 --merge` and forbids `--squash` / `--rebase`; the acceptance criteria assert the resulting commit has exactly two parents. Preserves every SHA the Phase 29-33 SUMMARYs cite. |
| T-34-10 | Tampering | wrong PR vehicle — closing #12 and opening a replacement | mitigate | Task 1 (b) uses `gh pr edit 12` and forbids `gh pr close` / `gh pr create`; the acceptance criteria assert exactly one open PR against `main` and that its number is 12 (D-05). |
| T-34-03a | Tampering | tagging a commit that is not the merge commit, or one whose tree lacks a bump | mitigate | This plan creates no tag at all; Task 3 (c) verifies the merged tree already reads `0.3.0` and `0.5.0` so plan 34-03 can tag `origin/main` directly and satisfy `release.yml:42-51` for both tags. A single-parent commit halts the phase. |
| T-34-07 | Information Disclosure | PR body / `gh` token | mitigate | The PR body carries no credential; the GitHub Releases use `--generate-notes`; `gh auth status` is used instead of ever echoing the token. ASVS L1 V2, V14. |
| T-34-SC | Tampering | uv/pip/cargo installs | accept | No package installs occur in this plan. |
</threat_model>

<verification>
1. Exactly one open PR against `main`, and it is #12 — edited in place, not replaced (D-05).
2. PR #12's title is the two-package release title and its body names both version transitions and the 29-34 phase span.
3. `gh pr checks 12` reports 15 rows; exactly 15 have status `pass`; exactly 2 are the iol-client matrix jobs and exactly 2 are the market-data-client matrix jobs.
4. The PR diff still contains `.planning/` paths (D-06) and no `.github/workflows/` paths (D-11).
5. The operator explicitly replied "approved" before any merge command ran, and the reply is recorded verbatim with its timestamp (D-08a).
6. `git rev-list --parents -n1 origin/main | wc -w` equals 3 (D-09).
7. The merged tree reads `0.3.0` for iol-client and `0.5.0` for market-data-client under `release.yml`'s own awk expression.
8. Neither `iol-client-v0.3.0` nor `market-data-client-v0.5.0` exists as a tag at the end of this plan.
</verification>

<success_criteria>
- The 15-green gate was enforced by explicit counting, not by pattern-matching for failure, because nothing on GitHub enforces it.
- No irreversible operation ran without an explicit, literal human "approved" (D-08a) — and that approval was not manufactured by auto-advance.
- PR #12 was updated in place, preserving the review history already attached to it (D-05).
- `main` carries a real merge commit with two parents whose tree satisfies the version-match gate for BOTH tags, ready to be tagged by plan 34-03.
- The release branch still exists on `origin` so plan 34-03 can land the release-memory commit on it.
</success_criteria>

<output>
Create `.planning/phases/34-releases-por-paquete/34-02-SUMMARY.md` when done.

Record in the SUMMARY: the PR number and URL, the final title, the literal counted check totals
(total / pass / iol-client rows / market-data-client rows), the operator's approval text and
timestamp for the D-08(a) gate recorded verbatim, the captured `MERGE_SHA` with its two parent SHAs,
both merged-tree versions as read by the `release.yml` awk expression, and the merge method used.
Record explicitly that no tag was created in this plan and that the second gate remains outstanding.
</output>
