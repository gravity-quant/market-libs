---
phase: 14-iol-disk-persistence-sec-01
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - packages/iol-client/src/iol_client/_token_cache.py
  - packages/iol-client/src/iol_client/_state.py
  - packages/iol-client/src/iol_client/client.py
autonomous: true
requirements: [SEC-01]
requirements_addressed: [SEC-01]

must_haves:
  truths:
    - "iol_client._token_cache module exists with load, save, delete public helpers + _resolve_default_path()"
    - "logger = logging.getLogger(__name__) namespace (= iol_client._token_cache) inherits v1.1 LOG-02 RedactingFilter (anti-Pitfall 7 mitigation)"
    - "_token_cache.save() uses atomic write-then-rename: tempfile + fcntl.flock(LOCK_EX) + json.dump + flush + os.fsync + chmod 0600 + os.replace + tempfile.unlink on exception"
    - "_resolve_default_path() returns None when os.environ.get('CI') == 'true' (anti-Pitfall 10 CI safety)"
    - "Corrupt-file recovery (D-C1) logs ONE warning with type(exc).__name__ ONLY (NEVER exc, exc.args, or file contents)"
    - "_ClientState gains token_cache_path: Path | None = None field (iol-only carve-out per D-T4)"
    - "Client.__init__ accepts token_cache_path kwarg; resolves precedence: explicit kwarg -> IOL_TOKEN_CACHE_PATH env var -> _resolve_default_path() (None if CI)"
    - "Client._ensure_token loads from disk on first access if state.refresh_token is None and state.token_cache_path is set"
    - "Client._refresh writes to disk on success (acquired_at=time.time()); on 401 deletes disk file BEFORE password fallback (anti-Pitfall 8); Client.login also writes on password-grant success"
    - "Negative D-T4: token_cache_path field NOT added to ambito/higyrus/matriz/wallets _state.py"
    - "Sync test rows + 3 CRITICAL merge gates pass GREEN; async test rows still RED (Plan 3)"
  artifacts:
    - path: "packages/iol-client/src/iol_client/_token_cache.py"
      provides: "Disk persistence helpers: load, save, delete, _resolve_default_path"
      contains: "logger = logging.getLogger(__name__)"
      min_lines: 120
    - path: "packages/iol-client/src/iol_client/_state.py"
      provides: "_ClientState gains token_cache_path field"
      contains: "token_cache_path: Path | None = None"
    - path: "packages/iol-client/src/iol_client/client.py"
      provides: "Client.__init__ accepts token_cache_path; _ensure_token loads from disk; _refresh writes/deletes via _token_cache"
      contains: "token_cache_path"
  key_links:
    - from: "Client.__init__ token_cache_path precedence resolution"
      to: "_token_cache._resolve_default_path()"
      via: "explicit kwarg | os.environ['IOL_TOKEN_CACHE_PATH'] | _resolve_default_path()"
      pattern: "_resolve_default_path"
    - from: "Client._refresh post-success path"
      to: "_token_cache.save(path, refresh_token, acquired_at=time.time())"
      via: "after parse_refresh_response returns truthy refresh + write-back to state"
      pattern: "_token_cache\\.save"
    - from: "Client._ensure_token post-401 (before password fallback)"
      to: "_token_cache.delete(path)"
      via: "IOLAuthError caught in _ensure_token then call delete BEFORE login()"
      pattern: "_token_cache\\.delete"
    - from: "Client._ensure_token cold start"
      to: "_token_cache.load(path)"
      via: "state.refresh_token is None and state.token_cache_path is not None then populate state.refresh_token from disk dict"
      pattern: "_token_cache\\.load"
---

<objective>
Implement the sync side of Phase 14 SEC-01: create `iol_client/_token_cache.py` (POSIX-locked atomic disk persistence helpers) + add `token_cache_path` field to `_ClientState` (iol-only D-T4 carve-out) + wire `Client.__init__` / `_ensure_token` / `_refresh` / `login` to consume the helpers. Honors decisions D-F1, D-A1, D-C1, D-P1, D-P3 from CONTEXT.md.

Purpose: Make the sync rows of `verification/test_iol_disk_persistence.py` (Plan 1) GREEN, including the 3 CRITICAL merge gates (anti-Pitfalls 7, 8, 9). The 3 CRITICAL tests exercise `_token_cache.save/load` directly + sync `Client(token_cache_path=...)._ensure_token()`, so once Plan 2 ships, those 3 + 4 sync regression tests turn GREEN. Async rows stay RED until Plan 3.

Output:
- `packages/iol-client/src/iol_client/_token_cache.py`: NEW module (~150 LOC). Public helpers `load(path) -> dict[str, Any] | None`, `save(path, refresh_token, *, acquired_at) -> None`, `delete(path) -> None`, plus internal `_resolve_default_path() -> Path | None` + `_SCHEMA_VERSION = 1`. Logger via `logging.getLogger(__name__)` (anti-Pitfall 7 mitigation).
- `packages/iol-client/src/iol_client/_state.py`: 1 line added (`token_cache_path: Path | None = None`) + `from pathlib import Path` import.
- `packages/iol-client/src/iol_client/client.py`: ~25 LOC across `Client.__init__`, `Client._ensure_token`, `Client._refresh`, `Client.login`.
- Sync rows of Plan 1 verification tests + 3 CRITICAL merge gates GREEN.
- Commit message per CONTEXT.md `<specifics>`: `feat(iol-client): _token_cache.py + Client(token_cache_path) sync integration (SEC-01)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md
@.planning/phases/14-iol-disk-persistence-sec-01/14-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@packages/iol-client/src/iol_client/_state.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/_logging.py
@verification/test_iol_disk_persistence.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create iol_client/_token_cache.py with load/save/delete + _resolve_default_path + LOG-02-inheriting logger</name>
  <files>packages/iol-client/src/iol_client/_token_cache.py</files>
  <read_first>
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<specifics>` "Atomic write-then-rename pattern (D-P3, planner uses verbatim)" — the exact `save()` shape
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<decisions>` D-F1 (JSON shape: `{version: 1, refresh_token, acquired_at}`), D-A1 (sync helpers), D-C1 (corrupt-recovery log line exact), D-P3 (atomic write-rename)
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<code_context>` "Integration Points" then "packages/iol-client/src/iol_client/_token_cache.py" sub-section (the module skeleton)
    - packages/iol-client/src/iol_client/_logging.py (confirm `iol_client` logger has RedactingFilter via NullHandler installed; `getLogger(__name__)` from a submodule inherits it)
    - .planning/research/PITFALLS.md §Pitfall 7 (logger namespace mitigation: MUST be under `iol_client.*` so RedactingFilter propagates up)
    - .planning/research/PITFALLS.md §Pitfall 10 (CI=true refusal + chmod 0600 + parent 0700)
  </read_first>
  <behavior>
    - Module exposes 3 public helpers: `load`, `save`, `delete`. Plus internal `_resolve_default_path` and `_SCHEMA_VERSION` constant. Module itself has leading underscore so it is package-private (operators do not import it directly).
    - `_resolve_default_path() -> Path | None`: returns `None` when `os.environ.get("CI") == "true"` (anti-Pitfall 10). Otherwise returns `Path(platformdirs.user_data_dir("iol-client", "market-libs")) / "refresh_token.json"`. Invariant: this function NEVER raises and NEVER touches the filesystem.
    - `load(path: Path) -> dict[str, Any] | None`:
      - If `path` does not exist (FileNotFoundError on open), return `None` silently (no log).
      - Otherwise open for read with `fcntl.flock(LOCK_SH)`, parse JSON, validate shape: `version == _SCHEMA_VERSION (== 1)` AND `refresh_token` is non-empty str AND `acquired_at` is float-compatible. On any failure (`json.JSONDecodeError`, `KeyError`, `TypeError`, `ValueError`, `OSError` other than FileNotFoundError), log EXACTLY ONE warning per D-C1 with `type(exc).__name__` ONLY (NEVER `exc`, `exc.args`, `repr(exc)`, or file contents), and return `None`. Do NOT delete the file (D-C1).
      - Return the validated dict on success: `{"version": 1, "refresh_token": "...", "acquired_at": 1234.0}`.
    - `save(path: Path, refresh_token: str, *, acquired_at: float) -> None`:
      - Verbatim implementation per CONTEXT.md `<specifics>` "Atomic write-then-rename pattern".
      - Sequence: `path.parent.mkdir(parents=True, exist_ok=True, mode=0o700)` then tempfile path `path.parent / f"{path.name}.tmp.{os.getpid()}.{secrets.token_hex(4)}"` then `with open(tmp, "w", encoding="utf-8") as f: fcntl.flock(f.fileno(), fcntl.LOCK_EX); json.dump({"version": _SCHEMA_VERSION, "refresh_token": refresh_token, "acquired_at": acquired_at}, f); f.flush(); os.fsync(f.fileno())` then `os.chmod(tmp, 0o600)` then `os.replace(tmp, path)`. Wrap in try/except: on any exception `tmp.unlink(missing_ok=True)` then re-raise.
      - chmod 0600 on the TEMPFILE before `os.replace` per Claude's Discretion in CONTEXT.md so the final file has correct perms from the first moment (no 0644 window).
      - Phase 14 targets POSIX (Linux/macOS) per CONTEXT.md `<domain>`. On Windows the `fcntl` import fails fast at module import — documented scope-lock.
      - NO log line on success (defense in depth: zero log sites equals zero leak risk).
    - `delete(path: Path) -> None`: `path.unlink(missing_ok=True)`. Idempotent. No log line.
    - Logger: top-of-module `logger = logging.getLogger(__name__)` resolves to `"iol_client._token_cache"` which inherits the `iol_client`-namespace `RedactingFilter` installed by v1.1 LOG-02 (anti-Pitfall 7 mitigation source). THIS is the critical line that turns `test_disk_persistence_never_logs_token` GREEN.
    - Module docstring per CLAUDE.md convention: describes purpose (disk persistence), API (sync helpers consumed by `client.py` directly + by `aio.py` via `asyncio.to_thread`), env vars (`CI` for safety refusal, `IOL_TOKEN_CACHE_PATH` for explicit override consumed in `client.py`).
    - `from __future__ import annotations` mandatory line 1 of imports.
  </behavior>
  <action>
    Create `packages/iol-client/src/iol_client/_token_cache.py`. Implement per `<behavior>` exactly. Key code shape per CONTEXT.md `<specifics>`:

    - Imports (after `from __future__ import annotations`): `import fcntl`, `import json`, `import logging`, `import os`, `import secrets`, `from pathlib import Path`, `from typing import Any`, `import platformdirs`.
    - Module-level: `logger = logging.getLogger(__name__)`, `_SCHEMA_VERSION = 1`.
    - `_resolve_default_path()` per `<behavior>` — single guard `if os.environ.get("CI") == "true": return None`, then platformdirs path construction.
    - `load(path)` per `<behavior>` — `try: with open(path, "rb") as f: fcntl.flock(f.fileno(), fcntl.LOCK_SH); data = json.load(f)` then validate. On `FileNotFoundError`: return None silently. On any OTHER exception: log warning with EXACTLY the format from CONTEXT.md `<specifics>` D-C1: `logger.warning("ignoring unreadable token cache at %s (%s); falling back to password grant", path, type(exc).__name__)`. Return None.
    - `save(path, refresh_token, *, acquired_at)` per CONTEXT.md `<specifics>` atomic write-then-rename — VERBATIM. Use `open(tmp, "w", encoding="utf-8")` (text mode for json.dump), flock LOCK_EX, json.dump + flush + os.fsync, chmod 0600 outside the `with` block (file closed), os.replace, exception handler unlinks tempfile and re-raises.
    - `delete(path)` per `<behavior>` — `path.unlink(missing_ok=True)`.

    Type hints strict (mypy `strict = true`): `load(path: Path) -> dict[str, Any] | None`, `save(path: Path, refresh_token: str, *, acquired_at: float) -> None`, `delete(path: Path) -> None`, `_resolve_default_path() -> Path | None`. No `Any`-returning functions except `load` (the dict shape is validated).

    Do NOT export `_token_cache` from `packages/iol-client/src/iol_client/__init__.py` — module is package-private (leading underscore). Consumers are `client.py` + `aio.py` siblings.

    Implements D-A1 (sync helpers), D-C1 (corrupt-recovery log), D-F1 (JSON shape), D-P3 (atomic write-rename), anti-Pitfalls 7 (logger namespace), 9 (fcntl.flock), 10 (CI refusal + 0600/0700 perms) per CONTEXT.md.
  </action>
  <verify>
    <automated>uv run pytest verification/test_iol_disk_persistence.py::test_disk_persistence_never_logs_token verification/test_iol_disk_persistence.py::test_disk_token_write_under_concurrent_processes --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/iol-client/src/iol_client/_token_cache.py` exists.
    - Source: `grep -c 'from __future__ import annotations' packages/iol-client/src/iol_client/_token_cache.py` returns `1`.
    - Source: `grep -c 'logger = logging.getLogger(__name__)' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (anti-Pitfall 7 namespace).
    - Source: `grep -c 'def save(path: Path, refresh_token: str' packages/iol-client/src/iol_client/_token_cache.py` returns `1`.
    - Source: `grep -c 'def load(path: Path)' packages/iol-client/src/iol_client/_token_cache.py` returns `1`.
    - Source: `grep -c 'def delete(path: Path) -> None' packages/iol-client/src/iol_client/_token_cache.py` returns `1`.
    - Source: `grep -c 'def _resolve_default_path() -> Path | None' packages/iol-client/src/iol_client/_token_cache.py` returns `1`.
    - Source: `grep -c 'fcntl.LOCK_EX' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (Anti-Pitfall 9 mutating lock).
    - Source: `grep -c 'fcntl.LOCK_SH' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (read lock).
    - Source: `grep -c 'os.replace(tmp, path)' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (atomic rename).
    - Source: `grep -c '0o600' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (Anti-Pitfall 10 chmod).
    - Source: `grep -c '0o700' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (parent dir 0700).
    - Source: `grep -c 'os.environ.get("CI") == "true"' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (anti-Pitfall 10 CI refusal).
    - Source: `grep -c 'type(exc).__name__' packages/iol-client/src/iol_client/_token_cache.py` returns `1` (D-C1 sanitized log line).
    - Negative D-C1: `grep -vE '^(#|\s*#)' packages/iol-client/src/iol_client/_token_cache.py | grep -cE '(repr\(exc\)|exc\.args|str\(exc\))'` returns `0` (NEVER leak exception detail outside comments).
    - Negative D-T4: `grep -r platformdirs packages/ambito-financiero-client/src/ packages/higyrus-client/src/ packages/matriz-client/src/ packages/wallets-client/src/ | wc -l | tr -d ' '` returns `0`.
    - Test: `uv run pytest verification/test_iol_disk_persistence.py::test_disk_persistence_never_logs_token` exits 0 (anti-Pitfall 7 CRITICAL merge gate).
    - Test: `uv run pytest verification/test_iol_disk_persistence.py::test_disk_token_write_under_concurrent_processes` exits 0 (anti-Pitfall 9 CRITICAL merge gate).
    - Quality: `uv run ruff check packages/iol-client/src/iol_client/_token_cache.py` exits 0.
    - Quality: `uv run ruff format --check packages/iol-client/src/iol_client/_token_cache.py` exits 0.
    - Quality: `uv run mypy --strict packages/iol-client/src/iol_client/_token_cache.py` exits 0.
  </acceptance_criteria>
  <done>
    `_token_cache.py` module exists, lints clean, mypy strict clean. 2 of 3 CRITICAL merge gates (anti-Pitfalls 7 + 9) GREEN. Third (anti-Pitfall 8 cleanup-on-401) requires client.py wiring in Task 3 to also turn GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add token_cache_path field to _ClientState (iol-only D-T4 carve-out)</name>
  <files>packages/iol-client/src/iol_client/_state.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/_state.py (full file — confirm the `@dataclass(slots=True) class _ClientState:` shape and the existing field ordering)
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<code_context>` "packages/iol-client/src/iol_client/_state.py" sub-section (the exact line shape)
    - .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-04-PLAN.md (Phase 13 D-T3 matriz-only `client_max_retries` carve-out precedent — Phase 14 mirrors this idiom but for iol)
  </read_first>
  <behavior>
    - `_ClientState` gains a single new field: `token_cache_path: Path | None = None`.
    - Placed at the END of the field list, AFTER the existing `client_lock: asyncio.Lock | None = None` field. This preserves the existing field ordering for any tests that introspect via `dataclasses.fields()`.
    - Import `from pathlib import Path` at the top of `_state.py` (currently NOT imported — confirmed by reading the file).
    - Inline comment on the field: `# Phase 14 SEC-01 (D-T4 iol-only carve-out): disk-cache path for refresh_token. None disables disk persistence (covers CI=true + operator opt-out path).`
    - D-T4 negative scope: do NOT modify ambito/higyrus/matriz/wallets `_state.py`. This field exists ONLY in iol-client.
  </behavior>
  <action>
    Edit `packages/iol-client/src/iol_client/_state.py`. Add `from pathlib import Path` to the imports block (after `import os`, before `from dataclasses import dataclass, field`). Add the new field as the LAST field of the `_ClientState` dataclass, after `client_lock: asyncio.Lock | None = None`. Use the inline comment per `<behavior>`.

    Do NOT modify `__all__` (field is referenced via `_state.token_cache_path`, not re-exported as a module-level name).

    Do NOT touch `_env_*` helpers (no env-driven default for `token_cache_path` at the state level — `Client.__init__` resolves precedence including the env var in Task 3, NOT the state default factory).

    Negative D-T4: do NOT modify `packages/ambito-financiero-client/src/ambito_financiero_client/_state.py`, `packages/higyrus-client/src/higyrus_client/_state.py`, `packages/matriz-client/src/matriz_client/_state.py`, or `packages/wallets-client/src/wallets_client/_state.py`.

    Implements D-T4 single-package carve-out per CONTEXT.md `<decisions>` "Plan 2 — `_token_cache.py` + sync Client integration".
  </action>
  <verify>
    <automated>grep -c 'token_cache_path: Path | None = None' packages/iol-client/src/iol_client/_state.py && for f in packages/ambito-financiero-client/src/ambito_financiero_client/_state.py packages/higyrus-client/src/higyrus_client/_state.py packages/matriz-client/src/matriz_client/_state.py packages/wallets-client/src/wallets_client/_state.py; do test ! -f "$f" || test "$(grep -c token_cache_path $f)" = "0"; done && uv run mypy --strict packages/iol-client/src</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'token_cache_path: Path | None = None' packages/iol-client/src/iol_client/_state.py` returns `1`.
    - Source: `grep -c 'from pathlib import Path' packages/iol-client/src/iol_client/_state.py` returns `1`.
    - Negative D-T4: every other package's `_state.py` returns `0` for `grep -c token_cache_path`.
    - Behavior: `python -c "from iol_client._state import _ClientState; s = _ClientState(); assert s.token_cache_path is None"` exits 0.
    - Behavior: `python -c "from pathlib import Path; from iol_client._state import _ClientState; s = _ClientState(token_cache_path=Path('/tmp/x')); assert str(s.token_cache_path) == '/tmp/x'"` exits 0.
    - Quality: `uv run mypy --strict packages/iol-client/src` exits 0 (field type annotation passes strict mode).
    - Quality: `uv run ruff check packages/iol-client/src/iol_client/_state.py` exits 0.
  </acceptance_criteria>
  <done>
    `_ClientState` carries `token_cache_path: Path | None = None`; iol-only per D-T4; mypy strict clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire Client.__init__ + _ensure_token + _refresh + login to consume _token_cache (sync side)</name>
  <files>packages/iol-client/src/iol_client/client.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/client.py:127-160 (`Client.__init__` body — confirm kwarg ordering and `self._state` assignment idiom)
    - packages/iol-client/src/iol_client/client.py:338-376 (`login`, `_refresh`, `_ensure_token` — confirm where to insert disk read/write/delete logic)
    - packages/iol-client/src/iol_client/_token_cache.py (Task 1 output — confirm helper signatures)
    - packages/iol-client/src/iol_client/_state.py (Task 2 output — confirm `token_cache_path` field exists)
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<code_context>` "Integration Points" then "packages/iol-client/src/iol_client/client.py — modifica `Client.__init__` + `_ensure_token` + `_refresh`" sub-section
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<decisions>` D-P1 "Plan 2" sub-bullet on `_ensure_token` and `_refresh` modifications
    - .planning/research/PITFALLS.md §Pitfall 8 (cleanup-on-401 BEFORE password fallback — CRITICAL ordering; "load -> use -> on 401: delete disk + clear in-memory -> password fallback -> write fresh disk")
  </read_first>
  <behavior>
    - `Client.__init__`: add `token_cache_path: Path | None = None` kwarg AFTER `token_expires_at` and BEFORE `max_retries`. Full new signature ordering: `base_url, username, password, token, token_expires_at, token_cache_path, max_retries, http_client`. All are keyword-only (the existing `*` is preserved).
    - In `__init__` body, AFTER setting all existing fields and BEFORE `self._max_retries = max_retries`, insert the precedence resolution. Logic: if `token_cache_path` is not None set state from kwarg, elif `os.environ.get("IOL_TOKEN_CACHE_PATH")` returns truthy set state from `Path(env_value)`, else set state from `_token_cache._resolve_default_path()` (which returns None on CI). Result: `self._state.token_cache_path` is always set (possibly to None).
    - `Client._ensure_token`: BEFORE the existing `if _core.token_is_fresh(self._state): return` line, insert cold-init disk load. Logic: if `self._state.refresh_token is None and self._state.token_cache_path is not None`, call `_token_cache.load(self._state.token_cache_path)`; if result is not None, set `self._state.refresh_token = loaded["refresh_token"]`. Rationale: runs once per cold instance; subsequent calls see `state.refresh_token` non-None and skip the disk read. Does NOT touch `state.token` or `state.token_expires_at` — those still trigger the existing refresh/login flow below.
    - `Client._ensure_token` cleanup-on-401 path (Anti-Pitfall 8 CRITICAL): the existing structure is `if self._state.refresh_token: try: self._refresh() except IOLAuthError: pass else: return; self.login()`. The cleanup-on-401 MUST happen INSIDE the `except IOLAuthError` block — BEFORE the fall-through to `self.login()`. New shape: inside the `except IOLAuthError:` block, if `self._state.token_cache_path is not None` call `_token_cache.delete(self._state.token_cache_path)`, then set `self._state.refresh_token = None` (clear in-memory). After the try/except/else block, the fall-through `self.login()` runs the password grant.
    - `Client._refresh`: after the existing `self._state.token = token` / `self._state.token_expires_at = expires_at` / `if refresh is not None: self._state.refresh_token = refresh` block, append disk write. Logic: if `self._state.token_cache_path is not None and self._state.refresh_token is not None` call `_token_cache.save(self._state.token_cache_path, self._state.refresh_token, acquired_at=time.time())`. The `acquired_at` clock-read happens at the call site (not inside the helper) per Claude's Discretion in CONTEXT.md — keeps the helper pure (testable with deterministic timestamps).
    - `Client.login`: after the existing `self._state.token = token` / `self._state.token_expires_at = expires_at` / `if refresh is not None: self._state.refresh_token = refresh` block, append the SAME disk write logic as `_refresh`. This is REQUIRED for the anti-Pitfall 8 CRITICAL test: the test calls `_ensure_token()` ONCE, which dispatches refresh-401 then password-grant — the password-grant path must write the FRESH token to disk in that same call. PITFALLS.md spec line 403: "load -> use -> on 401: delete disk + clear in-memory -> password fallback -> write fresh disk".
    - Negative-shape preservations: do NOT modify `with_options` (Phase 13 view inherits `token_cache_path` via shared `_state` — no extra wiring needed; this is the carry-forward documented in CONTEXT.md `<domain>`). Do NOT modify `configure()` (operator can pass a path via `Client(token_cache_path=...)` or env var; adding a kwarg to `configure()` is OPTIONAL scope and CONTEXT.md does NOT mandate it for Phase 14 — defer to v1.3 if operator demands).
    - Logging: NO new log sites in `client.py` related to disk persistence. All log lines come from `_token_cache.py`. Defense in depth for anti-Pitfall 7 (fewer log surfaces equals less leak risk).
  </behavior>
  <action>
    Edit `packages/iol-client/src/iol_client/client.py`. Add imports near the top of the imports block (preserve ruff isort `I` rule ordering — stdlib first, third-party next, project last): `import os`, `import time`, `from pathlib import Path`, then in the project-imports block `from iol_client import _token_cache`. The existing `from iol_client._state import _REQUEST_TIMEOUT, _ClientState` ordering reveals project imports use `from iol_client.X` — add `from iol_client import _token_cache` alongside `from iol_client import _core, _transport` (line 54). Run `uv run ruff format` after to confirm ordering.

    Modify `Client.__init__` signature: insert `token_cache_path: Path | None = None,` between `token_expires_at: float | None = None,` and `max_retries: int = 2,`. Modify body to set `self._state.token_cache_path` per the precedence ladder in `<behavior>`. Place this block AFTER the existing `if token_expires_at is not None: self._state.token_expires_at = token_expires_at` and BEFORE the Phase 8 D-15 comment + `self._max_retries = max_retries`.

    Modify `Client._ensure_token`: insert cold-init disk load as the FIRST statement of the method body (before `if _core.token_is_fresh(...)`). Modify the `except IOLAuthError` block to delete disk + clear in-memory refresh_token per `<behavior>`.

    Modify `Client._refresh`: append disk save AFTER the existing state mutation block (after the `if refresh is not None:` conditional, before the `return token` line).

    Modify `Client.login`: append disk save AFTER the existing state mutation block (mirror of `_refresh`, same shape).

    Do NOT modify `configure()`, `with_options()`, `_send_auth_request()`, `_ensure_http_client()`, `_request()`, or any endpoint methods.

    Do NOT modify `aio.py` (Plan 3 owns the async mirror).

    Implements per CONTEXT.md `<decisions>` "Plan 2 — `_token_cache.py` + sync Client integration" sub-bullets on `__init__`, `_ensure_token`, `_refresh`, `login`.
  </action>
  <verify>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/ -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'token_cache_path: Path | None = None,' packages/iol-client/src/iol_client/client.py` returns `1` (Client.__init__ kwarg signature).
    - Source: `grep -c 'IOL_TOKEN_CACHE_PATH' packages/iol-client/src/iol_client/client.py` returns `1` (env var precedence).
    - Source: `grep -c '_token_cache._resolve_default_path' packages/iol-client/src/iol_client/client.py` returns `1` (default path fallback).
    - Source: `grep -c '_token_cache.load' packages/iol-client/src/iol_client/client.py` returns `1` (cold init disk read).
    - Source: `grep -c '_token_cache.save' packages/iol-client/src/iol_client/client.py` returns `2` (one in _refresh, one in login).
    - Source: `grep -c '_token_cache.delete' packages/iol-client/src/iol_client/client.py` returns `1` (anti-Pitfall 8 cleanup-on-401).
    - Source: `grep -c 'acquired_at=time.time()' packages/iol-client/src/iol_client/client.py` returns `2` (one in _refresh, one in login).
    - Behavior: `python -c "from pathlib import Path; from iol_client import Client; c = Client(token_cache_path=Path('/tmp/iol-test.json'), username='x', password='y'); assert c._state.token_cache_path == Path('/tmp/iol-test.json')"` exits 0.
    - Behavior: `CI=true python -c "from iol_client import Client; c = Client(username='x', password='y'); assert c._state.token_cache_path is None"` exits 0 (anti-Pitfall 10 CI refusal).
    - Behavior: `IOL_TOKEN_CACHE_PATH=/tmp/from-env python -c "from pathlib import Path; from iol_client import Client; c = Client(username='x', password='y'); assert c._state.token_cache_path == Path('/tmp/from-env')"` exits 0.
    - Test: `uv run --package iol-client pytest packages/iol-client/tests/ -x` exits 0 (existing iol-client tests preserved; baseline-non-regression).
    - Test: `uv run pytest verification/test_iol_disk_persistence.py::test_disk_token_deleted_on_refresh_401` exits 0 (anti-Pitfall 8 CRITICAL merge gate — the third one).
    - Test: `uv run pytest verification/test_iol_disk_persistence.py -k "_sync"` shows 4 passing (4 sync regression tests).
    - Test (RED still expected): `uv run pytest verification/test_iol_disk_persistence.py -k "_async"` shows 4 failing (async tests pending Plan 3).
    - Quality: `uv run ruff check packages/iol-client/src/iol_client/client.py` exits 0.
    - Quality: `uv run ruff format --check packages/iol-client/src/iol_client/client.py` exits 0.
    - Quality: `uv run mypy --strict packages/iol-client/src` exits 0.
    - Negative D-D1 (driver scope lock): `git diff HEAD -- main_iol.py | wc -l | tr -d ' '` returns `0` — Phase 14 does NOT modify main_iol.py (Phase 15 REFAC-05 decides driver adoption).
  </acceptance_criteria>
  <done>
    Sync side fully wired. All 3 CRITICAL merge gates (anti-Pitfalls 7, 8, 9) GREEN. 4 sync regression tests GREEN. 4 async regression tests still RED (Plan 3 turns them GREEN). Existing iol-client test suite preserved (baseline non-regression). Commit message: `feat(iol-client): _token_cache.py + Client(token_cache_path) sync integration (SEC-01)`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem -> Client read | `_token_cache.load()` reads attacker-controllable bytes if filesystem is compromised; D-C1 corrupt-recovery returns None silently rather than crash/leak |
| Client write -> filesystem | `_token_cache.save()` writes secret-bearing JSON; anti-Pitfalls 9 (fcntl.flock) + 10 (chmod 0600 + parent 0700 + CI refusal) define the contract |
| IOL refresh-401 -> password fallback | anti-Pitfall 8 boundary: stale token MUST be deleted from disk BEFORE password grant (delete-then-grant-then-write) |
| _token_cache log records -> RedactingFilter | anti-Pitfall 7 boundary: `logging.getLogger(__name__)` must resolve under `iol_client.*` namespace so v1.1 LOG-02 filter propagates up |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 (T1) | Information Disclosure | `_token_cache` log sites (now IMPLEMENTED in Task 1) | mitigate | `logger = logging.getLogger(__name__)` resolves to `iol_client._token_cache` which inherits v1.1 LOG-02 RedactingFilter. Single warning line on corrupt-file uses `type(exc).__name__` ONLY (D-C1). No log site on successful save/load/delete. Anti-Pitfall 7. Verified by `test_disk_persistence_never_logs_token` (Task 1 acceptance). |
| T-14-02 (T2) | Tampering (stale state) | `Client._ensure_token` cleanup-on-401 path (Task 3) | mitigate | `except IOLAuthError:` block calls `_token_cache.delete(state.token_cache_path)` BEFORE falling through to `self.login()`. `Client.login` writes the FRESH refresh_token to disk. Net effect: disk file contains FRESH token after recovery, NOT STALE. Anti-Pitfall 8. Verified by `test_disk_token_deleted_on_refresh_401` (Task 3 acceptance). |
| T-14-03 (T3) | Denial of Service (data corruption) | concurrent disk writes (Task 1) | mitigate | `_token_cache.save()` uses `fcntl.flock(LOCK_EX)` inside the write critical section + `os.replace(tmp, path)` atomic rename. Operating in 20-thread stress test the file always contains EXACTLY one valid token (last-writer-wins is acceptable; corruption is not). Anti-Pitfall 9. Verified by `test_disk_token_write_under_concurrent_processes` (Task 1 acceptance). |
| T-14-04 (T4) | Information Disclosure (filesystem) | default-path resolution + perms (Task 1) | mitigate | `_resolve_default_path()` returns `None` when `os.environ.get("CI") == "true"` -> default-path persistence refused on shared CI runners. Where persistence enabled: `path.parent.mkdir(mode=0o700)` + `os.chmod(tmp, 0o600)` BEFORE `os.replace` so the final file has 0o600 from the first moment. Anti-Pitfall 10. Verified by `test_disk_token_preserved_when_no_kwarg_sync` (turns GREEN in Task 3). |
| T-14-SC | Tampering (supply chain) | `platformdirs>=4.0,<5` runtime dep | mitigate | Pinned per RESEARCH.md STACK. Widely-used (top 1k PyPI). Resolved via `uv lock` in Plan 1 Task 3 — lockfile content-hash-verified by uv on each `uv sync`. Not flagged as ASSUMED/SUS/SLOP. |
</threat_model>

<verification>
- `_token_cache.py` exists with `load`, `save`, `delete`, `_resolve_default_path` per D-A1/D-F1/D-C1/D-P3/anti-Pitfalls 7/9/10.
- `_state.py` carries `token_cache_path: Path | None = None` (iol-only per D-T4).
- `client.py` wires `Client.__init__` precedence + `_ensure_token` disk read/cleanup-on-401 + `_refresh`/`login` disk write.
- 3 CRITICAL merge gates GREEN (anti-Pitfalls 7, 8, 9).
- 4 sync regression rows of Plan 1 verification tests GREEN.
- 4 async regression rows still RED (Plan 3 closes them).
- Existing `packages/iol-client/tests/` suite preserved (baseline-non-regression).
- D-T4 negative grep clean on the other 4 packages' `_state.py` (no `token_cache_path` field).
- D-D1 negative: `main_iol.py` UNTOUCHED — Phase 15 REFAC-05 decides driver adoption.
- Quality: ruff check + ruff format + mypy strict all exit 0 for `packages/iol-client/`.

**Nyquist skip rationale:** No VALIDATION.md exists for Phase 14 — the user opted out of research in this phase (no Validation Architecture artifact required per planning context). Coverage of SEC-01 success criteria #1-5 delivered via the 3 CRITICAL cross-cutting tests + per-package mocked iol-client test suite. This carries forward the Phase 13 pattern. Plan 2 closes the SYNC half of the coverage; Plan 3 closes the ASYNC half and consolidates the final green gate.
</verification>

<success_criteria>
- `_token_cache.py` module exists per D-A1/D-F1/D-C1/D-P3 with anti-Pitfalls 7, 9, 10 mitigations in place.
- `_state.py` carries `token_cache_path` (iol-only D-T4).
- `client.py` wires sync side; all 3 CRITICAL merge gates + 4 sync regression rows GREEN.
- Async rows of `verification/test_iol_disk_persistence.py` still RED (intentional — Plan 3 owns).
- `packages/iol-client/tests/` baseline preserved.
- D-T4 + D-D1 negative scope verified.
- Plan 2 committed atomically per CONTEXT.md commit message.
</success_criteria>

<output>
Create `.planning/phases/14-iol-disk-persistence-sec-01/14-02-SUMMARY.md` when done.
</output>
