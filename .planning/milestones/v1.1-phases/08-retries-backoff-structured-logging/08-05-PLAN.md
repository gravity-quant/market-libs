---
phase: 08-retries-backoff-structured-logging
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - packages/matriz-client/src/matriz_client/_transport.py
  - packages/matriz-client/src/matriz_client/_logging.py
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/matriz-client/src/matriz_client/_core.py
  - packages/matriz-client/src/matriz_client/client.py
  - packages/matriz-client/tests/test_transport.py
  - packages/matriz-client/tests/test_logging.py
  - verification/snapshots/matriz_client-surface.txt
  - verification/test_sync_async_isolation.py
autonomous: true
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, matriz, atomic-commit, rely, log, sync-only, risk-api-no-reauth, auth-basic-redaction]
user_setup: []

must_haves:
  truths:
    - "`matriz_client/_transport.py` define `RetryTransport(httpx.HTTPTransport)` ÚNICAMENTE (NO `_atransport.py` per D-25 — Phase 10 territory); mirror Plans 2-4 con `_LOGGER_NAME='matriz_client'`"
    - "`matriz_client/aio.py` NO MODIFICADO (stub Phase 6 preservado; Phase 10 REFAC-04 lo extiende a full REST surface) — verified via wc -l aio.py == 103 (Phase 6 stub baseline)"
    - "`matriz_client/_logging.py` define `RedactingFilter` con D-22 matriz auth_basic redaction policy: detect `Authorization: Basic <base64>` header pattern + `auth_basic` tuple en `record.__dict__` → split to `auth_basic_user=<user>` + `auth_basic_password='<redacted>'`. Plus Bearer/X-Auth-Token/X-Username/X-Password headers (matriz login per `_core.py:255-262`)"
    - "`matriz_client/__init__.py` invoca `_logging.attach()` con idempotency + `del` cleanup (LOG-01)"
    - "`_core.py RequestSpec` extendido con `endpoint_name: str = ''` (D-09) + `account_id: str | None = None` (D-11 — matriz). `auth_basic: tuple[str,str] | None = None` (Phase 7) preserved"
    - "GET builders flippean `idempotent=True` + setean `endpoint_name=<fn>` + `account_id=account` cuando builder lo recibe (D-11). POST mutating builders (`build_new_order_request`, `build_cancel_order_request`, `build_replace_order_request`) MANTIENEN `idempotent=False` — CRITICAL: HTTP GET en Primary API (quirk), pero `idempotent=False` previene duplicate orders per D-01/D-07/D-24/Pitfall 4"
    - "`build_login_request` marcado `idempotent=True` (D-03)"
    - "`Client.__init__` + `configure()` extendidos con `max_retries=2` + `http_client=None` (D-15). `AsyncClient.__init__` UNCHANGED per D-25"
    - "Shell `_request()` sync: branch en `spec.auth_basic is not None` para Risk API path (D-23): construye request con `httpx.BasicAuth(*spec.auth_basic)`, sets extensions, NO 401 re-auth (auth_basic 401 = credenciales inválidas → AuthError inmediato). Token path: 401 re-auth-once estándar (D-02 + Plan 3 pattern)"
    - "Shell `_request()` setea `extensions['account_id']` desde `spec.account_id` (D-11) y `extensions['idempotent'/'request_id'/'endpoint_name']` estándar"
    - "matriz 200-OK con `status=='ERROR'` sigue funcionando per Phase 7 D-06 (`parse_envelope_response` body-consume-then-raise `PrimaryAPIError`); `PrimaryAPIError` NEVER en `retry_on=` per D-24"
    - "`verification/snapshots/matriz_client-surface.txt` actualizado SOLO con 2 nuevos kwargs en Client.__init__ + configure(); `AsyncClient.__init__` signature UNCHANGED per D-25"
    - "`verification/test_sync_async_isolation.py` parametrize EXTENDIDO: matriz async branch skip con razón actualizada `pytest.skip('matriz aio.py REST stub hasta Phase 10 REFAC-04')`"
    - "Cross-cutting guard tests: `test_retry_mutation_gate.py[matriz_new_order]` GREEN (POST mutating 503 → 1 wire request — CRITICAL duplicate-order prevention); `test_retry_401_reauth.py[matriz Primary]` GREEN; `test_retry_401_reauth.py[matriz Risk]` GREEN (auth_basic → 401 → AuthError + 1 wire request, NO re-auth — D-23); `test_logging_no_token_leak.py[matriz]` GREEN"
    - "matriz existing tests (Phase 7 baseline incluyendo matriz sweep snapshot 18 cases) GREEN; CR-03 (`parse_envelope_response` body consume) + CR-05 (`_envelope_probe` 18 sweeps) preservados"
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_transport.py"
      provides: "RetryTransport sync (mirror Plans 2-4)"
      contains: "class RetryTransport"
      min_lines: 90
    - path: "packages/matriz-client/src/matriz_client/_logging.py"
      provides: "RedactingFilter con D-22 auth_basic + X-Auth-Token/X-Username/X-Password headers redaction"
      contains: "auth_basic"
      min_lines: 80
    - path: "packages/matriz-client/tests/test_transport.py"
      provides: "Unit tests for matriz RetryTransport — mutation gate sobre new_order POST, Risk API auth_basic path no-reauth"
      contains: "auth_basic"
      min_lines: 90
    - path: "packages/matriz-client/tests/test_logging.py"
      provides: "Unit tests for matriz RedactingFilter — D-22 auth_basic tuple splitting + Authorization Basic header redaction"
      contains: "auth_basic"
      min_lines: 65
    - path: "verification/snapshots/matriz_client-surface.txt"
      provides: "matriz snapshot con 2 nuevos kwargs en Client/configure() (AsyncClient unchanged per D-25)"
      contains: "max_retries"
  key_links:
    - from: "matriz_client._core.build_new_order_request"
      to: "matriz_client._transport.RetryTransport mutation gate"
      via: "request.extensions['idempotent'] = False → transport pass-through (NO retry)"
      pattern: "idempotent=False"
    - from: "matriz_client.client.Client._request auth_basic branch"
      to: "matriz_client._transport.RetryTransport Risk API path sin re-auth"
      via: "spec.auth_basic is not None → skip 401 re-auth (D-23)"
      pattern: "auth_basic"
    - from: "matriz_client._logging.RedactingFilter"
      to: "auth_basic password redaction (D-22)"
      via: "scan record.__dict__ for 'auth_basic' tuple → split user/password + redact password"
      pattern: "auth_basic"
---

<objective>
matriz Plan 8-05 (sync-only + atomic + Risk API special): replicar el patrón higyrus (Plan 4) en `matriz-client` con los extras matriz-specific: `_transport.py` ÚNICAMENTE (NO `_atransport.py` per D-25 — Phase 10 REFAC-04 lo agrega cuando crea `aio.py` REST surface), `_logging.py` con D-22 auth_basic redaction policy (Risk API tuple-splitting), shell `_request()` con BRANCH para `spec.auth_basic is not None` (Risk API path: no 401 re-auth per D-23; Token path: re-auth-once estándar). matriz `aio.py` NO se modifica (Phase 6 stub preservado, 103 LOC unchanged). Plus snapshot update SOLO para Client + configure() (AsyncClient signature unchanged per D-25). Per-package atomic commit per D-21.

Purpose: matriz es el paquete más complejo de Phase 8 — combina (a) sync-only delivery (D-25 — async deferred Phase 10), (b) Risk API auth_basic exclusion del 401 re-auth flow (D-23), (c) PrimaryAPIError NEVER en retry_on= (D-24 — `status=='ERROR'` 200-OK son application-level), (d) mutation gate critical (matriz POST orders en HTTP GET — Pitfall 4 duplicate-order risk), (e) D-22 auth_basic logging redaction policy. Preservación intacta de CR-03 (`parse_envelope_response` body-consume) + CR-05 (`_envelope_probe` 18 sweeps) de Phase 7. Cumple Phase 8 ROADMAP Success Criteria #1-5 para el paquete con mayor surface + más specials.

Output: 1 commit atómico per D-21 con 1 nuevo módulo transport sync (`_transport.py`) — NO `_atransport.py` per D-25 + 1 nuevo módulo logging (`_logging.py`) + 3 extends (`_core.py`, `client.py`, `__init__.py`) — NOT `aio.py` + 2 nuevos test files + 1 snapshot update (Client + configure() ONLY) + 1 extend a `verification/test_sync_async_isolation.py` (matriz async skip reason refresh). Commit message: `feat(matriz): retries + structured logging — sync-only (aio.py defer Phase 10) + Risk API 401-no-reauth + status=ERROR no-retry (RELY-01..04, LOG-01..03)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md
@.planning/phases/08-retries-backoff-structured-logging/08-04-SUMMARY.md
@CLAUDE.md
@packages/matriz-client/src/matriz_client/_state.py
@packages/matriz-client/src/matriz_client/_core.py
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/aio.py
@packages/matriz-client/src/matriz_client/__init__.py
@packages/matriz-client/src/matriz_client/exceptions.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/tests/conftest.py
@verification/snapshots/matriz_client-surface.txt
@verification/test_sync_async_isolation.py
@verification/test_matriz_sweep_snapshot.py
@packages/higyrus-client/src/higyrus_client/_transport.py
@packages/higyrus-client/src/higyrus_client/_logging.py
@packages/higyrus-client/src/higyrus_client/client.py
@verification/test_retry_mutation_gate.py
@verification/test_retry_401_reauth.py
@verification/test_retry_after_cap.py
@verification/test_logging_root_unchanged.py
@verification/test_logging_no_token_leak.py
@verification/test_async_cancellation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create matriz _transport.py (sync ONLY — NO _atransport.py per D-25) + _logging.py with D-22 auth_basic redaction policy + unit tests</name>
  <files>packages/matriz-client/src/matriz_client/_transport.py, packages/matriz-client/src/matriz_client/_logging.py, packages/matriz-client/tests/test_transport.py, packages/matriz-client/tests/test_logging.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-01, D-04, D-07, D-08 — RetryTransport invariants; D-22 — matriz auth_basic redaction policy CRITICAL; D-23 — matriz Risk API path (transport YES, re-auth NO); D-24 — matriz 200-OK status=ERROR NEVER retry; D-25 — matriz `_atransport.py` NOT created in Plan 5)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 1 lines 348-504 — canonical RetryTransport; Pattern 4 lines 605-682 — canonical RedactingFilter; §"Common Pitfalls" Pitfall 2 lines 745-753 — matriz new_order duplicate-order risk; Pitfall 15 — Retries swallow PrimaryAPIError)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"Per-package redaction recipes" line 260 — matriz: D-22 Authorization Basic + auth_basic tuple → user/password split; X-Username/X-Password headers per `_core.py:255-262`; §"`packages/<pkg>/src/<pkg>/_transport.py`" line 130 — matriz no special-case status=ERROR; §"`packages/<pkg>/src/<pkg>/_atransport.py`" line 177-178 — matriz NOT in Phase 8)
    - packages/matriz-client/src/matriz_client/_core.py (lines 246-262 build_login_request — X-Username/X-Password headers; lines 128-142 RequestSpec with `auth_basic` Phase 7 baseline; mutating builders `build_new_order_request`, `build_cancel_order_request`, `build_replace_order_request`; `parse_envelope_response` — Phase 7 CR-03 body-consume-then-raise, MUST NOT be touched)
    - packages/higyrus-client/src/higyrus_client/_transport.py (Plan 4 baseline — verbatim duplicate template)
    - packages/higyrus-client/src/higyrus_client/_logging.py (Plan 4 baseline — strip JSON token + cuit higyrus-specific; replace with matriz-specific patterns)
    - packages/matriz-client/src/matriz_client/exceptions.py (AuthenticationError class — used in 401 re-auth catch)
  </read_first>
  <behavior>
    Create matriz sync-only transport + logging modules + tests.

    **`packages/matriz-client/src/matriz_client/_transport.py`** (~95 LOC — mechanical duplicate of Plan 4 higyrus _transport.py):
    - Identical body except `_LOGGER_NAME = "matriz_client"` + module docstring updated for matriz
    - All canonical RetryTransport invariants preserved (mutation gate, Retry-After cap, full-jitter backoff, structured log fields)
    - NO matriz-specific branch for `status=='ERROR'` (per PATTERNS line 130 — application-level errors raised by `_core.parse_envelope_response` AFTER transport returns; transport sees only HTTP status codes which for OK envelope responses = 200, NOT in `_RETRYABLE_STATUS`)
    - `_RETRYABLE_STATUS` SAME for all 4 packages: `frozenset({408, 409, 429, *range(500, 600)})` — no matriz-specific subset

    **NO `_atransport.py`** per D-25 — Phase 10 REFAC-04 creates this file when adding `aio.py` REST surface.

    **`packages/matriz-client/src/matriz_client/_logging.py`** (~85 LOC — matriz-specific D-22 auth_basic redaction):
    - Module docstring: matriz logger + LOG-01/02/03 + D-22 auth_basic redaction
    - `from __future__ import annotations`
    - Imports: `logging`, `re` (no need for `base64` if we only redact, not decode)
    - Module-level regex (matriz-specific set per PATTERNS line 260):
      - `_BEARER_RE = re.compile(r"Bearer\s+[A-Za-z0-9._\-]+")`
      - `_X_AUTH_TOKEN_RE = re.compile(r"(X-Auth-Token\s*:\s*)[A-Za-z0-9._\-+/=]+", re.IGNORECASE)`
      - `_X_USERNAME_RE = re.compile(r"(X-Username\s*:\s*)[^\r\n]+", re.IGNORECASE)`
      - `_X_PASSWORD_RE = re.compile(r"(X-Password\s*:\s*)[^\r\n]+", re.IGNORECASE)`
      - `_AUTH_BASIC_RE = re.compile(r"(Authorization\s*:\s*Basic\s+)[A-Za-z0-9+/=]+", re.IGNORECASE)`
      - `_PASSWORD_URLENC_RE = re.compile(r"(password=)[^&\s]+")`
      - `_PASSWORD_JSON_RE = re.compile(r'("password"\s*:\s*")[^"]+(")')`

      Notes:
      - `_AUTH_BASIC_RE` for D-22 — `Authorization: Basic <base64>` Risk API header redaction
      - `_X_USERNAME_RE` + `_X_PASSWORD_RE` for matriz login headers per `_core.py:255-262`
      - NO JSON token regex (matriz uses X-Auth-Token header not JSON token like higyrus)
      - NO `_REFRESH_TOKEN_*` (iol-specific)
      - NO cuit (higyrus-specific PII)

    - `def _redact(text: str) -> str`: apply all 7 substitutions in order
    - `def _redact_auth_basic_tuple(t) -> dict[str, str]` per D-22 — utility used inside RedactingFilter to split `auth_basic` tuple in `record.__dict__` into `{"auth_basic_user": user, "auth_basic_password": "***"}`. Defensive on malformed input (non-tuple, wrong arity).
    - `class RedactingFilter(logging.Filter)`:
      - `def filter(self, record: logging.LogRecord) -> bool`:
        - Standard string scan on `record.msg` and `record.args` (apply 7 regex substitutions)
        - SPECIAL D-22 handling: if `"auth_basic" in record.__dict__` AND value is a tuple of len 2: pop original key, set `auth_basic_user` + `auth_basic_password` keys via `_redact_auth_basic_tuple`
        - Standard `record.__dict__` scan for sentinel markers (Bearer, X-Auth-Token, X-Username, X-Password, Authorization Basic) with redaction
        - Always return True
    - `def attach() -> None`: attach to `getLogger("matriz_client")` with NullHandler + RedactingFilter idempotency
    - `__all__ = ["RedactingFilter", "attach"]`

    **`packages/matriz-client/tests/test_transport.py`** (~95 LOC):
    - Same baseline tests as Plans 2-4
    - PLUS matriz-specific (CRITICAL):
      - `test_new_order_POST_does_NOT_retry_on_503`: build a Request with extensions["idempotent"]=False (mimicking what shell does from build_new_order_request) → mock 503 → 1 wire request, NO retry (Pitfall 4 / D-01 / D-24)
      - `test_risk_api_auth_basic_in_extensions_does_NOT_retry_401`: with extensions["idempotent"]=True + extras["auth_basic"]=("u","p") → mock 5xx → retries; mock 401 → 401 returned (transport NEVER retries 401; shell handles re-auth decision based on auth_basic in Task 2)

    **`packages/matriz-client/tests/test_logging.py`** (~70 LOC):
    - Same baseline tests
    - PLUS matriz-specific (D-22):
      - `test_redact_authorization_basic_header`: msg `"Authorization: Basic YWRtaW46c2VjcmV0"` → after filter `"Authorization: Basic ***"` (D-22)
      - `test_redact_x_auth_token_header`: msg `"X-Auth-Token: abc.def-ghi_123"` → redacted (matriz token header)
      - `test_redact_x_password_header`: msg `"X-Username: admin\nX-Password: secret"` → X-Password redacted (X-Username preserved per D-22 user/password split philosophy)
      - `test_redact_auth_basic_tuple_in_extra`: LogRecord with extra={"auth_basic": ("u", "secret")} → after filter, record.__dict__ has auth_basic_user="u" AND auth_basic_password="***" AND original "auth_basic" key removed (D-22 splitting)
  </behavior>
  <action>
    1. Create `packages/matriz-client/src/matriz_client/_transport.py` by reading Plan 4 higyrus `_transport.py` and substituting `higyrus_client` → `matriz_client` for `_LOGGER_NAME` + module docstring. ALL other code identical.

    2. **DO NOT create `_atransport.py`** per D-25.

    3. Create `packages/matriz-client/src/matriz_client/_logging.py` with matriz-specific D-22 + X-Username/X-Password/X-Auth-Token + Authorization: Basic patterns. Implement `_redact_auth_basic_tuple` helper + special handling in `RedactingFilter.filter()` for `record.__dict__["auth_basic"]` tuple splitting per D-22.

    4. Create `packages/matriz-client/tests/test_transport.py` — duplicate Plan 4 shape with matriz imports + 2 matriz-specific tests (new_order no-retry + Risk API auth_basic flow).

    5. Create `packages/matriz-client/tests/test_logging.py` — duplicate Plan 4 shape; add 4 matriz-specific D-22 tests.

    6. Run `uv run pytest packages/matriz-client/tests/test_transport.py packages/matriz-client/tests/test_logging.py -v` — MUST GREEN.

    7. Verify `_atransport.py` was NOT created: `! test -f packages/matriz-client/src/matriz_client/_atransport.py`
  </action>
  <verify>
    <automated>test -f packages/matriz-client/src/matriz_client/_transport.py &amp;&amp; ! test -f packages/matriz-client/src/matriz_client/_atransport.py &amp;&amp; test -f packages/matriz-client/src/matriz_client/_logging.py &amp;&amp; test -f packages/matriz-client/tests/test_transport.py &amp;&amp; test -f packages/matriz-client/tests/test_logging.py &amp;&amp; grep -c "class RetryTransport" packages/matriz-client/src/matriz_client/_transport.py &gt;= 1 &amp;&amp; grep -c "class RedactingFilter" packages/matriz-client/src/matriz_client/_logging.py &gt;= 1 &amp;&amp; grep -c "matriz_client" packages/matriz-client/src/matriz_client/_transport.py &gt;= 1 &amp;&amp; grep -c "auth_basic\|_AUTH_BASIC_RE" packages/matriz-client/src/matriz_client/_logging.py &gt;= 2 &amp;&amp; grep -c "X-Auth-Token\|_X_AUTH_TOKEN_RE" packages/matriz-client/src/matriz_client/_logging.py &gt;= 1 &amp;&amp; grep -c "auth_basic_user\|auth_basic_password" packages/matriz-client/src/matriz_client/_logging.py &gt;= 2 &amp;&amp; uv run pytest packages/matriz-client/tests/test_transport.py packages/matriz-client/tests/test_logging.py -v 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_transport.py` exists with `RetryTransport` and `_LOGGER_NAME = "matriz_client"`
    - Source: `_atransport.py` does NOT exist (D-25 — Phase 10 territory)
    - Source: `_logging.py` exists with `RedactingFilter` AND `_AUTH_BASIC_RE` AND `_X_AUTH_TOKEN_RE` AND `_X_USERNAME_RE` AND `_X_PASSWORD_RE` (per D-22 + PATTERNS line 260)
    - Source: `_logging.py` does NOT contain `_REFRESH_TOKEN` (iol-specific) AND does NOT contain `cuit` (higyrus-specific) — verifies pattern isolation
    - Source: `_logging.py` attaches to `getLogger("matriz_client")` AND has `auth_basic_user` AND `auth_basic_password` literal markers (D-22 tuple splitting)
    - CLI: `uv run pytest packages/matriz-client/tests/test_transport.py -v` exits 0 with ≥8 tests including `test_new_order_POST_does_NOT_retry_on_503` PASS (Pitfall 4 critical)
    - CLI: `uv run pytest packages/matriz-client/tests/test_logging.py -v` exits 0 with ≥8 tests passing including D-22 auth_basic tuple splitting
    - CLI: `uv run ruff check packages/matriz-client/` exits 0
    - CLI: `uv run mypy --strict packages/matriz-client/` exits 0
  </acceptance_criteria>
  <done>
    matriz's `_transport.py` (sync only) + `_logging.py` (D-22 auth_basic policy) exist + pass unit tests; `_atransport.py` NOT created (D-25). Mutation gate test for new_order POST GREEN. D-22 auth_basic tuple splitting verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend matriz _core.py + client.py + __init__.py shell with Risk API branch + 401 re-auth-once (token path) + account_id; update snapshot (NO aio.py touch per D-25); update test_sync_async_isolation.py skip reason; atomic commit per D-21</name>
  <files>packages/matriz-client/src/matriz_client/_core.py, packages/matriz-client/src/matriz_client/client.py, packages/matriz-client/src/matriz_client/__init__.py, verification/snapshots/matriz_client-surface.txt, verification/test_sync_async_isolation.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-02 — 401 re-auth-once; D-03 — login idempotent=True; D-09 — account_id is conditional structured field; D-11 — `account_id: str | None = None` in RequestSpec; D-15 — 2 new kwargs; D-22 — auth_basic redaction; D-23 — Risk API no re-auth; D-24 — status=ERROR no retry; D-25 — aio.py NOT touched; D-29 — login through RetryTransport)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 3 lines 549-602 — 401 re-auth pattern; §"Architectural Responsibility Map" — capabilities by tier)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/src/<pkg>/_core.py`" lines 323-339 — matriz _core delta: RequestSpec adds `endpoint_name` + `account_id` + flip `idempotent=True` on GETs + login; POST mutating builders KEEP `idempotent=False` per D-01/D-07/D-24; §"`packages/<pkg>/src/<pkg>/client.py` shell `_request()`" lines 426-462 — matriz _request with Risk API branch + token path 401 re-auth; §"`packages/<pkg>/src/<pkg>/aio.py`" line 530 — matriz aio.py NOT touched in Phase 8)
    - packages/matriz-client/src/matriz_client/_core.py (Phase 7 baseline — RequestSpec line 128-142 has `auth_basic: tuple[str,str] | None = None` and `idempotent: bool = False` already; locate `build_login_request` at lines 246-262, all GET builders, mutating builders (`build_new_order_request`, `build_cancel_order_request`, `build_replace_order_request`), `parse_envelope_response` — Phase 7 CR-03 — MUST NOT BE TOUCHED)
    - packages/matriz-client/src/matriz_client/client.py (Phase 7 baseline — read FULL FILE; locate Client class, slots, __init__, _ensure_token, _ensure_http_client, _request lines 184-196, configure())
    - packages/matriz-client/src/matriz_client/aio.py (Phase 6 baseline — STUB; verify `wc -l aio.py == 103` BEFORE and AFTER Plan 5 — MUST be unchanged per D-25)
    - packages/matriz-client/src/matriz_client/exceptions.py (AuthenticationError class — used in 401 re-auth catch)
    - packages/higyrus-client/src/higyrus_client/client.py (Plan 4 baseline — _request with account_id; matriz extends with auth_basic branch)
    - verification/test_sync_async_isolation.py (Phase 7 D-10 — matriz async branch skip line ~175-176; verify exact skip reason for update)
    - packages/matriz-client/tests/conftest.py (existing autouse fixture)
  </read_first>
  <behavior>
    Extend matriz shell sync with Plan 4 pattern + Risk API branch + status=ERROR preservation.

    **`packages/matriz-client/src/matriz_client/_core.py`** (extend):
    - `RequestSpec`: add `endpoint_name: str = ""` (D-09) + `account_id: str | None = None` (D-11). `auth_basic: tuple[str,str] | None = None` (Phase 7) preserved.
    - `build_login_request`: set `idempotent=True` + `endpoint_name="login"` (D-03)
    - All GET builders (build_get_segments_request, build_get_all_instruments_request, build_get_currencies_request, etc.): set `idempotent=True` + `endpoint_name="<name>"`; if builder takes account/account_name parameter, set `account_id=account`
    - Mutating builders (`build_new_order_request`, `build_cancel_order_request`, `build_replace_order_request`): KEEP `idempotent=False` — CRITICAL: D-01/D-07/D-24/Pitfall 4 — Primary API uses HTTP GET for these but the explicit `idempotent=False` prevents the retry transport from looping (which would duplicate orders). Also set `endpoint_name="<name>"` + `account_id=account` propagation. Verify NO accidental flip to `idempotent=True`.
    - Risk API builders (with `auth_basic` set): set `idempotent=True` + `endpoint_name="<risk_endpoint>"` (Risk GETs are idempotent at the HTTP level; 5xx retry-eable per D-23 sentence "RetryTransport sí")
    - DO NOT touch `parse_envelope_response` — CR-03 (Phase 7 D-06) body-consume-then-raise pattern preserved

    **`packages/matriz-client/src/matriz_client/client.py`** (extend):
    - `Client.__init__` signature delta: add `max_retries: int = 2` + `http_client: httpx.Client | None = None` (D-15)
    - `Client.__slots__`: add `"_max_retries"`
    - `_ensure_http_client()`: wrap `httpx.Client(transport=_transport.RetryTransport(max_attempts=self._max_retries+1))`
    - `_request()` delta — combine PATTERNS line 426-462 + Plan 4 account_id propagation + D-23 Risk API branch + D-02 token re-auth-once:
      ```python
      def _request(self, spec: _core.RequestSpec) -> httpx.Response:
          http = self._ensure_http_client()
          url = f"{self._state.base_url}{spec.path}"
          request_id = uuid.uuid4().hex
          if spec.auth_basic is not None:
              # Risk API path — D-23: RetryTransport YES, 401 re-auth NO.
              req = http.build_request(spec.method, url, params=spec.params)
              req.extensions["idempotent"] = spec.idempotent
              req.extensions["request_id"] = request_id
              req.extensions["endpoint_name"] = spec.endpoint_name
              if spec.account_id is not None:
                  req.extensions["account_id"] = spec.account_id
              # D-22 auth_basic propagation for log redaction (filter scans extra)
              req.extensions["auth_basic"] = spec.auth_basic
              return http.send(req, auth=httpx.BasicAuth(*spec.auth_basic))
          # Token path
          self._ensure_token()
          if self._state.token is None:
              raise RuntimeError("matriz_client.client: _ensure_token() did not populate _token")
          headers = {"X-Auth-Token": self._state.token, **(spec.headers or {})}
          req = http.build_request(spec.method, url, params=spec.params, headers=headers)
          req.extensions["idempotent"] = spec.idempotent
          req.extensions["request_id"] = request_id
          req.extensions["endpoint_name"] = spec.endpoint_name
          if spec.account_id is not None:
              req.extensions["account_id"] = spec.account_id
          resp = http.send(req)
          try:
              _raise_for_response(resp)
          except AuthenticationError:
              # D-02 + D-23: skip re-auth for Risk API (auth_basic returned above); token path: re-auth-once
              self._state.token = None
              self._ensure_token()
              if self._state.token is None:
                  raise
              req.headers["X-Auth-Token"] = self._state.token
              resp = http.send(req)
              _raise_for_response(resp)
          return resp
      ```
      KEY INVARIANTS:
      - Risk API branch returns BEFORE token path — `spec.auth_basic` check is first
      - Token path matches Plan 3 iol / Plan 4 higyrus pattern
      - `_raise_for_response` calls `parse_envelope_response` internally for matriz which preserves Phase 7 D-06 CR-03 body-consume; `PrimaryAPIError` (raised from envelope status=ERROR) PROPAGATES from try block — NOT caught by `except AuthenticationError`, so D-24 preserved
    - `configure()`: add `max_retries` + `http_client` carry-forward params

    **`packages/matriz-client/src/matriz_client/aio.py`** (NOT MODIFIED per D-25):
    - DO NOT touch this file. Verify line count remains at Phase 6 stub: `wc -l packages/matriz-client/src/matriz_client/aio.py` MUST return 103
    - Phase 10 REFAC-04 extends this to full REST surface

    **`packages/matriz-client/src/matriz_client/__init__.py`** (extend):
    - Insert `_logging.attach()` block per Plan 2 pattern; `del _logging_attach`

    **`verification/snapshots/matriz_client-surface.txt`** (update per D-28 + D-25):
    - Update Client.__init__ signature with `max_retries: 'int' = 2` + `http_client: 'httpx.Client | None' = None`
    - Update configure() signature with same 2 kwargs
    - **DO NOT touch AsyncClient.__init__ signature** — per D-25, matriz AsyncClient keeps Phase 6 stub signature

    **`verification/test_sync_async_isolation.py`** (extend per D-25):
    - Locate the matriz async skip line (Phase 7 D-10 — `pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")`)
    - Verify the reason text matches the current text; if not, update to: `pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04")`
    - Verify the test still parametrizes matriz for sync branch AND skip-tests async branch (idempotent change — Plan 5 doesn't change matriz async behavior, just refresh reason text if drifted)

    **Atomic commit per D-21:**
    `feat(matriz): retries + structured logging — sync-only (aio.py defer Phase 10) + Risk API 401-no-reauth + status=ERROR no-retry (RELY-01..04, LOG-01..03)`

    **CR-03 + CR-05 preservation:** Plan 5 NEITHER touches `parse_envelope_response` body NOR `_envelope_probe` helper in `main_matriz.py`. Existing matriz tests (Phase 7 baseline including `test_matriz_sweep_snapshot.py` 18 cases + `test_parse_envelope_consumes_body_before_raise`) MUST stay GREEN.
  </behavior>
  <action>
    1. Edit `packages/matriz-client/src/matriz_client/_core.py`:
       - Add `endpoint_name: str = ""` and `account_id: str | None = None` to RequestSpec (preserve existing `auth_basic` and `idempotent`)
       - For `build_login_request`: `idempotent=True` + `endpoint_name="login"`
       - For each GET builder: `idempotent=True` + `endpoint_name="<name>"` + (if applicable) `account_id=account`
       - For mutating builders (new_order, cancel_order, replace_order): KEEP `idempotent=False` (CRITICAL — verify) + add `endpoint_name="<name>"` + `account_id=account` propagation
       - For Risk API builders: `idempotent=True` + `endpoint_name="<name>"` (the auth_basic field stays as already set)
       - DO NOT touch `parse_envelope_response`

    2. Edit `packages/matriz-client/src/matriz_client/client.py`:
       - Add `import uuid` + `from matriz_client import _transport`
       - Add 2 kwargs to `Client.__init__`; handle them in body; add `"_max_retries"` to slots
       - Modify `_ensure_http_client()` to wrap RetryTransport
       - REPLACE `_request()` body with the auth_basic-branch + token-path-with-401-re-auth variant per PATTERNS lines 426-462
       - Modify `configure()` to accept + pass through the 2 kwargs

    3. **DO NOT edit `aio.py`** per D-25 — verify line count: `wc -l packages/matriz-client/src/matriz_client/aio.py` MUST return 103 (Phase 6 stub)

    4. Edit `packages/matriz-client/src/matriz_client/__init__.py`:
       - Insert `_logging.attach()` block; `del _logging_attach`

    5. Update `verification/snapshots/matriz_client-surface.txt`:
       - Update Client.__init__ AND configure() lines with `max_retries: 'int' = 2` + `http_client: 'httpx.Client | None' = None`
       - **AsyncClient.__init__ line UNCHANGED** (per D-25)

    6. Verify (or refresh if drifted) `verification/test_sync_async_isolation.py` matriz async skip reason text. If the reason already matches Phase 7's `"matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore"` keep verbatim. If drifted, update; pragmatic: leave Phase 7 wording intact if functional (no behavioral change required).

    7. Run verification:
       ```
       wc -l packages/matriz-client/src/matriz_client/aio.py    # MUST be 103
       uv run pytest packages/matriz-client/ -v 2>&1 | tail -25
       uv run pytest verification/test_public_surface.py -k matriz -v 2>&1 | tail -5
       uv run pytest verification/test_retry_mutation_gate.py -k matriz -v 2>&1 | tail -10
       uv run pytest verification/test_retry_401_reauth.py -k matriz -v 2>&1 | tail -10
       uv run pytest verification/test_logging_no_token_leak.py -k matriz -v 2>&1 | tail -10
       uv run pytest verification/test_async_cancellation.py -k matriz -v 2>&1 | tail -5    # matriz skip per D-25
       uv run pytest verification/test_sync_async_isolation.py -v 2>&1 | tail -10            # cross-leak GREEN + matriz async skip
       uv run pytest verification/test_matriz_sweep_snapshot.py -v 2>&1 | tail -10           # CR-05 preservation (18 sweeps GREEN)
       uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v 2>&1 | tail -5   # CR-03 preservation
       uv run ruff check packages/matriz-client/ verification/
       uv run mypy --strict packages/matriz-client/
       uv run lint-imports
       ```

    8. Single atomic commit per D-21:
       ```
       git add packages/matriz-client/src/ packages/matriz-client/tests/ verification/snapshots/matriz_client-surface.txt verification/test_sync_async_isolation.py
       git commit -m "feat(matriz): retries + structured logging — sync-only (aio.py defer Phase 10) + Risk API 401-no-reauth + status=ERROR no-retry (RELY-01..04, LOG-01..03)

       Phase 8 Plan 5 — matriz. Sync-only delivery per D-25 (aio.py REST stub preservado para Phase 10 REFAC-04 + TokenStore). _transport.py sync (NO _atransport.py). _logging.py con D-22 auth_basic redaction policy (Authorization Basic + auth_basic tuple splitting → user/password). Shell _request() con BRANCH para spec.auth_basic (Risk API path: NO 401 re-auth per D-23) + token path con D-02 re-auth-once. POST mutating builders (new_order, cancel_order, replace_order) MANTIENEN idempotent=False (D-01/D-24/Pitfall 4 — previene duplicate orders). PrimaryAPIError NEVER en retry_on= (D-24). _core.parse_envelope_response intacto (Phase 7 CR-03). _envelope_probe sweep snapshot intacto (Phase 7 CR-05).

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
       ```
  </action>
  <verify>
    <automated>grep -c "endpoint_name" packages/matriz-client/src/matriz_client/_core.py &gt;= 1 &amp;&amp; grep -c "account_id" packages/matriz-client/src/matriz_client/_core.py &gt;= 2 &amp;&amp; grep -c "idempotent=True" packages/matriz-client/src/matriz_client/_core.py &gt;= 3 &amp;&amp; grep -c "idempotent=False" packages/matriz-client/src/matriz_client/_core.py &gt;= 1 &amp;&amp; grep -c "max_retries" packages/matriz-client/src/matriz_client/client.py &gt;= 2 &amp;&amp; grep -c "RetryTransport" packages/matriz-client/src/matriz_client/client.py &gt;= 1 &amp;&amp; grep -c "auth_basic" packages/matriz-client/src/matriz_client/client.py &gt;= 1 &amp;&amp; grep -c "AuthenticationError" packages/matriz-client/src/matriz_client/client.py &gt;= 2 &amp;&amp; grep -c "uuid.uuid4" packages/matriz-client/src/matriz_client/client.py &gt;= 1 &amp;&amp; grep -c "BasicAuth" packages/matriz-client/src/matriz_client/client.py &gt;= 1 &amp;&amp; grep -c "_logging_attach.attach\|_logging.attach" packages/matriz-client/src/matriz_client/__init__.py &gt;= 1 &amp;&amp; grep -c "max_retries" verification/snapshots/matriz_client-surface.txt &gt;= 2 &amp;&amp; wc -l packages/matriz-client/src/matriz_client/aio.py | awk '{print $1}' | grep -c "^103$" &gt;= 1 &amp;&amp; uv run pytest packages/matriz-client/ -v 2&gt;&amp;1 | tail -15 &amp;&amp; uv run pytest verification/test_public_surface.py -k matriz -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_retry_mutation_gate.py -k matriz -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_retry_401_reauth.py -k matriz -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_logging_no_token_leak.py -k matriz -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_matriz_sweep_snapshot.py -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run ruff check packages/matriz-client/ verification/ &amp;&amp; uv run mypy --strict packages/matriz-client/ &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_core.py` has `endpoint_name` + `account_id` fields; >= 3 instances of `idempotent=True` (login + GETs); >= 1 instance `idempotent=False` (mutating builders KEPT as False — CRITICAL Pitfall 4)
    - Source: `client.py` has `max_retries` (signature + body), `RetryTransport`, `auth_basic` branch, `AuthenticationError` (try + raise paths), `uuid.uuid4`, `BasicAuth`
    - Source: `__init__.py` has `_logging.attach()` + `del _logging_attach`
    - Source: snapshot has `max_retries: 'int' = 2` (Client + configure() = 2 places; AsyncClient UNCHANGED per D-25)
    - Source: `wc -l packages/matriz-client/src/matriz_client/aio.py` returns `103` (Phase 6 stub preserved per D-25)
    - Source: `_atransport.py` does NOT exist
    - CLI: `uv run pytest packages/matriz-client/ -v` exits 0 (existing tests + new test_transport + new test_logging)
    - CLI: `uv run pytest verification/test_public_surface.py -k matriz -v` exits 0 (snapshot zero diff)
    - CLI: `uv run pytest verification/test_retry_mutation_gate.py -k matriz -v` exits 0 (matriz new_order POST → 1 wire request — duplicate-order prevention)
    - CLI: `uv run pytest verification/test_retry_401_reauth.py -k matriz -v` exits 0 (Primary token path 401 chain GREEN; Risk auth_basic 401 → AuthError + 1 wire request)
    - CLI: `uv run pytest verification/test_logging_no_token_leak.py -k matriz -v` exits 0 (SECRET scrubbed in Bearer / X-Auth-Token / auth_basic password / X-Password)
    - CLI: `uv run pytest verification/test_sync_async_isolation.py -v` exits 0 (Phase 7 cross-leak GREEN; matriz async skip per D-25)
    - CLI: `uv run pytest verification/test_matriz_sweep_snapshot.py -v` exits 0 (Phase 7 CR-05 — 18 cases GREEN)
    - CLI: `uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v` PASS (Phase 7 CR-03 preserved)
    - CLI: `uv run ruff check` + `uv run mypy --strict` + `uv run lint-imports` all exit 0
    - CLI: `git log --oneline -1 | grep -c "feat(matriz)"` >= 1 (atomic commit per D-21)
  </acceptance_criteria>
  <done>
    matriz Plan 5 atomic commit landed: RetryTransport sync only (NO _atransport.py per D-25); _logging.py with D-22 auth_basic policy; shell `_request()` with Risk API branch (no re-auth) + token path 401 re-auth-once; account_id propagation; 2 new kwargs in Client/configure() (AsyncClient unchanged per D-25); snapshot updated; aio.py UNCHANGED (103 LOC stub); CR-03 + CR-05 preserved. Cross-cutting guard tests GREEN for matriz parametrize branches including critical new_order mutation gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `matriz_client.Client._request` auth_basic branch → `httpx.BasicAuth(*spec.auth_basic)` | Risk API path: BasicAuth header generated AT SEND TIME from spec.auth_basic tuple; tuple itself stays in `request.extensions["auth_basic"]` for log filter to split into user/password components (D-22) |
| `matriz_client._core.build_new_order_request` → `RequestSpec(method="GET", idempotent=False)` | Primary API quirk: HTTP method GET but explicit `idempotent=False` — mutation gate via `request.extensions["idempotent"]` prevents duplicate orders (Pitfall 4 / D-01 / D-24) |
| `matriz_client._core.parse_envelope_response` → `PrimaryAPIError` raised on `status=='ERROR'` | Application-level error path: `PrimaryAPIError` raised AFTER `handle_request` returns 200 OK; transport NEVER sees ERROR status; D-24 LOCKED — NEVER in `retry_on=` |
| `matriz_client._logging.RedactingFilter` ← `record.__dict__["auth_basic"]` tuple | D-22 special handling: tuple splitting into `auth_basic_user` (operational) + `auth_basic_password` (redacted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01-MATZ | Tampering | Mutation gate — new_order POST against 503 retry → duplicate matriz orders (CRITICAL — broker-level risk) | mitigate | `_core.py` mutating builders (`build_new_order_request`, `build_cancel_order_request`, `build_replace_order_request`) KEEP `idempotent=False`; mutation gate via `request.extensions["idempotent"]` (D-01); `test_retry_mutation_gate.py[matriz_new_order]` (turning GREEN in Plan 5) verifies exactly 1 wire request; Pitfall 2/4 prevention |
| T-8-02-MATZ-PRIMARY | Denial-of-Service | 401 retry storm — Primary API expired X-Auth-Token loop | mitigate | D-07 LOCKED: `retry_on=` excludes AuthenticationError; shell `_request()` token path: D-02 exactly-once re-auth; `test_retry_401_reauth.py[matriz Primary]` covers |
| T-8-02-MATZ-RISK | Denial-of-Service | Risk API auth_basic 401 (invalid credentials) → re-auth attempt → infinite recursion | mitigate | D-23 LOCKED: shell `_request()` branches on `spec.auth_basic is not None` → returns Risk path WITHOUT 401 re-auth flow; AuthError immediate; `test_retry_401_reauth.py[matriz Risk]` covers (1 wire request + AuthError) |
| T-8-03-MATZ | Tampering | Library hijacks root logger | mitigate | `_logging.attach()` to `getLogger("matriz_client")`; LOG015 + CI grep; `test_logging_root_unchanged.py` |
| T-8-04-MATZ-TOKEN | Information Disclosure | X-Auth-Token leak in DEBUG | mitigate | `_logging.py` `_X_AUTH_TOKEN_RE` regex scrubs; `test_logging_no_token_leak.py[matriz]` covers |
| T-8-04-MATZ-BASIC | Information Disclosure | auth_basic password leak via `record.__dict__["auth_basic"]` tuple OR Authorization Basic header | mitigate | D-22 LOCKED: `_logging.py` (a) `_AUTH_BASIC_RE` regex scrubs Authorization Basic header; (b) `_redact_auth_basic_tuple` splits tuple in record.__dict__ to `auth_basic_user` + `auth_basic_password="***"`; `test_redact_auth_basic_tuple_in_extra` (Task 1) verifies |
| T-8-04-MATZ-LOGIN | Information Disclosure | matriz login headers X-Username/X-Password leak | mitigate | `_logging.py` `_X_USERNAME_RE` (preserves user as operational) + `_X_PASSWORD_RE` (redacts) regex; tests verify |
| T-8-05-MATZ | Availability | async cancellation during retry | accept | matriz `aio.py` is STUB per D-25 (Phase 10 territory); `test_async_cancellation.py[matriz]` SKIPS per D-25; no async retry to cancel in Phase 8 |
| T-8-MATZ-D24-PRESERVE | Tampering (regression) | `PrimaryAPIError` accidentally added to `retry_on=` → status=ERROR responses retried → application-level fail loop | mitigate | D-24 LOCKED: `_transport.py` `_RETRYABLE_STATUS = frozenset({408, 409, 429, *range(500, 600)})` — 200 OK NEVER triggers retry; `parse_envelope_response` raises `PrimaryAPIError` AFTER transport returns. Pitfall 15 prevention. `test_matriz_sweep_snapshot.py` (Phase 7 CR-05) re-verified post-Plan-5 = no regression on application-error handling |
| T-8-CR03-PRESERVE | Tampering (regression) | Phase 7 CR-03 `parse_envelope_response` body-consume-then-raise pattern lost during Plan 5 _core.py edits | mitigate | Plan 5 ADDS fields to RequestSpec (endpoint_name + account_id) but does NOT touch `parse_envelope_response` body. `packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise` re-verified GREEN post-Plan-5 |
| T-8-CR05-PRESERVE | Tampering (regression) | Phase 7 CR-05 `_envelope_probe` (18 sweep probes) lost during Plan 5 | mitigate | Plan 5 does NOT touch `main_matriz.py`. `verification/test_matriz_sweep_snapshot.py` (18 cases) re-verified GREEN post-Plan-5 |
| T-8-D25-AIO-DRIFT | Tampering (Phase scope creep) | matriz `aio.py` accidentally modified in Plan 5 (should remain Phase 6 stub at 103 LOC until Phase 10) | mitigate | Plan 5 task list explicitly excludes `aio.py`; verification step `wc -l packages/matriz-client/src/matriz_client/aio.py == 103` captures pre-/post-Plan-5 line count; Plan 6 green gate re-verifies; SUMMARY.md documents "forward ref Phase 10" |
</threat_model>

<verification>
- matriz `_transport.py` (sync only) created mirror Plan 4 with `_LOGGER_NAME = "matriz_client"`
- matriz `_atransport.py` NOT created (D-25)
- matriz `_logging.py` created with D-22 auth_basic redaction policy (Authorization Basic + tuple splitting) + X-Auth-Token + X-Username/X-Password
- matriz `_core.py` RequestSpec extended with `endpoint_name` + `account_id`; builders flipped (login + GETs idempotent=True; mutating builders KEPT idempotent=False — CRITICAL)
- matriz shell `_request()` (sync) has Risk API branch (`spec.auth_basic is not None` → no 401 re-auth per D-23) + token path 401 re-auth-once (D-02)
- matriz `aio.py` UNCHANGED (Phase 6 stub at 103 LOC preserved per D-25)
- matriz Client.__init__ + configure() extended with 2 new kwargs; AsyncClient signature UNCHANGED per D-25
- matriz `__init__.py` invokes `_logging.attach()`
- matriz snapshot updated atomically (Client + configure only; AsyncClient line unchanged)
- matriz existing tests preserved including matriz sweep snapshot (Phase 7 CR-05 — 18 cases) and `parse_envelope_response` body-consume (Phase 7 CR-03)
- `test_sync_async_isolation.py` matriz async skip reason coherent
- Cross-cutting guard tests turn GREEN for matriz parametrize branches including CRITICAL `test_retry_mutation_gate.py[matriz_new_order]` (duplicate-order prevention)
- ruff + mypy strict + lint-imports GREEN
- Single atomic commit per D-21
</verification>

<success_criteria>
- matriz Phase 8 delivery completes Wave 5 (last per-package wave) — sync only with all matriz specials handled
- CRITICAL: `test_retry_mutation_gate.py[matriz_new_order]` GREEN (1 wire request against mock 503 — duplicate-order risk mitigated per Pitfall 4 / D-01 / D-24)
- `test_retry_401_reauth.py[matriz Primary]` GREEN: token path 401 → re-login → 200 = 3 wire requests with FRESH-TOKEN
- `test_retry_401_reauth.py[matriz Risk]` GREEN: auth_basic path 401 → AuthError + 1 wire request (NO re-auth per D-23)
- `test_logging_no_token_leak.py[matriz]` GREEN: Bearer + X-Auth-Token + auth_basic password + X-Password all redacted
- Phase 7 CR-03 + CR-05 + matriz sweep snapshot (18 cases) all preserved
- matriz aio.py UNCHANGED (103 LOC) — D-25 honored; SUMMARY.md documents forward ref Phase 10
- Single commit per D-21
</success_criteria>

<output>
Create `.planning/phases/08-retries-backoff-structured-logging/08-05-SUMMARY.md` documenting:
- LOC delta per Phase 8 specifics format: `_transport.py: 0 → ~95 (NEW; sync ONLY)`, `_atransport.py: N/A (Phase 10 territory per D-25)`, `_logging.py: 0 → ~85 (NEW; D-22 auth_basic + X-Auth-Token + X-Username/X-Password)`, `_core.py: +endpoint_name + account_id fields + builders flipped (login+GETs idempotent=True; mutating builders KEPT idempotent=False)`, `client.py: +~50 LOC (Risk API branch + token path 401 re-auth + account_id + 2 kwargs)`, `aio.py: 103 → 103 (UNCHANGED per D-25)`, `__init__.py: +3 lines (_logging.attach())`
- Snapshot diff: 2 new kwargs in Client + configure() = 2 places (AsyncClient UNCHANGED — explicit D-25 acknowledgment in SUMMARY.md)
- Cross-cutting guard tests status: test_retry_mutation_gate.py[matriz_new_order] GREEN (Pitfall 4 critical); test_retry_401_reauth.py[matriz Primary] GREEN; test_retry_401_reauth.py[matriz Risk] GREEN (D-23 no-reauth); test_logging_no_token_leak.py[matriz] GREEN
- CR-03 + CR-05 + matriz sweep snapshot preservation evidence
- D-25 forward-ref Phase 10 statement: "matriz _atransport.py + aio.py REST surface deferred to Phase 10 REFAC-04 + TokenStore — Plan 5 only delivers sync retry/log infra"
- Atomic commit hash
- "Phase 8 Plan 5 closes Wave 5 (last per-package). Wave 6 (Plan 6) runs full CI green gate consolidation."
</output>
