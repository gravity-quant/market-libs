---
phase: 33-verificaci-n-en-vivo-en-modo-estricto-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - verification/divergences.py
  - verification/__init__.py
  - verification/test_divergences.py
  - main_higyrus.py
  - .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md
autonomous: false
requirements: [LIVE-TYP-01]

must_haves:
  truths:
    - "An operator can bind an endpoint + surface around a probe call and every decode divergence the walker emits during that call lands as a SHAPE finding carrying that endpoint, the model FQN, the field path and the surface (criterion 1, D-01/D-02)."
    - "A divergence emitted at INFO level (the `extra` kind, 32 of the ratified 96-record floor) reaches the handler instead of being dropped by the package logger's inherited WARNING level (P-1)."
    - "A failure inside the handler is a visible number the driver can print, never a silent drop swallowed by `_decode._emit`'s `contextlib.suppress` (P-2)."
    - "`main_higyrus.py` emits fids above every fid already committed in `.planning/verification/higyrus-client-findings.md`, so no finding of a run is a silent no-op (P-3, D-16/D-24)."
    - "The pre-existing red state of `verification/` is a committed artifact, so a Phase 33 regression is distinguishable from the 19 failures / 19 errors already there (P-13)."
  artifacts:
    - path: "verification/divergences.py"
      provides: "DivergenceHandler + endpoint/surface ContextVars + divergence_capture install CM + probe_context decorator factory + endpoint_scope re-binding helper"
      exports: ["DivergenceHandler", "divergence_capture", "endpoint_scope", "probe_context", "PACKAGE_LOGGERS"]
      min_lines: 120
    - path: "verification/test_divergences.py"
      provides: "the five unit tests named in 33-VALIDATION.md Wave 0"
      contains: "def test_emit_never_raises"
    - path: ".planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md"
      provides: "committed red baseline of verification/ (P-13)"
    - path: "verification/__init__.py"
      provides: "barrel re-export of the four public divergence names"
      contains: "from verification.divergences import"
  key_links:
    - from: "packages/higyrus-client/src/higyrus_client/_decode.py"
      to: "verification/divergences.py"
      via: "logging.getLogger('higyrus_client') -> DivergenceHandler.emit (6-key extra record)"
      pattern: "logging\\.getLogger"
    - from: "verification/divergences.py"
      to: "verification/findings.py"
      via: "append_finding(slug, class_='SHAPE', status='OPEN', idempotent_by_title=True)"
      pattern: "append_finding\\("
    - from: "main_higyrus.py"
      to: "verification/divergences.py"
      via: "probe_context decorator applied to probe_get_health_sync / probe_get_health_async, and _next_fid injected into divergence_capture"
      pattern: "probe_context\\("
  prohibitions:
    - id: P-01
      category: privacy
      statement: "MUST NOT widen the frozen six-key type-not-value divergence record, and MUST NOT compose any finding field (title / expected / actual / diff) from anything other than those six keys plus the endpoint and surface the harness itself set — a git-committed findings file must never carry a live wire value."
      status: resolved
      verification: judgment
    - id: P-02
      category: transparency
      statement: "MUST NOT report a census number, a phase criterion, or a cycle-closure PASS produced by a signal that inspected nothing — a green arising from a dropped log level, a swallowed handler exception, an unseeded fid allocator, or a zero-applicable closure filter is a false clean, which is the exact failure this milestone exists to remove."
      status: resolved
      verification: judgment
    - id: P-03
      category: transparency
      statement: "MUST NOT shrink the census to fit the ratified floor — no narrowing the walker, no dropping a divergence kind, and no deferring a confirmed finding to an unnamed later; every deferred finding carries a named destination phase with its package and field recorded."
      status: resolved
      verification: judgment
    - id: P-04
      category: transparency
      statement: "MUST NOT widen, close, or enforce matriz's four pre-existing RESPONSE Literal aliases, and MUST NOT promote iol's mercado/plazo to a Literal on RESPONSE-side evidence — the signed D-lock (D-09) and D-10 forbid both, and a Literal closed on insufficient evidence breaks legitimate caller input."
      status: resolved
      verification: judgment
    - id: P-05
      category: safety
      statement: "MUST NOT let the two-pass runner fire a destructive or order-entry operation against a live vendor — the strict pass runs with the market-data mutation gate off and no probe places a real order."
      status: resolved
      verification: judgment
  flagged_assumptions:
    - "The deterministic edge-probe returned LIVE-TYP-01 as `unclassified` / `unresolved` (coverage: applicable 1, resolved 0). Its acceptance surface is compound — five ROADMAP success criteria — not a single classifiable edge, so the mechanical classifier abstained rather than mis-binning it. Per the no-auto-resolve rule this is carried as an explicit flagged assumption: the five ROADMAP Phase 33 success criteria (mirrored verbatim in 33-CONTEXT.md `<domain>`) are the real acceptance surface, and every `must_haves.truths` entry across plans 33-01..33-06 is derived from them directly rather than from the abstained probe row."
---

<objective>
TRACER: build the one bridge Phase 33 is missing — `verification/divergences.py`, the
`logging.Handler` that turns the frozen six-key decode record into a `SHAPE` finding — and prove it
end to end through a single real probe pair in `main_higyrus.py`.

Purpose: criterion 1 of the phase (D-01/D-02) has no mechanism today. Nothing else in Phase 33 can
run until a divergence emitted by `_decode._emit` can reach `append_finding` carrying its endpoint,
model FQN, field path and surface. Three verified silent-loss channels (P-1 logger level, P-2
swallowed handler exceptions, P-3 unseeded fid allocator) each turn a live run into a false clean, so
the handler is built hardened against all three from the first commit rather than retrofitted.

Output: `verification/divergences.py` + its five unit tests, the barrel re-export, the higyrus
`get_health` probe pair wired end to end (with the D-07 hand-rolled catch deleted), and the committed
red baseline of `verification/`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CONTEXT.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-RESEARCH.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-PATTERNS.md
@.planning/phases/29-decoder-observable/29-AGGREGATION-CONTRACT.md
@verification/findings.py
@verification/capture.py
@verification/test_logging_root_unchanged.py
</context>

## Artifacts this phase produces

Created or modified by **this plan**:

| Symbol / file | Kind | Notes |
|---|---|---|
| `verification/divergences.py` | new module | stdlib-only, mypy-strict by hand (P-9) |
| `DivergenceHandler` | class (`logging.Handler`) | `.emit()`, `.seen: set[tuple[str,str,str,str]]`, `.errors: list[str]` |
| `divergence_capture(logger_names, *, next_fid)` | context manager | the install CM: attaches the handler AND raises each package logger to `INFO`; restores both on exit |
| `probe_context(endpoint, surface)` | decorator factory | sync + async wrappers; binds the two ContextVars, intercepts `<Pkg>DecodeError` |
| `endpoint_scope(endpoint)` | context manager | P-5 re-binding for multi-endpoint probes |
| `_ENDPOINT`, `_SURFACE` | `ContextVar[str]` | module-private; the D-02 transport, never a `_decode.py` registry key |
| `PACKAGE_LOGGERS`, `_SLUG_BY_LOGGER` | constants | the five typed-package logger names and their findings-file slugs |
| `verification/test_divergences.py` | new test file | 5 tests (see `<behavior>` blocks below) |
| `verification/__init__.py` | modified barrel | docstring bullet + import + `__all__` |
| `main_higyrus.py::_seed_fid_counter` | new function | P-3 fix, ported verbatim from `main_iol.py:190-220` |
| `main_higyrus.py::_STRICT` | new module constant | two-pass flag |
| `33-BASELINE.md` | new artifact | committed red baseline of `verification/` |

Produced by **later plans in this phase** (listed here so the executor does not duplicate them):
`main_matriz.py` / `main_ambito_financiero.py` `_seed_fid_counter` + `_STRICT` (33-02, 33-03),
`main_market_data.py::_ENDPOINT_TEMPLATES` (33-03, D-03), `main_iol.py::_STRICT` (33-03),
`verification/test_probe_context_coverage.py` and `verification/test_finding_count_consistency.py`
and `scripts/preflight_33.py` and `33-CENSUS.md` (33-04), `scripts/literal_census_33.py` and
`33-LITERALS.md` (33-05), `verification/test_cycle_closure_phase33.py` and one
`packages/<pkg>/tests/test_<fix>.py` per confirmed fix (33-06).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Lock the finding-title convention and the matriz F-03..F-08 disposition</name>
  <reversibility rating="one-way">The title is the cross-run `idempotent_by_title` dedupe key; once 96+ findings are committed under one convention, switching it means rewriting every committed findings file in five packages and orphaning every operator triage note attached to the old titles.</reversibility>
  <decision>
    Two coupled choices that must be made before the first live write, because they are permanent
    once findings land (RESEARCH Assumptions A5 + A6, Open Question 1).

    (a) Does the auto-generated SHAPE title embed the surface — i.e. is the dedupe identity
    `(model, field_path, kind, declared, observed, surface)` or `(model, field_path, kind, declared,
    observed)`?

    (b) Do matriz's six existing hand-written `NO-FIX` findings `F-03`..`F-08` (the S-4 `extra` keys
    on `InstrumentDetail`) get absorbed by matching their titles, or does the handler write six new
    `OPEN` SHAPE findings alongside them?
  </decision>
  <context>
    On (a): including `[{surface}]` doubles the finding count relative to the distinct-triple count
    (one sync finding + one async finding per triple), which inflates every number in `33-CENSUS.md`
    against the ≥96 floor and makes criterion 5's contrast harder to read. Excluding it means the
    surface lives only in the `surface` column of the finding, not in its identity — criterion 1
    still requires surface to be recorded, and the column satisfies that. Either way the census is
    counted from `DivergenceHandler.seen` (distinct `(slug, model, field_path, kind)`), never from
    the finding count — that is the only unit directly comparable to `29-SIZING.md`.

    On (b): matriz's existing titles read like `.instrument_detail.securityIdSource: wire emite,
    model ignora (info)`. No deterministic generated title can match them without hand-crafted
    title gymnastics, and those six carry operator prose `append_finding` preserves. Absorbing them
    means writing a bespoke title-matching table; writing new ones means six duplicate-by-different-
    title records to triage to `NO-FIX` referencing the originals.
  </context>
  <options>
    <option id="surface-in-title-write-new">
      <name>(a) surface IN the title + (b) write six new findings, triage to NO-FIX referencing the originals</name>
      <pros>Mechanical and auditable — no title-matching table, no hand-crafted exceptions. A sync-only or async-only divergence is visible as its own finding, which is what criterion 1 asks for. This is the RESEARCH-recommended default (Open Question 1).</pros>
      <cons>Finding count is roughly 2x the distinct-triple count; `33-CENSUS.md` must report and label both numbers so the floor contrast is not misread. Six extra matriz findings to triage.</cons>
    </option>
    <option id="surface-out-of-title-write-new">
      <name>(a) surface NOT in the title + (b) write six new findings, triage to NO-FIX referencing the originals</name>
      <pros>Finding count tracks the distinct-triple count roughly 1:1, so the ≥96 contrast reads directly. Surface still recorded in the finding's `surface` column.</pros>
      <cons>A divergence that occurs on only one surface is indistinguishable in the title from one that occurs on both; the first surface to emit wins the `idempotent_by_title` slot and the second is deduped away, so sync/async asymmetry becomes invisible in the findings file.</cons>
    </option>
    <option id="surface-in-title-absorb">
      <name>(a) surface IN the title + (b) hand-craft titles that absorb matriz F-03..F-08</name>
      <pros>No duplicate matriz records; the six existing NO-FIX dispositions and their operator prose keep governing those wire keys.</pros>
      <cons>Introduces a bespoke per-finding title table inside the handler — the exact hand-rolled pattern D-07 deletes elsewhere in this phase — and any drift between the table and the real titles silently reverts to writing duplicates anyway.</cons>
    </option>
  </options>
  <resume-signal>Select: surface-in-title-write-new, surface-out-of-title-write-new, or surface-in-title-absorb. Record the selection verbatim in the plan SUMMARY; Task 2's title format and 33-05's census table both read from it.</resume-signal>
  <files>.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-01-SUMMARY.md</files>
  <read_first>
    - `33-RESEARCH.md` `## Assumptions Log` rows A5 and A6, and `## Open Questions` item 1
    - `verification/findings.py:583-706` — `append_finding`'s `idempotent_by_title` path and the non-`OPEN` preservation short-circuit that makes an existing title effectively permanent
    - `.planning/verification/matriz-client-findings.md` — the six `NO-FIX` records `F-03`..`F-08` and their exact hand-written titles
    - `.planning/phases/29-decoder-observable/29-AGGREGATION-CONTRACT.md` Lock 10 — the signed handler spec whose title field this decision fills in
  </read_first>
  <action>
    Present the decision above to the operator with all three options and wait for a verbatim
    selection. Do not pick a default and proceed; the title is the permanent cross-run dedupe key for
    96+ findings across five packages and a wrong choice is unwound only by rewriting every committed
    findings file. Once the selection arrives, record it verbatim in
    `33-01-SUMMARY.md` under a `## Title convention (locked)` heading, together with the exact f-string
    the handler will use and the matriz `F-03`..`F-08` disposition. Write no code in this task — Task 2
    reads the recorded format, and plans 33-02 through 33-07 read it from the SUMMARY.
  </action>
  <acceptance_criteria>
    - `33-01-SUMMARY.md` contains a `## Title convention (locked)` section naming one of the three option ids verbatim.
    - That section states the exact title f-string, whether the surface is embedded, and the matriz `F-03`..`F-08` disposition.
    - No source file was modified by this task: `git status --porcelain verification/ main_higyrus.py` is empty at the end of it.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Title convention (locked)' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-01-SUMMARY.md &amp;&amp; grep -qE 'surface-in-title-write-new|surface-out-of-title-write-new|surface-in-title-absorb' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-01-SUMMARY.md</automated>
  </verify>
  <done>The operator has selected one of the three options verbatim, and the selection plus the exact title f-string and the matriz `F-03`..`F-08` disposition are recorded in `33-01-SUMMARY.md`.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — divergence handler wired end to end through higyrus get_health</name>
  <reversibility rating="costly">The handler's public names (`divergence_capture`, `probe_context`, `endpoint_scope`) become the call surface all five drivers depend on; renaming them later is a coordinated edit across ~130 probe sites.</reversibility>
  <files>verification/divergences.py, verification/test_divergences.py, verification/__init__.py, main_higyrus.py</files>
  <read_first>
    - `verification/findings.py` — `append_finding` signature (`:583-598`), its `class_`/`status`/`title` `ValueError` validation (`:631-634`), `_PKG_SLUG_RE`, `findings_path`, `_FINDINGS_DIR` (`:55-56`), `max_existing_fid`
    - `verification/capture.py` (lines 1-34) — the module-header pattern every new `.py` in this phase copies: Spanish docstring with purpose + `Uso::` block, `from __future__ import annotations`, explicit sorted `__all__`, repo root via `Path(__file__).resolve().parent.parent`
    - `packages/higyrus-client/src/higyrus_client/_decode.py` — `_LOGGER_NAME` (`:72`), the six-key `extra` dict (`:147`), `_emit`'s `contextlib.suppress(Exception)`, `STRICT_DECODE` / `DECODE_SCOPE` ContextVars (`:248-251`), and the `Literal` branch (`:521-534`)
    - `packages/higyrus-client/src/higyrus_client/exceptions.py:64` — `HigyrusDecodeError` and its four certified attributes
    - `main_higyrus.py` — `_PKG` (`:129`), `_RESIDUAL_PROBE_EXCEPTIONS` (`:133-147`), `_ENDPOINT_TEMPLATES` (`:156-163`), `ProbeResult` (`:295-301`), `_next_fid` (`:237-241`), `probe_get_health_sync` and its `HigyrusDecodeError` block (`:648-712`), `probe_get_health_async`, the two ctor sites (`:2386` `AsyncClient()`, `:2507` `Client()`)
    - `main_iol.py:182-220` — `_seed_fid_counter` / `_next_fid`, to be ported verbatim
    - `main_market_data.py:3319-3324` — the canonical `require_env` -> `write_findings` -> `_seed_fid_counter` -> first probe ordering
    - `verification/__init__.py` — the barrel's three edit points (docstring bullet, import block, `__all__`)
    - `verification/test_logging_root_unchanged.py` — the snapshot/restore assertion pattern and the `caplog`-installs-root-handlers note
    - `33-RESEARCH.md` Patterns 1-3 and Pitfalls P-1/P-2/P-3/P-5
  </read_first>
  <behavior>
    RED first — write these two tests in `verification/test_divergences.py` before any of
    `verification/divergences.py` exists, and confirm they fail on `ModuleNotFoundError`:

    - `test_handler_maps_record_to_shape_finding`: with `verification.findings._FINDINGS_DIR`
      monkeypatched to `tmp_path`, installing the handler on logger `higyrus_client` and emitting one
      six-key record (`package="higyrus_client"`, `divergence="missing"`, `model="Movimiento"`,
      `field_path=".fecha"`, `declared_type="str"`, `observed_type="NoneType"`) inside a
      `probe_context(endpoint="/api/cuentas/{id_cuenta}/movimientos", surface="sync")`-decorated
      callable writes exactly one `### F-` block to `tmp_path/higyrus-client-findings.md` whose
      `Clase` is `SHAPE`, whose `surface` is `sync`, whose `diff` names the endpoint, and whose title
      carries `Movimiento`, `.fecha` and `missing`. `handler.seen` holds exactly the one tuple
      `("higyrus-client", "Movimiento", ".fecha", "missing")`.
    - `test_probe_context_binding`: inside the decorated callable the handler observes the bound
      endpoint and surface; after the callable returns, a record emitted outside any decorator
      observes the ContextVar defaults, proving `reset` ran. An `async def` callable decorated with
      the same factory behaves identically under `asyncio.run`.

    GREEN: implement `verification/divergences.py` until both pass. Do not write the other three
    tests here — Task 3 owns the hardening suite.
  </behavior>
  <action>
    Create `verification/divergences.py` (stdlib-only: `contextlib`, `contextvars`, `functools`,
    `inspect`, `logging`, `collections.abc`; plus `verification.findings.append_finding`). Follow the
    `verification/capture.py` module-header pattern exactly — Spanish docstring naming LIVE-TYP-01 and
    D-01/D-02 in the first line, a `Uso::` block, `from __future__ import annotations`, and an explicit
    sorted `__all__` of `["DivergenceHandler", "PACKAGE_LOGGERS", "divergence_capture",
    "endpoint_scope", "probe_context"]`.

    Define module-private `_ENDPOINT: contextvars.ContextVar[str]` and `_SURFACE:
    contextvars.ContextVar[str]`, both with default `"-"`. Define `PACKAGE_LOGGERS` as the tuple
    `("ambito_financiero_client", "higyrus_client", "iol_client", "market_data_client",
    "matriz_client")` — these are the five `_LOGGER_NAME` literals verified in each `_decode.py`.
    Define `_SLUG_BY_LOGGER` mapping each of those to its findings-file slug
    (`ambito-financiero-client`, `higyrus-client`, `iol-client`, `market-data-client`,
    `matriz-client`); those slugs are what `_PKG_SLUG_RE` accepts, and a mismatch raises `ValueError`
    inside `append_finding` which P-2 would swallow.

    `DivergenceHandler(logging.Handler)`: `__init__(self, next_fid: Callable[[str], str])` calls
    `super().__init__(level=logging.INFO)` so the INFO-level `extra` kind is admitted, stores
    `next_fid`, and initialises `self.seen: set[tuple[str, str, str, str]] = set()` and `self.errors:
    list[str] = []`. `emit(self, record)` wraps its ENTIRE body in `try/except Exception` and on
    failure appends `f"{type(exc).__name__}: {exc}"` to `self.errors` and returns — it must never
    propagate, because `_decode._emit` runs the whole emission inside `contextlib.suppress(Exception)`
    and a raise there is lost with no trace (P-2). Do not rely on `self.handleError` alone; the
    `errors` list is what the driver prints as a number. Inside the guard: resolve the slug from
    `getattr(record, "package", "")` via `_SLUG_BY_LOGGER` and the kind from `getattr(record,
    "divergence", None)`, returning early when either is absent (a foreign record on the same logger
    is not ours); read `record.model`, `record.field_path`, `record.declared_type`,
    `record.observed_type` (add `# type: ignore[attr-defined]` on each, documented, per P-9); read
    `_SURFACE.get()` and `_ENDPOINT.get()`; add `(slug, model, field_path, kind)` to `self.seen`
    BEFORE calling the sink so the census unit matches `29-SIZING.md`'s dedupe unit even if the write
    fails; then call `append_finding(slug, fid=self._next_fid(slug), class_="SHAPE", surface=surface,
    status="OPEN", title=<the format selected in Task 1>, expected=f"model declares {declared}",
    actual=f"wire sent {observed}", diff=f"{declared} -> {observed} at {model}{path} via {endpoint}",
    idempotent_by_title=True)`. Compose `title`, `expected`, `actual` and `diff` ONLY from the six
    record keys plus the endpoint and surface this module itself set (prohibition P-01, Lock 11) —
    never from a wire value, and never re-derive a field path (`_decode._safe_key` already sanitised
    it upstream).

    `divergence_capture(logger_names, *, next_fid)` as a `@contextlib.contextmanager`: build one
    `DivergenceHandler`, and for each name call `logging.getLogger(name)`, record `(logger,
    logger.level)`, then `setLevel(logging.INFO)` and `addHandler(handler)`; `yield handler`; in
    `finally` remove the handler and restore the recorded level on every logger. `INFO` and not
    `DEBUG` — the five packages log nothing at INFO, while DEBUG additionally admits
    `_transport.py`'s redaction-sensitive request records. Never touch `logging.root`: that is guarded
    three ways (ruff `LOG` rules, the CI `lint-logging` grep, and
    `verification/test_logging_root_unchanged.py`).

    `probe_context(endpoint: str, surface: str)` as a decorator factory returning a decorator that
    branches on `inspect.iscoroutinefunction(fn)` and emits either a `functools.wraps`-preserving sync
    wrapper or an `async` twin with the same body. Both set both ContextVars, call the wrapped
    function in a `try`, and `reset` both tokens in `finally`. Do NOT put a decode-error `except` in
    this generic wrapper — the per-package exception class differs and the driver owns the mapping;
    instead accept an optional keyword-only `decode_error: type[BaseException] | None = None` and, when
    supplied, catch it and return `(None, exc)`-free control to a caller-supplied
    `on_decode_error: Callable[[str, str, BaseException], object] | None = None` whose return value the
    wrapper returns unchanged. The driver passes its own `<Pkg>DecodeError` and its own
    `ProbeResult`-shaped fallback, so the harness never imports a package.

    `endpoint_scope(endpoint)` as a `@contextlib.contextmanager` that sets and resets `_ENDPOINT`
    only — for the multi-endpoint probes P-5 names (e.g. `main_market_data.py::probe_health_sync`
    calls both `get_health()` and `get_health_feed()`).

    Then edit `verification/__init__.py` in the three places the barrel's existing style requires: a
    docstring bullet in the same voice as the others describing `verification.divergences`; an import
    line alphabetically placed among the existing `from verification.<mod> import ...` block; and the
    four public names inserted into the sorted `__all__`.

    Finally, wire the tracer in `main_higyrus.py`: (i) import `max_existing_fid` alongside the existing
    `append_finding` import and add `_seed_fid_counter()` ported verbatim from `main_iol.py:190-220`
    (docstring included, adjusted to higyrus's own committed fids `F-01`/`F-02`), placed immediately
    above the existing `_next_fid` at `:237`; (ii) add module constant `_STRICT = os.getenv(
    "MARKET_LIBS_STRICT_DECODE") == "1"` near the other `os.getenv` constants at `:166` and thread it
    as `Client(strict_decode=_STRICT)` at `:2507` and `AsyncClient(strict_decode=_STRICT)` at `:2386`
    — still exactly one ctor site each, so `verification/test_main_higyrus_uses_single_client_instance.py`
    stays green; (iii) call `_seed_fid_counter()` in `main()` after `write_findings(_PKG)` and before
    the first probe, matching `main_market_data.py:3319-3324`; (iv) decorate `probe_get_health_sync`
    and `probe_get_health_async` with `probe_context(endpoint=_ENDPOINT_TEMPLATES["get_health"],
    surface="sync"|"async", decode_error=HigyrusDecodeError, on_decode_error=_shape_probe_result)`
    where `_shape_probe_result` is a new module-level helper returning the driver's
    `tuple[ProbeResult, None]` 2-tuple with status `"FINDING"`; (v) DELETE the hand-rolled `except
    HigyrusDecodeError` block in `probe_get_health_sync` (`main_higyrus.py:681-695`) and its async
    twin at `:780+`, plus the docstring paragraph at `:635-637` that describes it — keeping it would
    mint a second finding for a divergence the handler already wrote and break Lock 10's
    `idempotent_by_title` (D-07). Do NOT add `HigyrusDecodeError` to `_RESIDUAL_PROBE_EXCEPTIONS`:
    that tuple maps to `ERROR-MAP` with a per-probe-unique title, and the decode error must stay a
    deterministic-title `SHAPE`. Preserve the established handler ladder order —
    `HigyrusAuthError` -> `HigyrusAPIError` -> (decode, now via the decorator, outside the function)
    -> `_RESIDUAL_PROBE_EXCEPTIONS`. `_shape_probe_result` may read only `exc.model`,
    `exc.field_path`, `exc.declared_type` and `exc.observed_type` — the four attributes certified
    type-and-path-only by T-29-36 — and nothing else from the exception.
  </action>
  <acceptance_criteria>
    - `uv run pytest verification/test_divergences.py -q` passes with at least `test_handler_maps_record_to_shape_finding` and `test_probe_context_binding` present and green.
    - `uv run python -c "import verification; print(sorted(n for n in verification.__all__ if n in {'DivergenceHandler','divergence_capture','endpoint_scope','probe_context'}))"` prints all four names.
    - `grep -v '^#' main_higyrus.py | grep -c '_seed_fid_counter'` is at least 2 (definition plus the `main()` call site).
    - `grep -v '^#' main_higyrus.py | grep -c 'strict_decode=_STRICT'` equals 2 (one sync ctor, one async ctor).
    - `uv run pytest verification/test_main_higyrus_uses_single_client_instance.py verification/test_main_drivers_bare_except.py verification/test_logging_root_unchanged.py -q` passes.
    - `uv run python tools/check_decode_intactness.py` exits 0 — no `_decode.py` copy was edited.
    - `uv run mypy verification` reports no error in `verification/divergences.py`; every `record.<attr>` read carries a documented `# type: ignore[attr-defined]`.
    - `uv run ruff check . && uv run ruff format --check .` pass.
    - `uv run pytest packages/higyrus-client -q` passes (no package source was touched).
    - `verification/divergences.py` contains no reference to `logging.root` and no import of any `*_client` package.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest verification/test_divergences.py verification/test_main_higyrus_uses_single_client_instance.py verification/test_main_drivers_bare_except.py verification/test_logging_root_unchanged.py -q &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run mypy verification &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check .</automated>
  </verify>
  <done>A six-key decode record emitted on `logging.getLogger("higyrus_client")` inside a `probe_context`-bound call produces exactly one `SHAPE` finding carrying endpoint, model, field path and surface; higyrus's fid allocator starts above `F-02`; the D-07 hand-rolled catch is gone from both surfaces; all four CI-equivalent gates are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hardening suite for the three silent-loss channels, plus the verification/ red baseline</name>
  <files>verification/test_divergences.py, .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md</files>
  <read_first>
    - `verification/divergences.py` (as written in Task 2)
    - `verification/test_divergences.py` (as written in Task 2)
    - `verification/test_logging_root_unchanged.py` — the snapshot/restore assertion shape and its note that `caplog` deliberately installs handlers on `logging.root`, so a test asserting root is untouched must not request `caplog`
    - `packages/higyrus-client/src/higyrus_client/_logging.py` — what `attach()` does today (adds a `NullHandler`, never sets a level)
    - `33-RESEARCH.md` Pitfalls P-1, P-2, P-13 and the executed reproductions in `## Code Examples`
    - `33-VALIDATION.md` `## Wave 0 Requirements`
  </read_first>
  <behavior>
    Three more tests in `verification/test_divergences.py`, each written RED against the Task 2
    module and each closing one verified silent-loss channel:

    - `test_install_sets_level_and_restores`: before entering `divergence_capture(PACKAGE_LOGGERS,
      next_fid=...)`, each of the five package loggers has `level == 0` (NOTSET) and
      `isEnabledFor(logging.INFO)` is False; inside, every one has `level == logging.INFO`,
      `isEnabledFor(logging.INFO)` is True, and the handler is in its `.handlers`; after exit, every
      logger's `level` is back to its recorded pre-entry value and the handler is gone. Snapshot
      `logging.root.handlers`, `logging.root.filters` and `logging.root.level` before and assert all
      three are identical after — and do NOT request the `caplog` fixture in this test.
    - `test_extra_kind_is_captured`: a record emitted at `logging.INFO` with `divergence="extra"`
      reaches the handler and lands as a finding. Assert the same record is NOT delivered when the
      logger level is left at its default, so the test proves the level raise is load-bearing rather
      than incidental.
    - `test_emit_never_raises`: with `verification.findings.append_finding` monkeypatched to raise
      `ValueError("boom")`, emitting a well-formed record raises nothing out of the logging call,
      `handler.errors` has exactly one entry containing `ValueError`, and `handler.seen` still holds
      the triple (the census unit survives a sink failure). Repeat with an `OSError` to cover the
      findings-file write path.
  </behavior>
  <action>
    Add the three tests above to `verification/test_divergences.py`, following the header and
    assertion-message style of `verification/test_logging_root_unchanged.py`: `from __future__ import
    annotations` immediately after a docstring that names the invariant and the pitfall it closes, and
    assertion messages that state the *why* (e.g. that a NOTSET package logger inherits root's
    `WARNING`, so 32 of the 96 ratified floor records — every `extra` kind — are dropped before any
    handler runs). Use `monkeypatch.setattr(verification.findings, "_FINDINGS_DIR", tmp_path)` for
    isolation so no test ever writes to the committed `.planning/verification/` files.

    Then capture the red baseline required by P-13. Run `uv run pytest verification -q --tb=no -rfE`
    to completion (it takes roughly 14 minutes; run it in the background and poll) and write
    `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md` containing: the
    exact summary line, the full `-rfE` short-summary list of FAILED and ERROR node ids, the date and
    the git SHA it was measured at, and a short section stating that repairing this rot is NOT in
    LIVE-TYP-01's scope, naming `verification/test_matriz_sweep_snapshot.py` as the dominant root
    cause (one parametrized test calling `probe_get_segments()` with no `client` argument, stale since
    the Phase 15 driver migration; 17 of the failures and 17 of the errors), and routing repair to a
    named destination phase rather than absorbing it silently. Record explicitly that
    `verification/test_matriz_sweep_snapshot.py` calls probes directly and is therefore the canary for
    this phase's own decorator refactor — plans 33-02 and 33-03 must re-run it after applying the
    decorator.

    Do NOT attempt to repair the baseline failures in this plan. Do NOT make a full `pytest
    verification` run a gate on any task; gate on `pytest packages` (what CI enforces) plus the
    targeted `verification/` files this phase owns.
  </action>
  <acceptance_criteria>
    - `uv run pytest verification/test_divergences.py -q` passes and reports at least 5 tests collected.
    - `grep -v '^#' verification/test_divergences.py | grep -c 'def test_'` is at least 5.
    - `verification/test_divergences.py` defines all five names from 33-VALIDATION.md: `test_handler_maps_record_to_shape_finding`, `test_emit_never_raises`, `test_install_sets_level_and_restores`, `test_extra_kind_is_captured`, `test_probe_context_binding`.
    - `git status --porcelain .planning/verification/` is empty after the test run — no test wrote to a committed findings file.
    - `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md` exists, contains a pytest summary line with both a failure count and an error count, contains the string `test_matriz_sweep_snapshot`, and names a destination phase for the repair.
    - `uv run mypy verification && uv run ruff check . && uv run ruff format --check .` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest verification/test_divergences.py -q &amp;&amp; test -s .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md &amp;&amp; grep -q test_matriz_sweep_snapshot .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md &amp;&amp; test -z "$(git status --porcelain .planning/verification/)" &amp;&amp; uv run mypy verification &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>All five Wave-0 unit tests exist and pass; each of P-1, P-2 and P-3's failure modes is pinned by a test that fails if the hardening is removed; the pre-existing red state of `verification/` is a committed artifact with a named destination phase for its repair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vendor API -> `_decode` walker | Untrusted third-party JSON crosses here; field names in the payload become `field_path` segments. |
| `_decode._emit` -> `DivergenceHandler.emit` | The frozen six-key `LogRecord` is the only channel; the handler must not widen it or re-derive from it. |
| `DivergenceHandler.emit` -> `.planning/verification/<pkg>-findings.md` | A git-committed, permanently published artifact. Anything written here is public forever. |
| `.env` (per package) -> driver process | Credentials enter memory; they must never reach a log record, a finding, or an exception rendered into text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | `DivergenceHandler.emit` composing `title`/`expected`/`actual`/`diff` | mitigate | Compose only from the six record keys plus the endpoint/surface this module set; never read a wire value; never widen the record (Lock 1 + Lock 11, prohibition P-01). Enforced by review of `verification/divergences.py` and by the record contract itself being type-not-value upstream. |
| T-33-02 | Information Disclosure | an `extra`-kind `field_path` whose final segment is a payload-supplied, identifier-shaped key | accept | `_decode._safe_key` already neutralises every character outside `[0-9A-Za-z_-]` and truncates to 64 upstream; sanitisation stops injection and inflation, not identifier disclosure. Accepted for this phase; a consumer needing suppression filters on `divergence == "extra"`, a single stable predicate. |
| T-33-03 | Tampering | log injection — a newline or `.` forging a fake decode site into the findings markdown | mitigate | Do not re-derive the path in the handler; `_decode._safe_key` sanitised it, and `append_finding` rejects a multi-line `title` with `ValueError`. The self-guarding `emit` records that rejection in `handler.errors` instead of losing it. |
| T-33-04 | Repudiation | a security-relevant handler failure vanishing inside `_decode._emit`'s `contextlib.suppress` | mitigate | `DivergenceHandler.emit` wraps its whole body and tallies failures into `self.errors`; the driver prints `len(handler.errors)` as a number in its SUMMARY. A logging pipeline that can fail invisibly cannot be trusted as an audit record (P-2). |
| T-33-05 | Tampering / DoS | hijacking a consumer's `logging` configuration | mitigate | Attach only to `logging.getLogger("<pkg>")` for the five names in `PACKAGE_LOGGERS`; never `logging.root`; restore both handler set and prior level on context-manager exit. Pinned by `test_install_sets_level_and_restores` and by the existing `verification/test_logging_root_unchanged.py`. |
| T-33-06 | Tampering | path traversal through the findings package slug | mitigate | The handler passes only the five literals in `_SLUG_BY_LOGGER`; `findings.py::_PKG_SLUG_RE` (WR-04) is the enforcing check and is not reimplemented here. |
| T-33-07 | Information Disclosure | credential material reaching a finding via a rendered exception body | mitigate | `_shape_probe_result` reads only `exc.model` / `exc.field_path` / `exc.declared_type` / `exc.observed_type` — the four attributes certified type-and-path-only by T-29-36 — and nothing else from the decode exception. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this phase installs zero external packages (`33-RESEARCH.md` `## Package Legitimacy Audit`: the table is empty by construction, every module is Python stdlib or already-committed in-repo code, `uv.lock` is untouched). No install task exists, therefore no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `uv run pytest verification/test_divergences.py -q` — 5 tests green
- `uv run pytest verification/test_main_higyrus_uses_single_client_instance.py verification/test_main_drivers_bare_except.py verification/test_logging_root_unchanged.py -q`
- `uv run python tools/check_decode_intactness.py`
- `uv run python tools/check_surface_types.py && uv run python tools/check_uniform_structure.py`
- `uv run pytest packages/higyrus-client -q`
- `uv run mypy verification && uv run mypy`
- `uv run ruff check . && uv run ruff format --check .`
- `.planning/phases/33-.../33-BASELINE.md` exists and names the destination phase for the `verification/` repair
</verification>

<success_criteria>
- `verification/divergences.py` exists, is stdlib-only, imports no `*_client` package, and never references `logging.root`
- `DivergenceHandler.emit` cannot raise; a sink failure is a countable entry in `handler.errors`
- `divergence_capture` raises all five package loggers to `INFO` and restores both level and handler set on exit
- `probe_context` binds and resets endpoint + surface for both sync and `async def` probes
- `main_higyrus.py` seeds its fid allocator above `F-02` before the first probe, threads `strict_decode=_STRICT` through both single ctor sites, and no longer contains a hand-rolled `HigyrusDecodeError` catch on either surface
- The Task 1 title-convention decision is recorded verbatim in the SUMMARY
- `33-BASELINE.md` is committed with the exact pre-existing failure/error set
</success_criteria>

<output>
Create `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-01-SUMMARY.md` when done.
Record: the Task 1 selection verbatim (title convention + matriz F-03..F-08 disposition), the final
public signature of `divergence_capture` / `probe_context` / `endpoint_scope` so plans 33-02, 33-03 and
33-04 can apply them without re-deriving, and the `33-BASELINE.md` summary line.

Also record one carry-forward: `verification/` is outside both mypy scopes — `[tool.mypy] files` lists
only the six `packages/*/src` roots and the pre-commit mypy hook is scoped `files: ^packages/.*/src/`,
so `verification/divergences.py` is held to the strict bar only by the manual `uv run mypy verification`
step in this plan's verification block. Enrolling it was GATE-TYP-01 / D-16's job and that is closed;
this is a coverage gap, not a CI failure, and it is explicitly out of LIVE-TYP-01's scope (P-9,
RESEARCH Open Question 5).
</output>
