---
phase: 13-cross-package-ergonomics-with-options-max-retries-n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py
  - packages/higyrus-client/src/higyrus_client/_transport.py
  - packages/higyrus-client/src/higyrus_client/_atransport.py
  - packages/matriz-client/src/matriz_client/_transport.py
  - packages/matriz-client/src/matriz_client/_atransport.py
  - packages/iol-client/src/iol_client/_transport.py
  - packages/iol-client/src/iol_client/_atransport.py
  - verification/test_with_options.py
autonomous: true
requirements: [ERG-01]
requirements_addressed: [ERG-01]

must_haves:
  truths:
    - "RetryTransport.handle_request reads max_attempts override from request.extensions"
    - "AsyncRetryTransport.handle_async_request reads max_attempts override from request.extensions"
    - "Mutation gate (idempotent check) remains FIRST gate; max_attempts evaluated AFTER"
    - "max_attempts default falls back to constructor self._max_attempts when extension absent"
    - "Cross-cutting tests file exists for 4 with_options behaviors (3 parametrized × 4 pkgs + matriz mutation-gate)"
    - "Cross-cutting tests are RED in HEAD (with_options method not yet implemented in Plans 2-5)"
  artifacts:
    - path: "verification/test_with_options.py"
      provides: "4 cross-cutting tests covering share/mutation-gate/extension-honor/chaining (D-P2)"
      contains: "test_with_options_does_not_bypass_mutation_gate_matriz"
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py"
      provides: "RetryTransport that reads request.extensions['max_attempts']"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py"
      provides: "AsyncRetryTransport that reads request.extensions['max_attempts']"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/higyrus-client/src/higyrus_client/_transport.py"
      provides: "RetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/higyrus-client/src/higyrus_client/_atransport.py"
      provides: "AsyncRetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/matriz-client/src/matriz_client/_transport.py"
      provides: "RetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/matriz-client/src/matriz_client/_atransport.py"
      provides: "AsyncRetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/iol-client/src/iol_client/_transport.py"
      provides: "RetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
    - path: "packages/iol-client/src/iol_client/_atransport.py"
      provides: "AsyncRetryTransport extension read mirror"
      contains: "request.extensions.get(\"max_attempts\""
  key_links:
    - from: "Plans 2-5 shell _request() (will set req.extensions['max_attempts'])"
      to: "_transport.py RetryTransport.handle_request (reads extension)"
      via: "request.extensions['max_attempts'] key"
      pattern: "extensions\\.get\\(\"max_attempts\""
    - from: "verification/test_with_options.py mutation-gate test"
      to: "_transport.py mutation gate (idempotent FIRST gate)"
      via: "non-idempotent request.extensions['idempotent'] = False forces pass-through"
      pattern: "if not request\\.extensions\\.get\\(\"idempotent\""
---

<objective>
Land the cross-cutting wiring for Phase 13 ERG-01: extend `RetryTransport.handle_request` and `AsyncRetryTransport.handle_async_request` across 4 packages × 2 surfaces (8 files) to read a per-request `max_attempts` override from `request.extensions`, AND create the cross-cutting `verification/test_with_options.py` with the 4 tests that anchor the rest of Phase 13 (D-P1, D-P2).

Purpose: Tests-first idiom (Phase 8 D-21 carry-forward). The transport extension read is small and ships in Plan 1 so subsequent per-package plans (2-5) can land the `with_options` method incrementally with the transport already honoring the extension. The cross-cutting tests are committed RED in Plan 1 HEAD — they go GREEN as Plans 2-5 land their per-package `with_options` method. Plan 1 NEVER touches `client.py`/`aio.py` of any package.

Output: 8 transport file modifications (1 LOC each, same shape) + 1 new test file (`verification/test_with_options.py`) committed RED per D-P1 idiom.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md
@.planning/milestones/v1.1-phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py
@verification/test_retry_mutation_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RetryTransport sync × 4 packages to read max_attempts extension</name>
  <files>
    packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py,
    packages/higyrus-client/src/higyrus_client/_transport.py,
    packages/matriz-client/src/matriz_client/_transport.py,
    packages/iol-client/src/iol_client/_transport.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P1, `<code_context>` integration points, `<specifics>` RetryTransport extension wiring snippet)
    packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py (current `handle_request` shape: lines 118-194; the line `stop=stop_after_attempt(self._max_attempts)` is what changes)
    packages/higyrus-client/src/higyrus_client/_transport.py
    packages/matriz-client/src/matriz_client/_transport.py
    packages/iol-client/src/iol_client/_transport.py
  </read_first>
  <behavior>
    - For each of the 4 sync `_transport.py` files: inside `RetryTransport.handle_request`, after the mutation gate guard (`if not request.extensions.get("idempotent", False)`) and after the `max_attempts <= 1` bypass, compute `effective_max_attempts = request.extensions.get("max_attempts", self._max_attempts)` and pass it to `stop_after_attempt(effective_max_attempts)` inside the `Retrying(...)` call.
    - Mutation-gate eval order MUST remain unchanged: `if not request.extensions.get("idempotent", False): return super().handle_request(request)` stays the FIRST gate; the `max_attempts` extension only affects calls that already passed the idempotent gate.
    - The `self._max_attempts <= 1` constructor-default bypass also remains as the second gate; the extension does NOT override this bypass (constructor `max_retries=0` always wins).
    - The change is exactly ONE line replacement per file: the literal `stop=stop_after_attempt(self._max_attempts),` becomes `stop=stop_after_attempt(effective_max_attempts),` AND a preceding assignment `effective_max_attempts = request.extensions.get("max_attempts", self._max_attempts)` is added immediately before the `for attempt in Retrying(...)` block (placed alongside the `request_id = request.extensions.get(...)` block at the top of the function body).
  </behavior>
  <action>
    For each of the 4 sync `_transport.py` files (ambito → higyrus → matriz → iol), modify `RetryTransport.handle_request` body as follows:

    1. After the existing `request_id = request.extensions.get("request_id", "")` / `endpoint_name = ...` / `account_id = ...` block (the variables read from `request.extensions` near the top of the function), add ONE NEW LINE:

       `effective_max_attempts = request.extensions.get("max_attempts", self._max_attempts)`

    2. Inside the `for attempt in Retrying(...)` call, replace the existing literal:

       `stop=stop_after_attempt(self._max_attempts),`

       with:

       `stop=stop_after_attempt(effective_max_attempts),`

    3. DO NOT touch the mutation gate `if not request.extensions.get("idempotent", False): return super().handle_request(request)` (must remain FIRST gate eval).

    4. DO NOT touch the `if self._max_attempts <= 1: return super().handle_request(request)` constructor-default bypass (max_retries=0 still wins).

    5. Add an inline comment next to the new `effective_max_attempts = ...` line: `# Phase 13 ERG-01: per-request override via with_options(max_retries=N) view.`

    Constraint: each file diff must be EXACTLY 2 new lines (the assignment + the comment) and 1 line replacement (the `stop=` arg). No other changes.

    DO NOT change anything in `client.py` or `aio.py` of any package — Plan 1 ONLY touches `_transport.py`/`_atransport.py` + `verification/test_with_options.py`.

    DO NOT add `"max_attempts"` to any other extension-read site (only the `stop_after_attempt` consumes it).

    DO NOT change `__all__` of `_transport.py`. DO NOT change the constructor of `RetryTransport`.

    Run `uv run ruff check packages/<pkg>/` after each file modification to confirm the change is clean.
  </action>
  <verify>
    <automated>uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/ -x -q</automated>
    <automated>uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q</automated>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q</automated>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/ambito-financiero-client/ packages/higyrus-client/ packages/matriz-client/ packages/iol-client/</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "effective_max_attempts = request.extensions.get" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py` returns 1
    - Source: `grep -c "effective_max_attempts = request.extensions.get" packages/higyrus-client/src/higyrus_client/_transport.py` returns 1
    - Source: `grep -c "effective_max_attempts = request.extensions.get" packages/matriz-client/src/matriz_client/_transport.py` returns 1
    - Source: `grep -c "effective_max_attempts = request.extensions.get" packages/iol-client/src/iol_client/_transport.py` returns 1
    - Source: `grep -c "stop_after_attempt(effective_max_attempts)" packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py` returns 1 (same for higyrus, matriz, iol)
    - Source (eval order preserved): `grep -nE 'idempotent|max_attempts' packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py | grep -v '^#'` confirms `idempotent` gate appears BEFORE `effective_max_attempts` assignment by line number (same for the other 3 packages).
    - Behavior: existing Phase 8 tests still GREEN (mutation-gate bypass, retry-after, max_retries=0 bypass) — `uv run --package matriz-client pytest packages/matriz-client/tests/test_retry_transport.py -x -q exits 0` (and equivalent for each package's transport tests).
    - Quality: `uv run ruff check packages/ambito-financiero-client/ packages/higyrus-client/ packages/matriz-client/ packages/iol-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src exits 0`.
  </acceptance_criteria>
  <done>
    All 4 sync `_transport.py` files modified with the same shape (1 assignment + 1 comment + 1 line replacement). Mutation gate remains FIRST gate. Constructor-default `max_attempts <= 1` bypass preserved. Existing Phase 8 tests stay GREEN. Ruff + mypy strict GREEN for all 4 packages.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend AsyncRetryTransport × 4 packages to read max_attempts extension (mirror sync)</name>
  <files>
    packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py,
    packages/higyrus-client/src/higyrus_client/_atransport.py,
    packages/matriz-client/src/matriz_client/_atransport.py,
    packages/iol-client/src/iol_client/_atransport.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P1, `<specifics>` RetryTransport extension wiring snippet — identical to Task 1)
    packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py (current `handle_async_request` shape: lines 57-132; the line `stop=stop_after_attempt(self._max_attempts)` is what changes)
    packages/higyrus-client/src/higyrus_client/_atransport.py
    packages/matriz-client/src/matriz_client/_atransport.py
    packages/iol-client/src/iol_client/_atransport.py
    The 4 sync `_transport.py` files modified in Task 1 (mirror shape)
  </read_first>
  <behavior>
    - For each of the 4 async `_atransport.py` files: mirror Task 1 exactly inside `AsyncRetryTransport.handle_async_request`.
    - Mutation gate `if not request.extensions.get("idempotent", False): return await super().handle_async_request(request)` remains FIRST gate.
    - The `self._max_attempts <= 1` bypass remains second gate (constructor `max_retries=0` always wins).
    - Same `effective_max_attempts = request.extensions.get("max_attempts", self._max_attempts)` pattern, same `stop=stop_after_attempt(effective_max_attempts)` replacement inside the `AsyncRetrying(...)` call.
  </behavior>
  <action>
    For each of the 4 async `_atransport.py` files (ambito → higyrus → matriz → iol), modify `AsyncRetryTransport.handle_async_request` body identically to Task 1:

    1. After the existing `request_id = request.extensions.get("request_id", "")` / `endpoint_name = ...` / `account_id = ...` block, add ONE NEW LINE:

       `effective_max_attempts = request.extensions.get("max_attempts", self._max_attempts)`

    2. Inside the `async for attempt in AsyncRetrying(...)` call, replace:

       `stop=stop_after_attempt(self._max_attempts),`

       with:

       `stop=stop_after_attempt(effective_max_attempts),`

    3. Add inline comment: `# Phase 13 ERG-01: per-request override via with_options(max_retries=N) view.`

    4. DO NOT touch the mutation gate.

    5. DO NOT touch the `self._max_attempts <= 1` bypass.

    Constraint: each file diff must be EXACTLY 2 new lines + 1 line replacement. No other changes. No new imports.

    Verify intra-package coupling: each `_atransport.py` already imports `_LOGGER_NAME`, `_RETRYABLE_EXC`, `_is_retryable_status`, `_parse_retry_after`, `_RetryableStatus` from its sibling `_transport.py` (Phase 8 deliberate intra-package import). Do NOT add new imports — `effective_max_attempts` is computed inline from values already in scope.
  </action>
  <verify>
    <automated>uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/ -x -q</automated>
    <automated>uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q</automated>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q</automated>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "effective_max_attempts = request.extensions.get" packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py` returns 1 (same for higyrus, matriz, iol).
    - Source: `grep -c "stop_after_attempt(effective_max_attempts)" packages/ambito-financiero-client/src/ambito_financiero_client/_atransport.py` returns 1 (same for higyrus, matriz, iol).
    - Source (no new imports): `git diff packages/<pkg>/src/<pkg>/_atransport.py` shows zero changes to import lines (above the class definition).
    - Behavior: existing Phase 8 + Phase 10 async retry tests still GREEN (e.g., `verification/test_async_cancellation.py`, per-package `test_async_client.py`).
    - Quality: `uv run ruff check packages/ exits 0`.
    - Quality: `uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src exits 0`.
  </acceptance_criteria>
  <done>
    All 4 async `_atransport.py` files modified identically to Task 1. Mutation gate preserved. Constructor-default bypass preserved. No new imports. Existing Phase 8 + Phase 10 async retry tests GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create verification/test_with_options.py with 4 cross-cutting tests (D-P2)</name>
  <files>verification/test_with_options.py</files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P2 test list + `<specifics>` CRITICAL merge gate test shape for matriz `new_order` 503 single-request assertion)
    verification/test_retry_mutation_gate.py (Phase 8 D-26 cross-cutting pattern; Plan 1 creates SEPARATE file — do not extend this one)
    verification/test_max_retries_validation.py (cross-cutting parametrize idiom example)
    packages/matriz-client/src/matriz_client/client.py (new_order signature + idempotent=False flag — required for matriz mutation-gate test)
    packages/ambito-financiero-client/src/ambito_financiero_client/client.py (get_dollar_banco_nacion as idempotent GET — used for extension-honor test)
    packages/iol-client/src/iol_client/client.py (get_quote as idempotent GET)
    packages/higyrus-client/src/higyrus_client/client.py (get_movimientos as idempotent GET)
    packages/matriz-client/src/matriz_client/client.py (get_segments as idempotent GET)
  </read_first>
  <behavior>
    Create `verification/test_with_options.py` (NEW file) with 4 tests anchoring Phase 13 ERG-01:

    1. `test_with_options_shares_http_client_and_token` — parametrized × 4 packages (ambito, iol, higyrus, matriz). For each pkg, construct `Client(...)` (or `_get_default()`), call `view = parent.with_options(max_retries=5)`, assert `view._state is parent._state` AND `view._state.http_client is parent._state.http_client`. Anti-Pitfall 13 (resource leak); SC#1 ROADMAP.

    2. `test_with_options_does_not_bypass_mutation_gate_matriz` (matriz-only, CRITICAL MERGE GATE) — Anti-Pitfall 14. See `<specifics>` D-P2 CRITICAL merge gate test shape (full body below).

    3. `test_with_options_max_attempts_extension_honored` — parametrized × 4 packages. For each, pick 1 idempotent GET endpoint (ambito: `get_dollar_banco_nacion(date)`, iol: `get_quote("GGAL")`, higyrus: `get_movimientos(id_cuenta=1, fecha_desde=..., fecha_hasta=...)`, matriz: `get_segments()`). Mock httpx with persistent 503. Assert that `parent.<endpoint>(...)` produces 3 requests (default `max_retries=2 + 1`), and `view = parent.with_options(max_retries=10); view.<endpoint>(...)` produces 11 requests. Validates the extension propagates from view to transport. SC#3 ROADMAP.

    4. `test_with_options_chaining_inner_wins` — parametrized × 4 packages. For each, `view = parent.with_options(5).with_options(10)`; assert `view._max_retries == 10` AND `parent._max_retries == 2` (original untouched). Covers D-V2.

    The 4 tests are RED in Plan 1 HEAD because `with_options` method does not exist yet on any of the 4 Client/AsyncClient classes. They turn GREEN incrementally as Plans 2-5 land per-package implementations (Plan 2 turns the ámbito row green; Plan 5 turns all 4 green).

    Use `pytest.mark.parametrize("pkg_name,client_factory,endpoint_factory", [...])` to register the 4 packages × test pattern. Each parametrize row should `xfail` cleanly when `with_options` is missing (use `pytest.raises(AttributeError)` to make RED predictable, OR use `pytest.importorskip`/skip-on-AttributeError — planner discretion).

    Planner discretion (per CONTEXT.md "Claude's Discretion"): use `pytest.raises(AttributeError)` for the 4 tests so they pass RED→GREEN naturally: test asserts behavior; once `with_options` exists, the AttributeError-wrapping idiom no longer matches and the test re-validates against actual behavior. Alternative: mark each test `xfail(strict=False)` until Plan 5. Recommendation: use the SIMPLER approach — write tests as if `with_options` already exists, accept they fail in Plan 1 HEAD, and let Plans 2-5 flip them GREEN incrementally. Document this with a top-of-file docstring explaining the RED-in-HEAD idiom (Phase 8 D-21 pattern).

    Configuration fixture pattern: each test constructs Client via `<pkg>.Client(...)` directly (not via `configure()` + `_get_default()`) so test isolation is automatic. For matriz, pass `token="test-token", token_expires_at=9_999_999_999.0` to skip `_ensure_token` for the mutation-gate test (per `<specifics>` body).
  </behavior>
  <action>
    Create `verification/test_with_options.py` (NEW file) with the following structure:

    1. Module docstring: explain RED-in-HEAD idiom (Phase 8 D-21 carry-forward); 4 tests cover D-V1/D-V2/D-V3/D-P2 SC#1/SC#2/SC#3; CRITICAL merge gate is `test_with_options_does_not_bypass_mutation_gate_matriz` (Anti-Pitfall 14, money-on-the-line).

    2. Imports: `from __future__ import annotations`, `import datetime as dt`, `import pytest`, `import httpx`, plus the 4 client packages and `pytest_httpx`.

    3. Test 1 — `test_with_options_shares_http_client_and_token` parametrized via `pytest.mark.parametrize` over a list `[("ambito_financiero_client", ambito_factory), ("iol_client", iol_factory), ("higyrus_client", higyrus_factory), ("matriz_client", matriz_factory)]`:
       - Each factory constructs a Client with credentials sufficient to instantiate (matriz: `username="u", password="p"`; iol: same; higyrus: `username="u", password="p"`; ambito: no auth).
       - Force `_ensure_http_client()` to populate `_state.http_client` (e.g., via a mocked GET that returns 200 with a trivial body).
       - Call `view = parent.with_options(max_retries=5)`.
       - Assert `view._state is parent._state` (identity).
       - Assert `view._state.http_client is parent._state.http_client` (identity).
       - Assert `view._max_retries == 5` AND `parent._max_retries == 2`.

    4. Test 2 — `test_with_options_does_not_bypass_mutation_gate_matriz(httpx_mock)`:
       Use the EXACT body documented in `<specifics>`:

       ```
       import matriz_client
       matriz_client.configure(
           base_url="https://api.test",
           username="u", password="p",
           token="test-token", token_expires_at=9_999_999_999.0,
       )
       httpx_mock.add_response(status_code=503)
       client = matriz_client._get_default()
       with pytest.raises(matriz_client.exceptions.MatrizClientError):
           client.with_options(max_retries=10).new_order(
               symbol="GGAL", side="BUY", quantity=1, price=100.0,
               account="test-acct",
           )
       assert len(httpx_mock.get_requests()) == 1  # NO retry
       ```

       (NOTE: replace the inlined ```...``` literal in your `<action>` reading with the actual Python code in the new file — the planner Lint rule prohibits fenced code in `<action>` but the file content must be valid Python. Use `Write` tool to create the file with the actual code; the prose here is a directive.)

       Use `monkeypatch` to ensure `matriz_client.configure` is reset after the test (idempotent fixture). Confirm the inspector exactly that `len(httpx_mock.get_requests()) == 1` — this is the merge-gate assertion (SC#2 ROADMAP, Anti-Pitfall 14).

    5. Test 3 — `test_with_options_max_attempts_extension_honored` parametrized × 4 packages. For each row `(pkg_name, endpoint_call_factory, idempotent_get_signature)`:
       - `httpx_mock.add_response(status_code=503)` × N (use httpx_mock's "is_optional=True" pattern or `is_reusable=True` so the same mock answers all retries).
       - Construct Client with `max_retries=2`.
       - Call parent's idempotent GET — expect exactly 3 requests (default `2 + 1`).
       - Reset `httpx_mock`.
       - Call `view = parent.with_options(max_retries=10).<idempotent_get>(...)` — expect exactly 11 requests.
       - Assert via `len(httpx_mock.get_requests())`.

    6. Test 4 — `test_with_options_chaining_inner_wins` parametrized × 4 packages:
       - Construct Client with `max_retries=2`.
       - `view = parent.with_options(5).with_options(10)`.
       - Assert `view._max_retries == 10`.
       - Assert `parent._max_retries == 2` (untouched).
       - Assert `view._state is parent._state` (still shared after chaining).

    7. Configuration cleanup: use a `pytest.fixture(autouse=True)` per-test to reset module-level `_default_client` of each package between tests (precedent from `verification/test_retry_mutation_gate.py` and `verification/test_sync_async_isolation.py`).

    Final size target: ~150-200 LOC for the file (4 tests parametrized).

    Plan 1 commit: the file is committed RED (4 tests fail because `with_options` does not exist). Use `pytest verification/test_with_options.py --collect-only` to verify the tests are collected — the failure mode is acceptable.

    DO NOT extend `verification/test_retry_mutation_gate.py` — the Phase 13 cross-cutting tests live in their own file (D-P2 scope separation).
  </action>
  <verify>
    <automated>uv run pytest verification/test_with_options.py --collect-only -q</automated>
    <automated>uv run ruff check verification/test_with_options.py</automated>
    <automated>uv run mypy --strict verification/test_with_options.py</automated>
    <automated>test -f verification/test_with_options.py && echo "EXISTS" | grep -q "EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f verification/test_with_options.py exits 0`.
    - File is collected by pytest: `uv run pytest verification/test_with_options.py --collect-only -q` reports 4 test functions (Test 1: parametrize × 4 = 4 cases; Test 2: 1 case matriz-only; Test 3: parametrize × 4 = 4 cases; Test 4: parametrize × 4 = 4 cases — total 13 collected items).
    - Source: `grep -c "def test_with_options_shares_http_client_and_token" verification/test_with_options.py` returns 1.
    - Source: `grep -c "def test_with_options_does_not_bypass_mutation_gate_matriz" verification/test_with_options.py` returns 1.
    - Source: `grep -c "def test_with_options_max_attempts_extension_honored" verification/test_with_options.py` returns 1.
    - Source: `grep -c "def test_with_options_chaining_inner_wins" verification/test_with_options.py` returns 1.
    - Source (mutation-gate assertion present): `grep -c 'len(httpx_mock.get_requests()) == 1' verification/test_with_options.py` returns 1.
    - Source (parametrize covers 4 packages): `grep -c '"matriz_client"' verification/test_with_options.py` returns at least 4 (1 per parametrize block + matriz-only test).
    - Source (`__future__ annotations` mandatory per CLAUDE.md): `grep -c 'from __future__ import annotations' verification/test_with_options.py` returns 1.
    - Behavior RED in HEAD: `uv run pytest verification/test_with_options.py -q --no-header 2>&1 | grep -E '^FAILED|^ERROR' | wc -l` returns ≥ 1 (with_options method does not exist yet — all 4 tests fail by `AttributeError`). This is the EXPECTED RED state per D-P1.
    - Quality: `uv run ruff check verification/test_with_options.py exits 0`.
    - Quality: `uv run mypy --strict verification/test_with_options.py exits 0`.
    - Negative: `git diff verification/test_retry_mutation_gate.py` shows zero changes (Phase 13 does NOT touch Phase 8 cross-cutting file).
  </acceptance_criteria>
  <done>
    `verification/test_with_options.py` exists with 4 tests parametrized × 4 packages (matriz mutation-gate test is matriz-only). Tests are RED in HEAD per D-P1 idiom (with_options not yet implemented). The CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz` is present with `assert len(httpx_mock.get_requests()) == 1` (SC#2 ROADMAP merge gate). Ruff + mypy strict GREEN. `verification/test_retry_mutation_gate.py` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `RetryTransport` | Caller-controlled `request.extensions["max_attempts"]` value reaches retry-loop iteration count |
| caller → `RetryTransport` (mutation gate) | Caller-controlled `request.extensions["idempotent"]` value gates whether retry loop runs at all (inherited from Phase 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `RetryTransport.handle_request` mutation gate eval order | mitigate | Mutation gate (`if not request.extensions.get("idempotent", False)`) MUST remain FIRST gate; `effective_max_attempts` computed AFTER the gate. Acceptance criteria assert grep'd eval order (idempotent check appears before max_attempts assignment). |
| T-13-02 | Tampering | `request.extensions["max_attempts"]` injection via view | mitigate | The extension is set ONLY by the shell `_request()` of each package's Client/AsyncClient (Plans 2-5). Callers cannot reach `request.extensions` from public surface. The value originates from `_max_retries` (validated via `_validate_max_retries` Phase 8 WR-06 on `with_options` entry). |
| T-13-03 | Denial of Service | Unbounded retry via `max_attempts=BIGNUM` | accept | `_validate_max_retries` rejects negative ints and bool but allows arbitrary positive int (e.g., `with_options(max_retries=10_000_000)`). Threat: caller intentionally bumps retries to exhaust local CPU / connection pool. Acceptance: caller controls their own client; no anonymous remote actor can set this. Documented in `with_options` docstring; v1.3 may add upper bound. |
| T-13-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in Phase 13 (per CONTEXT.md `<code_context>`: "No nuevas runtime deps. Phase 13 NO agrega ni a `pyproject.toml`"). tenacity 9.1.4 already present from Phase 8. No supply-chain gate required. |

Mutation gate (Phase 8 D-01 inherited; Anti-Pitfall 14 inherited): non-idempotent requests (matriz `new_order`/`cancel_order`/`replace_order` + all POST/PATCH/DELETE across 4 packages) MUST execute EXACTLY 1 outgoing request regardless of `view._max_retries` value. Plan 1 Task 3 enforces this via `test_with_options_does_not_bypass_mutation_gate_matriz` (committed RED; turns GREEN in Plan 4 once matriz `with_options` lands).

Credential surface unchanged: view shares `_state.token`, `_state.refresh_token` (iol), `_state.token_store` (matriz) with parent — no new credential storage, no new auth flow. Inherited from Phase 6 D-13 + Phase 10 D-T2.
</threat_model>

<verification>
- All 4 sync `_transport.py` files have the `effective_max_attempts` line + `stop_after_attempt(effective_max_attempts)` replacement.
- All 4 async `_atransport.py` files mirror exactly.
- Mutation gate (idempotent check) appears BEFORE `effective_max_attempts` assignment in every file (grep'd eval order).
- Constructor-default bypass (`self._max_attempts <= 1`) preserved in every file.
- `verification/test_with_options.py` exists with 4 tests collecting 13 items.
- The CRITICAL test `test_with_options_does_not_bypass_mutation_gate_matriz` contains the literal `assert len(httpx_mock.get_requests()) == 1`.
- All 4 cross-cutting tests are RED in Plan 1 HEAD (with_options method missing) — this is EXPECTED per D-P1.
- Existing Phase 8/10 transport tests stay GREEN (no regression in `_transport.py` / `_atransport.py` behavior for normal calls).
- `uv run ruff check`, `uv run mypy --strict packages/` exit 0.
- `verification/test_retry_mutation_gate.py` is UNTOUCHED.
</verification>

<success_criteria>
- 8 transport file modifications committed with the same minimal shape (1 assignment + 1 comment + 1 line replacement per file).
- `verification/test_with_options.py` committed RED with 4 cross-cutting tests + matriz CRITICAL mutation-gate test.
- Eval order proven by acceptance grep: idempotent gate comes BEFORE max_attempts assignment in every transport file.
- Phase 8 + Phase 10 existing transport/retry tests stay GREEN.
- `uv run ruff check packages/ verification/test_with_options.py` exits 0.
- `uv run mypy --strict packages/ambito-financiero-client/src packages/higyrus-client/src packages/matriz-client/src packages/iol-client/src verification/test_with_options.py` exits 0.
- No changes to any `client.py` or `aio.py` of any package (verified by `git diff` enumeration).
- `verification/test_retry_mutation_gate.py` UNTOUCHED.
- ERG-01 wiring foundation laid; Plans 2-5 can land `with_options` per package without modifying transport again.
</success_criteria>

<output>
Create `.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-01-SUMMARY.md` when done. Document: 8 transport files modified; 1 new test file committed RED; eval order preserved; tests GREEN after Plan 5 (forward reference); no changes to `client.py`/`aio.py` of any package.
</output>
