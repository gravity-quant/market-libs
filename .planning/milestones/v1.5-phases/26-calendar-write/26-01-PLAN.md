---
phase: 26-calendar-write
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/tests/test_models.py
autonomous: true
requirements: [MUT-MD-02]
tags: [market-data-client, request-models, serialization, calendar, python]

must_haves:
  truths:
    - "D-08: MarketHoursIn, HolidayIn y HolidaysIn son @dataclass(frozen=True, slots=True) y NO heredan de SafeModel (serializan HACIA AFUERA)."
    - "D-09 / ROADMAP SC#2: MarketHoursIn expone confirm: bool = False y lo emite SIEMPRE en el wire; la firma pública queda model-only (no hay kwarg confirm suelto)."
    - "D-10: MarketHoursIn('10:00','17:00','America/Argentina/Buenos_Aires').to_dict() == {'open_time':'10:00','close_time':'17:00','timezone':'America/Argentina/Buenos_Aires','pre_open_minutes':10,'enabled':True,'updated_by':'','confirm':False}."
    - "D-10: HolidayIn requiere day y defaultea closed=True, open_time=None, close_time=None, description=''."
    - "D-11 / ROADMAP SC#3: to_dict() de MarketHoursIn y HolidayIn rutea por market_data_client._params.drop_none — HolidayIn('2026-12-25').to_dict() == {'day':'2026-12-25','closed':True,'description':''} (open_time/close_time DESAPARECEN cuando son None; closed=True y description='' se EMITEN)."
    - "D-11: HolidaysIn.to_dict() == {'days': [d.to_dict() for d in self.days]} (wrapper puro, sin drop_none)."
    - "D-12: HolidaysIn([]) y HolidaysIn([501 items]) levantan ValueError pelado en __post_init__ antes de cualquier dispatch; exactamente 1 y exactamente 500 construyen bien."
    - "D-13: ningún bound escalar se valida client-side (pre_open_minutes, timezone, updated_by, description, formato HH:MM) — van al 422 del servidor."
  prohibitions:
    - "Los tres modelos NUEVOS NO deben heredar de SafeModel ni construirse vía from_api (D-08)."
    - "El bound 1-500 NO debe levantar un error MarketData* — esa jerarquía queda reservada para errores de contrato del servidor (D-12)."
    - "NO validar el formato HH:MM ni ningún bound escalar client-side (D-13); un regex daría falsos negativos porque la OpenAPI usa format: time, que admite '10:00:00'."
    - "NO importar drop_none de higyrus_client — usar la copia local market_data_client._params (CLAUDE.md no-shared-internals)."
    - "NO tocar __version__ en __init__.py (hoy 0.3.1) — el bump es Phase 28."
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "MarketHoursIn, HolidayIn, HolidaysIn (request models frozen con to_dict), import de _params, 3 entradas nuevas en __all__"
      contains: "class MarketHoursIn"
    - path: "packages/market-data-client/tests/test_models.py"
      provides: "tests de to_dict/defaults verbatim OpenAPI/efecto drop_none/bound 1-500"
      contains: "MarketHoursIn"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/_params.py"
      via: "to_dict() de MarketHoursIn y HolidayIn rutea por _params.drop_none (D-11) — primer import intra-paquete de models.py"
      pattern: "_params\\.drop_none"
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "HolidaysIn.to_dict() anida los to_dict() de cada HolidayIn"
      pattern: "d\\.to_dict\\(\\)"
---

<objective>
Construir la capa de serialización HACIA AFUERA de MUT-MD-02: los tres request-models tipados
`MarketHoursIn`, `HolidayIn` y `HolidaysIn`, con defaults verbatim de la OpenAPI en vivo, el
guardrail `confirm` como campo defaulteado a `False`, ruteo por `_params.drop_none` y el bound
client-side 1–500 de `HolidaysIn`.

Purpose: son funciones puras con I/O totalmente definido (dataclass → dict) — target TDD ideal —
y no tocan ni el gate, ni `_core`, ni el transporte. Al tocar sólo `models.py`/`test_models.py`
corren en Wave 1 en paralelo con los builders (Plan 02).

Output: tres dataclasses frozen con `to_dict()` escrito a mano, el primer import intra-paquete de
`models.py` (`_params`), tres entradas nuevas en `models.__all__`, y tests unitarios de
serialización, defaults y bounds.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-calendar-write/26-CONTEXT.md
@.planning/phases/26-calendar-write/26-RESEARCH.md
@.planning/phases/26-calendar-write/26-PATTERNS.md
@.planning/phases/26-calendar-write/26-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces

> Roster completo de Phase 26 (los 4 planes). El pass de source-grounding de
> `plan-review-convergence` debe excluir estos símbolos de la verificación de drift: **ninguno
> existe hoy en el repo** (verificado por grep en 26-RESEARCH.md § Runtime State Inventory).

**Builders nuevos en `packages/market-data-client/src/market_data_client/_core.py`** (Plan 02):
`build_set_calendar_config_request`, `build_delete_calendar_config_request`,
`build_preview_calendar_config_request`, `build_add_holidays_request`,
`build_delete_holiday_request`.

**Parser nuevo en `_core.py`** (Plan 02): `parse_calendar_write_response`.

**Request models nuevos en `models.py`** (este plan): `MarketHoursIn`, `HolidayIn`, `HolidaysIn`.

**Métodos nuevos en `client.py`** (Plan 03): `Client.set_calendar_config`,
`Client.delete_calendar_config`, `Client.preview_calendar_config`, `Client.add_holidays`,
`Client.delete_holiday`.

**Métodos nuevos en `aio.py`** (Plan 03): `AsyncClient.set_calendar_config`,
`AsyncClient.delete_calendar_config`, `AsyncClient.preview_calendar_config`,
`AsyncClient.add_holidays`, `AsyncClient.delete_holiday`.

**Shims module-level nuevos** (Plan 03): en `client.py` (sync) y en `aio.py` (async), los cinco
nombres `set_calendar_config`, `delete_calendar_config`, `preview_calendar_config`,
`add_holidays`, `delete_holiday` — 10 funciones en total.

**Entradas nuevas de `__all__`:**
- `_core.__all__` +6 (los 5 builders + el parser) — Plan 02.
- `models.__all__` +3 (`HolidayIn`, `HolidaysIn`, `MarketHoursIn`) — este plan.
- `__init__.__all__` +8 (`HolidayIn`, `HolidaysIn`, `MarketHoursIn`, `add_holidays`,
  `delete_calendar_config`, `delete_holiday`, `preview_calendar_config`,
  `set_calendar_config`) — Plan 04.

**Archivos de test nuevos** (Plan 03): `packages/market-data-client/tests/test_calendar_write.py`,
`packages/market-data-client/tests/test_calendar_write_async.py` (cada uno con un helper local
`_open_gate()`).

**Archivos de test extendidos:** `tests/test_models.py` (este plan), `tests/test_core.py`
(Plan 02), `tests/test_public_surface_market_data.py` (Plan 04 — tuplas `_NEW_PUBLIC_NAMES` +8 y
`_MUTATION_METHODS` +5).

**Sin cambios (consumo puro):** `_state.py`, `exceptions.py`, `_transport.py`, `_atransport.py`,
`_params.py`, `_logging.py`, `tests/conftest.py`, `pyproject.toml`, `uv.lock`,
`.github/workflows/ci.yml`, `__version__` (sigue en `"0.3.1"`).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MarketHoursIn + HolidayIn (frozen, to_dict vía drop_none)</name>
  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/tests/test_models.py</files>
  <behavior>
    - Test: MarketHoursIn("10:00", "17:00", "America/Argentina/Buenos_Aires").to_dict() == {"open_time": "10:00", "close_time": "17:00", "timezone": "America/Argentina/Buenos_Aires", "pre_open_minutes": 10, "enabled": True, "updated_by": "", "confirm": False} — las 7 claves presentes, confirm explícitamente False (ROADMAP SC#2).
    - Test: MarketHoursIn("10:00", "17:00", "America/Argentina/Buenos_Aires", confirm=True).to_dict()["confirm"] is True.
    - Test: MarketHoursIn("10:00", "17:00", "TZ", pre_open_minutes=0, enabled=False, updated_by="ops").to_dict() emite pre_open_minutes == 0 y enabled is False (drop_none preserva falsy-pero-no-None).
    - Test: HolidayIn("2026-12-25").to_dict() == {"day": "2026-12-25", "closed": True, "description": ""} — exactamente 3 claves; "open_time" not in out y "close_time" not in out.
    - Test: HolidayIn("2026-12-24", closed=False, open_time="10:00", close_time="13:00", description="Nochebuena").to_dict() == las 5 claves con esos valores.
    - Test: MarketHoursIn y HolidayIn NO son subclases de SafeModel (issubclass(...) is False) y no exponen from_api.
    - Test: ambos son frozen — asignar a un campo levanta dataclasses.FrozenInstanceError.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/models.py:37-41 (bloque de imports actual — SOLO stdlib hoy; acá entra el import nuevo), :43-55 (`__all__` ordenado ASCII), :58-117 (`SafeModel` + `_coerce` — el patrón deserializa-HACIA-ADENTRO del que estos modelos son el CONTRARIO; NO heredar), :163-184 (`LatestRequest` — request-model frozen con `to_dict()`, nótese que usa camelCase `marketId`, distinto de estos), :197-238 (`NewSymbol` / `NewSymbols` — el template exacto de Phase 25: frozen+slots, `to_dict()` a mano, `__post_init__` con `ValueError`), :240-252 (`SymbolPatch` — modelo de un solo campo).
    - packages/market-data-client/src/market_data_client/_params.py (archivo completo, 28 líneas) — `drop_none` preserva falsy-pero-no-`None` (`False`, `0`, `""`); es lo que hace que `closed=True` y `description=""` sigan viajando.
    - packages/market-data-client/src/market_data_client/_core.py:479-491 — el call-site EXACTO a copiar para invocar `_params.drop_none({...})` (estilo de llamada + formato del dict literal).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:245-334 (sección «models.py — 3 request models frozen con to_dict()», con el excerpt shipeado y el delta D-11/X4 sobre el import de `_params`).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:279-311 (tabla verbatim de la OpenAPI en vivo: campos, tipos, defaults y bounds de `MarketHoursIn` y `HolidayIn`), :745-754 (Pitfall 6 — orden de imports y ausencia de ciclo).
    - packages/market-data-client/tests/test_models.py:134-152 (template de test de `to_dict()` con drop de opcionales), :154-190 (tests de `NewSymbol`/`SymbolPatch`).
  </read_first>
  <action>
    Escribí PRIMERO los tests que fallan en `tests/test_models.py` (los casos de `<behavior>`), después implementá.

    En `models.py` agregá el import intra-paquete `from market_data_client import _params` — es el PRIMER import local de este archivo (hoy sólo importa stdlib). Va en su propio bloque first-party, después de `from typing import ...`, separado por una línea en blanco (ruff `I001`). No hay ciclo: `_params.py` sólo importa `typing`.

    Agregá `MarketHoursIn` como `@dataclass(frozen=True, slots=True)` — NO subclase de `SafeModel` (D-08: estos modelos serializan HACIA AFUERA). Campos en el orden de la OpenAPI: `open_time: str`, `close_time: str`, `timezone: str` (los tres requeridos, sin default), luego `pre_open_minutes: int = 10`, `enabled: bool = True`, `updated_by: str = ""`, `confirm: bool = False`. `confirm` es campo del modelo, no kwarg del método (D-09) — es el análogo exacto de `NewSymbol.market_id`: defaulteado, no nullable, SIEMPRE emitido. `to_dict(self) -> dict[str, Any]` retorna `_params.drop_none({...})` con las 7 claves en ese mismo orden; acá `drop_none` es no-op (no hay campos nullable) y se rutea igual por consistencia (D-11), documentalo así en el docstring.

    Agregá `HolidayIn` como `@dataclass(frozen=True, slots=True)`: `day: str` (requerido), `closed: bool = True`, `open_time: str | None = None`, `close_time: str | None = None`, `description: str = ""`. `to_dict(self) -> dict[str, Any]` retorna `_params.drop_none({...})` con las 5 claves — acá `drop_none` SÍ es load-bearing: elimina `open_time`/`close_time` cuando son `None` y conserva `closed=True` y `description=""`. Es el primer modelo del paquete con campos nullable.

    Docstrings al estilo de `NewSymbol` (`models.py:197-215`): indicar que NO es `SafeModel`, citar D-08/D-09/D-10/D-11, y que los bounds escalares declarados por la OpenAPI (`pre_open_minutes` 0–120, `timezone` 1–64, `updated_by` ≤200, `description` ≤500, formato de hora) NO se validan client-side sino que van al `422` del servidor (D-13). Los nombres de campo son snake_case verbatim del wire — no hay excepción `N815` que agregar.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q` sale 0 con todos los casos de `<behavior>` en verde.
    - `models.py` contiene `class MarketHoursIn` y `class HolidayIn`, ambos decorados `@dataclass(frozen=True, slots=True)` y con lista de bases vacía.
    - `python -c "import market_data_client.models as m; assert m.MarketHoursIn('10:00','17:00','TZ').to_dict() == {'open_time':'10:00','close_time':'17:00','timezone':'TZ','pre_open_minutes':10,'enabled':True,'updated_by':'','confirm':False}"` sale 0.
    - `python -c "import market_data_client.models as m; d=m.HolidayIn('2026-12-25').to_dict(); assert d == {'day':'2026-12-25','closed':True,'description':''}"` sale 0.
    - `python -c "import market_data_client.models as m; assert not issubclass(m.MarketHoursIn, m.SafeModel) and not issubclass(m.HolidayIn, m.SafeModel)"` sale 0.
    - `models.py` contiene la llamada `_params.drop_none(` dentro de los cuerpos de ambos `to_dict()`.
    - `uv run mypy packages/market-data-client/src` sale 0.
    - `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` salen 0 (import nuevo bien ordenado, sin `I001`).
  </acceptance_criteria>
  <done>Los dos modelos escalares existen, serializan las claves y defaults verbatim de la OpenAPI, `confirm` viaja con default `False`, y el par de horas nullable de `HolidayIn` desaparece del wire vía `drop_none`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HolidaysIn (wrapper + bound 1-500 ValueError) + models.__all__</name>
  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/tests/test_models.py</files>
  <behavior>
    - Test: HolidaysIn([HolidayIn("2026-12-25")]).to_dict() == {"days": [{"day": "2026-12-25", "closed": True, "description": ""}]}.
    - Test: HolidaysIn([HolidayIn("2026-12-25"), HolidayIn("2026-12-24", closed=False, open_time="10:00")]).to_dict()["days"] tiene 2 elementos y el segundo incluye "open_time" pero no "close_time".
    - Test: HolidaysIn([]) levanta ValueError con match "1-500".
    - Test: HolidaysIn([HolidayIn(f"2026-01-{i:02d}") for i in range(501)]) levanta ValueError con match "1-500".
    - Test: HolidaysIn con exactamente 1 y con exactamente 500 elementos construyen sin error.
    - Test: el error del bound es ValueError pelado — no una subclase de MarketDataError (isinstance check negativo).
    - Test: "HolidayIn", "HolidaysIn" y "MarketHoursIn" están en market_data_client.models.__all__.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/models.py:216-238 (`NewSymbols` — el template EXACTO: `__post_init__` que sólo lee y levanta `ValueError`, válido sobre un frozen dataclass sin `object.__setattr__`; y el `to_dict()` wrapper), :43-55 (`__all__` a extender).
    - packages/market-data-client/tests/test_models.py:191-209 (el trío de tests de bounds de `NewSymbols`: vacío / 501 / boundary 1 y 500, con `pytest.raises(ValueError, match="1-500")`).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:266-295 (mapeo `HolidaysIn ← NewSymbols`; el mensaje DEBE conservar el substring `"1-500"` porque los tests matchean por ahí), :333-334 (`models.__all__`).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:305-306 (`HolidaysIn` en la OpenAPI en vivo: `days: {minItems: 1, maxItems: 500}` — el plan fuente omitía este bound), :666-671 (anti-pattern: NO usar un error `MarketData*` para el bound).
  </read_first>
  <action>
    Escribí PRIMERO los tests que fallan, después implementá.

    En `models.py` agregá `HolidaysIn` como `@dataclass(frozen=True, slots=True)` con un único campo `days: list[HolidayIn]`. `__post_init__(self) -> None` levanta un `ValueError` PELADO (no `MarketDataError` ni ninguna subclase — esa jerarquía queda reservada a errores de contrato del servidor, D-12) cuando `not 1 <= len(self.days) <= 500`; el mensaje debe contener el substring `1-500` (los tests matchean por ahí, igual que `NewSymbols`). El `__post_init__` sólo LEE campos, nunca los muta, así que es válido sobre un frozen dataclass sin `object.__setattr__`. `to_dict(self) -> dict[str, Any]` retorna `{"days": [d.to_dict() for d in self.days]}` — wrapper puro, SIN pasar por `drop_none` (mismo criterio que `NewSymbols.to_dict()`, D-11).

    Ubicá las tres clases nuevas juntas, después de `SymbolPatch` (`models.py:240-252`) y antes del bloque de reference models (`Instrument`, `models.py:268`), con un divisor de sección `# ---...---` al estilo del archivo.

    Extendé `models.__all__` (`models.py:43-55`) insertando `"HolidayIn"`, `"HolidaysIn"` y `"MarketHoursIn"` en orden ASCII — no al final (ruff `RUF022`). El orden resultante es: `CalendarConfig`, `CalendarDay`, `HolidayIn`, `HolidaysIn`, `Instrument`, `LatestRequest`, `MarketDataSnapshot`, `MarketHoursIn`, `NewSymbol`, `NewSymbols`, `SafeModel`, `Segment`, `Symbol`, `SymbolPatch`.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q` sale 0.
    - `python -c "import market_data_client.models as m; m.HolidaysIn([])"` termina con `ValueError` cuyo mensaje contiene `1-500`.
    - Un test del archivo asserta que el error del bound es `ValueError` exacto y NO una instancia de `market_data_client.exceptions.MarketDataError` (`type(exc) is ValueError` + `not isinstance(exc, MarketDataError)` sobre `pytest.raises(...).value`).
    - `python -c "import market_data_client.models as m; assert len(m.HolidaysIn([m.HolidayIn(f'2026-01-{i:02d}') for i in range(1,501)]).days) == 500"` sale 0.
    - `python -c "import market_data_client.models as m; assert {'HolidayIn','HolidaysIn','MarketHoursIn'} <= set(m.__all__); assert list(m.__all__) == sorted(m.__all__)"` sale 0.
    - `uv run ruff check packages/market-data-client` sale 0 (sin `RUF022`).
    - `uv run mypy packages/market-data-client/src` sale 0.
  </acceptance_criteria>
  <done>`HolidaysIn` envuelve la lista tipada, corta client-side fuera del rango 1–500 con un `ValueError` pelado antes de cualquier dispatch, y los tres modelos están exportados en `models.__all__` en orden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload del consumidor → request model | El batch de feriados y los valores de horario/timezone cruzan a un modelo tipado antes de cualquier dispatch. |
| request model → dict de wire | `to_dict()` fija la forma y las claves JSON exactas que se mandan al primary-extractor, incluido el guardrail `confirm`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02 | Tampering | config de mercado sobrescrita salteando el warning del servidor (`MarketHoursIn`) | mitigate | `confirm: bool = False` como campo del modelo, siempre emitido (D-09): el consumidor tiene que escribir `confirm=True` a propósito. Testeable: el caso de `<behavior>` que asserta `confirm: False` en el dict por default. |
| T-26-03 | Denial of Service / Input Validation | batch de feriados vacío o sobredimensionado (`HolidaysIn.days`) | mitigate | `ValueError` client-side 1–500 en `__post_init__`, antes de construir el spec y antes de cualquier HTTP (ASVS V5). Testeable: los casos vacío / 501 / boundary de la Task 2. |
| T-26-11 | Tampering | claves de wire equivocadas (camelCase vs snake_case) ignoradas silenciosamente por el servidor | mitigate (parcial) | Los tres modelos emiten snake_case verbatim de la OpenAPI en vivo (re-fetcheada 2026-07-31); los tests assertan el dict completo clave por clave. Confirmación final contra develop en Phase 27 (LIVE-MUT-01). |
| T-26-12 | Tampering | horario custom parcial de un feriado interpretado distinto por dropear la clave vs. mandar `null` | accept | La OpenAPI documenta `null = configured default` y el default del campo también es `None` → mismo efecto semántico (assumption A3 de 26-RESEARCH.md). Se revalida en vivo en Phase 27; el costo de equivocarse es un feriado con horario default en develop, reversible. |
| T-26-SC | Tampering | instalaciones npm/pip/cargo | accept | Phase 26 no instala ningún paquete — código aditivo puro sobre stdlib + deps ya en `uv.lock` (26-RESEARCH.md § Package Legitimacy Audit: no aplica). |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client/tests -q` en verde (baseline 191 tests, +N nuevos).
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` limpios.
- `uv run mypy packages/market-data-client/src` sale 0 (el gate global `uv run mypy` NO cubre este paquete — 26-RESEARCH.md X6).
- `python -c "import market_data_client"` importa sin error (prueba que el import nuevo de `_params` en `models.py` no introdujo ciclo).
- Grep de disciplina: en `models.py`, ninguna de las tres clases nuevas aparece con `(SafeModel)` en su línea `class`.
</verification>

<success_criteria>
La capa de serialización de MUT-MD-02 está completa: tres request-models frozen que emiten las
claves y defaults verbatim de la OpenAPI en vivo, con `confirm` expuesto y defaulteado a `False`
(ROADMAP SC#2), ruteo por `_params.drop_none` (ROADMAP SC#3) y el bound 1–500 cortando
client-side con `ValueError` pelado; tests unitarios verdes y los gates del paquete limpios.
</success_criteria>

<output>
Create `.planning/phases/26-calendar-write/26-01-SUMMARY.md` when done.
</output>
