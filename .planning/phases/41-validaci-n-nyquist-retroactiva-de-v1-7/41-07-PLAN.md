---
phase: 41
plan: 07
type: execute
wave: 3
depends_on: ["41-02", "41-03", "41-04", "41-05", "41-06"]
files_modified:
  - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md
  - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md
  - .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md
  - .planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md
  - .planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md
  - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md
  - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "El conteo por disposición cierra contra el total enumerado de 62 filas, con cero filas sin disponer y cero filas con dos disposiciones"
    - "Ningún nyquist_compliant pasó a true, y la única fase que conserva ítems no re-verificables lo dice en su propio front-matter"
    - "Cero locks nuevos quedaron en disco; el conteo de locks preexistentes sin enrolar queda declarado inerte por escrito con su enrolamiento ruteado a la Phase 45"
    - "El alcance quedó en las cinco fases nombradas: exactamente cinco artefactos de validación cambiaron y la fila de alcance excluido sigue byte-intacta"
    - "El invariante de árbol congelado se sostuvo desde antes del primer artefacto hasta después del último"
  artifacts:
    - path: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md"
      provides: "Índice de lectura secundario: aritmética de las 62 filas, tabla de enforcement, declaración inerte ruteada a la Phase 45, contención de alcance"
      min_lines: 80
      contains: "62"
    - path: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md"
      provides: "Auto-disposición de la propia Phase 41 con la misma vara que las cinco auditadas (D-10)"
      contains: "## Validation Audit 2026-08-31"
  key_links:
    - from: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md"
      to: ".planning/milestones/v1.7-phases/3[5-9]-*/3?-VALIDATION.md"
      via: "los conteos del rollup se derivan de las cinco tablas de disposición, no se declaran a mano"
      pattern: "VERIFIED-NOW"
    - from: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "la declaración inerte referencia la precondición cross-fase que la Phase 45 consume"
      pattern: "Phase 45"
---

<!-- planner-discipline-allow: NYQUIST-32-33 -->

<objective>
Cerrar la fase probando las cuatro afirmaciones que ninguna auditoría individual puede probar por sí
sola: que la aritmética cierra contra 62, que ningún flag se movió sin evidencia, que no quedó ningún
lock inerte sin declarar, y que el alcance no se ensanchó.

Purpose: cada uno de los cinco planes de la Wave 2 verificó su propia fase. Los criterios 2, 3, 4 y 5
del ROADMAP son afirmaciones **cross-fase**: el conteo total, la ausencia de flips mecánicos en el
conjunto, la inexistencia de locks inertes, y la contención del alcance. Además, seis otros archivos de
validación del repo están en el mismo estado de borrador que las cinco auditadas, y ampliarlas está a
un grep de distancia — la contención hay que probarla, no asumirla.

Output: `41-ROLLUP.md` (índice secundario, no fuente de verdad por D-01), `41-VALIDATION.md`
auto-dispuesto con la misma vara (D-10), y, si algún gate falla, la corrección puntual del artefacto
que lo rompió.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-02-SUMMARY.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-03-SUMMARY.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-04-SUMMARY.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-05-SUMMARY.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-06-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Cerrar la aritmética del criterio 2 y probar el criterio 3 sobre el conjunto de cinco</name>
  <files>
    .planning/milestones/v1.7-phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-VALIDATION.md,
    .planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md,
    .planning/milestones/v1.7-phases/37-matriz-client-dicts-residuales-tipados-alias/37-VALIDATION.md,
    .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md,
    .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 2, 4, 5, 6
    - .planning/ROADMAP.md § Phase 41 → Success Criteria (criterios 2 y 3, textuales)
    - Las cinco secciones `## Validation Audit 2026-08-31` recién escritas, en los cinco archivos de `files`
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Common Pitfalls 1, 2 y 4
  </read_first>
  <action>
    Este es un gate de lectura: no reescribe las cinco secciones de auditoría. Sólo corrige un artefacto
    si un gate falla, y en ese caso corrige el artefacto, nunca el número esperado.

    **Criterio 2 — aritmética.** Para cada uno de los cinco archivos, extraer las filas de la tabla de
    disposición acotando la extracción entre el encabezado de la sección de disposición por fila y la
    línea de leyenda que la cierra; ese acotamiento es obligatorio porque el bloque de métricas, que va
    más arriba, también contiene los tres tokens y sin acotar los conteos se duplicarían. Sobre ese
    rango, contar por archivo: filas totales y ocurrencias de cada uno de los tres tokens. Verificar por
    archivo que el total de filas es 13, 11, 14, 9 y 15 respectivamente, y que la suma de los tres
    conteos de token iguala ese total. Que la suma iguale el total prueba las dos mitades del criterio 2
    a la vez: cero filas sin disponer (si faltara una, la suma sería menor) y cero filas con dos
    disposiciones (si sobrara una, la suma sería mayor). Después sumar cross-fase: el total de filas debe
    dar **62** y el reparto esperado es **54** verificadas ahora, **4** verificadas históricamente y
    **4** no re-verificables. Escribir la aritmética completa, no sólo el resultado.

    **Criterio 2 — anti-vacuidad.** Sobre el mismo rango acotado de los cinco archivos, buscar líneas
    que reporten tests descartados sin reportar ningún test ejecutado. El conteo debe ser cero: una
    salida de pytest que sólo descarta sale con código 5, no imprime ninguna falla, y en un transcripto
    pegado se lee limpia mientras nada corrió. Si aparece alguna, la fila correspondiente está mal
    dispuesta y hay que corregirla en su archivo.

    **Criterio 3 — ausencia de flip mecánico.** Verificar sobre los cinco front-matters que ninguno
    quedó en `nyquist_compliant: true`, que los cinco quedaron en `status: validated`, que los cinco
    llevan la clave entera de ítems no re-verificables, y que la única con valor distinto de cero es la
    de la Phase 39 con valor 4. Verificar además la implicación en la dirección contraria: si algún
    archivo tuviera el flag en verdadero, su tabla debería tener cero filas no re-verificables y cero
    filas con calificador de corrección — como ninguno lo tiene, el gate se reduce a comprobar que el
    conteo de flags en verdadero es cero. Verificar por último que ninguna casilla preexistente del
    bloque de sign-off que menciona poner el flag en verdadero quedó tildada en ninguno de los cinco:
    tildarla retroactivamente es la misma especie de lavado que el criterio 3 prohíbe.

    **Consistencia de SHA.** Verificar que los cinco declaran el mismo commit de v1.7 y el mismo HEAD de
    baseline, ambos copiados del contrato. Cinco valores distintos de HEAD harían el conjunto ilegible.

    Registrar los resultados en un archivo de trabajo temporal o directamente en el material que la
    tarea 3 usa para escribir el rollup; no crear artefactos nuevos bajo `.planning/` en esta tarea.
  </action>
  <verify>
    <automated>T=0; VN=0; VH=0; NVR=0; for n in 35 36 37 38 39; do F=$(ls .planning/milestones/v1.7-phases/$n-*/$n-VALIDATION.md); R=$(awk -v p="^\\\\| $n-[rm][0-9]" '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&$0~p' "$F"); c=$(printf '%s\n' "$R" | grep -c .); a=$(printf '%s\n' "$R" | grep -c 'VERIFIED-NOW'); b=$(printf '%s\n' "$R" | grep -c 'VERIFIED-HISTORICALLY'); d=$(printf '%s\n' "$R" | grep -c 'NOT-VERIFIABLE-RETROACTIVELY'); test $((a+b+d)) -eq "$c" || { echo "MISMATCH $n: rows=$c sum=$((a+b+d))"; exit 1; }; test "$(printf '%s\n' "$R" | grep 'deselected' | grep -vc 'passed')" -eq 0 || { echo "VACUOUS $n"; exit 1; }; grep -q '^status: validated' "$F" || exit 1; grep -q '^nyquist_compliant: true' "$F" && exit 1; grep -q '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" || exit 1; T=$((T+c)); VN=$((VN+a)); VH=$((VH+b)); NVR=$((NVR+d)); done; echo "total=$T VN=$VN VH=$VH NVR=$NVR"; test "$T" -eq 62 && test "$VN" -eq 54 && test "$VH" -eq 4 && test "$NVR" -eq 4</automated>
  </verify>
  <acceptance_criteria>
    - Por archivo, el conteo de filas de la tabla de disposición es `13`, `11`, `14`, `9` y `15`.
    - Por archivo, la suma de los tres conteos de token iguala exactamente el conteo de filas (prueba conjunta de cero sin disponer y cero con dos disposiciones).
    - Cross-fase: filas totales `62`, `VERIFIED-NOW` = `54`, `VERIFIED-HISTORICALLY` = `4`, `NOT-VERIFIABLE-RETROACTIVELY` = `4`.
    - Anti-vacuidad: en los cinco rangos acotados, las líneas que mencionan descartes y no mencionan tests pasados suman `0`.
    - `grep -l '^nyquist_compliant: true' .planning/milestones/v1.7-phases/3[5-9]-*/3?-VALIDATION.md | wc -l` es `0`.
    - `grep -l '^status: validated' .planning/milestones/v1.7-phases/3[5-9]-*/3?-VALIDATION.md | wc -l` es `5`.
    - `grep -h '^not_verifiable_retroactively:' .planning/milestones/v1.7-phases/3[5-9]-*/3?-VALIDATION.md | sort | uniq -c` muestra cuatro con valor `0` y uno con valor `4`.
    - `grep -c '^- \[x\] .*nyquist_compliant: true' ` sobre los cinco archivos suma `0`.
    - Los cinco declaran el mismo commit de v1.7 y el mismo HEAD de baseline: `grep -h '^audited_commit_sha:' ... | sort -u | wc -l` es `1` y lo mismo para `audit_baseline_head`.
  </acceptance_criteria>
  <done>El criterio 2 cierra con aritmética escrita contra 62 y el criterio 3 queda probado sobre el conjunto: cero flags en verdadero, cinco en validated, y el marcador de ítems no re-verificables presente en los cinco con un solo valor distinto de cero.</done>
</task>

<task type="execute">
  <name>Task 2: Probar el criterio 4 (cero locks inertes, ruteo a la Phase 45) y el criterio 5 (contención de alcance), y re-verificar el árbol congelado</name>
  <files>.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 41 → Success Criteria (criterios 4 y 5, textuales) y § Phase 45
    - .planning/REQUIREMENTS.md § Out of Scope y la tabla de dependencias cross-fase de evidencia
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 1 y 7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Common Pitfalls 5 y 7, § Code Examples (los bloques de criterio 4 y criterio 5), § CI Enforcement Map
    - .github/workflows/ci.yml
    - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CENSUS.md (analog de forma para el rollup: párrafo de tesis + sección que fija la unidad de conteo antes de cualquier aritmética)
  </read_first>
  <action>
    **Criterio 4 — cero locks inertes.** El conteo esperado de archivos de test nuevos escritos por esta
    auditoría es **cero**: el criterio 4 es una cláusula de contingencia, no un entregable. Probarlo con
    tres medidas: `git status --porcelain verification/` vacío; el conteo de archivos que calzan el
    patrón de test bajo `verification/` sigue en **52**, idéntico al de antes de la fase; y el archivo de
    workflow de CI quedó sin modificar respecto del HEAD de baseline registrado en la sección 1 del
    contrato. Si por algún motivo un archivo nuevo hubiera aparecido, esa es la señal de que la
    contingencia se disparó: en ese caso hay que declararlo inerte por escrito y rutear su enrolamiento
    al edit consolidado del archivo de workflow de la Phase 45, y decirlo en el SUMMARY como desviación
    de la expectativa.

    Escribir además, en el rollup, la **declaración inerte formal** que el criterio 4 pide para los locks
    **preexistentes**, que es donde ese criterio realmente muerde: de los 52 archivos que calzan el
    patrón de test bajo `verification/`, sólo **12** están en el allowlist explícito del job de lint, así
    que **40** no corren en CI. Un lock que no corre no cuenta como cobertura. Verificar los tres números
    contra el árbol actual antes de escribirlos, y rutear el arreglo por escrito a la Phase 45, citando
    la precondición cross-fase ya registrada en `REQUIREMENTS.md`. La Phase 41 **no** edita el archivo de
    workflow: eso es precisamente lo que la precondición delega.

    **Criterio 5 — contención de alcance.** Probar tres cosas contra el HEAD de baseline del contrato.
    Primero, que exactamente **cinco** artefactos de validación bajo el archivo de fases de v1.7
    cambiaron, y que son los de las fases 35 a 39. Segundo, que **ningún** otro artefacto de validación
    del repo cambió — hay otros seis archivos en el mismo estado de borrador, correspondientes a las
    fases 18, 25, 29, 30, 32 y 33, y ampliar la auditoría a ellos está a un grep de distancia; el gate
    debe fallar si alguno aparece en el diff. Tercero, que la fila de la tabla de alcance excluido de
    `REQUIREMENTS.md` que nombra el gap del milestone anterior sigue presente y que el archivo entero
    quedó sin modificar respecto del baseline. Anotar el matiz que RESEARCH.md midió: no existe ninguna
    entrada de backlog con ese nombre literal en el ROADMAP — la entrada de backlog del ROADMAP es la de
    esta misma fase; el ítem excluido vive únicamente como fila de la tabla de alcance excluido de
    `REQUIREMENTS.md`, que es exactamente donde el criterio 5 dice que está. Verificar esa fila, y no
    salir a buscar una entrada de backlog que no existe.

    **Criterio 1 — invariante final.** Re-ejecutar la prueba de identidad del árbol excluyendo el
    directorio de planificación. Debe salir 0. Éste es el momento que el criterio 1 nombra: "ningún
    archivo fuente de v1.8 cambió antes de que el último de los cinco quedara escrito". Los cinco ya
    están escritos, así que esta corrida cierra la ventana.

    Crear `41-ROLLUP.md` con la forma del analog de censo: abrir con un párrafo de tesis en negrita que
    nombre **la manera en que este documento se lee mal más fácil** —que "54 de 62 verificadas ahora" se
    lea como "las cinco fases están cubiertas", cuando lo que dice es que el bookkeeping estaba stale y
    que ocho filas siguen sin cobertura re-derivable—, seguido de una sección que fije la unidad de
    conteo **antes** de cualquier aritmética. Esa sección fija las 62 filas con su desglose por fase y
    rechaza explícitamente los dos denominadores equivocados: las 51 filas as-declared, que cuentan la
    fila placeholder de la Phase 35 como una unidad, y los 25 criterios de éxito del ROADMAP de v1.7.
    Después: la tabla de aritmética por fase y cross-fase de la tarea 1; la tabla de enforcement con el
    conteo de filas sin cobertura de CI por fase y el total; la declaración inerte con el ruteo a la
    Phase 45; y la sección de contención de alcance con los tres gates y sus resultados. Dejar dicho en
    la cabecera que este archivo es un **índice secundario**, no la fuente de verdad: los artefactos
    autoritativos son los cinco archivos de validación de v1.7. El cuerpo va en español, como el resto de
    los artefactos de v1.7.
  </action>
  <verify>
    <automated>H=$(grep -oE 'audit_baseline_head: [0-9a-f]{40}' .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md | head -1 | awk '{print $2}'); test -n "$H" || exit 1; test -z "$(git status --porcelain verification/)" && test "$(ls verification/test_*.py | wc -l | tr -d ' ')" -eq 52 && git diff --quiet "$H" HEAD -- .github/workflows/ci.yml && git diff --quiet "$H" HEAD -- .planning/REQUIREMENTS.md && grep -Fq 'NYQUIST-32-33' .planning/REQUIREMENTS.md && test "$(git diff --name-only "$H" HEAD | grep -c 'v1\.7-phases/3[5-9]-.*-VALIDATION\.md')" -eq 5 && test "$(git diff --name-only "$H" HEAD | grep -cE 'VALIDATION\.md' )" -eq 6 && test -f .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md && grep -q '62' .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md && grep -q 'Phase 45' .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain verification/` está vacío y `ls verification/test_*.py | wc -l` es `52`.
    - `.github/workflows/ci.yml` no cambió respecto del HEAD de baseline: `git diff --quiet <baseline> HEAD -- .github/workflows/ci.yml` sale 0.
    - El rollup declara los tres números de enforcement verificados contra el árbol actual: 52 archivos que calzan el patrón, 12 enrolados, 40 sin correr, y rutea el arreglo a la Phase 45 citando la precondición de `REQUIREMENTS.md`.
    - Exactamente cinco artefactos de validación cambiaron: `git diff --name-only <baseline> HEAD | grep -c 'v1\.7-phases/3[5-9]-.*-VALIDATION\.md'` es `5`.
    - Ningún otro artefacto de validación del repo cambió salvo el de la propia Phase 41: el conteo total de rutas que terminan en `VALIDATION.md` en el diff es `6`, y `git diff --name-only <baseline> HEAD | grep -cE '(1[8]|2[59]|3[023])-.*-VALIDATION\.md'` es `0`.
    - `grep -Fc 'NYQUIST-32-33' .planning/REQUIREMENTS.md` es `>= 1` y `git diff --quiet <baseline> HEAD -- .planning/REQUIREMENTS.md` sale 0.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 — el invariante del criterio 1 se sostuvo desde antes del primer artefacto hasta después del último.
    - `41-ROLLUP.md` existe, abre con el párrafo de tesis en negrita, fija la unidad de conteo antes de la aritmética, rechaza los dos denominadores equivocados por su número (51 y 25), y se declara índice secundario.
  </acceptance_criteria>
  <done>Los criterios 4 y 5 quedan probados con comandos, no con afirmaciones: cero locks nuevos, la declaración inerte de los 40 preexistentes ruteada a la Phase 45, exactamente cinco artefactos cambiados y la fila de alcance excluido byte-intacta. El invariante del árbol congelado se sostuvo hasta el final.</done>
</task>

<task type="execute">
  <name>Task 3: Auto-disponer la Phase 41 con la misma vara que las cinco auditadas y commitear</name>
  <files>.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md (con el mapa ya realineado por 41-01)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 4, 5, 6
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-CONTEXT.md § D-10
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md (producido en la tarea 2)
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección)
  </read_first>
  <action>
    D-10 dice que `41-VALIDATION.md` no se flippea mecánicamente: sus filas se disponen con la misma
    vara que las cinco que audita. Aplicarla.

    Escribir `## Validation Audit 2026-08-31` al final de `41-VALIDATION.md` con el mismo esqueleto de
    ocho bloques de la sección 5 del contrato, y una tabla de disposición cuyas filas son las 14 tareas
    reales de los siete planes, con clave `41-r01`..`41-r14`. Cada fila lleva su comando de verificación
    real, la salida de esta sesión, y su superficie de enforcement — que para casi todas será la marca
    de no cobertura, porque son aserciones de shell sobre estado de archivos y no corren en CI. Ese
    resultado es honesto y hay que escribirlo, no maquillarlo: la Phase 41 produce documentación, y su
    verificación es de estado de archivo.

    Aplicar la regla R-09 a esta fase también: `nyquist_compliant` pasa a `true` sólo si las 14 filas
    disponen verificadas ahora, sin ninguna histórica, sin ninguna no re-verificable y sin ningún
    calificador de corrección. Calcularlo, no asumirlo. Escribir el valor que la evidencia sostenga y su
    rationale. `status` pasa de `draft` a `validated`. Agregar `not_verifiable_retroactively` con el
    conteo real de esta fase, más `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified`,
    `updated` y `last_audited`, igual que en las cinco.

    Recortar toda salida capturada a su línea de resumen antes de escribirla.

    Commitear los siete archivos tocados por la fase en un solo commit de documentación, usando el seam
    de commit del proyecto: los cinco artefactos de validación de v1.7, más `41-AUDIT-CONTRACT.md`,
    `41-ROLLUP.md` y `41-VALIDATION.md` de esta fase. Un solo commit de docs es lo esperado; si hubiera
    hecho falta un commit separado con archivos de test, eso sería la señal de que la contingencia del
    criterio 4 se disparó, y no debería ocurrir. Antes de commitear, revisar el diff completo buscando
    esquemas de URL, arrobas dentro de hosts, tokens, contraseñas o portadores de credencial que se
    hayan colado en algún transcripto — CLAUDE.md prohíbe exponer credenciales en reportes committeados.
  </action>
  <verify>
    <automated>V=.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md; grep -q '^## Validation Audit 2026-08-31' "$V" && grep -q '^status: validated' "$V" && grep -qE '^not_verifiable_retroactively: [0-9]+' "$V" && grep -q '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7' "$V" && R=$(awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 41-[rm][0-9]/' "$V"); test "$(printf '%s\n' "$R" | grep -c .)" -eq 14 && test "$(( $(printf '%s\n' "$R" | grep -c 'VERIFIED-NOW') + $(printf '%s\n' "$R" | grep -c 'VERIFIED-HISTORICALLY') + $(printf '%s\n' "$R" | grep -c 'NOT-VERIFIABLE-RETROACTIVELY') ))" -eq 14 && test -z "$(git status --porcelain)" && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - `41-VALIDATION.md` tiene la sección `## Validation Audit 2026-08-31` con una tabla de disposición de exactamente 14 filas (`41-r01`..`41-r14`).
    - La suma de los tres conteos de token dentro del rango acotado es exactamente `14`.
    - `grep -c '^status: validated' 41-VALIDATION.md` es `1` y la clave entera de ítems no re-verificables está presente con un valor numérico calculado, no asumido.
    - El valor de `nyquist_compliant` está justificado por escrito contra las tres condiciones de R-09, sea cual sea el valor resultante.
    - `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7' 41-VALIDATION.md` es `1`.
    - Higiene de credenciales sobre el diff completo de la fase: el grep de esquema de URL, arroba dentro de host, token, contraseña o portador devuelve `0` coincidencias fuera de rutas de archivo del repo.
    - El commit incluye exactamente los ocho archivos de la fase (5 de v1.7 + contrato + rollup + validación propia) y ningún `.py`.
    - Después del commit, `git status --porcelain` está vacío y `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
  </acceptance_criteria>
  <done>La propia Phase 41 queda dispuesta con la misma vara que las cinco que audita, y la fase cierra con un único commit de documentación sin ningún archivo de código.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producidos por este plan:
- `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-ROLLUP.md` — **nuevo**. Índice de lectura secundario (D-01: **no** es la fuente de verdad). Contiene: párrafo de tesis sobre cómo se lee mal el resultado; unidad de conteo fijada en 62 filas con desglose 13/11/14/9/15 y rechazo explícito de los denominadores 51 y 25; aritmética por fase y cross-fase (54 / 4 / 4); tabla de enforcement con el conteo de filas sin cobertura de CI por fase y su total; **declaración inerte por escrito** de los 40 locks preexistentes de `verification/` que no corren en CI, con su enrolamiento ruteado al edit consolidado del archivo de workflow de la Phase 45; sección de contención de alcance con los tres gates y sus resultados.
- `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md` — **modificado**. Sección `## Validation Audit 2026-08-31` con las 14 filas de las tareas reales de la fase dispuestas bajo la misma vara (D-10); front-matter con `status: validated`, `not_verifiable_retroactively`, `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified`, `updated`, `last_audited`, y `nyquist_compliant` con el valor que R-09 sostenga y su rationale escrito.
- Corrección puntual de cualquiera de los cinco `{N}-VALIDATION.md` si un gate de la tarea 1 falla (esperado: ninguna).

**Cero ediciones a `.github/workflows/ci.yml`.** El enrolamiento en CI de los 40 locks preexistentes queda **ruteado por escrito a la Phase 45**, no aplicado aquí. Cero archivos `.py` nuevos o modificados en toda la fase.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diff acumulado de la fase → commit permanente | Última oportunidad de detectar una credencial filtrada en un transcripto antes de que quede en la historia del repo |
| conteos derivados de artefactos → afirmación de cumplimiento | Si la aritmética se declarara a mano en vez de derivarse, el rollup podría afirmar una cobertura que las tablas no sostienen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Information Disclosure | commit final de ocho archivos con transcriptos de salida | mitigate | Revisión del diff completo antes de commitear buscando esquema de URL, arroba dentro de host, token, contraseña o portador; criterio de aceptación con conteo cero (ASVS V7; CLAUDE.md) |
| T-41-07-02 | Repudiation | rollup que declara conteos a mano en vez de derivarlos de las cinco tablas | mitigate | El gate de la tarea 1 deriva los conteos por extracción acotada de cada tabla y falla si la suma no iguala el total; el rollup transcribe ese resultado |
| T-41-07-03 | Repudiation | ensanchar el alcance a las otras seis fases en estado de borrador | mitigate | Gate de contención con conteo exacto de artefactos cambiados y un gate negativo sobre las seis fases fuera de alcance (RESEARCH.md Pitfall 7) |
| T-41-07-04 | Repudiation / false assurance | cerrar la fase reportando cobertura total mientras 40 locks preexistentes no corren en CI | mitigate | Declaración inerte por escrito con los tres números verificados y ruteo explícito a la Phase 45 (ASVS V14; RESEARCH.md Pitfall 5) |
| T-41-07-05 | Tampering | editar `.github/workflows/ci.yml` para "cerrar" el hallazgo de enforcement dentro de esta fase | mitigate | Criterio de aceptación que exige que el archivo de workflow quede sin cambios respecto del HEAD de baseline; el arreglo es de la Phase 45 por precondición registrada |
| T-41-07-SC | Tampering | instalaciones de paquetes | accept | Cero: la fase entera no invoca `uv add`, `npm`, `pip` ni `cargo`; `uv.lock` y `pyproject.toml` quedan byte-idénticos |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V5/V6 — el plan lee artefactos, escribe dos
markdown y commitea.
</threat_model>

<verification>
1. Aritmética cross-fase: 62 filas, reparto 54 / 4 / 4, suma por archivo igual al conteo de filas.
2. Cero `nyquist_compliant: true` entre los cinco; cinco en `status: validated`; el marcador de ítems no re-verificables presente en los cinco con un solo valor distinto de cero.
3. Cero locks nuevos; conteo de `verification/` sin cambios; archivo de workflow sin modificar.
4. Exactamente cinco artefactos de validación de v1.7 cambiados; ninguno de las seis fases fuera de alcance; fila de alcance excluido intacta.
5. `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al final de la fase.
6. Un solo commit de documentación, sin archivos `.py`.
</verification>

<success_criteria>
- El conteo por disposición cierra contra las 62 filas enumeradas, con cero filas sin disponer y cero con dos.
- Ningún flag de cumplimiento pasó a verdadero por flip mecánico, y la única fase que conserva ítems no re-verificables lo declara en su propio front-matter.
- Cero locks nuevos en disco; los 40 preexistentes sin enrolar quedan declarados inertes por escrito con su ruteo a la Phase 45.
- El alcance quedó en las cinco fases nombradas y la fila de alcance excluido sigue byte-intacta.
- El árbol de fuente de v1.7 nunca cambió durante la fase.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-07-SUMMARY.md` when done.
</output>
