---
phase: 39-verificaci-n-en-vivo-del-encadenamiento-profundo
plan: 03
type: tdd
wave: 2
depends_on: ["39-01"]
files_modified:
  - verification/run_evidence.py
  - verification/__init__.py
  - verification/test_run_evidence.py
  - verification/test_cycle_closure_phase33.py
  - main_iol.py
  - main_higyrus.py
  - main_matriz.py
  - main_ambito_financiero.py
  - .github/workflows/ci.yml
autonomous: true
requirements: [LIVE-NOBJ-01]

must_haves:
  truths:
    - "Cada uno de los 4 drivers en alcance deja, al terminar, un artefacto de evidencia de corrida con conteo de probes y la lista de triples distintas de handler.seen — D-09/D-10"
    - "El cierre de ciclo devuelve PASS sólo si hay evidencia positiva de que el driver corrió (conteo de probes > 0), no por ausencia de findings — D-09"
    - "Un paquete sin evidencia de corrida se reporta SKIPPED con causa medida y destino nombrado, nunca PASS ni cero silencioso — D-09/SC-1"
    - "La unidad persistida es la 4-tupla (slug, model, field_path, kind) de DivergenceHandler.seen, no el conteo FINDING=N ni los bloques crudos del findings-file — D-10"
    - "verification/test_cycle_closure_phase33.py pasa (2 rojos previos por ruta obsoleta quedan verdes)"
  artifacts:
    - path: "verification/run_evidence.py"
      provides: "Escritura y lectura del sobre de evidencia de corrida por paquete"
      exports: ["write_run_evidence", "read_run_evidence", "run_evidence_path", "probes_executed"]
    - path: "verification/test_run_evidence.py"
      provides: "Contrato del sobre de evidencia: round-trip, cero-probes, ausencia, orden estable de triples"
    - path: ".planning/verification/run-evidence/"
      provides: "Directorio de artefactos de evidencia por slug"
  key_links:
    - from: "main_matriz.py"
      to: "verification/run_evidence.py"
      via: "el loop de cierre de ciclo de 4 paquetes lee la evidencia antes de aceptar PASS"
      pattern: "run_evidence|probes_executed"
    - from: "main_iol.py"
      to: "verification/run_evidence.py"
      via: "escribe el sobre después del bloque divergence_capture, con sorted(handler.seen)"
      pattern: "write_run_evidence"
---

<objective>
Cerrar el PASS vacuo de `verify_cycle_closure` (D-09) y crear la costura que persiste la
unidad del censo (D-10, Pattern 4 de RESEARCH), con un único artefacto que sirve a las dos
cosas: un sobre de **evidencia de corrida** por paquete que lleva el conteo de probes
ejecutados y la lista ordenada de triples distintas de `DivergenceHandler.seen`.

Purpose: hoy `verify_cycle_closure(pkg)` devuelve `(True, [])` tanto cuando todo está
enlazado como cuando el archivo de findings ni existe — el "PASS vacuo" que SC-3 prohíbe. Y
los cuatro drivers imprimen `DIVERGENCES={len(handler.seen)}`, un **conteo**: los miembros
nunca se persisten, así que la diferencia de conjuntos que D-10 exige contra `33-CENSUS.md`
hoy es literalmente incomputable.

Output: `verification/run_evidence.py` + su suite, los cuatro drivers cableados, el loop de
cierre de ciclo de matriz endurecido con el predicado de conteo de probes, y
`verification/test_cycle_closure_phase33.py` repuntado y extendido.

**Decisión de implementación (Open Question 2 de RESEARCH, discreción de D-09):** la costura
de no-vacuidad se implanta en el loop de 4 paquetes que `main_matriz.py` ya tiene
(`ambito-financiero-client`, `iol-client`, `higyrus-client`, `matriz-client`), no en
`verification/cycle_report.py` — tres tests pinean el contrato `(True, [])` de la librería y
editarla los enrojece sin ganancia. Precedente: `main_market_data.py` ya endurece a nivel de
probe, no de librería. El acoplamiento resultante (si matriz no corre, el loop no corre) se
declara explícitamente y se cubre con una tarea de aceptación, no se deja implícito.

**Predicado (corrección explícita al precedente de market-data, Pitfall 6):** el criterio NO
es "existe al menos un finding CONFIRMED/FIXED" — ámbito y higyrus tienen cero y ese
predicado los reprobaría por estar limpios. El criterio es evidencia positiva de corrida:
conteo de probes ejecutados > 0.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-RESEARCH.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-PATTERNS.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `verification/run_evidence.py` — sobre de evidencia de corrida (D-09 + D-10 seam 1)</name>
  <files>verification/run_evidence.py, verification/test_run_evidence.py, verification/__init__.py, .github/workflows/ci.yml</files>
  <read_first>
    - `verification/divergences.py:112-196` — `DivergenceHandler`, su atributo `seen`
      (`set[tuple[str, str, str, str]]` = `(slug, model, field_path, kind)`), el docstring que
      lo declara **la unidad del censo**, y el `title` compuesto en `emit` (metadata de tipo y
      path, nunca un valor de wire).
    - `verification/findings.py` — `findings_path`, `_PKG_SLUG_RE` (guarda de path traversal
      sobre el slug) y `append_finding`. El módulo nuevo debe reutilizar la misma guarda de slug,
      no inventar otra.
    - `verification/cycle_report.py:90-176` — `_regression_is_resolvable` (rechaza rutas
      absolutas y `..`, exige contención bajo la raíz del repo) y `verify_cycle_closure`,
      incluyendo las líneas 142-143 donde el archivo ausente devuelve `(True, [])`.
    - `verification/schema.py:1-40` — idioma del sobre committeado (`endpoint`,
      `client_function`, `captured_at`, `base_url`, `sample_params`, `schema`) para escribir un
      sobre consistente con el resto del harness.
    - `verification/__init__.py:1-70` — el `__all__` ordenado alfabéticamente al que hay que
      agregar los nombres nuevos.
    - `.github/workflows/ci.yml:70-85` — la allowlist explícita.
  </read_first>
  <behavior>
    `write_run_evidence(slug, *, driver, triples, counts, skipped=None) -> Path`:
    - escribe JSON bajo `.planning/verification/run-evidence/<slug>.json` con un sobre que
      contiene: `slug`, `driver`, `captured_at` (ISO-8601 UTC), `counts` (el dict de conteos por
      estado de probe que el driver ya calcula), `probes_executed` (suma de `counts`),
      `n_triples`, `triples` (lista de listas de 4 strings, **ordenada** de forma estable) y
      `skipped` (string de causa medida + destino nombrado, o `null`).
    - reescribe (no append): el sobre describe **la última** corrida, y una corrida saltada debe
      poder invalidar un sobre viejo.
    - slug fuera de la forma permitida ⇒ `ValueError`, nunca escribe fuera del directorio.
    - la salida es determinística: dos llamadas con los mismos triples producen el mismo orden.
    `read_run_evidence(slug) -> dict | None`: `None` si el archivo no existe o no parsea;
    nunca lanza por JSON corrupto.
    `probes_executed(slug) -> int`: `0` si no hay sobre; si lo hay, el entero del sobre.
    Casos de la tabla:
    - sobre ausente ⇒ `read_run_evidence` `None`, `probes_executed` `0`.
    - sobre con `probes_executed = 0` y `skipped` poblado ⇒ `probes_executed` `0` y la causa
      legible desde el sobre.
    - round-trip: escribir 3 triples y leerlas devuelve las mismas 3, ordenadas igual.
    - JSON corrupto ⇒ `read_run_evidence` `None` sin excepción.
  </behavior>
  <action>
    Crear `verification/run_evidence.py` con `from __future__ import annotations` primero,
    `__all__` explícito con los cuatro nombres públicos, y anotaciones completas (mypy strict).
    Sólo stdlib: `json`, `datetime`, `pathlib`, `re`. Cero dependencias nuevas.

    El directorio destino se deriva de `__file__` (nunca de input), igual que `_SCHEMA_DIR` en
    los drivers, y se crea con `mkdir(parents=True, exist_ok=True)`. Reutilizar la guarda de
    forma de slug que `verification/findings.py` ya define en vez de escribir una nueva — un
    segundo regex de slug es una segunda superficie que puede divergir.

    Docstring del módulo: explicar en prosa (a) por qué el sobre existe — los cuatro drivers
    imprimen un conteo de divergencias pero nunca los miembros, y la diferencia de conjuntos que
    exige el censo necesita los miembros; (b) por qué es seguro versionarlo — una triple es
    `(slug, model, field_path, kind)`, cuatro piezas de metadata que ya aparecen verbatim dentro
    de los títulos de findings committeados, sin ningún valor de wire; (c) por qué el conteo de
    probes es el predicado correcto de no-vacuidad y no el conteo de findings promovidos —
    ámbito y higyrus tienen cero findings promovidos por estar limpios / por no haber sido
    medidos, y un predicado basado en findings los reprobaría.

    Escribir `verification/test_run_evidence.py` con la tabla completa de `<behavior>`,
    usando `tmp_path` + `monkeypatch` sobre el directorio del módulo para no tocar
    `.planning/verification/` real. Incluir un test negativo de slug (path traversal) que
    assertee que no se crea ningún archivo fuera del directorio.

    Agregar los cuatro nombres al `__all__` de `verification/__init__.py` (respetando el orden
    alfabético existente) y el re-export correspondiente.

    En el mismo commit, agregar `verification/test_run_evidence.py` a la lista explícita del job
    `test` de `.github/workflows/ci.yml`.
  </action>
  <verify>
    <automated>uv run --frozen python -m pytest -q verification/test_run_evidence.py &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen mypy &amp;&amp; grep -c test_run_evidence.py .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen python -m pytest -q verification/test_run_evidence.py` sale 0.
    - `grep -c 'test_run_evidence.py' .github/workflows/ci.yml` devuelve al menos 1.
    - `uv run --frozen python -c "from verification import write_run_evidence, read_run_evidence, run_evidence_path, probes_executed"` sale 0.
    - `uv run --frozen ruff check .` y `uv run --frozen mypy` salen 0.
  </acceptance_criteria>
  <done>El sobre de evidencia existe, es determinístico, seguro ante slug hostil y está en CI.</done>
</task>

<task type="auto">
  <name>Task 2: cablear los 4 drivers en alcance para que escriban su evidencia de corrida (D-09 + D-10)</name>
  <files>main_iol.py, main_higyrus.py, main_matriz.py, main_ambito_financiero.py</files>
  <read_first>
    - `main_iol.py:2152` (apertura de `with divergence_capture(("iol_client",), ...) as handler:`)
      y `:2248-2265` (cómputo de `counts` y la línea `SUMMARY: ... DIVERGENCES=... HANDLER_ERRORS=...`).
    - `main_higyrus.py:2727` y `:2840-2855` — el mismo par para higyrus.
    - `main_matriz.py:2603` y `:2725-2740` — el mismo par para matriz.
    - `main_ambito_financiero.py:788-845` — el mismo par para ámbito, incluido el comentario que
      explica por qué el cero de ámbito se **mide** y no se asume.
    - `main_higyrus.py` — la rama de vendor inalcanzable que dejó el plan 39-01 (globals nuevas +
      la línea SKIPPED + `sys.exit(0)` en `main()`).
    - `main_matriz.py` — el gate D-MATZ-33 que dejó el plan 39-01 (línea SKIPPED + `sys.exit(0)`
      antes de `write_findings`).
    - `verification/run_evidence.py` — recién creado en la Task 1.
    - **NO leer ni tocar `main_market_data.py`**: D-07 lo deja explícitamente fuera de alcance;
      ya cumplió su parte en Phase 36 y el loop de cierre de ciclo no lo incluye.
  </read_first>
  <action>
    En cada uno de los cuatro drivers en alcance (`main_iol.py`, `main_higyrus.py`,
    `main_matriz.py`, `main_ambito_financiero.py`), después de que `main()` computa el dict de
    conteos y emite la línea `SUMMARY`, agregar una llamada a `write_run_evidence` con: el slug
    del paquete (`_PKG`), el nombre del driver, `sorted(handler.seen)` como triples, y el dict de
    conteos. `handler` sigue ligado después del bloque `with divergence_capture(...)`, así que la
    llamada va fuera del bloque, junto al `SUMMARY`. Importar el nombre desde `verification`
    (import absoluto, sin relativos — TID).

    En los **dos caminos de skip** que dejó el plan 39-01, escribir también el sobre, con
    `triples` vacío, conteos vacíos y `skipped` poblado con la causa medida y el destino
    nombrado — el mismo texto que la línea de stdout, sin la base URL ni el hostname:
    - `main_matriz.py`: en la rama del gate D-MATZ-33 fuera del allowlist, antes de `sys.exit(0)`,
      con destino `LIVE-MATZ-33`.
    - `main_higyrus.py`: en la rama de vendor inalcanzable, antes de `sys.exit(0)`, con destino
      `LIVE-HIGY-33`.
    Esto es lo que impide que un sobre viejo de una corrida anterior se lea como evidencia de
    esta: una corrida saltada **invalida** el sobre en vez de dejarlo en pie.

    Crear `.planning/verification/run-evidence/.gitignore` **no**: los sobres se versionan como
    el resto de `.planning/verification/`. Verificar en su lugar que no exista una regla de
    `.gitignore` que los excluya; si la hay, ajustarla para que el directorio nuevo quede
    versionado.

    No cambiar la forma ni el contenido de la línea `SUMMARY` de ningún driver: hay tests que la
    transcriben verbatim (`verification/test_cycle_closure_phase33.py` assertea la línea de
    ámbito palabra por palabra).
  </action>
  <verify>
    <automated>uv run --frozen python -m pytest -q verification/test_run_evidence.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_main_verify_classification.py &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen mypy &amp;&amp; grep -lc write_run_evidence main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -l write_run_evidence main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py`
      lista los cuatro archivos.
    - `grep -c write_run_evidence main_matriz.py` devuelve al menos 2 (camino normal + camino de skip);
      `grep -c write_run_evidence main_higyrus.py` devuelve al menos 2, por la misma razón.
    - `git grep -n write_run_evidence main_market_data.py` no devuelve nada (D-07: market-data no se toca).
    - `uv run --frozen python -m pytest -q verification/test_cycle_closure_phase33.py` no empeora
      respecto del baseline previo (sigue con los mismos 2 rojos; la Task 3 los cierra).
    - `uv run --frozen ruff check .`, `ruff format --check .` y `mypy` salen 0.
  </acceptance_criteria>
  <done>
    Los cuatro drivers en alcance persisten su evidencia de corrida, incluidos los dos caminos de skip.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: cierre de ciclo no-vacuo por evidencia positiva de corrida (D-09) + repunte de `test_cycle_closure_phase33.py`</name>
  <files>main_matriz.py, verification/test_cycle_closure_phase33.py, .github/workflows/ci.yml</files>
  <read_first>
    - `main_matriz.py:2651-2684` — el loop de cierre de ciclo sobre los cuatro slugs
      (`ambito-financiero-client`, `iol-client`, `higyrus-client`, `matriz-client`): hoy sólo
      llama `verify_cycle_closure(pkg)`, arma `"PASS"` / `"FAIL"` y, si falla, escribe un finding
      `ERROR-MAP OPEN`. **Este es el sitio a endurecer.**
    - `main_market_data.py:3375-3412` — `probe_cycle_closure`: la **estructura** a copiar
      (try / `_finding_for_exc` / PASS con detalle / finding + FAIL). Copiar la estructura,
      **no** el predicado: su criterio de "cero findings CONFIRMED/FIXED ⇒ FAIL" reprobaría a
      ámbito (0) y a higyrus (0) por estar limpios (Pitfall 6 de RESEARCH).
    - `verification/cycle_report.py:123-176` — `verify_cycle_closure` y las líneas 142-143 donde
      el archivo ausente devuelve `(True, [])`. **Archivo de sólo lectura**: tres tests pinean su
      contrato actual.
    - `verification/test_cycle_closure_phase33.py` completo — en particular `_CENSUS` (`:83-89`,
      apuntando a `.planning/phases/33-…/33-CENSUS.md`, ruta obsoleta: el archivo fue archivado a
      `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md`),
      `_PRE_PHASE_BASELINE` / `_PHASE_33_PROMOTIONS` / `_LOWER_BOUND`,
      `_assert_zero_contribution_is_argued` (`:191-254`) y
      `test_the_two_exemptions_are_the_only_ones` (`:257-268`).
    - `verification/run_evidence.py` — `probes_executed` y `read_run_evidence`, de la Task 1.
  </read_first>
  <behavior>
    Para cada uno de los cuatro slugs del loop, con el sobre de evidencia como entrada:
    - sobre ausente, o `probes_executed == 0` ⇒ resultado `SKIPPED` con detalle que nombra la
      causa medida (leída del campo `skipped` del sobre si existe, o "sin evidencia de corrida")
      **y** el destino: `LIVE-HIGY-33` para higyrus, `LIVE-MATZ-33` para matriz, `LIVE-NOBJ-01`
      para el resto. **No** se escribe finding en este camino: no correr no es un defecto.
    - `probes_executed > 0` y `verify_cycle_closure(pkg)` devuelve `ok` ⇒ `PASS`, con detalle que
      incluye el conteo de probes y el `captured_at` del sobre (la evidencia positiva, transcribible
      al censo).
    - `probes_executed > 0` y `verify_cycle_closure(pkg)` devuelve no-`ok` ⇒ `FAIL` + el finding
      `ERROR-MAP OPEN` de siempre, con la lista de regresiones faltantes. Comportamiento actual
      preservado.
    - Cero findings promovidos **no** produce `FAIL` por sí solo: un paquete limpio que corrió
      da `PASS`.
  </behavior>
  <action>
    En `main_matriz.py`, reescribir el cuerpo del loop de cierre de ciclo según la tabla de
    `<behavior>`: consultar `probes_executed(pkg)` (y `read_run_evidence(pkg)` para la causa y el
    `captured_at`) **antes** de decidir; mapear el destino nombrado con un dict de módulo de tres
    entradas explícitas más un default. Mantener el nombre de cada `ProbeResult`
    (`cycle_closure_<slug con guiones bajos>`) sin cambios: los findings downstream están keyeados
    por nombre de probe. Mantener también el orden de los cuatro slugs.

    Documentar en el docstring/comentario del loop, en dos frases, el acoplamiento que esta
    costura introduce: el cierre de ciclo de los cuatro paquetes vive dentro del driver de matriz,
    así que si matriz sale por el gate D-MATZ-33 el loop no corre y **ningún** paquete recibe
    veredicto de cierre. Registrar que en ese caso el censo de la fase debe declarar
    `cycle_closure: NO CORRIÓ — LIVE-MATZ-33` para los cuatro, nunca un silencio que se lea como
    limpio.

    En `verification/test_cycle_closure_phase33.py`: (a) repuntar `_CENSUS` a
    `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md`
    — esto vuelve verdes los 2 rojos actuales; (b) extender el archivo, usando sus propios
    idiomas (`_PRE_PHASE_BASELINE` / `_PHASE_33_PROMOTIONS` / `_LOWER_BOUND`,
    `_assert_zero_contribution_is_argued` y `test_the_two_exemptions_are_the_only_ones`), con
    tests nuevos que pinean el predicado de no-vacuidad: que la decisión de cierre se toma sobre
    evidencia positiva de corrida y no sobre el conteo de findings promovidos, y que un paquete
    limpio que corrió obtiene `PASS` mientras que un paquete sin evidencia obtiene `SKIPPED` con
    destino nombrado. Estos tests trabajan sobre el módulo `verification/run_evidence.py` y sobre
    `main_matriz` parseado por `ast` (predicado presente en el loop), no sobre una corrida real.

    Si el archivo ya está en la allowlist de `.github/workflows/ci.yml`, no duplicar la entrada;
    si no lo está, agregarlo en este mismo commit.
  </action>
  <verify>
    <automated>uv run --frozen python -m pytest -q verification/test_cycle_closure_phase33.py verification/test_run_evidence.py &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen mypy</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen python -m pytest -q verification/test_cycle_closure_phase33.py` sale 0
      (baseline previo: 2 failed).
    - `uv run --frozen python -m pytest -q verification/test_cycle_closure_market_data.py` sigue
      saliendo 0 — la señal de que `verification/cycle_report.py` no se editó.
    - `git diff --name-only` de este commit NO incluye `verification/cycle_report.py`.
    - `grep -c 'LIVE-MATZ-33' main_matriz.py` devuelve al menos 2 (gate + loop de cierre).
    - `uv run --frozen ruff check .`, `ruff format --check .` y `mypy` salen 0.
  </acceptance_criteria>
  <done>
    El cierre de ciclo devuelve PASS sólo con evidencia positiva de corrida, SKIPPED con destino
    nombrado sin ella, y FAIL sólo por regresiones faltantes reales.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `slug` (input) → ruta de escritura del sobre | Un slug controlado podría escribir fuera del directorio de artefactos |
| sobre de evidencia → repo versionado | Artefacto público permanente que el censo cita |
| findings-file → `verify_cycle_closure` (`Regression:`) | Valor de archivo interpretado como ruta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-09 | Tampering | `run_evidence_path(slug)` | mitigate | Directorio derivado de `__file__`, nunca de input; guarda de forma de slug reutilizada de `verification/findings.py` (`_PKG_SLUG_RE`), no re-escrita; test negativo explícito de path traversal |
| T-39-10 | Information Disclosure | contenido del sobre versionado | mitigate | El sobre lleva sólo 4-tuplas `(slug, model, field_path, kind)` — metadata que ya aparece verbatim en títulos de findings committeados — más conteos y timestamps. Ningún valor de wire, ninguna base URL, ninguna credencial. Prohibido agregar campos que transporten payload |
| T-39-11 | Tampering | ruta `Regression:` de un finding | mitigate (sin cambio) | `_regression_is_resolvable` (`cycle_report.py:90-120`) rechaza rutas absolutas y `..` y exige contención bajo la raíz del repo. El endurecimiento de D-09 envuelve `verify_cycle_closure`, no lo rodea: esa guarda sigue en el camino |
| T-39-12 | Repudiation | sobre viejo leído como evidencia de esta corrida | mitigate | Los dos caminos de skip **reescriben** el sobre con `probes_executed = 0` y causa medida, en vez de dejar el sobre anterior en pie; `captured_at` viaja al detalle del `ProbeResult` y de ahí al censo |
| T-39-SC | Tampering | instalaciones de paquetes | accept | Cero dependencias nuevas; sólo stdlib |
</threat_model>

<verification>
- El sobre de evidencia se escribe en las tres situaciones: corrida normal, skip de matriz por
  allowlist, skip de higyrus por DNS.
- `verification/cycle_report.py` queda byte-idéntico.
- `main_market_data.py` queda byte-idéntico (D-07).
- `verification/test_cycle_closure_phase33.py` pasa de 2 rojos a verde.
</verification>

<success_criteria>
- `uv run --frozen python -m pytest -q verification/test_run_evidence.py verification/test_cycle_closure_phase33.py verification/test_cycle_closure_market_data.py verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py` sale 0.
- `uv run --frozen ruff check . && uv run --frozen ruff format --check . && uv run --frozen mypy` sale 0.
- Los cuatro drivers en alcance escriben evidencia; `main_market_data.py` sin cambios.
</success_criteria>

## Artifacts this phase produces

Creados por **este plan**:

| Símbolo / artefacto | Tipo | Ubicación |
|---|---|---|
| `write_run_evidence` | función pública | `verification/run_evidence.py` |
| `read_run_evidence` | función pública | `verification/run_evidence.py` |
| `run_evidence_path` | función pública | `verification/run_evidence.py` |
| `probes_executed` | función pública | `verification/run_evidence.py` |
| 4 nombres nuevos en `__all__` | re-export | `verification/__init__.py` |
| `test_run_evidence.py` | archivo de test nuevo | `verification/` |
| `.planning/verification/run-evidence/<slug>.json` × 4 | artefacto de corrida versionado | `.planning/verification/run-evidence/` |
| dict de destinos nombrados por slug | constante de módulo | `main_matriz.py` |
| predicado de no-vacuidad en el loop de cierre de ciclo | rama de código | `main_matriz.py` |
| tests nuevos de no-vacuidad + `_CENSUS` repuntado | extensión de test | `verification/test_cycle_closure_phase33.py` |
| 1 entrada nueva en la allowlist del job `test` | config | `.github/workflows/ci.yml` |

Artefactos de la fase completa (para contexto): ver el mismo encabezado en `39-08-PLAN.md`.

<output>
Crear `.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-03-SUMMARY.md` al terminar.
</output>