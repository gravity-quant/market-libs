---
phase: 21-market-data-lectura-modelos
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - packages/market-data-client/src/market_data_client/aio.py
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/tests/test_with_options_async.py
  - packages/market-data-client/tests/test_async_client.py
autonomous: true
requirements: [MD-01]

must_haves:
  truths:
    - "AsyncClient.get_market_data / get_latest / get_latest_batch dispatch authenticated async reads and return SafeModel results"
    - "AsyncClient.with_options(max_retries=N) is a shared-view clone whose calls use max_attempts = N+1"
    - "Async Authorization header WINS over spec.headers (D-09 fix) — token is never shadowed"
    - "Async authenticated 401 → re-auth exactly once → retry → succeed; persistent 401 re-raises"
    - "models + LatestRequest + new read methods are re-exported from __init__.py"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/aio.py"
      provides: "async with_options + _validate_max_retries + slots + D-09 fix + 3 read methods + max_attempts threading + async shims"
      contains: "def with_options"
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "re-exports of MarketDataSnapshot/MarketDataEntry/LatestRequest + new sync read methods"
      contains: "LatestRequest"
    - path: "packages/market-data-client/tests/test_with_options_async.py"
      provides: "async retry-propagation-by-request-count test"
      contains: "def test_"
    - path: "packages/market-data-client/tests/test_async_client.py"
      provides: "D-10 async 401 sequences + D-09 header-precedence regression + end-to-end async serialization"
      contains: "def test_"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/aio.py"
      to: "packages/market-data-client/src/market_data_client/_transport.py"
      via: "req.extensions['max_attempts'] = self._max_retries + 1 consumed by AsyncRetryTransport"
      pattern: "extensions\\[.max_attempts.\\]"
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "barrel re-export of MarketDataSnapshot/MarketDataEntry/LatestRequest"
      pattern: "from market_data_client.models import"
---

<objective>
Mirror the Plan-03 sync surface onto the async `AsyncClient` (`aio.py`): the D-08
`with_options` shared-view clone, `_max_retries`/`_is_view` slots + `_validate_max_retries`,
view-aware `aclose()` no-op, `max_attempts` threading in BOTH `_request` and
`_send_auth_request`, and the three async read methods + async module shims. Apply the D-09 fix
— reorder the async authenticated header build so the fresh token WINS over `spec.headers`
(`aio.py:236`). Wire `__init__.py` re-exports (models + `LatestRequest` + new sync read methods).
Add async D-10 401 tests, the async retry-propagation-by-count test, and the D-09
header-precedence regression.

Purpose: The CLAUDE.md dual sync/async constraint requires every Plan-03 logic change to land on
`aio.py` too; a one-sided change fails success criterion 3 on the async surface. D-09 hardens
async so a stray `spec.headers["Authorization"]` can never shadow the token. Output: extended
`aio.py`, extended `__init__.py`, `tests/test_with_options_async.py`, extended
`tests/test_async_client.py`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-market-data-lectura-modelos/21-CONTEXT.md
@.planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md
@.planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md
@.planning/phases/21-market-data-lectura-modelos/21-03-SUMMARY.md
</context>

## Artifacts this phase produces

See `21-01-PLAN.md` "Artifacts this phase produces" for the full symbol list. This plan
creates/modifies (async surface + barrel): `with_options`, `_validate_max_retries`,
`_max_retries`+`_is_view` slots, `get_market_data`/`get_latest`/`get_latest_batch` (async method +
async shim), the D-09 header reorder, and `max_attempts` threading in `aio.py`; `__init__.py`
re-exports; `tests/test_with_options_async.py`; extends `tests/test_async_client.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing async with_options + D-10 401 + D-09 header tests (RED)</name>
  <read_first>
    - packages/market-data-client/tests/test_async_client.py:22-40 (_BASE/_TOKEN_URL + _token_posts + async anonymous assertion shape)
    - packages/market-data-client/tests/test_with_options.py (Plan 03 — the sync retry-count test to mirror)
    - packages/market-data-client/tests/conftest.py (async autouse fixture; force token_expires_at=0.0 for the grant/401 path)
    - .planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md (§Pitfall 3 header precedence, §Pitfall 5 assert-by-count)
  </read_first>
  <behavior>
    - test_with_options_async.py: aio.AsyncClient().with_options(max_retries=5) meeting 6 transient 503 responses on GET /marketdata issues exactly 6 outgoing marketdata requests; max_retries=0 issues exactly 1. Assert by COUNT.
    - test_with_options_async.py: view shares parent _state; view.aclose() does NOT close the parent transport; with_options(-1) / with_options(True) raise ValueError.
    - test_async_client.py: authenticated async get_market_data with a 401 then 200 → returns the 200 result with exactly ONE token POST after the 401.
    - test_async_client.py: two consecutive 401s → raises MarketDataAuthError with exactly ONE re-auth token POST.
    - test_async_client.py (D-09): an authenticated request whose spec carries a decoy Authorization header still sends Authorization == "Bearer <fresh token>" (token wins). This is the regression that sync and async agree.
    - test_async_client.py: successful async get_market_data drops None optionals and encodes bool filters as "true"/"false" in req.url.params.
  </behavior>
  <action>
    Create `packages/market-data-client/tests/test_with_options_async.py` and add cases to
    `packages/market-data-client/tests/test_async_client.py`. Mirror the sync tests using the
    async surface (`aio.AsyncClient`, `await client.get_market_data(...)`) and the existing
    `_token_posts` helper + `_BASE`/`_TOKEN_URL` constants. For the D-09 test, dispatch an
    authenticated read whose builder path can carry a `spec.headers` with a decoy
    `"Authorization"` value (construct a RequestSpec with that header and dispatch via the
    AsyncClient `_request`, or assert at the header-merge level) and assert the SENT
    `Authorization` equals the fresh token, NOT the decoy. Assert by COUNT for the 401 sequences
    (Pitfall 5). Rely on the async autouse conftest fixture; force `token_expires_at=0.0` via
    `aio.configure(...)` only where the grant path must fire.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options_async.py packages/market-data-client/tests/test_async_client.py -q 2>&1 | grep -Eiq "error|failed|attributeerror|no attribute" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/market-data-client/tests/test_with_options_async.py` exists with an async retry-count test asserting `== max_retries + 1`.
    - `test_async_client.py` contains a D-09 test asserting the sent Authorization equals the fresh token even with a decoy spec header, plus D-10 401 cases asserting exactly one re-auth token POST.
    - The suite fails now because async `with_options`/`get_market_data` do not yet exist and the D-09 reorder is not yet applied — confirms RED.
  </acceptance_criteria>
  <done>Async with_options + D-10 401 + D-09 header + end-to-end tests exist and fail against the un-extended async surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror with_options plumbing + D-09 fix + max_attempts threading in aio.py (GREEN)</name>
  <read_first>
    - packages/market-data-client/src/market_data_client/client.py (COMPLETED Plan 03 — the sync reference to mirror verbatim: _validate_max_retries, slots, __init__ kwarg, with_options, close guard, max_attempts threading)
    - packages/iol-client/src/iol_client/aio.py:295-336 (async with_options + _send_auth_request max_attempts threading)
    - packages/market-data-client/src/market_data_client/aio.py (current — __slots__ :67, __init__ :69-99, aclose :112-118, _send_auth_request :164-183, _request :219-276, D-09 header build :236, _DEFAULT_MAX_ATTEMPTS :54)
    - .planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md ("aio.py" section — D-09 FIX quoted)
  </read_first>
  <action>
    In `aio.py`: add a module-level `_validate_max_retries(value)` (define it here too — no
    cross-module import; duplicated per the no-shared-internals constraint). Change
    `AsyncClient.__slots__` from `("_state",)` to `("_is_view", "_max_retries", "_state")`. Add
    `max_retries: int = 2` to `__init__`, call `_validate_max_retries(max_retries)` FIRST, set
    `self._max_retries = max_retries` and `self._is_view = False`. Add
    `with_options(self, *, max_retries: int) -> Self` identical to sync: validate, `__new__`,
    `view._state = self._state` (SHARE — the shared `asyncio.Lock`s already live on `_state`, so
    the view inherits the SAME locks — no lock-hoisting needed), `view._max_retries = max_retries`,
    `view._is_view = True`; return view. Prepend to `aclose()` the guard
    `if getattr(self, "_is_view", False): return`. Replace the `_DEFAULT_MAX_ATTEMPTS` usage in
    `_ensure_http_client` with `AsyncRetryTransport(max_attempts=self._max_retries + 1)` and REMOVE
    the now-unused `_DEFAULT_MAX_ATTEMPTS` constant + comment. Thread the extension (D-08) in BOTH
    `_request` AND `_send_auth_request`: after `req.extensions["endpoint_name"] = ...` add
    `req.extensions["max_attempts"] = self._max_retries + 1`. D-09 FIX (Pitfall 3): change the
    authenticated header build at `aio.py:236` from `{"Authorization": f"Bearer {token}",
    **(spec.headers or {})}` (spec wins — WRONG) to `{**(spec.headers or {}), "Authorization":
    f"Bearer {token}"}` (token wins — aligns to sync). Only that initial dict-build line changes;
    the 401 re-auth carve-out already sets `req.headers["Authorization"]` directly.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options_async.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `AsyncClient.__slots__` equals `("_is_view", "_max_retries", "_state")` and `_validate_max_retries` is defined module-level in `aio.py`.
    - The authenticated async header build is `{**(spec.headers or {}), "Authorization": f"Bearer {token}"}` (token spread LAST — grep confirms `"Authorization"` appears after the spec.headers spread on that line).
    - Both `_request` and `_send_auth_request` set `request.extensions["max_attempts"]` (grep: appears at least twice in `aio.py`); `_DEFAULT_MAX_ATTEMPTS` no longer appears.
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_with_options_async.py -q` exits 0.
  </acceptance_criteria>
  <done>Async with_options clone, view-aware aclose, max_attempts threading in both dispatch methods, and the D-09 token-wins header reorder are present; async with_options tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add async read methods + async shims + __init__ re-exports (GREEN)</name>
  <read_first>
    - packages/market-data-client/src/market_data_client/aio.py:282-292 (async get_health method shape) and :367-379 (async module shims)
    - packages/market-data-client/src/market_data_client/client.py (Plan 03 sync read methods — mirror their signatures async)
    - packages/market-data-client/src/market_data_client/__init__.py:39-70 (barrel: client import block, aio import, __all__ — keep alpha-sorted; note the `# noqa: E402` after _logging_attach.attach())
    - packages/market-data-client/src/market_data_client/models.py (MarketDataSnapshot, MarketDataEntry, LatestRequest to re-export)
  </read_first>
  <action>
    Add three async methods on `AsyncClient` mirroring `get_health` (build spec → `await
    self._request(spec)` → `_core.parse_*_response(resp)`): `get_market_data`, `get_latest`
    (same kwargs as sync), and `get_latest_batch(self, latest_request)`. Add matching async
    module-level shims (mirror `aio.get_health` at :367-374) delegating to
    `_get_default().<method>`. In `__init__.py`: import `MarketDataEntry`, `MarketDataSnapshot`,
    `LatestRequest` from `market_data_client.models`; add the new SYNC read methods
    (`get_market_data`, `get_latest`, `get_latest_batch`) to the existing
    `from market_data_client.client import (...)` block; add all new public names (models +
    `LatestRequest` + the three sync read shims) to `__all__`, keeping it alpha-sorted. The `aio`
    surface stays importable as `from market_data_client import aio` but its methods are NOT
    flattened into the top-level namespace (package convention — `AsyncClient` is the only aio
    re-export). Preserve the `# noqa: E402` markers on the post-`attach()` import block.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def get_market_data" packages/market-data-client/src/market_data_client/aio.py` and an async `get_market_data` module shim exists.
    - `grep -q "from market_data_client.models import" packages/market-data-client/src/market_data_client/__init__.py` and `LatestRequest`, `MarketDataSnapshot`, `MarketDataEntry` appear in `__all__`.
    - `python -c "import market_data_client; market_data_client.MarketDataSnapshot; market_data_client.LatestRequest; market_data_client.get_market_data"` resolves (run via `uv run --package market-data-client python -c ...`).
    - `uv run --package market-data-client pytest packages/market-data-client -q` exits 0 (full suite incl. async D-10 + D-09 + retry-count).
    - `uv run mypy packages/market-data-client/src` and `uv run ruff check packages/market-data-client` and `uv run ruff format --check packages/market-data-client` all exit 0.
  </acceptance_criteria>
  <done>Async read methods + async shims exist, __init__ re-exports models + LatestRequest + sync read methods, and the full package suite + all four gates are green against the package path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (authenticated, async) | Bearer token injected into every authenticated async read |
| spec.headers → request headers | a stray/decoy Authorization in spec.headers must never shadow the fresh token |

## STRIDE Threat Register (ASVS L1, block-on-high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Spoofing / Elevation | async header build lets spec.headers shadow the fresh token | mitigate | D-09 fix: reorder to `{**(spec.headers or {}), "Authorization": ...}` so the token ALWAYS wins; regression test asserts sent Authorization equals the token even with a decoy spec header. Severity: **high** (auth header integrity) — blocking; mitigated in Task 2 + tested Task 1. |
| T-21-04-02 | Denial of Service | unbounded async retries / auth-server hammering on repeated 401 | mitigate | `_validate_max_retries` bounds N; max_attempts=N+1 idempotent-gated; exactly-once re-auth under the token lock (double-checked). Severity: low. |
| T-21-04-03 | Information Disclosure | Bearer token in async logs on the new read path | mitigate | Existing RedactingFilter + `__repr__` redaction; add no raw-header/token log statements. Severity: low. |
| T-21-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this plan (zero new deps). No checkpoint required. |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client -q` green (full suite: sync + async, with_options both surfaces, D-09, D-10 both surfaces).
- `uv run mypy packages/market-data-client/src` clean (explicit package path).
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` green.
- Manual grep: async authenticated header build spreads `spec.headers` BEFORE the `Authorization` key (token wins); `max_attempts` set in both async dispatch methods.
- `uv run --package market-data-client python -c "import market_data_client; market_data_client.MarketDataSnapshot; market_data_client.LatestRequest"` resolves.
</verification>

<success_criteria>
Async `with_options(max_retries=N)` is a real shared-view clone (retry count verified), the
three async read methods + shims dispatch authenticated reads returning SafeModel results, the
D-09 token-wins header fix is applied and regression-tested, the D-10 async 401 sequences pass,
and `__init__.py` re-exports the models + LatestRequest + sync read methods; all four gates green
against the package path. Dual sync/async parity is complete.
</success_criteria>

<output>
Create `.planning/phases/21-market-data-lectura-modelos/21-04-SUMMARY.md` when done.
</output>
