---
phase: 36
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/tests/test_market_data_chain.py
  - packages/market-data-client/tests/test_models.py
  - packages/market-data-client/tests/test_decode.py
  - packages/market-data-client/tests/test_snapshot_no_data_row.py
  - packages/market-data-client/tests/test_public_surface_market_data.py
autonomous: true
requirements: [NOBJ-MD-01, NOBJ-MD-02]

must_haves:
  truths:
    - "A consumer writes snapshot.market_data.last.price and the expression type-checks under mypy --strict with no type: ignore anywhere in the chain (D-01, D-03)."
    - "That same expression returns a value or None and NEVER raises, for all four payloads of the matrix — the real /marketdata wire item, market_data absent, market_data null, market_data {} — on BOTH the sync Client and the async AsyncClient surface (C-3)."
    - "snapshot.market_data.bids[0].price is reachable when the wire carried a BI array, and snapshot.market_data.bids is [] (never None) in all three empty cases."
    - "A consumer can NAME the returned types: from market_data_client import BookLevel, EntryValue, MarketDataEntries resolves (D-01, RESEARCH Open Question 2)."
    - "snapshot.entries is a list[str] in every case and never None — the Phase 33 widening is revoked for the two chain links and left standing for the two leaves staleness_seconds and note (D-04)."
    - "bool(snapshot.market_data) is False exactly when the container carries nothing and True for the real wire item — the no-data row keeps its full expressive power without a single None (D-07)."
    - "market_data_client.models exposes no mapping machinery, and the package suite is green without it — the walker's nested-model branch took over the job (D-05)."
    - "EDGE boundary (NOBJ-MD-01): a market_data wire key outside the declared ten-key roster BI CL HI LA LO OF OI OP SE TV produces exactly one extra divergence record attributed to MarketDataEntries, never raises even under strict_decode=True, and the ten declared keys produce no record at all (D-02)."
    - "EDGE adjacency (NOBJ-MD-01): the six alias property names last bids offers settlement close open_interest and the ten wire field names are disjoint sets — no alias shadows a slot, and MarketDataEntries still constructs under frozen=True slots=True."
    - "EDGE empty (NOBJ-MD-01): market_data absent, null and {} each yield a container equal to MarketDataEntries.empty() with ZERO divergence records; last settlement close open_interest each answer EntryValue.empty() whose .price is None, and bids offers each answer []."
    - "EDGE ordering (NOBJ-MD-01): bids and offers reproduce the wire array order verbatim, including two levels that compare equal on price — the walker never sorts, dedupes or reorders a list field."
    - "EDGE precision (NOBJ-MD-01): a wire int in a float-declared slot (price on BookLevel and EntryValue, and the HI LO OP TV leaves) arrives widened to float with NO divergence record, because the walker widens before consulting scalar_passthrough; the int-declared slots size and date are NOT widened and stay int."
    - "EDGE adjacency (NOBJ-MD-02): LatestRequest(...).to_dict() omits the entries key when the list is empty and emits it when it holds one or more codes — the literal {\"entries\": []} never reaches the wire, preserving the absent-means-all semantics (D-06)."
    - "EDGE empty (NOBJ-MD-02): MarketDataSnapshot.entries is [] for an absent key, for null and for [], and is [] plus exactly one type divergence for a non-list scalar such as \"not-a-list\" — it never holds None."
    - "EDGE ordering (NOBJ-MD-02): entries reproduces wire order verbatim — [\"OF\", \"BI\"] stays [\"OF\", \"BI\"], never sorted and never deduped."
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "BookLevel, EntryValue, MarketDataEntries, the six aliases, the revoked MarketDataSnapshot annotations, and form A of from_api/empty"
      contains: "class MarketDataEntries(SafeModel)"
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "Package-level re-export of the three new names so a consumer can annotate with them"
      contains: "MarketDataEntries"
    - path: "packages/market-data-client/tests/test_market_data_chain.py"
      provides: "The SC-1 matrix — 4 payloads x 2 surfaces, plus the edge cases above"
      min_lines: 180
    - path: "packages/market-data-client/tests/test_models.py"
      provides: "The migrated field-set / hints / LatestRequest locks and the non-vacuous machinery-absence assertion"
      contains: "mapping_machinery"
    - path: "packages/market-data-client/tests/test_snapshot_no_data_row.py"
      provides: "The no-data row migrated to truthiness semantics rather than deleted (SC-4)"
      contains: "bool(row.market_data)"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/_decode.py"
      via: "MarketDataSnapshot.from_api reaches the walker directly; the nested-model branch now owns market_data"
      pattern: "_decode\\.walk_model"
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "MarketDataSnapshot declares the container as a non-optional nested model"
      pattern: "market_data: MarketDataEntries"
    - from: "packages/market-data-client/src/market_data_client/__init__.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "barrel re-export of the three new names, alphabetical in both the import block and __all__"
      pattern: "MarketDataEntries"
    - from: "packages/market-data-client/tests/test_market_data_chain.py"
      to: "packages/market-data-client/src/market_data_client/aio.py"
      via: "every sync case has an async twin driven through the aio surface (C-3)"
      pattern: "aio\\.(AsyncClient|_get_default)"
  prohibitions:
    - statement: "Do not resolve a red gate by weakening the gate. If mypy reddens, fix the shape — never add a type: ignore to the chain the phase exists to make type-safe. If a test reddens because its subject changed, migrate it to the property it still protects; never delete it to reach green."
    - statement: "Do not rewrite or regenerate the committed live-capture baselines under .planning/verification/schemas/market-data-client/. They are the only record of what the vendor actually sends; to_dict() becomes a lossy model-shaped projection in this plan (Pitfall 9), and feeding it to a snapshot site would overwrite wire evidence with model belief."
    - statement: "Do not reach outside the seven files this plan names to make the phase land. _decode.py, _core.py, client.py, aio.py, pyproject.toml, __version__ and uv.lock are all out of scope; a diff in any of them changes the blast radius the phase declared and, for _decode.py, invalidates a digest pinned across five packages."
---

<objective>
TRACER. Make `snapshot.market_data.last.price` real, end to end, on both surfaces, for the four
payloads the vendor can actually produce — then let nothing else in the phase proceed until that
slice is green.

This plan is the whole of NOBJ-MD-01 and the source half of NOBJ-MD-02: it introduces the three
Null Object classes as a LOCAL copy of the matriz pattern (D-01, D-02, D-03), revokes the Phase 33
widening on the two chain links while leaving it standing on the two leaves (D-04), aligns
`LatestRequest` (D-06), retires the mapping machinery from `models.py` (D-05), documents the
revocation against the checkpoint it revokes (D-08), and migrates every assertion in the suite
that spoke the old dict-and-`None` dialect (D-07).

Purpose: the consumer's chain is the phase goal, stated as an expression. Everything else here
exists to make that expression true, cheap and provable.

Output: three new public model classes with six read-only aliases, a `MarketDataSnapshot` with no
`None` in its chain links, a `models.py` on form A of D-07, and a new `test_market_data_chain.py`
carrying the 4x2 matrix plus the edge cases.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-CONTEXT.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-PATTERNS.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Everything in this table is created or changed by THIS plan except the last row.

| Symbol | Kind | Home |
|--------|------|------|
| `BookLevel` | `@dataclass(frozen=True, slots=True)` `SafeModel` — `price: float \| None = None`, `size: int \| None = None` | `market_data_client.models` |
| `EntryValue` | `@dataclass(frozen=True, slots=True)` `SafeModel` — `price: float \| None = None`, `size: int \| None = None`, `date: int \| None = None` | `market_data_client.models` |
| `MarketDataEntries` | `@dataclass(frozen=True, slots=True)` `SafeModel` container — wire order `BI`, `CL`, `HI`, `LA`, `LO`, `OF`, `OI`, `OP`, `SE`, `TV` | `market_data_client.models` |
| `MarketDataEntries.bids` | read-only `@property` over `BI` → `list[BookLevel]` | `market_data_client.models` |
| `MarketDataEntries.offers` | read-only `@property` over `OF` → `list[BookLevel]` | `market_data_client.models` |
| `MarketDataEntries.last` | read-only `@property` over `LA` → `EntryValue` | `market_data_client.models` |
| `MarketDataEntries.settlement` | read-only `@property` over `SE` → `EntryValue` | `market_data_client.models` |
| `MarketDataEntries.close` | read-only `@property` over `CL` → `EntryValue` | `market_data_client.models` |
| `MarketDataEntries.open_interest` | read-only `@property` over `OI` → `EntryValue` | `market_data_client.models` |
| `MarketDataSnapshot.entries` | annotation `list[str] \| None` → `list[str]`, still required, still in slot 4 | `market_data_client.models` |
| `MarketDataSnapshot.market_data` | annotation `dict[str, Any] \| None` → `MarketDataEntries`, still required, still in slot 5 | `market_data_client.models` |
| `LatestRequest.entries` | `list[str] \| None = None` → `list[str] = field(default_factory=list)` | `market_data_client.models` |
| `packages/market-data-client/tests/test_market_data_chain.py` | NEW test module — the SC-1 4x2 matrix + the eight edge cases | tests |
| `verification/test_main_market_data_deep_chain.py` | NEW AST lock (created by Plan 36-03) | verification |

REMOVED from `market_data_client.models` by this plan: `_mapping_value`, `_apply_mapping_policy`,
`_is_mapping`, `_strip_optional`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — write the SC-1 matrix and the edge cases against a shape that does not exist yet</name>
  <files>packages/market-data-client/tests/test_market_data_chain.py</files>
  <read_first>
    - packages/market-data-client/tests/test_snapshot_no_data_row.py (the exact precedent for this file: module-level payload fixtures with provenance comments, `httpx_mock`, the sync/async twin idiom, and the strict-mode context-manager idiom)
    - .planning/verification/schemas/market-data-client/get-market-data.json (the real wire — the `market_data` object with its ten keys, the `int` prices, the `NoneType` values for `OI` and `TV`)
    - .planning/verification/schemas/market-data-client/get-latest.json (the no-data row — note it carries NO `entries` key at all)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Key Measured Findings" F-1 (the expected value per cell — do NOT re-derive these) and F-3 (the silent int widening)
    - packages/market-data-client/tests/test_null_object.py:143-175 (the `_divergences` / `_records` caplog helpers to copy locally — this monorepo keeps such helpers module-local on purpose)
    - packages/market-data-client/src/market_data_client/_decode.py:417-540 (the walker branches the assertions are measuring: the union-with-None arm, the list arm, the nested-model arm, and the float arm that widens `int` before consulting the passthrough set)
  </read_first>
  <behavior>
    Four payloads x two surfaces, plus the eight edge probes. Every expected value below is
    MEASURED in RESEARCH F-1 / F-3 — assert them, do not recompute them.

    Matrix (each row runs once through the sync `Client` and once through `aio.AsyncClient`):
    - wire real (a `/marketdata` item mirroring `get-market-data.json` items[0]):
      `entries == ["BI"]`, `bool(market_data) is True`, `last.price == 10.0`,
      `bids[0].price == 10.0`, `offers == [BookLevel(price=11.0, size=2)]`,
      `settlement.price == 10.0`, `close.price == 9.0`, `open_interest.price is None`,
      zero divergence records.
    - `market_data` key absent: `entries == ["BI"]`, `bool(market_data) is False`,
      `last.price is None`, `bids == []`, `offers == []`, `settlement.price is None`,
      `close.price is None`, `open_interest.price is None`, zero divergence records.
    - `market_data: null`: same as absent except `entries == []`, zero divergence records.
    - `market_data: {}`: same as absent, zero divergence records.

    Chain safety: for every one of the four rows the full expression
    `snapshot.market_data.last.price` evaluates without raising, and so do `.bids`, `.offers`,
    `.settlement.price`, `.close.price` and `.open_interest.price`.

    Strict mode: each of the four rows also decodes under `strict_decode=True` without raising,
    on both surfaces.

    Edge probes (each its own test, each named for the edge it covers):
    - boundary / roster: a `market_data` carrying an eleventh key outside the declared ten emits
      exactly one `extra` record attributed to `MarketDataEntries`, and does NOT raise under
      `strict_decode=True`; the ten declared keys emit nothing.
    - adjacency / alias: the six alias names and the ten field names are disjoint, and reading
      each alias returns the identical object the wire-named attribute returns.
    - empty: each of the three empty payloads produces a container equal to
      `MarketDataEntries.empty()`.
    - ordering / book: two `BI` levels with the SAME price arrive in wire order, both present,
      neither merged nor reordered.
    - ordering / entries: `["OF", "BI"]` stays `["OF", "BI"]`.
    - precision: a wire `int` price arrives as a `float` with no divergence record; `size` and
      `date` arrive as `int` and are not widened.
    - empty / entries: absent, `null`, `[]` and `"not-a-list"` all land on `[]`, and only the
      last one emits a `type` divergence.
    - adjacency / request: `LatestRequest(symbols=["G"]).to_dict()` has no `entries` key;
      `LatestRequest(symbols=["G"], entries=["BI"]).to_dict()` has `{"entries": ["BI"]}`.
  </behavior>
  <action>
    Create the module. Copy the structural idiom of `test_snapshot_no_data_row.py` wholesale: the
    `from __future__ import annotations` header, the `HTTPXMock` import, the module-level payload
    dicts with a provenance comment naming the committed baseline each was derived from, and the
    sync test followed immediately by its async twin carrying the docstring
    `"""Async twin of :func:`...` (C-3)."""`.

    Drive the sync surface through `market_data_client.client._get_default().get_market_data()` /
    `.get_latest(symbol=...)` and the async surface through `aio._get_default()`; for the strict
    arm use the `market_data_client.Client(base_url=..., token=..., token_expires_at=...,
    strict_decode=True)` context manager and its `async with aio.AsyncClient(...)` mirror, exactly
    as `test_snapshot_no_data_row.py:96-128` does. Synthesise every identifier — never a real
    symbol or account id.

    Prefer `pytest.mark.parametrize` over the four payloads so the matrix reads as a matrix and a
    missing cell is visible. Keep the caplog divergence helpers module-local (copy the projection
    helpers from `test_null_object.py`); do not import them across modules.

    Because the target shape does not exist yet, the module also needs the three new names in
    scope: import `BookLevel`, `EntryValue` and `MarketDataEntries` from `market_data_client` (the
    package barrel, not `models`) — that import is itself part of what the phase must make true,
    and it is what makes this task RED for the right reason.

    Do NOT weaken any assertion to reach an earlier green. This task's success condition is a RED
    run with a comprehensible failure.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_market_data_chain.py -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_market_data_chain.py -q` FAILS, and the failure is an `ImportError` / `AttributeError` naming `MarketDataEntries` — not a syntax error and not a mock-plumbing error.
    - `uv run ruff check packages/market-data-client/tests/test_market_data_chain.py` is clean.
    - `grep -c 'Async twin' packages/market-data-client/tests/test_market_data_chain.py` is at least `4` — the C-3 sync/async pairing is present, not promised.
    - `grep -c 'strict_decode=True' packages/market-data-client/tests/test_market_data_chain.py` is at least `2` — both surfaces have a strict arm.
    - `uv run pytest packages/market-data-client -q --ignore=packages/market-data-client/tests/test_market_data_chain.py` still reports the count Plan 36-01 left behind — the new module breaks nothing existing.
    - The file contains no real symbol or account identifier: every `"symbol"` value is a synthesised placeholder.
  </acceptance_criteria>
  <done>The chain matrix exists, is well-formed, and fails for exactly one reason: the shape it asserts has not been built yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — build the typed container, revoke the widening, and retire the mapping machinery</name>
  <files>packages/market-data-client/src/market_data_client/models.py, packages/market-data-client/src/market_data_client/__init__.py</files>
  <read_first>
    - packages/matriz-client/src/matriz_client/models.py:387-428 (the reference implementation of the Null Object container pattern — copy the PATTERN, never import it; C-2 / D-NO-06)
    - packages/market-data-client/src/market_data_client/models.py:1-130 (module docstring including the CR-03 block at 56-64 that D-08 requires rewritten, the import block at 83-86 that F-5 rewrites, and `models.__all__`)
    - packages/market-data-client/src/market_data_client/models.py:204-270 (`SafeModel.from_api` and `SafeModel.empty` — the two call sites of the retired pass, and the form-B paragraph that D-07 turns into form A)
    - packages/market-data-client/src/market_data_client/models.py:338-450 (`MarketDataSnapshot` with the BREAKING-since-0.5.0 docstring block that D-08 mirrors, its `from_api` override where the `received_at` injection sits ADJACENT to the pass being deleted, and `LatestRequest`)
    - packages/market-data-client/src/market_data_client/models.py:1041-1073 (`HealthAuth` / `Health` — the LOCAL precedent for a non-optional nested-model field and for the live-capture provenance docstring idiom)
    - packages/market-data-client/tests/test_null_object.py:191-209 (`_AliasShaped` — the exact alias shape Phase 35 pinned, to be reproduced six times)
    - packages/market-data-client/src/market_data_client/__init__.py:72-135 (the barrel import block and `__all__`, both in strict alphabetical order)
    - .planning/verification/schemas/market-data-client/get-market-data.json (the provenance source for the ten-key roster)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Pattern 1", "Pattern 2", "Pitfall 3", "Pitfall 5", "Pitfall 6", and the F-5 import delta
  </read_first>
  <behavior>
    After this task the RED module from Task 1 turns GREEN in full, and mypy strict accepts the
    three-level chain with no suppression. Specifically:
    - `MarketDataEntries.empty().last.price` is `None`, not an `AttributeError`.
    - `MarketDataSnapshot.from_api({"received_at": "ignored"}, received_at=42.0).received_at`
      is `42.0` — the injection survives the deletion of its adjacent neighbour (Pitfall 5).
    - `hasattr(market_data_client.models, "_mapping_value")` is `False`, and so are the other
      three machinery names.
    - `market_data_client.models.SafeModel.from_api` reaches the walker and returns, with nothing
      between (form A).
  </behavior>
  <action>
    One source-side change, in `models.py` and the barrel. Order the edits so the module is
    importable at every intermediate point.

    **A. The three classes (D-01, D-02, D-03).** Place them in `models.py` immediately before
    `MarketDataSnapshot`, under a section divider in the `# ---` idiom the module already uses.
    Definition order matters: `BookLevel`, then `EntryValue`, then `MarketDataEntries` — the
    container's `default_factory` reads `EntryValue.empty` at class-body execution time.

    All three are `@dataclass(frozen=True, slots=True)` subclasses of the LOCAL `SafeModel`
    (C-6). Do not copy matriz's decorator, which omits `slots`, and do not copy its base class.
    None of the three declares a `from_api` or an `empty` of its own — they inherit, and that
    inheritance is exactly what keeps WR-03's intersection empty (RESEARCH Open Question 3).
    None of the three carries a `received_at`: they are sub-structures, not first-class snapshots.

    `BookLevel` fields: `price: float | None = None`, `size: int | None = None`.
    `EntryValue` fields: `price: float | None = None`, `size: int | None = None`,
    `date: int | None = None`.
    `MarketDataEntries` fields, in the wire order the capture shows — `BI: list[BookLevel]` with
    `field(default_factory=list)`, `CL: EntryValue` with `field(default_factory=EntryValue.empty)`,
    `HI: float | None = None`, `LA: EntryValue` with the same empty factory, `LO: float | None`,
    `OF: list[BookLevel]` with the list factory, `OI: EntryValue` with the empty factory,
    `OP: float | None`, `SE: EntryValue` with the empty factory, `TV: float | None`.

    Exactly ten fields. Do NOT add matriz's `IV`, `EV`, `NV` or `ACP`: D-02 scopes the roster to
    the keys the Phase 33 capture actually measured, and an eleventh key is designed to arrive as
    a non-fatal `extra` until Phase 39 corrects it in-cycle.

    Give each class a one-line docstring in the module's house style plus a live-capture
    provenance block in the `HealthAuth` idiom, naming
    `.planning/verification/schemas/market-data-client/get-market-data.json` as the source of the
    field set. Do not copy `Health`'s sentence promising a `missing` divergence for an absent
    nested model — that wording predates Phase 35, whose NOBJ-02 policy made the collapse silent.

    **B. The six aliases (D-03).** On `MarketDataEntries` only, declared next to the fields:
    `bids` → `BI` and `offers` → `OF`, both returning `list[BookLevel]`; `last` → `LA`,
    `settlement` → `SE`, `close` → `CL`, `open_interest` → `OI`, all four returning `EntryValue`.
    Plain read-only `@property`, one return statement each, no setter. No caching and no
    `functools.cached_property` — the former is prohibited by D-03, the latter is impossible over
    `slots=True`. Reproduce the shape `_AliasShaped` already pins, docstring idiom included.

    **C. Revoke the widening on the two chain links (D-04).** In `MarketDataSnapshot`, change
    `entries` to `list[str]` and `market_data` to `MarketDataEntries`. Both stay in their current
    positional slot and both stay WITHOUT a default, so `note` does not move and no default masks
    an absent key. `staleness_seconds` and `note` are leaves under D-NO-03 and are NOT touched.

    **D. Rewrite the `MarketDataSnapshot` docstring (D-08).** Keep the existing
    BREAKING-since-0.5.0 block — it is the record of what Phase 33 did — and add the symmetric
    block beneath it, in the same idiom, stating that Phase 36 revokes that widening BY FIELD
    ROLE: the two chain links return to required because a Null Object never needs `None`, while
    the two leaves keep their `| None` because a leaf legitimately has nothing to point at. Name
    the checkpoint being revoked (33-07 Task 1, `fix-shape-now`) and the source plan
    `.future_plans/api-tipada-null-objects.md`. Do not delete the Phase 33 block — this is an
    additive record, not an erasure.

    **E. `LatestRequest` (D-06).** `entries` becomes `list[str]` with `field(default_factory=list)`.
    In `to_dict`, the guard for that key becomes a truthiness test instead of a `None` test, so an
    empty list still omits the key and preserves the current absent-means-all semantics. Leave
    `symbols` and `marketId` untouched. Do not touch `_core.build_latest_request` — that is a
    different `entries` in a different layer (a GET query-param string), and a diff in `_core.py`
    is the signal that Pitfall 8 landed.

    **F. Retire the machinery (D-05).** Delete all four helper definitions and their three call
    sites. `SafeModel.from_api` collapses to: take the sink, walk, construct — form A, three
    lines, with the lock-8 comment about the conditional sink deleted along with the conditional.
    `SafeModel.empty` collapses the same way. In `MarketDataSnapshot.from_api`, delete the CR-03
    comment block and the pass call, and **KEEP the very next line, the `received_at` injection**
    — it is visually wrapped by the comment being deleted and is the single easiest thing in this
    phase to destroy by accident (Pitfall 5).

    **G. Rewrite the prose that describes form B (Pitfall 4).** The module docstring's CR-03
    paragraph and the form-B paragraph inside `SafeModel.empty`'s docstring both describe a pass
    that no longer exists; `empty`'s docstring even announces this retirement in the future tense.
    Rewrite both to state that this package now takes form A, that the nested-model branch of the
    walker owns `market_data`, and that per-package differences are declared policy axes and must
    not be harmonised by adding a no-op pass back.

    **H. Imports (Pitfall 3 / F-5).** Drop the four imports that existed only for the retired
    helpers — the `types` module, and `Union`, `get_args`, `get_origin` from `typing` — and drop
    `fields` from the `dataclasses` import. ADD `field` to that same import: it is not present
    today and `default_factory` needs it. `dataclasses` (for `asdict`), `dataclass`, `Any`,
    `Self` and `cast` all survive.

    **I. Export surface (Pitfall 6, Open Question 2).** Add the three class names to
    `models.__all__` in strict alphabetical position (`BookLevel` after `AddHolidaysResult`,
    `EntryValue` after `DeleteHolidayResult`, `MarketDataEntries` before `MarketDataSnapshot`) —
    this is mandatory, WR-02 derives its expected set from the module itself. Then re-export all
    three from the package barrel: add them to the `from market_data_client.models import (...)`
    block and to the package `__all__`, both in strict alphabetical position (in the package
    `__all__`, `MarketDataEntries` sorts after `MarketDataDecodeError` and before
    `MarketDataError`). Without the barrel re-export a consumer cannot NAME the type the library
    returns, which is the ergonomics half of NOBJ-MD-01.

    Do not touch `_decode.py`. The behaviour this plan relies on — a `null` or absent non-optional
    nested-model field collapsing silently to an empty instance — shipped in Phase 35 and is
    pinned by a digest asserted across five packages.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_market_data_chain.py -q &amp;&amp; uv run mypy packages/market-data-client/src &amp;&amp; uv run ruff check packages/market-data-client &amp;&amp; uv run python tools/check_decode_intactness.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_market_data_chain.py -q` passes in full, including both strict arms and all eight edge cases.
    - `uv run mypy packages/market-data-client/src` prints `Success: no issues found`, and `grep -c 'type: ignore' packages/market-data-client/src/market_data_client/models.py` has not increased over its pre-task value.
    - `uv run python -c "import market_data_client.models as m; assert not any(hasattr(m, n) for n in ('_mapping_value','_apply_mapping_policy','_is_mapping','_strip_optional')); assert m.MarketDataSnapshot.from_api({'received_at':'decoy'}, received_at=42.0).received_at == 42.0; import dataclasses as d; assert len(d.fields(m.MarketDataEntries)) == 10; print('ok')"` prints `ok` — the absence assertion is paired with two positive ones so it cannot pass vacuously.
    - `uv run python -c "import market_data_client as p; print(p.MarketDataEntries, p.BookLevel, p.EntryValue)"` succeeds — the three names are importable from the barrel.
    - `uv run python -c "import dataclasses as d; from market_data_client import MarketDataEntries as M; f={x.name for x in d.fields(M)}; a={'bids','offers','last','settlement','close','open_interest'}; assert f &amp; a == set(); assert f == {'BI','CL','HI','LA','LO','OF','OI','OP','SE','TV'}; print('ok')"` prints `ok` — the alias/field adjacency edge, asserted structurally.
    - `uv run python tools/check_decode_intactness.py` succeeds and reports the digest matching `CANONICAL_DIGEST` — SC-5's "sin mover el hash".
    - `uv run python tools/check_surface_types.py` reports `0 violations`.
    - `git rev-parse HEAD:packages/market-data-client/src/market_data_client/_core.py` is `295ce13c609e6bec98b22b5ccb0ad6ff68ad4162`, `..._decode.py` is `5446832dea23a74cec5cb1e91173edcd42a377a5`, `...client.py` is `7724342d2e0ae886058b22b79fb88c18674d81e4`, `...aio.py` is `c4a75c1d77344cda086a6d7152b22ab854515de4`.
    - `uv run ruff check packages/market-data-client` is clean — no unused import survives the retirement.
  </acceptance_criteria>
  <done>The chain matrix is green on both surfaces, mypy strict accepts the three-level chain unsuppressed, the machinery is gone with the `received_at` injection intact, and the three names are importable from the package.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate every assertion that spoke the old dict-and-None dialect, and lock the retirement non-vacuously</name>
  <files>packages/market-data-client/tests/test_models.py, packages/market-data-client/tests/test_decode.py, packages/market-data-client/tests/test_snapshot_no_data_row.py, packages/market-data-client/tests/test_public_surface_market_data.py</files>
  <read_first>
    - packages/market-data-client/tests/test_models.py:39-194 (the six sites asserting `entries is None` / `market_data is None` / `market_data["BI"]`, the field-set lock, and the two `LatestRequest` cases)
    - packages/market-data-client/tests/test_snapshot_no_data_row.py (whole file — the module docstring to rewrite and the seven assertion sites of D-07)
    - packages/market-data-client/tests/test_decode.py:1218-1309 (the four surviving tests that assert on `MarketDataSnapshot.market_data`)
    - packages/market-data-client/tests/test_public_surface_market_data.py:34-64 (`_NEW_PUBLIC_NAMES` — the curated tuple with one batch per phase)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Pattern 3" and § "Censo de aserciones de forma dict a migrar" (the per-line migration table) and § "Pitfall 2" (the divergence kind CHANGES)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md § "Wave 0 Requirements" (the non-vacuity requirement for the absence assertion)
  </read_first>
  <action>
    Work the migration census line by line. Every one of these tests reddened in Task 2 because
    its SUBJECT changed, not because the code is wrong — so every one gets migrated to the
    property it still protects. None gets deleted.

    **`test_models.py`.** The three tests asserting `entries is None` for an absent / `None` /
    extra-keys payload now assert `entries == []`, and their inline comments citing the Phase 33
    widening are rewritten to cite its revocation. The reconciled-wire test's three `market_data`
    assertions become chained-attribute assertions — the `BI` price reads through
    `.market_data.bids[0].price` and compares against `1.0` not `1`, because the walker widens the
    wire `int` silently (F-3), and the `OI` row becomes a truthiness assertion rather than an
    identity-with-`None` one. The no-data-row test's three assertions migrate the same way, with
    `staleness_seconds is None` LEFT ALONE — it is a leaf and D-04 keeps it nullable. The field-set
    lock keeps its eight-name set unchanged and gains an assertion on the two revoked ANNOTATIONS
    (via the walker's hint cache) so that a future re-widening of `entries` or `market_data` fails
    here. The two `LatestRequest` cases keep passing unedited; add a third that pins the empty-list
    boundary — `entries=[]` omits the key, which is the D-06 semantics.

    Add the non-vacuous machinery-absence lock here, named so `-k mapping_machinery` selects it.
    A bare "the module does not have this attribute" assertion is a vacuous green — it would pass
    against an empty module. Pair it with positive structural assertions in the same test,
    following the 33-07 criterio-4 precedent that a zero floor is declared by structural property
    and never by a `>= 0`: assert in the same body that `MarketDataSnapshot.from_api` still injects
    the client stamp over a decoy payload key, that `MarketDataEntries` declares exactly ten
    fields, and that the `SafeModel` roster reachable by introspection is exactly nineteen classes.

    **`test_snapshot_no_data_row.py` (D-07, SC-4).** Migrate, do not delete — SC-4 says so
    explicitly. The `entries is None` sites become `== []`; the `market_data is None` sites become
    a falsy-truthiness assertion; `staleness_seconds is None` is untouched. The populated-row test
    at the end compares the whole dict against a literal — migrate it to
    `.market_data.bids[0].price == 10.0` (note the widened `10` → `10.0`); RESEARCH flagged it as
    a site D-07 does not name but that must move regardless. Rewrite the module docstring to
    record the revocation: keep the finding-traceability idiom (`F-72` / `F-92` …) that makes the
    file's history readable, keep the record of what 33-07 decided, and add that Phase 36 revokes
    it by field role, citing `.future_plans/api-tipada-null-objects.md`. The wrong-typed test at
    the very end must stay green untouched — that a wrong type is still a divergence and still
    fatal under strict decode is the property the widening was never allowed to amnesty, and it
    survives the revocation intact.

    **`test_decode.py`.** Four tests survive from the old mapping block and all four migrate.
    The `null`-payload test asserts falsy rather than `is None` and keeps its "no record at this
    path" assertion, which RESEARCH verified still holds verbatim. The wrong-typed test is the one
    with a real semantic change: the divergence KIND moves from `type` to `non_dict` and the
    attribution moves from the snapshot to `MarketDataEntries`, because the walker's nested-model
    branch now owns the field. Migrate the expectation to the new kind and record in the SUMMARY
    that the property that matters is preserved — `non_dict` is not an info kind, so a wrong-typed
    `market_data` is still fatal under strict decode. The strict-mode null test keeps its verdict
    and changes only its value assertion. The lock-8 test keeps its record set, which RESEARCH
    verified is unchanged, changes its value assertion, and gets retitled to name the terminal
    record rather than the retired pass; rewrite its docstring accordingly.

    **`test_public_surface_market_data.py`.** Append a new batch to `_NEW_PUBLIC_NAMES` with the
    three class names, under a comment naming this phase and NOBJ-MD-01, replicating the shape of
    the four batches already there. This makes the barrel re-export enforced rather than merely
    recommended.

    Nothing in this task touches `models.py` or the barrel — Task 2 owns those.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client -q 2>&amp;1 | tail -3 &amp;&amp; uv run mypy packages/market-data-client/src packages/market-data-client/tests &amp;&amp; uv run ruff check packages/market-data-client &amp;&amp; uv run python tools/check_decode_intactness.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q` is fully green, and the count is the Plan 36-01 count plus the tests added by `test_market_data_chain.py` plus the two added here (the `LatestRequest` empty-list case and the machinery lock) — no existing test was deleted in this task.
    - `uv run pytest packages/market-data-client/tests/test_models.py -k mapping_machinery -q` passes, and the test body contains at least two POSITIVE structural assertions alongside the absence assertion.
    - `uv run pytest packages/market-data-client/tests/test_snapshot_no_data_row.py -q` is green with the same number of tests it had before this plan — SC-4's "migrado en vez de eliminado".
    - `uv run pytest packages/market-data-client/tests/test_snapshot_no_data_row.py -k wrong_typed -q` passes unedited — a wrong type is still a divergence and still fatal.
    - `uv run pytest packages/market-data-client/tests/test_public_surface_market_data.py -q` is green — the three names are in both `models.__all__` and the package `__all__`.
    - `uv run pytest packages/market-data-client/tests/test_null_object.py -q` is green with three more parametrized cases per parametrized test than before the phase (roster 16 → 19), with no edit to that file in this plan.
    - `uv run mypy packages/market-data-client/src packages/market-data-client/tests` prints `Success: no issues found` twice.
    - `uv run python tools/check_decode_intactness.py` succeeds.
    - `git diff --stat -- .planning/verification/schemas/market-data-client/` is empty — the committed live-capture baselines are untouched (Pitfall 9).
    - `git rev-parse HEAD:packages/market-data-client/pyproject.toml` is `cfb60655efb48907f417efd41b6b28d64499f99f` and `git rev-parse HEAD:uv.lock` is `5c8ea46c0be875d8634a573a3fb06dba78e8cb8e` — no version bump, no lock refresh (D-09).
  </acceptance_criteria>
  <done>The whole package suite is green on the new semantics, every migrated test still asserts a live property, and the retirement is locked by an assertion that cannot pass vacuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor HTTP response → `_decode` walker → model | Untrusted JSON crosses here. This plan STRENGTHENS the boundary: `market_data` moves from an unvalidated `dict[str, Any]` passthrough to ten declared, type-checked fields. |
| divergence record → `market_data_client` logger | Structured records leave the process. This plan adds three new nested paths (`.market_data.LA.price` and friends) to the set of paths that can appear in a record. |
| `to_dict()` projection → any consumer | The projection becomes model-shaped rather than wire-shaped in this plan (Pitfall 9). It must not reach a schema-snapshot site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Information Disclosure | The new nested divergence paths under `.market_data` — market-data payloads carry symbol and account identifiers | mitigate | Reuse the existing sink unchanged. Records carry field path plus declared/observed TYPE NAMES only, never values (T-29-22). The new paths are DECLARED field names, not payload content. Acceptance: no new record-emitting code is added in `models.py`; `git diff` for this plan shows no new call to the sink. |
| T-36-02-02 | Tampering | A hostile wire key inside `market_data` forging a log line or a fake decode path via the `extra` record | mitigate | `_safe_key` (lock 11) normalises undeclared keys to `[0-9A-Za-z_-]` and truncates at 64; `_decode.py` is not modified so the normaliser is intact by the digest gate. The eleventh-key path is exercised explicitly by the boundary edge case in `test_market_data_chain.py`, including under `strict_decode=True`. |
| T-36-02-03 | Denial of Service | Recursion depth grows from 1 to 3 levels over an unbounded `items[]` array | accept | Container width is fixed at ten declared fields and depth at three by declaration, not by payload — a hostile payload cannot deepen it. `_MAX_SCAN_ENTRIES` still bounds the logging path. No user-controlled recursion. |
| T-36-02-04 | Spoofing | `to_dict()` becoming a lossy model-shaped projection that overwrites the committed wire baselines with model belief | mitigate | The schema-snapshot sites feed RAW WIRE via `_raw_via_request_sync` / `_async` and are not touched by this plan. Acceptance criterion: `git diff --stat -- .planning/verification/schemas/market-data-client/` is empty. |
| T-36-02-05 | Elevation of Privilege | A read-surface change silently loosening the mutating gate | accept | No mutation path is touched. The gate's AST guard runs inside the package suite, which is a per-task gate on all three tasks. |
| T-36-02-SC | Tampering | npm / pip / cargo supply chain | accept | This plan installs nothing — RESEARCH § Package Legitimacy Audit records "no aplica". Pinned by blob hash in the Task 3 acceptance criteria: `uv.lock` and `packages/market-data-client/pyproject.toml` must be unchanged (D-09). |
</threat_model>

<verification>
- `uv run pytest packages/market-data-client -q` — fully green.
- `uv run mypy packages/market-data-client/src packages/market-data-client/tests` — `Success: no issues found` for both.
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` — clean.
- `uv run python tools/check_decode_intactness.py` — the five-copy digest still equals `CANONICAL_DIGEST` (SC-5).
- `uv run python tools/check_surface_types.py` — `0 violations`.
- `uv run python tools/check_uniform_structure.py` — clean.
- `git rev-parse HEAD:<path>` for `_core.py`, `_decode.py`, `client.py`, `aio.py`, `pyproject.toml`, `uv.lock` — all equal to the hashes pinned in the Task 2 and Task 3 acceptance criteria.
- `git diff --stat -- .planning/verification/schemas/market-data-client/` — empty.
</verification>

<success_criteria>
- ROADMAP SC-1: the six chained expressions pass `mypy --strict` and do not raise against all four payloads, on both surfaces.
- ROADMAP SC-2: the three classes exist as a local copy of the matriz pattern with the six aliases, and `MarketDataSnapshot.market_data` admits no `None` in its annotation.
- ROADMAP SC-3: the widening is revoked on the two chain links and left standing on the two leaves, with the revocation recorded in the docstring against the checkpoint it revokes.
- ROADMAP SC-4: the no-data row keeps its full expressive power without `None`, and its test file is migrated rather than deleted.
- ROADMAP SC-5 (source half): the mapping machinery and its precondition test are gone from the package without moving the `_decode.py` digest.
- All eight probe-derived edge behaviours are asserted by a named test in `test_market_data_chain.py` or `test_models.py`.
</success_criteria>

<output>
Create `.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-02-SUMMARY.md` when done.
Record: the final package test count; the divergence-kind change for a wrong-typed `market_data`
(`type` → `non_dict`, attributed to `MarketDataEntries`) and the note that it stays fatal under
strict decode; the `SafeModel` roster count moving 16 → 19; that this package moved from form B to
form A of D-07; and the additive semver consequence of the three new public names, so Phase 40 can
absorb it into the migration table.
</output>
