---
phase: 10-matriz-aio-py-creation-tokenstore
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - packages/matriz-client/src/matriz_client/_atransport.py
  - packages/matriz-client/src/matriz_client/aio.py
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/matriz-client/tests/conftest.py
  - packages/matriz-client/tests/test_atransport.py
  - packages/matriz-client/tests/test_async_auth.py
  - packages/matriz-client/tests/test_async_queries.py
  - packages/matriz-client/tests/test_async_mutations.py
autonomous: true
requirements:
  - REFAC-04
must_haves:
  truths:
    - "matriz-client async REST surface exposes the 22 endpoints from `client.Client` with matching signatures via `matriz_client.aio.AsyncClient`"
    - "Module-level `matriz_client.aio.{login, get_segments, get_all_instruments, get_instruments_details, get_instrument_detail, get_instruments_by_cfi, get_instruments_by_segment, new_order, replace_order, cancel_order, get_order_status, get_order_history, get_active_orders, get_filled_orders, get_all_orders, get_order_by_exec_id, get_market_data, get_trades, get_positions, get_detailed_positions, get_account_report, aclose}` are awaitable callables that delegate to `_get_default()`"
    - "AsyncRetryTransport mirrors RetryTransport semantics: retries 408/409/429/≥500 on `idempotent=True`, NO retry on POST/mutating requests, honors `Retry-After` via `await asyncio.sleep` (CancelledError-aware)"
    - "Mutation gate enforced in async: `new_order`/`cancel_order`/`replace_order` requests with `idempotent=False` issue EXACTLY 1 outgoing request even on 503 response (Pitfall 4 — duplicate-order prevention)"
    - "B8 lock-in preserved: `aio._raise_for_response` IS `client._raise_for_response` IS `_core.raise_for_response` (single function object)"
    - "PEP 562 shim back-compat: `matriz_client.aio._token`, `matriz_client.aio._base_url`, `matriz_client.aio._token_expires_at` resolve via `_get_default()._state` (no AttributeError for legacy callers)"
    - "Cross-package public surface snapshot grows ONLY for matriz (AsyncClient + 22 async delegators); iol/higyrus/ambito/wallets snapshots zero-diff at end of Plan"
    - "`assert state.token is not None` post-`_aensure_token` mypy-strict narrowing pattern preserved (matches iol async idiom)"
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_atransport.py"
      provides: "AsyncRetryTransport class mirroring sync RetryTransport semantics (tenacity AsyncRetrying + async for + asyncio.sleep for Retry-After) with D-22 auth_basic logging split"
      contains: "class AsyncRetryTransport"
      min_lines: 120
    - path: "packages/matriz-client/src/matriz_client/aio.py"
      provides: "AsyncClient class with 22 REST endpoints + module-level async delegators + PEP 562 shim back-compat"
      contains: "class AsyncClient"
      min_lines: 500
    - path: "packages/matriz-client/src/matriz_client/__init__.py"
      provides: "Re-export AsyncClient and async configure/login in `__all__` for public API consumption"
      contains: "AsyncClient"
    - path: "packages/matriz-client/tests/conftest.py"
      provides: "autouse `_configure_async` fixture mirror of iol-client conftest pattern for pytest-asyncio testing"
      contains: "_configure_async"
    - path: "packages/matriz-client/tests/test_atransport.py"
      provides: "AsyncRetryTransport unit tests: idempotent retry, mutation gate, Retry-After honor via asyncio.sleep, CancelledError propagation"
      contains: "AsyncRetryTransport"
    - path: "packages/matriz-client/tests/test_async_auth.py"
      provides: "Async auth tests: login, _aensure_token via TokenStore, 401 re-auth-once flow"
      contains: "async def test_"
    - path: "packages/matriz-client/tests/test_async_queries.py"
      provides: "Async query endpoint tests: get_segments, get_instruments_*, get_orders, get_market_data, get_trades, get_positions, get_account_report"
      contains: "async def test_"
    - path: "packages/matriz-client/tests/test_async_mutations.py"
      provides: "Async mutation endpoint tests: new_order/cancel_order/replace_order with idempotent=False mutation gate verification (EXACTLY 1 request on 503)"
      contains: "idempotent"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/aio.py"
      to: "packages/matriz-client/src/matriz_client/_core.py"
      via: "import builders/parsers (B8 D-04 lock-in: raise_for_response imported FROM _core, NOT from client.py)"
      pattern: "from matriz_client._core import"
    - from: "packages/matriz-client/src/matriz_client/aio.py"
      to: "packages/matriz-client/src/matriz_client/_atransport.py"
      via: "_ensure_http_client wires AsyncRetryTransport into httpx.AsyncClient"
      pattern: "AsyncRetryTransport"
    - from: "packages/matriz-client/src/matriz_client/aio.py"
      to: "packages/matriz-client/src/matriz_client/_token_store.py"
      via: "build_token_store factory composed lazily in _aensure_token (Plan 10-03 actually wires _state.token_store; here only the import is added)"
      pattern: "build_token_store"
    - from: "packages/matriz-client/src/matriz_client/_atransport.py"
      to: "packages/matriz-client/src/matriz_client/_transport.py"
      via: "intra-package import of constants (_RETRYABLE_STATUS, _RETRYABLE_EXC, _RetryableStatus, _parse_retry_after, _LOGGER_NAME, _RETRY_AFTER_CAP_S)"
      pattern: "from matriz_client._transport import"
    - from: "packages/iol-client/src/iol_client/aio.py"
      to: "packages/matriz-client/src/matriz_client/aio.py"
      via: "REFERENCE pattern — iol async surface is the canonical shape to mirror (lifecycle, _ensure_http_client, _request, delegators, PEP 562 shim)"
      pattern: "AsyncClient|_get_default|configure|__getattr__"
---

<objective>
Create the full matriz async REST surface (`aio.py` replaces 103-LOC stub with ~550 LOC AsyncClient class + 22 module-level async delegators + PEP 562 shim back-compat) and `_atransport.py` (AsyncRetryTransport — Phase 8 D-25 carve-out closed). This Plan delivers the user-visible async API for matriz-client without touching `client.py` (sync), `ws_client.py`, or `_state.py` — the state-wiring + cross-thread integration is Plan 10-03's atomic unit.

Purpose: REFAC-04 success criterion #1 — `packages/matriz-client/src/matriz_client/aio.py` exists with `AsyncClient` mirroring full REST surface of `client.py` (22 endpoints, mismas signatures), using `_core.py` for builders/parsers (no duplication of `raise_for_response`/`unwrap_envelope`/auth flow), with `_state` async independent of sync. Implements D-01 (4 new files + atomic plan), D-02 (TokenStore consumed via lazy `build_token_store` import in `_aensure_token`), D-03 (`max_retries` propagated to `RefreshPolicy`), D-25 carve-out closed (matriz `_atransport.py` created).

Output: ~8 file changes — 2 NEW src (`_atransport.py`, `aio.py` full replace from stub), 1 MODIFIED src (`__init__.py` adds AsyncClient to `__all__`), 1 MODIFIED test fixture (`conftest.py` adds `_configure_async`), 4 NEW test files (`test_atransport.py`, `test_async_auth.py`, `test_async_queries.py`, `test_async_mutations.py`). Test count delta: +30 tests (per CONTEXT D-07 line 1009). NO sync `client.py` change; NO `_state.py` change; NO `ws_client.py` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-CONTEXT.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-PATTERNS.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-01-SUMMARY.md
@.claude/skills/spike-findings-market-libs/references/tokenstore-3way.md
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/_atransport.py
@packages/iol-client/tests/conftest.py
@packages/iol-client/tests/test_async_client.py
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/aio.py
@packages/matriz-client/src/matriz_client/_core.py
@packages/matriz-client/src/matriz_client/_state.py
@packages/matriz-client/src/matriz_client/_transport.py
@packages/matriz-client/src/matriz_client/_logging.py
@packages/matriz-client/src/matriz_client/__init__.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/src/matriz_client/exceptions.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/ws_client.py
@packages/matriz-client/tests/conftest.py
@packages/matriz-client/tests/test_client.py
@packages/matriz-client/tests/test_client_class.py
@packages/matriz-client/tests/test_transport.py
@verification/snapshots/matriz-client-surface.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create `_atransport.py` (AsyncRetryTransport mirror of sync RetryTransport)</name>
  <files>packages/matriz-client/src/matriz_client/_atransport.py, packages/matriz-client/tests/test_atransport.py</files>
  <read_first>
    - `packages/iol-client/src/iol_client/_atransport.py` (132 LOC) — canonical reference; matriz mirror swaps `iol_client` → `matriz_client` AND adds the `auth_basic` extensions propagation (matriz delta)
    - `packages/matriz-client/src/matriz_client/_transport.py` (240 LOC) — sync RetryTransport with the matriz-specific D-22 `auth_basic` log redaction (lines 162-228); these constants get imported by `_atransport.py` (intra-package coupling allowed at module boundary)
    - `packages/matriz-client/tests/test_transport.py` — sync tests; `_atransport.py` tests mirror the relevant subset (idempotent retry, mutation gate, Retry-After, CancelledError)
    - `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-PATTERNS.md` lines 383-491 — `_atransport.py` pattern assignment
    - `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-CONTEXT.md` D-25 carve-out closure context
    - `packages/matriz-client/src/matriz_client/_logging.py` — RedactingFilter for `matriz_client` logger (no modification; auto-coverage)
  </read_first>
  <behavior>
    - Test T1: `AsyncRetryTransport(max_attempts=2)` is constructible and subclasses `httpx.AsyncHTTPTransport`
    - Test T2: Request with `request.extensions["idempotent"] == True` retries on a 503 response → 2 outgoing requests; final response is the second 503 (or success if mock returns success on retry)
    - Test T3 (MUTATION GATE — Pitfall 4): Request with `request.extensions["idempotent"] == False` against 503 mock → EXACTLY 1 outgoing request, no retry. Verify via `httpx_mock.get_requests()` count.
    - Test T4 (Retry-After honor): 429 response with `Retry-After: 1` triggers ONE `await asyncio.sleep(1.0)` before retry. Capture sleep delays via injected `asyncio.sleep` mock or `freezegun`.
    - Test T5 (Retry-After cap): 429 with `Retry-After: 120` is capped to `_RETRY_AFTER_CAP_S` (60s) before sleep.
    - Test T6 (CancelledError propagation — Pitfall 16, D-32): `asyncio.CancelledError` raised during the transport call propagates to caller (NOT swallowed by tenacity); the test cancels the awaiting task and asserts `pytest.raises(asyncio.CancelledError)`.
    - Test T7 (max_attempts bypass): `AsyncRetryTransport(max_attempts=1)` issues 1 request regardless of status code (no retry attempt).
    - Test T8 (auth_basic log split — D-22): A request with `request.extensions["auth_basic"] = ("user", "pass")` that triggers a WARNING log on retry produces a log record where `record.extra["auth_basic_user"] == "user"` AND `record.extra["auth_basic_password"] == "***"` (redacted). Use `caplog` fixture; assert via `record.__dict__` or LogRecord extras inspection.
    - Test T9 (mypy + ruff): no runtime test; deferred to acceptance criteria.
  </behavior>
  <action>
    Create `packages/matriz-client/src/matriz_client/_atransport.py` (~140-180 LOC) following 10-PATTERNS.md lines 383-491. Key points:

    1. Header: `from __future__ import annotations` + module docstring describing this as the Phase 10 D-25 carve-out closure (mirror of Phase 8 `_transport.RetryTransport` for `httpx.AsyncHTTPTransport`).

    2. Imports:
       ```python
       import asyncio
       import logging
       import time
       from typing import Any

       import httpx
       from tenacity import (
           AsyncRetrying,
           retry_if_exception_type,
           stop_after_attempt,
           wait_exponential_jitter,
       )

       from matriz_client._transport import (
           _LOGGER_NAME,
           _RETRY_AFTER_CAP_S,
           _RETRYABLE_EXC,
           _RetryableStatus,
           _is_retryable_status,
           _parse_retry_after,
       )
       ```
       (intra-package import — see iol `_atransport.py` for the established pattern)

    3. `__all__ = ["AsyncRetryTransport"]`

    4. Class `AsyncRetryTransport(httpx.AsyncHTTPTransport)`:
       - `__init__(self, *, max_attempts: int = 2, **kwargs: Any) -> None`: super init with kwargs; `self._max_attempts = max(max_attempts, 1)`; `self._logger = logging.getLogger(_LOGGER_NAME)`.
       - `async def handle_async_request(self, request: httpx.Request) -> httpx.Response`:
         - **Mutation gate** (D-01 / Pitfall 4): `if not request.extensions.get("idempotent", False): return await super().handle_async_request(request)`
         - **max_attempts bypass** (D-19): `if self._max_attempts <= 1: return await super().handle_async_request(request)`
         - Extract `request_id`, `endpoint_name`, `account_id`, `auth_basic` from `request.extensions`
         - `start = time.monotonic()`, `attempt_number = 0`
         - Wrap retry in `try: async for attempt in AsyncRetrying(stop=stop_after_attempt(self._max_attempts), wait=wait_exponential_jitter(initial=1.0, max=30.0, exp_base=2, jitter=1.0), retry=(retry_if_exception_type(_RETRYABLE_EXC) | retry_if_exception_type(_RetryableStatus)), reraise=True):`
           - Inside `with attempt:`:
             - `attempt_number = attempt.retry_state.attempt_number`
             - `response = await super().handle_async_request(request)`
             - `await response.aread()` (consume body inside the retry frame so connection is released before next attempt)
             - If `_is_retryable_status(response)`:
               - `retry_after = response.headers.get("Retry-After")`
               - If `retry_after is not None`: `delay = _parse_retry_after(retry_after); if delay is not None and delay > 0: await asyncio.sleep(min(delay, _RETRY_AFTER_CAP_S))` (D-32: `asyncio.sleep` is CancelledError-aware)
               - Build `extra: dict[str, Any]` with `package="matriz_client"`, `method`, `url`, `status_code`, `attempt`, `duration_ms`, `request_id`, `endpoint_name`
               - If `account_id is not None`: `extra["account_id"] = account_id`
               - If `auth_basic is not None`: `extra["auth_basic_user"] = auth_basic[0]; extra["auth_basic_password"] = "***"` (D-22 split)
               - `self._logger.warning("retry attempt", extra=extra)`
               - `raise _RetryableStatus(response)` (sentinel — tenacity catches and retries)
             - `return response` (non-retryable status: return early)
         - `except _RetryableStatus as exc:` — exhausted: `return exc.response`
         - `except` any of `_RETRYABLE_EXC`: log ERROR with same `extra` shape (incl. auth_basic_user/auth_basic_password split if applicable) then `raise`

    5. Match iol `_atransport.py` lines 43-131 verbatim where possible; the matriz delta vs iol is ONLY the `auth_basic` propagation (D-22) — iol's version omits it because iol has no Risk API BasicAuth.

    Create `packages/matriz-client/tests/test_atransport.py` (~5-9 tests, mirror sync `test_transport.py` cases). Use:
    - `pytest-httpx` `httpx_mock` for HTTP mocking
    - `pytest.mark.asyncio` (or rely on `asyncio_mode = "auto"`)
    - Construct `httpx.AsyncClient(transport=AsyncRetryTransport(max_attempts=2))` for each test
    - For T4/T5 use `monkeypatch.setattr("matriz_client._atransport.asyncio.sleep", ...)` with a list to capture invocations
    - For T6 (CancelledError): wrap the request in a task, `task.cancel()`, then `await task` expecting `asyncio.CancelledError`
    - For T8 (auth_basic redaction): build request with `req.extensions["auth_basic"] = ("u", "p")`, `req.extensions["idempotent"] = True`, mock 503 → 200 sequence; assert via `caplog` that the WARNING record has `auth_basic_user="u"` and `auth_basic_password="***"` AND that NO record contains the substring `"p"` matching the password
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_atransport.py -q && uv run mypy --strict packages/matriz-client/src/matriz_client/_atransport.py && uv run ruff check packages/matriz-client/src/matriz_client/_atransport.py packages/matriz-client/tests/test_atransport.py</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l packages/matriz-client/src/matriz_client/_atransport.py` returns 130-200
    - `grep -c "^from __future__ import annotations" packages/matriz-client/src/matriz_client/_atransport.py` returns 1
    - `grep -c "class AsyncRetryTransport" packages/matriz-client/src/matriz_client/_atransport.py` returns 1
    - `grep -c "httpx.AsyncHTTPTransport" packages/matriz-client/src/matriz_client/_atransport.py` returns ≥ 1 (base class)
    - `grep -c "AsyncRetrying\|async for attempt" packages/matriz-client/src/matriz_client/_atransport.py` returns ≥ 2
    - `grep -c "await asyncio.sleep" packages/matriz-client/src/matriz_client/_atransport.py` returns ≥ 1 (Retry-After honor — D-32 CancelledError-aware)
    - `grep -c "request.extensions.get.\"idempotent\"" packages/matriz-client/src/matriz_client/_atransport.py` returns ≥ 1 (mutation gate)
    - `grep -c "auth_basic_user\|auth_basic_password" packages/matriz-client/src/matriz_client/_atransport.py` returns ≥ 2 (D-22 log split)
    - `grep -c "from matriz_client._transport import" packages/matriz-client/src/matriz_client/_atransport.py` returns 1 (constants reuse)
    - `uv run pytest packages/matriz-client/tests/test_atransport.py -q` exits 0 and reports ≥ 7 tests passed
    - `uv run mypy --strict packages/matriz-client/src/matriz_client/_atransport.py` exits 0
    - `uv run ruff check packages/matriz-client/src/matriz_client/_atransport.py packages/matriz-client/tests/test_atransport.py` exits 0
    - `python -c "from matriz_client._atransport import AsyncRetryTransport; t = AsyncRetryTransport(max_attempts=2); print('ok')"` outputs "ok"
    - Mutation gate proof: `uv run pytest packages/matriz-client/tests/test_atransport.py -k mutation_gate -q` exits 0 (T3 test passes — EXACTLY 1 request on POST/non-idempotent 503)
  </acceptance_criteria>
  <done>
    `AsyncRetryTransport` mirrors `RetryTransport` semantics for async httpx: idempotent retry on 408/409/429/5xx + retryable exceptions, mutation gate via `req.extensions["idempotent"]`, Retry-After via `await asyncio.sleep` (CancelledError-aware), D-22 `auth_basic` log split. T1-T8 pass. D-25 carve-out closed for matriz.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace `aio.py` 103-LOC stub with AsyncClient full REST surface (22 endpoints + module-level delegators + PEP 562 shim) and extend `conftest.py` with `_configure_async`</name>
  <files>packages/matriz-client/src/matriz_client/aio.py, packages/matriz-client/src/matriz_client/__init__.py, packages/matriz-client/tests/conftest.py, packages/matriz-client/tests/test_async_auth.py, packages/matriz-client/tests/test_async_queries.py, packages/matriz-client/tests/test_async_mutations.py</files>
  <read_first>
    - `packages/iol-client/src/iol_client/aio.py` (594 LOC) — CANONICAL reference for AsyncClient lifecycle, _ensure_http_client, _request, endpoint shells, module-level singletons + delegators, PEP 562 shim. Read all sections.
    - `packages/matriz-client/src/matriz_client/client.py` (existing sync) — mirror endpoint signatures 1:1; matriz Risk API auth path (BasicAuth via `spec.auth_basic`) differs from iol — async `_request` MUST mirror sync `_request` behaviour
    - `packages/matriz-client/src/matriz_client/aio.py` (existing 103-LOC stub) — see what currently exists and PEP 562 attributes already forwarded (DO NOT regress)
    - `packages/matriz-client/src/matriz_client/_core.py` — public builders/parsers list: `build_get_segments_request`/`parse_get_segments_response`, `build_get_all_instruments_request`/`parse_get_all_instruments_response`, `build_get_instruments_details_request`/`parse_get_instruments_details_response`, `build_get_instrument_detail_request`/`parse_get_instrument_detail_response`, `build_get_instruments_by_cfi_request`/`parse_get_instruments_by_cfi_response`, `build_get_instruments_by_segment_request`/`parse_get_instruments_by_segment_response`, `build_new_order_request`/`parse_new_order_response`, `build_replace_order_request`/`parse_replace_order_response`, `build_cancel_order_request`/`parse_cancel_order_response`, `build_get_order_status_request`/`parse_get_order_status_response`, `build_get_order_history_request`/`parse_get_order_history_response`, `build_get_active_orders_request`/`parse_get_active_orders_response`, `build_get_filled_orders_request`/`parse_get_filled_orders_response`, `build_get_all_orders_request`/`parse_get_all_orders_response`, `build_get_order_by_exec_id_request`/`parse_get_order_by_exec_id_response`, `build_get_market_data_request`/`parse_get_market_data_response`, `build_get_trades_request`/`parse_get_trades_response`, `build_get_positions_request`/`parse_get_positions_response`, `build_get_detailed_positions_request`/`parse_get_detailed_positions_response`, `build_get_account_report_request`/`parse_get_account_report_response`, `build_login_request`/`parse_login_response`, `raise_for_response`
    - `packages/matriz-client/src/matriz_client/_state.py` — current `_ClientState` schema (note: `token_store` field is added by Plan 10-03; in Plan 10-02 we use a temporary local-instance `token_store` reference set via `getattr(self._state, "token_store", None)` to avoid breaking Plan 10-03's atomic _state mod; OR we can stash it in `self.__dict__` per-instance until Plan 10-03 lands. Recommendation: use a per-instance `self._token_store_local: TokenStore | None = None` attribute on AsyncClient — Plan 10-03 will refactor to read from `self._state.token_store`)
    - `packages/matriz-client/src/matriz_client/_token_store.py` (Plan 10-01) — `build_token_store(state, *, max_retries)` factory
    - `packages/matriz-client/src/matriz_client/_atransport.py` (Task 1 of this Plan) — AsyncRetryTransport for wiring into httpx.AsyncClient
    - `packages/matriz-client/src/matriz_client/exceptions.py` — `AuthenticationError`, `PrimaryAPIError`, `MatrizClientError` hierarchy
    - `packages/matriz-client/src/matriz_client/models.py` — return types for endpoints (Segment, Instrument, Order, etc.)
    - `packages/matriz-client/src/matriz_client/types.py` — Literal aliases (Side, OrderType, MarketId, etc.)
    - `packages/iol-client/tests/conftest.py` lines 41-65 — `_configure_async` autouse fixture pattern
    - `packages/iol-client/tests/test_async_client.py` first 200 LOC — test idioms for pytest-httpx async tests
    - `packages/matriz-client/tests/conftest.py` — existing matriz conftest (extend, don't replace)
    - `packages/matriz-client/tests/test_client.py` — sync test patterns to mirror for the 22 endpoints (test_get_segments, test_new_order_*, etc.)
    - 10-PATTERNS.md lines 493-781 — `aio.py` pattern assignment with code samples
    - 10-CONTEXT.md `<specifics>` lines 838-937 — AsyncClient skeleton
  </read_first>
  <behavior>
    **`conftest.py` extension:**
    - Add `_configure_async` autouse fixture mirroring iol-client's pattern: configure `aio` with test base_url/token/credentials, yield, then `aclose` the default async client and reset configure.

    **Async auth tests (`test_async_auth.py`, ~8 tests):**
    - Test AA1: `aio.login()` issues POST `/auth/getToken` with `X-Username`/`X-Password` headers; response `X-Auth-Token` header populates `aio._token` (PEP 562 forwarded)
    - Test AA2: `aio.AsyncClient()` is constructible with kwargs `base_url`, `username`, `password`, `token`, `token_expires_at`, `max_retries`, `http_client`
    - Test AA3: `async with aio.AsyncClient() as c: ...` enters and exits; `aclose` is called automatically (verify via http_client.is_closed)
    - Test AA4: `AsyncClient.__repr__` redacts password and token (no `pass` or token literal in repr output)
    - Test AA5: `AsyncClient.__reduce__` raises TypeError (not picklable)
    - Test AA6: `AsyncClient.__deepcopy__` raises TypeError
    - Test AA7 (401 re-auth-once flow — async mirror of Phase 8 D-02 sync flow): mock the `/auth/getToken` to return a token; mock first endpoint call → 401; mock second `/auth/getToken` → new token; mock retry → 200. Verify EXACTLY one re-auth attempt. After re-auth, request retries once with new token.
    - Test AA8: 401 followed by 401 (re-auth still fails) raises `AuthenticationError`.

    **Async query tests (`test_async_queries.py`, ~14 tests):**
    - Mirror sync `test_client.py` query patterns for: `get_segments`, `get_all_instruments`, `get_instruments_details`, `get_instrument_detail`, `get_instruments_by_cfi`, `get_instruments_by_segment`, `get_order_status`, `get_order_history`, `get_active_orders`, `get_filled_orders`, `get_all_orders`, `get_order_by_exec_id`, `get_market_data`, `get_trades`, `get_positions`, `get_detailed_positions`, `get_account_report` — at minimum one happy-path per endpoint hitting the correct method/URL with correct query params. (Some endpoints may be combined into a single test where their behavior is symmetric — target ≥ 14 tests covering all 20 query endpoints.)

    **Async mutation tests (`test_async_mutations.py`, ~5 tests):**
    - Test AM1: `aio.new_order(...)` with happy path; verify `request.extensions["idempotent"] == False` (mutation gate input)
    - Test AM2 (MUTATION GATE — Pitfall 4): `aio.new_order(...)` against a 503 mock → EXACTLY 1 outgoing request (NOT 3 with retry). Use `httpx_mock.get_requests()`.
    - Test AM3: `aio.cancel_order(...)` against 503 → EXACTLY 1 outgoing request.
    - Test AM4: `aio.replace_order(...)` against 503 → EXACTLY 1 outgoing request.
    - Test AM5: `aio.new_order(...)` against 200 success → parsed `NewOrderResponse` returned correctly.

    **B8 lock-in cross-check (in any of the above test files, 1 explicit test):**
    - Test B8: `import matriz_client; from matriz_client import aio; assert aio._raise_for_response is matriz_client.client._raise_for_response is matriz_client._core.raise_for_response` (same function object — Phase 7 D-04 invariant).
  </behavior>
  <action>
    **Step 1 — Replace `packages/matriz-client/src/matriz_client/aio.py` (current 103-LOC stub) with full REST surface (~500-650 LOC).**

    Follow 10-PATTERNS.md lines 493-781 + 10-CONTEXT.md <specifics> lines 838-937 + iol `aio.py` 594-LOC reference. Key structure:

    1. Header + imports (10-PATTERNS.md lines 501-543):
       ```python
       """matriz_client.aio — async REST mirror of matriz_client.client (Phase 10 REFAC-04).
       ...
       """
       from __future__ import annotations

       import asyncio
       import time
       import uuid
       import warnings
       from collections.abc import Sequence
       from typing import Any, Self

       import httpx

       from matriz_client import _atransport, _core
       from matriz_client._core import RequestSpec
       from matriz_client._core import raise_for_response as _raise_for_response  # B8 D-04
       from matriz_client._state import _REQUEST_TIMEOUT, _ClientState, _TOKEN_TTL
       from matriz_client._token_store import TokenStore, build_token_store
       from matriz_client.client import _validate_max_retries
       from matriz_client.exceptions import AuthenticationError, PrimaryAPIError
       from matriz_client.models import (
           AccountReport, DetailedPosition, Instrument, InstrumentDetail,
           MarketDataSnapshot, NewOrderResponse, Order, Position, Segment, Trade,
       )
       from matriz_client.types import (
           CFICode, MarketDataEntry, MarketId, OrderType, SegmentId, Side, TimeInForce,
       )

       _ = _raise_for_response  # suppress ruff F401 — re-export for B8 monkeypatch invariant
       ```
       (Use the actual symbol list from `models.py`/`types.py`; verify by reading those modules first.)

    2. `__all__` with `AsyncClient`, `configure`, `login`, all 22 endpoint delegators, `aclose`.

    3. **Class `AsyncClient`** with `__slots__ = ("_max_retries", "_state", "_token_store_local")`:
       - `__init__(self, *, base_url=None, username=None, password=None, token=None, token_expires_at=None, max_retries=2, http_client=None) -> None`:
         - `_validate_max_retries(max_retries)`
         - `self._state = _ClientState()`
         - Override `_state.base_url/username/password/token/token_expires_at` from kwargs (mirror iol pattern with `if x is not None`).
         - `self._max_retries = max_retries`
         - `if http_client is not None: self._state.http_client = http_client`
         - `self._token_store_local: TokenStore | None = None` (per-instance local stash; Plan 10-03 will migrate to `_state.token_store`)
       - `async def __aenter__(self) -> Self: return self`
       - `async def __aexit__(self, *exc: Any) -> None: await self.aclose()`
       - `async def aclose(self) -> None`: close `_state.http_client` if not None, set to None.
       - `def __repr__(self) -> str` → redacted (no password/token literals).
       - `def __reduce__(self) -> Any` → `raise TypeError(...)`
       - `def __deepcopy__(self, memo) -> AsyncClient` → `raise TypeError(...)`

    4. **`async def _ensure_http_client(self) -> httpx.AsyncClient`**: lazy-create `httpx.AsyncClient(timeout=_REQUEST_TIMEOUT, transport=_atransport.AsyncRetryTransport(max_attempts=self._max_retries + 1))` if `_state.http_client` is None; return it. NB: TokenStore handles refresh locking — NO separate `_client_lock` needed (10-PATTERNS.md line 617).

    5. **`async def _aensure_token(self) -> None`**:
       ```python
       await self._ensure_http_client()
       if self._token_store_local is None:
           self._token_store_local = build_token_store(self._state, max_retries=self._max_retries)
       snap = await self._token_store_local.get_async()
       self._state.token = snap.value     # mirror for legacy reads (PEP 562)
       ```
       NB: in Plan 10-03, we replace `self._token_store_local` with `self._state.token_store`. For Plan 10-02 (which does NOT touch `_state.py`), per-instance stash is acceptable.

       **CAVEAT for `build_token_store(state, max_retries)`:** The adapter `MatrizRefresh` takes a sync `httpx.Client`, not `httpx.AsyncClient`. For Plan 10-02 async path, we need a sync `httpx.Client` available. Options:
       - **(a)** Construct a separate sync `httpx.Client` lazily when the refresh adapter is invoked (the adapter runs inside `asyncio.to_thread`, so a sync client is safe).
       - **(b)** Modify `MatrizRefresh` to accept a callable that returns an httpx.Response (transport-agnostic).
       Recommendation: **(a)** — in `_aensure_token`, before calling `build_token_store`, set `self._state.http_client_sync_for_refresh: httpx.Client | None = ...` (a new ephemeral field, NOT part of `_ClientState` schema — store as instance attribute on AsyncClient instead). The Plan 10-03 wiring step will reconcile this with the sync Client's shared `httpx.Client`.

       **Simpler approach for Plan 10-02:** Inject a per-instance sync `httpx.Client` lazily:
       ```python
       def _ensure_refresh_http_client(self) -> httpx.Client:
           if not hasattr(self, "_refresh_sync_client") or self._refresh_sync_client is None:
               self._refresh_sync_client = httpx.Client(timeout=10.0)
           return self._refresh_sync_client
       ```
       Then in `build_token_store(state, ...)` call site (inside `_aensure_token`): temporarily swap `state.http_client = self._ensure_refresh_http_client()` before calling, then restore. **Cleaner**: build the adapter directly without going through `build_token_store` factory, OR add a `build_async_token_store(state, refresh_http_client, ...)` factory in `_token_store.py` (CONTEXT D-01 keeps factory in `_token_store.py`).

       **DECISION FOR PLAN 10-02:** Use approach (a) with a lazy per-instance sync `httpx.Client` for refresh, and call `build_token_store` after setting `state.http_client = self._refresh_sync_client` then restoring. Track this in the SUMMARY for Plan 10-03 reconciliation.

    6. **`async def _request(self, spec: RequestSpec) -> httpx.Response`** — mirror sync `client._request` (lines 242-337 of existing `client.py`) per 10-PATTERNS.md lines 643-696:
       - If `spec.auth_basic is not None`: Risk API path. Build request with `httpx.BasicAuth(*spec.auth_basic)`; set extensions (`idempotent`, `request_id`, `endpoint_name`, `account_id`, `auth_basic`); send; on 401 → raise `AuthenticationError` (NO re-auth per D-23); return response.
       - Else (token path):
         - `await self._aensure_token()`
         - `assert self._state.token is not None` (mypy strict narrowing)
         - Build request with `X-Auth-Token: self._state.token` header
         - Set extensions: `idempotent=spec.idempotent`, `request_id=uuid.uuid4().hex`, `endpoint_name=spec.endpoint_name`, `account_id` if set
         - Send; if not 401, return
         - On 401: `await resp.aread()`; `self._state.token = None`; if `self._token_store_local is not None: self._token_store_local.invalidate()`; `await self._aensure_token()`; rebuild request with new `X-Auth-Token`; send retry; on second 401 raise `AuthenticationError`.

    7. **`async def login(self) -> str`**: explicit eager login entry — for back-compat with callers that call `aio.login()` directly. Use `_core.build_login_request(self._state) → spec → await self._ensure_http_client().post → parse_login_response → store in state.token, state.token_expires_at`.

    8. **22 endpoint methods** — each is a 3-liner shell (per 10-PATTERNS.md lines 700-714):
       ```python
       async def get_segments(self) -> list[Segment]:
           spec = _core.build_get_segments_request(self._state)
           resp = await self._request(spec)
           return _core.parse_get_segments_response(resp)
       ```
       Repeat for ALL 22 endpoints. Signatures MUST match sync `client.Client` 1:1 (read `client.py` for each signature; do NOT invent params). Endpoints to mirror:
       - `get_segments`, `get_all_instruments`, `get_instruments_details`, `get_instrument_detail(symbol, market)`, `get_instruments_by_cfi(cfi_code)`, `get_instruments_by_segment(market_id, segment_id)`
       - `new_order(...)`, `replace_order(...)`, `cancel_order(...)`
       - `get_order_status(...)`, `get_order_history(...)`, `get_active_orders(...)`, `get_filled_orders(...)`, `get_all_orders(...)`, `get_order_by_exec_id(...)`
       - `get_market_data(...)`, `get_trades(...)`
       - `get_positions(account)`, `get_detailed_positions(account)`, `get_account_report(account)`

    9. **Module-level singleton + delegators** (10-PATTERNS.md lines 716-754):
       - `_default_async_client: AsyncClient | None = None`
       - `def _get_default() -> AsyncClient`: lazy-init.
       - `def configure(*, base_url=None, username=None, password=None, token=None, token_expires_at=None, max_retries=None, http_client=None) -> None`: replace `_default_async_client`. On `password is not None` reset `_state.token`, `_state.token_expires_at`, `_token_store_local`. On `max_retries is not None` reset `_state.http_client`, `_token_store_local`.
       - For each of the 22 endpoints + `login` + `aclose`: module-level `async def X(...) -> ...: return await _get_default().X(...)` delegator.

    10. **PEP 562 shim** (10-PATTERNS.md lines 756-772):
        ```python
        _FORWARDED_TO_STATE: dict[str, str] = {
            "_token": "token",
            "_token_expires_at": "token_expires_at",
            "_base_url": "base_url",
        }
        _FORWARDED_HTTP_CLIENT = "_client"

        def __getattr__(name: str) -> Any:
            if name in _FORWARDED_TO_STATE:
                return getattr(_get_default()._state, _FORWARDED_TO_STATE[name])
            if name == _FORWARDED_HTTP_CLIENT:
                return _get_default()._state.http_client
            raise AttributeError(f"module {__name__!r} has no attribute {name!r}")
        ```
        NB: do NOT add `_DENIED_LEGACY` entries in Phase 10 (CONTEXT Claude's-Discretion line 365).

    **Step 2 — Update `packages/matriz-client/src/matriz_client/__init__.py`:**
    - Add `from matriz_client.aio import AsyncClient` (if not already present) and ensure `AsyncClient` is in `__all__`.
    - Verify the existing top-level re-exports for sync `Client`, `configure`, `login`, etc. remain unchanged.
    - Keep the existing `logging.getLogger("matriz_client").addHandler(logging.NullHandler())` LOG-01 pattern.

    **Step 3 — Extend `packages/matriz-client/tests/conftest.py`:**
    - Add `AsyncIterator` import.
    - Add `import asyncio` + `from matriz_client import aio` imports.
    - Append the `_configure_async` autouse fixture per 10-PATTERNS.md lines 1191-1205:
      ```python
      @pytest.fixture(autouse=True)
      async def _configure_async() -> AsyncIterator[None]:
          aio.configure(base_url="https://api.test", username="test-user",
                        password="test-pass", token="test-token",
                        token_expires_at=9_999_999_999.0)
          yield
          await aio._get_default().aclose()
          aio.configure(base_url="https://api.test", username="", password="")
      ```

    **Step 4 — Create the 3 test files** (`test_async_auth.py`, `test_async_queries.py`, `test_async_mutations.py`) per the behavior list above. Use `pytest-httpx` `httpx_mock` fixture. Mock responses for:
    - `/auth/getToken` POST returning `X-Auth-Token` header.
    - Each endpoint URL with appropriate JSON envelope (`{"status": "OK", ...}`).

    **Critical test correctness:** For mutation tests (AM2/AM3/AM4), set `httpx_mock.non_mocked_hosts = []` (or use `assert_all_responses_were_requested=False`) and assert `len(httpx_mock.get_requests()) == 1` after the mutation call against 503.

    For B8 lock-in test, place in `test_async_auth.py`:
    ```python
    def test_b8_aio_raise_for_response_lock_in() -> None:
        from matriz_client import _core, aio, client as sync_client
        assert aio._raise_for_response is sync_client._raise_for_response
        assert aio._raise_for_response is _core.raise_for_response
    ```

    Mypy strict + ruff clean. Use `from __future__ import annotations` in every new file.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py packages/matriz-client/tests/test_atransport.py -q && uv run mypy --strict packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py && uv run ruff check packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py packages/matriz-client/tests/conftest.py packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l packages/matriz-client/src/matriz_client/aio.py` returns ≥ 500 (full REST surface; replaces 103-LOC stub)
    - `grep -c "^from __future__ import annotations" packages/matriz-client/src/matriz_client/aio.py` returns 1
    - `grep -c "^class AsyncClient" packages/matriz-client/src/matriz_client/aio.py` returns 1
    - `grep -c "^async def " packages/matriz-client/src/matriz_client/aio.py` returns ≥ 22 (22 endpoint delegators + others — login, aclose, _ensure_*, _aensure_token, _request, get_X × 20+, __aenter__/__aexit__ counted as `async def`)
    - `grep -c "    async def " packages/matriz-client/src/matriz_client/aio.py` returns ≥ 22 (class methods alone)
    - `grep "from matriz_client._core import raise_for_response as _raise_for_response" packages/matriz-client/src/matriz_client/aio.py | wc -l` returns 1 (B8 D-04 lock-in)
    - `grep -c "import _atransport\|from matriz_client._atransport import\|matriz_client._atransport\|_atransport.AsyncRetryTransport" packages/matriz-client/src/matriz_client/aio.py` returns ≥ 2
    - `grep -c "build_token_store\|TokenStore" packages/matriz-client/src/matriz_client/aio.py` returns ≥ 2 (Plan 10-01 primitive consumed)
    - `grep "def __getattr__" packages/matriz-client/src/matriz_client/aio.py | wc -l` returns 1 (PEP 562 shim)
    - `grep -c "_FORWARDED_TO_STATE\|_FORWARDED_HTTP_CLIENT" packages/matriz-client/src/matriz_client/aio.py` returns ≥ 4 (back-compat names)
    - `grep "assert self._state.token is not None" packages/matriz-client/src/matriz_client/aio.py | wc -l` returns ≥ 1 (mypy strict narrowing after _aensure_token)
    - All 22 endpoint names appear as module-level `async def` definitions: `python -c "from matriz_client import aio; missing = [n for n in ['login','get_segments','get_all_instruments','get_instruments_details','get_instrument_detail','get_instruments_by_cfi','get_instruments_by_segment','new_order','replace_order','cancel_order','get_order_status','get_order_history','get_active_orders','get_filled_orders','get_all_orders','get_order_by_exec_id','get_market_data','get_trades','get_positions','get_detailed_positions','get_account_report','aclose'] if not hasattr(aio, n)]; assert not missing, f'missing: {missing}'; print('ok')"` outputs "ok"
    - `python -c "from matriz_client import aio; assert hasattr(aio, 'AsyncClient'); c = aio.AsyncClient(base_url='https://api.test', username='u', password='p'); assert c._state.base_url == 'https://api.test'; print('ok')"` outputs "ok"
    - `python -c "from matriz_client import aio, client; assert aio._raise_for_response is client._raise_for_response; print('ok')"` outputs "ok" (B8 invariant)
    - `python -c "from matriz_client import aio; _ = aio._token; print('ok')"` outputs "ok" (PEP 562 shim works for legacy `_token`)
    - `grep -c "AsyncClient" packages/matriz-client/src/matriz_client/__init__.py` returns ≥ 1 (exported)
    - `grep -c "_configure_async" packages/matriz-client/tests/conftest.py` returns ≥ 1 (autouse fixture present)
    - `uv run pytest packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py -q` exits 0 and reports ≥ 25 tests passed (~8 auth + ~14 queries + ~5 mutations = 27+)
    - Mutation gate proof (Pitfall 4 async): `uv run pytest packages/matriz-client/tests/test_async_mutations.py -k "503 or mutation" -q` exits 0 (AM2/AM3/AM4 — exactly 1 request)
    - `uv run mypy --strict packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py` exits 0
    - `uv run ruff check packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py packages/matriz-client/tests/conftest.py packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py` exits 0
    - Baseline regression: `uv run pytest packages/matriz-client/tests/ -q --deselect packages/matriz-client/tests/test_fixture_reaches_production.py --deselect packages/matriz-client/tests/test_ws_client.py` exits 0 (existing sync tests untouched — `client.py`, `ws_client.py`, `_state.py` are NOT modified by Plan 10-02; `_aensure_token` change is in `aio.py` only)
    - Workspace baseline (excluding matriz async-related skips): `uv run pytest -q` exits 0 (allow the 3 forward-reference skips in `test_fixture_reaches_production.py:64`, `verification/test_async_cancellation.py:82`, `verification/test_sync_async_isolation.py:176` to remain — Plan 10-04 flips them)
  </acceptance_criteria>
  <done>
    `aio.py` exposes a complete async REST surface: 22 endpoint methods on `AsyncClient` (signatures mirror sync `Client`), 22 module-level async delegators, PEP 562 shim back-compat, lazy TokenStore-backed `_aensure_token`, 401 re-auth-once with TokenStore invalidate, async `_request` mirrors sync semantics (Risk API BasicAuth path + token path), AsyncRetryTransport wired into the httpx.AsyncClient. B8 lock-in preserved. ~27+ new tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `aio.AsyncClient` public methods | Library API boundary; caller may pass arbitrary strings (symbols, account IDs, prices); validated by `_core` builders before httpx send |
| `AsyncRetryTransport` → matriz REST API | Outbound HTTPS; idempotent operations may be retried; tokens flow in `X-Auth-Token` header; BasicAuth for Risk API |
| `_atransport.py` log emission → `matriz_client` logger | Logs WARNING/ERROR records with extras (status_code, url, account_id, auth_basic_user, auth_basic_password); `RedactingFilter` (Phase 8 LOG-02) scrubs sensitive substrings |
| AsyncClient `__repr__` / pickle / deepcopy | Object introspection boundary; password/token must not leak via repr; pickle/deepcopy disabled to prevent accidental secret persistence |
| `_aensure_token` → `TokenStore` (and inner `MatrizRefresh`) | In-process call; `MatrizRefresh` issues network call from inside `asyncio.to_thread` worker; credentials in request headers |
| PEP 562 `__getattr__` shim → legacy callers | Back-compat read path; returns current state.token/base_url/etc. without bypassing TokenStore semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Information Disclosure | `AsyncClient.__repr__` | mitigate | `__repr__` returns string with `password='***'` and `token='***'` literals; verified by acceptance test AA4 (no `pass` substring matching the actual password, no token substring matching the actual token). ASVS V8.3.1. |
| T-10-02-02 | Information Disclosure | Pickle/deepcopy of AsyncClient | mitigate | `__reduce__` and `__deepcopy__` raise `TypeError` to prevent token persistence to disk via accidental serialization (e.g., joblib, multiprocessing). Mirrors iol pattern. Acceptance tests AA5, AA6. |
| T-10-02-03 | Information Disclosure | `AsyncRetryTransport` log records with `auth_basic` | mitigate | D-22: `auth_basic` tuple split into `auth_basic_user` (operational) + `auth_basic_password="***"` (redacted) before passing to `extra={}`. Verified by test T8 (caplog assertion that the password literal does NOT appear in any record). LOG-02 + LOG-03 compliance. |
| T-10-02-04 | Information Disclosure | `_request` 401 re-auth flow logging | mitigate | When 401 triggers re-auth, the response body is consumed via `await resp.aread()` BEFORE raising or retrying (no half-read connection in pool with cached token bytes). Sync `client.py` already does this; async mirror preserves. |
| T-10-02-05 | Tampering | npm/pip/cargo installs | n/a | NO new dependencies. `tenacity`, `httpx`, `pytest-asyncio`, `pytest-httpx` are all pre-existing matriz-client deps (Phase 8 baseline). Package Legitimacy Gate not triggered. |
| T-10-02-06 | Tampering | Mutation gate bypass via crafted request extensions | mitigate | `AsyncRetryTransport.handle_async_request` reads `request.extensions.get("idempotent", False)`; default `False` means any request missing the explicit `idempotent=True` flag is NOT retried. Mutation endpoints (`new_order`, `cancel_order`, `replace_order`) have `idempotent=False` set by `_core` builders. Verified by tests AM2/AM3/AM4 (Pitfall 4 — duplicate-order prevention). ASVS V13.1.3. |
| T-10-02-07 | Denial of Service | Async retry budget exhaustion | mitigate | `AsyncRetryTransport(max_attempts=2)` caps at 2 attempts; `_RETRY_AFTER_CAP_S=60` caps server-requested delays. Combined with Plan 10-01's `RefreshPolicy` fail-cache, prevents runaway retry storms even under sustained server failure. |
| T-10-02-08 | Denial of Service | CancelledError propagation (Pitfall 16) | mitigate | `await asyncio.sleep` (not `time.sleep`) is used for Retry-After; this preserves `asyncio.CancelledError` propagation when the caller's task is cancelled. Test T6 explicitly cancels and asserts propagation. Without this mitigation, a stuck retry would block task cancellation indefinitely. ASVS V11.1.2 (resource exhaustion via cancellation hang). |
| T-10-02-09 | Repudiation | Async log records lack request_id | mitigate | Each request gets `request.extensions["request_id"] = uuid.uuid4().hex` in `_request`; the transport propagates this to log records via `extra={"request_id": ...}`. Operators can correlate retry attempts via request_id. LOG-03 convention. |
| T-10-02-10 | Elevation of Privilege | TokenStore not yet wired to `_state.token_store` (Plan 10-03 territory) | accept | Plan 10-02 uses per-instance `self._token_store_local` to defer the `_state.py` schema change to Plan 10-03's atomic unit. Risk: a Plan 10-02-deployed-without-Plan-10-03 build would leave the sync `Client` using the old `_ensure_token` path (no 3-way coordination). Mitigation: Plan 10-02 and Plan 10-03 ship as separate commits in the same phase merge cycle (CONTEXT D-08), gated by Plan 10-04 green-gate before phase merge. |
| T-10-02-11 | Information Disclosure | B8 lock-in regression risk | mitigate | The cross-check test `test_b8_aio_raise_for_response_lock_in` asserts `aio._raise_for_response is client._raise_for_response is _core.raise_for_response`. Without this invariant, a monkeypatch in tests could bind to the sync path but not the async path (or vice versa), creating a security-relevant test-vs-prod gap (e.g., 401 mapping divergence). Phase 7 D-04 carry-forward. |
</threat_model>

<verification>
- `uv run pytest packages/matriz-client/tests/test_atransport.py packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py -q` exits 0 and reports ≥ 30 tests passed (CONTEXT D-07 +30 target)
- `uv run mypy --strict packages/matriz-client/src/matriz_client/_atransport.py packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py` exits 0
- `uv run ruff check packages/matriz-client/src/matriz_client/_atransport.py packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/__init__.py packages/matriz-client/tests/conftest.py packages/matriz-client/tests/test_atransport.py packages/matriz-client/tests/test_async_auth.py packages/matriz-client/tests/test_async_queries.py packages/matriz-client/tests/test_async_mutations.py` exits 0
- `uv run ruff format --check packages/matriz-client/src/matriz_client/_atransport.py packages/matriz-client/src/matriz_client/aio.py` exits 0
- Baseline regression: `uv run pytest packages/matriz-client/tests/ -q` exits 0 (existing sync tests pass; the 1 permanent skip in `test_fixture_reaches_production.py:64` remains until Plan 10-04 flips it)
- `client.py` UNMODIFIED: `git diff packages/matriz-client/src/matriz_client/client.py | wc -l` returns 0 (Plan 10-02 does not touch sync surface)
- `_state.py` UNMODIFIED: `git diff packages/matriz-client/src/matriz_client/_state.py | wc -l` returns 0 (Plan 10-03 owns state schema change)
- `ws_client.py` UNMODIFIED: `git diff packages/matriz-client/src/matriz_client/ws_client.py | wc -l` returns 0 (Plan 10-03 owns ws_client migration)
- import-linter: `uv run lint-imports` exits 0 — `aio.py` may import `_core`, `_atransport`, `_state`, `_token_store`, `client._validate_max_retries`, `exceptions`, `models`, `types` only (no forbidden direction; matches iol allowance per Phase 7 D-09)
- CI grep `lint-logging`: `grep -rn "logging.basicConfig\|logging.root" packages/matriz-client/src/matriz_client/aio.py packages/matriz-client/src/matriz_client/_atransport.py` returns 0 matches
- B8 lock-in cross-check: `python -c "from matriz_client import _core, aio, client; assert aio._raise_for_response is client._raise_for_response is _core.raise_for_response; print('ok')"` outputs "ok"
- Workspace baseline (excluding 3 forward-reference skips): `uv run pytest -q` exits 0; skip count ≤ 3
- 1 commit atómico per D-08: `feat(matriz): AsyncClient full REST mirror + AsyncRetryTransport (REFAC-04)` with all 8 file changes
</verification>

<success_criteria>
1. **Full REST mirror:** `matriz_client.aio.AsyncClient` exposes the 22 endpoints with signatures matching sync `Client`; module-level delegators preserve top-level functional API (`aio.get_segments(...)`, `aio.new_order(...)`, etc.).
2. **Transport mirror:** `AsyncRetryTransport` mirrors `RetryTransport` semantics including D-22 `auth_basic` log redaction (D-25 carve-out closed for matriz).
3. **Mutation gate enforced in async (Pitfall 4):** `new_order`/`cancel_order`/`replace_order` against 503 → EXACTLY 1 outgoing request.
4. **CancelledError-safe Retry-After (D-32, Pitfall 16):** `await asyncio.sleep` preserves cancellation; test T6 explicitly cancels and asserts propagation.
5. **B8 lock-in preserved:** `aio._raise_for_response is client._raise_for_response is _core.raise_for_response`.
6. **PEP 562 back-compat:** Legacy `aio._token`, `aio._base_url`, `aio._token_expires_at`, `aio._client` resolve via `_get_default()._state`.
7. **TokenStore consumed via lazy `build_token_store`** in `_aensure_token` (per-instance stash for Plan 10-02; Plan 10-03 migrates to `_state.token_store`).
8. **No sync surface regression:** `client.py`, `_state.py`, `ws_client.py` untouched — existing sync tests pass without modification.
9. **CI green:** ruff + mypy strict + lint-imports + lint-logging + pytest workspace baseline pass.
10. **Atomic commit per D-08:** single `feat(matriz): AsyncClient full REST mirror + AsyncRetryTransport (REFAC-04)` commit.
</success_criteria>

<output>
Create `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-02-SUMMARY.md` documenting:
- `aio.py` LOC (target ≥ 500; replacing 103-LOC stub)
- `_atransport.py` LOC (target 130-200)
- 22 endpoint methods + 22 module-level delegators landed
- Test counts per file (test_atransport.py ≥ 7, test_async_auth.py ≥ 8, test_async_queries.py ≥ 14, test_async_mutations.py ≥ 5 — total ≥ 30 per D-07)
- B8 lock-in cross-check passes
- Mutation gate proof (AM2/AM3/AM4 — exactly 1 request on 503)
- D-22 auth_basic redaction proof (test T8)
- D-32 CancelledError propagation proof (test T6)
- Per-instance `_token_store_local` stash documented (will migrate to `_state.token_store` in Plan 10-03)
- No sync/state/ws_client modifications (Plan 10-03 territory)
- Snapshot diff NOT regenerated in Plan 10-02 (Plan 10-04 owns snapshot regen)
- Atomic commit hash
- CI baseline green
- Next: Plan 10-03 wires `_state.token_store` field + sync `client._ensure_token` + async `_aensure_token` (migrates from `_token_store_local`) + ws_client.py lines 145-147 swap + cross-thread integration tests
</output>
