---
phase: 34-releases-por-paquete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/README.md
  - packages/iol-client/pyproject.toml
  - packages/iol-client/src/iol_client/__init__.py
  - packages/market-data-client/pyproject.toml
  - packages/market-data-client/src/market_data_client/__init__.py
  - uv.lock
autonomous: true
requirements: [PUB-TYP-01]

must_haves:
  truths:
    - "The `### v0.5.0` section of packages/market-data-client/README.md names all three Phase 33 shape breaks (SC-1 `preview_calendar_config` -> `CalendarConfigPreview`; SC-2 `MarketDataSnapshot.entries`/`.market_data`/`.staleness_seconds` -> `| None`; SC-3 `Symbol.created_at`/`.updated_at` -> `str | None`) alongside the four Phase 31 ops-endpoint breaks — seven items, not four — per D-04"
    - "The `### v0.5.0` heading is de-provisionalized: the line reads exactly `### v0.5.0` with no trailing suffix, and the provisional preamble that deferred the bump to this phase and warned about stale wheel metadata is gone — per D-04"
    - "packages/iol-client reads 0.3.0 at both its version sites (pyproject.toml `version`, `iol_client.__version__`) — per D-01"
    - "packages/market-data-client reads 0.5.0 at both its version sites (pyproject.toml `version`, `market_data_client.__version__`) — per D-01"
    - "uv.lock was regenerated by a single `uv lock` run executed AFTER both pyproject edits, registers iol-client at 0.3.0 and market-data-client at 0.5.0, passes `uv lock --check`, and churned by exactly 2 insertions + 2 deletions — per D-11"
    - "No package other than iol-client and market-data-client has a changed version at any site — per D-01"
    - "No file under .github/workflows/ is modified by this phase — per D-11 and the read-only workflow contract"
    - "origin/milestone/v1.5-mutations resolves to the same commit as local HEAD via a plain fast-forward push, with no force flag and no rebase or merge of origin/main into the branch — per D-05 and D-07"
    - "The full origin/main...HEAD diff about to become public carries no JWT-shaped token, no client_secret-style assignment and no tracked .env file"
  prohibitions:
    - "Never rewrite history reachable from this branch: no `--force`, no `--force-with-lease`, no rebase and no interactive history edit. Dozens of SUMMARY files across Phases 29-33 cite these SHAs by value and a rewrite orphans every one of them."
    - "Never report a gate as green from anything except an exact positive count. `pending`, `skipping`, `cancelled` and a zero-check run all read as green under an absence-of-failure check."
    - "Never publish a package whose public surface did not change in this milestone. Exactly two packages bump; the other four are not touched at any version site."
    - "Never hand-edit uv.lock, and never accept a lock diff larger than the four expected version lines — a wider diff means uv re-resolved third-party dependencies and would smuggle an unreviewed dependency change into a release."
  artifacts:
    - path: "packages/market-data-client/README.md"
      provides: "consumer-facing v0.5.0 changelog covering all seven breaking items, de-provisionalized"
      contains: "### v0.5.0"
    - path: "packages/iol-client/pyproject.toml"
      provides: "the version string release.yml validates against the iol tag"
      contains: "version = \"0.3.0\""
    - path: "packages/iol-client/src/iol_client/__init__.py"
      provides: "runtime-visible version mirror for iol-client (NOT validated by the pipeline)"
      contains: "__version__ = \"0.3.0\""
    - path: "packages/market-data-client/pyproject.toml"
      provides: "the version string release.yml validates against the market-data tag"
      contains: "version = \"0.5.0\""
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "runtime-visible version mirror for market-data-client (NOT validated by the pipeline)"
      contains: "__version__ = \"0.5.0\""
    - path: "uv.lock"
      provides: "workspace-member version registration for both bumped members, single refresh"
      contains: "name = \"iol-client\""
  key_links:
    - from: "packages/iol-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "release.yml:42-51 awk-reads the FIRST ^version line and fails the build unless it equals the tag version captured from iol-client-v0.3.0"
      pattern: "version = \"0.3.0\""
    - from: "packages/market-data-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "same awk version-match gate, run against the tag market-data-client-v0.5.0"
      pattern: "version = \"0.5.0\""
    - from: "packages/iol-client/pyproject.toml"
      to: "uv.lock"
      via: "the single `uv lock` run re-registers both workspace members; `uv lock --check` is CI check 1 of 15 (ci.yml:32)"
      pattern: "name = \"iol-client\""
    - from: "packages/market-data-client/README.md"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "the v0.5.0 changelog documents the three Phase 33 shape fixes actually implemented in models.py (CalendarConfigPreview at :831, MarketDataSnapshot at :267, Symbol.created_at/.updated_at at :682-683)"
      pattern: "CalendarConfigPreview"
---

<objective>
Perform every REVERSIBLE step of the two-package release: finish the `market-data-client` v0.5.0
changelog with the three shape breaks Phase 33 locked, de-provisionalize that changelog section,
bump the four version sites across the two packages that changed public surface, refresh `uv.lock`
exactly once, mirror the CI gate locally, re-run the credential scan over the diff that is about to
become public, and fast-forward push the release branch.

Purpose: Everything in this plan can be amended or reverted with a commit. Isolating it from the
irreversible publish (plans 34-02 and 34-03) means a failed prep costs nothing.
Output: A pushed `milestone/v1.5-mutations` whose tip carries the changelog commit, the two
per-package bump commits and the single lock-refresh commit, with all local CI gates green.

**Two packages, not one.** `iol-client` 0.2.0 -> 0.3.0 (dict->modelo, Phase 30) and
`market-data-client` 0.4.0 -> 0.5.0 (four ops endpoints typed in Phase 31 plus the three
`fix-shape-now` dispositions locked in 33-07). Both are source-breaking; both need a changelog
callout. `iol-client/README.md` already has its complete `### v0.3.0` entry (D-03) — do not touch it.
No other package is bumped or published (D-01).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/34-releases-por-paquete/34-CONTEXT.md
@.planning/phases/34-releases-por-paquete/34-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Non-symbol artifacts produced across phase 34. This phase creates **no new public symbols, classes
or functions** — the surface it publishes was built and verified by Phases 29-33. This plan produces
the first group.

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| Completed + de-provisionalized `### v0.5.0` changelog | `packages/market-data-client/README.md` | 34-01 Tasks 1 & 2 | `grep -qx '### v0.5.0'` + the ten backticked identifier tokens asserted inside the sliced section |
| Two aligned iol-client version sites at `0.3.0` | `pyproject.toml`, `__init__.py` | 34-01 Task 2 | `awk`/`grep -qx` assertions |
| Two aligned market-data-client version sites at `0.5.0` | `pyproject.toml`, `__init__.py` | 34-01 Task 2 | `awk`/`grep -qx` assertions |
| Single-refresh `uv.lock` registering both members | `uv.lock` | 34-01 Task 3 | `grep -A1` per member + `uv lock --check` + exact 2/2 churn |
| Pushed release branch (fast-forward) | `git-ref:origin/milestone/v1.5-mutations` | 34-01 Task 3 | `git rev-parse HEAD` == `git rev-parse origin/...` |
| Updated release PR (#12, edited not replaced) | `gh-pr:12` | 34-02 Task 1 | `gh pr view 12 --json title,state` |
| Merge commit on `main` (two parents) | `git-ref:origin/main` | 34-02 Task 3 | `git rev-list --parents -n1 origin/main` → 3 fields |
| Release tag for iol-client | `git-tag:iol-client-v0.3.0` | 34-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Release tag for market-data-client | `git-tag:market-data-client-v0.5.0` | 34-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Public GitHub Release + wheel + sdist (iol) | `gh-release:iol-client-v0.3.0` | 34-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
| Public GitHub Release + wheel + sdist (market-data) | `gh-release:market-data-client-v0.5.0` | 34-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
| Refreshed release memory | `.claude/projects/-Users-admin-development-market-libs/memory/market-data-client-releases.md` | 34-03 Task 3 | region-by-region greps in that task |
</artifacts_this_phase_produces>

<source_coverage_audit>
Multi-source coverage audit. Every item below is COVERED by a plan; there are no unplanned items.

| Source | Item | Covered by |
|---|---|---|
| GOAL | ROADMAP § Phase 34 Goal — the packages whose public surface changed are published by the tag pipeline, with the dict->modelo break declared in the changelog and every irreversible op behind a human gate | 34-01 (changelog + bumps), 34-02 (PR + merge gate), 34-03 (tag gate + publish) |
| GOAL | SC#1 — only changed packages bump; iol 0.2.0->0.3.0 with source-breaking callout; market-data 0.4.0->0.5.0 with the three 33-07 shape breaks | 34-01 Tasks 1-2 |
| GOAL | SC#2 — `uv.lock` refreshed exactly once | 34-01 Task 3 |
| GOAL | SC#2 msgspec branch — README declares loss of pure-Python closure | N/A by D-02: `29-DLOCK-MSGSPEC.md` is signed `no-go-stdlib-only`; msgspec never entered the workspace. Not a gap. |
| GOAL | SC#3 — PR -> green CI (6 packages x py3.12/3.13) -> merge commit -> per-package tag -> unedited `release.yml` -> Release with wheel + sdist | 34-02 Tasks 1/3, 34-03 Task 2 |
| GOAL | SC#4 — each irreversible op behind an independent human checkpoint, never collapsed | 34-02 Task 2 (gate a), 34-03 Task 1 (gate b) |
| REQ | PUB-TYP-01 | all three plans (`requirements: [PUB-TYP-01]`) |
| RESEARCH | — | No RESEARCH.md for this phase (research explicitly skipped; CONTEXT.md cites literal Phase 28 precedent plans instead). No items to cover. |
| CONTEXT | D-01 exactly two packages bump | 34-01 Task 2 |
| CONTEXT | D-02 msgspec branch does not apply | recorded above; no work item |
| CONTEXT | D-03 iol README `### v0.3.0` already complete, no changes | 34-01 `<scope_decisions>` (explicit do-not-touch + a gate asserting it is unchanged) |
| CONTEXT | D-04 extend market-data `### v0.5.0` with the three 33-07 breaks + retitle at bump time | 34-01 Tasks 1 & 2 |
| CONTEXT | D-05 update PR #12, do not close or replace; push pending commits first | 34-01 Task 3 (push), 34-02 Task 1 (edit) |
| CONTEXT | D-06 keep `.planning/` artifacts in the PR | 34-02 Task 1 |
| CONTEXT | D-07 no rebase, no merge of origin/main into the branch | 34-01 Task 3 |
| CONTEXT | D-08 exactly two blocking human checkpoints, never collapsed; gate (b) covers both tags in one approval | 34-02 Task 2, 34-03 Task 1 |
| CONTEXT | D-09 `gh pr merge --merge`, never squash, never rebase; both tags on that merge commit | 34-02 Task 3, 34-03 Task 2 |
| CONTEXT | D-10 annotated tags created on the merge-commit SHA, not branch HEAD | 34-03 Task 2 |
| CONTEXT | D-11 single `uv lock` after both bumps; no workflow file touched | 34-01 Task 3 |
| CONTEXT | Discretion: wording of the three new breaks | 34-01 Task 1 (format contract specified, prose is the executor's) |
| CONTEXT | Discretion: PR title and body | 34-02 Task 1 (title convention fixed, body prose is the executor's) |
| CONTEXT | Discretion: whether to create in-repo memory files | Decided in `<scope_decisions>` below: refresh the existing one, create no new one |
| CONTEXT | Discretion: commit grouping and order | Decided per task: four commits in this plan, one per coherent unit |
</source_coverage_audit>

<flagged_assumptions>
No SPEC.md exists for this phase, so the deterministic edge-probe engine ran in the planner's place
against PUB-TYP-01. All three applicable probe categories came back **unresolved** and none of them
maps onto a release-publish requirement. Per the no-silent-drop rule they are surfaced here as
explicit flagged assumptions rather than omitted or forced into `must_haves`.

| Probe category | Probe question | Status | Flagged assumption |
|---|---|---|---|
| adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | unresolved — not applicable | PUB-TYP-01 defines no comparison or merge semantics over a data structure. The nearest real analogue is the two tags landing on the *same* merge commit, which is an intended, non-degenerate configuration explicitly locked by D-09/D-10 and asserted in 34-03 Task 2 (both tags resolve to `git rev-parse origin/main`). No collection or comparison logic exists to probe. |
| empty | What is the result for empty, single-element, or null input? | unresolved — not applicable | PUB-TYP-01 takes no input collection. The nearest real analogue is a CI run reporting **zero** checks, which `ci.yml`'s `paths-ignore: ["**.md", ".gitignore"]` makes genuinely reachable for a Markdown-only diff; that degenerate case is covered by the exact `TOTAL = 15` count gate in 34-02 Task 1, not by an edge-probe truth. |
| ordering | When elements compare equal, is output order specified and stable? | unresolved — not applicable | PUB-TYP-01 produces no ordered output. The nearest real analogue is the *operational* ordering that IS load-bearing and IS specified: pyproject before `__version__` before `uv lock` (34-01), and merge before tag (34-02 before 34-03). Those are sequencing constraints in the plan text, not sortable output with a stability question. |

These three rows are recorded verbatim in all three plans of this phase. If a future revision of
PUB-TYP-01 introduces collection or comparison semantics, re-run the probe rather than inheriting
this dismissal.
</flagged_assumptions>

<reversibility_gates>
| Task | Operation | Reversibility | Gate |
|---|---|---|---|
| Task 1 | README changelog content | fully reversible (`git revert` / amend) | none needed |
| Task 2 | four version-site edits | fully reversible | none needed |
| Task 3 | `uv lock` + local CI mirror | fully reversible | none needed |
| Task 3 | `git push origin milestone/v1.5-mutations` (fast-forward) | reversible-in-principle only; a published commit on a public repo can be reverted forward but not unpublished | no human gate — this is an append-only fast-forward to a *feature* branch, not to `main`. Force flags are forbidden, which is what keeps it append-only. |

The phase's two blocking human gates live in 34-02 (before the merge) and 34-03 (before the tag
push), per D-08. This plan deliberately contains **zero** checkpoints — adding a third gate here
would violate D-08.
</reversibility_gates>

<scope_decisions>
Conscious decisions and deferrals, recorded so the executor and verifier see a decision rather than
a gap. Do NOT reopen these unless the operator explicitly says so.

- **`packages/iol-client/README.md` — do NOT touch.** Its `### v0.3.0` section (line 112 onward)
  was authored complete in Phase 30 with the dict->modelo callout, the truthiness flip and the
  `to_dict()` escape hatch. D-03 locks it as finished. A gate in Task 2 asserts it is byte-unchanged
  across this phase.
- **`.github/workflows/release.yml` and `ci.yml` — do NOT touch (D-11).** `release.yml` is generic
  by tag regex (`:28`) and already matches both `iol-client-v0.3.0` and `market-data-client-v0.5.0`.
  `ci.yml`'s `matrix.package` (`:113-119`) already lists all six packages, so the 15-check count is
  unchanged by two packages bumping instead of one. If `release.yml` fails, surface the failing step;
  never patch the workflow.
- **`CLAUDE.md` — do NOT touch.** It is a `/gsd-map-codebase` generated artifact and CONTEXT
  § Deferred Ideas puts it out of scope on the same criterion as Phase 28's D-07. Known doc debt.
- **In-repo memory files — decided, not left ambiguous.** CONTEXT § Verification/Risk Notes claims
  no memory file exists for either package; 34-PATTERNS.md verified live that
  `.claude/projects/-Users-admin-development-market-libs/memory/market-data-client-releases.md`
  **does exist** and currently instructs every future agent to install `market-data-client-v0.4.0`.
  Decision: **refresh the existing market-data-client memory** (34-03 Task 3) because leaving it
  stale actively misdirects installs to a superseded version — that is a refresh of an existing
  artifact, not the "crear archivos de memory in-repo" item CONTEXT deferred. Decision: **do NOT
  create `iol-client-releases.md`** — creating a new memory file is exactly the deferred item, no
  ROADMAP criterion requires it, and this phase does not open it.
- **The other four packages** (`higyrus-client`, `ambito-financiero-client`, `matriz-client`,
  `wallets-client`) are not bumped, not tagged and not published — their public surface did not
  change in v1.6 (D-01). Their CI matrix jobs still run and still must pass.
- **TDD framing does not apply to this plan.** `workflow.tdd_mode` is enabled, but this phase adds
  no business logic, no API contract and no data transformation: it edits changelog prose, four
  version literals and a lockfile, then performs git/gh operations. Every task is `type: auto` in a
  `type: execute` plan. Forcing a RED/GREEN cycle onto a version-string edit would produce a test
  that asserts a literal the same commit writes.
- **Tracer-slice framing does not apply.** The natural wave structure (reversible prep -> gated
  merge -> gated publish) already supplies the correct ordering and the correct failure isolation;
  there is no user-visible vertical slice to thread through this phase.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Extend the `### v0.5.0` changelog of market-data-client with the three Phase 33 shape breaks (D-04, SC#1)</name>
  <files>packages/market-data-client/README.md</files>
  <read_first>
    - `packages/market-data-client/README.md` lines 123-206 — `## Changelog` H2 at 123, `### v0.5.0 — sin publicar todavía` at 125, `### v0.4.0` at 167. The four-endpoint break table and the surrounding bullets ARE the format contract; the new prose goes between line 166 and line 167, inside the existing section. Do NOT rewrite the section — append.
    - `packages/iol-client/README.md` lines 110-174 — the `### v0.3.0` entry: the reference voice for a source-breaking callout (bold lead + parenthetical semver justification + antes/después table + the truthiness-flip paragraph).
    - `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-07-SUMMARY.md` lines 178-198 — `## Shape-change dispositions (locked)`: the verbatim identity of SC-1, SC-2 and SC-3 and the live finding IDs behind each.
    - `packages/market-data-client/src/market_data_client/models.py` — `CalendarConfigPreview` at :831 (the SC-1 replacement type), `MarketDataSnapshot` at :267 with its `BREAKING since 0.5.0 (Phase 33, SC-2)` docstring note, `Symbol.created_at`/`.updated_at` at :682-683 with the SC-3 note. These docstrings are the authoritative statement of what actually shipped.
    - `packages/market-data-client/src/market_data_client/client.py` lines 696 and 1016 — the two `preview_calendar_config` declaration sites now annotated `-> CalendarConfigPreview` (class method and module shim); the `aio` module carries the same pair.
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` § Decisions D-03, D-04 and § Claude's Discretion.
    - `.planning/phases/34-releases-por-paquete/34-PATTERNS.md` § `packages/market-data-client/README.md` — the voice/format contract and the three-row wording-anchor table.
  </read_first>
  <action>
    Append the three Phase 33 shape breaks to the EXISTING `### v0.5.0` section of
    `packages/market-data-client/README.md`, immediately after the current final bullet (the
    "Tolerancia preservada en las dos mutaciones" bullet ending around line 166) and immediately
    before the `### v0.4.0` heading at line 167. Leave the heading text and the opening paragraph
    exactly as they are in this task — Task 2 owns both, because de-provisionalizing the section
    only becomes true once the version sites move.

    Hold every convention the surrounding entries establish: Spanish prose; a bold lead line naming
    the bump class followed by a parenthetical semver justification; antes/después markdown table
    where a signature changes; every identifier, field name and type name in backticks; wrapped
    ~95-100 columns; continuation lines indented exactly 2 spaces; no trailing whitespace (the
    `trailing-whitespace` and `end-of-file-fixer` pre-commit hooks run over this file in CI).

    Required content — all three breaks, none optional, each locked `fix-shape-now` by the operator
    at the 33-07 Task 1 checkpoint and all three source-breaking:

      1. **SC-1** — `preview_calendar_config` was declared `-> CalendarConfig` but the wire returns a
         distinct preview envelope: nine declared fields were absent and three real fields were being
         discarded. The return type is now the dedicated model `CalendarConfigPreview`. State that it
         changes on both surfaces (class method and module-level shim) and in both sync and async —
         four declaration sites. Present the change as an antes/después table row in the same shape
         as the four-endpoint table already in this section (`| Función | Antes | Ahora |`), i.e.
         `preview_calendar_config` / `CalendarConfig` / `CalendarConfigPreview`. Any consumer
         annotating the result as `CalendarConfig` now fails mypy; any consumer reading one of the
         nine phantom fields was already reading a zero value and now reads the real envelope.
      2. **SC-2** — `MarketDataSnapshot.entries`, `.market_data` and `.staleness_seconds` were
         declared non-optional and arrive `null` on the no-data row of `/marketdata/latest`; all
         three are now `| None`. Name the model and all three field names in backticks. Spell out the
         consumer consequence: code that indexed or iterated these without a `None` check now fails
         mypy, and at runtime the value can legitimately be `None` rather than a typed zero.
      3. **SC-3** — `Symbol.created_at` and `.updated_at` were declared `str = ""` and are absent
         from all three write acknowledgements; both are now `str | None = None`. Name the model and
         both field names in backticks. Spell out that a consumer treating either as a non-optional
         `str` now fails mypy.

    Also update the section's framing so the breaking callout covers **seven** items, not four: the
    four Phase 31 ops-endpoint dict->modelo breaks already documented, plus these three shape fixes.
    The existing bold lead sentence about the four ops endpoints stays; add framing that makes the
    total explicit rather than leaving a reader to count.

    Do NOT touch `packages/iol-client/README.md` (D-03 — its `### v0.3.0` entry is complete).
    Do NOT touch any `pyproject.toml`, any `__init__.py` or `uv.lock` in this task.

    Commit this one file alone. Subject:
    `docs(market-data-client): documentar los tres breaks de forma de la Phase 33 en el changelog v0.5.0`
  </action>
  <verify>
    <automated>SEC=$(awk '/^### v0\.5\.0/{f=1;next} /^### v/{f=0} f' packages/market-data-client/README.md) && test -n "$SEC" && for T in '`preview_calendar_config`' '`CalendarConfigPreview`' '`CalendarConfig`' '`MarketDataSnapshot`' '`entries`' '`market_data`' '`staleness_seconds`' '`Symbol`' '`created_at`' '`updated_at`'; do printf '%s\n' "$SEC" | grep -qF -- "$T" || { echo "CALLOUT INCOMPLETE: $T"; exit 1; }; done && test "$(git show --stat --format= HEAD -- packages/market-data-client/README.md | wc -l | tr -d ' ')" -ge 1 && test "$(git show --name-only --format= HEAD | grep -v '^$' | wc -l | tr -d ' ')" = "1" && test "$(git diff --name-only HEAD~1..HEAD -- packages/iol-client/README.md | wc -l | tr -d ' ')" = "0" && uv run pre-commit run --files packages/market-data-client/README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Slicing the README from the `### v0.5.0` heading to the next `### v` heading yields a non-empty section containing all ten of these literal backticked tokens: `` `preview_calendar_config` ``, `` `CalendarConfigPreview` ``, `` `CalendarConfig` ``, `` `MarketDataSnapshot` ``, `` `entries` ``, `` `market_data` ``, `` `staleness_seconds` ``, `` `Symbol` ``, `` `created_at` ``, `` `updated_at` ``
    - That section contains an antes/después table row whose "Ahora" cell is `` `CalendarConfigPreview` `` and whose "Antes" cell is `` `CalendarConfig` ``
    - That section states the breaking scope covers seven items (four Phase 31 ops endpoints + three Phase 33 shape fixes), not four
    - `git diff --name-only HEAD~1..HEAD -- packages/iol-client/README.md | wc -l` outputs `0` — D-03's do-not-touch holds
    - `git show --name-only --format= HEAD` lists exactly one file: `packages/market-data-client/README.md`
    - `git log -1 --format=%s` reads `docs(market-data-client): documentar los tres breaks de forma de la Phase 33 en el changelog v0.5.0`
    - `uv run pre-commit run --files packages/market-data-client/README.md` passes with zero file rewrites (no trailing whitespace, file ends with a newline)
    - No `pyproject.toml`, no `__init__.py` and not `uv.lock` appear in this commit
  </acceptance_criteria>
  <done>The `### v0.5.0` section of `packages/market-data-client/README.md` documents all three Phase 33 shape breaks alongside the four Phase 31 ops-endpoint breaks, in the established voice, and one single-file `docs(market-data-client): …` commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: De-provisionalize the v0.5.0 changelog heading and bump the four version sites across both packages (D-01, D-04, SC#1)</name>
  <files>packages/market-data-client/README.md, packages/iol-client/pyproject.toml, packages/iol-client/src/iol_client/__init__.py, packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/__init__.py</files>
  <read_first>
    - `packages/market-data-client/README.md` lines 123-135 — the `### v0.5.0` heading with its trailing em-dash qualifier, and the four-sentence opening paragraph directly beneath it (lines 127-132). That paragraph states that the version bump and the tag are the responsibility of a later phase and warns that anyone building from HEAD gets a wheel whose metadata reports the previous version. Both statements become false the moment this task lands, which is precisely why this task owns them.
    - `packages/iol-client/pyproject.toml` lines 1-10 — the `[project]` block; `version = "0.2.0"` is line 3
    - `packages/iol-client/src/iol_client/__init__.py` line 87 — `__version__ = "0.2.0"`
    - `packages/market-data-client/pyproject.toml` lines 1-10 — the `[project]` block; `version = "0.4.0"` is line 3
    - `packages/market-data-client/src/market_data_client/__init__.py` line 157 — `__version__ = "0.4.0"`
    - `.github/workflows/release.yml` lines 22-51 — READ-ONLY. The tag regex at :28 and the awk version-match gate at :42-51 that hard-fails the build on any tag-vs-pyproject mismatch.
    - `.planning/phases/34-releases-por-paquete/34-PATTERNS.md` § Version bumps — the current-live-values table, the load-bearing ordering, and the `__version__` local-assertion defense.
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` § Decisions D-01, D-03, D-04.
  </read_first>
  <action>
    Two packages, four version sites, plus the changelog de-provisionalization. Ordering within the
    task is load-bearing: every `pyproject.toml` edit must land before the `uv lock` run that Task 3
    owns.

    (a) `packages/iol-client/pyproject.toml` line 3 — set the `[project]` `version` value to
    `0.3.0`. Nothing else in the file moves. `release.yml:42-51` awk-reads the FIRST `^version` line
    in this file and hard-fails the build unless it equals the version captured from the tag
    `iol-client-v0.3.0`, so this value and that tag must agree exactly. The bump class is minor on a
    0.x line: the dict->modelo change across all 16 signatures is source-breaking, which on 0.x is a
    minor per the convention already applied by `market-data-client` v0.4.0.

    (b) `packages/iol-client/src/iol_client/__init__.py` line 87 — set it to
    `__version__ = "0.3.0"`. The release pipeline does NOT validate this site. If it drifts, the
    Release ships green and every consumer reading `iol_client.__version__` gets the wrong answer,
    undetectable at release time. The local `grep -qx` assertion in this task's verify block is the
    only defense.

    (c) Commit (a) and (b) together as the iol-client bump. Subject:
    `chore(iol-client): bump to v0.3.0 (dict→modelo en las 16 firmas)`. Body: one short paragraph
    naming the bump class and headline, then a line inventorying the two aligned sites. Exactly two
    files.

    (d) `packages/market-data-client/README.md` — de-provisionalize the `### v0.5.0` section, which
    Task 1 left provisional on purpose:
      - Rewrite the heading line so it is exactly `### v0.5.0` — the trailing em-dash qualifier that
        marked it as not-yet-published is removed. This exact-line form is what the automated gate
        matches, and it is the single strongest proof the retitle happened.
      - Delete the four-sentence opening paragraph beneath the heading (currently lines 127-132) in
        its entirety. It asserts that the version bump and the tag are deferred to a later phase and
        warns that a wheel built from HEAD reports the previous version — both statements are false
        once (e) lands in this same commit. Do not "soften" or reword it into a hedge; remove it and
        let the section open directly on the bold lead line about the four ops endpoints.
        This paragraph has no Phase 28 analogue: Phase 28 authored its changelog entry fresh at bump
        time and never had to de-provisionalize a previously written provisional entry. Nothing else
        in the file guards against leaving it in place, so it is called out here explicitly.
      - Change nothing else in the section. The three shape breaks Task 1 added stay verbatim; the
        four-endpoint table stays verbatim; the `### v0.4.0` section and everything below it stays
        verbatim.

    (e) `packages/market-data-client/pyproject.toml` line 3 — set the `[project]` `version` value to
    `0.5.0`, same awk gate as (a), matched against the tag `market-data-client-v0.5.0`. Minor on a
    0.x line: seven source-breaking items (four Phase 31 ops-endpoint dict->modelo changes + the
    three Phase 33 shape fixes), same criterion the package already used for v0.4.0.

    (f) `packages/market-data-client/src/market_data_client/__init__.py` line 157 — set it to
    `__version__ = "0.5.0"`. Same unvalidated-site reasoning as (b).

    (g) Commit (d), (e) and (f) together as the market-data-client bump. Subject:
    `chore(market-data-client): bump to v0.5.0 (endpoints de ops tipados + tres fixes de forma)`.
    Body: one short paragraph naming the bump class and headline, then a line inventorying the three
    aligned sites (pyproject + `__version__` + the de-provisionalized changelog). Exactly three files.

    Touch NO other package. `higyrus-client`, `ambito-financiero-client`, `matriz-client` and
    `wallets-client` keep their current versions at every site — their public surface did not change
    in v1.6 (D-01). Touch no file under `.github/workflows/` (D-11) and do not touch
    `packages/iol-client/README.md` (D-03). Do NOT run `uv lock` in this task — D-11 mandates exactly
    one refresh and Task 3 owns it.
  </action>
  <verify>
    <automated>IV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/iol-client/pyproject.toml) && test "$IV" = "0.3.0" && grep -qx '__version__ = "0.3.0"' packages/iol-client/src/iol_client/__init__.py && MV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml) && test "$MV" = "0.5.0" && grep -qx '__version__ = "0.5.0"' packages/market-data-client/src/market_data_client/__init__.py && grep -qx '### v0.5.0' packages/market-data-client/README.md && SEC=$(awk '/^### v0\.5\.0$/{f=1;next} /^### v/{f=0} f' packages/market-data-client/README.md) && test -n "$SEC" && for T in '`preview_calendar_config`' '`CalendarConfigPreview`' '`MarketDataSnapshot`' '`entries`' '`market_data`' '`staleness_seconds`' '`Symbol`' '`created_at`' '`updated_at`'; do printf '%s\n' "$SEC" | grep -qF -- "$T" || { echo "CALLOUT REGRESSED: $T"; exit 1; }; done && for P in higyrus-client ambito-financiero-client matriz-client wallets-client; do test "$(git diff --name-only iol-client-v0.2.0..HEAD -- packages/$P/pyproject.toml packages/$P/src/*/__init__.py | wc -l | tr -d ' ')" = "0" || { echo "UNCHANGED PACKAGE TOUCHED: $P"; exit 1; }; done && test "$(git diff --name-only HEAD~2..HEAD -- packages/iol-client/README.md .github/workflows | wc -l | tr -d ' ')" = "0" && uv run pytest packages/iol-client -q && uv run pytest packages/market-data-client -q && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/iol-client/pyproject.toml` outputs exactly `0.3.0`
    - `grep -qx '__version__ = "0.3.0"' packages/iol-client/src/iol_client/__init__.py` exits 0
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml` outputs exactly `0.5.0`
    - `grep -qx '__version__ = "0.5.0"' packages/market-data-client/src/market_data_client/__init__.py` exits 0
    - `grep -qx '### v0.5.0' packages/market-data-client/README.md` exits 0 — the heading is now an exact-line match, which is only true once the trailing qualifier is removed
    - The paragraph that previously opened the `### v0.5.0` section (deferring the bump to a later phase and warning that a HEAD build reports the prior version) no longer appears anywhere in the file; the section opens directly on the bold lead line about the four ops endpoints
    - Slicing the README on the exact heading `^### v0\.5\.0$` still yields the nine break-identifier tokens from Task 1 — the de-provisionalization did not delete the content
    - For each of `higyrus-client`, `ambito-financiero-client`, `matriz-client`, `wallets-client`: `git diff --name-only iol-client-v0.2.0..HEAD -- packages/<pkg>/pyproject.toml packages/<pkg>/src/*/__init__.py | wc -l` outputs `0`
    - `git diff --name-only HEAD~2..HEAD -- packages/iol-client/README.md .github/workflows | wc -l` outputs `0` (D-03, D-11)
    - `uv run pytest packages/iol-client -q` and `uv run pytest packages/market-data-client -q` both pass. Never a bare `uv run pytest` — the root `testpaths` includes `verification/`, which carries known out-of-scope matriz failures no CI job ever executes
    - Two commits exist with subjects `chore(iol-client): bump to v0.3.0 (dict→modelo en las 16 firmas)` (exactly 2 files) and `chore(market-data-client): bump to v0.5.0 (endpoints de ops tipados + tres fixes de forma)` (exactly 3 files)
    - `uv.lock` appears in neither commit — the single refresh belongs to Task 3 (D-11)
  </acceptance_criteria>
  <done>All four version sites read their target versions and agree per package, the `### v0.5.0` heading is an exact-line match with its provisional preamble removed, the four unchanged packages are untouched, both package suites pass, and two per-package bump commits exist.</done>
</task>

<task type="auto">
  <name>Task 3: Single `uv lock` refresh, local CI mirror, credential scan, and fast-forward push of the release branch (D-05, D-07, D-11)</name>
  <files>uv.lock</files>
  <read_first>
    - `uv.lock` lines 380-390 and 484-494 — the `[[package]] name = "iol-client"` block (`version` is line 384) and the `[[package]] name = "market-data-client"` block (`version` is line 488). These are the two lines a correct refresh changes.
    - `.github/workflows/ci.yml` lines 24-140 — READ-ONLY. The four jobs and what each runs: `Lint y formato (ruff)` (uv lock --check, ruff check, ruff format --check, import-linter, lint-logging, decode-intactness, uniform-structure, surface-types), `pre-commit hooks`, `Type check (mypy)`, and the `Tests · <pkg> · py<ver>` matrix over the six packages at `:113-119` × `["3.12","3.13"]` at `:120`. Also note `paths-ignore` at `:6`/`:11` and `cancel-in-progress: true` at `:20`.
    - `.planning/phases/34-releases-por-paquete/34-PATTERNS.md` § Version bumps (the `uv lock` churn expectation and the scoped-test rule) and § PR #12 update (the preconditions block and why the third assertion is currently false).
    - `.planning/phases/34-releases-por-paquete/34-CONTEXT.md` § Decisions D-05, D-07, D-11.
    - `.planning/milestones/v1.5-phases/28-release-prep-publish-v0-3-0/28-01-PLAN.md` Task 3 — the seven-command local CI mirror, the credential-scan patterns and the no-force push contract, all reusable verbatim with the tag baselines re-pointed.
  </read_first>
  <action>
    Refresh the lockfile once, run every gate CI will run, prove the diff about to become public
    carries no credential, then publish the branch.

    (a) Run `uv lock` — exactly once, and only now. D-11 mandates a single refresh covering both
    workspace members, and it MUST run AFTER both `pyproject.toml` edits from Task 2: `uv lock` reads
    workspace-member versions from `pyproject.toml`, so running it earlier would regenerate the lock
    at the old versions and desync it, failing CI check 1 of 15 (`ci.yml:32`). Never hand-edit
    `uv.lock`.

    Then assert the churn is exactly four lines — two members × (name line unchanged, version line
    changed) means `git diff --numstat uv.lock` must report 2 insertions and 2 deletions. Phase 28
    bumped one member and saw a 2-line churn; two members means 4. A larger diff means uv re-resolved
    third-party dependencies and would smuggle an unreviewed dependency change into a release PR:
    STOP and surface it rather than committing.

    Run `uv lock --check` (exit 0), then confirm the line following `^name = "iol-client"$` is
    `version = "0.3.0"` and the line following `^name = "market-data-client"$` is
    `version = "0.5.0"`.

    Commit `uv.lock` alone. Subject:
    `chore(deps): refrescar uv.lock para iol-client v0.3.0 + market-data-client v0.5.0`.

    (b) Mirror every CI job locally, in this order, each must succeed:
    `uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`;
    `uv run mypy`; `uv run pytest packages/iol-client -q`;
    `uv run pytest packages/market-data-client -q`; `uv run pre-commit run --all-files`.
    Measure the baselines at run time rather than trusting any literal — expected counts drift with
    every phase. If `pre-commit` rewrites any file (trailing whitespace or EOF on the changelog
    edits from Tasks 1-2), amend the owning commit with the fix and re-run the whole mirror. NEVER
    run a bare `uv run pytest`: the root `testpaths` includes `verification/`, which carries known
    pre-existing matriz failures that no CI job ever executes; a bare run would block this release
    indefinitely on out-of-scope failures.

    Note that the lint job also runs the four repo-specific gates added by Phases 29/31/32
    (`lint-logging`, `decode-intactness`, `uniform-structure`, `surface-types`); `uv run pre-commit
    run --all-files` plus `uv run ruff check .` do not cover them. Run whatever `ci.yml` invokes for
    those four steps directly so the local mirror is genuinely complete — read the step bodies at
    `ci.yml:42-66` for the exact commands rather than guessing.

    (c) Assert the do-not-touch guarantees hold across the whole release diff, using the last
    published tag of each package as the baseline:
    `git diff --name-only iol-client-v0.2.0..HEAD -- .github/workflows` must produce zero lines and
    `git diff --name-only market-data-client-v0.4.0..HEAD -- .github/workflows` must produce zero
    lines (D-11).

    (d) Re-run the credential scan over the FULL diff this PR will publish to a PUBLIC default
    branch. Run `git fetch origin main` first, then check `git diff origin/main...HEAD` for: a
    JWT-shaped token (pattern `eyJ[A-Za-z0-9_-]{20,}`); a `client_secret`-style assignment of a 20+
    character value; and separately that no tracked file has a path component equal to `.env`
    (`git ls-files` filtered — only the per-package `.env.example` templates are legitimate). If ANY
    of these matches, STOP: do not push, do not proceed to plan 34-02, and surface the exact file and
    line. Never echo a token, a secret value, or the `gh`/SSH credential itself into output — report
    file and line only. This scan must run here, immediately before the push, because the diff grew
    with every commit of Phases 29-33 and with Tasks 1-3 of this plan.

    (e) Push the branch: `git push origin milestone/v1.5-mutations`. This step is BLOCKING for plan
    34-02: local is currently ahead of `origin/milestone/v1.5-mutations` (recompute the exact count
    with `git rev-list --count origin/milestone/v1.5-mutations..HEAD` rather than trusting the "44"
    figure in CONTEXT.md — it was already 47 on 2026-08-27 and grows with this plan's own four
    commits), so all of Phase 33 plus this plan is local-only and `gh pr edit 12` would describe a
    PR whose head does not contain the release.

    `--force` and `--force-with-lease` are FORBIDDEN. A force-push would rewrite the commits whose
    SHAs the Phase 29-33 SUMMARYs cross-reference by value. If the push is rejected as
    non-fast-forward, STOP and surface it — do not escalate to a force flag, and do not rebase or
    merge `origin/main` into the branch (D-07: `HEAD..main` is only the four already-contained merge
    commits of PRs #8/#9/#10/#11, so the divergence is cosmetic and a rebase would rewrite published
    history).

    (f) Do NOT run `/gsd-pr-branch` and do NOT filter `.planning/` out of the branch (D-06).
    Do NOT open, edit, merge or comment on any PR in this plan — plan 34-02 owns PR #12. Do NOT
    create or push any tag in this plan.
  </action>
  <verify>
    <automated>uv lock --check && grep -A1 '^name = "iol-client"$' uv.lock | grep -qx 'version = "0.3.0"' && grep -A1 '^name = "market-data-client"$' uv.lock | grep -qx 'version = "0.5.0"' && test "$(git show --numstat --format= HEAD -- uv.lock | awk '{print $1, $2}')" = "2 2" && uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run mypy && uv run pytest packages/iol-client -q && uv run pytest packages/market-data-client -q && uv run pre-commit run --all-files && test "$(git diff --name-only iol-client-v0.2.0..HEAD -- .github/workflows | wc -l | tr -d ' ')" = "0" && test "$(git diff --name-only market-data-client-v0.4.0..HEAD -- .github/workflows | wc -l | tr -d ' ')" = "0" && git fetch origin main --quiet && ! git diff origin/main...HEAD | grep -qE 'eyJ[A-Za-z0-9_-]{20,}' && ! git diff origin/main...HEAD | grep -qiE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}' && test -z "$(git ls-files | grep -E '(^|/)\.env$')" && git fetch origin milestone/v1.5-mutations --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/milestone/v1.5-mutations)" && test -z "$(git status --porcelain)" && test -z "$(git tag -l 'iol-client-v0.3.0')" && test -z "$(git tag -l 'market-data-client-v0.5.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` exits 0
    - The line following `^name = "iol-client"$` in `uv.lock` is exactly `version = "0.3.0"`, and the line following `^name = "market-data-client"$` is exactly `version = "0.5.0"`
    - `git show --numstat --format= HEAD -- uv.lock | awk '{print $1, $2}'` outputs exactly `2 2` — two members × one version line each; a wider diff means uv re-resolved third-party dependencies and the task must halt instead of committing
    - `git log -1 --format=%s` reads `chore(deps): refrescar uv.lock para iol-client v0.3.0 + market-data-client v0.5.0` and `git show --name-only --format= HEAD` lists only `uv.lock`
    - All local-mirror commands exit 0: `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`, `uv run mypy`, `uv run pytest packages/iol-client -q`, `uv run pytest packages/market-data-client -q`, `uv run pre-commit run --all-files` (zero file rewrites), plus the four repo-specific lint gates invoked by `ci.yml:42-66`
    - No bare `uv run pytest` was executed at any point in this plan
    - `git diff --name-only iol-client-v0.2.0..HEAD -- .github/workflows | wc -l` outputs `0` and `git diff --name-only market-data-client-v0.4.0..HEAD -- .github/workflows | wc -l` outputs `0`
    - `git diff origin/main...HEAD | grep -E 'eyJ[A-Za-z0-9_-]{20,}'` produces no match
    - `git diff origin/main...HEAD | grep -iE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}'` produces no match
    - `git ls-files | grep -E '(^|/)\.env$'` produces empty output
    - `git rev-parse HEAD` equals `git rev-parse origin/milestone/v1.5-mutations`
    - The command history shows `git push origin milestone/v1.5-mutations` with no `--force` and no `--force-with-lease`, and shows no `git rebase` and no `git merge origin/main`
    - The pending-commit count was recomputed at run time with `git rev-list --count origin/milestone/v1.5-mutations..HEAD` before the push and recorded in the SUMMARY; the stale "44" figure from CONTEXT.md was not used
    - `git status --porcelain` is empty (clean tree — precondition for plan 34-02)
    - `git tag -l 'iol-client-v0.3.0'` and `git tag -l 'market-data-client-v0.5.0'` are both empty — no tag is created in this plan
  </acceptance_criteria>
  <done>`uv.lock` was refreshed by a single `uv lock` run registering both bumped members with a 2-insertion/2-deletion churn, every local CI-mirror gate is green, the workflow diffs are empty since both prior release tags, the credential scan over `origin/main...HEAD` is clean, and `origin/milestone/v1.5-mutations` resolves to local `HEAD` via a plain fast-forward push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → public GitHub repo | `gravity-quant/market-libs` is a PUBLIC repository. This push publishes the whole Phase 29-34 diff, including the `.planning/` artifacts kept by D-06. Everything crossing this boundary becomes permanently public. |
| `pyproject.toml` version strings → `release.yml` build gate | The pipeline reads each package's version from the tagged tree and refuses to build on mismatch; a wrong value produces a failed or mislabelled public artifact. Two packages means two independent instances of this boundary. |
| `uv lock` resolver → committed `uv.lock` | An unreviewed third-party dependency re-resolution can enter the repo through a lockfile refresh nobody reads. |
| agent shell → GitHub credentials (`gh` OAuth token, SSH key) | Credentials are ambient in the environment; any echo lands in transcripts and logs. |
| local branch history → published history | Dozens of SUMMARY files across Phases 29-33 cite commit SHAs by value; a history rewrite orphans every one of them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Information Disclosure | the `origin/main...HEAD` diff published to a PUBLIC default branch | mitigate | Task 3 (d) re-runs the credential scan immediately before the push — JWT pattern `eyJ[A-Za-z0-9_-]{20,}`, `client_secret`-style assignments, and any tracked `.env` — and STOPS the phase on any match. Findings report file and line only, never the matched value. ASVS L1 V14, V2. |
| T-34-02 | Tampering | version strings vs. the release tags, ×2 packages | mitigate | Task 2 asserts `pyproject` == `__version__` per package (0.3.0 for iol, 0.5.0 for market-data) and Task 3 asserts both `uv.lock` members agree. The pipeline validates ONLY `pyproject.toml` (`release.yml:47`), so a drifted `__version__` would ship a silently mislabelled artifact — the local assertions are the sole defense. ASVS L1 V5. |
| T-34-03 | Tampering | wrong package set published | mitigate | Task 2's per-package assertion loop proves the four unchanged packages have zero version-site diff since `iol-client-v0.2.0`. D-01 fixes the bump set at exactly two. |
| T-34-04 | Supply Chain / Tampering | `uv.lock` refresh | mitigate | Task 3 (a) asserts the churn is exactly 2 insertions + 2 deletions (two members × one version line); any larger diff means a third-party re-resolution and halts the task before commit. |
| T-34-05 | Tampering / Repudiation | published git history (Phases 29-33 SHAs) | mitigate | Task 3 (e) forbids `--force` / `--force-with-lease` and forbids rebasing or merging `origin/main` into the branch (D-07); the push is a plain fast-forward and a rejection halts the task instead of escalating. |
| T-34-07 | Information Disclosure | `gh` OAuth token / SSH key in agent output | mitigate | Never echo a token or secret value; `gh auth status` already redacts. Scan findings report file and line only. ASVS L1 V2. |
| T-34-SC | Tampering | uv/pip/cargo installs | accept | This phase installs zero external packages — `uv lock` re-resolves the existing dependency set with only two local workspace members' version strings changed, and the 2/2 churn assertion proves no third-party change entered. No package legitimacy audit is applicable. |
</threat_model>

<verification>
1. The `### v0.5.0` section names all three Phase 33 shape breaks with the ten backticked identifier tokens, alongside the four Phase 31 ops-endpoint breaks.
2. The `### v0.5.0` heading is an exact-line match and the provisional opening paragraph is gone.
3. `packages/iol-client/README.md` is byte-unchanged across this phase (D-03).
4. iol-client reads `0.3.0` at pyproject + `__version__`; market-data-client reads `0.5.0` at pyproject + `__version__`.
5. `uv.lock` registers both members at their new versions, `uv lock --check` exits 0, churn is exactly `2 2`.
6. The four unchanged packages have zero version-site diff since `iol-client-v0.2.0`.
7. Every local CI-mirror gate is green, including the four repo-specific lint gates from `ci.yml:42-66`.
8. `.github/workflows/` has a zero-line diff since both prior release tags.
9. The credential scan over `origin/main...HEAD` is clean; no `.env` is tracked.
10. `origin/milestone/v1.5-mutations` == local `HEAD`; the push used no force flag and no rebase.
11. Exactly four commits were produced: the changelog `docs(market-data-client)`, the two per-package `chore(<pkg>)` bumps, and the single-file `chore(deps)` lock refresh.
12. No tag exists and no PR was touched at the end of this plan.
</verification>

<success_criteria>
- Every edit in this plan is reversible with a commit; nothing outward-facing and irreversible has happened.
- Both source-breaking packages are release-ready with a changelog that makes every break discoverable rather than silent — seven items for `market-data-client`, the already-complete `### v0.3.0` entry for `iol-client`.
- The lockfile was refreshed exactly once for both bumps and provably introduced no third-party dependency change (D-11).
- The release branch exists on `origin` at local `HEAD`, unblocking `gh pr edit 12` in plan 34-02 (D-05).
- The two blocking human gates remain ahead, in plans 34-02 and 34-03 — this plan added none (D-08).
</success_criteria>

<output>
Create `.planning/phases/34-releases-por-paquete/34-01-SUMMARY.md` when done.

Record in the SUMMARY: the four commit SHAs, the measured `uv.lock` churn, both pytest counts, the
full local-mirror results including the four repo-specific lint gates, the credential-scan result,
the **recomputed** ahead/behind counts at push time (with the value actually measured, not the "44"
figure CONTEXT.md carries), and the explicit memory-file decision from `<scope_decisions>` (refresh
the existing `market-data-client-releases.md` in 34-03; do not create `iol-client-releases.md`).
</output>
