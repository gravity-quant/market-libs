---
phase: 04-higyrus-verification
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - main_higyrus.py
  - packages/higyrus-client/.env.example
autonomous: false
requirements: [HIGY-01, HIGY-02, HIGY-03, HIGY-05, HIGY-06, HIGY-07]
tags: [verification, driver, probes, live-api, schema-snapshot, safemodel, bidirectional-diff, drop-none-parity, dual-sync-async]
must_haves:
  truths:
    - "main_higyrus.py expone 18 probes nombrados en orden D-HIGY-10: probe_login_sync, probe_login_async, probe_get_health_sync, probe_get_health_async, probe_get_listado_cuentas_sync, probe_get_listado_cuentas_async, probe_get_movimientos_sync, probe_get_movimientos_async, probe_get_posicion_valuada_sync, probe_get_posicion_valuada_async, probe_get_posiciones_sync, probe_get_posiciones_async, probe_parity_sync_async, probe_field_type_map, probe_schema_snapshot, probe_errors_envelope_sync, probe_errors_envelope_async, probe_auth_401"
    - "D-HIGY-10 numbering describes the PRESENTATION order (printed PROBE lines + findings/summary), NOT the execution order; main() may consolidate async probes via D-HIGY-13 single asyncio.run"
    - "EXECUTION order in main(): (1) probe_login_sync → (2) probe_get_health_sync → (3) probe_get_listado_cuentas_sync (resolves _resolved_cuenta) → (4) probe_get_movimientos_sync → (5) probe_get_posicion_valuada_sync → (6) probe_get_posiciones_sync → (7) asyncio.run(_async_main(today, resolved_cuenta=_resolved_cuenta, sync_payloads=...)) consolidating probes 2/4/6/8/10/12/17 + async parity capture → (8) probe_parity_sync_async (probe 13) → (9) probe_field_type_map (probe 14) → (10) probe_schema_snapshot (probe 15) → (11) probe_errors_envelope_sync (probe 16) → (12) probe_auth_401 (probe 18) — guarantees _resolved_cuenta is populated by sync probe 5 BEFORE any async cuenta-dependent probe (6/8/10/12) runs"
    - "ProbeResult objects are COLLECTED during execution and PRINTED at the end in D-HIGY-10 declared order (sorted by presentation index); the per-probe stdout sequence reflects presentation order, not the actual execution order"
    - "NO async cuenta-dependent probe (probes 6/8/10/12 = get_movimientos_async, get_posicion_valuada_async, get_posiciones_async, parity capture for probe 13) returns SKIPPED with reason 'no _resolved_cuenta resuelto' on a successful run — _resolved_cuenta is GUARANTEED non-None before _async_main is invoked (unless probe 5 itself SKIPPED/FINDING, in which case async cascade is expected)"
    - "Cada probe maneja excepciones y el driver continúa todos los probes con exit 0 salvo crash inesperado (D-04 Phase 2 mirror)"
    - "stdout = una línea verbatim por probe `PROBE <name>: <status> <detail>` + summary final `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` (D-02 Phase 2 mirror)"
    - "Driver llama write_findings('higyrus-client') al inicio (idempotente, D-03 Phase 2 mirror)"
    - "Cualquier discrepancia detectada en vivo llama append_finding(...) con clases SHAPE/AUTH/ERROR-MAP/PARAM/SYNC-ASYNC-DRIFT (D-HIGY-5)"
    - "probe_auth_401 opt-in via VERIFY_HIGYRUS_BAD_CREDS=1 (D-HIGY-10 #18 + D-HIGY-14); sin la var imprime SKIPPED y sigue"
    - "probe_auth_401 es one-shot, sin retry ni sleep ni loop; usa try/finally para restaurar HIGYRUS_PASSWORD original tras inyección de bad_password (D-HIGY-10 #18, T-4-07 mitigation)"
    - "probe_auth_401 usa configure(password=...) directo en lugar de mutación de _password — Higyrus NO tiene _refresh_token a preservar, configure() es simétrico y safe (deviation explícita vs Phase 3 IOL CR-03)"
    - "Cascade SKIPPED: si probe_login_sync o probe_login_async fallan, los probes downstream emiten SKIPPED con razón 'auth failed' (D-HIGY-10 cascade) — implementado vía flag module-level `_auth_failed: bool` (Phase 3 D-IOL-3 mirror)"
    - "_resolved_cuenta: str | None resuelto por probe 5 (probe_get_listado_cuentas_sync) = cuentas[0].id o HIGYRUS_SAMPLE_CUENTA env override; downstream que necesita cuenta hace early-return SKIPPED si es None (D-HIGY-11)"
    - "Async lifecycle: un único asyncio.run(_async_main(...)) y await aio.aclose() dentro de contextlib.suppress(Exception) (D-HIGY-13, mirror IN-03 Phase 2)"
    - "probe_schema_snapshot escribe 5 JSON files en .planning/verification/schemas/higyrus-client/ con envelope D-21 y D-25 no-overwrite-on-drift (D-HIGY-16)"
    - "probe_field_type_map ejecuta `_diff_safemodel_bidirectional(payload, model_cls, path)` recursivo sobre los 4 endpoints con modelos (Cuenta, Movimiento, Posicion, PosicionValuada) y emite un finding `SHAPE OPEN` por discrepancia, ambas direcciones (D-HIGY-3/4/5)"
    - "probe_parity_sync_async captura `httpx.Request.url.query` via monkey-patch del `_client.request` bound method (try/finally restore) para get_movimientos (params optional=None) y verifica que sync.query == async.query — detecta o niega la deviation `drop_none` (D-HIGY-10 #13, T-4-05 mitigation)"
    - "probe_errors_envelope_sync y probe_errors_envelope_async son always-on: invocan get_movimientos con id_cuenta inválido y verifican que el HigyrusAPIError raised tiene errors=[{title, detail}] poblado (D-HIGY-10 #16-17)"
    - "Todos los prints pasan por safe_print(text, secrets=[HIGYRUS_USER, HIGYRUS_PASSWORD, _sync_token_snapshot, _async_token_snapshot]) (D-HIGY-15) — lista de secrets se EXTIENDE dinámicamente CAPTURANDO POR VALOR (snapshots) AMBOS tokens: `_sync_token_snapshot = higyrus_client.client._token` tras probe_login_sync exitoso y `_async_token_snapshot = higyrus_client.aio._token` tras probe_login_async exitoso; los snapshots se captuan por valor (no por referencia al atributo módulo) para que probe_auth_401 (que vía `configure(password=...+'_INVALID')` puede setear `_token = None`) no nulifique la lista de redacción mid-run — defense-in-depth Phase 3 CR-03 mirror"
    - "Driver stdout = conteos + shape descriptor únicamente, NUNCA contenido de payloads (D-HIGY-2 — primera fase con PII real)"
    - "El driver lee state privado higyrus_client.client._base_url / _token vía atributo directo (precedente Phase 2 mutation_gate.py:55)"
    - ".env.example tiene apendeadas las 4 env vars opcionales nuevas D-HIGY-14: HIGYRUS_SAMPLE_CUENTA, HIGYRUS_SAMPLE_TIPO_CUENTA (default propia), HIGYRUS_SAMPLE_NIVEL (default detalle), VERIFY_HIGYRUS_BAD_CREDS"
    - "El driver ejecuta exitosamente al menos una vez contra Higyrus en vivo y genera los 5 schema snapshots + findings file (checkpoint humano observa el run)"
  artifacts:
    - path: "main_higyrus.py"
      provides: "Rewritten Higyrus live verification driver with 18 probes (was smoke test)"
      contains: "def probe_login_sync"
      contains_all: ["probe_login_sync", "probe_login_async", "probe_get_health_sync", "probe_get_health_async", "probe_get_listado_cuentas_sync", "probe_get_listado_cuentas_async", "probe_get_movimientos_sync", "probe_get_movimientos_async", "probe_get_posicion_valuada_sync", "probe_get_posicion_valuada_async", "probe_get_posiciones_sync", "probe_get_posiciones_async", "probe_parity_sync_async", "probe_field_type_map", "probe_schema_snapshot", "probe_errors_envelope_sync", "probe_errors_envelope_async", "probe_auth_401", "_diff_safemodel_bidirectional", "_auth_failed", "_resolved_cuenta"]
    - path: "packages/higyrus-client/.env.example"
      provides: ".env.example with the 4 optional Phase 4 vars appended (D-HIGY-14)"
      contains: "HIGYRUS_SAMPLE_CUENTA"
      contains_also: "VERIFY_HIGYRUS_BAD_CREDS"
    - path: ".planning/verification/schemas/higyrus-client/get-health.json"
      provides: "DRIFT-01 mirror baseline schema for get_health (generated by live run)"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-listado-cuentas.json"
      provides: "DRIFT-01 mirror baseline schema for get_listado_cuentas (generated by live run)"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-movimientos.json"
      provides: "DRIFT-01 mirror baseline schema for get_movimientos (generated by live run)"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-posicion-valuada.json"
      provides: "DRIFT-01 mirror baseline schema for get_posicion_valuada (generated by live run)"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-posiciones.json"
      provides: "DRIFT-01 mirror baseline schema for get_posiciones (generated by live run)"
      contains: "\"schema\":"
    - path: ".planning/verification/higyrus-client-findings.md"
      provides: "Phase 4 findings file (skeleton + first live run findings)"
      contains: "# Findings: higyrus-client"
  key_links:
    - from: "main_higyrus.py"
      to: "verification.findings.append_finding"
      via: "from verification.findings import append_finding"
      pattern: "from verification.findings import append_finding"
    - from: "main_higyrus.py"
      to: "verification (safe_print, schema_of, write_findings, require_env)"
      via: "from verification import ..."
      pattern: "from verification import .*require_env"
    - from: "main_higyrus.py"
      to: "higyrus_client (sync) + aio submódulo + HigyrusAPIError + HigyrusAuthError + models"
      via: "import higyrus_client; from higyrus_client import aio, HigyrusAPIError, HigyrusAuthError; from higyrus_client.models import Cuenta, Movimiento, Posicion, PosicionValuada, SafeModel"
      pattern: "from higyrus_client.models import"
    - from: "main_higyrus.py::_diff_safemodel_bidirectional"
      to: "higyrus_client.models.{Cuenta,Movimiento,Posicion,PosicionValuada}"
      via: "typing.get_type_hints + recursive descent into nested SafeModel and list[SafeModel]"
      pattern: "get_type_hints"
    - from: "main_higyrus.py::probe_parity_sync_async"
      to: "higyrus_client.client._client.request / higyrus_client.aio._client.request"
      via: "bound-method monkey-patch with try/finally restore — captures httpx.Request.url.query"
      pattern: "_client.request"
    - from: "main_higyrus.py::probe_auth_401"
      to: "higyrus_client.configure(password=HIGYRUS_PASSWORD + '_INVALID')"
      via: "try/finally injection (D-HIGY-10 #18, T-4-07 mitigation)"
      pattern: "_INVALID"
    - from: "packages/higyrus-client/.env.example"
      to: "main_higyrus.py env vars (D-HIGY-14)"
      via: "appended optional vars block"
      pattern: "HIGYRUS_SAMPLE_CUENTA"
---

<objective>
Re-escribir `main_higyrus.py` desde su estado actual (smoke test mínimo: `require_env` + `login()` + `get_health` + `get_listado_cuentas`, ~50 líneas) a un driver completo con 18 probes nombrados en orden D-HIGY-10 que cierra el loop end-to-end de Phase 4 sobre Higyrus en vivo: auth (login sync + async, HIGY-01), happy-path sweep dual (5 endpoints × 2 surfaces = 10 probes, HIGY-02), parity sync↔async via `httpx.Request.url.query` capture (HIGY-06 + verifica deviation `drop_none` conocida en aio._request), diff bidireccional recursivo `_diff_safemodel_bidirectional(payload, model_cls)` sobre los 4 endpoints con modelos (HIGY-03), 5 schema snapshots (DRIFT-01 mirror, D-HIGY-16), errors envelope always-on en ambas surfaces con id_cuenta inválido (HIGY-05), y opt-in 401 last-in-sequence (HIGY-AUTH).

Todos los probes alimentan `.planning/verification/higyrus-client-findings.md` vía `append_finding` y emiten líneas verbatim a stdout más un summary final, con secrets dinámicamente extendidos para incluir `_token` capturado por `login()` (D-HIGY-15). Además, este plan **appendea** las 4 env vars opcionales de D-HIGY-14 a `.env.example` (renombrando los `HIGYRUS_TEST_*` heredados a `HIGYRUS_SAMPLE_*` por la convención Phase 4).

Es el **slice central de Phase 4**: el fix HIGY-04 del Plan 04-01 está implementado pero no verificado en vivo; este plan corre el cliente completo contra Higyrus real (primera fase con PII real en payloads) y deja al Plan 04-03 el cierre con tests Verified-live + commit baseline.

Purpose: Cumplir HIGY-01..03 + HIGY-05..07 corriendo el cliente Higyrus completo en vivo y produciendo, de forma reproducible y no-destructiva (auth-once discipline + opt-in 401 + single-shot + safe_print con secrets dinámicos + driver stdout sin valores de payload), tres clases de artefactos: el findings markdown + 5 schema snapshots JSON que se commitearán en el Plan 04-03 + `.env.example` actualizado.

Output: `main_higyrus.py` reescrito (~700 líneas estimadas); al invocarlo a mano con credenciales reales (`uv run --package higyrus-client python main_higyrus.py`), emite 18 líneas `PROBE <name>: <status>` + `SUMMARY: ...` y exit 0; en su primera corrida exitosa genera/actualiza `.planning/verification/higyrus-client-findings.md` (esqueleto + cualquier finding OPEN/EXPECTED) y los 5 schema snapshots en `.planning/verification/schemas/higyrus-client/`. `.env.example` con las 4 vars opcionales documentadas. NO se ejecuta dentro del suite pytest (D-05 Phase 2 mirror).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-higyrus-verification/04-CONTEXT.md
@.planning/phases/04-higyrus-verification/04-RESEARCH.md
@.planning/phases/04-higyrus-verification/04-PATTERNS.md
@.planning/phases/04-higyrus-verification/04-VALIDATION.md
@.planning/phases/04-higyrus-verification/04-01-PLAN.md
@.planning/phases/03-iol-verification/03-CONTEXT.md
@.planning/phases/03-iol-verification/03-02-PLAN.md
@.planning/phases/02-mbito-verification/02-CONTEXT.md
@.planning/phases/02-mbito-verification/02-02-PLAN.md
@.planning/phases/02-mbito-verification/02-REVIEW.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_higyrus.py
@main_iol.py
@main_ambito_financiero.py
@main_verify.py
@verification/findings.py
@verification/__init__.py
@verification/schema.py
@verification/redaction.py
@verification/env_gate.py
@verification/mutation_gate.py
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/src/higyrus_client/aio.py
@packages/higyrus-client/src/higyrus_client/exceptions.py
@packages/higyrus-client/src/higyrus_client/models.py
@packages/higyrus-client/src/higyrus_client/__init__.py
@packages/higyrus-client/.env.example
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Rewrite main_higyrus.py with 18 named probes + _diff_safemodel_bidirectional helper + parity capture + summary + safe_print dynamic secrets</name>
  <files>main_higyrus.py</files>
  <read_first>
    - main_higyrus.py (estado actual: ~50 líneas, smoke test mínimo con `require_env` + `iol_client.login()` implícito en `get_health()` + `get_listado_cuentas()` + `safe_print`; se reescribe completo)
    - main_iol.py (ANALOG DIRECTO Phase 3, 1666 líneas, 15 probes; copiar estructura — module docstring, imports, ProbeResult, _fid_counter/_next_fid, _auth_failed/_auth_failure_reason flags, cascade SKIPPED pattern, probe_login_* con append_finding AUTH, probe_get_*_sync/async con try/except patterns típicos auth/api/exception → finding, _write_or_check_schema helper con D-21 envelope + D-25 no-overwrite, _async_main con contextlib.suppress(Exception) aclose, main() orchestrator con safe_print dynamic secrets)
    - main_ambito_financiero.py (analog secundario Phase 2: 7 probes; copiar estructura del summary final y stdout verbatim D-02)
    - main_verify.py (multi-driver runner — el driver Higyrus se invoca standalone, pero el main_verify.py muestra el patrón de driver aggregation)
    - .planning/phases/04-higyrus-verification/04-PATTERNS.md (sección 1 "main_higyrus.py REWRITE" con imports verbatim, módule-level constants, probes structures; sección 2/3/4 con código verbatim de Pattern 1 (bidirectional diff), Pattern 2 (parity capture via monkey-patch), Pattern 3 (cascade SKIPPED via _auth_failed), Pattern 5 (opt-in 401 single-shot))
    - .planning/phases/04-higyrus-verification/04-RESEARCH.md (Pattern 1 `_diff_safemodel_bidirectional` código completo con `_is_optional`, `_nested_safemodel_class`, `_is_list_of_safemodel`; Pattern 2 `_capture_sync_query_string` y `_capture_async_query_string` code; Pattern 5 `probe_auth_401` con configure(password+_INVALID) try/finally; Architectural Responsibility Map; Pitfall analysis)
    - .planning/phases/04-higyrus-verification/04-CONTEXT.md (D-HIGY-1..18 lockedas — específicamente D-HIGY-2 stdout = counts + shape no values, D-HIGY-3 recursive nested models, D-HIGY-4 helper inline module-level, D-HIGY-5 path qualifier `.cuenta.administrador.operador.idExterno` formato con leading dot, D-HIGY-7/8 fix sentinel (verificado in-vivo NO via probe — los regression tests del Plan 04-01 lo cubren), D-HIGY-10 18 probes orden, D-HIGY-11 _resolved_cuenta flow, D-HIGY-12 date ranges today-derived, D-HIGY-13 single asyncio.run, D-HIGY-14 env vars, D-HIGY-15 safe_print dynamic _token, D-HIGY-16 5 schemas envelope D-21)
    - packages/higyrus-client/src/higyrus_client/client.py (DESPUÉS de Plan 04-01: contiene los 5 raises con `HigyrusAPIError(0, ...)`; el driver lee `higyrus_client.client._base_url` y `higyrus_client.client._token` vía atributo directo)
    - packages/higyrus-client/src/higyrus_client/aio.py (DESPUÉS de Plan 04-01: mirror async; el driver lee `higyrus_client.aio._token` y `higyrus_client.aio._client` para el monkey-patch de Pattern 2)
    - packages/higyrus-client/src/higyrus_client/models.py (SafeModel + 4 modelos top-level: Cuenta, Movimiento, Posicion, PosicionValuada; nested: DisposicionesGenerales, Domicilio, PersonaRelacionada, MedioComunicacion, CuentaBancaria, Administrador→{Agente,Operador,Sucursal}, Parking; `get_type_hints` introspection es la fuente de truth para el diff bidireccional)
    - packages/higyrus-client/src/higyrus_client/exceptions.py (jerarquía: `HigyrusAPIError(status_code, errors, timestamp)` típed; WR-01 mirror — usar `exc.status_code` directo)
    - packages/higyrus-client/src/higyrus_client/__init__.py (re-exports públicos: el driver importa `HigyrusAPIError, HigyrusAuthError` del barrel; `aio` se importa via `from higyrus_client import aio`)
    - verification/findings.py (append_finding firma hardened post Phase 2/3; class_ in FINDING_CLASSES; status in STATUS_LIFECYCLE; preserva status humano CONFIRMED/FIXED/EXPECTED/NO-FIX)
    - verification/__init__.py (barrel: `from verification import safe_print, schema_of, write_findings, require_env`; del directo `from verification.findings import append_finding` también válido)
    - verification/redaction.py (safe_print firma `safe_print(text: str, secrets: list[str]) -> None`; el regex `_BEARER` cubre tokens reflejados aún sin secrets enumerados; D-HIGY-15: secrets dinámicos extendidos post-login)
    - verification/env_gate.py (require_env firma; SKIPPED format `SKIPPED <pkg>: missing X, Y`)
    - verification/schema.py (schema_of firma; PII-free por construcción; recursivo)
    - verification/mutation_gate.py:55 (precedente para acceso a state privado: `matriz_client.client._base_url` — pattern de lectura directa)
    - .planning/verification/FINDINGS-TEMPLATE.md (clases fijas D-09 Phase 1: SHAPE, AUTH, ERROR-MAP, PARAM, SYNC-ASYNC-DRIFT, NO-DATA, ANTI-BOT; estados D-08 Phase 1: OPEN, CONFIRMED, FIXED, EXPECTED, NO-FIX)
    - CLAUDE.md (Python 3.12+, ruff line-length=100, double quotes, 4 espacios, mypy --strict, `from __future__ import annotations` mandatory, no wildcard/relative imports)
  </read_first>
  <action>
    Re-escribir COMPLETAMENTE `main_higyrus.py` siguiendo el bosquejo de `04-PATTERNS.md` sección 1 (~700 líneas estimadas; estructura igual a Phase 3 con 3 probes más + 3 mecanismos net-new). El smoke test actual se reemplaza enteramente.

    **Imports al tope (orden: future → stdlib → third-party → local; alfabético dentro de grupo):**
    - `from __future__ import annotations`
    - stdlib: `asyncio`, `contextlib`, `datetime as dt`, `json`, `os`, `dataclasses.dataclass`, `pathlib.Path`, `collections.abc.Iterator`, `types.{NoneType, UnionType}`, `typing.{Any, Union, get_args, get_origin, get_type_hints}`
    - local verification: `from verification import require_env, safe_print, schema_of, write_findings` y `from verification.findings import append_finding`
    - local higyrus_client: `import higyrus_client`, `from higyrus_client import HigyrusAPIError, HigyrusAuthError, aio`, `from higyrus_client.models import Cuenta, Movimiento, Posicion, PosicionValuada, SafeModel`

    NOTA: NO importar `HigyrusAuthorizationError` ni `HigyrusRateLimitError` salvo que se use explícitamente — son subclases de `HigyrusAPIError` y el driver las atrapa via padre. NO importar `httpx` — Higyrus NO tiene anti-bot UA.

    **Module docstring** (estructura per 04-PATTERNS.md sección 1):
    Describir los 18 probes en orden D-HIGY-10, env vars requeridas (`HIGYRUS_USER`, `HIGYRUS_PASSWORD`, `HIGYRUS_BASE_URL`), env vars opcionales (`HIGYRUS_CLIENT_ID`, `HIGYRUS_SAMPLE_CUENTA`, `HIGYRUS_SAMPLE_TIPO_CUENTA`, `HIGYRUS_SAMPLE_NIVEL`), opt-in `VERIFY_HIGYRUS_BAD_CREDS=1` para probe 18, artefactos generados (findings markdown + 5 schemas), reglas de seguridad (auth-once, single-shot 401, cascade SKIPPED, stdout sin valores PII, primera fase con datos reales).

    **Constantes module-level (después de imports + docstring):**
    - `_PKG = "higyrus-client"`
    - `_REPO_ROOT = Path(__file__).resolve().parent`
    - `_SCHEMA_DIR = _REPO_ROOT / ".planning" / "verification" / "schemas" / _PKG`
    - `_SCHEMA_FILES: dict[str, Path] = {"get_health": _SCHEMA_DIR / "get-health.json", "get_listado_cuentas": _SCHEMA_DIR / "get-listado-cuentas.json", "get_movimientos": _SCHEMA_DIR / "get-movimientos.json", "get_posicion_valuada": _SCHEMA_DIR / "get-posicion-valuada.json", "get_posiciones": _SCHEMA_DIR / "get-posiciones.json"}` (D-HIGY-16)
    - `_ENDPOINT_TEMPLATES: dict[str, str] = {"get_health": "/api/health", "get_listado_cuentas": "/api/cuentas/listadoCuentas", "get_movimientos": "/api/cuentas/{id_cuenta}/movimientos", "get_posicion_valuada": "/api/cuentas/{id_cuenta}/posicionValuada", "get_posiciones": "/api/cuentas/{id_cuenta}/posiciones"}` (verificar paths al leer client.py)
    - `_SAMPLE_CUENTA: str | None = os.getenv("HIGYRUS_SAMPLE_CUENTA")` (D-HIGY-14 optional override)
    - `_SAMPLE_TIPO_CUENTA: str = os.getenv("HIGYRUS_SAMPLE_TIPO_CUENTA", "propia")` (D-HIGY-14 default)
    - `_SAMPLE_NIVEL: str = os.getenv("HIGYRUS_SAMPLE_NIVEL", "detalle")` (D-HIGY-14 default)
    - `_fid_counter: int = 0` + `_next_fid()` helper (mirror Phase 2/3)
    - `_auth_failed: bool = False` + `_auth_failure_reason: str = ""` (cascade SKIPPED flag, mirror Phase 3 D-IOL-3)
    - `_resolved_cuenta: str | None = None` (D-HIGY-11; set por probe 5)

    **`ProbeResult` dataclass:** frozen+slots, fields `name: str`, `status: str` ("PASS"|"FAIL"|"SKIPPED"|"FINDING"), `detail: str` (mirror Phase 2/3 verbatim).

    **`_next_fid() -> str`:** mismo helper Phase 2/3 (`F-{counter:02d}` con `global _fid_counter`).

    **Helper bidireccional `_diff_safemodel_bidirectional(payload, model_cls, path)` (Pattern 1 verbatim de 04-RESEARCH.md):**
    Función privada module-level: `def _diff_safemodel_bidirectional(payload: Any, model_cls: type, path: str = "") -> Iterator[tuple[str, str, str]]`. Yields tuples `(path, direction, key)` donde `direction in {"model-only", "wire-only"}`. Reglas:
    - Si `not isinstance(payload, dict)`: return (no diff posible)
    - `hints = get_type_hints(model_cls)`; `model_keys = set(hints)`; `wire_keys = set(payload)`
    - Direction A (`model \ wire` → "model-only"): para cada key en `sorted(model_keys - wire_keys)`: si `_is_optional(hints[key])` continue (explícitamente nullable, no FALSE PASS); sino yield `(path, "model-only", key)`
    - Direction B (`wire \ model` → "wire-only"): para cada key en `sorted(wire_keys - model_keys)`: yield `(path, "wire-only", key)`
    - Recursión: para cada key en `model_keys & wire_keys`: si `_nested_safemodel_class(hints[key])` no es None: si `_is_list_of_safemodel(hints[key])` y `payload[key]` es lista no vacía: `yield from _diff_safemodel_bidirectional(payload[key][0], nested_cls, f"{path}.{key}[0]")`; sino `yield from _diff_safemodel_bidirectional(payload[key], nested_cls, f"{path}.{key}")`
    - Helpers privados: `_is_optional(hint) -> bool` (`get_origin in {Union, UnionType}` y `NoneType in get_args`), `_nested_safemodel_class(hint) -> type | None` (issubclass `SafeModel` o `list[SafeModel]`), `_is_list_of_safemodel(hint) -> bool`

    Path qualifier format: leading dot (D-HIGY-5 examples: `.cuenta.administrador.operador.idExterno`, `.movimiento.idMovimientos[0]`).

    **Helpers para parity capture (Pattern 2 verbatim de 04-RESEARCH.md):**
    `_capture_sync_query_string(cuenta, fecha_desde, fecha_hasta) -> str | None`: monkey-patch del **bound method** `higyrus_client.client._client.request` con try/finally restore; llama `higyrus_client.get_movimientos(cuenta, fecha_desde, fecha_hasta)` (los 4 params optional adicionales se omiten = None → activa drop_none); captura `resp.request.url.query` (decoded utf-8 si bytes).

    `_capture_async_query_string(cuenta, fecha_desde, fecha_hasta) -> str | None`: mirror async; primero hace `await aio._ensure_http_client()` (helper interno aio que asegura `_client is not None`) — si no existe ese helper en aio.py, hacer un init lazy llamando un endpoint dummy o forzando el primer request; luego monkey-patch sobre `aio._client.request` con `async def _spy_request` que await el original y captura query; try/finally restore.

    **Cascade SKIPPED via `_auth_failed` flag (Pattern 3 mirror Phase 3 D-IOL-3):**
    Cada probe downstream inicia con `if _auth_failed: return ProbeResult("<name>", "SKIPPED", f"auth failed: {_auth_failure_reason}")`. Probes que también dependen de `_resolved_cuenta` agregan segundo guard: `if _resolved_cuenta is None: return ProbeResult("<name>", "SKIPPED", "no _resolved_cuenta resuelto")`.

    **Schema snapshot helper `_write_or_check_schema(func_name, endpoint_template, sample_params, raw_payload, base_url) -> tuple[str, str]` (D-HIGY-16 + D-25 no-overwrite):**
    Construye envelope D-21 `{endpoint, client_function, captured_at (ISO-8601 UTC), base_url, sample_params, schema (= schema_of(raw_payload))}`; mkdir `_SCHEMA_DIR`; si `file_path` no existe: escribe + return `("PASS", f"escrito {file_path.name}")`; si existe: compara `committed["schema"]` con `actual_schema`: si iguales, return `("PASS", f"{file_path.name} sin drift")`; si diferentes, emite finding SHAPE OPEN via `append_finding(...)` con `expected/actual` = JSON serialized schemas, return `("FINDING", f"{fid}|{file_path.name}")` (NO sobrescribir).

    **Los 18 probes en orden D-HIGY-10:**

    **1. `probe_login_sync() -> ProbeResult`** (HIGY-01):
    - `global _auth_failed, _auth_failure_reason`
    - `base_url = higyrus_client.client._base_url`
    - try: `higyrus_client.login()`
    - except `HigyrusAuthError as exc`: setear `_auth_failed = True`, `_auth_failure_reason = f"sync login: {exc}"`; emitir `append_finding(_PKG, fid=_next_fid(), class_="AUTH", surface="sync", status="OPEN", title="login() sync falló", expected="login succeeds + cached token", actual=repr(exc), diff=f"status_code={exc.status_code!r}", base_url=base_url)`; return `ProbeResult("login_sync", "FINDING", f"{fid} (OPEN)")`
    - return `ProbeResult("login_sync", "PASS", "_token cached")`

    **2. `async def probe_login_async() -> ProbeResult`** (HIGY-01):
    - Mismo patrón con `await aio.login()`; setear el mismo `_auth_failed` global; mismo `append_finding` con `surface="async"`. Discretion: flag único global afecta a TODOS los downstream (los sync ya pasaron si entran al async fail; los downstream async + cualquier remaining sync que no haya corrido entran a SKIPPED).
    - return `ProbeResult("login_async", "PASS", "ok")` si éxito.

    **3. `probe_get_health_sync() -> tuple[ProbeResult, dict[str, Any] | None]`** (HIGY-02):
    - Cascade check (`if _auth_failed: return ..., None`).
    - `base_url = higyrus_client.client._base_url`
    - try: `health = higyrus_client.get_health()` (un solo call — WR-03 Phase 2)
    - except `HigyrusAPIError as exc`: append_finding ERROR-MAP surface=sync status=OPEN con `status_code={exc.status_code!r}` (WR-01 typed); return FINDING + None
    - except `Exception as exc`: append_finding ERROR-MAP surface=sync status=OPEN con `title=f"get_health_sync unexpected {type(exc).__name__}"`; return FINDING + None
    - Assert `isinstance(health, dict) and len(health) >= 1` (al menos una key — D-HIGY-6); si falla emit finding SHAPE OPEN
    - return `(ProbeResult("get_health_sync", "PASS", f"keys={len(health)}"), health)` — D-HIGY-2 stdout solo conteo, no contenido

    **4. `async def probe_get_health_async() -> tuple[ProbeResult, dict[str, Any] | None]`** (HIGY-02): mirror del 3 con `await aio.get_health()`.

    **5. `probe_get_listado_cuentas_sync() -> tuple[ProbeResult, list[Cuenta] | None]`** (HIGY-02 + D-HIGY-11):
    - Cascade check.
    - `global _resolved_cuenta`
    - try: `cuentas = higyrus_client.get_listado_cuentas(estado="alta")` (Discretion D-HIGY: filter por `estado="alta"` da sample útil; si Higyrus rechaza el filtro, switching a sin filtro y emit finding PARAM OPEN documentando)
    - except patterns idénticos al probe 3
    - Set `_resolved_cuenta`: si `_SAMPLE_CUENTA` (env override D-HIGY-11): `_resolved_cuenta = _SAMPLE_CUENTA`; sino si `cuentas`: `_resolved_cuenta = cuentas[0].id`; sino: emit finding NO-DATA OPEN "no cuentas en estado=alta — downstream SKIPPED", return `(ProbeResult("get_listado_cuentas_sync", "SKIPPED", "no cuentas"), None)`
    - return `(ProbeResult("get_listado_cuentas_sync", "PASS", f"{len(cuentas)} cuentas"), cuentas)` — solo conteo, no IDs

    **6. `async def probe_get_listado_cuentas_async() -> tuple[ProbeResult, list[Cuenta] | None]`** (HIGY-02): mirror; NO toca `_resolved_cuenta` (ya seteado por probe 5 sync; o si sync skippeó por cascade, el async también skippea).

    **7. `probe_get_movimientos_sync(today: dt.date) -> tuple[ProbeResult, list[Movimiento] | None]`** (HIGY-02 + HIGY-07):
    - Cascade check + `_resolved_cuenta` check.
    - `fecha_desde = today - dt.timedelta(days=30)` (D-HIGY-12); `fecha_hasta = today`
    - try: `movs = higyrus_client.get_movimientos(_resolved_cuenta, fecha_desde, fecha_hasta)`
    - except patterns
    - Si `movs == []`: return `(ProbeResult("get_movimientos_sync", "PASS", "0 items — empty path verified"), movs)` (D-HIGY-12 HIGY-07)
    - return `(ProbeResult("get_movimientos_sync", "PASS", f"{len(movs)} items"), movs)`

    **8. `async def probe_get_movimientos_async(today: dt.date) -> tuple[ProbeResult, list[Movimiento] | None]`** (HIGY-02): mirror.

    **9. `probe_get_posicion_valuada_sync(today: dt.date) -> tuple[ProbeResult, list[PosicionValuada] | None]`** (HIGY-02 + posible PARAM):
    - Cascade check + `_resolved_cuenta` check.
    - try: `pv = higyrus_client.get_posicion_valuada(_resolved_cuenta, tipo_cuenta=_SAMPLE_TIPO_CUENTA, nivel=_SAMPLE_NIVEL, desde=today, hasta=today)` (firma exacta — verificar al leer client.py; ajustar argumentos si difieren)
    - except `HigyrusAPIError as exc`: si `exc.status_code == 400` y `exc.errors` mencionan `tipo_cuenta`/`nivel`: emit finding PARAM OPEN con title="tipo_cuenta o nivel rechazado por server" + actual=repr(exc.errors); return FINDING + None
    - return `(ProbeResult("get_posicion_valuada_sync", "PASS", f"{len(pv)} items"), pv)`

    **10. `async def probe_get_posicion_valuada_async(today: dt.date) -> tuple[ProbeResult, list[PosicionValuada] | None]`** (HIGY-02): mirror.

    **11. `probe_get_posiciones_sync(today: dt.date) -> tuple[ProbeResult, list[Posicion] | None]`** (HIGY-02 + HIGY-07):
    - Cascade check + `_resolved_cuenta` check.
    - try: `posiciones = higyrus_client.get_posiciones(_resolved_cuenta, fecha=today)` (firma exacta — verificar al leer)
    - except patterns
    - return `(ProbeResult("get_posiciones_sync", "PASS", f"{len(posiciones)} items"), posiciones)` (con el empty-path check de D-HIGY-12 igual que probe 7)

    **12. `async def probe_get_posiciones_async(today: dt.date) -> tuple[ProbeResult, list[Posicion] | None]`** (HIGY-02): mirror.

    **13. `probe_parity_sync_async(today: dt.date, async_query: str | None) -> ProbeResult`** (HIGY-06):
    - Cascade check + `_resolved_cuenta` check.
    - `fecha_desde = today - dt.timedelta(days=30)`, `fecha_hasta = today`
    - `sync_q = _capture_sync_query_string(_resolved_cuenta, fecha_desde, fecha_hasta)` (params optional = None: drop_none activado)
    - Compare `sync_q != async_query` (async_query es injected via tuple-return de `_async_main` — el async capture corre dentro del `_async_main` event loop; en main() se desempaqueta y se pasa acá): si difieren, emit finding SYNC-ASYNC-DRIFT OPEN con `expected=f"sync.query == async.query (drop_none paridad)"`, `actual=f"sync={sync_q!r}; async={async_query!r}"`, `diff="diferencia en cómo drop_none se aplica en aio._request"`; return FINDING
    - return `ProbeResult("parity_sync_async", "PASS", f"query={sync_q!r}")` — no PII (solo nombres de keys + valores de params optional como id_cuenta + fechas, todos sintéticos del driver)

    **14. `probe_field_type_map(cuenta_raw, movimientos_raw, posiciones_raw, posicion_valuada_raw) -> ProbeResult`** (HIGY-03):
    - Cascade check.
    - `targets = [("cuenta", cuenta_raw, Cuenta), ("movimiento", movimientos_raw[0] if movimientos_raw else None, Movimiento), ("posicion", posiciones_raw[0] if posiciones_raw else None, Posicion), ("posicion_valuada", posicion_valuada_raw[0] if posicion_valuada_raw else None, PosicionValuada)]`
    - Para cada (root_name, payload, model_cls): si `payload is None` continue; para cada `(path, direction, key)` en `_diff_safemodel_bidirectional(payload, model_cls, path=f".{root_name}")`: `fid = _next_fid()`; si direction == "model-only": title=f"{path}.{key}: model declara, wire no emite (FALSE PASS riesgo)"; actual="<wire ausente; SafeModel sustituye default tipado>"; diff_detail=f"key `{key}` ausente en wire bajo `{path}` (model: {model_cls.__name__})"; sino (wire-only): title=f"{path}.{key}: wire emite, model ignora (info)"; actual=f"key `{key}` presente en wire bajo `{path}`"; diff_detail=f"backend posiblemente agregó campo nuevo; candidato a extender model"; emit `append_finding(_PKG, fid=fid, class_="SHAPE", surface="both", status="OPEN", title=title, expected=("model y wire coinciden en el set de claves" if direction=="model-only" else "model declara el superset del wire"), actual=actual, diff=diff_detail, base_url=base_url)`; append fid a `fids` list
    - **IMPORTANTE D-HIGY-2:** los raw payloads usados para el diff son obtenidos vía los wrappers high-level (`get_listado_cuentas` retorna `list[Cuenta]`); para el diff bidireccional necesitamos `dict` crudo, NO el dataclass parseado. Discretion: o (a) en probes 5/7/9/11 hacer un segundo `_request("GET", endpoint_template)` directo para capturar raw dict, O (b) usar internamente la deserialización del wrapper pero invertir vía `dataclasses.asdict()`. Opción (a) es más fiel al wire pero dobla calls — Pitfall WR-03 Phase 2 prefiere single-call. Opción (b) no funciona porque `from_api` ya borró los wire-only keys. **Recomendado:** probes 5/7/9/11 capturan el raw via `higyrus_client.client._request("GET", path)` DESPUÉS del wrapper call (segundo HTTP call deliberado, documentado en comentario inline; el diff es la única razón). Alternativa: monkey-patch `models.<Cls>.from_api` para conservar el raw payload — invasivo, NO recomendado.
    - **Recomendado (Discretion):** parametrizar el probe orchestrator (main()) para que pase al probe 14 los raw payloads obtenidos por una pasada extra de `_request` DESPUÉS de cada wrapper exitoso. El raw es solo `dict` (PII presente). D-HIGY-2 sigue cumpliéndose: el raw NO se imprime, solo se inspecciona keys.
    - return `ProbeResult("field_type_map", "FINDING", f"{', '.join(fids)} (OPEN)")` si fids; sino `ProbeResult("field_type_map", "PASS", "4 endpoints, no field drift")`

    **15. `probe_schema_snapshot(payloads: dict[str, Any], base_url: str) -> ProbeResult`** (D-HIGY-16):
    - Cascade check.
    - 5 calls a `_write_or_check_schema(func_name, endpoint_template, sample_params, raw_payload, base_url)`: uno por endpoint. `sample_params` por endpoint: `get_health = {}`, `get_listado_cuentas = {"estado": "alta"}`, `get_movimientos = {"id_cuenta": _resolved_cuenta, "fecha_desde": "<today-30d>", "fecha_hasta": "<today>"}`, `get_posicion_valuada = {"id_cuenta": _resolved_cuenta, "tipo_cuenta": _SAMPLE_TIPO_CUENTA, "nivel": _SAMPLE_NIVEL, "desde": "<today>", "hasta": "<today>"}`, `get_posiciones = {"id_cuenta": _resolved_cuenta, "fecha": "<today>"}` — todas las fechas serializadas como str ISO-8601.
    - Aggregate results: si todos PASS, return `ProbeResult("schema_snapshot", "PASS", "5 escritos / sin drift")`; si algún FINDING, return `ProbeResult("schema_snapshot", "FINDING", f"{fids_concat}")`

    **16. `probe_errors_envelope_sync() -> ProbeResult`** (HIGY-05 always-on):
    - Cascade check + `_resolved_cuenta` (no — usa id_cuenta literal inválida)
    - try: `higyrus_client.get_movimientos("INVALID-CUENTA-XXXXX", today, today)` — Discretion del string literal: el roadmap acepta cualquier id sintácticamente válido pero inexistente
    - except `HigyrusAPIError as exc`: verificar `isinstance(exc.errors, list) and exc.errors and "title" in exc.errors[0] and "detail" in exc.errors[0]`: si sí, return PASS con detail `errors envelope parseado: title+detail presentes`; si no, emit finding ERROR-MAP OPEN con title="errors envelope mal formado"
    - else (200 OK con id inválido — wire raro): emit finding ERROR-MAP OPEN title="200 OK con id_cuenta inválido" status=OPEN
    - except `Exception as exc`: emit finding ERROR-MAP OPEN con title=f"unexpected {type(exc).__name__}"

    **17. `async def probe_errors_envelope_async() -> ProbeResult`** (HIGY-05 always-on): mirror del 16.

    **18. `probe_auth_401() -> ProbeResult`** (HIGY-AUTH opt-in, D-HIGY-10 #18 + Pattern 5):
    - `if os.getenv("VERIFY_HIGYRUS_BAD_CREDS") != "1": return ProbeResult("auth_401", "SKIPPED", "(opt-in via VERIFY_HIGYRUS_BAD_CREDS=1)")`
    - `if _auth_failed: return ProbeResult("auth_401", "SKIPPED", f"auth failed: {_auth_failure_reason}")`
    - `base_url = higyrus_client.client._base_url`
    - `original_password = os.getenv("HIGYRUS_PASSWORD", "")`
    - `bad_password = original_password + "_INVALID"`
    - try: `higyrus_client.configure(password=bad_password)` (resetea _token, _token_ts — D-HIGY: `configure()` directo es safe en Higyrus, NO hay _refresh_token a preservar como en IOL Phase 3 CR-03; deviation explícita)
    - inner try: `higyrus_client.login()` — UNA SOLA LLAMADA, sin retry/sleep/loop
    - except `HigyrusAuthError as exc`: `status_code = exc.status_code` (typed WR-01); si 401: emit finding AUTH EXPECTED, return FINDING (EXPECTED); si otro código: emit finding AUTH OPEN con actual=f"unexpected status {status_code}"
    - except otra excepción: emit finding AUTH OPEN con title=f"unexpected {type(exc).__name__}" actual=repr(exc)
    - else (200 OK con bad password — defensa relajada): emit finding AUTH OPEN title="bad password aceptada por server" actual="login success" status=OPEN
    - **finally OBLIGATORIO:** `higyrus_client.configure(password=original_password)` (T-4-07 mitigation: restore inmediato del state)

    **`async def _async_main(today: dt.date) -> tuple[...]`** (D-HIGY-13 + IN-03 Phase 2):
    Compone todos los probes async (2, 4, 6, 8, 10, 12, 17) + el async capture de probe 13 + cualquier raw payload async necesario para probe 14 (Discretion: el diff bidireccional se puede correr solo con los raw sync — la paridad ya se verifica por probe 13 — pero la convención es ejercitar ambos surfaces igual; recomendado correr probe_field_type_map sobre raw_sync_payloads para evitar doblar la captura raw).
    Returns tuple de todos los ProbeResult y raw payloads necesarios.
    Finally: `with contextlib.suppress(Exception): await aio.aclose()` (IN-03 mirror).

    **`def main() -> None`** (orquestador):
    - `if not require_env(_PKG, ["HIGYRUS_USER", "HIGYRUS_PASSWORD", "HIGYRUS_BASE_URL"]): return` (HARN-01 exit limpio)
    - `today = dt.date.today()`
    - `write_findings(_PKG)` (D-03 mirror: idempotente)
    - `secrets: list[str] = [v for v in (os.getenv("HIGYRUS_USER"), os.getenv("HIGYRUS_PASSWORD")) if v and len(v) >= 4]` (D-HIGY-15 initial)
    - `_sync_token_snapshot: str | None = None` y `_async_token_snapshot: str | None = None` (D-HIGY-15 dynamic extension; ambas se capturan POR VALOR — no por referencia al atributo módulo — inmediatamente tras el login exitoso correspondiente, para que probe_auth_401 (que puede resetear `higyrus_client.client._token = None` vía `configure(password=...+'_INVALID')`) no nulifique la lista de redacción; defense-in-depth Phase 3 CR-03 mirror)
    - `results: list[ProbeResult] = []`
    - **EXECUTION order (D-HIGY-13 single `asyncio.run` + D-HIGY-11 _resolved_cuenta resolution-before-async):**
      D-HIGY-10 (1..18) describe la PRESENTATION order (qué se imprime + qué orden registra el summary); NO es la execution order — la interleaving sync↔async D-HIGY-10 (#1 sync, #2 async, #3 sync, ...) se **relaja durante ejecución** para garantizar que `_resolved_cuenta` queda seteado por sync probe 5 ANTES de que cualquier async cuenta-dependent probe corra dentro del único `asyncio.run`. Los ProbeResult objects se **COLECTAN durante ejecución** (en un `dict[str, ProbeResult]` keyed por probe name) y se **IMPRIMEN al final** en el orden D-HIGY-10 declarado (sorted por índice de presentación) — el stdout refleja D-HIGY-10 verbatim, la execution NO.

      Pasos ordenados de main():

      **(a) Probe 1 sync (login_sync):** invocar `probe_login_sync()`. Si éxito, **CAPTURAR POR VALOR** `_sync_token_snapshot = higyrus_client.client._token` y EXTENDER `secrets` con `[_sync_token_snapshot]` si es non-empty (D-HIGY-15). Si FINDING (auth fail), el flag `_auth_failed=True` ya quedó seteado por el probe — los pasos (b)..(g) entran a cascade SKIPPED igual; los pasos (h)..(l) siguen el mismo cascade. Continuar de todas formas para colectar SKIPPED results.

      **(b) Probe 3 sync (get_health_sync):** invocar `probe_get_health_sync()`, capturar tuple `(result, health_raw)`; `results["get_health_sync"] = result`; `payloads["get_health"] = health_raw`.

      **(c) Probe 5 sync (get_listado_cuentas_sync) — RESOLVE _resolved_cuenta:** invocar `probe_get_listado_cuentas_sync()`. Este probe SETEA `_resolved_cuenta` global como side effect (D-HIGY-11: env override `HIGYRUS_SAMPLE_CUENTA` > `cuentas[0].id` > None). `results["get_listado_cuentas_sync"] = result`; `payloads["get_listado_cuentas"] = cuentas_raw`. Tras este probe, `_resolved_cuenta` está definitivamente resuelto (o explícitamente None con finding NO-DATA OPEN).

      **(d) Probe 7 sync (get_movimientos_sync):** invocar `probe_get_movimientos_sync(today)`; con `_resolved_cuenta` ya seteado, el guard interno `if _resolved_cuenta is None` se evalúa una sola vez tras (c). `results["get_movimientos_sync"] = result`; `payloads["get_movimientos"] = movs_raw`.

      **(e) Probe 9 sync (get_posicion_valuada_sync):** invocar `probe_get_posicion_valuada_sync(today)`. `results["get_posicion_valuada_sync"] = result`; `payloads["get_posicion_valuada"] = pv_raw`.

      **(f) Probe 11 sync (get_posiciones_sync):** invocar `probe_get_posiciones_sync(today)`. `results["get_posiciones_sync"] = result`; `payloads["get_posiciones"] = pos_raw`.

      **(g) Single `asyncio.run(_async_main(today, resolved_cuenta=_resolved_cuenta, sync_payloads=payloads))` (D-HIGY-13):** entra al event loop ÚNICO y ejecuta dentro:
        - probe 2 (login_async) — al éxito, capturar POR VALOR `_async_token_snapshot = higyrus_client.aio._token` dentro de `_async_main` (retornado en el tuple); el caller en main() lo apendea a `secrets` después de `asyncio.run` retorna.
        - probe 4 (get_health_async)
        - probe 6 (get_listado_cuentas_async) — usa `resolved_cuenta` parametro (no toca el global; NO re-resuelve)
        - probe 8 (get_movimientos_async) — usa `resolved_cuenta` parametro pasado; NO returns SKIPPED por `_resolved_cuenta is None` porque el parámetro está GUARANTEED non-None (salvo cascade)
        - probe 10 (get_posicion_valuada_async) — idem
        - probe 12 (get_posiciones_async) — idem
        - **async parity capture for probe 13:** `async_query = await _capture_async_query_string(resolved_cuenta, today - 30d, today)` — esto consume `_capture_async_query_string`, monkey-patches `aio._client.request`, captura `httpx.Request.url.query`, restore via try/finally. Retorna `async_query: str | None`.
        - probe 17 (errors_envelope_async): `await probe_errors_envelope_async()` — NO depende de `_resolved_cuenta` (usa id_cuenta literal inválida).
        - **finally OBLIGATORIO:** `with contextlib.suppress(Exception): await aio.aclose()` (IN-03 mirror)
        - Returns: `(results_async, payloads_async_or_None, async_query, _async_token_snapshot)` — el tuple lo desempaqueta el caller en main(). Después del `asyncio.run`, EXTENDER `secrets` con `[_async_token_snapshot]` si non-empty.

      **(h) Probe 13 (parity_sync_async):** invocar `probe_parity_sync_async(today, async_query=async_query)`. El sync side llama internamente `_capture_sync_query_string(_resolved_cuenta, today-30d, today)`. El probe compara `sync_q != async_query` y emite finding SYNC-ASYNC-DRIFT si difieren. `results["parity_sync_async"] = result`.

      **(i) Probe 14 (field_type_map):** invocar `probe_field_type_map(payloads.get("get_listado_cuentas"), payloads.get("get_movimientos"), payloads.get("get_posiciones"), payloads.get("get_posicion_valuada"))` con los raw payloads sync ya colectados. Si en `_async_main` se decidió correr el diff también sobre raw_async, aceptable Discretion — pero el diff sync ya da cobertura del invariante por simetría sync↔async (el wrapper es el mismo).

      **(j) Probe 15 (schema_snapshot):** invocar `probe_schema_snapshot(payloads, base_url)` con los 5 raw payloads. Aggregate result.

      **(k) Probe 16 (errors_envelope_sync):** invocar `probe_errors_envelope_sync()`. NO depende de `_resolved_cuenta`. `results["errors_envelope_sync"] = result`.

      **(l) Probe 18 (auth_401):** invocar `probe_auth_401()` ÚLTIMO (opt-in single-shot). Si `configure(password=...+'_INVALID')` resetea `higyrus_client.client._token = None`, los snapshots `_sync_token_snapshot` y `_async_token_snapshot` (capturados por valor en (a) y (g)) preservan los valores en `secrets` — `safe_print` sigue redactándolos en cualquier output posterior.

      **(m) PRESENTATION print en orden D-HIGY-10:** después de colectar TODOS los ProbeResult en `results: dict[str, ProbeResult]`, iterar la lista declarada `_D_HIGY_10_ORDER = ["login_sync", "login_async", "get_health_sync", "get_health_async", "get_listado_cuentas_sync", "get_listado_cuentas_async", "get_movimientos_sync", "get_movimientos_async", "get_posicion_valuada_sync", "get_posicion_valuada_async", "get_posiciones_sync", "get_posiciones_async", "parity_sync_async", "field_type_map", "schema_snapshot", "errors_envelope_sync", "errors_envelope_async", "auth_401"]`, y por cada nombre invocar `safe_print(f"PROBE {name}: {results[name].status} {results[name].detail}", secrets)`. Esto produce 18 líneas verbatim en el orden D-HIGY-10 declarado (presentation order).

      **(n) SUMMARY final:** computar counts e imprimir `safe_print(f"SUMMARY: PASS={...} FAIL={...} SKIPPED={...} FINDING={...}", secrets)` (D-02 mirror).
    - Probe 13 (parity): toma `async_query` desde tuple-return de `_async_main` (capture async corrió en event loop), pasa a `probe_parity_sync_async(today, async_query)`.
    - Probe 14 (field_type_map): toma raw payloads (sync). Discretion sobre cómo extraerlos sin doblar HTTP (ver above).
    - Probe 15 (schema_snapshot): toma raw payloads, base_url.
    - Probes 16/17 (errors_envelope): los always-on, sin params extra.
    - Probe 18 (auth_401): último, opt-in, after-all-others-completed-cleanly.
    - Construir summary final: `counts = {"PASS": 0, "FAIL": 0, "SKIPPED": 0, "FINDING": 0}`; iterate `results`, increment counts[r.status]; emit `safe_print(f"SUMMARY: PASS={counts['PASS']} FAIL={counts['FAIL']} SKIPPED={counts['SKIPPED']} FINDING={counts['FINDING']}", secrets)`
    - Exit code 0 (D-04 mirror — findings OPEN son output, no error).

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - Mantener `from __future__ import annotations` línea 1
    - mypy --strict pasa sin errores
    - ruff line-length=100, double quotes, 4 espacios
    - NO importar `httpx` directamente (a diferencia de Phase 2 BAD_UA; Higyrus no necesita)
    - NO importar paquetes nuevos
    - NO usar `time.sleep` (anti-pattern; Pitfall 9 IOL Phase 3)
    - NO usar multiple `asyncio.run` (anti-pattern; D-HIGY-13)
    - NO commitear los 5 schemas ni el findings file en este task (eso es Plan 04-03)
    - NO modificar `verification/*` ni `conftest.py` (Phase 1 lockedo)
    - NO modificar `models.py` ni `client.py`/`aio.py`/`exceptions.py` (Plan 04-01 los cubrió)
    - Driver stdout NUNCA imprime contenido de payloads (D-HIGY-2 — primera fase con PII real)

    Commit sugerido: `feat(04-02): rewrite main_higyrus.py with 18 named probes + bidirectional SafeModel diff + drop_none parity capture (HIGY-01..03+05..07)`
  </action>
  <verify>
    <automated>uv run mypy main_higyrus.py</automated>
    <automated>uv run ruff check main_higyrus.py</automated>
    <automated>uv run ruff format --check main_higyrus.py</automated>
    <automated>uv run python -c "import ast; tree = ast.parse(open('main_higyrus.py').read()); funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]; expected = ['probe_login_sync', 'probe_login_async', 'probe_get_health_sync', 'probe_get_health_async', 'probe_get_listado_cuentas_sync', 'probe_get_listado_cuentas_async', 'probe_get_movimientos_sync', 'probe_get_movimientos_async', 'probe_get_posicion_valuada_sync', 'probe_get_posicion_valuada_async', 'probe_get_posiciones_sync', 'probe_get_posiciones_async', 'probe_parity_sync_async', 'probe_field_type_map', 'probe_schema_snapshot', 'probe_errors_envelope_sync', 'probe_errors_envelope_async', 'probe_auth_401']; missing = [p for p in expected if p not in funcs]; assert not missing, f'missing probes: {missing}'; print('all 18 probes present')"</automated>
    <automated>grep -c "_diff_safemodel_bidirectional" main_higyrus.py</automated>
    <automated>grep -c "append_finding" main_higyrus.py</automated>
    <automated>grep -c "safe_print" main_higyrus.py</automated>
    <automated>grep -c "_auth_failed" main_higyrus.py</automated>
    <automated>grep -c "_resolved_cuenta" main_higyrus.py</automated>
    <automated>grep -c "asyncio.run" main_higyrus.py</automated>
    <automated>grep -F "contextlib.suppress(Exception)" main_higyrus.py</automated>
    <automated>grep -F "VERIFY_HIGYRUS_BAD_CREDS" main_higyrus.py</automated>
    <automated>grep -F "configure(password=original_password)" main_higyrus.py</automated>
    <automated>grep -c "time.sleep" main_higyrus.py</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_higyrus.py` contiene las 18 firmas de probes (verificado por AST inspection en el verify block)
    - Source: `main_higyrus.py` contiene `def _diff_safemodel_bidirectional(` (helper Pattern 1)
    - Source: `main_higyrus.py` contiene `_is_optional`, `_nested_safemodel_class`, `_is_list_of_safemodel` helpers (Pattern 1)
    - Source: `main_higyrus.py` contiene `def _capture_sync_query_string(` y `async def _capture_async_query_string(` (Pattern 2)
    - Source: `main_higyrus.py` contiene `_auth_failed: bool = False` (cascade flag D-IOL-3 mirror)
    - Source: `main_higyrus.py` contiene `_resolved_cuenta: str | None = None` (D-HIGY-11)
    - Source: `main_higyrus.py` contiene `_SAMPLE_CUENTA = os.getenv("HIGYRUS_SAMPLE_CUENTA")` (D-HIGY-14)
    - Source: `main_higyrus.py` contiene `_SAMPLE_TIPO_CUENTA` con default `"propia"` (D-HIGY-14)
    - Source: `main_higyrus.py` contiene `_SAMPLE_NIVEL` con default `"detalle"` (D-HIGY-14)
    - Source: `main_higyrus.py` contiene `_SCHEMA_FILES` dict con 5 entries (D-HIGY-16)
    - Source: `main_higyrus.py` contiene `asyncio.run(_async_main` exactamente 1 vez (D-HIGY-13 single event loop)
    - Source: `main_higyrus.py` contiene `contextlib.suppress(Exception)` con `await aio.aclose()` (IN-03 mirror)
    - Source: `main_higyrus.py` contiene `VERIFY_HIGYRUS_BAD_CREDS` literal (opt-in 401 D-HIGY-10 #18)
    - Source: `main_higyrus.py` contiene `configure(password=original_password)` literal en el finally (T-4-07 mitigation)
    - Source: `main_higyrus.py` contiene `safe_print` con `secrets=...` (D-HIGY-15)
    - Source: `main_higyrus.py` contiene `higyrus_client.client._token` (dynamic secrets extension D-HIGY-15)
    - Source: `main_higyrus.py` contiene `append_finding(` con `class_="SHAPE"`, `class_="AUTH"`, `class_="SYNC-ASYNC-DRIFT"`, `class_="ERROR-MAP"` (al menos 4 clases finding usadas)
    - Source: `main_higyrus.py` contiene `from higyrus_client.models import` con los 4 modelos + SafeModel
    - Source: `main_higyrus.py` NO contiene `time.sleep` (anti-pattern)
    - Source: `main_higyrus.py` mantiene `from __future__ import annotations` línea 1
    - Behavior: `grep -c "asyncio.run" main_higyrus.py` == 1 (D-HIGY-13)
    - Behavior: `grep -c "time.sleep" main_higyrus.py` == 0 (anti-pattern)
    - Static: `uv run mypy main_higyrus.py` exits 0
    - Static: `uv run ruff check main_higyrus.py` exits 0
    - Static: `uv run ruff format --check main_higyrus.py` exits 0
    - Source: `main_higyrus.py` contiene `_D_HIGY_10_ORDER` (lista de 18 nombres de probes, presentation order)
    - Source: `main_higyrus.py` contiene `_sync_token_snapshot` y `_async_token_snapshot` como nombres locales/módulo (dual snapshot pattern)
    - Source: `main_higyrus.py` contiene `_async_main(today, resolved_cuenta=` con `resolved_cuenta` como parámetro explícito de `_async_main` (no lee `_resolved_cuenta` global desde dentro del event loop)
    - Source: `main_higyrus.py` contiene una llamada `probe_get_listado_cuentas_sync()` que precede textualmente a la línea `asyncio.run(_async_main(` en el cuerpo de `main()` (resolución antes del event loop — verificable por AST or string-order check)
    - Execution order: el orden en main() es (a) probe_login_sync → (b) probe_get_health_sync → (c) probe_get_listado_cuentas_sync (resolves _resolved_cuenta) → (d) probe_get_movimientos_sync → (e) probe_get_posicion_valuada_sync → (f) probe_get_posiciones_sync → (g) asyncio.run(_async_main(today, resolved_cuenta=_resolved_cuenta, sync_payloads=payloads)) → (h) probe_parity_sync_async → (i) probe_field_type_map → (j) probe_schema_snapshot → (k) probe_errors_envelope_sync → (l) probe_auth_401 (D-HIGY-13 + D-HIGY-11 enforce)
    - Presentation order: las 18 líneas `PROBE <name>: <status>` se imprimen al final en orden D-HIGY-10 declarado (login_sync, login_async, get_health_sync, get_health_async, ..., auth_401) — NO en el orden en que los probes corrieron
    - **No async cuenta-dependent probe (probes 6/8/10/12 = get_listado_cuentas_async / get_movimientos_async / get_posicion_valuada_async / get_posiciones_async, plus the parity capture for probe 13) returns SKIPPED with reason 'no _resolved_cuenta resuelto' on a successful run** — `_resolved_cuenta` está GUARANTEED non-None antes de `asyncio.run(_async_main(...))` salvo que probe 5 (probe_get_listado_cuentas_sync) haya FINDING/SKIPPED, en cuyo caso el cascade async es esperado y explicado por el flag/finding correlativo
    - Secrets snapshot: `secrets` list incluye `_sync_token_snapshot` capturado por valor inmediatamente tras `probe_login_sync` éxito Y `_async_token_snapshot` capturado por valor inmediatamente tras `probe_login_async` éxito (dentro de `_async_main`, retornado en tuple); ambos snapshots sobreviven a `probe_auth_401` aunque éste setee `higyrus_client.client._token = None` vía `configure()` (defense-in-depth Phase 3 CR-03 mirror)
    - Source: `main_higyrus.py` contiene `higyrus_client.aio._token` literal (captura del async token snapshot — D-HIGY-15 dual)
    - - Non-regression: `uv run pytest -q` exits 0 (suite mockeada del cliente Higyrus + IOL + Ámbito + harness sigue verde — el driver no se ejecuta en pytest)
  </acceptance_criteria>
  <done>main_higyrus.py reescrito con 18 probes nombrados en orden D-HIGY-10 + helper bidireccional + parity capture + cascade SKIPPED + opt-in 401 + single asyncio.run + safe_print dynamic secrets; mypy + ruff verdes; suite mockeada sin regresión; NO ejecutado en vivo aún (eso es Task 2.3).</done>
</task>

<task type="auto">
  <name>Task 2.2: Append 4 optional env vars to packages/higyrus-client/.env.example (D-HIGY-14)</name>
  <files>packages/higyrus-client/.env.example</files>
  <read_first>
    - packages/higyrus-client/.env.example (estado actual: tiene `HIGYRUS_USER`, `HIGYRUS_PASSWORD`, `HIGYRUS_BASE_URL` requeridos; `HIGYRUS_CLIENT_ID` opcional; **3 vars heredadas `HIGYRUS_TEST_ACCOUNT_ID`, `HIGYRUS_TEST_TIPO_CUENTA`, `HIGYRUS_TEST_NIVEL`** — Phase 4 las RENOMBRA a `HIGYRUS_SAMPLE_*` per D-HIGY-14 + PATTERNS.md flagging)
    - .planning/phases/04-higyrus-verification/04-CONTEXT.md (D-HIGY-14 lista completa: requeridas + opcionales + opt-in)
    - .planning/phases/04-higyrus-verification/04-PATTERNS.md ("flags HIGYRUS_TEST_* → HIGYRUS_SAMPLE_* rename per D-HIGY-14")
  </read_first>
  <action>
    Modificar `packages/higyrus-client/.env.example` con dos cambios:

    **Cambio 1 — Renombrar las 3 vars heredadas `HIGYRUS_TEST_*` a `HIGYRUS_SAMPLE_*` (Phase 4 convention per PATTERNS.md flagging):**

    Antes:
    ```
    # Optional — used by main.py smoke check
    HIGYRUS_TEST_ACCOUNT_ID=
    HIGYRUS_TEST_TIPO_CUENTA=
    HIGYRUS_TEST_NIVEL=
    ```

    Después (D-HIGY-14):
    ```
    # Optional — used by main_higyrus.py driver (Phase 4)
    # HIGYRUS_SAMPLE_CUENTA: override del cuentas[0].id auto-resuelto (sino vacío = primer cuenta en estado=alta)
    HIGYRUS_SAMPLE_CUENTA=
    # HIGYRUS_SAMPLE_TIPO_CUENTA: tipo de cuenta para get_posicion_valuada (default 'propia')
    HIGYRUS_SAMPLE_TIPO_CUENTA=
    # HIGYRUS_SAMPLE_NIVEL: nivel para get_posicion_valuada (default 'detalle')
    HIGYRUS_SAMPLE_NIVEL=
    ```

    **Cambio 2 — Agregar la 4ta var opt-in `VERIFY_HIGYRUS_BAD_CREDS` (D-HIGY-14 + D-HIGY-10 #18):**

    Apendear al final del archivo:
    ```
    # Optional — opt-in flag for probe_auth_401 (Phase 4)
    # Setear =1 para forzar un intento de login con password inválido y verificar el path 401.
    # SINGLE-SHOT por ejecución del driver; sin retry, sin loops.
    # CRÍTICO: solo activar deliberadamente; cada run consume 1 intento de auth fallido contra la cuenta real.
    VERIFY_HIGYRUS_BAD_CREDS=
    ```

    **Restricciones:**
    - El archivo NO contiene `.env` style comments con secrets reales
    - Las nombres en mayúsculas + snake_case + `_` separator
    - Los valores quedan vacíos (template solo)
    - El archivo termina con una línea vacía
    - NO modificar las vars requeridas existentes (`HIGYRUS_USER`, `HIGYRUS_PASSWORD`, `HIGYRUS_BASE_URL`)
    - NO modificar `HIGYRUS_CLIENT_ID` (sigue documentado en su comentario)

    Commit sugerido: `chore(04-02): rename HIGYRUS_TEST_* to HIGYRUS_SAMPLE_* and add VERIFY_HIGYRUS_BAD_CREDS in .env.example (D-HIGY-14)`
  </action>
  <verify>
    <automated>grep -c "HIGYRUS_TEST_" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_SAMPLE_CUENTA" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_SAMPLE_TIPO_CUENTA" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_SAMPLE_NIVEL" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "VERIFY_HIGYRUS_BAD_CREDS" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_USER" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_PASSWORD" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_BASE_URL" packages/higyrus-client/.env.example</automated>
    <automated>grep -F "HIGYRUS_CLIENT_ID" packages/higyrus-client/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/higyrus-client/.env.example` contiene `HIGYRUS_SAMPLE_CUENTA=` (D-HIGY-14)
    - Source: `packages/higyrus-client/.env.example` contiene `HIGYRUS_SAMPLE_TIPO_CUENTA=` con comentario que menciona default 'propia'
    - Source: `packages/higyrus-client/.env.example` contiene `HIGYRUS_SAMPLE_NIVEL=` con comentario que menciona default 'detalle'
    - Source: `packages/higyrus-client/.env.example` contiene `VERIFY_HIGYRUS_BAD_CREDS=` con comentario explicativo del single-shot opt-in
    - Source: `packages/higyrus-client/.env.example` NO contiene `HIGYRUS_TEST_` (renombrado completo)
    - Source: `packages/higyrus-client/.env.example` mantiene `HIGYRUS_USER=tu_usuario` (requerido inalterado)
    - Source: `packages/higyrus-client/.env.example` mantiene `HIGYRUS_PASSWORD=tu_password` (requerido inalterado)
    - Source: `packages/higyrus-client/.env.example` mantiene `HIGYRUS_BASE_URL=https://cliente.aunesa.com/Irmo` (requerido inalterado)
    - Source: `packages/higyrus-client/.env.example` mantiene `HIGYRUS_CLIENT_ID=` (opcional documentado inalterado)
    - Behavior: `grep -c "HIGYRUS_TEST_" packages/higyrus-client/.env.example` == 0
    - Behavior: `grep -c "HIGYRUS_SAMPLE_" packages/higyrus-client/.env.example` == 3
    - Behavior: archivo termina con newline
  </acceptance_criteria>
  <done>.env.example actualizado: 3 vars heredadas renombradas + 1 opt-in apendeada; vars requeridas inalteradas; las 4 vars opcionales nuevas documentadas con comentarios explicativos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2.3: Live driver run (one-shot baseline + optional opt-in 401)</name>
  <files>main_higyrus.py, .planning/verification/schemas/higyrus-client/get-health.json, .planning/verification/schemas/higyrus-client/get-listado-cuentas.json, .planning/verification/schemas/higyrus-client/get-movimientos.json, .planning/verification/schemas/higyrus-client/get-posicion-valuada.json, .planning/verification/schemas/higyrus-client/get-posiciones.json, .planning/verification/higyrus-client-findings.md</files>
  <read_first>
    - main_higyrus.py (reescrito en Task 2.1; entry point del live run con 18 probes en orden D-HIGY-10)
    - packages/higyrus-client/.env.example (actualizado en Task 2.2; documento de referencia para que el usuario complete su `.env`)
    - .planning/phases/04-higyrus-verification/04-CONTEXT.md (D-04 mirror Phase 2 — exit 0 siempre salvo crash inesperado; D-HIGY-10 #18 opt-in single-shot; D-HIGY-16 envelope D-21 + D-25 no-overwrite)
    - .planning/phases/04-higyrus-verification/04-VALIDATION.md (sección "Manual-Only Verifications" con criterios verificables del live run)
    - .planning/verification/FINDINGS-TEMPLATE.md (formato esperado del findings file)
    - packages/higyrus-client/.env.example (verificar que el usuario tenga HIGYRUS_USER + HIGYRUS_PASSWORD + HIGYRUS_BASE_URL en su `.env` antes de correr)
  </read_first>
  <what-built>
    El driver `main_higyrus.py` (reescrito en Task 2.1) y `.env.example` (actualizado en Task 2.2) están listos. Ahora ejecutamos el driver una vez contra Higyrus real para:

    1. **Generar los 5 primeros schema snapshots DRIFT-01 mirror:** `probe_schema_snapshot` escribirá los 5 archivos en `.planning/verification/schemas/higyrus-client/` con envelope D-21 (endpoint + client_function + captured_at + base_url + sample_params + schema). Estos son PII-free por construcción (`schema_of` solo emite keys + types, NUNCA values).

    2. **Generar el findings file inicial:** `write_findings("higyrus-client")` crea el esqueleto y cualquier finding OPEN/EXPECTED detectado por los 18 probes se append-ea vía `append_finding` (idempotente). Esperable: el `probe_field_type_map` (probe 14) emite múltiples findings SHAPE OPEN — el diff bidireccional sobre los 4 modelos suele descubrir gaps (D-HIGY-5: "30 findings es OK, el ciclo OPEN→CONFIRMED→FIXED filtra ruido").

    3. **Verificar HIGY-01..03 + HIGY-05..07 en vivo:** 18 líneas `PROBE <name>: <status>` + `SUMMARY: ...` en stdout; exit 0 (D-04: salvo crash inesperado). El driver respeta D-HIGY-2 (stdout = counts + shape only, NO valores) — primera fase con PII real.

    El comando exacto es:
    ```
    uv run --package higyrus-client python main_higyrus.py
    ```

    Para HIGY-AUTH (probe 401 opt-in), el run adicional es:
    ```
    VERIFY_HIGYRUS_BAD_CREDS=1 uv run --package higyrus-client python main_higyrus.py
    ```

    **CRÍTICO — Riesgo de lockout (Pitfall 9 IOL Phase 3 mirror):** la corrida con `VERIFY_HIGYRUS_BAD_CREDS=1` consume 1 intento de auth fallido por run contra la cuenta real Higyrus. Máximo 1 corrida por sesión. NO re-correr en loop. Si la cuenta queda lockeada, esperar ~15-60 min (política de Higyrus desconocida, asumir industria standard).

    NOTA: la corrida default (sin VERIFY_HIGYRUS_BAD_CREDS) emite `PROBE auth_401: SKIPPED (opt-in via VERIFY_HIGYRUS_BAD_CREDS=1)`. La corrida con el flag es OPCIONAL para esta sesión; el commit del baseline en Plan 04-03 puede hacerse con solo el run default, y el humano corre el opt-in deliberadamente después si quiere registrar el finding EXPECTED de HIGY-AUTH.

    **Importante (D-04 mirror):** los hallazgos OPEN son output esperado, NO errores. El driver exit 0 igual. El probe_field_type_map en particular probablemente emite varios SHAPE OPEN; el operador los clasifica ex-post.
  </what-built>
  <action>
    Acción operativa (a ejecutarse en checkpoint humano antes del commit del Plan 04-03):

    1. Asistente (o usuario) verifica que `packages/higyrus-client/.env` existe con `HIGYRUS_USER`, `HIGYRUS_PASSWORD`, `HIGYRUS_BASE_URL` válidos (sino driver imprime `SKIPPED higyrus-client: missing ...` y sale — HARN-01).

    2. Usuario corre el driver default contra Higyrus real:
       `uv run --package higyrus-client python main_higyrus.py`
       Verifica que stdout emite 18 líneas `PROBE <name>: <status>` (verbatim D-02 Phase 2 mirror) en el orden D-HIGY-10 (auth_401 último) seguidas de una línea `SUMMARY: ...` con conteos por estado. Exit code = 0 (D-04 mirror).

    3. Se generan 6 artefactos en disco (output del driver):
       - 5 schemas en `.planning/verification/schemas/higyrus-client/`:
         - `get-health.json` con envelope D-21 + schema observado
         - `get-listado-cuentas.json` con envelope D-21 + schema observado para `estado=alta`
         - `get-movimientos.json` con envelope D-21 + schema observado para `_resolved_cuenta` + rango 30d
         - `get-posicion-valuada.json` con envelope D-21 + schema observado para `_resolved_cuenta` + propia/detalle/today
         - `get-posiciones.json` con envelope D-21 + schema observado para `_resolved_cuenta` + today
       - `.planning/verification/higyrus-client-findings.md` con esqueleto generado por `write_findings` + cualquier finding del run (vía `append_finding`)

    4. Usuario inspecciona los artefactos:
       - Para cada JSON: keys = {endpoint, client_function, captured_at, base_url, sample_params, schema}; `base_url == "https://cliente.aunesa.com/Irmo"` (asumiendo no se overrideó con HIGYRUS_BASE_URL).
       - **CRÍTICO — PII-free verification (T-4-SC mitigation):** ningún JSON debe contener strings que parezcan PII real (no debe haber CBU, no debe haber nombres de titulares, no debe haber CUIT). El `schema_of` solo emite type names — esto debe ser verificable visualmente leyendo los JSONs. Si aparece algún valor real, STOP el commit en Plan 04-03 y reportar.
       - Para el markdown: header `# Findings: higyrus-client`, sección `## Run Context (ART)` con Timestamp ISO-8601 reciente + `base_url=https://cliente.aunesa.com/Irmo`, sección `## Index` (vacía o con rows), sección detalle por finding.
       - Findings esperables (no exhaustivo):
         - `SHAPE OPEN` desde `probe_field_type_map` con prefijo `(FALSE PASS riesgo)` para keys que el model declara pero el wire no emite (direction A)
         - `SHAPE OPEN` desde `probe_field_type_map` con prefijo `(info)` para keys que el wire emite pero el model no declara (direction B)
         - Posible `SYNC-ASYNC-DRIFT OPEN` desde `probe_parity_sync_async` si la deviation `drop_none` se confirma in-vivo (CONCERNS.md candidate)
         - Posible `PARAM OPEN` desde `probe_get_posicion_valuada_*` si Higyrus rechaza `tipo_cuenta=propia` o `nivel=detalle`

    5. Usuario clasifica findings OPEN encontrados según D-08 lifecycle Phase 1:
       - CONFIRMED: bug real reproducido → editar archivo manualmente con justificación
       - EXPECTED: comportamiento del server esperado terminal (ej. el 401 del opt-in si se corrió)
       - NO-FIX: dato puntual o ciclo no aplica
       - OPEN se mantiene si requiere más investigación

    6. **(OPCIONAL)** Usuario corre HIGY-AUTH opt-in probe deliberadamente:
       `VERIFY_HIGYRUS_BAD_CREDS=1 uv run --package higyrus-client python main_higyrus.py`
       UNA SOLA VEZ — riesgo de lockout prohíbe loops. Espera ver `PROBE auth_401: FINDING F-NN (EXPECTED)` y un finding nuevo con clase AUTH status EXPECTED, status_code = 401. **DESPUÉS** del run con el flag, verificar que un run sin el flag pasa login normalmente (no lockout).

    7. Usuario emite resume signal:
       - `"approved"` para que Plan 04-03 commitee los 6 artefactos al repo después de los Verified-live tests
       - `"holds-finding-X"` + descripción para insertar Plan 04-04 opportunistic con fix dual sync/async + 2 regression tests ANTES del commit (uso del patrón Phase 2/3 opportunistic)
       - `"reject"` + razón si el driver crashea o output no respeta D-02/D-04/D-HIGY-2 (vuelve al Task 2.1 a corregir)

    No se escribe código en este task. No se edita ningún archivo (los artefactos son OUTPUT del driver, no INPUT humano). El task solo bloquea la ejecución hasta que el humano apruebe el commit.
  </action>
  <how-to-verify>
    1. **Verificá `.env`:** asegurate de que `packages/higyrus-client/.env` tiene `HIGYRUS_USER`, `HIGYRUS_PASSWORD`, `HIGYRUS_BASE_URL` válidos. Si no existe, copialo de `.env.example` y completá los valores:
       ```
       cp packages/higyrus-client/.env.example packages/higyrus-client/.env
       # Editá packages/higyrus-client/.env con tus credenciales reales
       ```

    2. **Corré el driver default** (sin VERIFY_HIGYRUS_BAD_CREDS — el opt-in queda para el paso 6):
       ```
       uv run --package higyrus-client python main_higyrus.py
       ```

    3. **Verificá la salida stdout:** esperás ver 18 líneas `PROBE <name>: <status>` seguidas de `SUMMARY: ...`. Por ejemplo:
       ```
       PROBE login_sync: PASS _token cached
       PROBE login_async: PASS ok
       PROBE get_health_sync: PASS keys=5
       PROBE get_health_async: PASS keys=5
       PROBE get_listado_cuentas_sync: PASS 3 cuentas
       PROBE get_listado_cuentas_async: PASS 3 cuentas
       PROBE get_movimientos_sync: PASS 12 items
       PROBE get_movimientos_async: PASS 12 items
       PROBE get_posicion_valuada_sync: PASS 8 items
       PROBE get_posicion_valuada_async: PASS 8 items
       PROBE get_posiciones_sync: PASS 5 items
       PROBE get_posiciones_async: PASS 5 items
       PROBE parity_sync_async: PASS query='fechaDesde=2026-05-07&fechaHasta=2026-06-06'
       PROBE field_type_map: FINDING F-01, F-02, F-03 (OPEN)
       PROBE schema_snapshot: PASS 5 escritos
       PROBE errors_envelope_sync: PASS errors envelope parseado: title+detail presentes
       PROBE errors_envelope_async: PASS errors envelope parseado: title+detail presentes
       PROBE auth_401: SKIPPED (opt-in via VERIFY_HIGYRUS_BAD_CREDS=1)
       SUMMARY: PASS=15 FAIL=0 SKIPPED=1 FINDING=1
       ```
       (los nombres exactos de status y detalles pueden variar según Discretion del implementador, pero el orden y los nombres de probes deben coincidir con D-HIGY-10).

    4. **Verificá los 6 artefactos en disco:**
       ```
       ls -la .planning/verification/schemas/higyrus-client/
       cat .planning/verification/schemas/higyrus-client/get-health.json
       cat .planning/verification/schemas/higyrus-client/get-listado-cuentas.json
       cat .planning/verification/schemas/higyrus-client/get-movimientos.json
       cat .planning/verification/schemas/higyrus-client/get-posicion-valuada.json
       cat .planning/verification/schemas/higyrus-client/get-posiciones.json
       cat .planning/verification/higyrus-client-findings.md
       ```
       - Cada JSON debe ser un dict con keys `endpoint`, `client_function`, `captured_at`, `base_url`, `sample_params`, `schema` (envelope D-21).
       - **CRÍTICO — PII-free (T-4-SC mitigation):** Los valores en `schema` deben ser solo type names (e.g., `"str"`, `"int"`, `"float"`, `"list"`, `"dict"`, `"None"`). NO debe haber valores reales (no nombres, no CBU, no CUIT). Si ves algo que parece PII real, **STOP** y reportá como "reject".
       - El markdown debe contener header `# Findings: higyrus-client`, sección `## Run Context (ART)` con Timestamp + base_url.

    5. **Inspeccioná findings (si los hay):**
       - Los findings del `probe_field_type_map` son ESPERADOS — el diff bidireccional sobre 4 modelos suele descubrir gaps. Clasificalos:
         - `(FALSE PASS riesgo)` con direction "model-only": el model declara una key que el wire no emite → SafeModel sustituye default tipado silenciosamente. Si la key es importante, promover a CONFIRMED y considerar fix en model. Si no, NO-FIX.
         - `(info)` con direction "wire-only": el wire emite una key que el model no declara → el cliente la ignora. Si el campo aporta info útil, candidato a extender el model. Sino, NO-FIX o dejar OPEN.
       - Posible `SYNC-ASYNC-DRIFT`: si aparece, la deviation `drop_none` conocida (CONCERNS.md) se confirma in-vivo. Clasificarla y crear plan opportunistic 04-04 si se quiere fix in-cycle.
       - Posible `PARAM` en `get_posicion_valuada`: si el server rechaza los defaults (`propia`/`detalle`), setear `HIGYRUS_SAMPLE_TIPO_CUENTA` y/o `HIGYRUS_SAMPLE_NIVEL` en `.env` con valores correctos (consultar `documentation/higyrus-docs.pdf` pp. 49-52) y re-correr.

    6. **(OPCIONAL) Corré el HIGY-AUTH opt-in probe** una sola vez:
       ```
       VERIFY_HIGYRUS_BAD_CREDS=1 uv run --package higyrus-client python main_higyrus.py
       ```
       - Espera ver `PROBE auth_401: FINDING F-NN (EXPECTED)` y un finding nuevo con clase `AUTH` status `EXPECTED`, status_code = 401.
       - Si aparece `OPEN` con `actual=<otra cosa>`: registrar el hallazgo y NO re-correr el probe (riesgo de lockout).
       - **CRÍTICO:** después de este run, verificar que la sesión siguiente sin el flag pasa login normalmente. Si lockout: esperar 15-60 min.

    7. **Confirmá al asistente:**
       - **"approved"** si los 6 artefactos lucen bien (especialmente schemas PII-free) y querés que Plan 04-03 los commitee al repo
       - **"holds-finding-X"** + descripción si querés que se cree un Plan 04-04 opportunistic para promover un finding específico a CONFIRMED → fix dual sync+async → 2 regression tests **antes** de commitear el findings file
       - **"reject"** + razón si el driver crashea, el envelope D-21 sale mal formado, los schemas contienen PII real, o el orden D-HIGY-10 no se respeta: vuelve a Task 2.1 a corregir
  </how-to-verify>
  <verify>
    <human-check>Driver ejecutado al menos una vez con exit 0; output stdout respeta D-02 (18 líneas PROBE + SUMMARY); 6 artefactos generados (5 JSONs + 1 markdown) inspeccionados y aprobados por el humano; verificación PII-free de los 5 JSONs (schema solo type names, no values reales)</human-check>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-health.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-listado-cuentas.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-movimientos.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-posicion-valuada.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-posiciones.json</automated>
    <automated>test -f .planning/verification/higyrus-client-findings.md</automated>
    <automated>uv run python -c "import json; from pathlib import Path; files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json')); assert len(files) == 5, f'expected 5 schemas, got {len(files)}'; required_keys = {'endpoint', 'client_function', 'captured_at', 'base_url', 'sample_params', 'schema'}; [assert_ for f in files for assert_ in [(lambda d: (required_keys <= set(d.keys()), f'{f.name} missing envelope keys'))(json.loads(f.read_text()))]]; print(f'envelope D-21 OK in {len(files)} schemas')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/verification/schemas/higyrus-client/get-health.json` (generado por el driver run)
    - File exists: `.planning/verification/schemas/higyrus-client/get-listado-cuentas.json`
    - File exists: `.planning/verification/schemas/higyrus-client/get-movimientos.json`
    - File exists: `.planning/verification/schemas/higyrus-client/get-posicion-valuada.json`
    - File exists: `.planning/verification/schemas/higyrus-client/get-posiciones.json`
    - File exists: `.planning/verification/higyrus-client-findings.md`
    - File content (5 JSONs): cada uno contiene las 6 keys del envelope D-21 (`endpoint`, `client_function`, `captured_at`, `base_url`, `sample_params`, `schema`)
    - File content (5 JSONs PII-free, T-4-SC): el campo `schema` de cada JSON contiene solo type names (e.g., `"str"`, `"int"`, `"float"`, `"list"`, `"dict"`), NO valores reales — verificado por inspección humana
    - File content (markdown): contiene `# Findings: higyrus-client` (header generado por `write_findings(_PKG)`)
    - File content (markdown): contiene sección `## Run Context (ART)` con líneas `- Timestamp:` y `- Resolved base URL / env:`
    - Behavior: driver exit code = 0 (D-04 mirror)
    - Behavior: stdout contiene 18 líneas que matchean el regex `^PROBE [a-z_0-9]+: (PASS|FAIL|SKIPPED|FINDING)` (D-02 verbatim Phase 2/3 mirror)
    - Behavior: stdout contiene una línea que empieza con `SUMMARY: ` (D-02 summary final)
    - Behavior (D-HIGY-2): stdout NO contiene valores PII reales (no nombres reales de titulares, no CBU, no CUIT) — verificado por inspección humana
    - Behavior: si el opt-in HIGY-AUTH corre, el findings file contiene una row con `Class: AUTH | Status: EXPECTED` (o similar formato del template)
    - Behavior: tras el opt-in run, la sesión siguiente sin el flag pasa login normalmente (no lockout) — verificado por inspección humana
    - Human approval recibida (resume signal = "approved" o equivalente)
  </acceptance_criteria>
  <resume-signal>
    Type **"approved"** to proceed to Plan 04-03 (Verified-live tests + commit baseline: 5 schemas + findings file). Or describe issues / request opportunistic fix-plan via "holds-finding-X" / "reject".
  </resume-signal>
  <done>Driver ejecutado en vivo al menos una vez; 5 schema snapshots + findings markdown generados en disco; PII-free verification confirmada por inspección humana; humano inspeccionó y aprobó los artefactos para commit. Optional: opt-in HIGY-AUTH probe corrido una sola vez para registrar finding EXPECTED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Driver process → Higyrus API (live) | TLS via httpx; bearer token in Authorization header; the entire driver is a live HTTP client |
| Driver process → stdout (operator terminal) | `safe_print` is the ONLY emission path; secrets list explicit + `_BEARER` regex catch-all |
| Driver process → `.planning/verification/{schemas,*-findings.md}` (disk) | `schema_of` is PII-free by construction; `append_finding` writes title/expected/actual/diff strings — operator must verify these strings don't include payload values |
| Driver module-level state (`_auth_failed`, `_resolved_cuenta`, `_fid_counter`) → in-process | Singleton globals mutated by probes; restored across runs via fresh process |
| `probe_auth_401` → `higyrus_client.configure(password=...)` | The configure() resets `_token` and `_token_ts`; without try/finally restore, the process state would persist (T-4-07 risk) |
| `probe_parity_sync_async` → monkey-patched `_client.request` (bound method) | Scoped patch with try/finally restore; bypassing finally would leave the global httpx client with a spy permanently attached |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information Disclosure | Live PII in payloads (cuentas, titulares, CBU, domicilios, personasRelacionadas) — first phase with real PII | mitigate | D-HIGY-2 verbatim discipline: driver stdout = counts + shape descriptor only, NEVER values. `safe_print(text, secrets=[HIGYRUS_USER, HIGYRUS_PASSWORD, _token])` with dynamic `_token` extension post-login (D-HIGY-15). Acceptance criteria: behavior assertion "stdout NO contiene valores PII reales" verified by human inspection. The `_BEARER` regex in `safe_print` catches any reflected token in error strings. |
| T-4-02 | DoS (against self) | Higyrus 401 lockout on retry — opt-in probe consumes auth attempts | mitigate | D-HIGY-10 #18 opt-in single-shot pattern (D-IOL-1 mirror Phase 3). Probe runs once if `VERIFY_HIGYRUS_BAD_CREDS=1`, no retry, no sleep, no loop. Operator instructed (`how-to-verify` step 6) to wait 15-60 min if lockout suspected. |
| T-4-03 | Tampering / FALSE PASS | `SafeModel.from_api` silently substitutes typed defaults; mocked tests always pass | mitigate | `probe_field_type_map` (probe 14) runs `_diff_safemodel_bidirectional` recursive walk over `Cuenta`, `Movimiento`, `Posicion`, `PosicionValuada` and their nested models; emits `SHAPE OPEN` finding for every `model-only` key (FALSE PASS risk) and every `wire-only` key (info). The recursion descends into `Domicilio[]`, `PersonaRelacionada[]`, `MedioComunicacion[]`, `CuentaBancaria[]`, `Administrador→{Agente,Operador,Sucursal}`, `Parking[]`. Discretion: Optional[T] types skip direction-A finding (explicit nullable). |
| T-4-04 | DoS / data corruption | Empty list confused with None (HIGY-07) — wrapper crashes or returns None | mitigate | Driver probes 5/7/11 check for empty `[]` and report PASS with detail `(0 items — empty path verified)` per D-HIGY-12. The existing client behavior (`if raw is None: return []` guard at client.py:242,284,311,335) is locked by the Plan 04-03 Verified-live tests. |
| T-4-05 | SYNC-ASYNC-DRIFT | The known `drop_none` deviation in async `_request` (CONCERNS.md) — sync and async emit different query strings | mitigate | `probe_parity_sync_async` (probe 13) captures `httpx.Request.url.query` via bound-method monkey-patch on both `higyrus_client.client._client.request` and `higyrus_client.aio._client.request`; compares the literal query strings emitted for the same call with params optional=None. If different, emit finding `SYNC-ASYNC-DRIFT OPEN` with concrete expected vs actual query strings. |
| T-4-06 | Information Disclosure | Errors envelope leak in stdout (e.g., `e.errors[0]["detail"]` containing user input echo) | mitigate | D-HIGY-2: driver stdout is shape + counts only. `probe_errors_envelope_*` (probes 16-17) extract `e.errors[0]` to assert shape (`"title"` and `"detail"` keys present), but the PROBE line only reports `errors envelope parseado: title+detail presentes` — no actual values printed. Findings file may contain the raw `repr(exc.errors)` string only when the actual envelope is malformed (finding ERROR-MAP OPEN); operator must redact manually before commit if the actual envelope contains PII. |
| T-4-07 | Tampering / EoP | Token poisoning via `configure(password=...+"_INVALID")` not restored — process leaves with poisoned creds | mitigate | `probe_auth_401` uses **try/finally OBLIGATORIO** with `higyrus_client.configure(password=original_password)` in the finally block (Pattern 5 verbatim). Acceptance criterion: `grep -F "configure(password=original_password)" main_higyrus.py` exists. Discretion deviation from Phase 3 IOL CR-03: Higyrus has NO `_refresh_token` to preserve, so `configure()` (which resets `_token` and `_token_ts`) is symmetric and safe. |
| T-4-08 | Tampering | `assert isinstance` defeated in optimized Python `-O` | mitigate (Plan 04-01) | Already mitigated by Plan 04-01 fix: the 10 sites replaced by `raise HigyrusAPIError(0, ...)`. The driver of this plan verifies in-vivo that the raise still propagates (any shape mismatch in vivo would emit `HigyrusAPIError(0, ...)` finding ERROR-MAP). |
| T-4-SC | Information Disclosure | Schema snapshot file leaking PII | mitigate | `verification.schema.schema_of` is PII-free by construction (emits only key names + type names like `"str"`, `"int"`, `"list"`). Acceptance criterion: human inspection of 5 JSONs to verify schema only contains type names. NO raw payload JSONs are committed (D-HIGY-1 explicit). `verification.capture()` is available for gitignored `captures/` if operator wants raw payloads for debugging — those are NOT committed. |
| T-3-04 (inherited Phase 3) | Information Disclosure | `_token` reflected in stack trace or stdout | mitigate (inherited) | `safe_print(text, secrets=[HIGYRUS_USER, HIGYRUS_PASSWORD, _token])` with `_token` added dynamically after first `login()` (D-HIGY-15). `_BEARER` regex in `safe_print` catches reflected tokens in error strings even without explicit secrets list. Phase 3 D-IOL-22 mirror. |
| T-3-09 (inherited Phase 3) | Information Disclosure | Driver stdout values leak | mitigate (inherited) | D-HIGY-2 verbatim discipline preserved. All probes emit `PROBE <name>: <status> <detail>` with detail = counts + shape only (`"3 cuentas"`, `"5 items"`, `"keys=8"`, `"query='fechaDesde=...&fechaHasta=...'"`). |
| T-3-10 (inherited Phase 3) | Information Disclosure | Driver imports of httpx leak internal request URLs | accept (inherited) | Phase 4 does NOT import httpx; the parity capture uses higyrus_client's internal `_client` directly via bound-method monkey-patch. The captured query is a string of param names + values (id_cuenta + fechas — synthetic from driver), no inherent PII. |
| T-3-13 (inherited Phase 3) | Information Disclosure | Errors envelope content from server might contain PII | mitigate | Driver only PROBE-prints presence assertion, not envelope content. Findings file may contain raw envelope via `actual=repr(exc.errors)` for malformed-envelope finding; operator inspects pre-commit. |
| T-3-14 (inherited Phase 3) | Information Disclosure | Secrets list initialization race (first login() before secrets list extended) | mitigate (inherited) | Initial `secrets` list contains `HIGYRUS_USER` + `HIGYRUS_PASSWORD` (from env vars at module load); `_token` is added BEFORE any other PROBE line that could reflect it (immediately after `probe_login_sync` success). Probes 1-2 (login_*) themselves don't print token values (they print "_token cached" / "ok"). |
| T-3-15 (inherited Phase 3) | Repudiation | Cascade SKIPPED masks root cause | mitigate (inherited) | `probe_login_*` emits `AUTH OPEN` finding via `append_finding` BEFORE setting `_auth_failed = True`. The root cause is the first finding in the file; downstream SKIPPED probes are clearly correlated by the `auth_failure_reason` string. |
| T-3-16 (inherited Phase 3) | Repudiation | Cuenta resolution failure unreported | mitigate | Probe 5 emits `NO-DATA OPEN` finding if no cuentas in `estado=alta` AND `HIGYRUS_SAMPLE_CUENTA` env var unset; downstream probes that need `_resolved_cuenta` report SKIPPED with explicit reason `"no _resolved_cuenta resuelto"`. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | NO se instalan paquetes externos en este plan. Solo se usan paquetes ya pinneados en `uv.lock`. Package Legitimacy Audit de 04-RESEARCH.md vacía intencional. |
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Driver structure sanity
uv run python -c "
import ast
tree = ast.parse(open('main_higyrus.py').read())
funcs = [n.name for n in ast.walk(tree) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))]
expected = [
    'probe_login_sync', 'probe_login_async',
    'probe_get_health_sync', 'probe_get_health_async',
    'probe_get_listado_cuentas_sync', 'probe_get_listado_cuentas_async',
    'probe_get_movimientos_sync', 'probe_get_movimientos_async',
    'probe_get_posicion_valuada_sync', 'probe_get_posicion_valuada_async',
    'probe_get_posiciones_sync', 'probe_get_posiciones_async',
    'probe_parity_sync_async', 'probe_field_type_map', 'probe_schema_snapshot',
    'probe_errors_envelope_sync', 'probe_errors_envelope_async', 'probe_auth_401',
    '_diff_safemodel_bidirectional', '_capture_sync_query_string', '_capture_async_query_string',
]
missing = [p for p in expected if p not in funcs]
assert not missing, f'missing: {missing}'
print('OK')
"

# 2. .env.example sanity
grep -F "HIGYRUS_SAMPLE_CUENTA" packages/higyrus-client/.env.example
grep -F "HIGYRUS_SAMPLE_TIPO_CUENTA" packages/higyrus-client/.env.example
grep -F "HIGYRUS_SAMPLE_NIVEL" packages/higyrus-client/.env.example
grep -F "VERIFY_HIGYRUS_BAD_CREDS" packages/higyrus-client/.env.example
[ "$(grep -c "HIGYRUS_TEST_" packages/higyrus-client/.env.example)" = "0" ] || { echo "HIGYRUS_TEST_* not renamed"; exit 1; }

# 3. Static checks
uv run mypy main_higyrus.py
uv run ruff check main_higyrus.py packages/higyrus-client/.env.example 2>&1 || ruff check main_higyrus.py
uv run ruff format --check main_higyrus.py

# 4. Suite still green (mocked tests don't run the driver)
uv run pytest -q

# 5. Live driver checkpoint (Task 2.3 — operator-observed)
# uv run --package higyrus-client python main_higyrus.py
# (output: 18 PROBE lines + SUMMARY; 5 JSONs + 1 markdown generated)

# 6. Schema envelope D-21 + PII-free check (Task 2.3)
uv run python -c "
import json
from pathlib import Path
files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json'))
assert len(files) == 5
required = {'endpoint', 'client_function', 'captured_at', 'base_url', 'sample_params', 'schema'}
for f in files:
    d = json.loads(f.read_text())
    assert required <= set(d.keys()), f'{f.name} missing envelope keys'
print('5 schemas with envelope D-21 OK')
"
```

Per Wave 2 merge:
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `main_higyrus.py` reescrito con 18 probes nombrados en orden D-HIGY-10 (`probe_login_sync`, ..., `probe_auth_401`)
- `main_higyrus.py` contiene helper `_diff_safemodel_bidirectional(payload, model_cls, path)` recursivo con `_is_optional` / `_nested_safemodel_class` / `_is_list_of_safemodel` (Pattern 1)
- `main_higyrus.py` contiene helpers `_capture_sync_query_string` + `_capture_async_query_string` (Pattern 2 monkey-patch del bound method `_client.request` con try/finally restore)
- `main_higyrus.py` contiene flag `_auth_failed: bool = False` + `_auth_failure_reason: str = ""` (cascade SKIPPED, Pattern 3 mirror D-IOL-3)
- `main_higyrus.py` contiene `_resolved_cuenta: str | None = None` resuelto por probe 5 con override `HIGYRUS_SAMPLE_CUENTA` (D-HIGY-11)
- `main_higyrus.py` contiene un único `asyncio.run(_async_main(...))` (D-HIGY-13)
- `main_higyrus.py` contiene `await aio.aclose()` dentro de `contextlib.suppress(Exception)` (IN-03 mirror)
- `main_higyrus.py` contiene probe 18 opt-in via `VERIFY_HIGYRUS_BAD_CREDS=1` con `try/finally` restore del password real (Pattern 5 + T-4-07)
- `main_higyrus.py` contiene `configure(password=original_password)` literal en el finally (acceptance criterion T-4-07)
- `main_higyrus.py` usa `safe_print(text, secrets=[...])` con extensión dinámica de `_token` post-login (D-HIGY-15)
- `main_higyrus.py` emite las 18 líneas verbatim `PROBE <name>: <status> <detail>` + summary `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` (D-02 mirror)
- `main_higyrus.py` NO contiene `time.sleep` (anti-pattern)
- `main_higyrus.py` stdout NO imprime contenido de payloads (D-HIGY-2 — primera fase con PII real)
- `packages/higyrus-client/.env.example` con 3 vars `HIGYRUS_SAMPLE_*` (renombradas de `HIGYRUS_TEST_*`) + 1 var `VERIFY_HIGYRUS_BAD_CREDS` (D-HIGY-14)
- mypy strict + ruff check + format verdes en `main_higyrus.py`
- Suite mockeada completa `uv run pytest -q` verde sin regresiones (los tests mockeados del cliente Higyrus + IOL + Ámbito + harness siguen verdes)
- Driver ejecutado en vivo al menos una vez (Task 2.3 checkpoint) y 6 artefactos generados (5 schemas + findings markdown) inspeccionados y aprobados por el humano
- PII-free verification: los 5 schemas JSON solo contienen type names en el campo `schema` (T-4-SC mitigated)
- Findings OPEN del primer run clasificados por el humano (CONFIRMED/EXPECTED/NO-FIX/OPEN según D-08 lifecycle)
- NO se commitea ningún artefacto en este plan (eso es Plan 04-03 Task 3.3)
</success_criteria>

<output>
Crear `.planning/phases/04-higyrus-verification/04-02-SUMMARY.md` cuando done, registrando:
- Confirmación de los 18 probes y sus firmas finales (incluyendo cualquier ajuste de args de `get_posicion_valuada` / `get_posiciones` por mismatch con firma actual)
- Confirmación del `_diff_safemodel_bidirectional` helper (con/sin `Optional[T]` handling)
- Confirmación del `_capture_sync_query_string` / `_capture_async_query_string` (con/sin retorno de query string asíncrono via tuple-return de `_async_main`)
- Decisión final sobre cómo se extraen los raw payloads para el probe 14 (Discretion: segundo `_request` directo vs `from_api` reverse — probable: doble call deliberado per probe)
- Cantidad final de líneas del driver (estimado: ~700) y métricas de cobertura por requirement
- Resultados del live run de Task 2.3 (cantidad de findings OPEN del primer run, sus clases, decisiones de clasificación del humano)
- Confirmación de PII-free verification de los 5 schemas
- Confirmación de que el opt-in HIGY-AUTH se corrió (o no) y resultado
- Métrica de duración del plan
</output>
</content>
