---
phase: 33-verificaci-n-en-vivo-en-modo-estricto-fixes
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - main_higyrus.py
  - main_matriz.py
autonomous: true
requirements: [LIVE-TYP-01]

must_haves:
  truths:
    - "`main_higyrus.py` and `main_matriz.py` run to completion under `MARKET_LIBS_STRICT_DECODE=1` instead of dying on the first strict divergence with a traceback and zero findings (criterion 1, D-05)."
    - "Every one of higyrus's 19 and matriz's 46 probes binds its endpoint and surface before calling the client, so each SHAPE finding names the endpoint it came from (criterion 1, D-02)."
    - "`main_matriz.py` emits fids above `F-10`, so the first ten findings of a matriz run stop being a silent no-op while the SUMMARY still counts them (P-3)."
    - "Neither driver gains a broad-Exception guard — the per-probe isolation contract stays intact and the AST gate stays green (D-06)."
  artifacts:
    - path: "main_matriz.py"
      provides: "_seed_fid_counter, _STRICT, probe_context on all 46 probes"
      contains: "_seed_fid_counter"
    - path: "main_higyrus.py"
      provides: "probe_context on the remaining 17 probes"
      contains: "probe_context"
  key_links:
    - from: "main_matriz.py"
      to: "verification/divergences.py"
      via: "probe_context decorator on every probe_* + _next_fid injected into divergence_capture"
      pattern: "probe_context\\("
    - from: "main_matriz.py"
      to: "verification/findings.py"
      via: "_seed_fid_counter calls max_existing_fid(_PKG) before the first probe"
      pattern: "max_existing_fid\\("
  prohibitions:
    - id: P-01
      category: privacy
      statement: "MUST NOT widen the frozen six-key type-not-value divergence record, and MUST NOT compose any finding field from anything other than those six keys plus the endpoint and surface the harness itself set — a git-committed findings file must never carry a live wire value."
      status: resolved
      verification: judgment
    - id: P-02
      category: transparency
      statement: "MUST NOT report a census number, a phase criterion, or a cycle-closure PASS produced by a signal that inspected nothing — a green arising from a dropped log level, a swallowed handler exception, an unseeded fid allocator, or a zero-applicable closure filter is a false clean."
      status: resolved
      verification: judgment
    - id: P-03
      category: transparency
      statement: "MUST NOT shrink the census to fit the ratified floor — no narrowing the walker, no dropping a divergence kind, and no deferring a confirmed finding to an unnamed later."
      status: resolved
      verification: judgment
    - id: P-04
      category: transparency
      statement: "MUST NOT widen, close, or enforce matriz's four pre-existing RESPONSE Literal aliases, and MUST NOT promote iol's mercado/plazo to a Literal on RESPONSE-side evidence."
      status: resolved
      verification: judgment
    - id: P-05
      category: safety
      statement: "MUST NOT let the two-pass runner fire a destructive or order-entry operation against a live vendor."
      status: resolved
      verification: judgment
  flagged_assumptions:
    - "The deterministic edge-probe returned LIVE-TYP-01 as `unclassified` / `unresolved`; its acceptance surface is compound (five ROADMAP criteria), not a single classifiable edge. Carried as an explicit flagged assumption — the five ROADMAP Phase 33 success criteria are the real acceptance surface and every truth above derives from them directly. Full statement in 33-01-PLAN.md."
---

<objective>
Bring the two drivers that **die** under strict mode today — `main_higyrus.py` and `main_matriz.py` —
onto the Plan 01 mechanism: `probe_context` on every probe, a seeded fid allocator for matriz, and
the two-pass strict flag threaded through matriz's single ctor sites.

Purpose: `HigyrusDecodeError` and `MatrizDecodeError` are siblings of their `<Pkg>APIError`, not
subclasses, and neither driver's `_RESIDUAL_PROBE_EXCEPTIONS` tuple contains them. Both drivers have
**zero** broad-Exception handlers (AST-gated, D-06), so flipping `strict_decode=True` today kills the
process with a traceback and writes zero findings. These two, and only these two, have that failure
mode — the other three survive-but-misclassify and are Plan 33-03's problem.

Output: 63 probe sites decorated (higyrus 17 remaining + matriz 46), matriz's `_seed_fid_counter`
and `_STRICT`, and both drivers proven still green against the bare-except and single-Client AST
gates.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CONTEXT.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-PATTERNS.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-01-SUMMARY.md
@verification/divergences.py
@verification/test_main_drivers_bare_except.py
</context>

## Artifacts this phase produces

Created or modified by **this plan**: `main_higyrus.py` (the 17 probes Plan 01 did not wire, plus the
driver-level `divergence_capture` installation and the handler-error line in the SUMMARY),
`main_matriz.py::_seed_fid_counter`, `main_matriz.py::_STRICT`,
`main_matriz.py::_shape_probe_result`, and `probe_context` applied to all 46 matriz probes.

Everything else in the phase's artifact inventory is listed in `33-01-PLAN.md` under
"Artifacts this phase produces"; do not duplicate those symbols here.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: matriz — seed the fid allocator, thread the strict flag, decorate all 46 probes</name>
  <reversibility rating="costly">Applying the decorator to 46 probes is a coordinated edit across the driver; reverting it means touching all 46 sites again. No published contract or on-disk format changes, so it is not one-way.</reversibility>
  <files>main_matriz.py</files>
  <read_first>
    - `main_matriz.py` — `_PKG` (`:100`), `_ENDPOINT_TEMPLATES` (`:149-167`), the sample-var block (`:170-176`), `_auth_failed` cascade (`:179-180`), `_fid_counter` (`:185`), `_next_fid` (`:188-192`), `_RESIDUAL_PROBE_EXCEPTIONS` (`:110-125`, note it already contains `PrimaryAPIError`), the `verification` import block (`:69-77`), and the two ctor sites (`:2101` `AsyncClient()`, `:2162` `Client()`)
    - `main_iol.py:182-220` — `_seed_fid_counter` / `_next_fid` canonical pair to port verbatim
    - `main_market_data.py:3319-3324` — the `require_env` -> `write_findings` -> `_seed_fid_counter` -> first probe ordering
    - `main_higyrus.py` as left by Plan 01 — `_shape_probe_result`, the `probe_context` call shape and the `_STRICT` constant, all of which this task mirrors for matriz
    - `packages/matriz-client/src/matriz_client/exceptions.py:35` — `MatrizDecodeError` (note: **not** `PrimaryDecodeError`; `PrimaryAPIError` is a different, unrelated class already in the residual tuple)
    - `verification/divergences.py` — `probe_context`, `endpoint_scope`, `divergence_capture` signatures as finalised in Plan 01
    - `verification/test_main_drivers_bare_except.py` — the AST gate that forbids a broad-Exception guard in this driver
    - `.planning/verification/matriz-client-findings.md` — the committed `F-01`..`F-10` and their non-OPEN statuses
    - `33-RESEARCH.md` Pitfalls P-3, P-4, P-5
  </read_first>
  <behavior>
    - `_seed_fid_counter` raises `_fid_counter` to `max_existing_fid("matriz-client")`, so the first
      `_next_fid()` of a run returns a fid strictly greater than `F-10`. A regression assertion in the
      driver's own SUMMARY path proves this: the count of fids emitted equals the count of new `### F-`
      blocks in the findings file.
    - Every function in `main_matriz.py` whose name starts with `probe_` carries a `probe_context`
      decorator whose `surface` argument matches the function's `_sync` / `_async` suffix.
    - A probe that raises `MatrizDecodeError` returns a `tuple[ProbeResult, None]` with status
      `"FINDING"` rather than propagating; the driver survives to print its SUMMARY.
  </behavior>
  <action>
    Add `max_existing_fid` to the existing `from verification import (...)` block at `main_matriz.py:69`
    and define `_seed_fid_counter()` immediately above `_next_fid` at `:188`, ported verbatim from
    `main_iol.py:190-220` with its docstring adjusted to matriz's own committed fids (`F-01`..`F-10`,
    7 `NO-FIX` / 2 `EXPECTED` / 1 `FIXED` — every one non-`OPEN`, which is why `append_finding`'s
    preservation short-circuit currently discards the first ten findings of every run while `main()`
    still counts them in `FINDING=N`). Call `_seed_fid_counter()` inside `main()` after
    `write_findings(_PKG)` and before the first probe, matching `main_market_data.py:3319-3324`.

    Add the module constant `_STRICT = os.getenv("MARKET_LIBS_STRICT_DECODE") == "1"` alongside the
    other `os.getenv` constants at `:170`, and thread it as `Client(strict_decode=_STRICT)` at `:2162`
    and `AsyncClient(strict_decode=_STRICT)` at `:2101`. Keep exactly one constructor call per class
    so `verification/test_main_matriz_uses_single_client_instance.py` stays green.

    Add a module-level `_shape_probe_result(name: str, surface: str, exc: MatrizDecodeError) ->
    tuple[ProbeResult, None]` mirroring the higyrus helper Plan 01 created: it allocates a fid,
    calls `append_finding(_PKG, ..., class_="SHAPE", status="OPEN", ...)` reading ONLY `exc.model`,
    `exc.field_path`, `exc.declared_type` and `exc.observed_type` (the four attributes certified
    type-and-path-only by T-29-36), and returns the driver's canonical 2-tuple with status
    `"FINDING"`. Import `MatrizDecodeError` from `matriz_client` — verify the symbol name against
    `packages/matriz-client/src/matriz_client/exceptions.py:35` before writing the import; it is
    `MatrizDecodeError`, and `33-RESEARCH.md` refers to it once as `PrimaryDecodeError`, which is
    wrong.

    Then apply `probe_context(endpoint=..., surface=...)` to all 46 `probe_*` functions. Resolve each
    `endpoint` by looking up the client function the probe calls in `_ENDPOINT_TEMPLATES` (`:149-167`,
    keyed by client-function name — the same convention `main_matriz.py:1481` already consumes); pass
    the template string, not an interpolated URL, so no account id or symbol reaches the finding text.
    Set `surface` from the function's own suffix. Pass `decode_error=MatrizDecodeError` and
    `on_decode_error=_shape_probe_result`.

    Before decorating, audit for the P-5 multi-endpoint case: any probe whose `try` block calls more
    than one client function gets `endpoint_scope(...)` re-binding around the second and subsequent
    calls rather than a single misleading decorator default. List the probes that needed it in the
    SUMMARY.

    Do NOT add `MatrizDecodeError` to `_RESIDUAL_PROBE_EXCEPTIONS` — that tuple maps to `ERROR-MAP`
    with a per-probe-unique title that `idempotent_by_title` cannot dedupe, and the decode error must
    surface as a deterministic-title `SHAPE`. Do NOT add a broad-Exception guard anywhere in this
    driver; per-probe isolation via the decorator is the whole mechanism and the AST gate treats a
    global guard as a CI regression (D-06). Do NOT edit any `_decode.py` copy.

    Finally, install the handler in `main()`: wrap the probe sweep in
    `divergence_capture(("matriz_client",), next_fid=_next_fid)` — passing the driver's own allocator
    so handler and driver share ONE counter per process — and add the handler-error count to the
    printed SUMMARY line so a non-zero value is a visible number rather than a scrollback artifact.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' main_matriz.py | grep -c 'probe_context('` equals 46 — one per probe.
    - `grep -cE '^(async )?def probe_' main_matriz.py` equals 46, confirming the decorator count matches the probe count.
    - `grep -v '^#' main_matriz.py | grep -c '_seed_fid_counter'` is at least 2 (definition plus the `main()` call site).
    - `grep -v '^#' main_matriz.py | grep -c 'strict_decode=_STRICT'` equals 2.
    - `grep -v '^#' main_matriz.py | grep -c 'MatrizDecodeError'` is at least 2 (import plus the decorator argument or helper annotation).
    - `uv run pytest verification/test_main_drivers_bare_except.py -q` passes — the driver still has zero broad-Exception handlers.
    - `uv run pytest verification/ -q -k uses_single_client_instance` passes.
    - `uv run pytest verification/test_matriz_sweep_snapshot.py -q --tb=no -rfE` produces a failure/error set identical to the one recorded in `33-BASELINE.md` — the decorator introduced no new breakage in the harness test that calls probes directly.
    - `uv run python -c "import ast,sys; t=ast.parse(open('main_matriz.py').read()); ps=[n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name.startswith('probe_')]; bad=[n.name for n in ps if not any('probe_context' in ast.dump(d) for d in n.decorator_list)]; print(bad); sys.exit(1 if bad else 0)"` exits 0 and prints an empty list.
    - `uv run ruff check . && uv run ruff format --check . && uv run mypy` pass.
    - `uv run pytest packages/matriz-client -q` passes.
    - `uv run python tools/check_decode_intactness.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest verification/test_main_drivers_bare_except.py -q &amp;&amp; uv run pytest verification/ -q -k uses_single_client_instance &amp;&amp; uv run python -c "import ast,sys; t=ast.parse(open('main_matriz.py').read()); ps=[n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name.startswith('probe_')]; bad=[n.name for n in ps if not any('probe_context' in ast.dump(d) for d in n.decorator_list)]; sys.exit(1 if bad else 0)" &amp;&amp; uv run pytest packages/matriz-client -q &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>All 46 matriz probes carry `probe_context`; the fid allocator starts above `F-10`; both ctor sites take `strict_decode=_STRICT`; the bare-except and single-Client AST gates are green; `test_matriz_sweep_snapshot.py` is no worse than the committed baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: higyrus — decorate the remaining 17 probes and install the handler in main()</name>
  <files>main_higyrus.py</files>
  <read_first>
    - `main_higyrus.py` as left by Plan 01 — `_STRICT`, `_seed_fid_counter`, `_shape_probe_result`, and the two already-decorated health probes (the reference shape for the other 17)
    - `main_higyrus.py:156-163` — `_ENDPOINT_TEMPLATES`, keyed by client-function name; `:1976` is the existing consumption site
    - `main_higyrus.py:133-147` — `_RESIDUAL_PROBE_EXCEPTIONS` (extend nothing; the comment block explaining the residual-catch contract stays)
    - `main_higyrus.py:648-712` — the canonical probe exception ladder to preserve
    - `verification/divergences.py` — `probe_context`, `endpoint_scope`, `divergence_capture`
    - `verification/test_main_drivers_bare_except.py` and `verification/test_main_higyrus_uses_single_client_instance.py`
    - `33-RESEARCH.md` Pitfalls P-3, P-5
  </read_first>
  <behavior>
    - All 19 `probe_*` functions in `main_higyrus.py` carry `probe_context`; the two Plan 01 already
      wired are unchanged, the other 17 gain it.
    - The probe sweep in `main()` runs inside `divergence_capture(("higyrus_client",),
      next_fid=_next_fid)`, so `logging.getLogger("higyrus_client")` sits at `INFO` for the duration
      and every `extra`-kind divergence reaches the handler.
    - The SUMMARY line the driver prints carries the handler's error count as a number.
  </behavior>
  <action>
    Apply `probe_context(endpoint=..., surface=..., decode_error=HigyrusDecodeError,
    on_decode_error=_shape_probe_result)` to the 17 `probe_*` functions Plan 01 did not touch,
    resolving each `endpoint` from `_ENDPOINT_TEMPLATES` by the client function the probe calls and
    passing the template string uninterpolated (an interpolated `/api/cuentas/{id_cuenta}/movimientos`
    would put a real account id into a committed finding). Set `surface` from the `_sync` / `_async`
    suffix. Audit for the P-5 multi-endpoint case first and use `endpoint_scope(...)` inside any probe
    that calls more than one client function inside its `try`; list those probes in the SUMMARY.

    Wrap the probe sweep in `main()` with `divergence_capture(("higyrus_client",), next_fid=_next_fid)`
    — the driver's own allocator, never a second counter — and extend the printed SUMMARY line with
    the handler error count and the size of `handler.seen` (the distinct-triple census unit that
    `33-CENSUS.md` will contrast against the ≥22 floor; the finding count and `FINDING=N` are both
    unusable for that contrast).

    Leave `_RESIDUAL_PROBE_EXCEPTIONS` and the per-probe `HigyrusAuthError` / `HigyrusAPIError`
    branches exactly as they are — the ladder order stays `HigyrusAuthError` -> `HigyrusAPIError` ->
    (decode, now intercepted by the decorator outside the function) -> `_RESIDUAL_PROBE_EXCEPTIONS`.
    Add no broad-Exception guard. Edit no `_decode.py` copy.
  </action>
  <acceptance_criteria>
    - `grep -v '^#' main_higyrus.py | grep -c 'probe_context('` equals 19.
    - `grep -cE '^(async )?def probe_' main_higyrus.py` equals 19.
    - `uv run python -c "import ast,sys; t=ast.parse(open('main_higyrus.py').read()); ps=[n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name.startswith('probe_')]; bad=[n.name for n in ps if not any('probe_context' in ast.dump(d) for d in n.decorator_list)]; print(bad); sys.exit(1 if bad else 0)"` exits 0.
    - `grep -v '^#' main_higyrus.py | grep -c 'divergence_capture('` is at least 1.
    - `uv run pytest verification/test_main_drivers_bare_except.py verification/test_main_higyrus_uses_single_client_instance.py -q` passes.
    - `uv run pytest packages/higyrus-client -q` passes.
    - `uv run python tools/check_decode_intactness.py && uv run python tools/check_surface_types.py && uv run python tools/check_uniform_structure.py` all exit 0.
    - `uv run ruff check . && uv run ruff format --check . && uv run mypy` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import ast,sys; t=ast.parse(open('main_higyrus.py').read()); ps=[n for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,ast.AsyncFunctionDef)) and n.name.startswith('probe_')]; bad=[n.name for n in ps if not any('probe_context' in ast.dump(d) for d in n.decorator_list)]; sys.exit(1 if bad else 0)" &amp;&amp; uv run pytest verification/test_main_drivers_bare_except.py verification/test_main_higyrus_uses_single_client_instance.py packages/higyrus-client -q &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run ruff check .</automated>
  </verify>
  <done>All 19 higyrus probes carry `probe_context`; `main()` installs the handler with the driver's own fid allocator; the SUMMARY reports both the handler error count and the distinct-triple count; every CI-equivalent gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vendor API (Higyrus, MATBA ROFEX) -> driver probe | Untrusted third-party JSON; also the source of account-scoped identifiers. |
| driver probe -> `probe_context` endpoint argument | The endpoint string is written into a committed finding's `diff`; an interpolated path would carry a real account id there. |
| `<Pkg>DecodeError` -> `_shape_probe_result` -> finding | The only exception-to-text boundary these two drivers gain in this phase. |
| `.env` (higyrus 8 keys, matriz 8 keys) -> driver process | Credentials in memory; never into a log, finding, or rendered exception. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-08 | Information Disclosure | an interpolated endpoint path reaching a committed finding (`/rest/risk/position/getPositions/{account_id}`, `/api/cuentas/{id_cuenta}/movimientos`) | mitigate | Pass the `_ENDPOINT_TEMPLATES` template string verbatim to `probe_context`; never format it with the live sample value. Both drivers' template dicts already hold the uninterpolated form. |
| T-33-09 | Information Disclosure | `_shape_probe_result` rendering a decode exception into `actual` / `diff` | mitigate | Read only `exc.model`, `exc.field_path`, `exc.declared_type`, `exc.observed_type` — the four attributes certified type-and-path-only by T-29-36. Never `repr(exc)`, never `exc.args`. |
| T-33-10 | Tampering | a broad-Exception guard added to survive strict mode, silently swallowing unrelated defects | mitigate | Forbidden by D-06 and enforced by `verification/test_main_drivers_bare_except.py`, which is an acceptance criterion on both tasks. The decorator provides per-probe isolation instead. |
| T-33-11 | Repudiation | matriz's first ten findings of every run being discarded while `FINDING=N` still counts them | mitigate | `_seed_fid_counter()` before the first probe, plus the SUMMARY reporting `len(handler.seen)` and the handler error count so the census unit and the write count are independently visible (P-3). |
| T-33-12 | Tampering (data integrity on a live venue) | an order-entry probe firing against the real MATBA ROFEX account | accept | Verified read-only: all 46 matriz probes are `get_*` / `error_*` / snapshot shapes with no order-entry surface. This plan adds no probe. Re-verified by the probe-name census in the acceptance criteria. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages installed; `uv.lock` untouched (`33-RESEARCH.md` `## Package Legitimacy Audit` is empty by construction). No install task, therefore no legitimacy checkpoint. |
</threat_model>

<verification>
- `uv run pytest verification/test_main_drivers_bare_except.py -q`
- `uv run pytest verification/ -q -k uses_single_client_instance`
- `uv run pytest packages/higyrus-client packages/matriz-client -q`
- `uv run pytest verification/test_matriz_sweep_snapshot.py -q --tb=no -rfE` compared against `33-BASELINE.md`
- `uv run python tools/check_decode_intactness.py && uv run python tools/check_surface_types.py && uv run python tools/check_uniform_structure.py`
- `uv run mypy && uv run ruff check . && uv run ruff format --check .`
- AST census: every `probe_*` in both drivers carries `probe_context`
</verification>

<success_criteria>
- 19/19 higyrus and 46/46 matriz probes carry `probe_context` with an uninterpolated endpoint template and a surface matching the function suffix
- `main_matriz.py` defines and calls `_seed_fid_counter()` between `write_findings(_PKG)` and the first probe
- Both drivers thread `strict_decode=_STRICT` through exactly one `Client` and one `AsyncClient` call
- Neither driver contains a broad-Exception guard; `verification/test_main_drivers_bare_except.py` is green
- Each driver's `main()` installs the handler with its own `_next_fid` — one allocator per package process
- Each driver's SUMMARY prints the handler error count and `len(handler.seen)`
- `verification/test_matriz_sweep_snapshot.py` is no worse than the `33-BASELINE.md` baseline
</success_criteria>

<output>
Create `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-02-SUMMARY.md` when done.
Record: the list of probes that required `endpoint_scope` re-binding (P-5) per driver, the pre- and
post-decorator failure/error set of `verification/test_matriz_sweep_snapshot.py`, and the exact
SUMMARY line format each driver now prints (33-04 parses it for the census).
</output>
