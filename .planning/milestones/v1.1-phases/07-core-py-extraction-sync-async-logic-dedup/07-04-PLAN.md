---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/higyrus-client/src/higyrus_client/_core.py
  - packages/higyrus-client/src/higyrus_client/client.py
  - packages/higyrus-client/src/higyrus_client/aio.py
  - packages/higyrus-client/tests/test_core.py
autonomous: true
requirements: [REFAC-03]
requirements_addressed: [REFAC-03]
tags: [phase-07, higyrus, url-encoding-quirk, refac-03]
user_setup: []

must_haves:
  truths:
    - "`higyrus_client._core` define `RequestSpec` con `json_body` + `url_pre_encoded: str | None` field (D-01 higyrus row) + builders/parsers per endpoint + auth-flow primitives `build_login_request`/`parse_login_response`"
    - "El URL-encoding quirk (`urlencode(..., doseq=True, quote_via=quote, safe='/')` para preservar `/` literal; Higyrus IIS rechaza `%2F`) queda ENCAPSULADO en cada `build_<endpoint>_request` que retorna `RequestSpec(path=<pre-encoded URL>, url_pre_encoded=True)` — los transport shells NO conocen el quirk"
    - "Parsers preservan semántica de `errors` array (`HigyrusAPIError` raised con structured errors per Phase 6)"
    - "`client.py` y `aio.py` colapsan a transport shells; aliases D-04 preservan B8 identity"
    - "B8 test existente `packages/higyrus-client/tests/test_client_class.py:359-369` (`test_aio_imports_raise_for_response_from_client`) sigue verde"
    - "Tests URL-encoding existentes (`assert 'fechaHasta=07/06/2026' in query_str` + `assert '%2F' not in query_str`, `packages/higyrus-client/tests/test_client_class.py:340-352`) siguen verde — el quirk se preserva post-refactor"
    - "LOC drop ≥30% agregado vs Phase 6 baseline 1354 (client.py=685 + aio.py=669)"
    - "Public surface snapshot zero diff (D-16); `_core` NO entra a `__all__`"
    - "Cross-leak guard verde — higyrus sync + async sentinels aislados"
    - "PEP 562 shim Phase 6 intacto"
    - "lint-imports verde con 4 contracts; full suite verde"
  artifacts:
    - path: "packages/higyrus-client/src/higyrus_client/_core.py"
      provides: "RequestSpec (con json_body + url_pre_encoded) + auth-flow + per-endpoint builders/parsers; encapsula URL-encoding quirk"
      contains: "build_login_request"
      min_lines: 150
    - path: "packages/higyrus-client/src/higyrus_client/client.py"
      provides: "Sync transport shell — Client class preserved, endpoint bodies collapsed; URL-encoding quirk en _core (no aquí)"
      contains: "_raise_for_response = _core.raise_for_response"
    - path: "packages/higyrus-client/src/higyrus_client/aio.py"
      provides: "Async transport shell — AsyncClient preserved, mirror sync"
      contains: "_raise_for_response"
    - path: "packages/higyrus-client/tests/test_core.py"
      provides: "Unit tests for higyrus _core: builders/parsers + URL-encoding quirk + errors array parsing"
      min_lines: 60
  key_links:
    - from: "higyrus_client._core.build_get_movimientos_request"
      to: "URL-encoding quirk (urlencode doseq=True quote_via=quote safe='/')"
      via: "encapsulated within builder via pre-encoded path"
      pattern: "doseq=True|quote_via=quote|url_pre_encoded"
    - from: "higyrus_client.aio._raise_for_response"
      to: "higyrus_client._core.raise_for_response"
      via: "module-level alias D-04"
      pattern: "_raise_for_response = _core.raise_for_response|from higyrus_client\\._core import raise_for_response as _raise_for_response"
---

<objective>
Extraer `higyrus_client/_core.py` con auth-flow primitives + per-endpoint builders/parsers que ENCAPSULAN la quirk URL-encoding propia de Higyrus IIS (`urlencode(..., doseq=True, quote_via=quote, safe="/")` — preserva `/` literal; Higyrus IIS rechaza `%2F`); colapsar `client.py` y `aio.py` a transport shells; preservar B8 alias D-04 y PEP 562 shim Phase 6. higyrus es el paquete más voluminoso por endpoint count (1354 LOC client+aio aggregate).

Purpose: Phase 7 REFAC-03 success criterion #1 + #3 aplicado a higyrus. La quirk URL-encoding va a `_core.build_<endpoint>_request` (CONTEXT D-01 higyrus row + RESEARCH §"Established Patterns" + RESEARCH Pattern 1 higyrus row "opción A — encapsula la quirk en `_core.build_<endpoint>_request`"). Los transport shells NO conocen el detalle de encoding.

Output: 1 nuevo `_core.py` higyrus; 2 archivos colapsados; 1 nuevo `tests/test_core.py`. Single commit atómico per D-05/D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md
@CLAUDE.md
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/src/higyrus_client/aio.py
@packages/higyrus-client/src/higyrus_client/_state.py
@packages/higyrus-client/src/higyrus_client/_params.py
@packages/higyrus-client/src/higyrus_client/models.py
@packages/higyrus-client/src/higyrus_client/exceptions.py
@packages/higyrus-client/src/higyrus_client/__init__.py
@packages/higyrus-client/tests/conftest.py
@packages/higyrus-client/tests/test_client_class.py
@verification/snapshots/higyrus_client-surface.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build `higyrus_client/_core.py` — RequestSpec + auth-flow + endpoint builders (URL-encoding quirk encapsulated) + parsers (errors array → HigyrusAPIError)</name>
  <files>packages/higyrus-client/src/higyrus_client/_core.py, packages/higyrus-client/tests/test_core.py</files>
  <read_first>
    - packages/higyrus-client/src/higyrus_client/client.py (líneas 1-60 module shape + 78-103 `_raise_for_response` (parses `errors` array) MOVE verbatim + 111-303 Client class auth-flow + endpoints; CRITICAL: línea ~287 contiene `urlencode(..., doseq=True, quote_via=quote, safe='/')` — ese es el quirk a encapsular)
    - packages/higyrus-client/src/higyrus_client/aio.py (módulo async análogo — mismo quirk)
    - packages/higyrus-client/src/higyrus_client/_state.py (Phase 6; `_ClientState` con `username`, `password`, `token`, `token_expires_at`, `client_id`)
    - packages/higyrus-client/src/higyrus_client/_params.py (helpers `drop_none`, `format_date`, `format_bool` — REUSAR desde `_core.py`)
    - packages/higyrus-client/src/higyrus_client/models.py (SafeModel + Cuenta/Movimiento/PosicionValuada dataclasses)
    - packages/higyrus-client/src/higyrus_client/exceptions.py (`HigyrusClientError`/`HigyrusAPIError`/`HigyrusAuthorizationError`)
    - packages/higyrus-client/tests/test_client_class.py (líneas 340-352 URL-encoding test: `'fechaHasta=07/06/2026' in query_str` + `'%2F' not in query_str` — DEBE seguir verde; línea 359-369 B8 test)
    - packages/higyrus-client/tests/conftest.py (autouse `configure(token=..., token_expires_at=..., client_id=...)`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 1 higyrus row (json_body + url_pre_encoded); Pattern 5 auth-flow higyrus; § "Established Patterns" — higyrus URL-encoding quirk; § "Code Examples" Example 5 — analog para construir builders/parsers)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§1 module shape; §2 higyrus RequestSpec con json_body + url_pre_encoded; §7 auth-flow factoring; §10 tests/test_core.py higyrus shape — incluye URL-encoding quirk assertions)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-01 higyrus row — opción A encapsula quirk; D-02 auth-flow; D-04 alias; D-06 body-consume-then-raise)
    - CLAUDE.md (`from __future__ import annotations`; no relative imports; double quotes; line-length 100)
  </read_first>
  <behavior>
    - `_core.py` define `@dataclass(frozen=True, slots=True) RequestSpec` con fields higyrus: `method`, `path`, `params`, `headers`, `json_body: dict[str, Any] | None`, `url_pre_encoded: bool = False` (o alternativa: el path ya incluye query string si `url_pre_encoded=True`; planner decide entre opción A "path con query inline" vs "field separado" — preferencia A per RESEARCH Pattern 1 higyrus row).
    - `raise_for_response(resp)` MOVE la lógica actual (parsea `errors` array del body JSON y raise `HigyrusAPIError(...)` con structured details — preservar mensaje exacto Phase 6).
    - `token_is_fresh(state) -> bool` helper.
    - `build_login_request(state) -> RequestSpec`: usar el flow auth higyrus actual (POST con JSON body — diferente a iol que es form-encoded). Inspect `client.py login()` para detalles exactos.
    - `parse_login_response(resp) -> tuple[str, float]` con D-06 pattern.
    - Por cada endpoint público (`get_listado_cuentas`, `get_movimientos`, `get_posicion_valuada`, etc. — `grep -nE "^    def (get_)" packages/higyrus-client/src/higyrus_client/client.py`): `build_<endpoint>_request(state, ...) -> RequestSpec` que ENCAPSULA el URL-encoding quirk usando `urlencode(..., doseq=True, quote_via=quote, safe='/')` cuando el endpoint requiere params con date format `dd/mm/yyyy`. Returna `RequestSpec(method="GET", path=<path-con-query-pre-encoded>, url_pre_encoded=True, ...)`.
    - Per endpoint: `parse_<endpoint>_response(resp) -> <typed>` con D-06 pattern + parsing de SafeModel.from_api para retornar `list[Cuenta]`/`list[Movimiento]`/etc.
    - Casos 204 No Content: parser debe retornar `[]` (preservar Phase 6 behavior).
    - `_core.py` NO importa `higyrus_client.client` ni `higyrus_client.aio`.
    - `tests/test_core.py`: ≥6 tests cubriendo:
      1. `test_build_get_movimientos_request_preserves_slash_in_query` — assertion `'fechaHasta=07/06/2026' in spec.path` + `'%2F' not in spec.path` (PATTERNS §10 ejemplo verbatim).
      2. `test_parse_get_movimientos_response_returns_empty_on_204` — `httpx.Response(204, content=b"")` → `[]`.
      3. `test_raise_for_response_extracts_errors_array` — body con `{"errors": [...]}` → `HigyrusAPIError` con structured details.
      4. `test_build_login_request_returns_correct_json_shape` — JSON body con credentials shape.
      5. `test_parse_login_response_extracts_token_and_expires` — synthetic Response.
      6. (opt) `test_build_listado_cuentas_no_quirk` — endpoint without date params doesn't trigger quirk.
  </behavior>
  <action>
    1. Reemplazar el placeholder Plan 1 de `higyrus_client/_core.py` por el módulo completo:
       - Header docstring multi-linea: "Pure builders and parsers for the higyrus-client REST client. Phase 7 REFAC-03 + D-01 (per-package RequestSpec with url_pre_encoded) + D-02 (auth-flow factorized) + D-04 + D-06. URL-encoding quirk (Higyrus IIS rechaza %2F) encapsulated within build_<endpoint>_request via urlencode(doseq=True, quote_via=quote, safe='/'). No I/O. No imports from `higyrus_client.client` or `higyrus_client.aio` (enforced by import-linter)."
       - `from __future__ import annotations`.
       - Imports: `time`, `dataclasses (dataclass)`, `typing.Any`, `urllib.parse (urlencode, quote)`, `httpx`. Internos: `from higyrus_client._state import _ClientState, _TOKEN_TTL_*`, `from higyrus_client._params import drop_none, format_date, format_bool`, `from higyrus_client.exceptions import HigyrusAPIError, HigyrusAuthorizationError`, `from higyrus_client.models import Cuenta, Movimiento, PosicionValuada, ...` según necesidad.

    2. Section dividers (§1 PATTERNS):
       - `# --- RequestSpec ---`
       - `# --- Stateless helpers (D-04 — moved from client.py) ---`
       - `# --- Auth flow primitives (D-02) ---`
       - `# --- Endpoint builders ---`
       - `# --- Endpoint parsers ---`

    3. Implementar `RequestSpec` con field `json_body` + `url_pre_encoded`. Docstring describiendo la quirk encapsulation: "url_pre_encoded=True signals path includes query string already (Higyrus IIS rechaza %2F — see D-01)."

    4. `raise_for_response`: MOVE verbatim de `client.py:78-103` — preservar el parsing del `errors` array y el raise de `HigyrusAPIError` con structured details.

    5. `token_is_fresh(state) -> bool` helper.

    6. `build_login_request` + `parse_login_response`: inspect `client.py login()` actual y replicar la shape (JSON body POST, parse del response con token + expires_in/expires_at).

    7. Por cada endpoint público:
       - `build_<endpoint>_request(state, ...)`:
         - Si el endpoint requiere params con date format `dd/mm/yyyy` (e.g. `fechaDesde`, `fechaHasta`): construir `params_dict = drop_none({"fechaDesde": format_date(...), "fechaHasta": format_date(...), ...})`; `query_str = urlencode(params_dict, doseq=True, quote_via=quote, safe="/")`; `path = f"{base_path}?{query_str}"`; retornar `RequestSpec(method="GET", path=path, url_pre_encoded=True, headers={...})`.
         - Si no requiere: `RequestSpec(method="GET", path="/api/v1/listado", params=..., headers={...})` con `url_pre_encoded=False` (default).
       - `parse_<endpoint>_response(resp) -> list[Model] | ...`:
         - Caso 204: return `[]`.
         - Otherwise: `resp.read()` + `raise_for_response(resp)` + `data = resp.json()` + `return [Model.from_api(x) for x in data]` (o estructura según endpoint).

    8. Crear `packages/higyrus-client/tests/test_core.py` con ≥6 tests (per `<behavior>`).
  </action>
  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_core.py -x -v &amp;&amp; uv run mypy packages/higyrus-client/src/higyrus_client/_core.py packages/higyrus-client/tests/test_core.py &amp;&amp; uv run ruff check packages/higyrus-client/src/higyrus_client/_core.py packages/higyrus-client/tests/test_core.py &amp;&amp; uv run ruff format --check packages/higyrus-client/src/higyrus_client/_core.py packages/higyrus-client/tests/test_core.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'from __future__ import annotations' packages/higyrus-client/src/higyrus_client/_core.py` == 1
    - Source: `grep -c 'class RequestSpec' packages/higyrus-client/src/higyrus_client/_core.py` == 1
    - Source: `grep -c 'json_body' packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 1
    - Source: `grep -c 'url_pre_encoded' packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 1 (D-01 higyrus row)
    - Source: `grep -cE "urlencode\(.*doseq=True.*quote_via=quote.*safe=" packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 1 (quirk encapsulated)
    - Source: `grep -c 'def build_login_request' packages/higyrus-client/src/higyrus_client/_core.py` == 1
    - Source: `grep -c 'def parse_login_response' packages/higyrus-client/src/higyrus_client/_core.py` == 1
    - Source: `grep -c 'def raise_for_response' packages/higyrus-client/src/higyrus_client/_core.py` == 1
    - Source: `grep -c 'def build_' packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 4 (login + ≥3 endpoint builders)
    - Source: `grep -c 'def parse_' packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 3
    - Source: `grep -c 'resp.read()' packages/higyrus-client/src/higyrus_client/_core.py` &gt;= 1 (D-06 pattern)
    - Source: `grep -E "from higyrus_client\.(client|aio)" packages/higyrus-client/src/higyrus_client/_core.py | grep -v '^#'` returns NOTHING
    - Source: `wc -l packages/higyrus-client/src/higyrus_client/_core.py | awk '{print $1}'` &gt;= 150
    - Test command: `uv run pytest packages/higyrus-client/tests/test_core.py -x` exits 0 with ≥6 tests — including URL-encoding quirk assertion (`%2F not in` and `fechaHasta=DD/MM/YYYY in`)
    - CLI: `uv run mypy packages/higyrus-client/src/higyrus_client/_core.py packages/higyrus-client/tests/test_core.py` exits 0
    - CLI: `uv run ruff check packages/higyrus-client/src/higyrus_client/_core.py packages/higyrus-client/tests/test_core.py` exits 0
    - CLI: `uv run lint-imports` exits 0
  </acceptance_criteria>
  <done>
    `higyrus_client/_core.py` completo con URL-encoding quirk encapsulado; ≥6 unit tests verde; lint-imports verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse `higyrus_client/client.py` + `aio.py` to transport shells (D-04 alias; URL-encoding quirk lives in _core; LOC drop ≥30%)</name>
  <files>packages/higyrus-client/src/higyrus_client/client.py, packages/higyrus-client/src/higyrus_client/aio.py</files>
  <read_first>
    - packages/higyrus-client/src/higyrus_client/client.py (current Client class — preserve skeleton; collapse endpoint bodies + auth-flow body + _request signature; PEP 562 shim NO TOCAR)
    - packages/higyrus-client/src/higyrus_client/aio.py (current AsyncClient — mirror sync collapse)
    - packages/higyrus-client/src/higyrus_client/_core.py (Task 1 producto)
    - packages/higyrus-client/tests/test_client_class.py (líneas 340-352 URL-encoding assertion — verificar verde post-refactor; línea 359-369 B8 test)
    - packages/higyrus-client/tests/test_fixture_reaches_production.py (sentinel guard Phase 6)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§4 D-04 alias higyrus; §5 transport shell; §6 endpoint 3-liner; §7 auth-flow consumer; §"Established Patterns" higyrus URL-encoding goes-to-`_core`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-03; D-05 ≤30-50 LOC/group; D-14 LOC metric; D-16 snapshot preservation)
    - verification/snapshots/higyrus_client-surface.txt (D-16 — sin diff)
    - CLAUDE.md (no wildcard imports; dual sync/async mirroring; PEP 562 shim NO TOCAR; mandatory `from __future__ import annotations`)
  </read_first>
  <behavior>
    - `client.py` post-refactor:
      - Imports: agregar `from higyrus_client import _core` + `from higyrus_client._core import RequestSpec`.
      - REEMPLAZAR `_raise_for_response(...)` (líneas ~78-103) por alias module-level `_raise_for_response = _core.raise_for_response  # D-04`.
      - `Client.login()`: 3-liner orquestación con `_core.build_login_request` + `_core.parse_login_response`.
      - `Client._ensure_token()`: usar `_core.token_is_fresh(self._state)`.
      - `Client._request(spec: RequestSpec) -> httpx.Response`:
        - `self._ensure_token()`; `assert self._state.token is not None`.
        - `http = self._ensure_http_client()`.
        - URL construction: si `spec.url_pre_encoded`, `url = f"{base_url}{spec.path}"` (path ya tiene query encoded); else, construir normal con `params=spec.params`.
        - Headers: `Authorization` bearer + `**spec.headers`.
        - `return http.request(spec.method, url, params=None if spec.url_pre_encoded else spec.params, json=spec.json_body, headers=headers)`.
      - Endpoint methods: 3-liner.
      - PEP 562 shim: NO TOCAR.
    - `aio.py` post-refactor: mirror sync con `await`.
    - Identidad B8 verde.
    - LOC drop ≥30% (≤948 LOC client+aio agregado vs 1354 baseline).
    - Public surface snapshot zero diff.
    - URL-encoding test existente (`%2F not in query_str`) sigue verde — porque el quirk vive en `_core.py` builder, el wire request sigue tendiendo `/` literal.
  </behavior>
  <action>
    1. Identificar endpoint methods públicos: `grep -nE '^    (async )?def (get_|configure|login|_ensure_token|_request)' packages/higyrus-client/src/higyrus_client/{client,aio}.py`.

    2. Refactor de `client.py`:
       - Top: agregar imports `_core` + `RequestSpec`.
       - REEMPLAZAR `_raise_for_response(...)` por asignación module-level D-04.
       - `Client.login()`: body 3-liner según `<behavior>`.
       - `Client._ensure_token()`: usar `_core.token_is_fresh`; resto preservar.
       - `Client._request(spec)`: nuevo body según `<behavior>` con manejo de `url_pre_encoded`.
       - Endpoint methods: 3-liner body.
       - PEP 562 shim: NO TOCAR.

    3. Refactor de `aio.py`: mirror sync con `await`. Conservar async locks actuales.

    4. Validar identidad B8:
       ```
       uv run python -c "from higyrus_client.aio import _raise_for_response as a; from higyrus_client.client import _raise_for_response as c; assert a is c"
       ```

    5. Validar LOC drop:
       ```
       wc -l packages/higyrus-client/src/higyrus_client/{client,aio}.py
       ```
       Confirmar sum ≤ 948.

    6. Validar D-16 snapshot zero diff:
       ```
       uv run pytest verification/test_public_surface.py -k higyrus -x
       ```

    7. Validar URL-encoding test:
       ```
       uv run pytest packages/higyrus-client/tests/test_client_class.py -k "%2F or fechaHasta or movimientos" -x -v
       ```
       (los tests existentes de Phase 6 deben pasar; el wire request lleva `/` literal).

    8. Validar lint-imports + suite completa.
  </action>
  <verify>
    <automated>uv run pytest packages/higyrus-client/ -x &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -k higyrus -x -v &amp;&amp; uv run pytest verification/test_public_surface.py -k higyrus -x &amp;&amp; uv run python -c "from higyrus_client.aio import _raise_for_response as a; from higyrus_client.client import _raise_for_response as c; assert a is c" &amp;&amp; uv run mypy packages/higyrus-client/ &amp;&amp; uv run ruff check packages/higyrus-client/ &amp;&amp; uv run ruff format --check packages/higyrus-client/ &amp;&amp; uv run lint-imports &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run python -c "from higyrus_client.aio import _raise_for_response as a; from higyrus_client.client import _raise_for_response as c; assert a is c"` exits 0 (B8 D-04)
    - Source: `grep -c '_raise_for_response = _core.raise_for_response' packages/higyrus-client/src/higyrus_client/client.py` == 1
    - Source: `grep -cE 'from higyrus_client._core import raise_for_response as _raise_for_response' packages/higyrus-client/src/higyrus_client/aio.py` == 1
    - Source: `grep -cE 'def _request\(self,\s*spec' packages/higyrus-client/src/higyrus_client/client.py` == 1
    - Source: `grep -cE 'async def _request\(self,\s*spec' packages/higyrus-client/src/higyrus_client/aio.py` == 1
    - Source: `grep -c '_core.build_login_request' packages/higyrus-client/src/higyrus_client/client.py` == 1
    - Source: `grep -c '_core.build_' packages/higyrus-client/src/higyrus_client/client.py` &gt;= 3 (auth + at least 2 endpoint builders)
    - Source: `grep -c '_core.parse_' packages/higyrus-client/src/higyrus_client/client.py` &gt;= 3
    - Source: Verify URL-encoding quirk NO leaked into shell — `grep -cE "urlencode\(.*doseq=True" packages/higyrus-client/src/higyrus_client/client.py` == 0 (the quirk lives ONLY in _core)
    - Source: `grep -cE "urlencode\(.*doseq=True" packages/higyrus-client/src/higyrus_client/aio.py` == 0
    - Source: aggregated `wc -l packages/higyrus-client/src/higyrus_client/{client,aio}.py` sum ≤ 948 (≥30% drop vs 1354 baseline)
    - Test command: `uv run pytest packages/higyrus-client/ -x` exits 0 — including URL-encoding quirk test (`packages/higyrus-client/tests/test_client_class.py:340-352`) and B8 test (`:359-369`)
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -k higyrus -x -v` exits 0
    - Test command: `uv run pytest verification/test_public_surface.py -k higyrus -x` exits 0 (D-16 zero diff)
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -3` no failures
    - CLI: `uv run lint-imports` exits 0
    - CLI: `uv run mypy packages/higyrus-client/` exits 0
    - CLI: `uv run ruff check packages/higyrus-client/` exits 0
    - CLI: `uv run ruff format --check packages/higyrus-client/` exits 0
  </acceptance_criteria>
  <done>
    `client.py`/`aio.py` colapsados; B8 verde; URL-encoding quirk encapsulado en `_core` (no leak en shell); LOC drop ≥30%; snapshot zero diff; cross-leak guard verde; lint-imports verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_core.py` → `client.py`/`aio.py` | T-7-01 — import-linter enforça |
| sync surface → async surface | T-7-02 — cross-leak guard cubre |
| URL-encoding quirk → wire request | T-7-URLQUIRK — preservar `/` literal; cualquier accidental `%2F` rompe API contract con Higyrus IIS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | `_core.py` accidental transport import | mitigate | Task 1 source acceptance + Task 2 `uv run lint-imports` exit 0 |
| T-7-02 | Tampering | Sync/async token cross-contamination | mitigate | Task 2 includes `verification/test_sync_async_isolation.py -k higyrus` |
| T-7-03 | Tampering | B8 alias break (Pitfall 2) | mitigate | Task 2 B8 repl assert + `test_aio_imports_raise_for_response_from_client` (línea 359-369) verde |
| T-7-URLQUIRK | Tampering (API contract) | URL-encoding quirk accidentally bypassed (rendering `%2F` instead of `/`) | mitigate | Task 1 `tests/test_core.py` includes `%2F not in spec.path` assertion; Task 2 verify includes Phase 6 URL-encoding test (`test_client_class.py:340-352`) sigue verde |
| T-7-SHELL-LEAK | Tampering | URL-encoding quirk leaked from `_core` to shell (defeats encapsulation) | mitigate | Task 2 source assertion `grep "urlencode.*doseq=True" packages/higyrus-client/src/higyrus_client/client.py` == 0 |
</threat_model>

<verification>
- B8 identity verde.
- `uv run lint-imports` exit 0.
- Public surface snapshot zero diff.
- URL-encoding quirk test Phase 6 (`%2F not in query_str`) verde post-refactor; quirk vive en `_core.py` only.
- Cross-leak guard verde.
- LOC drop ≥30% (≤948 LOC).
- Full suite verde; ruff + mypy strict pasan.
</verification>

<success_criteria>
- `higyrus_client/_core.py` con RequestSpec (json_body + url_pre_encoded), auth-flow, builders/parsers, URL-encoding quirk encapsulado; PURO.
- `client.py`/`aio.py` colapsados; LOC drop ≥30% vs Phase 6 baseline 1354.
- B8 alias D-04 verde; tests verde.
- URL-encoding quirk: assertion `%2F not in` verde; encapsulation source assertion verde.
- Public surface snapshot Phase 6 unchanged.
- Cross-leak guard verde.
- lint-imports verde con 4 contracts; PEP 562 shim intacto.
- ≥6 nuevos tests de `_core` agregados.
</success_criteria>

<output>
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-04-SUMMARY.md`:
- LOC drop:
  ```
  LOC drop vs Phase 6 baseline:
  - client.py: 685 → <X> (-<Y>%)
  - aio.py:    669 → <X> (-<Y>%)
  - _core.py:  0 → <N> (NEW)
  - Aggregate client+aio: 1354 → <X> (-<Y>%)   <PASS/FAIL ≥30%>
  ```
- B8 identity confirmed.
- URL-encoding quirk: test_core assertion verde + Phase 6 test verde + shell encapsulation verde.
- Snapshot zero diff.
- Cross-leak guard verde.
- N nuevos tests `_core` agregados.
</output>
