---
phase: 42
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md
autonomous: true
requirements: [LIVE-02]

must_haves:
  truths:
    - "El censo de matriz corrió EN VIVO contra el sandbox bbsa detrás del gate portado (LIVE-02)"
    - "La salida lleva venue y timestamp en el encabezado, medidos, no hardcodeados (criterio 3)"
    - "Los cinco campos Literal de RESPONSE tienen valores observados, o una declaración explícita de por qué no se pudieron medir (criterio 3)"
    - "El D-lock (b) de la Phase 29 queda reafirmado: el censo NO autoriza promover ningún campo RESPONSE a Literal"
    - "census_iol() NO se ejecutó (D-04)"
    - "verification/mutation_gate.py sigue byte-idéntico tras la corrida (criterio 4)"
  artifacts:
    - path: ".planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md"
      provides: "Inventario committeado de los valores Literal de RESPONSE medidos en bbsa, con header venue+timestamp, disposición campo por campo y reafirmación del D-lock (b)"
      min_lines: 60
      contains: "CENSUS-HEADER"
  key_links:
    - from: ".planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md"
      to: "scripts/literal_census_33.py"
      via: "transcripción verbatim del stdout de la corrida --matriz-only (líneas CENSUS-HEADER, CENSUS-DLOCK y las líneas de _report)"
      pattern: "CENSUS-HEADER venue="
    - from: "scripts/literal_census_33.py"
      to: "main_matriz.py"
      via: "el token de venue del header sale de _venue_token(base), importado — el header no puede mentir sobre contra qué se midió"
      pattern: "_venue_token"
---

<objective>
Correr el censo de valores `Literal` de RESPONSE de `matriz-client` EN VIVO contra el sandbox
`bbsa`, detrás del gate portado y aprobado en el plan 42-01, y dejar el resultado como un artefacto
committeado (`42-CENSUS.md`) con venue y timestamp en el encabezado.

Purpose: LIVE-02 y el criterio 3 del ROADMAP. La mitad abierta del plan 33-06 —*qué valores manda
el vendor* en `marketId`, `cficode`, `currency`, `orderTypes` y `ordType`— nunca se midió porque el
gate remarkets-only del script bloqueaba la corrida. Con el gate portado, la medición es posible por
primera vez. El artefacto es una **medición**, no una autorización: el D-lock (b) de la Phase 29
sigue vigente y este documento debe decirlo explícitamente (Pitfall 3).

Output: `42-CENSUS.md` committeado, más los dumps crudos en el staging gitignored
`.planning/verification/captures/`. Cero mutaciones, cero órdenes emitidas.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CONTEXT.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos y archivos creados por la Phase 42 (no existen en HEAD). Excluir de cualquier verificación
de drift: son salida, no deriva.

| Artefacto | Tipo | Creado en |
|-----------|------|-----------|
| `verification/test_literal_census_venue_gate.py` (y sus 11 tests + `_census_matriz_node`, `_membership_offenders`, `_TARGET`) | archivo nuevo | 42-01 |
| `_census_header_lines()`, `_census_header()`, local `venue`, flag `--matriz-only` en `scripts/literal_census_33.py` | símbolos nuevos | 42-01 |
| `CENSUS-HEADER` / `CENSUS-DLOCK` | prefijos de línea de stdout | 42-01 |
| entrada `verification/test_literal_census_venue_gate.py` en `ci.yml` | línea de allowlist | 42-01 |
| `.planning/phases/42-…/42-CENSUS.md` | artefacto committeado | 42-02 |
| `.planning/verification/captures/matriz-census-*.json` (5 archivos) | artefactos gitignored | 42-02 |
| `capture` importado en `main_market_data.py`; labels `wire-instruments-42` / `wire-segments-42` | símbolos nuevos | 42-04 |
| `.planning/verification/captures/market-data-wire-{instruments,segments}-42.json` | artefactos gitignored | 42-04 |
| `.planning/phases/42-…/42-WIRE-READ.md` | artefacto committeado | 42-04 |
| `LIVE-HIGY-42` | identificador de destino nuevo | 42-05 |

<tasks>

<task type="auto">
  <name>Task 1: Corrida en vivo del censo de matriz contra bbsa (--matriz-only, D-04)</name>
  <files>.planning/verification/captures/matriz-census-get_segments.json, .planning/verification/captures/matriz-census-get_all_instruments.json, .planning/verification/captures/matriz-census-get_instruments_details.json, .planning/verification/captures/matriz-census-get_active_orders.json, .planning/verification/captures/matriz-census-get_all_orders.json (todos gitignored)</files>
  <read_first>
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md`
      — **verificar que el checkpoint bloqueante de la Task 3 del plan 42-01 fue aprobado por el
      operador y que la respuesta está transcrita verbatim**. Si no lo está, DETENER: esta task emite
      tráfico en vivo.
    - `scripts/literal_census_33.py` (LEER ÍNTEGRO tras el port del plan 42-01) — el gate, el header,
      `census_matriz` y el despacho `--matriz-only`.
    - `.planning/phases/42-…/42-RESEARCH.md` § "Trampa de resolución de `.env` (P-10)" — por qué la
      invocación debe ser un archivo `.py` real y nunca `python -c`, y § "Medición 3" (credenciales
      de matriz presentes, venue = `bbsa`, `PRIMARY_ACCOUNT` seteada).
    - `.planning/phases/42-…/42-RESEARCH.md` § Pitfall 3 — sobre-generalización de venue y el
      D-lock (b).
    - `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-06-SUMMARY.md:140-180`
      — cómo se transcribió la salida del censo la vez anterior (forma del reporte).
  </read_first>
  <action>
    Ejecutar el censo en vivo, sólo matriz, y persistir el stdout completo para transcribirlo en la
    Task 2:

    `uv run python scripts/literal_census_33.py --matriz-only 2>&1 | tee /tmp/42-census-run.log`

    y registrar el exit code real de la corrida (no el de `tee`).

    **Invocación obligatoriamente de archivo `.py` (P-10, verificado empíricamente en research):**
    bajo `python -c`, `find_dotenv()` cae a `os.getcwd()`, no encuentra `.env`, y el `Client()`
    apunta al **default remarkets** en vez de bbsa — reportaría credenciales ausentes fabricadas y
    mediría la venue equivocada. No sustituir esta invocación por ninguna variante `-c`.

    Lo esperado (medición de research en esta ventana): `PRIMARY_BASE_URL` resuelve a un host del
    allowlist con token `bbsa`, así que el gate PASA, el header sale con `venue=bbsa`, y corren los
    cinco endpoints de LECTURA: `get_segments`, `get_all_instruments`, `get_instruments_details`,
    `get_active_orders`, `get_all_orders` (los dos últimos porque `PRIMARY_ACCOUNT` está presente).

    **Nada que emita órdenes ni mutaciones.** `VERIFY_MUTATING` no se setea en ninguna variante de
    esta invocación; el barrido deliberado de 4xx para enumerar el conjunto de entrada aceptado
    sigue prohibido por D-10 / P-05.

    Registrar en el log de la sesión, para la Task 2: la línea `CENSUS-HEADER` completa (venue y
    `captured_at` verbatim), la línea `CENSUS-DLOCK`, todas las líneas `matriz-client <endpoint>
    <path>: rows=… types=… distinct=…` de `_report`, cualquier línea
    `matriz-client <endpoint>: NO TARGET FIELD PRESENT IN PAYLOAD`, cualquier línea `SKIPPED —`, y
    la línea final `CENSUS:`.

    Si el gate SALTEA (`matriz-client: SKIPPED — base URL fuera del allowlist…`): NO rodear el gate,
    NO editar el allowlist, NO cambiar `PRIMARY_BASE_URL`. Registrar el SKIP con su causa y escalar
    — ampliar el allowlist exige un checkpoint humano nuevo (P-05).

    Si algún endpoint devuelve un conjunto vacío para `ordType` (cuenta bbsa sin órdenes) o para
    cualquier otro campo, eso **no es un fallo**: el criterio 3 admite explícitamente la vía
    "declara qué campo no se pudo medir y por qué". Registrar el hecho tal cual, sin rellenar.
  </action>
  <verify>
    <automated>test -s /tmp/42-census-run.log &amp;&amp; grep -q "^CENSUS-HEADER venue=bbsa captured_at=$(date -u +%Y-%m-%d)" /tmp/42-census-run.log &amp;&amp; grep -q "^CENSUS-DLOCK" /tmp/42-census-run.log &amp;&amp; ! grep -q "^iol-client " /tmp/42-census-run.log &amp;&amp; test "$(git hash-object verification/mutation_gate.py)" = "6bdaec006cc16f7c8dbfac41701712a9085c691b"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/42-census-run.log` contiene una línea que empieza con `CENSUS-HEADER venue=bbsa
      captured_at=` seguida de la fecha UTC de hoy — el venue sale de `_venue_token`, no de un
      literal en el plan.
    - El log contiene una línea que empieza con `CENSUS-DLOCK`.
    - El log **no** contiene ninguna línea que empiece con `iol-client ` — `census_iol()` no corrió
      (D-04).
    - El log contiene al menos una línea `matriz-client get_segments …` — el censo llegó a emitir
      requests, no quedó atrapado en el gate.
    - `git status --porcelain .planning/verification/captures/` está vacío (el directorio es
      gitignored: `.gitignore:53`) y los cinco archivos `matriz-census-*.json` existen en disco.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
    - Ningún archivo de fuente cambió en esta task: `git status --porcelain` no lista ningún `.py`.
  </acceptance_criteria>
  <done>El censo corrió en vivo contra bbsa, con header medido, sin tocar iol y sin emitir ninguna
  mutación; el stdout completo está persistido para transcribirlo.</done>
</task>

<task type="auto">
  <name>Task 2: 42-CENSUS.md — inventario committeado con venue, timestamp y D-lock reafirmado</name>
  <files>.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md</files>
  <read_first>
    - `/tmp/42-census-run.log` — la salida verbatim de la Task 1; es la única fuente de este
      documento. No inventar valores ni completar los que no salieron.
    - `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md`
      — el analog de FORMA (historia congelada de v1.6: se copia su forma, **jamás** se edita; su
      contenido está aseverado por `verification/test_cycle_closure_phase33.py:250`).
    - `.planning/ROADMAP.md` § Phase 42 criterio 3 — el texto exacto que este artefacto satisface.
    - `.planning/ROADMAP.md` § Backlog, entrada `LIVE-MATZ-33` (~línea 210), § "Ampliación (plan
      33-06)" — los cinco campos y los cuatro alias (`MarketId`, `CFICode`, `Currency`, `OrderType`)
      cuyo vocabulario quedaba sin medir.
    - `scripts/literal_census_33.py:76-91` — `_MATRIZ_KEYS`, el conjunto exacto de campos objetivo, y
      el comentario que explica por qué el walker recolecta por *path* y no por nombre suelto.
    - `.planning/phases/42-…/42-RESEARCH.md` § Pitfall 3 — las dos trampas secundarias:
      sobre-generalización de venue y la corrección adeudada de `29-DLOCK-RESPONSE-LITERAL.md:140-142`,
      que pertenece al firmante y **no** se absorbe en esta fase.
  </read_first>
  <action>
    Escribir `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md`
    con estas secciones, en este orden:

    1. **Encabezado de la medición.** Transcribir verbatim la línea `CENSUS-HEADER` y la línea
       `CENSUS-DLOCK` del log, dentro de un bloque de salida, más una tabla con: venue medido,
       `captured_at` UTC, comando exacto ejecutado, y exit code de la corrida.
    2. **Alcance y no-alcance.** Dejar dicho que `census_iol()` no corrió (D-04, DT-07 ya cerrado en
       `STATE.md:405`) y que la corrida es de sólo lectura: cero órdenes, cero mutaciones, sin
       barrido de 4xx (D-10 / P-05).
    3. **Inventario por campo.** Una tabla con una fila por *path* observado, transcribiendo las
       columnas que `_report` imprime: `endpoint`, `path`, `rows`, `types`, `distinct`. Los paths
       son la unidad correcta: `Segment.marketId` e `InstrumentId.marketId` son dos modelos
       distintos con la misma clave de wire y no se mezclan.
    4. **Disposición de los cinco campos del criterio 3.** Una fila por cada uno de `marketId`,
       `cficode`, `currency`, `orderTypes`, `ordType`, con exactamente una disposición:
       `MEDIDO` (con el conjunto de valores observado y su conteo de filas) o `NO MEDIBLE EN ESTA
       CORRIDA` con la causa concreta (p.ej. cuenta bbsa sin órdenes en la ventana → `ordType` con
       `distinct=[]`; o el endpoint devolvió `NO TARGET FIELD PRESENT IN PAYLOAD`). **Cero filas sin
       disponer.** El criterio 3 admite explícitamente la segunda vía; lo que no admite es el
       silencio.
    5. **Reafirmación del D-lock (b) — sección obligatoria.** Declarar que el D-lock (b) de la
       Phase 29 (los campos RESPONSE **no** se cierran como `Literal`) SIGUE EN VIGOR y que este
       artefacto **no lo revoca**. Nombrar la razón operativa: cerrar un campo RESPONSE haría fatal
       un valor no visto del vendor.
    6. **Advertencia de alcance de venue.** `bbsa` es un sandbox **distinto** de remarkets: el
       vocabulario medido es el vocabulario **de bbsa**, y reportarlo como "el vocabulario RESPONSE
       de matriz" sería sobre-generalizar desde una sola venue. Ésa es la razón por la que el
       criterio 3 exige el venue en el encabezado.
    7. **Corrección adeudada, ruteada y no absorbida.** `29-DLOCK-RESPONSE-LITERAL.md:140-142`
       afirma que el stream de divergencias es el mecanismo del censo; es falso (la rama `Literal`
       de `walk_field` retorna temprano con `literal_enforced=False` y nunca llama al sink —
       `matriz_client/_decode.py:541`). El documento está **firmado**, así que la corrección
       pertenece al firmante y queda **fuera del alcance de esta fase**, nombrada acá para que no
       se pierda.
    8. **Qué NO cierra este censo.** LIVE-02 pide la medición; no pide (ni cierra) los ítems
       estructurales S-3/S-4/S-5 ni el piso `≥24` de `29-SIZING.md`. Decirlo explícitamente para
       que nadie lea "criterio 3 cumplido" como "LIVE-MATZ-33 cerrado".

    **Restricción de contenido (C-4 / D-11 / T-33-32):** este archivo se commitea. Sólo pueden
    entrar los conjuntos de valores distintos de los cinco campos —vocabulario enum-like, no
    identificadores— más conteos y nombres de path. **Nunca** el payload crudo, nunca un
    identificador de cuenta, nunca una credencial, nunca una URL resuelta. El payload crudo vive
    exclusivamente en `.planning/verification/captures/`, que es gitignored.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md &amp;&amp; grep -q "CENSUS-HEADER venue=bbsa captured_at=$(date -u +%Y-%m-%d)" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md &amp;&amp; grep -q "D-lock (b)" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md &amp;&amp; for f in marketId cficode currency orderTypes ordType; do grep -q "$f" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CENSUS.md || exit 1; done &amp;&amp; uv run pytest -q verification/test_cycle_closure_phase33.py</automated>
  </verify>
  <acceptance_criteria>
    - `42-CENSUS.md` existe y contiene la línea `CENSUS-HEADER venue=bbsa captured_at=<fecha UTC de
      hoy>` transcrita verbatim del log de la Task 1.
    - Los cinco nombres `marketId`, `cficode`, `currency`, `orderTypes` y `ordType` aparecen en la
      tabla de disposición, cada uno con exactamente una de `MEDIDO` / `NO MEDIBLE EN ESTA CORRIDA`;
      cero filas sin disponer.
    - Existe una sección que declara el D-lock (b) de la Phase 29 EN VIGOR y no revocado.
    - Existe una advertencia explícita de que el vocabulario medido es el de la venue `bbsa` y no
      generaliza a otras venues.
    - El documento declara que `census_iol()` no corrió y por qué (D-04).
    - El documento nombra la corrección adeudada de `29-DLOCK-RESPONSE-LITERAL.md:140-142` como
      fuera de alcance, ruteada al firmante.
    - `grep -c "PRIMARY_ACCOUNT\|PRIMARY_USER\|PRIMARY_PASSWORD" 42-CENSUS.md` no devuelve ninguna
      línea con un **valor**; el archivo no contiene ninguna URL con esquema `https://` de un
      vendor ni ningún identificador de cuenta.
    - `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md`
      **no fue modificado**: `git status --porcelain .planning/milestones/` está vacío y
      `uv run pytest -q verification/test_cycle_closure_phase33.py` sigue verde.
  </acceptance_criteria>
  <done>Existe un artefacto committeado que reporta los valores observados de los cinco campos con
  venue y timestamp en el encabezado, con disposición completa y con el D-lock (b) reafirmado.</done>
</task>

</tasks>

<threat_model>
> `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` de `matriz-client` → `Client()._state.base_url` → gate de venue | Entrada no confiable que decide contra qué host se emite tráfico |
| script de censo → API de MATBA ROFEX (bbsa) | Cruce de red real hacia un vendor financiero; sólo lectura |
| payload crudo del wire → `captures/` (gitignored) vs. `42-CENSUS.md` (committeado) | Frontera de datos: lo crudo (con PII) nunca cruza a git; sólo cruzan conjuntos de vocabulario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Spoofing | gate de venue de `census_matriz` | mitigate | Ya mitigado por el port del plan 42-01 (igualdad exacta de hostname importada). Esta corrida **consume** ese gate y no lo rodea: si saltea, la Task 1 escala en vez de editar el allowlist o `PRIMARY_BASE_URL` (P-05) |
| T-42-02 | Tampering | order entry contra bbsa | mitigate | Cero mutaciones: `VERIFY_MUTATING` no se setea; `verification/mutation_gate.py` byte-idéntico verificado en el `<verify>` de la Task 1 (`6bdaec00…`) |
| T-42-05 | Information Disclosure | `42-CENSUS.md` committeado | mitigate | Sólo entran conjuntos de valores enum-like, conteos y paths; el payload crudo queda exclusivamente en `.planning/verification/captures/` (`.gitignore:53`). Aserción explícita en `<acceptance_criteria>` de que no hay URLs de vendor ni identificadores de cuenta |
| T-42-04 | Information Disclosure | encabezado y líneas de skip del censo | mitigate | El header emite el **token** de venue (`bbsa`) y un conteo de allowlist, nunca un hostname resuelto; `_skip` nombra la causa sin interpolar la base URL (T-39-04 / C-4) |
| T-42-08 | Repudiation | Censo reportado como licencia para promover a `Literal` | mitigate | Sección obligatoria de reafirmación del D-lock (b) + advertencia de alcance de venue + sección "qué NO cierra este censo" (Pitfall 3) |
| T-42-09 | Tampering | Reescritura de historia congelada (`33-CENSUS.md`) | mitigate | `42-CENSUS.md` es un archivo NUEVO; el analog de v1.6 sólo se lee. Probado por `uv run pytest -q verification/test_cycle_closure_phase33.py` y por `git status --porcelain .planning/milestones/` vacío |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala ningún paquete (`42-RESEARCH.md § Package Legitimacy Audit`: N/A); cero cambios a `pyproject.toml` y `uv.lock`, verificado en el gate de cierre (plan 42-06) |
</threat_model>

<verification>
1. El checkpoint del plan 42-01 está aprobado y transcrito verbatim en `42-01-SUMMARY.md`.
2. `/tmp/42-census-run.log` contiene `CENSUS-HEADER venue=bbsa captured_at=<hoy>` y `CENSUS-DLOCK`, y
   ninguna línea `iol-client `.
3. `42-CENSUS.md` existe con los 5 campos dispuestos, D-lock (b) reafirmado y advertencia de venue.
4. `git hash-object verification/mutation_gate.py` = `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
5. `git status --porcelain .planning/milestones/` vacío y
   `uv run pytest -q verification/test_cycle_closure_phase33.py` verde.
6. `git status --porcelain .planning/verification/captures/` vacío (gitignored).
</verification>

<success_criteria>
- El censo de `matriz-client` corrió en vivo contra `bbsa` detrás del gate portado.
- La salida lleva venue y timestamp medidos en el encabezado (criterio 3).
- Los cinco campos `Literal` de RESPONSE tienen valores observados o una causa explícita de por qué
  no se pudieron medir; cero filas sin disponer.
- El D-lock (b) de v1.6 queda reafirmado y no revocado por la existencia del censo.
- `census_iol()` no corrió y el gate de mutación quedó byte-idéntico.
</success_criteria>

<output>
Create `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-02-SUMMARY.md` when done.

El SUMMARY debe incluir, además del formato estándar:
- La línea `CENSUS-HEADER` verbatim (venue + `captured_at`) y el comando exacto ejecutado.
- El conteo de paths reportados y la disposición resumida de los 5 campos.
- Cualquier campo `NO MEDIBLE EN ESTA CORRIDA` con su causa.
</output>
