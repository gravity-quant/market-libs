---
phase: 41
plan: 03
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - .planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "Las 11 filas de la Phase 36 llevan exactamente una disposición con evidencia re-ejecutada esta sesión"
    - "La fila que shipeó sin comando declarado queda nombrada como hallazgo y dispuesta con comando redactado retroactivamente, no en silencio"
    - "36-VALIDATION.md declara el SHA de v1.7 y pasa a status validated sin que nyquist_compliant cambie"
  artifacts:
    - path: ".planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md"
      provides: "Sección Validation Audit 2026-08-31 con 11 disposiciones y front-matter transformado"
      contains: "## Validation Audit 2026-08-31"
  key_links:
    - from: ".planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md"
      to: ".github/workflows/ci.yml"
      via: "cuarta columna: job + rango de líneas por fila (D-07)"
      pattern: "ci\\.yml:"
    - from: ".planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md"
      to: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      via: "copia literal del SHA de v1.7, del HEAD de baseline y del esqueleto de sección"
      pattern: "37a83fe693a303a551f4374f48fe6fc5521804f7"
---

<objective>
Auditar la Phase 36: 11 filas de mapa, cero filas manual-only, todas re-ejecutables — y una fila que
shipeó sin comando declarado.

Purpose: la Phase 36 es la más limpia de las cinco en apariencia, y por eso la que más fácil se
lava. Sus celdas de estado dicen pendiente y tres dicen que el archivo de test no existe; la medición
del researcher confirma que los 11 archivos citados existen en disco y que las 11 filas corren verde.
Ese bookkeeping stale es lo que D-04 pone explícitamente en alcance. Pero la fila `36-03-03` no tiene
comando: su celda dice que lo decida el planner. Disponerla como si el mapa hubiera declarado un
contrato sería grading contra lo que shipeó en vez de contra el criterio.

Output: `36-VALIDATION.md` con `## Validation Audit 2026-08-31`, 11 disposiciones y front-matter
transformado. Cero fuente de producto tocada.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Re-ejecutar las 11 filas de la Phase 36 y resolver la fila sin comando declarado</name>
  <files>.planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md (el archivo objetivo, completo)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 2, 3, 6, 7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Re-Execution Results → Phase 36, § CI Enforcement Map, § Common Pitfalls 2
    - verification/test_main_market_data_deep_chain.py (el cuerpo del lock candidato para la fila sin comando)
    - .github/workflows/ci.yml (allowlist explícito de `verification/`)
  </read_first>
  <action>
    Re-verificar el invariante de criterio 1 con `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`
    antes de ejecutar nada. Si sale distinto de 0, ABORTAR.

    Asignar claves ordinales a las 11 filas del mapa en su orden de aparición: `36-r01` corresponde a
    `36-01-01` y así hasta `36-r11`, que corresponde a `36-03-03`. Las claves son necesarias para el
    gate aritmético del cierre.

    Re-ejecutar las 10 filas que sí declaran comando, transcribiendo el comando literal del mapa. Las
    salidas de referencia medidas por el researcher esta sesión son: la suite de cadena de market data
    con 38 tests pasados; mypy sobre src y tests del paquete, limpio; el selector de entradas con 2
    pasados; la suite de Null Object con 61 pasados; el selector de conjunto de campos con 1 pasado; el
    selector de latest request con 3 pasados; la suite de fila sin datos con 8 pasados; su selector de
    tipo equivocado con 1 pasado; el selector de maquinaria de mapping con 1 pasado; y el gate de
    intactness del decodificador saliendo 0. Esos valores son referencia de contraste, no algo a copiar:
    la evidencia que se escribe es la salida de **esta** corrida. Si alguna difiere materialmente,
    escalarlo en el SUMMARY.

    Aplicar el guardia anti-vacuidad de la sección 6 del contrato: cada línea de resumen capturada debe
    reportar un conteo de tests pasados distinto de cero. RESEARCH.md no midió ninguna fila de selección
    vacía en la Phase 36; si aparece una, es un hallazgo real y se escala, no se re-apunta.

    Resolver la fila `36-r11` por la regla R-04. Su celda `Automated Command` no contiene un comando:
    contiene una instrucción de diseño pendiente sobre elegir entre un lock de AST reutilizando el
    precedente de la Phase 30 o una aserción en el plan. La conducta que esa fila debía cubrir —el
    driver consumiendo por encadenamiento profundo en sitios reales— **sí** está cubierta por
    `verification/test_main_market_data_deep_chain.py`, que existe en disco y además está dentro del
    allowlist explícito de `ci.yml`. Antes de disponer, leer el cuerpo de ese archivo y confirmar que
    asserta esa conducta y no otra (RESEARCH.md Assumptions Log A1: no basta el nombre). Ejecutarlo,
    capturar su línea de resumen, y disponer `VERIFIED-NOW (comando redactado retroactivamente)`. La
    celda de evidencia debe decir de forma inequívoca que el mapa original **no** declaraba comando y
    que el que aparece fue redactado por esta auditoría — no presentarlo como si hubiera estado ahí.

    Recortar toda salida a su línea de resumen antes de escribirla al archivo (CLAUDE.md: nunca exponer
    credenciales en reportes committeados).
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_market_data_chain.py -x -q && uv run pytest verification/test_main_market_data_deep_chain.py -q && uv run python tools/check_decode_intactness.py && grep -q 'verification/test_main_market_data_deep_chain.py' .github/workflows/ci.yml && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - Las 11 filas del mapa tienen clave ordinal asignada `36-r01`..`36-r11` y cada una una línea de resumen capturada de esta sesión.
    - Cada línea de resumen capturada para las filas de pytest reporta un conteo de tests pasados distinto de cero; ninguna reporta sólo descartes sin ejecución.
    - `uv run pytest verification/test_main_market_data_deep_chain.py -q` sale 0 con conteo de pasados distinto de cero.
    - El lock elegido para `36-r11` está en el allowlist de CI: `grep -c 'verification/test_main_market_data_deep_chain.py' .github/workflows/ci.yml` es `1`.
    - `uv run python tools/check_decode_intactness.py` sale 0.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al terminar.
    - `git status --porcelain verification/` está vacío.
  </acceptance_criteria>
  <done>Las 11 filas tienen evidencia re-ejecutada de esta sesión, y la fila sin comando declarado tiene un sustituto leído, ejecutado y confirmado como enrolado en CI.</done>
</task>

<task type="execute">
  <name>Task 2: Escribir la sección de auditoría de la Phase 36 y transformar su front-matter</name>
  <files>.planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 4, 5, 7, 8
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección + estilo de celda de evidencia)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (par de encabezados en negrita, escalaciones, línea tildada de sign-off)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (prosa modelo para un resultado PARTIAL)
  </read_first>
  <action>
    Escribir `## Validation Audit 2026-08-31` al final de `36-VALIDATION.md`, precedida de una regla
    horizontal, después de `## Validation Sign-Off`, con los ocho bloques de la sección 5 del contrato.

    Valores concretos para esta fase: denominador **11**; disposición esperada 11 `VERIFIED-NOW` (de los
    cuales 1 con comando redactado retroactivamente), 0 `VERIFIED-HISTORICALLY`,
    0 `NOT-VERIFIABLE-RETROACTIVELY`; archivos de test nuevos escritos **0**; filas cuya celda de
    enforcement dice que no corre en CI: **0** — la Phase 36 es la única de las cinco cuyas 11 filas
    tienen cobertura de CI real, y eso debe quedar dicho en la prosa porque es un resultado atípico,
    no el default. Si la medición da otro reparto, escribir el medido y anotar la divergencia en el
    SUMMARY; nunca ajustar la tabla al número esperado.

    Llenar la cuarta columna con la tabla de lookup de la sección 7 del contrato: las nueve filas de
    pytest sobre `packages/market-data-client/tests/` mapean al job `test`; la fila de mypy sobre src y
    tests mapea al job `typecheck` (los dos tramos, el global y el de tests por paquete); la fila del
    gate de intactness del decodificador mapea a su línea exacta del job `lint`; y la fila resuelta por
    R-04 mapea al allowlist explícito de `verification/` dentro del job `lint`.

    Escribir la sección de hallazgos de bookkeeping con los dos que esta auditoría produce: primero, que
    tres celdas de `File Exists` del mapa afirman que el archivo de test no existe cuando los once
    archivos citados existen en disco y corren verde — bookkeeping de plan-time, no gaps, exactamente
    lo que D-04 anticipa; segundo, y más importante, que la fila `36-r11` shipeó **sin contrato de
    verificación**: su celda de comando contenía una decisión de diseño pendiente en vez de un comando,
    y la auditoría tuvo que redactar uno. Nombrar eso como el hallazgo principal de la fase.

    Actualizar la columna `Status` de las 11 filas del mapa según la regla de la sección 5 del contrato
    y extender la línea de leyenda. No tocar las celdas de `File Exists`: son registro de plan-time y su
    contradicción con la realidad medida es el hallazgo — corregirlas lo borraría.

    Transformar el front-matter según la sección 4 del contrato: `status` pasa de `draft` a `validated`;
    `nyquist_compliant` se queda en `false` porque R-09 no se satisface — la fase tiene una fila con
    calificador de corrección, condición (c) de R-09. Escribir ese rationale en una línea de prosa junto
    al veredicto, siguiendo el modelo de prosa de `07-VALIDATION.md`, sin adoptar su valor de flag.
    `not_verifiable_retroactively` es `0`; se agregan `audited_commit_sha` con el commit de v1.7,
    `audit_baseline_head` copiado literal del contrato, `frozen_tree_verified: true`,
    `updated: 2026-08-31` y `last_audited: 2026-08-31`. Quedan intactos `phase`, `slug`, `created`,
    `wave_0_complete` y los dos comentarios de lifecycle.

    Cerrar con la línea de veredicto en la forma fijada y una línea nueva tildada en el
    `## Validation Sign-Off`, dejando sin tildar las casillas preexistentes.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md; X() { awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 36-[rm][0-9]/' "$F"; }; test "$(X | wc -l | tr -d ' ')" -eq 11 && test "$(( $(X | grep -c 'VERIFIED-NOW') + $(X | grep -c 'VERIFIED-HISTORICALLY') + $(X | grep -c 'NOT-VERIFIABLE-RETROACTIVELY') ))" -eq 11 && grep -q '^status: validated' "$F" && grep -q '^nyquist_compliant: false' "$F" && grep -q '^not_verifiable_retroactively: 0' "$F" && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$F" && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de disposición tiene exactamente 11 filas dentro del rango acotado entre `### Disposición por fila` y la línea de leyenda.
    - La suma de los tres conteos de token dentro de ese rango es exactamente `11` (una disposición por fila, ni cero ni dos).
    - El reparto medido es 11 / 0 / 0: `VERIFIED-NOW` = `11`, `VERIFIED-HISTORICALLY` = `0`, `NOT-VERIFIABLE-RETROACTIVELY` = `0`.
    - Exactamente una fila lleva el calificador de comando redactado retroactivamente, y es `36-r11`.
    - Cero evidencia vacua: dentro del rango acotado, las líneas que mencionan descartes y no mencionan tests pasados son `0`.
    - Cero celdas de la cuarta columna dicen que la fila no corre en CI: `grep -c 'NOT ENFORCED'` dentro del rango acotado es `0`.
    - Front-matter: `grep -c '^status: validated'` es `1`, `grep -c '^nyquist_compliant: false'` es `1`, `grep -c '^not_verifiable_retroactively: 0'` es `1`, `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7'` es `1`, `grep -cE '^audit_baseline_head: [0-9a-f]{40}'` es `1`.
    - `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 36-VALIDATION.md` es `1`.
    - `grep -c '^- \[x\] .*nyquist_compliant: true' 36-VALIDATION.md` es `0`.
    - `git status --porcelain` sólo lista `36-VALIDATION.md` bajo `.planning/milestones/v1.7-phases/`; `git status --porcelain verification/` vacío.
  </acceptance_criteria>
  <done>Las 11 filas de la Phase 36 están dispuestas con evidencia y enforcement; el hallazgo de la fila sin contrato está nombrado; el front-matter pasa a validated sin que el flag cambie.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producido por este plan:
- `.planning/milestones/v1.7-phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md` — **modificado**. Sección `## Validation Audit 2026-08-31` (métricas, tabla de disposición de 11 filas y 4 columnas, correcciones de comando, hallazgos de bookkeeping, escalaciones, veredicto); columna `Status` actualizada; front-matter con `status: validated`, `nyquist_compliant: false`, `not_verifiable_retroactively: 0`, `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified: true`, `updated`, `last_audited`.

Cero archivos nuevos, cero locks nuevos en `verification/`, cero ediciones a `.github/workflows/ci.yml`. Ninguna declaración inerte ruteada a Phase 45 se origina en este plan: las 11 filas de esta fase están enroladas en CI.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de pytest/mypy local → markdown committeado | Único cruce: las líneas de resumen transcritas quedan en el repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-03-01 | Information Disclosure | transcripción de salida de suite a `36-VALIDATION.md` | mitigate | Sección 6 del contrato: sólo línea de resumen; ninguna de las 11 filas lee `.env` ni abre red (ASVS V7) |
| T-41-03-02 | Repudiation | presentar el comando redactado retroactivamente para `36-r11` como si el mapa lo hubiera declarado | mitigate | R-04 exige el calificador explícito en la celda de disposición y el hallazgo nombrado en la prosa; criterio de aceptación que verifica el calificador en esa fila |
| T-41-03-03 | Repudiation | certificar como cubierto un lock que CI no corre | mitigate | Cuarta columna D-07 con job y rango de líneas verificados contra `ci.yml`; criterio de aceptación que confirma la presencia del archivo en el allowlist |
| T-41-03-SC | Tampering | instalaciones de paquetes | accept | Cero: ningún comando invoca `uv add`, `npm`, `pip` ni `cargo` |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V5/V6 — el plan edita un markdown y re-corre
suites locales existentes.
</threat_model>

<verification>
1. Tabla de 11 filas, suma de tokens exactamente 11, reparto 11 / 0 / 0.
2. Cero celdas de enforcement que declaren ausencia de CI en esta fase.
3. Front-matter con los cinco campos nuevos y `nyquist_compliant` sin cambiar.
4. `git status --porcelain verification/` vacío; `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
</verification>

<success_criteria>
- Ninguna de las 11 filas queda sin disponer y ninguna lleva dos disposiciones.
- La fila que shipeó sin comando declarado está resuelta por regla, con el sustituto leído y confirmado enrolado en CI, y nombrada como hallazgo.
- Toda celda `VERIFIED-NOW` cita comando y línea de resumen de esta sesión con conteo de pasados distinto de cero.
- `nyquist_compliant` sigue en `false` con rationale escrito que cita la condición de R-09 que falla.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-03-SUMMARY.md` when done.
</output>
