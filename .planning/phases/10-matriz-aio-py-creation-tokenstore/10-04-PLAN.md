---
phase: 10-matriz-aio-py-creation-tokenstore
plan: 04
type: execute
wave: 4
depends_on:
  - 10-03
files_modified:
  - main_matriz.py
  - packages/matriz-client/tests/test_fixture_reaches_production.py
  - verification/test_async_cancellation.py
  - verification/test_sync_async_isolation.py
  - verification/snapshots/matriz-client-surface.txt
  - .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md
autonomous: false
requirements:
  - REFAC-04
  - LIVE-02
must_haves:
  truths:
    - "`main_matriz.py` runs sync probes + async probes paired in the same `main()`, interleaved per `main_iol.py` pattern (no `--async` flag — D-06)"
    - "Live operator-driven run of `main_matriz.py` reports paridad sync↔async: same set of PASS/FINDING/SKIPPED across the surfaces (LIVE-02 satisfied)"
    - "The 3 forward-reference skip lines are flipped to active tests: `test_fixture_reaches_production.py:64`, `verification/test_async_cancellation.py:82`, `verification/test_sync_async_isolation.py:176`"
    - "`verification/test_sync_async_isolation.py` cross-leak sentinel extended to cover matriz async — sync Client and AsyncClient have ISOLATED `_state.token` and `_state.token_store` (no cross-leak)"
    - "Public surface snapshot for matriz REGENERATED — diff is EXACTLY the growth of AsyncClient + 22 async delegators (+~23-25 lines); iol/higyrus/ambito/wallets snapshots ZERO diff"
    - "Full CI green gate: pytest matrix (Python 3.12 + 3.13) + ruff + mypy strict + lint-imports + lint-logging + import-linter contracts + 6 Phase 8 cross-cutting guard tests still green + matriz Pitfall 4 mutation gate still green"
    - "Total test count: baseline ~785 + ~55 Phase 10 additions = ~840 tests verde (per CONTEXT D-07 line 1011-1013)"
    - "Operator approval signal captured in `10-VALIDATION.md` frontmatter (`status=approved`, `nyquist_compliant=true`, `live_paridad_sync_async=true`)"
  artifacts:
    - path: "main_matriz.py"
      provides: "Sync + async probes paired per probe; interleaved in main() per main_iol.py pattern; outcome reporter compares sync vs async PASS/FINDING/SKIPPED sets"
      contains: "async def probe_"
    - path: "packages/matriz-client/tests/test_fixture_reaches_production.py"
      provides: "matriz async fixture-reaches-production guard test (Phase 6 REFAC-01 idiom extended to async surface)"
      contains: "async"
    - path: "verification/test_async_cancellation.py"
      provides: "matriz async cancellation guard test (CancelledError-aware Retry-After per D-32)"
      contains: "matriz"
    - path: "verification/test_sync_async_isolation.py"
      provides: "matriz cross-leak sentinel — sync Client and AsyncClient have ISOLATED _state.token + _state.token_store"
      contains: "matriz"
    - path: "verification/snapshots/matriz-client-surface.txt"
      provides: "Regenerated public surface snapshot reflecting AsyncClient + 22 module-level async delegators"
      contains: "AsyncClient"
    - path: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md"
      provides: "Phase 10 closure document: Nyquist evidence + CI matrix output + snapshot delta inline + live paridad run report + operator checkpoint"
      contains: "nyquist_compliant"
  key_links:
    - from: "main_matriz.py"
      to: "main_iol.py"
      via: "REFERENCE pattern — interleaved sync+async probes in main() (D-06)"
      pattern: "probe_.*_sync\\|probe_.*_async"
    - from: "verification/test_sync_async_isolation.py"
      to: "packages/matriz-client/src/matriz_client/_state.py"
      via: "cross-leak sentinel extended for matriz async — separate _state instances → separate token_store instances"
      pattern: "matriz_client.*_state|token_store"
    - from: "verification/snapshots/matriz-client-surface.txt"
      to: "packages/matriz-client/src/matriz_client/aio.py"
      via: "Snapshot regen reflects Plan 10-02's AsyncClient + 22 async delegators"
      pattern: "AsyncClient|async def"
    - from: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md"
      to: ".planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md"
      via: "REFERENCE format — Phase 8 VALIDATION pattern (Nyquist + CI matrix + operator checkpoint)"
      pattern: "nyquist_compliant|status: approved"
---

<objective>
Close Phase 10 via live operator-driven verification of matriz async surface paridad with sync, a full CI green-gate sweep (pytest matrix Python 3.12+3.13, ruff, mypy strict, lint-imports, lint-logging, import-linter contracts), regeneration of the matriz public surface snapshot, and flipping of the 3 forward-reference skip tests left in the codebase since Phase 6/7. This Plan delivers LIVE-02 (matriz async REST verified live as part of `main_matriz.py`) and the final REFAC-04 success criterion #3/#5 (live paridad + CI green for Python 3.12+3.13).

Purpose: Plans 10-01/02/03 land the primitive + REST surface + integration in mockeado tests. Plan 10-04 is the operator-executed live gate where the matriz remarkets API is exercised against the new async surface to confirm paridad with the sync probes (LIVE-02 outcome = "the async run reproduces the same PASS/FINDING/SKIPPED set as the sync"). Implements CONTEXT D-06 (interleaved sync+async probes per `main_iol.py` pattern, NO `--async` flag), D-07 Plan 10-04 wave (live + green gate), D-08 (1 atomic commit), D-09 (live scope = matriz async paridad only, NOT full × 4 — Phase 11 LIVE-01).

Output: 5 file changes — `main_matriz.py` extension (sync+async paired probes), 3 skip-flip mechanical edits (`test_fixture_reaches_production.py:64`, `verification/test_async_cancellation.py:82`, `verification/test_sync_async_isolation.py:176`), 1 snapshot regen (`verification/snapshots/matriz-client-surface.txt` — diff: +AsyncClient + 22 async delegators), 1 NEW closure document (`10-VALIDATION.md` with Nyquist evidence + CI matrix output + live paridad report + operator checkpoint). Test count delta: +0 NEW tests (the 3 skip flips produce 3 NEW active tests that were skipped before — net +3 to the running count). Operator-executed live `main_matriz.py` run is the LIVE-02 acceptance signal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-CONTEXT.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-PATTERNS.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-01-SUMMARY.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-02-SUMMARY.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-03-SUMMARY.md
@.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md
@.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
@main_matriz.py
@main_iol.py
@packages/matriz-client/tests/test_fixture_reaches_production.py
@verification/test_async_cancellation.py
@verification/test_sync_async_isolation.py
@verification/snapshots/matriz-client-surface.txt
@packages/matriz-client/src/matriz_client/aio.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend `main_matriz.py` with paired async probes (D-06 interleaved pattern); flip the 3 forward-reference skips; extend cross-leak sentinel for matriz async</name>
  <files>main_matriz.py, packages/matriz-client/tests/test_fixture_reaches_production.py, verification/test_async_cancellation.py, verification/test_sync_async_isolation.py</files>
  <read_first>
    - `main_iol.py` (1666 LOC) — canonical pattern for interleaved sync+async probes in same `main()` (D-06 reference)
    - `main_matriz.py` (current — INT-01 fixed in 260613-nwb quick task) — existing sync probes that get paired
    - `packages/matriz-client/tests/test_fixture_reaches_production.py:64` — the EXACT skip line to flip; read 20 lines context above and below
    - `verification/test_async_cancellation.py:82` — the EXACT skip line to flip
    - `verification/test_sync_async_isolation.py:176` — the EXACT skip line to flip + read the surrounding cross-leak sentinel structure (Phase 7 D-10) to understand how to EXTEND for matriz async
    - 10-PATTERNS.md lines 907-944 (`main_matriz.py` EXTEND pattern), lines 1213-1254 (3 skip-flip patterns)
    - 10-CONTEXT.md D-06 (live verification entry point = main_iol.py-style interleaved sync+async), D-09 (live scope = matriz async paridad)
    - `packages/iol-client/tests/test_async_client.py` — for the iol async test idiom referenced by the cross-leak sentinel extension
    - `packages/matriz-client/src/matriz_client/aio.py` (Plan 10-02) — to confirm the 22 endpoint signatures for probe pairing
    - `packages/matriz-client/src/matriz_client/_state.py` (Plan 10-03) — for `token_store` field reference in cross-leak sentinel
  </read_first>
  <behavior>
    - `main_matriz.py` runs both sync and async probes per endpoint; outcome reporter at end of `main()` compares PASS/FINDING/SKIPPED set between sync and async; reports paridad as a final line.
    - `test_fixture_reaches_production.py:64` skip removed; the test body actively exercises the matriz async fixture-reaches-production guard (sync sentinel via monkeypatch propagates to wire request via async surface).
    - `verification/test_async_cancellation.py:82` skip removed; the test body actively exercises matriz async cancellation (`asyncio.CancelledError` propagates through `_atransport.AsyncRetryTransport.handle_async_request`).
    - `verification/test_sync_async_isolation.py:176` skip removed; the cross-leak sentinel extended — sync Client sets `_state.token = "SYNC-sentinel"`; async AsyncClient sets `_state.token = "ASYNC-sentinel"`; assert neither sees the other's sentinel value (verifies per-instance `_state` and per-instance `_state.token_store`).
  </behavior>
  <action>
    **Step 1 — Extend `main_matriz.py` with paired async probes (D-06):**

    Inspect current `main_matriz.py` to enumerate the existing sync probes. For each probe `probe_X_sync()` that exercises the matriz REST surface, add a paired `probe_X_async()` mirroring signature and outcome reporting per 10-PATTERNS.md lines 918-939.

    **Paridad coverage target (≥ 18 paired async probes):** The matriz REST surface has 22 endpoints. The async paridad scope per D-06 / D-09 covers the **REST-only** subset of those endpoints — excluding the ~4 endpoints that are WebSocket-mediated or use the Risk API `auth_basic` path (which is out-of-scope for async paridad in Phase 10; Risk API async is Phase 11 CR-08 territory). The minimum acceptable async-probe count is therefore **18** (22 REST − 4 out-of-scope = 18). Concrete out-of-scope mapping (executor confirms during inspection — exact endpoint names may vary slightly):
    - WebSocket-only / order-entry-via-WS: ~3 endpoints (orders pushed via `ws_client.py` rather than REST)
    - Risk API `auth_basic` (account-segregated): ~1 endpoint (Phase 11 CR-08 scope per CONTEXT D-09)

    If the executor's reading of `client.py` reveals a different REST/WS split (e.g., 17 REST-only + 5 WS/auth_basic = 17 min, or 19 REST-only + 3 = 19 min), the executor documents the actual split in 10-04-SUMMARY.md and the acceptance grep below remains `≥ 18` as the conservative floor; raising the floor in SUMMARY is acceptable, lowering it requires returning to the planner.

    Pattern (mirror `main_iol.py` — read it carefully):
    ```python
    async def probe_get_segments_async() -> ProbeResult:
        try:
            segments = await matriz_client.aio.get_segments()
            return PASS(f"aio.get_segments returned {len(segments)} segments")
        except Exception as e:
            return FINDING(f"aio.get_segments failed: {e}")
    ```

    In `main()`, run sync probe immediately followed by async probe (using `asyncio.run` per probe or batch all async probes via `asyncio.gather`; follow `main_iol.py`'s actual pattern — DO NOT improvise the harness):
    ```python
    report.append(probe_get_segments_sync())
    report.append(asyncio.run(probe_get_segments_async()))
    ```

    OR (if iol uses gather): collect all async probes in a list, run them all once via `asyncio.run(asyncio.gather(...))`. Choose whichever idiom `main_iol.py` uses CURRENTLY.

    Probes that depend on `ws_client` (WebSocket-only operations) SKIP async with documented reason — `SKIPPED("aio.X not applicable — WebSocket-only surface")`. These SKIPPED probes count toward the ≥ 18 floor only if they are written as `async def probe_X_async()` shells that return `SKIPPED(...)`; if you choose to omit them entirely, the floor still applies to the REST-only subset (a pure REST endpoint MUST have a real async probe — no exceptions).

    At end of `main()`, after the report is assembled, add a paridad section:
    ```python
    sync_outcomes = {p.name: p.outcome for p in report if p.kind == "sync"}
    async_outcomes = {p.name: p.outcome for p in report if p.kind == "async"}
    paridad = sync_outcomes == async_outcomes
    print(f"\n=== Phase 10 LIVE-02 Paridad sync↔async: {'PASS' if paridad else 'FAIL'} ===")
    if not paridad:
        diff = set(sync_outcomes.items()) ^ set(async_outcomes.items())
        print(f"Divergences: {diff}")
    ```

    (The exact `ProbeResult` / `report.append` API depends on the existing main_matriz.py harness. Inspect first; extend in keeping with the existing structure. If the harness uses dict-based reporting, adapt the paridad check accordingly.)

    **CRITICAL:** preserve the INT-01 fix (260613-nwb quick task — `_get_default()._state.base_url` pattern, NOT `_base_url`). The async probes MUST use the same `aio._get_default()._state.base_url` (or equivalent) for any state introspection; never use the denied legacy `aio._base_url` attribute reads (PEP 562 shim works for reads but the explicit `_state` path is preferred per the INT-01 idiom).

    **Step 2 — Flip skip in `packages/matriz-client/tests/test_fixture_reaches_production.py:64`:**

    Read `test_fixture_reaches_production.py` lines 40-80 to see the EXACT skip line. Pattern expected (per 10-PATTERNS.md lines 1219-1228):
    ```python
    pytest.skip(
        "matriz async REST surface is Phase 10 REFAC-04; "
        ...
    )
    ```

    Remove the `pytest.skip(...)` call. The test body should now exercise the matriz async fixture-reaches-production guard — mirror the sync guard pattern at the top of the same file (which monkeypatches `_state.token` to a sentinel value via `configure(token=...)`, calls an async endpoint, and asserts the sentinel appears in the outgoing `X-Auth-Token` header).

    Use `pytest-httpx` `httpx_mock` to intercept the outgoing request; assert `request.headers["X-Auth-Token"] == "SENTINEL-VALUE-MATRIZ-ASYNC"`.

    **Step 3 — Flip skip in `verification/test_async_cancellation.py:82`:**

    Read `verification/test_async_cancellation.py` lines 60-110 to see the EXACT skip line. Pattern expected:
    ```python
    pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")
    ```

    Remove the skip; the test body should exercise matriz async with `asyncio.CancelledError` (mirror the iol async cancellation test earlier in the file). Use the pattern:
    - Mock a slow response via `httpx_mock` (or `unittest.mock.patch`).
    - Create a task awaiting `matriz_client.aio.get_segments()`.
    - `task.cancel()`.
    - `with pytest.raises(asyncio.CancelledError): await task`.

    **Step 4 — Flip skip in `verification/test_sync_async_isolation.py:176` + EXTEND cross-leak sentinel for matriz async:**

    Read `verification/test_sync_async_isolation.py` lines 150-200 to see the skip + surrounding cross-leak sentinel structure (Phase 7 D-10). Pattern expected:
    ```python
    pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")
    ```

    Remove the skip. EXTEND the cross-leak sentinel to cover matriz async:
    - `matriz_client.configure(token="SYNC-MATRIZ-sentinel")`
    - `matriz_client.aio.configure(token="ASYNC-MATRIZ-sentinel")`
    - Verify via `matriz_client.client._get_default()._state.token == "SYNC-MATRIZ-sentinel"`
    - Verify via `matriz_client.aio._get_default()._state.token == "ASYNC-MATRIZ-sentinel"`
    - Assert NEITHER sentinel appears in the OTHER state (cross-instance isolation: separate `_ClientState` per surface).
    - EXTENDED: also assert `_state.token_store` instances are DIFFERENT — `matriz_client.client._get_default()._state.token_store is not matriz_client.aio._get_default()._state.token_store` (this is the matriz-specific extension that Plan 10-03 wiring makes possible to assert).

    All test files: preserve `from __future__ import annotations` if present.

    **mypy strict + ruff clean for all 4 modified files.**
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py -q && uv run ruff check main_matriz.py packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py && uv run mypy --strict main_matriz.py packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py</automated>
  </verify>
  <acceptance_criteria>
    - **W3 — probe count floor:** `grep -c "async def probe_" main_matriz.py` returns ≥ 18. Rationale: the matriz REST surface has 22 endpoints; LIVE-02 requires paridad sync↔async coverage of the REST-only subset. The floor of 18 = 22 REST endpoints − ~4 out-of-scope (WebSocket-mediated order entry + Risk API `auth_basic`, which is Phase 11 CR-08 territory per D-09). If the executor's audit of `client.py` shows a different REST/WS split, document the actual count in 10-04-SUMMARY.md; the floor ≥ 18 stays as the conservative gate.
    - `grep -c "matriz_client.aio\." main_matriz.py` returns ≥ 5 (async API surface invoked in probes)
    - `grep "Paridad sync↔async\|paridad" main_matriz.py | wc -l` returns ≥ 1 (paridad reporter present)
    - `grep "pytest.skip.*Phase 10\|pytest.skip.*REFAC-04" packages/matriz-client/tests/test_fixture_reaches_production.py | wc -l` returns 0 (skip removed)
    - `grep "pytest.skip.*Phase 10\|pytest.skip.*REFAC-04" verification/test_async_cancellation.py | wc -l` returns 0 (skip removed)
    - `grep "pytest.skip.*Phase 10\|pytest.skip.*REFAC-04" verification/test_sync_async_isolation.py | wc -l` returns 0 (skip removed)
    - `grep -c "matriz" verification/test_sync_async_isolation.py` returns ≥ 3 (matriz cross-leak sentinel extension landed)
    - `grep -c "ASYNC-MATRIZ-sentinel\|SYNC-MATRIZ-sentinel" verification/test_sync_async_isolation.py` returns ≥ 2 (cross-leak sentinels present)
    - `grep -c "_state.token_store is not\|token_store is not" verification/test_sync_async_isolation.py` returns ≥ 1 (matriz-specific token_store instance isolation assertion)
    - `uv run pytest packages/matriz-client/tests/test_fixture_reaches_production.py -q` exits 0 and reports ≥ 1 NEW active test (the matriz async guard)
    - `uv run pytest verification/test_async_cancellation.py -q` exits 0 and the matriz cancellation test runs (no skip)
    - `uv run pytest verification/test_sync_async_isolation.py -q` exits 0; the matriz cross-leak sentinel runs and PASSES
    - `uv run pytest -q --collect-only 2>&1 | grep -c "SKIPPED.*Phase 10\|SKIPPED.*REFAC-04"` returns 0 (no Phase 10 / REFAC-04 forward-reference skips remain)
    - `python main_matriz.py --help 2>&1 | grep -i "live\|paridad" || python -c "import ast; tree = ast.parse(open('main_matriz.py').read()); print('ok' if any(isinstance(n, ast.AsyncFunctionDef) for n in ast.walk(tree)) else 'no async')" ` outputs "ok"
    - `uv run ruff check main_matriz.py packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py` exits 0
    - `uv run mypy --strict main_matriz.py packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py` exits 0
  </acceptance_criteria>
  <done>
    `main_matriz.py` has paired sync+async probes interleaved per D-06 with at least 18 async probes (REST-only subset of the 22 matriz endpoints, excluding ~4 WebSocket-mediated / Risk API auth_basic endpoints out-of-scope for Phase 10); paridad reporter present. The 3 forward-reference skips at the documented line numbers are FLIPPED to active tests. Cross-leak sentinel extended for matriz async — both `_state` and `_state.token_store` instance isolation asserted. ruff + mypy strict clean. The 3 NEW active tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OPERATOR live verification of `main_matriz.py` paridad sync↔async (LIVE-02 acceptance) + snapshot regen + green gate + atomic commit</name>
  <files>verification/snapshots/matriz-client-surface.txt, .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md</files>
  <action>
    Pre-operator setup (Claude executes BEFORE operator handoff): see `<how-to-verify>` block below for the exact commands (snapshot regen, full green-gate sweep, 10-VALIDATION.md pre-fill). After pre-setup is green, present the operator handoff prompt verbatim. Block on operator approval signal.
  </action>
  <verify>
    <automated>test -f verification/snapshots/matriz-client-surface.txt && test -f .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md && grep -c "status: pending_operator_approval\|status: approved" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md</automated>
    <human-check>Operator runs `main_matriz.py` live and confirms paridad sync↔async in `10-VALIDATION.md` per the handoff prompt in `<how-to-verify>`.</human-check>
  </verify>
  <done>
    Operator has updated `10-VALIDATION.md` frontmatter with `status: approved`, `live_paridad_sync_async: true`, `operator_signoff_date`, `operator_signoff_run_log`. CI green gate sweep output saved inline in `10-VALIDATION.md`. matriz snapshot regenerated; iol/higyrus/ambito/wallets snapshots ZERO diff. Resume signal "approved" received.
  </done>
  <what-built>
    - Plan 10-01: TokenStore + RefreshPolicy primitive (4 src files + 3 test files + CONCERNS.md entry)
    - Plan 10-02: AsyncClient full REST surface + AsyncRetryTransport (2 src + 1 init + 1 conftest + 4 test files)
    - Plan 10-03: State wiring + sync/async/ws_client migration + cross-thread regression (4 src modifications + 2 test files)
    - Plan 10-04 Task 1: main_matriz.py paired probes + 3 skip flips + cross-leak sentinel extension

    What this checkpoint covers:
    1. **Operator-driven live run** of `main_matriz.py` against matriz remarkets (LIVE-02 acceptance signal — operator can NOT be replaced by automation; live API keys live in `.env` and live API state is operator-controlled).
    2. **Snapshot regeneration** for matriz (D-09 — public API CRECE with AsyncClient + 22 module-level async delegators).
    3. **Full CI green gate sweep** — pytest matrix (Python 3.12 + 3.13) + ruff + ruff format + mypy strict + lint-imports + lint-logging + import-linter contracts + matriz sweep snapshot (CR-05) + parse_envelope_consumes_body (CR-03) + Phase 6 public surface snapshot (matriz REGENERATED; iol/higyrus/ambito/wallets ZERO diff) + matriz Pitfall 4 mutation gate still PASS.
    4. **`10-VALIDATION.md` creation** with Nyquist evidence + CI matrix output + live paridad run report + operator checkpoint frontmatter.
    5. **Atomic commit per D-08** with all 6 file changes (main_matriz.py + 3 skip flips + matriz snapshot regen + 10-VALIDATION.md).
  </what-built>
  <how-to-verify>
    **Pre-operator setup (Claude executes BEFORE operator handoff):**

    1. **Regenerate matriz snapshot:**
       ```bash
       uv run python verification/snapshot.py matriz_client > verification/snapshots/matriz-client-surface.txt
       # OR if there's a Make target:
       # make snapshot pkg=matriz_client
       ```
       Confirm via `git diff verification/snapshots/matriz-client-surface.txt`:
       - matriz snapshot shows +AsyncClient class line + +22 `async def` module-level delegators (~23-25 new lines)
       - iol/higyrus/ambito/wallets snapshots ZERO diff (use `git status verification/snapshots/` to confirm)
       - If iol/higyrus/ambito/wallets snapshots have ANY diff, STOP — Plan 10-02/03 introduced a cross-package regression; do not proceed to operator.

    2. **Run full green-gate sweep locally** (capture output for VALIDATION.md):
       ```bash
       # Test matrix Python 3.12 + 3.13
       UV_PYTHON=3.12 uv run pytest -q 2>&1 | tee /tmp/phase10-pytest-312.log
       UV_PYTHON=3.13 uv run pytest -q 2>&1 | tee /tmp/phase10-pytest-313.log
       # Lint + format + types
       uv run ruff check . 2>&1 | tee /tmp/phase10-ruff.log
       uv run ruff format --check . 2>&1 | tee /tmp/phase10-ruff-format.log
       uv run mypy --strict packages/ 2>&1 | tee /tmp/phase10-mypy.log
       # Import contracts
       uv run lint-imports 2>&1 | tee /tmp/phase10-lint-imports.log
       # Logging grep
       grep -rn "logging.basicConfig\|logging.root" packages/*/src/ 2>&1 | tee /tmp/phase10-lint-logging.log
       # 6 Phase 8 cross-cutting guard tests
       uv run pytest verification/ -q 2>&1 | tee /tmp/phase10-verification.log
       ```
       ALL must exit 0; lint-logging grep must return 0 matches. Save outputs into VALIDATION.md inline.

    3. **Pre-fill `10-VALIDATION.md`** with sections:
       - Frontmatter: `phase: 10`, `status: pending_operator_approval`, `nyquist_compliant: true`, `live_paridad_sync_async: pending`, `wave_status: ready_for_live`
       - Nyquist gates table (8 truth gates from must_haves)
       - CI matrix output (pytest 3.12 + 3.13, ruff, mypy strict, lint-imports, lint-logging, import-linter — all green)
       - Snapshot delta inline: full matriz snapshot diff (+~23-25 lines)
       - Plan 10-01/02/03/04 file change summary (LOC + test counts)
       - Phase 8/9 carry-forward invariants (Pitfall 4 mutation gate, RedactingFilter, B8 lock-in) — all confirmed
       - Pending sections:
         - `## Live Paridad Run` (operator fills in)
         - `## Operator Approval` (operator fills in)

    4. **Handoff prompt to operator:** present this exact text:

    ---

    **OPERATOR LIVE VERIFICATION — Phase 10 LIVE-02**

    All automated checks have passed. Please now execute the live `main_matriz.py` run to validate sync↔async paridad:

    ```bash
    # Confirm credentials present:
    grep -c "PRIMARY_API_USERNAME\|PRIMARY_API_PASSWORD" packages/matriz-client/.env
    # Should return 2.

    # Run main_matriz.py with both sync + async probes interleaved:
    uv run --package matriz-client python main_matriz.py 2>&1 | tee /tmp/phase10-live-paridad.log
    ```

    **Acceptance criteria — operator confirms in `10-VALIDATION.md` `## Live Paridad Run` section:**

    1. The run completed without unhandled exceptions.
    2. The final paridad line at end of `main()` reports `PASS` (sync outcomes == async outcomes).
    3. Spot-check ≥ 5 sync probes returned `PASS` (e.g., `get_segments`, `get_all_instruments`, `get_positions`, `get_account_report`, `get_market_data`).
    4. The same ≥ 5 paired async probes returned `PASS`.
    5. Any `FINDING` or `SKIPPED` outcomes match between sync and async (or are documented in the divergences section if not).
    6. The token refresh succeeded at least once (look for `X-Auth-Token` in the request log OR the `_state.token` populated post-run).
    7. ws_client smoke (optional but recommended): if `--ws` flag exists or a separate ws_client probe is wired, confirm WebSocket connect succeeded and the token came from `state.token_store.get_sync()` (no extra `/auth/getToken` call).

    **If paridad PASS:** Update `10-VALIDATION.md` frontmatter:
    - `status: approved`
    - `live_paridad_sync_async: true`
    - `operator_signoff_date: <YYYY-MM-DD>`
    - `operator_signoff_run_log: /tmp/phase10-live-paridad.log` (or paste excerpt)

    Then resume execution to commit atomically.

    **If paridad FAIL:** Document divergences in `10-VALIDATION.md` `## Live Paridad Run` section with the specific probes that differ. Return to planner; this becomes a Plan 10-05 gap-closure target.

    ---

    Resume signal: type "approved" or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" once `10-VALIDATION.md` is updated with `status: approved` AND `live_paridad_sync_async: true`. Then Claude executes the atomic commit. If issues, describe divergences and return to planner.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: After operator approval — finalize 10-VALIDATION.md + commit atomically per D-08</name>
  <files>.planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` (after operator update from Task 2) — to confirm frontmatter approved + `live_paridad_sync_async: true`
    - `.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` — reference for the final closure format (especially the close-out spot-checks list)
    - `.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md` — second reference
    - `git status` — to confirm exactly the files-modified set per the plan
  </read_first>
  <behavior>
    - `10-VALIDATION.md` frontmatter has `status: approved`, `nyquist_compliant: true`, `live_paridad_sync_async: true`, `wave_status: complete`
    - VALIDATION.md `## Close-out Spot Checks` section added with ≥ 6 spot checks (mirror Phase 8 close-out)
    - 1 atomic commit per D-08 with all Phase 10 closure files
  </behavior>
  <action>
    **Step 1 — Append `## Close-out Spot Checks` section to `10-VALIDATION.md`** (mirror Phase 8 Plan 6 Task 2 spot-check pattern):

    1. **AsyncClient surface complete:** `python -c "from matriz_client import aio; missing = [n for n in [...22 names...] if not hasattr(aio, n)]; assert not missing"` — PASS
    2. **B8 lock-in invariant:** `python -c "from matriz_client import _core, aio, client; assert aio._raise_for_response is client._raise_for_response is _core.raise_for_response"` — PASS
    3. **Mutation gate Pitfall 4 (Plan 10-02 AM2/AM3/AM4):** `uv run pytest packages/matriz-client/tests/test_async_mutations.py -k "503 or mutation" -q` — PASS (EXACTLY 1 request on 503)
    4. **TokenStore 3-way regression (Plan 10-03 I1):** `uv run pytest packages/matriz-client/tests/test_token_store_integration.py::test_async_caller_waits_for_concurrent_sync_refresh -q` — PASS
    5. **Cross-leak sentinel extended (Plan 10-04 Task 1):** `uv run pytest verification/test_sync_async_isolation.py -k matriz -q` — PASS
    6. **Snapshot diff scope correctness:** `git diff verification/snapshots/iol-client-surface.txt verification/snapshots/higyrus-client-surface.txt verification/snapshots/ambito-financiero-client-surface.txt verification/snapshots/wallets-client-surface.txt | wc -l` returns 0 — only matriz snapshot changed
    7. **`_state.account_id` ORP-01 preserved:** `git diff packages/matriz-client/src/matriz_client/_state.py | grep -E "^[-+].*account_id" | wc -l` returns 0
    8. **3 forward-reference skips closed:** `uv run pytest -q --collect-only 2>&1 | grep -c "SKIPPED.*Phase 10\|SKIPPED.*REFAC-04"` returns 0

    Each spot check: command + expected output + actual output (PASS marker).

    **Step 2 — Atomic commit per D-08:**

    Stage and commit all 6 files in a single commit. Use the commit message from CONTEXT specifics line 1005:
    ```
    git add main_matriz.py packages/matriz-client/tests/test_fixture_reaches_production.py verification/test_async_cancellation.py verification/test_sync_async_isolation.py verification/snapshots/matriz-client-surface.txt .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md

    git commit -m "ci(phase-10): green gate — live paridad sync↔async + snapshot regen + 3 skips flipped (LIVE-02)

    Plan 10-04 closure:
    - main_matriz.py extended with paired sync+async probes (D-06 interleaved pattern, no --async flag)
    - 3 forward-reference skips flipped: test_fixture_reaches_production.py:64, verification/test_async_cancellation.py:82, verification/test_sync_async_isolation.py:176
    - Cross-leak sentinel extended for matriz async (_state + token_store instance isolation)
    - matriz snapshot regenerated: +AsyncClient + 22 module-level async delegators (~23-25 lines diff)
    - iol/higyrus/ambito/wallets snapshots ZERO diff
    - Full CI green: pytest matrix 3.12+3.13, ruff, mypy strict, lint-imports, lint-logging
    - LIVE-02 paridad sync↔async confirmed by operator (10-VALIDATION.md)
    - Nyquist compliant: 8/8 truth gates green

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    Confirm: `git log -1 --stat` shows exactly the 6 files in the single commit.
  </action>
  <verify>
    <automated>git log -1 --pretty="%s" | grep -c "ci(phase-10).*LIVE-02" && git log -1 --stat | grep -c "main_matriz.py\|test_fixture_reaches_production.py\|test_async_cancellation.py\|test_sync_async_isolation.py\|matriz-client-surface.txt\|10-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty="%s"` returns a single line starting with `ci(phase-10):` and containing `LIVE-02`
    - `git log -1 --stat | grep -c "^ \(main_matriz.py\| packages/matriz-client/tests/test_fixture_reaches_production.py\| verification/test_async_cancellation.py\| verification/test_sync_async_isolation.py\| verification/snapshots/matriz-client-surface.txt\| .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md\)"` returns 6 (all 6 files in single commit)
    - `grep -c "status: approved" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` returns 1
    - `grep -c "live_paridad_sync_async: true" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` returns 1
    - `grep -c "nyquist_compliant: true" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` returns 1
    - `grep -c "^## Close-out Spot Checks" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` returns 1
    - `grep -c "PASS" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md` returns ≥ 8 (8 spot checks marked PASS)
    - Workspace baseline post-commit: `uv run pytest -q` exits 0; total test count ≥ ~840 per CONTEXT D-07
    - `git status` returns clean (no uncommitted changes outside the Plan 10-04 scope)
  </acceptance_criteria>
  <done>
    Phase 10 closed atomically with operator approval. `10-VALIDATION.md` has frontmatter `status=approved`, `nyquist_compliant=true`, `live_paridad_sync_async=true`, and 8 close-out spot checks all PASS. Single commit `ci(phase-10): green gate — live paridad sync↔async + snapshot regen + 3 skips flipped (LIVE-02)` lands per D-08. Phase 10 ready for handoff to Phase 11 (HARN-07..10, CR-01..08, LIVE-01 full 4-package live re-verification).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → `main_matriz.py` live run | Operator-controlled execution against real matriz remarkets API; credentials live in `packages/matriz-client/.env` |
| Live matriz API → `main_matriz.py` probes | Outbound HTTPS to remarkets endpoint; real auth flow exercised (no mocks) |
| Operator → `10-VALIDATION.md` approval signal | Operator writes `status: approved` to frontmatter; subsequent commit is gated on this signal |
| `verification/snapshots/matriz-client-surface.txt` regenerated artifact | Snapshot tool reflection of `dir(matriz_client)` + `dir(matriz_client.aio)`; could theoretically include credential-derived attributes IF the AsyncClient `__repr__` is wrong |
| Cross-leak sentinel test → process-shared module state | Verifies isolation but exercises both sync and async surfaces concurrently in one process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | Live `main_matriz.py` run logs (operator captures to `/tmp/phase10-live-paridad.log`) | mitigate | `_logging.RedactingFilter` (Phase 8 LOG-02) scrubs `X-Auth-Token`, `X-Username`, `X-Password`, `Authorization Basic` from log records. Operator instruction: do NOT paste raw log into `10-VALIDATION.md` — paste only outcome summary (probe names + PASS/FINDING markers). ASVS V8.3.4. |
| T-10-04-02 | Information Disclosure | `verification/snapshots/matriz-client-surface.txt` regenerated content | mitigate | Snapshot tool reflects PUBLIC attribute names + signatures only; it does NOT read attribute VALUES. AsyncClient `__repr__` is redacted (Plan 10-02 acceptance T-10-02-01). Verify the snapshot file has 0 occurrences of test credentials: `grep -c "test-pass\|test-token\|sentinel" verification/snapshots/matriz-client-surface.txt` returns 0 in close-out spot check. |
| T-10-04-03 | Information Disclosure | Operator `10-VALIDATION.md` content | mitigate | Operator instruction explicitly says: paste only outcome summary, NOT raw log. Pre-fill template includes redacted placeholder for live run log path (operator pastes path, not content). |
| T-10-04-04 | Tampering | Cross-leak sentinel reveals an actual leak | block | If the cross-leak sentinel test FAILS in Task 1, the sync↔async isolation has regressed — this is a security-relevant bug (credential cross-leak path). STOP planning; return to Plan 10-03 to identify the regression. `verification/test_sync_async_isolation.py` is the trip-wire. ASVS V3.5.2 (session isolation). |
| T-10-04-05 | Tampering | Snapshot regen unintentionally includes private internals | mitigate | The snapshot tool filters to PUBLIC attrs only (`__all__` + non-underscore attributes per Phase 6 D-09). Plan 10-04 acceptance: `grep -c "^_token_store\|^_refresh\|^_atransport" verification/snapshots/matriz-client-surface.txt` returns 0 (private modules NOT exposed). |
| T-10-04-06 | Repudiation | Operator approval signal lacks audit trail | mitigate | `10-VALIDATION.md` frontmatter records `operator_signoff_date: <YYYY-MM-DD>` + `operator_signoff_run_log: <path>` per the handoff prompt. Git history preserves the approval commit timestamp. |
| T-10-04-07 | Denial of Service | Live `main_matriz.py` run hangs or rate-limits the auth server | mitigate | Plan 10-01's `RefreshPolicy` fail-cache (`fail_cache_s=30.0`) prevents the probes from hammering a degraded auth server. Plan 10-02's `AsyncRetryTransport` cap (`_RETRY_AFTER_CAP_S=60`) caps server-requested delays. Combined: operator interrupts the run if it hangs > 5 min total. |
| T-10-04-08 | Tampering | npm/pip/cargo installs | n/a | NO new dependencies in Plan 10-04. Only edits + snapshot regen + commit. Package Legitimacy Gate not triggered. |
| T-10-04-09 | Elevation of Privilege | The 3 skip-flips reveal a previously-untested path that fails security checks | accept | If `test_fixture_reaches_production.py:64` flipped active test FAILS, it surfaces a bug where the async monkeypatch sentinel does NOT propagate to the wire — this is a security-relevant test infrastructure regression (Pitfall #1 carry-forward from Phase 6). STOP planning; return to Plan 10-02/03. The Task 1 verify command catches this. |
| T-10-04-10 | Information Disclosure | `10-VALIDATION.md` checked into git contains run-log paths or excerpts | accept | Operator-pasted log excerpts have already been filtered through RedactingFilter (T-10-04-01). The path `/tmp/phase10-live-paridad.log` is operator-local and not committed. Operator instruction enforces this. |
</threat_model>

<verification>
- Task 1 verification (already in Task 1) — all 4 modified files pass ruff + mypy strict + the 3 NEW active tests PASS
- Task 2 verification — operator-driven, signaled via `10-VALIDATION.md` frontmatter `status: approved`
- Task 3 verification — single atomic commit; 8 close-out spot checks PASS
- Full pytest matrix: `UV_PYTHON=3.12 uv run pytest -q && UV_PYTHON=3.13 uv run pytest -q` BOTH exit 0
- Full lint: `uv run ruff check . && uv run ruff format --check . && uv run mypy --strict packages/ && uv run lint-imports` ALL exit 0
- CI grep lint-logging: `grep -rn "logging.basicConfig\|logging.root" packages/*/src/` returns 0
- Phase 8 6 cross-cutting guard tests still GREEN (no regression from Plan 10 wiring)
- Phase 7 CR-03 `parse_envelope_consumes_body` still GREEN
- Phase 7 CR-05 `_envelope_probe` matriz sweep snapshot still GREEN
- Snapshot delta correctness: matriz snapshot grows by ~23-25 lines (AsyncClient + 22 delegators); iol/higyrus/ambito/wallets ZERO diff
- 3 forward-reference skips flipped → `uv run pytest -q --collect-only 2>&1 | grep -c "SKIPPED.*Phase 10\|SKIPPED.*REFAC-04"` returns 0
- Test count target: ~840 verde (baseline ~785 + Plan 10-01 +20 + Plan 10-02 +30 + Plan 10-03 +5 + Plan 10-04 +3 from skip flips = ~843)
- Operator approval captured in `10-VALIDATION.md` per D-08 idiom (mirror Phase 8 Plan 6 Task 2 close-out signal)
- 1 atomic commit per D-08
</verification>

<success_criteria>
1. **LIVE-02 satisfied:** Operator runs `main_matriz.py` against matriz remarkets; sync probes + async probes report identical PASS/FINDING/SKIPPED sets; paridad PASS reported in `10-VALIDATION.md`.
2. **3 skip flips landed:** `test_fixture_reaches_production.py:64`, `verification/test_async_cancellation.py:82`, `verification/test_sync_async_isolation.py:176` all FLIPPED; 3 NEW active tests pass.
3. **Cross-leak sentinel extended for matriz async:** sync↔async `_state` + `_state.token_store` instance isolation asserted; PASS.
4. **Matriz snapshot regen correctness:** +AsyncClient + 22 module-level async delegators (~23-25 lines); iol/higyrus/ambito/wallets snapshots ZERO diff.
5. **CI green matrix:** Python 3.12 + 3.13 pytest pass; ruff + ruff format + mypy strict + lint-imports + lint-logging + import-linter contracts all PASS.
6. **Phase 8 6 cross-cutting guard tests still GREEN; CR-03 + CR-05 still GREEN; Pitfall 4 mutation gate still GREEN** (no Phase 10 regression on Phase 7/8 invariants).
7. **`10-VALIDATION.md` operator approval frontmatter:** `status: approved`, `nyquist_compliant: true`, `live_paridad_sync_async: true`, 8 close-out spot checks PASS.
8. **Atomic commit per D-08:** single `ci(phase-10): green gate — live paridad sync↔async + snapshot regen + 3 skips flipped (LIVE-02)` commit with all 6 file changes.
9. **Phase 10 closure handoff ready:** Phase 11 (HARN-07..10, CR-01..08, LIVE-01 full 4-package live re-verification) can proceed with confidence in REFAC-04 + LIVE-02 closure.
</success_criteria>

<output>
Create `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md` documenting:
- main_matriz.py extension scope (LOC delta; number of paired async probes ≥ 18; paridad reporter line)
- 3 skip flips landed at exact line numbers
- Cross-leak sentinel matriz async extension landed
- Snapshot regen: matriz +~23-25 lines; iol/higyrus/ambito/wallets ZERO diff (verified per close-out spot check #6)
- CI green matrix: pytest 3.12 + 3.13, ruff, mypy strict, lint-imports, lint-logging — all PASS
- Operator-driven live paridad run: PASS (per operator signoff in 10-VALIDATION.md)
- 10-VALIDATION.md created with full Nyquist + CI evidence + close-out spot checks
- Atomic commit hash
- Phase 10 closed: REFAC-04 + LIVE-02 satisfied
- 3 forward-reference skips closed
- Phase 11 unblocked: full 4-package live re-verification (LIVE-01) can proceed
</output>
</output>
