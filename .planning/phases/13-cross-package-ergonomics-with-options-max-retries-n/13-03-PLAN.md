---
phase: 13-cross-package-ergonomics-with-options-max-retries-n
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified:
  - packages/higyrus-client/src/higyrus_client/client.py
  - packages/higyrus-client/src/higyrus_client/aio.py
  - packages/higyrus-client/tests/test_client.py
  - packages/higyrus-client/tests/test_async_client.py
  - verification/snapshots/higyrus-client-surface.txt
autonomous: true
requirements: [ERG-01]
requirements_addressed: [ERG-01]

must_haves:
  truths:
    - "higyrus_client.Client.with_options(max_retries=N) returns a view that shares _state (including token + refresh_token + account_id propagation)"
    - "higyrus_client.AsyncClient.with_options(max_retries=N) async mirror returns view sharing _state"
    - "view.close() / view.aclose() are no-op — parent's http_client + token stay intact (no re-auth)"
    - "_request shell propagates extensions['max_attempts'] = self._max_retries + 1 alongside the existing extensions (idempotent, request_id, endpoint_name, account_id)"
    - "RedactingFilter (Phase 8 LOG-02) still redacts tokens under view's retry logs (no regression — view shares logger via shared package namespace)"
    - "Token/refresh-token flow unchanged: view.get_movimientos(...) re-uses parent's cached token; no extra _ensure_token call from view path"
    - "Cross-cutting test_with_options_*[higyrus_client] rows flip RED→GREEN in this plan (3 of 4: shares, extension-honored, chaining)"
  artifacts:
    - path: "packages/higyrus-client/src/higyrus_client/client.py"
      provides: "Client.with_options + _is_view + no-op close() + _request extensions['max_attempts']"
      contains: "def with_options"
    - path: "packages/higyrus-client/src/higyrus_client/aio.py"
      provides: "AsyncClient.with_options + _is_view + no-op aclose() + async _request extensions['max_attempts']"
      contains: "def with_options"
    - path: "packages/higyrus-client/tests/test_client.py"
      provides: "Per-package mocked tests (close-is-noop, chaining, repr-prefix, invalid args, RedactingFilter integrity)"
    - path: "packages/higyrus-client/tests/test_async_client.py"
      provides: "Async-mirror mocked tests"
    - path: "verification/snapshots/higyrus-client-surface.txt"
      provides: "Snapshot regenerated (likely zero body diff; D-P4 atomicity discipline preserved)"
  key_links:
    - from: "Client.with_options"
      to: "_validate_max_retries (Phase 8 WR-06 helper)"
      via: "first call in with_options body"
      pattern: "_validate_max_retries\\(max_retries\\)"
    - from: "_request shell sets req.extensions['max_attempts']"
      to: "RetryTransport.handle_request reads request.extensions.get('max_attempts')"
      via: "request.extensions key threading (Plan 1 wired the read; Plan 3 wires the write for higyrus)"
      pattern: "extensions\\[\"max_attempts\"\\] = self\\._max_retries \\+ 1"
    - from: "view's retry warning log emit"
      to: "RedactingFilter (Phase 8 LOG-02)"
      via: "shared logger `higyrus_client` namespace; view shares _state so token never reaches log args"
      pattern: "_LOGGER_NAME = \"higyrus_client\""
---

<objective>
Land `with_options(max_retries=N)` on `higyrus_client.Client` and `higyrus_client.AsyncClient` — second package in the per-package serial roll-out (ámbito → higyrus → matriz → iol).

Purpose: higyrus is the first authenticated package in the serial. Has `token`, `refresh_token`, `account_id` propagation via `_state` and `_request` shell extensions. The view shares all of this (anti-Pitfall 13: zero re-auth, zero pool fragmentation, zero account_id state divergence). RedactingFilter (Phase 8 LOG-02) must continue redacting tokens under the view's retry log records — verified via per-package `caplog` test.

Output:
- `Client.with_options` + `AsyncClient.with_options` methods + `_is_view` slot + lifecycle no-op guards + `_request` shell extension write.
- Per-package mocked tests including a RedactingFilter integrity test (view's retry log under 503 does NOT leak token sentinel).
- Snapshot regenerated per D-P4 atomicity.
- 3 of 4 cross-cutting tests in `verification/test_with_options.py` flip RED→GREEN for `[higyrus_client]` row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/src/higyrus_client/aio.py
@packages/higyrus-client/src/higyrus_client/_state.py
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@verification/test_with_options.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Client.with_options + AsyncClient.with_options to higyrus (sync + async in one task — same idiom)</name>
  <files>
    packages/higyrus-client/src/higyrus_client/client.py,
    packages/higyrus-client/src/higyrus_client/aio.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md (ámbito reference implementation — read first to mirror exact shape)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-V1, D-V2, D-V3; `<specifics>` View constructor pattern + Shell _request extension set)
    packages/higyrus-client/src/higyrus_client/client.py (current Client class shape: __slots__, __init__, close, _request — note `account_id` propagation via `req.extensions["account_id"] = ...` if spec carries one)
    packages/higyrus-client/src/higyrus_client/aio.py (current AsyncClient shape; mirrors sync per Phase 7)
    packages/ambito-financiero-client/src/ambito_financiero_client/client.py (just-modified Plan 2 reference — exact with_options method shape)
    packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (Plan 2 async mirror reference)
  </read_first>
  <behavior>
    - `c = higyrus_client.Client(); view = c.with_options(max_retries=5)` returns fresh `Client`.
    - `view._state is c._state` AND `view._state.token is c._state.token` (no re-auth) AND `view._state.refresh_token is c._state.refresh_token`.
    - `view._is_view is True`; `c._is_view is False`.
    - `view.close()` is no-op (parent's http_client + cached token intact).
    - `with view:` enters and exits without disturbing parent state.
    - `view.get_movimientos(id_cuenta=..., fecha_desde=..., fecha_hasta=...)` works; on persistent 503, produces `view._max_retries + 1` outgoing requests.
    - `_request` shell sets `extensions["max_attempts"] = self._max_retries + 1` AFTER the existing extensions (idempotent, request_id, endpoint_name, account_id).
    - Async mirror: `await view.get_movimientos(...)` works; `await view.aclose()` no-op.
  </behavior>
  <action>
    Modify both files in this single task (same idiom — minimize context split):

    **`packages/higyrus-client/src/higyrus_client/client.py`** (mirror Plan 2 Task 1 exactly):

    1. Add `_is_view` to `Client.__slots__` (alphabetically sorted).

    2. Set `self._is_view = False` in `__init__` after `self._max_retries = max_retries`.

    3. Add `Client.with_options` method:

       - Signature: `def with_options(self, *, max_retries: int) -> Self:`
       - Body: `_validate_max_retries(max_retries)` → `view = type(self).__new__(type(self))` → `view._state = self._state` → `view._max_retries = max_retries` → `view._is_view = True` → `return view`
       - Docstring: mirror Plan 2 docstring style; mention that higyrus's `token`, `refresh_token`, `account_id` propagation all share via `_state` (anti-Pitfall 13).
       - Place after `_ensure_http_client` and before `_request` / `_send_auth_request`.

    4. Modify `Client.close()` to no-op when `_is_view=True`: add `if getattr(self, "_is_view", False): return` at top.

    5. Modify `Client._request()` shell — locate the existing `req.extensions["idempotent"] = spec.idempotent` / `request_id` / `endpoint_name` / optional `account_id` block (around lines 295-310 per the grep evidence) and add:

       `req.extensions["max_attempts"] = self._max_retries + 1  # Phase 13 ERG-01 view override.`

       Place after `req.extensions["endpoint_name"] = spec.endpoint_name` (last of always-set extensions). If `account_id` is set conditionally below, `max_attempts` goes ABOVE it (uniform).

    6. DO NOT modify `_send_auth_request()` shell — auth/refresh requests already have their `extensions["idempotent"] = True` (Phase 8 D-03). The view's `_max_retries` should ALSO flow through auth requests (per D-T6: "view's `extensions["max_attempts"]` SÍ se honra para esos calls"). Therefore, add the SAME `req.extensions["max_attempts"] = self._max_retries + 1` line to `_send_auth_request()` as well — alongside the existing `req.extensions["endpoint_name"]` line in that method. (Locate `_send_auth_request` around lines 203-228 per grep; the extensions block is at 224-226.) Per CONTEXT.md D-T6: auth-flow override is already covered by the cross-cutting `test_with_options_max_attempts_extension_honored` test (login marked `idempotent=True`).

    7. Modify `__repr__` to prefix `"view of "` when `_is_view=True`. Higyrus's repr already redacts password and token (Phase 6 D-18); keep that intact, just prepend `"view of "` when applicable.

    8. DO NOT change `__all__`.

    9. DO NOT add new imports — `_validate_max_retries`, `Self` already in scope.

    **`packages/higyrus-client/src/higyrus_client/aio.py`** (mirror sync):

    1. Add `_is_view` to `AsyncClient.__slots__`.

    2. Set `self._is_view = False` in `AsyncClient.__init__`.

    3. Add `AsyncClient.with_options` method (sync method even on async class; returns view; subsequent endpoint call is async).

    4. Modify `aclose()` to no-op when `_is_view=True`.

    5. Modify async `_request()` shell — same `extensions["max_attempts"]` add.

    6. Modify async `_send_auth_request()` if higyrus has an async auth path. (Read aio.py first to confirm; higyrus aio.py from Phase 7 should mirror sync.)

    7. `__repr__` prefix.

    8. DO NOT change `__all__`. DO NOT add new imports.

    Constraint: total diff to `client.py` ≤ +35 LOC additions (slightly larger than ámbito's because of `_send_auth_request` second extension write); `aio.py` ≤ +30 LOC additions.

    After modifying, run `uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q` to confirm Phase 6/7/8/9 higyrus tests stay GREEN.
  </action>
  <verify>
    <automated>uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/higyrus-client/</automated>
    <automated>uv run mypy --strict packages/higyrus-client/src</automated>
    <automated>uv run pytest verification/test_with_options.py -k "higyrus_client" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def with_options" packages/higyrus-client/src/higyrus_client/client.py` returns 1.
    - Source: `grep -c "def with_options" packages/higyrus-client/src/higyrus_client/aio.py` returns 1.
    - Source: `grep -c "_is_view" packages/higyrus-client/src/higyrus_client/client.py` returns ≥ 4.
    - Source: `grep -c "_is_view" packages/higyrus-client/src/higyrus_client/aio.py` returns ≥ 4.
    - Source (extension set in _request shell): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/higyrus-client/src/higyrus_client/client.py` returns ≥ 2 (in `_request` AND `_send_auth_request`).
    - Source (extension set in async _request shell): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/higyrus-client/src/higyrus_client/aio.py` returns ≥ 1 (or ≥ 2 if higyrus has async `_send_auth_request`).
    - Slot correctness: `python -c "from higyrus_client import Client, AsyncClient; assert '_is_view' in Client.__slots__ and '_is_view' in AsyncClient.__slots__"` exits 0.
    - Behavior: `uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q exits 0` (Phase 6-10 tests still GREEN).
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_shares_http_client_and_token -k higyrus_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_max_attempts_extension_honored -k higyrus_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_chaining_inner_wins -k higyrus_client -x -q exits 0`.
    - Quality: `uv run ruff check packages/higyrus-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/higyrus-client/src exits 0`.
  </acceptance_criteria>
  <done>
    `Client.with_options` + `AsyncClient.with_options` added to higyrus, both with `_is_view` slot and lifecycle no-op. `_request` AND `_send_auth_request` shells (sync + async) propagate `extensions["max_attempts"]`. Existing higyrus tests GREEN. 3 of 4 cross-cutting tests GREEN for higyrus row. Ruff + mypy strict GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-package mocked tests + RedactingFilter integrity test + regenerate snapshot</name>
  <files>
    packages/higyrus-client/tests/test_client.py,
    packages/higyrus-client/tests/test_async_client.py,
    verification/snapshots/higyrus-client-surface.txt
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P3 — per-package mocked tests live in `packages/<pkg>/tests/`; D-P4 snapshot atomicity)
    packages/ambito-financiero-client/tests/test_client.py (just-added Plan 2 mocked-test patterns — mirror)
    packages/higyrus-client/tests/test_client.py (existing test patterns — note `caplog` + `RedactingFilter` integration if already used; higyrus uses LOG-02 redaction)
    packages/higyrus-client/tests/test_async_client.py (existing async patterns)
    verification/snapshots/higyrus-client-surface.txt (current snapshot)
    verification/test_logging_no_token_leak.py (Phase 8 caplog redaction pattern — reuse as analog for the integrity test)
  </read_first>
  <behavior>
    - Sync + async mocked tests live in `packages/higyrus-client/tests/`.
    - One NEW test (specific to higyrus, not in ámbito Plan 2 because ámbito has no auth): `test_with_options_view_retry_log_still_redacts_token` — under 503 with retries enabled, the view's retry log records do NOT contain the token sentinel; RedactingFilter (Phase 8 LOG-02) still applies.
    - Snapshot regenerated per D-P4; expected body diff is zero (`Client` already in `__all__`; `with_options` is method).
  </behavior>
  <action>
    1. **Add mocked tests to `packages/higyrus-client/tests/test_client.py`** (mirror Plan 2 Task 3 set + 1 new test):

       - `test_with_options_close_is_noop`: construct `Client(username="u", password="p", token="test-token", token_expires_at=9_999_999_999.0)`, call mocked GET to populate `http_client`, call `view.close()`, assert parent's `http_client is not None` AND `_state.token == "test-token"` (no re-auth).
       - `test_with_options_exit_is_noop`: `with view:` mirror.
       - `test_with_options_chaining_inner_wins_local`: as Plan 2.
       - `test_with_options_repr_shows_view_prefix`: as Plan 2 — verify higyrus's repr STILL redacts password and token (existing Phase 6 D-18 behavior) AND prepends `"view of "`.
       - `test_with_options_invalid_max_retries_raises_value_error`: as Plan 2.
       - **NEW `test_with_options_view_retry_log_still_redacts_token(caplog, httpx_mock)`**: per Phase 8 LOG-02 pattern. Construct `Client(token="HIGYRUS-TOKEN-SENTINEL-DO-NOT-LEAK", token_expires_at=9_999_999_999.0)`. Mock httpx with 503 × 3. Call `view = c.with_options(max_retries=2)`; trigger `view.get_movimientos(...)` (idempotent GET). Capture caplog at DEBUG for logger `higyrus_client`. Assert: 2 retry WARNING records emitted AND sentinel substring `"HIGYRUS-TOKEN-SENTINEL-DO-NOT-LEAK"` does NOT appear in any record's `message`, `args`, or `repr(extra)`. Reuse the assertion idiom from `verification/test_logging_no_token_leak.py`.

       ~70-80 LOC for the 6 tests.

    2. **Add async-mirror tests to `packages/higyrus-client/tests/test_async_client.py`**:

       - `test_with_options_aclose_is_noop`
       - `test_with_options_aexit_is_noop`
       - `test_with_options_chaining_inner_wins_local_async`
       - `test_with_options_async_repr_shows_view_prefix`
       - `test_with_options_async_invalid_max_retries_raises_value_error`
       - `test_with_options_view_async_retry_log_still_redacts_token`: async mirror of the redaction test using `AsyncClient` + `AsyncRetryTransport` retry path.

       ~70-80 LOC.

    3. **Regenerate snapshot for higyrus-client only**:

       Run `uv run python verification/regen_snapshots.py`. Expect body diff to be empty (same reasoning as Plan 2 Task 3). Commit the (likely no-diff) snapshot file to maintain D-P4 atomicity discipline. If regen script touched other packages' snapshots, `git checkout` them before committing — Plan 4 (matriz) and Plan 5 (iol) regen their own.

    4. Run `uv run pytest verification/test_public_surface.py -x -q` to confirm GREEN.

    5. Run cross-cutting + per-package full suite; expect all GREEN except matriz mutation-gate (Plan 4).
  </action>
  <verify>
    <automated>uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q</automated>
    <automated>uv run pytest verification/test_public_surface.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "higyrus_client" -x -q</automated>
    <automated>uv run ruff check packages/higyrus-client/ verification/</automated>
    <automated>uv run mypy --strict packages/higyrus-client/src packages/higyrus-client/tests</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def test_with_options_close_is_noop" packages/higyrus-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_view_retry_log_still_redacts_token" packages/higyrus-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_aclose_is_noop" packages/higyrus-client/tests/test_async_client.py` returns 1.
    - Source: `grep -c "def test_with_options_view_async_retry_log_still_redacts_token" packages/higyrus-client/tests/test_async_client.py` returns 1.
    - Source: `grep -c "HIGYRUS-TOKEN-SENTINEL-DO-NOT-LEAK" packages/higyrus-client/tests/` returns ≥ 2 (sync + async).
    - Behavior: `uv run --package higyrus-client pytest packages/higyrus-client/tests/ -x -q exits 0`. New tests count: ≥ 12 (6 sync + 6 async).
    - Behavior: `uv run pytest verification/test_public_surface.py -x -q exits 0`.
    - Cross-cutting (3 of 4 GREEN for higyrus): `uv run pytest verification/test_with_options.py -k "higyrus_client" -x -q exits 0`.
    - Snapshot integrity: `git diff verification/snapshots/higyrus-client-surface.txt` reports empty OR only header-no-op (per snapshot enumeration logic).
    - Negative (other packages untouched): `git diff verification/snapshots/ambito-financiero-client-surface.txt verification/snapshots/matriz-client-surface.txt verification/snapshots/iol-client-surface.txt` reports empty (Plan 3 only touches higyrus snapshot).
    - Quality: `uv run ruff check packages/higyrus-client/ verification/ exits 0`.
    - Quality: `uv run mypy --strict packages/higyrus-client/ exits 0`.
  </acceptance_criteria>
  <done>
    Per-package mocked tests added (6 sync + 6 async), including RedactingFilter integrity test. Snapshot regenerated (zero body diff acceptable). 3 of 4 cross-cutting tests for higyrus GREEN. All higyrus tests GREEN. Ruff + mypy strict GREEN. Plan 4 (matriz) can begin from a stable ámbito+higyrus HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.with_options` | Caller-controlled `max_retries` validated via `_validate_max_retries` Phase 8 WR-06 |
| view → `_state.token` (shared) | View shares parent's cached token; no token re-write; no new credential storage |
| view → `_state.refresh_token` (shared) | View shares; refresh logic flows through parent's `_send_auth_request` (Phase 8 D-03) |
| view → `_state.account_id` (shared) | View shares; `_request` shell propagates `extensions["account_id"]` for log redaction (Phase 8 LOG-03) |
| RetryTransport log emit → log handlers | RedactingFilter (Phase 8 LOG-02) under `higyrus_client` namespace; view retry log records flow through SAME logger; redaction still applies (no logger swap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | View retry log leaks token sentinel | mitigate | `test_with_options_view_retry_log_still_redacts_token` asserts caplog records do NOT contain token sentinel. Reuses Phase 8 LOG-02 `RedactingFilter`. Logger namespace `higyrus_client` UNCHANGED by view (anti-Pitfall 7 inherited from Phase 14 planning). |
| T-13-03-02 | Tampering | `Client.with_options` max_retries arg | mitigate | `_validate_max_retries` rejects negative/bool/non-int. Test: `pytest.raises(ValueError)` for -1, True, 1.5. |
| T-13-03-03 | Denial of Service | View closes parent's http_client / token | mitigate | `_is_view` flag + no-op `close()` / `aclose()` guard. Cross-cutting `test_with_options_shares_http_client_and_token` + per-package `test_with_options_close_is_noop` assert parent's `http_client` AND `_state.token` survive `view.close()`. |
| T-13-03-04 | Spoofing | View's auth-request override could bypass mutation gate | mitigate | `_send_auth_request` requests are marked `idempotent=True` (Phase 8 D-03), so the view's `extensions["max_attempts"]` SÍ se honra for auth requests (per D-T6). This is intended behavior (auth retry is safe). Mutation gate still prevents non-idempotent mutations from bypassing (no mutating higyrus endpoints today — but the wire is preserved). |
| T-13-03-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies in Plan 3. |

Mutation gate (Phase 8 D-01 inherited): higyrus's non-idempotent endpoints (if any future POST/PATCH/DELETE land) still flow through `req.extensions["idempotent"] = spec.idempotent`. The Plan 1 transport extension change preserves the eval order: `idempotent` gate fires BEFORE `max_attempts` is consulted. Acceptance grep in Plan 1 Task 1 already validated this.

Credential surface: view shares `_state.token`, `_state.refresh_token`, `_state.username`, `_state.password`. No new storage path. Parent's `_state` is the single source. Anti-Pitfall 13 verified by `test_with_options_shares_http_client_and_token[higyrus_client]` cross-cutting.
</threat_model>

<verification>
- `Client.with_options` + `AsyncClient.with_options` exist on higyrus.
- Both classes have `_is_view` in `__slots__`.
- `close()` / `aclose()` / `__exit__` / `__aexit__` no-op when `_is_view=True`.
- `_request()` (sync + async) sets `req.extensions["max_attempts"] = self._max_retries + 1` after `endpoint_name` extension.
- `_send_auth_request()` (sync + async) also sets `extensions["max_attempts"]` (per D-T6 — auth flow is `idempotent=True` so override is honored).
- `__repr__` prefixes "view of " for views; existing password/token redaction preserved.
- Per-package mocked tests added: close-is-noop, chaining, repr-prefix, invalid args, RedactingFilter integrity (6 sync + 6 async).
- Snapshot regenerated; test stays GREEN; body diff empty.
- 3 of 4 cross-cutting tests for `[higyrus_client]` row flip GREEN (mutation-gate stays RED until Plan 4 — matriz-only).
- `uv run --package higyrus-client pytest` GREEN.
- `uv run ruff check packages/higyrus-client/ verification/` exits 0.
- `uv run mypy --strict packages/higyrus-client/ exits 0`.
- Other packages' snapshots untouched.
</verification>

<success_criteria>
- higyrus sync + async `with_options` lands.
- View's retry log under 503 still redacts token sentinel (RedactingFilter integrity confirmed via per-package caplog test).
- 3 of 4 cross-cutting tests for higyrus row GREEN.
- All higyrus package tests GREEN.
- Ruff + mypy strict GREEN.
- Plan 4 (matriz) can begin from stable ámbito+higyrus HEAD with confidence in the lifecycle no-op + extension-write idiom.
</success_criteria>

<output>
Create `.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-03-SUMMARY.md` when done. Document:
- Same shape as ámbito Plan 2 + adapted for higyrus (token/refresh_token/account_id propagation).
- `_send_auth_request` also gets `extensions["max_attempts"]` write per D-T6 (auth flow is idempotent=True).
- RedactingFilter integrity verified via new `test_with_options_view_retry_log_still_redacts_token` (sync + async).
- 3 of 4 cross-cutting tests GREEN.
- Forward reference: matriz Plan 4 adds the CRITICAL mutation-gate test (Anti-Pitfall 14, money-on-the-line) + D-T1..T6 TokenStore isolation.
</output>
