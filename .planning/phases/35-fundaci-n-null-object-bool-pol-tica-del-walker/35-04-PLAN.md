---
phase: 35-fundaci-n-null-object-bool-pol-tica-del-walker
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - packages/matriz-client/src/matriz_client/models.py
  - packages/matriz-client/tests/test_null_object.py
  - packages/matriz-client/tests/test_decode.py
  - packages/ambito-financiero-client/tests/test_null_object.py
  - packages/ambito-financiero-client/tests/test_decode.py
  - packages/wallets-client/tests/test_null_object.py
autonomous: true
requirements: [NOBJ-01, NOBJ-02]

must_haves:
  truths:
    - "matriz-client's 17 shipped _SafeModel subclasses are falsy when empty and truthy when perturbed, proven by enumerating the real models module (D-15) — including the 17 classes whose empty() has no scalar field at all, which the None-first perturbation branch is what makes reachable."
    - "UnknownFrame — a member of the public PrimaryWsMessage union that does NOT inherit the base — gains a hand-written __bool__ mirroring the base semantics, so `if frame:` reads consistently across every variant of the union (D-08)."
    - "matriz's _SafeModel keeps its existing empty(); this plan adds __bool__ only, and does not restate or re-shape the constructor that Phase 29 shipped."
    - "ambito-financiero-client and wallets-client declare ZERO response models by decision, and criterio 1's 'six packages' is satisfied there by an EXPLICIT enumeration-is-empty assertion with the reason cited — never by a skipped parametrize, never by a `>= 0` bound (D-05)."
    - "@property aliases stay invisible to get_type_hints() and cannot change the divergence record list, proven in matriz and in ambito (whose walker copy is dormant-but-exercised by design)."
    - "Wrong-typed values on a non-optional list field still emit the six-key record and still raise under strict_decode in matriz and ambito — pinned before the walker edit lands."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/models.py"
      provides: "_SafeModel.__bool__() and UnknownFrame.__bool__(), plus the corrected UnknownFrame class docstring"
      contains: "def __bool__"
    - path: "packages/matriz-client/tests/test_null_object.py"
      provides: "matriz enumeration + truthiness + UnknownFrame coverage + alias-invisibility (roster >= 17)"
      min_lines: 100
    - path: "packages/ambito-financiero-client/tests/test_null_object.py"
      provides: "Explicit zero-roster property assertion for ambito + the alias-invisibility pair"
      min_lines: 45
    - path: "packages/wallets-client/tests/test_null_object.py"
      provides: "Explicit zero-roster + import-free property assertion for wallets"
      min_lines: 35
  key_links:
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/_decode.py"
      via: "the pre-existing _SafeModel.empty() already walks an empty dict with SILENT_SINK plus the mapping pass; __bool__ consumes it unchanged"
      pattern: "return self != type\\(self\\)\\.empty\\(\\)"
    - from: "packages/matriz-client/tests/test_null_object.py"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "roster by introspection over _SafeModel subclasses, plus a separate non-parametrized case for UnknownFrame which is outside that hierarchy"
      pattern: "UnknownFrame"
  prohibitions:
    - statement: "matriz's existing empty() is not rewritten, re-docstringed, or 'harmonized' with the other three bases. Each row of the 6-way semantics matrix is a declared policy axis, not an inconsistency — an edit that unifies something in passing is a silent break on a published wheel."
      status: flagged-unverified
      verification: "git diff on matriz models.py touches only the two added __bool__ methods and the UnknownFrame class docstring's method-count sentence."
    - statement: "No SafeModel base is added to ambito-financiero-client or wallets-client to make the roster look non-empty. Wallets has no _decode.py; a copied base would ImportError on package import and redden all twelve wallets CI matrix legs."
      status: flagged-unverified
      verification: "Both models.py files stay byte-unchanged; the new tests assert their emptiness rather than removing it."
    - statement: "A zero count is never asserted as `>= 0`. The two empty packages carry a positive structural assertion (zero class definitions, empty __all__, import discipline) plus the cited reason."
      status: flagged-unverified
      verification: "grep for a `>= 0` bound in either new test file returns 0; each carries an explicit equality against zero plus a docstring naming D-05."
    - statement: "No pre-existing test is edited in this plan; the only test_decode.py changes are appended functions."
      status: flagged-unverified
      verification: "git diff --numstat on both touched test_decode.py files shows 0 deletions."
---

<objective>
Close NOBJ-01 across the remaining three packages: `matriz-client` (the reference implementation of the pattern, 17 classes plus the union outlier `UnknownFrame`), and the two packages that declare no response models at all — `ambito-financiero-client` and `wallets-client` — whose contribution to criterio 1 is an *explicitly asserted emptiness*, not a skipped test.

Purpose: matriz is the one hierarchy that already had `empty()` before this phase (Phase 29 shipped it), so this is the one base where the change is `__bool__` alone and the temptation to "tidy up while we're in here" is highest and most dangerous. And ambito/wallets are where a zero silently becomes a false clean if it is not asserted with its reason — the exact failure mode this milestone exists to eliminate.

Output: `_SafeModel.__bool__` and `UnknownFrame.__bool__` in matriz, three new `tests/test_null_object.py` files, and 2 new wrong-type tests each in matriz's and ambito's `tests/test_decode.py`.

No `_decode.py` byte moves. The canonical digest is untouched; all 4 v1.6 gates must be green at plan end.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-CONTEXT.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-PATTERNS.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol / file | Owner plan |
|---|---|
| `matriz_client.models._SafeModel.__bool__()` (`empty()` pre-exists from Phase 29) | **35-04 [this plan]** |
| `matriz_client.models.UnknownFrame.__bool__()` (hand-written, D-08) | **35-04 [this plan]** |
| `packages/matriz-client/tests/test_null_object.py` | **35-04 [this plan]** |
| `packages/ambito-financiero-client/tests/test_null_object.py` | **35-04 [this plan]** |
| `packages/wallets-client/tests/test_null_object.py` | **35-04 [this plan]** |
| `test_wrong_typed_list_field_still_reports_type` ×2 (matriz, ambito) | **35-04 [this plan]** |
| `test_strict_mode_still_raises_on_a_wrong_typed_list` ×2 (matriz, ambito) | **35-04 [this plan]** |
| higyrus base + suite + wrong-type tests | 35-01 (tracer) |
| D-17 ledger `35-RETIRED-TRIPLES.md` | 35-02 |
| iol + market-data bases, suites, wrong-type tests | 35-03 |
| `_decode.py` EDIT 1 + EDIT 2 ×5, rewritten `# Phase 35, NOBJ-02:` comment ×5, the 11 inverted assertions, recomputed `CANONICAL_DIGEST` | 35-05 |
</artifacts_this_phase_produces>

<flagged_assumptions>
| Requirement | Probe verdict | Resolved by | Residual risk |
|---|---|---|---|
| NOBJ-01 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 1 + CONTEXT D-05, D-06, D-08, D-15 → the `must_haves.truths` rows above. | matriz declares nearly every field `Optional` with a default, so 17 of the 18 classes RESEARCH F-3 flagged have NO scalar field to perturb. The `None`-first branch of `_perturb` is the only reason the truthy half is non-vacuous here; if a future class defeats it, the helper raises by name rather than passing silently. |
| NOBJ-02 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 2 + D-01..D-04, D-13, D-14. This plan owns the falsification half for matriz and ambito. | Green day-0; their value is as a tripwire for 35-05. |

Additional flag, specific to this plan: `matriz-client` is blocked from live verification by the
remarkets-only policy assert (`LIVE-MATZ-33`) and that block is **not routed around** here. Nothing
in this plan needs live traffic — the entire scope is offline model semantics — so the block costs
nothing now. It is named so the Phase 39 planner does not discover it fresh.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: matriz — _SafeModel.__bool__, UnknownFrame.__bool__, the 17-class suite, and the two wrong-type tests</name>

  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/tests/test_null_object.py, packages/matriz-client/tests/test_decode.py</files>

  <read_first>
    - `packages/higyrus-client/tests/test_null_object.py` (as 35-01 left it — the source of the port)
    - `packages/matriz-client/src/matriz_client/models.py:186-255` — the `_SafeModel` mixin at `:192`, its `from_api` at `:199-236` (note the `empty_classmethod` early-return branch at `:223-228`, which is why `empty()` must never be expressed through `from_api` here — it would recurse without end), and the pre-existing `empty()` at `:238-250`
    - `packages/matriz-client/src/matriz_client/models.py:500-534` — `UnknownFrame` at `:505`, its class docstring ending at `:516` with a sentence counting its hand-written methods, its two undocstringed classmethods at `:522-530`, and the `PrimaryWsMessage` union at `:533`
    - `packages/matriz-client/tests/test_decode.py` — read the module header, the autouse decode-context fixture, the local `_divergences` helper, and the fixture dataclasses; then `:330-350` (the list test at `:335` — 35-05 inverts it, do not touch) and `:1250-1270` (the model-site wrong-type test at `:1255` — the structural model for the new list-site test)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Findings F-3 (17/17 matriz classes perturbable ONLY with the `None`-first branch) and §Anti-Patterns (the "never harmonize" rule)
    - `.planning/milestones/v1.6-phases/29-decoder-observable/29-SEMANTICS-MATRIX.md` § "Never harmonize" — read it before touching this file, because matriz is the package whose cells differ most and it is the one most likely to be "corrected" by accident
  </read_first>

  <behavior>
    - `test_every_shipped_model_is_falsy_when_empty` / `..._truthy_when_populated` / `test_empty_emits_nothing` parametrize over exactly the 17 `_SafeModel` subclasses declared in matriz's own models module.
    - `test_the_model_roster_is_not_vacuous` asserts `>= 17`.
    - Two dedicated, non-parametrized cases cover `UnknownFrame`, which is deliberately outside the roster because it does not inherit the base: `bool(UnknownFrame.empty()) is False`, and an instance built from a payload carrying a frame type is truthy.
    - `test_property_aliases_are_invisible_to_get_type_hints` and `test_adding_a_property_alias_does_not_change_the_divergence_count` hold, built on matriz's frozen-without-slots dataclass convention.
    - `test_wrong_typed_list_field_still_reports_type` and `test_strict_mode_still_raises_on_a_wrong_typed_list` appended to `tests/test_decode.py`, green against the unedited walker.
  </behavior>

  <action>
First the source edit, and keep it surgical. In `packages/matriz-client/src/matriz_client/models.py`, add `__bool__` to `_SafeModel` immediately after the existing `empty()` at `:238-250`. Its body is the single expression `return self != type(self).empty()` — the D-06 body, identical to the other three bases. It is annotated `-> bool`. Do **not** touch `from_api`, do **not** touch `empty()`, do **not** re-docstring anything that already exists in this class: matriz's cells in the 6-way semantics matrix are declared policy axes, and an edit that tidies one of them in passing is a silent behaviour break on a published wheel.

Add a second, hand-written `__bool__` to `UnknownFrame` after its `empty()` at `:528-530` (D-08). This class does not inherit `_SafeModel`, is a member of the public `PrimaryWsMessage` union, and already carries its own `empty()`, so the body is the same expression and the semantics match the base — the point is that `if frame:` reads the same for every variant of the union. Match this class's LOCAL convention, not the base's: its classmethods are undocstringed because the class docstring carries the contract, so the new method is undocstringed too. Then update the class docstring's final sentence, which currently counts the hand-written methods as two — it is three now. That sentence is prose in a class docstring, and this file's docstrings are not part of any hashed body, so no cross-copy coordination is needed.

The 7 request dataclasses in market-data and every input-shaped dataclass anywhere are out of scope; nothing else in this file changes.

Then port the suite. Copy `packages/higyrus-client/tests/test_null_object.py` to `packages/matriz-client/tests/test_null_object.py` — copy, never import cross-package. Adaptations: swap the package import and the `caplog.at_level` logger-name string to `matriz_client`; point the roster filter at `models._SafeModel` (the base is private here — the leading underscore is intentional and the filter should reference it directly rather than duck-typing around it, because this test lives inside the package); raise the non-vacuity floor to `>= 17` citing RESEARCH F-1's measured count; and build the alias fixtures as `@dataclass(frozen=True)` WITHOUT `slots=True`, because that is matriz's convention and the criterio-5 invariant needs to be proven on the shape this package actually ships.

Add the two `UnknownFrame` cases as standalone tests, not parametrize entries, with a docstring explaining why the class sits outside the roster (it does not inherit the base but is a public union member, so `if frame:` must still read consistently — D-08). This is the one place where a class outside the enumeration is covered by name, and the reason has to be written down or a future reader will "fix" the roster filter to swallow it.

Keep the `_perturb` helper byte-for-byte as ported, `None` branch first. RESEARCH F-3 measured that all 17 matriz classes are perturbable only because of that branch — matriz declares nearly everything `Optional` with a default, so a helper that starts at `str` covers nothing here and passes vacuously.

Finally append the two wrong-type list tests to `packages/matriz-client/tests/test_decode.py`, next to the existing model-site wrong-type test at `:1255`. Read matriz's local helpers before writing: this module's idiom differs from higyrus's — several of its tests drive `from_api` directly and assert with membership rather than equality. For the NEW tests use full three-tuple equality against a one-element list anyway (membership would tolerate a spurious extra record from a mis-scoped 35-05 edit), assert the returned field is `[]`, and in the strict-mode test assert on the raised exception's field-path and kind attributes rather than only its type. Modify no existing test — 35-05 owns the inversions at `:335` and `:1234`, and the one at `:1234` is the copy that is structurally unlike the other four.
  </action>

  <verify>
    <automated>uv run pytest packages/matriz-client -q && uv run mypy packages/matriz-client/src && uv run ruff check packages/matriz-client && uv run ruff format --check packages/matriz-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/matriz-client/tests/test_null_object.py -q` exits 0.
    - `uv run pytest packages/matriz-client/tests/test_null_object.py --collect-only -q | grep -c "test_every_shipped_model_is_falsy_when_empty"` returns `17`.
    - The same collect-only output contains at least two test ids naming `UnknownFrame` outside the parametrized roster.
    - `grep -c "def __bool__" packages/matriz-client/src/matriz_client/models.py` returns `2` — one on the base, one on `UnknownFrame`.
    - `grep -c "return self != type(self).empty()" packages/matriz-client/src/matriz_client/models.py` returns `2`.
    - `grep -c "def empty" packages/matriz-client/src/matriz_client/models.py` returns the SAME value as `git show HEAD:packages/matriz-client/src/matriz_client/models.py | grep -c "def empty"` — no `empty()` was added, removed, or duplicated; the Phase 29 constructor is untouched.
    - `git diff -U0 packages/matriz-client/src/matriz_client/models.py | grep -c "^-" ` returns `2` or fewer, and every removed line belongs to the `UnknownFrame` class-docstring sentence being re-counted — nothing in `from_api` or `empty()` was rewritten.
    - `uv run pytest packages/matriz-client/tests/test_decode.py -q -k "wrong_typed_list"` exits 0 and reports 2 passing.
    - `git diff --numstat packages/matriz-client/tests/test_decode.py` shows `0` deletions.
    - `uv run pytest packages/matriz-client -q` exits 0 — the whole matriz suite including the WebSocket and daemon-thread paths is unbroken.
  </acceptance_criteria>

  <done>matriz's 17 shipped models plus `UnknownFrame` all answer the truthiness question correctly, the Phase 29 `empty()` is provably untouched, and both wrong-type tests pass against the unedited walker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ambito — assert the zero roster as a property, prove the alias invariant, pin the wrong-type half</name>

  <files>packages/ambito-financiero-client/tests/test_null_object.py, packages/ambito-financiero-client/tests/test_decode.py</files>

  <read_first>
    - `packages/ambito-financiero-client/src/ambito_financiero_client/models.py` (whole file — the deliberately-empty module: prose explaining the dormant walker and the dead-weight-on-a-wheel argument, `from __future__ import annotations`, and an empty `__all__`)
    - `packages/ambito-financiero-client/tests/test_decode.py:1-140` — the module docstring's "dormant is not the same as unverified" argument, the autouse `_pristine_decode_context` fixture, the `_CONTRACT_KEYS` tuple, and the module-local `_Model` / `_Leaf` / `_Scalars` fixture family that gives this package a walker-exercising surface without a `models.py` base
    - `packages/ambito-financiero-client/tests/test_decode.py:345-375` — the list test at `:354` (35-05 inverts it, do not touch) and the "Divergence class 2 — wrong type" section header that follows, which is where the new tests belong
    - `packages/higyrus-client/tests/test_null_object.py` (35-01's file — the source for the alias-invariant pair)
    - `.planning/STATE.md` § Accumulated Context, the `[Phase 32]` and `[Phase 33 / 33-07]` entries on non-vacuous zero floors and on restating (not suppressing) a property when the file-absence proxy stops being the right assertion
  </read_first>

  <behavior>
    - `test_the_package_declares_no_response_models_by_decision` — parse or introspect `ambito_financiero_client.models` and assert THREE things: zero class definitions, `__all__ == []`, and no import beyond the `__future__` flag. Cite D-05 in the docstring.
    - `test_property_aliases_are_invisible_to_get_type_hints` and `test_adding_a_property_alias_does_not_change_the_divergence_count` — the criterio-5 pair, built on this package's module-local `_Model` fixture shape.
    - `test_wrong_typed_list_field_still_reports_type` and `test_strict_mode_still_raises_on_a_wrong_typed_list` appended to `tests/test_decode.py`, green against the unedited walker.
  </behavior>

  <action>
Create `packages/ambito-financiero-client/tests/test_null_object.py`.

The roster assertion here is the delicate part and the reason this task exists separately. ROADMAP criterio 1 says "the six packages"; ambito's contribution is an empty enumeration, and an empty `parametrize` list is silently skipped by pytest — a green that inspects nothing. So instead of a parametrize, assert the emptiness as a positive structural property of the module: zero class definitions, `__all__` equal to the empty list, and imports limited to the `__future__` flag. Do NOT write a `>= 0` bound anywhere; a `>= 0` is the vacuous green this assertion exists to prevent, and the repo has a signed precedent for that exact distinction. The docstring must state the reason for the emptiness rather than only the fact: this package's single endpoint returns a scraped decimal parsed straight into a float, so there is no envelope to model, and adding a base with no subclass would be dead weight on a published wheel (D-05). Cross-reference the module docstring in `src/.../models.py`, which already carries the argument — the test pins the property, the module explains it.

Then add the criterio-5 alias pair. This is worth doing here even though ambito has no shipped base, and the package's own suite already made the argument: its walker copy is dormant BY DESIGN, and its module-local fixtures exist precisely so the dormant copy is exercised anyway. Build the two fixtures on this module's `_Model`-shaped convention (a frozen slotted dataclass whose `from_api` delegates to `walk_model`), one with a read-only `@property` alias beside its wire fields and one without, and assert both that `get_type_hints()` returns exactly the declared fields and that the two classes produce equal divergence record lists on the same payload. Equality between the two — never an absolute record count — so the test survives 35-05's disposition change unchanged.

Finally append the two wrong-type list tests to `packages/ambito-financiero-client/tests/test_decode.py`, inside the existing "Divergence class 2 — wrong type" section. Drive the module's `list[Model]`-carrying fixture through its local `_walk` helper, assert the full three-tuple against a one-element list, assert the returned field is `[]`, and in the strict-mode test assert on the raised exception's field-path and kind attributes. Note the line offsets in this package are shifted relative to the other four (its module docstring is longer), so navigate by symbol name and not by the line numbers quoted for higyrus. Modify no existing test — 35-05 owns the inversions at `:354` and `:1272`.
  </action>

  <verify>
    <automated>uv run pytest packages/ambito-financiero-client -q && uv run mypy packages/ambito-financiero-client/src && uv run ruff check packages/ambito-financiero-client && uv run ruff format --check packages/ambito-financiero-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/ambito-financiero-client/tests/test_null_object.py -q` exits 0 and reports at least 3 tests passing (the roster property plus the alias pair) — never `0 selected` or `no tests ran`.
    - `grep -c ">= 0" packages/ambito-financiero-client/tests/test_null_object.py` returns `0` — the zero is asserted as a property, not as a bound that anything satisfies.
    - `grep -c "D-05" packages/ambito-financiero-client/tests/test_null_object.py` returns at least `1` — the reason for the emptiness is cited, not merely implied.
    - `git diff --exit-code packages/ambito-financiero-client/src/` exits 0 — no base was added to make the roster look non-empty.
    - `uv run pytest packages/ambito-financiero-client/tests/test_decode.py -q -k "wrong_typed_list"` exits 0 and reports 2 passing.
    - `git diff --numstat packages/ambito-financiero-client/tests/test_decode.py` shows `0` deletions.
    - `uv run pytest packages/ambito-financiero-client -q` exits 0.
  </acceptance_criteria>

  <done>ambito's emptiness is an asserted property with its reason cited, the criterio-5 alias invariant is proven on its dormant walker copy, and both wrong-type tests pass against the unedited walker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wallets — assert the zero roster and the import-free property, with the exemption cited</name>

  <files>packages/wallets-client/tests/test_null_object.py</files>

  <read_first>
    - `packages/wallets-client/src/wallets_client/models.py` (whole file — the deliberately-empty, deliberately import-free module and the reasoning: no `_decode.py` exists in this package, so a copied base would raise on import and redden all twelve wallets CI matrix legs)
    - `packages/wallets-client/tests/` — the four files that exist today (`conftest.py`, `test_client.py`, `test_async_client.py`, `test_surface_parity.py`); read `conftest.py` and the header of one test module to match this package's local conventions, which are thinner than the other five
    - `packages/ambito-financiero-client/tests/test_null_object.py` (as Task 2 left it — the source for the roster-property assertion; the alias pair does NOT port here, see below)
    - `.planning/STATE.md` § Accumulated Context, the `[Phase 31]` and `[Phase 32]` entries recording why wallets stays out of the decode-intactness roster and out of import-linter `root_packages` for structural reasons
  </read_first>

  <behavior>
    - `test_the_package_declares_no_response_models_by_decision` — zero class definitions in `wallets_client.models`, `__all__ == []`.
    - `test_the_models_module_imports_nothing_beyond_the_future_flag` — the import discipline is load-bearing here, not stylistic: this package has no walker to import, and a base copied in for cosmetic uniformity would fail at package-import time.
    - No alias-invisibility test and no wrong-type test in this package: it has no `_decode.py`, so there is no walker copy to exercise. That absence is asserted, not left implicit.
  </behavior>

  <action>
Create `packages/wallets-client/tests/test_null_object.py` with a module docstring stating what it pins and why this package's contribution to ROADMAP criterio 1 is an asserted emptiness rather than an enumeration (D-05).

Port the roster-property assertion from ambito's file: zero class definitions in `wallets_client.models` and `__all__` equal to the empty list. Again, no `>= 0` bound anywhere.

Add the import-discipline assertion, which is specific to this package and is the more load-bearing of the two: assert that the models module's imports are limited to the `__future__` flag. State the consequence in the docstring in your own words — this package carries no walker module, so a base copied here to make the six packages look more uniform than they are would fail at import time and take every one of this package's CI matrix legs with it. A cosmetically-uniform package that cannot be imported trades a documented gap for a hidden outage.

Add a third test asserting the ABSENCE of a walker module in this package, so that the reason the other two tests are the whole of wallets' contribution is itself executable rather than a comment. Assert it by attempting the import and expecting failure, or by checking the package directory listing — either is acceptable, but the assertion must fail loudly if a `_decode.py` ever appears here without this file being revisited, because that is precisely the day wallets stops being exempt.

Do NOT port the alias-invisibility pair here and do NOT port the wrong-type tests: both require a walker copy this package does not have. The absence is covered by the third test above.

Change nothing under `packages/wallets-client/src/`.
  </action>

  <verify>
    <automated>uv run pytest packages/wallets-client -q && uv run ruff check packages/wallets-client && uv run ruff format --check packages/wallets-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/wallets-client/tests/test_null_object.py -q` exits 0 and reports exactly 3 tests passing — never `no tests ran`.
    - `grep -c ">= 0" packages/wallets-client/tests/test_null_object.py` returns `0`.
    - `grep -c "D-05" packages/wallets-client/tests/test_null_object.py` returns at least `1`.
    - `git diff --exit-code packages/wallets-client/src/` exits 0 — the deliberately-empty module is untouched.
    - `test ! -f packages/wallets-client/src/wallets_client/_decode.py` — the exemption still holds; if this ever becomes false, the third test must have reddened first.
    - `uv run pytest packages/wallets-client -q` exits 0 across the package's existing suite.
  </acceptance_criteria>

  <done>wallets' contribution to criterio 1 is three executable assertions — empty roster, import-free module, absent walker — each with its reason cited, and nothing was added to `src/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party JSON / WebSocket frame → `walk_model` / `walk_field` | The workspace's input-validation layer (ASVS V5). Untouched: no `_decode.py` byte moves in this plan. matriz's WebSocket daemon thread reaches the same walker via its own per-frame decode scope, also untouched. |
| divergence record → package logger | ASVS V7 log sink; `_safe_key` neutralisation and the key-length cap unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Tampering | Accidental rewrite of matriz's Phase-29 `empty()` / `from_api` while adding `__bool__` | mitigate | Enforced by two acceptance checks: the `def empty` count must equal the count at `HEAD`, and `git diff -U0` on the file must show at most two removed lines, all in the `UnknownFrame` class-docstring sentence. The "Never harmonize" rule is required reading in `read_first`. |
| T-35-10 | Denial of service | A `SafeModel` base copied into `wallets-client` for cosmetic uniformity | mitigate | Prohibited by a `must_haves.prohibitions` row and enforced by `git diff --exit-code packages/wallets-client/src/`. This package has no walker module; the copied base would raise at package-import time and take all twelve wallets CI legs down. |
| T-35-11 | Repudiation | A zero roster reading as "already clean" | mitigate | Both empty packages assert emptiness as a positive structural property (zero class definitions, empty `__all__`, import discipline, absent walker) with the reason cited, and a `>= 0` bound is grep-prohibited in both new files. |
| T-35-12 | Information disclosure | `UnknownFrame.raw` — the documented escape hatch preserving unmodelled frame payloads | accept | Out of scope here: this plan adds truthiness to the class, it does not change what `raw` holds or who can read it. The single documented exemption for that field is Phase 37's subject. |
| T-35-SC | Tampering | pip / uv dependency installs | accept | No installs. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` clean at plan end. |
</threat_model>

<verification>
Never run bare `uv run pytest` (RESEARCH Pitfall 5 / F-9).

1. `uv run pytest packages/matriz-client -q`, `uv run pytest packages/ambito-financiero-client -q`, `uv run pytest packages/wallets-client -q` — all green.
2. `uv run pytest packages -q` — green.
3. `uv run ruff check packages/ && uv run ruff format --check packages/` — clean.
4. `uv run mypy` and `uv run mypy packages/market-data-client/src` — both `Success`.
5. The 4 v1.6 gates, all still green (no `_decode.py` byte moved):
   `uv run python tools/check_decode_intactness.py`,
   `uv run python tools/check_uniform_structure.py`,
   `uv run python tools/check_surface_types.py`,
   `uv run python tools/surface_parity.py`.
6. `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` — byte-identical. matriz IS one of the four snapshotted packages (65 symbols), so this is a real check here and not a formality (D-12, RESEARCH F-8).
7. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` — clean (T-35-SC).
</verification>

<success_criteria>
- matriz's `_SafeModel` and `UnknownFrame` each expose `__bool__` with the verbatim D-06 body; matriz's Phase-29 `empty()` is provably byte-unchanged and the `UnknownFrame` class docstring now counts three hand-written methods.
- matriz's suite collects 17 parametrized truthiness cases plus dedicated `UnknownFrame` cases.
- ambito and wallets each assert their empty roster as a positive structural property with D-05 cited, and neither `src/` tree changed.
- wallets additionally asserts the import-free module and the absent walker module.
- The criterio-5 alias pair passes in matriz and ambito (5 of 6 packages total across the phase; wallets genuinely cannot, and that is asserted).
- Four new wrong-type tests (2 matriz, 2 ambito) pass against the unedited walker.
- `pytest packages`, ruff, both mypy invocations, the 4 gates and the snapshot byte-identity check are green; both touched `test_decode.py` diffs show zero deletions.
</success_criteria>

<output>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-04-SUMMARY.md` when done.
Record the matriz roster count, the exact `git diff -U0` removed-line count for matriz's `models.py`
(the evidence that `empty()` was not touched), the shape of the ambito/wallets zero assertions, and a
one-line statement that the canonical digest was NOT touched.
</output>
