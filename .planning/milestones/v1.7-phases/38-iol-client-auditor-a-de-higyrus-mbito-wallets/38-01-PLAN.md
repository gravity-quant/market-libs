---
phase: 38-iol-client-auditor-a-de-higyrus-mbito-wallets
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/iol-client/tests/test_models.py
  - packages/iol-client/tests/test_null_object.py
  - packages/iol-client/src/iol_client/models.py
  - verification/snapshots/iol-client-surface.txt
autonomous: true
requirements: [NOBJ-IOL-01]

must_haves:
  truths:
    - "A get_quote/historical payload whose `puntas` key is absent, `null`, or `[]` decodes to `quote.puntas == []` with zero divergence records (specless edge-probe NOBJ-IOL-01/empty — RESOLVED explicit; acceptance idiom from D-06, collapse behaviour measured in RESEARCH F-1)"
    - "A Titulo payload whose `puntas` key is absent or `null` decodes to a Punta instance for which `bool(titulo.puntas) is False` and `titulo.puntas == Punta.empty()` both hold (D-06 pair, never the compound predicate alone)"
    - "`titulo.puntas.precioCompra` and `quote.puntas[0].precioCompra` typecheck under `mypy --strict` with no None-narrowing guard and raise no AttributeError at runtime"
    - "A wrong-typed `puntas` value (a scalar where a list/model is declared) still emits a divergence record and still raises under strict_decode — only the null/absent case is silent (ASVS V5 boundary preserved, RESEARCH F-1)"
    - "The regenerated `verification/snapshots/iol-client-surface.txt` differs from its pre-change content by exactly two lines, and within those lines only the `puntas` annotation token changed — field order and every other field are byte-identical"
    - "`packages/iol-client/` still declares exactly 4 SafeModel subclasses and `test_null_object.py` carries zero logic changes (D-03)"
  artifacts:
    - path: "packages/iol-client/src/iol_client/models.py"
      provides: "Cotizacion.puntas declared list[Punta] and Titulo.puntas declared Punta, both non-Optional, both without a dataclass default"
      contains: "puntas: list[Punta]"
    - path: "packages/iol-client/tests/test_models.py"
      provides: "7 migrated assertions pinning the Null Object semantics, plus the renamed collapse test"
      contains: "Punta.empty()"
    - path: "verification/snapshots/iol-client-surface.txt"
      provides: "Regenerated public-surface freeze reflecting the two new annotations"
      contains: "puntas: 'list[Punta]'"
  key_links:
    - from: "packages/iol-client/src/iol_client/models.py"
      to: "packages/iol-client/src/iol_client/_decode.py"
      via: "the frozen NOBJ-02 collapse arms supply [] and Punta.empty(); models.py declares no dataclass default and no from_api override"
      pattern: "puntas: list\\[Punta\\]$"
    - from: "verification/snapshots/iol-client-surface.txt"
      to: "packages/iol-client/src/iol_client/models.py"
      via: "regen_snapshots.py transcribes the positional __init__ signature; the snapshot is regenerated, never hand-edited"
      pattern: "puntas: 'Punta'"
  prohibitions:
    - statement: "MUST NOT alter any committed live-wire evidence capture under `.planning/verification/schemas/` to make an assertion pass — the round-trip drift is absorbed in the EXPECTED value with a stated reason, never in the recorded wire observation"
      status: resolved
      verification: test
    - statement: "MUST NOT make the Null Object collapse absorb a wrong-typed wire value — silence is licensed only for null/absent on a non-Optional link; any other shape must keep emitting its divergence record"
      status: resolved
      verification: test
  edge_coverage:
    - requirement_id: NOBJ-IOL-01
      category: empty
      status: resolved
      verification: explicit
      note: "Resolved into truths[1] and truths[2] above using the D-06 acceptance idiom; behaviour measured in RESEARCH F-1 (null / absent / empty-list all collapse with zero divergences)"
    - requirement_id: NOBJ-IOL-01
      category: adjacency
      status: unresolved
      reason: "Not applicable — NOBJ-IOL-01 is a field-nullability retype, not an interval or range operation; there is no 'touching' or 'merging' semantics to specify. Carried forward as an explicit flagged assumption rather than force-fitted into a truth."
    - requirement_id: NOBJ-IOL-01
      category: ordering
      status: unresolved
      reason: "Not applicable — `Cotizacion.puntas` preserves whatever order the wire sends; this phase adds no sort, no dedup, and no tie-break. Carried forward as an explicit flagged assumption rather than force-fitted into a truth."
---

<objective>
Retype the two iol `puntas` links as Null Objects: `Cotizacion.puntas` becomes `list[Punta]`
and `Titulo.puntas` becomes `Punta`, both non-Optional and both without a dataclass default
(D-01), so `titulo.puntas.precioCompra` is always valid. This is the tracer slice for
NOBJ-IOL-01 — the annotation change plus the test migration that proves it plus the
regenerated public-surface snapshot, in one plan.

Purpose: closes the last `None` links in the iol decode chain. Every other artifact in this
phase (the CI ratchet in 38-02, the README callout in 38-03, the census in 38-04) is only
truthful once this lands, which is why it is wave 1 and everything else depends on it.

Output: `models.py` with two flipped annotations and rewritten provenance docstrings,
`test_models.py` with 7 migrated assertions and one renamed test, a one-sentence docstring
correction in `test_null_object.py`, and a regenerated surface snapshot.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-CONTEXT.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-RESEARCH.md
@.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-PATTERNS.md
@packages/iol-client/src/iol_client/models.py
@packages/iol-client/tests/test_models.py
</context>

## Artifacts this phase produces (plan 38-01)

Newly-created or renamed symbols introduced by this plan. Drift verification must treat
these as net-new, not as references to pre-existing code:

| Symbol | File | Kind |
|---|---|---|
| `test_puntas_nula_colapsa_a_lista_vacia` | `packages/iol-client/tests/test_models.py` | renamed test function (replaces the retired-semantics name at `:247`) |

No new classes, no new modules, no new public exports. `models.py` gains zero symbols —
only two annotations change and four docstring passages are rewritten.

<!-- planner-discipline-allow: test_puntas_nula_queda_nula -->

<reversibility>
| Task | Rating | Rationale |
|---|---|---|
| Task 1 (test migration) | two-way | Pure test edits; `git revert` restores them with no external consequence. |
| **Task 2 (annotation flip)** | **one-way** | Changes the published read surface of `iol-client`. Undo after Phase 40 tags the release would require a second breaking bump and a consumer migration. **No `checkpoint:decision` is inserted**: this exact break is already locked by CONTEXT D-01 and ROADMAP Phase 38 SC-1, and relitigating a locked decision is out of the planner's authority. Flagged here for visibility only. |
| Task 3 (snapshot regen) | two-way | The snapshot is a derived artifact; re-running `regen_snapshots.py` on a reverted tree restores it exactly. |
</reversibility>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — migrate the 7 breaking assertions, rename the retired-semantics test, and rewrite the 3 stale provenance docstrings</name>

  <files>packages/iol-client/tests/test_models.py, packages/iol-client/tests/test_null_object.py</files>

  <read_first>
    - `packages/iol-client/tests/test_models.py` — the file being edited; read the whole file, not only the cited lines. The already-correct sibling `test_puntas_lista_vacia_queda_vacia` (around line 243) is the naming and assertion template.
    - `packages/iol-client/tests/test_null_object.py` lines 105-140 and 218-235 — the `_perturb` docstring being corrected and the roster-floor assertion that must NOT be bumped.
    - `packages/iol-client/src/iol_client/models.py` — current annotations and `Punta` / `SafeModel.empty()` / `SafeModel.__bool__` definitions, so the replacement idiom is written against the real API.
    - `packages/iol-client/README.md` lines 150-165 — the already-published `to_dict()` lossiness caveat this task cites in the round-trip comment.
    - `.planning/phases/38-.../38-RESEARCH.md` F-3, F-4, F-5 — the exact line inventory and the measured post-change values.
  </read_first>

  <behavior>
    After this task the following 7 test functions FAIL with AssertionError against the
    unchanged `models.py` — that failure is the RED gate, and it is what task 2 turns green:

    - `test_from_api_historical_row_has_zero_divergences` (assertion at line 209) — now asserts the empty-list result instead of the retired identity
    - `test_from_api_empty_dict_yields_typed_zeros` (line 229) — same
    - `test_from_api_none_does_not_raise` (line 235) — same
    - `test_puntas_nula_colapsa_a_lista_vacia` (renamed; assertion at line 248) — same
    - `test_round_trip_reproduce_el_schema_committeado_de_serie_historica` (lines 353-355) — expected schema now overrides the `puntas` key to the empty list
    - `test_titulo_from_api_empty_dict_yields_typed_zeros` (line 412) — now asserts the Null Object pair
    - `test_titulo_puntas_nula_no_emite_registro` (line 441) — same

    Everything else in the file stays green: `test_puntas_lista_vacia_queda_vacia`,
    `test_puntas_poblada_construye_modelos_punta`, `test_titulo_puntas_es_singular_no_una_lista`,
    the two `is not None` assertions at lines 264 and 389 (left in place — RESEARCH A4:
    `warn_unreachable` is off, so they stay legal and dropping them is hygiene, not a
    requirement), and both sibling round-trips (`get-quote`, `get-instruments-by-type`),
    whose committed captures already record a non-null `puntas`.

    Collection must succeed: a NameError, an ImportError, or a collection error is a
    BROKEN red, not a RED.
  </behavior>

  <action>
Edit `packages/iol-client/tests/test_models.py`:

1. Replace the retired-semantics identity assertion at each of lines 209, 229, 235 and 248
   with the Cotizacion idiom from D-06: the decoded `puntas` equals the empty list. Use the
   spelling already present at line 244 in `test_puntas_lista_vacia_queda_vacia` verbatim.

2. Replace the assertion at line 412 and the one at line 441 with the Titulo idiom from
   D-06 — BOTH lines, always: `bool(titulo.puntas) is False` on one line and
   `titulo.puntas == Punta.empty()` on the next. Never the compound predicate on its own:
   a bare falsy check is satisfied by values that are not the Null Object, which is the
   regression this pair exists to catch (RESEARCH Pitfall 4). `Punta` is already imported
   in this file; confirm before adding an import.

3. Rename the test function at line 247 to `test_puntas_nula_colapsa_a_lista_vacia`. The
   old name encodes semantics this phase retires (D-04); a re-assertion under the old name
   leaves a false claim in the test roster. Its sibling at line 243 is the naming template.

4. Fix `test_round_trip_reproduce_el_schema_committeado_de_serie_historica` at lines
   353-355 (RESEARCH F-3 — this is the 7th breaking assertion, and it mentions `puntas`
   nowhere, which is why it is missing from CONTEXT D-04's enumeration). Build the expected
   value as the committed schema's first row with the single `puntas` key overridden to the
   empty list, and attach a comment naming the cause: `to_dict()` reproduces the DECLARED
   shape, not the received one, so a collapsed link cannot survive the round trip. Cite the
   already-published caveat in `packages/iol-client/README.md` around line 157. The
   measured post-change value of the `puntas` entry is the empty list (RESEARCH F-3);
   confirm it by running rather than trusting the number. **Do not touch**
   `.planning/verification/schemas/iol-client/get-historical-quotes.json` — it is a
   2026-06-06 live wire capture and the corpus of record.

5. Rewrite the one-line docstring of `test_from_api_historical_row_has_zero_divergences`
   (around line 202) and of `test_titulo_puntas_nula_no_emite_registro` (around line 437),
   per D-05. The `_divergences(caplog) == []` assertions STAY; only the provenance sentence
   changes. Today both cite the walker's Optional/Union early return as the source of the
   zero. After task 2 that branch no longer executes for these fields: the zero comes from
   the NOBJ-02 collapse arm — `_decode.py:448-452` for the list shape (Cotizacion) and
   `_decode.py:504-505` for the nested-model shape (Titulo). Name the correct arm in each.
   Leaving the old sentence is a false-provenance claim about a branch that no longer runs
   — the Phase 36 CR-02 defect class D-05 exists to prevent.

6. Update the module docstring bullets at lines 10 and 21 that enumerate `puntas` in its
   three observed wire forms. The three WIRE forms are unchanged; what changes is that the
   null form now decodes to the empty collapse rather than to a null attribute. State the
   decoded outcome, not just the wire inventory.

Edit `packages/iol-client/tests/test_null_object.py`:

7. Correct the one sentence inside the `_perturb` docstring at lines 113-118 which states
   as fact that Titulo's order-book field is declared with an optional wrapper (RESEARCH
   F-5 — a CONTEXT D-03 correction: the BEHAVIOURAL claim in D-03 is right and re-verified,
   the PROSE is not). Keep the rest of the docstring, keep the trailing nested-model branch,
   and explain it the way the current text already does — the branch is retained so the
   helper stays the same helper across the six paquetes. Change **no logic**. Do **not**
   bump the roster floor at line 226: iol still ships exactly 4 SafeModel classes (`Punta`,
   `Cotizacion`, `Instrumento`, `Titulo`) and this phase adds none (D-03).

Do not edit `models.py` in this task. Do not edit `client.py`, `aio.py`, `_core.py`,
`_decode.py`, `main_iol.py`, or `test_decode.py` — the local `_Leaf` fixture at
`test_decode.py:861-870` is already non-Optional and needs no change (D-04, RESEARCH F-2).
  </action>

  <verify>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/test_models.py -q 2>&1 | tail -20</automated>
    <automated>uv run --package iol-client pytest packages/iol-client/tests/test_null_object.py -q 2>&1 | tail -3</automated>
    <automated>uv run ruff check packages/iol-client/tests/test_models.py packages/iol-client/tests/test_null_object.py</automated>
  </verify>

  <acceptance_criteria>
    - `pytest packages/iol-client/tests/test_models.py -q` reports exactly `7 failed` and
      `0 error` — a collection error, an ImportError, or any count other than 7 means the
      migration is wrong, not that the RED gate fired.
    - Every one of the 7 failures is an `AssertionError`; none is a `NameError`,
      `AttributeError`, `TypeError`, or collection failure.
    - The 7 failing node IDs are exactly:
      `test_from_api_historical_row_has_zero_divergences`,
      `test_from_api_empty_dict_yields_typed_zeros`, `test_from_api_none_does_not_raise`,
      `test_puntas_nula_colapsa_a_lista_vacia`,
      `test_round_trip_reproduce_el_schema_committeado_de_serie_historica`,
      `test_titulo_from_api_empty_dict_yields_typed_zeros`,
      `test_titulo_puntas_nula_no_emite_registro`.
    - `pytest packages/iol-client/tests/test_null_object.py -q` still reports all-passed
      (docstring-only edit; the roster floor assertion is untouched).
    - `grep -c 'puntas == \[\]' packages/iol-client/tests/test_models.py` returns at least 5.
    - `grep -c 'Punta.empty()' packages/iol-client/tests/test_models.py` returns at least 2.
    - `grep -c 'bool(titulo.puntas) is False' packages/iol-client/tests/test_models.py`
      returns at least 2.
    - `grep -c 'def test_puntas_nula_colapsa_a_lista_vacia' packages/iol-client/tests/test_models.py`
      returns 1.
    - `grep -v '^\s*#' packages/iol-client/tests/test_models.py | grep -c 'test_puntas_nula_queda_nula'`
      returns 0 (the retired name is gone from the roster).
    - `git status --porcelain .planning/verification/schemas/` produces no output — the
      committed wire captures were not touched.
    - `git status --porcelain packages/iol-client/src/` produces no output — no source file
      moved in the RED commit.
    - `ruff check` is clean on both edited files.
  </acceptance_criteria>

  <done>
    7 tests fail with AssertionError against unchanged source, everything else in the iol
    suite is unchanged, the retired test name is gone, and no committed wire capture and no
    source file was touched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — flip the two annotations in models.py and rewrite the four provenance docstring passages</name>

  <files>packages/iol-client/src/iol_client/models.py</files>

  <read_first>
    - `packages/iol-client/src/iol_client/models.py` — the file being edited. Read the whole
      file: the two `Cotizacion` / `Titulo` class docstrings, the module docstring at lines
      12-20 and around line 119, the `Punta` class docstring around lines 157-169, and
      `SafeModel.__bool__` / `empty()` / `to_dict()` around lines 114-137.
    - `packages/market-data-client/src/market_data_client/models.py` around lines 15 and 187
      — the Phase 36 D-04 precedent for the exact same retype, and the prose framing to
      mirror (chained-access guarantee plus caller idiom).
    - `packages/iol-client/src/iol_client/_decode.py` lines 440-510 — READ ONLY. The two
      collapse arms whose line numbers the new docstrings cite. This file is byte-frozen and
      guarded by `tools/check_decode_intactness.py`; it must not appear in the diff.
    - `.planning/phases/38-.../38-PATTERNS.md` section "packages/iol-client/src/iol_client/models.py"
      — the surrounding field block that makes a dataclass default mechanically impossible.
  </read_first>

  <behavior>
    Flipping the two annotations turns all 7 RED tests from task 1 green with no other
    change: the frozen `_decode` walker already collapses null and absent values on a
    non-Optional list field to the empty list and on a non-Optional model field to an empty
    instance, in both cases without emitting a divergence record (RESEARCH F-1, measured).

    Post-flip, all of the following hold and are covered by the migrated tests:
    - Cotizacion with `puntas` null, absent, or `[]` → the empty list, zero divergences
    - Cotizacion with `puntas` populated → a one-element list of Punta, zero divergences
    - Titulo with `puntas` null or absent → a falsy Punta equal to `Punta.empty()`, zero divergences
    - Titulo with `puntas` a populated dict → a populated Punta (Phase 30 polymorphism intact)
    - `Cotizacion.from_api(None)` and `Titulo.from_api(None)` still emit exactly the outer
      terminal non-dict record, and no `puntas` record
    - A wrong-typed `puntas` (a scalar) still emits its divergence record and still raises
      under strict_decode — the input-validation boundary is not widened
  </behavior>

  <action>
Edit `packages/iol-client/src/iol_client/models.py` and nothing else.

1. At line 213 inside `Cotizacion`, drop the optional wrapper so the annotation reads
   `puntas: list[Punta]`. At line 301 inside `Titulo`, drop it so the annotation reads
   `puntas: Punta`.

2. Add **no** `field(default_factory=...)` and do **not** reorder any field (D-01).
   `Cotizacion.puntas` sits at position 16 of 20 with 4 no-default fields after it
   (`tendencia`, `ultimoPrecio`, `variacion`, `volumenNominal`); `Titulo.puntas` sits at
   position 14 of 20 with 6 no-default fields after it (`simbolo`, `tipoOpcion`,
   `ultimoCierre`, `ultimoPrecio`, `variacionPorcentual`, `volumen`). A default in either
   position raises `TypeError: non-default argument follows default argument`, and the only
   escape — reordering — would move the positional `__init__` signature frozen at
   `verification/snapshots/iol-client-surface.txt` lines 11 and 21. The field order is
   alphabetical by convention; do not disturb it. The "default `[]`" that SC-1 asks for is
   the walker's behaviour, not a Python default. Exact precedent: Phase 36 D-04 on
   `MarketDataSnapshot.entries`.

3. Rewrite the `Cotizacion` class-docstring bullet around lines 182-186 that currently
   lists `descripcionTitulo`, `plazo` and `puntas` together as optional-because-the-
   historical-series-sends-them-null. `puntas` leaves that list; `descripcionTitulo` and
   `plazo` stay in it. The separate bullet at line 186 recording that the `puntas` element
   shape is inobservado (D-02) STAYS unchanged. Add the new provenance in its place: the
   null and absent cases now take the NOBJ-02 collapse arm at `_decode.py:448-452` and
   yield the empty list without a divergence record.

4. Rewrite the mirror sentence in the `Titulo` class docstring around lines 267-269 the
   same way, citing the nested-model collapse arm at `_decode.py:504-505` and stating the
   Null Object guarantee: the attribute is always a `Punta`, falsy when the wire sent
   nothing, so `titulo.puntas.precioCompra` never raises.

5. Update the two module-docstring passages at line 15 and around line 119 that hedge the
   chained-access claim. Both can now be stated unconditionally, mirroring the framing in
   `market_data_client/models.py`: state the chained-access guarantee and give the caller
   idiom (branch on truthiness) instead of threading a null check.

Do **not** touch `client.py`, `aio.py`, `_core.py` or `_decode.py` (D-02, RESEARCH F-2):
both surfaces already delegate to the shared `_core.py` parsers, which call
`Cotizacion.from_api` / `Titulo.from_api`, and neither surface contains a single `puntas`
reference. Adding a mirrored edit "to satisfy D-NO-06" would duplicate decode logic and
reintroduce exactly the drift `tools/surface_parity.py` exists to prevent. Do not add a
`__post_init__` or a `from_api` override. Do not bump `__version__` or any
`pyproject.toml` version — Phase 40 owns that.
  </action>

  <verify>
    <automated>uv run --package iol-client pytest packages/iol-client -q 2>&1 | tail -5</automated>
    <automated>uv run mypy packages/iol-client</automated>
    <automated>uv run ruff check packages/iol-client &amp;&amp; uv run ruff format --check packages/iol-client</automated>
    <automated>uv run python tools/check_decode_intactness.py</automated>
    <automated>git diff --name-only -- packages/iol-client/src/iol_client/ | sort</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "puntas: list\[Punta\]$" packages/iol-client/src/iol_client/models.py`
      returns 1 (anchored: the annotation carries no trailing union arm).
    - `grep -c "puntas: Punta$" packages/iol-client/src/iol_client/models.py` returns 1.
    - `grep -c "default_factory" packages/iol-client/src/iol_client/models.py` returns the
      same value as before this task (no dataclass default was introduced).
    - `uv run --package iol-client pytest packages/iol-client -q` reports `289 passed`
      (the F-13 baseline restored — any lower count is a regression, and the count is
      unchanged because this plan renames a test rather than adding one).
    - `uv run mypy packages/iol-client` exits 0.
    - `uv run python tools/check_decode_intactness.py` exits 0 and `_decode.py` does not
      appear in `git diff --name-only`.
    - `git diff --name-only -- packages/iol-client/src/iol_client/` lists exactly one path:
      `packages/iol-client/src/iol_client/models.py`.
    - The `Cotizacion` docstring no longer groups the order-book field with the two fields
      that remain optional, and both rewritten class docstrings cite a `_decode.py` collapse
      arm rather than the Union early return.
    - `ruff check` and `ruff format --check` are clean on the package.
  </acceptance_criteria>

  <done>
    Both annotations are non-Optional with no dataclass default, the 7 RED tests are green,
    the full iol suite is back to its 289 baseline, mypy strict passes, the decode-intactness
    gate is green, and `models.py` is the only source file in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the public-surface snapshot and prove the whole-workspace floor is intact</name>

  <files>verification/snapshots/iol-client-surface.txt</files>

  <read_first>
    - `verification/regen_snapshots.py` — read the docstring before running it; it states
      the same-commit rule and which snapshot files it rewrites (there is no wallets and no
      market-data snapshot).
    - `verification/snapshots/iol-client-surface.txt` lines 1-25 — the 8-line header that
      must survive bit-for-bit and the two signature lines (11 and 21) that move.
    - `verification/test_public_surface.py` — the companion assertion that consumes the
      snapshot.
  </read_first>

  <action>
Run `uv run python verification/regen_snapshots.py`. The script rewrites all four snapshot
files (ambito-financiero, higyrus, iol, matriz); only the iol one may change.

Inspect the diff before committing. The expected change is exactly two lines: on line 11
the `Cotizacion` signature's `puntas` annotation loses its union arm and reads
`'list[Punta]'`, and on line 21 the `Titulo` signature's reads `'Punta'`. Field order,
every other field annotation, and the 8-line header must be byte-identical. Any third
changed line, or a change to a non-iol snapshot, means something outside this plan's blast
radius moved — stop and report rather than committing it.

Do not hand-edit the snapshot; it is a derived artifact and the header is bit-for-bit
validated. Commit the regenerated snapshot in the SAME commit as the source change from
task 2 (the script docstring's rule), amending task 2's commit if it was already made.

Then run the full workspace floor to prove nothing outside iol moved: the four package
suites, the three `tools/` gates, and the six per-package `surface_parity` tests. The
surface-parity leg is how SC-4's sync/async obligation is discharged for iol — vacuously
but correctly, since this plan changed no logic (RESEARCH F-2, F-13).
  </action>

  <verify>
    <automated>uv run python verification/regen_snapshots.py &amp;&amp; git diff --numstat -- verification/snapshots/</automated>
    <automated>uv run --package iol-client pytest packages/iol-client -q 2>&1 | tail -3 &amp;&amp; uv run --package higyrus-client pytest packages/higyrus-client -q 2>&1 | tail -3 &amp;&amp; uv run --package ambito-financiero-client pytest packages/ambito-financiero-client -q 2>&1 | tail -3 &amp;&amp; uv run --package wallets-client pytest packages/wallets-client -q 2>&1 | tail -3</automated>
    <automated>uv run python tools/check_decode_intactness.py &amp;&amp; uv run python tools/check_uniform_structure.py &amp;&amp; uv run python tools/check_surface_types.py</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --numstat -- verification/snapshots/` reports exactly one file
      (`verification/snapshots/iol-client-surface.txt`) with exactly `2` added and `2`
      deleted lines.
    - `grep -c "puntas: 'list\[Punta\]'" verification/snapshots/iol-client-surface.txt`
      returns 1 and `grep -c "puntas: 'Punta'" verification/snapshots/iol-client-surface.txt`
      returns 1.
    - `git diff -- verification/snapshots/iol-client-surface.txt | grep -c '^[+-]'` returns
      6 (2 changed lines each way, plus the 2 `+++`/`---` headers) — no third line moved.
    - The four package suites report `289 passed`, `289 passed`,
      `208 passed, 1 deselected`, and `10 passed` respectively (the F-13 baselines; any
      decrease is a regression).
    - `tools/check_decode_intactness.py`, `tools/check_uniform_structure.py` and
      `tools/check_surface_types.py` all exit 0; the surface-types gate still prints
      `0 violations` (the predicate is not widened until plan 38-02).
    - `pytest packages/iol-client/tests/test_surface_parity.py -q` passes.
    - `git diff --name-only -- uv.lock` produces no output.
  </acceptance_criteria>

  <done>
    The snapshot diff is exactly the two `puntas` annotation tokens, all four package suites
    are at or above their measured baselines, the three CI gates are green, and `uv.lock`
    has not moved.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream IOL wire JSON → `_decode.walk_model` | The only untrusted input crossing into this plan's blast radius. This plan changes what the walker is asked to produce for two fields; it does not change the walker. |
| repository working tree → CI lint/test jobs | Derived artifacts (the surface snapshot) and committed evidence (the wire schema captures) are consumed as ground truth by later phases. |

**No new attack surface.** This plan adds no network call, no auth path, no credential
read, no user-supplied input handler, no new dependency, and no new file that executes. It
changes two type annotations, seven test assertions, and four docstring passages. The
STRIDE register below is scoped honestly to that, at ASVS L1.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `_decode` input-validation boundary (ASVS V5) | mitigate | Silence stays licensed only for null/absent on a non-Optional link. A wrong-typed `puntas` keeps emitting its six-key divergence record and keeps raising under `strict_decode`. Proven by leaving `_decode.py` byte-frozen (`tools/check_decode_intactness.py` in task 3's verify) rather than by adding a coercion. |
| T-38-02 | Repudiation | `.planning/verification/schemas/iol-client/get-historical-quotes.json` | mitigate | The round-trip drift is absorbed in the EXPECTED value with a stated cause; the 2026-06-06 wire capture is not rewritten. Enforced by the `git status --porcelain .planning/verification/schemas/` criterion in task 1. |
| T-38-03 | Tampering | `verification/snapshots/iol-client-surface.txt` | mitigate | The snapshot is regenerated by `regen_snapshots.py`, never hand-edited, and the diff is bounded to exactly 2 lines / 2 tokens by an explicit acceptance criterion. |
| T-38-04 | Information Disclosure | `iol_client` logger (ASVS V7) | accept | The divergence channel is unchanged in shape and this plan emits strictly FEWER records than before (two link sites go silent on null). `verification/test_logging_no_token_leak.py` already pins that no credential reaches a log line and is untouched. The leak surface cannot grow. |
| T-38-05 | Spoofing / Elevation | iol OAuth flow, `IOL_USER` / `IOL_PASSWORD` | accept | Out of the blast radius: no auth code, no `.env` read, and no credential path is touched. Verified by the criterion that `models.py` is the only source file in the diff. |
| T-38-SC | Tampering | `uv`/PyPI supply chain | accept | Zero package-manager installs in this phase; RESEARCH's Package Legitimacy Audit is "not applicable" for that reason. Enforced by the `git diff --name-only -- uv.lock` criterion in task 3 — the lockfile is refreshed exactly once, in Phase 40. |
</threat_model>

<verification>
Whole-plan checks, run after task 3:

1. `uv run --package iol-client pytest packages/iol-client -q` → `289 passed`
2. `uv run mypy packages/iol-client` → exit 0
3. `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` → clean
4. `uv run python tools/check_decode_intactness.py` → exit 0
5. `uv run python tools/check_uniform_structure.py` → exit 0
6. `uv run python tools/check_surface_types.py` → `0 violations`
7. `git diff --name-only HEAD~..HEAD` names only: `packages/iol-client/src/iol_client/models.py`,
   `packages/iol-client/tests/test_models.py`, `packages/iol-client/tests/test_null_object.py`,
   `verification/snapshots/iol-client-surface.txt`

**Validation-map crosswalk** — this plan covers rows `38-02-01`, `38-02-02`, `38-03-01` and
`38-03-02` of the Per-Task Verification Map in `38-VALIDATION.md`. The draft map put the
test migration and the annotation flip in separate waves; they are folded into one plan
here because they are a single RED→GREEN cycle and splitting them across plans would commit
a red test suite across a wave boundary.
</verification>

<success_criteria>
- `Cotizacion.puntas` is `list[Punta]` and `Titulo.puntas` is `Punta`, both without a
  dataclass default and with the field order untouched (ROADMAP SC-1, D-01)
- `titulo.puntas.precioCompra` and `quote.puntas[0].precioCompra` pass `mypy --strict` and
  raise nothing with a payload that omits `puntas`, with `puntas: null`, and with the real
  polymorphic `puntas` resolved in Phase 30 (ROADMAP SC-1)
- The public-surface snapshot is regenerated and committed with the source change
- Four package suites green at their measured baselines with the three `tools/` gates
  active, and `surface_parity` green for all six packages (ROADMAP SC-4)
- No edit to `client.py`, `aio.py`, `_core.py`, `_decode.py`, `main_iol.py`,
  `packages/matriz-client/**`, `.planning/verification/schemas/**`, any version string, or
  `uv.lock`
</success_criteria>

<output>
Create `.planning/phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-01-SUMMARY.md` when done
</output>
