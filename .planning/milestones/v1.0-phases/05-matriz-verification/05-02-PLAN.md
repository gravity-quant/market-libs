---
phase: 05-matriz-verification
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - main_matriz.py
  - main_higyrus.py
  - packages/matriz-client/.env.example
autonomous: true
requirements: [MATZ-01, MATZ-02, MATZ-03, MATZ-05, MATZ-07]
tags: [verification, matriz-client, sync-only, driver, probes, env-example, refactor-higyrus]
must_haves:
  truths:
    - "main_matriz.py reescrito completamente: de smoke-test mínimo (57 líneas) a driver con ~25 probes nombrados en orden D-MATZ-29 con stdout verbatim `PROBE <name>: <status> <detail>` + `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N`"
    - "main_matriz.py es **sync-only**: NO contiene `asyncio.run`, NO contiene `_async_main`, NO importa `asyncio`/`contextlib` (per D-MATZ-30)"
    - "main_matriz.py al inicio invoca `require_env('matriz-client', ['PRIMARY_USER', 'PRIMARY_PASSWORD'])` y exit 0 si falta alguna (per D-MATZ-33 HARN-01)"
    - "main_matriz.py al inicio (después de require_env) ejecuta hostname assert remarkets D-MATZ-33: `if 'remarkets' not in primary.client._base_url: print('ABORT: ...', file=sys.stderr); sys.exit(1)`"
    - "Lista verbatim de probes en main_matriz.py per D-MATZ-29: probe_login_sync (MATZ-01), probe_get_segments / probe_get_all_instruments / ... / probe_get_account_report (~18 read probes MATZ-02), probe_field_type_map (MATZ-03), probe_error_bogus_symbol / probe_error_invalid_account / probe_error_malformed_cfi (MATZ-05), probe_schema_snapshot (DRIFT-01 mirror), probe_cycle_closure_<pkg> × 4 (DRIFT-02 — invocación de verify_cycle_closure de Plan 05-01)"
    - "main_matriz.py implementa cascade SKIPPED tras login failure D-MATZ-31: flag module-level `_auth_failed: bool = False` + `_auth_failure_reason: str`; cada probe downstream checkea al inicio `if _auth_failed: return ProbeResult(<name>, 'SKIPPED', f'auth failed: {_auth_failure_reason}')`"
    - "main_matriz.py implementa `_resolved_symbol` y `_resolved_segment` resolution flow D-MATZ-1 + D-MATZ-2: probe_get_segments setea `_resolved_segment = segments[0].marketSegmentId`; probe_get_all_instruments setea `_resolved_symbol = instruments[0].instrumentId.symbol`; override opcional via env `MATRIZ_SAMPLE_SYMBOL`"
    - "main_matriz.py implementa SKIPPED selectivo D-MATZ-3 + D-MATZ-4: 6 probes account-scoped (3 Risk + 3 order reads) SKIPPED si falta `PRIMARY_ACCOUNT`; 3 probes ID-scoped (get_order_status/history/by_exec_id) SKIPPED si falta `MATRIZ_SAMPLE_CL_ORD_ID`/`MATRIZ_SAMPLE_PROPRIETARY`/`MATRIZ_SAMPLE_EXEC_ID`"
    - "main_matriz.py implementa market-hours guard D-MATZ-5: probe_get_market_data inspecciona `LA.date` (epoch ms); si stale > 2h respecto a `time.time() * 1000` → finding NO-DATA OPEN + PASS-shape (no asserts de valor, solo shape/type/presence)"
    - "main_matriz.py implementa CFI sanity sweep D-MATZ-6: probe_get_instruments_by_cfi_ESXXXX (baseline con snapshot) + probe_get_instruments_by_cfi_sanity (los 8 CFI restantes con type-only assertions, sin snapshot por cada uno)"
    - "main_matriz.py implementa los 3 error probes always-on D-MATZ-22: bogus symbol (`'ZZZZZZ-NOT-A-SYMBOL'` en get_market_data), invalid account (`'INVALID-ACCT-XXXXX'` en get_active_orders), malformed CFI (`cast(CFICode, 'INVALID-CFI')` en get_instruments_by_cfi); cada uno distingue HTTP 4xx no mapeado (finding ERROR-MAP OPEN) de `{'status':'ERROR'}` mapeado (PASS) per D-MATZ-23"
    - "main_matriz.py invoca `probe_field_type_map` usando `from verification import diff_safemodel_bidirectional` sobre los 11 modelos `_SafeModel` matriz (Segment, Instrument, InstrumentDetail, Order, Trade, MarketDataSnapshot, Position, DetailedPosition, AccountReport — NewOrderResponse cubierto por mock-only Plan 05-03)"
    - "main_matriz.py invoca `probe_schema_snapshot` con envelope D-21 (6 keys: endpoint, client_function, captured_at, base_url, sample_params, schema) y D-25 no-overwrite-on-drift: si schema actual ≠ committed → finding SHAPE OPEN, NO sobreescribe baseline; archivos en `.planning/verification/schemas/matriz-client/<func>.json` (~11-19 archivos esperados, conteo variable según env vars opt-in)"
    - "main_matriz.py invoca `verify_cycle_closure` para los 4 paquetes verificados al final del run; emite `PROBE cycle_closure_<pkg>: PASS|FAIL`; si FAIL → finding ERROR-MAP OPEN en el pkg correspondiente"
    - "main_matriz.py emite finding EXPECTED terminal D-MATZ-27 prod-vs-remarkets al final via append_finding (class=SHAPE, status=EXPECTED, surface=sync); ES la última operación de append_finding sobre matriz-client en el run (Assumption A3)"
    - "main_matriz.py usa `safe_print(line, secrets=[PRIMARY_USER, PRIMARY_PASSWORD, _token])` D-MATZ-32 para emitir todos los `PROBE` lines + `SUMMARY`; `_token` se agrega dinámicamente tras probe_login_sync"
    - "main_higyrus.py refactoreado D-MATZ-20: la copia inline de `_diff_safemodel_bidirectional` (líneas 205-293) ELIMINADA; importa el helper promovido desde el barrel `from verification import diff_safemodel_bidirectional`; tests Phase 4 siguen verdes post-refactor"
    - "packages/matriz-client/.env.example actualizado D-MATZ-33: agrega 5 env vars opcionales con comentarios (`PRIMARY_ACCOUNT`, `MATRIZ_SAMPLE_SYMBOL`, `MATRIZ_SAMPLE_CL_ORD_ID`, `MATRIZ_SAMPLE_PROPRIETARY`, `MATRIZ_SAMPLE_EXEC_ID`)"
    - "Sin .env.example NO se rompe el cliente — las nuevas vars son OPCIONALES con SKIPPED selectivo handling"
    - "Driver pasa `uv run mypy main_matriz.py main_higyrus.py` strict + `ruff check .` + `ruff format --check .`"
    - "D-MATZ-7: `probe_get_all_instruments` y `probe_get_instruments_details` están AMBOS presentes en main_matriz.py — modelos diferentes (`Instrument` minimal vs `InstrumentDetail` con 18 fields y nested `Segment`) requieren 2 probes + 2 snapshots distintos para detectar field-drops por separado en cada uno"
    - "D-MATZ-21: `probe_field_type_map` aplica `diff_safemodel_bidirectional` sobre los 11 modelos `_SafeModel` matriz importando del barrel `from verification import diff_safemodel_bidirectional` (Segment, Instrument, InstrumentDetail recursivo en Segment + InstrumentId, Order recursivo en InstrumentId, Trade, MarketDataSnapshot recursivo en MarketDataLevel + MarketDataEntryValue, Position, DetailedPosition, AccountReport, NewOrderResponse mockeado)"
    - "D-MATZ-24: secuencia de los 3 error probes (bogus symbol / invalid account / malformed CFI) en main_matriz.py va DESPUÉS del happy-path sweep + field-type-map y ANTES de schema snapshots — si un error probe rompe state inesperadamente (throttling per-IP, error rate counter), el schema snapshot ya está generado y persistido"
  artifacts:
    - path: "main_matriz.py"
      provides: "Live verification driver, sync-only, ~25 named probes per D-MATZ-29, hostname-asserted remarkets, secrets-redacted, fid-numbered findings emission, cycle closure invocation × 4 packages"
      contains: "probe_login_sync"
      contains_also: "diff_safemodel_bidirectional"
      contains_also_2: "verify_cycle_closure"
      contains_also_3: "remarkets"
      contains_also_4: "PROBE"
      min_lines: 800
    - path: "main_higyrus.py"
      provides: "Phase 4 driver refactored to consume `diff_safemodel_bidirectional` from `verification` barrel (inline copy removed)"
      contains: "from verification import"
      contains_also: "diff_safemodel_bidirectional"
    - path: "packages/matriz-client/.env.example"
      provides: ".env template documenting 5 new opt-in env vars (PRIMARY_ACCOUNT, MATRIZ_SAMPLE_*)"
      contains: "PRIMARY_ACCOUNT"
      contains_also: "MATRIZ_SAMPLE_SYMBOL"
      contains_also_2: "MATRIZ_SAMPLE_CL_ORD_ID"
      contains_also_3: "MATRIZ_SAMPLE_PROPRIETARY"
      contains_also_4: "MATRIZ_SAMPLE_EXEC_ID"
  key_links:
    - from: "main_matriz.py::probe_field_type_map"
      to: "verification/safemodel_diff.py::diff_safemodel_bidirectional"
      via: "from verification import diff_safemodel_bidirectional"
      pattern: "diff_safemodel_bidirectional\\("
    - from: "main_matriz.py::main"
      to: "verification/cycle_report.py::verify_cycle_closure"
      via: "from verification.cycle_report import verify_cycle_closure (loop over 4 pkgs)"
      pattern: "verify_cycle_closure\\("
    - from: "main_matriz.py::probe_login_sync"
      to: "matriz_client.login"
      via: "primary.login() — sets _auth_failed flag on AuthenticationError"
      pattern: "primary\\.login\\(\\)"
    - from: "main_matriz.py::main"
      to: "verification/redaction.py::safe_print"
      via: "safe_print(line, secrets=[user, password, _token])"
      pattern: "safe_print\\("
    - from: "main_matriz.py::probe_get_positions"
      to: "matriz_client.client::_request"
      via: "_request('GET', path, auth_basic=_risk_auth()) — bypasses _get (X-Auth-Token); Pitfall 2 RESEARCH L640"
      pattern: "auth_basic="
    - from: "main_higyrus.py"
      to: "verification/safemodel_diff.py::diff_safemodel_bidirectional"
      via: "from verification import diff_safemodel_bidirectional (replaces inline copy lines 205-293)"
      pattern: "from verification import.*diff_safemodel_bidirectional"
    - from: "main_matriz.py::main"
      to: "sys.exit(1)"
      via: "if 'remarkets' not in primary.client._base_url: ABORT"
      pattern: "remarkets.*not in"
---

<objective>
Reescribir `main_matriz.py` desde el smoke-test mínimo actual (57 líneas: solo `get_segments` + `get_all_instruments`) a un **driver completo de verificación en vivo** con ~25 probes nombrados en orden D-MATZ-29, ejercitando MATZ-01 (auth), MATZ-02 (read sweep), MATZ-03 (bidirectional diff), MATZ-05 (3 error probes), MATZ-07 (market-hours guard), y la **invocación de `verify_cycle_closure` × 4 paquetes** para DRIFT-02. El driver es **sync-only** (no `asyncio`, no `_async_main`) — matriz no tiene `aio.py` por diseño.

Adicionalmente:
- **Refactor in-cycle de `main_higyrus.py`** (D-MATZ-20): reemplazar la copia inline de `_diff_safemodel_bidirectional` (líneas 205-293) por `from verification import diff_safemodel_bidirectional`. Tests Phase 4 (`packages/higyrus-client/tests/`) deben seguir verdes. Esto cierra el patrón "helper inline → barrel promotion" que Phase 4 D-HIGY-4 difirió condicionalmente a Phase 5.
- **`.env.example` update D-MATZ-33**: agregar 5 vars opcionales (`PRIMARY_ACCOUNT`, `MATRIZ_SAMPLE_SYMBOL`, `MATRIZ_SAMPLE_CL_ORD_ID`, `MATRIZ_SAMPLE_PROPRIETARY`, `MATRIZ_SAMPLE_EXEC_ID`) con comentarios explícitos de su rol (gate selectivo per D-MATZ-3 / D-MATZ-4).

Este plan **NO ejecuta el live run** — eso es Plan 05-03 (`uv run --package matriz-client python main_matriz.py` contra remarkets reales). Aquí se construye el código del driver, se verifica que importa todo correctamente, mypy strict + ruff verdes, y que `python main_matriz.py` con ENV vars vacías emite el `SKIPPED matriz-client: missing PRIMARY_USER, PRIMARY_PASSWORD` correcto (HARN-01 path).

Purpose: El driver es la pieza central de verificación en vivo de Phase 5. Sin él, Plan 05-03 no tiene qué ejecutar. Los ~25 probes son la única forma de descubrir divergencias entre el cliente sync (matriz no tiene async) y la API real de Primary/remarkets. El refactor de `main_higyrus.py` es la confirmación de que el helper promovido en Plan 05-01 es cross-package compatible (verificación final de la duck-typing decision Open Question #3). El `.env.example` documenta el contrato opt-in para que el operador configure el live run de Plan 05-03 correctamente.

Output: 3 archivos modificados (`main_matriz.py` rewrite, `main_higyrus.py` refactor, `.env.example` append), mypy + ruff verdes, `python main_matriz.py` exits clean cuando faltan env vars (HARN-01), `python main_matriz.py` no falla en `import` time (sintaxis válida + imports resuelven + barrel exports OK).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-matriz-verification/05-CONTEXT.md
@.planning/phases/05-matriz-verification/05-RESEARCH.md
@.planning/phases/05-matriz-verification/05-PATTERNS.md
@.planning/phases/05-matriz-verification/05-VALIDATION.md
@.planning/phases/05-matriz-verification/05-01-PLAN.md
@.planning/phases/04-higyrus-verification/04-02-PLAN.md
@.planning/phases/04-higyrus-verification/04-02-SUMMARY.md
@.planning/phases/03-iol-verification/03-02-PLAN.md
@.planning/phases/02-mbito-verification/02-02-PLAN.md
@.planning/codebase/INTEGRATIONS.md
@verification/__init__.py
@verification/findings.py
@verification/env_gate.py
@verification/mutation_gate.py
@verification/redaction.py
@verification/schema.py
@main_matriz.py
@main_higyrus.py
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/__init__.py
@packages/matriz-client/.env.example
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Append 5 opt-in env vars to packages/matriz-client/.env.example with comments (D-MATZ-33)</name>
  <files>packages/matriz-client/.env.example</files>
  <read_first>
    - packages/matriz-client/.env.example (estado actual: vars existentes `PRIMARY_USER`, `PRIMARY_PASSWORD`, `PRIMARY_BASE_URL`; verificar formato — si tiene comentarios `# ...` por var, mantener el estilo; si es solo `VAR=` por línea, ese estilo)
    - packages/higyrus-client/.env.example (líneas 8-21: HIGYRUS_SAMPLE_* pattern de opt-in vars con comentario sobre por qué son opcionales y qué probes se skippean si faltan — usar mismo estilo)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-33 verbatim lista las 5 vars opt-in + razones)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 8 .env.example APPEND L1059-1075 con texto verbatim recomendado)
  </read_first>
  <behavior>
    - `.env.example` actualizado contiene las 5 vars opcionales con comentarios `# ...` que explican: (a) cuál es opcional, (b) qué probes se gatean por cada una, (c) qué efecto tiene si está ausente (SKIPPED selectivo)
    - El archivo NO contiene valores reales (esto es un template, no un secret); cada var queda con `VAR=` (vacío) para que el operador la complete
    - Las vars existentes (`PRIMARY_USER`, `PRIMARY_PASSWORD`, `PRIMARY_BASE_URL`) NO se modifican
    - Sin estas vars (todas ausentes en `.env`), el driver Plan 05-03 corre con las 6 probes account-scoped SKIPPED + 3 ID-scoped SKIPPED + resto OK
  </behavior>
  <action>
    Modificar `packages/matriz-client/.env.example` agregando AL FINAL del archivo (después de las vars existentes) el bloque verbatim de 5 vars opt-in con comentarios.

    **Texto a agregar** (mirror RESEARCH L1059-1075 + 05-PATTERNS L1060-1075):

    ```
    # Optional — used by main_matriz.py driver (Phase 5)
    # PRIMARY_ACCOUNT: requerido SOLO para los 6 probes account-scoped (3 Risk API + 3 order reads).
    # Sin esta var, los 6 probes emiten SKIPPED; el resto del driver corre normal (D-MATZ-3).
    PRIMARY_ACCOUNT=

    # MATRIZ_SAMPLE_SYMBOL: override opcional del símbolo auto-resuelto desde get_all_instruments()[0]
    # Sin él, el driver resuelve dinámicamente el primer instrumento (D-MATZ-1).
    MATRIZ_SAMPLE_SYMBOL=

    # MATRIZ_SAMPLE_CL_ORD_ID / MATRIZ_SAMPLE_PROPRIETARY: opt-in para los 2 probes ID-scoped
    # (get_order_status, get_order_history). Sin ellos → SKIPPED (D-MATZ-4).
    MATRIZ_SAMPLE_CL_ORD_ID=
    MATRIZ_SAMPLE_PROPRIETARY=

    # MATRIZ_SAMPLE_EXEC_ID: opt-in para get_order_by_exec_id. Sin él → SKIPPED.
    MATRIZ_SAMPLE_EXEC_ID=
    ```

    **Restricciones:**
    - NO modificar las vars existentes (`PRIMARY_USER=`, `PRIMARY_PASSWORD=`, `PRIMARY_BASE_URL=`); el append va DESPUÉS
    - NO incluir valores de ejemplo (estás definiendo el template, no un `.env` real)
    - NO commitear un `.env` real — solo `.env.example`
    - El bloque inicia con `# Optional — used by main_matriz.py driver (Phase 5)` para delimitarlo del bloque pre-existente
    - El estilo de comentarios sigue convenciones de higyrus `.env.example` (multi-línea explicativo)
    - Si la última línea del archivo no termina con newline, agregar uno antes del bloque nuevo

    Commit sugerido: `docs(05-02): append 5 opt-in env vars to matriz .env.example (D-MATZ-33)`
  </action>
  <verify>
    <automated>grep -c "PRIMARY_ACCOUNT=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "MATRIZ_SAMPLE_SYMBOL=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "MATRIZ_SAMPLE_CL_ORD_ID=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "MATRIZ_SAMPLE_PROPRIETARY=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "MATRIZ_SAMPLE_EXEC_ID=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "PRIMARY_USER=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "PRIMARY_PASSWORD=" packages/matriz-client/.env.example</automated>
    <automated>grep -c "Optional.*main_matriz" packages/matriz-client/.env.example</automated>
    <automated>test -f packages/matriz-client/.env || echo "no leaked .env file"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/matriz-client/.env.example` contiene `PRIMARY_ACCOUNT=` (al menos 1 ocurrencia)
    - Source: contiene `MATRIZ_SAMPLE_SYMBOL=`, `MATRIZ_SAMPLE_CL_ORD_ID=`, `MATRIZ_SAMPLE_PROPRIETARY=`, `MATRIZ_SAMPLE_EXEC_ID=` (1 cada una)
    - Source: contiene comentario `# Optional — used by main_matriz.py driver (Phase 5)` (delimita el bloque nuevo)
    - Source: comentarios mencionan `D-MATZ-3`, `D-MATZ-4`, y `SKIPPED` para documentar el comportamiento
    - Source: vars existentes (`PRIMARY_USER=`, `PRIMARY_PASSWORD=`, `PRIMARY_BASE_URL=`) NO modificadas (siguen apareciendo 1 vez cada una)
    - Source: NO contiene valores reales después del `=` (template, no secret)
    - Source: las 5 vars nuevas están en el orden D-MATZ-33: PRIMARY_ACCOUNT, MATRIZ_SAMPLE_SYMBOL, MATRIZ_SAMPLE_CL_ORD_ID, MATRIZ_SAMPLE_PROPRIETARY, MATRIZ_SAMPLE_EXEC_ID
    - Behavior: `grep -c "PRIMARY_ACCOUNT=" .env.example` == 1
    - Behavior: `grep -c "MATRIZ_SAMPLE_" .env.example` == 4 (las 4 vars MATRIZ_SAMPLE_*)
    - Behavior: NO existe `packages/matriz-client/.env` real con secretos commiteados (gitignored per CLAUDE.md security policy)
  </acceptance_criteria>
  <done>.env.example actualizado con 5 vars opt-in + comentarios; vars existentes intactas; sin .env real leaked.</done>
</task>

<task type="auto">
  <name>Task 2.2: Refactor main_higyrus.py to import diff_safemodel_bidirectional from verification barrel (D-MATZ-20) — remove inline copy</name>
  <files>main_higyrus.py</files>
  <read_first>
    - main_higyrus.py (estado actual completo: ~2380 líneas. Sitios clave: líneas 89-93 imports stdlib usados solo por el helper inline (`from collections.abc import Iterator`, `from types import NoneType, UnionType`, `from typing import Any, Union, get_args, get_origin, get_type_hints`); líneas 96-97 imports actuales del barrel `from verification import require_env, safe_print, schema_of, write_findings` + `from verification.findings import append_finding`; líneas 205-293 los 4 helpers inline a remover (`_is_optional`, `_nested_safemodel_class`, `_is_list_of_safemodel`, `_diff_safemodel_bidirectional`); línea ~1755 invocación `for path, direction, key in _diff_safemodel_bidirectional(...)`. Verificar conteos exactos al leer)
    - verification/__init__.py (POST-Task 1.1 del Plan 05-01: barrel exporta `diff_safemodel_bidirectional` alfabéticamente)
    - verification/safemodel_diff.py (POST-Plan 05-01: helper público con duck typing; signature: `diff_safemodel_bidirectional(payload: Any, model_cls: type, path: str = '') -> Iterator[tuple[str, str, str]]`)
    - packages/higyrus-client/tests/test_client.py + test_async_client.py (12 sync + 5 async + 5 sync regressions + 5 async regressions de Phase 4 = ~27+ tests — verificar conteo final; tests Phase 4 no dependen del helper inline directamente — el helper se invoca solo desde el driver)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-20 verbatim)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 2 main_higyrus.py REFACTOR L502-557 con refactor pattern verbatim)
    - .planning/phases/04-higyrus-verification/04-02-SUMMARY.md (Phase 4 patterns + tests count final)
  </read_first>
  <behavior>
    - `main_higyrus.py` ya NO contiene la copia inline de `_diff_safemodel_bidirectional` (ni los 3 helpers privados `_is_optional`, `_nested_safemodel_class`, `_is_list_of_safemodel`)
    - El driver importa `diff_safemodel_bidirectional` desde el barrel: `from verification import (append_finding, diff_safemodel_bidirectional, require_env, safe_print, schema_of, write_findings)`
    - La invocación line ~1755 ahora llama `diff_safemodel_bidirectional(payload, model_cls, path=f".{root_name}")` (sin underscore)
    - Stdlib imports que solo servían al helper (e.g., `from collections.abc import Iterator`, `from types import NoneType, UnionType`, `from typing import Union, get_args, get_origin, get_type_hints`) — REMOVIDOS si no se usan en otra parte del archivo. **Excepción**: `from typing import Any` SE MANTIENE si se usa en otra parte (verificar al leer)
    - `uv run python main_higyrus.py` (sin env vars) emite `SKIPPED higyrus-client: missing HIGYRUS_USER, HIGYRUS_PASSWORD` (el path HARN-01 sigue funcionando — sin regresión)
    - `uv run pytest packages/higyrus-client/ -q` sigue verde (los tests no dependen del driver; el refactor del driver no debe afectar al cliente ni a sus tests)
    - mypy --strict pasa: la signature del helper promovido es idéntica; duck typing en el helper aplica vía `dataclasses.is_dataclass` + `from_api` (Phase 4 `SafeModel` cumple)
    - ruff line-length=100 + double quotes + format check verdes
  </behavior>
  <action>
    Modificar `main_higyrus.py` con **3 cambios precisos** preservando todo el resto del archivo (~2200 líneas restantes que NO se tocan).

    **Cambio 1 — Imports stdlib innecesarios al promover (líneas 89-93 aprox.):**

    Verificar al leer si los imports stdlib `from collections.abc import Iterator`, `from types import NoneType, UnionType`, `from typing import ..., Union, get_args, get_origin, get_type_hints` se usan EN OTRAS PARTES del archivo (e.g., `Iterator` en alguna anotación que no sea el helper inline). Si NO se usan en otra parte → REMOVERLOS. Si SE USAN en otra parte (probable que `Any` se use; verificar `Iterator` también) → mantener solo los necesarios.

    Heurística:
    - `Iterator` — probable que solo se use en el helper inline. Si grep en el archivo (excluyendo líneas 205-293) no muestra otras ocurrencias → remover el import
    - `NoneType`, `UnionType` — específicos al helper. Probable remover
    - `Union`, `get_args`, `get_origin`, `get_type_hints` — específicos al helper. Probable remover
    - `Any` — se usa ampliamente en signatures del driver. **Mantener**

    Documentar al SUMMARY qué imports finalmente se removieron.

    **Cambio 2 — Imports del barrel verification (líneas 96-97 aprox.):**

    ANTES:
    ```python
    from verification import require_env, safe_print, schema_of, write_findings
    from verification.findings import append_finding
    ```

    DESPUÉS (consolidar en una sola línea import del barrel, agregando `diff_safemodel_bidirectional` en orden alfabético):
    ```python
    from verification import (
        append_finding,
        diff_safemodel_bidirectional,
        require_env,
        safe_print,
        schema_of,
        write_findings,
    )
    ```

    El `from verification.findings import append_finding` se elimina porque `append_finding` ya está disponible vía el barrel. Verificar al leer que `append_finding` está en `verification/__init__.py __all__` (sí, está — el Plan 05-01 Task 1.1 lo confirmó).

    **Cambio 3 — Eliminar 4 helpers inline (líneas 205-293 aprox.):**

    Localizar las 4 funciones inline:
    - `def _is_optional(hint):`
    - `def _nested_safemodel_class(hint):`
    - `def _is_list_of_safemodel(hint):`
    - `def _diff_safemodel_bidirectional(payload, model_cls, path=""):`

    REMOVER todos los bloques completos (incluyendo docstrings y todo el cuerpo) — desde la línea donde empieza el primer `def _is_optional` hasta el final de `_diff_safemodel_bidirectional` (línea ~293).

    **Cambio 4 — Actualizar invocación (línea ~1755 aprox.):**

    Buscar la línea que llama `_diff_safemodel_bidirectional(payload, model_cls, ...)`. Reemplazar el nombre `_diff_safemodel_bidirectional` por `diff_safemodel_bidirectional` (sin underscore). El resto de la línea (args, sintaxis) queda idéntico.

    Sample del refactor (verbatim 05-PATTERNS L544-553):

    ANTES:
    ```python
    for path, direction, key in _diff_safemodel_bidirectional(
        payload, model_cls, path=f".{root_name}"
    ):
    ```

    DESPUÉS:
    ```python
    for path, direction, key in diff_safemodel_bidirectional(
        payload, model_cls, path=f".{root_name}"
    ):
    ```

    Si hay múltiples invocaciones (verificar con `grep -n "_diff_safemodel_bidirectional" main_higyrus.py` antes del edit), reemplazar TODAS.

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - Preservar `from __future__ import annotations` línea 1
    - mypy --strict debe pasar tras el refactor
    - ruff line-length=100, double quotes, 4 espacios
    - NO modificar ningún probe del driver (`probe_*` functions)
    - NO modificar `main()`, `_async_main()`, lifecycle, o cualquier otra parte del archivo
    - NO modificar tests Phase 4 (`packages/higyrus-client/tests/`)
    - NO modificar `verification/safemodel_diff.py` ni `verification/__init__.py` (Plan 05-01 los dejó bien)
    - El refactor debe ser **funcionalmente equivalente**: misma signature, mismo behavior. Phase 4 tests deben seguir verdes
    - Si el grep encuentra que `_diff_safemodel_bidirectional` aparece en docstrings/comentarios del archivo (no solo invocaciones de código), opcionalmente actualizar también la mención en docstrings — pero NO es load-bearing (los docstrings no afectan ejecución)

    Verificación inline post-edit (sanity grep):
    - `grep -c "_diff_safemodel_bidirectional" main_higyrus.py` debe ser **0** (todas las invocaciones renombradas, todas las definiciones removidas)
    - `grep -c "diff_safemodel_bidirectional" main_higyrus.py` debe ser >= 2 (1 import + 1+ invocación)
    - `grep -c "def _is_optional" main_higyrus.py` debe ser **0** (helper inline removido)
    - `uv run pytest packages/higyrus-client/ -q` debe pasar verde sin regresión

    Commit sugerido: `refactor(05-02): main_higyrus.py consumes diff_safemodel_bidirectional from verification barrel (D-MATZ-20)`
  </action>
  <verify>
    <automated>uv run mypy main_higyrus.py</automated>
    <automated>uv run ruff check main_higyrus.py</automated>
    <automated>uv run ruff format --check main_higyrus.py</automated>
    <automated>uv run pytest packages/higyrus-client/ -q</automated>
    <automated>uv run python -c "import main_higyrus; print('import OK')"</automated>
    <automated>grep -c "_diff_safemodel_bidirectional" main_higyrus.py</automated>
    <automated>grep -c "def _is_optional" main_higyrus.py</automated>
    <automated>grep -c "def _nested_safemodel_class" main_higyrus.py</automated>
    <automated>grep -c "def _is_list_of_safemodel" main_higyrus.py</automated>
    <automated>grep -c "from verification import" main_higyrus.py</automated>
    <automated>grep -c "diff_safemodel_bidirectional" main_higyrus.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_higyrus.py` NO contiene `def _is_optional(` (helper inline removido)
    - Source: NO contiene `def _nested_safemodel_class(` ni `def _is_list_of_safemodel(`
    - Source: NO contiene `def _diff_safemodel_bidirectional(` (helper inline removido)
    - Source: NO contiene `_diff_safemodel_bidirectional(` invocación (la línea ~1755 ahora llama el nombre público)
    - Source: contiene `from verification import` con `diff_safemodel_bidirectional` en la lista (orden alfabético)
    - Source: contiene `diff_safemodel_bidirectional(` invocación >= 1 vez (renamed call site)
    - Source: si removió `from collections.abc import Iterator`, ese símbolo NO debe usarse en otras partes del archivo (sin orphan import refs)
    - Source: preserva `from __future__ import annotations` línea 1
    - Source: NO modifica probes ni `main()`/`_async_main()` ni structure global
    - Behavior: `grep -c "_diff_safemodel_bidirectional" main_higyrus.py` == 0
    - Behavior: `grep -c "diff_safemodel_bidirectional" main_higyrus.py` >= 2 (1 import + 1+ invocación)
    - Behavior: `grep -c "def _is_optional" main_higyrus.py` == 0
    - Behavior: `uv run python -c "import main_higyrus"` exits 0 (imports resuelven)
    - Static: `uv run mypy main_higyrus.py` exits 0
    - Static: `uv run ruff check main_higyrus.py` exits 0
    - Static: `uv run ruff format --check main_higyrus.py` exits 0
    - Non-regression: `uv run pytest packages/higyrus-client/ -q` exits 0 — tests Phase 4 siguen verdes (helper promovido = misma signature + duck-typing = compatible)
  </acceptance_criteria>
  <done>main_higyrus.py refactoreado: helper inline removido (4 funciones líneas 205-293); imports consolidados via barrel; invocación usa nombre público sin underscore. Tests Phase 4 verdes. mypy + ruff verdes.</done>
</task>

<task type="auto">
  <name>Task 2.3: Rewrite main_matriz.py from smoke-test to full driver with ~25 named probes (D-MATZ-29) — Part A: module setup, helpers, login + 18 read-sweep probes</name>
  <files>main_matriz.py</files>
  <read_first>
    - main_matriz.py (estado actual: 57 líneas — smoke-test mínimo con solo `get_segments` + `get_all_instruments`. Confirmar al leer; se REESCRIBE completamente)
    - packages/matriz-client/src/matriz_client/client.py (POST Plan 05-01: `_unwrap` helper presente, 18 envelope sites refactoreados, `_token` raise RuntimeError, docstrings warnings; los 18 endpoints públicos con sus firmas exactas — esenciales para construir las invocaciones de los probes)
    - packages/matriz-client/src/matriz_client/client.py (helpers internos accesibles desde el driver: `_base_url`, `_token`, `_risk_auth()`, `_request()` — el driver los usa para retención de payload crudo bypassing `_get` cuando necesario)
    - packages/matriz-client/src/matriz_client/models.py (11 modelos `_SafeModel`: Segment, Instrument, InstrumentDetail, Order, Trade, MarketDataSnapshot + nested MarketDataLevel + MarketDataEntryValue, Position, DetailedPosition, AccountReport, NewOrderResponse — para el probe field_type_map)
    - packages/matriz-client/src/matriz_client/types.py (Literal type aliases: `Side`, `OrderType`, `TimeInForce`, `MarketId`, `SegmentId`, `CFICode`, `MarketDataEntry`, `OrderStatus`, `Currency` — para los args de los probes; CFICode tiene 9 valores)
    - packages/matriz-client/src/matriz_client/__init__.py (__all__ public — confirmar exports)
    - main_higyrus.py (Phase 4 driver — ~2380 líneas. Especialmente: lifecycle pattern (líneas 110-198 module-level state), ProbeResult dataclass, `_next_fid` (~líneas 140-145), `probe_*` con `_resolved_cuenta` pattern, schema snapshot envelope D-21 con `_write_or_check_schema` ~líneas 409-455, `safe_print(secrets=[...])` pattern, finding emission via `append_finding(...)`)
    - verification/__init__.py (POST Plan 05-01: barrel exporta `diff_safemodel_bidirectional`, `append_finding`, `require_env`, `safe_print`, `schema_of`, `write_findings` + helpers existentes)
    - verification/cycle_report.py (POST Plan 05-01: helper `verify_cycle_closure(pkg) -> tuple[bool, list[str]]`)
    - verification/findings.py (función `append_finding(pkg, *, fid, class_, surface, status, title, expected, actual, diff, base_url=None, ...)` — firma exacta)
    - verification/schema.py (función `schema_of(payload) -> dict | list` — PII-free)
    - verification/redaction.py (función `safe_print(text, secrets: list[str])` — redacta)
    - verification/env_gate.py (función `require_env(pkg, names: list[str]) -> bool` — True si todas presentes, sino imprime "SKIPPED ..." y retorna False)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-29 tabla literal L656-707 con los ~25 probes en orden; D-MATZ-1..D-MATZ-8 read-sweep samples & resolution; D-MATZ-31 cascade SKIPPED; D-MATZ-32 secrets dinámicos; D-MATZ-33 env vars del driver)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 3 Live Driver Probes L482-535; Example main() L700-831)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 1 main_matriz.py REWRITE L36-498 con imports, module state, probe pattern verbatim, Risk API HTTP Basic pattern, error probes, schema_snapshot helper, field_type_map probe, main() lifecycle)
    - .planning/codebase/CONVENTIONS.md (mypy strict, ruff line-length=100, double quotes, `from __future__ import annotations`)
    - .planning/codebase/INTEGRATIONS.md sección Primary API (24 endpoints REST, auth dual, base URL remarkets)
  </read_first>
  <behavior>
    - `main_matriz.py` reescrito **completamente** con la estructura del Phase 4 driver adaptada a sync-only:
      - Module-level state: `_PKG = "matriz-client"`, `_REPO_ROOT`, `_SCHEMA_DIR`, `_SCHEMA_FILES` dict, `_ENDPOINT_TEMPLATES` dict, env vars opcionales precargadas, `_auth_failed/_auth_failure_reason`, `_resolved_symbol/_resolved_segment`, `_fid_counter`
      - `_next_fid()` returns `F-NN` zero-padded
      - `ProbeResult(name, status, detail)` frozen dataclass
      - `_write_or_check_schema(func_name, endpoint_template, sample_params, raw_payload, base_url)` helper para snapshots con envelope D-21 + D-25 no-overwrite-on-drift
      - Login probe `probe_login_sync()` (D-MATZ-29 #1) — invoca `primary.login()`, setea `_auth_failed` si falla con `AuthenticationError`
      - 18 read-sweep probes en orden D-MATZ-29 #2-#19 con:
        - cascade SKIPPED check al inicio (`if _auth_failed: return ProbeResult(...)`)
        - try/except sobre `PrimaryAPIError`/Exception → finding ERROR-MAP OPEN
        - envelope check post-payload retrieval (segments_raw, instruments_raw, etc.) → finding SHAPE OPEN si shape incorrecta
        - resolved sample setting (probe_get_segments → `_resolved_segment`, probe_get_all_instruments → `_resolved_symbol`)
        - retorno tuple `(ProbeResult, raw_payload | None)` para que main() acumule payloads para field_type_map y schema snapshots
      - SKIPPED selectivo D-MATZ-3 (PRIMARY_ACCOUNT-gated): probe_get_active_orders/filled_orders/all_orders + probe_get_positions/detailed_positions/account_report skippean si `_PRIMARY_ACCOUNT is None`
      - SKIPPED selectivo D-MATZ-4 (MATRIZ_SAMPLE_*-gated): probe_get_order_status, probe_get_order_history, probe_get_order_by_exec_id
      - CFI sanity probe D-MATZ-6: probe_get_instruments_by_cfi_ESXXXX (baseline + snapshot) + probe_get_instruments_by_cfi_sanity (8 CFI restantes type-only)
      - Market-hours guard D-MATZ-5: probe_get_market_data inspecciona `LA.date`, emite finding NO-DATA OPEN si stale > 2h, PASS-shape sin asserts de valor
      - Trades probe D-MATZ-8: `date_from=today-7d, date_to=today`; finding NO-DATA OPEN si lista vacía + PASS-shape
    - Risk API probes (positions/detailed_positions/account_report) llaman `_request("GET", path, auth_basic=_risk_auth())` DIRECTO (NO `_get`) — Pitfall 2 RESEARCH L640
    - El probe `probe_get_positions` ejercita `_unwrap(_request(...), "positions", path)` que se introdujo en Plan 05-01
    - Imports al tope completos: `__future__`, stdlib (`datetime`, `json`, `os`, `sys`, `dataclasses`, `pathlib`, `typing`, `time`), `httpx`, barrel `verification`, `verification.cycle_report`, `matriz_client as primary`, exception y modelos, `CFICode` cast
    - **Esta task NO incluye los probes 20-25** (field_type_map, error probes, schema snapshot, cycle closure, EXPECTED terminal, main()) — esos van en Task 2.4. La task termina en el probe #19 (get_account_report) y deja los siguientes hooks definidos.
  </behavior>
  <action>
    Crear/reescribir `main_matriz.py` con la estructura completa hasta el probe #19. Esta task entrega ~600-700 líneas; Task 2.4 agrega ~200 más para llegar a ~800-900 líneas finales.

    **Estructura del archivo (Parte A — Tasks 2.3):**

    1. **Docstring de módulo** (~10 líneas): describe que es el Phase 5 live verification driver, ejercita ~25 probes contra `https://api.remarkets.primary.com.ar`, mencionar MATZ-01..07 + DRIFT-02 cubiertos.

    2. **Imports al tope** (mirror 05-PATTERNS L42-83):
       ```python
       from __future__ import annotations

       import datetime as dt
       import json
       import os
       import sys
       import time
       from dataclasses import dataclass
       from pathlib import Path
       from typing import Any, cast

       import httpx
       from verification import (
           append_finding,
           diff_safemodel_bidirectional,
           require_env,
           safe_print,
           schema_of,
           write_findings,
       )
       from verification.cycle_report import verify_cycle_closure

       import matriz_client as primary
       from matriz_client import PrimaryAPIError
       from matriz_client.client import _request as _matriz_request, _risk_auth
       from matriz_client.exceptions import AuthenticationError
       from matriz_client.models import (
           AccountReport,
           DetailedPosition,
           Instrument,
           InstrumentDetail,
           MarketDataSnapshot,
           NewOrderResponse,
           Order,
           Position,
           Segment,
           Trade,
       )
       from matriz_client.types import CFICode
       ```

       **Nota**: confirmar al leer si `_risk_auth` está exportado desde `matriz_client.client` (search por `def _risk_auth` en client.py). Si NO está exportado, inventarlo como helper del driver o reusar `httpx.BasicAuth(os.getenv("PRIMARY_USER", ""), os.getenv("PRIMARY_PASSWORD", ""))` inline donde se necesite.

    3. **Module-level constants & state** (mirror 05-PATTERNS L88-151):
       - `_PKG = "matriz-client"`
       - `_REPO_ROOT = Path(__file__).resolve().parent`
       - `_SCHEMA_DIR = _REPO_ROOT / ".planning" / "verification" / "schemas" / _PKG`
       - `_SCHEMA_FILES: dict[str, Path]` mapping cada probe func_name a su schema file path. Incluye **al menos 14 entries** para los probes always-on/account-scoped y 3 más para ID-scoped (total 17 entries esperadas; ajustar según probes realmente snap-shoteados)
       - `_ENDPOINT_TEMPLATES: dict[str, str]` mapping cada probe a su endpoint path canonical (con `{account_id}` placeholder donde aplica)
       - Env vars opt-in precargadas: `_SAMPLE_SYMBOL`, `_SAMPLE_CL_ORD_ID`, `_SAMPLE_PROPRIETARY`, `_SAMPLE_EXEC_ID`, `_PRIMARY_ACCOUNT` (cada uno `os.getenv("VAR")`)
       - State globals: `_auth_failed: bool = False`, `_auth_failure_reason: str = ""`, `_resolved_symbol: str | None = None`, `_resolved_segment: str | None = None`, `_fid_counter: int = 0`

    4. **`_next_fid()` helper**: incrementa `_fid_counter`, retorna `f"F-{_fid_counter:02d}"`.

    5. **`ProbeResult` dataclass** (frozen, slots): `name: str`, `status: str`, `detail: str`.

    6. **`_write_or_check_schema(func_name, endpoint_template, sample_params, raw_payload, base_url)` helper** (mirror 05-PATTERNS L275-312):
       - Construye envelope D-21: `{"endpoint": endpoint_template, "client_function": func_name, "captured_at": dt.datetime.now(dt.UTC).isoformat(), "base_url": base_url, "sample_params": sample_params, "schema": schema_of(raw_payload)}`
       - Crea `_SCHEMA_DIR` si no existe
       - Si el archivo no existe → escribir + return `("PASS", "escrito ...")`
       - Si existe y `committed["schema"] == actual_schema` → return `("PASS", "... sin drift")`
       - Si existe y difiere → append_finding SHAPE OPEN + return `("FINDING", f"{fid}|filename")` (D-25 no-overwrite-on-drift)

    7. **`_first_dict(payload)` helper** (utility para probe_field_type_map en Task 2.4): retorna el primer dict de una lista, o None si payload no es lista no vacía o el primer elemento no es dict.

    8. **`probe_login_sync()` -> ProbeResult** (D-MATZ-29 #1):
       - Llama `primary.login()`
       - En `AuthenticationError`: setea `_auth_failed = True`, `_auth_failure_reason = str(exc)`, return ProbeResult("login_sync", "FAIL", redacted detail). Emite finding AUTH OPEN si la causa es código diferente al esperado
       - En Exception inesperada: cascade SKIPPED downstream; finding ERROR-MAP OPEN
       - En éxito: return ProbeResult("login_sync", "PASS", f"token obtenido en {duration:.2f}s") — usa `time.monotonic()` para medir

    9. **18 read-sweep probes en orden D-MATZ-29 #2-#19** (cada uno retorna `tuple[ProbeResult, raw_payload | None]`):

       Mirror Pattern 3 RESEARCH L498-528 + Pattern PATTERNS L158-205. Estructura de cada probe:
       - Check cascade `if _auth_failed: return (SKIPPED, None)`
       - Check selective SKIPPED (PRIMARY_ACCOUNT, MATRIZ_SAMPLE_*) donde aplica
       - try `_matriz_request(...)` o `primary.<func>(...)` — preferir `_matriz_request` para retener raw payload (en lugar del cliente que envuelve en model)
       - except `PrimaryAPIError` → finding ERROR-MAP OPEN + return (FINDING, None)
       - except Exception → finding ERROR-MAP OPEN + return (FINDING, None)
       - Envelope check: `if not isinstance(raw.get("<key>"), list/dict)` → finding SHAPE OPEN + return (FINDING, None)
       - Resolved sample setting (solo donde aplica: probe_get_segments → `_resolved_segment`; probe_get_all_instruments → `_resolved_symbol`; con override de env si presente)
       - return (ProbeResult(<name>, "PASS", detail), raw_payload)

       **Probes (orden D-MATZ-29 #2-#19):**

       2. `probe_get_segments()` — `/rest/segment/all`, envelope `["segments"]`, resuelve `_resolved_segment = first["marketSegmentId"]`
       3. `probe_get_all_instruments()` — `/rest/instruments/all`, envelope `["instruments"]`, resuelve `_resolved_symbol` desde `first["instrumentId"]["symbol"]` (mirror D-MATZ-1); si `_SAMPLE_SYMBOL` está presente, usar ese override; setea `_resolved_symbol` con el override O el resuelto
       4. `probe_get_instruments_details()` — `/rest/instruments/details`, envelope `["instruments"]`
       5. `probe_get_instrument_detail()` — `/rest/instruments/detail` con `_resolved_symbol`, envelope `["instrument"]`. SKIPPED si `_resolved_symbol is None`
       6. `probe_get_instruments_by_cfi_ESXXXX()` — `/rest/instruments/byCFICode` con `CFICode="ESXXXX"`, envelope `["instruments"]` (baseline, genera snapshot)
       7. `probe_get_instruments_by_cfi_sanity()` — itera los 8 CFI restantes (DBXXXX, OCASPS, OPASPS, FXXXSX, OPAFXS, OCAFXS, EMXXXX, DBXXFR); para cada uno hace `_get("/rest/instruments/byCFICode", CFICode=cfi)["instruments"]`; verifica `isinstance(result, list)` + `len(result) >= 0` + (si len > 0) `isinstance(result[0], dict)`. Sin snapshot por cada uno (D-MATZ-6 baseline-only). Si algún CFI falla shape → finding SHAPE OPEN.
       8. `probe_get_instruments_by_segment()` — `/rest/instruments/bySegment` con `_resolved_segment`, envelope `["instruments"]`. SKIPPED si `_resolved_segment is None`
       9. `probe_get_market_data()` — `/rest/marketdata/get` con `_resolved_symbol` + default entries + depth=None. Envelope `["marketData"]`. **Market-hours guard D-MATZ-5**: tras envelope check OK, inspeccionar `raw["marketData"]["LA"]["date"]` (epoch ms); si `(time.time() * 1000) - LA_date > 7200000` (2h en ms) → finding NO-DATA OPEN ("market-hours: LA.date stale by Xh") + PASS-shape (return PASS con detail "shape OK, value asserts skipped — stale LA.date"). Si `LA` o `date` ausente, treat as PASS-shape (no fail).
       10. `probe_get_trades()` — `/rest/data/getTrades` con `_resolved_symbol` + `date_from=today-7d`, `date_to=today`. Envelope `["trades"]`. Si list vacía → finding NO-DATA OPEN + PASS-shape ("no trades for X in last 7 days").
       11. `probe_get_active_orders()` — SKIPPED si `_PRIMARY_ACCOUNT is None`. Sino `/rest/order/actives` con account, envelope `["orders"]`
       12. `probe_get_filled_orders()` — idem (`/rest/order/filleds`)
       13. `probe_get_all_orders()` — idem (`/rest/order/all`)
       14. `probe_get_order_status()` — SKIPPED si `_SAMPLE_CL_ORD_ID is None` OR `_SAMPLE_PROPRIETARY is None`. `/rest/order/id` con `clOrdId`, `proprietary`. Envelope `["order"]`
       15. `probe_get_order_history()` — idem args. `/rest/order/allById`. Envelope `["orders"]`
       16. `probe_get_order_by_exec_id()` — SKIPPED si `_SAMPLE_EXEC_ID is None`. `/rest/order/byExecId` con `execId`. Envelope `["order"]`
       17. `probe_get_positions()` — SKIPPED si `_PRIMARY_ACCOUNT is None`. **Risk API HTTP Basic** D-MATZ Pitfall 2: `_matriz_request("GET", f"/rest/risk/position/getPositions/{_PRIMARY_ACCOUNT}", auth_basic=_risk_auth())`. Envelope `["positions"]`
       18. `probe_get_detailed_positions()` — SKIPPED si `_PRIMARY_ACCOUNT is None`. `_matriz_request("GET", f"/rest/risk/detailedPosition/{_PRIMARY_ACCOUNT}", auth_basic=_risk_auth())`. **SIN envelope key** — el payload raíz es el dict completo de `DetailedPosition.from_api(raw)`. Asertar `isinstance(raw, dict)` y campos presentes; sin `_unwrap`.
       19. `probe_get_account_report()` — SKIPPED si `_PRIMARY_ACCOUNT is None`. `_matriz_request("GET", f"/rest/risk/accountReport/{_PRIMARY_ACCOUNT}", auth_basic=_risk_auth())`. **SIN envelope key** — idem #18.

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - `from __future__ import annotations` línea 1
    - mypy --strict pasa (usar type hints completos en cada probe)
    - ruff line-length=100, double quotes, 4 espacios
    - NO incluir `asyncio`, `asyncio.run`, `_async_main`, `contextlib` (D-MATZ-30 sync-only)
    - NO incluir tests live (no se ejecuta el live run en esta task)
    - NO incluir mutaciones (`new_order`, `replace_order`, `cancel_order`) reachables vivas — sus funciones se importan/usan solo por mock-only en Plan 05-03
    - NO commitear payloads/findings reales en este task (eso es Plan 05-03)
    - NO usar print() raw — usar `safe_print(line, secrets=...)` consistentemente
    - Estructurar el código para que Task 2.4 pueda extenderlo sin reescribir lo entregado acá

    Commit sugerido: `feat(05-02): rewrite main_matriz.py with module setup + login + 18 read-sweep probes (D-MATZ-29 #1-#19, Part A)`
  </action>
  <verify>
    <automated>uv run mypy main_matriz.py</automated>
    <automated>uv run ruff check main_matriz.py</automated>
    <automated>uv run ruff format --check main_matriz.py</automated>
    <automated>uv run python -c "import main_matriz; print('import OK')"</automated>
    <automated>grep -c "def probe_login_sync" main_matriz.py</automated>
    <automated>grep -c "def probe_get_segments" main_matriz.py</automated>
    <automated>grep -c "def probe_get_all_instruments" main_matriz.py</automated>
    <automated>grep -c "def probe_get_instruments_details" main_matriz.py</automated>
    <automated>grep -c "def probe_get_instrument_detail" main_matriz.py</automated>
    <automated>grep -c "def probe_get_instruments_by_cfi_ESXXXX" main_matriz.py</automated>
    <automated>grep -c "def probe_get_instruments_by_cfi_sanity" main_matriz.py</automated>
    <automated>grep -c "def probe_get_instruments_by_segment" main_matriz.py</automated>
    <automated>grep -c "def probe_get_market_data" main_matriz.py</automated>
    <automated>grep -c "def probe_get_trades" main_matriz.py</automated>
    <automated>grep -c "def probe_get_active_orders" main_matriz.py</automated>
    <automated>grep -c "def probe_get_filled_orders" main_matriz.py</automated>
    <automated>grep -c "def probe_get_all_orders" main_matriz.py</automated>
    <automated>grep -c "def probe_get_order_status" main_matriz.py</automated>
    <automated>grep -c "def probe_get_order_history" main_matriz.py</automated>
    <automated>grep -c "def probe_get_order_by_exec_id" main_matriz.py</automated>
    <automated>grep -c "def probe_get_positions" main_matriz.py</automated>
    <automated>grep -c "def probe_get_detailed_positions" main_matriz.py</automated>
    <automated>grep -c "def probe_get_account_report" main_matriz.py</automated>
    <automated>grep -c "asyncio" main_matriz.py</automated>
    <automated>grep -c "auth_basic=" main_matriz.py</automated>
    <automated>grep -c "_resolved_symbol" main_matriz.py</automated>
    <automated>grep -c "_resolved_segment" main_matriz.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_matriz.py` reescrito completamente (mínimo 600 líneas; previo era 57)
    - Source: `from __future__ import annotations` línea 1
    - Source: contiene `import matriz_client as primary`
    - Source: contiene `from verification import (... diff_safemodel_bidirectional ...)` (preparado para Task 2.4)
    - Source: contiene `from verification.cycle_report import verify_cycle_closure` (preparado para Task 2.4)
    - Source: contiene `_PKG = "matriz-client"`
    - Source: contiene `@dataclass(frozen=True, slots=True)` para `ProbeResult`
    - Source: contiene `def probe_login_sync(`
    - Source: contiene los 18 probes read-sweep (cada uno con `def probe_get_*`): segments, all_instruments, instruments_details, instrument_detail, instruments_by_cfi_ESXXXX, instruments_by_cfi_sanity, instruments_by_segment, market_data, trades, active_orders, filled_orders, all_orders, order_status, order_history, order_by_exec_id, positions, detailed_positions, account_report
    - Source: contiene `def _write_or_check_schema(` (helper para snapshots)
    - Source: NO contiene `asyncio` ni `asyncio.run` ni `_async_main` (sync-only D-MATZ-30); `grep -c asyncio` == 0
    - Source: contiene `auth_basic=` >= 3 veces (Risk API probes #17, #18, #19)
    - Source: contiene `_resolved_symbol` y `_resolved_segment` (state global + uso)
    - Source: contiene cascade SKIPPED pattern (`if _auth_failed:` aparece >= 18 veces — uno por probe downstream del login)
    - Source: contiene `_PRIMARY_ACCOUNT` y verifica `if _PRIMARY_ACCOUNT is None: return (ProbeResult(..., "SKIPPED", ...))` en al menos 6 probes (account-scoped)
    - Source: contiene `_SAMPLE_CL_ORD_ID`, `_SAMPLE_PROPRIETARY`, `_SAMPLE_EXEC_ID` con SKIPPED selectivo en los 3 probes ID-scoped
    - Source: market-hours guard probe_get_market_data inspecciona `LA.date` (busca subset: `LA` o `"LA"` + `date` o `"date"` + `time.time() * 1000` o `* 1000`)
    - Source: CFI sanity probe itera al menos los 8 CFI codes (string-matching: contiene `"DBXXXX"` y `"OCASPS"` y otros)
    - Source: `_write_or_check_schema` implementa D-25 no-overwrite-on-drift (busca patrón `if committed.get("schema") == actual_schema` o equivalente)
    - Behavior: `uv run python -c "import main_matriz"` exits 0 (sintaxis válida + imports resuelven via barrel + cliente)
    - Behavior: con `unset PRIMARY_USER PRIMARY_PASSWORD; uv run python main_matriz.py` (sin env vars) → imprime `SKIPPED matriz-client: missing PRIMARY_USER, PRIMARY_PASSWORD` (o equivalent) y exits 0 — verificable via `uv run python main_matriz.py 2>&1 | head -5` (este test específico va en Task 2.4 con main() completo; en Task 2.3 verificable que el module-level code arranca sin errores via `import main_matriz`)
    - Static: `uv run mypy main_matriz.py` exits 0
    - Static: `uv run ruff check main_matriz.py` exits 0
    - Static: `uv run ruff format --check main_matriz.py` exits 0
    - Non-regression: `uv run pytest -q` no falla (matriz package + cross-package)
  </acceptance_criteria>
  <done>main_matriz.py reescrito Parte A: imports + module state + 18 read-sweep probes en orden D-MATZ-29 con cascade SKIPPED, selective gates, market-hours guard, CFI sanity sweep, Risk API HTTP Basic. mypy + ruff verdes. Task 2.4 completa main() + probes 20-25.</done>
</task>

<task type="auto">
  <name>Task 2.4: Complete main_matriz.py with field_type_map + error probes + schema_snapshot + cycle_closure + EXPECTED terminal + main() lifecycle (D-MATZ-29 #20-#25, Part B)</name>
  <files>main_matriz.py</files>
  <read_first>
    - main_matriz.py (POST Task 2.3: contiene module setup + login probe + 18 read-sweep probes. Esta task agrega los probes 20-25 + main() lifecycle AL FINAL del archivo)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-22 verbatim los 3 error probes con strings literales `"ZZZZZZ-NOT-A-SYMBOL"`, `"INVALID-ACCT-XXXXX"`, `"INVALID-CFI"`; D-MATZ-23 distinción HTTP 4xx vs status=ERROR; D-MATZ-27 EXPECTED terminal verbatim; D-MATZ-28 verify_cycle_closure invocation)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 4 schema snapshot envelope D-21 + D-25; example error probe L883-938; example main() L700-831)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 1 main() lifecycle L373-498; probe_field_type_map L319-369; probe_error_bogus_symbol L235-269)
    - verification/cycle_report.py (POST Plan 05-01: signature `verify_cycle_closure(pkg: str) -> tuple[bool, list[str]]`)
    - main_higyrus.py (POST Task 2.2: main() lifecycle pattern adaptable — pero sync-only para matriz; ProbeResult acumulation + safe_print loop + SUMMARY line)
  </read_first>
  <behavior>
    - `main_matriz.py` ahora tiene el archivo COMPLETO (~800-900 líneas) con todos los componentes:
      - 18 read-sweep probes (de Task 2.3) +
      - **Probe 20** `probe_field_type_map(payloads)` — itera los 11 modelos `_SafeModel` matriz usando `diff_safemodel_bidirectional`, emite findings SHAPE OPEN por cada divergencia model-only (FALSE PASS riesgo) y wire-only (info)
      - **Probes 21-23** error probes always-on: `probe_error_bogus_symbol()`, `probe_error_invalid_account()`, `probe_error_malformed_cfi()` — cada uno distingue `PrimaryAPIError(status='ERROR')` mapeado (PASS) de `httpx.HTTPStatusError` HTTP 4xx no mapeado (finding ERROR-MAP OPEN)
      - **Probe 24** `probe_schema_snapshot(payloads, base_url)` — invoca `_write_or_check_schema(...)` para cada entry en `_SCHEMA_FILES` cuyo payload está en `payloads` dict (solo PASS-aron probes). Acumula resultados, emite finding por cada drift OPEN
      - **Probe 25** integrated en `main()`: loop sobre 4 paquetes invocando `verify_cycle_closure(pkg)` y emitiendo `PROBE cycle_closure_<pkg>: PASS|FAIL`
      - **EXPECTED terminal D-MATZ-27**: al final de main(), `append_finding(_PKG, ..., status="EXPECTED", title="prod-vs-remarkets divergence acknowledged", ...)` — ES LA ÚLTIMA llamada a `append_finding` sobre matriz-client en el run (Assumption A3)
      - **main()** lifecycle completo: require_env → hostname assert remarkets D-MATZ-33 → write_findings(_PKG) → secrets discovery (PRIMARY_USER/PASSWORD del env, _token tras login) → execute probes 1-24 en orden → cycle_closure loop x4 → EXPECTED terminal → emit `PROBE` lines via `safe_print(line, secrets)` → emit `SUMMARY` line
    - `python main_matriz.py` (sin env vars) imprime `SKIPPED matriz-client: missing PRIMARY_USER, PRIMARY_PASSWORD` y exits 0 (HARN-01)
    - `python main_matriz.py` (con PRIMARY_USER/PASSWORD pero PRIMARY_BASE_URL=https://api.primary.com.ar) imprime `ABORT: PRIMARY_BASE_URL=... is not a remarkets sandbox URL` y exits 1 (D-MATZ-33 hostname assert)
    - mypy strict + ruff verdes
  </behavior>
  <action>
    Extender `main_matriz.py` con los componentes que faltan, agregando al FINAL del archivo (después del probe_get_account_report) las siguientes secciones.

    **Sección 1: `probe_field_type_map(payloads: dict[str, Any]) -> ProbeResult`** (D-MATZ-29 #20, MATZ-03):

    Mirror 05-PATTERNS L319-367. Estructura:
    - Check cascade `if _auth_failed: return ProbeResult("field_type_map", "SKIPPED", ...)`
    - Define lista `targets: list[tuple[str, Any, type]]` con (root_name, payload, model_cls) para los 9 modelos sampleables desde payloads:
      - `("segment", _first_dict(payloads.get("get_segments")), Segment)`
      - `("instrument", _first_dict(payloads.get("get_all_instruments")), Instrument)`
      - `("instrument_detail", payloads.get("get_instrument_detail"), InstrumentDetail)` — no usar _first_dict porque InstrumentDetail es el payload entero (envelope ya unwrapped)
      - `("market_data", payloads.get("get_market_data"), MarketDataSnapshot)`
      - `("trade", _first_dict(payloads.get("get_trades")), Trade)`
      - `("order", _first_dict(payloads.get("get_all_orders")), Order)` — usa get_all_orders si presente
      - `("position", _first_dict(payloads.get("get_positions")), Position)`
      - `("detailed_position", payloads.get("get_detailed_positions"), DetailedPosition)`
      - `("account_report", payloads.get("get_account_report"), AccountReport)`
    - (NewOrderResponse cubierto por mock-only Plan 05-03, no acá; los modelos nested se cubren recursivamente por el helper)
    - Para cada target con payload no-None: itera `diff_safemodel_bidirectional(payload, model_cls, path=f".{root_name}")`
    - Por cada `(path, direction, key)` yieldeado: `_next_fid()` + `append_finding(_PKG, fid=fid, class_="SHAPE", surface="sync", status="OPEN", title=..., expected=..., actual=..., diff=..., base_url=base_url)` con title que distingue model-only ("FALSE PASS riesgo") vs wire-only ("info")
    - Acumula fids en lista; retorna ProbeResult("field_type_map", "FINDING", f"{', '.join(fids)} (OPEN)") si fids no vacía, sino PASS

    **Sección 2: 3 error probes always-on** (D-MATZ-29 #21-#23, MATZ-05):

    Cada uno sigue la estructura verbatim de 05-PATTERNS L235-269 / RESEARCH L883-938. Estructura común:

    ```python
    def probe_error_<name>() -> ProbeResult:
        """Probe error #N (D-MATZ-22): <descripcion>."""
        if _auth_failed:
            return ProbeResult(f"error_{name}", "SKIPPED", f"auth failed: {_auth_failure_reason}")
        base_url = primary.client._base_url
        try:
            primary.<func>(<bogus_arg>)
        except PrimaryAPIError as e:
            if e.status == "ERROR":
                return ProbeResult(f"error_{name}", "PASS", f"PrimaryAPIError as expected: {e.description}")
            # status != "ERROR" → finding ERROR-MAP OPEN
            fid = _next_fid()
            append_finding(_PKG, fid=fid, class_="ERROR-MAP", ...)
            return ProbeResult(f"error_{name}", "FINDING", f"{fid} (OPEN)")
        except httpx.HTTPStatusError as e:
            # HTTP 4xx no mapeado → finding ERROR-MAP OPEN
            fid = _next_fid()
            append_finding(_PKG, fid=fid, class_="ERROR-MAP", title="HTTP 4xx not mapped to PrimaryAPIError", ...)
            return ProbeResult(f"error_{name}", "FINDING", f"{fid} (OPEN)")
        except Exception as e:
            # Tipo inesperado → finding ERROR-MAP OPEN
            fid = _next_fid()
            append_finding(_PKG, fid=fid, class_="ERROR-MAP", title=f"unexpected {type(e).__name__}", ...)
            return ProbeResult(f"error_{name}", "FINDING", f"{fid} (OPEN)")

        # No exception levantada → finding ERROR-MAP OPEN
        fid = _next_fid()
        append_finding(_PKG, fid=fid, class_="ERROR-MAP", title=f"{func} bogus input did not raise", ...)
        return ProbeResult(f"error_{name}", "FINDING", f"{fid} (OPEN)")
    ```

    **3 probes específicos:**
    1. `probe_error_bogus_symbol()`: `primary.get_market_data("ZZZZZZ-NOT-A-SYMBOL")` (D-MATZ-22 #1)
    2. `probe_error_invalid_account()`: `primary.get_active_orders("INVALID-ACCT-XXXXX")` (D-MATZ-22 #2)
    3. `probe_error_malformed_cfi()`: `primary.get_instruments_by_cfi(cast(CFICode, "INVALID-CFI"))` (D-MATZ-22 #3 — requiere `cast` por mypy strict)

    **Sección 3: `probe_schema_snapshot(payloads: dict[str, Any], base_url: str) -> ProbeResult`** (D-MATZ-29 #24):

    - Itera `_SCHEMA_FILES` items
    - Para cada `(func_name, schema_path)`:
      - Si `func_name not in payloads or payloads[func_name] is None` → skip (no se generó payload, no se snapshotea)
      - Sino llama `_write_or_check_schema(func_name, _ENDPOINT_TEMPLATES[func_name], <sample_params>, payloads[func_name], base_url)`
      - Acumula resultado: PASS / FINDING
    - Retorna ProbeResult("schema_snapshot", "PASS", f"N snapshots OK") si todos PASS; FINDING con fids si hay drifts

    **Sección 4: `main()` lifecycle** (sin asyncio, mirror 05-PATTERNS L373-495):

    ```python
    def main() -> None:
        if not require_env(_PKG, ["PRIMARY_USER", "PRIMARY_PASSWORD"]):
            sys.exit(0)

        # D-MATZ-33 belt-and-suspenders hostname assert
        base = primary.client._base_url
        if "remarkets" not in base:
            print(
                f"ABORT: PRIMARY_BASE_URL={base!r} is not a remarkets sandbox URL — "
                "Phase 5 verification is remarkets-only by safety policy",
                file=sys.stderr,
            )
            sys.exit(1)

        write_findings(_PKG)

        # D-MATZ-32 secrets dinámicos
        secrets: list[str] = []
        _password_env = os.getenv("PRIMARY_PASSWORD", "")
        if _password_env:
            secrets.append(_password_env)
        _user_env = os.getenv("PRIMARY_USER", "")
        if _user_env and len(_user_env) >= 4:
            secrets.append(_user_env)

        results: list[ProbeResult] = []
        payloads: dict[str, Any] = {}

        # Probe 1: login
        r1 = probe_login_sync()
        results.append(r1)
        token = getattr(primary.client, "_token", None)
        if token:
            secrets.append(token)

        # Probes 2-19: happy-path sweep
        sweep_probes = [
            ("get_segments", probe_get_segments),
            ("get_all_instruments", probe_get_all_instruments),
            ("get_instruments_details", probe_get_instruments_details),
            ("get_instrument_detail", probe_get_instrument_detail),
            ("get_instruments_by_cfi_ESXXXX", probe_get_instruments_by_cfi_ESXXXX),
            ("get_instruments_by_cfi_sanity", probe_get_instruments_by_cfi_sanity),
            ("get_instruments_by_segment", probe_get_instruments_by_segment),
            ("get_market_data", probe_get_market_data),
            ("get_trades", probe_get_trades),
            ("get_active_orders", probe_get_active_orders),
            ("get_filled_orders", probe_get_filled_orders),
            ("get_all_orders", probe_get_all_orders),
            ("get_order_status", probe_get_order_status),
            ("get_order_history", probe_get_order_history),
            ("get_order_by_exec_id", probe_get_order_by_exec_id),
            ("get_positions", probe_get_positions),
            ("get_detailed_positions", probe_get_detailed_positions),
            ("get_account_report", probe_get_account_report),
        ]
        for key, probe_fn in sweep_probes:
            result, raw = probe_fn()
            results.append(result)
            if raw is not None:
                payloads[key] = raw

        # Probe 20: field_type_map
        results.append(probe_field_type_map(payloads))

        # Probes 21-23: error probes
        results.append(probe_error_bogus_symbol())
        results.append(probe_error_invalid_account())
        results.append(probe_error_malformed_cfi())

        # Probe 24: schema snapshots
        results.append(probe_schema_snapshot(payloads, base))

        # Probe 25: cycle_closure × 4 packages (D-MATZ-28)
        for pkg in ("ambito-financiero-client", "iol-client", "higyrus-client", "matriz-client"):
            ok, missing = verify_cycle_closure(pkg)
            status_str = "PASS" if ok else "FAIL"
            detail = "" if ok else f" — missing regressions: {', '.join(missing)}"
            results.append(
                ProbeResult(f"cycle_closure_{pkg.replace('-', '_')}", status_str, detail)
            )
            if not ok:
                fid = _next_fid()
                append_finding(
                    pkg, fid=fid, class_="ERROR-MAP", surface="sync", status="OPEN",
                    title=f"cycle closure: {len(missing)} CONFIRMED/FIXED without regression test",
                    expected="every CONFIRMED/FIXED finding linked to existing test path",
                    actual=f"missing regressions: {', '.join(missing)}",
                    diff="see verify_cycle_closure output",
                )

        # D-MATZ-27: EXPECTED terminal prod-vs-remarkets (última operación sobre matriz-client; Assumption A3)
        fid = _next_fid()
        append_finding(
            _PKG, fid=fid, class_="SHAPE", surface="sync", status="EXPECTED",
            title="prod-vs-remarkets divergence acknowledged",
            expected=(
                "verification limited to remarkets sandbox by safety policy "
                "(REQUIREMENTS.md Out of Scope)"
            ),
            actual=(
                "prod (api.primary.com.ar) shape unverified; sandbox shape "
                "committed in .planning/verification/schemas/matriz-client/"
            ),
            diff="N/A (acknowledged limitation, not detected drift)",
        )

        # Stdout verbatim D-02 + SUMMARY
        counts = {"PASS": 0, "FAIL": 0, "SKIPPED": 0, "FINDING": 0}
        for r in results:
            line = f"PROBE {r.name}: {r.status} {r.detail}".rstrip()
            safe_print(line, secrets=secrets)
            counts[r.status] = counts.get(r.status, 0) + 1
        safe_print(
            f"SUMMARY: PASS={counts['PASS']} FAIL={counts['FAIL']} "
            f"SKIPPED={counts['SKIPPED']} FINDING={counts['FINDING']}",
            secrets=secrets,
        )


    if __name__ == "__main__":
        main()
    ```

    **Restricciones:**
    - mypy strict pasa
    - ruff line-length=100, double quotes, 4 espacios
    - **NO ejecutar live run en este task** — solo construir el código; el live run real es Plan 05-03
    - Verificar al final con `python main_matriz.py` SIN env vars que emite el SKIPPED HARN-01 correctamente
    - Verificar con `PRIMARY_USER=test PRIMARY_PASSWORD=test PRIMARY_BASE_URL=https://api.primary.com.ar python main_matriz.py` que aborta con el hostname assert (D-MATZ-33). NO ejecutar contra prod real
    - El `safe_print` recibe secrets como `keyword argument`: verificar firma actual en `verification/redaction.py`; el patrón Phase 4 era `safe_print(text, secrets=...)` keyword

    Commit sugerido: `feat(05-02): complete main_matriz.py with field_type_map, error probes, schema_snapshot, cycle_closure, EXPECTED terminal, main() (D-MATZ-29 #20-#25, Part B)`
  </action>
  <verify>
    <automated>uv run mypy main_matriz.py</automated>
    <automated>uv run ruff check main_matriz.py</automated>
    <automated>uv run ruff format --check main_matriz.py</automated>
    <automated>uv run python -c "import main_matriz; print('import OK')"</automated>
    <automated>grep -c "def probe_field_type_map" main_matriz.py</automated>
    <automated>grep -c "def probe_error_bogus_symbol" main_matriz.py</automated>
    <automated>grep -c "def probe_error_invalid_account" main_matriz.py</automated>
    <automated>grep -c "def probe_error_malformed_cfi" main_matriz.py</automated>
    <automated>grep -c "def probe_schema_snapshot" main_matriz.py</automated>
    <automated>grep -c "def main(" main_matriz.py</automated>
    <automated>grep -c "verify_cycle_closure" main_matriz.py</automated>
    <automated>grep -c "diff_safemodel_bidirectional" main_matriz.py</automated>
    <automated>grep -c "prod-vs-remarkets divergence acknowledged" main_matriz.py</automated>
    <automated>grep -c "ZZZZZZ-NOT-A-SYMBOL" main_matriz.py</automated>
    <automated>grep -c "INVALID-ACCT-XXXXX" main_matriz.py</automated>
    <automated>grep -c "INVALID-CFI" main_matriz.py</automated>
    <automated>grep -c "ABORT.*remarkets" main_matriz.py</automated>
    <automated>grep -c "remarkets.*not in\|not in.*remarkets" main_matriz.py</automated>
    <automated>env -u PRIMARY_USER -u PRIMARY_PASSWORD uv run python main_matriz.py 2>&1 | head -3</automated>
    <automated>PRIMARY_USER=test PRIMARY_PASSWORD=test PRIMARY_BASE_URL=https://api.primary.com.ar uv run python main_matriz.py 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_matriz.py` ahora >= 800 líneas (rewrite completo Task 2.3 + 2.4)
    - Source: contiene `def probe_field_type_map(`
    - Source: contiene `def probe_error_bogus_symbol(`, `def probe_error_invalid_account(`, `def probe_error_malformed_cfi(`
    - Source: contiene `def probe_schema_snapshot(`
    - Source: contiene `def main()` con `if __name__ == "__main__": main()` al final del archivo
    - Source: contiene `verify_cycle_closure(` invocación >= 4 veces (una por pkg) o iterado en loop con string keys de los 4 pkgs (ambito-financiero-client, iol-client, higyrus-client, matriz-client)
    - Source: contiene `diff_safemodel_bidirectional(` invocación >= 1 vez (en probe_field_type_map)
    - Source: contiene `"prod-vs-remarkets divergence acknowledged"` (EXPECTED terminal D-MATZ-27)
    - Source: contiene los 3 strings literales de error probes: `"ZZZZZZ-NOT-A-SYMBOL"`, `"INVALID-ACCT-XXXXX"`, `"INVALID-CFI"` (o `cast(CFICode, "INVALID-CFI")`)
    - Source: contiene `cast(CFICode, ` para malformed CFI probe
    - Source: contiene el ABORT remarkets pattern: busca `"remarkets" not in` o `"not in" + "remarkets"` (D-MATZ-33 hostname assert)
    - Source: contiene `require_env(_PKG, ["PRIMARY_USER", "PRIMARY_PASSWORD"])` (HARN-01)
    - Source: contiene `safe_print(` invocación con `secrets=` keyword en el loop final + SUMMARY
    - Source: contiene `SUMMARY: PASS=` literal (formato verbatim del Phase 2-4)
    - Source: contiene `PROBE` literal en el f-string del safe_print line
    - Source: NO contiene `asyncio` (sync-only)
    - Source: NO contiene mutaciones live `new_order(`/`replace_order(`/`cancel_order(` reachables en código de probes (verificable: NO existe línea no comentada con `primary.new_order(` ni `primary.replace_order(` ni `primary.cancel_order(`)
    - Source: el EXPECTED terminal `append_finding` es la última invocación de `append_finding` sobre `_PKG` en `main()` (Assumption A3)
    - Behavior: `uv run python -c "import main_matriz"` exits 0
    - Behavior: con env vars vacías, `uv run python main_matriz.py` imprime `SKIPPED matriz-client: missing PRIMARY_USER, PRIMARY_PASSWORD` (verificable por contenido stdout) y exit code 0
    - Behavior: con `PRIMARY_USER=test PRIMARY_PASSWORD=test PRIMARY_BASE_URL=https://api.primary.com.ar` el driver imprime `ABORT: PRIMARY_BASE_URL=...primary.com.ar` (stderr) y exit code 1 — verificable por contenido + `$?`
    - Static: `uv run mypy main_matriz.py` exits 0
    - Static: `uv run ruff check main_matriz.py` exits 0
    - Static: `uv run ruff format --check main_matriz.py` exits 0
    - Non-regression: `uv run pytest -q` exits 0 (whole repo green; el driver no afecta tests)
  </acceptance_criteria>
  <done>main_matriz.py completo (~800-900 líneas) con 25 probes en orden D-MATZ-29 + cycle_closure × 4 pkgs + EXPECTED terminal + main() con HARN-01 + D-MATZ-33 hostname abort. mypy + ruff verdes. Driver SKIPPED-clean sin env vars; ABORT-clean contra prod. Listo para Plan 05-03 live run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `main_matriz.py` ← env vars (`PRIMARY_USER`, `PRIMARY_PASSWORD`, `PRIMARY_BASE_URL`, optionals) | Driver lee credenciales del entorno del proceso; `require_env` gate + hostname assert protegen contra ejecución sin creds o contra prod |
| `main_matriz.py` ← stdout (`safe_print` con secrets dinámicos) | `safe_print(line, secrets=[PRIMARY_USER, PRIMARY_PASSWORD, _token])` redacta credenciales antes de emitir. `_token` se agrega tras probe_login_sync |
| `main_matriz.py` → Primary API (`https://api.remarkets.primary.com.ar` SOLO) | Hostname assert D-MATZ-33 al inicio aborta si base_url no contiene `"remarkets"` — prevención de exfiltración cross-environment |
| `main_matriz.py` → `.planning/verification/matriz-client-findings.md` (via `append_finding`) | Findings file en path validado por `_PKG_SLUG_RE` en `verification.findings` (WR-04 hardened Phase 2) |
| `main_matriz.py` → `.planning/verification/schemas/matriz-client/*.json` (via `schema_of`) | Schema files PII-free por construcción; solo nombres de tipo, no valores |
| `main_higyrus.py` → `from verification import diff_safemodel_bidirectional` | El refactor consume el helper promovido sin cross-package coupling; el archivo cliente higyrus NO se toca |
| `verification/cycle_report.py::verify_cycle_closure` ← `<pkg>-findings.md` (4 paquetes) | Parseo estructural read-only; regex valida path de regression test sin permitir traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-08 | Tampering | Driver corrido contra prod (`https://api.primary.com.ar`) por error de configuración | mitigate | D-MATZ-33 belt-and-suspenders hostname assert al inicio (Task 2.4): si `"remarkets" not in primary.client._base_url` → ABORT con loud message + `sys.exit(1)`. Cubre escenario donde operador setea `PRIMARY_BASE_URL=https://api.primary.com.ar` por accidente. Acceptance: ABORT verificable con env var de prueba |
| T-5-09 | Information Disclosure | `_token` impreso en logs/stdout del driver tras login exitoso | mitigate | D-MATZ-32 `safe_print(line, secrets=[..., _token])` con `_token` agregado dinámicamente tras `probe_login_sync`. Regex `_BEARER` interno en `redaction.py` cubre tokens reflejados aun sin enumeración explícita |
| T-5-10 | Information Disclosure | Findings file y schemas commiteables exponen estructura interna de la API | accept | Findings + schemas son artefactos de verificación intencionalmente committeables. `schema_of` es PII-free por construcción (nombres de tipo solamente, no valores). Mirror Phase 2-4 pattern |
| T-5-11 | DoS | Driver ejecuta loops/retries contra Primary remarkets — riesgo de rate-limit del tenant | mitigate | Anti-pattern explícito (RESEARCH L603-604): single-shot driver, sin retries, sin loops. Cada probe es single-shot. Los 3 error probes always-on son lookups read-only sin auth-flow involucrado (Assumption A6 — marginal risk). Sin auth_401 deliberado live (REQUIREMENTS Out of Scope) |
| T-5-12 | Tampering | Mutación accidental contra remarkets (incluso si MATZ-06 es mock-only) | mitigate | `main_matriz.py` NO contiene llamadas a `new_order`/`replace_order`/`cancel_order` reachables en código de probes (verificable: acceptance criteria `grep` rechaza `primary.new_order(` no-comentado). El hostname assert + `mutating_allowed()` están disponibles como belt-and-suspenders. MATZ-06 tests live en Plan 05-03 son SOLO mocked, sin live calls |
| T-5-13 | Repudiation | Schema snapshot drift sobreescribe baseline silenciosamente | mitigate | D-25 verbatim en `_write_or_check_schema` (Task 2.3): si schema actual ≠ committed → finding SHAPE OPEN + NO sobreescribir. El operador clasifica el drift manualmente |
| T-5-14 | Information Disclosure | Risk API HTTP Basic Auth credentials pueden leak si Pitfall 7 (_token=None en rama auth_basic) dispara incorrectamente | mitigate | D-MATZ-12 fix solo en rama else (no-auth_basic) — Pitfall 7 RESEARCH L673-678. La rama auth_basic queda intacta. Risk API probes Plan 05-03 confirman este path. |
| T-5-15 | Tampering | main_higyrus.py refactor introduce regresión en Phase 4 tests | mitigate | Task 2.2 mandata `uv run pytest packages/higyrus-client/ -q` como acceptance non-regression. Helper promovido tiene **signature idéntica** + duck typing cross-package = compatible. Si tests fallan, refactor es bug |
| T-5-16 | Information Disclosure | Driver `main_matriz.py` puede leakear credenciales (`PRIMARY_USER`, `PRIMARY_PASSWORD`, `_token`) en stdout — PROBE/SUMMARY lines emitidas por la reescritura de la Task 2.3/2.4 | mitigate | **D-MATZ-32 verbatim**: el driver usa `safe_print(line, secrets=[PRIMARY_USER, PRIMARY_PASSWORD, _token])` para emitir TODA línea `PROBE` + `SUMMARY`. La lista de secrets se inicializa al import con `[PRIMARY_USER, PRIMARY_PASSWORD]` desde env; `_token` se agrega dinámicamente tras `probe_login_sync` (mirror D-IOL-7 / D-HIGY-15). Regex `_BEARER` interno en `verification/redaction.py` cubre tokens reflejados aun sin enumeración explícita. ASVS L1 V7.1.1 / V8.3.4. Acceptance Task 2.3/2.4: `grep -v '^#' main_matriz.py \| grep -c 'print('` == 0 (solo `safe_print`, no `print` raw); must_have truth #16 lo verifica explícitamente. **Implementa D-MATZ-32** (reformulación de T-5-09 con cita de decisión + acceptance gate). |
| T-5-17 | Elevation of Privilege / Tampering | Driver `main_matriz.py` ejecutado por error contra prod (`https://api.primary.com.ar`) por mis-configuración de `PRIMARY_BASE_URL` — riesgo de hitting prod con queries no-aprobadas + posible bloqueo de cuenta + telemetría real a Primary | mitigate | **D-MATZ-33 verbatim** belt-and-suspenders hostname assert al inicio del driver (Task 2.4 main()): `if 'remarkets' not in primary.client._base_url: print('ABORT: PRIMARY_BASE_URL=... is not a remarkets sandbox URL', file=sys.stderr); sys.exit(1)`. Ejecuta DESPUÉS de `require_env` (HARN-01 gate) y ANTES de cualquier llamada API. ASVS L1 V1.1.4 / V5.1.4. Acceptance Task 2.4: `PRIMARY_USER=test PRIMARY_PASSWORD=test PRIMARY_BASE_URL=https://api.primary.com.ar python main_matriz.py` aborta con exit 1 + stderr ABORT message; must_have truth #4 lo verifica explícitamente. **Implementa D-MATZ-33** (reformulación de T-5-08 con cita de decisión + acceptance gate). |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | Plan 05-02 NO instala paquetes externos. Toda la lógica usa stdlib + módulos pre-existentes. |

**Inherited threats from Phases 1-4 (still active):**
- T-1-01 (Information Disclosure): Credenciales en `.env` files gitignored por `.gitignore`; `.env.example` template público sin valores reales — confirmado por Task 2.1 acceptance.
- T-3-03 (Information Disclosure): `safe_print` con secrets dinámicos (Phase 3 pattern) replicado en main_matriz.py.
- T-4-02 (Repudiation): `append_finding` idempotent + status humano preservado (Phase 2 CR-01/CR-02/WR-04 hardened) — Plan 05-02 reusa sin modificación.
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. .env.example completo
grep -c "PRIMARY_ACCOUNT=" packages/matriz-client/.env.example   # == 1
grep -c "MATRIZ_SAMPLE_" packages/matriz-client/.env.example     # == 4

# 2. main_higyrus.py refactor
grep -c "_diff_safemodel_bidirectional" main_higyrus.py           # == 0 (removed)
grep -c "diff_safemodel_bidirectional" main_higyrus.py            # >= 2 (import + invocación)
grep -c "def _is_optional" main_higyrus.py                        # == 0

# 3. main_matriz.py reescrito completo
grep -c "def probe_" main_matriz.py                                # >= 22 (18 read-sweep + login + field_type_map + 3 error + schema_snapshot = 24, sin contar cycle_closure que es inline en main)
grep -c "def main(" main_matriz.py                                 # == 1
grep -c "asyncio" main_matriz.py                                   # == 0

# 4. Imports y barrel correctos
grep -c "from verification import" main_matriz.py                  # >= 1
grep -c "diff_safemodel_bidirectional" main_matriz.py              # >= 1
grep -c "verify_cycle_closure" main_matriz.py                      # >= 1

# 5. HARN-01 + D-MATZ-33 paths
env -u PRIMARY_USER -u PRIMARY_PASSWORD uv run python main_matriz.py 2>&1 | grep -F "SKIPPED matriz-client"
PRIMARY_USER=test PRIMARY_PASSWORD=test PRIMARY_BASE_URL=https://api.primary.com.ar uv run python main_matriz.py 2>&1 | grep -F "ABORT"

# 6. Static checks
uv run mypy main_matriz.py main_higyrus.py verification packages/matriz-client packages/higyrus-client
uv run ruff check .
uv run ruff format --check .

# 7. Suite completa green (cross-package non-regression)
uv run pytest -q
```

Per Wave 2 merge:
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `packages/matriz-client/.env.example` con 5 vars opt-in y comentarios documentando SKIPPED selectivo (D-MATZ-33)
- `main_higyrus.py` refactoreado: copia inline de `_diff_safemodel_bidirectional` removida; importa el helper promovido del barrel (D-MATZ-20)
- Tests Phase 4 (`packages/higyrus-client/tests/`) siguen verdes post-refactor
- `main_matriz.py` reescrito completo (~800-900 líneas) con:
  - ~25 probes nombrados en orden D-MATZ-29
  - sync-only (sin asyncio) — D-MATZ-30
  - HARN-01 require_env + D-MATZ-33 hostname assert remarkets
  - Cascade SKIPPED tras login failure (D-MATZ-31)
  - `_resolved_symbol` + `_resolved_segment` resolution flow (D-MATZ-1, D-MATZ-2)
  - SKIPPED selectivo D-MATZ-3 (PRIMARY_ACCOUNT) + D-MATZ-4 (MATRIZ_SAMPLE_*)
  - Market-hours guard probe-based D-MATZ-5 (LA.date stale > 2h)
  - CFI sanity sweep D-MATZ-6 (1 baseline ESXXXX + 8 type-only)
  - `get_all_instruments` + `get_instruments_details` ambos cubiertos D-MATZ-7
  - `get_trades` con date_from=today-7d D-MATZ-8
  - `_unwrap` ejercitado en Risk API probes (verifica el fix Plan 05-01 estructuralmente)
  - 3 error probes always-on con distinción HTTP 4xx vs status=ERROR (D-MATZ-22, D-MATZ-23)
  - field_type_map sobre 9 modelos sampleables via `diff_safemodel_bidirectional` (D-MATZ-21)
  - Schema snapshot envelope D-21 + D-25 no-overwrite-on-drift
  - cycle_closure loop × 4 paquetes (D-MATZ-28) con finding ERROR-MAP OPEN si falla
  - EXPECTED terminal prod-vs-remarkets como última operación sobre matriz-client (D-MATZ-27)
  - `safe_print(secrets=[user, password, _token])` D-MATZ-32 con secrets dinámicos
- mypy strict sin errores en todos los archivos modificados
- ruff check + format verdes en todo el repo
- `python main_matriz.py` (sin env) emite SKIPPED HARN-01 y exit 0
- `python main_matriz.py` (prod URL) emite ABORT D-MATZ-33 y exit 1
- `uv run pytest -q` sigue verde (whole repo)
- Plan 05-03 puede ejecutar el live run con `uv run --package matriz-client python main_matriz.py` contra remarkets reales
</success_criteria>

<output>
Crear `.planning/phases/05-matriz-verification/05-02-SUMMARY.md` cuando done, registrando:
- Conteo final de líneas en main_matriz.py (esperado: 800-900)
- Conteo final de probes en main_matriz.py (esperado: ~25 — enumerar)
- Confirmación del refactor main_higyrus.py: qué imports stdlib se removieron, qué líneas eliminadas (rango 205-293 + imports inicializados)
- Confirmación del `.env.example` con las 5 vars + comentarios
- Confirmación de que `python main_matriz.py` sin env emite SKIPPED HARN-01
- Confirmación de que `python main_matriz.py` con prod URL emite ABORT D-MATZ-33
- Cualquier dificultad con la firma de los endpoints matriz (e.g., kwargs requeridos vs defaults)
- Cualquier dificultad con `_risk_auth` (si no estaba exportado, cómo se resolvió)
- Métrica de duración del plan
</output>
</content>
</invoke>