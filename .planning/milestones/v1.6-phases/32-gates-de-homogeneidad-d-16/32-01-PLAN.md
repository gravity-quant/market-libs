---
phase: 32-gates-de-homogeneidad-d-16
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/matriz-client/tests/test_decode.py
  - packages/matriz-client/tests/test_ws_decode_mode.py
  - packages/higyrus-client/tests/test_decode.py
  - packages/ambito-financiero-client/tests/test_decode.py
autonomous: true
requirements:
  - GATE-TYP-01

must_haves:
  truths:
    - "`uv run mypy packages/<pkg>/tests` exits 0 for all six packages, run one package at a time exactly as ci.yml:92-99 does"
    - "`uv run mypy` (src global) still reports no issues in 62 source files"
    - "All 1682 package tests still pass; no test was deleted, skipped or xfailed to reach green"
    - "pyproject.toml is byte-unchanged — strictness policy was not relaxed to reach green"
  artifacts:
    - path: "packages/matriz-client/tests/test_decode.py"
      provides: "Narrow per-line type: ignore[attr-defined] on custom LogRecord attribute reads, mirroring the green iol analog"
      contains: "attr-defined"
    - path: "packages/matriz-client/tests/test_ws_decode_mode.py"
      provides: "arg-type fix at the single offending call site"
    - path: "packages/higyrus-client/tests/test_decode.py"
      provides: "func-returns-value fix (bare call, not `is None` assertion) + dead ignore removed"
    - path: "packages/ambito-financiero-client/tests/test_decode.py"
      provides: "func-returns-value fix + dead ignore removed"
  key_links:
    - from: "packages/matriz-client/tests/test_decode.py"
      to: "packages/iol-client/tests/test_decode.py"
      via: "same narrow-ignore shape on the identical custom LogRecord attributes (iol is already green under the same strict config)"
      pattern: "type: ignore\\[attr-defined\\]"
    - from: "packages/*/tests"
      to: ".github/workflows/ci.yml"
      via: "the per-package mypy loop at ci.yml:92-99 is the consumer that must go green"
      pattern: "mypy packages/\\$pkg/tests"
  prohibitions:
    - "Wave 0 must not be closed by relaxing type-checking policy: no edit to pyproject.toml [tool.mypy], no file-level or blanket `# type: ignore`, no per-file-ignores entry, no `--no-strict` flag. The 33 errors are fixed in test code only. (flagged — unverified)"
    - "A green mypy loop must not be bought by removing, skipping or xfailing a test. Test count per package must not drop. (flagged — unverified)"
---

<objective>
Restore the repository to a CI-green baseline before any Phase 32 gate work starts.

The `typecheck` job's per-package mypy loop is **RED today** — 33 errors across 3 of 6
packages, all inside Phase 29 test files — and has been invisible since 2026-08-18 because
every commit since then was docs-only and `ci.yml:6-8,11-13` carries `paths-ignore: ["**.md"]`.
Success criterion 5 of Phase 32 ("la matriz completa de CI queda verde con los gates nuevos
activos") is unachievable until this is fixed, and every later plan's CI-dependent verification
would otherwise be reported against an already-red job.

This is **pre-existing debt surfaced by research, not Phase 32 scope creep** (RESEARCH.md
§ The Criterion-5 Blocker, OQ-4 recommendation: include as Wave 0 with an explicit note).
It is recorded as such here so the phase SUMMARY does not read as silent scope expansion.

Purpose: give Waves 1-3 a truthful CI signal to verify against.
Output: four repaired test files; a recorded green baseline for every `ci.yml` job.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-gates-de-homogeneidad-d-16/32-RESEARCH.md
@.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the 29 mypy errors in matriz-client's Phase-29 decode tests</name>
  <files>packages/matriz-client/tests/test_decode.py, packages/matriz-client/tests/test_ws_decode_mode.py</files>
  <read_first>
    - `packages/matriz-client/tests/test_decode.py` (the file being modified — read fully; 28 of the 29 errors live here)
    - `packages/matriz-client/tests/test_ws_decode_mode.py` (the file being modified — the remaining 1 error, an `arg-type` around line 355)
    - `packages/iol-client/tests/test_decode.py` — the **green analog under the identical strict config**. Read lines 160-200, 290-310, 355-370, 425-445 and 1085-1100: it reads the same custom `LogRecord` attributes (`field_path`, `divergence`, `declared_type`, `observed_type`) and passes because each read carries a narrow `# type: ignore[attr-defined]`, and because the dynamic-name case at :435 uses `getattr(record, key)` instead.
    - `pyproject.toml` lines 83-97 — the `[tool.mypy]` block. Read it to confirm what is **off limits**: `strict`, `warn_unused_ignores`, `files` and `per-file-ignores` are not to be touched by this task.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md` § "Wave 0 — the 33 pre-existing mypy errors" — the analog excerpts with exact iol line numbers.
  </read_first>
  <action>
    Begin by re-enumerating the live error set rather than trusting the counts in RESEARCH.md: run `uv run mypy packages/matriz-client/tests` and capture the full output. Research measured 28 errors in `test_decode.py` (20 × `attr-defined`, 6 × `comparison-overlap`, 2 × `arg-type`) plus 1 × `arg-type` in `test_ws_decode_mode.py` at line 355; reconcile any drift against the live run and use the live run as authoritative.

    Fix each error class with the shape iol already proves green:

    (a) `attr-defined` on custom `LogRecord` attributes — these come from `caplog`-based assertions on the six flat divergence-record keys the Phase 29 decoder emits (`package`, `divergence`, `field_path`, `declared_type`, `observed_type`, `model`). `logging.LogRecord` does not declare them, and it must not be made to: the record contract is deliberately dynamic. Add a narrow `# type: ignore[attr-defined]` on the exact offending line, one per line, never a file-level or module-level suppression. Where the attribute name is a variable rather than a literal, switch the read to `getattr(record, name)` instead of adding an ignore — that is the shape iol uses at `test_decode.py:435`.

    (b) `comparison-overlap` (matriz `test_decode.py`, around lines 285-288) — mypy proves the two operands can never be equal because the declared field type and the asserted literal do not overlap. This happens where a test asserts a tolerantly-decoded wire value against a mismatched literal. Do not delete the assertion: it is pinning the tolerant-decode substitution behaviour. Narrow the comparison so the declared type is honoured — read the surrounding test to determine whether the correct fix is to assert against the substituted typed-zero value, or to compare through a `cast`-free intermediate typed as the wire type. Prefer whichever preserves the assertion's original intent; state which you chose in the SUMMARY.

    (c) `arg-type` (2 in `test_decode.py`, 1 in `test_ws_decode_mode.py` near line 355) — a value of the wrong declared type is being handed to a decoder/sink helper. Fix at the call site by passing a correctly typed value; only if the test's whole point is to feed a wrong-typed value should a narrow `# type: ignore[arg-type]` be used, and in that case add a one-line comment saying the wrong type is the subject of the test.

    Constraint A1 from RESEARCH.md is binding: **fix in test code only**. Do not edit `pyproject.toml`. Do not add a `[tool.ruff.lint.per-file-ignores]` entry. Do not weaken `warn_unused_ignores`. Any config change here is a policy decision requiring the developer's sign-off, not a mechanical fix, and this task has no authority to make it.

    Every ignore you add must be narrow (bracketed error code) and on the offending line. `warn_unused_ignores = true` is already on, so a stale ignore will itself become a mypy error later — that is the intended self-invalidation, keep it.
  </action>
  <acceptance_criteria>
    - `uv run mypy packages/matriz-client/tests` exits 0 and prints `Success: no issues found`.
    - `uv run pytest packages/matriz-client -q` reports 427 passed, 0 failed, 0 skipped-that-were-not-skipped-before (compare against the pre-change run captured at the start of the task).
    - `git diff --stat -- pyproject.toml` produces no output (the mypy/ruff config was not touched).
    - `git diff -- packages/matriz-client/tests | grep -c '^-.*def test_'` equals 0 — no test function was removed to reach green.
    - Every `# type: ignore` added in the diff carries a bracketed error code: `git diff -- packages/matriz-client/tests | grep '^+' | grep -c 'type: ignore\[' ` equals the total count of added ignores, i.e. `git diff -- packages/matriz-client/tests | grep '^+' | grep 'type: ignore' | grep -cv 'type: ignore\['` equals 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy packages/matriz-client/tests && uv run pytest packages/matriz-client -q</automated>
  </verify>
  <done>matriz-client's tests typecheck clean under strict mypy and its 427 tests still pass; no configuration file was modified.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the 4 mypy errors in higyrus-client and ambito-financiero-client decode tests</name>
  <files>packages/higyrus-client/tests/test_decode.py, packages/ambito-financiero-client/tests/test_decode.py</files>
  <read_first>
    - `packages/higyrus-client/tests/test_decode.py` around lines 610-700 (the file being modified — `func-returns-value` at ~624, `unused-ignore` at ~693)
    - `packages/ambito-financiero-client/tests/test_decode.py` around lines 785-880 (the file being modified — `func-returns-value` at ~799, `unused-ignore` at ~868)
    - `packages/iol-client/tests/test_decode.py` lines 670-730 — the green analog: it invokes the silent sink as a bare statement rather than asserting on its return value.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md` § "Analog for the `func-returns-value` class" and § "`unused-ignore`" — the exact offending and corrected shapes.
    - `.planning/STATE.md` § Blockers/Concerns, the two final bullets — these two packages' errors are already logged as deferred debt from Phase 31 (`deferred-items.md` D-2/D-3, "needs a five-copy repair plan before v1.6 ships"). This task is that repair.
  </read_first>
  <action>
    Run `uv run mypy packages/higyrus-client/tests` and `uv run mypy packages/ambito-financiero-client/tests` separately (never as one multi-path invocation — that fails with a duplicate-`conftest` module error, which is why `ci.yml:92-99` loops one package at a time) and capture both outputs.

    Fix the two error classes:

    (a) `func-returns-value` (higyrus ~624, ambito ~799) — the test asserts that the decoder's silent sink returns `None`. mypy knows the sink is annotated to return `None`, so the comparison is statically meaningless and strict mode rejects it. The assertion is not load-bearing: what the test is actually pinning is that invoking the sink does not raise and does not emit a record. Replace the `assert <sink call> is None` form with a bare call statement, exactly as `packages/iol-client/tests/test_decode.py` does. If the surrounding test would then assert nothing at all, add the assertion it was really trying to make (that the caplog record list for that scope stayed empty) rather than leaving an empty test body.

    (b) `unused-ignore` (higyrus ~693, ambito ~868) — the `# type: ignore[arg-type]` on the dataclass-fields dict comprehension is genuinely dead; the annotation it once suppressed now resolves. Delete the ignore comment. This is exactly the self-invalidation `warn_unused_ignores = true` exists to produce; deleting the dead ignore is the sanctioned response, not switching the setting off.

    Same binding constraint as Task 1: test code only. `pyproject.toml` is off limits.

    These two files are described elsewhere in the repo as "byte-frozen" copies. Verify before editing whether any intactness gate hashes them: run `uv run python tools/check_decode_intactness.py` before and after the edit. That gate hashes the five `_decode.py` **source** copies under `packages/*/src/`, not these test files, so it must stay green across the change — if it does not, stop and report rather than editing the gate.
  </action>
  <acceptance_criteria>
    - `uv run mypy packages/higyrus-client/tests` exits 0 with `Success: no issues found`.
    - `uv run mypy packages/ambito-financiero-client/tests` exits 0 with `Success: no issues found`.
    - `uv run pytest packages/higyrus-client -q` reports 236 passed; `uv run pytest packages/ambito-financiero-client -q` reports 200 passed.
    - `uv run python tools/check_decode_intactness.py` exits 0 (Checks A-D green, digest unchanged).
    - `git diff --stat -- pyproject.toml` produces no output.
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy packages/higyrus-client/tests && uv run mypy packages/ambito-financiero-client/tests && uv run pytest packages/higyrus-client packages/ambito-financiero-client -q && uv run python tools/check_decode_intactness.py</automated>
  </verify>
  <done>Both packages typecheck clean under strict mypy, their suites pass unchanged in count, and the decode-intactness gate is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Reproduce every ci.yml job locally and record the green baseline</name>
  <files>packages/matriz-client/tests/test_decode.py, packages/matriz-client/tests/test_ws_decode_mode.py, packages/higyrus-client/tests/test_decode.py, packages/ambito-financiero-client/tests/test_decode.py</files>
  <read_first>
    - `.github/workflows/ci.yml` in full — this task's job is to execute the same commands the four jobs execute. Note in particular: `lint` steps at lines 32-60, `typecheck` at 90-99 (the per-package loop), `test` at 122-128 (the explicit `packages/${{ matrix.package }}` path that overrides `testpaths`).
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-RESEARCH.md` § "The Criterion-5 Blocker" — the measured per-job baseline table this task must reproduce, now with the FAIL row flipped to PASS.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-VALIDATION.md` § "Wave 0 Requirements" — the checklist this task closes.
  </read_first>
  <action>
    Run, in order, the full local equivalent of every `ci.yml` job and record each command's exit status and headline output in the plan SUMMARY under a heading `CI-green baseline (Wave 0 close)`:

    `lint` job: `uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`; the `lint-logging` grep from `ci.yml:47` verbatim; `uv run python tools/check_decode_intactness.py`; `uv run python tools/check_uniform_structure.py`.

    `typecheck` job: `uv run mypy` (the src-global run — expect 62 source files at this point, since the market-data `files` enrollment is Plan 32-05's job, not this one); then the per-package loop over `higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client`, invoking `uv run mypy packages/<pkg>/tests` **one package per invocation** under `set -e` semantics.

    `test` job: `uv run pytest packages/<pkg> -q` for each of the six packages individually, matching the matrix shape. Record the per-package test counts; the expected totals from research are ambito 200, wallets 4, iol 242, higyrus 236, market-data 573, matriz 427 = 1682.

    Do not attempt the py3.13 matrix leg locally unless `uv` already has 3.13 provisioned; if it does (research measured 3.13.12 as uv-managed), run at least one package under `uv run --python 3.13 pytest packages/iol-client -q` as a smoke check and say so in the SUMMARY. If it does not, record that the 3.13 leg is deferred to the real CI run in Plan 32-06 rather than claiming it passed.

    Record explicitly in the SUMMARY, in one sentence, that this wave repaired **pre-existing Phase-29 debt discovered by Phase 32 research and not part of GATE-TYP-01's stated scope**, so the phase's scope history stays honest.

    Do not modify any file in this task. If a command fails, return to Task 1 or Task 2 rather than patching around it here.
  </action>
  <acceptance_criteria>
    - Every command listed above exits 0.
    - The per-package mypy loop prints `Success: no issues found` six times, once per package.
    - Total collected tests across the six package runs is 1682 (or higher — never lower).
    - `git status --porcelain` shows no unstaged modification introduced by this task.
    - The plan SUMMARY contains a `CI-green baseline (Wave 0 close)` section listing each command with its exit status, and the one-sentence pre-existing-debt disclosure.
  </acceptance_criteria>
  <verify>
    <automated>uv lock --check && uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run mypy && for p in higyrus-client wallets-client matriz-client iol-client ambito-financiero-client market-data-client; do uv run mypy packages/$p/tests || exit 1; done && uv run pytest packages -q</automated>
  </verify>
  <done>Every ci.yml job passes locally; the baseline and the pre-existing-debt disclosure are recorded in the SUMMARY.</done>
</task>

</tasks>

## Artifacts this phase produces

Phase-wide list of symbols and files created by Phase 32 (used by the plan-review-convergence
source-grounding pass to exclude newly-created symbols from drift verification). This plan
(32-01) creates **none of them** — it only repairs existing test files — but the list is
reproduced in every plan of the phase so any single plan can be reviewed in isolation.

**New files**
- `tools/check_surface_types.py` (Plan 32-02)
- `tools/surface_parity.py` (Plan 32-04)
- `packages/iol-client/tests/test_surface_types_red.py` (Plan 32-02)
- `packages/market-data-client/tests/test_surface_parity.py` (Plan 32-04)
- `packages/ambito-financiero-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/iol-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/higyrus-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/matriz-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/wallets-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/market-data-client/tests/test_core_boundary_red.py` (Plan 32-05)

**New symbols in `tools/check_surface_types.py`** — `REPO_ROOT`, `_BUILD_ARTIFACT_SUFFIX`,
`CheckFailure`, `_fail`, `_import_root`, `_all_names`, `_definition_sites`, `_is_exempt`,
`_annotation_mentions_any`, `ScanResult`, `scan_surface_types`, `check_surface_types`, `main`

**New symbols in `tools/surface_parity.py`** — `MODULE_LOWER_BOUNDS`, `ParityReport`,
`public_names`, `normalized_hints`, `module_parity_report`, `class_parity_report`,
`assert_module_parity`, `assert_class_parity`, `assert_module_lower_bound`

**New CI step** — `surface-types (Phase 32 GATE-TYP-01 …)` in the `lint` job of
`.github/workflows/ci.yml` (Plan 32-02)

**New public API** — keyword-only parameter `http_client: httpx.AsyncClient | None = None` on
`market_data_client.aio.configure`, plus an `import warnings` at module scope in
`packages/market-data-client/src/market_data_client/aio.py` (Plan 32-04)

## Assumptions (flagged — unverified)

- **Interruption / parallelism safety of the phase's new gates is asserted by construction, not
  verified.** CI runs one package × one Python version per matrix cell with no shared mutable
  state across legs, and the new gates are read-only (`tools/check_surface_types.py` and
  `tools/surface_parity.py` never write) except the import-linter RED test of Plan 32-05, which
  mutates a tracked file inside `try/finally`. Nobody has exercised the interrupted or
  concurrently-running case. Recorded as an open assumption, not a verified property.
- **A4 (RESEARCH.md):** the 33 mypy errors are attributed to Phase 29 source rather than a mypy
  version regression, on the strength of `git log` provenance plus the 2026-08-18 green CI under
  the same pinned mypy 1.20.2. An isolated cross-check against mypy 1.13 was inconclusive.
- **A1 (RESEARCH.md):** the 33 errors are fixable in test code alone. If any one of them proves
  to require a `pyproject.toml` strictness change, that is a policy decision needing the
  developer's sign-off — stop and escalate rather than making it inside this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository source | The typecheck loop reads repository source only; no network, no credentials |
| test process → per-package `.env` files | Must never be crossed: `caplog`-based decode tests assert on log records that are contractually type-not-value and carry no wire values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Information Disclosure | `caplog` assertions in the four repaired decode test files | mitigate | Fixes are limited to type annotations, narrow ignores and call shape; no assertion is changed to print a wire value. The Phase 29 SEC-01 sentinel per package (`test_logging.py`) stays green and is re-run by Task 2's verify. |
| T-32-08 | Repudiation | The typecheck gate itself | mitigate | Green is proven by re-running the exact `ci.yml:92-99` loop one package at a time (Task 3), not by a single multi-path mypy invocation that silently fails on duplicate `conftest` modules |
| T-32-09 | Tampering | `pyproject.toml` strictness policy | mitigate | Prohibition + acceptance criterion: `git diff --stat -- pyproject.toml` must be empty in Tasks 1 and 2 |
| T-32-SC | Tampering | package-manager installs (uv/pip) | accept | This phase installs zero external packages; `uv lock --check` is the first `lint` step and fails on any lockfile movement. No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required (RESEARCH.md § Package Legitimacy Audit). |
</threat_model>

<verification>
- `uv run mypy packages/<pkg>/tests` exits 0 for all six packages, invoked one package per run
- `uv run mypy` (src global) exits 0
- `uv run pytest packages -q` collects ≥ 1682 tests, all passing
- `uv run ruff check . && uv run ruff format --check .` exit 0
- `uv run lint-imports` reports 5 contracts kept, 0 broken
- Both existing `tools/` gates exit 0
- `git diff --stat -- pyproject.toml` is empty
</verification>

<success_criteria>
The `typecheck` job is green for the first time since 2026-08-18, reproduced locally with the
same per-package loop CI uses, with zero configuration changes and zero test removals. Waves 1-3
can now verify against a truthful CI signal.
</success_criteria>

<output>
Create `.planning/phases/32-gates-de-homogeneidad-d-16/32-01-SUMMARY.md` when done.
</output>
