---
phase: 15-driver-migration-4-refac-05
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - main_ambito_financiero.py
  - verification/test_main_ambito_financiero_uses_single_client_instance.py
  - .planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md
autonomous: true
requirements: [REFAC-05]
user_setup:
  - service: ambito-financiero (live)
    why: "Per-package LIVE smoke at end of migration (D-11, Criterion #4) — operator-driven, not the Phase 17 gate"
    dashboard_config:
      - task: "Operator runs `uv run --package ambito-financiero-client python main_ambito_financiero.py` and confirms it exits 0 with findings written"
        location: "Local terminal (no dashboard; no auth required for ámbito)"

must_haves:
  truths:
    - "main_ambito_financiero.py main() constructs exactly one sync Client(); _async_main() constructs exactly one AsyncClient() — D-01/D-02"
    - "Every probe_* receives its client/aclient as a parameter; no probe reaches _get_default() — D-01"
    - "The AST-guard test fails RED against the un-migrated driver and passes GREEN after migration — Criterion #1"
    - "Finding titles/fids/probe-names in ambito-financiero-client-findings.md are byte-stable vs baseline — Criterion #2"
    - "LOC-attestation doc pins the baseline anchor and records the residual delta — Criterion #5 / D-08/D-09"
  artifacts:
    - path: "verification/test_main_ambito_financiero_uses_single_client_instance.py"
      provides: "AST-walker asserting <=2 Client/AsyncClient ctors in main_ambito_financiero.py"
      contains: "test_main_ambito_financiero_uses_single_client_instance"
      min_lines: 25
    - path: "main_ambito_financiero.py"
      provides: "Migrated driver — single sync Client + single async AsyncClient threaded into probes"
      contains: "Client()"
    - path: ".planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md"
      provides: "Baseline-anchor resolution + LOC delta attestation (D-08/D-09)"
      contains: "baseline anchor"
  key_links:
    - from: "main_ambito_financiero.py"
      to: "ambito_financiero_client (Client/AsyncClient)"
      via: "bare construction Client() / AsyncClient() threaded into every probe signature"
      pattern: "(Async)?Client\\(\\)"
    - from: "verification/test_main_ambito_financiero_uses_single_client_instance.py"
      to: "main_ambito_financiero.py"
      via: "ast.parse + ast.walk counting ast.Call ctor nodes (ast.Name AND ast.Attribute)"
      pattern: "ast\\.walk"
---

<objective>
Migrate `main_ambito_financiero.py` (simplest driver, migrated FIRST per D-11) so `main()` builds exactly one sync `Client()` and `_async_main()` builds exactly one async `AsyncClient()`, both threaded as parameters into every `probe_*`. Author the AST-guard test FIRST (RED → GREEN) and establish the LOC-attestation doc with the resolved baseline anchor (D-08/D-09) reused by all later plans.

Purpose: This is Wave 1 — it establishes (a) the AST-walker test idiom every later driver copies, (b) the constructor-style pinning convention (D-05), and (c) the baseline-anchor attestation. Smallest blast radius first.
Output: Migrated `main_ambito_financiero.py`, the first AST-guard test, and `15-LOC-ATTESTATION.md`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-driver-migration-4-refac-05/15-CONTEXT.md
@.planning/phases/15-driver-migration-4-refac-05/15-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — AST-guard test for single-Client invariant (Criterion #1)</name>
  <files>verification/test_main_ambito_financiero_uses_single_client_instance.py</files>
  <read_first>
    - verification/test_main_drivers_bare_except.py (the ONLY AST-walker analog in the repo; copy `_REPO_ROOT`, parametrize idiom, ast.parse + ast.walk body)
    - main_ambito_financiero.py:48-54 (current import block — module-qualified `import ambito_financiero_client as ambito`)
    - 15-PATTERNS.md lines 75-110 (copy-ready ADAPT excerpt for the ctor-counting walker, both ast.Name and ast.Attribute)
  </read_first>
  <behavior>
    - Test asserts: walking the AST of main_ambito_financiero.py counts <=2 Client/AsyncClient constructor Calls.
    - RED: against the un-migrated driver (which constructs ZERO classes today — all via `_get_default()`), the count is 0, which is <=2, so a naive count-only assertion would FALSE-PASS. To make RED meaningful, the test must ALSO assert the migrated driver constructs at least 1 (sync) — i.e. assert `1 <= count <= 2`. Against the un-migrated driver count==0 → FAILS (RED). After Task 2 migration count==2 → PASSES (GREEN).
    - Walker matches BOTH ast.Name(id in {"Client","AsyncClient"}) and ast.Attribute(attr in {"Client","AsyncClient"}) per D-05 — covers whichever import style Task 2 pins, eliminating false-green.
    - `with_options(...)` (ast.Attribute attr="with_options") is NOT counted.
  </behavior>
  <action>
    Create `verification/test_main_ambito_financiero_uses_single_client_instance.py` modeled exactly on `test_main_drivers_bare_except.py`. Reuse `from __future__ import annotations`, `import ast`, `from pathlib import Path`, `import pytest`, and `_REPO_ROOT = Path(__file__).resolve().parent.parent`. Define test function `test_main_ambito_financiero_uses_single_client_instance` (no parametrize — one file per driver per D-04, PATTERNS.md recommendation). Walk `ast.parse((_REPO_ROOT / "main_ambito_financiero.py").read_text(encoding="utf-8"))`; collect ctor sites where `node` is `ast.Call` AND (`node.func` is `ast.Name` with `id` in the ctor-name set OR `node.func` is `ast.Attribute` with `attr` in the ctor-name set). Define the ctor-name set as a module constant holding the two class names. Assert `1 <= len(ctor_sites) <= 2` with a message embedding the driver name and the collected `(lineno, name)` sites. Do NOT inline the literal class-name strings into any commented "what NOT to count" example — name `with_options` only by concept in a one-line comment. Run RED first against un-migrated driver (must FAIL with count 0 violating the lower bound).
    <!-- planner-discipline-allow: with_options -->
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_ambito_financiero_uses_single_client_instance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED: before Task 2, the test FAILS (count==0, violates `>=1` lower bound) — proving it is not vacuously green.
    - GREEN: after Task 2, the test PASSES (count==2).
    - Walker covers both ast.Name and ast.Attribute ctor forms (grep the file for both `ast.Name` and `ast.Attribute`).
    - `with_options` is not in the ctor-name set.
  </acceptance_criteria>
  <done>Test file exists, fails RED on un-migrated driver, passes GREEN post-migration; matches both constructor styles per D-05.</done>
</task>

<task type="execute">
  <name>Task 2: Migrate main_ambito_financiero.py — one Client() + one AsyncClient(), threaded into probes (D-01/D-02/D-03)</name>
  <files>main_ambito_financiero.py</files>
  <read_first>
    - main_ambito_financiero.py (full driver — 734 LOC; the file being modified)
    - 15-PATTERNS.md lines 114-188 (ámbito migration recipe: import block, probe signatures, _state read sites :141,:228,:318,:375,:456,:510,:575,:576, _async_main + aclose template)
    - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py:30-51 (Client/AsyncClient flat-namespace exports)
    - CLAUDE.md anti-patterns (never share sync httpx.Client into async code)
  </read_first>
  <action>
    Use MINIMAL bare construction only — `Client()` / `AsyncClient()` with no args (env-driven), NO `with_options()`/`from_env()` showcasing (D-10). PIN the constructor style: add `from ambito_financiero_client import Client, AsyncClient` to the import block (main_ambito_financiero.py:48-54) — bare style → `ast.Name` in the walker. (Either bare or `ambito.Client(...)` qualified is valid since the Task-1 walker covers both; document the chosen style is BARE in the SUMMARY.)
    In `main()` (around :687-701): construct `client = Client()` once near the top, and thread `client` as a parameter into every sync probe call (e.g. `probe_happy_sync(today, client)`). Add the `client` parameter to each sync `def probe_*` signature.
    In `_async_main()` (around :661-679): construct `aclient = AsyncClient()` at the top, thread `aclient` into each async probe call, and change the `finally` teardown from `await aio.aclose()` to `await aclient.aclose()` (keep the existing `contextlib.suppress` so teardown failure cannot crash exit 0). Add `aclient` to each async `def probe_*` signature.
    Mechanical 1:1 `_state` swaps (D-03), CODE sites only — `:141, :318, :375, :456, :510, :575, :576` (sync) → `client._state.<attr>`; `:228` (async) → `aclient._state.<attr>`. DO NOT rewrite the docstring occurrences at `:561, :563` (operator-facing prose per D-03).
    Swap transport/public calls: `ambito.client._request(...)` → `client._request(...)`; `aio._request(...)` → `await aclient._request(...)`; `ambito.get_dollar_banco_nacion(...)` (:458,:580) → `client.get_dollar_banco_nacion(...)`.
    DO NOT touch any `title=`/`fid=`/`class_=`/probe-name literal passed to `append_finding(...)` (D-06). Keep sync `Client` and async `AsyncClient` as SEPARATE instances (D-02); never pass the sync client into the async coroutine.
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_ambito_financiero_uses_single_client_instance.py -x && uv run --frozen ruff check main_ambito_financiero.py && uv run --frozen mypy main_ambito_financiero.py</automated>
  </verify>
  <acceptance_criteria>
    - AST-guard test passes GREEN (count==2: one Client + one AsyncClient).
    - `grep -c '_get_default()' main_ambito_financiero.py` returns 0 for CODE lines (docstring prose at :561,:563 excluded; verify with `grep -n '_get_default' main_ambito_financiero.py` showing only docstring lines remain).
    - ruff check + mypy strict pass on the driver.
    - Every `def probe_*` signature carries a `client` or `aclient` parameter (grep `def probe_` then confirm each has the param).
    - No `append_finding(... title=/fid=/class_= ...)` literal changed (verify via `git diff main_ambito_financiero.py` showing no title/fid/class_ string edits).
  </acceptance_criteria>
  <done>Driver constructs exactly 1 sync + 1 async client, threads them into all probes, mechanical _state swaps complete, finding literals untouched, CI-green (ruff+mypy).</done>
</task>

<task type="execute">
  <name>Task 3: Establish LOC-attestation doc — resolve baseline anchor + record delta (D-08/D-09, Criterion #5)</name>
  <files>.planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md</files>
  <read_first>
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-03-SUMMARY.md:95-130 (LOC Drop Analysis — Phase 6 baseline iol client.py 522→490, aio.py 476→457; structurally-unreachable 30% disposition)
    - 15-CONTEXT.md D-08/D-09 (measure-only attestation; pin exact baseline anchor before computing delta)
    - spike-findings-codegen-market-libs SKILL (Phase 12 codegen NO-GO that makes physical LOC reduction out of scope — context only)
  </read_first>
  <action>
    Create `.planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md`. Resolve and PIN the baseline anchor: there is NO `v1.0` git tag (only `v1.1` and per-package `*-v0.1.1` tags); the documented anchor is the Phase 6/7 extraction baseline from 07-03-SUMMARY.md (iol client.py 522 + aio.py 476 = 998 aggregate). Record CURRENT measured LOC via `wc -l`: iol client.py=756, aio.py=755 (aggregate 1511); matriz client.py=922. Compute and document the delta vs the pinned anchor (note LOC has GROWN since Phase 7 extraction, so the −30% Criterion #5 target is structurally unreachable — physical reduction was Phase 16 codegen work, DROPPED per Phase 12 NO-GO; this phase is MEASURE-ONLY attestation per D-08). State explicitly that back-compat shims STAY intact (no shim deletion to chase the target). Record that the milestone test baseline (≥907 passing) must hold — capture the actual collected count by running the suite. Document the residual gap as operator-accepted carry-forward to v1.3.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md && grep -qi 'baseline anchor' .planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md && grep -q '1511' .planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Doc exists; pins the Phase 6/7 baseline anchor (998 aggregate iol) and states no v1.0 tag exists.
    - Records current LOC iol=1511, matriz=922; documents delta and the structurally-unreachable −30% disposition.
    - States shims STAY; references the ≥907-test baseline obligation.
  </acceptance_criteria>
  <done>Attestation doc pins the anchor, records the delta, and documents the measure-only disposition — no physical LOC edits to library code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → ámbito live API | driver issues live HTTP to a third-party FX service (no auth for ámbito) |
| driver process → module singleton state | `_state` reads/writes cross from probe code into per-instance client state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | findings/logs/commits | mitigate | Driver migration changes only how the client is acquired; no credential is logged or written to findings. ámbito requires no auth, so no secret surface — but the no-secret-in-logs rule is preserved verbatim. |
| T-15-02 | Tampering | multiple Client construction → state churn | mitigate | The ≤2-ctor AST gate (Task 1) IS the mitigation; ámbito has no token store but the invariant is established here for later TokenStore-sensitive drivers. |
| T-15-03 | Denial of Service | live ámbito API rate limits | accept | Operator-driven single smoke run; low request volume, below `high` severity. |
| T-15-SC | Tampering | uv/pip installs | mitigate | No new packages installed this phase (drivers + tests only). RESEARCH.md skipped — no package legitimacy audit required because no installs occur. |
</threat_model>

<verification>
- AST-guard test passes (count==2); RED→GREEN transition observed.
- `_get_default()` eliminated from CODE lines of main_ambito_financiero.py (docstring prose excluded).
- ruff check + mypy strict green on the driver.
- LOC-attestation doc exists with pinned anchor.
- Operator confirms the per-package LIVE smoke exits 0 (Criterion #4, operator-driven).
</verification>

<success_criteria>
- main_ambito_financiero.py constructs exactly 1 sync Client + 1 async AsyncClient, threaded into every probe.
- AST-guard test asserts 1<=ctors<=2 and is not vacuously green (RED proven).
- Finding titles/fids/probe-names byte-stable.
- LOC-attestation doc pins the Phase 6/7 baseline anchor and records the residual.
</success_criteria>

<artifacts_this_phase_produces>
**New test file + test function:**
- `verification/test_main_ambito_financiero_uses_single_client_instance.py` → `test_main_ambito_financiero_uses_single_client_instance()`

**New driver import line:**
- `main_ambito_financiero.py`: `from ambito_financiero_client import Client, AsyncClient` (bare style, pinned per D-05)

**New helper signatures (threading parameters added):**
- Every sync `def probe_*(...)` in main_ambito_financiero.py gains a `client` parameter.
- Every async `def probe_*(...)` gains an `aclient` parameter.

**New planning artifact:**
- `.planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-driver-migration-4-refac-05/15-01-SUMMARY.md` when done
</output>
