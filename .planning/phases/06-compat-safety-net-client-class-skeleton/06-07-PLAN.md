---
phase: 06-compat-safety-net-client-class-skeleton
plan: 07
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - verification/mutation_gate.py
autonomous: false
requirements: [REFAC-01, REFAC-02]

user_setup: []

must_haves:
  truths:
    - "Full test suite (277 baseline + Phase 6 new tests) passes via `uv run pytest -q` locally and in CI for Python 3.12 AND Python 3.13 — resolves checker B4: both Python versions explicitly verified in Task 1's automated block via `uv run --python 3.13`."
    - "ruff check + ruff format --check + mypy --strict packages/*/src pass for all 4 target packages on both Python 3.12 and 3.13."
    - "Public-surface snapshot test green: `uv run pytest verification/test_public_surface.py -q` produces no diff against committed snapshots; baseline entries preserved per D-06."
    - "All 8 fixture-reaches-production guards across the 4 per-package files (4 sync + 3 async + 1 matriz async skip) pass."
    - "No regressions in main_*.py drivers — `python -m py_compile main_*.py` succeeds for every driver (resolves checker W4: replaces the fragile `exec(open(...).read().split('if __name__')[0])` approach with a single safe syntax check)."
    - "verification/mutation_gate.py audited: confirmed it works unchanged via the matriz `_base_url` shim extension; smoke test confirms the gate still returns False for non-sandbox hosts (resolves checker B6: Plan 07 owns the file audit, not Plan 06)."
  artifacts: []
  key_links:
    - from: "CI workflow (.github/workflows/ci.yml)"
      to: "Python 3.12 + 3.13 matrix × 4 packages"
      via: "Matrix job already established; no workflow file edit needed"
      pattern: "matrix.python-version"
    - from: "verification/mutation_gate.py (audit)"
      to: "matriz_client.client._base_url (read through PEP 562 shim)"
      via: "Plan 07 audits cross-package gate file; Plan 06 left it untouched (B6 split of ownership)"
      pattern: "matriz_client\\.client\\._base_url"
---

<objective>
Final phase gate: prove the 4-package refactor (Plans 03-06) plus the safety net (Plans 01-02) are green end-to-end on BOTH Python 3.12 AND Python 3.13. This plan does NOT modify production code or tests — it RUNS the full verification stack on both interpreter versions, and audits `verification/mutation_gate.py` to confirm the matriz shim's `_base_url` forwarding (Open Q #4) does not break the gate logic.

**B4 — Python 3.13 in automated verify:** Task 1's `<automated>` block now explicitly runs `pytest`, `ruff check`, `ruff format --check`, and `mypy --strict` under BOTH `uv run` (default Python — typically 3.12) AND `uv run --python 3.13`. Acceptance: exit 0 on both 3.12 AND 3.13. ROADMAP.md Phase 6 success criterion 5 requires both.

**B6 — Plan 07 owns mutation_gate.py audit:** Plan 06 explicitly left `verification/mutation_gate.py` untouched. This plan audits it: confirm the `_base_url` PEP 562 shim from matriz forwards correctly, the sandbox-vs-prod gate logic is unaffected, and no edit is needed. If an inconsistency is discovered during audit, this plan performs the minimal fix. `verification/mutation_gate.py` IS in this plan's `files_modified` to capture the (likely no-op) audit + any small adjustment.

**W4 — Driver smoke uses `python -m py_compile`:** Replaces the fragile `exec(open(...).read().split('if __name__')[0])` pattern with `python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py` (single command checks all 4 drivers' syntax without executing them). Wallets driver out of phase scope; optional to include.

Purpose: REFAC-01 and REFAC-02 success criteria 1-5 are all proven by THIS plan running green on BOTH interpreter versions. Any failure here means a defect in Plans 03-06 (Wave 1) — go back and fix it before merging the phase.

Output: 1 file potentially modified (`verification/mutation_gate.py` — likely unchanged after audit) + 1 checkpoint task for operator approval after automated checks pass. The checkpoint message includes a copy-paste-ready CI status check and a structured driver smoke result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-01-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-02-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-03-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-04-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-05-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-06-PLAN.md
@verification/mutation_gate.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run automated phase verification on Python 3.12 AND 3.13 — full pytest + ruff + mypy + snapshot diff + driver py_compile + mutation_gate audit</name>
  <files>verification/mutation_gate.py</files>
  <read_first>
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (Sampling Rate; Validation Sign-Off checklist)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-07 test cadence)
    - verification/mutation_gate.py (line 55: `base = matriz_client.client._base_url` — audit target)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-06-PLAN.md (matriz `_base_url` shim extension that this audit depends on)
  </read_first>
  <behavior>
    - `uv run pytest -q` exits 0 on Python 3.12; collected test count = 277 baseline + Phase 6 new tests (estimate: 277 + ~50 new = ~327 tests + ~1 skipped for matriz async).
    - `uv run --python 3.13 pytest -q` exits 0 on Python 3.13 with the same collection count.
    - `uv run ruff check .` exits 0 (no lint errors in any modified file).
    - `uv run ruff format --check .` exits 0 (no formatting drift).
    - `uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src` exits 0 on 3.12.
    - `uv run --python 3.13 mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src` exits 0 on 3.13.
    - `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` exits 0 (snapshots match what's committed — proves the regen script is stable AND the live surface matches expectations).
    - `python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py` exits 0 — confirms all 4 driver scripts parse cleanly under the new module shape (W4 replacement for the fragile exec/split idiom). The drivers are NOT executed end-to-end against live APIs in CI (env_gate would block them); this task only confirms syntax is intact.
    - `verification/mutation_gate.py` audit: configure matriz to sandbox host → `mutating_allowed()` returns True; configure to prod host → returns False. The line 55 read `matriz_client.client._base_url` goes through the matriz PEP 562 shim (Plan 06 extension) and resolves to `_get_default()._state.base_url`. If the audit reveals an inconsistency (e.g., shim returns None when it should return the configured URL), THIS task makes the minimal fix to `verification/mutation_gate.py` (e.g., switch to explicit `matriz_client._get_default()._state.base_url` if the shim path is broken). If no inconsistency, the file is left untouched and this plan's `files_modified` entry is a no-op audit record.
    - All checks above pass on Python 3.12. Same set repeats on 3.13.
  </behavior>
  <action>
    Execute the following commands in order, capturing exit codes. Stop on first failure and surface the failure to the checkpoint task (Task 2). NO source code edits in this task EXCEPT possibly `verification/mutation_gate.py` if the audit (step 7) finds an inconsistency.

    1. **Full pytest sweep on Python 3.12:**
       `uv run pytest -q`
       Expected: 0 failures; ~327 tests passed + ~1 skipped (matriz async guard).

    2. **Full pytest sweep on Python 3.13 (B4):**
       `uv run --python 3.13 pytest -q`
       Expected: same as 3.12. Same count of passed + skipped.

    3. **Ruff lint + format check (both versions):**
       `uv run ruff check .`
       `uv run ruff format --check .`
       Ruff is interpreter-agnostic (Rust), so running once is sufficient. Both exit 0.

    4. **Mypy strict on Python 3.12:**
       `uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src`
       Exit 0.

    5. **Mypy strict on Python 3.13 (B4):**
       `uv run --python 3.13 mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src`
       Exit 0. Note: `wallets-client` is intentionally excluded per phase scope.

    6. **Public-surface snapshot self-check:**
       `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/`
       Exit 0 — proves snapshots are reproducible and match committed state.

    7. **Driver py_compile smoke (W4 — no live calls):**
       `uv run python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py`
       Single command checks all 4 driver scripts for syntax errors. Exit 0. Does NOT execute any code in the drivers — purely a parse/byte-compile check. The shim contract (e.g., main_higyrus.py mutating `pkg.client._client.event_hooks` per D-21) is NOT exercised by py_compile; it is exercised by the OPTIONAL operator-driven manual smoke in Task 2 step 4.
       `main_wallets.py` is OPTIONALLY included: `uv run python -m py_compile main_wallets.py` — wallets is out of phase scope but py_compile is cheap and confirms the broader workspace is still parseable. Failure here is informational, not blocking.

    8. **mutation_gate audit (B6):** import matriz_client, `configure(base_url="https://api.remarkets.primary.com.ar")`, set `VERIFY_MUTATING=1`, assert `mutating_allowed() is True`. Then `configure(base_url="https://api.primary.com.ar")` and assert `mutating_allowed() is False`. This exercises the matriz `_base_url` shim extension (Plan 06 Open Q #4) end-to-end. If the audit PASSES, leave `verification/mutation_gate.py` untouched and record "audit-only, no edits required" in the SUMMARY. If the audit FAILS (e.g., the shim returns None when a configured URL is expected), apply the minimal fix to `verification/mutation_gate.py` (e.g., replace `base = matriz_client.client._base_url` with `base = matriz_client._get_default()._state.base_url`) and re-run the audit.

    9. **Compile a structured pass/fail report** for Task 2 to consume. Format:

        Phase 6 verification report
        ============================
        [PASS|FAIL] uv run pytest -q                                        (3.12, <N> tests, <S> skipped)
        [PASS|FAIL] uv run --python 3.13 pytest -q                          (3.13, <N> tests, <S> skipped)
        [PASS|FAIL] uv run ruff check .
        [PASS|FAIL] uv run ruff format --check .
        [PASS|FAIL] uv run mypy --strict packages/*/src                     (3.12, per package)
        [PASS|FAIL] uv run --python 3.13 mypy --strict packages/*/src       (3.13, per package)
        [PASS|FAIL] Snapshot regen idempotent                                (regen + git diff --exit-code)
        [PASS|FAIL] python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py
        [PASS|FAIL] python -m py_compile main_wallets.py                    (optional / out of phase scope)
        [PASS|FAIL] mutation_gate audit (sandbox=True / prod=False)         (B6 — file edit only if audit fails)
        [PASS|FAIL] verification/mutation_gate.py edits required             (PASS = no edits; FAIL = audit found a defect, edits applied)

  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run --python 3.13 pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src &amp;&amp; uv run --python 3.13 mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src &amp;&amp; uv run python verification/regen_snapshots.py &amp;&amp; git diff --exit-code verification/snapshots/ &amp;&amp; uv run python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py</automated>
  </verify>
  <done>
    All 11 checks in the structured pass/fail report (step 9) show PASS, with both Python 3.12 AND Python 3.13 runs passing for pytest and mypy. Any FAIL surfaces a defect in a prior plan — STOP and fix that plan before proceeding to Task 2. The report is the input to the checkpoint. `verification/mutation_gate.py` is either untouched (audit pass) or minimally edited (audit fix applied) — both outcomes are acceptable as long as the gate audit ends PASS.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies CI green on push (Python 3.12 + 3.13 matrix) + reviews snapshot diff</name>
  <files></files>
  <what-built>
    Phase 6 ships: 2 verification harness additions (snapshot test + 8 per-package fixture-reaches-production guards
    across 4 packages),
    4 per-package skeletons (`_state.py` + `Client` + `AsyncClient` per package; matriz async is stub-only),
    PEP 562 read-only shims preserving legacy `pkg.client._token` etc., conftest migrations away from
    `monkeypatch.setattr(... raising=False)` to `pkg.configure(token=...)`, all inline test-body
    monkeypatch sites migrated, ws_client.py + cross-package mutation gate test migrated, and a Phase 6
    entry-baseline file at `verification/baselines/phase-06-baseline.txt`.

    All automated checks in Task 1 must be PASS before this checkpoint fires (including BOTH Python 3.12
    AND Python 3.13 runs). The pass/fail report from Task 1 step 9 is included verbatim.
  </what-built>
  <action>
    Operator action: review Task 1 pass/fail report, push the branch, watch the CI matrix on Python 3.12 + 3.13,
    review the snapshot diff for additive-only changes, and approve. See `<how-to-verify>` for the exact steps.
  </action>
  <how-to-verify>
    1. Confirm the Task 1 pass/fail report shows 11 PASS lines (no FAIL). Confirm that BOTH 3.12 and 3.13
       pytest/mypy lines are PASS (B4).

    2. Review the snapshot diff: `git diff verification/snapshots/`. Each of the 4
       `<pkg>-surface.txt` files should show ONLY additive lines (`Client : class : ...`,
       `AsyncClient : class : ...`, etc.) — NO line should be REMOVED from any baseline file
       (D-06 invariant). Each snapshot header still has 8 lines starting with `#`, line 8 == `#` (W3 invariant).

    3. Review the baseline diff: `git show HEAD verification/baselines/phase-06-baseline.txt`. Confirm it
       contains `test_count:`, `coverage_total:`, `git_sha:` lines (B5 invariant from Plan 01 Task 4).

    4. Push the branch and check the CI Actions tab:
       - lint job: green
       - typecheck job: green
       - tests job × Python 3.12 × 4 packages: green
       - tests job × Python 3.13 × 4 packages: green
       Expected total: ~10 green jobs depending on the matrix.

    5. Manual smoke (OPTIONAL — operator-driven, NOT required for phase close):
       For one of the 4 drivers (recommend `main_higyrus.py` because it mutates `_client.event_hooks`
       per D-21), run `uv run --package higyrus-client python main_higyrus.py --help` and confirm
       it does NOT raise an AttributeError on `pkg.client._client`. If a real live run is feasible
       (operator has `.env` populated), run a single probe and confirm the network call uses the
       refactored Client surface.

    6. Confirm with `uv run python -c "import iol_client; print(type(iol_client.Client).__name__, type(iol_client.AsyncClient).__name__); print(type(iol_client._get_default()).__name__)"` — should print without errors.

    Resume signal:
    - "approved" if all checks pass and CI is green on both 3.12 and 3.13.
    - Describe any issues otherwise (e.g., "ruff complains about UP032 in iol/client.py line 88" — Plan 04 needs a fix).
  </how-to-verify>
  <verify>
    <human-check>Operator approves once Task 1 report shows 11 PASS lines (including BOTH 3.12 and 3.13 pytest+mypy) AND CI matrix on Python 3.12 + 3.13 is green AND snapshot diff is additive-only AND baseline file contains test_count + coverage_total.</human-check>
  </verify>
  <done>
    Operator types "approved" after confirming: Task 1 report = 11 PASS (with 3.12 AND 3.13 both passing pytest+mypy per B4), CI matrix green for 3.12 + 3.13, snapshot diff is additive-only (no baseline entries removed), Phase 6 entry-baseline file is committed and complete (B5).
  </done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local pre-CI checks → CI matrix | Local checks pass on the operator's Python 3.12 AND 3.13; CI matrix repeats both versions for full parity. The gate is binary green/red on both interpreters (B4). |
| Driver smoke → unchanged main_*.py code | The phase explicitly does NOT touch `main_*.py` per D-21. The drivers are expected to work transparently through the shim. The W4 py_compile smoke is a safety net for the shim contract's syntactic correctness; the OPTIONAL operator manual smoke (step 5) exercises actual runtime behavior. |
| Plan 07 audits verification/mutation_gate.py (B6) | Plan 06 deliberately left the file untouched; this plan audits and edits only if needed. Split of ownership reduces blast radius if matriz shim is later changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | snapshot file regression unnoticed | mitigate | Task 2 explicitly requires operator review of `git diff verification/snapshots/` with the D-06 additive-only invariant AND the W3 header invariant (8 lines, line 8 == `#`). The PR template review step is the human gate. |
| T-06-11 | Denial of Service | CI matrix flaky failure | accept | If a single CI job is flaky and others green, the operator may re-run. Persistent failures are addressed by reverting the offending plan and re-planning. |
| T-06-14 | Tampering | mutation_gate audit miss | mitigate | Step 8 of Task 1 exercises sandbox vs prod assertions. If the audit fails, Task 1 applies the minimal fix to `verification/mutation_gate.py` (already in `files_modified`). Operator review in Task 2 confirms either no edit or a minimal edit. |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed by this plan. |
</threat_model>

<verification>
- Task 1 automated check (long shell pipeline running on 3.12 AND 3.13) exits 0.
- `python -m py_compile main_iol.py main_higyrus.py main_matriz.py main_ambito_financiero.py` exits 0 (W4 driver smoke).
- mutation_gate audit (Task 1 step 8) returns sandbox=True, prod=False.
- Task 2 operator approval ("approved" resume signal) after reviewing CI status + snapshot diff.
</verification>

<success_criteria>
- REFAC-01 success criteria 1, 2 fully met (snapshot test green; 8 fixture-reaches-production guards green across 4 per-package files; Phase 6 entry-baseline committed).
- REFAC-02 success criteria 3, 4, 5 fully met (4 packages expose Client+AsyncClient with lifecycle, top-level API intact, 277 baseline tests green, CI matrix 3.12 + 3.13 green).
- Phase 6 deliverable complete: `_state.py` per package + Client/AsyncClient class skeletons + PEP 562 shim + conftest migration; refresh_token forward-declared (`None` for non-iol; populated for iol via shim and configure() kwarg).
- B4 closure: Task 1 explicitly runs pytest+mypy on Python 3.13 in the `<automated>` block.
- B6 closure: this plan owns the mutation_gate.py audit; Plan 06 did not touch the file.
- W4 closure: driver smoke uses `python -m py_compile` (single safe syntax check, not the fragile exec/split idiom).
- Ready to launch Phase 7 (`_core.py` extraction).
</success_criteria>

<output>
Create `.planning/phases/06-compat-safety-net-client-class-skeleton/06-07-SUMMARY.md` when done.
</output>
