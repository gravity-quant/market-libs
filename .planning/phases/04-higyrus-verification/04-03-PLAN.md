---
phase: 04-higyrus-verification
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - packages/higyrus-client/tests/test_client.py
  - packages/higyrus-client/tests/test_async_client.py
  - .planning/verification/schemas/higyrus-client/get-health.json
  - .planning/verification/schemas/higyrus-client/get-listado-cuentas.json
  - .planning/verification/schemas/higyrus-client/get-movimientos.json
  - .planning/verification/schemas/higyrus-client/get-posicion-valuada.json
  - .planning/verification/schemas/higyrus-client/get-posiciones.json
  - .planning/verification/higyrus-client-findings.md
autonomous: true
requirements: [HIGY-02, HIGY-03, HIGY-05, HIGY-06, HIGY-07]
tags: [verification, tests, invariants, live-run, schema-commit, drift-01-mirror, mocked-regressions]
must_haves:
  truths:
    - "test_client.py contiene la sección `# ------ Verified live (Phase 4) ------` con los invariantes mockeados de D-HIGY-17 (URLs verbatim HIGY-02, SafeModel tolerance HIGY-03, errors envelope parsing HIGY-05, drop_none parity HIGY-06, empty path HIGY-07)"
    - "test_async_client.py contiene la sección `# ------ Verified live (Phase 4) ------` espejo async (D-HIGY-17)"
    - "La sección `# ------ Regressions ------` (Plan 04-01) preservada intacta en ambos test files"
    - "El orden en cada test file es: tests pre-existentes → Verified live (Phase 4) → Regressions"
    - "Tests mockeados sync verifican URLs verbatim emitidas por get_health (no params), get_listado_cuentas (con drop_none aplicado al estado opcional), get_posicion_valuada (con los 5 params optional via drop_none) — HIGY-02 URL lock"
    - "Tests mockeados verifican SafeModel.from_api({}) devuelve modelo con typed defaults para los 4 modelos Cuenta/Movimiento/Posicion/PosicionValuada (HIGY-03 invariant)"
    - "Tests mockeados verifican errors envelope parsing: response 400 con {timestamp, errors:[{title,detail}]} → HigyrusAPIError con e.errors poblado, e.timestamp capturado (HIGY-05 lock)"
    - "Tests mockeados verifican drop_none parity: get_movimientos sync vs async con params optional=None emiten queries idénticos (HIGY-06 mocked equivalent)"
    - "Tests mockeados verifican empty path: response [] o 204 en los 3 list endpoints (get_listado_cuentas, get_movimientos, get_posiciones) → [] retornado (no None, no crash) — HIGY-07 lock"
    - "Test_client.py + test_async_client.py preservan `from __future__ import annotations`"
    - "Suite completa `uv run pytest -q` + mypy strict + ruff check + format verdes tras los appends"
    - "El driver corrió exitosamente al menos una vez contra Higyrus en vivo (Plan 04-02 Task 2.3 — completed antes de este plan via wave dependency)"
    - "`.planning/verification/schemas/higyrus-client/get-health.json` committeado al repo (DRIFT-01 mirror 1 de 5)"
    - "`.planning/verification/schemas/higyrus-client/get-listado-cuentas.json` committeado al repo (DRIFT-01 mirror 2 de 5)"
    - "`.planning/verification/schemas/higyrus-client/get-movimientos.json` committeado al repo (DRIFT-01 mirror 3 de 5)"
    - "`.planning/verification/schemas/higyrus-client/get-posicion-valuada.json` committeado al repo (DRIFT-01 mirror 4 de 5)"
    - "`.planning/verification/schemas/higyrus-client/get-posiciones.json` committeado al repo (DRIFT-01 mirror 5 de 5)"
    - "`.planning/verification/higyrus-client-findings.md` committeado al repo con esqueleto + findings clasificados del primer live run"
  artifacts:
    - path: "packages/higyrus-client/tests/test_client.py"
      provides: "Sync Verified live (Phase 4) section with HIGY-02/03/05/06/07 invariants"
      contains: "# ------ Verified live (Phase 4) ------"
    - path: "packages/higyrus-client/tests/test_async_client.py"
      provides: "Async Verified live (Phase 4) section mirror"
      contains: "# ------ Verified live (Phase 4) ------"
    - path: ".planning/verification/schemas/higyrus-client/get-health.json"
      provides: "DRIFT-01 mirror baseline schema for get_health"
      contains: "\"schema\":"
      contains_also: "\"endpoint\":"
    - path: ".planning/verification/schemas/higyrus-client/get-listado-cuentas.json"
      provides: "DRIFT-01 mirror baseline schema for get_listado_cuentas"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-movimientos.json"
      provides: "DRIFT-01 mirror baseline schema for get_movimientos"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-posicion-valuada.json"
      provides: "DRIFT-01 mirror baseline schema for get_posicion_valuada"
      contains: "\"schema\":"
    - path: ".planning/verification/schemas/higyrus-client/get-posiciones.json"
      provides: "DRIFT-01 mirror baseline schema for get_posiciones"
      contains: "\"schema\":"
    - path: ".planning/verification/higyrus-client-findings.md"
      provides: "Phase 4 findings file (skeleton + first live run findings, classified)"
      contains: "# Findings: higyrus-client"
  key_links:
    - from: "packages/higyrus-client/tests/test_client.py::Verified live (Phase 4)"
      to: "higyrus_client.get_movimientos (drop_none param emission)"
      via: "httpx_mock.add_response(url='https://api.test/api/cuentas/CTA-001/movimientos?fechaDesde=...&fechaHasta=...', json=[...])"
      pattern: "fechaDesde"
    - from: "packages/higyrus-client/tests/test_async_client.py::Verified live (Phase 4)"
      to: "higyrus_client.aio.get_movimientos (drop_none param emission)"
      via: "await aio.get_movimientos(...)"
      pattern: "await aio.get_movimientos"
    - from: ".planning/verification/schemas/higyrus-client/*.json"
      to: "main_higyrus.py::probe_schema_snapshot"
      via: "first live driver run generated these files (Plan 04-02 Task 2.3)"
      pattern: "get-.*\\.json"
    - from: ".planning/verification/higyrus-client-findings.md"
      to: "main_higyrus.py::probe_*"
      via: "first live run via append_finding + write_findings (Plan 04-02 Task 2.3)"
      pattern: "higyrus-client-findings.md"
---

<objective>
Cerrar Phase 4 agregando los **invariantes Verified-live mockeados** como tests de bloque en `test_client.py` + `test_async_client.py` (sección `# ------ Verified live (Phase 4) ------` per D-HIGY-17) y commiteando los 6 artefactos generados por el live run del Plan 04-02 (5 schema snapshots DRIFT-01 mirror + 1 findings file). Este plan corre **autónomo** (no human checkpoint) porque el checkpoint humano del live run ya se completó en Plan 04-02 Task 2.3 — el operador inspeccionó y aprobó los artefactos antes de llegar acá.

La sección `# ------ Regressions ------` ya fue agregada en Plan 04-01 Task 1.4 con los 10 tests del fix HIGY-04 (5 sync + 5 async); este plan agrega la sección `# ------ Verified live (Phase 4) ------` (D-HIGY-17 cinco invariantes: URLs verbatim HIGY-02 + SafeModel tolerance HIGY-03 + errors envelope HIGY-05 + drop_none parity HIGY-06 + empty path HIGY-07). La estructura de Wave 3 es idéntica a Plan 03-03 Task 3.1 (test append) + Task 3.3 (commit baseline), con 5 schema snapshots en lugar de 4.

Purpose: HIGY-01..07 quedan cumplidos (HIGY-01/02/05/06/07 via driver live run + Verified-live mocked invariants, HIGY-03 via field_type_map findings del driver, HIGY-04 via Plan 04-01 fix + regression tests). DRIFT-01 mirror cumplido con 5 schema snapshots committeados. La regla MVP: la fase pasa aun sin findings clasificados como CONFIRMED; cualquier finding OPEN del primer live run queda committeado como baseline para clasificación humana futura.

Output:
1. `test_client.py` + `test_async_client.py` con sección `# ------ Verified live (Phase 4) ------` (D-HIGY-17 invariantes HIGY-02/03/05/06/07) y la sección `# ------ Regressions ------` (de Plan 04-01) preservada
2. `.planning/verification/schemas/higyrus-client/{get-health,get-listado-cuentas,get-movimientos,get-posicion-valuada,get-posiciones}.json` committeados (5 archivos, DRIFT-01 mirror)
3. `.planning/verification/higyrus-client-findings.md` committeado (esqueleto + findings OPEN/EXPECTED del primer live run)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-higyrus-verification/04-CONTEXT.md
@.planning/phases/04-higyrus-verification/04-RESEARCH.md
@.planning/phases/04-higyrus-verification/04-PATTERNS.md
@.planning/phases/04-higyrus-verification/04-VALIDATION.md
@.planning/phases/04-higyrus-verification/04-01-PLAN.md
@.planning/phases/04-higyrus-verification/04-02-PLAN.md
@.planning/phases/03-iol-verification/03-03-PLAN.md
@.planning/phases/02-mbito-verification/02-03-PLAN.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_higyrus.py
@verification/findings.py
@packages/higyrus-client/tests/test_client.py
@packages/higyrus-client/tests/test_async_client.py
@packages/higyrus-client/tests/conftest.py
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/src/higyrus_client/aio.py
@packages/higyrus-client/src/higyrus_client/models.py
@packages/higyrus-client/src/higyrus_client/exceptions.py
@packages/higyrus-client/src/higyrus_client/__init__.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Append Verified live (Phase 4) invariants to test_client.py + test_async_client.py (D-HIGY-17, HIGY-02/03/05/06/07)</name>
  <files>packages/higyrus-client/tests/test_client.py, packages/higyrus-client/tests/test_async_client.py</files>
  <read_first>
    - packages/higyrus-client/tests/test_client.py (estado actual DESPUÉS de Plan 04-01 Task 1.4: 12 tests sync pre-existentes + sección `# ------ Regressions ------` con 5 tests sync de HIGY-04; agregar la sección `# ------ Verified live (Phase 4) ------` ENTRE los pre-existentes y la sección Regressions — orden: pre-existentes → Verified live → Regressions, mirror Phase 3 Plan 03-03)
    - packages/higyrus-client/tests/test_async_client.py (estado actual DESPUÉS de Plan 04-01 Task 1.4: 5 tests async pre-existentes + sección Regressions con 5 tests async; mismo pattern)
    - packages/higyrus-client/tests/conftest.py (autouse fixtures precargan `_token = "test-token"` y `_token_ts = 9_999_999_999.0` con `base_url = "https://api.test"`; los Verified-live tests heredan setup sin modificación)
    - packages/higyrus-client/src/higyrus_client/client.py (DESPUÉS de Plan 04-01: contiene los raises tipados; los Verified-live tests assertan happy path con shapes correctas — los regression tests del Plan 04-01 ya cubren shape mismatch)
    - packages/higyrus-client/src/higyrus_client/aio.py (DESPUÉS de Plan 04-01: mirror)
    - packages/higyrus-client/src/higyrus_client/models.py (SafeModel + 4 modelos top-level — los tests HIGY-03 verifican `Cuenta.from_api({})` retorna modelo con typed defaults: `Cuenta.titular == ""`, `Movimiento.cantidad == 0.0`, etc.)
    - packages/higyrus-client/src/higyrus_client/exceptions.py (firma `HigyrusAPIError(status_code, errors, timestamp)` — el test HIGY-05 mockea response con envelope `{timestamp, errors:[{title,detail}]}` y verifica que `e.errors` y `e.timestamp` se capturan)
    - packages/higyrus-client/src/higyrus_client/__init__.py (re-exports: el barrel exporta `HigyrusAPIError`, `HigyrusAuthError`, `Cuenta`, `Movimiento`, `Posicion`, `PosicionValuada`, etc.)
    - packages/higyrus-client/src/higyrus_client/_params.py (`drop_none` helper — la base del invariante HIGY-06 mocked: los params optional=None se omiten del query string)
    - .planning/phases/04-higyrus-verification/04-CONTEXT.md (D-HIGY-17 lista verbatim los 5 invariantes: URLs HIGY-02, SafeModel tolerance HIGY-03, errors envelope HIGY-05, drop_none parity HIGY-06, empty path HIGY-07)
    - .planning/phases/04-higyrus-verification/04-VALIDATION.md (tabla per-task verification map: tasks 04-03-01..05 con criterios verificables; 04-03-03 nota "extend existing test_errors_envelope coverage to 4xx on any endpoint, not just login"; 04-03-05 nota "extend 204 test to get_movimientos, get_posiciones")
    - .planning/phases/04-higyrus-verification/04-RESEARCH.md (Code Examples: pattern verbatim de `httpx_mock.add_response(url=...)` con URL full incluyendo query string)
    - .planning/phases/03-iol-verification/03-03-PLAN.md Task 3.1 (analog directo: mismo divider pattern + tests Verified-live mockeados con URL exacta + docstring `Phase N: ...`)
    - .planning/phases/02-mbito-verification/02-03-PLAN.md (analog Phase 2: orden divider preservado, tests con docstring `Phase 2: locking de ...`)
    - .planning/codebase/TESTING.md (pytest-httpx con URL full string + query string; `async def test_async_*` sin decorator por `asyncio_mode=auto`; convención docstring `"Phase N: locking de ..."`)
  </read_first>
  <behavior>
    - **Test 1 sync (HIGY-02 URL verbatim get_listado_cuentas con estado=alta):** `test_get_listado_cuentas_url_con_estado_alta` mockea `https://api.test/api/cuentas/listadoCuentas?estado=alta` con response `[{"id": "CTA-001", "titular": "<x>", ...}]` y verifica que el cliente emite GET a esa URL exacta y devuelve `list[Cuenta]`
    - **Test 2 sync (HIGY-03 SafeModel tolerance):** `test_safemodel_from_api_typed_defaults` invoca `Cuenta.from_api({})`, `Movimiento.from_api({})`, `Posicion.from_api({})`, `PosicionValuada.from_api({})` y verifica que cada modelo se construye sin levantar con todos los campos en sus typed defaults (`""` para str, `0` para int, `0.0` para float, `False` para bool, `[]` para list)
    - **Test 3 sync (HIGY-05 errors envelope):** `test_errors_envelope_parsed_on_4xx` mockea `https://api.test/api/cuentas/CTA-INVALID/movimientos?fechaDesde=...&fechaHasta=...` con response `status_code=400, json={"timestamp": "2026-06-06T10:00:00Z", "errors": [{"title": "invalid cuenta", "detail": "CTA-INVALID not found"}]}`; espera `HigyrusAPIError` raised con `e.errors == [{"title": "invalid cuenta", "detail": "CTA-INVALID not found"}]` y `e.timestamp == "2026-06-06T10:00:00Z"`
    - **Test 4 sync (HIGY-06 drop_none param emission):** `test_get_movimientos_drop_none_emits_only_required_params` mockea `https://api.test/api/cuentas/CTA-001/movimientos?fechaDesde=2026-05-07&fechaHasta=2026-06-06` (sin params optional) con response `[]`; llama `get_movimientos("CTA-001", dt.date(2026, 5, 7), dt.date(2026, 6, 6))` SIN pasar los 4 params optional adicionales; verifica que el match es exitoso (el drop_none omitió los None)
    - **Test 5 sync (HIGY-07 empty path):** `test_get_movimientos_empty_path_returns_list` mockea endpoint con response `status_code=204` y body vacío (o `[]` explícito); llama `get_movimientos(...)` y verifica que retorna `[]` (no None, no crash) — análogo para `get_listado_cuentas` y `get_posiciones` (3 sub-tests o 1 test parametrizado, Discretion)
    - **Tests 6-10 async:** espejos exactos con `await aio.<func>(...)`, prefijos `test_async_*`, mismos mocks, mismos asserts
    - Las secciones quedan ordenadas (de top a bottom dentro del archivo):
      1. Tests pre-existentes (12 sync / 5 async) — sin tocar
      2. `# ------ Verified live (Phase 4) ------` (NUEVO en este plan) + 5 tests sync (o más si HIGY-07 se desdobla)
      3. `# ------ Regressions ------` (ya existía de Plan 04-01) + 5 tests de HIGY-04
  </behavior>
  <action>
    Append a `packages/higyrus-client/tests/test_client.py` la sección `# ------ Verified live (Phase 4) ------` (divider verbatim — D-HIGY-17) ANTES del divider `# ------ Regressions ------` que Plan 04-01 ya colocó. NO mover el divider Regressions ni los 5 tests HIGY-04 que ya están abajo — solo insertar el nuevo divider + N tests Verified-live en posición intermedia.

    Estructura final del archivo (verificable por orden de aparición de strings):
    ```
    # (12 tests sync pre-existentes)

    # ------ Verified live (Phase 4) ------

    def test_get_listado_cuentas_url_con_estado_alta(...):
        ...

    def test_safemodel_from_api_typed_defaults(...):
        ...

    def test_errors_envelope_parsed_on_4xx(...):
        ...

    def test_get_movimientos_drop_none_emits_only_required_params(...):
        ...

    def test_get_movimientos_empty_path_returns_list(...):
        ...

    def test_get_listado_cuentas_empty_path_returns_list(...):
        ...

    def test_get_posiciones_empty_path_returns_list(...):
        ...

    # ------ Regressions ------

    # (5 tests de HIGY-04 que Plan 04-01 agregó)
    ```

    Mismo append a `packages/higyrus-client/tests/test_async_client.py`: divider Verified live + N tests async espejo ANTES del divider Regressions de Plan 04-01.

    **Convenciones obligatorias (CONVENTIONS.md / TESTING.md):**
    - Mantener `from __future__ import annotations` línea 3 (ya presente)
    - Imports ya existentes en cada archivo son suficientes (de Plan 04-01 Task 1.4 ya importan `HigyrusAPIError`, `Cuenta`, `Movimiento`, etc. del barrel — verificar al leer; si falta algún model, ampliar el import)
    - pytest-httpx con URL completa incluyendo query string para `get_listado_cuentas` y `get_movimientos` (los wrappers emiten query con `drop_none` aplicado — verificar al implementar inspeccionando el actual client.py)
    - Para los tests con `dt.date(...)`: `import datetime as dt` ya está en línea 5 de ambos test files
    - Async tests sin decorador (asyncio_mode=auto)
    - Docstring corto en cada test con formato `"""Phase 4: locking de <invariant> (HIGY-NN)."""` referenciando el requirement ID
    - ruff line-length=100, double quotes, 4 espacios
    - mypy --strict pasa

    **Tests sync (test_client.py) — N nuevos en posición intermedia:**

    1. `def test_get_listado_cuentas_url_con_estado_alta(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking URL exacta de get_listado_cuentas con estado=alta (HIGY-02)."""`
       - Mock: `httpx_mock.add_response(url="https://api.test/api/cuentas/listadoCuentas?estado=alta", method="GET", json=[{"id": "CTA-001", "titular": "<x>", "denominacion": "<y>"}])` (Discretion: el URL exacto depende de cómo client.py construye el path; verificar al leer client.py:290 línea — algunos wrappers usan path `/api/cuentas/listadoCuentas` y emiten `?estado=alta` via query)
       - Call: `cuentas = higyrus_client.get_listado_cuentas(estado="alta")`
       - Asserts:
         - `isinstance(cuentas, list)`
         - `len(cuentas) == 1`
         - `isinstance(cuentas[0], Cuenta)`
         - `cuentas[0].id == "CTA-001"`

    2. `def test_safemodel_from_api_typed_defaults(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de SafeModel.from_api({}) → typed defaults invariant for los 4 modelos top-level (HIGY-03)."""`
       - NO requiere httpx_mock (test puro de unit, no hace request) — pero el autouse fixture lo activa igual; usar el parámetro `httpx_mock` con un pass (sin add_response) y esperar que pytest-httpx no se queje por unused mock (configurar `assert_all_responses_were_requested=False` si necesario via decorator `@pytest.mark.httpx_mock(assert_all_responses_were_requested=False)`)
       - Calls:
         - `c = Cuenta.from_api({})`
         - `m = Movimiento.from_api({})`
         - `p = Posicion.from_api({})`
         - `pv = PosicionValuada.from_api({})`
       - Asserts: cada uno debe ser instance del modelo correspondiente; verificar 1-2 campos representativos por modelo con typed defaults — e.g.:
         - `c.id == "" if hasattr(c, 'id') else True` (el campo id default vacío)
         - `m.cantidad == 0.0` (Movimiento.cantidad: float per docstring)
         - `m.fechaConcertacion == "" if hasattr(m, 'fechaConcertacion') else True`
         - `p` y `pv` con un campo numérico cada uno con default 0/0.0
       - Discretion: el implementador inspecciona `models.py` para identificar 2-3 campos concretos por modelo y assertar sus typed defaults. El roadmap NO pide campos específicos — el invariante es que `from_api({})` NO levanta y devuelve modelo válido con defaults.

    3. `def test_errors_envelope_parsed_on_4xx(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de errors envelope parsing en 4xx response (HIGY-05)."""`
       - Mock: `httpx_mock.add_response(method="GET", status_code=400, json={"timestamp": "2026-06-06T10:00:00Z", "errors": [{"title": "invalid cuenta", "detail": "CTA-INVALID not found"}]})`
       - Call+assert: `with pytest.raises(HigyrusAPIError) as exc_info: higyrus_client.get_movimientos("CTA-INVALID", dt.date(2026, 1, 1), dt.date(2026, 1, 1))`
       - Asserts: `exc_info.value.errors == [{"title": "invalid cuenta", "detail": "CTA-INVALID not found"}]`, `exc_info.value.timestamp == "2026-06-06T10:00:00Z"`, `exc_info.value.status_code == 400` (HTTP real, NO el sentinel 0)

    4. `def test_get_movimientos_drop_none_emits_only_required_params(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de drop_none(params) — None params omitidos del query string (HIGY-06 mocked equivalent)."""`
       - Mock: `httpx_mock.add_response(url="https://api.test/api/cuentas/CTA-001/movimientos?fechaDesde=2026-05-07&fechaHasta=2026-06-06", method="GET", json=[])` (URL contiene SOLO los 2 params requeridos)
       - Call: `movs = higyrus_client.get_movimientos("CTA-001", dt.date(2026, 5, 7), dt.date(2026, 6, 6))` (sin params optional adicionales)
       - Asserts: `movs == []` (la URL matchea y la response se devuelve)
       - NOTA: el matcher de pytest-httpx hace strict URL matching — si el cliente emite params extra (por bug), el match falla y el test reporta unused mock

    5. `def test_get_movimientos_empty_path_returns_list(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de empty path / 204 returns [] (HIGY-07) para get_movimientos."""`
       - Mock: `httpx_mock.add_response(method="GET", status_code=204)` (204 sin body — `raw is None` en client.py:242 path: `if raw is None: return []`)
       - Call+assert: `movs = higyrus_client.get_movimientos("CTA-001", dt.date(2026, 1, 1), dt.date(2026, 1, 1))`
       - Asserts: `movs == []` (no None, no crash)

    6. `def test_get_listado_cuentas_empty_path_returns_list(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de empty path / 204 returns [] (HIGY-07) para get_listado_cuentas."""`
       - Mock: `httpx_mock.add_response(method="GET", status_code=204)`
       - Call+assert: `cuentas = higyrus_client.get_listado_cuentas()`; asserts: `cuentas == []`
       - NOTA: ya existe `test_get_listado_cuentas_204_devuelve_lista_vacia` pre-existente — Discretion: NO duplicar; agregar solo los tests para `get_movimientos` y `get_posiciones` que aún NO existen (per VALIDATION.md nota "partial — existing covers get_listado_cuentas; extend to get_movimientos, get_posiciones")

    7. `def test_get_posiciones_empty_path_returns_list(httpx_mock: HTTPXMock) -> None:`
       - Docstring: `"""Phase 4: locking de empty path / 204 returns [] (HIGY-07) para get_posiciones."""`
       - Mock: `httpx_mock.add_response(method="GET", status_code=204)`
       - Call+assert: `posiciones = higyrus_client.get_posiciones("CTA-001", fecha=dt.date(2026, 1, 1))`; asserts: `posiciones == []`

    **Discretion sobre HIGY-07:** Si el implementador prefiere parametrizar HIGY-07 en un solo test (`@pytest.mark.parametrize("endpoint_func", [get_movimientos, get_listado_cuentas, get_posiciones])`), también válido. Lo importante es cobertura de los 3 endpoints con return list.

    **Tests async (test_async_client.py) — N espejos:**

    Mismos tests con prefijo `test_async_*`, firma `async def`, mocks idénticos, calls `await aio.<func>(...)`:
    - `async def test_async_get_listado_cuentas_url_con_estado_alta`
    - `async def test_async_safemodel_from_api_typed_defaults` (test puro — NO requiere mock; mismo behavior que sync)
    - `async def test_async_errors_envelope_parsed_on_4xx`
    - `async def test_async_get_movimientos_drop_none_emits_only_required_params` (validates HIGY-06 async side — junto al sync emite paridad para el mismo URL)
    - `async def test_async_get_movimientos_empty_path_returns_list`
    - `async def test_async_get_listado_cuentas_empty_path_returns_list` (si no existe ya en pre-existentes)
    - `async def test_async_get_posiciones_empty_path_returns_list`

    **Restricciones:**
    - NO modificar tests pre-existentes (12 sync + 5 async + sección Regressions de Plan 04-01 — todos quedan intactos)
    - NO mover ni alterar el divider `# ------ Regressions ------` ni los 5 tests de HIGY-04 que están debajo (de Plan 04-01 Task 1.4)
    - NO agregar tests para HIGY-04 (esos ya están en sección Regressions de Plan 04-01)
    - NO ejecutar el driver en este task (eso ya pasó en Plan 04-02 Task 2.3)
    - NO commitear ningún archivo de `.planning/verification/` en este task (eso es Task 3.2)
    - Verificar al final que el orden en cada archivo es: pre-existentes → Verified live (Phase 4) → Regressions

    Commit sugerido: `test(04-03): add Verified live (Phase 4) invariants for HIGY-02/03/05/06/07 (D-HIGY-17)`
  </action>
  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/higyrus-client/tests/test_async_client.py -v</automated>
    <automated>uv run pytest packages/higyrus-client -q</automated>
    <automated>uv run mypy packages/higyrus-client</automated>
    <automated>uv run ruff check packages/higyrus-client/tests/test_client.py packages/higyrus-client/tests/test_async_client.py</automated>
    <automated>uv run ruff format --check packages/higyrus-client/tests/test_client.py packages/higyrus-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 4) ------" packages/higyrus-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 4) ------" packages/higyrus-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/higyrus-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/higyrus-client/tests/test_async_client.py</automated>
    <automated>uv run python -c "src = open('packages/higyrus-client/tests/test_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 4) ------'); idx_re = src.find('# ------ Regressions ------'); assert idx_vl > 0 and idx_re > 0, 'both dividers must exist'; assert idx_vl < idx_re, f'Verified live divider (at {idx_vl}) must precede Regressions divider (at {idx_re})'; print('order OK')"</automated>
    <automated>uv run python -c "src = open('packages/higyrus-client/tests/test_async_client.py').read(); idx_vl = src.find('# ------ Verified live (Phase 4) ------'); idx_re = src.find('# ------ Regressions ------'); assert idx_vl > 0 and idx_re > 0; assert idx_vl < idx_re, f'Verified live must precede Regressions'; print('order OK')"</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `# ------ Verified live (Phase 4) ------`
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `# ------ Regressions ------` (de Plan 04-01, preservado)
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `# ------ Verified live (Phase 4) ------`
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `# ------ Regressions ------` (de Plan 04-01, preservado)
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `def test_get_listado_cuentas_url_con_estado_alta(` (HIGY-02 URL lock)
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `def test_safemodel_from_api_typed_defaults(` (HIGY-03 invariant)
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `def test_errors_envelope_parsed_on_4xx(` (HIGY-05 envelope lock)
    - Source: `packages/higyrus-client/tests/test_client.py` contiene `def test_get_movimientos_drop_none_emits_only_required_params(` (HIGY-06 mocked equivalent)
    - Source: `packages/higyrus-client/tests/test_client.py` contiene al menos un test de empty path para `get_movimientos` y `get_posiciones` (HIGY-07; `get_listado_cuentas` ya cubierto por test pre-existente)
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `async def test_async_get_listado_cuentas_url_con_estado_alta(` (mirror)
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `async def test_async_safemodel_from_api_typed_defaults(`
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `async def test_async_errors_envelope_parsed_on_4xx(`
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene `async def test_async_get_movimientos_drop_none_emits_only_required_params(`
    - Source: `packages/higyrus-client/tests/test_async_client.py` contiene al menos un test async de empty path para `get_movimientos` y `get_posiciones`
    - Source: ambos archivos contienen `Cuenta.from_api({})` literal (HIGY-03 invariant)
    - Source: ambos archivos contienen `"errors": [{"title":` literal en el mock del test HIGY-05
    - Source: ambos archivos contienen `status_code=204` literal en al menos un test (HIGY-07 empty path 204)
    - Source: ambos archivos contienen `?estado=alta` literal en al menos un test (HIGY-02 URL lock)
    - Source: ambos archivos contienen `fechaDesde=` literal en al menos un test (HIGY-06 drop_none verification)
    - Source: el divider `# ------ Verified live (Phase 4) ------` aparece ANTES del divider `# ------ Regressions ------` en ambos archivos (verificable por byte offset comparison; el script python inline en `<verify>` lo chequea)
    - Source: ambos archivos preservan `from __future__ import annotations` línea 3
    - Source: tests pre-existentes (12 sync + 5 Regressions sync + 5 async + 5 Regressions async) NO modificados
    - Behavior: `uv run pytest packages/higyrus-client/tests/test_client.py -v` reporta >= 22 PASSED (12 viejos + 5+ Verified live + 5 Regressions)
    - Behavior: `uv run pytest packages/higyrus-client/tests/test_async_client.py -v` reporta >= 15 PASSED (5 viejos + 5+ Verified live + 5 Regressions)
    - Behavior: `uv run pytest packages/higyrus-client -q` exits 0
    - Static: `uv run mypy packages/higyrus-client` exits 0
    - Static: `uv run ruff check packages/higyrus-client/tests` exits 0
    - Static: `uv run ruff format --check packages/higyrus-client/tests` exits 0
    - Non-regression: `uv run pytest -q` (suite completa) exits 0 — confirma que los tests Verified-live + Regressions + fix de Plan 04-01 no rompieron el resto del repo
  </acceptance_criteria>
  <done>Sección `# ------ Verified live (Phase 4) ------` agregada en ambos test files con N tests por surface (al menos 5 invariantes mínimos: URL HIGY-02, SafeModel tolerance HIGY-03, errors envelope HIGY-05, drop_none parity HIGY-06, empty path HIGY-07); orden divider preservado (Verified live ANTES de Regressions). Tests pre-existentes y sección Regressions de Plan 04-01 sin modificar. Suite completa verde, mypy + ruff limpios.</done>
</task>

<task type="auto">
  <name>Task 3.2: Commit baseline artifacts (5 DRIFT-01 schemas + Phase 4 findings file)</name>
  <files>.planning/verification/schemas/higyrus-client/get-health.json, .planning/verification/schemas/higyrus-client/get-listado-cuentas.json, .planning/verification/schemas/higyrus-client/get-movimientos.json, .planning/verification/schemas/higyrus-client/get-posicion-valuada.json, .planning/verification/schemas/higyrus-client/get-posiciones.json, .planning/verification/higyrus-client-findings.md</files>
  <read_first>
    - .planning/verification/schemas/higyrus-client/get-health.json (generado por Plan 04-02 Task 2.3 live run, aprobado por humano)
    - .planning/verification/schemas/higyrus-client/get-listado-cuentas.json (idem)
    - .planning/verification/schemas/higyrus-client/get-movimientos.json (idem)
    - .planning/verification/schemas/higyrus-client/get-posicion-valuada.json (idem)
    - .planning/verification/schemas/higyrus-client/get-posiciones.json (idem)
    - .planning/verification/higyrus-client-findings.md (generado por Plan 04-02 Task 2.3 live run, findings clasificados por humano)
    - .gitignore (verificar que `.planning/verification/captures/` está ignored pero NO `.planning/verification/schemas/higyrus-client/` ni `.planning/verification/higyrus-client-findings.md`)
    - .planning/phases/03-iol-verification/03-03-PLAN.md Task 3.3 (analog directo: mismo pattern de commit atómico — verificación + comando + mensaje + Co-Authored-By)
    - .planning/phases/02-mbito-verification/02-03-PLAN.md Task 3.3 (analog Phase 2: misma estructura)
  </read_first>
  <action>
    Confirmar que los 6 artefactos existen en disco y NO están gitignored:

    1. `test -f .planning/verification/schemas/higyrus-client/get-health.json` exits 0
    2. `test -f .planning/verification/schemas/higyrus-client/get-listado-cuentas.json` exits 0
    3. `test -f .planning/verification/schemas/higyrus-client/get-movimientos.json` exits 0
    4. `test -f .planning/verification/schemas/higyrus-client/get-posicion-valuada.json` exits 0
    5. `test -f .planning/verification/schemas/higyrus-client/get-posiciones.json` exits 0
    6. `test -f .planning/verification/higyrus-client-findings.md` exits 0
    7. Para cada archivo: `git check-ignore <path>` exits 1 (NO ignored — committeable)

    Si alguno reporta ignored: STOP y arreglar `.gitignore` antes de continuar (esperamos que solo `.planning/verification/captures/` esté ignored, no `schemas/` ni `<pkg>-findings.md`).

    Verificar envelope D-21 una vez más antes del commit (defense-in-depth):
    ```
    uv run python -c "
    import json
    from pathlib import Path
    files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json'))
    assert len(files) == 5, f'expected 5 schemas, got {len(files)}'
    required = {'endpoint', 'client_function', 'captured_at', 'base_url', 'sample_params', 'schema'}
    for f in files:
        d = json.loads(f.read_text())
        assert required <= set(d.keys()), f'{f.name} missing envelope keys: {required - set(d.keys())}'
    print(f'5 schemas with envelope D-21 OK')
    "
    ```

    Verificar PII-free defense (T-4-SC mitigation): scan rápido de cada JSON por patrones que claramente serían PII real:
    ```
    uv run python -c "
    import json
    import re
    from pathlib import Path
    # Patterns that would indicate PII leak in schema_of output (which should only have type names)
    PII_PATTERNS = [
        re.compile(r'\d{10,}'),  # CBU/CUIT-like digit strings
        re.compile(r'[A-Z][a-z]+ [A-Z][a-z]+'),  # Proper name patterns (FirstName LastName)
    ]
    files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json'))
    for f in files:
        d = json.loads(f.read_text())
        schema_str = json.dumps(d.get('schema', {}))
        for p in PII_PATTERNS:
            matches = p.findall(schema_str)
            assert not matches, f'PII-like pattern found in {f.name} schema: {matches[:3]}'
    print('PII-free defense OK in 5 schemas')
    "
    ```
    Si falla: STOP commit, reportar al humano (algún valor real escapó al `schema_of` — el run del Plan 04-02 Task 2.3 debe revisitarse).

    Verificar que el findings markdown tiene el header esperado:
    ```
    head -1 .planning/verification/higyrus-client-findings.md
    # Esperado: "# Findings: higyrus-client"
    ```

    Stageá los 6 archivos:
    ```bash
    git add .planning/verification/schemas/higyrus-client/get-health.json
    git add .planning/verification/schemas/higyrus-client/get-listado-cuentas.json
    git add .planning/verification/schemas/higyrus-client/get-movimientos.json
    git add .planning/verification/schemas/higyrus-client/get-posicion-valuada.json
    git add .planning/verification/schemas/higyrus-client/get-posiciones.json
    git add .planning/verification/higyrus-client-findings.md
    ```

    Stageá también los archivos de tests + .env.example modificados en este wave (Plan 04-01 Tasks 1.1-1.4 + Plan 04-02 Tasks 2.1-2.2 + Plan 04-03 Task 3.1), si aún no se committearon individualmente — Discretion del implementador: el orden recomendado es commits separados por plan ejecutados ya en sus respectivos plans; este Task 3.2 commitea SOLO los 6 artefactos de verificación. Verificar `git status` antes para confirmar que solo los 6 artefactos están unstaged.

    Commit atómico (DRIFT-01 mirror + Phase 4 lifecycle):
    ```bash
    git commit -m "feat(04-03): commit DRIFT-01 mirror baseline (5 Higyrus schemas + Phase 4 findings)

    First live run of main_higyrus.py against Higyrus production:
    - Schema snapshots for get_health, get_listado_cuentas, get_movimientos,
      get_posicion_valuada, and get_posiciones (envelope D-21, PII-free by construction).
    - Findings file populated by the 18 probes (D-HIGY-1..18).

    Verifies HIGY-01..07 end-to-end:
    - HIGY-01/02: login + happy-path sweep on both surfaces.
    - HIGY-03: bidirectional SafeModel diff (probe_field_type_map) detected
      field-drop candidates (classified OPEN/CONFIRMED/NO-FIX by operator).
    - HIGY-04: in-cycle dual sync+async fix shipped in Plan 04-01 with 10
      mocked regression tests.
    - HIGY-05: errors envelope parsed live via probe_errors_envelope_*.
    - HIGY-06: drop_none parity verified or denied via probe_parity_sync_async
      (httpx.Request.url.query capture on both surfaces).
    - HIGY-07: empty path / 204 returns [] verified across all 3 list endpoints.

    Opt-in HIGY-AUTH 401 probe available via VERIFY_HIGYRUS_BAD_CREDS=1
    (single-shot per session, no retry, T-4-07 try/finally restore).
    Drift detection re-runs use the 5 schema snapshots (D-25) without overwriting.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    Verificar el commit en git log:
    ```bash
    git log --oneline -1
    # Esperado: <hash> feat(04-03): commit DRIFT-01 mirror baseline (5 Higyrus schemas + Phase 4 findings)
    ```

    Verificar los stats del commit:
    ```bash
    git diff --stat HEAD~1..HEAD
    # Esperado: 6 archivos changed (5 .json + 1 .md), líneas insertadas según contenido de los 5 schemas y findings file
    ```

    Commit sugerido (alternativo, si el git commit ya quedó hecho por el agente principal con los tests del Task 3.1): este task solo verifica que los 6 artefactos están committed y reporta status.
  </action>
  <verify>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-health.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-listado-cuentas.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-movimientos.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-posicion-valuada.json</automated>
    <automated>test -f .planning/verification/schemas/higyrus-client/get-posiciones.json</automated>
    <automated>test -f .planning/verification/higyrus-client-findings.md</automated>
    <automated>git check-ignore .planning/verification/schemas/higyrus-client/get-health.json; test $? -eq 1</automated>
    <automated>git check-ignore .planning/verification/higyrus-client-findings.md; test $? -eq 1</automated>
    <automated>uv run python -c "import json; from pathlib import Path; files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json')); assert len(files) == 5; required = {'endpoint', 'client_function', 'captured_at', 'base_url', 'sample_params', 'schema'}; [d for f in files for d in [json.loads(f.read_text())] if not (required <= set(d.keys()))] and (_ for _ in ()).throw(AssertionError('envelope D-21 violated')); print('envelope D-21 OK')"</automated>
    <automated>head -1 .planning/verification/higyrus-client-findings.md | grep -F "# Findings: higyrus-client"</automated>
    <automated>git log --oneline -1 | grep -E "(04-03|DRIFT-01)"</automated>
    <automated>git diff --stat HEAD~1..HEAD | grep -E "verification/schemas/higyrus-client/.*\.json|higyrus-client-findings.md"</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run mypy .</automated>
    <automated>uv run ruff check .</automated>
    <automated>uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - File exists: 5 archivos JSON en `.planning/verification/schemas/higyrus-client/` (get-health, get-listado-cuentas, get-movimientos, get-posicion-valuada, get-posiciones)
    - File exists: `.planning/verification/higyrus-client-findings.md`
    - File content: cada JSON contiene las 6 keys del envelope D-21 (`endpoint`, `client_function`, `captured_at`, `base_url`, `sample_params`, `schema`)
    - File content: el findings markdown contiene `# Findings: higyrus-client` en la primera línea
    - Git state: los 6 archivos NO están gitignored (`git check-ignore` exits 1 para cada uno)
    - Git state: los 6 archivos están committed (último commit los incluye en `git diff --stat HEAD~1..HEAD`)
    - Git state: el último commit message comienza con `feat(04-03):` y menciona `DRIFT-01` y `Phase 4 findings`
    - Git state: el commit message incluye `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`
    - Non-regression: `uv run pytest -q` (suite completa) exits 0 después del commit
    - Non-regression: `uv run mypy .` exits 0
    - Non-regression: `uv run ruff check .` exits 0
    - Non-regression: `uv run ruff format --check .` exits 0
  </acceptance_criteria>
  <done>5 schemas DRIFT-01 mirror + 1 findings file commiteados al repo con commit atómico `feat(04-03): commit DRIFT-01 mirror baseline (5 Higyrus schemas + Phase 4 findings)`. Envelope D-21 verificado defense-in-depth. PII-free defense pasada. Suite completa verde. Phase 4 cerrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files (`test_client.py` / `test_async_client.py`) → autouse fixtures from conftest | Fixtures precarga `_token = "test-token"` y `base_url = "https://api.test"`; ningún test Verified-live ejercita auth path (autouse cubre) |
| Verified-live tests → `httpx_mock.add_response(url=..., json=<synthetic>)` | Valores sintéticos inline; ningún valor real toca los mocks; PII-free por construcción del test |
| Schema JSONs committed → repo público | `schema_of` PII-free por construcción; D-HIGY-1 explicita NO commit raw payloads ni fixtures anonymized |
| Findings markdown → repo público | `append_finding(...)` writes title/expected/actual/diff — operator verified pre-commit en Plan 04-02 Task 2.3 |
| Git commit → repo HEAD | Commit message documenta el lifecycle Phase 4 + Co-Authored-By; no incluye credenciales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SC | Information Disclosure | Schema snapshot file leaking PII (commit del baseline) | mitigate | Defense-in-depth en Task 3.2: el script Python inline scanea los 5 JSONs por patrones que indicarían PII real (digit strings >=10 chars como CBU/CUIT, ProperName patterns); si match, abort commit. Adicional: el `schema_of` ya es PII-free por construcción (Phase 1 lockedo). El operador ya inspeccionó visualmente en Plan 04-02 Task 2.3 antes de aprobar. |
| T-4-03 | Tampering / FALSE PASS | Verified-live tests pasan con SafeModel.from_api stub (sin real wire shape verification) | mitigate | Test `test_safemodel_from_api_typed_defaults` lockea la invariante de TOLERANCIA (no levanta), NO la invariante de fidelidad al wire — esa fidelidad la cubre el driver vivo del Plan 04-02 (`probe_field_type_map`), cuyos findings clasificados quedan committeados en el findings markdown. El gap entre mock-stub-pass y wire-fidelity es explícitamente documentado en el roadmap como la raison d'être del bidirectional diff. |
| T-4-04 | DoS / data corruption | Empty path test pasa pero el wire real cambia comportamiento | mitigate | Tests `test_get_movimientos_empty_path_returns_list` + similar para `get_posiciones` lockean que el cliente devuelve `[]` para 204 — coverage explícita. Si el wire empieza a 200-empty-list en lugar de 204, el test sigue verde (`[]` también pasa). Si el wire 200-None, el cliente fallaba antes (HIGY-04 lo absorbe ahora con un raise tipado en lugar de assert). |
| T-4-05 | SYNC-ASYNC-DRIFT | Mocked test sync vs async no detecta drift real en `_request` | mitigate | Test `test_get_movimientos_drop_none_emits_only_required_params` + mirror async verifican que ambos surfaces emiten EL MISMO URL exacto contra el mock — la URL es el contract verificable. El wire-level drift se cubre por `probe_parity_sync_async` del Plan 04-02. Combinados, los dos checks dan cobertura completa (mocked = contract, live = wire conformance). |
| T-4-06 | Information Disclosure | Errors envelope leak del test contiene PII real | accept | Test `test_errors_envelope_parsed_on_4xx` usa valores sintéticos inline (`"CTA-INVALID"`, `"invalid cuenta"`, `"CTA-INVALID not found"`); ningún valor real. El envelope que termina en el findings file (Plan 04-02 Task 2.3) sí puede contener PII real si el server lo refleja — el operador inspeccionó pre-commit. |
| T-3-04 (inherited Phase 3) | Information Disclosure | Test imports leak | mitigate (inherited) | Los tests importan `Cuenta`, `Movimiento`, etc. del barrel — solo nombres de clase, no instancias con datos reales. La autouse fixture precarga `_token = "test-token"` (valor sentinela conocido). |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | NO se instalan paquetes externos. Package Legitimacy Audit vacía intencional. |
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Test sections present and ordered
grep -F "# ------ Verified live (Phase 4) ------" packages/higyrus-client/tests/test_client.py
grep -F "# ------ Verified live (Phase 4) ------" packages/higyrus-client/tests/test_async_client.py
grep -F "# ------ Regressions ------" packages/higyrus-client/tests/test_client.py
grep -F "# ------ Regressions ------" packages/higyrus-client/tests/test_async_client.py

# 2. Order check: Verified live ANTES de Regressions en ambos archivos
uv run python -c "
for fp in ['packages/higyrus-client/tests/test_client.py', 'packages/higyrus-client/tests/test_async_client.py']:
    src = open(fp).read()
    vl, re = src.find('# ------ Verified live (Phase 4) ------'), src.find('# ------ Regressions ------')
    assert vl > 0 and re > 0 and vl < re, f'{fp}: order violated'
print('order OK in both files')
"

# 3. 5 schemas exist with envelope D-21
uv run python -c "
import json
from pathlib import Path
files = sorted(Path('.planning/verification/schemas/higyrus-client').glob('*.json'))
assert len(files) == 5
required = {'endpoint', 'client_function', 'captured_at', 'base_url', 'sample_params', 'schema'}
for f in files:
    d = json.loads(f.read_text())
    assert required <= set(d.keys()), f'{f.name} missing keys'
print('5 schemas with D-21 envelope OK')
"

# 4. Findings markdown header
head -1 .planning/verification/higyrus-client-findings.md  # → "# Findings: higyrus-client"

# 5. Static checks
uv run mypy packages/higyrus-client
uv run ruff check packages/higyrus-client/tests
uv run ruff format --check packages/higyrus-client/tests

# 6. Suite green (22+ sync + 15+ async ≥ 37 min)
uv run pytest packages/higyrus-client -q

# 7. Whole-repo non-regression
uv run pytest -q
uv run mypy .
uv run ruff check .
uv run ruff format --check .

# 8. Commit baseline present in git log
git log --oneline -1 | grep -E "(04-03|DRIFT-01)"
git diff --stat HEAD~1..HEAD | grep -E "schemas/higyrus-client/.*\.json|higyrus-client-findings.md"
```

Per Wave 3 merge (Phase 4 close):
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
git status  # → clean
git log --oneline -5  # → últimos commits: Plan 04-01 fix + tests, Plan 04-02 driver + .env, Plan 04-03 tests + baseline
```
</verification>

<success_criteria>
- `test_client.py` + `test_async_client.py` con sección `# ------ Verified live (Phase 4) ------` agregada en posición intermedia (entre pre-existentes y `# ------ Regressions ------`) con tests para HIGY-02 (URL lock), HIGY-03 (SafeModel tolerance), HIGY-05 (errors envelope), HIGY-06 (drop_none parity), HIGY-07 (empty path) per D-HIGY-17
- Sección `# ------ Regressions ------` de Plan 04-01 preservada intacta abajo de Verified live
- Orden divider verificado: pre-existentes → Verified live (Phase 4) → Regressions
- Tests pre-existentes (12 sync + 5 async) sin modificar
- 5 schema snapshots DRIFT-01 mirror committeados (get-health.json, get-listado-cuentas.json, get-movimientos.json, get-posicion-valuada.json, get-posiciones.json) con envelope D-21
- 1 findings markdown committeado (`.planning/verification/higyrus-client-findings.md`) con esqueleto + findings clasificados del primer live run
- Defense-in-depth PII scan pasada en los 5 JSONs antes del commit (T-4-SC mitigated)
- Suite completa `uv run pytest -q` verde con >= 37 tests Higyrus (22+ sync + 15+ async)
- Suite completa de TODO el repo `uv run pytest -q` verde sin regresiones (Phase 1/2/3 intactos)
- mypy --strict sin errores
- ruff check + format limpios
- Commit atómico `feat(04-03): commit DRIFT-01 mirror baseline (5 Higyrus schemas + Phase 4 findings)` con Co-Authored-By
- Phase 4 cerrada: HIGY-01..07 cumplidos (HIGY-01/02/05/06/07 via driver + Verified-live, HIGY-03 via field_type_map findings, HIGY-04 via Plan 04-01 fix + regressions)
- DRIFT-01 mirror cumplido para higyrus-client
- Plan 04-03 corre autonomous porque el checkpoint humano del live run ya pasó en Plan 04-02 Task 2.3
</success_criteria>

<output>
Crear `.planning/phases/04-higyrus-verification/04-03-SUMMARY.md` cuando done, registrando:
- Lista de los N tests Verified-live (Phase 4) agregados por surface (sync + async) con sus nombres exactos
- Confirmación de la decisión sobre HIGY-07 (test único parametrizado vs N tests sub-divididos por endpoint)
- Cantidad final de tests por surface en cada archivo (esperado: 12 pre + 5+ Verified live + 5 Regressions ≥ 22 sync; 5 pre + 5+ Verified live + 5 Regressions ≥ 15 async)
- Confirmación de orden divider (Verified live ANTES de Regressions) en ambos archivos
- Confirmación del commit del baseline (hash del commit, archivos committeados)
- Cantidad final de findings en el markdown committeado (cuántos OPEN, CONFIRMED, EXPECTED, NO-FIX clasificados por el humano en Plan 04-02 Task 2.3)
- Cualquier deviation de los patches descritos en 04-PATTERNS.md (Discretion)
- Phase 4 final report: requirements cubiertos HIGY-01..07, schemas + findings committeados, fix HIGY-04 cerrado
- Métrica de duración del plan
</output>
</content>
