---
phase: 43-market-data-client-forma-de-instrument-segment-5-claves-extr
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/tests/test_reference_models.py
  - packages/market-data-client/tests/test_reference_core.py
  - packages/market-data-client/tests/test_reference_client.py
  - packages/market-data-client/tests/test_reference_async_client.py
  - packages/market-data-client/tests/test_decode.py
  - packages/market-data-client/tests/test_reference_envelope_unwrap.py
autonomous: true
requirements: [SHAPE-01]

must_haves:
  truths:
    - "Un consumidor que llama get_instruments() lee currency, days_to_maturity, maturity, outright, subscribed y active del wire — hoy esos seis valores se descartan en silencio (D-02, D-03)"
    - "Un consumidor que llama get_segments() recibe filas pobladas (segment + live_instruments), no filas de tres strings vacios (D-06)"
    - "Instrument.marketId sigue existiendo como alias publicado y ahora lleva el valor real de market_id, sin que un marketId explicito del payload sea pisado (D-04)"
    - "Instrument ya no declara el campo camelCase de tipo de instrumento — el wire nunca lo mando y toda instancia liberada lo leia vacio (D-05)"
    - "Las fixtures de los 6 archivos de test afectados usan el key-set medido del wire con valores sinteticos, y ninguna se renombro para seguir pasando (D-12)"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "Instrument (11 campos + override from_api) y Segment (2 campos) reconciliados contra la lectura fresca del 2026-08-31"
      contains: "class Instrument(SafeModel)"
    - path: "packages/market-data-client/tests/test_reference_models.py"
      provides: "field-set exacto de Instrument y Segment, par de tests de alias-mirror, fixtures _WIRE_INSTRUMENT_ROW y _WIRE_SEGMENT_ROW"
      contains: "test_instrument_field_set_matches_reconciled_wire"
    - path: "packages/market-data-client/tests/test_reference_envelope_unwrap.py"
      provides: "aserciones de VALOR sobre las filas desenvueltas — criterio 2 reproducible"
      contains: "live_instruments"
  key_links:
    - from: "packages/market-data-client/tests/test_reference_models.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "dataclasses.fields(Instrument) y dataclasses.fields(Segment) — asercion de conjunto exacto"
      pattern: "dataclasses\\.fields\\((Instrument|Segment)\\)"
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/_decode.py"
      via: "Instrument.from_api espeja market_id hacia el alias camelCase ANTES de que walk_model vea el payload"
      pattern: "super\\(Instrument, cls\\)\\.from_api"
    - from: "packages/market-data-client/tests/test_decode.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "el set exacto de clases que declaran un override propio de from_api pasa a tres elementos"
      pattern: "overriding =="
---

<objective>
Reconciliar `Instrument` y `Segment` de `market-data-client` contra la lectura fresca del wire del
2026-08-31 (`42-WIRE-READ.md` seccion 2 mas los dos captures del 42), con disposicion campo por
campo: `Instrument` gana los 6 campos wire-only medidos mas `active` nullable y conserva su alias
camelCase como alias aditivo (D-04, precedente D-22); `Segment` se reemplaza por completo (D-06).
Los sitios de test que la correccion rompe se **re-derivan** al key-set medido con valores
sinteticos — ninguno se renombra para que siga pasando.

Cubre las decisiones locked **D-01, D-02, D-03, D-04, D-05, D-06** y, en su mitad de fixtures,
**D-12**.

Purpose: hoy `get_segments()` devuelve filas enteramente vacias (los tres campos declarados son
disjuntos del wire) y `get_instruments()` descarta seis valores que el servidor si manda. Es la
mitad SHAPE-01 del requisito de la fase.

Output: `models.py` con `Instrument` y `Segment` corregidos, y 6 archivos de test re-derivados con
la suite del paquete verde.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-CONTEXT.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-PATTERNS.md
</context>

<interface_context>
## Forma objetivo de `Instrument` (11 campos, orden load-bearing)

Sin default, en este orden: `symbol: str`, `marketId: str`, `segment: str`, `expired: bool`,
`market_id: str`, `currency: str`, `days_to_maturity: int`, `maturity: str`, `outright: bool`,
`subscribed: bool`.

Con default, **ultimo y unico**: `active: bool | None = None`.

Metodo de clase nuevo: `from_api(cls, payload: Any) -> Self`.

Campo removido: el `str` camelCase de tipo de instrumento declarado hoy en `models.py:798`.

## Forma objetivo de `Segment` (2 campos)

`segment: str`, `live_instruments: int`. Sin defaults, sin override.

Campos removidos: los tres declarados hoy en `models.py:811-813`.

## Key-set medido del wire (fuente de las fixtures)

`GET /instruments` — 50/50 filas con key-set homogeneo:
`active` (NoneType en las 50), `currency` (str), `days_to_maturity` (int), `expired` (bool),
`market_id` (str), `maturity` (str), `outright` (bool), `segment` (str), `subscribed` (bool),
`symbol` (str). El wire **no** manda el alias camelCase ni el campo de tipo de instrumento.

`GET /instruments/segments` — 4/4 filas: `segment` (str), `live_instruments` (int).

## Simbolos ya existentes que se reutilizan sin re-implementar

- `SafeModel.from_api` / `.empty()` / `.to_dict()` / `__bool__` — `models.py:126-229`
- `_from_api(factory, caplog, payload)` y `_tuples(records)` — helpers locales de
  `packages/market-data-client/tests/test_decode.py`
- `_strip_optional` — `test_decode.py:49`
- `_WIRE_SYMBOL_ROW` — `test_reference_models.py:198-208`, contrato de provenance a copiar
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — field-set exacto, par de alias-mirror y fixtures medidas para Instrument y Segment</name>

  <files>packages/market-data-client/tests/test_reference_models.py</files>

  <read_first>
    - `packages/market-data-client/tests/test_reference_models.py` (archivo completo — es el que se edita; hay que ver los imports, `_ALL_MODELS` en :32-38, los tests T1/T2 en :41-54, el patron de field-set de `CalendarConfig` en :161-180, el contrato de provenance de `_WIRE_SYMBOL_ROW` en :198-208, el field-set de `Symbol` en :211-228 y el par de alias-mirror en :262-277)
    - `packages/market-data-client/src/market_data_client/models.py:786-813` — la declaracion ACTUAL de `Instrument` y `Segment` (estado de partida, para no asumir)
    - `packages/market-data-client/src/market_data_client/models.py:816-901` — `Symbol`, el precedente D-22 completo cuyo par de tests se clona
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-PATTERNS.md`, secciones "Tests — field-set exacto" y "Tests — el par de alias-mirror de `Instrument`" — los excerpts verbatim a adaptar
  </read_first>

  <behavior>
    - `test_instrument_field_set_matches_reconciled_wire`: el conjunto de nombres de `dataclasses.fields(Instrument)` es exactamente los 11 de `<interface_context>`, y `Instrument.from_api({})` NO tiene el atributo del campo de tipo de instrumento removido.
    - `test_segment_field_set_matches_reconciled_wire`: el conjunto de nombres de `dataclasses.fields(Segment)` es exactamente `{"segment", "live_instruments"}`, y `Segment.from_api({})` no tiene ninguno de los tres atributos removidos.
    - `test_instrument_market_id_alias_mirrors_wire_snake_case`: `Instrument.from_api(_WIRE_INSTRUMENT_ROW).marketId` es igual a `.market_id` y no es cadena vacia.
    - `test_instrument_explicit_camel_case_payload_key_still_wins`: sobre un dict hand-built con AMBAS claves, el valor camelCase explicito sobrevive y el snake_case queda en su propio campo — el espejo solo rellena una clave ausente, nunca pisa.
    - `test_instrument_from_api_populated_wire_row`: cada una de las 10 claves de `_WIRE_INSTRUMENT_ROW` aterriza en un campo real — `currency`, `days_to_maturity`, `maturity`, `outright`, `subscribed` con sus valores, y `active` es `None`.
    - `test_segment_from_api_populated_wire_row`: `segment` y `live_instruments` llevan los valores de `_WIRE_SEGMENT_ROW`.
    - T1 re-derivado (`test_instrument_from_api_empty_dict_typed_zero_defaults`): sobre `{}` los seis campos escalares nuevos toman su cero tipado y `active` es `None`.
    - T2 re-derivado (`test_segment_from_api_none_does_not_raise`): sobre `None` los dos campos nuevos toman su cero tipado.
  </behavior>

  <action>
Editar SOLO `packages/market-data-client/tests/test_reference_models.py`. Este task escribe la
especificacion ejecutable ANTES de tocar el modelo: al terminar, los tests nuevos y los dos
re-derivados deben FALLAR. No tocar `models.py` en este task.

(1) Agregar dos fixtures de modulo siguiendo el contrato de provenance verbatim de
`_WIRE_SYMBOL_ROW` (`:198-208`): un comentario que diga que los VALORES son sinteticos y que solo el
KEY SET y los TIPOS vienen de la medicion en vivo, citando `42-WIRE-READ.md` seccion 2 y los
findings F-205 a F-218 como procedencia. Colocarlas antes de los tests que las usan.

`_WIRE_INSTRUMENT_ROW` — las 10 claves medidas, con valores inventados de los tipos medidos:
`active` con valor `None`, `currency` una cadena de 3 letras, `days_to_maturity` un entero,
`expired` con valor `False`, `market_id` una cadena, `maturity` una fecha ISO como cadena,
`outright` con valor `True`, `segment` una cadena, `subscribed` con valor `False`, `symbol` una
cadena de simbolo. NO incluir el alias camelCase (el wire no lo manda, y su ausencia es lo que hace
disparar el espejo) y NO incluir el campo de tipo de instrumento que se remueve.

`_WIRE_SEGMENT_ROW` — exactamente `segment` (cadena) y `live_instruments` (entero).

PROHIBIDO leer `.planning/verification/captures/market-data-wire-*-42.json` desde el test: el
directorio esta gitignored (`.gitignore:53`, capturas crudas con PII, nunca committeables) y un test
que lo abra pasa en la maquina del ejecutor y falla en CI con `FileNotFoundError`. Valores
sinteticos, key-set real.

(2) Agregar los seis tests nuevos descritos en el bloque `<behavior>`. Los dos de field-set se
clonan de `test_calendar_config_field_set_matches_reconciled_wire` (`:161-180`), que es el unico
analog que combina la asercion de conjunto exacto CON el `assert not hasattr(...)` de la remocion, y
llevan un comentario que explique —como lo hace `test_symbol_field_set_matches_reconciled_wire`
(`:211-228`)— que solo el conjunto exacto prueba las dos direcciones a la vez: que las adiciones
aterrizaron Y que el alias publicado sobrevivio (D-22 prohibe el rename). El par de alias-mirror se
clona verbatim de `test_symbol_market_id_alias_mirrors_wire_snake_case` y
`test_symbol_explicit_camel_case_payload_key_still_wins` (`:262-277`), sustituyendo `Symbol` por
`Instrument` y `_WIRE_SYMBOL_ROW` por `_WIRE_INSTRUMENT_ROW`.

(3) Re-derivar T1 (`:41-47`) y T2 (`:50-54`) a la forma nueva. NO renombrarlos: conservan su nombre
y su intencion (ceros tipados sobre payload vacio y sobre `None`), y cambian solo las aserciones
para cubrir los campos nuevos y dejar de leer los removidos. Renombrar un test para que siga pasando
esta prohibido por el criterio 4 de la fase y por el texto verbatim de SHAPE-01.

(4) Construir SIEMPRE via `Model.from_api(payload)`. Ni un solo constructor por kwargs: la
convencion del archivo es construccion exclusiva por `from_api`, y un `__post_init__` o una property
serian invisibles a `dataclasses.fields()`, al walker y a `to_dict()`.

(5) No tocar `_ALL_MODELS` (`:32-38`): `Instrument` y `Segment` siguen ahi y siguen sin
`received_at` (D-05, reference data unstamped), asi que
`test_reference_models_have_no_received_at` (`:183-190`) sigue verde sin edicion.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client/tests/test_reference_models.py -q --no-cov; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_reference_models.py -q --no-cov` termina con exit code distinto de 0 (RED) y el resumen nombra al menos `test_instrument_field_set_matches_reconciled_wire`, `test_segment_field_set_matches_reconciled_wire`, `test_instrument_market_id_alias_mirrors_wire_snake_case`, `test_instrument_from_api_populated_wire_row` y `test_segment_from_api_populated_wire_row`.
    - `uv run pytest packages/market-data-client/tests/test_reference_models.py --collect-only -q --no-cov` termina con exit code 0 y lista 6 nombres de test nuevos ademas de los ya existentes. Un fallo de COLECCION aqui es un error de sintaxis, no un RED valido.
    - `git diff --name-only` lista exactamente un archivo: `packages/market-data-client/tests/test_reference_models.py`.
    - `grep -c 'verification/captures' packages/market-data-client/tests/test_reference_models.py` devuelve `0`.
    - `grep -c 'test_instrument_from_api_empty_dict_typed_zero_defaults' packages/market-data-client/tests/test_reference_models.py` devuelve `1` y `grep -c 'test_segment_from_api_none_does_not_raise' packages/market-data-client/tests/test_reference_models.py` devuelve `1` (ningun test preexistente fue renombrado).
  </acceptance_criteria>

  <done>
    Los 6 tests nuevos existen y fallan por la razon correcta (atributo inexistente o conjunto de
    campos distinto), los dos re-derivados fallan por la misma razon, y ningun test preexistente fue
    renombrado ni borrado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — reemplazar Instrument y Segment en models.py</name>

  <files>packages/market-data-client/src/market_data_client/models.py</files>

  <read_first>
    - `packages/market-data-client/src/market_data_client/models.py:770-815` — el bloque de comentario de reference data y las declaraciones ACTUALES de `Instrument` y `Segment`
    - `packages/market-data-client/src/market_data_client/models.py:816-901` — `Symbol` completo: el patron D-22 a copiar, incluidos sus tres comentarios justificativos y el `super()` de dos argumentos
    - `packages/market-data-client/src/market_data_client/models.py:904-958` — `CalendarDay` y `CalendarConfig`: el patron de docstring de remocion no-breaking (argumento D-13) a re-escribir
    - `packages/market-data-client/src/market_data_client/models.py:126-229` — `SafeModel`, para confirmar la firma exacta de `from_api` y que `Self` y `Any` ya estan importados
    - `packages/market-data-client/tests/test_reference_models.py` — los tests recien escritos que definen el contrato objetivo
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md`, secciones "Patron 1 — Alias aditivo D-22" y "Code Examples · `Segment` reemplazado (D-06)"
  </read_first>

  <action>
Editar SOLO `packages/market-data-client/src/market_data_client/models.py`, y dentro de el SOLO el
bloque de `Instrument` (`:786-800`) y el de `Segment` (`:802-813`). No tocar `Symbol`, ni el bloque
de health, ni `__all__`: los cambios de `__all__` pertenecen al plan 43-02, que agrega
`FeedSubscription`.

(1) `Instrument` — reemplazar la lista de campos por la forma objetivo de `<interface_context>`,
respetando el orden: los diez sin default primero, `active: bool | None = None` ultimo. El orden es
load-bearing. `@dataclass` exige que todo campo sin default preceda a los que tienen default, y una
violacion revienta la COLECCION ENTERA de pytest en tiempo de import, no un test.

Disposicion campo por campo a implementar:
- `symbol`, `segment`, `expired` — MANTENER (D-01, los tres estan en el wire).
- `market_id`, `currency`, `days_to_maturity`, `maturity`, `outright`, `subscribed` — AGREGAR
  (D-02, medidos en 50/50 filas, delta vacio contra el baseline del 2026-07-31).
- `active: bool | None = None` — AGREGAR nullable (D-03). El wire mando `null` en las 50 filas
  medidas; declararlo `bool` plano convertiria una `extra` medida en una `missing` permanente sobre
  cada lectura de catalogo, que es la regresion que el criterio 3 de la fase prohibe por nombre.
- `marketId` — MANTENER como alias aditivo DEPRECADO (D-04), con remocion programada para el
  proximo MAJOR. Nunca renombrar: prohibido en `REQUIREMENTS.md` seccion Out of Scope.
- El campo `str` camelCase de tipo de instrumento (`models.py:798`) — REMOVER (D-05).

(2) `Instrument.from_api` — agregar el override, copiado del de `Symbol` (`:878-901`) con los cuatro
elementos load-bearing intactos:
  a. el guard `isinstance(payload, dict)` y la doble condicion "la clave camelCase NO esta en el
     payload Y la clave snake_case SI esta";
  b. la copia por desempaquetado del dict, nunca una mutacion in-place del dict del caller. Pisar el
     payload del llamador es el vector Tampering que el par de tests del Task 1 pinnea;
  c. `super(Instrument, cls).from_api(payload)` con DOS argumentos explicitos. Un `super()` de cero
     argumentos levanta `TypeError: obj must be an instance or subtype of type` en runtime porque
     `@dataclass(slots=True)` RECONSTRUYE la clase y la celda implicita `__class__` sigue apuntando
     a la clase pre-slots;
  d. el docstring completo explicando que el espejo corre ANTES del walker (por eso la clave
     sintetizada no dispara un record `extra`: el cliente sintetizo esa clave, el vendor no la
     mando) y que solo RELLENA una clave ausente.

(3) Docstring de `Instrument` — reemplazar el parrafo que hoy declara la forma como PROVISIONAL
(A1/A2) por uno de procedencia real siguiendo el patron de `FeedMarket` (`:1199-1201`): field set
tomado de la lectura en vivo del 2026-08-31 (`42-WIRE-READ.md` seccion 2, 50 filas medidas), no de
la OpenAPI y no de un mock. Agregar el parrafo de justificacion de `| None` para `active` con la
doctrina option-b, declarando explicitamente que el miembro `bool` de la union NUNCA se observo
(50/50 filas `null`) y que es una ASUNCION DECLARADA autocorrectiva via el censo de divergencias
— mismo patron que `FeedIngestor.last_error` (`:1271-1275`). Agregar el parrafo de remocion
no-breaking del campo de tipo de instrumento con el argumento D-13 (el wire nunca lo mando, toda
instancia liberada lo leia como cadena vacia), modelado sobre `CalendarDay` (`:917-921`). Conservar
la nota de que `Instrument` NO lleva `received_at` (D-05, reference data unstamped).

(4) `Segment` — reemplazar la clase entera por los dos campos de `<interface_context>`, sin override
y sin defaults. Docstring nuevo con: endpoint, procedencia (`42-WIRE-READ.md` seccion 2, 4 filas
medidas), el argumento de remocion no-breaking (el key-set del wire y el declarado eran DISJUNTOS,
asi que ninguna fila que un consumidor liberado haya leido pudo tener un valor poblado), la nota
explicita de que NO se alias-mapea bajo D-22 —la precondicion de D-22 es UNA misma clave con
variante de spelling camelCase/snake_case, y aca son nombres distintos (D-06, rechazo explicito)— y
la nota `received_at` (D-05).

(5) No agregar `received_at` a ninguno de los dos: `test_reference_models_have_no_received_at`
(`test_reference_models.py:183-190`) lo pinnea sobre todo `_ALL_MODELS` excepto `Symbol`.

(6) No agregar ningun campo anotado como mapping ni con `Any` en profundidad: el paso
`surface-types` del job `lint` (`tools/check_surface_types.py`) reddenea cualquiera de los dos sobre
una clase exportada, y `_FIELD_EXEMPTIONS` tiene una unica entrada que no es de este paquete.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client/tests/test_reference_models.py -q --no-cov; uv run python -c "import dataclasses; from market_data_client.models import Instrument, Segment; print(sorted(f.name for f in dataclasses.fields(Instrument))); print(sorted(f.name for f in dataclasses.fields(Segment)))"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_reference_models.py -q --no-cov` termina con exit code 0 (GREEN): los 6 tests nuevos y los 2 re-derivados pasan.
    - `uv run python -c "import dataclasses; from market_data_client.models import Instrument; print(sorted(f.name for f in dataclasses.fields(Instrument)))"` imprime exactamente `['active', 'currency', 'days_to_maturity', 'expired', 'marketId', 'market_id', 'maturity', 'outright', 'segment', 'subscribed', 'symbol']`.
    - `uv run python -c "import dataclasses; from market_data_client.models import Segment; print(sorted(f.name for f in dataclasses.fields(Segment)))"` imprime exactamente `['live_instruments', 'segment']`.
    - `uv run python -c "from market_data_client.models import Instrument; print(Instrument.__dict__.get('from_api') is not None)"` imprime `True`, y `uv run python -c "from market_data_client.models import Segment; print(Segment.__dict__.get('from_api') is None)"` imprime `True`.
    - `uv run python -c "from market_data_client.models import Instrument as I; print(I.from_api({'symbol':'S','market_id':'ROFX'}).marketId, I.from_api({'symbol':'S','marketId':'LEGACY','market_id':'ROFX'}).marketId)"` imprime `ROFX LEGACY` (el espejo rellena una clave ausente y respeta una explicita).
    - `uv run python tools/check_surface_types.py` termina con exit code 0 y reporta `0 violations`.
    - `git diff --name-only` lista exactamente `packages/market-data-client/src/market_data_client/models.py` (mas el archivo de test ya commiteado en el Task 1).
  </acceptance_criteria>

  <done>
    Los dos modelos declaran exactamente su forma objetivo, el override de alias existe solo en
    `Instrument` (no en `Segment`), el espejo respeta una clave camelCase explicita, y el gate
    `surface-types` sigue en cero violaciones.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-derivar los 5 archivos de test restantes y agregar las aserciones de valor del criterio 2</name>

  <files>packages/market-data-client/tests/test_reference_core.py, packages/market-data-client/tests/test_reference_client.py, packages/market-data-client/tests/test_reference_async_client.py, packages/market-data-client/tests/test_decode.py, packages/market-data-client/tests/test_reference_envelope_unwrap.py</files>

  <read_first>
    - `packages/market-data-client/tests/test_reference_core.py:155-200` — los dos parsers de coleccion (`test_parse_instruments_response_returns_list_of_models` en :167-176, `test_parse_segments_response_returns_list_of_models` en :185-190) y el contrato "valores sinteticos, key-sets reales" de :208-213
    - `packages/market-data-client/tests/test_reference_client.py:40-92` — el body de instruments (:44-52) y el de segments (:76-92)
    - `packages/market-data-client/tests/test_reference_async_client.py:35-88` — el gemelo async exacto de los dos anteriores
    - `packages/market-data-client/tests/test_decode.py:645-700` — `test_real_reference_model_missing_field_defaults_and_now_reports` (:664-673) y `test_real_model_extra_wire_key_reports_and_builds_unchanged` (:676-689), mas los helpers locales `_from_api` y `_tuples`
    - `packages/market-data-client/tests/test_decode.py:1330-1400` — `test_models_with_a_from_api_override_are_never_a_nested_field_type` (:1339-1360), cuyo docstring nombra los DOS overrides de hoy y cuya asercion `overriding == {...}` es de igualdad exacta
    - `packages/market-data-client/tests/test_reference_envelope_unwrap.py` (archivo completo) — `_INSTRUMENTS_ENVELOPE` y `_SEGMENTS_ENVELOPE` ya usan el key-set real; faltan las aserciones de VALOR
    - `packages/market-data-client/src/market_data_client/models.py:786-830` — la forma nueva ya implementada en el Task 2
  </read_first>

  <behavior>
    - `test_parse_segments_response_returns_list_of_models` deja de pasar vacuamente: sobre un body con el key-set real asserta que `result[0].segment` y `result[0].live_instruments` llevan los valores del body.
    - `test_get_segments_sends_bearer_no_params` (sync) y su gemelo async assertan sobre `result[0].segment`, no sobre un campo inexistente.
    - `test_real_reference_model_missing_field_defaults_and_now_reports` conserva su asercion de pertenencia sobre un payload SIN la clave snake_case (donde el espejo no dispara y el alias sigue emitiendo `missing`) y GANA un caso con la clave snake_case presente que demuestra que el espejo ELIMINA ese `missing`.
    - `test_real_model_extra_wire_key_reports_and_builds_unchanged` usa el key-set real de `Segment` mas una clave desconocida del vendor, y sigue probando que la clave desconocida se reporta `extra`.
    - `test_models_with_a_from_api_override_are_never_a_nested_field_type` asserta un conjunto de TRES nombres y sigue probando que ninguno de ellos aparece como tipo de campo anidado.
    - Los cuatro tests de desenvolvimiento de sobre (sync y async, instruments y segments) assertan VALORES de las filas, no solo el conteo y el tipo.
  </behavior>

  <action>
Re-derivar los cinco archivos. Ninguno se renombra ni se borra: cada test conserva su nombre y su
intencion, y solo cambian las fixtures y las aserciones para reflejar el key-set medido. Todas las
fixtures llevan valores SINTETICOS con key-set y tipos reales, siguiendo el contrato ya redactado en
`test_reference_core.py:208-213`. Ningun test lee el capture gitignored.

**A. `test_reference_core.py`**
- `test_parse_instruments_response_returns_list_of_models` (`:167-176`): el body de hoy manda el
  alias camelCase explicito, lo que SUPRIME el espejo y deja la fixture stale. Reemplazar las dos
  filas por el key-set medido de 10 claves (el mismo de `_WIRE_INSTRUMENT_ROW`), con
  `market_id` en snake_case y sin el alias camelCase, y agregar una asercion de que
  `result[0].marketId` quedo poblado por el espejo con el mismo valor que `result[0].market_id`.
- `test_parse_segments_response_returns_list_of_models` (`:185-190`): reemplazar el body por una
  fila con las dos claves reales y agregar las dos aserciones de valor. Hoy pasa vacuamente porque
  solo comprueba `isinstance`.

**B. `test_reference_client.py`**
- `test_get_instruments_sends_bearer_and_encodes_params` (`:40-73`): el `json=` de `:44-52` pasa al
  key-set medido de 10 claves. Conservar intactas todas las aserciones sobre el request (Bearer,
  path, encoding de bools, `offset=0` preservado, `subscribed` ausente): ese es el proposito del
  test y no cambia.
- `test_get_segments_sends_bearer_no_params` (`:76-92`): el `json=` de `:80` pasa a una fila con las
  dos claves reales, y la asercion de `:87` pasa a leer `result[0].segment`, agregando ademas
  `result[0].live_instruments`. Conservar las aserciones de request.
- NO tocar `:139` ni ninguna otra linea de este archivo: la `.description` de `:139` pertenece a
  `CalendarDay`, un modelo distinto que esta fase no toca.

**C. `test_reference_async_client.py`** — aplicar exactamente la misma re-derivacion que en B sobre
`:40-48` y `:74-83`. Los dos archivos son gemelos y deben quedar simetricos; una divergencia entre
ellos es la senal que `test_public_surface_market_data.py` existe para detectar. NO tocar `:133`
(`CalendarDay`).

**D. `test_decode.py`**
- `test_real_reference_model_missing_field_defaults_and_now_reports` (`:664-673`): conservar el
  payload actual, que no lleva la clave snake_case, y conservar la asercion de pertenencia de que el
  alias camelCase emite `missing` (con el espejo no disparando, sigue siendo verdad). Agregar un
  SEGUNDO bloque en el mismo test que llame a `_from_api` con un payload que SI lleve la clave
  snake_case y asserte que la tupla del alias con `missing` ya NO esta en `_tuples(records)`: esa es
  la prueba de D-04 del lado del decode y lo que impide que este test siga pasando vacuamente.
  Actualizar el docstring para nombrar las dos mitades. Usar `_from_api` (no `Model.from_api`
  directo) para abrir un scope fresco: sin el, el dedupe por triple modelo/path/kind de un test
  previo apaga la asercion en silencio.
- `test_real_model_extra_wire_key_reports_and_builds_unchanged` (`:676-689`): reemplazar las tres
  claves viejas del payload por las dos reales, conservando la clave desconocida del vendor.
  Sustituir la asercion `obj.marketSegmentId == "DDF"` por `obj.segment` y `obj.live_instruments`
  con los valores del payload, y conservar la asercion de pertenencia de la clave desconocida como
  `extra`.
- `test_models_with_a_from_api_override_are_never_a_nested_field_type` (`:1339-1360`): la asercion
  `overriding == {...}` es de IGUALDAD EXACTA y pasa a tener tres elementos: los dos de hoy mas
  `"Instrument"`. Actualizar tambien el docstring, que hoy enumera solo dos overrides y explica
  cada uno; agregar la frase que explica el tercero (espeja la clave snake_case sobre el alias
  camelCase deprecado, D-04). La segunda mitad del test —que ningun override aparece como tipo de
  campo anidado— sigue en pie sin cambio: nada declara un campo tipado `Instrument`.
- NO tocar `test_no_call_site_exempt_safemodel_appears_as_a_nested_field_type` (`:690-745`): deriva
  su conjunto dinamicamente y ningun hint contiene la subcadena del nombre de la clase.

**E. `test_reference_envelope_unwrap.py`** — este archivo cierra el criterio 2 de la fase de forma
reproducible.
- `_INSTRUMENTS_ENVELOPE` y `_SEGMENTS_ENVELOPE` (`:37-68`) ya usan el key-set real del wire
  (agregado en Phase 33) y NO se re-derivan.
- `test_get_instruments_unwraps_the_items_envelope` (`:70-83`) y su gemelo async (`:86-96`):
  agregar aserciones de valor sobre los campos nuevos — `market_id`, el alias camelCase espejado
  (mismo valor que `market_id`), `currency`, `days_to_maturity`, `maturity`, `outright`,
  `subscribed`, y `active is None`. Antes de esta fase esos siete valores se descartaban.
- `test_get_segments_unwraps_the_segments_envelope` (`:99-108`) y su gemelo async (`:111-118`):
  agregar `result[0].segment` y `result[0].live_instruments` con los valores del sobre. Es la
  demostracion reproducible de que la fila deja de salir vacia. Actualizar el comentario del test
  para decir que ahora ademas prueba que la fila viene POBLADA, no solo que hay una sola.
- `test_bare_list_bodies_still_parse` (`:120-139`): el proposito del test es que una respuesta de
  lista pelada siga aceptandose, no la forma de los campos. Re-derivar su fixture al key-set medido
  CONSERVANDO el alias camelCase explicito (que es lo que la asercion de `:138` comprueba y sigue
  siendo el caso valido de "una fixture vieja gana sobre el espejo") y removiendo el campo de tipo
  de instrumento, que ya no es un campo declarado.

**Regla transversal:** nada en este task toca `client.py` ni `aio.py`. Ambas superficies delegan al
mismo objeto funcion de `_core.py` bajo REFAC-03 y los parsers son field-agnostic (D-14); que la
correccion llegue a las dos superficies con cero cambios de fuente es precisamente lo que los
gemelos sync/async de B y C miden.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client -q --no-cov; uv run mypy packages/market-data-client/tests</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q --no-cov` termina con exit code 0 y reporta al menos 711 tests pasados (el baseline medido) sin fallos ni errores.
    - `uv run mypy packages/market-data-client/tests` termina con exit code 0.
    - `uv run mypy` (src global) termina con exit code 0.
    - `uv run ruff check .` y `uv run ruff format --check .` terminan con exit code 0.
    - `uv run python -c "import re,pathlib; s=pathlib.Path('packages/market-data-client/tests/test_decode.py').read_text(); m=re.search(r'overriding == \{[^}]*\}', s); print(m.group(0))"` muestra un conjunto de tres nombres que incluye `Instrument`.
    - `uv run pytest packages/market-data-client/tests/test_reference_envelope_unwrap.py -q --no-cov` termina con exit code 0, y `grep -c 'live_instruments' packages/market-data-client/tests/test_reference_envelope_unwrap.py` devuelve un valor mayor o igual a `3` (la fixture mas al menos las dos aserciones de valor sync/async).
    - `git diff --stat` no lista `packages/market-data-client/src/market_data_client/client.py` ni `.../aio.py` (D-14: cero cambios de fuente en las dos superficies).
    - `git diff --stat` no lista `main_market_data.py` (D-16: fuera del alcance de esta fase; ver la nota de seguimiento en `<flagged_followups>`).
  </acceptance_criteria>

  <done>
    La suite completa del paquete esta verde, los cuatro gemelos sync/async quedaron simetricos, el
    conjunto exacto de overrides refleja D-04, y las aserciones de valor del sobre demuestran de
    forma reproducible que `get_segments()` devuelve filas pobladas.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts que este plan produce

**Simbolos nuevos en `models.py`:**
- `Instrument.from_api` — classmethod override (espejo del alias D-22)

**Campos nuevos en `Instrument`:** `market_id: str`, `currency: str`, `days_to_maturity: int`,
`maturity: str`, `outright: bool`, `subscribed: bool`, `active: bool | None = None`

**Campos removidos de `Instrument`:** el `str` camelCase de tipo de instrumento (`models.py:798`)

**Campos nuevos en `Segment`:** `segment: str`, `live_instruments: int`

**Campos removidos de `Segment`:** los tres declarados hoy en `models.py:811-813`

**Fixtures de test nuevas:**
- `_WIRE_INSTRUMENT_ROW` (`test_reference_models.py`)
- `_WIRE_SEGMENT_ROW` (`test_reference_models.py`)

**Funciones de test nuevas (6):**
- `test_instrument_field_set_matches_reconciled_wire`
- `test_segment_field_set_matches_reconciled_wire`
- `test_instrument_market_id_alias_mirrors_wire_snake_case`
- `test_instrument_explicit_camel_case_payload_key_still_wins`
- `test_instrument_from_api_populated_wire_row`
- `test_segment_from_api_populated_wire_row`

**Funciones de test re-derivadas (9, ninguna renombrada):**
`test_instrument_from_api_empty_dict_typed_zero_defaults`,
`test_segment_from_api_none_does_not_raise`,
`test_parse_instruments_response_returns_list_of_models`,
`test_parse_segments_response_returns_list_of_models`,
`test_get_instruments_sends_bearer_and_encodes_params` (+ gemelo async),
`test_get_segments_sends_bearer_no_params` (+ gemelo async),
`test_real_reference_model_missing_field_defaults_and_now_reports`,
`test_real_model_extra_wire_key_reports_and_builds_unchanged`,
`test_models_with_a_from_api_override_are_never_a_nested_field_type`,
mas las cinco de `test_reference_envelope_unwrap.py` que ganan aserciones de valor.

**Ningun archivo nuevo.** Cero dependencias externas agregadas.
</artifacts_this_phase_produces>

<flagged_followups>
## Seguimiento marcado — NO se corrige en esta fase

`main_market_data.py:1541-1542` dereferencia el campo de `Segment` que D-06 remueve, dentro de
`probe_parity`. Ningun gate estatico lo detecta (`mypy` tiene `files = ["packages/*/src"]`, el hook
de pre-commit `files: ^packages/.*/src/`, y `verification/test_main_market_data_deep_chain.py` parsea
el driver con `ast` sin importarlo y solo audita cadenas de acceso profundo de market data). El sitio
esta dentro de un `try/except Exception` que lo degrada a un FINDING silencioso de handler en vez de
un crash.

**Disposicion de esta fase: NO TOCAR.** D-16 lockea el alcance a `models.py` mas tests mas el
docstring de D-14, y el sitio no es ninguno de los tres. Se documenta como item de seguimiento en
`43-DISPOSITION.md` (plan 43-03, Task 2) y como entrada de backlog en `ROADMAP.md`, con destino
nombrado. No se corrige en silencio y no se descarta en silencio.
</flagged_followups>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendor HTTP response -> `SafeModel.from_api` | El payload es entrada no confiable controlada por el servidor; las claves y los tipos pueden cambiar sin aviso |
| capture en vivo (`.planning/verification/captures/`) -> git | El capture crudo puede contener PII; `.gitignore:53` lo excluye y T-42-05 prohibe transcribir su clave `payload` |
| record de divergencia -> logger `market_data_client` | El sink emite nombres de tipo, nunca valores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | `Instrument.from_api` (espejo de alias) | mitigate | El espejo solo RELLENA una clave camelCase ausente y copia el dict; nunca pisa un valor explicito ni muta el payload del caller. Pinneado por `test_instrument_explicit_camel_case_payload_key_still_wins` (Task 1) |
| T-43-02 | Information Disclosure | fixtures de test vs. captures gitignored | mitigate | Todas las fixtures llevan valores SINTETICOS con key-set y tipos reales. Ningun test abre `.planning/verification/captures/`; verificado por el criterio de aceptacion `grep -c 'verification/captures'` igual a `0` (Task 1) |
| T-43-03 | Information Disclosure | sink de divergencias (`_decode.py`) | accept | Sin cambio en esta fase: el record de 6 claves lleva `type(value).__name__`, nunca el valor. No se toca `_decode.py` |
| T-43-04 | Denial of Service | payload malformado del vendor | mitigate | `SafeModel.from_api` es tolerante por contrato y `walk_model` no levanta bajo la politica por defecto. `Segment.from_api(None)` se pinnea explicitamente en el Task 1 |
| T-43-05 | Tampering | orden de campos de dataclass | mitigate | `active: bool | None = None` es el unico campo con default y va ultimo; una violacion revienta la coleccion de pytest en tiempo de import — falla ruidosa, no silenciosa. Verificado por el criterio `--collect-only` con exit code 0 (Task 1) |
| T-43-SC | Tampering | instalaciones npm/pip/cargo | accept | N/A verificado: esta fase no instala ni actualiza ninguna dependencia externa. `43-RESEARCH.md` seccion "Package Legitimacy Audit" registra cero paquetes, y D-16 prohibe tocar `uv.lock`. El paso `uv lock --check` del job `lint` queda verde por no-op |
</threat_model>

<verification>
Al cerrar el plan, los tres deben ser ciertos simultaneamente:

1. `uv run pytest packages/market-data-client -q --no-cov` — exit code 0, sin fallos.
2. `uv run python tools/check_surface_types.py` — exit code 0, `0 violations`.
3. `uv run ruff check .`, `uv run ruff format --check .`, `uv run mypy`,
   `uv run mypy packages/market-data-client/tests` — los cuatro con exit code 0.

Y el diff del plan NO contiene: `client.py`, `aio.py`, `main_market_data.py`, `pyproject.toml`,
`__init__.py`, `uv.lock`.
</verification>

<success_criteria>
- `dataclasses.fields(Instrument)` devuelve exactamente los 11 nombres de `<interface_context>`.
- `dataclasses.fields(Segment)` devuelve exactamente `segment` y `live_instruments`.
- `Instrument.from_api` existe como override propio de la clase; `Segment.from_api` no.
- El espejo del alias rellena una clave ausente y respeta una explicita (dos casos verificados).
- Los cuatro tests de desenvolvimiento de sobre assertan valores de fila, no solo conteos.
- La suite del paquete pasa con al menos 711 tests y sin regresiones.
- Ningun test fue renombrado ni borrado para que la suite siguiera pasando.
</success_criteria>

<output>
Create `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-01-SUMMARY.md` when done.

El SUMMARY debe incluir, ademas del formato estandar:
- La tabla de disposicion campo por campo de `Instrument` y `Segment` tal como quedo implementada
  (input directo para el criterio 1 de la fase, que el plan 43-03 consolida).
- El conteo antes/despues de tests del paquete.
- Confirmacion explicita de que `client.py`, `aio.py`, `main_market_data.py` y los tres sitios de
  version quedaron sin tocar.
</output>
