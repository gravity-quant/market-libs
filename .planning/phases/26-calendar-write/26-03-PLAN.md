---
phase: 26-calendar-write
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - packages/market-data-client/src/market_data_client/client.py
  - packages/market-data-client/src/market_data_client/aio.py
  - packages/market-data-client/tests/test_calendar_write.py
  - packages/market-data-client/tests/test_calendar_write_async.py
autonomous: true
requirements: [MUT-MD-02]
tags: [market-data-client, mutation, dispatch, calendar, sync-async-parity, python]

must_haves:
  truths:
    - "ROADMAP SC#1: con el gate abierto, los cinco métodos despachan — set_calendar_config → PUT /api/calendar/config, delete_calendar_config → DELETE /api/calendar/config, preview_calendar_config → POST /api/calendar/config/preview, add_holidays → POST /api/calendar/holidays, delete_holiday(day) → DELETE /api/calendar/holidays/{day} — en sync y async."
    - "D-14: _ensure_mutation_allowed() es la PRIMERA sentencia literal de los cinco métodos en ambos shells — antes del builder, antes de self._request y antes de cualquier token fetch."
    - "D-14 / ROADMAP SC#3: preview_calendar_config pasa por el gate igual que los otros (es POST); la excepción read-safe —no persiste— queda DOCUMENTADA en el docstring, no implementada como carve-out."
    - "D-05: set_calendar_config, delete_calendar_config y preview_calendar_config retornan CalendarConfig reusando _core.parse_calendar_config_response SIN modificarlo."
    - "D-06: add_holidays y delete_holiday retornan dict[str, Any] vía _core.parse_calendar_write_response."
    - "D-09 / ROADMAP SC#2: el body de PUT /calendar/config lleva confirm: false por default y confirm: true cuando el consumidor lo setea; pre_open_minutes=10, enabled=true y updated_by='' también viajan."
    - "D-02: los dos DELETE salen con content b'' y sin header Content-Type."
    - "D-17: los cinco métodos existen con nombre idéntico en Client y AsyncClient, y hay 10 shims module-level (5 sync en client.py, 5 async en aio.py)."
    - "Un 422 del servidor levanta MarketDataAPIError por el _core.raise_for_response existente, sin manejo de status nuevo en los métodos."
  prohibitions:
    - "Ningún método debe llamar _core.build_*, self._request ni _ensure_token antes de _ensure_mutation_allowed() — rompería la garantía cero-round-trip."
    - "NO carve-out del gate para preview_calendar_config — crearía un segundo camino, más débil, a la superficie de mutación."
    - "Los métodos async NO se re-exportan al namespace plano de market_data_client — viven bajo aio."
    - "NO agregar manejo de status nuevo en los métodos (422 y 4xx fluyen por raise_for_response sin cambios)."
    - "NO serializar dentro del builder — el shell llama to_dict() y le pasa el dict ya serializado (precedente Phase 25, no el de Phase 20)."
    - "NO loguear payloads ni credenciales en los métodos nuevos; el RedactingFilter y el __repr__ redactor existentes no se tocan."
    - "NO tocar los constructores de Client/AsyncClient (asimetría pre-existente, fuera de scope)."
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/client.py"
      provides: "5 métodos sync gated + 5 shims module-level"
      contains: "def set_calendar_config"
    - path: "packages/market-data-client/src/market_data_client/aio.py"
      provides: "5 métodos async gated + 5 shims async (espejo idéntico)"
      contains: "async def set_calendar_config"
    - path: "packages/market-data-client/tests/test_calendar_write.py"
      provides: "dispatch sync: método/URL/Bearer/body, confirm default False, DELETE sin body ni Content-Type, 422 tipado"
      contains: "set_calendar_config"
    - path: "packages/market-data-client/tests/test_calendar_write_async.py"
      provides: "espejo async de los mismos casos sobre aio._get_default()"
      contains: "set_calendar_config"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "cada método llama su builder de _core, despacha por self._request y parsea con parse_calendar_config_response o parse_calendar_write_response"
      pattern: "build_set_calendar_config_request"
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/client.py"
      via: "cada método de mutación abre con self._ensure_mutation_allowed()"
      pattern: "self\\._ensure_mutation_allowed\\(\\)"
    - from: "packages/market-data-client/src/market_data_client/aio.py"
      to: "packages/market-data-client/src/market_data_client/aio.py"
      via: "espejo async: _ensure_mutation_allowed() NO-awaited como primera sentencia, resp = await self._request(spec)"
      pattern: "await self\\._request\\(spec\\)"
---

<objective>
Cablear la capa pura (Planes 01 y 02) en los cinco métodos públicos de calendar write sobre AMBOS
shells, con el gate de Phase 25 como primera sentencia literal, más los 10 shims module-level y
los dos archivos de test nuevos que prueban el contrato de wire: método, URL, Bearer, body
serializado (incluido `confirm: false` por default), DELETE sin body y `422` tipado.

Purpose: es la superficie que ejercita el gate end-to-end. Depende de los dos planes de Wave 1
(necesita los tres modelos y los seis símbolos de `_core`) y comparte `client.py`/`aio.py`, así que
corre solo en Wave 2.

Output: `set_calendar_config` / `delete_calendar_config` / `preview_calendar_config` /
`add_holidays` / `delete_holiday` como métodos + shims, sync y async, con paridad línea a línea; y
`tests/test_calendar_write.py` + `tests/test_calendar_write_async.py` con la cobertura de dispatch
y serialización.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-calendar-write/26-CONTEXT.md
@.planning/phases/26-calendar-write/26-RESEARCH.md
@.planning/phases/26-calendar-write/26-PATTERNS.md
@.planning/phases/26-calendar-write/26-VALIDATION.md
@.planning/phases/26-calendar-write/26-01-PLAN.md
@.planning/phases/26-calendar-write/26-02-PLAN.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces

> Roster completo de Phase 26 (los 4 planes) — ninguno de estos símbolos existe hoy en el repo
> (verificado por grep, 26-RESEARCH.md § Runtime State Inventory). El pass de source-grounding de
> `plan-review-convergence` debe excluirlos de la verificación de drift.

**Builders nuevos en `_core.py`** (Plan 02): `build_set_calendar_config_request`,
`build_delete_calendar_config_request`, `build_preview_calendar_config_request`,
`build_add_holidays_request`, `build_delete_holiday_request`.

**Parser nuevo en `_core.py`** (Plan 02): `parse_calendar_write_response`.

**Request models nuevos en `models.py`** (Plan 01): `MarketHoursIn`, `HolidayIn`, `HolidaysIn`.

**Métodos nuevos en `client.py`** (este plan): `Client.set_calendar_config`,
`Client.delete_calendar_config`, `Client.preview_calendar_config`, `Client.add_holidays`,
`Client.delete_holiday`.

**Métodos nuevos en `aio.py`** (este plan): `AsyncClient.set_calendar_config`,
`AsyncClient.delete_calendar_config`, `AsyncClient.preview_calendar_config`,
`AsyncClient.add_holidays`, `AsyncClient.delete_holiday`.

**Shims module-level nuevos** (este plan): en `client.py` (sync) y en `aio.py` (async), los cinco
nombres `set_calendar_config`, `delete_calendar_config`, `preview_calendar_config`,
`add_holidays`, `delete_holiday` — 10 funciones en total.

**Entradas nuevas de `__all__`:** `_core.__all__` +6 (Plan 02); `models.__all__` +3 (Plan 01);
`__init__.__all__` +8 (Plan 04).

**Archivos de test nuevos** (este plan): `packages/market-data-client/tests/test_calendar_write.py`
y `packages/market-data-client/tests/test_calendar_write_async.py`, cada uno con un helper local
`_open_gate()` y las constantes `_BASE` / `_TOKEN_URL` / `_CONFTEST_HOST`.

**Archivos de test extendidos:** `tests/test_models.py` (Plan 01), `tests/test_core.py` (Plan 02),
`tests/test_public_surface_market_data.py` (Plan 04).

**Sin cambios (consumo puro):** `_state.py`, `exceptions.py`, `_transport.py`, `_atransport.py`,
`_params.py`, `_logging.py`, `tests/conftest.py`, `pyproject.toml`, `uv.lock`, `__version__`
(sigue en `"0.3.1"` — el bump es Phase 28).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trío de config (set / delete / preview) en ambos shells + 6 shims</name>
  <files>packages/market-data-client/src/market_data_client/client.py, packages/market-data-client/src/market_data_client/aio.py, packages/market-data-client/tests/test_calendar_write.py, packages/market-data-client/tests/test_calendar_write_async.py</files>
  <behavior>
    - Test (sync, gate abierto): set_calendar_config(MarketHoursIn("10:00","17:00","America/Argentina/Buenos_Aires")) emite method PUT, req.url.path == "/api/calendar/config", header Authorization == "Bearer test-token", y json.loads(req.content) == {"open_time":"10:00","close_time":"17:00","timezone":"America/Argentina/Buenos_Aires","pre_open_minutes":10,"enabled":True,"updated_by":"","confirm":False} — este assert ES el criterio 2 del ROADMAP.
    - Test: set_calendar_config con confirm=True emite "confirm": True en el body.
    - Test: el 200 mockeado con el shape real de get-calendar-config.json parsea a un CalendarConfig con open/close/enabled/timezone/warnings poblados.
    - Test: un 200 con body vacío retorna un CalendarConfig de defaults tipados (no levanta) — tolerancia D-07.
    - Test: delete_calendar_config() emite method DELETE a "/api/calendar/config", con req.content == b"" y "content-type" not in req.headers (D-02), y retorna un CalendarConfig.
    - Test: preview_calendar_config(MarketHoursIn(...)) emite method POST a "/api/calendar/config/preview" con el mismo body serializado, y retorna un CalendarConfig.
    - Test: un 422 en cualquiera de los tres levanta MarketDataAPIError.
    - Todos los casos espejados en test_calendar_write_async.py contra await aio._get_default().<método>(...).
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/client.py:535-574 (divisor de sección + los tres métodos de mutación de Phase 25 — el template EXACTO: gate primero, builder, `self._request`, parser), :576-587 (`get_calendar` / `get_calendar_config` — los vecinos de lectura del mismo recurso, para ubicar la sección nueva), :257-283 (`_ensure_mutation_allowed` — consumir SIN modificar), :337-394 (`_request` — donde viven `_ensure_token` y `build_request(..., json=spec.json_body)`; el gate debe precederlo), :597-603 (`_get_default`), :764-786 (los shims module-level de Phase 25 + los de calendar read — template de shim).
    - packages/market-data-client/src/market_data_client/aio.py:548-598 (los tres métodos async de Phase 25 + los dos de calendar read), :215-240 (`_ensure_mutation_allowed` async — idéntico al sync, NO-awaited), :344-406 (`_request` async), :608-618 (`_get_default`), :773-796 (shims async).
    - packages/market-data-client/src/market_data_client/_core.py:733-746 (`parse_calendar_config_response` — reuso verbatim, D-05), :567-580 (`build_calendar_config_request` — el vecino GET).
    - packages/market-data-client/src/market_data_client/models.py:326-350 (`CalendarConfig` — campos reales reconciliados contra el wire de develop: open/close/enabled/editable/env_bypass/pre_open_minutes/source/timezone/updated_by/warnings/updated_at).
    - .planning/verification/schemas/market-data-client/get-calendar-config.json (la forma de wire REAL capturada — usala para armar el `json=` de los mocks de los tres métodos).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:338-437 (secciones «client.py — 5 métodos gated», «aio.py — espejo async» con la lista de las 4 divergencias sync/async permitidas, y «Shims module-level»), :474-565 (templates de test con excerpts verbatim de `test_symbols_write.py`, incluida la nota de que el `base_url` del conftest ya trae el prefijo `/api`).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:577-615 (Patterns 7 y 8 con la tabla de las 5 firmas públicas), :775-795 (el test de body en el wire + el equivalente calendar ya redactado), :663-668 (anti-patterns: carve-out del gate para preview, y `_ensure_token`/`build_*` antes del gate).
    - packages/market-data-client/tests/test_symbols_write.py:18-61 (header, constantes, `_open_gate()` y el happy path con body — copiar tal cual cambiando los imports de modelos), :98-104 (422 → typed).
    - packages/market-data-client/tests/test_symbols_write_async.py:9-51 (los deltas exactos del espejo async: import de `aio`, `_open_gate()` con `aio.configure(...)`, tests `async def` planos sin decorador).
    - packages/market-data-client/tests/conftest.py (fixtures autouse: el `base_url` sembrado es `https://market-data-develop.test/api` y el gate se resetea en teardown — NO agregar fixtures nuevas).
  </read_first>
  <action>
    Escribí PRIMERO los tests que fallan en `tests/test_calendar_write.py` y
    `tests/test_calendar_write_async.py` (creá ambos archivos copiando el header, las constantes
    `_BASE` / `_TOKEN_URL` / `_CONFTEST_HOST` y el helper `_open_gate()` de sus contrapartes
    `test_symbols_write*.py`, cambiando los imports de modelos a `MarketHoursIn` / `HolidayIn` /
    `HolidaysIn`). Después implementá.

    En `client.py`, agregá una sección nueva con divisor `# ---...---` al estilo de la de Phase 25
    (`client.py:535-537`), rotulada como los writes de calendar y gateada. Tres métodos que siguen
    el shape de `create_symbol` (`client.py:539-552`), con `self._ensure_mutation_allowed()` como
    PRIMERA sentencia literal (antes del builder, antes de `self._request`, antes de cualquier
    token fetch — D-14):
    - `set_calendar_config(self, config: MarketHoursIn) -> CalendarConfig` → gate → `spec = _core.build_set_calendar_config_request(self._state, config.to_dict())` → `resp = self._request(spec)` → `return _core.parse_calendar_config_response(resp)`.
    - `delete_calendar_config(self) -> CalendarConfig` → gate → `_core.build_delete_calendar_config_request(self._state)` → dispatch → mismo parser.
    - `preview_calendar_config(self, config: MarketHoursIn) -> CalendarConfig` → gate → `_core.build_preview_calendar_config_request(self._state, config.to_dict())` → dispatch → mismo parser.

    El docstring de `preview_calendar_config` DEBE documentar la excepción read-safe: es
    compute-only y no persiste, pero es un POST y por eso pasa por el gate igual — la excepción se
    documenta, no se implementa (D-14, sin carve-out). El docstring de `set_calendar_config` debe
    explicar que `confirm` es campo de `MarketHoursIn` con default `False` (D-09) y que, según la
    OpenAPI, es una segunda opinión requerida sólo cuando el cambio produce warnings — no un force
    flag; el flujo previsto es `preview_calendar_config(...)` → inspeccionar `warnings` → reemitir
    con `confirm=True`. Los tres reusan `_core.parse_calendar_config_response` SIN modificarlo
    (D-05) y no agregan manejo de status: el `422` fluye por `_core.raise_for_response` existente.

    Agregá los tres shims module-level en `client.py`, junto a los de Phase 25
    (`client.py:764-786`), cada uno delegando a `_get_default()` con la misma firma.

    Espejá TODO idénticamente en `aio.py` (métodos junto a `aio.py:548-586`, shims junto a
    `aio.py:773-796`). Las ÚNICAS divergencias permitidas respecto del sync son cuatro:
    (1) `async def` en la firma; (2) `resp = await self._request(spec)`;
    (3) `self._ensure_mutation_allowed()` sigue NO-awaited y sigue siendo la primera sentencia
    literal; (4) los docstrings del shell async están en castellano y los del sync en inglés —
    mantené esa convención por archivo. Los shims async son `async def` y hacen
    `return await _get_default().<método>(...)`.

    NO re-exportes nada todavía en `__init__.py` — eso es el Plan 04.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q` sale 0 con todos los casos de `<behavior>` (sync + async) en verde.
    - Un test asserta literalmente `json.loads(req.content) == {"open_time": "10:00", "close_time": "17:00", "timezone": "America/Argentina/Buenos_Aires", "pre_open_minutes": 10, "enabled": True, "updated_by": "", "confirm": False}` sobre el `PUT` — ROADMAP SC#2.
    - Un test asserta `req.content == b""` y `"content-type" not in req.headers` sobre el `DELETE /api/calendar/config` — D-02.
    - `python -c "import market_data_client as m; assert all(hasattr(m.Client, n) and hasattr(m.AsyncClient, n) for n in ('set_calendar_config','delete_calendar_config','preview_calendar_config'))"` sale 0.
    - `python -c "import inspect, market_data_client as m; import asyncio; assert asyncio.iscoroutinefunction(m.AsyncClient.set_calendar_config) and not asyncio.iscoroutinefunction(m.Client.set_calendar_config)"` sale 0.
    - En `client.py` y `aio.py`, para cada uno de los tres métodos nuevos la primera sentencia del cuerpo tras el docstring es `self._ensure_mutation_allowed()` — verificado leyendo el cuerpo de cada método (no hay ninguna llamada a `_core.build_`, `self._request` o `_ensure_token` antes).
    - `python -c "import market_data_client.client as c, market_data_client.aio as a; assert all(callable(getattr(c,n)) and callable(getattr(a,n)) for n in ('set_calendar_config','delete_calendar_config','preview_calendar_config'))"` sale 0.
    - `uv run mypy packages/market-data-client/src` sale 0.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` sale 0 (sin regresiones).
  </acceptance_criteria>
  <done>El trío de configuración despacha con el método, path, Bearer y body correctos en ambos shells, con `confirm` viajando en `false` por default, el DELETE sin body ni `Content-Type`, y el gate como primera sentencia también en `preview`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Par de feriados (add_holidays / delete_holiday) en ambos shells + 4 shims</name>
  <files>packages/market-data-client/src/market_data_client/client.py, packages/market-data-client/src/market_data_client/aio.py, packages/market-data-client/tests/test_calendar_write.py, packages/market-data-client/tests/test_calendar_write_async.py</files>
  <behavior>
    - Test (sync, gate abierto): add_holidays(HolidaysIn([HolidayIn("2026-12-25")])) emite method POST a "/api/calendar/holidays", Authorization "Bearer test-token", y json.loads(req.content) == {"days": [{"day": "2026-12-25", "closed": True, "description": ""}]} — sin las claves open_time/close_time (efecto drop_none, ROADMAP SC#3).
    - Test: add_holidays con un HolidayIn que trae open_time/close_time emite ambas claves en ese elemento.
    - Test: add_holidays retorna el dict del body 200 tal cual (passthrough) y {} ante un 200 con body vacío.
    - Test: delete_holiday("2026-12-25") emite method DELETE a "/api/calendar/holidays/2026-12-25", con req.content == b"" y "content-type" not in req.headers (D-02), y retorna el dict del body.
    - Test: el tipo de retorno de ambos es dict — nunca una lista de modelos de día de calendario (D-06).
    - Test: un 422 en cualquiera de los dos levanta MarketDataAPIError.
    - Todos los casos espejados en test_calendar_write_async.py.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/client.py:539-574 (los tres métodos de Phase 25; `update_symbol` :565-574 es el template del método con path-param), la sección nueva de calendar write creada en la Task 1.
    - packages/market-data-client/src/market_data_client/aio.py:552-586 (los espejos async de esos tres).
    - packages/market-data-client/src/market_data_client/_core.py — `build_add_holidays_request`, `build_delete_holiday_request` y `parse_calendar_write_response` recién creados por el Plan 02 (leer sus firmas y docstrings reales antes de llamarlos).
    - packages/market-data-client/src/market_data_client/models.py — `HolidayIn` y `HolidaysIn` recién creados por el Plan 01 (leer el contrato real de `to_dict()`).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:371-386 (tabla de las 5 firmas con builder y parser por método), :389-437 (espejo async + shims).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:596-615 (tabla de firmas públicas y templates de shim), :796-809 (el test de DELETE sin body ya verificado contra httpx 0.28.1 esta sesión — copiar los asserts), :714-723 (Pitfall 3 — por qué el retorno es `dict[str, Any]` y no un modelo de día de calendario).
    - packages/market-data-client/tests/test_symbols_write.py:64-97 (happy path de batch y de PATCH con path-param).
  </read_first>
  <action>
    Escribí PRIMERO los tests que fallan en ambos archivos de test, después implementá.

    En `client.py`, dentro de la sección de calendar write creada en la Task 1, agregá:
    - `add_holidays(self, holidays: HolidaysIn) -> dict[str, Any]` → `self._ensure_mutation_allowed()` como primera sentencia → `spec = _core.build_add_holidays_request(self._state, holidays.to_dict())` → `resp = self._request(spec)` → `return _core.parse_calendar_write_response(resp)`.
    - `delete_holiday(self, day: str) -> dict[str, Any]` → gate → `spec = _core.build_delete_holiday_request(self._state, day)` → dispatch → mismo parser.

    Docstrings: `add_holidays` debe indicar que el endpoint es `POST /calendar/holidays`, que el
    builder lo marca `idempotent=False` (DM-03 / D-04) y que por eso el transporte NO lo reintenta;
    y que el bound 1–500 ya cortó client-side en `HolidaysIn.__post_init__` antes de llegar acá.
    `delete_holiday` debe indicar que `day` es una fecha ISO `YYYY-MM-DD` (la OpenAPI declara el
    path param como `format: date`), que el builder aplica el guard de path-safety D-18 y levanta
    `ValueError` para cualquier `day` capaz de escapar el segmento de path, y que el formato de la
    fecha en sí lo valida el `422` del servidor (D-13). Ambos retornan `dict[str, Any]` vía
    `_core.parse_calendar_write_response` (D-06): NO uses el modelo de día de calendario ni el
    parser de lectura `parse_calendar_response`, que están rotos contra el wire real (D-16) — el
    bug de lectura NO se arregla en esta fase, se registra como finding de Phase 27.

    Agregá los dos shims module-level en `client.py` junto a los de la Task 1.

    Espejá todo idénticamente en `aio.py` (métodos + shims async) con las mismas cuatro
    divergencias permitidas de la Task 1.

    NO agregues acá el test dispatch-level de no-retry (D-15) ni la matriz adversarial de refusals:
    van en el Plan 04.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_calendar_write.py packages/market-data-client/tests/test_calendar_write_async.py -q` sale 0 con todos los casos de `<behavior>` (sync + async) en verde.
    - Un test asserta literalmente `json.loads(req.content) == {"days": [{"day": "2026-12-25", "closed": True, "description": ""}]}` sobre el `POST /api/calendar/holidays` — ROADMAP SC#3 (efecto de `drop_none`).
    - Un test asserta `req.url.path == "/api/calendar/holidays/2026-12-25"`, `req.content == b""` y `"content-type" not in req.headers` sobre el `DELETE`.
    - Un test asserta `isinstance(out, dict)` para el retorno de `add_holidays` y de `delete_holiday`.
    - Un test asserta que un `200` con body vacío devuelve `{}` en ambos métodos (tolerancia D-07 vista desde el shell).
    - `python -c "import market_data_client as m; assert all(hasattr(m.Client, n) and hasattr(m.AsyncClient, n) for n in ('add_holidays','delete_holiday'))"` sale 0.
    - `python -c "import market_data_client.client as c, market_data_client.aio as a; assert all(callable(getattr(c,n)) and callable(getattr(a,n)) for n in ('add_holidays','delete_holiday'))"` sale 0.
    - En `client.py` y `aio.py`, la primera sentencia del cuerpo de ambos métodos tras el docstring es `self._ensure_mutation_allowed()`.
    - `uv run mypy packages/market-data-client/src` sale 0.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` sale 0.
  </acceptance_criteria>
  <done>El par de feriados despacha con el body anidado correcto (sin las horas `None`), el DELETE con path interpolado y sin body, retorno `dict` passthrough tolerante, y el gate primero en ambos shells.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| llamada del consumidor → método de mutación | La intención de mutar el calendario cruza al camino de dispatch gateado. |
| método de mutación → red | Que se emita (o no) un token fetch más un PUT/POST/DELETE lo decide `_ensure_mutation_allowed()` en la entrada del método. |
| respuesta del servidor → tipo de retorno público | Los cinco `200` son `object` sin schema declarado; el parseo tolerante acota el radio de explosión. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | mutación de calendario disparada sin opt-in (end-to-end, los 5 métodos × 2 shells) | mitigate | `self._ensure_mutation_allowed()` es la primera sentencia literal de los cinco métodos en ambos shells (D-14), incluido `preview_calendar_config` — sin carve-out. Verificado acá por lectura de cuerpo; la prueba adversarial de CERO requests vive en el Plan 04 Task 1. |
| T-26-05 | Spoofing / Tampering | mutación despachada contra un host inesperado o de producción | mitigate | La pata de host exacto del gate heredado (`urlsplit(base_url).hostname == expected_host`) corre antes de cualquier dispatch. Testeable en el Plan 04 Task 1 (host mismatch ⇒ 0 requests). |
| T-26-08 | Tampering | un DELETE que emite un body JSON inesperado | mitigate | Verificado en el wire acá: `req.content == b""` y `"content-type" not in req.headers` para los dos DELETE (D-02). |
| T-26-09 | Information Disclosure | fuga de credencial o de `updated_by` (campo de texto libre) en logs o mensajes de error de mutación | accept | El `RedactingFilter` y el `__repr__` redactor existentes quedan sin cambios; los métodos nuevos no loguean payloads ni incluyen `updated_by` en mensajes. No regresionar. |
| T-26-15 | Tampering | la forma real del `200` no encaja en `CalendarConfig` y el consumidor recibe un objeto silenciosamente vacío | accept | La OpenAPI declara los cinco `200` como `object` sin schema, así que D-05/D-06 son apuestas basadas en evidencia; los parsers tolerantes (D-07) son el hedge y Phase 27 (LIVE-MUT-01) captura la forma real. Registrado como assumption A1 de 26-RESEARCH.md. |
| T-26-SC | Tampering | instalaciones npm/pip/cargo | accept | Phase 26 no instala ningún paquete (26-RESEARCH.md § Package Legitimacy Audit: no aplica). |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client/tests -q` en verde.
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` limpios.
- `uv run mypy packages/market-data-client/src` sale 0.
- Grep de disciplina D-14: en `client.py` y `aio.py`, `_ensure_mutation_allowed()` aparece como primera sentencia de los cinco métodos nuevos, antes de cualquier `_core.build_` o `self._request` / `await self._request`.
- Grep de paridad D-17: los cinco nombres de método aparecen exactamente una vez como `def <n>(self` en `client.py` y una vez como `async def <n>(self` en `aio.py`; los shims aparecen una vez como `def <n>(` en `client.py` y una vez como `async def <n>(` en `aio.py`.
- Grep de disciplina de logging: los métodos nuevos no contienen `logging.basicConfig` ni `logging.root.`.
</verification>

<success_criteria>
Los cinco métodos de calendar write despachan correctamente en sync y async (ROADMAP SC#1) con
`confirm` en `false` por default en el wire (SC#2), los request-models serializando vía
`drop_none` y `preview` pasando por el gate con su excepción read-safe documentada (SC#3), los
dos DELETE sin body ni `Content-Type`, el `422` como error tipado, y paridad sync/async línea a
línea; tests mockeados verdes y gates del paquete limpios.
</success_criteria>

<output>
Create `.planning/phases/26-calendar-write/26-03-SUMMARY.md` when done.
</output>
