---
phase: 30-iol-client-tipado
plan: 04
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - main_iol.py
  - verification/snapshots/iol-client-surface.txt
  - packages/iol-client/README.md
autonomous: true
requirements: [TYP-01]

must_haves:
  truths:
    - "`main_iol.py` lee el precio por **acceso por atributo** en sus 2 sitios reales de consumo (criterio 3, D-07)."
    - "Los probes que comparan formas siguen siendo **no-vacuos**: reciben un dict proyectado desde el modelo, no el modelo, y por lo tanto siguen discriminando (D-07/D-08, RESEARCH C-2)."
    - "Los 3 baselines de schema committeados afectados siguen byte-idénticos tras la migración — la prueba end-to-end más fuerte de la fase, verificable sin credenciales."
    - "El snapshot de superficie pública refleja las 4 firmas cambiadas y los 5 símbolos nuevos, regenerado con el script y committeado junto al cambio que lo justifica (D-09)."
    - "El README de iol registra la ruptura dict→modelo, incluido el flip de truthiness, en una sección de changelog para v0.3.0, alimentando el bump que ejecuta F34 (criterio 5, D-11)."
    - "La sección de uso del README describe la superficie que existe de verdad (D-12)."
    - "`__version__` sigue en 0.2.0: el bump es de Phase 34 (D-11)."
  artifacts:
    - path: "main_iol.py"
      provides: "Driver migrado a acceso por atributo + adaptador de frontera hacia el harness"
      contains: "_as_wire"
    - path: "verification/snapshots/iol-client-surface.txt"
      provides: "Golden file de superficie regenerado"
      contains: "Cotizacion : class"
    - path: "packages/iol-client/README.md"
      provides: "Sección Changelog con v0.3.0 + sección de uso corregida"
      contains: "## Changelog"
  key_links:
    - from: "main_iol.py"
      to: "packages/iol-client/src/iol_client/models.py"
      via: "el driver lee atributos del modelo y proyecta a dict con to_dict en la frontera del harness"
      pattern: "\\.to_dict\\(\\)"
    - from: "main_iol.py"
      to: "verification/schema.py"
      via: "schema_of recibe siempre un dict proyectado, nunca una instancia de modelo"
      pattern: "schema_of\\(_as_wire\\("
  prohibitions:
    - statement: "La salida de `to_dict()` llega a `schema_of` y a nada más: nunca a un archivo de findings ni directamente a un snapshot — `schema_of` es libre de datos personales por construcción (claves y nombres de tipo, jamás valores) y saltearlo committearía valores de mercado o de cuenta."
      status: unverified
      flagged: true
    - statement: "Un probe no se declara verde por reportar PASS: tres de los cuatro sitios de frontera reportan PASS precisamente cuando están rotos. La verificación exige no-vacuidad, no ausencia de findings."
      status: unverified
      flagged: true
    - statement: "El modo estricto de decodificación no se activa en esta fase: es entregable de Phase 33 y haría fallar la corrida viva en la primera clave faltante."
      status: unverified
      flagged: true
    - statement: "`__version__` no se bumpea y `uv.lock` no se modifica en Phase 30 (D-11); un tag 0.3.0 antes de la verificación viva de F33 desincronizaría el pipeline de release."
      status: unverified
      flagged: true
    - statement: "El snapshot de superficie no se edita a mano: se regenera con el script y el diff se committea junto al cambio de código que lo justifica."
      status: unverified
      flagged: true
---

<objective>
Cerrar la fase por el lado del consumidor: migrar el driver `main_iol.py` a acceso por atributo, adaptar la frontera hacia el harness de verificación para que sus probes no pierdan poder discriminante, regenerar el golden file de superficie, y escribir en el README la ruptura dict→modelo que alimenta el bump 0.2.0 → 0.3.0 de F34.

Purpose: la migración de la superficie sin este plan deja tres instrumentos de medición **verdes y ciegos** — el probe de paridad sync/async compara dos veces el mismo nombre de clase, y dos probes de mapa de campos saltean su bucle entero por un guard de tipo que ya no se cumple. Además el golden file de superficie queda rojo de forma invisible (ese directorio nunca corrió en CI) y el README documenta una API que no existe, contra la cual un callout de ruptura es ilegible.

Output: `main_iol.py` migrado con adaptador de frontera; snapshot regenerado; README con changelog v0.3.0 y sección de uso real.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-iol-client-tipado/30-CONTEXT.md
@.planning/phases/30-iol-client-tipado/30-RESEARCH.md
@.planning/phases/30-iol-client-tipado/30-PATTERNS.md
@.planning/phases/30-iol-client-tipado/30-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Ver la tabla completa en `30-01-PLAN.md` § "Artifacts this phase produces". Símbolos **creados por este plan**: `main_iol._as_wire`, la sección `## Changelog` y la subsección `### v0.3.0` de `packages/iol-client/README.md`, y las 5 líneas de clase nuevas del snapshot de superficie (`Cotizacion`, `Instrumento`, `Punta`, `SafeModel`, `Titulo`). Todos los modelos y métodos que este plan consume los crearon los planes 30-01 a 30-03 dentro de esta misma fase y **no** son drift.

## Flagged Assumptions (no resueltas — no borrar)

| # | Assumption | Estado | Por qué queda abierta |
|---|---|---|---|
| FA-01 | La fila de cobertura de edges para **TYP-01** llegó `unclassified` → **unresolved**; los edges concretos se derivan de los 5 criterios del ROADMAP § Phase 30 y de D-01..D-12. | unresolved | Sin SPEC file para la fase. |
| FA-04 (RESEARCH A3) | Se asume que nada fuera de este repo consume `iol-client` 0.2.0. **Pregunta abierta al operator**, registrada como blocker en STATE.md. | abierta → F34 | No bloquea: `to_dict()` + el callout del README son la mitigación correcta bajo cualquier resultado. El framing del release de F34 sí depende de la respuesta. El texto del changelog debe redactarse de modo que siga siendo correcto si aparece un consumidor externo. |
| FA-07 (RESEARCH C-1) | CONTEXT D-07 dice que sin `to_dict()` la próxima corrida viva **escribiría** un nombre de clase en los baselines committeados. Es **imposible**: el escritor sólo escribe cuando el archivo no existe y los 4 existen; ante un desajuste emite un finding y no sobrescribe. El riesgo real es **peor**: verde vacuo. | corregida | No escribir un paso de verificación que afirme "baseline no corrompido": nunca puede fallar. Verificar no-vacuidad. |
| FA-08 (RESEARCH C-3) | El envelope de instrumentos-por-tipo **no** pasa por el wrapper del cliente (se captura desde una petición cruda), así que es inmune a la migración. Es un dict, no un modelo. | resuelta | No rutearlo por el adaptador de frontera esperando un modelo; el adaptador debe pasarlo tal cual. |
| FA-09 (RESEARCH Pitfall 5) | Tras esta fase, para los endpoints modelados de iol la señal autoritativa de drift es el **censo de divergencias**, no el diff del snapshot de schema: `to_dict()` proyecta el drift *afuera* por construcción. | carry-forward → F33 | Debe quedar registrada en el SUMMARY: una corrida de F33 que lea "sin drift de schema" y se detenga ahí estaría leyendo un instrumento que esta fase desafiló a propósito. |

## Reversibility Ratings (REVERSIBILITY_GATES)

| Decisión de este plan | Rating | Gate |
|---|---|---|
| Eliminar el guard de tipo del precio, hoy código muerto | two-way (una línea) | ninguno |
| Adaptador de frontera hacia el harness | two-way — helper local del driver | ninguno |
| Regenerar el golden file de superficie | two-way — el script lo re-deriva | ninguno |
| Escribir el changelog v0.3.0 **sin** bumpear la versión | two-way; es exactamente lo que D-11 pide para no desincronizar el pipeline de tags | ninguno |
| Bump 0.2.0 → 0.3.0 + tag + release | **one-way** | **fuera de scope — pertenece a Phase 34 (PUB-TYP-01)**. Este plan no lo ejecuta, así que no hay gate que insertar acá. |

Ninguna decisión one-way se toma en este plan → **no se inserta ningún `checkpoint:decision`**.

<tasks>

<task type="auto">
  <name>Task 1: `main_iol.py` — acceso por atributo + adaptador de frontera hacia el harness</name>

  <files>main_iol.py</files>

  <read_first>
    - `main_iol.py` líneas 310-320 y 390-398 — los **2 sitios reales de consumo** (D-07): ambos leen el precio de una cotización por clave; el primero lo envuelve además en un guard de tipo.
    - `main_iol.py` líneas 895-935 — el probe de paridad sync/async: compara la forma reducida del payload sync contra la del async (:918-919).
    - `main_iol.py` líneas 1055-1110 — los dos probes de mapa de campos: reducen el payload y luego **guardan con un chequeo de tipo dict** antes de recorrer el mapa de campos asumidos (:1066, :1102).
    - `main_iol.py` líneas 1151-1200 — el escritor/verificador de snapshots de schema (:1164 reduce el payload; :1174-1180 sólo escribe si el archivo no existe; ante desajuste emite finding y **no** sobrescribe).
    - `main_iol.py` líneas 976-1062 y 1200-1260 — de dónde sale el envelope de instrumentos-por-tipo: de una petición **cruda** (:995), no del wrapper del cliente. Es inmune a la migración.
    - `verification/schema.py` líneas 1-45 — la función de reducción: colapsa a claves+tipos y cae a nombre-de-tipo para cualquier cosa que no sea dict ni lista. Una instancia de dataclass se reduce al **string** con el nombre de la clase; de ahí los tres modos de falla.
    - `main_higyrus.py` líneas 435-470 — el análogo con la misma disciplina de no-sobrescritura.
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § The `schema_of` Blast Radius (la tabla de los 3 modos de falla), § Code Examples (la forma exacta del adaptador), § Probe annotations to update.
    - `packages/iol-client/src/iol_client/models.py` — los 4 modelos y `to_dict`.
  </read_first>

  <action>
    Tres bloques de edición en `main_iol.py`. El archivo **no** lo typechequea mypy (su configuración cubre `packages/*/src`) pero **sí** lo revisa ruff, así que las anotaciones de este archivo son documentación y un error en ellas es invisible al CI: revisarlas a mano.

    **Bloque A — los 2 sitios reales de consumo (D-07, criterio 3).**
    En `main_iol.py:316` y `main_iol.py:395` la cotización se lee por clave. Cambiar ambas a **acceso por atributo** sobre el modelo. En el sitio de :316-317 el guard de tipo que envuelve la comparación queda como **código muerto demostrable**: el campo está declarado decimal y el walker garantiza que un decimal llega al atributo (sustituyendo typed-zero si el wire diverge). Eliminarlo y dejar la comparación de rango desnuda: es la demostración más legible de que la fase entregó su garantía, y dejarlo puesto implicaría que el tipo no se cree. **Conservar** el guard de ausencia sobre la cotización misma: el probe sigue recibiendo un valor opcional.

    **Bloque B — la frontera hacia el harness (D-07/D-08).**
    La función de reducción de formas del harness colapsa cualquier cosa que no sea dict ni lista a su nombre de tipo, así que una instancia de modelo se reduce a un string. Eso produce **tres** modos de falla distintos y **dos son silenciosos**:
    - Probe de paridad (:918-919): compararía el nombre de clase contra sí mismo → siempre igual → el probe reporta PASS habiendo perdido **todo** su poder discriminante. El peor de los tres.
    - Probes de mapa de campos (:1066, :1102): el guard de tipo dict pasa a ser falso, el bucle entero se saltea y el probe reporta PASS con su mensaje de "sin drift". Silencioso.
    - Escritor/verificador de snapshots (:1164): compara un string contra el dict committeado → 3 findings de forma espurios. Ruidoso, y **no** sobrescribe el baseline.

    Corrección: introducir en `main_iol.py` un helper local `_as_wire(value)` que, si recibe una lista, se aplica recursivamente a sus elementos, y si el valor expone `to_dict` lo invoca; en cualquier otro caso lo devuelve tal cual. Normalizar en la frontera, no en cada sitio de llamada:
    - :918-919 — pasar ambos payloads por `_as_wire` antes de reducirlos. Los payloads llegan opacos, así que el adaptador es el lugar correcto.
    - :1066 — reducir la proyección a dict de la cotización.
    - :1102 — reducir la proyección a dict de la fila 0 de la serie histórica.
    - :1164 vía sus llamadores — aplicar la proyección a **3 de los 4** destinos de snapshot: cotización, serie histórica y listado de instrumentos.
    - El envelope de instrumentos-por-tipo (:995, :1032, y el 4º destino de snapshot) **no** se toca: viene de una petición cruda y ya es un dict. El adaptador lo pasa tal cual, que es lo correcto; no forzarlo por una ruta que asuma un modelo.

    La salida de `to_dict()` alimenta la función de reducción y **nada más**: esa función es libre de datos personales por construcción (claves y nombres de tipo, jamás valores). No escribir la proyección a un archivo de findings ni directamente a un snapshot.

    **Bloque C — anotaciones de probe.**
    Actualizar las anotaciones de tipo de los probes y de sus retornos en :256, :340, :402, :482, :682, :804, :889-896, :953-956, :1203-1206, :1536-1542: el tipo opcional de cotización pasa a ser el modelo, el de serie histórica pasa a ser la lista del modelo, y el del listado de instrumentos pasa de sin-tipar a lista del modelo. La anotación del envelope de instrumentos-por-tipo **no cambia**: sigue siendo un dict opcional.

    **No** activar el modo estricto de decodificación en ningún punto del driver: no está activado hoy en ningún `main_*.py`, las corridas estrictas son entregable de Phase 33, y activarlo acá haría fallar la corrida viva en la primera clave faltante.
  </action>

  <verify>
    <automated>uv run ruff check main_iol.py && uv run ruff format --check main_iol.py && uv run python -c "
import ast, pathlib
src = pathlib.Path('main_iol.py').read_text()
ast.parse(src)
assert '_as_wire' in src
" && uv run pytest verification/test_main_iol_uses_single_client_instance.py verification/test_main_drivers_bare_except.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run ruff check main_iol.py && uv run ruff format --check main_iol.py` sale 0.
    - `python -c "import ast,pathlib; ast.parse(pathlib.Path('main_iol.py').read_text())"` sale 0.
    - Ningún sitio del driver lee la cotización por clave: `grep -c 'quote\.get(' main_iol.py` imprime `0`.
    - `grep -c 'ultimoPrecio' main_iol.py` imprime ≥ `2` y todas las apariciones fuera de comentarios son accesos por atributo o claves de mapas de campos asumidos (revisión visual con `grep -n`).
    - `grep -c 'def _as_wire' main_iol.py` imprime `1`.
    - `grep -c 'schema_of(_as_wire(' main_iol.py` imprime ≥ `2` (el probe de paridad).
    - `grep -c '\.to_dict()' main_iol.py` imprime ≥ `2` (los dos probes de mapa de campos).
    - **No-vacuidad, la verificación que realmente importa** (RESEARCH C-2 — tres de los cuatro sitios reportan PASS precisamente cuando están rotos): `uv run python -c "
from verification.schema import schema_of
from iol_client.models import Cotizacion, Instrumento
q = Cotizacion.from_api({})
assert isinstance(schema_of(q.to_dict()), dict), 'probe vacuo: la reduccion no devolvio un dict'
assert not isinstance(schema_of(q), dict), 'el modelo crudo NO debe reducirse a dict — si lo hace, la premisa del plan cambio'
assert isinstance(schema_of([Instrumento.from_api({}).to_dict()]), list)
"` sale 0.
    - **Round-trip contra los baselines committeados** (la prueba end-to-end más fuerte de la fase, sin credenciales): un script que, para cada uno de `get-quote.json`, `get-historical-quotes.json` y `get-instruments.json`, construye el/los modelo(s) desde un payload derivado del `schema` committeado, aplica `_as_wire` y reduce, y compara contra el valor de la clave `schema` del archivo. Los 3 deben coincidir **exactamente**. Registrar la salida en el SUMMARY.
    - `git diff --exit-code .planning/verification/schemas/iol-client/` sale 0: los 4 baselines siguen byte-idénticos.
    - `uv run pytest verification/test_main_iol_uses_single_client_instance.py verification/test_main_drivers_bare_except.py -q` sale 0.
  </acceptance_criteria>

  <verify-human>
    Las anotaciones de tipo de los probes de `main_iol.py` no las verifica ningún gate (mypy no cubre este archivo). Revisión visual: `git diff main_iol.py` y confirmar que cada anotación de probe nombra el modelo correcto y que la del envelope de instrumentos-por-tipo quedó intacta.
  </verify-human>

  <done>
    Los 2 sitios reales de consumo leen por atributo y el guard de tipo muerto fue eliminado; existe `_as_wire` y los 4 sitios de frontera aplicables lo usan; el envelope de instrumentos-por-tipo quedó intacto; los 3 baselines afectados round-trippean byte-idénticos; las anotaciones de probe fueron revisadas a mano; ruff y format verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: regenerar el golden file de superficie pública</name>

  <files>verification/snapshots/iol-client-surface.txt</files>

  <read_first>
    - `verification/regen_snapshots.py` líneas 1-40 — uso operativo del script y la disciplina: correrlo **después** de un cambio intencional de la superficie y committear el diff en el **mismo commit** que el cambio de código que lo justifica; el header de 8 líneas es invariante y el escritor lo preserva bit a bit.
    - `verification/snapshots/iol-client-surface.txt` — estado actual: 8 líneas de header y luego la superficie; las 4 firmas objetivo están pinneadas con sus retornos viejos.
    - `verification/snapshots/higyrus-client-surface.txt` líneas 9-16 — cómo se renderiza un modelo exportado: como clase, con su firma de constructor completa. Es lo que iol va a ganar.
    - `verification/test_public_surface.py` líneas 40-55 — el test compañero; **no corre en CI** (el workflow pasa una ruta de paquete explícita que anula la configuración de rutas de test), así que este golden file es un gate sólo local. De ahí que haya que regenerarlo deliberadamente.
  </read_first>

  <action>
    Correr `uv run python verification/regen_snapshots.py` y revisar el diff resultante sobre `verification/snapshots/iol-client-surface.txt`. **No editar el archivo a mano.**

    El diff esperado tiene dos partes y ambas deben estar presentes:
    - **4 líneas de función modificadas**: las entradas de las cuatro funciones públicas migradas, ahora con el retorno del modelo o de la lista de modelos en vez del retorno viejo.
    - **5 líneas de clase agregadas**: `Cotizacion`, `Instrumento`, `Punta`, `SafeModel` y `Titulo`, renderizadas con su firma de constructor completa.

    Señal de alarma explícita (Pitfall 3): si el diff muestra líneas modificadas pero **ninguna** agregada, los modelos no llegaron al `__all__` — el snapshot no los puede enumerar, y el gate AST de Phase 32 tampoco tendría qué chequear. En ese caso volver a los planes 30-01/30-02/30-03 y corregir el export antes de continuar.

    El otro golden file de este directorio y los snapshots de los demás paquetes **no** deben aparecer en el diff: si aparecen, el script se corrió sobre un árbol con cambios ajenos.

    Correr después el subconjunto de `verification/` relativo a iol. Esa carpeta nunca corre en CI, así que es la única oportunidad de verla verde.
  </action>

  <verify>
    <automated>uv run python verification/regen_snapshots.py && uv run pytest verification/test_public_surface.py -q && git diff --exit-code verification/snapshots/</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python verification/regen_snapshots.py` sale 0 y, re-corrido, deja el árbol limpio: `git diff --exit-code verification/snapshots/` sale 0 (idempotente).
    - `uv run pytest verification/test_public_surface.py -q` sale 0.
    - `grep -c 'Cotizacion : class' verification/snapshots/iol-client-surface.txt` imprime `1`; ídem para `Instrumento : class`, `Punta : class`, `SafeModel : class` y `Titulo : class`.
    - `grep -c "Cotizacion'" verification/snapshots/iol-client-surface.txt` imprime ≥ `2` (las firmas de `get_quote` y de serie histórica ahora nombran el modelo).
    - `head -8 verification/snapshots/iol-client-surface.txt | grep -c '^#'` imprime `8` (header invariante preservado).
    - `git status --porcelain verification/snapshots/ | wc -l` imprime `1`: sólo el snapshot de iol cambió.
    - `uv run pytest verification/ -q -k iol` sale 0.
  </acceptance_criteria>

  <done>
    El golden file de superficie de iol fue regenerado con el script (nunca a mano), muestra las 4 firmas actualizadas **y** las 5 clases nuevas, el header quedó intacto, el script es idempotente y ningún otro snapshot cambió.
  </done>
</task>

<task type="auto">
  <name>Task 3: README — changelog v0.3.0 con el callout de ruptura + corrección de la sección de uso</name>

  <files>packages/iol-client/README.md</files>

  <read_first>
    - `packages/iol-client/README.md` — 44 líneas; la sección de uso ocupa las líneas 13-31 y documenta una clase cliente con constructor de token y un método de cartera que **no existen** en `src/` (cero apariciones). No hay sección de changelog.
    - `packages/market-data-client/README.md` líneas 123-193 — el precedente exacto de estructura: encabezado de changelog, secciones por versión de más nueva a más vieja, cada una abriendo con un veredicto de una línea en negrita más un paréntesis con la razón semver, luego viñetas que nombran los IDs de decisión. En :154-163 está el precedente de una ruptura con su propio sub-bloque en negrita explicando **por qué** viaja en ese bump: la forma exacta que necesita el caso de iol.
    - `packages/iol-client/src/iol_client/__init__.py` líneas 1-21 — el docstring de módulo es el enunciado canónico de la superficie real: shim de nivel superior, cliente basado en clase con context manager, y superficie async con su cierre explícito.
    - `packages/iol-client/src/iol_client/models.py` — los 4 modelos, sus campos y `to_dict`.
    - `.planning/ROADMAP.md` § Phase 30 criterio 5 y § Phase 34 — el bump lo ejecuta F34, no esta fase.
  </read_first>

  <action>
    Dos ediciones en `packages/iol-client/README.md`, **en el mismo commit** (D-12: el callout de ruptura es ilegible contra una descripción ficticia).

    **Edición 1 — corregir la sección de uso (D-12).** Reemplazar las líneas 13-31 por la superficie que existe de verdad, tomando como fuente canónica el docstring de módulo de `__init__.py:1-21`: los tres modos de consumo (shim de nivel superior con login explícito; cliente basado en clase usado como context manager con credenciales por constructor; superficie async con su cierre explícito). Los ejemplos deben mostrar **acceso por atributo** sobre el resultado, que es la superficie nueva. Alcance aceptado por D-12; no expandir más allá de esta sección.

    **Edición 2 — agregar `## Changelog` con una única subsección `### v0.3.0`.** Estructura según el precedente de market-data: veredicto de una línea en negrita, paréntesis con la razón semver, y viñetas. El contenido debe cubrir:
    - **La ruptura dict→modelo**, en su propio sub-bloque en negrita: las cuatro funciones públicas y sus dos superficies (método de clase y shim de módulo, en sync y async) devuelven modelos tipados en vez de diccionarios. Enumerar el mapeo función → tipo devuelto para las cuatro.
    - **El flip de truthiness**, nombrado explícitamente: un diccionario vacío es falso, una instancia de dataclass es verdadera siempre. Todo consumidor que ramifique sobre la verdad del resultado cambia de comportamiento aunque el código compile. Es la parte de la ruptura que un typechecker **no** atrapa y por eso es la que más necesita el callout.
    - **El escape hatch**, en el mismo release que la ruptura (criterio 5): todos los modelos exponen `to_dict()`, que devuelve un diccionario plano equivalente al payload del wire. Documentar la pérdida conocida y aceptada: un valor nulo del wire decodificado a un campo opcional y cualquier clave no declarada por el modelo no sobreviven la ida y vuelta (D-08). Es un blind spot documentado, contrastable en la verificación viva de Phase 33.
    - **Los tipos nuevos exportados**: los cinco símbolos que aparecen en el `__all__` del paquete.
    - **El cambio de forma en el listado de instrumentos**: la respuesta real es una lista top-level, y una respuesta con otra forma ahora levanta en vez de degradar a lista vacía (D-06).
    - **Lo que NO cambia**: los campos de respuesta siguen siendo texto libre, sin conjuntos cerrados, en este release; los parámetros de mercado y plazo siguen siendo texto (criterio 4).

    Redactar el texto de modo que siga siendo correcto tanto si existe un consumidor externo del 0.2.0 como si no (FA-04 sigue abierta).

    **`__version__` NO se toca**: queda en `"0.2.0"` (D-11). La sección de changelog se escribe ahora y el bump, el tag y el release los ejecuta Phase 34 — bumpear acá desincronizaría el pipeline de tags y publicaría un 0.3.0 antes de la verificación viva de F33.
  </action>

  <verify>
    <automated>uv run python -c "
import pathlib, re
r = pathlib.Path('packages/iol-client/README.md').read_text()
assert '## Changelog' in r
assert '### v0.3.0' in r
assert 'to_dict' in r
for m in ('Cotizacion','Instrumento','Punta','Titulo'):
    assert m in r, m
" && uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'" && git diff --exit-code packages/iol-client/src/</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '## Changelog' packages/iol-client/README.md` imprime `1`.
    - `grep -c '### v0.3.0' packages/iol-client/README.md` imprime `1`.
    - `grep -c 'to_dict' packages/iol-client/README.md` imprime ≥ `1`.
    - Los 4 modelos figuran en el README: `grep -c 'Cotizacion' packages/iol-client/README.md` ≥ `1`, ídem `Instrumento`, `Punta`, `Titulo`.
    - La API ficticia desapareció: `grep -cE 'IOLClient\(|get_portfolio' packages/iol-client/README.md` imprime `0`.
    - Los ejemplos del README importan símbolos que existen: `uv run python -c "
import re, pathlib, importlib
r = pathlib.Path('packages/iol-client/README.md').read_text()
names = set(re.findall(r'from iol_client import ([A-Za-z_, ]+)', r))
mod = importlib.import_module('iol_client')
for group in names:
    for n in (x.strip() for x in group.split(',')):
        assert hasattr(mod, n), n
"` sale 0.
    - `uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'"` sale 0 (D-11: sin bump en esta fase).
    - `git diff --exit-code packages/iol-client/src/ uv.lock` sale 0 (tarea sólo de documentación).
    - El README menciona el flip de truthiness de forma explícita (revisión humana, abajo).
  </acceptance_criteria>

  <verify-human>
    Leer `packages/iol-client/README.md` completo y confirmar: (1) la sección de uso describe únicamente superficie que existe, y los ejemplos corren si se copian; (2) el callout de ruptura nombra el flip de truthiness en términos que un consumidor entienda sin leer el código; (3) el escape hatch y su pérdida conocida están documentados; (4) el texto no promete un bump que esta fase no ejecuta.
  </verify-human>

  <done>
    El README describe la superficie real, tiene una sección de changelog con `### v0.3.0` que registra la ruptura dict→modelo, el flip de truthiness, el escape hatch con su pérdida conocida, los tipos nuevos y el cambio de forma del listado; `__version__` sigue en 0.2.0 y ni `src/` ni el lockfile fueron tocados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modelo decodificado → función de reducción de formas del harness | El único camino permitido para proyectar un modelo a dict; la función es libre de datos personales por construcción. |
| Driver → artefactos versionados (`verification/`, `.planning/verification/schemas/`) | Lo que cruce esta frontera queda committeado; nunca debe llevar valores de mercado ni de cuenta. |
| README → consumidores externos | Es la única notificación de la ruptura para quien no lee el repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Information disclosure | frontera `to_dict()` → artefactos | mitigate | La proyección a dict alimenta **exclusivamente** la función de reducción de formas, que emite claves y nombres de tipo y jamás valores. Ningún sitio la escribe a un archivo de findings ni directamente a un snapshot. Verificado porque `git diff --exit-code .planning/verification/schemas/iol-client/` debe salir 0: los baselines quedan byte-idénticos. |
| T-30-11 | Repudiation | probes vacuos | mitigate | **El riesgo central de este plan.** Tres de los cuatro sitios de frontera reportan PASS precisamente cuando pierden su poder discriminante: el probe de paridad compara dos veces el mismo nombre de clase; los dos probes de mapa de campos saltean su bucle entero por un guard de tipo que deja de cumplirse. La verificación exige **no-vacuidad** (la reducción devuelve un dict, el modelo crudo no) más el round-trip byte-idéntico contra los 3 baselines — nunca la mera ausencia de findings. |
| T-30-12 | Tampering | golden file de superficie | mitigate | El directorio de verificación **nunca corre en CI**, así que un snapshot desactualizado queda rojo de forma invisible. Se regenera con el script (jamás a mano), el diff se committea junto al cambio que lo justifica, la idempotencia se verifica re-corriendo el script, y el subconjunto de tests relativo a iol se corre localmente. |
| T-30-13 | Repudiation | notificación de la ruptura | mitigate | Un callout de ruptura contra una descripción de API ficticia es ilegible: se corrige la sección de uso en el mismo commit (D-12). El flip de truthiness se nombra explícitamente porque es la parte de la ruptura que el typechecker **no** atrapa. |
| T-30-03 | Tampering | integridad del walker | mitigate | `_decode.py` sin cambios; el gate de intactness corre en el job de lint del CI. |
| T-30-SC | Tampering | instalaciones vía gestor de paquetes | mitigate | Cero paquetes externos y cero bump de versión en esta fase. `git diff --exit-code uv.lock` es criterio de aceptación en la tarea 3. Sin `[ASSUMED]`/`[SUS]`/`[SLOP]` que gatear. |
</threat_model>

<verification>
1. `uv run pytest packages/iol-client -q` → verde, ≥ 205 tests.
2. `uv run mypy packages/iol-client/src packages/iol-client/tests` → 0.
3. `uv run ruff check . && uv run ruff format --check .` → 0.
4. `uv run pytest verification/ -q -k iol` → 0 (**no corre en CI**; obligatorio localmente, D-09).
5. `uv run python tools/check_decode_intactness.py` → 0.
6. `git diff --exit-code .planning/verification/schemas/iol-client/` → 0 (los 4 baselines byte-idénticos).
7. `git diff --exit-code uv.lock packages/iol-client/src/iol_client/_decode.py` → 0.
8. `uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'"` → 0.
9. `uv run pytest -q` (suite completa del monorepo) → verde.
</verification>

<success_criteria>
- El driver lee por atributo en sus 2 sitios reales y el guard de tipo muerto desapareció.
- Los probes de frontera son no-vacuos, demostrado por aserción, no por ausencia de findings.
- Los 3 baselines de schema afectados siguen byte-idénticos — la prueba end-to-end más fuerte de la fase, obtenible sin credenciales.
- El snapshot de superficie muestra 4 firmas actualizadas **y** 5 clases nuevas.
- El README describe la superficie real y registra la ruptura con su flip de truthiness; `__version__` sigue en 0.2.0.
</success_criteria>

<output>
Crear `.planning/phases/30-iol-client-tipado/30-04-SUMMARY.md`. Debe registrar: (a) la salida de la verificación de no-vacuidad y del round-trip contra los 3 baselines; (b) el diff del snapshot resumido (4 modificadas / 5 agregadas); (c) el **carry-forward FA-09 a Phase 33**: para los endpoints modelados de iol la señal autoritativa de drift pasa a ser el censo de divergencias, no el diff del snapshot de schema — una corrida de F33 que lea "sin drift de schema" y se detenga ahí estaría leyendo un instrumento que esta fase desafiló a propósito; (d) el estado de FA-04 (consumidores externos) para el framing del release de Phase 34.
</output>
