---
phase: 20-scaffold-auth0-client-credentials-fundaciones-de-transporte
plan: 03
type: tdd
wave: 2
depends_on: ["20-01"]
files_modified:
  - packages/market-data-client/src/market_data_client/_logging.py
  - packages/market-data-client/tests/test_logging.py
autonomous: true
requirements: [CORE-MD-01]
user_setup: []

must_haves:
  truths:
    - "client_secret is redacted in BOTH form-encoded (client_secret=...) and JSON (\"client_secret\":\"...\") shapes (CORE-MD-01 gate, D-11)"
    - "Bearer tokens and JSON access_token values are redacted (CORE-MD-01)"
    - "attach() is idempotent and touches only the market_data_client package logger, never the root logger (CI LOG-01 gate)"
    - "Zero credential substrings survive in a log record after RedactingFilter.filter()"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/_logging.py"
      provides: "RedactingFilter + attach(); patterns Bearer / access_token / client_secret (urlenc + JSON)"
      contains: "def attach"
      min_lines: 60
    - path: "packages/market-data-client/tests/test_logging.py"
      provides: "RED-first redaction + attach-idempotency tests"
      contains: "def test_"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/_logging.py"
      to: "logging.getLogger(\"market_data_client\")"
      via: "attach() adds NullHandler + RedactingFilter to the package logger only"
      pattern: "getLogger\\(\"market_data_client\"\\)"
---

<objective>
Build the credential-redaction filter (`_logging.py`) test-first, mirroring iol's `RedactingFilter` + `attach()` structure but swapping the pattern set per D-11: KEEP `Bearer` and JSON `access_token`; DROP iol's password / refresh_token / X-Auth-Token patterns; ADD `client_secret` in both form-encoded and JSON shapes. Zero credential leakage in logs is a hard CORE-MD-01 gate (SC4).

Purpose: The Auth0 `client_credentials` grant sends `client_secret` in the form body and the response carries `access_token` — both must be scrubbed before any DEBUG-level record reaches a downstream handler. `attach()` must touch ONLY `getLogger("market_data_client")` — the CI LOG-01 grep gate fails the build if package src calls the root-logger config helpers.
Output: `_logging.py` + `test_logging.py`, green standalone (pure — imports only stdlib + the module under test).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-CONTEXT.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-RESEARCH.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-PATTERNS.md
</context>

<artifacts_produced>
This plan creates:
- `_logging.py` with `RedactingFilter`, `attach()`, constant `_LOGGER_NAME`-equivalent target `market_data_client`, and patterns `_BEARER_RE`, `_ACCESS_TOKEN_JSON_RE`, `_CLIENT_SECRET_URLENC_RE`, `_CLIENT_SECRET_JSON_RE`
- `tests/test_logging.py`
</artifacts_produced>

<feature>
  <name>_logging.py RedactingFilter + attach() with client_secret patterns (D-11)</name>
  <files>packages/market-data-client/src/market_data_client/_logging.py, packages/market-data-client/tests/test_logging.py</files>
  <behavior>
    RED — author `tests/test_logging.py` (adapt iol's `_make_record` helper with logger name `market_data_client`; import `RedactingFilter`/`attach` directly from `market_data_client._logging`) covering:
    - test_redact_bearer: a record message containing `Authorization: Bearer eyJabc.def-ghi` → after `RedactingFilter().filter(rec)` the raw token is gone and `Bearer ***` is present.
    - test_redact_access_token_json: message `body={"access_token":"eyJsecret"}` → `eyJsecret` absent; `"access_token":"***"` present.
    - test_redact_client_secret_urlenc: message `grant_type=client_credentials&client_secret=sup3r-s3cret&audience=aud` → `sup3r-s3cret` absent; `client_secret=***` present; the surrounding `grant_type`/`audience` fields survive.
    - test_redact_client_secret_json: message `body={"client_secret":"sup3r-s3cret"}` → `sup3r-s3cret` absent; `"client_secret":"***"` present.
    - test_redact_scans_args_and_dict: a credential placed in `record.args` (dict and tuple forms) and in `record.__dict__` extra field is also scrubbed.
    - test_attach_idempotent: calling `attach()` twice adds the `RedactingFilter` and a `NullHandler` to `getLogger("market_data_client")` exactly once each (no duplicates); the root logger has neither added by `attach()`.
    Run; confirm RED (module absent).
    GREEN — implement `_logging.py` to pass.
  </behavior>
  <implementation>
    Mirror iol `_logging.py` structure verbatim: `from __future__ import annotations`, `import logging`, `import re`, `__all__ = ["RedactingFilter", "attach"]`. `RedactingFilter.filter()` rewrites `record.msg`, `record.args` (dict and tuple), and string values in `record.__dict__` through `_redact()` before emission, returning True. `attach()` is idempotent: add a `NullHandler` and a `RedactingFilter` to `logging.getLogger("market_data_client")` ONLY (guard against duplicate handler/filter on re-run); it must NEVER configure or touch the root logger (CI LOG-01 gate greps package src for the root-logger call/attribute forms — do not emit those forms in code).
    Pattern set (D-11): `_BEARER_RE = re.compile(r"Bearer\s+[A-Za-z0-9._\-]+")`; `_ACCESS_TOKEN_JSON_RE = re.compile(r'("access_token"\s*:\s*")[^"]+(")')`; ADD `_CLIENT_SECRET_URLENC_RE = re.compile(r"(client_secret=)[^&\s]+")` and `_CLIENT_SECRET_JSON_RE = re.compile(r'("client_secret"\s*:\s*")[^"]+(")')`. REMOVE iol's `_X_AUTH_TOKEN_RE`, `_PASSWORD_*`, `_REFRESH_TOKEN_*`. `_REDACTION_MARKERS = ("Bearer ", "client_secret=", '"client_secret"', '"access_token"')` (fast-path substring gate). `_redact(text)` runs exactly four passes in order: Bearer → `Bearer ***`; client_secret urlenc → `\1***`; client_secret JSON → `\1***\2`; access_token JSON → `\1***\2`. Idempotent on already-redacted text.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package logger → downstream handlers | Log records may carry credential substrings; must be scrubbed before emission (Sentry/console/etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Information Disclosure | `client_secret` in form body or JSON reaching logs | mitigate | `_CLIENT_SECRET_URLENC_RE` + `_CLIENT_SECRET_JSON_RE` redact both shapes — CORE-MD-01 zero-leak gate, tested; HIGH severity, blocking |
| T-20-06 | Information Disclosure | Bearer / access_token in logs | mitigate | `_BEARER_RE` + `_ACCESS_TOKEN_JSON_RE` scrub token values |
| T-20-07 | Tampering | attach() touching the root logger | mitigate | `attach()` binds NullHandler + filter to the `market_data_client` logger only; CI LOG-01 grep gate enforces no root-logger config in src |
</threat_model>

<verification>
- RED captured: initial `test_logging.py` run fails before `_logging.py` exists (recorded in SUMMARY).
- GREEN: `uv run --package market-data-client pytest packages/market-data-client/tests/test_logging.py -x -q` exits 0.
- `uv run ruff check` + `uv run mypy` (strict) on `_logging.py` exit 0.
- CI LOG-01 gate honored: `grep -rnE --include='*.py' 'logging\.basicConfig\s*\(|logging\.root\.\w' packages/market-data-client/src/` returns no matches (exit non-zero from grep = clean).
</verification>

<success_criteria>
`RedactingFilter` scrubs Bearer / access_token / client_secret (urlenc + JSON) with zero surviving credential substrings; `attach()` is idempotent and package-logger-scoped. Satisfies SC4 and the CORE-MD-01 zero-leak gate.
</success_criteria>

<output>
Create `.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-03-SUMMARY.md` when done.
</output>
