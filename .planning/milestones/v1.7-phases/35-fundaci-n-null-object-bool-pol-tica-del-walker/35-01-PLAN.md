---
phase: 35-fundaci-n-null-object-bool-pol-tica-del-walker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/higyrus-client/src/higyrus_client/models.py
  - packages/higyrus-client/tests/test_null_object.py
  - packages/higyrus-client/tests/test_decode.py
autonomous: true
requirements: [NOBJ-01, NOBJ-02]

must_haves:
  truths:
    - "TRACER: higyrus-client is proven end-to-end (base gains truthiness, its 15 shipped models are falsy when empty and truthy when perturbed, the 4 v1.6 gates stay green) before any other package is touched."
    - "bool(X.from_api(None)) is False for every one of higyrus's 15 shipped SafeModel subclasses, enumerated by introspection of models.py, never from a fixture (D-15, ROADMAP criterio 1)."
    - "bool(perturbed_instance) is True for every one of those 15 classes, where the perturbation helper handles a None-valued field first (RESEARCH Pitfall 3)."
    - "SafeModel.empty() exists on higyrus, is invocable, and emits zero divergence records (T-29-33, D-07)."
    - "get_type_hints() over a frozen+slots dataclass carrying a @property alias next to wire fields returns exactly the declared fields; the alias name is absent (ROADMAP criterio 5, D-16)."
    - "Adding a @property alias to an otherwise identical dataclass produces a byte-identical divergence record list — the alias cannot fabricate a missing nor change the divergence count (ROADMAP criterio 5)."
    - "A wrong-typed value on a non-optional list field emits the six-key record with kind 'type' and stays fatal under strict_decode — pinned BEFORE the walker edit so plan 35-05 must preserve it (ROADMAP criterio 2, falsification half)."
  artifacts:
    - path: "packages/higyrus-client/tests/test_null_object.py"
      provides: "Enumeration + truthiness + empty()-emits-nothing + property-alias-invisibility suite for higyrus"
      min_lines: 90
    - path: "packages/higyrus-client/src/higyrus_client/models.py"
      provides: "SafeModel.empty() (form A) and SafeModel.__bool__()"
      contains: "def __bool__"
    - path: "packages/higyrus-client/tests/test_decode.py"
      provides: "Two NEW wrong-type-on-list falsification tests (green day-0, must stay green after 35-05)"
      contains: "test_wrong_typed_list_field_still_reports_type"
  key_links:
    - from: "packages/higyrus-client/src/higyrus_client/models.py"
      to: "packages/higyrus-client/src/higyrus_client/_decode.py"
      via: "empty() synthesizes kwargs through walk_model(cls, {}, policy=POLICY, sink=SILENT_SINK) — the same mechanism the walker's own model site uses"
      pattern: "walk_model\\(cls, \\{\\}, policy=_decode\\.POLICY, sink=_decode\\.SILENT_SINK\\)"
    - from: "packages/higyrus-client/tests/test_null_object.py"
      to: "packages/higyrus-client/src/higyrus_client/models.py"
      via: "inspect.getmembers roster filtered by issubclass(SafeModel) + is_dataclass + __module__ identity"
      pattern: "obj\\.__module__ == models\\.__name__"
  prohibitions:
    - statement: "empty() is never implemented as cls.from_api(None) — that emits a non_dict record (violates T-29-33) and infinitely recurses in matriz."
      status: flagged-unverified
      verification: "Read the added classmethod body in higyrus models.py; it must route through walk_model with SILENT_SINK, not through from_api."
    - statement: "empty() is never memoized (no lru_cache, no per-class _EMPTY singleton) — several shipped classes carry dict/list default_factory fields and a cached instance becomes process-wide shared mutable state."
      status: flagged-unverified
      verification: "Read the added classmethod; no caching decorator and no module-level instance cache may appear."
    - statement: "No existing test is edited in this plan. The only edits to test_decode.py are two NEW test functions appended."
      status: flagged-unverified
      verification: "git diff on packages/higyrus-client/tests/test_decode.py shows additions only — zero deleted or modified lines inside pre-existing test bodies."
---

<objective>
TRACER SLICE. Prove the whole Null Object foundation end-to-end on ONE package — `higyrus-client` — before it is copied anywhere else: the base learns to say "I am empty" (`empty()` + `__bool__`, NOBJ-01), the enumeration test proves it over the 15 real shipped classes rather than a fixture (D-15), the property-alias invisibility invariant that phases 36-38 depend on is retired here (ROADMAP criterio 5 / D-16), and the wrong-type falsification half of NOBJ-02 gets the test coverage it does not have today.

Purpose: `higyrus-client` is the reference twin for `iol-client` and the smallest base with a non-trivial roster (15 classes, one of which — `Administrador` — has zero scalar fields and is the exact shape that breaks a naive perturbation helper). Getting it right once means plans 35-03 and 35-04 are a mechanical fan-out, and the walker landing in 35-05 arrives at a codebase where the behaviour it must NOT change is already pinned.

Output: `higyrus_client.models.SafeModel.empty()` + `.__bool__()`, `packages/higyrus-client/tests/test_null_object.py`, and two new falsification tests inside `packages/higyrus-client/tests/test_decode.py`.

Nothing in this plan touches `_decode.py`. Per RESEARCH F-4c and F-5, the 4 v1.6 CI gates must be **green** at the end of this plan — the canonical digest does not move until 35-05.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-CONTEXT.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Full phase inventory. Items marked **[this plan]** land here; the rest are named so no downstream
plan invents a second copy of them.

| Symbol / file | Owner plan |
|---|---|
| `higyrus_client.models.SafeModel.empty()` (form A) | **35-01 [this plan]** |
| `higyrus_client.models.SafeModel.__bool__()` | **35-01 [this plan]** |
| `packages/higyrus-client/tests/test_null_object.py` | **35-01 [this plan]** |
| `test_wrong_typed_list_field_still_reports_type` (higyrus) | **35-01 [this plan]** |
| `test_strict_mode_still_raises_on_a_wrong_typed_list` (higyrus) | **35-01 [this plan]** |
| `.planning/phases/35-…/35-RETIRED-TRIPLES.md` (D-17 ledger) | 35-02 |
| `iol_client.models.SafeModel.empty()` + `.__bool__()` | 35-03 |
| `market_data_client.models.SafeModel.empty()` (form B) + `.__bool__()` | 35-03 |
| `packages/{iol,market-data}-client/tests/test_null_object.py` | 35-03 |
| `matriz_client.models._SafeModel.__bool__()` (`empty()` already exists) | 35-04 |
| `matriz_client.models.UnknownFrame.__bool__()` (hand-written, D-08) | 35-04 |
| `packages/{matriz,ambito-financiero,wallets}-client/tests/test_null_object.py` | 35-04 |
| `test_wrong_typed_list_field_still_reports_type` ×4 (iol, market-data, matriz, ambito) | 35-03 / 35-04 |
| `test_strict_mode_still_raises_on_a_wrong_typed_list` ×4 | 35-03 / 35-04 |
| EDIT 1 + EDIT 2 in `_decode.py` ×5 (the walker disposition) | 35-05 |
| Rewritten `# Phase 35, NOBJ-02:` comment block in `_decode.py` ×5 | 35-05 |
| The 11 inverted assertions (10 in `test_decode.py` ×5, 1 in market-data `test_core.py:1052`) | 35-05 |
| NEW `CANONICAL_DIGEST` value in `tools/check_decode_intactness.py:222` (**recomputed**, never copied) | 35-05 |
</artifacts_this_phase_produces>

<flagged_assumptions>
The deterministic edge-coverage probe returned `unclassified` / `unresolved` for **both** phase
requirements. Neither is silently dropped; both are resolved here by named source criteria, and the
resolution is recorded as an assumption rather than a fact:

| Requirement | Probe verdict | Resolved by | Residual risk |
|---|---|---|---|
| NOBJ-01 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 1 + CONTEXT D-05..D-09, D-15. Operationalised as the `must_haves.truths` rows above (enumeration over real classes, falsy-when-empty, truthy-when-perturbed, `empty()` emits nothing). | The enumeration guard is `>= 15`, not `== 15`, so phases 36-38 adding classes does not redden it. A class *disappearing* would go unnoticed by the guard alone — mitigated because every shipped class is also parametrized individually. |
| NOBJ-02 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 2 + CONTEXT D-01..D-04, D-13, D-14. This plan owns only the **falsification** half (wrong-type still reports, still fatal under strict); the collapse half lands in 35-05. | The two new wrong-type tests are green day-0. Their value is entirely as a *tripwire for 35-05* — if 35-05's edit over-reaches and silences wrong-typed values too, these reddened. Stated so a reviewer does not mistake a day-0 green for vacuous coverage. |

Assumption A2 of RESEARCH (the exact source form of the list-site gate fixes the digest) is
**closed by 35-05**, which locks the form. Assumption A3 (the red set is identical on Python 3.13)
is left open and is discovered by CI's 6×2 matrix on first push.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write higyrus's Null Object suite RED (enumeration, truthiness, empty()-silence, property-alias invisibility)</name>

  <files>packages/higyrus-client/tests/test_null_object.py</files>

  <read_first>
    - `packages/higyrus-client/src/higyrus_client/models.py` (whole file — the base at `:52-87` and the 15 shipped dataclasses you are about to enumerate)
    - `packages/higyrus-client/tests/test_decode.py:1-141` (module docstring form, the autouse `_pristine_decode_context` fixture at `:42-63`, the module-local frozen+slots fixture shape at `:81-112`, and the `_divergences` / `_walk` / `_tuples` helpers at `:119-141`)
    - `verification/safemodel_diff.py:49-62` (the roster-predicate shape and the rationale paragraph explaining why the helper is copied per package instead of imported cross-package)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Pitfall 3 (the `_perturb` helper that must test `None` first) and §Code Examples 6 and 7
  </read_first>

  <behavior>
    - `test_every_shipped_model_is_falsy_when_empty[<ClassName>]` — parametrized over the introspected roster: `bool(cls.from_api(None)) is False` AND `cls.from_api(None) == cls.empty()`. RED today (`empty` does not exist).
    - `test_every_shipped_model_is_truthy_when_populated[<ClassName>]` — `bool(_perturb(cls.empty())) is True`. RED today.
    - `test_empty_emits_nothing[<ClassName>]` — attach a capturing handler to the `higyrus_client` logger, call `cls.empty()`, assert zero divergence records (T-29-33). RED today.
    - `test_the_model_roster_is_not_vacuous` — `len(_safemodel_classes()) >= 15`, with an inline comment citing RESEARCH F-1 as the measurement (higyrus 15). GREEN today.
    - `test_property_aliases_are_invisible_to_get_type_hints` — `get_type_hints(_AliasShaped)` keys equal `{f.name for f in dataclasses.fields(_AliasShaped)}` and `"last" not in hints`. GREEN today.
    - `test_adding_a_property_alias_does_not_change_the_divergence_count` — walk the SAME payload into `_AliasShaped` (wire fields + `@property last`) and `_AliasFree` (the identical wire fields, no property) and assert the two record lists are equal. GREEN today AND after 35-05 — the invariant is *equality between the two*, not an absolute count.
  </behavior>

  <action>
Create `packages/higyrus-client/tests/test_null_object.py` with a module docstring in the house style (what the suite pins + why fixtures are module-local + a sentence naming NOBJ-01 and ROADMAP Phase 35 criterio 1 and 5), then `from __future__ import annotations`, stdlib imports, `pytest`, then absolute `higyrus_client` imports. No relative imports, no wildcard (ruff TID/F).

Copy the roster helper into this file — do NOT import anything from `verification/` or from another package (repo policy: no shared code across packages; `verification/safemodel_diff.py` documents this constraint in its own docstring and you should mirror that rationale paragraph). Name it `_safemodel_classes()`, returning a list sorted by `__name__`, built from `inspect.getmembers(models, inspect.isclass)` filtered by four conditions: `issubclass(obj, models.SafeModel)`, `obj is not models.SafeModel`, `dataclasses.is_dataclass(obj)`, and `obj.__module__ == models.__name__`. The `__module__` filter is load-bearing — without it a class re-exported from another module enters the roster.

Copy the perturbation helper as `_perturb(empty)`. Its branch order is fixed and is the whole point: test `cur is None` FIRST, then `str`, then `bool`, then `int | float`, then `list`, then `dict`, each returning `dataclasses.replace(empty, **{f.name: <sentinel>})`. Fall through to `raise AssertionError` naming the class, so a class the helper cannot perturb fails loudly instead of being covered vacuously. `higyrus_client.models.Administrador` has three fields and all three are nested models with no scalar — it is the local class that proves the `None`-first branch is needed (RESEARCH Pitfall 3).

Declare two module-local fixture dataclasses for the criterio-5 pair. Both are `@dataclass(frozen=True, slots=True)` subclassing `models.SafeModel`, both declare the SAME wire fields (one nested-leaf field and one `list[leaf]` field, mirroring the `_Leaf`/`_Nested` shape at `test_decode.py:81-112`); `_AliasShaped` additionally carries a read-only `@property last` returning the nested-leaf field — the exact shape phases 36-38 introduce (D-16). `_AliasFree` carries no property. The equality assertion between their record lists is what makes the test survive the 35-05 disposition change; do not write an absolute `== []` or `== [<one record>]` there.

Drive the walker through a local `_walk`-style helper copied from `test_decode.py:130-141` (fresh `DecodeScope`, `caplog.at_level(logging.DEBUG, logger="higyrus_client")`), never through `from_api`, so the record list is the walker's and not a `from_api` override's. Access dynamically-attached record attributes with the established `# type: ignore[attr-defined]` idiom on the comprehension line (`test_decode.py:141`).

Parametrize with `@pytest.mark.parametrize("cls", _safemodel_classes(), ids=lambda c: c.__name__)` — argnames as a plain string, since ruff `PT` is enabled.

This task commits RED on purpose (`test(35-01): add failing Null Object suite for higyrus`). Task 2 turns it green. Do not add `empty()` or `__bool__` here.
  </action>

  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_null_object.py -q -k "not_vacuous or invisible_to_get_type_hints or does_not_change_the_divergence_count" && ! uv run pytest packages/higyrus-client/tests/test_null_object.py -q -k "falsy_when_empty or truthy_when_populated or empty_emits_nothing"</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` exits 0: the three day-0 tests pass and the three `empty()`-dependent tests fail. A run where ALL tests pass means `empty()` leaked into this task; a run where the first half also fails means the fixtures or helper are broken.
    - `uv run pytest packages/higyrus-client/tests/test_null_object.py --collect-only -q | grep -c "test_every_shipped_model_is_falsy_when_empty"` returns `15` — the roster is real and matches RESEARCH F-1's measured count for higyrus.
    - `grep -c "obj.__module__ == models.__name__" packages/higyrus-client/tests/test_null_object.py` returns `1`.
    - `grep -n "cur is None" packages/higyrus-client/tests/test_null_object.py` reports a line number strictly LESS than the line reported by `grep -n "isinstance(cur, str)"` — the `None` branch is first.
    - `grep -c "^from \\.\\|^from verification\\|import verification\\|iol_client\\|matriz_client\\|market_data_client\\|ambito_financiero_client\\|wallets_client" packages/higyrus-client/tests/test_null_object.py` returns `0` — no cross-package import, no relative import.
    - `uv run ruff check packages/higyrus-client && uv run ruff format --check packages/higyrus-client` both exit 0.
  </acceptance_criteria>

  <done>The file exists, collects 15 parametrized cases per model-level test, the three day-0 invariants pass, and the three truthiness invariants fail with `AttributeError` on `empty`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give higyrus's SafeModel base empty() (form A) and __bool__ — turns Task 1 green</name>

  <files>packages/higyrus-client/src/higyrus_client/models.py</files>

  <read_first>
    - `packages/higyrus-client/src/higyrus_client/models.py:40-90` (imports at `:43-49` — `Self` is already imported, nothing to add — and the base at `:52-87` with `from_api` at `:60` and `to_dict` at `:67`)
    - `packages/matriz-client/src/matriz_client/models.py:186-251` (the REFERENCE implementation of `empty()`: docstring shape = one-line summary + a paragraph justifying `SILENT_SINK` and citing `T-29-33`, then the two-line body)
    - `packages/higyrus-client/src/higyrus_client/_decode.py:454-487` (the walker's own model site — it builds a nested default with `walk_model(hint, {}, …, sink=SILENT_SINK)`; `empty()` must use the same mechanism so `empty()` and the nested default are the same object by construction)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Code Examples 1 and 2, §Pitfall 4 (the real per-call cost) and §Pitfall 8 (why truthiness is a field-level test, not a snapshot-level one)
  </read_first>

  <behavior>
    - `SafeModel.empty()` returns an all-defaults instance of the concrete subclass and emits zero records.
    - `SafeModel.__bool__()` returns `False` for an all-defaults instance and `True` for any instance differing from it in at least one field.
    - `Model.from_api(None) == Model.empty()` for all 15 shipped classes (RESEARCH F-1: 0 mismatches).
    - No existing behaviour changes: `from_api`, `to_dict` and every shipped model keep their current output.
  </behavior>

  <action>
Add exactly two methods to `class SafeModel` in `packages/higyrus-client/src/higyrus_client/models.py`. Placement follows the analog ordering in matriz: `from_api` first, then `empty()`, then `__bool__`, then `to_dict` (so `empty()` is inserted between the existing `:60` and `:67` methods and `__bool__` immediately after it).

`empty()` is a `@classmethod` annotated `-> Self` (form A of D-07 — higyrus and iol get the no-mapping-pass form because this package has no `_apply_mapping_policy`). Body is two statements: bind `kwargs` to `_decode.walk_model(cls, {}, policy=_decode.POLICY, sink=_decode.SILENT_SINK)`, then `return cls(**kwargs)`. Do NOT add a `cast` or a `# type: ignore` — the adjacent `from_api` proves `cls(**kwargs)` already compiles under mypy strict on a non-dataclass base.

`__bool__` is an instance method annotated `-> bool`; its body is the single expression `return self != type(self).empty()`. This exact form is locked by D-06. `self.empty()` would be equivalent and shorter but is not what D-06 signed, and `type(self).empty()` makes it explicit that the comparison is against the concrete subclass's empty, not the base's.

Docstrings, following the matriz reference shape (one-line summary + justifying paragraph with a lock/test ID):
  - `empty()` — summary "Build an all-defaults instance. Emits nothing (T-29-33)." plus a paragraph explaining that routing it through an emitting sink would produce one spurious record per field, and one sentence recording D-07's prohibition in your own words: this constructor must not be expressed in terms of the tolerant `from_api` entry point, because that path emits a terminal record for a non-dict payload and, in the matriz twin, would recurse without end.
  - `__bool__()` — summary naming NOBJ-01 and the Null Object semantics, plus TWO recorded facts from RESEARCH: (a) the call rebuilds the whole empty subtree each time, measured at ~11 µs for a two-level shipped model and ~19 µs for a three-level one, so it is not free inside a hot loop; (b) per D-09, truthiness is a field-level emptiness test — a model whose `from_api` injects a client-stamped value post-walk is truthy even when the wire carried nothing, so callers ask about the field, not the envelope.

Do not memoize. No `functools.lru_cache`, no module-level `_EMPTY` dict, no class attribute caching the instance — several shipped classes in this workspace carry mutable `default_factory` fields and a cached instance becomes process-wide shared mutable state (RESEARCH Pitfall 4b). If a caller needs speed, that is Phase 36's problem with its own analysis.

Touch nothing else in this file: no changes to `from_api`, `to_dict`, `__all__`, or any shipped dataclass.
  </action>

  <verify>
    <automated>uv run pytest packages/higyrus-client -q && uv run mypy packages/higyrus-client/src && uv run ruff check packages/higyrus-client && uv run ruff format --check packages/higyrus-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/higyrus-client/tests/test_null_object.py -q` exits 0 with all cases passing (the three RED tests from Task 1 are now green).
    - `uv run pytest packages/higyrus-client -q` exits 0 — the pre-existing higyrus suite is unchanged and unbroken (RESEARCH F-4c: the base methods are additive-only).
    - `grep -c "def __bool__" packages/higyrus-client/src/higyrus_client/models.py` returns `1`; `grep -c "def empty" packages/higyrus-client/src/higyrus_client/models.py` returns `1`.
    - `grep -c "return self != type(self).empty()" packages/higyrus-client/src/higyrus_client/models.py` returns `1` — the D-06 body verbatim.
    - Non-memoization is proven behaviourally, not by grepping for a decorator name: `uv run python -c "from higyrus_client import models as m; assert m.Cuenta.empty() is not m.Cuenta.empty()"` exits 0. Two calls must build two objects — a cached instance would be shared mutable state across every caller in the process.
    - `uv run mypy` and `uv run mypy packages/higyrus-client/src` both print `Success: no issues found`.
    - All four v1.6 gates still pass — this plan does not move the canonical digest: `uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py && uv run python tools/surface_parity.py` exits 0.
    - `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` exits 0 — the public-surface snapshots are byte-identical (D-12, RESEARCH F-8).
  </acceptance_criteria>

  <done>higyrus's 15 shipped models are falsy when empty and truthy when perturbed, `empty()` emits nothing, mypy strict and ruff are clean, and all 4 v1.6 gates plus the surface snapshots are unmoved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the wrong-type falsification half of NOBJ-02 in higyrus's test_decode.py</name>

  <files>packages/higyrus-client/tests/test_decode.py</files>

  <read_first>
    - `packages/higyrus-client/tests/test_decode.py:200-230` (the existing `test_missing_list_field_returns_empty_list_and_reports` at `:215` — the sibling that plan 35-05 will invert; do not touch it here)
    - `packages/higyrus-client/tests/test_decode.py:1140-1160` (`test_non_dict_nested_payload_keeps_the_nested_attribution` at `:1149` — the structural model for the new list-site test; it is the model-site wrong-type test that already exists and already stays green)
    - `packages/higyrus-client/src/higyrus_client/_decode.py:442-452` (the list site as it stands today — the branch whose reporting 35-05 will narrow)
    - `packages/higyrus-client/src/higyrus_client/_decode.py:195-225` (`DecodeScope.__call__` — the single choke point where `strict_decode` raises; this is what the strict-mode test exercises)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Pattern 3 (the falsification matrix: these two rows are the only ones with NO coverage today)
  </read_first>

  <behavior>
    - `test_wrong_typed_list_field_still_reports_type` — walking `{"titulo": "t", "hojas": "garbage"}` into the module's `list[Model]`-carrying fixture returns `hojas == []` AND emits exactly one record, the three-tuple `("_Nested", ".hojas", "type")`.
    - `test_strict_mode_still_raises_on_a_wrong_typed_list` — the same payload under a strict `DecodeScope` raises the package's decode error; the exception's attributes name the same field path and kind.
    - Both are GREEN today, on the unedited walker, and must remain green after plan 35-05 lands the disposition change.
  </behavior>

  <action>
Append two test functions to `packages/higyrus-client/tests/test_decode.py`, inside the section that already groups wrong-type ("divergence class 2") cases — locate it by reading around the existing model-site wrong-type test at `:1149` and place the new pair adjacent to it so the two halves of the falsification argument sit together.

Both tests drive the existing module-local `list[Model]`-carrying fixture (the one used by the test at `:215`) through the module's `_walk` helper — never through `from_api`. Reuse the existing `_walk`, `_divergences` and `_tuples` helpers; do not re-implement them and do not re-implement the autouse `_pristine_decode_context` fixture, which already isolates decode scope for every test in this module.

Test 1 asserts the payload with a *string* where the list goes still produces a record. Assert the full three-tuple `(model, field_path, divergence)` with an equality against a one-element list, not a membership check — a membership check would still pass if the edit in 35-05 accidentally emitted a second, spurious record. Assert the returned field is `[]` as well, so the test pins the value AND the reporting.

Test 2 opens a strict `DecodeScope` the same way the pre-existing strict-mode tests in this module do (read one of them first — `test_strict_mode_does_not_make_empty_fatal` is the closest neighbour and is named in D-14 as a test that must stay green), feeds the same wrong-typed payload, and asserts with `pytest.raises` that the package decode error is raised. Assert on the raised exception's field-path and kind attributes, not merely on the exception type — an exception raised for a *different* divergence would otherwise pass.

Write a docstring on each naming ROADMAP Phase 35 criterio 2 and stating explicitly that the test is green before the walker edit and must stay green after it: its job is to redden if 35-05's silencing over-reaches from `None` to every non-list value.

Do not modify, rename, or re-docstring any existing test in this file. Plan 35-05 owns the two inversions here; this task is additive only.
  </action>

  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_decode.py -q -k "wrong_typed_list or still_raises_on_a_wrong_typed_list" && uv run pytest packages/higyrus-client -q</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/higyrus-client/tests/test_decode.py -q -k "wrong_typed_list"` exits 0 and reports exactly 2 tests passing.
    - `uv run pytest packages/higyrus-client -q` exits 0 — no pre-existing test reddened.
    - `git diff --numstat packages/higyrus-client/tests/test_decode.py` shows `0` in the deletions column — the change is purely additive (proves no existing assertion was edited in this plan).
    - `grep -c "def test_wrong_typed_list_field_still_reports_type\|def test_strict_mode_still_raises_on_a_wrong_typed_list" packages/higyrus-client/tests/test_decode.py` returns `2`.
    - `grep -c "pytest.raises" packages/higyrus-client/tests/test_decode.py` increases by exactly 1 relative to `git show HEAD:packages/higyrus-client/tests/test_decode.py | grep -c "pytest.raises"`.
    - `uv run ruff check packages/higyrus-client && uv run ruff format --check packages/higyrus-client` exit 0.
  </acceptance_criteria>

  <done>Both halves of NOBJ-02's wrong-type falsification are covered by executable tests in higyrus, green against the current walker, with the higyrus suite fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party JSON payload → `walk_model` / `walk_field` | The only untrusted input this phase's code path sees. It is already the workspace's input-validation layer (ASVS V5). |
| divergence record → package logger | Untrusted wire-derived strings reach a log sink (ASVS V7). Neutralised upstream by `_safe_key` + a 64-char key cap; this plan adds no new path into that sink. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Information disclosure | `SafeModel.__bool__` / `empty()` in `higyrus_client/models.py` | accept | Both methods are pure and payload-independent: `empty()` walks an empty dict, `__bool__` compares two in-memory instances. Neither reads wire data, credentials, nor env. Verified by the behavioural non-memoization check in Task 2's acceptance criteria. |
| T-35-02 | Tampering | Cached empty instance becoming shared mutable state | mitigate | Memoization is prohibited by a `must_haves.prohibitions` row and enforced behaviourally: two successive `empty()` calls on the same class must return distinct objects. Shipped classes with `dict`/`list` `default_factory` fields would otherwise be corruptible process-wide by any caller mutating the cached instance. |
| T-35-03 | Repudiation | Divergence-channel observability | accept | This plan does not change what is emitted — it only ADDS coverage of the wrong-type half. Reduced emission is 35-05's scope and is bounded there to `value is None`. |
| T-35-SC | Tampering | pip / uv dependency installs | accept | This phase installs nothing. Enforced: `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` must be clean at plan end. RESEARCH §Package Legitimacy Audit records "no aplica" with a measured diff (9 files, none of them dependency manifests). |
</threat_model>

<verification>
Run from the repo root, in this order. NEVER run bare `uv run pytest` — `testpaths` includes
`verification/`, which hangs past 10 minutes and is red at baseline under `HARN-VERIF-01`
(RESEARCH Pitfall 5 / F-9).

1. `uv run pytest packages/higyrus-client -q` — green.
2. `uv run pytest packages -q` — green; the full workspace suite is unaffected by an additive base.
3. `uv run ruff check packages/ && uv run ruff format --check packages/` — clean.
4. `uv run mypy` and `uv run mypy packages/market-data-client/src` — both `Success` (the second is not redundant: market-data is outside the root mypy `files` list, RESEARCH Pitfall 6).
5. The 4 v1.6 gates, all still green because no byte of `_decode.py` moved:
   `uv run python tools/check_decode_intactness.py`,
   `uv run python tools/check_uniform_structure.py`,
   `uv run python tools/check_surface_types.py`,
   `uv run python tools/surface_parity.py`.
6. `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` — byte-identical (D-12).
7. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` — clean (T-35-SC).
</verification>

<success_criteria>
- `higyrus_client.models.SafeModel` exposes `empty() -> Self` and `__bool__() -> bool`; `__bool__`'s body is `return self != type(self).empty()` verbatim.
- All 15 shipped higyrus models satisfy `bool(from_api(None)) is False`, `from_api(None) == empty()`, `bool(perturbed) is True`, and `empty()` emitting zero records — proven by enumeration of the real module, not fixtures.
- `get_type_hints()` provably ignores `@property` on a frozen+slots dataclass, and an alias-carrying class produces the same divergence record list as its alias-free twin.
- Two new wrong-type-on-list tests exist and pass against the *unedited* walker.
- `uv run pytest packages -q`, ruff, both mypy invocations, all 4 gates, and the snapshot byte-identity check are green.
- `git diff --numstat` shows zero deletions in `packages/higyrus-client/tests/test_decode.py`.
</success_criteria>

<output>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-SUMMARY.md` when done.
Record in it: the measured roster count for higyrus, the exact command output of the 4 gates, and a
one-line statement that the canonical digest was NOT touched by this plan.
</output>
