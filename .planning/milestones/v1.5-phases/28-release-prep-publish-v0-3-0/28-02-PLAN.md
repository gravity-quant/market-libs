---
phase: 28-release-prep-publish-v0-3-0
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified: []
autonomous: false
requirements: [PUB-MUT-01]
user_setup:
  - service: github
    why: "Opening the PR and merging it to the public default branch are outward-facing GitHub operations; the merge is irreversible in practice"
    dashboard_config:
      - task: "Ensure the `gh` CLI is authenticated (asserted in Task 1 via `gh auth status`; never echo the token)"
        location: "local shell — `gh auth status`"
      - task: "Be available to answer the blocking go/no-go checkpoint before the merge (D-18a)"
        location: "the executing terminal session"

must_haves:
  truths:
    - "A single PR `milestone/v1.5-mutations` → `main` is open, titled per the branch convention, carrying the whole Phase 25-27 diff INCLUDING the .planning/ artifacts — D-08, D-09, D-12"
    - "The PR reports exactly 15 checks and all 15 have status `pass`, two of which are `Tests · market-data-client · py3.12` and `· py3.13` — asserted by COUNT, never by absence of the word fail — D-13, C-2, SC#2"
    - "The PR is merged into `main` only after the operator has explicitly replied approved at a blocking checkpoint — D-18(a)"
    - "`origin/main` advances to a real merge commit with TWO parents, produced by `gh pr merge --merge` — never squash, never rebase — D-11"
  artifacts:
    - path: "gh-pr:milestone/v1.5-mutations -> main"
      provides: "the single release PR carrying Phases 25-27 plus the v0.4.0 prep commits"
      contains: "release: market-data-client v0.4.0"
    - path: "git-ref:origin/main"
      provides: "merge commit that becomes the tag anchor for plan 28-03"
      contains: "Merge pull request"
  key_links:
    - from: "gh-pr:milestone/v1.5-mutations -> main"
      to: ".github/workflows/ci.yml"
      via: "the PR triggers ci.yml (4 jobs; `test` fans out over 6 packages × 2 Python versions) producing exactly 15 checks"
      pattern: "Tests · market-data-client · py3.1[23]"
    - from: "git-ref:origin/main"
      to: "git-tag:market-data-client-v0.4.0"
      via: "plan 28-03 creates the annotated tag ON this merge commit so release.yml's version-match gate runs against the merged tree"
      pattern: "market-data-client-v0.4.0"
---

<objective>
Open the single release PR, prove the CI gate is genuinely green by counting passing checks, and —
only after an explicit human go/no-go — merge it into `main` with a real merge commit.

Purpose: `main` has NO branch protection (verified: protection API 404, `rulesets` `[]`,
`protected: false`). GitHub will merge this PR with red, pending or cancelled checks and will not
stop anything. The count-based assertion plus the D-18(a) checkpoint are therefore the ONLY
enforcement standing between an unverified build and a public default branch.
Output: A merge commit on `origin/main` with two parents, carrying the v0.4.0 bump.

This plan is `autonomous: false`. The merge is irreversible in practice and is gated by a blocking
`checkpoint:human-verify`. It requires a clean working tree and an authenticated `gh`. The tag push
and the public Release are NOT part of this plan — they are gated behind a SECOND checkpoint in
plan 28-03 (D-18 requires two separate gates; do not collapse them).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Non-symbol artifacts produced across phase 28 (this plan produces the middle group):

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| `### v0.4.0` README changelog section | `packages/market-data-client/README.md` | 28-01 Task 1 | `grep -qx '### v0.4.0'` |
| ROADMAP § Backlog `### Deferred to v1.6+` entry | `.planning/ROADMAP.md` | 28-01 Task 2 | `grep -q '^### Deferred to v1.6'` |
| Pushed release branch (fast-forward) | `git-ref:origin/milestone/v1.5-mutations` | 28-01 Task 3 | `git rev-parse HEAD` == `git rev-parse origin/...` |
| **The release PR** | `gh-pr:milestone/v1.5-mutations -> main` | **28-02 Task 1** | `gh pr view --json number,title,state` |
| **Merge commit on `main` (two parents)** | `git-ref:origin/main` | **28-02 Task 3** | `git rev-list --parents -n1 origin/main` → 3 fields |
| Release tag on the merge commit | `git-tag:market-data-client-v0.4.0` | 28-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Public GitHub Release + wheel + sdist | `gh-release:market-data-client-v0.4.0` | 28-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Open the release PR and assert 15/15 checks pass BY COUNT (D-08, D-09, D-12, D-13, C-2, SC#2)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Release Mechanics Phase B (lines 755-786) — the exact `gh pr create` form and the count-based assertion, with the reason `&& echo PASS` alone is insufficient
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § CI Gate Anatomy (lines 608-674) — the 15 check names read verbatim off completed run PR #9, what each job runs, the `paths-ignore` and `cancel-in-progress` subtleties
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Contradictions C-2 (lines 289-319) — `main` has no branch protection; the gate is entirely self-enforced
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md` § S-2 (lines 705-723) — why Phase 24's `! … | grep -qiE '\bfail'` gate is inadequate and what replaces it
    - `.github/workflows/ci.yml` — read-only; trigger block (`:9-13`), `concurrency` (`:20`), the four jobs, `matrix.package` (`:98-103`), `matrix.python-version` (`:104`)
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` D-08, D-09, D-12, D-13, D-14
  </read_first>
  <action>
    (a) Preconditions, all three must hold before anything else: `gh auth status` exits 0 (never
    echo the token — the command already redacts it); `git status --porcelain` is empty; and
    `git rev-parse HEAD` equals `git rev-parse origin/milestone/v1.5-mutations` (plan 28-01 Task 3
    pushed the branch — C-1). If the branch is not pushed, STOP: `gh pr create` against an unpushed
    branch fails or prompts interactively, which an agent cannot answer (RESEARCH P-1).

    (b) Create ONE pull request with `gh pr create --base main --head milestone/v1.5-mutations`.
    Title must follow the convention already established by PRs #8 and #9 on this branch:
    `release: market-data-client v0.4.0 (<one-line scope>)` — use
    `release: market-data-client v0.4.0 (calendar write + live-verified mutation fixes)` (D-12).
    Body wording is Claude's discretion (the GitHub Release itself uses `--generate-notes`, so the
    body does not need to be release-notes-shaped); it must contain no credential and no secret
    value. Do NOT run `/gsd-pr-branch` and do NOT filter `.planning/` out of the diff (D-09) —
    PRs #8 and #9 both carried `.planning/` and dropping it would strip ~60 KB of
    live-verification evidence from `main`. Do NOT open a second PR; D-08 mandates exactly one.

    (c) Wait for the checks to settle with `gh pr checks --watch`, then take a final snapshot with
    `gh pr checks`.

    (d) Assert the gate BY COUNT, not by the absence of the word "fail". Capture the PR number
    (`gh pr view --json number --jq .number`), then require all three of: the total row count is
    exactly 15; the number of rows whose status field is `pass` is exactly 15; and exactly 2 rows
    match `Tests · market-data-client · py3.1[23]`. Statuses other than `pass` include `fail`,
    `pending`, `skipping` and `cancelled` — a naive "no failures" check reads all of those as
    green, and `cancelled` is genuinely reachable because `ci.yml:20` sets
    `concurrency: cancel-in-progress: true`. Also note that a Markdown-only diff would trigger ZERO
    checks (`paths-ignore: ["**.md", ".gitignore"]`): "no checks" is NOT green, which is exactly
    why the total must equal 15.

    (e) Do NOT push any further commit to the branch while checks are running — it cancels the
    in-flight run (RESEARCH P-7). If a fix is genuinely required, land it, then re-run
    `gh pr checks --watch` to completion and re-assert the full 15/15 count from scratch.

    (f) Do NOT merge in this task. Do NOT create or push any tag in this task or in this plan.
    The merge is gated by Task 2; the tag is gated by a separate checkpoint in plan 28-03.

    Expected 15 check names, verbatim from completed run PR #9: `Lint y formato (ruff)`,
    `pre-commit hooks`, `Type check (mypy)`, and `Tests · <pkg> · py3.12` / `· py3.13` for each of
    `ambito-financiero-client`, `higyrus-client`, `iol-client`, `market-data-client`,
    `matriz-client`, `wallets-client`. Never wait for a full local suite to be green before
    opening the PR — no CI job executes `verification/` (D-14), and waiting would block the release
    indefinitely on 19 known out-of-scope matriz failures.
  </action>
  <verify>
    <automated>gh auth status >/dev/null 2>&1 && test -z "$(git status --porcelain)" && PR=$(gh pr view --json number --jq .number) && test -n "$PR" && TOTAL=$(gh pr checks "$PR" | wc -l | tr -d ' ') && PASSED=$(gh pr checks "$PR" | awk -F'\t' '$2=="pass"' | wc -l | tr -d ' ') && MD=$(gh pr checks "$PR" | grep -c 'Tests · market-data-client · py3\.1[23]') && test "$TOTAL" = "15" && test "$PASSED" = "15" && test "$MD" = "2" && test "$(gh pr view "$PR" --json state --jq .state)" = "OPEN" && test "$(gh pr view "$PR" --json baseRefName --jq .baseRefName)" = "main" && test "$(gh pr view "$PR" --json headRefName --jq .headRefName)" = "milestone/v1.5-mutations" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --state open --base main` shows exactly one PR, whose `headRefName` is `milestone/v1.5-mutations`
    - That PR's title starts with `release: market-data-client v0.4.0 (`
    - `gh pr checks <N> | wc -l` outputs exactly `15`
    - `gh pr checks <N> | awk -F'\t' '$2=="pass"' | wc -l` outputs exactly `15` — no row is `fail`, `pending`, `skipping` or `cancelled`
    - `gh pr checks <N> | grep -c 'Tests · market-data-client · py3\.1[23]'` outputs exactly `2`
    - `gh pr view <N> --json files --jq '.files[].path'` includes at least one path under `.planning/` (D-09 — the artifacts were NOT filtered out)
    - `git rev-parse HEAD` still equals `git rev-parse origin/milestone/v1.5-mutations` (no commit was pushed mid-run)
    - No tag was created or pushed during this task: `git tag -l 'market-data-client-v0.4.0'` is empty
  </acceptance_criteria>
  <done>Exactly one PR `milestone/v1.5-mutations` → `main` is open with the D-12 title, carrying the `.planning/` artifacts, and `gh pr checks` reports 15 rows of which 15 are `pass`, including both market-data-client matrix jobs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking go/no-go gate before the IRREVERSIBLE merge to the public default branch (D-18a)</name>
  <read_first>
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` D-18 — two blocking checkpoints, this is gate (a)
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-VALIDATION.md` § Manual-Only Verifications — the row "Go/no-go before merging the PR (D-18a)" defines what must be presented
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Contradictions C-2 (lines 289-319) — the evidence that no branch protection exists, which is what makes this gate load-bearing
    - The 28-02 Task 1 output already in context: the PR URL/number and the counted check totals
  </read_first>
  <acceptance_criteria>
    - The operator was shown the PR URL, the literal counted totals (15 rows / 15 `pass` / 2 market-data-client rows) and the explicit statement that `main` has no branch protection
    - No `gh pr merge`, `git tag`, or `git push` command was executed before the operator's reply
    - The operator's reply is recorded verbatim in the SUMMARY together with its timestamp
    - Execution continues to Task 3 ONLY if the reply is an explicit "approved"; any other reply (including silence, ambiguity, or "abort") halts the plan with the PR still open and `origin/main` unchanged
  </acceptance_criteria>
  <action>PAUSE. This is a blocking human checkpoint (`autonomous: false`). Do NOT merge the PR, do NOT create any tag, and do NOT push anything until the operator explicitly replies "approved". If the operator replies "abort", stop cleanly with no irreversible action taken — the PR stays open and everything so far is revertible. Present: the PR URL and number, the literal `gh pr checks` output with the counted totals (15 rows / 15 pass / 2 market-data-client rows), the diff stat, and an explicit statement that `main` has NO branch protection so this approval is the only gate.</action>
  <what-built>
    A single PR (`milestone/v1.5-mutations` → `main`, title
    `release: market-data-client v0.4.0 (calendar write + live-verified mutation fixes)`) is open
    with ALL 15 CI checks reporting `pass`, counted explicitly — 15 total rows, 15 with status
    `pass`, 2 of them `Tests · market-data-client · py3.12` and `· py3.13`. The working tree is
    clean and `gh` is authenticated.

    The next task performs an operation that is irreversible in practice: merging this PR into
    `main`, the default branch of a PUBLIC repository. The merge will use `gh pr merge --merge`
    (a real merge commit with two parents — never `--squash`, never `--rebase`), and it publishes
    ~90 changed files including the whole Phase 25-27 diff, the `.planning/` artifacts, ~60 KB of
    live-verification evidence and the 143 KB live driver `main_market_data.py`.

    CRITICAL CONTEXT: `main` has NO branch protection whatsoever — the protection API returns 404,
    `rulesets` is `[]`, and `protected: false`. GitHub will merge this PR regardless of check
    status. The 15/15 assertion above and YOUR approval are the only things preventing an
    unverified build from landing on a public default branch.

    A credential scan over the full `origin/main...HEAD` diff was re-run immediately before the
    push (plan 28-01 Task 3) and came back clean: no JWT-shaped token, no `client_secret`
    assignment, no tracked `.env`.

    The release TAG and the public GitHub Release are NOT part of this approval. They are gated by
    a second, separate checkpoint in plan 28-03 (D-18 requires two independent gates).
  </what-built>
  <how-to-verify>
    1. Open the PR on GitHub. Confirm the title is exactly
       `release: market-data-client v0.4.0 (calendar write + live-verified mutation fixes)` and the
       base is `main`, head is `milestone/v1.5-mutations`.
    2. Confirm the diff scope looks right: the `market-data-client` package source + tests, the
       version bump in `pyproject.toml` / `__init__.py` / `uv.lock`, the `### v0.4.0` README
       changelog entry, and the `.planning/` artifacts (these are intentionally kept — D-09).
    3. Confirm the `### v0.4.0` changelog entry explicitly names the `CalendarDay` field
       replacement (`date` / `marketId` / `isBusinessDay` removed → `day` / `closed` /
       `description` / `open_time` / `close_time`). This callout is the locked mitigation for
       shipping a source-breaking model change inside a minor bump (D-03); without it the release
       is silently breaking.
    4. Confirm no file under `.github/workflows/` appears in the diff (D-06).
    5. Confirm the check table shows 15 rows and all 15 are `pass` — not pending, not cancelled.
    6. Accept the known residual risk: D-13's claim that the `CalendarDay` swap is non-breaking was
       never independently audited (`27-VERIFICATION.md` re-verified only D-22). The changelog
       callout is the accepted mitigation.
    7. Confirm you authorize the irreversible merge into public `main` NOW.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `gh pr merge --merge`, or "abort" (optionally describing blockers) to stop before any irreversible action. Anything other than an explicit "approved" means do not merge.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Merge the PR with a real merge commit and verify `origin/main` advanced to a two-parent commit (D-11, D-17)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Release Mechanics Phase C (lines 787-796) — the exact merge + verification command forms
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Anti-Patterns to Avoid (lines 931-944) — why squash and rebase are forbidden
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` D-10, D-11, D-17
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-VALIDATION.md` § Per-Task Verification Map — the two-parent assertion row
  </read_first>
  <action>
    Execute ONLY after Task 2 returned an explicit "approved". If it returned anything else, stop.

    (a) Merge with `gh pr merge <N> --merge`. The `--merge` flag is mandatory (D-11): `--squash`
    would collapse ~99 commits into one and orphan every SHA that the Phase 25-27 SUMMARYs
    cross-reference under `.planning/phases/2[5-7]-*/`; `--rebase` would rewrite published history
    reachable from the v0.3.0 and v0.3.1 tags. The repository allows all three merge methods
    (`allow_merge_commit: true`), so nothing but this instruction prevents the wrong one.
    `delete_branch_on_merge` is `false`, so `milestone/v1.5-mutations` survives the merge — plan
    28-03 commits the release-memory update onto it afterwards. Do NOT pass a branch-delete flag.

    (b) Refresh local refs: `git fetch origin main --tags`. Then capture the merge commit as
    `MERGE_SHA=$(git rev-parse origin/main)` and inspect it with
    `git log -1 --format='%H %p %s' "$MERGE_SHA"` — the `%p` field MUST list two parent SHAs and
    the subject must be a `Merge pull request #<N> …` line. A single-parent commit means the merge
    was squashed or rebased: STOP and surface it rather than proceeding to the tag, because
    plan 28-03 tags exactly this commit and `release.yml` validates the version against the tagged
    tree (RESEARCH P-3).

    (c) Assert the merged tree actually carries the bump: `git show
    origin/main:packages/market-data-client/pyproject.toml` must report version `0.4.0` when read
    with the same `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` expression `release.yml:47`
    uses. If it does not, the tag would be rejected by the pipeline's version-match gate.

    (d) Do NOT create or push any tag here. Do NOT push to `main` directly; the merge goes through
    the PR so attribution, PR closure and the merge-commit shape all match the five prior releases.
    Never echo the `gh` token or any credential.

    Record `MERGE_SHA` and the PR number in the SUMMARY — plan 28-03 recomputes `MERGE_SHA` from
    `git rev-parse origin/main` rather than trusting a literal, but the recorded value is the
    cross-check.
  </action>
  <verify>
    <automated>git fetch origin main --tags --quiet && test "$(git rev-list --parents -n1 origin/main | wc -w | tr -d ' ')" -eq 3 && git log -1 --format=%s origin/main | grep -q '^Merge pull request' && V=$(git show origin/main:packages/market-data-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$V" = "0.4.0" && test "$(gh pr view --json state --jq .state 2>/dev/null || echo MERGED)" != "OPEN" && test -z "$(git tag -l 'market-data-client-v0.4.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --parents -n1 origin/main | wc -w` outputs `3` — the commit plus exactly two parents (D-11)
    - `git log -1 --format=%s origin/main` begins with `Merge pull request`
    - `git show origin/main:packages/market-data-client/pyproject.toml` parsed with `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` outputs `0.4.0` — the merged tree will satisfy `release.yml:42-51`
    - The PR state is `MERGED` (not `OPEN`, not `CLOSED`)
    - `git tag -l 'market-data-client-v0.4.0'` is still empty — no tag was created in this plan
    - `git ls-remote --heads origin milestone/v1.5-mutations` still returns a ref — the branch survived the merge and is available for the plan 28-03 memory commit
    - The command history shows `gh pr merge <N> --merge` and contains no `--squash` and no `--rebase`
  </acceptance_criteria>
  <done>The PR is merged; `origin/main` points at a real merge commit with two parents whose tree carries `version = "0.4.0"`; no tag exists yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release branch → public `main` | The merge publishes ~90 files to the default branch of a PUBLIC repository. This crossing is irreversible in practice. |
| CI check results → merge decision | `main` has NO branch protection (protection API 404, `rulesets` `[]`, `protected: false`), so GitHub applies no gate of its own; the check status only influences the merge if the agent enforces it. |
| agent → irreversible GitHub operation | `gh pr merge` succeeds unconditionally; only the human checkpoint stands between an unverified build and `main`. |
| agent shell → `gh` OAuth token | Ambient credential; any echo lands in transcripts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering | merging red, pending or cancelled code into an unprotected public `main` | mitigate | Task 1 (d) asserts the gate by COUNT — 15 total rows, 15 with status `pass`, 2 market-data-client rows — instead of the inadequate "no row says fail" form, which passes on pending, cancelled and empty output. `cancel-in-progress: true` makes `cancelled` genuinely reachable, and a Markdown-only diff would produce zero checks. ASVS V4. |
| T-28-06 | Elevation of Privilege / Repudiation | the irreversible merge itself | mitigate | Task 2 is a blocking `checkpoint:human-verify` in an `autonomous: false` plan; the merge in Task 3 executes only on an explicit "approved", and "abort" stops cleanly with the PR still open. With C-2 proving no branch protection exists, this checkpoint is the sole access control on the operation. ASVS V4. |
| T-28-09 | Tampering | merge strategy (squash / rebase would rewrite history) | mitigate | Task 3 (a) mandates `gh pr merge --merge` and forbids `--squash` / `--rebase`; the acceptance criteria assert the resulting commit has exactly two parents. Preserves every SHA the Phase 25-27 SUMMARYs cite. |
| T-28-03a | Tampering | tagging a commit that is not the merge commit | mitigate | This plan creates no tag at all; Task 3 verifies the merged tree already reads `version = "0.4.0"` so plan 28-03 can tag `origin/main` directly and satisfy `release.yml:42-51`. A single-parent commit halts the phase (RESEARCH P-3). |
| T-28-07 | Information Disclosure | PR body / `gh` token | mitigate | The PR body carries no credential; the GitHub Release uses `--generate-notes`; `gh auth status` is used instead of ever echoing the token. ASVS V2. |
| T-28-SC | Tampering | uv/pip/cargo installs | accept | No package installs occur in this plan. |
</threat_model>

<verification>
1. Exactly one open PR `milestone/v1.5-mutations` → `main` with the D-12 title.
2. `gh pr checks <N>` reports 15 rows; exactly 15 have status `pass`; exactly 2 are the market-data-client matrix jobs.
3. The PR diff still contains `.planning/` paths (D-09) and no `.github/workflows/` paths (D-06).
4. The operator explicitly replied "approved" before any merge command ran (D-18a).
5. `git rev-list --parents -n1 origin/main | wc -w` equals 3 (D-11).
6. `git show origin/main:packages/market-data-client/pyproject.toml` reads version `0.4.0`.
7. No tag named `market-data-client-v0.4.0` exists at the end of this plan.
</verification>

<success_criteria>
- The 15-green gate was enforced by explicit counting, not by pattern-matching for failure, because nothing on GitHub enforces it (C-2).
- No irreversible operation ran without an explicit human "approved" (D-18a).
- `main` carries a real merge commit with two parents, ready to be tagged by plan 28-03.
- The release branch still exists on `origin` so plan 28-03 can land the release-memory commit on it.
</success_criteria>

<output>
Create `.planning/phases/28-release-prep-publish-v0-3-0/28-02-SUMMARY.md` when done.

Record in the SUMMARY: the PR number and URL, the literal counted check totals (total / pass /
market-data-client rows), the operator's approval text and timestamp for the D-18(a) gate, the
captured `MERGE_SHA` with its two parent SHAs, and the merge method used.
</output>
