---
phase: 17-final-live-re-verification-4-live-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/verification/iol-client-findings.md
autonomous: true
requirements: [LIVE-03]
must_haves:
  truths:
    - "verify_cycle_closure returns (True, []) for all 4 packages BEFORE the operator runs the live gate (D-03 closure: cycle-closure markers updated, verify_cycle_closure × 4 PASS, schema snapshot vs verification-cycle-2026-Q2 clean)"
    - "The D-06 static title-stability gate (git diff 71bf201..HEAD scoped to title=/fid=/class_= literals in the 4 drivers) shows zero changed literals"
    - "Public-surface snapshot, sync/async wire-isolation, and credential-leak blocking gates are GREEN at HEAD"
    - "pytest collects >= 989 tests (Phase 15 baseline) at HEAD"
  artifacts:
    - path: ".planning/verification/iol-client-findings.md"
      provides: "iol F-02 FIXED finding with a resolvable Regression: <path>.py::<test> link consumed by verify_cycle_closure"
      contains: "Regression:"
  key_links:
    - from: ".planning/verification/iol-client-findings.md"
      to: "packages/iol-client/tests/test_refresh_token_lifecycle.py"
      via: "F-02 operator-field Regression: line points at test_refresh_token_success_path_rotates"
      pattern: "test_refresh_token_lifecycle\\.py::test_refresh_token_success_path_rotates"
---

<objective>
Make the v1.2 cycle-closure gate green and deterministic BEFORE the operator runs the
Phase 17 live re-verification. The single blocking gap is that
`verify_cycle_closure("iol-client")` currently returns `(False, ['F-02'])`: iol F-02 is
status FIXED but its `Regression:` operator field is prose, not a resolvable
`<path>.py::<test_name>` token, so `_regression_is_resolvable` rejects it. Success Criterion #2
requires `verify_cycle_closure × 4` PASS, so this must be closed up-front. This plan also
re-attests the deterministic (no-live-creds) gates the operator plan (17-02) will reference:
the D-06 static title-stability diff, the public-surface snapshot, the blocking-regression
tests, and the pytest >= 989 baseline.

Purpose: De-risk the operator gate. Every check here is deterministic and credential-free,
so 17-02 starts from a known-green cycle-closure state and only adds live evidence.
Output: iol-client-findings.md F-02 with a resolvable regression link; an attestation that
`verify_cycle_closure × 4` PASS and the deterministic blocking gates are GREEN at HEAD.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-final-live-re-verification-4-live-03/17-CONTEXT.md
@.planning/phases/17-final-live-re-verification-4-live-03/17-PATTERNS.md
@verification/cycle_report.py
@.planning/verification/iol-client-findings.md
@.planning/verification/matriz-client-findings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link iol F-02 FIXED to a resolvable regression test so verify_cycle_closure(iol-client) flips to PASS</name>
  <files>.planning/verification/iol-client-findings.md</files>
  <read_first>
    - .planning/verification/iol-client-findings.md (the file being modified; F-02 operator-field block lines 28-51, below the `<!-- END AUTO-GENERATED -->` marker at line 35)
    - verification/cycle_report.py (the contract: `_REGRESSION_RE = ^([^:\s]+\.py)::([A-Za-z_][A-Za-z0-9_]*)$`; `_REGRESSION_BULLET_RE` searches for `Regression...<path>.py::<test>`; `_regression_is_resolvable` requires the path to exist under repo root and the file text to contain `def <test_name>(`)
    - .planning/verification/matriz-client-findings.md (analog: F-09 Resolution + Regression operator fields at lines 107-108 — the correct `Regression: packages/.../tests/test_core.py::test_name` token shape)
    - packages/iol-client/tests/test_refresh_token_lifecycle.py (the existing sync regression that asserts `state.token_expires_at > time.time()` after a successful refresh — `test_refresh_token_success_path_rotates`, lines 40-77; this is the in-tree coverage for the F-02 fix domain)
  </read_first>
  <action>
    iol F-02 was dispositioned PROBE_STALE in Phase 11 (the probe at main_iol.py:1289 shadowed the
    PEP 562 forward to `_state.token_expires_at`; the client itself was correct). Its `Regression:`
    operator field is prose ("`main_iol.py` re-run post-fix reporta ...") which `_REGRESSION_BULLET_RE`
    cannot resolve to a `path.py::test_name` token, so `verify_cycle_closure("iol-client")` returns
    `(False, ['F-02'])`. Append a resolvable regression link to the F-02 operator-field block, BELOW the
    `<!-- END AUTO-GENERATED -->` marker (operator-owned region; never hand-edit inside BEGIN/END, per
    HARN-09 / D-05). The client-behavior guarantee underlying the F-02 PROBE_STALE disposition — that
    `_refresh()` updates `_state.token_expires_at` after a successful refresh — is already covered by
    `packages/iol-client/tests/test_refresh_token_lifecycle.py::test_refresh_token_success_path_rotates`.
    Add a `Regression:` bullet whose value is exactly that
    `packages/iol-client/tests/test_refresh_token_lifecycle.py::test_refresh_token_success_path_rotates`
    token (a relative path under repo root, no leading slash, no `..`). Keep the existing prose
    Resolution and Operator-signoff bullets; do NOT alter the F-02 status (stays FIXED) and do NOT touch
    the AUTO-GENERATED zone, the Index table, or any other finding. Add a one-line operator note that
    this Phase 17 link closes the prose-only-Regression gap that left `verify_cycle_closure` FAIL after
    Phase 11. Do NOT re-open or restate terminal findings (D-05). Preserve column alignment and the
    existing operator-field bullet idiom (`**Regression:** ...`).
  </action>
  <verify>
    <automated>uv run python -c "from verification.cycle_report import verify_cycle_closure; r=verify_cycle_closure('iol-client'); print(r); assert r==(True, []), r"</automated>
  </verify>
  <acceptance_criteria>
    - `verify_cycle_closure("iol-client")` returns exactly `(True, [])` (was `(False, ['F-02'])`).
    - All 4 packages PASS: `uv run python -c "from verification.cycle_report import verify_cycle_closure; assert all(verify_cycle_closure(p)==(True,[]) for p in ['ambito-financiero-client','iol-client','higyrus-client','matriz-client'])"` exits 0.
    - The added `Regression:` token resolves: `packages/iol-client/tests/test_refresh_token_lifecycle.py` exists and its text contains `def test_refresh_token_success_path_rotates(`.
    - The `<!-- BEGIN AUTO-GENERATED -->` / `<!-- END AUTO-GENERATED -->` region of iol-client-findings.md is byte-unchanged vs HEAD (only operator-owned lines below END are added): `git diff HEAD -- .planning/verification/iol-client-findings.md` shows additions only below the END marker.
    - F-02 status remains FIXED (Index table + per-finding section unchanged for the status literal).
  </acceptance_criteria>
  <done>
    `verify_cycle_closure × 4` all return `(True, [])`; iol F-02 carries a resolvable
    `path.py::test_name` Regression link; AUTO-GENERATED zone untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Attest the deterministic (credential-free) blocking gates the operator plan will reference</name>
  <files>(read-only verification — no files modified; attestation recorded in 17-01-SUMMARY.md)</files>
  <read_first>
    - .planning/phases/17-final-live-re-verification-4-live-03/17-CONTEXT.md (D-06 static title-stability gate mechanics; baseline anchor 71bf201)
    - .planning/phases/17-final-live-re-verification-4-live-03/17-PATTERNS.md ("Static title-stability diff vs frozen baseline 71bf201" shared pattern; blocking-regressions table rows (a)/(c)/(d))
    - verification/test_sync_async_isolation.py (blocking gate (a): wire URL changes sync vs async)
    - verification/test_logging_no_token_leak.py (blocking gate (c): credential leak in logs)
    - verification/test_public_surface.py (public-surface snapshot drift; 4 packages)
  </read_first>
  <action>
    Run and record the four deterministic gates that do NOT require live credentials, producing the
    evidence the operator plan (17-02) cites in the `17-VALIDATION.md` blocking-regressions table.
    Gate (d) D-06 static title-stability: `git diff 71bf201..HEAD` scoped to `title=`/`fid=`/`class_=`
    literals across the four drivers main_ambito_financiero.py, main_iol.py, main_higyrus.py,
    main_matriz.py — expected result is ZERO changed literal lines (drivers changed +584/-344 lines
    overall but no finding-identity literal moved). Gate (a) sync/async wire-isolation via
    test_sync_async_isolation.py. Gate (c) credential-leak via test_logging_no_token_leak.py. Plus the
    public-surface snapshot via test_public_surface.py and the full-suite collection count (Phase 15
    baseline = 989 collected, 1 deselected). Do NOT modify any source or test; this task is read-only
    verification. Record the pass/fail of each gate in the plan SUMMARY so 17-02 references the
    attestation rather than re-deriving it. If any gate is RED, STOP and surface it — a RED deterministic
    gate means the migration regressed something and the live gate must not proceed.
  </action>
  <verify>
    <automated>test "$(git diff 71bf201..HEAD -- main_ambito_financiero.py main_iol.py main_higyrus.py main_matriz.py | grep -E '^[-+].*(title=|fid=|class_=)' | grep -vcE '^(---|[+][+][+])')" = 0 && uv run pytest verification/test_sync_async_isolation.py verification/test_logging_no_token_leak.py verification/test_public_surface.py -q</automated>
  </verify>
  <acceptance_criteria>
    - D-06 gate: the `git diff 71bf201..HEAD` scoped to `title=`/`fid=`/`class_=` literal lines across the four drivers reports exactly 0 changed literal lines.
    - Blocking gate (a): `uv run pytest verification/test_sync_async_isolation.py -q` exits 0 (GREEN).
    - Blocking gate (c): `uv run pytest verification/test_logging_no_token_leak.py -q` exits 0 (GREEN); no credential sentinel appears in any record.
    - Public surface: `uv run pytest verification/test_public_surface.py -q` exits 0 (4 passed; zero snapshot drift).
    - pytest baseline: `uv run pytest -q --collect-only` reports >= 989 tests collected (Phase 15 baseline; 1 deselected).
    - SUMMARY records each gate's pass/fail verbatim for 17-02 to cite.
  </acceptance_criteria>
  <done>
    All four deterministic gates GREEN at HEAD; pytest collects >= 989; the attestation is
    recorded in the plan SUMMARY for the operator plan (17-02) to reference.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → cycle-closure harness | findings markdown is operator-authored input parsed by `verify_cycle_closure`; no live data crosses here in this plan |
| git history → static diff | the D-06 gate reads committed driver source only; no live API bytes involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | iol-client-findings.md AUTO-GENERATED zone | mitigate | edits restricted to the operator-owned region BELOW `<!-- END AUTO-GENERATED -->` (HARN-09); acceptance asserts BEGIN/END region byte-unchanged |
| T-17-02 | Information Disclosure | findings markdown / SUMMARY | mitigate | this plan touches no live API; no account numbers/balances/credentials are read, written, or logged; the regression link is a static test path |
| T-17-03 | Repudiation | F-02 disposition | accept | F-02 stays FIXED with its existing operator signoff; the added link is additive provenance, not a status revert (D-05 forbids re-opening terminal findings) |
| T-17-SC | Tampering | npm/pip/cargo installs | mitigate | this plan installs NO packages; no package-manager invocation occurs |
</threat_model>

<verification>
- `verify_cycle_closure × 4` all return `(True, [])` at HEAD.
- D-06 static title-stability gate: 0 changed `title=`/`fid=`/`class_=` literals in the 4 drivers.
- Blocking gates (a) sync/async isolation and (c) credential-leak GREEN; public-surface snapshot GREEN.
- `uv run pytest -q --collect-only` reports >= 989 collected.
- iol-client-findings.md AUTO-GENERATED zone byte-unchanged; only operator-owned additions below END marker.
</verification>

<success_criteria>
- iol F-02 carries a resolvable `path.py::test_name` Regression link and `verify_cycle_closure("iol-client")` returns `(True, [])`.
- All 4 packages' cycle closure PASS before the operator runs the live gate (Success Criterion #2 prerequisite).
- The deterministic blocking gates and pytest baseline are attested GREEN in the SUMMARY for 17-02 to consume.
</success_criteria>

<output>
Create `.planning/phases/17-final-live-re-verification-4-live-03/17-01-SUMMARY.md` when done.
Record: the F-02 link change, the `verify_cycle_closure × 4` result, and the four deterministic
gate results (D-06 literal count, isolation, leak, public-surface) + pytest collection count.
</output>