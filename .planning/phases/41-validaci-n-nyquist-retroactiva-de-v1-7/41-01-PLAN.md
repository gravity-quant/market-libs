---
phase: 41
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
  - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "El árbol de fuente de v1.7 está probado idéntico al tag antes de que se escriba el primer artefacto de auditoría (criterio 1)"
    - "Existe un contrato único que las 5 auditorías paralelas consumen: claves de fila, denominador 62, reglas de disposición, forma de front-matter, tabla de enforcement de CI"
    - "Las dos Wave 0 gaps de 41-VALIDATION.md quedan cerradas antes de la primera escritura (marcador de front-matter elegido; denominador fijado en 62)"
    - "Las dos Open Questions de RESEARCH.md quedan resueltas por escrito con su rationale"
  artifacts:
    - path: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      provides: "Contrato compartido de auditoría (identidad de árbol, claves de fila, reglas R-01..R-09, front-matter objetivo, mapa de enforcement de CI)"
      min_lines: 120
      contains: "37a83fe693a303a551f4374f48fe6fc5521804f7"
    - path: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md"
      provides: "Mapa de verificación de la propia Phase 41, alineado a los 7 planes reales; Wave 0 cerrada"
      contains: "wave_0_complete: true"
  key_links:
    - from: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      to: ".planning/milestones/v1.7-phases/3[5-9]-*/3?-VALIDATION.md"
      via: "los planes 41-02..41-06 copian de aquí el SHA, la forma de sección y el front-matter objetivo"
      pattern: "audit_baseline_head"
    - from: ".planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md"
      to: ".github/workflows/ci.yml"
      via: "tabla de lookup de la 4ª columna (D-07): job + rango de líneas por superficie de comando"
      pattern: "ci\\.yml:"
---

<!-- planner-discipline-allow: <row selector> -->
<!-- planner-discipline-allow: pytest 8 -->
<!-- planner-discipline-allow: c4dc6eafdc9e37032c6513f624745b270e2156ec -->

<objective>
Establecer la precondición de atribución (criterio 1) y el contrato compartido que hace que las cinco
auditorías paralelas de la Wave 2 produzcan artefactos consistentes en vez de cinco formatos
divergentes.

Purpose: RESEARCH.md § Workflow Deviations Required encontró que el workflow stock de
`/gsd-validate-phase` viola el criterio 3 por sí solo (§3 pone `nyquist_compliant: true` cuando no hay
gaps, y la medición dice que efectivamente no hay gaps). Ese comportamiento tiene que quedar
prohibido **por escrito** antes de que ningún ejecutor toque un archivo. Además,
`41-VALIDATION.md § Wave 0 Requirements` declara dos gaps que deben cerrarse antes del primer
artefacto o los cinco saldrán inconsistentes.

Output: `41-AUDIT-CONTRACT.md` (contrato autoritativo de la fase) + `41-VALIDATION.md` actualizado.
Cero cambios a fuente de producto. Cero cambios a los cinco `{N}-VALIDATION.md` (eso es Wave 2).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-CONTEXT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Probar el árbol congelado y capturar los dos SHA de atribución</name>
  <files>.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md</files>
  <read_first>
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Code Examples ("Criterion 1: prove the tree is frozen and declare both SHAs") y § Common Pitfalls → Pitfall 6
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-CONTEXT.md § D-02
    - .planning/ROADMAP.md § Phase 41 → Success Criteria (criterio 1)
  </read_first>
  <action>
    Este es el gate Wave 0 de la fase: nada más se escribe hasta que pase.

    Resolver el tag anotado al commit con `git rev-parse v1.7^{commit}`. El valor esperado es
    `37a83fe693a303a551f4374f48fe6fc5521804f7`. NO usar `git rev-parse v1.7` a secas: `v1.7` es un tag
    anotado y esa forma devuelve el objeto-tag `c4dc6eafdc9e37032c6513f624745b270e2156ec`, que haría el
    criterio 1 no re-verificable por terceros (RESEARCH.md Pitfall 6). Si el valor resuelto difiere de
    `37a83fe6...`, ABORTAR el plan y reportarlo — la medición completa de RESEARCH.md queda invalidada.

    Capturar el HEAD de esta sesión con `git rev-parse HEAD`. Este valor pasa a llamarse
    `audit_baseline_head` y es el mismo literal que los cinco artefactos de la Wave 2 declararán; NO se
    re-captura por plan (el HEAD se mueve con cada commit y cinco valores distintos harían el conjunto
    ilegible).

    Ejecutar la prueba de identidad del árbol: `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`.
    Debe salir con código 0. El pathspec de exclusión es obligatorio: `.planning/` churnea
    legítimamente y sin excluirlo la prueba siempre falla. Si sale distinto de 0, ABORTAR: significa
    que fuente de v1.8 aterrizó antes de que la auditoría cerrara, lo que invalida la atribución
    entera (criterio 1) — no continuar ni "documentar la excepción".

    Capturar también las versiones de herramienta con `--version` (uv, pytest vía uv, mypy, ruff, git,
    node) porque las evidencias `VERIFIED-NOW` se producen con ellas y `35-VALIDATION.md` declara una
    versión stale (dice pytest 8.3; el real es 9.0.3 — hallazgo a nombrar, no a corregir en silencio).

    Crear `41-AUDIT-CONTRACT.md` con front-matter (`phase: 41`, `kind: audit-contract`,
    `created: 2026-08-31`) y una primera sección `## 1. Identidad del árbol auditado` que registre, en
    una tabla: `audited_commit_sha` (el commit de v1.7), `audit_baseline_head` (el HEAD capturado),
    `frozen_tree_verified: true` con el comando exacto y su código de salida, y la tabla de versiones
    de herramienta medidas. Escribir explícitamente que criterio 1 es un **invariante continuo**, no un
    gate de una sola vez: se re-verifica al inicio de cada plan de la Wave 2 y otra vez en el cierre
    (41-07).
  </action>
  <verify>
    <automated>test -f .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md && git diff --quiet v1.7 HEAD -- . ':(exclude).planning' && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md && grep -qE 'audit_baseline_head[^0-9a-f]*[0-9a-f]{40}' .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse v1.7^{commit}` devuelve exactamente `37a83fe693a303a551f4374f48fe6fc5521804f7`.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale con código 0.
    - `grep -c '37a83fe693a303a551f4374f48fe6fc5521804f7' 41-AUDIT-CONTRACT.md` es `>= 1`.
    - `grep -cE 'audit_baseline_head[^0-9a-f]*[0-9a-f]{40}' 41-AUDIT-CONTRACT.md` es `>= 1` (40 hex, no abreviado).
    - `grep -c 'c4dc6eafdc9e37032c6513f624745b270e2156ec' 41-AUDIT-CONTRACT.md` es `0` (el SHA del objeto-tag no aparece como SHA declarado).
    - La sección `## 1. Identidad del árbol auditado` existe: `grep -c '^## 1\. Identidad del árbol auditado' 41-AUDIT-CONTRACT.md` es `1`.
  </acceptance_criteria>
  <done>El contrato declara ambos SHA y la prueba de identidad del árbol, con el comando y su código de salida transcritos.</done>
</task>

<task type="execute">
  <name>Task 2: Escribir el contrato de disposición — denominador 62, reglas R-01..R-09, front-matter objetivo, mapa de enforcement de CI</name>
  <files>.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md</files>
  <read_first>
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Row Inventory, § Disposition Decision Rules, § CI Enforcement Map, § Workflow Deviations Required, § Open Questions (1 y 2), § Common Pitfalls 1-7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md (secciones 1-6 de Pattern Assignments y § Shared Patterns)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-CONTEXT.md § D-03, D-04, D-06, D-07, D-09, D-10
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (precedente de sección de auditoría retroactiva)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (precedente multi-tabla)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (modelo de PROSA para explicar un resultado PARTIAL — el valor `partial` NO se adopta)
    - .planning/milestones/v1.7-phases/38-iol-client-auditor-a-de-higyrus-mbito-wallets/38-VALIDATION.md (forma de fila de mapa mejor poblada, 10 columnas)
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Extender `41-AUDIT-CONTRACT.md` con las secciones 2 a 7. Todos los valores de abajo son medidos,
    no estimados: vienen de RESEARCH.md y fueron re-confirmados al planificar.

    `## 2. Denominador y claves de fila (D-03 + D-05)`. El denominador del criterio 2 es **62**, con el
    desglose por fase 13 / 11 / 14 / 9 / 15. Escribir la aritmética explícita. Rechazar por escrito los
    dos denominadores equivocados: los **51** as-declared (pre-D-05, que cuentan la fila placeholder de
    la Phase 35 como una sola unidad) y los **25** criterios de éxito del ROADMAP de v1.7 (D-03 los
    excluye). Fijar el esquema de claves: cada fila de la `## Per-Task Verification Map` recibe la
    clave `{N}-r{NN}` con `NN` = ordinal 1-indexado en el orden del mapa; cada fila de
    `## Manual-Only Verifications` recibe `{N}-m{NN}`. Este esquema es obligatorio porque el mapa de la
    Phase 37 usa Task IDs no únicos (`37-01-xx` aparece 3 veces, `37-03-xx` 5 veces) y sin clave
    ordinal no hay forma de probar "exactamente una disposición por fila". La celda `Row` de la tabla
    de disposición se escribe como `{clave} · {Task ID original}`.

    `## 3. Reglas de disposición (R-01..R-08)`. Escribirlas como reglas mutuamente excluyentes,
    aplicadas en orden, para que ningún ejecutor tenga que juzgar:
    R-01 — comando re-corre hoy, selecciona ≥1 test y sale 0 → `VERIFIED-NOW`.
    R-02 — el `-k` del mapa selecciona 0 tests (pytest sale 5) pero un test con otro nombre cubre la
    misma conducta, y el comando corregido corre verde → `VERIFIED-NOW (comando corregido)`. Aplica
    exactamente dos veces en toda la fase: `37-r11` y `39-r03`. Cualquier **tercera** fila de selección
    vacía que aparezca en ejecución es un hallazgo real: escalar en el SUMMARY, no re-apuntar en
    silencio. Antes de re-apuntar, leer el **cuerpo** del test sustituto, no sólo su nombre
    (RESEARCH.md Assumptions Log A1).
    R-03 — el comando asserta sobre una ruta de `.planning/` que la mudanza de archivo del milestone
    invalidó → `VERIFIED-NOW (ruta corregida)`, corrigiendo la ruta en el comando ejecutado. Aplica a
    `35-r04` y `35-r05`. NO editar `35-02-PLAN.md`: los archivos de plan son registro histórico.
    R-04 — el mapa no declara comando (celda de tipo "planner decide") y la conducta está cubierta por
    un lock existente → `VERIFIED-NOW (comando redactado retroactivamente)`, nombrando en la prosa que
    la fase shipeó con una fila sin contrato. Aplica exactamente una vez: `36-r11`.
    R-05 — el comando es una aserción de paso RED de TDD que se invierte contra el árbol post-GREEN
    (contiene `&& !` sobre un selector que hoy pasa) → `VERIFIED-HISTORICALLY`, citando el SUMMARY de
    plan-time y su commit. Aplica una vez: `35-r01`. Escanear todo comando reconstruido buscando `&& !`
    **antes** de correrlo (RESEARCH.md Pitfall 3).
    R-06 — la conducta es una revisión de documento o un artefacto único con confirmación humana
    fechada y registrada en disco → `VERIFIED-HISTORICALLY`. Aplica a `38-r08`, `38-m01`, `39-r11`.
    R-07 — la conducta exigía una corrida de red en vivo o una ventana de sesión de mercado o un
    checkpoint humano que no se puede re-derivar → `NOT-VERIFIABLE-RETROACTIVELY`. Aplica a las 4 filas
    manual-only de la Phase 39 (`39-m01`..`39-m04`).
    R-08 — ningún comando de esta fase abre un socket. Si una fila sólo se pudiera cerrar corriendo
    red, se dispone por R-06 o R-07, nunca con una corrida en vivo fresca.

    Registrar aquí la resolución de las dos Open Questions de RESEARCH.md, con rationale:
    **OQ#1 (las 4 filas manual-only de la Phase 39)** → se resuelven `NOT-VERIFIABLE-RETROACTIVELY`,
    honrando el ejemplo explícito de D-04 sobre la recomendación alternativa del researcher. Rationale:
    D-04 nombra esas cuatro filas por su nombre como el arquetipo del token; ante un conflicto entre el
    texto de una decisión lockeada y una inferencia posterior, gana la decisión, y en una auditoría la
    dirección segura es sub-declarar, no sobre-declarar (D-10). La evidencia superviviente NO se
    descarta: la celda `Evidence` de cada una de esas cuatro filas debe nombrar el artefacto fechado que
    sí existe (los envelopes de `.planning/verification/run-evidence/{iol,higyrus,ambito-financiero,matriz}-client.json`
    del 2026-08-29, `39-07-SUMMARY.md`, `39-CENSUS.md`, el sign-off del operador citado en
    `39-VALIDATION.md`), calificado como evidencia parcial que no alcanza a re-derivar la conducta.
    **OQ#2 (¿36 y 37 cierran limpias?)** → no. Ver R-09.

    `## 4. Regla de front-matter R-09 (D-09 + criterio 3)`. `nyquist_compliant` pasa de `false` a
    `true` para una fase SÓLO si se cumplen las tres a la vez: (a) las 100% de sus filas disponen
    `VERIFIED-NOW` **plano**, (b) cero filas `VERIFIED-HISTORICALLY` y cero
    `NOT-VERIFIABLE-RETROACTIVELY`, y (c) cero filas con calificador de corrección
    (`comando corregido` / `ruta corregida` / `comando redactado retroactivamente`). Documentar el
    resultado medido: **ninguna de las cinco fases** califica — 35 falla por (b) y (c), 36 por (c)
    (`36-r11`), 37 por (c) (`37-r11`), 38 por (b), 39 por (b) y (c). Por lo tanto las cinco quedan en
    `nyquist_compliant: false`, que coincide con la predicción de D-09 pero llega por evidencia, no por
    decreto. Escribir el front-matter objetivo, idéntico en los cinco archivos salvo el conteo:
    `status` pasa de `draft` a `validated`; `nyquist_compliant` se queda en `false`;
    `not_verifiable_retroactively` es una clave **nueva** con el entero real (35→0, 36→0, 37→0, 38→0,
    39→4) — ésta es la forma en que el criterio 3b se cumple, y se pone en los cinco (no sólo donde es
    distinto de cero) para que el conjunto sea uniforme; `audited_commit_sha`, `audit_baseline_head` y
    `frozen_tree_verified: true` se copian literales de la sección 1; `updated` y `last_audited` son
    `2026-08-31`. Se dejan intactos `phase`, `slug`, `created`, `wave_0_complete` y los dos comentarios
    de lifecycle. NO se adopta el valor `partial` de `07-VALIDATION.md` (D-09 fija `false`); sí se copia
    su **prosa** como modelo para explicar el resultado PARTIAL. NO se copia nada de
    `40-VALIDATION.md`: su `nyquist_compliant: true` lo puso plan-check, no validate-phase, y no tiene
    sección de auditoría (RESEARCH.md § Anti-Patterns).

    `## 5. Forma de la sección de auditoría`. Fijar el esqueleto exacto que los cinco planes de la
    Wave 2 escriben, apéndice al final del archivo precedido de una regla horizontal, después de
    `## Validation Sign-Off` (precedentes `01-VALIDATION.md:106` y `09-VALIDATION.md:327`; la
    colocación de `06` antes del sign-off es la minoritaria y no se sigue). Encabezado exacto:
    `## Validation Audit 2026-08-31`. Orden obligatorio de bloques: (1) párrafo de procedencia de dos
    frases nombrando mecanismo, estado de entrada y "sin subagente"; (2) par de encabezados en negrita
    `**Auditor:**` / `**Árbol auditado:**` con los dos SHA y la prueba de diff vacío; (3) bloque de
    métricas `| Metric | Count |` con las filas `Filas auditadas (denominador)`, `VERIFIED-NOW`,
    `VERIFIED-HISTORICALLY`, `NOT-VERIFIABLE-RETROACTIVELY`, `Correcciones de comando`,
    `Archivos de test nuevos escritos`, `Filas NOT ENFORCED en CI`,
    `Suite de re-ejecución de esta sesión`; (4) `### Disposición por fila` con la tabla de 4 columnas
    `Row | Disposition | Evidence (this session) | CI enforcement surface`, cerrada por la línea de
    leyenda que empieza con `*Disposiciones:`; (5) `### Correcciones de comando` (o `Ninguna.`);
    (6) `### Hallazgos de bookkeeping`; (7) `### Escalaciones`, cuyo cuerpo termina con la frase
    literal `Cero archivos de test nuevos; cero escalaciones.`; (8) línea de veredicto con la forma
    `Veredicto de auditoría: **Phase {N} queda PARTIAL** — status draft → validated,
    nyquist_compliant sigue en false.` El bloque de métricas va SIEMPRE antes de la tabla de
    disposición: el gate aritmético del cierre acota su conteo entre `### Disposición por fila` y la
    línea de leyenda, y si el bloque de métricas quedara dentro de ese rango los conteos se duplicarían.
    La prosa se escribe en español para calzar con los artefactos de v1.7; los encabezados de tabla y
    los tres tokens de disposición se mantienen en inglés (son los literales que el criterio 2 y los
    gates greppean).
    Fijar además la regla de la columna `Status` del mapa preexistente, que hoy dice pendiente en todas
    las filas de las cinco fases: se actualiza a `✅ (VN 2026-08-31)` **sólo** en filas dispuestas
    `VERIFIED-NOW` con re-ejecución de esta sesión y conteo de tests pasados distinto de cero; las
    dispuestas `VERIFIED-HISTORICALLY` pasan a `⬜ histórico` y las `NOT-VERIFIABLE-RETROACTIVELY` a
    `⬜ no re-verificable`. Extender la línea de leyenda de Status con esos dos valores nuevos. Nunca
    poner `✅` en una fila cuyo `Test Type` sea manual o revisión de documento.
    Las casillas preexistentes del `## Validation Sign-Off` se dejan **sin tildar** —
    tildar `nyquist_compliant: true` retroactivamente es exactamente la violación del criterio 3; se
    agrega una sola línea nueva tildada con el resumen de la auditoría, siguiendo la convención de
    `06-VALIDATION.md:170`.

    `## 6. Higiene de la evidencia`. Toda celda `Evidence (this session)` lleva el comando en backticks
    seguido de su línea de resumen. Regla anti-vacuidad (RESEARCH.md Pitfall 2): la línea de resumen
    debe contener un conteo de tests que pasaron distinto de cero; una salida que sólo reporte tests
    descartados sin ninguno ejecutado NO es evidencia y la fila cae en R-02. Regla de seguridad
    (CLAUDE.md): la salida se recorta a la línea de resumen, jamás se pega la salida verbosa completa,
    porque podría arrastrar un host, un token, un id de cuenta o un valor derivado de `.env`.

    `## 7. Mapa de enforcement de CI (4ª columna, D-07)`. Tabla de lookup verificada contra
    `.github/workflows/ci.yml` en HEAD (byte-idéntico al de v1.7): `pytest packages/<pkg>/...` →
    `job test, ci.yml:133-165`; `mypy` sobre src → `job typecheck, ci.yml:122-123`; `mypy` sobre tests
    de paquete → `job typecheck, ci.yml:124-131`; `ruff check` / `ruff format --check` →
    `job lint, ci.yml:36-39`; `lint-imports` → `job lint, ci.yml:40-41`;
    `tools/check_decode_intactness.py` → `job lint, ci.yml:55`;
    `tools/check_uniform_structure.py` → `job lint, ci.yml:60`;
    `tools/check_surface_types.py` → `job lint, ci.yml:66`; los 12 archivos allowlisted de
    `verification/` → `job lint, ci.yml:81-92`; `tools/surface_parity.py` como script → `NOT ENFORCED`
    (no aparece en `ci.yml`; los seis `packages/*/tests/test_surface_parity.py` sí corren en
    `job test`); los otros 40 de los 52 `verification/test_*.py` → `NOT ENFORCED`;
    `verification/regen_snapshots.py` más su `git diff` → `NOT ENFORCED`; revisión de documento,
    checkpoint humano y corrida de driver en vivo → `NOT ENFORCED (por naturaleza)`. Antes de escribir
    la tabla, re-verificar los números de línea contra el `ci.yml` actual y corregirlos si el archivo
    se movió; los valores de arriba son la medición de esta sesión, no un dogma.

    `## 8. Prohibiciones del workflow`. Tres desviaciones obligatorias respecto del comportamiento
    stock de `/gsd-validate-phase`, con la advertencia de que la copia project-local en
    `.claude/gsd-core/workflows/validate-phase.md` es la que gana sobre la de usuario:
    (a) nunca spawnear `gsd-nyquist-auditor` ni tomar la rama de "fix gaps" — la auditoría lee y
    dispone, no repara (D-06a); (b) resolver requirements y roadmap contra
    `.planning/milestones/v1.7-REQUIREMENTS.md` y `.planning/milestones/v1.7-ROADMAP.md`, nunca contra
    los archivos raíz homónimos, que ya contienen el roster de v1.8 y no resuelven los IDs `NOBJ-*` que
    las fases 35-39 referencian (D-06b); (c) la instrucción de §3 que pone `nyquist_compliant: true`
    cuando no hay gaps queda **deshabilitada** — el flag sólo cambia bajo R-09, y la medición dice que
    R-09 no se satisface en ninguna de las cinco. Anotar además que §6 de la copia project-local NO
    trae la instrucción de poner `status: validated`, así que esa transición es trabajo explícito de
    cada plan, no un efecto colateral del workflow.
  </action>
  <verify>
    <automated>C=.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md; for s in '^## 2\.' '^## 3\.' '^## 4\.' '^## 5\.' '^## 6\.' '^## 7\.' '^## 8\.'; do grep -qE "$s" "$C" || { echo "MISSING $s"; exit 1; }; done; grep -q '13 / 11 / 14 / 9 / 15' "$C" && grep -q 'not_verifiable_retroactively' "$C" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$C" && grep -q 'ci.yml:81-92' "$C" && test "$(grep -cE 'R-0[1-9]' "$C")" -ge 9</automated>
  </verify>
  <acceptance_criteria>
    - Las ocho secciones numeradas existen: `grep -cE '^## [1-8]\.' 41-AUDIT-CONTRACT.md` es `8`.
    - El denominador aparece con su desglose: `grep -c '13 / 11 / 14 / 9 / 15' 41-AUDIT-CONTRACT.md` es `>= 1` y el literal `62` aparece como total.
    - Las nueve reglas están presentes: `grep -oE 'R-0[1-9]' 41-AUDIT-CONTRACT.md | sort -u | wc -l` es `9`.
    - La clave de front-matter nueva está definida: `grep -c 'not_verifiable_retroactively' 41-AUDIT-CONTRACT.md` es `>= 1`, y los cinco valores por fase (35→0, 36→0, 37→0, 38→0, 39→4) aparecen en la sección 4.
    - El literal de cierre de D-08 está fijado: `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 41-AUDIT-CONTRACT.md` es `>= 1`.
    - El mapa de CI cita rangos de línea reales: `grep -c 'ci.yml:81-92' 41-AUDIT-CONTRACT.md` es `>= 1` y `grep -c 'NOT ENFORCED' 41-AUDIT-CONTRACT.md` es `>= 4`.
    - Los rangos de línea declarados en la sección 7 se re-verifican contra el archivo real: `grep -n 'verification/test_main_market_data_deep_chain.py' .github/workflows/ci.yml` cae dentro del rango declarado para el allowlist.
    - Las resoluciones de OQ#1 y OQ#2 están escritas con rationale: la sección 3 nombra las cuatro filas `39-m01`..`39-m04` y la sección 4 explica por qué ninguna de las cinco fases satisface R-09.
    - `grep -c 'partial' 41-AUDIT-CONTRACT.md` sólo aparece en el contexto de "no se adopta" / prosa modelo, nunca como valor prescrito de `nyquist_compliant`.
  </acceptance_criteria>
  <done>El contrato fija denominador, claves de fila, nueve reglas de disposición, front-matter objetivo, esqueleto de sección, higiene de evidencia, mapa de enforcement y las tres prohibiciones de workflow. Un ejecutor de la Wave 2 puede producir su artefacto leyendo sólo este archivo más su propio `{N}-VALIDATION.md`.</done>
</task>

<task type="execute">
  <name>Task 3: Cerrar las dos Wave 0 gaps de 41-VALIDATION.md y realinear su mapa a los 7 planes reales</name>
  <files>.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md</files>
  <read_first>
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md (el archivo completo — mapa sembrado, Wave 0 Requirements, Manual-Only, Sign-Off)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md (producido en las tareas 1 y 2)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Validation Architecture → Wave 0 Gaps y § Phase Requirements → Test Map
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-CONTEXT.md § D-10
  </read_first>
  <action>
    `41-VALIDATION.md` fue sembrado por plan-phase con un mapa de 8 filas que asume una estructura de
    planes distinta a la real. Realinearlo, y cerrar sus dos Wave 0 gaps, que por su propio texto deben
    resolverse antes de que se escriba el primer artefacto.

    Reescribir `## Per-Task Verification Map` para que sus filas correspondan uno a uno con las tareas
    reales de los siete planes: `41-01-01` (invariante de árbol congelado, wave 1), `41-01-02`
    (contrato de disposición, wave 1), `41-01-03` (esta tarea, wave 1), `41-02-01`/`41-02-02`
    (reconstrucción y disposición de la Phase 35, wave 2), `41-03-01`/`41-03-02` (Phase 36, wave 2),
    `41-04-01`/`41-04-02` (Phase 37, wave 2), `41-05-01`/`41-05-02` (Phase 38, wave 2),
    `41-06-01`/`41-06-02` (Phase 39, wave 2), `41-07-01` (cierre aritmético de 62), `41-07-02`
    (criterios 4 y 5: cero locks nuevos, contención de alcance) y `41-07-03` (rollup + auto-auditoría),
    todos wave 3. Conservar las 10 columnas y el vocabulario existentes (`—`/`N/A` de relleno, comando
    en backticks, `✅`/`❌ W0`/`❌ manual` en File Exists, y la línea de leyenda de Status). En la
    columna `Automated Command` poner el comando real de cada tarea (los que están en los bloques
    `<verify><automated>` de los siete planes), no un placeholder con `<row selector>`.

    Cerrar la Wave 0 gap #1 tildándola y registrando la decisión: el marcador de front-matter para
    "esta fase conserva ítems NOT-VERIFIABLE-RETROACTIVELY" (criterio 3b) es la clave entera
    `not_verifiable_retroactively`, presente en los cinco archivos con su valor real; se descartó
    `nyquist_compliant: partial` porque D-09 fija el valor `false` y PATTERNS.md marca `partial` como
    un one-off del repo que no se adopta como valor.

    Cerrar la Wave 0 gap #2 tildándola y fijando el denominador **62** con el desglose
    13 / 11 / 14 / 9 / 15 dentro del propio `41-VALIDATION.md`, para que el criterio 2 tenga
    denominador declarado en el artefacto de validación de esta fase y no sólo en el contrato.

    Poner `wave_0_complete: true` en el front-matter (las dos gaps quedan cerradas por esta tarea).
    NO tocar `status` ni `nyquist_compliant` de este archivo: por D-10, `41-VALIDATION.md` se sostiene
    con la misma vara que los cinco que audita, y su propia disposición se escribe en 41-07 con
    evidencia re-ejecutada, no aquí.

    Corregir también la tabla `## Test Infrastructure`, que dice pytest 8.x: las versiones medidas son
    pytest 9.0.3, mypy 1.20.2, ruff 0.15.12, uv 0.11.3 (capturadas en la sección 1 del contrato). Y
    actualizar `## Manual-Only Verifications` para que refleje la resolución de OQ#1 ya tomada: las 4
    filas manual-only de la Phase 39 se disponen `NOT-VERIFIABLE-RETROACTIVELY` por R-07, con la
    evidencia parcial nombrada — la fila ya no dice "asignar uno u otro por fila".
  </action>
  <verify>
    <automated>V=.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md; grep -q 'wave_0_complete: true' "$V" && grep -q 'not_verifiable_retroactively' "$V" && grep -q '13 / 11 / 14 / 9 / 15' "$V" && test "$(grep -c '^| 41-0[1-7]-0[0-9] ' "$V")" -eq 14 && ! grep -q '<row selector>' "$V" && ! grep -q 'pytest 8' "$V"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'wave_0_complete: true' 41-VALIDATION.md` es `1`.
    - `grep -c '^| 41-0[1-7]-0[0-9] ' 41-VALIDATION.md` es `14` (3 + 2 + 2 + 2 + 2 + 2 + 3 = 14 tareas reales).
    - `grep -c '<row selector>' 41-VALIDATION.md` es `0` (ningún comando placeholder sobrevive).
    - `grep -c 'pytest 8' 41-VALIDATION.md` es `0` y `grep -c '9.0.3' 41-VALIDATION.md` es `>= 1`.
    - Las dos Wave 0 gaps quedan tildadas: `grep -c '^- \[x\]' 41-VALIDATION.md` en la sección `## Wave 0 Requirements` es `>= 2`, y ninguna línea `- [ ]` sobrevive en esa sección.
    - `grep -c '13 / 11 / 14 / 9 / 15' 41-VALIDATION.md` es `>= 1`.
    - `grep -c '^status: draft' 41-VALIDATION.md` sigue siendo `1` y `grep -c '^nyquist_compliant: false' 41-VALIDATION.md` sigue siendo `1` (la auto-disposición es trabajo de 41-07, no de aquí).
  </acceptance_criteria>
  <done>El artefacto de validación de la propia Phase 41 describe los siete planes reales, declara el denominador 62 y el marcador de front-matter elegido, y sus dos Wave 0 gaps están cerradas antes de que la Wave 2 escriba nada.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producidos por este plan:
- `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md` — **nuevo**. Contrato autoritativo de la fase: identidad del árbol (dos SHA + prueba de diff vacío), denominador 62 con desglose 13/11/14/9/15, esquema de claves `{N}-r{NN}`/`{N}-m{NN}`, reglas de disposición R-01..R-08, regla de front-matter R-09, esqueleto de la sección `## Validation Audit 2026-08-31`, higiene de evidencia, mapa de enforcement de CI (D-07), y las tres prohibiciones de workflow.
- `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-VALIDATION.md` — **modificado**. Mapa realineado a los 7 planes / 14 tareas reales, `wave_0_complete: true`, denominador declarado, versiones de herramienta corregidas.

NO producidos por este plan (y por diseño): cero archivos `.py`, cero ediciones a `.github/workflows/ci.yml`, cero ediciones a los cinco `{N}-VALIDATION.md` de v1.7, cero ediciones a archivos de plan históricos de las fases 35-39.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de comando local → markdown committeado | Único cruce real: lo que este plan transcribe a disco queda en el repo para siempre |

Ninguna otra frontera se cruza: este plan no abre sockets, no lee credenciales, no parsea input no
confiable, no añade dependencias y no toca fuente de producto.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Information Disclosure | transcripción de salida de `--version` y de `git` a `41-AUDIT-CONTRACT.md` | mitigate | Sección 6 del contrato fija la regla: sólo líneas de resumen, nunca salida verbosa; ningún comando de este plan lee `.env` ni abre red (ASVS V7) |
| T-41-02 | Repudiation | `audited_commit_sha` mal resuelto desde el tag anotado | mitigate | Tarea 1 exige `git rev-parse v1.7^{commit}` y un criterio de aceptación que verifica que el SHA del objeto-tag NO aparece como SHA declarado |
| T-41-03 | Repudiation | falsa garantía por `nyquist_compliant` flipeado mecánicamente aguas abajo (`audit-milestone` lee ese flag) | mitigate | Regla R-09 escrita en el contrato + sección 8 que deshabilita explícitamente la rama §3 del workflow project-local |
| T-41-SC | Tampering | instalaciones de paquetes | accept | Cero: este plan no invoca `npm`/`pip`/`cargo`/`uv add`. `uv.lock` y `pyproject.toml` quedan byte-idénticos (RESEARCH.md § Package Legitimacy Audit: no aplica) |

**Categorías STRIDE no aplicables:** Spoofing, Tampering (fuera del renglón de paquetes) y Denial of
Service no tienen superficie aquí — el plan escribe dos archivos markdown bajo `.planning/` y no
expone ningún endpoint, proceso ni credencial. ASVS L1 V2/V3/V4/V5/V6 no aplican (sin auth, sesión,
control de acceso, input no confiable ni cripto).
</threat_model>

<verification>
1. `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 (invariante de criterio 1 sigue en pie al cerrar el plan).
2. `git status --porcelain` no lista ningún archivo fuera de `.planning/phases/41-*/`.
3. `git status --porcelain verification/` está vacío y `ls verification/test_*.py | wc -l` sigue en 52.
4. `41-AUDIT-CONTRACT.md` tiene las 8 secciones numeradas y las 9 reglas R-01..R-09.
5. `41-VALIDATION.md` tiene `wave_0_complete: true` y 14 filas de tarea.
</verification>

<success_criteria>
- El árbol de fuente de v1.7 está probado idéntico al tag `v1.7` (commit `37a83fe6...`) y ambos SHA quedan declarados.
- Un ejecutor de la Wave 2 puede producir su artefacto sin tomar ninguna decisión de formato ni de disposición: todas están escritas en el contrato.
- Las tres prohibiciones de workflow (no spawnear el auditor, resolver contra los archivos de v1.7, no flipear el flag por ausencia de gaps) están escritas, no implícitas.
- Las dos Open Questions de RESEARCH.md están resueltas con rationale escrito.
- Cero archivos fuera de `.planning/phases/41-*/` modificados.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md` when done.
</output>
