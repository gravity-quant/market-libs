---
phase: 13-cross-package-ergonomics-with-options-max-retries-n
plan: 04
type: execute
wave: 4
depends_on: [13-03]
files_modified:
  - packages/matriz-client/src/matriz_client/_state.py
  - packages/matriz-client/src/matriz_client/client.py
  - packages/matriz-client/src/matriz_client/aio.py
  - packages/matriz-client/tests/test_client.py
  - packages/matriz-client/tests/test_async_client.py
  - packages/matriz-client/tests/test_with_options.py
  - verification/snapshots/matriz-client-surface.txt
autonomous: true
requirements: [ERG-01]
requirements_addressed: [ERG-01]

must_haves:
  truths:
    - "matriz_client.Client.with_options(max_retries=N) returns view that shares _state.token_store (3-way concurrency primitive Phase 10 unchanged)"
    - "matriz_client.AsyncClient.with_options(max_retries=N) async mirror"
    - "view.close() / view.aclose() are no-op — parent's http_client + token + token_store intact"
    - "CRITICAL: view.new_order(...) on matriz Primary executes EXACTLY 1 outgoing request under 503 (anti-Pitfall 14; SC#2 ROADMAP merge gate)"
    - "matriz-only field _state.client_max_retries: int = 2 added; Client.__init__ + AsyncClient.__init__ set it from max_retries arg (D-T3)"
    - "_ensure_token() (sync + async) reads state.client_max_retries (NOT self._max_retries) when calling build_token_store (D-T3)"
    - "view does NOT rebind TokenStore to view._max_retries — TokenStore retry cap stays at parent's client_max_retries (D-T1; matriz-specific test)"
    - "TokenStore 3-way concurrency (threading.Lock + asyncio.Lock per loop) UNCHANGED — view shares state.token_store (D-T2)"
    - "Risk API auth_basic flow (extensions['auth_basic']) preserved; view's max_attempts also flows through auth_basic requests"
    - "Cross-cutting test_with_options_does_not_bypass_mutation_gate_matriz flips RED→GREEN in this plan (CRITICAL merge gate)"
    - "All 4 cross-cutting test_with_options_*[matriz_client] rows GREEN at end of plan"
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_state.py"
      provides: "_ClientState with new client_max_retries: int = 2 field (D-T3)"
      contains: "client_max_retries: int"
    - path: "packages/matriz-client/src/matriz_client/client.py"
      provides: "Client.with_options + _is_view + close() no-op + _request extensions['max_attempts'] + __init__ sets state.client_max_retries + _ensure_token reads state.client_max_retries"
      contains: "def with_options"
    - path: "packages/matriz-client/src/matriz_client/aio.py"
      provides: "AsyncClient.with_options + async _aensure_token reads state.client_max_retries (D-T3 async mirror)"
      contains: "def with_options"
    - path: "packages/matriz-client/tests/test_with_options.py"
      provides: "Matriz-specific test_with_options_does_not_rebind_tokenstore_max_retries (D-T5)"
      contains: "test_with_options_does_not_rebind_tokenstore_max_retries"
    - path: "packages/matriz-client/tests/test_client.py"
      provides: "Per-package mocked tests (close-is-noop, chaining, repr-prefix, invalid args)"
    - path: "packages/matriz-client/tests/test_async_client.py"
      provides: "Async-mirror per-package tests"
    - path: "verification/snapshots/matriz-client-surface.txt"
      provides: "Snapshot regenerated (D-P4 atomicity)"
  key_links:
    - from: "Client.__init__ / AsyncClient.__init__"
      to: "_state.client_max_retries"
      via: "self._state.client_max_retries = max_retries assignment"
      pattern: "self\\._state\\.client_max_retries = max_retries"
    - from: "Client._ensure_token (line 253) + AsyncClient._aensure_token (line ~306)"
      to: "build_token_store(state, max_retries=state.client_max_retries)"
      via: "TokenStore parametrized by state.client_max_retries, NOT self._max_retries"
      pattern: "build_token_store\\(self\\._state, max_retries=self\\._state\\.client_max_retries\\)"
    - from: "view.new_order(...)"
      to: "RetryTransport.handle_request mutation gate (idempotent=False → pass-through)"
      via: "spec.idempotent=False set by _core.build_new_order_request → req.extensions['idempotent']=False → transport skips Retrying loop"
      pattern: "if not request\\.extensions\\.get\\(\"idempotent\", False\\)"
---

<objective>
Land `with_options(max_retries=N)` on `matriz_client.Client` and `matriz_client.AsyncClient` — the third package in the per-package serial roll-out, and the most complex due to:

1. **TokenStore 3-way concurrency primitive (Phase 10)** — view shares `_state.token_store`; view's `_max_retries` must NOT rebind the TokenStore retry cap. Per D-T1: HTTP-only override; auth-server retries (TokenStore + RefreshPolicy) stay gobernado por the Client constructor.
2. **CRITICAL merge gate (Anti-Pitfall 14)** — `view.new_order(...)` under 503 must execute EXACTLY 1 outgoing request. Money-on-the-line. This is the SC#2 ROADMAP merge condition and the single most important assertion in Phase 13.
3. **D-T3 new field `_state.client_max_retries: int`** — matriz-only; `_ensure_token`/`_aensure_token` consume it instead of `self._max_retries` so view's HTTP-level cap stays isolated from TokenStore's auth-server cap.
4. **D-T5 matriz-specific test** — `test_with_options_does_not_rebind_tokenstore_max_retries` in `packages/matriz-client/tests/test_with_options.py` (NEW file).
5. **Risk API auth_basic flow** — preserved; view's `max_attempts` propagates through.

Purpose: matriz is the package where Anti-Pitfall 14 (duplicate-order) can materialize as money-on-the-line risk. This plan is the merge gate of Phase 13.

Output: matriz sync + async `with_options` + `_state.client_max_retries` field + `_ensure_token` rebind + per-package mocked tests + D-T5 TokenStore isolation test + CRITICAL mutation-gate cross-cutting test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-03-SUMMARY.md
@.planning/milestones/v1.1-phases/10-matriz-aio-py-creation-tokenstore/10-CONTEXT.md
@.claude/skills/spike-findings-market-libs/SKILL.md
@packages/matriz-client/src/matriz_client/_state.py
@packages/matriz-client/src/matriz_client/_token_store.py
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/aio.py
@verification/test_with_options.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add client_max_retries field to _state.py + wire to __init__ + rebind _ensure_token (sync + async)</name>
  <files>
    packages/matriz-client/src/matriz_client/_state.py,
    packages/matriz-client/src/matriz_client/client.py,
    packages/matriz-client/src/matriz_client/aio.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-T1, D-T2, D-T3, D-T4; `<code_context>` Integration Points for matriz; `<specifics>` D-T3 LOC delta)
    .planning/milestones/v1.1-phases/10-matriz-aio-py-creation-tokenstore/10-CONTEXT.md (TokenStore 3-way primitive design; `_aensure_token` double-checked locking; `build_token_store(state, *, max_retries)` shape)
    .claude/skills/spike-findings-market-libs/SKILL.md (TokenStore + RefreshPolicy validated patterns — must remain accurate after Plan 4)
    packages/matriz-client/src/matriz_client/_state.py (full file: 65 LOC; add 1 field)
    packages/matriz-client/src/matriz_client/_token_store.py (line 146 `build_token_store(state, *, max_retries)` signature — confirms the rebind shape)
    packages/matriz-client/src/matriz_client/client.py (lines 125-160: __init__; line 234-260: _ensure_token; especially line 253 `build_token_store(...)` call)
    packages/matriz-client/src/matriz_client/aio.py (lines 140-180: __init__; line 251-313: _aensure_token; especially line 305-307 `build_token_store(...)` call)
  </read_first>
  <behavior>
    - `_state.client_max_retries: int = 2` field exists (matriz-only).
    - `Client(max_retries=2)` → `self._state.client_max_retries == 2`.
    - `Client(max_retries=5)` → `self._state.client_max_retries == 5`.
    - `AsyncClient(max_retries=5)` → mirror.
    - `_ensure_token()` (sync) calls `build_token_store(self._state, max_retries=self._state.client_max_retries)` (NOT `self._max_retries`).
    - `_aensure_token()` (async) mirror.
    - View created via `with_options(max_retries=10)` does NOT change `state.client_max_retries` (stays at 2 if constructor was 2). View's `_max_retries=10` ONLY affects `req.extensions["max_attempts"]` for HTTP calls.
    - Existing Phase 10 tests (TokenStore 3-way concurrency, `_aensure_token` double-checked locking) stay GREEN.
  </behavior>
  <action>
    1. **`packages/matriz-client/src/matriz_client/_state.py`**:

       Add `client_max_retries: int = 2` field to `_ClientState` dataclass (after `token_store: TokenStore | None = None`, line 64). Add comment: `# Phase 13 D-T3: TokenStore retry cap source (separate from HTTP-level _max_retries which view can override).`

       Constraint: ≤ +2 LOC.

    2. **`packages/matriz-client/src/matriz_client/client.py`**:

       (a) In `Client.__init__` (lines 125-160), after `self._max_retries = max_retries` (line 153), add:

       `self._state.client_max_retries = max_retries  # Phase 13 D-T3: TokenStore retry cap (separate from HTTP-level override via with_options).`

       Constraint: ≤ +1 LOC.

       (b) In `Client._ensure_token` (around line 253), change the call:

       FROM: `self._state.token_store = build_token_store(self._state, max_retries=self._max_retries)`

       TO: `self._state.token_store = build_token_store(self._state, max_retries=self._state.client_max_retries)`

       Reason: when called from a view, `self._max_retries` would be the view's override (e.g., 10), but we want the TokenStore retry cap to stay at the parent's constructor value (state.client_max_retries=2). Per D-T1.

       Constraint: 1 LOC replacement.

    3. **`packages/matriz-client/src/matriz_client/aio.py`**:

       (a) In `AsyncClient.__init__` (around line 167), after `self._max_retries = max_retries`, add:

       `self._state.client_max_retries = max_retries  # Phase 13 D-T3 mirror sync.`

       (b) In `AsyncClient._aensure_token` (around lines 305-307), change:

       FROM: `self._state.token_store = build_token_store(self._state, max_retries=self._max_retries)`

       TO: `self._state.token_store = build_token_store(self._state, max_retries=self._state.client_max_retries)`

    4. **`configure()` parity check** (matriz `configure()` at line 580 sync + line 623 async): when `configure(max_retries=N)` is called, it should also update `_state.client_max_retries` of the new `_default_client`. Read the existing `configure()` body and confirm: if it replaces `_default_client` with a new `Client(max_retries=N)`, the new Client's `__init__` sets `_state.client_max_retries` automatically (no change needed). If `configure()` mutates an existing client's `_max_retries` without recreating the Client, add the equivalent mutation for `state.client_max_retries`. Verify the actual existing pattern.

    5. **DO NOT touch `_token_store.py`** — the TokenStore primitive (3-way concurrency, RefreshPolicy) is the Phase 10 validated pattern. Plan 4 ONLY changes the SOURCE of the `max_retries` arg (from `self._max_retries` to `self._state.client_max_retries`).

    6. **DO NOT touch `ws_client.py`** — out of Phase 13 scope (CONTEXT.md `<deferred>`: "WebSocket layer queda intacta").

    7. **DO NOT touch `_transport.py` / `_atransport.py`** — already modified in Plan 1.

    Run `uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q` to confirm Phase 10 TokenStore tests stay GREEN. Specifically check `test_async_token_store_concurrent_access`, `test_token_store_3way_sharing` (or whatever the existing 3-way tests are named).
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/matriz-client/</automated>
    <automated>uv run mypy --strict packages/matriz-client/src</automated>
    <automated>uv run pytest verification/test_retry_mutation_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "client_max_retries: int" packages/matriz-client/src/matriz_client/_state.py` returns 1.
    - Source: `grep -c "self._state.client_max_retries = max_retries" packages/matriz-client/src/matriz_client/client.py` returns ≥ 1 (in __init__; also possibly in configure()).
    - Source: `grep -c "self._state.client_max_retries = max_retries" packages/matriz-client/src/matriz_client/aio.py` returns ≥ 1.
    - Source: `grep -c "build_token_store(self._state, max_retries=self._state.client_max_retries)" packages/matriz-client/src/matriz_client/client.py` returns 1.
    - Source: `grep -c "build_token_store(self._state, max_retries=self._state.client_max_retries)" packages/matriz-client/src/matriz_client/aio.py` returns 1.
    - Source (no regression in old call): `grep -c "build_token_store(self._state, max_retries=self._max_retries)" packages/matriz-client/src/matriz_client/client.py` returns 0 (replaced).
    - Source (no regression in old call): `grep -c "build_token_store(self._state, max_retries=self._max_retries)" packages/matriz-client/src/matriz_client/aio.py` returns 0 (replaced).
    - Field default: `python -c "from matriz_client._state import _ClientState; assert _ClientState().client_max_retries == 2"` exits 0.
    - Behavior (existing Phase 10 tests GREEN): `uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q exits 0`.
    - Behavior (Phase 8 mutation-gate cross-cutting still GREEN — matriz new_order under 503 = 1 request): `uv run pytest verification/test_retry_mutation_gate.py -x -q exits 0`.
    - Quality: `uv run ruff check packages/matriz-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/matriz-client/src exits 0`.
  </acceptance_criteria>
  <done>
    `_state.client_max_retries` field added; `Client.__init__` + `AsyncClient.__init__` set it from `max_retries` arg; `_ensure_token` + `_aensure_token` read from `state.client_max_retries` (not `self._max_retries`). Existing Phase 10 TokenStore tests + Phase 8 mutation-gate cross-cutting still GREEN. Ruff + mypy strict GREEN. TokenStore 3-way concurrency primitive UNCHANGED (no logic shift in `_token_store.py`).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Client.with_options + AsyncClient.with_options to matriz (sync + async; mirror Plans 2-3 + auth_basic)</name>
  <files>
    packages/matriz-client/src/matriz_client/client.py,
    packages/matriz-client/src/matriz_client/aio.py
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md (ámbito reference)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-03-SUMMARY.md (higyrus reference — _send_auth_request extension write)
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-V1, D-V2, D-V3, D-T6 — auth_basic flow)
    packages/matriz-client/src/matriz_client/client.py (current Client shape: __slots__ line 125, __init__ 127-160, close around close(), _request 267-310 — note `extensions["auth_basic"]` for Risk API at line 307; account_id at line 304)
    packages/matriz-client/src/matriz_client/aio.py (current AsyncClient shape; mirrors sync per Phase 10)
    packages/higyrus-client/src/higyrus_client/client.py (Plan 3 reference for _send_auth_request extension write idiom)
  </read_first>
  <behavior>
    - Standard `with_options` shape: shares `_state`, sets `_is_view=True`, returns fresh `Client`/`AsyncClient`.
    - `view._state.token_store is parent._state.token_store` (anti-Pitfall 13 extended to TokenStore — D-T2).
    - View's `_max_retries=10` does NOT propagate to TokenStore — TokenStore retry cap stays at parent's `state.client_max_retries=2` (D-T1).
    - `view.new_order(...)` under 503 mock: EXACTLY 1 outgoing request (mutation gate Anti-Pitfall 14 — money-on-the-line).
    - `view.get_segments()` under 503 mock with `max_retries=10`: 11 outgoing requests (idempotent GET path; extension override honored).
    - `view._send_auth_request(login_spec)` propagates `extensions["max_attempts"]` (login is `idempotent=True` per Phase 8 D-03; D-T6).
    - Risk API path: `view.<risk_api_endpoint>(...)` propagates `extensions["auth_basic"]` AND `extensions["max_attempts"]` (uniform).
  </behavior>
  <action>
    **`packages/matriz-client/src/matriz_client/client.py`** (mirror Plans 2-3 + matriz-specific):

    1. Add `_is_view` to `Client.__slots__` (alphabetically sorted with `_max_retries`, `_state`).

    2. Set `self._is_view = False` in `__init__` after `self._max_retries = max_retries` (and after the new `self._state.client_max_retries = max_retries` from Task 1).

    3. Add `Client.with_options` method:

       Signature: `def with_options(self, *, max_retries: int) -> Self:`
       Body (per `<specifics>` View constructor pattern):
         - `_validate_max_retries(max_retries)`
         - `view = type(self).__new__(type(self))`
         - `view._state = self._state` (SHARE — includes `token_store` per D-T2 — anti-Pitfall 13 + Pitfall 14)
         - `view._max_retries = max_retries` (HTTP-only override — D-T1)
         - `view._is_view = True`
         - `return view`

       Docstring: ~20 lines covering:
         - Per `<specifics>` View constructor pattern docstring base.
         - **CRITICAL** mention: "On matriz, calling `view.new_order(...)` does NOT bypass the mutation gate — non-idempotent requests still execute exactly 1 outgoing request regardless of view's max_retries. The mutation gate (Phase 8 D-01) is the absolute authority on retry permission. Use view's max_retries for idempotent GET endpoints (e.g., `get_segments`, `get_accounts`, `get_market_data_snapshot`) that benefit from retry tightening."
         - **D-T1 mention**: "View's max_retries is HTTP-only; the TokenStore retry cap (auth-server retries) stays gobernado por the Client constructor (`state.client_max_retries`). Calling `view._ensure_token()` does NOT rebind the TokenStore."

    4. Modify `Client.close()` to no-op when `_is_view=True`: add `if getattr(self, "_is_view", False): return` at top.

    5. Modify `Client._request()` shell (lines 267-310): add `req.extensions["max_attempts"] = self._max_retries + 1` after `req.extensions["endpoint_name"] = spec.endpoint_name` (line 302) — uniform with existing `idempotent`/`request_id`/`endpoint_name` extensions, ABOVE the conditional `account_id` and `auth_basic` extensions.

    6. Modify `Client._send_auth_request()` (around lines 217-228 per `<code_context>` Integration Points): add same `req.extensions["max_attempts"] = self._max_retries + 1` after `req.extensions["endpoint_name"]` (line 227). Per D-T6: auth requests are `idempotent=True` so override is honored.

    7. Modify `__repr__` to prefix `"view of "` when `_is_view=True`. Matriz repr redacts password and token (Phase 6 D-18) — preserve.

    8. DO NOT change `__all__`. DO NOT add new imports.

    **`packages/matriz-client/src/matriz_client/aio.py`** (mirror sync):

    1. Add `_is_view` to `AsyncClient.__slots__`.

    2. Set `self._is_view = False` in `__init__`.

    3. Add `AsyncClient.with_options` method (sync method on async class; same body as sync Client.with_options).

       Docstring includes async-specific note: `view = c.with_options(5); result = await view.get_segments()`.

    4. Modify `aclose()` to no-op when `_is_view=True`.

    5. Modify async `_request()` shell: add `req.extensions["max_attempts"]` line, same position as sync.

    6. Modify async `_send_auth_request()` if exists (read aio.py to confirm; Phase 10 created the async auth path).

    7. `__repr__` prefix.

    8. DO NOT change `__all__`. DO NOT add new imports.

    Constraint: total diff to `client.py` ≤ +40 LOC, `aio.py` ≤ +35 LOC.

    Run `uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q` to confirm Phase 10 tests stay GREEN.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q</automated>
    <automated>uv run ruff check packages/matriz-client/</automated>
    <automated>uv run mypy --strict packages/matriz-client/src</automated>
    <automated>uv run pytest verification/test_with_options.py -k "matriz_client" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def with_options" packages/matriz-client/src/matriz_client/client.py` returns 1.
    - Source: `grep -c "def with_options" packages/matriz-client/src/matriz_client/aio.py` returns 1.
    - Source: `grep -c "_is_view" packages/matriz-client/src/matriz_client/client.py` returns ≥ 4.
    - Source: `grep -c "_is_view" packages/matriz-client/src/matriz_client/aio.py` returns ≥ 4.
    - Source (extension set in _request + _send_auth_request): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/matriz-client/src/matriz_client/client.py` returns ≥ 2.
    - Source (async mirror): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/matriz-client/src/matriz_client/aio.py` returns ≥ 2 (assuming async _send_auth_request from Phase 10).
    - Slot correctness: `python -c "from matriz_client import Client, AsyncClient; assert '_is_view' in Client.__slots__ and '_is_view' in AsyncClient.__slots__"` exits 0.
    - Behavior: `uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q exits 0` (Phase 6-10 tests stay GREEN).
    - **CRITICAL merge gate GREEN**: `uv run pytest verification/test_with_options.py::test_with_options_does_not_bypass_mutation_gate_matriz -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_shares_http_client_and_token -k matriz_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_max_attempts_extension_honored -k matriz_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_chaining_inner_wins -k matriz_client -x -q exits 0`.
    - Quality: `uv run ruff check packages/matriz-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/matriz-client/src exits 0`.
  </acceptance_criteria>
  <done>
    matriz `Client.with_options` + `AsyncClient.with_options` added with `_is_view` slot, lifecycle no-op, `_request` + `_send_auth_request` shells set `extensions["max_attempts"]`. ALL 4 cross-cutting tests for matriz row GREEN — including the CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz` (SC#2 ROADMAP merge gate). Existing Phase 10 TokenStore tests + ws_client tests UNCHANGED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create matriz-specific test_with_options.py with D-T5 TokenStore-not-rebound test + per-package mocked tests + snapshot regen</name>
  <files>
    packages/matriz-client/tests/test_with_options.py,
    packages/matriz-client/tests/test_client.py,
    packages/matriz-client/tests/test_async_client.py,
    verification/snapshots/matriz-client-surface.txt
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-T5 — test_with_options_does_not_rebind_tokenstore_max_retries; `<specifics>` D-T5 matriz TokenStore test shape exact body)
    packages/matriz-client/src/matriz_client/_token_store.py (build_token_store signature)
    packages/matriz-client/tests/ (any existing matriz test patterns for `monkeypatch` + `httpx_mock` interaction with `_ensure_token`)
    packages/higyrus-client/tests/test_client.py (Plan 3 per-package mocked test pattern — mirror)
    verification/snapshots/matriz-client-surface.txt (current snapshot)
  </read_first>
  <behavior>
    - NEW file `packages/matriz-client/tests/test_with_options.py` contains 1 test (matriz-specific D-T5).
    - Test body per CONTEXT.md `<specifics>` D-T5 matriz TokenStore test shape: monkeypatch `build_token_store` to spy on calls; force `_ensure_token` from view; assert spy was called with `max_retries=2` (the constructor value), NOT `10` (the view's override).
    - Per-package mocked tests in `test_client.py` / `test_async_client.py` mirror Plans 2-3 + 1 matriz-specific: `test_with_options_does_not_change_token_store_3way_sharing` — assert `view._state.token_store is parent._state.token_store` (anti-Pitfall 13 extended to TokenStore primitive; D-T2 verified).
    - Snapshot regenerated; body diff zero (per Plan 2-3 reasoning).
  </behavior>
  <action>
    1. **Create `packages/matriz-client/tests/test_with_options.py`** (NEW file) with `test_with_options_does_not_rebind_tokenstore_max_retries`:

       Use the body documented in CONTEXT.md `<specifics>` D-T5 matriz TokenStore test shape. The test must:
         - Monkeypatch `matriz_client._token_store.build_token_store` with a spy that records `max_retries` arg.
         - Construct `Client(base_url="https://api.test", username="u", password="p", max_retries=2)`.
         - `view = client.with_options(max_retries=10)`.
         - Force first `_ensure_token` from view: set `client._state.token = None` AND `client._state.token_expires_at = 0.0`.
         - Mock httpx with the refresh-response JSON: `{"status": "OK", "token": "fresh"}`.
         - Call `view._ensure_token()`.
         - Assert `build_calls == [2]` (NOT 10) — confirming `state.client_max_retries=2` was used, NOT view's `self._max_retries=10`.

       Add module docstring: "D-T5 — view's max_retries is HTTP-only; TokenStore retry cap stays gobernado por the Client constructor."

       Add `from __future__ import annotations` at top.

       Add a sync variant AND an async variant: `test_with_options_does_not_rebind_tokenstore_max_retries_async` using `AsyncClient` + `_aensure_token`.

       ~80 LOC total for the file.

    2. **Append mocked tests to `packages/matriz-client/tests/test_client.py`** (mirror Plan 3 set):

       - `test_with_options_close_is_noop`
       - `test_with_options_exit_is_noop`
       - `test_with_options_chaining_inner_wins_local`
       - `test_with_options_repr_shows_view_prefix`
       - `test_with_options_invalid_max_retries_raises_value_error`
       - **NEW (matriz-specific)** `test_with_options_view_shares_token_store_3way_primitive`: assert `view._state.token_store is parent._state.token_store` (D-T2 — anti-Pitfall 13 extended to TokenStore). Construct Client, force first `_ensure_token` (mock httpx), get the parent's `state.token_store`, call `view = c.with_options(5)`, assert `view._state.token_store is parent._state.token_store`.

       ~60 LOC.

    3. **Append async-mirror tests to `packages/matriz-client/tests/test_async_client.py`**:

       - 5 standard async-mirror tests (mirror Plan 3 set).
       - `test_with_options_async_view_shares_token_store_3way_primitive`: async mirror — force `_aensure_token` via mocked refresh, then assert view shares token_store.

       ~60 LOC.

    4. **Regenerate snapshot for matriz-client**:

       Run `uv run python verification/regen_snapshots.py`. Confirm matriz snapshot body diff is zero (same reasoning as Plans 2-3). Commit the snapshot file to maintain D-P4 atomicity. Reset other packages' snapshots via `git checkout` if regen touched them.

    5. Run `uv run pytest verification/test_public_surface.py -x -q` to confirm GREEN.

    6. Run full matriz package test suite + cross-cutting tests; all 4 cross-cutting tests for matriz row should be GREEN (including the CRITICAL merge-gate test).

    7. **Optional sanity check**: run `uv run pytest verification/test_async_cancellation.py` to confirm the async retry path under cancellation is unchanged (Phase 8 D-32).
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q</automated>
    <automated>uv run pytest verification/test_public_surface.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "matriz_client or mutation_gate_matriz" -x -q</automated>
    <automated>uv run pytest verification/test_async_cancellation.py -x -q</automated>
    <automated>uv run ruff check packages/matriz-client/ verification/</automated>
    <automated>uv run mypy --strict packages/matriz-client/</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f packages/matriz-client/tests/test_with_options.py exits 0`.
    - Source: `grep -c "def test_with_options_does_not_rebind_tokenstore_max_retries" packages/matriz-client/tests/test_with_options.py` returns ≥ 1.
    - Source (async mirror): `grep -c "def test_with_options_does_not_rebind_tokenstore_max_retries_async" packages/matriz-client/tests/test_with_options.py` returns 1.
    - Source: `grep -c "assert build_calls == \[2\]" packages/matriz-client/tests/test_with_options.py` returns ≥ 1 (asserts NOT 10).
    - Source: `grep -c "def test_with_options_view_shares_token_store_3way_primitive" packages/matriz-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_async_view_shares_token_store_3way_primitive" packages/matriz-client/tests/test_async_client.py` returns 1.
    - Source (`from __future__ import annotations` mandatory): `grep -c 'from __future__ import annotations' packages/matriz-client/tests/test_with_options.py` returns 1.
    - Behavior: `uv run --package matriz-client pytest packages/matriz-client/tests/ -x -q exits 0`.
    - Behavior: `uv run pytest verification/test_public_surface.py -x -q exits 0`.
    - Behavior: `uv run pytest verification/test_async_cancellation.py -x -q exits 0` (Phase 8 D-32 unchanged).
    - **ALL 4 cross-cutting tests GREEN for matriz** (including CRITICAL merge gate): `uv run pytest verification/test_with_options.py -k "matriz_client or mutation_gate_matriz" -x -q exits 0`.
    - Snapshot integrity: `git diff verification/snapshots/matriz-client-surface.txt` reports empty.
    - Negative (other packages untouched): `git diff verification/snapshots/ambito-financiero-client-surface.txt verification/snapshots/higyrus-client-surface.txt verification/snapshots/iol-client-surface.txt` reports empty.
    - Quality: `uv run ruff check packages/matriz-client/ verification/ exits 0`.
    - Quality: `uv run mypy --strict packages/matriz-client/ exits 0` (tests + src).
  </acceptance_criteria>
  <done>
    Matriz-specific D-T5 test file created with sync + async variants. Per-package mocked tests added (6 sync + 6 async), including TokenStore-sharing test (D-T2). Snapshot regenerated (zero body diff). All 4 cross-cutting tests GREEN for matriz row — CRITICAL merge gate satisfied. Phase 8 D-32 async cancellation unchanged. spike-findings-market-libs SKILL.md remains accurate (TokenStore 3-way concurrency unchanged). Plan 5 (iol) can begin from stable HEAD.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.with_options` | Caller-controlled `max_retries` validated via `_validate_max_retries` |
| view → `_state.token_store` (shared) | View shares Phase 10 3-way concurrency primitive; no new locks; concurrency invariants UNCHANGED |
| view → `state.client_max_retries` (read-only access) | View does NOT mutate; TokenStore retry cap remains parent's constructor value (D-T1 isolation) |
| **CRITICAL** view.new_order → mutation gate | Non-idempotent requests bypass retry loop via `extensions["idempotent"]=False`; view's `extensions["max_attempts"]=11` is IGNORED for non-idempotent calls (transport pre-Retrying-loop pass-through) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-13-04-01** | **Tampering / DoS** | **view.new_order() bypassing mutation gate → duplicate-order money loss** | **mitigate (CRITICAL)** | **Mutation gate (Phase 8 D-01) is the FIRST gate in `RetryTransport.handle_request`: `if not request.extensions.get("idempotent", False): return super().handle_request(request)`. Plan 1 Task 1 acceptance grep proved eval order: idempotent check BEFORE `effective_max_attempts` assignment. Test `test_with_options_does_not_bypass_mutation_gate_matriz` (cross-cutting) asserts `len(httpx_mock.get_requests()) == 1` under `with_options(max_retries=10).new_order(...)` with 503. Per-package shadow test in Plan 4 Task 3 not required — cross-cutting test IS the merge gate. Money-on-the-line.** |
| T-13-04-02 | Information Disclosure | View retry log on auth_basic Risk API leaks credentials | mitigate | Risk API uses `extensions["auth_basic"]` (Phase 10) which RedactingFilter (Phase 8 LOG-02) does not directly read; the BasicAuth header is set by httpx via the auth callback, NOT logged. View shares logger namespace `matriz_client` — RedactingFilter still applies to any token/password emitted via `extra=`. No new log emit site in Plan 4. |
| T-13-04-03 | Tampering | View rebinds TokenStore retry cap | mitigate | D-T1 + D-T3: `_ensure_token` reads `state.client_max_retries` (parent's constructor value), NOT `self._max_retries`. Test `test_with_options_does_not_rebind_tokenstore_max_retries` (D-T5) monkey-patches `build_token_store` to spy; asserts spy called with parent's value. Both sync + async variants. |
| T-13-04-04 | Denial of Service | View closes parent's http_client / token_store | mitigate | `_is_view` flag + no-op `close()` / `aclose()`. Per-package `test_with_options_view_shares_token_store_3way_primitive` asserts `view._state.token_store is parent._state.token_store` (identity); close() no-op preserves it. |
| T-13-04-05 | Spoofing | View's `_max_retries=10` could double-count attempts in TokenStore | mitigate | The HTTP-level retry loop (RetryTransport) and the TokenStore RefreshPolicy retry loop are SEPARATE primitives with separate caps. View affects only the HTTP loop via `extensions["max_attempts"]`. TokenStore uses `state.client_max_retries` independently. No interleaving. |
| T-13-04-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies in Plan 4. |

**Mutation gate is the absolute authority on retry permission.** Anti-Pitfall 14 (CONTEXT.md `<domain>` / SC#2 ROADMAP / STATE.md Blockers): `client.with_options(max_retries=10).new_order(...)` MUST execute EXACTLY 1 outgoing request under 503 — verified by `test_with_options_does_not_bypass_mutation_gate_matriz` (Plan 1 Task 3 created RED; Plan 4 Task 2 flips it GREEN). This is the Phase 13 MERGE GATE — without this test GREEN, Plan 5 cannot proceed and Plan 4 cannot merge.

TokenStore 3-way concurrency unchanged: spike-findings-market-libs auto-loaded skill remains accurate. View shares `_state.token_store`; threading.Lock + asyncio.Lock per loop primitives operate on the SAME TokenStore instance regardless of view vs. parent caller.

Credential surface unchanged: view shares `_state.token`, `_state.username`, `_state.password`, `_state.account_id`. No new storage path.
</threat_model>

<verification>
- `_state.client_max_retries: int = 2` field exists.
- `Client.__init__` + `AsyncClient.__init__` set `state.client_max_retries` from `max_retries` arg.
- `_ensure_token` (sync) + `_aensure_token` (async) read `state.client_max_retries`, NOT `self._max_retries`.
- `Client.with_options` + `AsyncClient.with_options` exist with `_is_view` flag + shared `_state` (including `token_store`).
- `_request()` (sync + async) + `_send_auth_request()` (sync + async) set `req.extensions["max_attempts"] = self._max_retries + 1` after `endpoint_name` extension.
- `close()` / `aclose()` no-op when `_is_view=True`.
- `__repr__` prefixes "view of"; password/token redaction preserved.
- `packages/matriz-client/tests/test_with_options.py` exists with D-T5 test (sync + async).
- Per-package mocked tests added (6 sync + 6 async) including TokenStore-sharing test (D-T2 verified).
- Snapshot regenerated; body diff zero.
- ALL 4 cross-cutting tests GREEN for matriz row — including CRITICAL `test_with_options_does_not_bypass_mutation_gate_matriz`.
- Phase 10 TokenStore tests, Phase 8 mutation-gate cross-cutting, Phase 8 D-32 async cancellation all GREEN.
- `uv run ruff check packages/matriz-client/ verification/` exits 0.
- `uv run mypy --strict packages/matriz-client/ exits 0`.
- Other packages' snapshots untouched.
</verification>

<success_criteria>
- matriz sync + async `with_options` lands with TokenStore-isolation (D-T1) + `_state.client_max_retries` field (D-T3).
- **CRITICAL merge gate GREEN**: `test_with_options_does_not_bypass_mutation_gate_matriz` asserts 1 request under 503 with `with_options(max_retries=10).new_order(...)`. Anti-Pitfall 14 / SC#2 ROADMAP. Money-on-the-line.
- D-T5 test GREEN: `test_with_options_does_not_rebind_tokenstore_max_retries` (sync + async).
- D-T2 verified: view shares `_state.token_store` (identity assertion).
- All 4 cross-cutting tests for matriz row GREEN.
- spike-findings-market-libs SKILL.md remains accurate (TokenStore 3-way concurrency primitive unchanged).
- All matriz package tests GREEN.
- Ruff + mypy strict GREEN.
- Plan 5 (iol) can begin from stable HEAD with the matriz merge gate proven.
</success_criteria>

<output>
Create `.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-04-SUMMARY.md` when done. Document:
- `_state.client_max_retries` field added; `__init__` sets it; `_ensure_token` + `_aensure_token` consume it (D-T3).
- `with_options` sync + async added; `_is_view` slot; lifecycle no-op; `_request` + `_send_auth_request` shells set `extensions["max_attempts"]`.
- D-T5 matriz-specific test file created (sync + async).
- Per-package mocked tests added including D-T2 TokenStore-sharing assertion.
- **CRITICAL merge gate `test_with_options_does_not_bypass_mutation_gate_matriz` GREEN** — anti-Pitfall 14 satisfied; Phase 13 merge gate met.
- TokenStore 3-way concurrency primitive UNCHANGED; spike-findings-market-libs SKILL.md still accurate.
- Forward reference: Plan 5 (iol) adds the LAST package + consolidated green gate (pytest ≥907, ruff, mypy strict, pre-commit).
</output>
