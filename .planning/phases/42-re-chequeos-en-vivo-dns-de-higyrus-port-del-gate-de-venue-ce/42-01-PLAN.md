---
phase: 42
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - verification/test_literal_census_venue_gate.py
  - .github/workflows/ci.yml
  - scripts/literal_census_33.py
autonomous: false
requirements: [LIVE-02]

must_haves:
  truths:
    - "El censo decide el venue por igualdad exacta de hostname contra el MISMO objeto que publica main_matriz.py (D-01), nunca por substring"
    - "Un host de spoofing por sufijo hostil o por userinfo es rechazado, y hay un test que lo falsifica (criterio 1)"
    - "El lock corre en CI: está en la allowlist explícita de .github/workflows/ci.yml, no sólo en testpaths local"
    - "El censo emite venue + timestamp en un encabezado antes de la primera request (criterio 3)"
    - "El censo puede correr sólo matriz, sin disparar census_iol() (D-04)"
    - "El gate de mutación sigue fail-closed bajo bbsa y verification/mutation_gate.py queda byte-idéntico (criterio 4)"
    - "Un operador humano confirma la fidelidad del port ANTES de que salga la primera llamada de red"
  artifacts:
    - path: "verification/test_literal_census_venue_gate.py"
      provides: "Lock de falsificación del gate de venue del censo: identidad de fuente, 13 casos de spoofing, aserción AST anti-substring, header, gate de mutación"
      min_lines: 150
      contains: "_census_matriz_node"
    - path: "scripts/literal_census_33.py"
      provides: "Gate portado por import + _census_header_lines/_census_header + flag --matriz-only + selftest extendido"
      contains: "from main_matriz import _VENUE_ALLOWLIST, _venue_token"
    - path: ".github/workflows/ci.yml"
      provides: "Enrolamiento del lock nuevo en la allowlist explícita de 12 rutas (pasa a 13)"
      contains: "verification/test_literal_census_venue_gate.py"
  key_links:
    - from: "scripts/literal_census_33.py"
      to: "main_matriz.py"
      via: "from main_matriz import _VENUE_ALLOWLIST, _venue_token — una sola fuente, pinneada por identidad de objeto (is)"
      pattern: "from main_matriz import _VENUE_ALLOWLIST, _venue_token"
    - from: "verification/test_literal_census_venue_gate.py"
      to: "scripts/literal_census_33.py"
      via: "import scripts.literal_census_33 as census (namespace package, pythonpath=['.']) + ast.parse del fuente"
      pattern: "scripts.literal_census_33"
    - from: ".github/workflows/ci.yml"
      to: "verification/test_literal_census_venue_gate.py"
      via: "allowlist explícita del step 'driver locks' del job lint — sin esta línea el lock es INERTE (WR-01)"
      pattern: "test_literal_census_venue_gate"
---

<objective>
Portar el gate de venue de `scripts/literal_census_33.py` de substring-match (`if "remarkets" not in
base:`, línea 192) a igualdad exacta de hostname importada de `main_matriz.py` (D-01), agregar el
encabezado venue+timestamp que el criterio 3 exige y el script hoy no emite, agregar el modo
`--matriz-only` (D-04), y dejar todo pinneado por un lock de falsificación ENROLADO en CI. El plan
cierra con el checkpoint humano bloqueante que habilita el tráfico en vivo de los planes 42-02,
42-03 y 42-04.

Purpose: el ROADMAP declara explícitamente que la entrada de backlog `LIVE-MATZ-33` sobreestima el
estado — el script sigue en la forma pre-Phase-39 y **saltearía en silencio** contra el sandbox
`bbsa` ya desbloqueado. Además, `42-RESEARCH.md § Pitfall 5` encontró que `_report()` no emite
ningún encabezado, así que un plan derivado sólo de CONTEXT.md produciría una salida que falla el
criterio 3 por una razón puramente mecánica, y `§ Pitfall 2` encontró que un test nuevo bajo
`verification/` es INERTE hasta que alguien lo agrega a mano a la allowlist de `ci.yml`.

Output: `verification/test_literal_census_venue_gate.py` (nuevo, enrolado en CI),
`scripts/literal_census_33.py` portado, y la aprobación humana transcrita verbatim que gatea toda
la red de la fase. **Cero llamadas de red en este plan.**
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CONTEXT.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos, archivos y tokens que **no existen en HEAD** y que esta fase crea. Cualquier verificación
de drift contra el árbol actual debe excluirlos: son salida, no deriva.

| Artefacto | Tipo | Creado en |
|-----------|------|-----------|
| `verification/test_literal_census_venue_gate.py` | archivo nuevo | 42-01 |
| `_census_matriz_node()` | función helper (test) | 42-01 |
| `_membership_offenders()` | función helper (test) | 42-01 |
| `_TARGET` (en el archivo de test nuevo) | constante de módulo | 42-01 |
| `test_census_shares_the_single_venue_source` | test | 42-01 |
| `test_venue_allowlist_has_exactly_the_two_known_hosts` | test | 42-01 |
| `test_venue_token_resolves_by_exact_hostname` | test parametrizado (13 casos) | 42-01 |
| `test_no_substring_membership_check_inside_census_matriz` | test | 42-01 |
| `test_membership_walker_detects_an_injected_offender` | test (control positivo) | 42-01 |
| `test_census_header_precedes_the_first_request` | test | 42-01 |
| `test_census_header_lines_carry_venue_and_timestamp` | test | 42-01 |
| `test_mutation_gate_stays_fail_closed_under_bbsa` | test (criterio 4) | 42-01 |
| `test_mutation_gate_allows_remarkets_control` | test (control positivo) | 42-01 |
| `_census_header_lines()` en `scripts/literal_census_33.py` | función de módulo | 42-01 |
| `_census_header()` en `scripts/literal_census_33.py` | función de módulo | 42-01 |
| `venue` (local en `census_matriz`) | variable local | 42-01 |
| `--matriz-only` | flag de CLI de `scripts/literal_census_33.py` | 42-01 |
| `CENSUS-HEADER` / `CENSUS-DLOCK` | prefijos de línea de stdout | 42-01 |
| entrada `verification/test_literal_census_venue_gate.py` en `ci.yml` | línea de allowlist | 42-01 |
| `wire-instruments-42` / `wire-segments-42` | labels de `capture()` en `main_market_data.py` | 42-04 |
| `.planning/verification/captures/market-data-wire-instruments-42.json` | artefacto gitignored | 42-04 |
| `.planning/verification/captures/market-data-wire-segments-42.json` | artefacto gitignored | 42-04 |
| `42-CENSUS.md` | artefacto committeado | 42-02 |
| `42-WIRE-READ.md` | artefacto committeado | 42-04 |
| `LIVE-HIGY-42` | identificador de destino (reemplaza al viejo en sitios VIVOS) | 42-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock de falsificación del gate de venue + enrolamiento en CI (RED)</name>
  <files>verification/test_literal_census_venue_gate.py, .github/workflows/ci.yml</files>
  <read_first>
    - `verification/test_main_matriz_skip_line_shape.py` (269 líneas — LEER ÍNTEGRO). Es el analog
      exacto: docstring de módulo declarativo (`:1-24`), bloque de imports (`:26-36`), pin del
      allowlist (`:205-215`), tabla de 13 casos parametrizados (`:218-244`), aserción AST
      anti-substring (`:247-268`) y control positivo de no-vacuidad (`:195-197`).
    - `scripts/literal_census_33.py` (367 líneas — LEER ÍNTEGRO). Es el sujeto del lock. Sitios
      relevantes: `sys.path.insert` (`:68-70`), import con `# noqa: E402` (`:72`), `_report`/`_skip`
      (`:154-167`), `census_matriz` (`:175-233`) con el gate stale en `:192` y el primer
      `client._request` en `:212`, `_selftest` (`:315-347`), `main` (`:353-362`) con
      `if "--selftest" in argv:` en `:355`.
    - `main_matriz.py:110-250` — el comentario D-02 (`:115-138`), `_VENUE_ALLOWLIST` (`:139-142`) y
      `_venue_token` (`:228-247`), que es la política que el script va a importar.
    - `verification/mutation_gate.py` (LEER ÍNTEGRO, 5877 bytes) — `mutating_allowed_for` con su
      firma keyword-only (`env_var`, `base_url`, `expected_host`) y `_SANDBOX_HOST` (`:73`).
      **Este archivo NO se modifica en ninguna task de la fase.**
    - `.github/workflows/ci.yml:60-93` — el comentario `:67-78` explica por qué la allowlist es
      manual (WR-01 / HARN-VERIF-01) y el bloque `run:` `:79-92` enumera las 12 rutas actuales.
    - `pyproject.toml:72-121` — `per-file-ignores` (confirma que `E402` NO está ignorado),
      `pythonpath = ["."]`, `--import-mode=importlib`, `--strict-markers`.
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-PATTERNS.md`
      § `verification/test_literal_census_venue_gate.py` — la divergencia obligatoria de Q4.
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md`
      § Pitfall 2 (CI enrollment), § Code Examples, Open Question Q4.
  </read_first>
  <behavior>
    Tests que este archivo debe contener (todos fallan hoy salvo los dos del gate de mutación, que
    pasan desde ya y sirven de piso de no-vacuidad):

    - **Identidad de fuente única (D-01).** `census._venue_token is main_matriz._venue_token` y
      `census._VENUE_ALLOWLIST is main_matriz._VENUE_ALLOWLIST`. Identidad de objeto (`is`), NO
      igualdad de contenido (`==`): con import, la divergencia entre sitios es estructuralmente
      imposible en vez de sólo detectada.
    - **Allowlist de exactamente dos hosts.** `set(main_matriz._VENUE_ALLOWLIST)` es
      `{"api.remarkets.primary.com.ar", "api.bbsa.matrizoms.com.ar"}` y `len(...) == 2`.
    - **Igualdad exacta de hostname — 13 casos parametrizados contra `census._venue_token`:**
      `api.remarkets.primary.com.ar` → `remarkets`;
      `https://api.remarkets.primary.com.ar` → `remarkets`;
      `api.bbsa.matrizoms.com.ar` → `bbsa`;
      `https://api.bbsa.matrizoms.com.ar` → `bbsa`;
      `https://api.bbsa.matrizoms.com.ar/` → `bbsa` (trailing slash);
      `api.bbsa.matrizoms.com.ar.attacker.example` → `None` (sufijo hostil);
      `https://api.bbsa.matrizoms.com.ar.attacker.example` → `None`;
      `https://api.bbsa.matrizoms.com.ar@attacker.example` → `None` (userinfo);
      `https://api.remarkets.primary.com.ar@attacker.example` → `None`;
      `api.primary.com.ar` → `None` (producción);
      `https://api.primary.com.ar` → `None`;
      `""` → `None` (fail-closed);
      `https://[oops/api` → `None` (imparseable, nunca crash).
    - **Anti-substring por AST, restringido a `census_matriz`.** Cero nodos `ast.Compare` con
      operador `In`/`NotIn` y `ast.Constant` de tipo `str` a la izquierda, dentro del cuerpo del
      `FunctionDef` `census_matriz` de `scripts/literal_census_33.py`.
    - **Control positivo del walker.** El mismo helper de detección, aplicado a un snippet
      sintético que SÍ contiene la forma prohibida, devuelve al menos un offender. Sin esto el
      guard podría estar verde por no inspeccionar nada.
    - **Header antes de la primera request.** Dentro de `census_matriz` existe una llamada a
      `_census_header`, y su `lineno` es estrictamente menor que el `lineno` de la primera llamada
      a `client._request` del mismo cuerpo.
    - **Contenido del header.** `census._census_header_lines("bbsa")` devuelve una lista cuya primera
      línea empieza con `CENSUS-HEADER venue=bbsa captured_at=` y contiene `allowlist_size=2`, y
      alguna línea empieza con `CENSUS-DLOCK`.
    - **Gate de mutación fail-closed bajo bbsa (criterio 4, T-39-02).** Con `VERIFY_MUTATING` puesto
      en `"1"` vía `monkeypatch.setenv`, `mutating_allowed_for(env_var="VERIFY_MUTATING",
      base_url="https://api.bbsa.matrizoms.com.ar", expected_host=mutation_gate._SANDBOX_HOST)` es
      `False`.
    - **Control positivo del gate de mutación.** La misma llamada con
      `base_url="https://api.remarkets.primary.com.ar"` es `True`. Prueba que el `False` de arriba
      sale del hostname y no de un env var mal seteado.
  </behavior>
  <action>
    Crear `verification/test_literal_census_venue_gate.py` espejando la estructura del analog
    `verification/test_main_matriz_skip_line_shape.py`, que es exactamente lo que D-03 lockea: el
    test de spoofing es un archivo **nuevo** que reusa el patrón de casos ya existente en ese analog
    (sufijo hostil `<host>.attacker.example` y variante userinfo `https://<host>@attacker.example`),
    apuntado al uso **importado** de `scripts/literal_census_33.py` en vez de al driver.

    Convenciones obligatorias del repo (ver CLAUDE.md § Conventions y 42-PATTERNS.md § Shared
    Patterns 1): docstring de módulo declarando qué lock es, cuáles son sus capas y qué NO toca
    (`verification/mutation_gate.py`); `from __future__ import annotations` como primera sentencia
    tras el docstring; ruff line-length 100, comillas dobles, 4 espacios.

    Imports: `ast`, `pathlib.Path`, `main_matriz`, `pytest`, `verification.mutation_gate` (para
    `mutating_allowed_for` y `_SANDBOX_HOST`), y `scripts.literal_census_33 as census`. Este último
    funciona como namespace package sin `__init__.py` gracias a `pythonpath = ["."]`
    (`pyproject.toml:109`) — no hace falta ningún hack de `sys.path` y **ningún** `# noqa` en este
    archivo (todos los imports son top-level).

    Constantes de módulo: `_REPO_ROOT = Path(__file__).resolve().parent.parent` y
    `_TARGET = _REPO_ROOT / "scripts" / "literal_census_33.py"`.

    Helper `_census_matriz_node() -> ast.FunctionDef`: parsea `_TARGET` con
    `ast.parse(..., encoding="utf-8")`, recorre `tree.body` buscando el `ast.FunctionDef` de nombre
    `census_matriz` y lo devuelve; si no lo encuentra, `raise AssertionError` con un mensaje que diga
    que el lock quedaría vacuo. El `raise` en el else es parte del patrón del analog (mismo espíritu
    que `_MIN_PRINT_SITES = 2` en `:47`): un lock nunca puede pasar por no encontrar su sujeto.

    Helper `_membership_offenders(node: ast.AST) -> list[int]`: recorre con `ast.walk(node)`,
    acumula el `lineno` de cada `ast.Compare` que tenga algún operador `ast.In | ast.NotIn` y cuyo
    `node.left` sea un `ast.Constant` de valor `str`.

    **Decisión Q4, cerrada acá y no en ejecución:** el walk anti-substring se restringe al
    `FunctionDef` de `census_matriz`, NO al módulo entero. Recorrer el módulo entero produce un
    falso positivo sobre el despacho de flags de `main()` (`:355`), y la Task 2 agrega un segundo
    despacho de la misma forma. Descubrir esto corriendo el test invita al reflejo de relajar la
    aserción, que es lo contrario de lo que el criterio 1 quiere. `census_matriz` es además el único
    lugar donde el gate puede vivir, así que la restricción no debilita nada.

    Los comentarios inline que agrupan los 13 casos por clase de ataque (sufijo hostil, userinfo,
    producción, fail-closed ante basura) son parte del patrón del analog: conservarlos.

    Enrolar el archivo en CI: en `.github/workflows/ci.yml`, dentro del bloque `run:` del step
    "driver locks" (`:79-92`), agregar una continuación de línea al final de la última ruta actual
    (`verification/test_cycle_closure_phase33.py`) y una línea nueva con la ruta
    `verification/test_literal_census_venue_gate.py`, con la misma indentación de 12 espacios que
    las 12 rutas existentes. Sin esta línea el lock es INERTE: `verification/` nunca corre entero en
    CI porque el job `test` pasa rutas explícitas que pisan `testpaths` (WR-01, `ci.yml:67-78`).
  </action>
  <verify>
    <automated>uv run --frozen ruff check verification/test_literal_census_venue_gate.py &amp;&amp; uv run --frozen ruff format --check verification/test_literal_census_venue_gate.py &amp;&amp; test "$(grep -c 'verification/test_literal_census_venue_gate.py' .github/workflows/ci.yml)" = "1" &amp;&amp; ! uv run pytest -q verification/test_literal_census_venue_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "verification/test_literal_census_venue_gate.py" .github/workflows/ci.yml` devuelve
      exactamente `1`, y la línea está dentro del bloque `run:` del step "driver locks" del job
      `lint` (no en otro job, no en un comentario).
    - `uv run pytest -q verification/test_literal_census_venue_gate.py` sale con código distinto de
      cero (RED esperado), y entre los fallos figuran al menos
      `test_census_shares_the_single_venue_source` y
      `test_no_substring_membership_check_inside_census_matriz`.
    - En la misma corrida, `test_mutation_gate_stays_fail_closed_under_bbsa` y
      `test_mutation_gate_allows_remarkets_control` PASAN — el RED es de los tests del port, no de
      un archivo roto.
    - El fuente del test declara `_census_matriz_node` y el test anti-substring lo usa; no hay
      ningún `ast.walk` sobre el módulo completo dentro de ese test (decisión Q4).
    - El archivo declara un docstring de módulo y `from __future__ import annotations`.
    - `uv run --frozen ruff check` y `ruff format --check` verdes sobre el archivo nuevo.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
  </acceptance_criteria>
  <done>El lock existe, está enrolado en la allowlist de CI, y está ROJO por las razones correctas
  (el port todavía no existe), con dos tests verdes que prueban que el archivo no está vacuo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port del gate por import + header venue/timestamp + flag --matriz-only (GREEN)</name>
  <files>scripts/literal_census_33.py</files>
  <read_first>
    - `scripts/literal_census_33.py` (LEER ÍNTEGRO antes de editar) — el archivo que se modifica.
    - `main_matriz.py:110-250` — la política que se importa: `_VENUE_ALLOWLIST` (`:139-142`),
      `_venue_token` (`:228-247`) y el comentario D-02 (`:115-138`) que documenta por qué la
      comparación es por igualdad exacta.
    - `verification/test_literal_census_venue_gate.py` — el lock recién creado; es el contrato que
      esta task tiene que satisfacer.
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-PATTERNS.md`
      § `scripts/literal_census_33.py` — forma exacta del import, del gate portado y del header.
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md`
      § Code Examples ("Header del censo"), § Pitfall 3 (sobre-generalización de venue), § Pitfall 5.
    - `main_matriz.py:3155-3162` — confirmación de que `main_matriz` no tiene side-effects de import
      fuera de `if __name__ == "__main__":`.
  </read_first>
  <behavior>
    Los 11 tests de `verification/test_literal_census_venue_gate.py` pasan, y
    `uv run python scripts/literal_census_33.py --selftest` imprime `SELFTEST: PASS` y sale 0.
  </behavior>
  <action>
    Seis cambios en `scripts/literal_census_33.py`, todos en el mismo archivo:

    1. **Import de la política (D-01).** Inmediatamente después de la línea 72
       (`from verification.capture import capture  # noqa: E402`) agregar una línea
       `from main_matriz import _VENUE_ALLOWLIST, _venue_token  # noqa: E402`. El `# noqa: E402` es
       obligatorio: `E402` no está en `per-file-ignores` de `pyproject.toml`, igual que en la línea
       72 existente. La forma `from main_matriz import ...` —y no `import main_matriz` con uso
       calificado— es la que habilita el pin de identidad `is` del lock. El import va DESPUÉS del
       `sys.path.insert` de `:68-70`; no hace falta ningún hack adicional.

    2. **Import de `datetime`.** Agregar `import datetime as dt` al bloque stdlib de `:59-62`
       (ruff/isort lo ordena junto a `contextlib` y `sys`).

    3. **Port del gate (el sitio stale de `:192`).** Reemplazar la condición
       `if "remarkets" not in base:` por la resolución del token de venue con `_venue_token(base)`
       ligada a un local `venue`, y el gate pasa a ser `if venue is None:`. **La posición no cambia:**
       sigue después de `client = Client()` (que no hace IO) y de `base = client._state.base_url`, y
       ANTES de `login()` y de cualquier request — el docstring de `census_matriz` (`:176-180`) lo
       declara: *"un SKIP no debe costar ni un round trip contra un host fuera de política"*. Mover
       el gate sería una regresión de seguridad aunque el predicado fuera correcto.
       Preservar literalmente del bloque actual: el comentario de no-fuga (no se imprime la URL), la
       llamada a `_skip("matriz-client", ...)`, el `with contextlib.suppress(Exception): client.close()`
       y el `return False`. Actualizar el texto de la razón para que nombre el allowlist en vez de
       "remarkets-only", sin interpolar hostname ni base URL (T-39-04 / C-4).
       Inmediatamente después del gate, y antes del `try:` que abre las requests, llamar
       `_census_header(venue)`.

    4. **Header del censo (criterio 3, gap de Pitfall 5).** Agregar dos funciones de módulo junto a
       `_report`/`_skip` (`:154-167`), siguiendo el mismo molde: docstring de una línea que dice el
       porqué, sin logging, un `print` por línea.
       - `_census_header_lines(venue: str) -> list[str]` devuelve dos líneas. La primera es una
         f-string con el prefijo `CENSUS-HEADER` y los campos `venue=`, `captured_at=` (UTC ISO vía
         `dt.datetime.now(dt.UTC).isoformat()`) y `allowlist_size=` (el `len` de `_VENUE_ALLOWLIST`
         importado — es un conteo, nunca un hostname, y hace que el import de la política sea
         genuinamente usado en vez de un `# noqa: F401`). La segunda empieza con `CENSUS-DLOCK:` y
         dice, en prosa, que el D-lock (b) de la Phase 29 SIGUE EN VIGOR — los campos RESPONSE no se
         cierran como `Literal` — y que este inventario es una MEDICIÓN de una sola venue, no una
         autorización de promoción (Pitfall 3).
       - `_census_header(venue: str) -> None` imprime cada línea de `_census_header_lines(venue)`.
       El `venue` sale de `_venue_token(base)`, **nunca** hardcodeado: así el header no puede mentir
       sobre contra qué venue se midió.

    5. **Flag `--matriz-only` (D-04).** En `main(argv)` (`:353-362`), agregar un despacho con
       retorno temprano ANTES de las llamadas actuales a `census_matriz()`/`census_iol()`,
       espejando la forma del `--selftest` de `:355`. La rama corre sólo `census_matriz()`, imprime
       una línea `CENSUS:` que reporte `matriz=RAN`/`matriz=SKIPPED` y deje explícito que iol no fue
       pedido, y devuelve `0` si el censo corrió y `1` si quedó SKIPPED.
       **Trampa a evitar (verificada en research):** NO dejar que esta rama caiga al
       `return 0 if (ran_matriz and ran_iol) else 1` de `:362` — con `ran_iol=False` daría exit 1 en
       una corrida exitosa. El retorno temprano es lo que lo evita.

    6. **Selftest extendido + docstring de módulo.**
       - En `_selftest()` (`:315-347`), agregar aserciones con la MISMA forma acumulativa que ya usa
         (acumular en `ok: bool`, imprimir `SELFTEST FAIL <detalle>` por cada fallo, nunca `assert`):
         la primera línea de `_census_header_lines` con un venue sintético empieza con
         `CENSUS-HEADER venue=<ese venue> captured_at=`; alguna línea empieza con `CENSUS-DLOCK`;
         `_venue_token("api.bbsa.matrizoms.com.ar")` devuelve `"bbsa"`; y
         `_venue_token("api.bbsa.matrizoms.com.ar.attacker.example")` devuelve `None`. Esto da
         cobertura offline del criterio 3 y del criterio 1 sin red ni credenciales.
       - Actualizar el docstring de módulo: el párrafo de `:38-44` ("matriz corre detrás del gate
         remarkets-only") pasa a describir el allowlist por igualdad exacta de hostname importado de
         `main_matriz`, mencionando que el gate corre antes del login y que la ampliación del
         allowlist es una decisión humana. En el bloque `Uso::` (`:51-54`) agregar la invocación con
         `--matriz-only`. No transcribir ningún hostname de vendor en prosa nueva del docstring más
         allá de los dos que `main_matriz.py` ya publica en el código.
  </action>
  <verify>
    <automated>uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; uv run pytest -q verification/test_literal_census_venue_gate.py &amp;&amp; uv run python scripts/literal_census_33.py --selftest | tail -1 | grep -q "SELFTEST: PASS" &amp;&amp; test "$(git hash-object verification/mutation_gate.py)" = "6bdaec006cc16f7c8dbfac41701712a9085c691b"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q verification/test_literal_census_venue_gate.py` → 0 failed (GREEN).
    - `uv run python scripts/literal_census_33.py --selftest` imprime `SELFTEST: PASS` y sale 0.
    - `scripts/literal_census_33.py` contiene exactamente una línea
      `from main_matriz import _VENUE_ALLOWLIST, _venue_token  # noqa: E402`, ubicada después del
      bloque `sys.path.insert` de `:68-70`.
    - Dentro de `census_matriz`, la llamada a `_census_header` aparece en una línea anterior a la
      primera llamada a `client._request` (pinneado por
      `test_census_header_precedes_the_first_request`).
    - `uv run pytest -q` sobre las 13 rutas de la allowlist de `.github/workflows/ci.yml` reporta
      `0 failed` y un número de `passed` estrictamente mayor que el baseline `129`.
    - `uv run --frozen ruff check .` y `uv run --frozen ruff format --check .` verdes sobre todo el
      repo.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b` (5877 bytes) — criterio 4.
    - `git diff --stat` de esta task toca **un solo archivo**: `scripts/literal_census_33.py`.
  </acceptance_criteria>
  <done>El censo decide el venue por igualdad exacta de hostname contra la fuente única importada,
  emite venue+timestamp en el encabezado, sabe correr sólo matriz, y el lock está verde y enrolado
  en CI. Cero llamadas de red hasta acá.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint bloqueante — fidelidad del port y habilitación del tráfico en vivo (D-02)</name>
  <files>(ninguno — checkpoint humano bloqueante, no escribe código)</files>
  <read_first>
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CONTEXT.md`
      § D-02 — el encuadre exacto de este checkpoint: **confirmación de fidelidad del port**, no
      re-autorización de bbsa desde cero.
    - `.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md`
      § D-02 — la autorización original del operador (2026-08-29) de `api.bbsa.matrizoms.com.ar`
      como sandbox no-producción.
    - `.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-01-PLAN.md:87-141`
      — el precedente de forma de este checkpoint, incluida la declaración de que `mode: yolo` y
      `auto_advance: true` NO aplican.
    - `verification/mutation_gate.py` — el segundo gate, independiente, que mantiene el order entry
      fail-closed bajo bbsa **sin cambio de código** (T-39-02).
    - `scripts/literal_census_33.py` — el diff de la Task 2 (gate portado, header, `--matriz-only`).
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md`
      § Security Domain, incluido el riesgo residual A1.
  </read_first>
  <action>
    Presentar al operador, ANTES de que salga la primera llamada de red de esta fase, el diff
    completo de las Tasks 1 y 2 y los seis puntos del `<how-to-verify>`. Esperar respuesta explícita
    del operador en esta sesión. **No derivar la aprobación de la configuración de avance
    automático.** Transcribir la respuesta verbatim en el SUMMARY del plan. Si la respuesta no es
    una aprobación, detener el plan acá y NO ejecutar los planes 42-02, 42-03 ni 42-04.

    Nota de workflow: `.planning/config.json` tiene `workflow.human_verify_mode: "end-of-phase"`.
    Ese modo **no aplica a este checkpoint**: el criterio 1 del ROADMAP exige que la autorización
    ocurra *antes de que salga tráfico*, y una verificación de fin de fase no puede gatear tráfico
    que ya ocurrió. El tipo y el gate son los que CONTEXT.md D-02 lockea:
    `checkpoint:human-verify` + `gate="blocking-human"`.
  </action>
  <what-built>
    El port ya está escrito y verde **offline**: `scripts/literal_census_33.py` importa
    `_VENUE_ALLOWLIST` y `_venue_token` de `main_matriz.py` y decide el venue por igualdad exacta de
    hostname; emite `CENSUS-HEADER`/`CENSUS-DLOCK` antes de la primera request; acepta
    `--matriz-only`. `verification/test_literal_census_venue_gate.py` pinnea 11 tests (13 casos de
    spoofing incluidos) y está enrolado en la allowlist de `.github/workflows/ci.yml`.
    **Cero llamadas de red han ocurrido.** Este checkpoint es **bloqueante e incolapsable**:
    `mode: yolo` y `workflow.auto_advance: true` están activos en `.planning/config.json` y NO
    aplican acá — el bug de autoría del gate ya se coló dos veces en este proyecto
    (`ROADMAP.md:54`).
  </what-built>
  <how-to-verify>
    1. **Qué es nuevo y qué no.** `api.bbsa.matrizoms.com.ar` ya es un host autorizado a nivel de
       sistema desde la Phase 39 D-02 (2026-08-29). Lo nuevo es que
       `scripts/literal_census_33.py` específicamente nunca hizo una llamada de red hasta ahora:
       hasta este diff, su gate era un substring-match que lo habría hecho saltear en silencio
       contra bbsa. La pregunta a confirmar es de **fidelidad**: ¿el port reusa fielmente la lógica
       ya aprobada, sin debilitarla?
    2. **Confirmar la forma del predicado.** El script no re-declara ninguna política: importa
       `_venue_token` y `_VENUE_ALLOWLIST` de `main_matriz.py`. No hay `in`, no hay `endswith`, no
       hay regex de hostname nuevo. Correr
       `uv run pytest -q verification/test_literal_census_venue_gate.py` y ver los 13 casos verdes,
       incluidos el sufijo hostil (`…bbsa.matrizoms.com.ar.attacker.example`) y la variante userinfo
       (`https://…@attacker.example`), ambos rechazados.
    3. **Confirmar la posición del gate.** Corre después de construir el `Client` (que no hace IO) y
       ANTES de `login()` y de toda request: un host fuera del allowlist no cuesta ni un round trip.
    4. **Confirmar que el gate de mutación NO se toca.**
       `git hash-object verification/mutation_gate.py` debe seguir siendo
       `6bdaec006cc16f7c8dbfac41701712a9085c691b`. Su `_SANDBOX_HOST` remarkets-only deja el order
       entry fail-closed bajo bbsa **sin cambio de código** — el widening es del gate de LECTURA del
       censo, jamás del de mutación (criterio 4).
    5. **Confirmar el alcance del tráfico que se habilita.** Al aprobar, los planes siguientes
       ejecutan: (a) `main_higyrus.py` completo contra higyrus, (b)
       `scripts/literal_census_33.py --matriz-only` contra bbsa — cinco endpoints de LECTURA, cero
       órdenes, cero mutaciones (D-10 / P-05 siguen vigentes), y (c) `main_market_data.py` completo.
       `census_iol()` **no** corre (D-04).
    6. **Riesgo residual declarado (A1).** La seguridad y el carácter no-producción del sandbox
       `api.bbsa.matrizoms.com.ar` son una **aserción del operador**, no verificable por máquina. Es
       la mayor dependencia de confianza de esta fase.
  </how-to-verify>
  <resume-signal>Escribir "approved" para habilitar el tráfico en vivo de los planes 42-02/42-03/42-04, o describir objeciones</resume-signal>
  <verify>
    <automated>test "$(git hash-object verification/mutation_gate.py)" = "6bdaec006cc16f7c8dbfac41701712a9085c691b" &amp;&amp; grep -q "from main_matriz import _VENUE_ALLOWLIST, _venue_token" scripts/literal_census_33.py &amp;&amp; uv run pytest -q verification/test_literal_census_venue_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - El operador responde explícitamente en esta sesión; la aprobación NO se deriva de
      `workflow.auto_advance`, de `mode: yolo` ni de `workflow.human_verify_mode`.
    - La respuesta queda transcrita **verbatim** en el SUMMARY de este plan.
    - Si la respuesta no es una aprobación, el SUMMARY lo registra y ninguno de los planes 42-02,
      42-03 y 42-04 arranca.
    - En el momento del checkpoint, `git status --porcelain` no muestra cambios en
      `verification/mutation_gate.py`.
  </acceptance_criteria>
  <done>Aprobación humana explícita registrada verbatim, o el plan se detiene acá y la fase no emite
  tráfico.</done>
</task>

</tasks>

<threat_model>
> `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → `Client()._state.base_url` → gate de venue del censo | La base URL es entrada NO confiable: viene de un archivo de configuración editable a mano y decide contra qué host se emite tráfico |
| script de verificación → API de MATBA ROFEX (bbsa) | Cruce de red hacia un vendor financiero real; sólo lectura |
| gate de lectura (`_venue_token`) ↔ gate de mutación (`mutation_gate._SANDBOX_HOST`) | Dos controles de acceso deliberadamente independientes; ampliar el primero no debe tocar el segundo |
| plan → operador humano | La autorización del tráfico en vivo cruza acá y no puede derivarse de configuración de automatización |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Spoofing | `scripts/literal_census_33.py` gate de venue (`census_matriz`) | mitigate | Importar `main_matriz._venue_token` (igualdad exacta de hostname vía `urlsplit`, fail-closed ante `ValueError`/`host is None`/host no listado) y remover el substring-match. Falsificado por 13 casos parametrizados que incluyen sufijo hostil y userinfo, más una aserción AST que impide el retorno de la forma insegura (Task 1 + Task 2) |
| T-42-02 | Tampering | `verification/mutation_gate.py` / order entry bajo bbsa | mitigate | El archivo queda byte-idéntico: `test "$(git hash-object verification/mutation_gate.py)" = 6bdaec006cc16f7c8dbfac41701712a9085c691b` en el `<verify>` de las tres tasks. Comportamiento pinneado por `test_mutation_gate_stays_fail_closed_under_bbsa` con su control positivo (T-39-02) |
| T-42-03 | Elevation of Privilege | Checkpoint de autorización de tráfico bajo `mode: yolo` + `auto_advance: true` | mitigate | `type="checkpoint:human-verify"` + `gate="blocking-human"` (nunca `gate="blocking"` a secas, que ya se auto-aprobó dos veces en este proyecto), `<files>` vacío, transcripción verbatim exigida en `<acceptance_criteria>` |
| T-42-04 | Information Disclosure | Líneas de veredicto del censo (`_skip`, `_census_header`) | mitigate | La razón del `_skip` nombra la causa y no interpola hostname ni base URL (T-39-04 / C-4). El header emite el **token de venue** (`bbsa`/`remarkets`) y un **conteo** de allowlist, nunca un hostname resuelto |
| T-42-05 | Denial of Service | Base URL imparseable (`https://[oops/api`) | mitigate | `_venue_token` captura `ValueError` y devuelve `None` (fail-closed, nunca crash); pinneado como caso 13 de la tabla |
| T-42-06 | Repudiation | Un lock verde en local pero INERTE en CI (WR-01) | mitigate | La ruta se agrega a mano a la allowlist explícita de `.github/workflows/ci.yml:79-92` y se prueba con `grep -c ... = 1` en el `<verify>` de la Task 1 |
| T-42-07 | Tampering | Un guard que pasa por no encontrar su sujeto (lock vacuo) | mitigate | `_census_matriz_node` levanta `AssertionError` si no encuentra `census_matriz`; control positivo del walker sobre un snippet sintético; controles positivos del gate de mutación |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala ningún paquete: `42-RESEARCH.md § Package Legitimacy Audit` es N/A, cero cambios a `pyproject.toml` de cualquier paquete y a `uv.lock`. Verificado en el gate de cierre (plan 42-06) con `git diff --exit-code uv.lock` |
</threat_model>

<verification>
1. `uv run --frozen ruff check .` → sin errores.
2. `uv run --frozen ruff format --check .` → sin diferencias.
3. `uv run --frozen mypy` → `Success` (scope `packages/*/src`; `scripts/` y `verification/` quedan
   fuera del gate global por `pyproject.toml:97`).
4. `uv run pytest -q` sobre las **13** rutas de la allowlist de `.github/workflows/ci.yml` →
   `0 failed`, `passed` > 129 (baseline medido en HEAD).
5. `uv run python scripts/literal_census_33.py --selftest` → `SELFTEST: PASS`, exit 0.
6. `git hash-object verification/mutation_gate.py` = `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
7. La respuesta del operador al checkpoint está transcrita verbatim en el SUMMARY.
8. **Cero llamadas de red ocurrieron durante este plan.**
</verification>

<success_criteria>
- El gate de venue del censo es igualdad exacta de hostname contra la MISMA referencia que
  `main_matriz.py` publica, pinneado por identidad de objeto (`is`).
- Un host de spoofing por sufijo hostil y uno por userinfo son rechazados, probados por test.
- El lock corre en CI (línea presente en la allowlist explícita), no sólo en local.
- El censo emite `CENSUS-HEADER venue=… captured_at=…` antes de la primera request, y una línea
  `CENSUS-DLOCK` que reafirma el D-lock (b) de la Phase 29.
- `--matriz-only` corre sólo el censo de matriz y devuelve exit 0 en una corrida exitosa.
- `verification/mutation_gate.py` byte-idéntico y su fail-closed bajo bbsa pinneado por test.
- Aprobación humana explícita registrada antes de cualquier tráfico.
</success_criteria>

<output>
Create `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md` when done.

El SUMMARY debe incluir, además del formato estándar:
- La respuesta del operador al checkpoint, **transcrita verbatim**.
- El conteo `passed` de la corrida de las 13 rutas de la allowlist, comparado contra el baseline 129.
- El `git hash-object` de `verification/mutation_gate.py` medido al cierre.
</output>
