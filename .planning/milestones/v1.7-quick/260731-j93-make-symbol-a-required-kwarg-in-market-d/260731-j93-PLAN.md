---
phase: quick-260731-j93
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/src/market_data_client/client.py
  - packages/market-data-client/src/market_data_client/aio.py
  - packages/market-data-client/src/market_data_client/_core.py
  - main_market_data.py
  - packages/market-data-client/tests/test_client.py
  - packages/market-data-client/tests/test_async_client.py
  - packages/market-data-client/tests/test_market_data.py
autonomous: true
requirements: [F-01, F-13]

must_haves:
  truths:
    - "Calling get_latest() with no symbol raises TypeError at call time (sync + async, method + top-level shim)"
    - "build_latest_request requires a symbol argument"
    - "The main_market_data.py latest probes pass a symbol and no longer trigger HTTP 422"
    - "get_latest_batch (POST) is unchanged"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/client.py"
      provides: "sync get_latest with required symbol (method + top-level shim)"
      contains: "symbol: str,"
    - path: "packages/market-data-client/src/market_data_client/aio.py"
      provides: "async get_latest with required symbol (method + top-level shim)"
      contains: "symbol: str,"
    - path: "packages/market-data-client/src/market_data_client/_core.py"
      provides: "build_latest_request with required symbol"
      contains: "symbol: str,"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/client.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "get_latest calls build_latest_request(state, symbol=symbol, ...)"
      pattern: "build_latest_request"
    - from: "main_market_data.py"
      to: "packages/market-data-client/src/market_data_client/client.py"
      via: "probe_latest_sync/async call get_latest(symbol=_SAMPLE_SYMBOLS[0])"
      pattern: "get_latest\\(symbol="
---

<objective>
Make `symbol` a REQUIRED keyword-only `str` in the market-data-client `GET /marketdata/latest` surface — the sync `Client.get_latest`, the async `AsyncClient.get_latest`, both top-level shims, and the `build_latest_request` builder — then update the live-verification probes to pass a symbol and add mocked regression tests proving the enforcement.

Purpose: Close confirmed live findings F-01 (latest_sync) and F-13 (latest_async). The live develop API marks the `symbol` query param `required=True` in its OpenAPI; omitting it returns HTTP 422 `{"detail":[{"type":"missing","loc":["query","symbol"],"msg":"Field required"}]}`. The current client defaults `symbol` to `None`, which `_params.drop_none` then strips, guaranteeing a 422 on every call.

Output: Signature changes across `client.py`, `aio.py`, `_core.py`; probe fixes in `main_market_data.py`; new mocked sync + async regression tests.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# Production surface (5 signature sites)
@packages/market-data-client/src/market_data_client/client.py
@packages/market-data-client/src/market_data_client/aio.py
@packages/market-data-client/src/market_data_client/_core.py

# Probe vehicle (4 call sites)
@main_market_data.py

# Test patterns to mirror
@packages/market-data-client/tests/test_client.py
@packages/market-data-client/tests/test_async_client.py
@packages/market-data-client/tests/test_market_data.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make symbol a required kwarg across the production latest surface</name>
  <files>packages/market-data-client/src/market_data_client/client.py, packages/market-data-client/src/market_data_client/aio.py, packages/market-data-client/src/market_data_client/_core.py</files>
  <action>
    Change `symbol` from `symbol: str | None = None` to a required keyword-only `symbol: str` (drop the ` | None = None`) at all FIVE sites, keeping the `*` so it stays keyword-only, and keeping `market_id` and `entries` optional (they are `required=False` in the live OpenAPI). Sites, per F-01/F-13:
    1. `client.py` — `Client.get_latest` method (signature around line 403-408).
    2. `client.py` — top-level `get_latest` shim (around line 610-617). It delegates `symbol=symbol`, so its param must also become required.
    3. `aio.py` — `AsyncClient.get_latest` method (around line 417-423).
    4. `aio.py` — top-level async `get_latest` shim (around line 622-629).
    5. `_core.py` — `build_latest_request` builder (around line 329-335). `symbol` still flows through `_params.drop_none` but is now always present.
    Do NOT touch `get_latest_batch` / `build_latest_batch_request` (POST body path — correct as-is). Update the `build_latest_request` docstring line that says None optionals are dropped so it notes `symbol` is now required (market_id/entries remain droppable). Mirror sync and async verbatim per the CLAUDE.md dual-surface rule. Preserve `from __future__ import annotations`, double quotes, 4-space indent, 100-col.
  </action>
  <verify>
    <automated>uv run --package market-data-client python -c "import inspect, market_data_client as m, market_data_client.aio as a, market_data_client._core as c; p=lambda f:inspect.signature(f).parameters['symbol']; assert all(x.default is inspect.Parameter.empty for x in [p(m.get_latest), p(a.get_latest), p(c.build_latest_request), p(m.client.Client.get_latest), p(a.AsyncClient.get_latest)]), 'symbol still optional somewhere'; print('symbol required at all 5 sites')"</automated>
  </verify>
  <done>All five signatures declare `symbol: str` with no default; `get_latest_batch` untouched; sync and async mirrors identical.</done>
</task>

<task type="auto">
  <name>Task 2: Pass a symbol in the main_market_data.py latest probes</name>
  <files>main_market_data.py</files>
  <action>
    Fix the four probe call sites that call the latest surface without a symbol so they draw from the existing `_SAMPLE_SYMBOLS` constant (`_SAMPLE_SYMBOLS = ["GGAL"]`, line 88), using the first element `_SAMPLE_SYMBOLS[0]`:
    1. `probe_latest_sync` — `client.get_latest()` (line ~392) → `client.get_latest(symbol=_SAMPLE_SYMBOLS[0])`.
    2. `probe_latest_sync` — `_core.build_latest_request(client._state)` (line ~394) → add `symbol=_SAMPLE_SYMBOLS[0]`.
    3. `probe_latest_async` — `await aclient.get_latest()` (line ~678) → `await aclient.get_latest(symbol=_SAMPLE_SYMBOLS[0])`.
    4. `probe_latest_async` — `_core.build_latest_request(aclient._state)` (line ~680) → add `symbol=_SAMPLE_SYMBOLS[0]`.
    Do not change `get_latest_batch(LatestRequest(symbols=_SAMPLE_SYMBOLS))` calls — already correct.
  </action>
  <verify>
    <automated>grep -n "get_latest(" main_market_data.py | grep -v batch | grep -v "symbol=" | grep -v "def " | grep -c . | grep -qx 0 && echo "no symbol-less get_latest calls remain"</automated>
  </verify>
  <done>Both latest probes (sync + async) pass `symbol=_SAMPLE_SYMBOLS[0]` to `get_latest` and to `build_latest_request`; batch calls unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add mocked sync + async regression tests for required symbol</name>
  <files>packages/market-data-client/tests/test_client.py, packages/market-data-client/tests/test_async_client.py, packages/market-data-client/tests/test_market_data.py</files>
  <behavior>
    - Sync: `market_data_client.client._get_default().get_latest()` (no symbol) raises `TypeError` at call time — no HTTP request is made.
    - Async: `await market_data_client.aio._get_default().get_latest()` (no symbol) raises `TypeError`.
    - Builder: `_core.build_latest_request(_ClientState())` with no `symbol` raises `TypeError`.
    - (Regression already covered) existing `test_get_latest_sends_bearer_and_params` / async mirror still pass `symbol="GGAL"` and assert `req.url.params.get("symbol") == "GGAL"` — leave them green.
  </behavior>
  <action>
    Add three focused tests mirroring the existing style in each file (import `pytest`, use `pytest.raises(TypeError)`):
    1. In `test_client.py`, next to `test_get_latest_sends_bearer_and_params` (line ~219): `test_get_latest_requires_symbol` — `with pytest.raises(TypeError): market_data_client.client._get_default().get_latest()`.
    2. In `test_async_client.py`, next to `test_async_get_latest_sends_bearer_and_params` (line ~243): async `test_async_get_latest_requires_symbol` — `with pytest.raises(TypeError): await market_data_client.aio._get_default().get_latest()`.
    3. In `test_market_data.py`, next to `test_build_latest_request_shape` (line ~99): `test_build_latest_request_requires_symbol` — `with pytest.raises(TypeError): _core.build_latest_request(_ClientState())`.
    These assert signature enforcement (TypeError raised before any HTTP dispatch), so no httpx_mock response is needed. Follow the file's existing import aliases and fixture conventions.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_client.py::test_get_latest_requires_symbol packages/market-data-client/tests/test_async_client.py::test_async_get_latest_requires_symbol packages/market-data-client/tests/test_market_data.py::test_build_latest_request_requires_symbol -q</automated>
  </verify>
  <done>Three new tests pass; the pre-existing latest tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → live develop API | Query params cross to a third-party service; `symbol` is now mandatory input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j93-01 | Tampering | get_latest query params | accept | Signature-level `str` requirement is the only change; no new trust surface, no new packages, no credential handling touched |

No package-manager installs in this plan → no legitimacy gate required.
</threat_model>

<verification>
Run the package quality gates (per quick-task constraints), all must be green:
- `uv run --package market-data-client ruff check`
- `uv run --package market-data-client ruff format --check`
- `uv run --package market-data-client mypy packages/market-data-client/src`
- `uv run --package market-data-client pytest packages/market-data-client -q`
</verification>

<success_criteria>
- `symbol` is a required keyword-only `str` at all 5 production sites (sync method + sync shim + async method + async shim + `build_latest_request`); `get_latest_batch` untouched.
- Both latest probes in `main_market_data.py` pass `symbol=_SAMPLE_SYMBOLS[0]`.
- Three new regression tests (sync, async, builder) prove `TypeError` on omission; existing latest tests still pass.
- ruff check + ruff format --check + mypy strict + pytest all green for the package.
</success_criteria>

<output>
Create `.planning/quick/260731-j93-make-symbol-a-required-kwarg-in-market-d/260731-j93-SUMMARY.md` when done.
</output>
