---
phase: 12-codegen-spike
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit-run.log
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/README.md
autonomous: false
requirements: [REFAC-06]
tags: [spike, codegen, unasync, marker, matriz, audit, deny-list]
must_haves:
  truths:
    - "@generated marker comment line (`# @generated by unasync from aio.py — DO NOT EDIT BY HAND.`) prepended to ámbito client_generated.py does NOT break ruff check, ruff format --check, mypy --strict, or ast.parse (D-RIGOR-01 item 8)"
    - "Every async-only construct in matriz aio.py 852 LOC is classified as either deny-list-confirmed (lives in _token_store.py / _refresh_policy.py / _refresh.py / ws_client.py) or manual-sync-proof (additional_replacements covers it) or comment-only (inside docstring) — ZERO TBD/REVIEW/DENY-LIST-VIOLATION rows (D-SCOPE-02 merge gate)"
    - "Sha256 of matriz _token_store.py, _refresh_policy.py, ws_client.py computed before AND after a simulated single-file Rule run is BYTE-IDENTICAL (deny-list intact; codegen scope is aio.py-only, never the deny-listed files)"
    - "Per-package Rule config drafts captured for ámbito (Plan 01 FINDING.md), iol, higyrus, matriz — inferred from ámbito recipe plus matriz construct audit deltas"
  artifacts:
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py"
      provides: "Prepends @generated marker to client_generated.py, runs ruff check + ruff format --check + mypy --strict + ast.parse, writes verification_transcripts.txt"
      min_lines: 40
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt"
      provides: "Captured stdout/stderr + exit codes of the 4 verification commands"
      contains: "exit"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md"
      provides: "Verdict (PASS|FAIL) + exact marker syntax that passed/failed + final 6-line file header layout (marker / blank / docstring / blank / future import)"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit.py"
      provides: "stdlib ast.walk over matriz aio.py emitting the classification table"
      min_lines: 60
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md"
      provides: "Classification table of every async-only construct in matriz aio.py 852 LOC, with operator-triaged rows showing zero unresolved rows OR FAIL gate"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md"
      provides: "Verdict (PASS|FAIL) + summary of operator triage of REVIEW rows + Rule config draft for matriz"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py"
      provides: "sha256 before+after simulated single-file Rule run targeting matriz aio.py only"
      min_lines: 30
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md"
      provides: "Verdict (PASS|FAIL) + per-file sha256 hexdigests confirming deny-list files untouched + draft of how Rule scope (file list, not directory) achieves the deny-list"
  key_links:
    - from: "001b/experiment.py"
      to: ".../001a-ambito-round-trip/output/client_generated.py"
      via: "Path.read_text/write_text — prepend marker comment"
      pattern: "@generated by unasync"
    - from: "001c/audit.py"
      to: "packages/matriz-client/src/matriz_client/aio.py"
      via: "ast.parse(path.read_text()) + ast.walk"
      pattern: "ast\\.walk"
    - from: "001d/experiment.py"
      to: "packages/matriz-client/src/matriz_client/{_token_store,_refresh_policy,ws_client,_refresh}.py"
      via: "hashlib.sha256(path.read_bytes()).hexdigest() before+after"
      pattern: "sha256"
---

<objective>
Wave 2 + Wave 3 + Wave 4 of the codegen spike: validate the `@generated` marker compatibility with `from __future__ import annotations` (D-RIGOR-01 item 8), enumerate every async-only construct in matriz `aio.py` 852 LOC and prove zero unresolved rows (D-SCOPE-02 merge gate), and confirm matriz deny-list files (`_token_store.py`, `_refresh_policy.py`, `ws_client.py`, `_refresh.py`) are byte-identical before and after a simulated codegen Rule run targeting only aio.py.

Purpose: covers 3 of the remaining 5 evidence items in D-RIGOR-01 (items 4, 5, 8 are addressed in 001b; item 7 import-linter is verified in Plan 03; matriz scope items not in D-RIGOR-01 but mandated by ROADMAP SC#3 + D-SCOPE-02 are addressed in 001c + 001d). Outputs feed directly into Plan 03's 8-item evidence checklist execution and the DECISION.md per-package Rule config block.

Output: 3 sub-experiments fully executed (001b + 001c + 001d) with verdict-flipped READMEs, populated FINDING.md files (each containing PASS|FAIL + transcripts + draft Rule config piece), and the matriz construct audit `matriz-aio-constructs.md` with zero unresolved rows OR explicit FAIL gate documentation.

Honors locked decisions: D-RIGOR-01 (items 4, 5, 8), D-SCOPE-02 (zero TBD merge gate), D-NOGO-01 (no libcst inline if 001b/c/d surface unresolvable issues). Anti-Pitfalls 5 (matriz audit TBD soft-relax) and 6 (marker placement breaking future import).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/12-codegen-spike/12-CONTEXT.md
@.planning/phases/12-codegen-spike/12-RESEARCH.md
@.planning/phases/12-codegen-spike/12-VALIDATION.md
@.planning/phases/12-codegen-spike/12-01-SUMMARY.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/README.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
@.planning/spikes/CONVENTIONS.md
@packages/matriz-client/src/matriz_client/aio.py
@packages/matriz-client/src/matriz_client/_token_store.py
@packages/matriz-client/src/matriz_client/_refresh_policy.py
@packages/matriz-client/src/matriz_client/ws_client.py
</context>

<tasks>

<task id="12-02-01" type="auto" tdd="false">
  <name>Task 12-02-01: 001b @generated marker × from __future__ import compatibility experiment</name>
  <read_first>
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 5 — @generated Marker × from __future__ import annotations Compatibility" (full)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Pitfall 6 — Marker comment placement breaking the future import"
    - CLAUDE.md §"Code Style" (`from __future__ import annotations` mandatory at module top after comments + optional docstring)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py (the file Plan 01 produced — input to this experiment)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md (need the verdict — if Plan 01 was FAIL, this task still runs to document marker compatibility independent of 001a outcome)
  </read_first>
  <action>
    Decision precondition: if `.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md` has `**Verdict:** FAIL`, this experiment STILL RUNS (the marker test is independent of the round-trip outcome — even on the NO-GO branch, Plan 03 needs to know whether the marker would have worked). Note in 001b/FINDING.md: "Ran for evidence completeness despite 001a FAIL."

    Replace the placeholder `.../001b-ambito-marker-future-compat/experiment.py` with a script following Recipe 5 exactly. Required behavior:

    Step 1 — `from __future__ import annotations` + module docstring `"""Spike 001b: verify @generated marker × from __future__ import annotations compatibility (D-RIGOR-01 item 8)."""` + imports (`subprocess`, `pathlib.Path`).

    Step 2 — Path resolution: `REPO_ROOT = Path(__file__).resolve().parents[4]`; `GENERATED_ORIG = REPO_ROOT / ".planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py"`. Make a copy at `WORK = Path(__file__).resolve().parent / "client_with_marker.py"` via `WORK.write_text(GENERATED_ORIG.read_text())`. The original file is preserved (we operate on a copy so Plan 03's evidence collection sees the unaltered output).

    Step 3 — Prepend the canonical marker. EXACT marker block (per Recipe 5, line ordering: marker → blank → existing-content):
        `marker = "# @generated by unasync from aio.py — DO NOT EDIT BY HAND.\n# To modify, edit aio.py and run `make codegen` (Phase 16 setup).\n\n"`
        `WORK.write_text(marker + WORK.read_text())`
    The resulting file MUST have the docstring (which was already at line 1 of client_generated.py) shifted down — line 1 = marker / line 2 = marker continuation / line 3 = blank / line 4+ = docstring → blank → `from __future__ import annotations` → rest. This is the layout Recipe 5 confirms is PEP 263 / future-statement compliant.

    Step 4 — Run 4 verification commands in sequence via `subprocess.run(..., capture_output=True, text=True, cwd=REPO_ROOT)`. Capture stdout, stderr, and exit code for each.
      (a) `uv run ruff check <WORK>`  — D-RIGOR-01 item 4
      (b) `uv run ruff format --check <WORK>` — D-RIGOR-01 item 3 (idempotency / format-stable)
      (c) `uv run mypy --strict <WORK>` — D-RIGOR-01 item 5
      (d) `uv run python -c "import ast; ast.parse(open('<WORK>').read())"` — D-RIGOR-01 item 8 (proves marker doesn't break Python grammar)

    Step 5 — Compose `.../001b.../verification_transcripts.txt` with 4 clearly-labeled sections (one per command). Format per Recipe 5:
        `=== ruff check ===\nexit <rc>\n<stdout>\n<stderr>\n=== ruff format --check ===\n...`

    Step 6 — Aggregate PASS/FAIL: marker is compatible iff all 4 commands exit 0. Script exits 0 if compatible, 1 otherwise. Print summary: `SPIKE 001b VERDICT: <PASS|FAIL>` with per-command pass/fail breakdown.

    Then RUN the experiment from the repo root:
      `uv run python .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py`

    Then update `.../001b.../FINDING.md`:
      - `# FINDING: @generated Marker × `from __future__` Compatibility`
      - `**Verdict:** PASS|FAIL`
      - `## Marker Syntax Verified` section: emit the exact marker block used.
      - `## File Header Layout After Marker` section: paste lines 1-10 of WORK (so the operator can see marker / blank / docstring / blank / future import sequence).
      - `## 4-Command Transcripts` section: paste contents of verification_transcripts.txt.
      - `## Anti-Pitfall 6 Compliance` section: confirm the marker was prepended via `str.__add__` (not `str.replace`), so line ordering is deterministic.
      - `## Recommendation for Phase 16` section: if PASS, recommend Phase 16 pre-commit hook detect the marker via `head -1 <file> | grep -q '^# @generated by unasync'`. If FAIL, document which command failed + suggest alternative marker syntax to try (e.g., shebang-style on line 1 vs comment, or marker AFTER docstring vs before).

    Update `.../001b/README.md` frontmatter: flip `verdict: TBD` to actual verdict.

    Anti-Pitfall 6 enforced via script structure (Recipe 5 requires `str.__add__` concat to line-1, not `str.replace`).
  </action>
  <verify>
    <automated>
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/client_with_marker.py && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt && \
      grep -q '^# @generated by unasync' .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/client_with_marker.py && \
      head -1 .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/client_with_marker.py | grep -q '@generated' && \
      grep -qE '^\*\*Verdict:\*\* (PASS|FAIL)' .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md && \
      grep -qE '^verdict: (PASS|FAIL)$' .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/README.md && \
      grep -cE '^=== (ruff check|ruff format --check|mypy --strict|ast\.parse) ===$' .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/verification_transcripts.txt | grep -q '^4$' && \
      # Negative assertion (WARNING #3 from plan checker): confirm 001a/output/client_generated.py was NOT mutated by the 001b marker experiment.
      # 001b writes the marker only to its local client_with_marker.py copy; the original 001a output must stay marker-free.
      head -1 .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py | grep -qv "^# @generated" && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/{experiment.py,client_with_marker.py,verification_transcripts.txt,FINDING.md,README.md}</files>
  <done>experiment.py runs; client_with_marker.py has the marker on line 1; verification_transcripts.txt has 4 labeled sections; FINDING.md verdict + README.md verdict no longer TBD; zero mutations under packages/.</done>
  <acceptance_criteria>
    - `client_with_marker.py` exists and has the @generated marker on line 1.
    - `verification_transcripts.txt` has 4 sections (ruff check, ruff format --check, mypy --strict, ast.parse) each with its exit code.
    - FINDING.md has Verdict + Marker Syntax Verified + File Header Layout (lines 1-10 pasted) + 4-Command Transcripts + Anti-Pitfall 6 Compliance + Recommendation for Phase 16 sections.
    - README.md `verdict:` flipped from TBD.
    - Anti-Pitfall 6: marker prepended via `str.__add__` (verifiable by re-reading experiment.py and confirming there is no `.replace(...)` of the marker line — assert via `grep -L 'replace' experiment.py`).
    - 001a output integrity (negative assertion): `head -1 .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py` does NOT start with `# @generated` — the 001a generated file remains marker-free; 001b writes the marker only to `client_with_marker.py` copy.
    - `git status --porcelain packages/` is empty.
  </acceptance_criteria>
</task>

<task id="12-02-02" type="auto" tdd="false">
  <name>Task 12-02-02: 001c Matriz construct audit — ast.walk + classification table + operator triage</name>
  <read_first>
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 4 — Matriz Construct Audit Recipe" (entire including DENY_LIST set, KNOWN_SYNC_EMISSIONS, HARD_DENY, sample expected REVIEW rows)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Pitfall 5 — Matriz audit with TBD rows accepted"
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §D-SCOPE-02 (zero TBD merge gate)
    - packages/matriz-client/src/matriz_client/aio.py (the audit subject — 852 LOC; only read targeted ranges via Grep to identify async-only construct locations; do NOT load the whole file into context twice)
  </read_first>
  <action>
    Replace the placeholder `.../001c-matriz-construct-audit/audit.py` with a script implementing Recipe 4 exactly. The script:

    Step 1 — `from __future__ import annotations` + module docstring + imports (`ast`, `pathlib.Path`, `sys`).

    Step 2 — Path resolution: `REPO_ROOT = Path(__file__).resolve().parents[4]`; `SOURCE = REPO_ROOT / "packages/matriz-client/src/matriz_client/aio.py"`; `OUTPUT = Path(__file__).resolve().parent / "matriz-aio-constructs.md"`.

    Step 3 — Define constants per Recipe 4:
      - `DENY_LIST = {"_token_store.py", "_refresh_policy.py", "_refresh.py", "ws_client.py"}` (4 files explicitly out of codegen scope)
      - `KNOWN_SYNC_EMISSIONS` dict mapping construct name → sync emission rule (AsyncFunctionDef→FunctionDef, AsyncWith→With, AsyncFor→For, __aenter__→__enter__, aclose→close, AsyncClient→Client, AsyncRetryTransport→RetryTransport, _atransport→_transport, etc.)
      - `HARD_DENY` dict mapping `asyncio.Lock`, `asyncio.to_thread`, `_get_async_lock`, `_async_locks` to "lives in _token_store.py — deny-listed". HARD_DENY presence in aio.py module body (NOT in import statements that re-export from `_token_store`) would be a deny-list violation.

    Step 4 — Parse the source: `tree = ast.parse(SOURCE.read_text())`.

    Step 5 — Walk: for each `ast.AsyncFunctionDef` / `ast.AsyncWith` / `ast.AsyncFor` / `ast.Await`, append a row `(line, construct, classification, evidence)` where classification = `manual-sync-proof` and evidence references the KNOWN_SYNC_EMISSIONS rule. For each `ast.Attribute` whose `.value.id == "asyncio"`: lookup name in HARD_DENY first (if present → classification = `DENY-LIST-VIOLATION`); else classification = `REVIEW` (operator must triage).

    Step 6 — Detect docstring-only mentions: walk top-level docstrings (module docstring, function docstrings) and string literals. If an `asyncio.*` mention appears inside a string (not bare module-level code), tag as `comment-only` — unasync tokenizer preserves these verbatim. (Heuristic: any `asyncio.*` line whose `ast.lineno` is INSIDE a `ast.Expr(value=ast.Constant(value=str))` node is comment-only.)

    Step 7 — Emit the table to OUTPUT:
        `# Matriz aio.py — async-only construct audit\n`
        `**Source:** packages/matriz-client/src/matriz_client/aio.py (852 LOC)\n`
        `**Found:** N async-only construct occurrences\n\n`
        `| Line | Construct | Classification | Evidence |\n|------|-----------|----------------|----------|\n`
        ... rows sorted by line number ...
        `\n## Unresolved rows: <count>\n`
        followed by either `\n**MERGE GATE PASS:** zero unresolved rows.\n` OR `\n**MERGE GATE FAILURE:** D-SCOPE-02 requires zero unresolved rows.\n`.

    Step 8 — Exit codes: script exits 0 if zero `REVIEW` / `TBD` / `DENY-LIST-VIOLATION` rows REMAIN after operator triage (see Step 9). Initially, after the automated run, the table will have `REVIEW` rows that the operator triages MANUALLY by editing the markdown.

    Step 9 — After running the audit, perform operator triage in this same task (since the operator's role is delegated to the Claude executor reading the spike context): for each `REVIEW` row, classify according to Recipe 4 guidance:
      - If the `asyncio.*` reference appears INSIDE a `_token_store.py` / `_refresh_policy.py` / `_refresh.py` / `ws_client.py` import statement (i.e., aio.py imports a NAME re-exported by a deny-listed file) → flip to `deny-list-confirmed` with evidence pointer.
      - If the `asyncio.*` reference appears inside a docstring (string literal) → flip to `comment-only` with evidence "inside docstring at line N".
      - If the `asyncio.*` reference is genuine async-only code in aio.py module body (e.g., `asyncio.gather`, `asyncio.wait_for`) → MUST find an `additional_replacements` rule OR a deny-list rule. If neither exists, classification stays `REVIEW` and the merge gate FAILS (D-SCOPE-02). Document in FINDING.md as NO-GO root cause if unfixable.
      - HARD_DENY (`asyncio.Lock`, `asyncio.to_thread`, `_get_async_lock`, `_async_locks`) appearances directly in aio.py body (NOT via import re-export) are FATAL — flip to `DENY-LIST-VIOLATION` and merge gate FAILS.

    Step 10 — Run the audit + post-triage gate:
        `uv run python audit.py 2>&1 | tee audit-run.log` (run from `.planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/`).
        The `tee` capture writes `audit-run.log` next to audit.py — WARNING #4 mitigation: this proves the audit was actually executed (not hand-crafted). The log MUST contain the `Audit written to ...` line emitted by audit.py Step 7.
        Then manually edit `.../001c.../matriz-aio-constructs.md` to flip REVIEW rows per the criteria above (do NOT re-run the script — once triaged the table is the source-of-truth). After triage, the gate check:
        `grep -cE '\| (REVIEW|TBD|DENY-LIST-VIOLATION) \|' .planning/spikes/.../matriz-aio-constructs.md` MUST return 0.

    Then update `.../001c-matriz-construct-audit/FINDING.md`:
      - `# FINDING: Matriz aio.py Construct Audit`
      - `**Verdict:** PASS|FAIL` (PASS iff zero unresolved rows after triage; FAIL otherwise)
      - `## Audit Summary` section: total rows / breakdown by classification (manual-sync-proof, deny-list-confirmed, comment-only, REVIEW, DENY-LIST-VIOLATION).
      - `## Triage Notes` section: row-by-row reasoning for each REVIEW → final classification flip (the operator audit trail).
      - `## Matriz Rule Config Draft` section: emit the `unasync.Rule(fromdir=..., todir=..., additional_replacements={...})` block for matriz (a SUPERSET of ámbito's, with matriz-specific tokens like `_async_locks_remove`, `_get_async_lock`, etc. excluded because they live in `_token_store.py` per deny-list). Per CONTEXT.md inferred from ámbito + matriz construct audit deltas.
      - `## Anti-Pitfall 5 Compliance` section: confirm zero TBD/REVIEW/DENY-LIST-VIOLATION rows + the programmatic grep gate (Recipe 4 + Pitfall 5).
      - `## Open Questions for Operator` section: any DENY-LIST-VIOLATION rows or unfixable REVIEW rows that need operator approval before signoff.

    Update `.../001c/README.md` frontmatter: flip `verdict: TBD` to actual verdict.

    Anti-Pitfall 5 enforced via the grep-gate (Step 10) and via the FINDING.md Triage Notes section.
  </action>
  <verify>
    <automated>
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit.py && \
      grep -q '^| Line | Construct |' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md && \
      test "$(grep -cE '\| (REVIEW|TBD|DENY-LIST-VIOLATION) \|' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md)" = "0" && \
      grep -qE '^\*\*Verdict:\*\* (PASS|FAIL)' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md && \
      grep -qE '^verdict: (PASS|FAIL)$' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/README.md && \
      grep -q 'MERGE GATE PASS' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md && \
      # Audit-run evidence (WARNING #4 from plan checker): confirm audit.py was actually executed (not hand-crafted file).
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit-run.log && \
      grep -q "Audit written to" .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit-run.log && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/{audit.py,audit-run.log,matriz-aio-constructs.md,FINDING.md,README.md}</files>
  <done>audit.py runs; matriz-aio-constructs.md exists with classification table AND zero REVIEW/TBD/DENY-LIST-VIOLATION rows (D-SCOPE-02 merge gate); FINDING.md + README.md verdict no longer TBD; zero mutations under packages/.</done>
  <acceptance_criteria>
    - `audit.py` exists, starts with `from __future__ import annotations`, uses stdlib `ast` only.
    - `matriz-aio-constructs.md` has the classification table.
    - After operator triage: ZERO `REVIEW` / `TBD` / `DENY-LIST-VIOLATION` rows (D-SCOPE-02 merge gate; Anti-Pitfall 5).
    - The MERGE GATE PASS line is present in `matriz-aio-constructs.md`.
    - Audit-run evidence: `audit-run.log` exists next to `audit.py` AND contains the line `Audit written to ...` emitted by audit.py Step 7 (proves the script was actually executed, not the table hand-crafted).
    - FINDING.md has Verdict + Audit Summary (with breakdown counts) + Triage Notes (per-REVIEW reasoning) + Matriz Rule Config Draft + Anti-Pitfall 5 Compliance + Open Questions.
    - README.md `verdict:` flipped from TBD.
    - `git status --porcelain packages/` is empty.
    - If the verdict is FAIL (unresolvable rows survive), the FINDING.md "Open Questions" section MUST escalate to the operator with concrete reproducible failure (e.g., "Line 437 `asyncio.gather` has no sync equivalent and is not in any deny-listed module — cannot codegen") and the spike is heading toward NO-GO.
  </acceptance_criteria>
</task>

<task id="12-02-03" type="auto" tdd="false">
  <name>Task 12-02-03: 001d Matriz deny-list intactness — sha256 before/after simulated single-file Rule run</name>
  <read_first>
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Wave 4: 001d — Matriz deny-list scope verification" (in Architecture Patterns diagram)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 1 — unasync 0.6.0 Invocation Recipe" (Rule fpath_list mechanics — file-list scope NOT directory scope; the deny-list achieved by EXCLUDING files from fpath_list)
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §"Decisiones implícitas heredadas" (Codegen deny-list = `_token_store.py` + `_refresh_policy.py` + `ws_client.py`; locked by ARCHITECTURE.md research)
    - packages/matriz-client/src/matriz_client/_token_store.py (one of the deny-listed files; sha256 sanity check)
    - packages/matriz-client/src/matriz_client/_refresh_policy.py (deny-listed)
    - packages/matriz-client/src/matriz_client/ws_client.py (deny-listed — daemon thread)
  </read_first>
  <action>
    Replace the placeholder `.../001d-matriz-deny-list-config/experiment.py` with a script that:

    Step 1 — `from __future__ import annotations` + module docstring `"""Spike 001d: matriz deny-list intactness — sha256 before/after simulated codegen run."""` + imports (`hashlib`, `shutil`, `subprocess`, `sys`, `pathlib.Path`).

    Step 2 — Path resolution: `REPO_ROOT = Path(__file__).resolve().parents[4]`; `MATRIZ_DIR = REPO_ROOT / "packages/matriz-client/src/matriz_client"`; `WORK = Path(__file__).resolve().parent / "matriz_copy"` (recreated each run via `shutil.rmtree(WORK, ignore_errors=True); WORK.mkdir()`).

    Step 3 — Define the deny-list: `DENY_LIST = ["_token_store.py", "_refresh_policy.py", "_refresh.py", "ws_client.py"]`. Note: `_refresh.py` is included per Recipe 4 (matriz refresh adapter, also not codegen-targeted).

    Step 4 — Copy the ENTIRE matriz_client/ directory to WORK via `shutil.copytree(MATRIZ_DIR, WORK / "matriz_client")`. The work directory now contains a sandboxed mirror of the production package.

    Step 5 — Compute pre-run sha256 hashes for all 5 files of interest: `aio.py` (the codegen target) + each of the 4 deny-listed files. Store as a dict `pre_hashes = {name: hashlib.sha256((WORK / "matriz_client" / name).read_bytes()).hexdigest() for name in ["aio.py", *DENY_LIST]}`.

    Step 6 — Simulate the codegen run with file-list scope (NOT directory scope), per Recipe 1's note that unasync's `fpath_list` is a per-file list. Invoke `unasync.unasync_files(fpath_list=[str(WORK / "matriz_client" / "aio.py")], rules=[unasync.Rule(fromdir=str(WORK / "matriz_client") + "/", todir=str(WORK / "matriz_client") + "/", additional_replacements={...})])`. additional_replacements: use the matriz Rule draft from 001c FINDING.md (read it first; if 001c FINDING.md says FAIL, use the BEST-EFFORT draft AND document the limitation in 001d FINDING.md). The KEY POINT being validated: `fpath_list` contains ONLY `aio.py`, so unasync operates on aio.py alone — the 4 deny-listed files are NEVER passed to the tool.

    Step 7 — Compute post-run sha256 hashes for the same 5 files via the same dict comprehension into `post_hashes`. The post-hash for `aio.py` will DIFFER (it was transformed → became client.py-shape). The post-hashes for the 4 deny-listed files MUST be IDENTICAL to their pre-hashes (the deny-list intactness invariant).

    Step 8 — Verdict: PASS iff `pre_hashes[name] == post_hashes[name]` for ALL names in DENY_LIST (the 4 deny-listed files unchanged). FAIL if any deny-listed file's sha256 changed.

    Step 9 — Write `.../001d.../verification_transcripts.txt` with per-file sha256 hex tuples:
        `_token_store.py:\n  pre:  <hex>\n  post: <hex>\n  intact: PASS|FAIL\n`
        ... for each deny-listed file ...
        `aio.py:\n  pre:  <hex>\n  post: <hex>\n  transformed: PASS|FAIL  (post MUST differ from pre)\n`

    Step 10 — Script exits 0 if all 4 deny-list intactness checks PASS AND aio.py transformed (post differs from pre); exits 1 otherwise.

    RUN: `uv run --with unasync python .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py`

    Then update `.../001d.../FINDING.md`:
      - `# FINDING: Matriz Deny-List Intactness`
      - `**Verdict:** PASS|FAIL`
      - `## Deny-List Scope` section: list the 4 files + rationale (`_token_store.py` 3-way TokenStore, `_refresh_policy.py` retry+backoff+fail-cache, `_refresh.py` adapter, `ws_client.py` daemon thread). Confirm these are LOCKED per ARCHITECTURE.md.
      - `## Sha256 Transcript` section: paste contents of verification_transcripts.txt.
      - `## How the Deny-List Is Achieved in unasync 0.6.0` section: explain that unasync has no `exclude=` param (per Recipe 1 note); instead, the spike (and Phase 16 production) achieves the deny-list by ONLY listing `aio.py` in `fpath_list`. Provide the recommended Phase 16 production pattern: a `make codegen` Makefile target that explicitly enumerates the file list.
      - `## Anti-Pitfall 6 Compliance` section: confirm the codegen never even SEES the deny-listed files (sha256 immutable, fpath_list scope confirmed).
      - `## Recommended Phase 16 Implementation` section: a 5-line snippet showing how Phase 16's codegen script would invoke `unasync.unasync_files(fpath_list=[<each aio.py per package>], rules=[<per-package Rule>])` to keep the deny-list automatically respected.

    Update `.../001d/README.md` frontmatter: flip `verdict: TBD` to actual verdict.
  </action>
  <verify>
    <automated>
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt && \
      grep -q '_token_store.py:' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt && \
      grep -q '_refresh_policy.py:' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt && \
      grep -q 'ws_client.py:' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt && \
      grep -q 'aio.py:' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt && \
      grep -qE '^\*\*Verdict:\*\* (PASS|FAIL)' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md && \
      grep -qE '^verdict: (PASS|FAIL)$' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/README.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/{experiment.py,verification_transcripts.txt,FINDING.md,README.md}</files>
  <done>experiment.py runs; verification_transcripts.txt has pre/post sha256 for the 4 deny-list files + aio.py; ALL 4 deny-list files intact (pre==post); aio.py transformed (pre!=post); FINDING.md + README.md verdict no longer TBD; zero mutations under packages/.</done>
  <acceptance_criteria>
    - `experiment.py` runs to completion and produces `verification_transcripts.txt` with pre/post sha256 hex digits for all 5 files of interest.
    - For each deny-listed file (`_token_store.py`, `_refresh_policy.py`, `_refresh.py`, `ws_client.py`): `pre_hash == post_hash` AND the transcript line says `intact: PASS`.
    - For `aio.py`: `pre_hash != post_hash` (the file was transformed) AND transcript line says `transformed: PASS`.
    - FINDING.md has Verdict + Deny-List Scope + Sha256 Transcript + How the Deny-List Is Achieved in unasync 0.6.0 + Anti-Pitfall 6 Compliance + Recommended Phase 16 Implementation sections.
    - README.md `verdict:` flipped from TBD.
    - `git status --porcelain packages/` is empty (sha256 reading does NOT mutate; the work happens entirely in `WORK / matriz_copy`).
    - If FAIL: the FINDING.md MUST identify WHICH deny-listed file mutated and why — this would be a CRITICAL bug in the spike pattern itself (unexpected; unasync respects fpath_list scope by design). Operator escalation required.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike experiment → matriz_client/* source files (READ via shutil.copytree) | 001d copies the entire matriz_client/ to a sandbox; original is never mutated |
| spike experiment → ámbito client_generated.py (READ from sibling spike dir) | 001b reads Plan 01's output, writes a marker-augmented copy locally |
| stdlib ast → matriz aio.py source | 001c reads the file via `Path.read_text` then `ast.parse`; no execution, no side effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-01 | Tampering | 001d spike accidentally mutating packages/matriz-client/* | mitigate | shutil.copytree to a sandbox (`WORK / matriz_copy`); all unasync writes target the sandbox. Acceptance criteria gate: `git status --porcelain packages/` must remain empty after every task. |
| T-12-02-02 | Information Disclosure | spike transcripts embedding matriz credentials | accept | matriz credentials live in `packages/matriz-client/.env` (per repo gitignore); spike never reads .env files. `_token_store.py` source code contains no embedded secrets — only the lock/refresh logic. sha256 hex digests are not sensitive. |
| T-12-02-03 | Tampering | 001c audit miscategorizing async-only construct as comment-only, masking a real codegen gap | mitigate | Triage logic (Step 9 of Task 12-02-02) requires per-row evidence pointer in FINDING.md Triage Notes section. Anti-Pitfall 5 enforced via `grep -c '| REVIEW |'` gate — soft-relax is impossible. |
| T-12-02-04 | Repudiation | 001b marker FINDING.md flipped to PASS without 4 transcripts | mitigate | Acceptance criteria gates the presence of all 4 sections (ruff check, ruff format --check, mypy --strict, ast.parse) in `verification_transcripts.txt`. |
| T-12-02-SC | Tampering (Supply Chain) | `uv run --with unasync` transient install (carried from T-12-01-SC) | mitigate | Already verified in Plan 01 Task 12-01-02 slopcheck rerun. No new install in this plan; reuses Plan 01's environment. |

## ASVS L1 Alignment

- V1.1: trust boundaries documented above; new boundaries (001d sandbox via copytree) added.
- V14.2 (Dependency management): same `uv run --with unasync` constraint as Plan 01; legitimacy validated.
- No new attack surface introduced. The matriz audit is READ-ONLY against production code; the deny-list verification operates in a sandbox.
</threat_model>

<verification>
After all 3 tasks complete:

1. `cat .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md` shows Verdict + 4-Command Transcripts section with 4 labeled blocks (ruff check / ruff format --check / mypy --strict / ast.parse).
2. `grep -cE '\| (REVIEW|TBD|DENY-LIST-VIOLATION) \|' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/matriz-aio-constructs.md` returns 0 (D-SCOPE-02 merge gate; Anti-Pitfall 5).
3. `grep "intact: PASS" .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/verification_transcripts.txt | wc -l` returns 4 (one PASS per deny-listed file).
4. All 3 sub-experiment FINDING.md files have non-TBD `**Verdict:**` lines.
5. All 3 sub-experiment README.md frontmatters have non-TBD `verdict:` values.
6. `git status --porcelain packages/` is empty (no production code mutated).
</verification>

<success_criteria>
- Wave 2 (001b marker test), Wave 3 (001c matriz audit), and Wave 4 (001d deny-list intactness) deliverables complete.
- All 3 sub-experiments have a verdict (PASS|FAIL) recorded in both FINDING.md and README.md frontmatter.
- Matriz construct audit table has ZERO unresolved rows (D-SCOPE-02 merge gate).
- Deny-list sha256 verification confirms ZERO mutation to the 4 deny-listed matriz files.
- Marker compatibility confirmed across 4 verification commands (or failure mode documented).
- Zero mutations under `packages/`.
- All Python files start with `from __future__ import annotations`.
</success_criteria>

<output>
Create `.planning/phases/12-codegen-spike/12-02-SUMMARY.md` with frontmatter:

```yaml
---
phase: 12-codegen-spike
plan: 02
status: complete
wave: 2
tasks_completed: [12-02-01, 12-02-02, 12-02-03]
files_modified: [list]
marker_compatibility: PASS|FAIL
marker_4_commands_exit_codes:
  ruff_check: 0|N
  ruff_format_check: 0|N
  mypy_strict: 0|N
  ast_parse: 0|N
matriz_audit_total_rows: <N>
matriz_audit_unresolved_rows: 0  # MUST be 0 for PASS
deny_list_intactness: PASS|FAIL
deny_list_files_intact: [_token_store.py, _refresh_policy.py, _refresh.py, ws_client.py]
---
```

Body sections: ## Summary / ## 001b Marker Compatibility Outcome / ## 001c Matriz Audit Outcome / ## 001d Deny-List Intactness Outcome / ## Cumulative Spike State (PASS|FAIL count for 001a/b/c/d) / ## Anti-Pitfall Compliance (5, 6, 7) / ## Next Steps (→ Plan 03 collects all 8 D-RIGOR-01 evidence items + operator signoff + GO/NO-GO close-out routing).
</output>
