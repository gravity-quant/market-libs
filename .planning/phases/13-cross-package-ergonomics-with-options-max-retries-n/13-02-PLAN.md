---
phase: 13-cross-package-ergonomics-with-options-max-retries-n
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/client.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
  - packages/ambito-financiero-client/tests/test_client.py
  - packages/ambito-financiero-client/tests/test_async_client.py
  - verification/snapshots/ambito-financiero-client-surface.txt
autonomous: true
requirements: [ERG-01]
requirements_addressed: [ERG-01]

must_haves:
  truths:
    - "Operator can call ambito_financiero_client.Client().with_options(max_retries=5) and get a view that shares _state"
    - "Operator can call ambito_financiero_client.AsyncClient().with_options(max_retries=5) and get an async view that shares _state"
    - "view.close() / view.__exit__ are no-op (do NOT close parent's http_client) — anti-Pitfall 13"
    - "await view.aclose() / view.__aexit__ are no-op for async view"
    - "view._state is parent._state (zero http_client pool fragmentation; zero re-auth — ambito has no auth but pool sharing matters)"
    - "_validate_max_retries called early in with_options; invalid args raise ValueError before view construction"
    - "Parent client._max_retries stays at constructor value after view is created (D-V2 chaining truth)"
    - "Cross-cutting test_with_options_shares_http_client_and_token[ambito_financiero_client] flips RED→GREEN in this plan"
    - "Cross-cutting test_with_options_max_attempts_extension_honored[ambito_financiero_client] flips RED→GREEN"
    - "Cross-cutting test_with_options_chaining_inner_wins[ambito_financiero_client] flips RED→GREEN"
  artifacts:
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/client.py"
      provides: "Client.with_options(*, max_retries: int) -> Client + _is_view slot + no-op close() guard + _request sets extensions['max_attempts']"
      contains: "def with_options"
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/aio.py"
      provides: "AsyncClient.with_options(*, max_retries: int) -> AsyncClient + _is_view slot + no-op aclose() guard + _request sets extensions['max_attempts']"
      contains: "def with_options"
    - path: "packages/ambito-financiero-client/tests/test_client.py"
      provides: "Per-package mocked tests for with_options (close-is-noop, basic returns) — D-P3"
      contains: "test_with_options_close_is_noop"
    - path: "packages/ambito-financiero-client/tests/test_async_client.py"
      provides: "Per-package mocked async tests for with_options"
      contains: "test_with_options_aclose_is_noop"
    - path: "verification/snapshots/ambito-financiero-client-surface.txt"
      provides: "Public-surface snapshot regenerated post-with_options addition (snapshot enumerates __all__ module-level only; verifies no top-level surface drift)"
  key_links:
    - from: "Client.with_options method body"
      to: "_validate_max_retries (existing Phase 8 helper)"
      via: "first call inside with_options"
      pattern: "_validate_max_retries\\(max_retries\\)"
    - from: "Client._request (shell) sets req.extensions['max_attempts']"
      to: "RetryTransport.handle_request reads request.extensions.get('max_attempts')"
      via: "request.extensions key 'max_attempts'"
      pattern: "extensions\\[\"max_attempts\"\\] = self\\._max_retries \\+ 1"
    - from: "Client.close() / Client.__exit__ / AsyncClient.aclose() / AsyncClient.__aexit__"
      to: "_is_view flag (D-V1)"
      via: "if getattr(self, '_is_view', False): return"
      pattern: "if getattr\\(self, \"_is_view\", False\\)"
---

<objective>
Land `with_options(max_retries=N)` per-call override on `ambito_financiero_client.Client` and `ambito_financiero_client.AsyncClient` (canary package, no auth, smallest blast radius). Phase 13 per-package serial roll-out starts here.

Purpose: ámbito = canary because no auth + no account_id + no TokenStore. Validates the view shape (`type(self).__new__(type(self))` + share `_state` + override `_max_retries` + flag `_is_view`) and the lifecycle no-op guard in the simplest case before the more complex packages land. Surfaces any structural issues with `__slots__` (must add `_is_view`), surfaces any `_request` shell behavior issues with `extensions["max_attempts"]`, and turns 3 of the 4 cross-cutting RED tests GREEN for the ámbito row (the matriz CRITICAL gate test stays RED until Plan 4).

Output:
- `Client.with_options` + `AsyncClient.with_options` methods added (sync + async).
- `_is_view` added to `__slots__` on both classes.
- `close()` / `__exit__` / `aclose()` / `__aexit__` get the `_is_view` no-op guard.
- `_request()` shell (sync + async) sets `req.extensions["max_attempts"] = self._max_retries + 1` uniformly.
- Per-package mocked tests added: close-is-noop + chaining + basic with_options returns.
- Snapshot regenerated (likely zero body diff: snapshot enumerates `__all__` module-level only — `Client` already in `__all__`; `with_options` as method does not appear; regen confirms no drift).
- 3 of 4 cross-cutting tests in `verification/test_with_options.py` flip GREEN for `[ambito_financiero_client]` parametrize row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
@verification/test_with_options.py
@verification/snapshots/ambito-financiero-client-surface.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Client.with_options + _is_view slot + close() no-op + _request extension to ambito client.py</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/client.py</files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-V1, D-V2, D-V3, D-V4; `<specifics>` View constructor pattern + Shell _request extension set; `<code_context>` Integration Points)
    packages/ambito-financiero-client/src/ambito_financiero_client/client.py (full file — current Client class shape: __slots__ line 84, __init__ 86-107, close 115-119, _request 148-167, _validate_max_retries 41-62)
    packages/ambito-financiero-client/src/ambito_financiero_client/_transport.py (confirms `max_attempts` extension read shape from Plan 1)
  </read_first>
  <behavior>
    - After change: `c = ambito_financiero_client.Client(); view = c.with_options(max_retries=5)` works and returns a fresh `Client` instance.
    - `view._state is c._state` — same `_ClientState` reference (no clone).
    - `view._max_retries == 5`; `c._max_retries == 2` (unchanged).
    - `view._is_view is True`; `c._is_view is False` (parent constructor default).
    - `view.close()` is a no-op (does NOT close `c._state.http_client`).
    - `with view:` enters and exits; on exit, `view.__exit__` calls `view.close()` which short-circuits.
    - `_validate_max_retries(-1)` raises ValueError; `with_options(max_retries=-1)` raises ValueError BEFORE constructing the view.
    - `with_options(max_retries=True)` raises ValueError (bool not accepted, per existing helper).
    - Every call (parent or view) sets `req.extensions["max_attempts"] = self._max_retries + 1` in `_request()` shell uniformly.
    - Calling `c.get_dollar_banco_nacion(date)` works exactly as before for the parent (no behavior regression).
    - Calling `view.get_dollar_banco_nacion(date)` works and triggers retry-loop with `max_attempts = view._max_retries + 1 = 6` on retryable status (vs. `2 + 1 = 3` for parent).
  </behavior>
  <action>
    Modify `packages/ambito-financiero-client/src/ambito_financiero_client/client.py` as follows:

    1. **Add `_is_view` to `Client.__slots__`** (around line 84):
       Change `__slots__ = ("_max_retries", "_state")` to `__slots__ = ("_is_view", "_max_retries", "_state")` (alphabetically sorted per existing convention).

    2. **Set `self._is_view = False` in `Client.__init__`** — add immediately after `self._max_retries = max_retries` (around line 104, before the `if http_client is not None:` block). Comment: `# Phase 13 D-V1: False for normally-constructed Clients; True for views returned by with_options().`

    3. **Add `Client.with_options` method** after `_ensure_http_client` and before `_request` (preserve logical grouping: lifecycle methods first, then internals, then endpoints). Method shape (exact LOC per `<specifics>` View constructor pattern):

       Method signature: `def with_options(self, *, max_retries: int) -> Self:`
       Body (positional ordering):
         - Call `_validate_max_retries(max_retries)` per D-V3.
         - `view = type(self).__new__(type(self))`
         - `view._state = self._state` (share — anti-Pitfall 13)
         - `view._max_retries = max_retries` (override)
         - `view._is_view = True` (flag for lifecycle no-op)
         - `return view`

       Docstring: ~15 lines covering: per-CONTEXT.md `<specifics>` "View constructor pattern" docstring (purpose, share semantics, lifecycle no-op, chaining inner-wins, configure() invariance). Reference `Endpoint: N/A` since this is a method, not an endpoint. Reference Phase 8 D-19 in the docstring (`max_retries=N → max_attempts=N+1` semantics).

       Implements decisions: D-V1 (slot + no-op), D-V2 (chaining), D-V3 (parity with async), D-V4 (configure-invariance documented).

    4. **Modify `Client.close()` (lines 115-119) to no-op when `_is_view=True`** — add at the top of the method:

       `if getattr(self, "_is_view", False): return  # Phase 13 D-V1: views don't own the http_client.`

       Use `getattr` with default `False` (not direct `self._is_view`) so older pickled state or pre-Phase-13 in-memory Clients still work (defensive).

    5. **Modify `_request()` shell (lines 148-167)** — add ONE NEW LINE alongside the existing extension-set block (`req.extensions["idempotent"] = ...`):

       `req.extensions["max_attempts"] = self._max_retries + 1  # Phase 13 ERG-01: per-call override; parent or view's _max_retries (Phase 8 D-19 N→N+1).`

       Place AFTER `req.extensions["endpoint_name"] = spec.endpoint_name` (last of the existing extensions). Set uniformly (parent or view — uniform per Claude's Discretion in CONTEXT.md `<decisions>` "uniformidad simplifica el shape del test cross-cutting y elimina branches en el shell").

    6. **Update `__repr__` of Client** (lines 121-128): per Claude's Discretion in CONTEXT.md `<decisions>` ("incluir para debug ergonomics"), prepend `"view of "` to the repr string when `self._is_view` is True. Implementation:

       Add at the top of `__repr__`:
       `prefix = "view of " if getattr(self, "_is_view", False) else ""`

       And change the return to `f"{prefix}AmbitoFinancieroClient(..."`.

    7. **DO NOT touch `__exit__`** — it already calls `self.close()`; the no-op guard inside `close()` covers `__exit__` automatically.

    8. **DO NOT change `__all__`** — `with_options` is a method on `Client`, not a module-level export. The snapshot enumeration (`verification/test_public_surface.py::_enumerate_surface`) only walks `__all__`; method addition does NOT change the snapshot body.

    9. **DO NOT touch `_validate_max_retries`** — reuse as-is from Phase 8 WR-06.

    10. **DO NOT introduce any new imports**. `Self` is already imported from `typing`; everything else needed is in scope.

    Constraint: total diff to `client.py` ≤ +30 LOC additions + ≤ 5 LOC modifications (no deletions).

    After modification, run `uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_client.py -x -q` to confirm existing tests stay GREEN.

    Then run `uv run pytest verification/test_with_options.py -k "ambito_financiero_client and not async" -x -q` to confirm the cross-cutting sync tests turn GREEN for ámbito.
  </action>
  <verify>
    <automated>uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_client.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "ambito_financiero_client and not async" -x -q</automated>
    <automated>uv run ruff check packages/ambito-financiero-client/</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def with_options" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` returns 1.
    - Source: `grep -c "_is_view" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` returns ≥ 4 (slots + __init__ assignment + close() guard + __repr__ prefix + with_options assignment).
    - Source (slots correct): `python -c "from ambito_financiero_client import Client; assert '_is_view' in Client.__slots__"` exits 0.
    - Source: `grep -c "_validate_max_retries(max_retries)" packages/ambito-financiero-client/src/ambito_financiero_client/client.py` returns ≥ 2 (existing __init__ + new with_options).
    - Source (extension set in shell): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` returns 1.
    - Source (close no-op guard): `grep -c 'if getattr(self, "_is_view", False): return' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` returns ≥ 1.
    - Source (__all__ unchanged): `grep -A 10 "__all__ = \[" packages/ambito-financiero-client/src/ambito_financiero_client/client.py | grep -c "with_options"` returns 0 (with_options is method, not module-level).
    - Behavior: `uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_client.py -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_shares_http_client_and_token -k ambito_financiero_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_max_attempts_extension_honored -k ambito_financiero_client -x -q exits 0`.
    - Cross-cutting GREEN: `uv run pytest verification/test_with_options.py::test_with_options_chaining_inner_wins -k ambito_financiero_client -x -q exits 0`.
    - Quality: `uv run ruff check packages/ambito-financiero-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/ambito-financiero-client/src exits 0`.
  </acceptance_criteria>
  <done>
    `Client.with_options` exists, returns a view with shared `_state`. `_is_view` slot added. `close()` no-op for views. `_request` sets `extensions["max_attempts"]` uniformly. `__repr__` prefixes "view of" for views. 3 of 4 cross-cutting sync tests for `[ambito_financiero_client]` GREEN. Existing per-package tests GREEN. Ruff + mypy strict GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror AsyncClient.with_options in ambito aio.py (mirror Task 1)</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/aio.py</files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-V3 — AsyncClient mismo idiomático sync)
    packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (full file — current AsyncClient shape: __slots__ line 63, __init__ 65-84, aclose 92-96, _request 128-145; already imports `_validate_max_retries` from `client.py` per Phase 8 D-15)
    packages/ambito-financiero-client/src/ambito_financiero_client/client.py (modified in Task 1 — async mirrors sync)
  </read_first>
  <behavior>
    - Same as Task 1 but for `AsyncClient` and `aio.py`.
    - `view = parent.with_options(max_retries=5)` returns fresh `AsyncClient`.
    - `view._state is parent._state` (shared).
    - `view._is_view is True`; `parent._is_view is False`.
    - `await view.aclose()` is no-op when `_is_view=True`.
    - `async with view:` enters and exits; on exit, `view.__aexit__` calls `view.aclose()` which short-circuits.
    - `await view.get_dollar_banco_nacion(date)` works and propagates `extensions["max_attempts"] = 6`.
    - No `event-loop` interaction with view creation — `with_options` is sync (returns view; the async operation is the subsequent endpoint call).
  </behavior>
  <action>
    Modify `packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` mirror-of-Task-1:

    1. **Add `_is_view` to `AsyncClient.__slots__`** (line 63): `__slots__ = ("_is_view", "_max_retries", "_state")`.

    2. **Set `self._is_view = False` in `AsyncClient.__init__`** — after `self._max_retries = max_retries` (line 81), before `if http_client is not None:`. Comment: `# Phase 13 D-V1 mirror sync.`

    3. **Add `AsyncClient.with_options` method** after `_ensure_http_client` and before `_request`:

       Signature: `def with_options(self, *, max_retries: int) -> Self:` — NOTE: `with_options` itself is SYNC even on `AsyncClient` (it returns a view; only the subsequent endpoint call is async). Per D-V3.

       Body: identical to sync Task 1 step 3 — `_validate_max_retries`, `view = type(self).__new__(type(self))`, share `_state`, override `_max_retries`, set `_is_view = True`, return.

       Docstring: short — refers to `Client.with_options` for full semantics; reiterates the async-class case (e.g., `view = c.with_options(5); await view.get_dollar_banco_nacion(date)`).

    4. **Modify `AsyncClient.aclose()` (lines 92-96) to no-op when `_is_view=True`**:

       `if getattr(self, "_is_view", False): return  # Phase 13 D-V1 mirror sync.`

    5. **Modify `_request()` shell (lines 128-145)** — add:

       `req.extensions["max_attempts"] = self._max_retries + 1  # Phase 13 ERG-01 mirror sync.`

       After `req.extensions["endpoint_name"] = spec.endpoint_name`.

    6. **Modify `__repr__` of AsyncClient (lines 98-105)** — add `prefix = "view of " if getattr(self, "_is_view", False) else ""` and use in the return string (mirror sync).

    7. DO NOT touch `__aexit__` — it already calls `await self.aclose()`; guard inside `aclose` covers it.

    8. DO NOT change `__all__`.

    9. DO NOT introduce new imports — `_validate_max_retries` is already imported from `.client` per Phase 8 D-15; `Self` is already imported from `typing`.

    Constraint: total diff to `aio.py` ≤ +25 LOC additions + ≤ 5 LOC modifications.

    Run `uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_async_client.py -x -q` to confirm async tests stay GREEN. Then run cross-cutting tests for ambito async row.
  </action>
  <verify>
    <automated>uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_async_client.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "ambito_financiero_client" -x -q</automated>
    <automated>uv run ruff check packages/ambito-financiero-client/</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def with_options" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` returns 1.
    - Source: `grep -c "_is_view" packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` returns ≥ 4.
    - Source (slots correct): `python -c "from ambito_financiero_client import AsyncClient; assert '_is_view' in AsyncClient.__slots__"` exits 0.
    - Source (extension set): `grep -c 'req.extensions\["max_attempts"\] = self._max_retries + 1' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` returns 1.
    - Source (aclose no-op): `grep -c 'if getattr(self, "_is_view", False): return' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` returns ≥ 1.
    - Source (no new imports): `git diff packages/ambito-financiero-client/src/ambito_financiero_client/aio.py | head -50` shows no new `import` or `from` lines.
    - Behavior: `uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/test_async_client.py -x -q exits 0`.
    - Quality: `uv run ruff check packages/ambito-financiero-client/ exits 0`.
    - Quality: `uv run mypy --strict packages/ambito-financiero-client/src exits 0`.
  </acceptance_criteria>
  <done>
    `AsyncClient.with_options` exists; mirrors sync semantics. `_is_view` slot added. `aclose()` no-op for views. `_request` sets `extensions["max_attempts"]`. `__repr__` prefixes "view of". Existing async tests GREEN. Ruff + mypy strict GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add per-package mocked tests + regenerate snapshot for ambito</name>
  <files>
    packages/ambito-financiero-client/tests/test_client.py,
    packages/ambito-financiero-client/tests/test_async_client.py,
    verification/snapshots/ambito-financiero-client-surface.txt
  </files>
  <read_first>
    .planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-CONTEXT.md (D-P3 — per-package mocked tests live in `packages/<pkg>/tests/`, NOT verification/; D-P4 — snapshot atómico per Plan 2-5; D-V5 — 2 entries documented but snapshot enumerates __all__ only)
    packages/ambito-financiero-client/tests/test_client.py (existing mocked-test patterns: close() idempotente, pickle raise; serve as analog for new tests)
    packages/ambito-financiero-client/tests/test_async_client.py (same — async patterns)
    verification/snapshots/ambito-financiero-client-surface.txt (current snapshot — 8-line header + body lines from `__all__` enumeration)
    verification/test_public_surface.py (snapshot enumeration logic — only walks `__all__` module-level, NOT class methods)
    verification/regen_snapshots.py (operator-run regen script)
  </read_first>
  <behavior>
    - Per D-P3, mocked tests for `test_with_options_close_is_noop` + chaining + basic with_options return live in `packages/ambito-financiero-client/tests/`, NOT in `verification/`.
    - Snapshot regenerated per D-P4. Per the surface enumeration logic (only walks `__all__`), `with_options` as a method on `Client` does NOT appear in the snapshot body. The regen will likely produce ZERO diff in the body — this is EXPECTED and ACCEPTABLE: the snapshot's job is to detect module-level surface drift; adding a method to an existing class does not change the constructor signature already captured.
    - The snapshot regen step is preserved for D-P4 atomicity discipline: every Plan 2-5 runs the regen and commits whatever diff results (or no diff). This locks the snapshot atomic-commit idiom across the phase.
  </behavior>
  <action>
    1. **Add mocked tests to `packages/ambito-financiero-client/tests/test_client.py`** (append after existing tests):

       - `test_with_options_close_is_noop`: construct `Client()`, call `_ensure_http_client()` once (via mocked GET), assign `parent_http = client._state.http_client`, call `view = client.with_options(max_retries=5)`, call `view.close()`, assert `client._state.http_client is parent_http` AND `client._state.http_client is not None` (parent's pool still open).
       - `test_with_options_exit_is_noop`: same shape but using `with view:` context manager — assert parent's `http_client` stays open after the `with` block exits.
       - `test_with_options_chaining_inner_wins_local`: `c.with_options(5).with_options(10)._max_retries == 10`; `c._max_retries == 2`; `view._state is c._state`.
       - `test_with_options_repr_shows_view_prefix`: `repr(c.with_options(5))` starts with `"view of "`.
       - `test_with_options_invalid_max_retries_raises_value_error`: `pytest.raises(ValueError)` on `c.with_options(max_retries=-1)`, `c.with_options(max_retries=True)`, `c.with_options(max_retries=1.5)`.

       Use existing autouse fixtures from `conftest.py` for `configure()` reset. ~50-60 LOC for the 5 tests.

    2. **Add async-mirror tests to `packages/ambito-financiero-client/tests/test_async_client.py`**:

       - `test_with_options_aclose_is_noop`: async mirror of close-is-noop.
       - `test_with_options_aexit_is_noop`: `async with view:` mirror.
       - `test_with_options_chaining_inner_wins_local_async`: mirror.
       - `test_with_options_async_repr_shows_view_prefix`: mirror.
       - `test_with_options_async_invalid_max_retries_raises_value_error`: mirror.

       ~50-60 LOC.

    3. **Regenerate snapshot for ambito-financiero-client only**:

       Run `uv run python verification/regen_snapshots.py` (note: per `verification/regen_snapshots.py:48`, the script likely regenerates ALL 4 packages; if a `--packages` argument exists per D-P4 wording, prefer it; otherwise run the full regen and commit ONLY the ámbito snapshot diff in Plan 2 — atomicity is per-plan-commit, not per-script-invocation).

       If `regen_snapshots.py` does NOT accept `--packages`, modify ONLY the ámbito snapshot file in the commit (`git checkout` the other 3 snapshot files if the regen touched them; they will be regenerated in Plans 3-5 anyway).

       NOTE on D-V5 expectation: CONTEXT.md says "agrega exactamente 2 entries". The actual enumeration mechanism (`verification/test_public_surface.py::_enumerate_surface`) only walks `__all__` of the package (module-level). `Client.with_options` is a METHOD, not a module-level name. So the snapshot body diff will be ZERO. This is acceptable — `Client` itself is already in the snapshot, and its constructor signature does not change. Document this discrepancy in `13-02-SUMMARY.md` for forward visibility (D-D2 reference).

    4. Run `uv run pytest verification/test_public_surface.py -x -q` to confirm the snapshot test still GREEN.

    5. Run full ambito package test suite + cross-cutting tests; expect all GREEN except `test_with_options_does_not_bypass_mutation_gate_matriz` (matriz-only, lands in Plan 4).
  </action>
  <verify>
    <automated>uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/ -x -q</automated>
    <automated>uv run pytest verification/test_public_surface.py -x -q</automated>
    <automated>uv run pytest verification/test_with_options.py -k "ambito_financiero_client" -x -q</automated>
    <automated>uv run ruff check packages/ambito-financiero-client/ verification/</automated>
    <automated>uv run mypy --strict packages/ambito-financiero-client/src packages/ambito-financiero-client/tests verification/test_with_options.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "def test_with_options_close_is_noop" packages/ambito-financiero-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_chaining_inner_wins_local" packages/ambito-financiero-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_invalid_max_retries_raises_value_error" packages/ambito-financiero-client/tests/test_client.py` returns 1.
    - Source: `grep -c "def test_with_options_aclose_is_noop" packages/ambito-financiero-client/tests/test_async_client.py` returns 1.
    - Source: `grep -c "def test_with_options_async_invalid_max_retries_raises_value_error" packages/ambito-financiero-client/tests/test_async_client.py` returns 1.
    - Behavior: `uv run --package ambito-financiero-client pytest packages/ambito-financiero-client/tests/ -x -q exits 0` AND test count is ≥ existing count + 10 (5 sync + 5 async).
    - Behavior: `uv run pytest verification/test_public_surface.py -x -q exits 0` (snapshot stays GREEN; ámbito body unchanged because `with_options` is a method).
    - Behavior: `uv run pytest verification/test_with_options.py -k "ambito_financiero_client" -x -q exits 0` (3 of 4 cross-cutting tests GREEN for ámbito; matriz mutation-gate test remains RED until Plan 4).
    - Snapshot integrity: `wc -l verification/snapshots/ambito-financiero-client-surface.txt` matches expected (header 8 lines + N body lines unchanged from pre-Plan-2 state — verified via `git diff verification/snapshots/ambito-financiero-client-surface.txt` reports empty OR only header re-ordering tolerance).
    - Quality: `uv run ruff check packages/ambito-financiero-client/ verification/ exits 0`.
    - Quality: `uv run mypy --strict packages/ambito-financiero-client/ exits 0` (tests + src).
    - Documentation: `13-02-SUMMARY.md` (output of this plan) documents the D-V5 snapshot mechanism discrepancy: snapshot body is unchanged because the enumerator only walks `__all__` module-level. Forward reference noted for Phase 17 LIVE-03.
  </acceptance_criteria>
  <done>
    Per-package mocked tests added (5 sync + 5 async). Snapshot regenerated per D-P4 atomicity (zero body diff is acceptable; snapshot test stays GREEN). All ámbito package tests GREEN. 3 of 4 cross-cutting tests for ámbito row GREEN. Discrepancy with D-V5 expectation documented for downstream visibility.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.with_options` | Caller-controlled `max_retries` arg validated via `_validate_max_retries` Phase 8 WR-06; rejects bool, negative int, non-int |
| view → `_state.http_client` (shared) | View shares parent's `_state` reference; view does NOT own the http_client; lifecycle no-op prevents view from accidentally closing parent's TCP pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | `Client.with_options` max_retries arg | mitigate | `_validate_max_retries(max_retries)` is the FIRST call inside `with_options`; rejects negative/bool/non-int before view construction. Acceptance: `pytest.raises(ValueError)` test covers -1, True, 1.5. |
| T-13-02-02 | Denial of Service | View closes parent's http_client (anti-Pitfall 13) | mitigate | `_is_view` flag + no-op `close()` / `aclose()` / `__exit__` / `__aexit__` guard. Acceptance: `test_with_options_close_is_noop` + `test_with_options_aclose_is_noop` + `test_with_options_shares_http_client_and_token` (cross-cutting). |
| T-13-02-SC | Tampering | npm/pip/cargo installs | n/a | No new dependencies in Plan 2. |

Credential surface unchanged: ámbito has no auth (`B7 divergence`), no token, no account_id. The view shares `_state` (which contains only `base_url`, `user_agent`, `http_client`). No new credential storage or exposure path. Inherited from Phase 6 D-13.

Mutation gate (Phase 8 D-01 inherited): not directly applicable to ámbito (no mutating endpoints — only `get_dollar_banco_nacion`). But the wiring (`req.extensions["idempotent"] = spec.idempotent`) is preserved unchanged; `_request` shell still propagates the idempotent flag. The new `req.extensions["max_attempts"]` line is set AFTER the idempotent extension, NOT before — preserving Phase 8 eval order.
</threat_model>

<verification>
- `Client.with_options` + `AsyncClient.with_options` exist with `_validate_max_retries` early-call.
- Both classes have `_is_view` in `__slots__`.
- Both have `__init__` set `self._is_view = False`.
- `close()` / `aclose()` / `__exit__` / `__aexit__` short-circuit when `_is_view=True`.
- `_request()` / async `_request()` set `req.extensions["max_attempts"] = self._max_retries + 1` after `endpoint_name` extension.
- `__repr__` prefixes "view of " when `_is_view=True`.
- Per-package mocked tests cover close-is-noop, chaining, repr-prefix, invalid args (5 sync + 5 async).
- Snapshot regenerated; test stays GREEN. Body diff is empty (acceptable per snapshot enumeration logic).
- Cross-cutting tests for `[ambito_financiero_client]` parametrize row flip RED→GREEN (3 of 4; matriz mutation-gate stays RED until Plan 4).
- Existing ámbito package tests stay GREEN.
- `uv run ruff check packages/ambito-financiero-client/ verification/` exits 0.
- `uv run mypy --strict packages/ambito-financiero-client/src packages/ambito-financiero-client/tests verification/test_with_options.py` exits 0.
</verification>

<success_criteria>
- ámbito sync + async `with_options` lands with shared `_state` and `_is_view` flag.
- Lifecycle no-op verified by per-package and cross-cutting tests (anti-Pitfall 13).
- `_request` shell sets `extensions["max_attempts"]` uniformly (uniform path per Claude's Discretion).
- 3 of 4 cross-cutting tests in `verification/test_with_options.py` GREEN for `[ambito_financiero_client]` parametrize row.
- Snapshot test stays GREEN; ámbito snapshot body diff is empty (acceptable; documented).
- Existing ámbito test suite GREEN.
- Ruff + mypy strict GREEN.
- Plan 3 (higyrus) can begin from a stable ámbito-with-with_options HEAD.
</success_criteria>

<output>
Create `.planning/phases/13-cross-package-ergonomics-with-options-max-retries-n/13-02-SUMMARY.md` when done. Document:
- Sync + async `with_options` method shape + LOC delta.
- `_is_view` flag added to `__slots__` (sorted alphabetically).
- `close()` / `aclose()` / `__exit__` / `__aexit__` no-op guard via `getattr(self, "_is_view", False)`.
- `_request` shell adds `extensions["max_attempts"]` uniformly.
- Per-package mocked tests added (5 sync + 5 async).
- Snapshot regenerated; body diff empty because enumerator only walks `__all__` module-level (D-V5 discrepancy noted; forward reference to Phase 17 LIVE-03 if snapshot extension is needed).
- 3 of 4 cross-cutting tests GREEN for ámbito row.
- Forward reference: Plans 3-5 follow this exact shape per package.
</output>
