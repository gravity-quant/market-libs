---
phase: 21-market-data-lectura-modelos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - pyproject.toml
  - packages/market-data-client/tests/test_models.py
autonomous: true
requirements: [MD-01]

must_haves:
  truths:
    - "MarketDataSnapshot.from_api tolerates partial/None/extra-key payloads without raising"
    - "received_at is the client-supplied stamp on the snapshot, never coerced to 0.0 from the payload"
    - "Nested entries deserialize via the base SafeModel.from_api (no received_at on entries)"
    - "LatestRequest serializes to a dict with None optionals dropped"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "SafeModel + _coerce copy, MarketDataEntry, MarketDataSnapshot (received_at first-class), LatestRequest"
      contains: "class SafeModel"
      min_lines: 90
    - path: "packages/market-data-client/tests/test_models.py"
      provides: "from_api tolerance + received_at injection + LatestRequest.to_dict tests"
      contains: "def test_"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "MarketDataSnapshot.from_api injects received_at kwarg, skipping _coerce for that field"
      pattern: "received_at"
---

<objective>
Create `models.py` (net-new) for `market-data-client`: a package-local copy of higyrus's
`SafeModel` + `_coerce` (D-03 — copy, never import cross-package), the tolerant
`MarketDataSnapshot` (with a nested `MarketDataEntry` list and `received_at` as a first-class
CLIENT-STAMPED field, D-01/D-04), and the `LatestRequest` request dataclass (D-05). Add the
D-04 `N815` per-file-ignore for the module in the ROOT `pyproject.toml`.

Purpose: The tolerant model layer is the deserialization foundation the `_core.py` parsers
(Plan 02) and the client read methods (Plans 03/04) build on. Getting `received_at` injection
right here (bypassing `_coerce`) is the single highest-risk fidelity point of the phase.
Output: `models.py`, root `pyproject.toml` N815 entry, `tests/test_models.py`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-market-data-lectura-modelos/21-CONTEXT.md
@.planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md
@.planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md
</context>

## Artifacts this phase produces

Newly-created symbols across ALL four Phase-21 plans (for source-grounding / drift exclusion):
- Methods: `get_market_data`, `get_latest`, `get_latest_batch` (on `Client`, `AsyncClient`, and module-level sync + async shims); `with_options` (on `Client` and `AsyncClient`).
- `_core.py` builders: `build_market_data_request`, `build_latest_request`, `build_latest_batch_request`; parsers: `parse_market_data_response`, `parse_latest_response`.
- `models.py` (net-new): `SafeModel`, `_coerce`, `MarketDataSnapshot`, `MarketDataEntry`, `LatestRequest`.
- `_params.py` (net-new): `drop_none`.
- New slots `_max_retries` + `_is_view` and module-level `_validate_max_retries` in `client.py` and `aio.py`.
- New files: `models.py`, `_params.py`, `tests/test_models.py`, `tests/test_market_data.py`, `tests/test_with_options.py`, `tests/test_with_options_async.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing model tests (RED)</name>
  <files>packages/market-data-client/tests/test_models.py</files>
  <read_first>
    - packages/market-data-client/tests/conftest.py (autouse fixtures already seed creds + NEVER_EXPIRES token; do NOT re-seed state)
    - packages/higyrus-client/src/higyrus_client/models.py:37-89 (SafeModel.from_api + _coerce behavior these tests pin)
    - .planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md ("models.py" section, received_at injection design)
  </read_first>
  <behavior>
    - MarketDataSnapshot.from_api({}) returns an instance with typed zero-defaults and entries == [] (no raise).
    - MarketDataSnapshot.from_api(None) does not raise; entries == [].
    - MarketDataSnapshot.from_api with extra/unknown keys ignores them (no raise).
    - MarketDataSnapshot.from_api(payload, received_at=1234.5) sets snapshot.received_at == 1234.5 (NOT 0.0), even when the payload also carries a "received_at" key (payload value is ignored, kwarg wins).
    - MarketDataSnapshot.from_api(payload) WITHOUT the kwarg defaults received_at to 0.0 (parser always supplies it in Plan 02).
    - A payload with an entries list of partial dicts produces list[MarketDataEntry]; entries carry NO received_at attribute.
    - LatestRequest(...).to_dict() drops None-valued optional fields and keeps supplied values.
  </behavior>
  <action>
    Create `packages/market-data-client/tests/test_models.py`. Import the not-yet-existing
    `MarketDataSnapshot`, `MarketDataEntry`, `LatestRequest` from `market_data_client.models`
    (import failure IS the RED signal). Write the tests enumerated in `<behavior>`. Assert
    `received_at` injection by passing `received_at=1234.5` and asserting equality, plus a
    decoy `{"received_at": 999.0, ...}` payload key that must be ignored (D-01). Assert
    tolerance with `{}`, `None`, and extra-key payloads. Do NOT construct models via their
    constructors directly — use `from_api` (and `LatestRequest(...)` + `.to_dict()` for the
    request model). Rely on the existing conftest autouse fixtures; add no new fixtures.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q 2>&1 | grep -Eiq "error|failed|no module|cannot import" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `packages/market-data-client/tests/test_models.py` exists and contains `def test_` functions.
    - Running the module fails at import/collection (symbols not yet defined) — confirms RED before Task 2.
    - Test bodies reference `received_at=` as a keyword and assert a non-zero injected value.
  </acceptance_criteria>
  <done>test_models.py exists, encodes the D-01/D-04/D-05 behaviors, and fails because models.py does not yet exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create models.py — SafeModel copy + snapshot/entry + LatestRequest (GREEN)</name>
  <files>packages/market-data-client/src/market_data_client/models.py</files>
  <read_first>
    - packages/higyrus-client/src/higyrus_client/models.py:1-89 (module docstring + SafeModel + _coerce — COPY verbatim)
    - packages/higyrus-client/src/higyrus_client/models.py:92-134 (PosicionValuada — dataclass shape convention: frozen, slots, camelCase-verbatim fields)
    - .planning/phases/21-market-data-lectura-modelos/21-RESEARCH.md (§Pattern 2 — received_at injection override)
    - packages/market-data-client/tests/test_models.py (the RED tests this task must satisfy)
  </read_first>
  <action>
    Create `packages/market-data-client/src/market_data_client/models.py` starting with
    `from __future__ import annotations` (mandatory project convention). COPY the `SafeModel`
    base class and the `_coerce` helper VERBATIM from `higyrus_client/models.py:30-89` (D-03 —
    do NOT import from higyrus; the no-shared-internals constraint requires a local copy).
    Define `@dataclass(frozen=True, slots=True)` `MarketDataEntry` (a plain `SafeModel`
    subclass, NO received_at) and `MarketDataSnapshot` (a `SafeModel` subclass with an
    `entries: list[MarketDataEntry]` field and a `received_at: float` field). Field names are
    camelCase wire-verbatim and PROVISIONAL (A1/A2 — OpenAPI not vendored; Phase-23 reconciles);
    use a minimal provisional shape (e.g. snapshot: `symbol: str`, `marketId: str`, `entries`,
    `received_at`; entry: `entryType: str`, `price: float`, `size: float`) — `from_api`
    tolerance bounds the blast radius. Override `from_api` on `MarketDataSnapshot` ONLY (per
    D-01, RESEARCH §Pattern 2): accept `*, received_at: float = 0.0`, iterate `fields(cls)`,
    and for the `received_at` field set the kwarg value directly WITHOUT routing through
    `_coerce`; for every other field call `_coerce(data.get(field.name), hints[field.name])`.
    Add `LatestRequest` as a frozen dataclass (D-05) — NOT a SafeModel; it serializes OUT — with
    a provisional shape (`symbols: list[str]`, optional `marketId: str | None = None`,
    `entries: list[str] | None = None`) and an explicit `to_dict()` that drops None-valued
    optionals so Phase-23 can adjust field names in one place. Mirror the
    `higyrus_client/models.py:1-21` module docstring (purpose, safe-default table,
    camelCase-verbatim rationale, N815-exempt reference). Add an explicit `__all__` listing
    `MarketDataEntry`, `MarketDataSnapshot`, `LatestRequest`, and `SafeModel`.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "class SafeModel" packages/market-data-client/src/market_data_client/models.py` and `grep -q "def _coerce" packages/market-data-client/src/market_data_client/models.py`.
    - `MarketDataSnapshot.from_api` defines its own signature accepting `received_at` and injects it without calling `_coerce` for that field (grep: the `received_at` branch sets the kwarg directly).
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q` exits 0.
    - `uv run mypy packages/market-data-client/src` exits 0.
    - No `import` of any symbol from `higyrus_client` appears in `models.py`.
  </acceptance_criteria>
  <done>models.py exists with a local SafeModel/_coerce copy, tolerant MarketDataSnapshot/MarketDataEntry, received_at injection bypassing _coerce, and LatestRequest.to_dict; test_models.py is green; mypy strict clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add N815 per-file-ignore for models.py (root pyproject.toml, D-04)</name>
  <read_first>
    - pyproject.toml (root) lines 71-73 ([tool.ruff.lint.per-file-ignores] — the "**/tests/**" = ["S101"] precedent; ALL ruff config lives in the ROOT file, NOT the package pyproject)
    - .planning/phases/21-market-data-lectura-modelos/21-PATTERNS.md ("pyproject.toml — ruff N815" section — location correction + no-op note)
  </read_first>
  <action>
    In the ROOT `/Users/admin/development/market-libs/pyproject.toml`, under the existing
    `[tool.ruff.lint.per-file-ignores]` table, add an entry mapping
    `"packages/market-data-client/src/market_data_client/models.py"` to `["N815"]` (D-04,
    forward-safety for camelCase wire fields). Do NOT add a `[tool.ruff]` section to the
    package pyproject (non-standard for this repo — all packages defer to root). NOTE: `N`
    (pep8-naming) is NOT in the ruff `select` list (root pyproject.toml:53-67), so this entry
    is a no-op under the current config — its ABSENCE would NOT fail lint. Do NOT gate this
    task's success on a lint delta; gate only on the lint suite staying green.
  </action>
  <verify>
    <automated>grep -q 'market_data_client/models.py' pyproject.toml && uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'packages/market-data-client/src/market_data_client/models.py' pyproject.toml` and the line contains `N815`.
    - `uv run ruff check packages/market-data-client` exits 0.
    - `uv run ruff format --check packages/market-data-client` exits 0.
    - The package `packages/market-data-client/pyproject.toml` has NO new `[tool.ruff]` section.
  </acceptance_criteria>
  <done>Root pyproject.toml carries the models.py N815 per-file-ignore; ruff check + format remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server JSON → SafeModel.from_api | untrusted, possibly partial/malformed API payload deserialized into frozen dataclasses |

## STRIDE Threat Register (ASVS L1, block-on-high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Tampering | `SafeModel.from_api` on malformed/partial payload | mitigate | Tolerant `_coerce` never raises and substitutes typed zero-values; from_api tolerates None/non-dict/extra keys (D-04). Severity: low. |
| T-21-01-02 | Information Disclosure | model `__repr__`/fields carrying response data into logs | accept | Read-only market-data payloads carry no credentials; no token/secret flows through models. Severity: low. |
| T-21-01-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan (RESEARCH Package Legitimacy Audit: zero new deps). No checkpoint required. |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client/tests/test_models.py -q` green.
- `uv run mypy packages/market-data-client/src` clean (run explicitly against the package path — it is absent from the root mypy `files` list, Phase-24 scope).
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` green.
</verification>

<success_criteria>
models.py exists with a local SafeModel/_coerce copy (no cross-package import), tolerant
MarketDataSnapshot/MarketDataEntry, received_at client-injection bypassing _coerce, and a
LatestRequest with to_dict; N815 per-file-ignore present in root pyproject.toml; all four gates
green against the package path.
</success_criteria>

<output>
Create `.planning/phases/21-market-data-lectura-modelos/21-01-SUMMARY.md` when done.
</output>
