---
phase: 08-retries-backoff-structured-logging
plan: 06
type: execute
wave: 6
depends_on: ["08-02", "08-03", "08-04", "08-05"]
files_modified:
  - .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md
autonomous: false
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, green-gate, ci-consolidation, validation]
user_setup: []

must_haves:
  truths:
    - "Phase 8 green gate consolidado — toda la phase corre verde sobre Python 3.12 + 3.13: full pytest matrix + ruff check + ruff format check + mypy strict + lint-imports + 6 cross-cutting guard tests (D-26) GREEN cross-paquete + matriz sweep snapshot + Phase 7 baseline tests"
    - "Public surface snapshot (Phase 6) zero-diff EXCEPTO los kwargs nuevos `max_retries: 'int' = 2` y `http_client` per Client/AsyncClient/configure() en los 3 paquetes con full async surface (ámbito, iol, higyrus); matriz solo agrega kwargs en Client + configure() (AsyncClient unchanged per D-25)"
    - "CI grep step `lint-logging` bloquea `logging.basicConfig`/`logging.root` en `packages/*/src/` — verificado en CI lint job Python 3.12 + 3.13"
    - "ruff `LOG` ruleset (LOG001..LOG015) verde sobre `packages/` y `verification/` (D-27 — LOG015 cubre `logging.root.*` direct calls)"
    - "import-linter contracts (Phase 7 baseline) verdes; opcional Plan 6 puede agregar contracts defensivos para `_transport.py`/`_atransport.py`/`_logging.py` ⊬ {client, aio} en los 4 paquetes (D-09 Phase 7 pattern aplicado a Phase 8 modules)"
    - "Cross-leak sentinel test (`verification/test_sync_async_isolation.py`) GREEN: sync 4/4 PASS; async 3 PASS (ámbito, iol, higyrus) + 1 SKIPPED (matriz async per D-25)"
    - "matriz sweep snapshot (`verification/test_matriz_sweep_snapshot.py`) GREEN — 18 cases preservados (Phase 7 CR-05)"
    - "CR-03 preservation: `test_parse_envelope_consumes_body_before_raise` GREEN (Phase 7 baseline)"
    - "matriz aio.py UNCHANGED — `wc -l packages/matriz-client/src/matriz_client/aio.py` == 103 (Phase 6 stub preservado per D-25)"
    - "matriz `_atransport.py` does NOT exist (D-25 — Phase 10 territory)"
    - "tenacity 9.1.4 importable + py.typed compatible con mypy strict (verified post-Plan-1 + post-Plans-2-5)"
    - "08-VALIDATION.md con `nyquist_compliant: true` (post-checker approval) + evidencia consolidada (gates output + LOC delta + test count delta)"
    - "Operator checkpoint: human verificación que (a) CI matrix verde sobre el merge commit (no solo local), (b) ningún test fue weakeneado o skipeado para hacer pasar Phase 8 (Pitfall 18 statement), (c) matriz aio.py preservado a 103 LOC, (d) duplicate-order risk (Pitfall 4) explicitamente verified — `test_retry_mutation_gate.py[matriz_new_order]` PASS"
  artifacts:
    - path: ".planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md"
      provides: "Final validation report con nyquist_compliant: true + green-gate matrix evidence + LOC delta + commit-message log + critical guard test confirmation (test_retry_mutation_gate.py[matriz_new_order])"
      contains: "nyquist_compliant"
  key_links:
    - from: "Plan 6 green gate"
      to: "Plans 1-5 deliverables"
      via: "depends_on enforces ordering; this plan re-verifies the combined result"
      pattern: "lint-imports|test_sync_async_isolation|test_matriz_sweep_snapshot|test_public_surface|test_retry_mutation_gate"
---

<objective>
Green-gate consolidation Phase 8 — verificar que el delivery agregado de Plans 1-5 satisface los 5 success criteria del ROADMAP Phase 8 sobre la matriz CI completa (Python 3.12 + 3.13) + ruff + mypy strict + pytest + lint-imports + 6 cross-cutting guard tests (D-26) cross-paquete + matriz sweep snapshot + Phase 6 public-surface snapshot (zero-diff excepto los 2 nuevos kwargs por paquete) + CI grep `lint-logging` + ruff LOG ruleset. Producir `08-VALIDATION.md` con `nyquist_compliant: true` si todas las gates pasan; checkpoint de human-verify final antes de cerrar la phase.

Purpose: Phase 8 success criteria #1-5 del ROADMAP + cierre formal de la phase (D-21 Plan 6 — mirror Phase 6 Plan 7 / Phase 7 Plan 6). Sin este plan, los Plans 2-5 podrían dejar un estado parcial donde alguna gate aislada falla porque depende de un cambio cross-paquete que no fue testeado conjuntamente (e.g., `test_logging_root_unchanged.py` import los 4 paquetes — solo turns GREEN cuando los 4 paquetes attachean NullHandler a su propio logger).

Output: 1 update a `08-VALIDATION.md` con evidencia de toda la matrix verde + 1 checkpoint final human-verify antes de cerrar phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md
@.planning/phases/08-retries-backoff-structured-logging/08-01-SUMMARY.md
@.planning/phases/08-retries-backoff-structured-logging/08-02-SUMMARY.md
@.planning/phases/08-retries-backoff-structured-logging/08-03-SUMMARY.md
@.planning/phases/08-retries-backoff-structured-logging/08-04-SUMMARY.md
@.planning/phases/08-retries-backoff-structured-logging/08-05-SUMMARY.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-06-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-07-PLAN.md
@CLAUDE.md
@pyproject.toml
@.github/workflows/ci.yml
@verification/test_public_surface.py
@verification/test_sync_async_isolation.py
@verification/test_matriz_sweep_snapshot.py
@verification/test_retry_mutation_gate.py
@verification/test_retry_401_reauth.py
@verification/test_retry_after_cap.py
@verification/test_logging_root_unchanged.py
@verification/test_logging_no_token_leak.py
@verification/test_async_cancellation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run full green-gate matrix locally + produce 08-VALIDATION.md with evidence</name>
  <files>.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-06-PLAN.md (Phase 7 green gate plantilla — Plan 6 mirror; gate commands list)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-07-PLAN.md (Phase 6 green gate Plan 7 — secondary analog)
    - .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md (preexisting validation file — `nyquist_compliant: false` placeholder; EDITAR with evidence, no overwrite arbitrario)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Validation Architecture" lines 1212-1272 — automated commands per requirement; Sampling Rate; Wave 0 Gaps)
    - .planning/phases/08-retries-backoff-structured-logging/08-01-SUMMARY.md (Plan 1 output — guard tests RED status snapshot baseline)
    - .planning/phases/08-retries-backoff-structured-logging/08-02-SUMMARY.md (Plan 2 ámbito LOC delta + commit hash)
    - .planning/phases/08-retries-backoff-structured-logging/08-03-SUMMARY.md (Plan 3 iol LOC delta + 401 re-auth + commit hash)
    - .planning/phases/08-retries-backoff-structured-logging/08-04-SUMMARY.md (Plan 4 higyrus LOC delta + account_id + JSON password + commit hash)
    - .planning/phases/08-retries-backoff-structured-logging/08-05-SUMMARY.md (Plan 5 matriz LOC delta + Risk API + D-25 forward ref + commit hash)
    - .github/workflows/ci.yml (verify Plan 1 lint-logging step landed; CI matrix Python 3.12 + 3.13 covers Plans 2-5 deliverables)
  </read_first>
  <behavior>
    Run secuencialmente todas las gates locally + capturar output + escribirlo a `08-VALIDATION.md` con marca `nyquist_compliant: true` si todas pasan. `nyquist_compliant: true` es signal binario para `/gsd-verify-work`: si ANY gate falla, marca `nyquist_compliant: false` y describe el blocker.

    Documentar:
    - Test count delta: Phase 7 baseline 527 tests → Phase 8 final (527 + ~40-60 new tests entre `tests/test_transport.py` × 4 + `tests/test_logging.py` × 4 + 6 cross-cutting guard tests parametrizados)
    - LOC delta agregado por paquete (consolidar Plans 02-05 SUMMARY.md):
      ```
      | Pkg     | _transport.py (NEW) | _atransport.py (NEW)        | _logging.py (NEW)        | client.py delta | aio.py delta        | __init__.py delta |
      |---------|---------------------|------------------------------|---------------------------|------------------|----------------------|---------------------|
      | ámbito  | ~95                 | ~95                          | ~65 (Bearer/password)     | +~40 LOC         | +~40 LOC             | +3 LOC              |
      | iol     | ~95                 | ~95                          | ~75 (+refresh_token URL/JSON) | +~45 LOC      | +~45 LOC             | +3 LOC              |
      | higyrus | ~95                 | ~95                          | ~80 (+JSON pwd/token/cuit)| +~50 LOC         | +~50 LOC             | +3 LOC              |
      | matriz  | ~95                 | N/A (Phase 10 D-25)          | ~85 (+auth_basic D-22)    | +~50 LOC         | 0 (UNCHANGED D-25)   | +3 LOC              |
      ```
    - Cross-cutting guard tests final status (todas GREEN post-Plan-5):
      - `test_retry_mutation_gate.py[ambito|iol|higyrus|matriz_new_order]` — CRITICAL: matriz new_order branch (Pitfall 4 duplicate-order) GREEN
      - `test_retry_401_reauth.py[iol|higyrus|matriz Primary|matriz Risk]` — all paths covered
      - `test_retry_after_cap.py` — 60s cap verified
      - `test_logging_root_unchanged.py` — logging.root.handlers preserved post-import × 4 packages
      - `test_logging_no_token_leak.py[ambito|iol|higyrus|matriz]` — SECRET-LITERAL-12345 NO aparece (con cobertura de Bearer, OAuth refresh_token, JSON password, JSON token, cuit, auth_basic)
      - `test_async_cancellation.py[ambito|iol|higyrus]` (matriz skip per D-25) — CancelledError propagation
    - CI grep `lint-logging` output: `! grep -rn ... packages/*/src/` exit 0
    - ruff LOG ruleset output: ruff check exit 0
    - import-linter contracts output: `4 kept, 0 broken` or equivalent
    - Phase 6 public surface snapshot diff (per package):
      - ámbito/iol/higyrus: Client.__init__, AsyncClient.__init__, configure() — exactly 2 new kwargs per signature (6 lines changed total per package: 2 kwargs × 3 signatures)
      - matriz: Client.__init__ + configure() — exactly 2 new kwargs per signature; AsyncClient.__init__ UNCHANGED (D-25)
    - matriz aio.py preserved: `wc -l packages/matriz-client/src/matriz_client/aio.py` == 103
    - matriz `_atransport.py` does NOT exist
    - tenacity import + py.typed verified
    - Pitfall 18 statement: explicit assertion "No tests were weakened during Phase 8 — all pre-existing tests in `packages/*/tests/` + `verification/` pass with their original assertions; only new tests were added (in `tests/test_transport.py`, `tests/test_logging.py`, `verification/test_retry_*`, `verification/test_logging_*`, `verification/test_async_cancellation.py`)."

    Si CUALQUIER gate falla:
    - Marcar `nyquist_compliant: false`
    - Documentar el blocker exacto (test name + error output + suspected root cause + suggested fix path)
    - Task 2 checkpoint solicita decision del operator
  </behavior>
  <action>
    1. Correr secuencialmente y capturar output (in 08-VALIDATION.md "Phase 8 — Green Gate Evidence" section):
       ```
       # Lock + sync (no new deps after Plan 1)
       uv lock --check
       uv sync --all-packages --all-extras --dev --frozen

       # Static gates
       uv run ruff check .
       uv run ruff format --check .
       uv run mypy --strict packages/
       uv run lint-imports

       # Phase 6 baseline preserved (snapshots)
       uv run pytest verification/test_public_surface.py -v

       # Phase 7 baseline preserved (cross-leak + matriz sweep + CR-03)
       uv run pytest verification/test_sync_async_isolation.py -v
       uv run pytest verification/test_matriz_sweep_snapshot.py -v
       uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v

       # Phase 8 cross-cutting guard tests (the 6 D-26 files)
       uv run pytest verification/test_retry_mutation_gate.py -v
       uv run pytest verification/test_retry_401_reauth.py -v
       uv run pytest verification/test_retry_after_cap.py -v
       uv run pytest verification/test_logging_root_unchanged.py -v
       uv run pytest verification/test_logging_no_token_leak.py -v
       uv run pytest verification/test_async_cancellation.py -v

       # Phase 8 per-package unit tests
       uv run pytest packages/ambito-financiero-client/tests/test_transport.py packages/ambito-financiero-client/tests/test_logging.py -v
       uv run pytest packages/iol-client/tests/test_transport.py packages/iol-client/tests/test_logging.py -v
       uv run pytest packages/higyrus-client/tests/test_transport.py packages/higyrus-client/tests/test_logging.py -v
       uv run pytest packages/matriz-client/tests/test_transport.py packages/matriz-client/tests/test_logging.py -v

       # CRITICAL Pitfall 4 verification (duplicate-order prevention)
       uv run pytest verification/test_retry_mutation_gate.py -k matriz_new_order -v

       # Full suite + count
       uv run pytest packages/ verification/ -q --no-header 2>&1 | tail -10

       # CI grep lint-logging (manually replicate the CI step locally)
       grep -rn --include='*.py' 'logging\.basicConfig\|logging\.root' packages/*/src/ ; echo "exit_code=$?"
       # MUST return exit_code=1 (no matches = grep returns 1 = success for our purpose)
       ```

    2. Capturar matriz aio.py preservation:
       ```
       wc -l packages/matriz-client/src/matriz_client/aio.py
       ! test -f packages/matriz-client/src/matriz_client/_atransport.py && echo "_atransport.py confirmed absent (D-25)"
       ```

    3. Capturar Phase 8 commit log (5 atomic commits per D-21):
       ```
       git log --oneline -10 .planning/phases/08-retries-backoff-structured-logging/ packages/ verification/ pyproject.toml .github/workflows/ci.yml | head -10
       ```

    4. Read existing `08-VALIDATION.md` — EDITAR (no overwrite), preservando metadata pre-existente; agregar las secciones nuevas con la evidencia.

    5. Actualizar `08-VALIDATION.md` con secciones:
       - **Phase 8 — Green Gate Final**: front-matter `nyquist_compliant: true` (o `false` si gate falla) + `phase_status: ready_for_verify` (o `blocked`)
       - **LOC delta consolidated** (4 paquetes matrix)
       - **Cross-cutting guard tests final status** (6 files × parametrize branches)
       - **CRITICAL Pitfall 4 verification**: `test_retry_mutation_gate.py[matriz_new_order]` output line
       - **CR-03 + CR-05 preservation evidence**: `test_parse_envelope_consumes_body_before_raise` + `test_matriz_sweep_snapshot.py` outputs
       - **D-25 matriz aio.py preservation**: `wc -l == 103` + `_atransport.py` absent
       - **Public surface snapshot diffs**: 4 paquete snapshot updates (deltas exactly = 2 new kwargs per signature; matriz AsyncClient unchanged)
       - **import-linter contracts**: 4 kept (or with new Phase 8 contracts if planner added defensive ones)
       - **Cross-leak sentinel**: sync 4/4 PASS, async 3 PASS + 1 SKIPPED (matriz)
       - **CI lint-logging grep step**: exit code 1 (no matches = success)
       - **Test count delta**: 527 baseline → 527 + ~40-60 new = ~580+
       - **tenacity verification**: 9.1.4 importable + py.typed
       - **Pitfall 18 statement**: explicit assertion "No tests were weakened during Phase 8"
       - **Commit log**: 5 atomic commits (Plan 1 + 4 per-package per D-21)

    6. Si CUALQUIER gate falla:
       - Marcar `nyquist_compliant: false`
       - Documentar el blocker exacto (test name + error output)
       - Task 2 checkpoint solicita decision del operator
  </action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv sync --all-packages --all-extras --dev --frozen &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run mypy --strict packages/ &amp;&amp; uv run lint-imports &amp;&amp; uv run pytest verification/test_public_surface.py -v &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -v &amp;&amp; uv run pytest verification/test_matriz_sweep_snapshot.py -v &amp;&amp; uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v &amp;&amp; uv run pytest verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_retry_after_cap.py verification/test_logging_root_unchanged.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py -v &amp;&amp; uv run pytest verification/test_retry_mutation_gate.py -k matriz_new_order -v &amp;&amp; uv run pytest packages/ verification/ -q --no-header 2&gt;&amp;1 | tail -10 &amp;&amp; (! grep -rn --include='*.py' 'logging\.basicConfig\|logging\.root' packages/*/src/) &amp;&amp; [ "$(wc -l &lt; packages/matriz-client/src/matriz_client/aio.py)" = "103" ] &amp;&amp; ! test -f packages/matriz-client/src/matriz_client/_atransport.py &amp;&amp; grep -c 'nyquist_compliant: true' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `uv lock --check` exits 0
    - CLI: `uv sync --all-packages --all-extras --dev --frozen` exits 0
    - CLI: `uv run ruff check .` exits 0 (incluyendo LOG ruleset enabled in Plan 1)
    - CLI: `uv run ruff format --check .` exits 0
    - CLI: `uv run mypy --strict packages/` exits 0 (tenacity py.typed compatible)
    - CLI: `uv run lint-imports` exits 0 — output text contains "4 kept" or equivalent success (Phase 7 contracts + optional Phase 8 additions all GREEN)
    - CLI: `uv run pytest verification/test_public_surface.py -v` exits 0 — 4 paquetes snapshot zero diff (excepto kwargs nuevos)
    - CLI: `uv run pytest verification/test_sync_async_isolation.py -v` exits 0 — sync 4/4 PASS; async 3 PASS + 1 SKIPPED (matriz)
    - CLI: `uv run pytest verification/test_matriz_sweep_snapshot.py -v` exits 0 — 18 cases PASS (Phase 7 CR-05 preserved)
    - CLI: `uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v` PASS (Phase 7 CR-03 preserved)
    - CLI: `uv run pytest verification/test_retry_mutation_gate.py -v` exits 0 — parametrize × 4 paquetes ALL GREEN including matriz_new_order
    - CLI: `uv run pytest verification/test_retry_mutation_gate.py -k matriz_new_order -v` PASS (CRITICAL Pitfall 4 / D-01 / D-24 — duplicate-order prevention)
    - CLI: `uv run pytest verification/test_retry_401_reauth.py -v` exits 0 — iol/higyrus/matriz Primary GREEN; matriz Risk branch (auth_basic 401 → AuthError) GREEN per D-23
    - CLI: `uv run pytest verification/test_retry_after_cap.py -v` exits 0 — 60s cap verified
    - CLI: `uv run pytest verification/test_logging_root_unchanged.py -v` exits 0 — root logger UNCHANGED post import × 4 paquetes
    - CLI: `uv run pytest verification/test_logging_no_token_leak.py -v` exits 0 — parametrize × 4 paquetes ALL GREEN with SECRET-LITERAL-12345 sentinel
    - CLI: `uv run pytest verification/test_async_cancellation.py -v` exits 0 — ámbito/iol/higyrus PASS, matriz SKIP per D-25
    - CLI: `uv run pytest packages/ verification/ -q --no-header 2>&1 | tail -10` shows total tests ≥ 527 baseline + 40 new ≈ ≥570; no failures
    - CLI: `! grep -rn --include='*.py' 'logging\.basicConfig\|logging\.root' packages/*/src/` exits 0 (no matches found — D-27 LOG-01 verified)
    - CLI: `wc -l < packages/matriz-client/src/matriz_client/aio.py` returns 103 (Phase 6 stub preservado per D-25)
    - CLI: `! test -f packages/matriz-client/src/matriz_client/_atransport.py` exits 0 (file NOT created per D-25)
    - Source: `grep -c 'nyquist_compliant: true' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` == 1
    - Source: `grep -c 'matriz_new_order\|Pitfall 4' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` >= 1
    - Source: `grep -c 'CR-03\|CR-05' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` >= 2
    - Source: `grep -c 'D-25\|aio.py.*103' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` >= 1
    - Source: `grep -ci 'pitfall 18\|no tests were weakened' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` >= 1
    - Source: `grep -c 'LOC delta\|tenacity' .planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` >= 2
  </acceptance_criteria>
  <done>
    Todas las gates locally verde; `08-VALIDATION.md` actualizado con `nyquist_compliant: true` + evidencia consolidada (LOC delta + 6 guard tests final status + CR-03/CR-05 preservation + D-25 matriz aio.py UNCHANGED + Pitfall 18 statement + commit log + tenacity py.typed verified).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator checkpoint — review CI matrix on PR + confirm SUMMARY.md drops + 5 atomic commits + duplicate-order risk closure</name>
  <files>.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md</files>
  <action>
    Pause for operator. Operator reviews PR CI matrix + each plan's SUMMARY.md + matriz aio.py preservation + duplicate-order risk per the steps in `<how-to-verify>`, then types "approved" in chat. No file modification — this is the final close-out gate of Phase 8.
  </action>
  <what-built>
    Task 1 completó toda la matrix locally verde y produjo `08-VALIDATION.md` con `nyquist_compliant: true`. Antes de cerrar Phase 8, el operator humano confirma:
    1. CI matrix Python 3.12 + 3.13 corre verde sobre el merge commit (no solo local) — incluye nueva CI step `lint-logging` (Plan 1).
    2. Cada `08-0X-SUMMARY.md` (X=1..5) reporta sus deliverables específicos:
       - Plan 1: 6 cross-cutting guard tests creados + tenacity dep agregada + CI grep + ruff LOG ruleset; test count baseline confirmed (527).
       - Plan 2 (ámbito canary): _transport.py + _atransport.py + _logging.py + Client/AsyncClient/configure() 2 kwargs + snapshot update + cross-cutting guard tests turn GREEN for ámbito.
       - Plan 3 (iol): + OAuth refresh_token URL/JSON redaction + 401 re-auth-once flow shell pattern + CR-01 preservation.
       - Plan 4 (higyrus): + account_id propagation via RequestSpec + JSON password/token + cuit query redaction + URL-encoding quirk preservation.
       - Plan 5 (matriz): sync-only delivery (D-25 — aio.py UNCHANGED 103 LOC) + Risk API auth_basic branch (no re-auth per D-23) + matriz mutating builders KEEP idempotent=False (Pitfall 4 critical) + D-22 auth_basic redaction + CR-03 + CR-05 preservation.
    3. Ningún test pre-existente fue weakeneado o skipeado para hacer pasar Phase 8 (Pitfall 18 statement).
    4. Public surface snapshot Phase 6 sigue idéntico EXCEPTO los kwargs nuevos: ámbito/iol/higyrus add 2 kwargs in 3 signatures each; matriz adds 2 kwargs in Client + configure() (AsyncClient UNCHANGED per D-25).
    5. matriz `aio.py` sigue siendo el stub Phase 6 (103 LOC), no fue modificado por error.
    6. matriz `_atransport.py` does NOT exist (D-25).
    7. **CRITICAL — Duplicate-order risk closure verified**: `verification/test_retry_mutation_gate.py[matriz_new_order]` PASS (Pitfall 4 / D-01 / D-24). Esta es la red de seguridad principal del milestone para matriz Primary API HTTP-GET-for-mutation quirk — un fallo aquí significa risk de duplicate orders en producción.
    8. tenacity 9.1.4 confirmed (importlib.metadata + py.typed verified post-Plan-1).
  </what-built>
  <how-to-verify>
    1. Visit el PR Phase 8 en GitHub (URL provista al final del execute-phase output). Verificar CI matrix status: todos los jobs (lint incluyendo lint-logging, lint-imports, typecheck, tests Python 3.12, tests Python 3.13) muestran ✓.
    2. Abrir cada `08-0X-SUMMARY.md` (X=1..5) y confirmar:
       - Plan 1: lista de 6 guard tests RED initially → GREEN tras Plan 5; tenacity 9.1.4 verified; CI grep + ruff LOG enabled; baseline 527 confirmed.
       - Plan 2 (ámbito): LOC delta (_transport ~95, _atransport ~95, _logging ~65, client +~40, aio +~40, init +3); snapshot update 2 kwargs in 3 signatures; canary pattern verified.
       - Plan 3 (iol): LOC delta + IOL refresh_token URL + JSON redaction + 401 re-auth-once pattern + CR-01 preserved.
       - Plan 4 (higyrus): LOC delta + account_id propagation + JSON password + JSON token + cuit query + URL-encoding quirk preserved.
       - Plan 5 (matriz): LOC delta (NOTE: _atransport.py = N/A per D-25; aio.py = 103 UNCHANGED) + Risk API auth_basic branch + mutating builders idempotent=False + D-22 auth_basic redaction + CR-03 + CR-05 + matriz sweep snapshot 18 cases preserved.
    3. Correr `git log --oneline .planning/phases/08-retries-backoff-structured-logging/ packages/ verification/ pyproject.toml .github/workflows/ci.yml | head -10` y confirmar 5 commits (Plan 1 + 4 per-package per D-21) con commit messages siguiendo el patrón RESEARCH §"Commit message patterns Phase 8" lines 412-415.
    4. Run `wc -l packages/matriz-client/src/matriz_client/aio.py` — debe devolver exactamente `103` (Phase 6 stub unchanged per D-25).
    5. Run `! test -f packages/matriz-client/src/matriz_client/_atransport.py && echo OK` — debe imprimir `OK` (matriz _atransport.py NOT created per D-25).
    6. Run `uv run pytest verification/test_retry_mutation_gate.py -k matriz_new_order -v` — DEBE pasar verde (CRITICAL — duplicate-order risk closure for matriz Primary API; this single test is the most important guard of Phase 8).
    7. Visit el merge commit + verificar tenacity 9.1.4 IS in uv.lock (`grep -A 2 "name = \"tenacity\"" uv.lock | head -3`).
    8. Mensaje de aprobación esperado: "approved — all 5 phase 8 success criteria met; CI matrix green; lint-logging grep step active; SUMMARYs consistent; Pitfall 18 honored; matriz aio.py preserved at 103 LOC; matriz _atransport.py confirmed absent (D-25); test_retry_mutation_gate.py[matriz_new_order] PASS (Pitfall 4 duplicate-order risk closed); tenacity 9.1.4 in uv.lock."
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 8 close-out items in `<how-to-verify>` pass; CI matrix on PR is green; SUMMARYs are consistent; matriz aio.py preserved at 103 LOC; matriz _atransport.py absent; CRITICAL test_retry_mutation_gate.py[matriz_new_order] PASS.</human-check>
  </verify>
  <done>
    Operator types "approved" — Phase 8 ready_for_verify y el STATE.md update + final commit proceed.
  </done>
  <resume-signal>Type "approved" para cerrar la phase y avanzar a STATE.md update + final commit. Type "blocked: &lt;reason&gt;" si encuentra alguna gate inconsistente.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local execution → CI matrix (Python 3.12 + 3.13) | Task 1 corre local; Task 2 verifica CI on merged PR |
| Plan 1-5 isolated outputs → consolidated state | Task 1 valida el delta combinado, no solo cada plan aislado |
| matriz aio.py preservation across Phase 8 → Phase 10 boundary | D-25 LOCK — Plan 5 explicitamente NO toca aio.py; Plan 6 re-verifica via `wc -l == 103` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-CI-DRIFT | Tampering (CI integrity) | Local verde pero CI Python 3.13 rojo (e.g., tenacity version diff resolve en lockfile diff) | mitigate | Task 2 human-verify CHECKS CI matrix status on PR (not just local); tenacity semver pin `>=9.1.0,<10` prevents major drift |
| T-8-WEAK | Tampering (test weakening — Pitfall 18) | Algún Plan 2-5 podría haber removido o skipeado un test pre-existente para hacer pasar el refactor | mitigate | Task 1 incluye Pitfall 18 statement; Task 2 human-verify spotchecks SUMMARYs por mention de skipped/removed tests; CI test count delta visible |
| T-8-D25-AIO-DRIFT | Tampering (Phase scope creep) | matriz `aio.py` accidentally modificado en Plan 5 (debería seguir stub Phase 6 → Phase 10) | mitigate | Task 1 captura `wc -l matriz/aio.py == 103` en 08-VALIDATION.md; Task 2 human-verify re-corre `wc -l` confirma; Plan 6 acceptance_criteria explicitamente bloquea si != 103 |
| T-8-D25-ATRANSPORT-DRIFT | Tampering (Phase scope creep) | matriz `_atransport.py` accidentally created in Plan 5 (should be deferred to Phase 10) | mitigate | Task 1 verifies `! test -f matriz/_atransport.py`; Task 2 human-verify re-verifies; acceptance_criteria blocks if exists |
| T-8-PITFALL-4-CRITICAL | Tampering (duplicate orders in production) | `test_retry_mutation_gate.py[matriz_new_order]` accidentally weakened OR matriz mutating builder accidentally flipped to `idempotent=True` | mitigate (HIGH SEVERITY) | Task 1 explicit step runs `uv run pytest verification/test_retry_mutation_gate.py -k matriz_new_order -v` and captures output; Task 2 human-verify re-runs as final gate. If this PASS, Phase 8's most critical safety property is confirmed; if FAIL, Phase 8 BLOCKED — duplicate-order risk in production |
| T-8-D27-LINT-LOGGING-DRIFT | Tampering (LOG-01 enforcement bypassed) | CI lint-logging step accidentally removed OR ruff LOG ruleset disabled | mitigate | Task 1 verifies CI step + ruff LOG present; Task 2 human-verify checks `.github/workflows/ci.yml` lint-logging step + pyproject.toml ruff select for "LOG" |
| T-8-CR-PRESERVE | Tampering (regression) | Phase 7 CR-03 (parse_envelope_response body-consume) or CR-05 (envelope_probe 18 sweeps) accidentally broken during Plan 5 | mitigate | Task 1 runs both regression tests; Task 2 confirms via SUMMARY.md |
</threat_model>

<verification>
- `08-VALIDATION.md` con `nyquist_compliant: true`
- CI matrix verde (operator confirms via PR status) including new lint-logging grep step
- Plans 1-5 SUMMARYs consistent y atomic commits = 5 total (per D-21)
- Pitfall 18 honored: no pre-existing test weakened
- Public surface snapshot Phase 6 unchanged excepto los 2 kwargs por paquete (ámbito/iol/higyrus 3 signatures; matriz 2 signatures — AsyncClient excluded per D-25)
- matriz aio.py UNCHANGED (103 LOC stub)
- matriz `_atransport.py` does NOT exist
- CRITICAL: `test_retry_mutation_gate.py[matriz_new_order]` PASS (Pitfall 4 / duplicate-order prevention)
- Phase 7 CR-03 + CR-05 preserved (regression tests GREEN)
- tenacity 9.1.4 in uv.lock + importable + py.typed
</verification>

<success_criteria>
- `nyquist_compliant: true` en 08-VALIDATION.md
- Toda CI matrix verde (Python 3.12 + 3.13)
- 5 Phase 8 ROADMAP success criteria cubiertos:
  1. RetryTransport per paquete con full-jitter backoff + Retry-After cap 60s — verified by 6 guard tests × 4 paquetes parametrize
  2. Mutation-aware retry gate funciona end-to-end (POST 503 → 1 wire request) — verified by `test_retry_mutation_gate.py[matriz_new_order]` CRITICAL
  3. 401 re-auth-once en shell — verified by `test_retry_401_reauth.py[iol/higyrus/matriz Primary]` (3 wire requests) + matriz Risk path NO re-auth per D-23
  4. NullHandler + grep CI rule + ruff LOG015 — verified by `test_logging_root_unchanged.py` + CI lint-logging step
  5. RedactingFilter con per-paquete patterns — verified by `test_logging_no_token_leak.py[ambito/iol/higyrus/matriz]` (SECRET-LITERAL-12345 nunca aparece)
- Operator human-verify approval marca phase ready_for_verify
</success_criteria>

<output>
Actualizar `.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md` con `nyquist_compliant: true` + evidencia consolidada (Task 1).
Create `.planning/phases/08-retries-backoff-structured-logging/08-06-SUMMARY.md` documentando:
- Green gate matrix verde
- LOC delta consolidated (4 paquetes; matriz _atransport.py N/A; matriz aio.py UNCHANGED 103)
- Test count baseline (527 Phase 7) → final (≥570 Phase 8) — at least 40 new tests
- Cross-cutting guard tests final GREEN status (6 files × parametrize branches)
- CR-03 + CR-05 + matriz sweep snapshot preservation evidence
- D-25 forward-ref Phase 10 acknowledged
- CRITICAL Pitfall 4 verification: test_retry_mutation_gate.py[matriz_new_order] PASS
- 5 atomic commit log (Plan 1 + 4 per-package per D-21)
- Operator approval marca + timestamp
- "Phase 8 ready_for_verify — `/gsd-verify-work 8` next"
</output>
