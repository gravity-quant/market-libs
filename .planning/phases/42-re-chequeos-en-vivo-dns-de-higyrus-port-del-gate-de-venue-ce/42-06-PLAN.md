---
phase: 42
plan: 06
type: execute
wave: 4
depends_on: ["42-02", "42-04", "42-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/research/ARCHITECTURE.md
  - .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md
autonomous: true
requirements: [LIVE-01, LIVE-02]

must_haves:
  truths:
    - "El backlog deja de afirmar algo verificado falso: la entrada LIVE-MATZ-33 ya no dice que el script tiene el gate listo"
    - "Las entradas de backlog forward-looking llevan el resultado medido de esta corrida y el destino renombrado"
    - "Los cinco criterios de éxito del ROADMAP § Phase 42 tienen cada uno una disposición explícita con su evidencia nombrada"
    - "Los 4 gates de CI quedan verdes y los tres artefactos fechados existen en disco"
    - "La fase no instaló ningún paquete: uv.lock y los pyproject.toml quedan sin cambios"
  artifacts:
    - path: ".planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md"
      provides: "Disposición explícita de los 5 criterios de éxito con evidencia nombrada, más el resultado de los 4 gates y el inventario de artefactos"
      min_lines: 50
      contains: "criterio 5"
    - path: ".planning/ROADMAP.md"
      provides: "Entradas de backlog corregidas y anotadas con el resultado medido de esta corrida"
      contains: "LIVE-HIGY-42"
  key_links:
    - from: ".planning/phases/42-…/42-CLOSURE.md"
      to: ".planning/ROADMAP.md"
      via: "una fila por cada uno de los 5 criterios de éxito de § Phase 42, con la evidencia de la fase nombrada"
      pattern: "Success Criteria"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/42-…/42-WIRE-READ.md"
      via: "la entrada SHAPE-MD-REF-33 apunta a la lectura fresca que la Phase 43 debe consumir en vez del baseline congelado"
      pattern: "42-WIRE-READ"
---

<!-- planner-discipline-allow: LIVE-HIGY-33 -->

<objective>
Cerrar la fase: corregir las afirmaciones forward-looking del backlog que esta corrida volvió
falsas o desactualizadas, anotar el resultado medido en las entradas que lo esperaban, y producir
una disposición explícita de los **cinco** criterios de éxito del `ROADMAP.md § Phase 42` con su
evidencia nombrada.

Purpose: dos razones concretas. (1) `42-RESEARCH.md § State of the Art` verificó que la entrada de
backlog `LIVE-MATZ-33` afirma que el script "ya tiene el gate listo para correr contra bbsa" —
**verificado falso en HEAD**, y el ROADMAP se auto-corrige en su nota de sizing pero la entrada no
se actualizó. Dejarla así hace que el próximo milestone herede la misma afirmación falsa (Open
Question Q5, recomendación: corregirla en el cierre). (2) Sin una disposición criterio por criterio,
"la fase corrió" no es lo mismo que "los cinco criterios están satisfechos", y la Phase 43 arranca
sobre una precondición que nadie verificó.

Output: `42-CLOSURE.md` con la disposición de los 5 criterios, el backlog corregido y anotado, y los
4 gates de CI verdes.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-02-SUMMARY.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-03-SUMMARY.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-04-SUMMARY.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-05-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos y archivos creados por la Phase 42 (no existen en HEAD). Excluir de cualquier verificación
de drift: son salida, no deriva.

| Artefacto | Tipo | Creado en |
|-----------|------|-----------|
| `verification/test_literal_census_venue_gate.py` (11 tests + `_census_matriz_node`, `_membership_offenders`, `_TARGET`) | archivo nuevo | 42-01 |
| `_census_header_lines()`, `_census_header()`, local `venue`, flag `--matriz-only`, prefijos `CENSUS-HEADER`/`CENSUS-DLOCK` | símbolos nuevos en `scripts/literal_census_33.py` | 42-01 |
| entrada `verification/test_literal_census_venue_gate.py` en `ci.yml` | línea de allowlist | 42-01 |
| `.planning/phases/42-…/42-CENSUS.md` | artefacto committeado | 42-02 |
| `capture` importado en `main_market_data.py`; labels `wire-instruments-42` / `wire-segments-42`; capturas gitignored | símbolos y artefactos nuevos | 42-04 |
| `.planning/phases/42-…/42-WIRE-READ.md` | artefacto committeado | 42-04 |
| `LIVE-HIGY-42` | identificador de destino nuevo | 42-05 |
| `.planning/phases/42-…/42-CLOSURE.md` | artefacto committeado | 42-06 |

<tasks>

<task type="auto">
  <name>Task 1: Corregir y anotar los artefactos de planificación forward-looking</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/research/ARCHITECTURE.md</files>
  <read_first>
    - `.planning/phases/42-…/42-03-SUMMARY.md` y `42-05-SUMMARY.md` — el veredicto medido de higyrus
      y si el rename D-06 disparó. Todo lo que se anote acá tiene que salir de esas mediciones.
    - `.planning/phases/42-…/42-02-SUMMARY.md` — la disposición de los 5 campos del censo.
    - `.planning/phases/42-…/42-04-SUMMARY.md` — el `captured_at` de la lectura fresca del wire.
    - `.planning/ROADMAP.md` línea ~210 — la entrada de backlog del bloqueo de higyrus (**abierta**,
      forward-looking): se le anota el resultado de esta corrida y se le renombra el identificador
      si D-06 disparó.
    - `.planning/ROADMAP.md` líneas ~206-208 — las dos entradas de `LIVE-MATZ-33` (la de v1.7 y la
      histórica de la Phase 33). La primera contiene la afirmación **verificada falsa** de que
      `scripts/literal_census_33.py` "ya lleva el gate remarkets-only y corre el censo completo apenas
      haya un `PRIMARY_BASE_URL` de remarkets".
    - `.planning/ROADMAP.md` línea ~181 — la línea de trazabilidad que mapea los ítems de backlog a
      sus fases (menciona el identificador viejo de higyrus).
    - `.planning/ROADMAP.md` línea ~52 — la nota de sizing del milestone v1.8 que **ya** declara el
      gate STALE: es la afirmación correcta, y la entrada de backlog es la que hay que alinear a
      ella, no al revés.
    - `.planning/PROJECT.md` líneas `174` y `286` — "Next milestone" y el requisito abierto LIVE-01:
      son forward-looking y se renombran/anotan. Las líneas `29`, `78`, `278` y `417` son
      **históricas** y **no se tocan**.
    - `.planning/research/ARCHITECTURE.md` línea ~85 — transcribe verbatim el valor de
      `_VENDOR_UNREACHABLE_SKIP_LINE`; si D-06 disparó, esa transcripción quedó stale.
    - `.planning/STATE.md` — **no se toca** en esta task: el log de decisiones es inmutable por
      convención y sus menciones históricas se quedan como están.
  </read_first>
  <action>
    Cuatro ediciones **acotadas** (usar reemplazo dirigido en cada sitio; nunca reescribir un archivo
    entero — `ROADMAP.md` y `PROJECT.md` tienen contenido fuera de la ventana de este diff que se
    destruiría):

    1. **`ROADMAP.md`, entrada de backlog del bloqueo de higyrus (~línea 210).** Anotar al final de
       la entrada, con la forma que ya usan las otras entradas (`**→ …**` / `**Estado …**`): la fecha
       de esta corrida, el veredicto medido, la clase de excepción citada, el `captured_at` del sobre
       regenerado, y —si D-06 disparó— el identificador de destino nuevo. Renombrar el identificador
       del título de la entrada **sólo si D-06 disparó**. Dejar explícito que el resultado es una
       **re-confirmación medida**, no el cierre del ítem: los 22 triples sin contrastar
       (`Movimiento` 9, `PosicionValuada` 11, `Posicion` 2) siguen abiertos.

    2. **`ROADMAP.md`, entrada `LIVE-MATZ-33` de v1.7 (~línea 206) — la corrección de Q5.** Reemplazar
       la afirmación de que el script "ya lleva el gate remarkets-only y corre el censo completo apenas
       haya un `PRIMARY_BASE_URL` de remarkets" por el hecho verificado: el script estaba en
       substring-match pre-Phase-39 y habría salteado en silencio contra `bbsa`; la Phase 42 portó el
       gate a igualdad exacta de hostname por import desde `main_matriz.py` y corrió el censo.
       Anotar el resultado: fecha, venue medido, y la disposición resumida de los cinco campos
       (`marketId`, `cficode`, `currency`, `orderTypes`, `ordType`) — cuáles quedaron `MEDIDO` y
       cuáles `NO MEDIBLE EN ESTA CORRIDA`, con puntero a `42-CENSUS.md`. Dejar dicho que el
       D-lock (b) sigue en vigor.

    3. **`ROADMAP.md` línea de trazabilidad (~181) y `PROJECT.md` líneas `174` y `286`.** Renombrar el
       identificador de destino de higyrus **sólo si D-06 disparó**, y anotar en `PROJECT.md:286` el
       estado medido del requisito LIVE-01. **No tocar** `PROJECT.md:29`, `:78`, `:278` ni `:417`:
       son párrafos históricos de fases completadas.

    4. **`.planning/research/ARCHITECTURE.md` (~línea 85).** Si D-06 disparó, actualizar la
       transcripción verbatim de la constante para que refleje HEAD. Es coherencia documental, no
       bloqueante; si la línea no existe o ya coincide, no hacer nada y decirlo en el SUMMARY.

    **Lo que NO se toca en esta task, por escrito:**
    - `.planning/STATE.md` (log de decisiones, inmutable por convención).
    - `.planning/milestones/**` (árbol congelado de v1.6/v1.7 auditado por la Phase 41).
    - `.planning/PROJECT.md:29,78,278,417` (párrafos históricos).
    - `.planning/research/PITFALLS.md` (prosa narrativa sobre el bloqueo histórico; no transcribe una
      constante de HEAD).
    - La corrección de `29-DLOCK-RESPONSE-LITERAL.md:140-142`: el documento está **firmado**, así que
      la corrección pertenece al firmante y queda **fuera del alcance** de esta fase — sólo se nombra
      en `42-CENSUS.md` para que no se pierda.
  </action>
  <verify>
    <automated>test "$(grep -c 'LIVE-HIGY-42' .planning/ROADMAP.md)" -ge "1" &amp;&amp; test "$(grep -c 'LIVE-HIGY-42' .planning/PROJECT.md)" -ge "1" &amp;&amp; grep -q "42-CENSUS" .planning/ROADMAP.md &amp;&amp; test -z "$(git status --porcelain -- .planning/milestones/ .planning/STATE.md)" &amp;&amp; test "$(grep -c 'LIVE-HIGY-33' .planning/PROJECT.md)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - La entrada de backlog del bloqueo de higyrus en `ROADMAP.md` lleva la fecha de esta corrida, el
      veredicto medido, la clase de excepción citada y —si D-06 disparó— el identificador nuevo, y
      dice explícitamente que los 22 triples siguen sin contrastar.
    - La entrada `LIVE-MATZ-33` de v1.7 ya **no** afirma que el script tiene el gate listo; en su
      lugar registra el hecho verificado (substring-match pre-Phase-39) y el resultado de la corrida,
      con puntero a `42-CENSUS.md`.
    - `.planning/PROJECT.md` conserva sus menciones **históricas** del identificador viejo (líneas
      29/78/278/417 intactas — `grep -c` del identificador viejo sobre `PROJECT.md` sigue siendo
      ≥ 1) y sus líneas forward-looking (`174`, `286`) están actualizadas.
    - `git status --porcelain -- .planning/milestones/ .planning/STATE.md` vacío.
    - `git diff --stat` de esta task toca como máximo tres archivos:
      `.planning/ROADMAP.md`, `.planning/PROJECT.md` y `.planning/research/ARCHITECTURE.md`.
    - Ningún archivo de fuente (`*.py`, `*.yml`) cambió en esta task.
  </acceptance_criteria>
  <done>El backlog deja de arrastrar una afirmación verificada falsa, las entradas forward-looking
  llevan el resultado medido y el destino correcto, y la historia congelada quedó intacta.</done>
</task>

<task type="auto">
  <name>Task 2: 42-CLOSURE.md — disposición de los 5 criterios y gate cross-fase</name>
  <files>.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § Phase 42 → **Success Criteria**, los cinco puntos verbatim. Son el
      denominador de este documento.
    - Los cinco SUMMARYs de la fase (`42-01` … `42-05`) — la evidencia que se cita, criterio por
      criterio.
    - `.planning/phases/42-…/42-CENSUS.md` y `42-WIRE-READ.md` — los dos artefactos committeados que
      sostienen los criterios 3 y 5.
    - `.planning/verification/run-evidence/higyrus-client.json` — el sobre que sostiene el criterio 2.
    - `.planning/phases/42-…/42-VALIDATION.md` — el mapa de verificación por task; sus filas
      `⬜ pending` se cierran con el resultado real, y el front-matter `nyquist_compliant` /
      `wave_0_complete` se actualiza sólo con evidencia re-ejecutada en esta sesión, nunca por flip
      mecánico.
    - `.github/workflows/ci.yml:79-92` — las 13 rutas de la allowlist tras el enrolamiento del plan
      42-01; es el comando que refleja CI, no `pytest -q` a secas.
    - `.planning/phases/42-…/42-RESEARCH.md` § Validation Architecture — el baseline medido
      (`129 passed`) contra el que se compara.
  </read_first>
  <action>
    Escribir `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md`
    con estas secciones:

    1. **Disposición de los cinco criterios.** Una tabla con una fila por criterio (1 a 5), y las
       columnas: enunciado abreviado, disposición (`SATISFECHO` / `SATISFECHO POR LA VÍA DECLARADA` /
       `NO SATISFECHO`), y **evidencia nombrada** (archivo:línea, comando + salida, o SUMMARY que la
       registra). **Cero filas sin disponer.** `SATISFECHO POR LA VÍA DECLARADA` es la disposición
       correcta cuando el criterio admite explícitamente una salida alternativa y ésa fue la que
       ocurrió — p.ej. el criterio 3 admite "declara qué campo no se pudo medir y por qué", y el
       criterio 2 admite `SKIPPED con causa re-confirmada`.
    2. **Requisitos.** `LIVE-01` y `LIVE-02` con su estado y la evidencia que lo sostiene.
    3. **Resultado de los 4 gates de CI**, con el comando exacto y su salida:
       `uv run --frozen ruff check .`; `uv run --frozen ruff format --check .`;
       `uv run --frozen mypy`; y `uv run pytest -q` sobre las **13** rutas de la allowlist de
       `.github/workflows/ci.yml`, comparando el conteo de `passed` contra el baseline `129` medido
       en HEAD antes de la fase. Registrar el delta `129 → 129+N` y `0 failed`.
    4. **Inventario de artefactos fechados**, con su ruta y su `captured_at`:
       `run-evidence/higyrus-client.json`, las dos capturas de wire de market-data (gitignored), y
       los dos documentos committeados `42-CENSUS.md` y `42-WIRE-READ.md`.
    5. **Pins de seguridad al cierre:** `git hash-object verification/mutation_gate.py` medido, más
       la confirmación de que `verification/test_literal_census_venue_gate.py` está en la allowlist
       de CI y verde.
    6. **Ausencia de instalación de paquetes.** Registrar la salida de
       `git diff --stat HEAD~ -- uv.lock packages/*/pyproject.toml pyproject.toml` (o el rango de la
       fase) probando que la fase no instaló, actualizó ni adoptó ninguna dependencia — el milestone
       v1.8 se define explícitamente como "sin superficie nueva" (`REQUIREMENTS.md:4`).
    7. **Lo que esta fase NO cierra.** Lista explícita para que nadie la lea de más:
       `LIVE-HIGY-33`/`LIVE-HIGY-42` sigue abierto con sus 22 triples sin contrastar; el gap WR-02
       (`httpx.ConnectTimeout`) queda re-declarado fuera de alcance con destino nombrado (decisión
       escrita en `42-03-PLAN.md`); la corrección de `29-DLOCK-RESPONSE-LITERAL.md:140-142` pertenece
       a su firmante; SHAPE-01 / HARN-02 son la Phase 43; el churn del ledger de market-data lo
       limpia HARN-01 en la Phase 45.
    8. **Precondición entregada a la Phase 43.** Nombrar `42-WIRE-READ.md` con su `captured_at` como
       la evidencia que `REQUIREMENTS.md:82` declara producida por esta fase, y decir explícitamente
       que los baselines del 2026-07-31 quedaron marcados no-autoritativos.

    Después de escribir el documento, actualizar `.planning/phases/42-…/42-VALIDATION.md`: cerrar las
    filas `⬜ pending` de la tabla "Per-Task Verification Map" con su estado real (`✅`/`❌`), apuntar
    la columna `Plan` a los planes reales (`42-01` … `42-06`) y poner `wave_0_complete: true` sólo si
    los cinco ítems de "Wave 0 Requirements" quedaron efectivamente cerrados. **No poner
    `nyquist_compliant: true` por flip mecánico**: sólo si cada fila tiene un comando automatizado
    que se re-ejecutó en esta sesión, y las dos filas `manual-only` (censo en vivo y checkpoint
    humano) están registradas como tales con su evidencia.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md &amp;&amp; for n in "criterio 1" "criterio 2" "criterio 3" "criterio 4" "criterio 5"; do grep -qi "$n" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md || exit 1; done &amp;&amp; grep -q "6bdaec006cc16f7c8dbfac41701712a9085c691b" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CLOSURE.md &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; uv run --frozen mypy &amp;&amp; uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_run_evidence.py verification/test_main_iol_deep_chain.py verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py verification/test_cycle_closure_phase33.py verification/test_literal_census_venue_gate.py &amp;&amp; git diff --exit-code -- uv.lock</automated>
  </verify>
  <acceptance_criteria>
    - `42-CLOSURE.md` existe y dispone los **cinco** criterios del `ROADMAP.md § Phase 42`, cada uno
      con exactamente una disposición y con su evidencia nombrada (archivo:línea, comando+salida o
      SUMMARY). Cero filas sin disponer.
    - `LIVE-01` y `LIVE-02` aparecen con su estado y evidencia.
    - Los 4 gates de CI están registrados con su comando exacto y su salida:
      `ruff check` y `ruff format --check` sin diferencias, `mypy` con `Success`, y la corrida de las
      **13** rutas de la allowlist con `0 failed` y `passed` > `129`.
    - El documento registra `git hash-object verification/mutation_gate.py` =
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
    - El documento registra que `uv.lock` y los `pyproject.toml` no cambiaron
      (`git diff --exit-code -- uv.lock` sale 0).
    - El documento lleva la sección "Lo que esta fase NO cierra" con los cinco ítems enumerados.
    - `42-VALIDATION.md` no tiene filas `⬜ pending` en su "Per-Task Verification Map", y
      `nyquist_compliant` sólo pasa a `true` si toda fila tiene comando re-ejecutado en esta sesión
      (las dos filas `manual-only` registradas como tales con su evidencia).
    - `git status --porcelain -- .planning/milestones/ .planning/STATE.md` vacío.
  </acceptance_criteria>
  <done>Los cinco criterios tienen disposición explícita con evidencia nombrada, los 4 gates están
  verdes y registrados, y lo que la fase NO cierra quedó escrito para que nadie lo lea de más.</done>
</task>

</tasks>

<threat_model>
> `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artefactos forward-looking (`ROADMAP`, `PROJECT`) ↔ historia congelada (`STATE`, `milestones/`) | El cierre escribe sólo del lado forward-looking |
| resultado medido → afirmación escrita en el backlog | Toda anotación debe salir de una medición de la fase, nunca de una expectativa |
| árbol de dependencias (`uv.lock`, `pyproject.toml`) → cierre de fase | El milestone v1.8 se define sin superficie nueva; el cierre lo prueba |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-17 | Tampering | Historia congelada (`STATE.md`, `.planning/milestones/`, párrafos históricos de `PROJECT.md`) | mitigate | Ediciones dirigidas sitio por sitio con lista escrita de "lo que NO se toca"; `<verify>` asevera `git status --porcelain -- .planning/milestones/ .planning/STATE.md` vacío y que `PROJECT.md` conserva sus menciones históricas |
| T-42-21 | Repudiation | Marcar criterios como satisfechos sin evidencia nombrada | mitigate | `42-CLOSURE.md` exige una fila por criterio con disposición única **y** evidencia nombrada; `<acceptance_criteria>` prohíbe filas sin disponer. `nyquist_compliant` no se flipea mecánicamente |
| T-42-22 | Repudiation | Backlog que arrastra una afirmación verificada falsa a otro milestone | mitigate | Corrección explícita de la entrada `LIVE-MATZ-33` (Q5), alineada a la nota de sizing del ROADMAP que ya declara el gate STALE |
| T-42-SC | Tampering | npm/pip/cargo installs (supply chain) | mitigate | `git diff --exit-code -- uv.lock` en el `<verify>` y registro explícito en `42-CLOSURE.md § Ausencia de instalación de paquetes`. `42-RESEARCH.md § Package Legitimacy Audit` es N/A porque la fase no instala nada; cero paquetes `[ASSUMED]`, `[SUS]` o `[SLOP]` |
| T-42-02 | Tampering | `verification/mutation_gate.py` | mitigate | Byte-identidad registrada en `42-CLOSURE.md` y aseverada en el `<verify>` (`6bdaec00…`) |
| T-42-06 | Repudiation | Lock verde en local pero inerte en CI | mitigate | El `<verify>` corre las **13** rutas de la allowlist de `ci.yml` (no `pytest -q` a secas) y `42-CLOSURE.md` registra que el lock nuevo está enrolado |
</threat_model>

<verification>
1. `uv run --frozen ruff check .` → sin errores.
2. `uv run --frozen ruff format --check .` → sin diferencias.
3. `uv run --frozen mypy` → `Success`.
4. `uv run pytest -q` sobre las 13 rutas de la allowlist de `.github/workflows/ci.yml` → `0 failed`,
   `passed` > 129.
5. `uv run python scripts/literal_census_33.py --selftest` → `SELFTEST: PASS`.
6. `git hash-object verification/mutation_gate.py` = `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
7. `git diff --exit-code -- uv.lock` → sin cambios.
8. Los cinco artefactos de la fase existen: `42-CENSUS.md`, `42-WIRE-READ.md`, `42-CLOSURE.md`,
   `run-evidence/higyrus-client.json` fechado hoy, y las dos capturas de wire (gitignored).
9. `git status --porcelain -- .planning/milestones/ .planning/STATE.md` vacío.
</verification>

<success_criteria>
- Los cinco criterios de éxito del `ROADMAP.md § Phase 42` tienen disposición explícita con
  evidencia nombrada; cero filas sin disponer.
- El backlog ya no afirma que el gate del script está listo; registra el hecho verificado y el
  resultado de la corrida.
- Las entradas forward-looking llevan el destino renombrado y el resultado medido.
- Los 4 gates de CI verdes, con el conteo de tests comparado contra el baseline 129.
- La fase no instaló ningún paquete y el gate de mutación quedó byte-idéntico.
- Lo que la fase NO cierra está escrito, con destino nombrado por ítem.
</success_criteria>

<output>
Create `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-06-SUMMARY.md` when done.

El SUMMARY debe incluir, además del formato estándar:
- La tabla de disposición de los 5 criterios, resumida.
- El resultado de los 4 gates con el conteo `129 → 129+N`.
- El inventario de artefactos con su `captured_at`.
- La lista de "lo que esta fase NO cierra" con su destino por ítem.
</output>
