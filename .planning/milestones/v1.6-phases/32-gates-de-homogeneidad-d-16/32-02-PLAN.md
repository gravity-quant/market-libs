---
phase: 32-gates-de-homogeneidad-d-16
plan: 02
type: execute
wave: 1
depends_on:
  - 32-01
files_modified:
  - tools/check_surface_types.py
  - packages/iol-client/tests/test_surface_types_red.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - GATE-TYP-01

must_haves:
  truths:
    - "`uv run python tools/check_surface_types.py` exits 0 on today's tree and prints a line reporting how many packages, __all__ names and definitions it scanned"
    - "A deliberately broken synthetic tree makes the gate raise CheckFailure naming the offending symbol — the non-vacuity proof runs as a test, not as a manual demonstration"
    - "An empty packages/ tree, an unresolvable src/ import root, and a missing or non-literal __all__ are each a FAILURE, never a silent skip or a green"
    - "The gate walks methods of exported classes, not only module-level functions — the to_dict() exemption is load-bearing and provably reachable"
    - "The gate never imports a package module, so no load_dotenv() or client construction runs inside it"
    - "The gate runs in real CI as a step of the existing `lint` job and can fail that job"
  artifacts:
    - path: "tools/check_surface_types.py"
      provides: "stdlib-AST-only surface-type gate with an injectable root, a structured ScanResult, and the DT-06 exemption taxonomy"
      exports: ["CheckFailure", "ScanResult", "scan_surface_types", "check_surface_types", "main"]
      min_lines: 180
    - path: "packages/iol-client/tests/test_surface_types_red.py"
      provides: "Automated two-bound proof: green on the real tree, RED on an injected regression, RED on an empty scan"
      min_lines: 70
    - path: ".github/workflows/ci.yml"
      provides: "surface-types step in the lint job, after the uniform-structure step"
      contains: "tools/check_surface_types.py"
  key_links:
    - from: "packages/iol-client/tests/test_surface_types_red.py"
      to: "tools/check_surface_types.py"
      via: "cross-package import of the repo-root tools namespace package, enabled by pythonpath = ['.'] at pyproject.toml:109 (Patrón 1)"
      pattern: "from tools\\.check_surface_types import"
    - from: ".github/workflows/ci.yml"
      to: "tools/check_surface_types.py"
      via: "lint job step invoking the gate through uv run python"
      pattern: "uv run python tools/check_surface_types\\.py"
    - from: "tools/check_surface_types.py"
      to: "packages/*/src/*/__init__.py"
      via: "ast.parse of each package __init__.py, resolving __all__ to definition sites through explicit ImportFrom statements"
      pattern: "ast\\.ImportFrom"
  prohibitions:
    - "The gate must never be wired into CI in a form that cannot fail the job. A step that reports a violation while the job stays green manufactures the appearance of enforcement and is worse than no gate at all. (flagged — unverified)"
    - "A red gate is never resolved by weakening the gate. The exemption predicate must not be broadened (for example from `to_dict` to any `to_*` name, or the underscore rule widened) in order to silence a real violation; the fix is to type the return properly, or to add an exemption WITH a stated reason in the docstring. (flagged — unverified)"
    - "The package roster must never be hardcoded in the gate. A seventh package entering `packages/` must be scanned automatically rather than exempted by omission. (flagged — unverified)"
---

<objective>
**TRACER SLICE.** Build the surface-type gate end to end — checker, automated RED proof, and
real CI wiring — and verify the whole path works before the phase expands into parity tests and
D-16 closure. This plan is deliberately a complete, shippable vertical slice of criterion 1 and
criterion 2, not a stub or a foundation layer.

Today the six packages' exported surfaces are clean (research simulated the gate over 319
definitions and found 0 non-exempt hits). The gate is therefore a **ratchet**, not a migration
tool: its entire value is that it fails the day someone adds `Client.get_x() -> dict[str, Any]`.
That makes non-vacuity the deliverable, which is why the RED proof is a test in the CI matrix and
not a note in a SUMMARY.

Purpose: make surface-type regression impossible to land silently.
Output: `tools/check_surface_types.py`, an automated RED fixture, and a new `lint` job step.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-gates-de-homogeneidad-d-16/32-CONTEXT.md
@.planning/phases/32-gates-de-homogeneidad-d-16/32-RESEARCH.md
@.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md
@tools/check_uniform_structure.py
@CLAUDE.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED — write the automated non-vacuity proof for the surface gate before the gate exists</name>
  <files>packages/iol-client/tests/test_surface_types_red.py</files>
  <read_first>
    - `packages/iol-client/tests/test_typed_surface_red.py` (48 lines, read in full) — the analog. Its docstring states *why* a RED fixture lives under `packages/<pkg>/tests/` (that path is what the CI mypy loop typechecks and what the 6×2 test matrix runs) and *what machinery was deliberately rejected*. Mirror both moves.
    - `verification/test_main_matriz_uses_single_client_instance.py` lines 75-100 — the lower-bound/upper-bound assertion shape and the docstring sentence that names the lower bound as the non-vacuity guard.
    - `packages/ambito-financiero-client/tests/test_harness_schema.py` lines 1-25 — the "Patrón 1" cross-package import and the docstring paragraph that justifies importing a repo-root module from inside a package test.
    - `pyproject.toml` lines 102-120 — `testpaths` at 106, `pythonpath = ["."]` at **109** (this is what makes `tools.check_surface_types` importable; note `tools/` has no `__init__.py` and resolves as an implicit namespace package), `addopts` at 115-120.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-RESEARCH.md` § "Gate Simulation Results" — the measured baseline this test asserts against: 319 definitions scanned, 22 exempted (12 dunder / 9 `to_dict` / 1 underscore), 0 non-exempt hits.
    - `.planning/milestones/v1.2-phases/15-driver-migration-4-refac-05/15-REVIEW.md` WR-01/WR-02 (approximately lines 61-164) — the canonical postmortem of a vacuous AST guard in this repo. This test exists to make that failure mode impossible here.
  </read_first>
  <behavior>
    Five tests, all against the not-yet-existing `tools.check_surface_types` module. The API the
    tests pin (and therefore the API Task 2 must implement):

    - `scan_surface_types(root: Path) -> ScanResult` — pure scan; raises `CheckFailure` on a
      structural problem (unresolvable import root, missing or non-literal `__all__`, unparseable
      source, empty scan).
    - `ScanResult` — a frozen dataclass carrying at least `packages: int`, `all_names: int`,
      `definitions: int`, `exempted: int`, `exempted_by_reason: tuple[tuple[str, int], ...]`,
      `violations: tuple[str, ...]`.
    - `check_surface_types(root: Path = REPO_ROOT) -> str` — calls the scan, raises `CheckFailure`
      when `violations` is non-empty, otherwise returns a one-line operator-readable summary
      carrying the counts.
    - `CheckFailure` — the exception type, same name and role as in `check_uniform_structure.py`.

    Test 1 — `test_gate_is_green_on_the_real_tree`: `check_surface_types()` (default root)
    returns a string; `scan_surface_types(REPO_ROOT)` has empty `violations`, `packages >= 6`,
    `definitions >= 300`, `exempted >= 20`. Bounds are floors, not equalities, so a seventh
    package or a new exported model does not falsely redden the suite — but a collapse to a
    trivial scan does.

    Test 2 — `test_gate_fails_on_an_injected_regression`: build a synthetic package under
    `tmp_path` at `packages/fake-client/src/fake_client/` with an `__init__.py` that imports
    `get_thing` from `fake_client.client` and lists it in `__all__`, and a `client.py` defining
    `get_thing` with a return annotation of an untyped mapping. `pytest.raises(CheckFailure)`
    with a match on the symbol name `get_thing`. This is the non-vacuity guard.

    Test 3 — `test_regression_inside_an_exported_class_is_caught`: same synthetic tree, but the
    offending definition is a **method** on an exported class rather than a module-level
    function. Must also raise. This is the executable form of D-03 — the most likely real
    regression vector is a new `Client.get_x()` returning an untyped mapping.

    Test 4 — `test_exempt_members_do_not_trip_the_gate`: synthetic tree whose only
    untyped-mapping returns are a `to_dict` method on an exported class, a dunder method, and an
    underscore-prefixed method. `scan_surface_types(tmp_path).violations` is empty and
    `exempted` counts all three, attributed to the reasons `serialize-out`, `dunder` and
    `private-helper` respectively.

    Test 5 — `test_empty_and_unresolvable_trees_are_failures_not_greens`: three sub-cases, each
    raising `CheckFailure` — (a) `tmp_path` with an empty `packages/` directory; (b) a package
    directory with no `src/`; (c) a package whose `__init__.py` has no module-level `__all__`.
    An empty scan reporting green is the exact failure this phase exists to prevent.
  </behavior>
  <action>
    Create `packages/iol-client/tests/test_surface_types_red.py`. iol is the host because it
    already carries the phase-30 RED-fixture precedent (`test_typed_surface_red.py`), so a reader
    finds both non-vacuity proofs in one place.

    Open with `from __future__ import annotations` (mandatory per CLAUDE.md, present in every
    analog). Import `pytest`, `Path` from `pathlib`, and the three names `CheckFailure`,
    `check_surface_types`, `scan_surface_types` from `tools.check_surface_types` — an absolute
    import, since ruff's `TID` rules ban relative imports.

    Write a module docstring in the shape of `test_typed_surface_red.py`'s: state that the file
    lives under `packages/iol-client/tests/` because that path runs in the 6×2 CI matrix while
    `verification/` never has, name the injectable-root seam (D-04) as the reason the gate is
    testable at all, and state that the two bounds are complementary — the green-tree test is the
    upper bound and the injected-regression test is the lower bound, and neither alone is a proof.

    Build the synthetic trees with `tmp_path` rather than committed fixture files. Committed
    fixtures under `packages/` would be picked up by `tools/check_decode_intactness.py`'s Check D
    roster and by `tools/check_uniform_structure.py`'s `models.py`/`types.py` requirement, turning
    two unrelated gates red. Factor the tree-building into a module-level helper taking the root
    and the source text for `client.py`, so the four synthetic cases do not duplicate `mkdir`
    plumbing.

    Every test function needs a `-> None` return annotation (`disallow_untyped_defs`). Follow
    ruff `PT` pytest-style: no test classes, `pytest.raises` with a `match=` argument, no bare
    `assert True`.

    RED evidence: run the file now. It must fail at collection with a `ModuleNotFoundError` for
    `tools.check_surface_types`, because the gate does not exist yet. Capture that output and
    record it in the SUMMARY as the RED observation. Commit this task alone as
    `test(32-02): add failing non-vacuity proof for the surface-types gate`.
  </action>
  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_surface_types_red.py -q` currently fails, and the failure is a `ModuleNotFoundError` naming `tools.check_surface_types` — not a syntax error, not a fixture error.
    - `uv run ruff check packages/iol-client/tests/test_surface_types_red.py` exits 0.
    - `uv run ruff format --check packages/iol-client/tests/test_surface_types_red.py` exits 0.
    - The file defines exactly five test functions: `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('packages/iol-client/tests/test_surface_types_red.py').read_text()); print(sum(1 for n in t.body if isinstance(n, ast.FunctionDef) and n.name.startswith('test_')))"` prints `5`.
    - Every test function carries a return annotation: the same AST walk shows `n.returns is not None` for all five.
    - The RED output is quoted in the plan SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check packages/iol-client/tests/test_surface_types_red.py && uv run pytest packages/iol-client/tests/test_surface_types_red.py -q; test $? -ne 0</automated>
  </verify>
  <done>Five tests exist, are lint-clean, and fail for exactly one reason: the gate they describe has not been written yet.</done>
</task>

<task type="tdd">
  <name>Task 2: GREEN — implement tools/check_surface_types.py with an injectable root</name>
  <files>tools/check_surface_types.py</files>
  <read_first>
    - `tools/check_uniform_structure.py` (166 lines, read in full) — the template. Carry over verbatim in structure: the four named docstring sections (lines 1-52), `CheckFailure` + `_fail` (71-76), `_BUILD_ARTIFACT_SUFFIX` + `_import_root` with the `.egg-info` filter (65-97), the problem-accumulation and empty-scan-is-a-failure body (100-143), and `main()` with the `::error::`-on-stderr convention (146-165).
    - `tools/check_decode_intactness.py` lines 155-205 — the typed `ExemptPackage(package, reason, resolved_by)` dataclass, if this gate ever needs an exemption record with provenance.
    - `packages/iol-client/tests/test_surface_types_red.py` (written in Task 1) — the API contract this task must satisfy exactly.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-RESEARCH.md` § "Gate Simulation Results" and § "Code Examples" — the verified `__all__`-to-definition-site resolution, the `__all__` AST reader, and the exemption predicate matching the measured taxonomy.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md` § "`tools/check_surface_types.py`" — the docstring skeleton, the four conventions to carry over, and the explicit list of adaptations required relative to the analog.
    - One real `__init__.py` per shape: `packages/matriz-client/src/matriz_client/__init__.py` (imports from 8 distinct submodules and has **two separate** `ImportFrom` blocks from `.client` — this is why `ast.walk` is required and a first-match-wins shortcut is wrong) and `packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py` (the smallest shape).
  </read_first>
  <behavior>
    Implementation must satisfy every assertion Task 1 wrote. Behaviour in terms of inputs and
    outputs:

    - Real tree in, success line out: at least 6 packages, at least 300 definitions scanned, at
      least 20 exemptions, zero violations, exit code 0.
    - Synthetic tree with a module-level exported function returning an untyped mapping in,
      `CheckFailure` naming that symbol out.
    - Synthetic tree with an exported class whose method returns an untyped mapping in,
      `CheckFailure` naming `Class.method` out.
    - Synthetic tree whose only untyped-mapping returns are `to_dict`, a dunder and an
      underscore-prefixed member in, zero violations out, with each exemption attributed to a
      named reason.
    - Empty `packages/`, missing `src/`, or absent module-level `__all__` in, `CheckFailure` out —
      never a green.
  </behavior>
  <action>
    Create `tools/check_surface_types.py`, stdlib-only (`ast`, `sys`, `pathlib`, `dataclasses`).
    Never import a package module: `import <pkg>` triggers `load_dotenv()` and client
    construction at import time, which both existing gates forbid by name in their docstrings.

    Module docstring — reuse `check_uniform_structure.py`'s four named sections, adapted, and add
    two more:

    - `WHY THIS IS A ``tools/`` SCRIPT IN THE ``lint`` JOB` — the two rejected alternatives
      (a per-package matrix leg cannot state anything cross-package; `verification/` has never
      executed in CI because the `test` job passes an explicit `packages/${{ matrix.package }}`
      path that overrides `testpaths`). **Record D-05 here**: `ROADMAP.md:25` says "job de CI
      nuevo", Phase 31's locked D-12 fixes the "step en `lint`" pattern, and the D-lock wins over
      the roadmap-summary prose. Say so explicitly so a future reader does not read it as an
      unresolved contradiction.
    - `STDLIB-ONLY, ON PURPOSE` — `uv lock --check` is the first `lint` step; a third-party helper
      would move `uv.lock` to buy nothing.
    - `THE ROSTER COMES FROM DISK` — no hardcoded package names; a seventh package is scanned
      automatically rather than exempted by omission.
    - `THE ROOT IS INJECTABLE (D-04)` — `REPO_ROOT` is a **default argument value only**; the
      check body threads a `root: Path` parameter. This is the seam the RED fixture needs, and
      it is the one deliberate departure from the two existing gates, neither of which has a test
      precisely because it lacks this.
    - `WHAT IS EXEMPT, AND WHAT DT-06 CLAUSE IS SUBSUMED` — dunders, underscore-prefixed members
      (this subsumes every `_request`), and `to_dict()` serialize-out. Record the OQ-2 resolution:
      the gate implements the `Any`-only rule from criterion 1; DT-06's clause about `_request`
      returning `httpx.Response` is dead letter under that rule (no `_request` reachable from any
      `__all__` returns `Any`, and every module-level one is underscore-prefixed and absent from
      every `__all__`), so it is subsumed by the underscore rule rather than forgotten.

    Structure:

    `REPO_ROOT = Path(__file__).resolve().parent.parent` as the default only.
    `_BUILD_ARTIFACT_SUFFIX = ".egg-info"` and `_import_root(package_dir)` copied verbatim from
    the analog — build artifacts exist under `packages/*/src/` today and without this filter the
    lookup finds two candidates and reports every package unresolvable.
    `CheckFailure` and `_fail` copied verbatim.

    `_all_names(tree)` — find the module-level `__all__` binding (`ast.Assign` or `ast.AnnAssign`
    whose target is a `Name` with id `__all__`); require a list or tuple literal of string
    constants; a missing binding or a non-literal value is a `CheckFailure`, never a skip.

    `_definition_sites(init_tree, import_name)` — walk the whole `__init__.py` with `ast.walk`
    (never first-match-wins) collecting every `ast.ImportFrom` whose `module` starts with
    `f"{import_name}."`, mapping each alias's bound name (`asname` when present, else `name`) to
    the submodule after the first dot. Additionally register names defined directly in
    `__init__.py` itself — a `FunctionDef`, `AsyncFunctionDef`, `ClassDef`, or `Assign`/`AnnAssign`
    target at module level — as resolving to `__init__` itself. That covers `__version__`-style
    locally-bound exports. Any `__all__` name still unresolved after both passes is a **problem**;
    if the real tree produces one, do not add a skip — widen the resolution rule and record the
    widening in the docstring with a stated reason.

    `_is_exempt(name)` — returns the reason string or `None`: `dunder` when the name starts and
    ends with a double underscore; `private-helper` when it starts with a single underscore;
    `serialize-out` when it is exactly `to_dict`; otherwise `None`. Attribute the exemption by
    the **simple** member name, not the qualified `Class.method` form.

    `_annotation_mentions_any(node)` — walk the return-annotation subtree and report whether any
    `ast.Name` with id `Any` or `ast.Attribute` with attr `Any` occurs. This is what catches
    the bare form, the mapping-of-Any form, the list-of-mapping-of-Any form and the
    optional-Any form with one predicate instead of a string match on the unparsed annotation.
    A definition with no return annotation at all is a violation too — an unannotated exported
    return is exactly the untyped surface the ratchet exists to prevent — unless it is exempt.

    Scanning: for each package directory under `root / "packages"` (sorted, directories only),
    resolve the import root, parse its `__init__.py`, read `__all__`, resolve every name to a
    submodule, then parse each referenced submodule file once and collect candidates — module-level
    `FunctionDef`/`AsyncFunctionDef` whose name is in the `__all__` set, and for each `ClassDef`
    whose name is in the `__all__` set, its direct-child `FunctionDef`/`AsyncFunctionDef` members
    recorded as `ClassName.member`. Names resolving to a module-level assignment (matriz's
    `Literal` aliases in `types.py`, for instance) are resolved-but-contribute-no-definitions;
    that is correct, not a problem. An `ast.parse` failure on any file is a `CheckFailure`; never
    `eval` or `exec` anything.

    Counters and anti-vacuity: accumulate `packages`, `all_names`, `definitions`, `exempted`,
    `exempted_by_reason` and `violations`. Raise `CheckFailure` when the package list is empty,
    when zero `__all__` names resolved across the whole scan, or when zero definitions were
    scanned. Emit problems as two-space-indented lines joined into one message, matching the
    analog's format.

    `scan_surface_types(root)` returns the frozen `ScanResult`. `check_surface_types(root=REPO_ROOT)`
    calls it, raises `CheckFailure` listing every violation when `violations` is non-empty, and
    otherwise returns a success line reporting all five counts. `main()` follows the analog: a
    one-element `checks` tuple, success printed to stdout, failures printed to stderr prefixed
    `::error::Phase 32 GATE-TYP-01 surface types -- `, a trailing `::error::` summary line, and
    `raise SystemExit(main())` under the `__main__` guard.

    Now run the Task 1 test file. All five tests must pass without editing a single assertion. If
    a test fails, the gate is wrong — fix the gate. Editing the test to match the implementation
    is the exact move this plan's prohibitions forbid. Commit as
    `feat(32-02): implement the stdlib-AST surface-types gate with an injectable root`.
  </action>
  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_surface_types_red.py -q` reports 5 passed, 0 failed.
    - `git diff HEAD~1 -- packages/iol-client/tests/test_surface_types_red.py` is empty — the tests were not edited to fit the implementation.
    - `uv run python tools/check_surface_types.py` exits 0 and its stdout line reports at least 6 packages, at least 300 definitions and zero violations.
    - `uv run mypy tools/check_surface_types.py` exits 0 under the repo's strict settings.
    - `uv run ruff check tools/check_surface_types.py && uv run ruff format --check tools/check_surface_types.py` exit 0.
    - The gate imports no package module: `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tools/check_surface_types.py').read_text()); mods=[a.name.split('.')[0] for n in ast.walk(t) if isinstance(n, ast.Import) for a in n.names]+[ (n.module or '').split('.')[0] for n in ast.walk(t) if isinstance(n, ast.ImportFrom)]; assert set(mods) <= {'ast','sys','pathlib','dataclasses','__future__',''}, mods"` exits 0.
    - The gate contains no hardcoded package roster: `uv run python -c "import pathlib; s=pathlib.Path('tools/check_surface_types.py').read_text(); body='\n'.join(l for l in s.splitlines() if not l.lstrip().startswith('#')); assert 'higyrus_client' not in body and 'matriz_client' not in body, 'roster leaked into code'"` exits 0.
    - Both existing gates stay green: `uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_surface_types_red.py -q && uv run python tools/check_surface_types.py && uv run mypy tools/check_surface_types.py && uv run ruff check tools/check_surface_types.py</automated>
  </verify>
  <done>The gate exists, is stdlib-only, is green on the real tree, is RED on four distinct broken trees, and typechecks under strict mypy.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate into the existing lint job as a blocking CI step</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` lines 22-60 — the `lint` job. Note the shape of the two sibling gates: `decode-intactness` at **51-55** and `uniform-structure` at **56-60**, each with the same three-line rationale comment and each invoked through `uv run python`. The new step appends immediately after line **60**.
    - `.github/workflows/ci.yml` lines 1-13 — the `paths-ignore` trigger. Understanding this is load-bearing: docs-only commits do not fire CI, which is how the red typecheck job stayed invisible for a week.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-PATTERNS.md` § "`.github/workflows/ci.yml`" — the verbatim comment block to copy and the exact new step text.
    - `.planning/phases/32-gates-de-homogeneidad-d-16/32-CONTEXT.md` D-05 — the locked decision that this is a step in `lint`, not a new job, and the reason the roadmap prose disagrees.
  </read_first>
  <action>
    Append one step to the end of the `lint` job's `steps:` list in `.github/workflows/ci.yml`,
    immediately after the `uniform-structure` step that currently ends at line 60. Do not create a
    new job and do not rename the existing one.

    Name the step `surface-types (Phase 32 GATE-TYP-01 — cero Any/dict[str, Any] en la superficie
    exportada)`. Carry the same three-line rationale comment the two sibling gates carry verbatim
    (cross-package by nature so it does not belong in the per-package `test` job; and it cannot
    live under `verification/` because the `test` job passes an explicit path that overrides
    `testpaths`, so that directory has never executed in CI). Add a fourth comment line recording
    D-05: step in `lint`, not a new job, because Phase 31's D-12 supersedes the "job de CI nuevo"
    wording of `ROADMAP.md:25`.

    The step's `run:` value is `uv run python tools/check_surface_types.py` — `uv run python`, never
    a bare `python`. All five `_decode.py` copies contain PEP 695 generic syntax which is a
    `SyntaxError` on Python 3.11 and earlier; a bare interpreter would crash the gate. Lines 55 and
    60 already establish the `uv run python` form.

    The step declares exactly two keys, `name` and `run`. It must carry **no** failure-suppression
    key — specifically no `continue-on-error`, no `if: always()` guard that would let the job
    succeed past a failure. A gate that cannot fail the job is the phase's own primary threat.

    <!-- planner-discipline-allow: continue-on-error -->

    Keeping this as a step rather than a job also means no branch-protection required-check name
    changes: adding a step does not rename the job. Note this in the SUMMARY, since it is one of
    the arguments for D-05 and it closes research assumption A2.

    Because `paths-ignore` excludes `**.md`, a commit touching only this plan's markdown would not
    fire CI. This task's commit touches `ci.yml`, so it will.
  </action>
  <acceptance_criteria>
    - `grep -c 'uv run python tools/check_surface_types.py' .github/workflows/ci.yml` equals 1.
    - The step lives in the `lint` job, not a new one: `uv run python -c "import pathlib; ls=pathlib.Path('.github/workflows/ci.yml').read_text().splitlines(); i=[n for n,l in enumerate(ls) if 'check_surface_types' in l][0]; j=[n for n,l in enumerate(ls) if l.strip().startswith('lint:')][0]; k=min([n for n,l in enumerate(ls) if l.strip().startswith('pre-commit:')]); assert j < i < k, (j,i,k)"` exits 0.
    - The job count is unchanged at four: `grep -cE '^  (lint|pre-commit|typecheck|test):' .github/workflows/ci.yml` equals 4.
    - No failure-suppression key was introduced: `grep -v '^\s*#' .github/workflows/ci.yml | grep -c 'continue-on-error'` equals 0.
    - The gate still passes when invoked exactly as the step invokes it: `uv run python tools/check_surface_types.py` exits 0.
    - The whole `lint` job still passes locally: `uv lock --check && uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/check_surface_types.py && grep -c 'uv run python tools/check_surface_types.py' .github/workflows/ci.yml</automated>
  </verify>
  <done>The gate runs in real CI as a blocking step of the existing lint job; the job roster is unchanged and no branch-protection check name moved.</done>
</task>

</tasks>

## Artifacts this phase produces

**New files**
- `tools/check_surface_types.py` (**this plan**)
- `packages/iol-client/tests/test_surface_types_red.py` (**this plan**)
- `tools/surface_parity.py` (Plan 32-04)
- `packages/market-data-client/tests/test_surface_parity.py` (Plan 32-04)
- `packages/{ambito-financiero,iol,higyrus,matriz,wallets}-client/tests/test_surface_parity.py` (Plan 32-06)
- `packages/market-data-client/tests/test_core_boundary_red.py` (Plan 32-05)

**New symbols in `tools/check_surface_types.py`** (this plan) — `REPO_ROOT`,
`_BUILD_ARTIFACT_SUFFIX`, `CheckFailure`, `_fail`, `_import_root`, `_all_names`,
`_definition_sites`, `_is_exempt`, `_annotation_mentions_any`, `ScanResult`,
`scan_surface_types`, `check_surface_types`, `main`

**New symbols in `packages/iol-client/tests/test_surface_types_red.py`** (this plan) —
`test_gate_is_green_on_the_real_tree`, `test_gate_fails_on_an_injected_regression`,
`test_regression_inside_an_exported_class_is_caught`,
`test_exempt_members_do_not_trip_the_gate`,
`test_empty_and_unresolvable_trees_are_failures_not_greens`, plus one module-level
tree-building helper

**New symbols in `tools/surface_parity.py`** (Plan 32-04) — `MODULE_LOWER_BOUNDS`,
`ParityReport`, `public_names`, `normalized_hints`, `module_parity_report`,
`class_parity_report`, `assert_module_parity`, `assert_class_parity`,
`assert_module_lower_bound`

**New CI step** (this plan) — `surface-types (Phase 32 GATE-TYP-01 …)` in the `lint` job of
`.github/workflows/ci.yml`

**New public API** (Plan 32-04) — keyword-only `http_client: httpx.AsyncClient | None = None`
on `market_data_client.aio.configure`, plus module-scope `import warnings` in that file

## Assumptions (flagged — unverified)

- **Interruption / parallelism safety of the phase's new gates is asserted by construction, not
  verified.** CI runs one package × one Python version per matrix cell with no shared mutable
  state across legs, and this plan's gate is strictly read-only — `scan_surface_types` opens
  files for reading and writes nothing. Two concurrent invocations would therefore be
  independent, and an interrupted invocation leaves no partial state. Nobody has exercised
  either case. Recorded as an open assumption, not a verified property.
- **A2 (RESEARCH.md):** GitHub branch protection does not name a required status check that a new
  `lint` step would alter. Not inspected (needs repo admin API). Risk is low because adding a
  step does not rename the job; Task 3 records this in the SUMMARY.
- **C-6 / OQ-2:** DT-06's `_request`/`httpx.Response` exemption is treated as subsumed by the
  underscore rule rather than as a second gate rule. This is a deliberate reading of criterion 1's
  literal `Any`-only wording, recorded in the gate docstring. If the developer intended a second
  rule banning raw transport types on the exported surface, that is a scope addition, not a bug
  fix.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repository source | The gate's only input is repository source it already trusts; it parses, never executes |
| gate process → per-package `.env` files | Must never be crossed. The gate's scan scope is `packages/*/src/**/*.py`; it never reads `.env`, and it never imports a package (which would run `load_dotenv()`) |
| test process → filesystem outside `tmp_path` | The RED fixture writes only inside pytest-managed `tmp_path`; it creates nothing under `packages/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | `tools/check_surface_types.py` | mitigate | AST/text reads only; a docstring invariant states the gate never imports a package module, and Task 2's acceptance criterion asserts the import set is a subset of `{ast, sys, pathlib, dataclasses}` |
| T-32-02 | Information Disclosure | Gate failure annotations rendered on the PR | mitigate | Failure messages embed only file paths relative to the root and symbol names drawn from `packages/*/src`; no file content outside that scope is read, so no credential value can reach a `::error::` line |
| T-32-03 | Tampering | Untrusted source parsing | mitigate | `ast.parse` only — never `eval`/`exec`. An unparseable file is a `CheckFailure`, never a skip (ASVS V5) |
| T-32-04 | Repudiation | A weakened gate passing vacuously | mitigate | Four independent RED cases in Task 1 (module-level regression, class-method regression, empty scan, unresolvable root) plus floor assertions on the real-tree scan counts. This is the phase's own primary threat — see `15-REVIEW.md` WR-01/WR-02 |
| T-32-05 | Tampering | CI wiring made non-blocking | mitigate | Task 3 acceptance criterion asserts no failure-suppression key exists in `ci.yml`, and that the step's job placement is inside `lint` |
| T-32-06 | Denial of Service | Gate walks an unbounded tree | accept | Scope is `packages/*/src` in a six-package monorepo; the research simulation scanned 319 definitions in well under a second. No untrusted input controls the tree size |
| T-32-SC | Tampering | package-manager installs | accept | Zero external packages installed by this phase; the gate is stdlib-only by design (D-12) precisely so `uv.lock` does not move. `uv lock --check` is the first `lint` step and fails on any lockfile movement (RESEARCH.md § Package Legitimacy Audit) |
</threat_model>

<verification>
- `uv run pytest packages/iol-client/tests/test_surface_types_red.py -q` — 5 passed
- `uv run python tools/check_surface_types.py` — exit 0, counts line reports ≥ 6 packages / ≥ 300 definitions / 0 violations
- `uv run mypy tools/check_surface_types.py` — exit 0
- `uv run ruff check . && uv run ruff format --check .` — exit 0
- `grep -c 'uv run python tools/check_surface_types.py' .github/workflows/ci.yml` — 1
- The full `lint` job reproduced locally (all seven steps) — exit 0
- The Wave 0 baseline is unbroken: `uv run pytest packages -q` still ≥ 1682 passing
</verification>

<success_criteria>
Criterion 1 and criterion 2 of Phase 32 are satisfied by one shippable slice: a stdlib-AST-only
gate that walks `__all__` including exported class methods, fails on `Any` in an exported return
with the DT-06 exemptions, runs in a real CI job, and is proven non-vacuous by an automated test
that runs in the 6×2 matrix rather than by a manual demonstration.
</success_criteria>

<output>
Create `.planning/phases/32-gates-de-homogeneidad-d-16/32-02-SUMMARY.md` when done.
</output>
