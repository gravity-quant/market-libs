---
phase: 06-compat-safety-net-client-class-skeleton
plan: 03
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/client.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
  - packages/ambito-financiero-client/tests/conftest.py
  - packages/ambito-financiero-client/tests/test_client_class.py
  - packages/ambito-financiero-client/tests/test_fixture_reaches_production.py
  - verification/snapshots/ambito-financiero-client-surface.txt
autonomous: true
requirements: [REFAC-02]

user_setup: []

must_haves:
  truths:
    - "ambito_financiero_client.Client (sync) and ambito_financiero_client.AsyncClient (async) exist with __enter__/__exit__/close() and __aenter__/__aexit__/aclose() respectively."
    - "Per-instance state lives in _ClientState; the legacy module-level globals (_base_url, _user_agent, _client) are removed from module namespace and ONLY accessible via the PEP 562 read-only shim that forwards _client → _get_default()._state.http_client."
    - "Top-level ambito_financiero_client.get_dollar_banco_nacion(...) and ambito_financiero_client.configure(base_url=..., user_agent=...) keep their v1.0 signatures and behavior; existing 277 baseline tests for ambito still pass."
    - "Client.__repr__ never leaks user_agent if it contains an identifier; for ambito there is no token, so the redaction reduces to base_url and a literal `client_open=True/False`."
    - "Client.__reduce__() and Client.__deepcopy__() raise TypeError per D-23."
    - "AsyncClient.__slots__ = ('_state',) — NO _client_lock attribute. Ambito has no auth and no token refresh race, so the asyncio.Lock pattern used by iol/higyrus/matriz is unnecessary; AsyncClient creates the httpx.AsyncClient on first call without locking (resolves checker B7)."
    - "aio.py imports the shared _raise_for_response helper from ambito_financiero_client.client — does NOT duplicate the helper (resolves checker B8). Follows the same import pattern ARCHITECTURE.md confirms for aio.py importing shared types like InstrumentType from client.py."
    - "verification/snapshots/ambito-financiero-client-surface.txt is regenerated to ADD `Client`, `AsyncClient` entries; no baseline entries are removed (D-06)."
    - "packages/ambito-financiero-client/tests/test_fixture_reaches_production.py ambito guards already use configure(base_url=...) (no token); re-run them in this plan to confirm they pass against the refactored client."
  artifacts:
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_state.py"
      provides: "@dataclass(slots=True) _ClientState (mutable; no frozen)"
      min_lines: 25
    - path: "packages/ambito-financiero-client/tests/test_client_class.py"
      provides: "Client/AsyncClient skeleton tests: __enter__/__exit__, close idempotent, __repr__ redaction, __reduce__/__deepcopy__ raise, configure replaces default"
      min_lines: 60
  key_links:
    - from: "ambito_financiero_client.get_dollar_banco_nacion"
      to: "_get_default().get_dollar_banco_nacion"
      via: "Top-level shim delegating to lazy default Client"
      pattern: "_get_default\\(\\)\\.get_dollar_banco_nacion"
    - from: "ambito_financiero_client.client._client (legacy read)"
      to: "_get_default()._state.http_client"
      via: "PEP 562 __getattr__ shim"
      pattern: "__getattr__"
    - from: "ambito_financiero_client.aio._raise_for_response (usage)"
      to: "ambito_financiero_client.client._raise_for_response (definition)"
      via: "Direct import — no duplication"
      pattern: "from ambito_financiero_client.client import _raise_for_response"
---

<objective>
Land the FIRST per-package skeleton (ambito-financiero — smallest blast radius per v1.0 lesson + CONTEXT.md serial-order decision). One atomic plan that delivers: `_state.py` + `Client` class + `AsyncClient` class + PEP 562 read-only shim + removal of legacy module-level globals + conftest migration + new `test_client_class.py` + snapshot regeneration + re-running the ambito per-package guard from `packages/ambito-financiero-client/tests/test_fixture_reaches_production.py`.

Purpose: Ambito has no auth, no token lock, no `_token_ts`/`_token_expires_at`, no `_refresh_token`. It's the cleanest stress test of the Client class skeleton + PEP 562 shim pattern. If Plans 04/05/06 explode (iol/higyrus/matriz), Plans 01/02/03 stay shippable on their own.

**Divergence from iol/higyrus/matriz (resolves checker B7):** Ambito has no token refresh race condition because it has no auth. The asyncio.Lock pattern from PATTERNS.md Section 7 (used by iol/higyrus/matriz to serialize token refresh) is unnecessary here. Ambito's `AsyncClient.__slots__ = ("_state",)` and `_ensure_http_client` creates the `httpx.AsyncClient` on first call without locking. Documenting this explicitly so future readers understand why ambito's slots tuple differs from the other three.

**Shared helper import policy (resolves checker B8):** `aio.py` imports `_raise_for_response` from `ambito_financiero_client.client` — does NOT duplicate. Justification: ARCHITECTURE.md confirms `aio.py` already imports shared types (e.g., `InstrumentType` for iol) from `client.py` without circular issues; `_raise_for_response` is stateless and follows the same pattern.

Output:
- 1 NEW source module: `_state.py`
- 4 MODIFIED source modules: `client.py`, `aio.py`, `__init__.py`, (+ 1 NEW test file)
- 1 MODIFIED conftest (drop `monkeypatch` parameter; nothing for ambito to monkeypatch since no token)
- 1 NEW test file: `tests/test_client_class.py`
- 1 REGENERATED snapshot: `verification/snapshots/ambito-financiero-client-surface.txt`
- 1 RE-RUN of `packages/ambito-financiero-client/tests/test_fixture_reaches_production.py` (no edits expected — ambito guard already uses `configure(base_url=...)`)

Per CONTEXT.md D-13 / D-14 / D-15 / D-17 / D-18 / D-19 / D-23. Per RESEARCH.md Open Q #5 resolution: `configure()` carries forward unset fields from the prior default Client's state (preserves v1.0 semantics).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
@packages/ambito-financiero-client/tests/conftest.py
@packages/ambito-financiero-client/tests/test_client.py
@packages/ambito-financiero-client/tests/test_async_client.py
@packages/ambito-financiero-client/tests/test_fixture_reaches_production.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _state.py + Client + sync shim + conftest migration + test_client_class.py</name>
  <files>
    packages/ambito-financiero-client/src/ambito_financiero_client/_state.py,
    packages/ambito-financiero-client/src/ambito_financiero_client/client.py,
    packages/ambito-financiero-client/tests/conftest.py,
    packages/ambito-financiero-client/tests/test_client_class.py
  </files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (89 LOC — current top-level functions: `configure`, `get_dollar_banco_nacion`, etc.; constants `_DEFAULT_USER_AGENT`, `_REQUEST_TIMEOUT`; globals `_base_url`, `_user_agent`, `_client`)
    - packages/ambito-financiero-client/tests/conftest.py (current autouse fixtures — already minimal, no monkeypatch needed)
    - packages/ambito-financiero-client/tests/test_client.py (canonical pytest-httpx patterns for ambito)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 5 _ClientState shape; Section 6 Client class skeleton; Section 8 PEP 562 shim for ambito — only `_client` forwarded)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md (Pattern 1, Pattern 2, Pattern 3; Open Q #5 — carry-forward semantics)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-13 ambito kwargs: `(*, base_url=None)`; D-19 load_dotenv at client.py top only)
  </read_first>
  <behavior>
    - `from ambito_financiero_client._state import _ClientState` works; the dataclass has fields `base_url: str`, `user_agent: str`, `http_client: httpx.Client | httpx.AsyncClient | None = None`, NOT frozen, `slots=True`. Defaults come from `field(default_factory=_env_base_url)` / `field(default_factory=_env_user_agent)` so env vars set AFTER import are honored. NO `token_lock` field — ambito has no auth.
    - `ambito_financiero_client.Client` class exists with `__slots__ = ("_state",)`, `__init__(*, base_url: str | None = None, user_agent: str | None = None)`, `__enter__ -> Self`, `__exit__`, `close()` (idempotent — calling twice does not raise), `__repr__` redacted, `__reduce__` raises `TypeError`, `__deepcopy__` raises `TypeError`, and instance methods mirroring every existing top-level function (`get_dollar_banco_nacion`, etc. — read `client.py` to enumerate).
    - `ambito_financiero_client.configure(base_url=..., user_agent=...)` per RESEARCH.md Open Q #5 carry-forward semantics: if a kwarg is `None`, preserve the prior `_default_client._state.<field>` value; if non-None, override. Token-like reset semantics N/A for ambito.
    - `ambito_financiero_client.get_dollar_banco_nacion(...)` (and every other top-level function) delegates to `_get_default().get_dollar_banco_nacion(...)`.
    - PEP 562 shim: `ambito_financiero_client.client._client` returns `_get_default()._state.http_client`; reads of removed names (`_base_url`, `_user_agent`) raise `AttributeError`.
    - 277 baseline tests still pass after conftest migration.
    - `Client(base_url="X").__repr__()` does NOT contain `"X"` if X looks like a secret (defensive — ambito has no secrets, but the pattern is uniform); actually contains `base_url='X'` since ambito has no credentials. The `__repr__` includes the literal text `client_open=False` when `http_client is None`, `client_open=True` otherwise.
    - `Client.close()` called twice in a row does not raise (idempotency for T-06-04 mitigation).
    - `with Client() as c: pass` calls `close()` on exit; second `close()` is no-op.
    - `pickle.dumps(Client())` raises `TypeError`. `copy.deepcopy(Client())` raises `TypeError`.
    - `configure(base_url="X")` followed by `_get_default()._state.base_url == "X"`. A subsequent `configure(user_agent="Y")` preserves `base_url="X"` (carry-forward) and updates `user_agent="Y"`.
    - An explicit `c = Client(base_url="Z")` is NOT affected by a later top-level `configure(base_url="W")` (RESEARCH.md Pitfall #2; CONTEXT.md D-14).
  </behavior>
  <action>
    Create `packages/ambito-financiero-client/src/ambito_financiero_client/_state.py` per PATTERNS.md Section 5 + RESEARCH.md Pattern 2. Module starts with `from __future__ import annotations`. Imports: `os`, `dataclasses.dataclass`, `dataclasses.field`, `httpx`. Define module-private helpers `_env_base_url()` reading `os.getenv("AMBITO_BASE_URL", "https://mercados.ambito.com").rstrip("/")` and `_env_user_agent()` returning the existing `_DEFAULT_USER_AGENT` constant (read it from the current `client.py` lines 36-39 and move it to `_state.py`). Define `@dataclass(slots=True) class _ClientState` with fields `base_url: str = field(default_factory=_env_base_url)`, `user_agent: str = field(default_factory=_env_user_agent)`, `http_client: httpx.Client | httpx.AsyncClient | None = None`. Class docstring documents NOT-frozen rationale (mutable token in other packages; uniform for cross-pkg consistency) AND explicitly notes "Ambito has no token_lock — no auth, no refresh race; AsyncClient lazy-creates http_client without locking."

    Restructure `packages/ambito-financiero-client/src/ambito_financiero_client/client.py`:
    1. Keep `from __future__ import annotations`, `import os`, `import httpx`, `load_dotenv()` at module top (D-19 preserves this).
    2. Import `from ambito_financiero_client._state import _ClientState`.
    3. Move `_REQUEST_TIMEOUT = 30.0` and `_DEFAULT_USER_AGENT` constant references into `_state.py` (do this in step 1 above) — but the original `client.py` keeps `_REQUEST_TIMEOUT` as a constant used by `httpx.Client(timeout=_REQUEST_TIMEOUT)` inside `Client.__init__` (or factor it).
    4. Define `class Client` with `__slots__ = ("_state",)`. `__init__(self, *, base_url: str | None = None, user_agent: str | None = None) -> None`: `self._state = _ClientState()`; if `base_url is not None: self._state.base_url = base_url.rstrip("/")`; if `user_agent is not None: self._state.user_agent = user_agent`. `__enter__(self) -> Self: return self`. `__exit__(self, exc_type, exc, tb) -> None: self.close()`. `close(self) -> None`: if `self._state.http_client is not None: self._state.http_client.close(); self._state.http_client = None` (idempotent because of the None guard). `__repr__(self) -> str`: returns f-string `f"AmbitoFinancieroClient(base_url={self._state.base_url!r}, user_agent={self._state.user_agent!r}, client_open={self._state.http_client is not None})"`. `__reduce__(self)` raises `TypeError("AmbitoFinancieroClient is not picklable; use multiprocessing's fork start method or recreate via ambito_financiero_client.Client(...)")`. `__deepcopy__(self, memo)` raises `TypeError("AmbitoFinancieroClient is not deepcopy-safe (httpx.Client owns TCP pool + SSL context)")`. Methods: `_ensure_http_client(self) -> httpx.Client` — lazy-create `self._state.http_client = httpx.Client(timeout=_REQUEST_TIMEOUT, headers={"User-Agent": self._state.user_agent})`. `_request(self, method, path, *, params=None) -> httpx.Response` migrated verbatim from the current top-level `_request`, replacing `_client` with `self._ensure_http_client()` and `_base_url` with `self._state.base_url`. Each public method (`get_dollar_banco_nacion`, plus every other top-level function in current client.py — enumerate by reading the file) becomes an instance method that calls `self._request(...)`.
    5. Define module-level `_raise_for_response(resp: httpx.Response) -> None` helper (preserve existing logic verbatim; stateless). This helper is SHARED with `aio.py` per the B8 lock-in: `aio.py` will import this symbol directly.
    6. Module-level `_default_client: Client | None = None` and `def _get_default() -> Client: global _default_client; if _default_client is None: _default_client = Client(); return _default_client`.
    7. Top-level `def configure(*, base_url: str | None = None, user_agent: str | None = None) -> None`: implements RESEARCH.md Open Q #5 carry-forward semantics. If `_default_client is not None`, read prior state into local vars; build `new = Client(base_url=base_url if base_url is not None else prior_base_url, user_agent=user_agent if user_agent is not None else prior_user_agent)`; set `_default_client = new`. Otherwise build fresh `Client(base_url=base_url, user_agent=user_agent)` and assign. Docstring documents D-14 + carry-forward.
    8. Top-level delegators: `def get_dollar_banco_nacion(*args, **kwargs): return _get_default().get_dollar_banco_nacion(*args, **kwargs)` for EACH original top-level function. Keep the original signatures verbatim (read current file).
    9. PEP 562 shim at module bottom per PATTERNS.md Section 8: `_FORWARDED_HTTP_CLIENT = "_client"`. `def __getattr__(name: str) -> Any:` if `name == _FORWARDED_HTTP_CLIENT: return _get_default()._state.http_client`; else `raise AttributeError(f"module {__name__!r} has no attribute {name!r}")`. NO `_token` / `_user` / `_password` / `_base_url` forwarding (D-02 — ambito has no creds). NO `DeprecationWarning` (D-03).
    10. REMOVE all the legacy module-level globals: `_base_url`, `_user_agent`, `_client`, `_DEFAULT_USER_AGENT` (now in _state.py). Keep `_REQUEST_TIMEOUT` if still referenced.

    Migrate `packages/ambito-financiero-client/tests/conftest.py`: drop the `monkeypatch` parameter from `_configure_sync` (ambito has no token to monkeypatch — the autouse fixture only calls `ambito.configure(base_url=...)`). Keep the rest unchanged.

    Create `packages/ambito-financiero-client/tests/test_client_class.py`. Module starts with `from __future__ import annotations`. Test cases:
    - `test_client_context_manager_calls_close()`: builds `Client()`, enters context, exits, asserts `c._state.http_client is None` after.
    - `test_close_is_idempotent()`: `c = Client(); c.close(); c.close()` (no raise).
    - `test_repr_contains_no_secrets_redacted()`: `Client(base_url="https://x")` repr contains `base_url='https://x'` AND contains `client_open=False`; does NOT contain raw passwords (vacuously true for ambito).
    - `test_pickle_raises()`: `import pickle; with pytest.raises(TypeError, match="not picklable"): pickle.dumps(Client())`.
    - `test_deepcopy_raises()`: `import copy; with pytest.raises(TypeError, match="not deepcopy-safe"): copy.deepcopy(Client())`.
    - `test_configure_carry_forward()`: `configure(base_url="https://a")`; assert `_get_default()._state.base_url == "https://a"`; then `configure(user_agent="UA1")`; assert `_get_default()._state.base_url == "https://a"` (carry-forward) AND `_get_default()._state.user_agent == "UA1"`.
    - `test_explicit_client_unaffected_by_top_level_configure()` (RESEARCH.md Pitfall #2): `c = Client(base_url="https://explicit")`; `configure(base_url="https://other")`; assert `c._state.base_url == "https://explicit"`.
    - `test_pep_562_shim_forwards_http_client()`: import `ambito_financiero_client.client as mod`; `c = mod._client` (PEP 562 shim fires); since no calls yet, may be None — call any get_* via top-level first to materialize `http_client`, then assert `mod._client is mod._get_default()._state.http_client`.
    - `test_pep_562_shim_raises_for_unknown()`: `with pytest.raises(AttributeError): mod._user`.
  </action>
  <verify>
    <automated>uv run pytest packages/ambito-financiero-client/tests -q</automated>
  </verify>
  <done>
    `uv run pytest packages/ambito-financiero-client/tests -q` exits 0; all existing ambito tests + new `test_client_class.py` tests pass. `Client` is exported from `ambito_financiero_client` (Task 2 wires `__init__.py`). 277 baseline (other packages unchanged) still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AsyncClient (no lock) in aio.py + __init__.py re-exports + snapshot regen + guard test re-run</name>
  <files>
    packages/ambito-financiero-client/src/ambito_financiero_client/aio.py,
    packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py,
    packages/ambito-financiero-client/tests/test_client_class.py,
    verification/snapshots/ambito-financiero-client-surface.txt,
    packages/ambito-financiero-client/tests/test_fixture_reaches_production.py
  </files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (current async surface)
    - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py (current `__all__`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py (Task 1 output — `_ClientState` reused for async, no token_lock for ambito)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (Task 1 output — Client class for reference shape; `_raise_for_response` helper to import)
    - packages/ambito-financiero-client/tests/test_async_client.py (current async tests; must keep passing)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 7 AsyncClient skeleton — note the divergence: ambito's slots tuple is just ('_state',))
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-16 caller-responsible cleanup; D-18 redact __repr__)
    - verification/snapshots/ambito-financiero-client-surface.txt (baseline from Plan 01)
    - packages/ambito-financiero-client/tests/test_fixture_reaches_production.py (Plan 02 output — ambito sync+async guards)
  </read_first>
  <behavior>
    - `ambito_financiero_client.AsyncClient` class exists with `__slots__ = ("_state",)` (NO `_client_lock` — resolves checker B7), same `__init__` kwargs as sync `Client`, `__aenter__ -> Self`, `__aexit__`, `aclose()` (idempotent), `__repr__` redacted, `__reduce__`/`__deepcopy__` raise.
    - `AsyncClient._ensure_http_client(self) -> httpx.AsyncClient`: lazy-create `self._state.http_client = httpx.AsyncClient(timeout=_REQUEST_TIMEOUT, headers={"User-Agent": self._state.user_agent})` WITHOUT locking. Justification documented in the method docstring: "Ambito has no auth and no token refresh race; the asyncio.Lock pattern from iol/higyrus/matriz is unnecessary." First call wins; if a second concurrent call enters before the first assignment completes, the worst case is a second `httpx.AsyncClient` is created and one is leaked — acceptable because ambito calls are infrequent (FX rate polling, not high-frequency trading).
    - `ambito_financiero_client.aio.configure(...)` follows same carry-forward semantics as sync.
    - `await ambito_financiero_client.aio.get_dollar_banco_nacion(...)` delegates through `_get_default().get_dollar_banco_nacion(...)`.
    - `aio.py` imports `_raise_for_response` from `ambito_financiero_client.client` (resolves checker B8 — do NOT duplicate the helper).
    - PEP 562 shim in `aio.py`: only `_client` forwarded; `_base_url`, `_user_agent` not forwarded — `AttributeError` raised for any other name (D-02 ambito row).
    - `ambito_financiero_client.__init__.py` re-exports `Client` and `AsyncClient` at top-level.
    - Snapshot file `verification/snapshots/ambito-financiero-client-surface.txt` regenerated via `python verification/regen_snapshots.py` — adds `Client`, `AsyncClient`, `close` (only if in `__all__`), `aclose` (only if in `__all__`) sorted alphabetically. Baseline entries (`configure`, `get_dollar_banco_nacion`, exception classes) PRESERVED (D-06: never remove).
    - `packages/ambito-financiero-client/tests/test_fixture_reaches_production.py` ambito guards: sync test already uses `configure(base_url=...)`, no migration needed. Async test already uses `aio.configure(base_url=...)`. Both still pass — re-run them to confirm.
    - `test_client_class.py` Task 1 tests are amended with `AsyncClient` mirror tests: `test_async_client_context_manager_calls_aclose`, `test_aclose_is_idempotent`, `test_async_repr_redacted`, etc.
  </behavior>
  <action>
    Restructure `packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` mirroring the sync structure from Task 1, with the B7/B8 divergences locked in:
    1. Module starts with `from __future__ import annotations`. Imports: `httpx`, `from typing import Self`, `from ambito_financiero_client._state import _ClientState`, `from ambito_financiero_client.client import _raise_for_response` (B8: import — do NOT duplicate; justification cited in module docstring referencing ARCHITECTURE.md's note that `aio.py` already imports shared types like `InstrumentType` from `client.py`).
    2. Define `class AsyncClient` with `__slots__ = ("_state",)` (B7: NO `_client_lock`). `__init__(self, *, base_url=None, user_agent=None) -> None`: same body as sync `Client.__init__`. `__aenter__(self) -> Self: return self`. `__aexit__(self, exc_type, exc, tb) -> None: await self.aclose()`. `aclose(self) -> None`: if `self._state.http_client is not None: assert isinstance(self._state.http_client, httpx.AsyncClient); await self._state.http_client.aclose(); self._state.http_client = None`. `__repr__`, `__reduce__`, `__deepcopy__` same as sync (substitute "AsyncClient" in name strings). `_ensure_http_client(self) -> httpx.AsyncClient`: lazy-create WITHOUT a lock — `if self._state.http_client is None: self._state.http_client = httpx.AsyncClient(timeout=_REQUEST_TIMEOUT, headers={"User-Agent": self._state.user_agent})`. Docstring on `_ensure_http_client` documents the B7 divergence verbatim: "Ambito has no auth, no token refresh race. The asyncio.Lock pattern used by iol/higyrus/matriz AsyncClient._ensure_http_client is unnecessary here — concurrent first-call would at worst leak one httpx.AsyncClient (acceptable for low-frequency FX rate polling)." `_request(self, ...)` migrated from current top-level `_request` in `aio.py`, calling `_raise_for_response(resp)` (imported from `client.py`). Each public async method migrated from current top-level functions.
    3. Module-level `_default_async_client: AsyncClient | None = None` and `def _get_default() -> AsyncClient:` lazy singleton.
    4. Top-level `configure(...)` — per project convention `configure` is sync in `aio.py` (see current code; it just sets globals synchronously). Confirm by reading current `aio.py`: if sync, keep sync with carry-forward semantics. (CONTEXT.md D-19 says load_dotenv stays in client.py only — aio.py's configure does NOT call load_dotenv.)
    5. Top-level delegators for each existing async function.
    6. PEP 562 shim: only `_client` forwarded.
    7. REMOVE legacy globals (`_base_url`, `_user_agent`, `_client`).

    Edit `packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py`: extend `__all__` to include `Client` and `AsyncClient` (alphabetical insertion); add `from ambito_financiero_client.client import Client` and `from ambito_financiero_client.aio import AsyncClient` (or equivalent re-export).

    Regenerate the ambito snapshot: `uv run python verification/regen_snapshots.py` — this rewrites `verification/snapshots/ambito-financiero-client-surface.txt` adding `AsyncClient` and `Client` entries (alphabetical). Verify D-06 invariant by hand: every PRE-refactor entry (`configure`, `get_dollar_banco_nacion`, exception classes, etc.) is STILL present in the new snapshot. If any baseline entry is gone, the `__init__.py` `__all__` was clobbered — fix.

    Append to `packages/ambito-financiero-client/tests/test_client_class.py` the AsyncClient mirror tests:
    - `async def test_async_client_context_manager_calls_aclose()`: `async with AsyncClient() as c: pass`; assert `c._state.http_client is None` after.
    - `async def test_aclose_is_idempotent()`: `c = AsyncClient(); await c.aclose(); await c.aclose()`.
    - `test_async_pickle_raises()`, `test_async_deepcopy_raises()`.
    - `async def test_async_configure_carry_forward()`: same as sync test but with `aio.configure`.
    - `async def test_async_pep_562_shim_forwards_http_client()` + `test_async_pep_562_shim_raises_for_unknown()`.
    - `async def test_async_client_has_no_client_lock_attribute()` (B7 enforcement): `c = AsyncClient(); assert "_client_lock" not in AsyncClient.__slots__`. Doc-string the test explains "Ambito diverges from iol/higyrus/matriz — no auth, no race, no lock."
    - `def test_aio_imports_raise_for_response_from_client()` (B8 enforcement): `from ambito_financiero_client.client import _raise_for_response as sync_helper`; `from ambito_financiero_client.aio import _raise_for_response as async_helper`; `assert sync_helper is async_helper` (proves the import is a real re-export, not a duplicate definition).

    Re-run `packages/ambito-financiero-client/tests/test_fixture_reaches_production.py` and confirm the 2 ambito guards (sync + async) still pass against the refactored ambito (they already used `configure(base_url=...)` so no test code changes — but verify; if a header was being read it would now go through the shim).
  </action>
  <verify>
    <automated>uv run pytest packages/ambito-financiero-client/tests verification/test_public_surface.py -q -k "ambito or public_surface"</automated>
  </verify>
  <done>
    `AsyncClient` exported from `ambito_financiero_client` top-level. `AsyncClient.__slots__ == ("_state",)` (no `_client_lock`). `aio._raise_for_response is client._raise_for_response`. Snapshot file `verification/snapshots/ambito-financiero-client-surface.txt` regenerated with `Client` and `AsyncClient` entries; all pre-refactor entries preserved (verify with `grep -c configure ...` ≥ 1 and similar for the other baseline names). All ambito tests + public surface test + ambito guards pass. Full suite `uv run pytest -q` is green (other packages still pre-refactor — they pass; 277 baseline preserved).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → `Client` instance state | Each `Client()` owns its `_state`; explicit instances must NOT be mutated by top-level `configure(...)` (RESEARCH.md Pitfall #2). |
| PEP 562 shim → legacy module reads | Read-only forwarding via `__getattr__`. Writes from tests land on the module dict; production reads `self._state.<field>` — the mismatch IS Pitfall #1, mitigated by conftest migration. |
| AsyncClient concurrent `_ensure_http_client` (B7 divergence) | Without a lock, two concurrent first-calls could each create an `httpx.AsyncClient`. One is leaked. Acceptable for ambito's low-frequency calls; documented in code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PEP 562 shim allowlist for ambito | mitigate | Shim forwards ONLY `_client` (D-02 row for ambito). Any other legacy read (`_base_url`, `_user_agent`) raises `AttributeError`. `test_pep_562_shim_raises_for_unknown` in Task 1 enforces this. |
| T-06-02 | Information Disclosure | `Client.__repr__` | mitigate | Ambito has no credentials/tokens; `__repr__` only shows `base_url`, `user_agent` (non-secret), and `client_open` boolean. Task 1 test `test_repr_contains_no_secrets_redacted` enforces the pattern. |
| T-06-03 | Tampering | conftest migration (autouse fixture) | mitigate | Ambito conftest already does not monkeypatch token (no token). Task 1 drops the `monkeypatch` parameter; no semantic change. Public-surface snapshot in Plan 01 + guard tests in Plan 02 prove no regression. |
| T-06-04 | Denial of Service | `Client.close()`/`AsyncClient.aclose()` lifecycle | mitigate | Idempotent via `if self._state.http_client is not None: ...; self._state.http_client = None` pattern. Task 1 `test_close_is_idempotent` and Task 2 `test_aclose_is_idempotent` enforce. |
| T-06-13 | Resource exhaustion | leaked `httpx.AsyncClient` from B7 lock-less _ensure_http_client | accept | Documented in `_ensure_http_client` docstring. Ambito's call frequency is low (FX rate polling). Per-process leak ceiling is 1 client per accidental race per process — bounded by process lifetime. |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed. |
</threat_model>

<verification>
- `uv run pytest packages/ambito-financiero-client/tests -q` → all ambito tests pass (existing + new `test_client_class.py`).
- `uv run pytest verification/test_public_surface.py -q -k ambito` → ambito snapshot test green; snapshot file contains both `Client` and `AsyncClient` lines plus all baseline lines.
- `uv run pytest packages/ambito-financiero-client/tests/test_fixture_reaches_production.py -q` → 2 ambito guards (sync + async) pass.
- `uv run pytest -q` → full suite green; 277 baseline + new tests; no regressions in other packages.
- `uv run ruff check packages/ambito-financiero-client/` and `uv run mypy --strict packages/ambito-financiero-client/src` → exit 0.
- B7 spot-check: `python -c "from ambito_financiero_client.aio import AsyncClient; print(AsyncClient.__slots__)"` prints `('_state',)` (no `_client_lock`).
- B8 spot-check: `python -c "from ambito_financiero_client.client import _raise_for_response as s; from ambito_financiero_client.aio import _raise_for_response as a; print(s is a)"` prints `True`.
</verification>

<success_criteria>
- REFAC-02 (ambito coverage) success criteria 3, 4 satisfied for ambito-financiero-client:
  - `Client` and `AsyncClient` exposed with `close()`/`aclose()` and `__enter__`/`__exit__` / `__aenter__`/`__aexit__`.
  - State scoped to instance via `_ClientState`.
  - Top-level API (`ambito_financiero_client.get_X(...)`, `configure(...)`) unchanged for callers.
- Ambito-specific divergences locked in: no `_client_lock` (B7); `_raise_for_response` imported, not duplicated (B8).
- Ambito baseline tests + new skeleton tests all green.
- Public-surface snapshot updated with new entries; no baseline entries removed.
- Fixture-reaches-production guards (ambito) remain green at the per-package path.
</success_criteria>

<output>
Create `.planning/phases/06-compat-safety-net-client-class-skeleton/06-03-SUMMARY.md` when done.
</output>
