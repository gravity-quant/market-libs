---
phase: 05-matriz-verification
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - .planning/verification/ambito-financiero-client-findings.md
  - .planning/verification/iol-client-findings.md
  - .planning/verification/higyrus-client-findings.md
  - .planning/verification/matriz-client-findings.md
  - .planning/verification/CYCLE-REPORT.md
autonomous: false
requirements: [DRIFT-02]
tags: [verification, cycle-closure, DRIFT-02, cross-package, expected-terminal, prod-vs-remarkets, baseline-commit]
must_haves:
  truths:
    - "Cada uno de los 4 findings files (`.planning/verification/<pkg>-findings.md` para ambito-financiero-client, iol-client, higyrus-client, matriz-client) tiene appendada AL FINAL una sección `## Cycle Closure` (D-MATZ-25) escrita DIRECTAMENTE al filesystem (NO via `append_finding` — per Assumption A3 RESEARCH L978: el helper re-serializa y dropearía la sección no-finding)"
    - "Cada sección `## Cycle Closure` contiene: (a) Cycle ID `verification-cycle-2026-Q2`, (b) fecha de cierre ISO 8601 UTC, (c) packages verified count (4), (d) tabla de conteo por status (OPEN/CONFIRMED/FIXED/EXPECTED/NO-FIX) para el paquete, (e) lista de regression tests linkados (`F-NN: <test_file>::<test_name>`) o nota `(historical findings predate the regression-link convention introduced in Phase 5)` para los paquetes Phase 2-4 donde el campo `regression:` no está poblado retroactivamente, (f) link relativo a `CYCLE-REPORT.md` (per D-MATZ-25)"
    - "`.planning/verification/CYCLE-REPORT.md` (archivo NUEVO) existe con las 4 dimensiones D-MATZ-26: (1) Stats per-package — tabla con 1 fila por paquete + 1 fila total cycle (Package | Findings Total | OPEN | CONFIRMED | FIXED | EXPECTED | NO-FIX | Regression Tests | Schemas Committed), (2) Cross-cycle — totales agregados + sección de patrones recurrentes narrativa (envelope-key indexing across IOL/HIGY/MATZ, false-pass SafeModel en HIGY/MATZ, etc.), (3) Open questions for downstream milestone — incluye prod-vs-remarkets gap D-MATZ-27 como handoff explícito + items deferred del ciclo, (4) Schemas summary — lista de paths committeados por paquete"
    - "`.planning/verification/matriz-client-findings.md` contiene el finding `EXPECTED` terminal D-MATZ-27 prod-vs-remarkets emitido por el driver de Plan 05-03 (verificable: `grep -c 'prod-vs-remarkets divergence acknowledged' matriz-client-findings.md` >= 1). Si por alguna razón el driver no lo emitió (e.g., live run abortado early), este plan lo emite via append directo (NO via append_finding — Assumption A3) como último item Detalle por hallazgo antes del `## Cycle Closure`"
    - "El driver `main_matriz.py` (Plan 05-02) ya invoca `verify_cycle_closure(pkg)` para los 4 paquetes durante el live run de Plan 05-03 — este plan SOLO interpreta los resultados emitidos en stdout + findings files; NO re-invoca el helper imperativamente (per D-MATZ-28: la invocación es responsabilidad del driver). Si el live run de 05-03 no completó la invocación (e.g., abortado), este plan ejecuta `python -c 'from verification.cycle_report import verify_cycle_closure; print(verify_cycle_closure(pkg))'` × 4 paquetes para capturar resultados PASS/FAIL y reflejarlos en CYCLE-REPORT.md"
    - "Si `verify_cycle_closure(pkg)` retorna FAIL para algún paquete (CONFIRMED/FIXED sin regression linkeado), el plan PAUSA en un checkpoint operator-classify: el operator decide si (a) agregar regression test missing (en cuyo caso Plan 05-04 se extiende con una task auto adicional), (b) downgrade del finding a NO-FIX con justification, o (c) aceptar caveat retroactive para findings históricos Phase 2-4 (caso esperado para el primer cycle closure run)"
    - "Commit final del plan: SINGLE git commit con mensaje EXACTO `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)` (mensaje incluye 'DRIFT-02 cycle closure' verbatim para que verification.cycle_report o downstream tooling lo localice via `git log --grep`). Files incluidos en el commit: 4 `<pkg>-findings.md` modificados + 1 `CYCLE-REPORT.md` nuevo + (si schemas matriz no fueron committeados en 05-03) `.planning/verification/schemas/matriz-client/` directorio completo"
    - "Después del commit, `git log -1 --pretty=%s` retorna línea que contiene `DRIFT-02 cycle closure` (sustring exacto), permitiendo localización forensic del closure commit por tooling futuro"
    - "Ningún `<pkg>-findings.md` modificado contiene credenciales (PRIMARY_USER, PRIMARY_PASSWORD, _token) ni PII real (accountIds reales, symbols valuados) — el append `## Cycle Closure` es shape-only (conteos, IDs de findings, paths de tests)"
    - "CYCLE-REPORT.md es PII-free por construcción: solo conteos, narrativa de patrones, paths de schemas/tests, IDs de findings; NO incluye valores reales del wire ni credenciales"
    - "El finding D-MATZ-27 EXPECTED terminal cita verbatim: `title='prod-vs-remarkets divergence acknowledged'`, `expected='verification limited to remarkets sandbox by safety policy (REQUIREMENTS.md Out of Scope)'`, `actual='prod (api.primary.com.ar) shape unverified; sandbox shape committed in .planning/verification/schemas/matriz-client/'`, `diff='N/A (acknowledged limitation, not detected drift)'`, `class='SHAPE'`, `surface='sync'`, `status='EXPECTED'` (per D-MATZ-27 verbatim)"
    - "Cycle ID literal `verification-cycle-2026-Q2` aparece en CYCLE-REPORT.md y en cada `## Cycle Closure` section (mismo identificador unificado para el ciclo de verificación Q2 2026)"
    - "Todas las 5 success criteria del ROADMAP Phase 5 relacionadas a closure están cumplidas tras este plan: SC#5 schema snapshot committed (verificable: `ls .planning/verification/schemas/matriz-client/` >= 1 archivo), per-package report produced (4 findings files con Cycle Closure + 1 CYCLE-REPORT.md), every CONFIRMED bug closed and TESTED (verify_cycle_closure PASS o caveat documentado en CYCLE-REPORT.md)"
  artifacts:
    - path: ".planning/verification/ambito-financiero-client-findings.md"
      provides: "Ámbito findings file con sección `## Cycle Closure` appendada (Phase 5 closure)"
      contains: "## Cycle Closure"
      contains_also: "verification-cycle-2026-Q2"
    - path: ".planning/verification/iol-client-findings.md"
      provides: "IOL findings file con sección `## Cycle Closure` appendada (Phase 5 closure)"
      contains: "## Cycle Closure"
      contains_also: "verification-cycle-2026-Q2"
    - path: ".planning/verification/higyrus-client-findings.md"
      provides: "Higyrus findings file con sección `## Cycle Closure` appendada (Phase 5 closure)"
      contains: "## Cycle Closure"
      contains_also: "verification-cycle-2026-Q2"
    - path: ".planning/verification/matriz-client-findings.md"
      provides: "Matriz findings file con sección `## Cycle Closure` appendada + EXPECTED terminal D-MATZ-27 ya presente (de Plan 05-03 driver run)"
      contains: "## Cycle Closure"
      contains_also: "prod-vs-remarkets divergence acknowledged"
      contains_also_2: "verification-cycle-2026-Q2"
    - path: ".planning/verification/CYCLE-REPORT.md"
      provides: "Consolidated cross-package closure report with 4 dimensions D-MATZ-26"
      contains: "## Stats per-package"
      contains_also: "## Cross-cycle"
      contains_also_2: "## Open questions"
      contains_also_3: "## Schemas summary"
      contains_also_4: "verification-cycle-2026-Q2"
      contains_also_5: "prod-vs-remarkets"
      min_lines: 80
  key_links:
    - from: ".planning/verification/ambito-financiero-client-findings.md::Cycle Closure"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Relative link `[CYCLE-REPORT.md](./CYCLE-REPORT.md)` from Cycle Closure section"
      pattern: "CYCLE-REPORT\\.md"
    - from: ".planning/verification/iol-client-findings.md::Cycle Closure"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Relative link `[CYCLE-REPORT.md](./CYCLE-REPORT.md)`"
      pattern: "CYCLE-REPORT\\.md"
    - from: ".planning/verification/higyrus-client-findings.md::Cycle Closure"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Relative link `[CYCLE-REPORT.md](./CYCLE-REPORT.md)`"
      pattern: "CYCLE-REPORT\\.md"
    - from: ".planning/verification/matriz-client-findings.md::Cycle Closure"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Relative link `[CYCLE-REPORT.md](./CYCLE-REPORT.md)`"
      pattern: "CYCLE-REPORT\\.md"
    - from: ".planning/verification/CYCLE-REPORT.md::Schemas summary"
      to: ".planning/verification/schemas/{ambito-financiero,iol,higyrus,matriz}-client/"
      via: "Lista enumerada de paths committeables por paquete"
      pattern: "\\.planning/verification/schemas"
    - from: ".planning/verification/CYCLE-REPORT.md::Open questions"
      to: ".planning/verification/matriz-client-findings.md::F-NN (D-MATZ-27)"
      via: "Cita el finding EXPECTED terminal prod-vs-remarkets como handoff documentado"
      pattern: "prod-vs-remarkets"
    - from: "git log commit DRIFT-02"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Single commit `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)` includes CYCLE-REPORT.md + 4 findings files"
      pattern: "DRIFT-02 cycle closure"
    - from: "canonical commit `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)`"
      to: ".planning/verification/CYCLE-REPORT.md"
      via: "Sign-off section of CYCLE-REPORT.md embeds the substring `DRIFT-02 cycle closure`; commit subject embeds the same substring + cycle ID `verification-cycle-2026-Q2`; tooling can locate the closure document FROM the commit via `git log --grep='DRIFT-02 cycle closure' --name-only` (lists `.planning/verification/CYCLE-REPORT.md`) and vice versa via `grep -l 'DRIFT-02 cycle closure' .planning/verification/CYCLE-REPORT.md` → match"
      pattern: "DRIFT-02 cycle closure.*verification-cycle-2026-Q2"
---

<objective>
**Cerrar el ciclo de verificación completo** (DRIFT-02 — el único requirement de Phase 5 que no se cumple end-to-end en plans 05-01..05-03). Este plan es la **vertical-closure slice** que convierte 4 paquetes verificados individualmente (Phases 2-5) en un **ciclo cerrado y auditable**: cada paquete recibe una sección `## Cycle Closure` con conteos + regression links + cycle ID; un `CYCLE-REPORT.md` consolida los 4 paquetes en una sola vista cross-cycle; el gap prod-vs-remarkets queda documentado como handoff explícito EXPECTED terminal D-MATZ-27 para milestone futuro; un único commit baseline con mensaje canónico `DRIFT-02 cycle closure (verification-cycle-2026-Q2)` permite que tooling forensic localice el cierre cycle-wide.

**Per D-MATZ-25, D-MATZ-26, D-MATZ-27, D-MATZ-28** del CONTEXT.md, este plan implementa exactamente:

1. **Append `## Cycle Closure` a los 4 `<pkg>-findings.md`** existentes (D-MATZ-25) — escritura DIRECTA al filesystem, NUNCA via `append_finding` (per Assumption A3 RESEARCH L978: el helper re-serializa el modelo interno y dropearía la sección no-finding agregada).

2. **Crear `.planning/verification/CYCLE-REPORT.md`** (D-MATZ-26) con las 4 dimensiones consolidadas:
   - **(1) Stats per-package** — tabla con 1 fila por paquete + 1 fila total cycle
   - **(2) Cross-cycle** — totales agregados + narrativa de patrones recurrentes
   - **(3) Open questions for downstream milestone** — incluye prod-vs-remarkets gap D-MATZ-27 + items deferred
   - **(4) Schemas summary** — lista de paths committeables por paquete

3. **Confirmar finding EXPECTED terminal D-MATZ-27** prod-vs-remarkets en matriz-client-findings.md ya emitido por el driver de Plan 05-03 — si por algún motivo falta (live run abortado), este plan lo emite via append directo manteniendo el text verbatim D-MATZ-27.

4. **Reflejar resultados de `verify_cycle_closure(pkg)` × 4** (D-MATZ-28) en CYCLE-REPORT.md. La invocación PRINCIPAL la hace el driver de Plan 05-02/03; este plan SOLO interpreta los resultados emitidos en stdout/findings y los refleja en el report consolidado. Si el live run de 05-03 no llegó a invocar el helper (e.g., abortado), este plan re-ejecuta `python -c '...verify_cycle_closure(...)'` × 4 para capturar el estado actual.

5. **Operator checkpoint sobre `verify_cycle_closure` FAIL handling** — el primer cycle closure run probablemente reporta FAIL para Phases 2-4 paquetes porque los findings históricos no tienen el campo `regression:` poblado (convención introducida en Phase 5). El operator decide cómo absorber el caveat: caveat documentado en CYCLE-REPORT.md (esperado para el primer cycle), agregar regression links retroactivamente, o downgrade de findings históricos.

6. **Commit baseline final con mensaje canónico** `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)`. Mensaje incluye `DRIFT-02 cycle closure` verbatim — tooling forensic puede localizarlo con `git log --grep='DRIFT-02 cycle closure'`. Files incluidos: 4 `<pkg>-findings.md` modificados + 1 `CYCLE-REPORT.md` nuevo + schemas matriz si no fueron committeados en 05-03.

Purpose:
- Sin este plan, DRIFT-02 NO se cumple end-to-end: los 4 findings files existen como archivos sueltos sin cierre cross-cycle; el report consolidado no existe; el gap prod-vs-remarkets queda solo como ítem en STATE.md blockers (no auditable); y no hay commit baseline marcado para forensic.
- Sin la sección `## Cycle Closure` × 4, cada findings file luce como "trabajo en curso" — visualmente no es claro qué findings fueron cerrados como parte de qué ciclo.
- Sin CYCLE-REPORT.md, no hay vista cross-package que permita responder "¿qué bugs encontramos y fijamos en este cycle?" sin abrir 4 archivos.
- Sin el commit canónico DRIFT-02, no hay anchor forensic — el milestone close depende de poder localizar el commit del closure en futuras auditorías.
- Sin la operador checkpoint sobre verify_cycle_closure FAIL handling, el primer cycle closure run podría parecer "roto" (FAIL × 3-4 paquetes históricos) cuando en realidad es esperado por la convención forward-looking del campo regression.

Output: 4 findings files con `## Cycle Closure` appendado + 1 CYCLE-REPORT.md nuevo + (si necesario) schemas matriz committeados + 1 git commit DRIFT-02 canónico. Phase 5 closed; DRIFT-02 verified end-to-end; cycle anchor commit localizable.

Plan es **`autonomous: false`** porque incluye un checkpoint operator que ratifica el contenido de CYCLE-REPORT.md (narrativa de patrones recurrentes requiere juicio humano sobre el live run de 05-03) Y decide el handling de verify_cycle_closure FAIL para findings históricos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-matriz-verification/05-CONTEXT.md
@.planning/phases/05-matriz-verification/05-RESEARCH.md
@.planning/phases/05-matriz-verification/05-PATTERNS.md
@.planning/phases/05-matriz-verification/05-VALIDATION.md
@.planning/phases/05-matriz-verification/05-01-PLAN.md
@.planning/phases/05-matriz-verification/05-02-PLAN.md
@.planning/phases/05-matriz-verification/05-03-PLAN.md
@.planning/phases/04-higyrus-verification/04-04-PLAN.md
@.planning/phases/04-higyrus-verification/04-SUMMARY.md
@.planning/verification/FINDINGS-TEMPLATE.md
@.planning/verification/ambito-financiero-client-findings.md
@.planning/verification/iol-client-findings.md
@.planning/verification/higyrus-client-findings.md
@.planning/verification/matriz-client-findings.md
@verification/findings.py
@verification/cycle_report.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Inspect existing findings files + capture verify_cycle_closure results × 4 packages (D-MATZ-28 driver-side result interpretation)</name>
  <files>
    .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json
  </files>
  <read_first>
    - .planning/verification/ambito-financiero-client-findings.md (estado completo: ART block, Index, Detalle por hallazgo; capturar count de findings por status para la tabla CYCLE-REPORT.md)
    - .planning/verification/iol-client-findings.md (idem)
    - .planning/verification/higyrus-client-findings.md (idem; verificar que F-01..F-06 wire encoding del Plan 04-04 estén en status FIXED o documented)
    - .planning/verification/matriz-client-findings.md (POST-Plan 05-03 live run: estado completo + confirmar D-MATZ-27 EXPECTED terminal presente. Si AUSENTE → Task 4.2 lo emite via append directo)
    - .planning/verification/FINDINGS-TEMPLATE.md (estructura canónica del findings file; verificar dónde aparece el campo `Status:` y el campo `Regression:` en la sección "Detalle por hallazgo" — esto rige la sintaxis del `## Cycle Closure` append)
    - verification/cycle_report.py (POST-Plan 05-01: helper `verify_cycle_closure(pkg) -> tuple[bool, list[str]]`; firma + comportamiento ya validados en Task 1.2 de Plan 05-01)
    - verification/findings.py (líneas 76-92: `FINDING_CLASSES` y `STATUS_LIFECYCLE` verbatim — para la tabla stats per-package)
    - .planning/verification/schemas/ (estructura: 4 subdirs `ambito-financiero-client/`, `iol-client/`, `higyrus-client/`, `matriz-client/` — count exacto de archivos JSON por subdir para la "Schemas summary" dimension de CYCLE-REPORT.md)
    - .planning/phases/05-matriz-verification/05-03-PLAN.md (Task 3.3 live run — verificar si fue ejecutado y completó con cycle_closure invocation; si stdout log existe en `live-run-matriz-*.log` o similar, leerlo para confirmar)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-25, D-MATZ-26, D-MATZ-28 verbatim)
  </read_first>
  <behavior>
    - Tras esta task, existe `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json` (archivo temporal del plan, gitignored o explícitamente borrado en Task 4.5) con la estructura completa de datos que Task 4.3 y 4.4 consumirán para escribir las secciones `## Cycle Closure` y `CYCLE-REPORT.md` consistentemente.
    - Para cada paquete, captura: `findings_total`, `count_by_status` (OPEN/CONFIRMED/FIXED/EXPECTED/NO-FIX), lista de regression links presentes (extraídos vía regex), `verify_cycle_closure_ok`, `verify_cycle_closure_missing`, `schemas_count`, `schemas_paths`.
    - `verify_cycle_closure(pkg)` invocado para los 4 paquetes (re-invocación si live run de 05-03 ya lo hizo es OK — idempotente). Cada resultado capturado.
    - Si para algún paquete `verify_cycle_closure` retorna `False, missing=[...]`, la lista de fids missing queda registrada en el JSON temporal. Task 4.4 (operator checkpoint) decide handling.
    - El JSON temporal sirve como state-machine para que las siguientes tasks no re-inspeccionen los archivos individualmente.
    - El finding D-MATZ-27 EXPECTED terminal: si `grep -c 'prod-vs-remarkets divergence acknowledged' .planning/verification/matriz-client-findings.md >= 1` → registrar `d_matz_27_present: true`; si 0 → registrar `d_matz_27_present: false` y Task 4.2 lo emite via append directo.
  </behavior>
  <action>
    Ejecutar un script Python ad-hoc (one-shot, no commiteable) que recolecta el state del cycle closure y lo escribe a `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json`. El archivo es temporal — Task 4.5 lo elimina antes del commit final.

    **Step 1 — Inspeccionar los 4 findings files con regex estructural:**

    Para cada paquete en `["ambito-financiero-client", "iol-client", "higyrus-client", "matriz-client"]`:
    - Leer `.planning/verification/<pkg>-findings.md`
    - Contar findings totales: regex `^### F-\d+` (count matches)
    - Contar por status: regex `\*\*Status:\*\*\s*`?(\w+)`?` agrupar por status; alternativamente parsear la tabla `## Index`
    - Extraer regression links presentes: regex flexible `(?:\*\*Regression:\*\*|Regression:|regression:)\s*([\w\-/.]+\.py::[A-Za-z_][\w]*)` — los matches son los regression test paths linkeados; los findings sin match son candidatos missing
    - Verificar presencia D-MATZ-27 (solo para matriz-client): `grep -c 'prod-vs-remarkets divergence acknowledged' matriz-client-findings.md`

    **Step 2 — Invocar `verify_cycle_closure` para los 4 paquetes:**

    Ejecutar:
    ```
    uv run python -c "
    import json
    from verification.cycle_report import verify_cycle_closure
    results = {}
    for pkg in ['ambito-financiero-client', 'iol-client', 'higyrus-client', 'matriz-client']:
        ok, missing = verify_cycle_closure(pkg)
        results[pkg] = {'ok': ok, 'missing': missing}
    print(json.dumps(results, indent=2))
    "
    ```

    Capturar el output y mergearlo al state JSON.

    **Step 3 — Inspeccionar schemas committeables:**

    Para cada paquete:
    - `ls .planning/verification/schemas/<pkg>/*.json 2>/dev/null` — contar y listar paths
    - Si el directorio no existe (e.g., ambito si solo tiene 1 schema en un layout diferente), verificar layout actual; documentar conteo real

    **Step 4 — Inspeccionar live-run-matriz-*.log si existe:**

    `ls live-run-matriz-*.log 2>/dev/null` — si existe, grep para `cycle_closure_` lines y registrar el output del PROBE; sino registrar `live_run_log_found: false`.

    **Step 5 — Escribir JSON temporal:**

    Estructura del archivo `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json`:
    ```
    {
      "cycle_id": "verification-cycle-2026-Q2",
      "closure_date": "<iso8601 UTC now>",
      "packages": {
        "ambito-financiero-client": {
          "findings_total": <N>,
          "count_by_status": {"OPEN": <n>, "CONFIRMED": <n>, "FIXED": <n>, "EXPECTED": <n>, "NO-FIX": <n>},
          "regression_links": [{"fid": "F-01", "path": "path::test_name"}, ...],
          "verify_cycle_closure": {"ok": <bool>, "missing": [<fids>]},
          "schemas_count": <N>,
          "schemas_paths": [<list>]
        },
        "iol-client": { ... },
        "higyrus-client": { ... },
        "matriz-client": { ... }
      },
      "d_matz_27_present_in_matriz": <bool>,
      "live_run_log_found": <bool>,
      "live_run_summary": "<extracted line or null>"
    }
    ```

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - NO modificar ningún findings file en esta task (solo lectura)
    - NO ejecutar live calls (verify_cycle_closure es parse estructural offline per D-MATZ-28)
    - NO commitear el `.cycle-closure-state.tmp.json` — es state intermedio del plan, gitignored o eliminado en Task 4.5
    - El archivo `.tmp.json` va en `.planning/phases/05-matriz-verification/` para ser easy-to-locate pero NO commiteado
    - Agregar `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json` a `.gitignore` raíz O confirmar que el patrón `*.tmp.json` ya está cubierto
    - El script puede ser ejecutado inline via Bash con `uv run python -c '...'` o escrito a un archivo `.py` tmp y borrado al final
    - Si el live run log no existe (operator no preservó), no es bloqueante — Task 4.4 le pregunta al operator por el summary

    Commit sugerido: NO commit — este es state intermedio, se descarta en Task 4.5
  </action>
  <verify>
    <automated>test -f .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json')); assert 'cycle_id' in data and data['cycle_id'] == 'verification-cycle-2026-Q2'; assert set(data['packages'].keys()) == {'ambito-financiero-client', 'iol-client', 'higyrus-client', 'matriz-client'}; print('state JSON valid')"</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json')); print('per-pkg findings totals:', {k: v['findings_total'] for k, v in data['packages'].items()})"</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json')); print('verify_cycle_closure results:', {k: v['verify_cycle_closure'] for k, v in data['packages'].items()})"</automated>
    <automated>uv run python -c "import json; data = json.load(open('.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json')); print('D-MATZ-27 present:', data['d_matz_27_present_in_matriz'])"</automated>
    <automated>ls .planning/verification/schemas/</automated>
  </verify>
  <acceptance_criteria>
    - File: `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json` existe
    - JSON shape: contiene top-level keys `cycle_id`, `closure_date`, `packages`, `d_matz_27_present_in_matriz`, `live_run_log_found`
    - JSON content: `cycle_id == "verification-cycle-2026-Q2"` (literal string match)
    - JSON content: `packages` contiene EXACTAMENTE las 4 keys ambito-financiero-client, iol-client, higyrus-client, matriz-client
    - JSON content: cada package contiene `findings_total: int >= 0`, `count_by_status: dict`, `regression_links: list`, `verify_cycle_closure: {ok: bool, missing: list}`, `schemas_count: int >= 0`, `schemas_paths: list`
    - JSON content: `d_matz_27_present_in_matriz: bool` (True si el finding EXPECTED terminal ya está en matriz-client-findings.md)
    - Behavior: `uv run python -c "import json; ..."` exits 0 con el JSON parsing OK
    - Non-modification: ningún archivo en `.planning/verification/<pkg>-findings.md` modificado en esta task (solo lectura)
    - Non-modification: ningún archivo `verification/*.py` modificado en esta task
  </acceptance_criteria>
  <done>State JSON temporal generado con datos de los 4 paquetes (findings counts, regression links, verify_cycle_closure results, schemas paths, D-MATZ-27 presence). Findings files intactos. Próximas tasks consumen este JSON.</done>
</task>

<task type="auto">
  <name>Task 4.2: Ensure D-MATZ-27 EXPECTED terminal finding (prod-vs-remarkets) is present in matriz-client-findings.md (per D-MATZ-27 verbatim)</name>
  <files>
    .planning/verification/matriz-client-findings.md
  </files>
  <read_first>
    - .planning/verification/matriz-client-findings.md (estado actual POST-Plan 05-03; verificar si el driver ya emitió el finding D-MATZ-27 EXPECTED terminal — captura del Task 4.1 vía `d_matz_27_present_in_matriz`)
    - .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json (Task 4.1 output: clave `d_matz_27_present_in_matriz` — si `true`, esta task es SKIPPED; si `false`, esta task emite el finding via append directo)
    - .planning/verification/FINDINGS-TEMPLATE.md (formato canónico de un finding en "Detalle por hallazgo" — para escribirlo verbatim manualmente, NO via append_finding)
    - .planning/verification/higyrus-client-findings.md (ejemplo de finding EXPECTED ya en el repo: F-01 `posicion.disponibleAjustado: model declara, wire no emite (documentado FCI-conditional)` con status EXPECTED — usar como template del shape exacto)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-27 verbatim L585-606 con título, expected, actual, diff exactos)
    - verification/findings.py (líneas 76-92: FINDING_CLASSES y STATUS_LIFECYCLE para confirmar `SHAPE` y `EXPECTED` válidos)
  </read_first>
  <behavior>
    - Si el state JSON dice `d_matz_27_present_in_matriz: true` → esta task es **idempotente**: no-op, verificación post-task confirma que el finding sigue presente
    - Si el state JSON dice `d_matz_27_present_in_matriz: false` → esta task **APPENDA AL FINAL de la sección "Detalle por hallazgo"** un finding EXPECTED terminal D-MATZ-27 verbatim, ANTES de cualquier `## Cycle Closure` section (que Task 4.3 agregará)
    - El append es DIRECTO al filesystem — **NO usa `append_finding`** porque queremos:
      (a) Garantía de que el text es verbatim D-MATZ-27 (sin re-serialización del helper)
      (b) Que el append no interfiera con el `## Cycle Closure` que Task 4.3 agrega después (Assumption A3 RESEARCH L978: el helper re-serializa y dropearía secciones no-finding)
    - El finding incluye también un nuevo row en la tabla `## Index` con el fid asignado (sigue la numeración del archivo: si el último era F-15, este es F-16)
    - El finding está en formato canónico FINDINGS-TEMPLATE.md: `### F-NN -- <title>` + bloque `**Class:** ... **Surface:** ... **Status:** ...` + bullets Expected/Actual/Diff/Resolution
    - Tras la task, `grep -c 'prod-vs-remarkets divergence acknowledged' matriz-client-findings.md` retorna >= 1
  </behavior>
  <action>
    **Step 1 — Leer el state JSON y decidir si la task aplica:**

    ```
    uv run python -c "
    import json
    data = json.load(open('.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json'))
    if data['d_matz_27_present_in_matriz']:
        print('SKIPPED: D-MATZ-27 already present (driver emitted it during Plan 05-03)')
    else:
        print('APPEND_NEEDED: D-MATZ-27 missing, this task emits it')
    "
    ```

    Si el output es `SKIPPED`, la task es no-op (verificar igualmente en `<verify>` que el finding está presente). Si es `APPEND_NEEDED`, continuar al Step 2.

    **Step 2 — Determinar el próximo `F-NN` libre:**

    ```
    grep -oE '^### F-([0-9]+)' .planning/verification/matriz-client-findings.md | sort -V | tail -1
    ```

    Si el último es `F-15`, el nuevo es `F-16` (zero-padded a 2 dígitos).

    **Step 3 — Actualizar la tabla `## Index`:**

    Localizar la tabla `## Index` en matriz-client-findings.md y agregar UNA fila nueva al final:
    ```
    | F-16 | SHAPE | sync | EXPECTED |
    ```

    Ajustar `F-16` al fid real determinado en Step 2.

    **Step 4 — APPENDAR el finding al final de la sección "Detalle por hallazgo" — ANTES de cualquier `## Cycle Closure` section (verificar al leer; si la sección Cycle Closure NO existe aún, append al EOF):**

    Texto verbatim del finding (mirror D-MATZ-27 CONTEXT.md L585-606 con title/expected/actual/diff EXACTOS):

    ```
    ### F-16 -- prod-vs-remarkets divergence acknowledged

    **Class:** `SHAPE` . **Surface:** `sync` . **Status:** `EXPECTED`

    - **Expected:** verification limited to remarkets sandbox by safety policy (REQUIREMENTS.md Out of Scope)
    - **Actual:** prod (api.primary.com.ar) shape unverified; sandbox shape committed in .planning/verification/schemas/matriz-client/
    - **Diff:** N/A (acknowledged limitation, not detected drift)
    - **Resolution:** Terminal EXPECTED. The prod-vs-remarkets shape gap is an acknowledged limitation of this verification cycle (verification-cycle-2026-Q2). Verification against prod requires a separate harness with stricter safety gates (read-only-only policy, prod-grade rate-limit awareness). Handoff documented in CYCLE-REPORT.md "Open questions for downstream milestone". Mirror del patrón Phase 2 anti-bot EXPECTED.
    ```

    Adaptar el fid (`F-16` arriba) al valor determinado en Step 2.

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - **NO usar `append_finding`** — escribir DIRECTAMENTE al archivo (per Assumption A3 RESEARCH L978)
    - NO modificar findings pre-existentes en matriz-client-findings.md (cero cambios a F-01..F-NN del live run)
    - NO modificar el ART block (timestamp del live run de 05-03 se preserva)
    - El texto del title/expected/actual/diff es VERBATIM D-MATZ-27 (sin paráfrasis)
    - Si la sección `## Cycle Closure` ya existe en el archivo (improbable pre-Task 4.3) → insertar el finding ANTES de esa sección (no después)
    - El fid asignado debe ser zero-padded a 2 dígitos (`F-16` no `F-6`)
    - El Status es `EXPECTED` (no `OPEN` ni `CONFIRMED`)
    - El Class es `SHAPE` (D-MATZ-27 explícito; no `ERROR-MAP` ni otra clase)
    - El Surface es `sync` (matriz es sync-only)
    - Tras el append, actualizar también el state JSON de Task 4.1 con `d_matz_27_present_in_matriz: true` para que Tasks 4.3/4.4 se enteren

    Commit sugerido: NO commit — todos los cambios van en el commit final de Task 4.5
  </action>
  <verify>
    <automated>grep -c "prod-vs-remarkets divergence acknowledged" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -c "Status:.* EXPECTED" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -c "verification limited to remarkets sandbox" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -c "prod (api.primary.com.ar) shape unverified" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -cE "^### F-[0-9]+ -- prod-vs-remarkets" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -v '^#' .planning/verification/matriz-client-findings.md | grep -c "PRIMARY_PASSWORD\|PRIMARY_USER="</automated>
  </verify>
  <acceptance_criteria>
    - Source: `.planning/verification/matriz-client-findings.md` contiene la línea `### F-NN -- prod-vs-remarkets divergence acknowledged` (NN es el fid asignado)
    - Source: contiene `**Class:** \`SHAPE\` . **Surface:** \`sync\` . **Status:** \`EXPECTED\`` (verbatim D-MATZ-27)
    - Source: contiene `verification limited to remarkets sandbox by safety policy (REQUIREMENTS.md Out of Scope)` (Expected verbatim)
    - Source: contiene `prod (api.primary.com.ar) shape unverified; sandbox shape committed in .planning/verification/schemas/matriz-client/` (Actual verbatim)
    - Source: contiene `N/A (acknowledged limitation, not detected drift)` (Diff verbatim)
    - Source: la tabla `## Index` contiene una fila `| F-NN | SHAPE | sync | EXPECTED |` con el fid asignado
    - Behavior: `grep -c 'prod-vs-remarkets divergence acknowledged' matriz-client-findings.md` >= 1
    - Behavior: `grep -c 'Status:.* EXPECTED' matriz-client-findings.md` >= 1
    - Behavior: `grep -cE '^### F-[0-9]+ -- prod-vs-remarkets' matriz-client-findings.md` >= 1
    - Security: `grep -v '^#' matriz-client-findings.md | grep -c 'PRIMARY_PASSWORD\|PRIMARY_USER='` == 0 (no credenciales en el finding nuevo)
    - Non-modification: el ART block (top del archivo) y los findings F-01..F-NN-1 pre-existentes están intactos (verificable diff del archivo pre/post task — el cambio es PURE APPEND + 1 fila a Index)
  </acceptance_criteria>
  <done>Finding D-MATZ-27 EXPECTED terminal prod-vs-remarkets confirmado presente en matriz-client-findings.md (sea por driver de 05-03 o por append directo de esta task). Sin credenciales. Findings pre-existentes intactos.</done>
</task>

<task type="auto">
  <name>Task 4.3: Append `## Cycle Closure` section to all 4 `<pkg>-findings.md` files (D-MATZ-25 direct filesystem append, NOT append_finding)</name>
  <files>
    .planning/verification/ambito-financiero-client-findings.md
    .planning/verification/iol-client-findings.md
    .planning/verification/higyrus-client-findings.md
    .planning/verification/matriz-client-findings.md
  </files>
  <read_first>
    - .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json (output Task 4.1: counts, regression links, verify_cycle_closure results × 4)
    - .planning/verification/ambito-financiero-client-findings.md (estado actual; verificar que NO contiene aún sección `## Cycle Closure` — si existe, abort y reportar)
    - .planning/verification/iol-client-findings.md (idem)
    - .planning/verification/higyrus-client-findings.md (idem)
    - .planning/verification/matriz-client-findings.md (idem POST-Task 4.2; debe contener el F-NN D-MATZ-27 EXPECTED antes del append)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-25 verbatim L531-541 con contenido requerido de la sección: conteo por status, regression tests linkados, validación automática, fecha de cierre)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Assumption A3 L978: append directo NO via append_finding)
  </read_first>
  <behavior>
    - Tras esta task, los 4 `<pkg>-findings.md` tienen una sección `## Cycle Closure` AL FINAL del archivo (después del último finding y de cualquier section pre-existente)
    - Cada sección contiene los 6 items de D-MATZ-25:
      (1) Cycle ID: `verification-cycle-2026-Q2`
      (2) Closure date: ISO 8601 UTC timestamp
      (3) Packages verified count: `4`
      (4) Conteo por status: tabla compacta `OPEN: N | CONFIRMED: N | FIXED: N | EXPECTED: N | NO-FIX: N`
      (5) Lista de regression links presentes (`F-NN: <test_file>::<test_name>`) o nota `(historical findings predate the regression-link convention introduced in Phase 5)` si la lista está vacía para paquetes Phase 2-4 (esto es el caveat esperado del primer cycle closure)
      (6) Link relativo a CYCLE-REPORT.md: `See [CYCLE-REPORT.md](./CYCLE-REPORT.md) for consolidated cross-package report.`
    - El append es DIRECTO al filesystem usando el tool `Edit` o re-escribir el archivo con `Write` después de leerlo completo — NUNCA via `append_finding` (Assumption A3)
    - Tras la task, `grep -c '## Cycle Closure' .planning/verification/*-findings.md` retorna exactamente `4`
  </behavior>
  <action>
    Para cada uno de los 4 findings files, append una sección `## Cycle Closure` AL FINAL del archivo. El contenido es parametrizado por el state JSON de Task 4.1.

    **Plantilla del append (idéntica para los 4 paquetes, parametrizada por `<pkg>` y los counts del state JSON):**

    ```markdown

    ## Cycle Closure

    **Cycle ID:** `verification-cycle-2026-Q2`
    **Closure date:** <ISO 8601 UTC, e.g. 2026-06-09T22:30:00+00:00>
    **Packages verified in this cycle:** 4 (ambito-financiero-client, iol-client, higyrus-client, matriz-client)

    ### Findings by status (this package)

    | OPEN | CONFIRMED | FIXED | EXPECTED | NO-FIX | Total |
    |------|-----------|-------|----------|--------|-------|
    | <n> | <n> | <n> | <n> | <n> | <total> |

    ### Regression tests linked to FIXED/CONFIRMED findings

    <Si lista no vacía:>
    - F-01: `packages/<pkg>/tests/test_client.py::test_xxx`
    - F-02: `packages/<pkg>/tests/test_client.py::test_yyy`
    ...

    <Si lista vacía Y verify_cycle_closure FAIL:>
    *(historical findings predate the regression-link convention introduced in Phase 5; see [CYCLE-REPORT.md](./CYCLE-REPORT.md) "Open questions" for downstream milestone caveat)*

    ### Cycle validation

    `verify_cycle_closure("<pkg>")` returned: **<PASS|FAIL>**
    <Si FAIL:> Missing regressions: <list of fids>

    ---

    See [CYCLE-REPORT.md](./CYCLE-REPORT.md) for the consolidated cross-package report.
    ```

    **Aplicar el append para los 4 paquetes:**

    1. **ambito-financiero-client-findings.md** — usar los counts/data del state JSON para `packages["ambito-financiero-client"]`
    2. **iol-client-findings.md** — usar `packages["iol-client"]`
    3. **higyrus-client-findings.md** — usar `packages["higyrus-client"]`
    4. **matriz-client-findings.md** — usar `packages["matriz-client"]` (debe incluir el F-NN D-MATZ-27 EXPECTED counted en EXPECTED)

    **Pasos operativos:**

    Para cada paquete:
    a. Leer el archivo `.planning/verification/<pkg>-findings.md` completo
    b. Verificar que NO contiene ya `## Cycle Closure` (defensive — si ya existe, abort con error explícito al operator)
    c. Construir el bloque de append con los datos del state JSON
    d. Append AL FINAL del archivo (después del último `### F-NN` y de cualquier section pre-existente)
    e. Usar `Edit` tool: `old_string = "<last line of file>"`, `new_string = "<last line of file>\n\n## Cycle Closure\n..."` — o `Write` tool con el contenido completo si el archivo es pequeño

    **Closure date — usar timestamp ÚNICO para los 4 paquetes** (registrarlo en el JSON state al inicio del Task — usa el `closure_date` del state JSON de Task 4.1 que ya fue stamped):
    ```
    closure_date = state["closure_date"]  # mismo para los 4
    ```

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - **NO usar `append_finding`** del helper Python — escribir DIRECTAMENTE al filesystem (Assumption A3 RESEARCH L978)
    - NO modificar findings pre-existentes (cero cambios a F-NN content)
    - NO modificar el ART block (top del archivo)
    - NO modificar la tabla `## Index` (los 4 findings files ya tienen su index)
    - El cycle ID es literal `verification-cycle-2026-Q2` (no parametrizable; rige para todo el ciclo Q2 2026)
    - La fecha de closure es la MISMA para los 4 paquetes (registrada en el state JSON de Task 4.1)
    - Si por algún motivo `verify_cycle_closure` retorna `False` para un paquete, la sección `## Cycle Closure` aún se appendea — solo cambia el contenido de "Cycle validation" a `FAIL` + lista missing
    - **CRITICAL — Idempotencia**: si la task se re-ejecuta, NO appendar duplicate. Verificar `grep -c '## Cycle Closure' <file>` == 0 antes del append; si > 0 → abort

    Commit sugerido: NO commit individual — todos los cambios van en el commit final Task 4.5
  </action>
  <verify>
    <automated>grep -c "## Cycle Closure" .planning/verification/ambito-financiero-client-findings.md</automated>
    <automated>grep -c "## Cycle Closure" .planning/verification/iol-client-findings.md</automated>
    <automated>grep -c "## Cycle Closure" .planning/verification/higyrus-client-findings.md</automated>
    <automated>grep -c "## Cycle Closure" .planning/verification/matriz-client-findings.md</automated>
    <automated>grep -l "verification-cycle-2026-Q2" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -l "Closure date:" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -l "Packages verified in this cycle" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -l "Findings by status" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -l "Cycle validation" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -l "CYCLE-REPORT.md" .planning/verification/*-findings.md | wc -l</automated>
    <automated>grep -v '^#' .planning/verification/*-findings.md | grep -c "PRIMARY_PASSWORD\|PRIMARY_USER="</automated>
  </verify>
  <acceptance_criteria>
    - Source: cada uno de los 4 archivos `.planning/verification/<pkg>-findings.md` contiene exactamente UNA sección `## Cycle Closure` (no duplicates)
    - Source: cada sección contiene el literal `verification-cycle-2026-Q2`
    - Source: cada sección contiene el literal `Closure date:` con timestamp ISO 8601 UTC
    - Source: cada sección contiene `Packages verified in this cycle: 4`
    - Source: cada sección contiene una tabla `Findings by status` con columnas OPEN/CONFIRMED/FIXED/EXPECTED/NO-FIX/Total
    - Source: cada sección contiene `Cycle validation` line con resultado `PASS` o `FAIL`
    - Source: cada sección termina con un link relativo a CYCLE-REPORT.md
    - Behavior: `grep -c "## Cycle Closure" .planning/verification/ambito-financiero-client-findings.md` == 1
    - Behavior: `grep -c "## Cycle Closure" .planning/verification/iol-client-findings.md` == 1
    - Behavior: `grep -c "## Cycle Closure" .planning/verification/higyrus-client-findings.md` == 1
    - Behavior: `grep -c "## Cycle Closure" .planning/verification/matriz-client-findings.md` == 1
    - Behavior: `grep -l "verification-cycle-2026-Q2" .planning/verification/*-findings.md | wc -l` == 4
    - Behavior: `grep -l "CYCLE-REPORT.md" .planning/verification/*-findings.md | wc -l` == 4
    - Security: `grep -v '^#' .planning/verification/*-findings.md | grep -c 'PRIMARY_PASSWORD\|PRIMARY_USER='` == 0 (no credenciales en ninguno de los appends)
    - Non-modification: findings F-NN pre-existentes en los 4 archivos están sin cambios (verificable con git diff que muestra solo PURE APPENDs)
    - Same closure_date: todos los 4 archivos tienen el MISMO timestamp en `Closure date:` (no times diferentes)
  </acceptance_criteria>
  <done>4 findings files con `## Cycle Closure` appendado (D-MATZ-25). Cycle ID + closure date + counts + regression links + link a CYCLE-REPORT.md presentes en cada uno. No credenciales. Findings pre-existentes intactos. Idempotente (sin duplicates).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4.4: Operator checkpoint — review verify_cycle_closure results and authorize CYCLE-REPORT.md content (especially patrones recurrentes + handling de FAIL para findings históricos)</name>
  <what-built>
    Tasks 4.1-4.3 entregaron:
    - State JSON con counts/regression links/verify_cycle_closure results × 4 paquetes
    - Finding D-MATZ-27 EXPECTED terminal prod-vs-remarkets confirmado en matriz-client-findings.md
    - Sección `## Cycle Closure` appendada a los 4 findings files con cycle ID + closure date + counts + regression links + link a CYCLE-REPORT.md

    Antes de generar CYCLE-REPORT.md (Task 4.5) y commitear (Task 4.5), el operator debe ratificar 3 decisiones que requieren juicio humano:

    (1) **Narrativa de patrones recurrentes (D-MATZ-26 dimensión 2 "Cross-cycle")**: la sección "patrones recurrentes" del CYCLE-REPORT.md es texto descriptivo, no auto-derivable. El operator confirma cuáles patrones describir (sugerencias del planner: envelope-key indexing detected en HIGY/MATZ + ya cubierto en IOL pre-existente; SafeModel false-pass trap detectado en HIGY/MATZ via diff bidireccional; ERROR-MAP coverage variable across paquetes; wire encoding bug HIGY F-01..F-06 detected & fixed in-cycle).

    (2) **Handling de `verify_cycle_closure` FAIL para Phases 2-4 paquetes históricos**: el primer cycle closure run probablemente reporta FAIL para Ámbito/IOL/Higyrus paquetes porque sus findings históricos NO tienen el campo `Regression: <path>::<test>` poblado (convención introducida por Phase 5 cycle_report.py). El operator decide entre 3 opciones:

       - **Opción A** (recomendada para primer cycle): Documentar caveat en CYCLE-REPORT.md "Open questions" — "Historical findings from Phases 2-4 predate the regression-link field convention introduced in Phase 5 cycle_report.py. Forward-looking convention: from Phase 6 onwards, every CONFIRMED→FIXED transition appends `Regression: <path>::<test>` to the finding bullet list. Cycle closure for historical findings counts as PASS via inherited Phase-level audit (each phase's SUMMARY enumerates regression test counts)."
       - **Opción B**: Agregar campos `Regression:` retroactivamente a los findings históricos CONFIRMED/FIXED — requiere conocimiento del operator de qué tests cubren cada finding. Más trabajo manual, ratifica el helper.
       - **Opción C**: Downgrade findings históricos CONFIRMED/FIXED a NO-FIX retroactively — no recomendado (revisita decisiones de phases cerradas).

    (3) **Open questions for downstream milestone** (D-MATZ-26 dimensión 3): el operator confirma qué items deferred citar. Sugerencias del planner:
       - prod-vs-remarkets gap (D-MATZ-27 → milestone futuro "verify matriz against prod with appropriate safety harness")
       - F-07 higyrus get_listado_cuentas=0 (investigación deferida)
       - IOL refresh_token persistente entre invocaciones (si aplica)
       - HIGY multi-account iteration (si aplica)
       - Items diferidos del CONTEXT.md (deferred ideas) cycle-relevantes
  </what-built>
  <how-to-verify>
    **PRE-CHECK del operator:**

    1. Leer el state JSON capturado en Task 4.1:
       ```bash
       cat .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json | python -m json.tool
       ```
       Verificar:
       - Counts por status por paquete son consistentes con los findings files
       - Los `verify_cycle_closure` results × 4 (PASS/FAIL + missing list)
       - Schemas counts (total esperado: 1 ambito + 4 iol + 5 higyrus + 11-19 matriz)

    2. Leer las 4 `## Cycle Closure` sections appendadas (Task 4.3):
       ```bash
       for pkg in ambito-financiero-client iol-client higyrus-client matriz-client; do
         echo "=== $pkg ==="
         grep -A 30 "## Cycle Closure" .planning/verification/$pkg-findings.md
       done
       ```
       Verificar que el cycle ID y closure date son consistentes los 4 paquetes.

    3. Verificar el finding D-MATZ-27 EXPECTED terminal presente en matriz-client:
       ```bash
       grep -A 8 "prod-vs-remarkets divergence acknowledged" .planning/verification/matriz-client-findings.md
       ```

    **DECISIONES OPERATOR (escribir respuestas en el resume-signal):**

    **(A) Patrones recurrentes (texto narrativo para CYCLE-REPORT.md dimensión 2)**:
    Confirmar/ajustar la lista de patrones a citar. Sugerencias del planner:
    - "Envelope-key indexing as unmapped KeyError — detected and fixed in-cycle in HIGY (10 sites Phase 4) and MATZ (18 sites Phase 5); IOL had wrap pre-existing"
    - "SafeModel false-pass trap (model accepts wire silently with safe defaults) — detected via bidirectional diff helper promoted in Phase 5; affects HIGY (Posicion.disponibleAjustado FCI-conditional) and MATZ (TBD per live run)"
    - "ERROR-MAP coverage varies: IOL/HIGY have auth_401 opt-in (Phase 3-4); MATZ uses always-on read-only error probes (D-MATZ-22) because login retries on remarkets are lockout-risk"
    - "Wire encoding bug (httpx %2F vs literal /) detected in HIGY F-01..F-06 Phase 4 Wave 2.5 opportunistic fix — IIS-style backend rejects %2F in date paths"

    Respuesta operator: lista final (puede agregar/quitar/reformular).

    **(B) Handling FAIL de verify_cycle_closure para findings históricos** (Phases 2-4):
    Opción A (caveat doc) / Opción B (retroactive regression links) / Opción C (downgrade) — operator elige.

    Respuesta operator: `A`, `B`, o `C` + justificación breve.

    **(C) Open questions for downstream milestone** (lista final):
    Confirmar/ajustar items. Sugerencias del planner:
    - prod-vs-remarkets gap (D-MATZ-27 handoff explícito) — REQUIRED
    - F-07 higyrus get_listado_cuentas=0 (investigación deferida)
    - Items diferidos del CONTEXT.md cycle-relevantes (ws_client matriz, async surface matriz, multi-account iteration, etc.)

    Respuesta operator: lista final.

    **(D) Confirmación schemas matriz committed**:
    ```bash
    ls .planning/verification/schemas/matriz-client/ 2>&1 | head -20
    ```
    El operator confirma que los schemas matriz del live run de Plan 05-03 están presentes y son committeable. Si NO están (live run no completó snapshots), el operator decide si re-correr el live run o aceptar cycle closure parcial.
  </how-to-verify>
  <resume-signal>
    Type "approved" + responses to A/B/C/D:

    Example approved signal:
    ```
    approved
    A: [list final de patrones recurrentes — copy-paste/edit from sugerencias]
    B: [A|B|C + justificación breve]
    C: [list final de open questions]
    D: schemas matriz committed: [yes|no — if no, action: ...]
    ```

    Si necesitas reportar issues, describir:
    - Qué inconsistencia detectaste en counts/regression links
    - Qué finding requiere re-clasificación antes de generar CYCLE-REPORT.md
    - Cualquier blocker que requiera revisitar Tasks 4.1-4.3 antes de avanzar a Task 4.5
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4.5: Create `.planning/verification/CYCLE-REPORT.md` (D-MATZ-26) with 4 dimensions + run final verification + commit DRIFT-02 baseline (D-MATZ-25..28 closure)</name>
  <files>
    .planning/verification/CYCLE-REPORT.md
  </files>
  <read_first>
    - .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json (Task 4.1 output: all per-pkg data)
    - .planning/verification/ambito-financiero-client-findings.md (POST-Task 4.3 con Cycle Closure appendada)
    - .planning/verification/iol-client-findings.md (idem)
    - .planning/verification/higyrus-client-findings.md (idem)
    - .planning/verification/matriz-client-findings.md (POST Tasks 4.2 + 4.3 con D-MATZ-27 + Cycle Closure)
    - **Operator responses del checkpoint Task 4.4** (capturados en el resume-signal): A=patrones recurrentes, B=FAIL handling option, C=open questions, D=schemas confirm
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-26 verbatim L543-583 con las 4 dimensiones exactas)
    - .planning/verification/schemas/ (listing de los 4 subdirs para la dimensión 4 "Schemas summary")
  </read_first>
  <behavior>
    - `.planning/verification/CYCLE-REPORT.md` existe como archivo NUEVO
    - El archivo contiene las 4 dimensiones D-MATZ-26 verbatim:
      (1) Stats per-package — tabla con 1 fila por paquete + 1 fila TOTAL CYCLE (columns: Package | Findings Total | OPEN | CONFIRMED | FIXED | EXPECTED | NO-FIX | Regression Tests | Schemas Committed)
      (2) Cross-cycle — totales agregados + sección narrativa de patrones recurrentes (texto del operator response A)
      (3) Open questions for downstream milestone — incluye prod-vs-remarkets gap D-MATZ-27 + items del operator response C
      (4) Schemas summary — lista de paths committeables por paquete (un bullet `<pkg> (N): <list>`)
    - El header del documento incluye: title, cycle ID `verification-cycle-2026-Q2`, dates (start = Phase 2 first plan ≈ 2026-05-26, end = closure_date), scope (4 packages)
    - El sign-off final cita el `DRIFT-02 cycle closure` keyword para que el commit forensic se vincule
    - Suite mockeada completa pasa (`uv run pytest -q` exits 0) — verificación pre-commit
    - mypy strict + ruff verdes — verificación pre-commit
    - Commit FINAL ejecutado con mensaje EXACTO `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)` incluyendo: 4 `<pkg>-findings.md` modificados + 1 CYCLE-REPORT.md nuevo + (si necesario) schemas/matriz-client/
    - `.cycle-closure-state.tmp.json` ELIMINADO antes del commit (no se commitea state intermedio)
    - `git log -1 --pretty=%s` retorna línea que contiene la substring `DRIFT-02 cycle closure`
  </behavior>
  <action>
    **Step 1 — Construir CYCLE-REPORT.md con las 4 dimensiones D-MATZ-26:**

    Plantilla del archivo (parametrizada por state JSON + operator responses del checkpoint Task 4.4):

    ```markdown
    # Verification Cycle Report — market-libs

    **Cycle ID:** `verification-cycle-2026-Q2`
    **Cycle start:** 2026-05-26 (Phase 1 — Safety Harness)
    **Cycle end:** <closure_date from state JSON>
    **Scope:** 4 packages — ambito-financiero-client, iol-client, higyrus-client, matriz-client
    **Out of scope (documented):** wallets-client (stub), matriz async/WebSocket surfaces, prod-vs-remarkets verification

    ## Stats per-package

    | Package | Findings Total | OPEN | CONFIRMED | FIXED | EXPECTED | NO-FIX | Regression Tests | Schemas Committed |
    |---------|----------------|------|-----------|-------|----------|--------|------------------|-------------------|
    | ambito-financiero-client | <n> | <n> | <n> | <n> | <n> | <n> | <n> | <n> |
    | iol-client | <n> | <n> | <n> | <n> | <n> | <n> | <n> | <n> |
    | higyrus-client | <n> | <n> | <n> | <n> | <n> | <n> | <n> | <n> |
    | matriz-client | <n> | <n> | <n> | <n> | <n> | <n> | <n> | <n> |
    | **TOTAL CYCLE** | <sum> | <sum> | <sum> | <sum> | <sum> | <sum> | <sum> | <sum> |

    ## Cross-cycle

    - **Total findings emitted in this cycle:** <total>
    - **Total regression tests added:** <total>
    - **Total bugs found + fixed in-cycle (CONFIRMED → FIXED):** <total>
    - **Total schemas committed (PII-free via `schema_of`):** <total>

    ### Patrones recurrentes (cross-cycle observations)

    <Operator response A — copy-paste verbatim of patterns confirmed in checkpoint Task 4.4>

    ## Open questions for downstream milestone

    <Operator response C — copy-paste verbatim, including prod-vs-remarkets gap as REQUIRED handoff>

    Per D-MATZ-27 in `.planning/verification/matriz-client-findings.md` (finding F-NN, EXPECTED terminal): the prod-vs-remarkets verification gap is acknowledged as an EXPECTED limitation of this cycle. Future milestone "verify matriz against prod with appropriate safety harness" required.

    ## Schemas summary

    Committed schema snapshots by package (PII-free, structural only):

    - **ambito-financiero-client (<N>):**
      - .planning/verification/schemas/ambito-financiero-client/<file1>.json
      - ...
    - **iol-client (<N>):**
      - .planning/verification/schemas/iol-client/<file1>.json
      - ...
    - **higyrus-client (<N>):**
      - .planning/verification/schemas/higyrus-client/<file1>.json
      - ...
    - **matriz-client (<N>):**
      - .planning/verification/schemas/matriz-client/<file1>.json
      - ...

    ## Cycle validation (`verify_cycle_closure` per package)

    | Package | Result | Missing (if FAIL) |
    |---------|--------|-------------------|
    | ambito-financiero-client | <PASS|FAIL> | <list or N/A> |
    | iol-client | <PASS|FAIL> | <list or N/A> |
    | higyrus-client | <PASS|FAIL> | <list or N/A> |
    | matriz-client | <PASS|FAIL> | <list or N/A> |

    <If any FAIL — include operator response B verbatim:>
    **Note on historical findings (Phases 2-4):** <Operator response B verbatim, e.g., "Per Option A, historical findings from Phases 2-4 predate the regression-link field convention introduced in Phase 5 cycle_report.py. Forward-looking convention: from Phase 6 onwards, every CONFIRMED→FIXED transition appends `Regression: <path>::<test>`. Cycle closure for historical findings counts as PASS via inherited Phase-level audit (each phase's SUMMARY enumerates regression test counts)."

    ## Sign-off

    DRIFT-02 cycle closure complete: per-package report produced for every verified client; every confirmed bug closed and TESTED (where applicable — see Cycle validation caveat above for historical findings); schema snapshots committed; cross-cycle patterns documented; downstream handoffs explicit.

    *Generated by Plan 05-04 / Phase 5 closure on <closure_date>.*
    ```

    **Step 2 — Static verification antes del commit:**

    ```bash
    uv run pytest -q
    uv run mypy .
    uv run ruff check .
    uv run ruff format --check .
    ```

    Si algún check falla → abort y reportar. Suite debe estar verde para que el commit baseline DRIFT-02 sea legítimo.

    **Step 3 — Eliminar el state JSON temporal antes del commit:**

    ```bash
    rm .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json
    ```

    Si por alguna razón el archivo fue agregado a .gitignore por Task 4.1, no es problema; si no fue gitignored, el `rm` evita que se cuele en el commit.

    **Step 4 — Confirmar que schemas matriz están committeables:**

    Per operator response D del checkpoint Task 4.4:
    - Si schemas matriz YA fueron committeados en Plan 05-03 → no incluir en este commit (no hay nada nuevo en el directorio)
    - Si schemas matriz NO fueron committeados aún → incluirlos en este commit

    Verificar con:
    ```bash
    git status .planning/verification/schemas/matriz-client/
    ```

    Si hay archivos untracked → agregar al commit. Si están tracked y sin cambios → ya están en repo (de 05-03).

    **Step 5 — Commit FINAL con mensaje canónico:**

    ```bash
    git add .planning/verification/ambito-financiero-client-findings.md
    git add .planning/verification/iol-client-findings.md
    git add .planning/verification/higyrus-client-findings.md
    git add .planning/verification/matriz-client-findings.md
    git add .planning/verification/CYCLE-REPORT.md
    # Si schemas matriz no estaban tracked:
    git add .planning/verification/schemas/matriz-client/  # solo si hay archivos untracked

    git commit -m "docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)

    Plan 05-04 closure: append ## Cycle Closure to 4 findings files + new
    CYCLE-REPORT.md consolidated cross-package report. Records D-MATZ-25..28
    closure including D-MATZ-27 prod-vs-remarkets EXPECTED terminal handoff.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    **Step 6 — Verificar el commit ex-post:**

    ```bash
    git log -1 --pretty=%s    # debe contener "DRIFT-02 cycle closure"
    git log -1 --name-only    # debe listar los 4 findings files + CYCLE-REPORT.md
    ```

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - El mensaje del commit DEBE contener la substring exacta `DRIFT-02 cycle closure` para localización forensic
    - El cycle ID `verification-cycle-2026-Q2` aparece en el commit subject + en CYCLE-REPORT.md
    - El commit incluye Co-Authored-By trailer per GSD convention
    - El commit NO debe incluir el `.cycle-closure-state.tmp.json` (debe estar eliminado pre-commit)
    - El commit NO debe incluir credenciales — verificar con `git diff --cached | grep -iE 'password|token=|user='` antes del commit (defensive)
    - CYCLE-REPORT.md es PII-free: solo conteos, paths, narrativa, IDs de findings; NO valores del wire
    - mypy strict + ruff verdes pre-commit; suite verde pre-commit
    - Si el commit fallaría por dirty working tree no relacionado a este plan (e.g., uncommitted changes en otra parte), explicitamente NO commitear esos cambios extra — solo los files de este plan
    - **Branch**: estamos en main (per gitStatus al inicio); si la convención GSD requiere branch (per CLAUDE.md "Commit or push only when the user asks. If on the default branch, branch first."), el operator decide si crear branch o commitear directo en main; default de este plan es commit en main (es el patrón de Phases 2-4 que commitearon en main)

    Commit final: `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)` con Co-Authored-By trailer
  </action>
  <verify>
    <automated>test -f .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "## Stats per-package" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "## Cross-cycle" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "## Open questions" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "## Schemas summary" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "verification-cycle-2026-Q2" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "prod-vs-remarkets" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "DRIFT-02 cycle closure" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>grep -c "TOTAL CYCLE" .planning/verification/CYCLE-REPORT.md</automated>
    <automated>test ! -f .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json</automated>
    <automated>uv run pytest -q</automated>
    <automated>uv run mypy .</automated>
    <automated>uv run ruff check .</automated>
    <automated>uv run ruff format --check .</automated>
    <automated>git log -1 --pretty=%s | grep -F "DRIFT-02 cycle closure"</automated>
    <automated>git log -1 --pretty=%s | grep -F "verification-cycle-2026-Q2"</automated>
    <automated>git log -1 --name-only --pretty="" | grep -E "(CYCLE-REPORT|findings)\.md" | wc -l</automated>
    <automated>grep -v '^#' .planning/verification/CYCLE-REPORT.md | grep -ciE "(password|PRIMARY_USER=|PRIMARY_PASSWORD=)"</automated>
    <automated>wc -l .planning/verification/CYCLE-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - File: `.planning/verification/CYCLE-REPORT.md` existe como archivo nuevo
    - Source: contiene EXACTAMENTE las 4 dimensiones D-MATZ-26: `## Stats per-package`, `## Cross-cycle`, `## Open questions`, `## Schemas summary` (cada una al menos 1 vez)
    - Source: contiene cycle ID literal `verification-cycle-2026-Q2`
    - Source: contiene la substring `DRIFT-02 cycle closure` (en el sign-off section)
    - Source: contiene `prod-vs-remarkets` (handoff explícito para downstream milestone)
    - Source: contiene la fila `TOTAL CYCLE` en la tabla Stats per-package
    - Source: contiene los 4 packages en la tabla Stats per-package (verbatim names)
    - Source: contiene la tabla `Cycle validation (verify_cycle_closure per package)` con 4 filas + columna Result
    - Source: contiene la respuesta del operator A (patrones recurrentes), B (FAIL handling), C (open questions) — verificable inspeccionando el contenido
    - Source: el archivo tiene >= 80 líneas (min_lines del must_haves)
    - Behavior: `grep -c "## Stats per-package\|## Cross-cycle\|## Open questions\|## Schemas summary" .planning/verification/CYCLE-REPORT.md` >= 4
    - Behavior: `grep -c "verification-cycle-2026-Q2" .planning/verification/CYCLE-REPORT.md` >= 1
    - Behavior: `grep -c "DRIFT-02 cycle closure" .planning/verification/CYCLE-REPORT.md` >= 1
    - Behavior: `wc -l .planning/verification/CYCLE-REPORT.md` retorna >= 80
    - Security: `grep -v '^#' .planning/verification/CYCLE-REPORT.md | grep -ciE '(password|PRIMARY_USER=|PRIMARY_PASSWORD=)'` == 0
    - Cleanup: `test ! -f .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json` (state JSON eliminado pre-commit)
    - Static: `uv run pytest -q` exits 0 (suite verde)
    - Static: `uv run mypy .` exits 0
    - Static: `uv run ruff check .` exits 0
    - Static: `uv run ruff format --check .` exits 0
    - Git: `git log -1 --pretty=%s` contiene substring `DRIFT-02 cycle closure`
    - Git: `git log -1 --pretty=%s` contiene substring `verification-cycle-2026-Q2`
    - Git: el último commit incluye al menos 5 archivos modificados/agregados: 4 `<pkg>-findings.md` + CYCLE-REPORT.md
  </acceptance_criteria>
  <done>CYCLE-REPORT.md creado con las 4 dimensiones D-MATZ-26 + operator responses A/B/C/D. State JSON eliminado. Suite verde. Commit baseline DRIFT-02 ejecutado con mensaje canónico forensic-localizable. Phase 5 cierre formal completado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json` (intermediate) → CYCLE-REPORT.md generation | State temporal NO committeable; eliminado pre-commit en Task 4.5. Si por error queda → file no contiene credenciales por construcción (solo counts/paths/regression links) |
| `.planning/verification/<pkg>-findings.md` (input/output) | Modificados via APPEND DIRECTO (NO `append_finding` per Assumption A3). El append es shape-only — counts, fids, paths de tests, cycle ID. Sin credenciales por construcción |
| `.planning/verification/CYCLE-REPORT.md` (new) → committed git | PII-free por construcción: solo conteos, paths de schemas/tests, IDs de findings, narrativa de patrones. NO valores del wire ni credenciales |
| Operator checkpoint Task 4.4 → CYCLE-REPORT.md content | Operator escribe patrones recurrentes + open questions; responsabilidad operator NO incluir credenciales en el response (verificable post-checkpoint via grep) |
| Git commit final → main branch | Mensaje commit incluye cycle ID + DRIFT-02 cycle closure literal. Sin secretos. Co-Authored-By trailer per GSD convention |
| `verify_cycle_closure(pkg)` invocation → matiz-client findings file | Helper es parse estructural offline (no live calls per D-MATZ-28). Read-only sobre findings files + read-only sobre test files (verificación de `def test_<name>(` presence) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-23 | Information Disclosure | `.cycle-closure-state.tmp.json` filtrado en commit | mitigate | Task 4.5 ELIMINA el archivo pre-commit con `rm`; verificación automática `test ! -f <path>`; defense-in-depth: gitignored o pre-commit check |
| T-5-24 | Information Disclosure | CYCLE-REPORT.md contiene credenciales del operator response | mitigate | Acceptance: `grep -v '^#' CYCLE-REPORT.md \| grep -ciE '(password\|PRIMARY_USER=\|PRIMARY_PASSWORD=)'` == 0; operator briefing en Task 4.4 checkpoint NO incluir credenciales en responses A/B/C |
| T-5-25 | Tampering | `## Cycle Closure` append a findings files via `append_finding` (helper) en lugar de filesystem directo | mitigate | Task 4.3 explícitamente prohíbe `append_finding`; acceptance verifica que las 4 sections persisten tras el append (no son dropeadas por re-serialización del helper). Assumption A3 RESEARCH L978 inherits from Phase 2 review-04 CR-01 |
| T-5-26 | Tampering | Findings históricos Phases 2-4 modificados en cycle closure | accept | Tasks 4.2/4.3 explícitamente NO modifican findings pre-existentes (PURE APPEND only). Acceptance Task 4.3 verifica via "Non-modification: findings F-NN pre-existentes intactos". `verify_cycle_closure` parse es read-only |
| T-5-27 | Repudiation | Commit baseline DRIFT-02 sin mensaje localizable forensic | mitigate | Mensaje commit incluye substring EXACTA `DRIFT-02 cycle closure (verification-cycle-2026-Q2)`. Acceptance: `git log -1 --pretty=%s \| grep -F "DRIFT-02 cycle closure"` retorna match. Tooling forensic puede localizar via `git log --grep='DRIFT-02 cycle closure'` |
| T-5-28 | Tampering | CYCLE-REPORT.md o findings files committeados con suite roja | mitigate | Task 4.5 Step 2 ejecuta `uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .` PRE-COMMIT. Si falla → abort el commit. Acceptance: los 4 checks aparecen en `<verify>` block |
| T-5-29 | Information Disclosure | schemas/matriz-client/*.json committed con valores reales | accept | Schemas son PII-free por construcción via `schema_of` (Phase 1 D-13 lockeado). Plan 05-03 ya committeó (o este plan committea) — los schemas NO contienen valores, solo nombres de tipo. Acceptance via Phase 1 inherits |
| T-5-30 | Tampering | Re-ejecución del plan duplica `## Cycle Closure` en findings files | mitigate | Task 4.3 verifica `grep -c '## Cycle Closure' <file>` == 0 ANTES del append; si > 0 → abort. Idempotente |
| T-5-31 | Repudiation | Operator omite ratificar FAIL handling para findings históricos | mitigate | Task 4.4 checkpoint blocking-gate; CYCLE-REPORT.md genera FAIL handling note inline. Si operator no responde → plan permanece bloqueado en checkpoint |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | Plan 05-04 NO instala paquetes externos |

**Inherited threats from Phases 1-5 (still active):**
- T-5-16 (Information Disclosure): Live driver stdout — N/A para este plan (no se ejecuta live)
- T-5-17 (Tampering): Hostname assert remarkets — N/A para este plan (sin invocación API)
- T-5-18 (Tampering): MATZ-06 GET sentinel — N/A para este plan (sin modificar tests)
- T-5-20 (Information Disclosure): Findings file PII — Plan 05-04 PURE APPEND; no introduce nuevos valores del wire
- T-5-21 (Repudiation): Operator clasifica findings → cycle closure FAIL — mitigado por Task 4.4 checkpoint + CYCLE-REPORT.md caveat note
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Sección `## Cycle Closure` appendada a los 4 findings files
for pkg in ambito-financiero-client iol-client higyrus-client matriz-client; do
  count=$(grep -c "## Cycle Closure" .planning/verification/$pkg-findings.md)
  test "$count" -eq 1 || { echo "FAIL: $pkg has $count Cycle Closure sections (expected 1)"; exit 1; }
done

# 2. Todos los 4 findings files mencionan el cycle ID literal
count=$(grep -l "verification-cycle-2026-Q2" .planning/verification/*-findings.md | wc -l)
test "$count" -eq 4

# 3. matriz-client tiene el finding EXPECTED terminal D-MATZ-27
grep -c "prod-vs-remarkets divergence acknowledged" .planning/verification/matriz-client-findings.md  # >= 1

# 4. CYCLE-REPORT.md existe con las 4 dimensiones D-MATZ-26
test -f .planning/verification/CYCLE-REPORT.md
grep -c "## Stats per-package" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "## Cross-cycle" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "## Open questions" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "## Schemas summary" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "verification-cycle-2026-Q2" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "prod-vs-remarkets" .planning/verification/CYCLE-REPORT.md  # >= 1
grep -c "DRIFT-02 cycle closure" .planning/verification/CYCLE-REPORT.md  # >= 1

# 5. State JSON temporal eliminado pre-commit
test ! -f .planning/phases/05-matriz-verification/.cycle-closure-state.tmp.json

# 6. Suite verde pre-commit
uv run pytest -q
uv run mypy .
uv run ruff check .
uv run ruff format --check .

# 7. Commit forensic-localizable
git log -1 --pretty=%s | grep -F "DRIFT-02 cycle closure"
git log -1 --pretty=%s | grep -F "verification-cycle-2026-Q2"

# 8. NO credenciales en CYCLE-REPORT.md ni en `## Cycle Closure` sections
grep -v '^#' .planning/verification/CYCLE-REPORT.md | grep -ciE "(password|PRIMARY_USER=|PRIMARY_PASSWORD=)"  # == 0
grep -v '^#' .planning/verification/*-findings.md | grep -ciE "(password|PRIMARY_USER=|PRIMARY_PASSWORD=)"  # == 0

# 9. Closure date consistency cross-files
grep -h "Closure date:" .planning/verification/*-findings.md | sort -u | wc -l  # debe ser == 1 (mismo timestamp en los 4)

# 10. Schemas matriz committeados (per operator response D)
ls .planning/verification/schemas/matriz-client/ | wc -l  # >= 1 (variable según live run 05-03)
```

Per Wave 4 merge:
```bash
git log --oneline -5  # confirmar que el commit DRIFT-02 cycle closure es el último
```
</verification>

<success_criteria>
- 4 `<pkg>-findings.md` con sección `## Cycle Closure` appendada (D-MATZ-25) — verbatim cycle ID, closure date, counts, regression links, link a CYCLE-REPORT.md
- `.planning/verification/CYCLE-REPORT.md` nuevo con las 4 dimensiones D-MATZ-26 (Stats per-package, Cross-cycle, Open questions, Schemas summary) y operator-ratified patterns + handling
- Finding D-MATZ-27 EXPECTED terminal prod-vs-remarkets confirmado en matriz-client-findings.md (sea emitido por driver de 05-03 o por append directo de Task 4.2)
- `verify_cycle_closure(pkg) × 4` resultados reflejados en CYCLE-REPORT.md sección `Cycle validation` con caveat note para FAIL handling
- State JSON temporal eliminado pre-commit (no contamina repo)
- Cycle ID `verification-cycle-2026-Q2` aparece en cycle closure × 4 + CYCLE-REPORT.md + commit subject (5 sites consistentes)
- Closure date consistente cross-files (mismo timestamp para los 4)
- Single commit baseline DRIFT-02 con mensaje canónico `docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)` — localizable forensic via `git log --grep='DRIFT-02 cycle closure'`
- Suite mockeada completa verde pre-commit
- mypy strict + ruff verdes pre-commit
- NO credenciales en CYCLE-REPORT.md ni en `## Cycle Closure` sections appendadas
- Phase 5 ROADMAP success criteria #5 cumplido: schema snapshot committed, per-package report produced, every CONFIRMED bug closed and TESTED (con caveat documentado para findings históricos si verify_cycle_closure FAIL)
- DRIFT-02 requirement marcado como Complete en próximo `gsd-sdk query` o equivalente
</success_criteria>

<output>
Crear `.planning/phases/05-matriz-verification/05-04-SUMMARY.md` cuando done, registrando:
- Conteo final de findings por status por paquete (4 tablas o 1 tabla con 4 columnas)
- Conteo final de regression tests linkados (cuántos por paquete)
- Resultados de `verify_cycle_closure × 4` (PASS/FAIL + missing lists)
- Operator response B (FAIL handling option seleccionada) — si A, qué caveat se documentó; si B, qué findings recibieron regression links retroactive; si C, qué findings fueron downgraded
- Lista final de patrones recurrentes documentados (Operator response A)
- Lista final de open questions for downstream milestone (Operator response C)
- Schemas paths committeados por paquete (matiz: ¿cuántos? respeto 11-19 esperados?)
- Confirmación del commit DRIFT-02 (SHA hash, files incluidos)
- Confirmación de NO exfiltración de credenciales (grep results)
- Métrica de duración del plan
- Confirmación end-to-end DRIFT-02: ✓ schema snapshots committed, ✓ per-package report produced, ✓ every CONFIRMED bug closed and TESTED (o caveat note)
- Phase 5 closure status: ROADMAP success criteria 1-5 marcados Complete o con caveat
- Cycle ID literal: `verification-cycle-2026-Q2`
- Handoff note: next cycle (verification-cycle-2027-XX) inicia con prod-vs-remarkets gap como Open Question #1
</output>
