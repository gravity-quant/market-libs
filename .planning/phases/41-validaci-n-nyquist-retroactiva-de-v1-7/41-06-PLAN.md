---
phase: 41
plan: 06
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md
autonomous: true
requirements: [NYQ-01]

must_haves:
  truths:
    - "Las 15 filas de la Phase 39 (11 de mapa + 4 manual-only) llevan exactamente una disposición con evidencia nombrada"
    - "Las 4 filas que dependen de red en vivo o de una ventana de sesión de mercado quedan NOT-VERIFIABLE-RETROACTIVELY, y ese hecho queda declarado en el front-matter de la fase (criterio 3b)"
    - "La fila cuyo selector no selecciona ningún test queda re-apuntada al archivo que sí contiene los tests de allowlist de hostname, con ambos comandos escritos"
    - "Ningún comando de esta auditoría abre un socket contra una API financiera en vivo"
  artifacts:
    - path: ".planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md"
      provides: "Sección Validation Audit 2026-08-31 con 15 disposiciones y front-matter con el marcador de criterio 3b"
      contains: "not_verifiable_retroactively: 4"
  key_links:
    - from: ".planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md"
      to: ".planning/verification/run-evidence/"
      via: "las 4 filas no re-verificables citan los envelopes fechados que sobreviven como evidencia parcial"
      pattern: "run-evidence"
    - from: ".planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md"
      to: "verification/test_main_matriz_skip_line_shape.py"
      via: "la corrección de comando apunta al archivo que sí contiene los tests de allowlist exacto de hostname"
      pattern: "test_main_matriz_skip_line_shape"
---

<objective>
Auditar la Phase 39, la única de las cinco que conserva ítems no re-verificables: 11 filas de mapa más
4 filas manual-only cuya conducta exigió red en vivo, una ventana de sesión de mercado o un sign-off
humano.

Purpose: esta fase es donde el criterio 3 se decide. Si las cuatro filas manual-only se lavaran como
cubiertas, las cinco fases cerrarían "limpias" y el flag de cumplimiento quedaría sin ancla. Y es
también la fase con el riesgo de seguridad más concreto de toda la auditoría: sus filas describen
corridas contra APIs financieras reales, y re-correrlas para "verificar ahora" abriría tráfico contra
un tercero. Ninguna se re-corre: se disponen contra los artefactos fechados que quedaron en disco.

Output: `39-VALIDATION.md` con `## Validation Audit 2026-08-31`, 15 disposiciones, y el front-matter
que declara explícitamente que la fase conserva 4 ítems no re-verificables. Cero fuente de producto
tocada; cero tráfico de red.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-PATTERNS.md
@.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-01-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Re-ejecutar las 10 filas automatizadas, re-apuntar el selector vacío, y reunir la evidencia de las 5 filas no automatizadas</name>
  <files>.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md (el archivo objetivo, completo — mapa de 11 filas + 4 filas manual-only)
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 2, 3 (incluida la resolución escrita de OQ#1), 6, 7
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-RESEARCH.md § Re-Execution Results → Phase 39, § Architecture Patterns → Pattern 3, § Disposition Decision Rules, § Common Pitfalls 2 y 5, § Security Domain
    - verification/test_main_matriz_skip_line_shape.py (cuerpo de los tests de allowlist exacto de hostname)
    - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CENSUS.md
    - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VERIFICATION.md
    - .planning/verification/run-evidence/iol-client.json
    - .planning/verification/run-evidence/higyrus-client.json
    - .planning/verification/run-evidence/ambito-financiero-client.json
    - .planning/verification/run-evidence/matriz-client.json
    - .github/workflows/ci.yml
  </read_first>
  <action>
    Re-verificar el invariante de criterio 1 con `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'`
    antes de ejecutar nada. Si sale distinto de 0, ABORTAR.

    Asignar claves ordinales: `39-r01`..`39-r11` a las 11 filas del mapa en su orden de aparición
    (`39-01-01` a `39-03-02`), y `39-m01`..`39-m04` a las cuatro filas de
    `## Manual-Only Verifications` en su orden. Denominador 15.

    **Restricción dura de esta tarea: ningún comando abre un socket.** Las conductas que la Phase 39
    verificó en vivo se disponen contra artefactos, jamás con una corrida fresca contra una API
    financiera de terceros. Ningún driver de `main_*.py` se ejecuta en este plan.

    Re-ejecutar las 10 filas automatizadas que funcionan, transcribiendo el comando literal del mapa.
    Salidas de referencia medidas por el researcher esta sesión: la suite de clasificación de
    verificación con 7 pasados; las dos suites de forma de línea de skip, matriz con 19 e higyrus con 8;
    la suite de cierre de ciclo con 21 pasados; la cadena profunda de iol con 6; la de higyrus con 8; la
    de matriz con 9; el selector de ámbito en la suite de cierre de ciclo con 2 pasados; las tres suites
    de casos límite de cadena profunda con 50 entre las tres; y la suite de forma de identificador plano
    de instrumentos de matriz con 13 pasados. La evidencia que se escribe es la salida de esta corrida.

    Tratar la fila `39-r03` por la regla R-02. Su selector de allowlist selecciona 0 de 9 tests y pytest
    sale con código 5. La conducta —que el allowlist de hostname de la política D-MATZ-33 admite el host
    conocido por igualdad exacta y rechaza las variantes de spoofing por superstring y por userinfo—
    vive en otro archivo: `verification/test_main_matriz_skip_line_shape.py`, que contiene el test de que
    el allowlist tiene exactamente los dos hosts conocidos, el test parametrizado de resolución por
    hostname exacto (con 14 parámetros que incluyen la variante de superstring y la de userinfo), y el
    test de que no hay chequeo de pertenencia por substring sobre un literal de host. Antes de
    re-apuntar, **leer el cuerpo de esos tres tests** y confirmar que assertan esa conducta
    (RESEARCH.md Assumptions Log A1). Ejecutar el comando corregido, capturar su conteo de pasados, y
    disponer `VERIFIED-NOW (comando corregido)` transcribiendo **ambos** comandos: el original con su
    resultado de selección vacía y el corregido con su conteo. Esta es la segunda y última corrección de
    comando autorizada en toda la fase 41; si apareciera una tercera fila de selección vacía, es un
    hallazgo real y se escala en el SUMMARY.

    Disponer la fila `39-r11` por la regla R-06: es una revisión de artefacto, el censo, que sigue en
    disco y está citado por `39-VERIFICATION.md`. Confirmar que el archivo existe y citar la verdad de
    `39-VERIFICATION.md` que lo referencia. No re-hacer el censo.

    Disponer las cuatro filas manual-only `39-m01`..`39-m04` como `NOT-VERIFIABLE-RETROACTIVELY` por la
    regla R-07, según la resolución de OQ#1 ya escrita en la sección 3 del contrato. Esa resolución
    honra el ejemplo explícito de D-04, que nombra estas cuatro filas por su nombre como el arquetipo
    del token, por encima de la lectura alternativa que el researcher propuso. La evidencia
    superviviente **no se descarta**: la celda de evidencia de cada una debe nombrar el artefacto
    fechado que sí existe, calificado como evidencia parcial que no alcanza a re-derivar la conducta.
    Concretamente: para la fila de la corrida de driver en vivo por paquete, los cuatro envelopes de
    `.planning/verification/run-evidence/` con su conteo de sondas ejecutadas y su fecha; para la fila
    de discriminación entre mercado cerrado y modelado incorrecto en matriz, la sección de casos límite
    de `39-CENSUS.md`, más el hecho de que reproducirla exige una ventana de sesión de negociación
    específica que no se puede recrear; para la fila de contraste de censo, `39-CENSUS.md` y los censos
    históricos que contrasta; y para la fila del checkpoint del operador sobre el ensanchamiento del
    allowlist de hostname, el sign-off registrado que `39-VALIDATION.md` ya cita. Escribir en cada celda,
    de forma explícita, por qué la evidencia parcial no basta: un envelope prueba que la corrida ocurrió
    y con qué conteo, no que la conducta observada sea reproducible hoy.

    Aplicar el guardia anti-vacuidad de la sección 6 del contrato a las 10 filas automatizadas.

    Recortar toda salida a su línea de resumen antes de escribirla al archivo. Esta fase es la de mayor
    riesgo de fuga: sus artefactos de evidencia hablan de hosts, cuentas y sondas contra servicios
    reales. Antes de escribir, revisar que ninguna celda contenga una URL con esquema, un signo de
    arroba dentro de un host, un token, una contraseña ni un portador de credencial.
  </action>
  <verify>
    <automated>uv run pytest verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_cycle_closure_phase33.py verification/test_main_iol_deep_chain.py verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py -q && uv run pytest packages/matriz-client/tests/test_instruments_flat_identifier_shape.py -q && test -f .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CENSUS.md && test "$(ls .planning/verification/run-evidence/*.json | wc -l | tr -d ' ')" -eq 4 && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - Las 15 filas tienen clave ordinal asignada (`39-r01`..`39-r11` más `39-m01`..`39-m04`), únicas.
    - Las 7 suites de `verification/` citadas por el mapa corren juntas con salida 0 y conteo de pasados distinto de cero.
    - El comando original de `39-r03` queda documentado con su resultado real: sale con código 5 y no reporta ningún test pasado.
    - El comando corregido de `39-r03` apunta a `verification/test_main_matriz_skip_line_shape.py`, sale 0 con conteo de pasados distinto de cero, y los tres tests de allowlist fueron leídos y su aserción resumida en la celda de evidencia.
    - Los cuatro envelopes existen: `ls .planning/verification/run-evidence/*.json | wc -l` es `4`, y el conteo de sondas ejecutadas de cada uno queda transcrito en la evidencia de `39-m01`.
    - `.planning/milestones/v1.7-phases/39-*/39-CENSUS.md` existe en disco.
    - Cero tráfico de red: ningún `main_*.py` fue ejecutado; el SUMMARY lo afirma explícitamente.
    - Higiene de credenciales: dentro de la sección de auditoría escrita, `grep -cE '://|@[a-z0-9.-]+\.(com|ar|example)|[Tt]oken|[Pp]assword|Bearer'` es `0`, salvo rutas de archivo del repo.
    - `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0 al terminar; `git status --porcelain verification/` vacío.
  </acceptance_criteria>
  <done>Las 10 filas automatizadas tienen evidencia de esta sesión, la fila de selección vacía está re-apuntada con el cuerpo de los sustitutos leído, y las 5 filas no automatizadas tienen su artefacto fechado citado con su límite explicado.</done>
</task>

<task type="execute">
  <name>Task 2: Escribir la sección de auditoría de la Phase 39 y transformar su front-matter con el marcador del criterio 3b</name>
  <files>.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md</files>
  <read_first>
    - .planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md
    - .planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-AUDIT-CONTRACT.md §§ 3, 4, 5, 7, 8
    - .planning/milestones/v1.1-phases/09-deferred-bug-fixes/09-VALIDATION.md (esqueleto de sección y el párrafo modelo que justifica por qué las filas manual-only siguen siendo manuales)
    - .planning/milestones/v1.1-phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (par de encabezados en negrita, escalaciones, línea tildada de sign-off)
    - .planning/milestones/v1.1-phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (prosa modelo para un resultado PARTIAL)
    - .github/workflows/ci.yml (para verificar el rango real del allowlist explícito)
  </read_first>
  <action>
    Escribir `## Validation Audit 2026-08-31` al final de `39-VALIDATION.md`, precedida de una regla
    horizontal, después de `## Validation Sign-Off`, con los ocho bloques de la sección 5 del contrato.

    Valores concretos para esta fase: denominador **15**; disposición esperada 10 `VERIFIED-NOW` (de los
    cuales 1 con comando corregido), 1 `VERIFIED-HISTORICALLY`,
    4 `NOT-VERIFIABLE-RETROACTIVELY`; correcciones de comando **1**; archivos de test nuevos escritos
    **0**; filas cuya celda de enforcement dice que no corre en CI: **5** — la fila de revisión del censo
    y las cuatro manual-only. Si la medición da otro reparto, escribir el medido y anotar la divergencia
    en el SUMMARY.

    Llenar la cuarta columna con la tabla de lookup de la sección 7 del contrato. Esta fase tiene un
    resultado que merece prosa propia: **todas** las suites de `verification/` que su mapa cita están
    dentro del allowlist explícito de CI, porque la propia Phase 39 cerró ese defecto. Decirlo: el
    conteo de filas sin cobertura de CI en esta fase no viene de sus locks, viene de sus filas manuales.
    Las dos filas que corren suites bajo `packages/` mapean al job `test`.

    Escribir la sección `### Correcciones de comando` con la única entrada de esta fase, la fila
    `39-r03`, con el comando original, su resultado de selección vacía, los tres tests de allowlist
    nombrados con lo que assertan, el comando corregido y su conteo de pasados.

    Escribir el párrafo que justifica las cuatro filas no re-verificables, copiando la forma del
    precedente de `09-VALIDATION.md` pero divergiendo en el veredicto: nunca escribir que la fase
    "sigue siendo compliant". Nombrar por qué cada una es irrecuperable —red viva de terceros, ventana
    de sesión de mercado, juicio cruzado sobre artefactos históricos, sign-off humano— y qué evidencia
    parcial sobrevive. Registrar además, con su rationale, que esta disposición honra el ejemplo
    explícito de D-04 por encima de la lectura alternativa que RESEARCH.md propuso en su primera
    pregunta abierta, y que en una auditoría la dirección segura es sub-declarar: los envelopes fechados
    prueban que la corrida ocurrió, no que la conducta sea reproducible.

    Escribir la sección de hallazgos de bookkeeping con tres: primero, que ocho celdas de `File Exists`
    del mapa afirman que el archivo falta cuando todas las suites citadas existen y corren verde;
    segundo, que la sección de Wave 0 de este archivo cita un rango de líneas del allowlist de CI que
    quedó desactualizado —el allowlist se ensanchó a 12 archivos durante la propia Phase 39 y hoy ocupa
    otro rango, que hay que verificar contra el archivo de workflow actual y escribir en la prosa sin
    editar la sección histórica de Wave 0—; y tercero, el hallazgo transversal de seguridad de la
    auditoría: de los 52 archivos que calzan el patrón de test de `verification/`, sólo 12 están en el
    allowlist explícito, así que 40 locks no corren en CI. Ese conteo se reporta; **no** se arregla
    aquí: el edit consolidado del archivo de workflow es trabajo de la Phase 45.

    Actualizar la columna `Status` de las 15 filas según la regla de la sección 5 del contrato y
    extender la línea de leyenda. Las cuatro filas manual-only reciben el valor de no re-verificable,
    nunca la marca verde.

    Transformar el front-matter según la sección 4 del contrato: `status` pasa de `draft` a `validated`;
    `nyquist_compliant` se queda en `false` porque R-09 falla por sus condiciones (b) y (c) a la vez.
    `not_verifiable_retroactively` es **`4`** — esta es la única de las cinco fases con valor distinto de
    cero, y es exactamente la forma en que el criterio 3 se satisface: la fase dice en su propio
    front-matter que conserva ítems no re-verificables en vez de reportarse limpia. Se agregan
    `audited_commit_sha` con el commit de v1.7, `audit_baseline_head` copiado literal del contrato,
    `frozen_tree_verified: true`, `updated: 2026-08-31` y `last_audited: 2026-08-31`. Quedan intactos
    `phase`, `slug`, `created` y `wave_0_complete`.

    Cerrar con la línea de veredicto en la forma fijada, nombrando el conteo de 4 filas no
    re-verificables, y con una línea nueva tildada en el `## Validation Sign-Off`, dejando sin tildar
    las casillas preexistentes.
  </action>
  <verify>
    <automated>F=.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md; X() { awk '/^### Disposición por fila/{f=1} /^\*Disposiciones:/{f=0} f&&/^\| 39-[rm][0-9]/' "$F"; }; test "$(X | wc -l | tr -d ' ')" -eq 15 && test "$(X | grep -c 'VERIFIED-NOW')" -eq 10 && test "$(X | grep -c 'VERIFIED-HISTORICALLY')" -eq 1 && test "$(X | grep -c 'NOT-VERIFIABLE-RETROACTIVELY')" -eq 4 && test "$(X | grep -c 'NOT ENFORCED')" -eq 5 && grep -q '^status: validated' "$F" && grep -q '^nyquist_compliant: false' "$F" && grep -q '^not_verifiable_retroactively: 4' "$F" && grep -q '37a83fe693a303a551f4374f48fe6fc5521804f7' "$F" && grep -q 'Cero archivos de test nuevos; cero escalaciones.' "$F" && git diff --quiet v1.7 HEAD -- . ':(exclude).planning'</automated>
  </verify>
  <acceptance_criteria>
    - La tabla de disposición tiene exactamente 15 filas dentro del rango acotado entre `### Disposición por fila` y la línea de leyenda.
    - La suma de los tres conteos de token dentro de ese rango es exactamente `15`.
    - El reparto medido es 10 / 1 / 4: `VERIFIED-NOW` = `10`, `VERIFIED-HISTORICALLY` = `1`, `NOT-VERIFIABLE-RETROACTIVELY` = `4`.
    - Las cuatro filas no re-verificables son exactamente `39-m01`..`39-m04`, y cada una cita al menos un artefacto fechado en disco como evidencia parcial más la frase que explica por qué esa evidencia no basta.
    - `grep -c 'NOT ENFORCED'` dentro del rango acotado es `5`.
    - Exactamente una fila lleva el calificador de comando corregido, y es `39-r03`.
    - Cero evidencia vacua: dentro del rango acotado, las líneas que mencionan descartes y no mencionan tests pasados son `0`.
    - Ninguna fila de tipo manual lleva la marca verde en la columna `Status`.
    - Front-matter: `grep -c '^status: validated'` es `1`, `grep -c '^nyquist_compliant: false'` es `1`, `grep -c '^not_verifiable_retroactively: 4'` es `1`, `grep -c '^audited_commit_sha: 37a83fe693a303a551f4374f48fe6fc5521804f7'` es `1`, `grep -cE '^audit_baseline_head: [0-9a-f]{40}'` es `1`.
    - El hallazgo transversal de enforcement queda reportado con números: la sección de hallazgos nombra los 52 archivos que calzan el patrón, los 12 enrolados y los 40 que no corren, y rutea el arreglo a la Phase 45 en vez de aplicarlo.
    - El rango de líneas del allowlist citado en la prosa coincide con el real: `grep -n 'verification/test_main_market_data_deep_chain.py' .github/workflows/ci.yml` cae dentro del rango escrito.
    - `grep -c 'Cero archivos de test nuevos; cero escalaciones.' 39-VALIDATION.md` es `1`.
    - `grep -c '^- \[x\] .*nyquist_compliant: true' 39-VALIDATION.md` es `0`.
    - `git status --porcelain` sólo lista `39-VALIDATION.md` bajo `.planning/milestones/v1.7-phases/`; `git status --porcelain verification/` vacío y `.github/workflows/ci.yml` sin modificar.
  </acceptance_criteria>
  <done>Las 15 filas de la Phase 39 están dispuestas, las 4 no re-verificables están declaradas en el front-matter con la clave entera en 4, y el hallazgo de 40 locks sin cobertura de CI queda reportado y ruteado a la Phase 45 sin tocar el archivo de workflow.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Producido por este plan:
- `.planning/milestones/v1.7-phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-VALIDATION.md` — **modificado**. Sección `## Validation Audit 2026-08-31` (métricas, tabla de disposición de 15 filas y 4 columnas, una corrección de comando, párrafo que justifica las 4 filas no re-verificables con su rationale de resolución de la pregunta abierta, hallazgos de bookkeeping incluido el conteo transversal de enforcement, escalaciones, veredicto); columna `Status` actualizada; front-matter con `status: validated`, `nyquist_compliant: false`, **`not_verifiable_retroactively: 4`** (el marcador del criterio 3b), `audited_commit_sha`, `audit_baseline_head`, `frozen_tree_verified: true`, `updated`, `last_audited`.

Cero archivos nuevos, cero locks nuevos en `verification/`, **cero ediciones a `.github/workflows/ci.yml`**. Las 5 filas marcadas como no cubiertas por CI y el conteo transversal de 40 locks preexistentes sin enrolar son un **hallazgo reportado**, cuyo enrolamiento queda ruteado por escrito al edit consolidado del archivo de workflow de la Phase 45 (precondición ya registrada en `REQUIREMENTS.md`). La declaración inerte formal y su ruteo se consolidan en el plan 41-07.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| salida de pytest local → markdown committeado | Las líneas de resumen transcritas quedan en el repo; esta fase es la de mayor riesgo de arrastrar un host o una cuenta |
| artefactos de evidencia de corridas en vivo → markdown committeado | Los envelopes describen sondas contra APIs financieras reales |
| auditor → APIs financieras de terceros | Frontera que este plan **no cruza**: prohibido explícitamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Information Disclosure | transcripción de evidencia de corridas en vivo a `39-VALIDATION.md` | mitigate | Sección 6 del contrato más un criterio de aceptación que greppea la sección escrita buscando esquema de URL, arroba dentro de host, token, contraseña o portador de credencial, y exige cero coincidencias fuera de rutas del repo (ASVS V7; CLAUDE.md) |
| T-41-06-02 | Tampering | re-correr un driver de `main_*.py` durante la auditoría y abrir tráfico contra una API financiera de terceros | mitigate | Restricción dura escrita en el `<action>`: ningún comando abre un socket; las 4 filas de red se disponen por R-07 contra artefactos; criterio de aceptación que exige la afirmación explícita en el SUMMARY |
| T-41-06-03 | Repudiation | lavar las 4 filas manual-only como cubiertas, dejando al criterio 3 sin ancla | mitigate | R-07 más la clave `not_verifiable_retroactively: 4` en front-matter (criterio 3b) más criterios de aceptación sobre el reparto exacto 10 / 1 / 4 |
| T-41-06-04 | Repudiation | certificar como cubierto el lock de allowlist de hostname cuando el selector del mapa no ejecutaba nada | mitigate | R-02 con lectura obligatoria del cuerpo de los tres tests sustitutos; ambos comandos transcritos con su resultado |
| T-41-06-05 | Repudiation / false assurance | reportar 100% verde ocultando que 40 de 52 locks de `verification/` no corren en CI | mitigate | Hallazgo transversal reportado con números en la sección de bookkeeping; cuarta columna D-07 por fila (RESEARCH.md Pitfall 5) |
| T-41-06-SC | Tampering | instalaciones de paquetes | accept | Cero: ningún comando invoca `uv add`, `npm`, `pip` ni `cargo` |

**No aplican:** Spoofing, Denial of Service, ASVS V2/V3/V4/V6 — el plan edita un markdown y re-corre
suites locales existentes. ASVS V5 no aplica: no se parsea input no confiable. ASVS V14 sí aplica y se
atiende reportando el conteo de enforcement sin modificar la configuración de CI.
</threat_model>

<verification>
1. Tabla de 15 filas, suma de tokens exactamente 15, reparto 10 / 1 / 4.
2. Cinco celdas de enforcement marcadas como no cubiertas por CI, con motivo escrito.
3. `not_verifiable_retroactively: 4` en front-matter — el marcador del criterio 3b.
4. Cero tráfico de red; cero coincidencias del grep de higiene de credenciales.
5. `.github/workflows/ci.yml` sin modificar; `git status --porcelain verification/` vacío.
6. `git diff --quiet v1.7 HEAD -- . ':(exclude).planning'` sale 0.
</verification>

<success_criteria>
- Ninguna de las 15 filas queda sin disponer y ninguna lleva dos disposiciones.
- Las 4 filas que dependen de red viva, ventana de mercado o sign-off humano quedan no re-verificables, con su evidencia parcial nombrada y su límite explicado.
- La fase declara en su propio front-matter que conserva ítems no re-verificables, en vez de reportarse limpia.
- Ningún comando de este plan abrió un socket contra una API financiera.
- El hallazgo de 40 locks sin enrolar se reporta con números y se rutea a la Phase 45, sin tocar el archivo de workflow.
</success_criteria>

<output>
Create `.planning/phases/41-validaci-n-nyquist-retroactiva-de-v1-7/41-06-SUMMARY.md` when done.
</output>
