---
phase: 05-matriz-verification
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - packages/matriz-client/tests/test_client.py
  - .planning/verification/matriz-client-findings.md
  - .planning/verification/schemas/matriz-client/
autonomous: false
requirements: [MATZ-01, MATZ-02, MATZ-05, MATZ-06, MATZ-07, DRIFT-02]
tags: [verification, matriz-client, live-run, verified-live, mock-only-mutation, MATZ-06, GET-as-write, sentinel, schema-snapshot]
must_haves:
  truths:
    - "packages/matriz-client/tests/test_client.py contiene sección `# ------ Verified live (Phase 5) ------` (divider verbatim) AGREGADA después de la sección `# ------ Regressions ------` (la sección Regressions vino en Plan 05-01 Task 1.4)"
    - "La sección Verified live contiene invariantes mockeados que lockean URLs exactas + envelope unwrap para los probes happy-path del driver — al menos 8 tests cubriendo segments/all_instruments/instrument_detail/market_data/trades/active_orders/positions + 1 sentinel del market-hours guard (D-MATZ-5 LA.date staleness shape-only assertion)"
    - "La sección Verified live también lockea MATZ-05: 3 tests mockeados con `{'status':'ERROR'}` para bogus symbol / invalid account / malformed CFI verificando que el cliente levanta `PrimaryAPIError` con `e.status == 'ERROR'`"
    - "packages/matriz-client/tests/test_client.py contiene **MATZ-06 mock-only contract** D-MATZ-14/15/16: 5 tests de `new_order` cubriendo cada rama del param building (baseline LIMIT/DAY/price, MARKET sin price, iceberg+displayQty, GTD+expireDate, cancelPrevious=True) + 1 test de `replace_order` + 1 test de `cancel_order` + **3 sentinels GET-as-write quirk** (uno por función: new/replace/cancel) que asertan `request.method == 'GET'` con docstring citando §6.3 spec. Total MATZ-06: **11 tests mock-only**"
    - "Los 3 sentinel tests GET-as-write usan docstring verbatim: `\"\"\"GET-as-write quirk: Primary API mandates GET for order mutations (§6.3). Never refactor to POST without explicit API confirmation — this test breaks if anyone changes the method.\"\"\"`"
    - "Cada test mock-only usa pytest-httpx `httpx_mock.add_response(url=<full URL with query string verbatim>, method='GET', json={'order': {...}})` y verifica el return value del cliente"
    - "**Live run ejecutado** contra `https://api.remarkets.primary.com.ar` con credenciales del operador en `.env`: `uv run --package matriz-client python main_matriz.py` produce stdout con ~25 PROBE lines + SUMMARY"
    - "**Live run produce**: `.planning/verification/matriz-client-findings.md` con findings clasificados (SHAPE / AUTH / ERROR-MAP / NO-DATA / etc.) + ART run-context + finding EXPECTED terminal D-MATZ-27 prod-vs-remarkets"
    - "**Live run produce**: ~11-19 archivos en `.planning/verification/schemas/matriz-client/<func>.json` (rango variable según env vars opt-in — Assumption A4 RESEARCH L981) con envelope D-21 (endpoint, client_function, captured_at, base_url, sample_params, schema) — todos PII-free via `schema_of`"
    - "Operator captura SUMMARY del live run: PASS / FAIL / SKIPPED / FINDING counts emitidos por el driver"
    - "Operator clasifica manualmente cada finding OPEN del live run (CONFIRMED / NO-FIX / EXPECTED / etc.) — los CONFIRMED requieren regression test en Plan 05-04 cycle closure"
    - "Suite mockeada completa pasa: `uv run pytest packages/matriz-client -q` verde con 19 regression tests (Plan 05-01) + N Verified-live invariants + 11 MATZ-06 mock-only = tests count >= 50"
    - "mypy strict + ruff check + ruff format verdes en todo el repo tras los appends"
  artifacts:
    - path: "packages/matriz-client/tests/test_client.py"
      provides: "Test file with full Phase 5 sections: pre-existing tests + Regressions (19 from Plan 05-01) + Verified-live (Phase 5) invariants + MATZ-06 mock-only contract (11 tests)"
      contains: "# ------ Verified live (Phase 5) ------"
      contains_also: "§6.3"
      contains_also_2: "request.method == \"GET\""
    - path: ".planning/verification/matriz-client-findings.md"
      provides: "Phase 5 findings file generated by live run + EXPECTED terminal D-MATZ-27 + operator-classified bugs"
      contains: "# Findings: matriz-client"
      contains_also: "prod-vs-remarkets divergence acknowledged"
    - path: ".planning/verification/schemas/matriz-client/get-segments.json"
      provides: "Baseline schema snapshot for get_segments (D-21 envelope)"
      contains: "endpoint"
      contains_also: "client_function"
      contains_also_2: "schema"
  key_links:
    - from: "packages/matriz-client/tests/test_client.py::Verified live (Phase 5)"
      to: "matriz_client.client::_unwrap"
      via: "URL invariant + envelope unwrap structural verification"
      pattern: "missing envelope key|envelope"
    - from: "packages/matriz-client/tests/test_client.py::MATZ-06 mock-only"
      to: "matriz_client.client::new_order/replace_order/cancel_order"
      via: "httpx_mock + assert request.method == 'GET'"
      pattern: "method == \"GET\""
    - from: ".planning/verification/matriz-client-findings.md"
      to: "verification/findings.py::append_finding"
      via: "Driver invoca append_finding durante el live run; este archivo es el output committeable"
      pattern: "## Detalle por hallazgo"
    - from: ".planning/verification/schemas/matriz-client/"
      to: "verification/schema.py::schema_of"
      via: "Driver invoca schema_of(raw_payload) durante el live run para ~11-19 archivos"
      pattern: "schema"
    - from: "main_matriz.py live run"
      to: "verify_cycle_closure × 4 packages"
      via: "Plan 05-02 driver invoca cycle closure check — emite findings ERROR-MAP OPEN si faltan regressions"
      pattern: "cycle_closure"
---

<objective>
**Ejecutar el live run** de Phase 5 contra `https://api.remarkets.primary.com.ar` y **lockear las invariantes descubiertas** con tres outputs:

1. **Append `# ------ Verified live (Phase 5) ------` section** a `packages/matriz-client/tests/test_client.py` con invariantes mockeados que lockean URLs exactas + envelope unwrap + market-hours guard shape + MATZ-05 error mapping (esto SE HACE ANTES del live run para que el live run los ejercite estructuralmente).

2. **Append MATZ-06 mock-only contract** con 11 tests verbatim D-MATZ-14/15/16: 5 new_order + 1 replace_order + 1 cancel_order + 3 sentinels GET-as-write quirk (esto SE HACE ANTES del live run; MATZ-06 NUNCA toca live por diseño — REQUIREMENTS.md Out of Scope explícito).

3. **Live run con `uv run --package matriz-client python main_matriz.py`** contra remarkets, observando el stdout (~25 PROBE lines + SUMMARY) y produciendo:
   - `.planning/verification/matriz-client-findings.md` (con EXPECTED terminal D-MATZ-27)
   - `.planning/verification/schemas/matriz-client/*.json` (~11-19 archivos)
   - Clasificación operator-driven de findings OPEN → CONFIRMED / NO-FIX / EXPECTED

El plan tiene **`autonomous: false`** porque incluye un **checkpoint human-verify** para el live run (D-MATZ-30 sin asyncio — el operator ejecuta manualmente y reporta resultado). Los pasos pre-live run son auto. El live run + clasificación de findings es human-verify.

Purpose:
- Sin Verified-live + MATZ-06 mock-only **antes** del live run, no hay safety net contra cambios accidentales en el cliente que rompan el contract con Primary
- El live run es la única forma de descubrir divergencias entre cliente sync y la API real de Primary/remarkets (MATZ-01, MATZ-02 cubiertos solo via live)
- MATZ-06 mock-only contract es el **lock final del GET-as-write quirk** (D-MATZ-16 sentinels) — sin estos tests, un contributor futuro podría "corregir" GET→POST por intuición y romper el cliente silenciosamente
- DRIFT-02 (DRIFT cierre del ciclo) requiere que el driver invoque `verify_cycle_closure × 4 pkgs` durante el live run — emite findings ERROR-MAP OPEN si algún CONFIRMED histórico carece de regression test
- EXPECTED terminal D-MATZ-27 anchora el gap prod-vs-remarkets como handoff documentado para milestone futuro

Output: test_client.py con secciones Verified-live + MATZ-06 mock-only completas; findings file matriz-client-findings.md poblado; ~11-19 schemas committeables; operator-classified findings; suite verde con >= 50 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-matriz-verification/05-CONTEXT.md
@.planning/phases/05-matriz-verification/05-RESEARCH.md
@.planning/phases/05-matriz-verification/05-PATTERNS.md
@.planning/phases/05-matriz-verification/05-VALIDATION.md
@.planning/phases/05-matriz-verification/05-01-PLAN.md
@.planning/phases/05-matriz-verification/05-02-PLAN.md
@.planning/phases/04-higyrus-verification/04-03-PLAN.md
@.planning/phases/04-higyrus-verification/04-03-SUMMARY.md
@.planning/phases/03-iol-verification/03-03-PLAN.md
@.planning/phases/02-mbito-verification/02-03-PLAN.md
@.planning/verification/FINDINGS-TEMPLATE.md
@.planning/verification/higyrus-client-findings.md
@.planning/verification/schemas/higyrus-client/get-health.json
@packages/matriz-client/src/matriz_client/client.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/models.py
@packages/matriz-client/tests/test_client.py
@packages/matriz-client/tests/conftest.py
@main_matriz.py
@verification/findings.py
@verification/schema.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Append `# ------ Verified live (Phase 5) ------` section to test_client.py with invariants (URL exact + envelope + market-hours shape + MATZ-05 error mapping)</name>
  <files>packages/matriz-client/tests/test_client.py</files>
  <read_first>
    - packages/matriz-client/tests/test_client.py (POST Plan 05-01: contiene `# ------ Regressions ------` con 18 envelope tests + 1 _token sentinel. Esta task agrega `# ------ Verified live (Phase 5) ------` ANTES de la sección Regressions O después; el patrón Phase 4 (higyrus) coloca Verified-live ANTES de Regressions. Verificar al leer test_client.py de higyrus para confirmar el orden)
    - packages/higyrus-client/tests/test_client.py (Phase 4 con secciones `# ------ Verified live (Phase 4) ------` + `# ------ Regressions ------` — verificar el orden exacto, contenido tipo, y patrones de assertion)
    - packages/matriz-client/src/matriz_client/client.py (POST Plan 05-01: 18 endpoints refactoreados con `_unwrap` + Risk API)
    - packages/matriz-client/tests/conftest.py (autouse fixtures — precarga `_token` + `base_url="https://api.test"`)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-34 dos secciones verbatim; D-MATZ-5 market-hours guard pattern; D-MATZ-23 distinción HTTP 4xx vs status=ERROR)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 7 verbatim L948-1006 con divider verbatim + test patterns)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 1 URL invariant L388-401)
    - .planning/codebase/TESTING.md (pytest-httpx `httpx_mock.add_response(url=..., method=..., json=...)` patterns)
  </read_first>
  <behavior>
    - `packages/matriz-client/tests/test_client.py` tiene una sección nueva `# ------ Verified live (Phase 5) ------` con N invariantes mockeados que lockean el comportamiento del cliente para los probes del driver. Decisión de ubicación: **AGREGADA AL FINAL del archivo**, después de la sección Regressions creada en Plan 05-01 (consistente con la regla "appends at the end" de los planes pre-existentes; verificar al leer test_client.py de higyrus para confirmar)
    - Cada Verified-live test es **mockeado** (sin live calls), con docstring `"""Verified Phase 5: <invariante> (finding F-NN si rompe)."""`
    - **8+ tests happy-path verifican URL exacta + envelope unwrap correcto + return value tipo** para los probes core: get_segments, get_all_instruments, get_instruments_details, get_instrument_detail, get_instruments_by_segment, get_market_data, get_trades, get_positions (Risk API con HTTP Basic)
    - **1 sentinel market-hours guard test (D-MATZ-5)**: mock `get_market_data` con `LA.date` artificially stale (epoch ms > 2h del current time); verifica que el cliente NO levanta error (PASS-shape), retorna `MarketDataSnapshot` con `LA.date` correcto (sin assert sobre el valor de `LA.price`)
    - **3 tests MATZ-05 error mapping**: mock cada endpoint con `{'status': 'ERROR', 'description': '...'}`; verifica que el cliente levanta `PrimaryAPIError(status='ERROR', ...)` con la description capturada
    - mypy strict + ruff verdes
    - Suite pre-existente (Plan 05-01 + tests originales) sigue pasando
  </behavior>
  <action>
    Modificar `packages/matriz-client/tests/test_client.py` agregando AL FINAL del archivo (después de la sección `# ------ Regressions ------` de Plan 05-01) la sección nueva con divider verbatim y N tests.

    **Divider verbatim:**
    ```python
    # ------ Verified live (Phase 5) ------
    ```

    **Estructura de cada test**: docstring + httpx_mock setup + call + asserts.

    **A. Invariantes happy-path (8 tests)** — uno por endpoint core:

    1. `test_get_segments_url_invariant_phase5(httpx_mock: HTTPXMock) -> None`
       - Docstring: `"""Verified Phase 5: URL exacta + envelope unwrap segments (finding F-NN si rompe)."""`
       - Mock: `httpx_mock.add_response(url="https://api.test/rest/segment/all", method="GET", json={"status": "OK", "segments": [{"marketSegmentId": "DDF", "marketId": "ROFX"}]})`
       - Call: `result = matriz_client.get_segments()`
       - Asserts:
         - `len(result) == 1`
         - `result[0].marketSegmentId == "DDF"`
         - `result[0].marketId == "ROFX"`

    2. `test_get_all_instruments_url_invariant_phase5`
       - Mock URL: `"https://api.test/rest/instruments/all"`
       - Mock JSON: `{"status": "OK", "instruments": [{"instrumentId": {"symbol": "DLR/JUN26", "marketId": "ROFX"}, "cficode": "ESXXXX"}]}`
       - Asserts: `len(result) == 1`, `result[0].instrumentId.symbol == "DLR/JUN26"`

    3. `test_get_instruments_details_url_invariant_phase5`
       - URL: `"https://api.test/rest/instruments/details"`
       - JSON: `{"status": "OK", "instruments": [<InstrumentDetail-shape minimal dict>]}`

    4. `test_get_instrument_detail_url_invariant_phase5`
       - URL con query string: `"https://api.test/rest/instruments/detail?symbol=X&marketId=ROFX"` (o el order de params que el cliente emite; opcionalmente omitir url=)
       - Call: `matriz_client.get_instrument_detail("X")`
       - JSON: `{"status": "OK", "instrument": <InstrumentDetail-shape minimal dict>}`

    5. `test_get_instruments_by_segment_url_invariant_phase5`
       - URL: con query string del segment param
       - Call: `matriz_client.get_instruments_by_segment(<args>)` (verificar firma al leer client.py)
       - JSON: `{"status": "OK", "instruments": [...]}`

    6. `test_get_market_data_url_invariant_phase5`
       - URL con query string verbatim del cliente (entries, depth, marketId, symbol)
       - Call: `matriz_client.get_market_data("X")`
       - JSON: `{"status": "OK", "marketData": {"BI": [], "OF": [], "LA": {"price": 100.0, "size": 1, "date": <current epoch_ms>}, "SE": None}}`
       - Asserts: `result.LA.price == 100.0`, `result.LA.date == <current epoch_ms>`

    7. `test_get_trades_url_invariant_phase5`
       - URL con date_from/date_to query string
       - Call: `matriz_client.get_trades("X", dt.date(2026, 5, 1), dt.date(2026, 5, 7))` (verificar firma)
       - JSON: `{"status": "OK", "trades": [{"symbol": "X", "price": 100.0, "size": 1, "datetime": ...}]}`

    8. `test_get_positions_url_invariant_phase5`
       - URL: `"https://api.test/rest/risk/position/getPositions/ACC"`
       - Call: `matriz_client.get_positions("ACC")` — usa HTTP Basic en el cliente, pero pytest-httpx no chequea auth headers por default
       - JSON: `{"status": "OK", "positions": [{"symbol": "X", "buySize": 1, "sellSize": 0, ...}]}`

    **B. Market-hours guard sentinel (1 test)** — D-MATZ-5:

    ```python
    def test_get_market_data_returns_snapshot_with_stale_LA_date(httpx_mock: HTTPXMock) -> None:
        """Verified Phase 5: market-hours guard (D-MATZ-5) — el cliente retorna MarketDataSnapshot con LA.date sin levantar error aunque sea stale; el driver decide si asertar valor (finding F-NN)."""
        # LA.date artificially stale (3h ago)
        stale_date_ms = int((time.time() - 3 * 3600) * 1000)
        httpx_mock.add_response(
            method="GET",
            json={
                "status": "OK",
                "marketData": {
                    "BI": [],
                    "OF": [],
                    "LA": {"price": 100.0, "size": 1, "date": stale_date_ms},
                    "SE": None,
                },
            },
        )
        result = matriz_client.get_market_data("X")
        # El cliente NO levanta error por staleness — eso es responsabilidad del driver
        assert result.LA.date == stale_date_ms
        assert result.LA.price == 100.0
    ```

    Importar `import time` al tope del archivo si no estaba.

    **C. MATZ-05 error mapping tests (3 tests)**:

    ```python
    def test_get_market_data_raises_primary_api_error_on_status_error(
        httpx_mock: HTTPXMock,
    ) -> None:
        """Verified Phase 5: MATZ-05 — {'status':'ERROR'} → PrimaryAPIError(status='ERROR') para bogus symbol (finding F-NN)."""
        httpx_mock.add_response(
            method="GET",
            json={
                "status": "ERROR",
                "description": "bogus symbol ZZZZZZ-NOT-A-SYMBOL not found",
                "message": None,
            },
        )
        with pytest.raises(PrimaryAPIError) as exc_info:
            matriz_client.get_market_data("ZZZZZZ-NOT-A-SYMBOL")
        assert exc_info.value.status == "ERROR"
        assert "bogus" in (exc_info.value.description or "")
    ```

    Idem para `test_get_active_orders_raises_primary_api_error_on_invalid_account` y `test_get_instruments_by_cfi_raises_primary_api_error_on_malformed_cfi` (los 3 escenarios D-MATZ-22).

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - Mantener `from __future__ import annotations` línea 1
    - mypy strict pasa
    - ruff line-length=100, double quotes, 4 espacios
    - NO modificar tests pre-existentes (los 19 del Plan 05-01 + los originales siguen verdes)
    - Imports adicionales al tope si faltan: `import time` (para market-hours sentinel), `from typing import cast` (si los tests usan CFICode literal), confirmar `dt` (datetime) ya importado
    - El divider verbatim `# ------ Verified live (Phase 5) ------` va en una sola línea, sin extra spaces
    - Los Verified-live tests son **mockeados** — NO se ejecutan contra live API en este test file
    - Docstring formato: `"""Verified Phase 5: <descripción> (finding F-NN si rompe)."""` — el `F-NN` es placeholder, se reemplaza cuando se asigna el fid concreto en Plan 05-04

    Commit sugerido: `test(05-03): append Verified live (Phase 5) section with URL invariants + market-hours sentinel + MATZ-05 error mapping`
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/matriz-client -q</automated>
    <automated>uv run mypy packages/matriz-client</automated>
    <automated>uv run ruff check packages/matriz-client/tests/test_client.py</automated>
    <automated>uv run ruff format --check packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 5) ------" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "^def test_get_.*_url_invariant_phase5" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "_raises_primary_api_error_on_status_error\|_raises_primary_api_error_on_invalid_account\|_raises_primary_api_error_on_malformed_cfi" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "test_get_market_data_returns_snapshot_with_stale_LA_date" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "Verified Phase 5" packages/matriz-client/tests/test_client.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/matriz-client/tests/test_client.py` contiene `# ------ Verified live (Phase 5) ------` (divider verbatim, 1 línea)
    - Source: contiene 8 tests happy-path con sufijo `_url_invariant_phase5` (uno por endpoint listado)
    - Source: contiene `test_get_market_data_returns_snapshot_with_stale_LA_date` (market-hours sentinel D-MATZ-5)
    - Source: contiene los 3 tests MATZ-05 con prefijo `test_<endpoint>_raises_primary_api_error_on_*` (al menos 3 nuevos: bogus symbol, invalid account, malformed CFI)
    - Source: docstring `Verified Phase 5:` aparece >= 12 veces (uno por test nuevo de esta sección)
    - Source: imports al tope incluyen `import time` (para market-hours sentinel) si no estaba
    - Source: tests pre-existentes (originales + 19 de Plan 05-01) NO modificados
    - Source: preserva `from __future__ import annotations` línea 1
    - Behavior: `grep -F "# ------ Verified live (Phase 5) ------" test_client.py` retorna 1 línea
    - Behavior: `grep -c "^def test_get_.*_url_invariant_phase5" test_client.py` >= 8
    - Behavior: `grep -c "Verified Phase 5" test_client.py` >= 12
    - Behavior: `uv run pytest packages/matriz-client/tests/test_client.py -v` reporta TODOS los tests nuevos en PASSED, sin failures
    - Behavior: `uv run pytest packages/matriz-client -q` exits 0
    - Static: `uv run mypy packages/matriz-client` exits 0
    - Static: `uv run ruff check packages/matriz-client/tests/test_client.py` exits 0
    - Static: `uv run ruff format --check packages/matriz-client/tests/test_client.py` exits 0
    - Non-regression: tests pre-existentes (regressions + originales) siguen verdes
  </acceptance_criteria>
  <done>Sección Verified live (Phase 5) agregada con 8+ URL invariants + 1 market-hours sentinel D-MATZ-5 + 3 MATZ-05 error mapping = 12+ tests nuevos. Suite verde. mypy + ruff verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2: Append MATZ-06 mock-only contract — 5 new_order + 1 replace_order + 1 cancel_order + 3 sentinels GET-as-write = 11 tests (D-MATZ-14, D-MATZ-15, D-MATZ-16)</name>
  <files>packages/matriz-client/tests/test_client.py</files>
  <read_first>
    - packages/matriz-client/tests/test_client.py (POST Task 3.1: contiene Verified live (Phase 5). Esta task agrega los 11 MATZ-06 tests AL FINAL del archivo en una sub-sección dedicada — decidir el divider: agregar como continuación de Verified live o como sub-sección `# ------ MATZ-06 mock-only contract ------` para visibilidad. Recomendación: continuación de Verified live, con docstrings explícitos que marcan "MATZ-06 mock-only contract")
    - packages/matriz-client/src/matriz_client/client.py (POST Plan 05-01: docstrings warning §6.3 ya agregados a new_order/replace_order/cancel_order; firma exacta de cada función para construir los 5 escenarios de new_order)
    - packages/matriz-client/tests/test_client.py (verificar tests pre-existentes `test_new_order_builds_params` líneas ~153-184 y `test_new_order_omits_optional_fields` — los tests pre-existentes cubren el param building base; los nuevos MATZ-06 cubren las ramas individuales completas)
    - .planning/phases/05-matriz-verification/05-CONTEXT.md (D-MATZ-14 verbatim los 5 escenarios; D-MATZ-15 replace/cancel; D-MATZ-16 sentinel GET-quirk con docstring verbatim)
    - .planning/phases/05-matriz-verification/05-PATTERNS.md (sección 7 MATZ-06 tests L1012-1047 con código verbatim)
    - .planning/phases/05-matriz-verification/05-RESEARCH.md (Pattern 5 MATZ-06 Mock-Only Contract L564-593; sentinel test verbatim L573-593)
  </read_first>
  <behavior>
    - `packages/matriz-client/tests/test_client.py` contiene 11 tests nuevos del MATZ-06 mock-only contract:
      - **5 new_order tests**: cada uno mockea URL con query string verbatim diferente correspondiente a la rama del param building (LIMIT baseline, MARKET sin price, iceberg+displayQty, GTD+expireDate, cancelPrevious=True), llama `matriz_client.new_order(...)` con args correspondientes, verifica el `NewOrderResponse` returned
      - **1 replace_order test**: URL verbatim de `/rest/order/replaceById?...`, envelope `["order"]`, return `NewOrderResponse(clientId, proprietary)`
      - **1 cancel_order test**: URL verbatim de `/rest/order/cancelById?...`, envelope `["order"]`, return `NewOrderResponse`
      - **3 sentinels GET-as-write quirk**: uno por función (new/replace/cancel) con docstring verbatim D-MATZ-16 que cita §6.3, asertando `request.method == "GET"`
    - Cada sentinel test usa `[request] = httpx_mock.get_requests()` para obtener el request emitido y aserta el método HTTP
    - Suite mockeada total tras este task: tests pre-existentes + 19 regression (Plan 05-01) + 12+ Verified-live (Task 3.1) + 11 MATZ-06 (esta task) >= 50 tests total
    - mypy strict + ruff verdes
    - Tests pre-existentes (incluyendo `test_new_order_builds_params`/`test_new_order_omits_optional_fields`) NO modificados — quedan como cobertura complementaria
  </behavior>
  <action>
    Modificar `packages/matriz-client/tests/test_client.py` agregando AL FINAL del archivo (después de la sección Verified-live de Task 3.1) **11 tests** del MATZ-06 mock-only contract.

    **Estructura de tests:**

    **A. 5 new_order tests (D-MATZ-14)** — cada uno cubre una rama distinta del param building. URL exact con query string verbatim:

    1. `test_new_order_baseline_limit_day_with_price(httpx_mock: HTTPXMock) -> None`
       - Docstring: `"""MATZ-06 mock-only contract: new_order baseline LIMIT/DAY con price set y defaults (D-MATZ-14 #1)."""`
       - URL verbatim: `"https://api.test/rest/order/newSingleOrder?marketId=ROFX&symbol=X&side=BUY&orderQty=1&ordType=LIMIT&timeInForce=DAY&account=ACC&cancelPrevious=False&iceberg=False&price=100.0"` (verificar al leer client.py el orden exacto de los params en el query string; ajustar si difiere)
       - Mock: `httpx_mock.add_response(url=<URL>, method="GET", json={"status": "OK", "order": {"clientId": "C1", "proprietary": "P1"}})`
       - Call: `result = matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0)`
       - Asserts: `result.clientId == "C1"`, `result.proprietary == "P1"`, `isinstance(result, NewOrderResponse)`

    2. `test_new_order_market_without_price`
       - Docstring: `"""MATZ-06 mock-only: MARKET sin price (omite 'price' del query string, D-MATZ-14 #2)."""`
       - URL: omite el parámetro `price=` y `ordType=MARKET` en su lugar
       - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", order_type="MARKET")` (verificar firma; price=None default omitido)

    3. `test_new_order_with_iceberg_and_display_qty`
       - URL: incluye `iceberg=True&displayQty=10`
       - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0, iceberg=True, display_qty=10)`

    4. `test_new_order_with_expire_date_and_gtd`
       - URL: incluye `timeInForce=GTD&expireDate=20261231`
       - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0, time_in_force="GTD", expire_date="20261231")`

    5. `test_new_order_with_cancel_previous_true`
       - URL: incluye `cancelPrevious=True`
       - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0, cancel_previous=True)`

    **B. 1 replace_order test (D-MATZ-15)**:

    `test_replace_order_url_invariant_and_envelope`
    - Docstring: `"""MATZ-06 mock-only: replace_order URL exacta + envelope ['order'] + NewOrderResponse return (D-MATZ-15)."""`
    - URL verbatim del cliente: `"https://api.test/rest/order/replaceById?clOrdId=C1&proprietary=P1&..."` (verificar al leer client.py la firma exacta — params requeridos pueden incluir `orderQty`, `price`, etc.)
    - Mock: `httpx_mock.add_response(url=<URL>, method="GET", json={"status": "OK", "order": {"clientId": "C2", "proprietary": "P2"}})`
    - Call: `result = matriz_client.replace_order(<args verbatim>)`
    - Asserts: `result.clientId == "C2"`, `result.proprietary == "P2"`

    **C. 1 cancel_order test (D-MATZ-15)**:

    `test_cancel_order_url_invariant_and_envelope`
    - Docstring: `"""MATZ-06 mock-only: cancel_order URL exacta + envelope ['order'] + NewOrderResponse return (D-MATZ-15)."""`
    - URL: `"https://api.test/rest/order/cancelById?clOrdId=C1&proprietary=P1"`
    - Mock JSON: `{"status": "OK", "order": {"clientId": "C1", "proprietary": "P1"}}`
    - Call: `result = matriz_client.cancel_order("C1", "P1")`
    - Asserts: `result.clientId == "C1"`, `result.proprietary == "P1"`

    **D. 3 sentinels GET-as-write quirk (D-MATZ-16)** — verbatim:

    `test_new_order_uses_GET_method_per_primary_api_quirk(httpx_mock: HTTPXMock) -> None`
    ```
    """GET-as-write quirk: Primary API mandates GET for order mutations (§6.3).

    Never refactor to POST without explicit API confirmation — this test
    breaks if anyone changes the method.
    """
    ```
    - Mock con URL verbatim (puede ser el mismo de test #1 o uno minimal)
    - JSON: `{"status": "OK", "order": {"clientId": "C", "proprietary": "P"}}`
    - Call: `matriz_client.new_order("X", "BUY", 1, "ACC", price=100.0)`
    - Asserts:
      - `[request] = httpx_mock.get_requests()` (un solo request emitido)
      - `assert request.method == "GET", "Primary API §6.3 mandates GET for order submission"`

    `test_replace_order_uses_GET_method_per_primary_api_quirk`
    - Docstring análogo citando §6.3
    - Setup análogo con replace_order

    `test_cancel_order_uses_GET_method_per_primary_api_quirk`
    - Docstring análogo
    - Setup análogo con cancel_order

    **Restricciones (CLAUDE.md / CONVENTIONS.md):**
    - mypy strict pasa
    - ruff line-length=100, double quotes, 4 espacios
    - NO modificar tests pre-existentes (`test_new_order_builds_params`, `test_new_order_omits_optional_fields`, etc.)
    - **CRITICAL — NUNCA hacer llamadas live**: TODOS los tests MATZ-06 son mockeados via httpx_mock. NO existe condición bajo la cual se invoca `new_order`/`replace_order`/`cancel_order` contra una URL real
    - El autouse `_configure_sync` precarga `_token` — los tests heredan sin modificación
    - Si el orden de params en query string es brittle, opcionalmente usar `httpx_mock.add_response(method="GET", json={...})` SIN `url=` (match por method) + asertar request URL via `httpx_mock.get_requests()[0].url`
    - Los 3 sentinel tests deben usar el docstring verbatim D-MATZ-16 que cita §6.3 y "Never refactor to POST"

    Commit sugerido: `test(05-03): append MATZ-06 mock-only contract (5 new_order + 1 replace + 1 cancel + 3 GET-quirk sentinels)`
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/matriz-client -q</automated>
    <automated>uv run mypy packages/matriz-client</automated>
    <automated>uv run ruff check packages/matriz-client/tests/test_client.py</automated>
    <automated>uv run ruff format --check packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "test_new_order_.*\(baseline_limit_day\|market_without_price\|with_iceberg\|with_expire_date\|with_cancel_previous\)" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "test_replace_order_url_invariant_and_envelope\|test_cancel_order_url_invariant_and_envelope" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "_uses_GET_method_per_primary_api_quirk" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "§6.3" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "request.method == \"GET\"" packages/matriz-client/tests/test_client.py</automated>
    <automated>grep -c "Never refactor to POST" packages/matriz-client/tests/test_client.py</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: contiene 5 tests new_order con sufijos `baseline_limit_day_with_price`, `market_without_price`, `with_iceberg_and_display_qty`, `with_expire_date_and_gtd`, `with_cancel_previous_true`
    - Source: contiene `test_replace_order_url_invariant_and_envelope`
    - Source: contiene `test_cancel_order_url_invariant_and_envelope`
    - Source: contiene 3 sentinels `_uses_GET_method_per_primary_api_quirk` (new_order, replace_order, cancel_order)
    - Source: el string `§6.3` aparece >= 3 veces (uno por sentinel docstring)
    - Source: el string `request.method == "GET"` aparece >= 3 veces (uno por sentinel assert)
    - Source: `Never refactor to POST` aparece >= 3 veces (uno por sentinel docstring; el cliente client.py también lo tiene 3 veces de Plan 05-01 — pero esta task se mide solo sobre test_client.py)
    - Source: NO modifica tests pre-existentes (`test_new_order_builds_params`, etc.)
    - Source: NO contiene llamadas live a `new_order`/`replace_order`/`cancel_order` (verificable: solo aparecen dentro de funciones `def test_*`)
    - Source: cada test usa `httpx_mock.add_response(... method="GET", json={"order": ...})` o similar (mock-only)
    - Behavior: `grep -c "test_new_order_\(baseline\|market\|with_\)" test_client.py` >= 5
    - Behavior: `grep -c "_uses_GET_method_per_primary_api_quirk" test_client.py` == 3
    - Behavior: `grep -c "§6.3" test_client.py` >= 3
    - Behavior: `grep -c 'request.method == "GET"' test_client.py` >= 3
    - Behavior: `uv run pytest packages/matriz-client/tests/test_client.py -v` reporta los 11 tests MATZ-06 nuevos en PASSED
    - Behavior: total tests en `test_client.py` >= 50 (pre-existentes + 19 Plan 05-01 + 12+ Task 3.1 + 11 esta task) — verificable via pytest count
    - Behavior: `uv run pytest -q` exits 0 (whole repo)
    - Static: `uv run mypy packages/matriz-client` exits 0
    - Static: `uv run ruff check packages/matriz-client/tests/test_client.py` exits 0
    - Static: `uv run ruff format --check packages/matriz-client/tests/test_client.py` exits 0
  </acceptance_criteria>
  <done>MATZ-06 mock-only contract completo: 5 new_order + 1 replace + 1 cancel + 3 sentinels GET-quirk = 11 tests. Suite mockeada total ≥ 50 tests. NUNCA invoca live; mypy + ruff verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.3: Execute live run of main_matriz.py against remarkets (MATZ-01, MATZ-02, MATZ-05, MATZ-07, DRIFT-02)</name>
  <what-built>
    Plan 05-01 y 05-02 entregaron:
    - `verification/safemodel_diff.py` + `verification/cycle_report.py` (helpers nuevos)
    - `packages/matriz-client/src/matriz_client/client.py` con `_unwrap` helper + 18 sites refactoreados + `_token` raise + docstrings §6.3
    - `main_matriz.py` reescrito (~800-900 líneas, ~25 probes nombrados sync-only)
    - `main_higyrus.py` refactoreado consumiendo helper promovido
    - `packages/matriz-client/.env.example` con 5 vars opt-in

    Plan 05-03 Tasks 3.1 y 3.2 agregaron:
    - `# ------ Verified live (Phase 5) ------` con 12+ tests (URL invariants + market-hours sentinel + MATZ-05 error mapping)
    - 11 tests MATZ-06 mock-only contract (5 new_order + 1 replace + 1 cancel + 3 sentinels GET-quirk con docstring §6.3)

    Ahora corresponde **ejecutar el live run** contra remarkets para producir findings + schemas committeables. El operator ejecuta el driver manualmente y reporta resultado (no se ejecuta automáticamente desde el agente Claude porque requiere credenciales reales en `.env`).
  </what-built>
  <how-to-verify>
    **PRE-CHECK (responsabilidad del operator antes del run):**

    1. Confirmar que `packages/matriz-client/.env` (gitignored) tiene credenciales reales:
       - `PRIMARY_USER=<your-user>`
       - `PRIMARY_PASSWORD=<your-password>`
       - `PRIMARY_BASE_URL=https://api.remarkets.primary.com.ar` (sandbox SOLO — NO prod)
       - Opcional: `PRIMARY_ACCOUNT=<your-account>` (activa los 6 probes account-scoped)
       - Opcional: `MATRIZ_SAMPLE_SYMBOL`, `MATRIZ_SAMPLE_CL_ORD_ID`, `MATRIZ_SAMPLE_PROPRIETARY`, `MATRIZ_SAMPLE_EXEC_ID` (activan los 3 probes ID-scoped)

    2. **CRITICAL** — confirmar que `PRIMARY_BASE_URL` contiene `remarkets` (D-MATZ-33). Si por accidente apunta a prod, el driver aborta con `ABORT: ...` — no toca prod.

    3. Confirmar que la suite mockeada está verde antes del live run:
       ```bash
       uv run pytest packages/matriz-client -q
       uv run pytest -q   # whole repo
       ```

    **EJECUTAR EL LIVE RUN:**

    ```bash
    cd /Users/sebadlf/development/becerra/market-libs
    uv run --package matriz-client python main_matriz.py 2>&1 | tee live-run-matriz-$(date +%Y%m%d-%H%M%S).log
    ```

    Esto:
    - Carga credenciales desde `packages/matriz-client/.env`
    - Ejecuta ~25 probes en orden D-MATZ-29 contra `https://api.remarkets.primary.com.ar`
    - Genera/actualiza `.planning/verification/matriz-client-findings.md` via `append_finding`
    - Genera/actualiza ~11-19 schemas en `.planning/verification/schemas/matriz-client/`
    - Emite stdout con ~25 PROBE lines + SUMMARY line
    - Invoca `verify_cycle_closure × 4 pkgs` al final
    - Append finding EXPECTED terminal D-MATZ-27 prod-vs-remarkets

    **VERIFICAR EL OUTPUT:**

    1. **Stdout esperado** (~25 PROBE lines + SUMMARY):
       ```
       PROBE login_sync: PASS token obtenido en 0.45s
       PROBE get_segments: PASS 12 segments
       PROBE get_all_instruments: PASS 1500 instrumentos
       ...
       PROBE field_type_map: <PASS o FINDING F-NN (OPEN) — depende del live run>
       PROBE error_bogus_symbol: PASS PrimaryAPIError as expected: ...
       PROBE error_invalid_account: PASS ...
       PROBE error_malformed_cfi: PASS ...
       PROBE schema_snapshot: PASS N snapshots OK
       PROBE cycle_closure_ambito_financiero_client: PASS
       PROBE cycle_closure_iol_client: PASS
       PROBE cycle_closure_higyrus_client: PASS
       PROBE cycle_closure_matriz_client: PASS|FAIL
       SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N
       ```

    2. **Confirmar redaction**: ningún PROBE line debe contener tokens, usernames, passwords completos. Si aparecen, falla T-5-09 mitigation — investigar.

    3. **Verificar archivos generados**:
       ```bash
       cat .planning/verification/matriz-client-findings.md | head -40    # ART block + Index
       ls -la .planning/verification/schemas/matriz-client/                # 11-19 archivos JSON
       ```

    4. **Confirmar EXPECTED terminal** en findings file:
       ```bash
       grep -c "prod-vs-remarkets divergence acknowledged" .planning/verification/matriz-client-findings.md
       # debe ser >= 1
       ```

    5. **Confirmar schemas committeables (PII-free)**:
       ```bash
       cat .planning/verification/schemas/matriz-client/get-segments.json | python -m json.tool | head -20
       # Debe contener: endpoint, client_function, captured_at, base_url, sample_params, schema
       # NO debe contener: valores reales de symbols, prices, accountIds (solo nombres de tipo)
       ```

    **CLASIFICAR FINDINGS DEL LIVE RUN:**

    Para cada finding emitido en status `OPEN`, el operator clasifica manualmente actualizando el findings file:
    - **CONFIRMED**: bug real detectado, requiere fix + regression test en Plan 05-04 cycle closure
    - **NO-FIX**: comportamiento esperado del server, no se fixea, pero queda documentado
    - **EXPECTED**: gap conocido (e.g., D-MATZ-27 prod-vs-remarkets es EXPECTED terminal — ya en el file)
    - Findings nuevos descubiertos durante el live run (e.g., un campo nuevo `wire-only` en algún modelo, una shape divergente, un error HTTP 4xx no mapeado): clasificar uno a uno

    **NOTAS IMPORTANTES:**

    - Si `cycle_closure_<pkg>: FAIL` para Phase 2/3/4 paquetes (Ámbito/IOL/Higyrus) — eso indica que los findings históricos de phases anteriores no tienen campo `regression:` poblado en el formato que `verify_cycle_closure` espera. Esto es esperado en el primer run y se aborda en Plan 05-04 (decisión: agregar campo retroactivo a los findings históricos o documentar como caveat en CYCLE-REPORT.md)
    - Si el driver imprime `PROBE get_market_data: PASS shape OK, value asserts skipped — stale LA.date` → market está cerrado. Es OK; el guard funcionó (D-MATZ-5)
    - Si el driver imprime `PROBE get_active_orders: SKIPPED (missing PRIMARY_ACCOUNT env var)` → el operator no seteo la var opcional. Es OK; SKIPPED selectivo (D-MATZ-3)
    - El live run NO debe tomar > 60s normalmente (~25 probes × ~1-2s cada uno + cycle closure parsing). Si toma > 5min, hay un loop/retry indebido — investigar
  </how-to-verify>
  <resume-signal>
    Type "approved" si:
    - El live run completó sin crashes inesperados (NO importa si hay PROBE FAIL/FINDING — esos son findings legítimos del ciclo)
    - El SUMMARY line se emitió correctamente
    - El findings file existe con al menos 1 finding (mínimo el EXPECTED terminal D-MATZ-27)
    - Los schemas existen en `.planning/verification/schemas/matriz-client/` (al menos 1 archivo si solo PASS-aron probes minimal; idealmente 11-19)
    - No se exfiltraron credenciales en stdout (verificar con `grep -E '(password|token=)' live-run-matriz-*.log` — debería estar limpio)

    Si fallas o necesitas reportar issues, describir:
    - Qué probe(s) fallaron con FINDING / FAIL
    - Cuáles findings se generaron y cómo los clasificaste
    - Si hubo necesidad de ajustes al driver (e.g., firma de algún endpoint matriz no coincide con la asunción del Plan 05-02)
    - Cualquier blocker que requiera Plan 05-04 antes de cerrar
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `packages/matriz-client/.env` (operator) → `main_matriz.py` runtime env | Credenciales reales viven en `.env` gitignored; driver carga via `python-dotenv` automáticamente; `safe_print(secrets=[user, password, _token])` redacta en stdout |
| `main_matriz.py` → Primary API `https://api.remarkets.primary.com.ar` | TLS via httpx + hostname assert D-MATZ-33 al inicio; NO posible ejecutar contra prod (`api.primary.com.ar`) por el ABORT guard |
| Live driver stdout → operator terminal → `live-run-matriz-*.log` file | Operator debe NO commitear el log file con secrets; `.gitignore` no cubre `*.log` por default — operator responsable de cleanup |
| Tests mock-only MATZ-06 → fixtures pytest-httpx (test_client.py) | Tests NUNCA ejecutan live; URL "https://api.test" es sentinel inalcanzable; sentinel asserts `request.method == "GET"` defensive contra refactor accidental |
| `.planning/verification/matriz-client-findings.md` → committed git | Findings file contiene descripciones de bugs + schemas; verificar que NO contiene PII / tokens / passwords antes del commit (operator responsabilidad) |
| `.planning/verification/schemas/matriz-client/*.json` → committed git | Schemas son PII-free por construcción (`schema_of` reemplaza valores por nombres de tipo); committeable sin riesgo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-16 | Information Disclosure | Live driver stdout exfiltra token/credenciales | mitigate | D-MATZ-32 `safe_print(line, secrets=[user, password, _token])` con `_token` dinámico tras login. Acceptance: operator verifica `grep -E '(password\|token=)' live-run-*.log` retorna 0 líneas |
| T-5-17 | Tampering | Live driver ejecuta contra prod por config incorrecta | mitigate | D-MATZ-33 hostname assert al inicio de main(); `ABORT` con `sys.exit(1)` si `"remarkets" not in primary.client._base_url`. Pre-check del operator verifica `.env` antes del run |
| T-5-18 | Tampering | MATZ-06 sentinel test `request.method == "GET"` "corregido" a `"POST"` por contributor | mitigate | 3 sentinels (uno por new/replace/cancel) con docstring verbatim citando §6.3 + warning "Never refactor to POST". Cualquier cambio al método rompe los tests — el refactor IS el bug. Mirror Pitfall 4 RESEARCH L652-657 |
| T-5-19 | DoS | Live driver loops sobre probes que fallan, generando rate-limit en remarkets | mitigate | Anti-pattern explícito RESEARCH L603: single-shot driver, sin retries, sin loops. Cada probe single-shot. SKIPPED selectivo (D-MATZ-3/4) reduce probes innecesarios. Assumption A6: 3 error probes always-on son lookups read-only, riesgo marginal |
| T-5-20 | Information Disclosure | Findings file contiene valores reales del wire (e.g., accountId, symbols) | accept | Findings emiten conteos + shape descriptors (D-HIGY-2 discipline inherited Phase 4); NO valores. Schemas via `schema_of` son PII-free. Operator review pre-commit confirma. Matriz no tiene PII (accountId es ID interno tenant) |
| T-5-21 | Repudiation | Operator omite clasificar un finding OPEN → cycle closure FAIL en Plan 05-04 | mitigate | `verify_cycle_closure` detecta findings CONFIRMED/FIXED sin regression test path + emite finding ERROR-MAP OPEN. Plan 05-04 cierra el ciclo si todos clasificados. Mirror DRIFT-02 mechanism |
| T-5-22 | Tampering | Schema drift sobreescribe baseline en futuro run | mitigate | D-25 no-overwrite-on-drift verificado en Plan 05-02 (`_write_or_check_schema` helper). Si schema actual ≠ committed → finding SHAPE OPEN + NO sobreescribir. Operator clasifica drift como CONFIRMED/EXPECTED |
| T-5-SC | Tampering | npm/pip/cargo installs | accept | Plan 05-03 NO instala paquetes externos |

**Inherited threats from Phases 1-4 (still active):**
- T-4-08 (Tampering): `assert` stripped por `-O` — `_token` raise RuntimeError (Plan 05-01) y sentinel test (Plan 05-01) cubren matriz.
- T-3-04 (Information Disclosure): Tests usan valores sintéticos inline en mocks; el live run usa credenciales reales del operator pero pasa por `safe_print`.
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Verified live (Phase 5) section presente
grep -F "# ------ Verified live (Phase 5) ------" packages/matriz-client/tests/test_client.py
grep -c "Verified Phase 5" packages/matriz-client/tests/test_client.py    # >= 12

# 2. MATZ-06 mock-only contract presente (11 tests)
grep -c "_uses_GET_method_per_primary_api_quirk" packages/matriz-client/tests/test_client.py    # == 3
grep -c "§6.3" packages/matriz-client/tests/test_client.py    # >= 3
grep -c 'request.method == "GET"' packages/matriz-client/tests/test_client.py    # >= 3

# 3. Suite mockeada total >= 50 tests
uv run pytest packages/matriz-client/tests/test_client.py -v 2>&1 | tail -5

# 4. Static checks pre-live run
uv run mypy packages/matriz-client
uv run ruff check packages/matriz-client
uv run ruff format --check packages/matriz-client

# 5. Suite completa pre-live run
uv run pytest -q

# 6. Live run executed (manual operator step in Task 3.3)
# Output verifications:
test -f .planning/verification/matriz-client-findings.md
grep -c "prod-vs-remarkets divergence acknowledged" .planning/verification/matriz-client-findings.md    # >= 1
ls .planning/verification/schemas/matriz-client/ | wc -l    # 1 to 19 depending on env vars

# 7. No exfiltrated credentials
grep -E '(password|PRIMARY_PASSWORD=)' .planning/verification/matriz-client-findings.md    # == 0
grep -E '(password|PRIMARY_PASSWORD=)' .planning/verification/schemas/matriz-client/*.json    # == 0

# 8. Cycle closure status
grep -F "cycle_closure" .planning/verification/matriz-client-findings.md    # may or may not appear depending on if cycle_closure created findings (only fires if missing regressions)
```

Per Wave 3 merge:
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `packages/matriz-client/tests/test_client.py` con sección `# ------ Verified live (Phase 5) ------` con 12+ tests (URL invariants + market-hours sentinel + MATZ-05 error mapping)
- `packages/matriz-client/tests/test_client.py` con MATZ-06 mock-only contract: 5 new_order + 1 replace + 1 cancel + 3 sentinels GET-quirk = 11 tests
- Total suite mockeada matriz: >= 50 tests, todos PASSED
- mypy strict + ruff verdes
- Live run ejecutado contra `https://api.remarkets.primary.com.ar` con stdout verbatim PROBE/SUMMARY lines
- `.planning/verification/matriz-client-findings.md` generado con findings clasificados + EXPECTED terminal D-MATZ-27
- ~11-19 archivos en `.planning/verification/schemas/matriz-client/` con envelope D-21 PII-free
- Operator-classified findings (OPEN → CONFIRMED / NO-FIX / EXPECTED)
- NO exfiltración de credenciales en stdout / findings / schemas
- NO mutaciones live ejecutadas (MATZ-06 tests son mock-only por diseño)
- Suite completa post-live-run sigue verde
- Plan 05-04 puede ejecutar cycle closure con findings clasificados
</success_criteria>

<output>
Crear `.planning/phases/05-matriz-verification/05-03-SUMMARY.md` cuando done, registrando:
- Conteo final de tests en test_client.py (Verified-live + MATZ-06 + pre-existentes)
- Conteo final de findings emitidos por el live run (OPEN / CONFIRMED / NO-FIX / EXPECTED) por clase
- Conteo final de schema snapshots generados (incluyendo cuáles SKIPPED por env vars opt-in ausentes)
- Lista de findings CONFIRMED del live run (resumen por título + clase + surface)
- Lista de findings EXPECTED (incluye D-MATZ-27 prod-vs-remarkets)
- Resultado de cycle_closure × 4 pkgs (PASS/FAIL) — si FAIL para Phases 2-4 paquetes, anotar el caveat para Plan 05-04
- Confirmación de NO exfiltración de credenciales
- Confirmación de que el ABORT D-MATZ-33 funcionó cuando se probó (opcional — verificación de la mitigación)
- Cualquier ajuste hecho al driver durante el live run (e.g., firma de algún endpoint que difería)
- Métrica de duración del live run (target: < 60s; investigar si > 5min)
- Métrica de duración del plan
</output>