---
phase: 15-driver-migration-4-refac-05
plan: 04
type: tdd
wave: 4
depends_on: ["15-03"]
files_modified:
  - main_matriz.py
  - verification/test_main_matriz_uses_single_client_instance.py
  - .planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md
autonomous: true
requirements: [REFAC-05]
user_setup:
  - service: matriz-client (live, remarkets)
    why: "Per-package LIVE smoke at end of migration (D-11, Criterion #4) — TokenStore-sensitive"
    env_vars:
      - name: MATRIZ_USERNAME
        source: ".env in packages/matriz-client/ (never committed)"
      - name: MATRIZ_PASSWORD
        source: ".env in packages/matriz-client/ (never committed)"
    dashboard_config:
      - task: "Operator runs `uv run --package matriz-client python main_matriz.py` against remarkets and confirms exit 0 + no TokenStore corruption"
        location: "Local terminal (remarkets endpoint hostname-asserted in main())"

must_haves:
  truths:
    - "main_matriz.py main() constructs exactly one sync Client(); _async_main() constructs exactly one AsyncClient() — D-01/D-02"
    - "The sync `primary.client._base_url` hostname-safety read in main() (:2079) migrates to `client._state.base_url` — D-03 (module attr, not _state idiom)"
    - "All 6 async _state reads and the _ainvoke threading migrate to the single aclient — D-01/D-03"
    - "AST-guard test fails RED, passes GREEN — Criterion #1"
    - "STATIC git diff over committed *-findings.md shows ZERO title/fid/probe-name changes across ALL 4 drivers vs baseline 71bf201 — Criterion #2 (final gate)"
    - "Full pytest suite reports >=907 passing — Criterion #4 baseline preserved"
  artifacts:
    - path: "verification/test_main_matriz_uses_single_client_instance.py"
      provides: "AST-walker asserting <=2 Client/AsyncClient ctors in main_matriz.py"
      contains: "test_main_matriz_uses_single_client_instance"
      min_lines: 25
    - path: "main_matriz.py"
      provides: "Migrated driver — single sync + single async client; hostname-safety read on threaded instance"
      contains: "Client()"
    - path: ".planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md"
      provides: "STATIC title/fid-scoped diff result vs baseline 71bf201 + >=907-test attestation (Criterion #2/#4)"
      contains: "71bf201"
  key_links:
    - from: "main_matriz.py main() hostname assert"
      to: "the threaded sync client"
      via: "client._state.base_url replaces primary.client._base_url, then remarkets hostname check"
      pattern: "client\\._state\\.base_url"
    - from: "main_matriz.py _ainvoke async probes"
      to: "the single aclient"
      via: "aclient threaded through _ainvoke so aio.get_X resolves to aclient.get_X"
      pattern: "aclient"
---

<objective>
Migrate `main_matriz.py` (async-only `aio` surface, 22 async probes, 2283 LOC, TokenStore-sensitive) so `main()` builds one sync `Client()` and `_async_main()` builds one `AsyncClient()`. This is the riskiest driver: matriz TokenStore is a 3-way concurrency primitive, so >1 `AsyncClient` risks corruption (anti-Pitfall 1) — the ≤2-ctor gate is the mitigation. Migrate the matriz-specific `_ainvoke` lambda-wrapped async-call helper and the sync hostname-safety read. Author the AST-guard test FIRST (RED→GREEN). Then perform the FINAL cross-driver stability-gate verification and the ≥907-test attestation.

Purpose: Wave 4 — last in the serial order (D-11). Closes Criterion #2 (probe-name stability) and Criterion #4 (test baseline) for the whole phase.
Output: Migrated `main_matriz.py`, its AST-guard test, and `15-STABILITY-GATE.md`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-driver-migration-4-refac-05/15-CONTEXT.md
@.planning/phases/15-driver-migration-4-refac-05/15-PATTERNS.md
@.planning/phases/15-driver-migration-4-refac-05/15-03-SUMMARY.md
@.claude/skills/spike-findings-market-libs/SKILL.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — AST-guard test for main_matriz.py (Criterion #1)</name>
  <files>verification/test_main_matriz_uses_single_client_instance.py</files>
  <read_first>
    - verification/test_main_higyrus_uses_single_client_instance.py (Wave-3 sibling — copy idiom, retarget)
    - main_matriz.py:79-84 (current import block — `import matriz_client as primary` + `from matriz_client import ... aio`)
  </read_first>
  <behavior>
    - Test asserts `1 <= count <= 2` Client/AsyncClient ctor Calls in main_matriz.py.
    - RED: un-migrated driver count 0 → FAILS. GREEN: post-migration count==2 → PASSES.
    - Walker matches both ast.Name and ast.Attribute (D-05).
  </behavior>
  <action>
    Create `verification/test_main_matriz_uses_single_client_instance.py` as a copy of the higyrus AST test retargeted to `main_matriz.py` with test function `test_main_matriz_uses_single_client_instance`. Same dual ctor matching and `1 <= len(ctor_sites) <= 2` assertion. Reference `with_options` by concept only in any comment. Run RED first.
    <!-- planner-discipline-allow: with_options -->
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_matriz_uses_single_client_instance.py -x</automated>
  </verify>
  <acceptance_criteria>
    - RED before Task 2 (count 0). GREEN after (count==2).
    - Both ctor styles covered (grep `ast.Name` and `ast.Attribute`).
  </acceptance_criteria>
  <done>Test exists, RED→GREEN, both ctor styles matched.</done>
</task>

<task type="execute">
  <name>Task 2: Migrate main_matriz.py — one sync Client + one async AsyncClient; thread _ainvoke + hostname read (D-01/D-02/D-03)</name>
  <files>main_matriz.py</files>
  <read_first>
    - main_matriz.py (full driver — 2283 LOC; the file being modified)
    - 15-PATTERNS.md lines 292-343 (matriz recipe: import block, sync `_base_url` hostname read :2079-2080, 6 async _state sites :1468/:1524/:1606/:1779/:1855/:1931, the `_ainvoke` helper pattern :1524-1560 + call-sites, _async_main batch :2011-2047, TokenStore anti-Pitfall warning)
    - packages/matriz-client/src/matriz_client/__init__.py:112,117 (Client/AsyncClient exports)
    - .claude/skills/spike-findings-market-libs/SKILL.md (TokenStore 3-way concurrency — why >1 AsyncClient is dangerous)
  </read_first>
  <action>
    PIN constructor style: add `from matriz_client import Client, AsyncClient` (bare → ast.Name). In `main()` (around :2055) construct `client = Client()` once. Migrate the sync hostname-safety read: `primary.client._base_url` (:2079) → `client._state.base_url` (this is a PEP 562 module-attr read, NOT the `_state` idiom — it maps to `client._state.base_url` on the sync instance); keep the subsequent `if "remarkets" not in base:` assertion intact. Thread `client` into the sync public calls: `primary.login()` (:464), `primary.get_market_data(...)` (:1138), `primary.get_active_orders(...)` (:1216), `primary.get_instruments_by_cfi(...)` (:1295) → `client.get_X(...)`; add `client` param to each sync `def probe_*`.
    In `_async_main()` (:2011-2047) construct `aclient = AsyncClient()` ONCE at the top (CRITICAL: exactly one — TokenStore corruption risk if >1); thread it into all 22 async probes; change `await aio.aclose()` (:2046) → `await aclient.aclose()` in the existing finally/suppress.
    Migrate the 6 async `_state` reads (:1468,:1524,:1606,:1779,:1855,:1931) `aio._get_default()._state.base_url` → `aclient._state.base_url`.
    Migrate the `_ainvoke` helper (:1524-1560): thread `aclient` so the lambda-wrapped `aio.get_X` / `lambda: aio.get_X(args)` passed by each `probe_*_async` resolves to `aclient.get_X` / `lambda: aclient.get_X(args)`. Claude's Discretion (D-01 tail): choose signature-param-on-`_ainvoke`-plus-each-`probe_*_async` OR a closure capturing `aclient`, provided the AST gate counts exactly ONE `AsyncClient()`.
    DO NOT touch `title=`/`fid=`/`class_=`/probe-name literals in `append_finding(...)` (D-06). Sync and async clients SEPARATE (D-02); never share the sync client into the async coroutine.
  </action>
  <verify>
    <automated>uv run --frozen pytest verification/test_main_matriz_uses_single_client_instance.py -x && uv run --frozen ruff check main_matriz.py && uv run --frozen mypy main_matriz.py</automated>
  </verify>
  <acceptance_criteria>
    - AST-guard test GREEN (count==2).
    - `grep -c 'client\._state\.base_url' main_matriz.py` >= 1 (hostname read migrated) and the `remarkets` hostname assert is preserved.
    - `grep -n '_get_default' main_matriz.py` returns no CODE sites.
    - Exactly one `AsyncClient()` constructed (the AST test count==2 with one Client + one AsyncClient confirms).
    - ruff check + mypy strict green.
    - No `append_finding` title/fid/class_ literal changed (`git diff main_matriz.py`).
  </acceptance_criteria>
  <done>Single sync + single async client; hostname read + _ainvoke + 6 async _state sites migrated; TokenStore invariant held (1 AsyncClient); finding literals untouched; CI-green.</done>
</task>

<task type="execute">
  <name>Task 3: FINAL stability gate — STATIC title/fid diff vs 71bf201 + >=907-test attestation (Criterion #2 + #4)</name>
  <files>.planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md</files>
  <read_first>
    - verification/findings.py:192,595-603 (content-addressed dedupe by title — why unchanged titles produce byte-identical zones)
    - 15-CONTEXT.md D-06/D-07 (static diff scoped to title/fid/probe-name; baseline anchor 71bf201; NO live re-run)
    - .planning/phases/15-driver-migration-4-refac-05/15-LOC-ATTESTATION.md (created in Plan 01 — append the test-baseline attestation here or cross-reference)
  </read_first>
  <action>
    Create `.planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md`. Run the STATIC gate (D-07, NO live probe re-run): `git diff 71bf201..HEAD -- .planning/verification/ambito-financiero-client-findings.md .planning/verification/iol-client-findings.md .planning/verification/higyrus-client-findings.md .planning/verification/matriz-client-findings.md`, then scope the result to lines containing finding titles / `F-NN` ids / probe-name literals (the `### F-NN -- <title>` detail headers parsed by `_DETAIL_HEADER_RE` at findings.py:192). Record that ZERO title/fid/probe-name lines changed across all 4 committed findings files vs baseline 71bf201 (the migration changed only HOW the client is acquired, never the `append_finding` literals — D-06). If ANY title/fid/probe-name line differs, STOP and flag it as a Criterion #2 gate failure (a real finding-title regression, not data volatility). Do NOT diff `actual=`/`diff=` bytes — those are non-deterministic live data (deferred to Phase 17 / LIVE-03).
    Then run the full suite `uv run --frozen pytest -q` and record the passing count; attest it is >=907 (Criterion #4 milestone baseline). Cross-reference the LOC-attestation doc from Plan 01.
  </action>
  <verify>
    <automated>test -f .planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md && grep -q '71bf201' .planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md && uv run --frozen pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Stability-gate doc exists; records the STATIC `git diff 71bf201..HEAD` over the 4 committed findings files scoped to title/fid/probe-name lines, with the result ZERO changes.
    - Doc explicitly states no live re-run was performed and `actual=`/`diff=` byte volatility is out of scope (Phase 17).
    - Full pytest passing count recorded and attested >=907 (the 4 new AST tests are additive on top of the prior baseline).
  </acceptance_criteria>
  <done>STATIC title/fid diff vs 71bf201 shows zero changes (Criterion #2 closed); >=907-test baseline attested (Criterion #4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → MATBA ROFEX remarkets API | driver authenticates with MATRIZ credentials; hostname-asserted to remarkets in main() |
| driver process → matriz TokenStore | the single AsyncClient holds the 3-way TokenStore concurrency primitive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | MATRIZ credentials in logs/findings/commits | mitigate | Migration changes only client acquisition; no credential surfaces into findings/logs; `.env` stays uncommitted; no credential literal touched. |
| T-15-02 | Tampering | >1 AsyncClient() → TokenStore corruption | mitigate | The ≤2-ctor AST gate (Task 1) caps async construction at exactly ONE AsyncClient — directly preventing TokenStore 3-way-concurrency corruption (anti-Pitfall 1; spike-findings TokenStore primitive). |
| T-15-06 | Tampering | accidental remarkets→production endpoint switch | mitigate | The sync hostname-safety read migrates to `client._state.base_url` and the `if "remarkets" not in base` assert is preserved intact — guards against live production order entry. |
| T-15-03 | Denial of Service | remarkets API rate limits | accept | Single operator-driven smoke; below `high` severity. |
| T-15-SC | Tampering | uv/pip installs | mitigate | No new packages installed; no package legitimacy audit required. |
</threat_model>

<verification>
- AST-guard test GREEN (count==2; exactly one AsyncClient → TokenStore-safe).
- Hostname-safety read migrated; remarkets assert preserved.
- `_get_default()` eliminated from CODE lines.
- STATIC title/fid diff vs 71bf201 = zero changes across all 4 findings files.
- Full pytest >=907 passing.
- ruff + mypy strict green.
- Operator confirms LIVE remarkets smoke exit 0.
</verification>

<success_criteria>
- main_matriz.py constructs exactly 1 sync + 1 async client; TokenStore invariant held.
- Criterion #2 closed: zero title/fid/probe-name drift vs baseline 71bf201 (static, all 4 drivers).
- Criterion #4 closed: >=907 tests passing.
</success_criteria>

<artifacts_this_phase_produces>
**New test file + test function:**
- `verification/test_main_matriz_uses_single_client_instance.py` → `test_main_matriz_uses_single_client_instance()`

**New driver import line:**
- `main_matriz.py`: `from matriz_client import Client, AsyncClient` (bare, pinned per D-05)

**New helper signatures:**
- Every sync `def probe_*` gains `client`; every async `def probe_*` gains `aclient`.
- `_ainvoke(...)` threaded with `aclient` (mechanism per Claude's Discretion).

**New planning artifact:**
- `.planning/phases/15-driver-migration-4-refac-05/15-STABILITY-GATE.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/15-driver-migration-4-refac-05/15-04-SUMMARY.md` when done
</output>
