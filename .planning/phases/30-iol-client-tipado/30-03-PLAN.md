---
phase: 30-iol-client-tipado
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - packages/iol-client/src/iol_client/models.py
  - packages/iol-client/src/iol_client/__init__.py
  - packages/iol-client/src/iol_client/_core.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_models.py
  - packages/iol-client/tests/test_core.py
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
  - packages/iol-client/tests/test_refresh_token_lifecycle.py
  - packages/iol-client/tests/test_refresh_token_lifecycle_async.py
  - packages/iol-client/tests/test_fixture_reaches_production.py
autonomous: true
requirements: [TYP-01]

must_haves:
  truths:
    - "`get_instruments` devuelve una lista de `Instrumento` — la última de las 16 firmas sale de retorno sin tipar (criterio 2)."
    - "Los mocks de la suite reflejan la forma que el schema capturado en vivo demuestra: una lista top-level, no el envelope que la suite venía asumiendo (D-06)."
    - "Una respuesta de instrumentos que no es una lista top-level levanta `IOLAPIError`; no degrada a lista vacía (D-06)."
    - "Las 16 firmas están migradas: cero retornos sin tipar en la superficie pública de `iol-client` (criterio 2)."
    - "`mypy --strict` limpio sobre `packages/iol-client/src` y `packages/iol-client/tests`; `ruff` y `ruff format` verdes (criterio 2)."
  artifacts:
    - path: "packages/iol-client/src/iol_client/models.py"
      provides: "Instrumento — 2 campos de la lista top-level"
      contains: "class Instrumento(SafeModel)"
  key_links:
    - from: "packages/iol-client/src/iol_client/_core.py"
      to: "packages/iol-client/src/iol_client/models.py"
      via: "parse_get_instruments_response delega en el helper de lista con el modelo Instrumento"
      pattern: "_parse_list_or_raise\\(resp, Instrumento\\)"
    - from: "packages/iol-client/src/iol_client/client.py"
      to: "packages/iol-client/src/iol_client/_core.py"
      via: "método y shim de get_instruments delegan al parser y re-anotan el retorno"
      pattern: "-> list\\[Instrumento\\]:"
  prohibitions:
    - statement: "El parser no adopta tolerancia dict-o-lista para acomodar a los mocks viejos: reintroduciría exactamente el bug del vacío silencioso que este milestone existe para eliminar (D-06)."
      status: unverified
      flagged: true
    - statement: "La suite no se pone verde debilitando aserciones que tienen equivalente tipado; los re-mocks cambian el payload, nunca la fuerza de la aserción."
      status: unverified
      flagged: true
    - statement: "Los tokens centinela de los tests de propagación de fixtures no se tocan: los re-mocks alcanzan payloads de respuesta únicamente."
      status: unverified
      flagged: true
    - statement: "Ningún campo de RESPONSE gana un tipo de conjunto cerrado; `pais` e `instrumento` se declaran como texto libre (D-09/F29, criterio 4)."
      status: unverified
      flagged: true
---

<objective>
Cerrar la migración de la superficie: modelar `Instrumento`, reescribir el último parser (el único que hoy devuelve un retorno sin tipar), migrar las 4 firmas restantes y **corregir los 16 mocks** que la suite tiene con una forma de payload que el schema capturado en vivo contradice.

Purpose: `get_instruments` es el caso con mayor divergencia entre lo que la suite cree y lo que la API devuelve — 16 sitios de test asumen un envelope; la captura del 2026-06-06 muestra una lista top-level de objetos de dos claves. Es también el único parser que hoy pasa el payload sin tipar. Con este plan las 16 firmas quedan migradas y el criterio 2 del ROADMAP queda cerrado.

Output: `Instrumento` en `models.py` y en `__all__`; `parse_get_instruments_response` con guard de forma que levanta; 4 firmas re-anotadas; 16 payloads de mock corregidos en 6 archivos.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-iol-client-tipado/30-CONTEXT.md
@.planning/phases/30-iol-client-tipado/30-RESEARCH.md
@.planning/phases/30-iol-client-tipado/30-PATTERNS.md
@.planning/phases/30-iol-client-tipado/30-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Ver la tabla completa en `30-01-PLAN.md` § "Artifacts this phase produces". Símbolo **creado por este plan**: `iol_client.models.Instrumento`. Todo lo demás que consume (`SafeModel`, `Punta`, `Cotizacion`, `Titulo`, `to_dict`, `_parse_list_or_raise`) lo crearon los planes 30-01 y 30-02 dentro de esta misma fase y **no** es drift.

## Flagged Assumptions (no resueltas — no borrar)

| # | Assumption | Estado | Por qué queda abierta |
|---|---|---|---|
| FA-01 | La fila de cobertura de edges para **TYP-01** llegó `unclassified` → **unresolved**; los edges concretos se derivan de los 5 criterios del ROADMAP § Phase 30 y de D-01..D-12. | unresolved | Sin SPEC file para la fase. |
| FA-04 (RESEARCH A3) | Se asume que nada fuera de este repo consume `iol-client` 0.2.0. | abierta → F34 | Requiere al operator; no bloquea. |
| FA-06 | RESEARCH corrige a CONTEXT: los sitios de re-mock son **16 payloads + 2 aserciones = 18 líneas en 6 archivos**, no "~12". Un trabajo dimensionado para 12 dejaría 4 atrás y rompería la suite. | resuelta a favor de RESEARCH | El inventario exacto está en el `<action>` de la tarea 2. |

## Reversibility Ratings (REVERSIBILITY_GATES)

| Decisión de este plan | Rating | Gate |
|---|---|---|
| Guard de forma que levanta ante una respuesta no-lista | two-way técnicamente; **no se abre** — D-06 la fija y su alternativa es un bug conocido | ninguno |
| Re-mock de 16 payloads a la forma del schema vivo | two-way (son datos de test) | ninguno |
| 4 firmas más migradas a modelos | two-way en el repo; la publicación es de F34 | ninguno |

Ninguna decisión one-way sin autorización previa → **no se inserta ningún `checkpoint:decision`**.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: modelo `Instrumento` — la lista top-level de dos claves</name>

  <files>
    packages/iol-client/src/iol_client/models.py,
    packages/iol-client/src/iol_client/__init__.py,
    packages/iol-client/tests/test_models.py
  </files>

  <read_first>
    - `.planning/verification/schemas/iol-client/get-instruments.json` — el schema capturado: una **lista top-level** cuyos elementos tienen exactamente dos claves de texto. Fuente de verdad.
    - `packages/iol-client/src/iol_client/models.py` — estado post-30-02: `SafeModel`, `Punta`, `Cotizacion`, `Titulo`. `Instrumento` sigue el mismo patrón de declaración.
    - `packages/iol-client/src/iol_client/__init__.py` — bloque de imports de modelos y `__all__` ordenado.
    - `packages/iol-client/tests/test_models.py` — convenciones del archivo.
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Model Field Tables → `Instrumento`.
  </read_first>

  <behavior>
    Tests nuevos en `test_models.py`, en rojo antes de declarar la clase:
    - `Instrumento.from_api({"instrumento": "acciones", "pais": "argentina"})` construye los dos campos con esos valores y **cero** registros de divergencia.
    - `Instrumento.from_api({})` devuelve texto vacío en ambos campos (typed zeros).
    - `Instrumento.from_api(None)` no levanta.
    - `schema_of(Instrumento.from_api(elemento).to_dict())` reproduce la forma del elemento 0 del schema committeado de instrumentos.
  </behavior>

  <action>
    RED → GREEN → REFACTOR.

    **RED.** Agregar los casos de `<behavior>` a `packages/iol-client/tests/test_models.py` y verlos fallar.

    **GREEN.** Agregar a `models.py` `@dataclass(frozen=True, slots=True) class Instrumento(SafeModel)` con exactamente dos campos de texto: `instrumento: str` y `pais: str`. Sin valores por defecto.

    Docstring: registrar que modela cada elemento de la **lista top-level** que devuelve el endpoint de listado, y que la captura del 2026-06-06 contradice la forma de envelope que la suite venía asumiendo — el desajuste que motiva D-06 y los re-mocks de la tarea 2. Los dos campos son texto libre; no se cierran como conjunto (criterio 4, DT-07 → F33).

    Exportar `Instrumento` en `__init__.py`: agregarlo al bloque de imports de modelos y al `__all__`, ambos en orden alfabético (Pitfall 3). Con esto el `__all__` de iol contiene los 5 símbolos nuevos de la fase: `Cotizacion`, `Instrumento`, `Punta`, `SafeModel`, `Titulo`.

    Sin overrides de `from_api` (Pitfall 7). `__version__` sigue en `"0.2.0"` (D-11).

    **REFACTOR.** `ruff format` + `ruff check --fix`; re-correr tests.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_models.py -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_models.py -q` sale 0 con ≥ 18 tests.
    - `uv run python -c "from iol_client.models import Instrumento; import dataclasses; f=[x.name for x in dataclasses.fields(Instrumento)]; assert f==['instrumento','pais'], f"` sale 0.
    - `uv run python -c "import iol_client; assert {'Cotizacion','Instrumento','Punta','SafeModel','Titulo'} <= set(iol_client.__all__), sorted(iol_client.__all__)"` sale 0.
    - `uv run python -c "import iol_client; assert iol_client.__all__ == sorted(iol_client.__all__), iol_client.__all__"` sale 0 (el `__all__` sigue ordenado).
    - `grep -c 'frozen=True, slots=True' packages/iol-client/src/iol_client/models.py` imprime `4`.
    - `uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'"` sale 0.
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py uv.lock` sale 0.
  </acceptance_criteria>

  <done>
    `Instrumento` existe con sus dos campos de texto, exportado en un `__all__` ordenado junto a los otros 4 símbolos nuevos; sus tests cubren construcción, typed zeros, payload nulo y round-trip; los 4 modelos de la fase están declarados.
  </done>
</task>

<task type="auto">
  <name>Task 2: corregir los 16 payloads de mock a la forma del schema vivo (preparatorio, suite verde)</name>

  <files>
    packages/iol-client/tests/test_refresh_token_lifecycle.py,
    packages/iol-client/tests/test_refresh_token_lifecycle_async.py,
    packages/iol-client/tests/test_client.py,
    packages/iol-client/tests/test_async_client.py,
    packages/iol-client/tests/test_fixture_reaches_production.py,
    packages/iol-client/tests/test_core.py
  </files>

  <read_first>
    - `.planning/verification/schemas/iol-client/get-instruments.json` — la forma correcta.
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Blast Radius → "D-06 re-mocks": la tabla archivo-por-línea con los 16 payloads y las 2 aserciones.
    - `packages/iol-client/tests/test_fixture_reaches_production.py` líneas 35-75 — usa `get_instruments` como llamada autenticada barata; los tokens centinela son literales de prueba y **no se tocan**.
    - `packages/iol-client/tests/test_refresh_token_lifecycle.py` y su espejo async — el payload es incidental: estos tests afirman sobre el header de autorización y sobre el conteo de requests.
  </read_first>

  <action>
    <!-- planner-discipline-allow: instrumentos -->

    Tarea puramente mecánica: **una sustitución idéntica de payload repetida 16 veces**, sin lógica nueva ni símbolos nuevos. Toca 6 archivos porque el mismo mock está replicado en 6 archivos; no hay una frontera semántica por la que partirla sin dejar la suite en un estado incoherente.

    Se ejecuta **antes** del cambio de parser (tarea 3) a propósito: hoy el parser pasa el payload sin tipar, así que un payload en forma de lista ya funciona y la suite **queda verde en este commit**. Invertir el orden dejaría la suite roja en la frontera de commit.

    Localizar los sitios con `grep -rn 'instrumentos' packages/iol-client/tests/`. El inventario verificado es:

    | Archivo | Líneas de payload | Aserciones |
    |---|---|---|
    | `tests/test_refresh_token_lifecycle.py` | 66, 118, 159, 201 | — |
    | `tests/test_refresh_token_lifecycle_async.py` | 71, 118, 152, 188 | — |
    | `tests/test_client.py` | 87, 180, 217 | 90 |
    | `tests/test_async_client.py` | 142, 177 | — |
    | `tests/test_fixture_reaches_production.py` | 43, 64 | — |
    | `tests/test_core.py` | 354 (payload como bytes) | 356 |

    Reglas de sustitución:
    - En los 14 sitios donde el test afirma únicamente sobre el header de autorización o sobre el conteo de requests, el payload es genuinamente incidental: la re-mock más barata y correcta es una **lista vacía**. Es segura precisamente porque el guard que llega en la tarea 3 rechaza lo-que-no-es-lista, no lo-que-está-vacío: una lista vacía es una lista válida.
    - En los 2 sitios que sí afirman sobre el contenido devuelto (`test_client.py:87` con su aserción en :90, y `test_core.py:354` con la suya en :356), usar **un** elemento realista: un objeto con las dos claves del schema, p.ej. instrumento `"acciones"` y país `"argentina"`. Ajustar la aserción a la nueva forma manteniendo su fuerza: sigue afirmando el contenido devuelto, ahora como lista.
    - `test_core.py:354` mockea el cuerpo como **bytes**, no como JSON: sustituir el literal de bytes por la forma de lista equivalente.
    - **No** tocar los tokens centinela de `test_fixture_reaches_production.py` (son literales de prueba, no credenciales) ni ninguna aserción sobre el header de autorización.

    Al terminar, ningún archivo de test debe seguir construyendo el envelope viejo.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client -q && uv run mypy packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client -q` sale 0 (la suite queda verde **en este commit**, antes del cambio de parser).
    - `grep -rc 'instrumentos' packages/iol-client/tests/ 2>/dev/null | grep -v ':0$'` no devuelve resultados (exit 1): ningún archivo de test conserva el envelope viejo.
    - `uv run pytest packages/iol-client/tests/test_refresh_token_lifecycle.py packages/iol-client/tests/test_refresh_token_lifecycle_async.py -q` sale 0.
    - `uv run pytest packages/iol-client/tests/test_fixture_reaches_production.py -q` sale 0.
    - `git diff packages/iol-client/tests/test_fixture_reaches_production.py | grep -c 'sentinel'` imprime `0` (los centinelas no fueron tocados).
    - `git diff --exit-code packages/iol-client/src/` sale 0 (tarea sólo de tests).
    - `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` sale 0.
  </acceptance_criteria>

  <done>
    Los 16 payloads y las 2 aserciones asociadas reflejan la lista top-level que el schema capturado demuestra; los 14 sitios incidentales usan lista vacía y los 2 sitios con aserción de contenido usan un elemento realista; ningún token centinela ni aserción de header fue modificado; la suite queda verde sin haber tocado `src/`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: `parse_get_instruments_response` con guard de forma + las 4 firmas restantes</name>

  <files>
    packages/iol-client/src/iol_client/_core.py,
    packages/iol-client/src/iol_client/client.py,
    packages/iol-client/src/iol_client/aio.py,
    packages/iol-client/tests/test_core.py,
    packages/iol-client/tests/test_client.py
  </files>

  <read_first>
    - `packages/iol-client/src/iol_client/_core.py` — estado post-30-02: `_parse_list_or_raise` ya existe y ya es dueño del scope per-response; `parse_get_instruments_response` (:341-350) es el último parser sin migrar y hoy devuelve el payload sin tipar, con un docstring que explica esa decisión de Phase 6.
    - `packages/iol-client/src/iol_client/client.py` líneas 549-556 (método) y 697-699 (shim) — las dos firmas sin tipar de sync.
    - `packages/iol-client/src/iol_client/aio.py` líneas 564-568 (método) y 715-716 (shim) — las dos de async.
    - `packages/iol-client/src/iol_client/exceptions.py` línea 13 — `IOLAPIError(status_code, message)`.
    - `packages/iol-client/tests/test_core.py` líneas 345-360 — tests del parser de instrumentos.
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Pattern 3, § The 16 Signatures (filas 9-12), § Security Domain (ASVS V5).
  </read_first>

  <behavior>
    Tests en `test_core.py`, en rojo antes del cambio:
    - `parse_get_instruments_response(resp)` con un body de lista de 2 elementos devuelve 2 `Instrumento` y el atributo `instrumento` del primero tiene el valor mockeado.
    - `parse_get_instruments_response(resp)` con un body que **no** es una lista top-level levanta `IOLAPIError`, y el mensaje nombra el tipo recibido. **No** devuelve lista vacía. Este es el test que fija D-06 y ASVS V5.
    - `parse_get_instruments_response(resp)` con una lista vacía devuelve una lista vacía sin levantar: el guard rechaza lo-que-no-es-lista, no lo-que-está-vacío.
    - `iol_client.get_instruments()` devuelve una lista de `Instrumento` y `await aio.get_instruments()` devuelve el mismo tipo (paridad sync/async).
  </behavior>

  <action>
    RED → GREEN → REFACTOR. Las tres ediciones de código en **un solo commit** (Pitfall 6).

    **RED.** Escribir los casos de `<behavior>` en `test_core.py` y las dos aserciones de paridad en `test_client.py` / su espejo async.

    **GREEN.**

    1. `_core.py` — reescribir `parse_get_instruments_response` **in place**: retorno anotado como lista de `Instrumento`, cuerpo de una línea con un local anotado que recibe `_parse_list_or_raise(resp, Instrumento)` y lo retorna. Reemplazar el docstring que hoy justifica el retorno sin tipar por uno que registre D-06: el wire real es una lista top-level de objetos de dos claves, y el guard **levanta** en vez de degradar a lista vacía — degradar sería reintroducir el bug del vacío silencioso que este milestone elimina. No adoptar tolerancia dict-o-lista para acomodar a mocks viejos; los mocks ya se corrigieron en la tarea 2.
    2. `client.py` — re-anotar el retorno del método (:549-556) y del shim (:697-699) a lista de `Instrumento`; importar el modelo. Sin lógica en los shells.
    3. `aio.py` — idéntico en el método (:564-568) y el shim (:715-716).

    Con esto las 16 firmas quedan migradas: 4 devuelven `Cotizacion`, 4 devuelven lista de `Cotizacion`, 4 devuelven lista de `Instrumento`, 4 devuelven lista de `Titulo`. Verificar contra la tabla de RESEARCH § The 16 Signatures que ninguna quedó atrás.

    Migrar además las aserciones de contenido que la tarea 2 dejó afirmando sobre dicts crudos (`test_client.py:90`, `test_core.py:356`): ahora el retorno es una lista de modelos, así que la aserción pasa a leer el atributo del elemento y, donde corresponda, el largo de la lista. Mantener la fuerza de la aserción.

    **REFACTOR.** `ruff format` + `ruff check --fix`; suite completa del paquete.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run lint-imports && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client -q` sale 0 y reporta ≥ 205 tests.
    - `grep -c -- '-> list\[Instrumento\]:' packages/iol-client/src/iol_client/client.py` imprime `2`; ídem en `aio.py`.
    - `grep -c 'Instrumento' packages/iol-client/src/iol_client/_core.py` imprime ≥ `2`.
    - Las 16 firmas, contadas de una vez: `grep -c -- '-> Cotizacion:\|-> list\[Cotizacion\]:\|-> list\[Instrumento\]:\|-> list\[Titulo\]:' packages/iol-client/src/iol_client/client.py` imprime `8`; el mismo comando sobre `aio.py` imprime `8`.
    - Superficie pública sin retornos sin tipar: `uv run python -c "
import inspect, typing, iol_client
from iol_client.models import Cotizacion, Instrumento, Titulo
ok = {Cotizacion, list[Cotizacion], list[Instrumento], list[Titulo]}
for name in ('get_quote','get_historical_quotes','get_instruments','get_instruments_by_type'):
    fn = getattr(iol_client, name)
    ann = typing.get_type_hints(fn)['return']
    assert ann in ok, (name, ann)
"` sale 0.
    - El test de guard existe y es no-vacuo: `uv run pytest packages/iol-client/tests/test_core.py -k instruments -q` sale 0 con ≥ 3 tests.
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `uv run lint-imports` sale 0.
    - `uv run python tools/check_decode_intactness.py` sale 0.
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py uv.lock` sale 0.
  </acceptance_criteria>

  <done>
    `get_instruments` devuelve una lista de `Instrumento` en las 4 superficies; una respuesta que no es lista top-level levanta con el tipo recibido en el mensaje; una lista vacía sigue siendo válida; las 16 firmas están migradas y verificadas de una sola pasada por introspección; suite, mypy, ruff, format e import-linter verdes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API IOL en vivo → `parse_get_instruments_response` | Es el parser cuyo contrato de forma estaba más desalineado con la realidad; entra una respuesta no confiable. |
| Fixtures de test → suite | Los payloads de mock definen lo que la suite cree del mundo; un mock equivocado convierte un test verde en una afirmación falsa. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering | `parse_get_instruments_response` | mitigate | Es el punto exacto donde una degradación silenciosa enmascararía un upstream cambiado o comprometido: hoy pasa el payload sin tipar y sin verificar forma. El guard **levanta** `IOLAPIError(0, ...)` nombrando el tipo recibido (D-06, ASVS V5), con test dedicado en la tarea 3. Una lista vacía sigue siendo válida — el guard discrimina forma, no cardinalidad. |
| T-30-09 | Spoofing | suite de tests como oráculo | mitigate | 16 mocks afirmaban una forma que el schema capturado en vivo contradice: la suite estaba validando contra un mundo inexistente. La tarea 2 los alinea con la captura del 2026-06-06 y el criterio de aceptación verifica que ninguno sobreviva. |
| T-30-05 | Information disclosure | credenciales en fixtures | accept | Los re-mocks alcanzan payloads de respuesta únicamente. Los tokens centinela de `test_fixture_reaches_production.py` son literales de prueba; el criterio de aceptación de la tarea 2 verifica por diff que no fueron tocados. |
| T-30-01 | Information disclosure | `models.py` | mitigate | `Instrumento` no agrega ningún canal de salida; la allowlist de imports verificada en 30-01 sigue vigente. Registros tipo-y-ruta, nunca valor. |
| T-30-03 | Tampering | integridad del walker | mitigate | `_decode.py` sin cambios; verificado con `git diff --exit-code` y con el gate de intactness, que corre en el job de lint del CI. |
| T-30-SC | Tampering | instalaciones vía gestor de paquetes | mitigate | Cero paquetes externos. `git diff --exit-code uv.lock` es criterio de aceptación. Sin `[ASSUMED]`/`[SUS]`/`[SLOP]` que gatear. |
</threat_model>

<verification>
1. `uv run pytest packages/iol-client -q` → verde, ≥ 205 tests.
2. `uv run mypy packages/iol-client/src packages/iol-client/tests` → 0. **Éste es el criterio 2 del ROADMAP.**
3. `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` → 0.
4. `uv run lint-imports` → 0.
5. `uv run python tools/check_decode_intactness.py` → 0.
6. `git diff --exit-code uv.lock packages/iol-client/src/iol_client/_decode.py` → 0.
7. Introspección de las 4 funciones públicas: ningún retorno fuera del conjunto de los 4 tipos de modelo.
</verification>

<success_criteria>
- Las 16 firmas devuelven modelos o listas de modelos; cero retornos sin tipar en la superficie pública.
- Una respuesta de instrumentos con forma inesperada levanta; una lista vacía no.
- Ningún test conserva el envelope que el schema vivo contradice.
- `mypy --strict`, `ruff`, `ruff format`, import-linter y el gate de intactness, todos verdes.
</success_criteria>

<output>
Crear `.planning/phases/30-iol-client-tipado/30-03-SUMMARY.md`. Debe registrar: (a) el conteo real de sitios re-mockeados frente al "~12" de CONTEXT (FA-06); (b) la salida de la verificación por introspección de las 16 firmas; (c) conteo de tests antes/después.
</output>
