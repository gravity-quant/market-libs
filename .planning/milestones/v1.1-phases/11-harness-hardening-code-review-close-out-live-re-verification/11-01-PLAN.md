---
phase: 11-harness-hardening-code-review-close-out-live-re-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - verification/findings.py
  - verification/test_findings_append_only.py
  - verification/test_findings_dedupe_by_title.py
  - .planning/verification/ambito-financiero-client-findings.md
  - .planning/verification/iol-client-findings.md
  - .planning/verification/higyrus-client-findings.md
  - .planning/verification/matriz-client-findings.md
  - main_matriz.py
  - main_iol.py
  - main_higyrus.py
  - main_ambito_financiero.py
autonomous: true
requirements:
  - HARN-07
  - HARN-08
  - HARN-09
  - HARN-10
tags:
  - harness
  - findings
  - dedupe
  - append-only
user_setup: []

must_haves:
  truths:
    - "verification/findings.py is append-only with BEGIN/END zone parser (per D-HARN-01) — content outside markers (operator narrative ABOVE the BEGIN line and BELOW the END line) survives verbatim across N re-runs of any driver"
    - "Content-addressed dedupe by finding ID works end-to-end across the 4 drivers — re-running a driver produces a git-clean diff for the auto-zone besides ART block timestamp refresh (HARN-08)"
    - "main_matriz.py D-MATZ-27 EXPECTED terminal dedupes in a single run (no N re-runs needed to detect duplication) — per D-HARN-01 + HARN-10 fix sketch in folded todo (HARN-10)"
    - "Operator fields (Classification:, Rationale:, Regression:, Resolution:) added by the human to ANY finding section survive verbatim across N re-runs of the driver (HARN-09, regression test validates SHA256-stable operator content cross-runs)"
    - "The 4 existing <pkg>-findings.md baseline files (committed at 4d48e07) are migrated to include the new BEGIN AUTO-GENERATED / END AUTO-GENERATED markers without losing operator content — one-time migration task (per D-HARN-01)"
  artifacts:
    - path: "verification/findings.py"
      provides: "EXTENDED — _ParsedFile gets operator_prefix/operator_suffix fields; _parse_findings tracks BEGIN/END zone state; _serialize_findings emits markers + preserves prefix/suffix; append_finding accepts idempotent_by_title=False kwarg (HARN-07/08/10)"
      contains: "BEGIN AUTO-GENERATED"
    - path: "verification/test_findings_append_only.py"
      provides: "NEW — HARN-07/09 regression: N re-runs of append_finding preserve operator narrative ABOVE+BELOW markers AND operator-added bullet labels (Classification:, Rationale:, Regression:, Resolution:) INSIDE finding sections; SHA256 of operator content stable cross-runs"
      contains: "test_operator_fields_survive_N_runs"
    - path: "verification/test_findings_dedupe_by_title.py"
      provides: "NEW — HARN-08/10 regression: parametric × 4 packages; calling append_finding 5× with same title and idempotent_by_title=True writes exactly 1 occurrence; differing-fid same-title does NOT duplicate"
      contains: "idempotent_by_title"
    - path: ".planning/verification/ambito-financiero-client-findings.md"
      provides: "MIGRATED — BEGIN/END markers injected around current Index + Detail blocks; operator narrative preserved verbatim"
      contains: "BEGIN AUTO-GENERATED"
    - path: ".planning/verification/iol-client-findings.md"
      provides: "MIGRATED — BEGIN/END markers injected; operator narrative preserved"
      contains: "BEGIN AUTO-GENERATED"
    - path: ".planning/verification/higyrus-client-findings.md"
      provides: "MIGRATED — BEGIN/END markers injected; operator narrative preserved"
      contains: "BEGIN AUTO-GENERATED"
    - path: ".planning/verification/matriz-client-findings.md"
      provides: "MIGRATED — BEGIN/END markers injected; operator narrative + Phase 5 classification rationale preserved"
      contains: "BEGIN AUTO-GENERATED"
    - path: "main_matriz.py"
      provides: "MODIFIED — D-MATZ-27 EXPECTED terminal append_finding call (lines 2097-2114) gains idempotent_by_title=True kwarg per HARN-10 fix sketch"
      contains: "idempotent_by_title=True"
  key_links:
    - from: "verification/findings.py::append_finding(idempotent_by_title=True)"
      to: "main_matriz.py D-MATZ-27 EXPECTED terminal call site (lines 2097-2114)"
      via: "driver-side kwarg flip — single 5-LOC change in harness closes HARN-08 + HARN-10 simultaneously"
      pattern: "idempotent_by_title"
    - from: "verification/findings.py::_parse_findings (BEGIN/END zone state machine)"
      to: "verification/findings.py::_serialize_findings (markers wrap auto-zone + operator_prefix/suffix preserved)"
      via: "_ParsedFile.operator_prefix / operator_suffix list[str] fields round-trip the operator content"
      pattern: "operator_prefix|operator_suffix|BEGIN AUTO-GENERATED|END AUTO-GENERATED"
    - from: "verification/test_findings_append_only.py"
      to: "verification/findings.py"
      via: "regression test — runs append_finding 3× and asserts SHA256(operator_blob) stable + operator bullet labels in finding sections survive"
      pattern: "hashlib.sha256|operator_blob"
---

<objective>
Hacer `verification/findings.py` append-only mediante BEGIN/END zone parser y abrir el kwarg `idempotent_by_title=True` en `append_finding`. Cubre HARN-07 (zone parser preserva contenido operador fuera de markers), HARN-08 (content-addressed dedupe by title aplica a los 4 drivers), HARN-09 (campos operator-added — Classification:/Rationale:/Regression:/Resolution: — sobreviven N re-runs), HARN-10 (matriz `D-MATZ-27 EXPECTED` terminal dedupe en 1 run vía driver-side kwarg flip).

Purpose: D-HARN-01 locked HÍBRIDO approach — extender `_parse_findings`/`_serialize_findings` (NO full rewrite) + injectar markers HTML-comment alrededor de los bloques auto-generados (`## Index` + `## Detalle por hallazgo`). El kwarg `idempotent_by_title=True` cierra HARN-08 + HARN-10 con el mismo delta de ~5 LOC en `append_finding`. Migration task one-time inyecta markers en los 4 findings files existentes (committeados en baseline `4d48e07`).

Output: 10 file changes — 1 extended (`verification/findings.py` + ~70 LOC), 2 new regression tests (HARN-07/09 + HARN-08/10), 4 migrated findings.md files (one-time marker injection preservando contenido current), 4 drivers donde se identifican terminales EXPECTED/NO-FIX que repiten title cross-run (matriz `D-MATZ-27` confirmed, iol `auth_401 EXPECTED` confirmed, ambito `antibot EXPECTED` confirmed, higyrus `auth_401 EXPECTED` confirmed — flip al `idempotent_by_title=True`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-CONTEXT.md
@.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-PATTERNS.md
@.planning/todos/pending/matriz-driver-findings-file-handling.md
@verification/findings.py
@verification/test_logging_root_unchanged.py
@verification/test_retry_mutation_gate.py
@.planning/verification/matriz-client-findings.md
@.planning/verification/iol-client-findings.md
@.planning/verification/higyrus-client-findings.md
@.planning/verification/ambito-financiero-client-findings.md
@main_matriz.py
@main_iol.py
@main_higyrus.py
@main_ambito_financiero.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend verification/findings.py with BEGIN/END zone parser + idempotent_by_title kwarg (HARN-07/08/10) — RED-then-GREEN single commit</name>
  <files>verification/findings.py, verification/test_findings_append_only.py, verification/test_findings_dedupe_by_title.py</files>
  <read_first>
    - `verification/findings.py` (FULL 494 LOC — module docstring lines 1-35, _ParsedFile dataclass line 156-160, _parse_findings line 212-342, _serialize_findings line 345-400, append_finding line 403-494)
    - `11-PATTERNS.md` §`verification/findings.py` (lines 32-157) — exact extension patterns for _parse_findings state machine, _serialize_findings markers wrapping, append_finding idempotent_by_title fix sketch
    - `11-PATTERNS.md` §`verification/test_findings_append_only.py` (lines 321-371) — regression test pattern + SHA256 assertion idiom
    - `11-PATTERNS.md` §`verification/test_findings_dedupe_by_title.py` (lines 374-391) — parametric × packages pattern
    - `verification/test_logging_root_unchanged.py` lines 1-40 — cross-cutting verification test idiom (analog for test_findings_append_only.py)
    - `verification/test_retry_mutation_gate.py` lines 1-40 — parametric × packages idiom (analog for test_findings_dedupe_by_title.py)
    - `.planning/todos/pending/matriz-driver-findings-file-handling.md` — Bug 1 (HARN-10) + Bug 2 (HARN-07/09) root cause analysis + fix sketches
  </read_first>
  <behavior>
    - Test 1 (`test_operator_fields_survive_N_runs`): Initialize temp findings file via `write_findings("test-pkg")` under monkeypatched `_FINDINGS_DIR=tmp_path`. Prepend operator narrative (e.g. `"## Operator Narrative\nClassification: NO-FIX (out of scope v1.1)\n\n"`) ABOVE the existing content. Append operator suffix BELOW (e.g. `"\n\n## Operator Triage Notes\nRationale: matriz-only divergence\n"`). Compute initial SHA256 of full file. Run `append_finding(...)` 3× with identical args. After 3 runs, assert operator_prefix and operator_suffix appear byte-identical in final file (ART block timestamp WILL change between runs — but operator content above BEGIN marker + below END marker is byte-identical).
    - Test 2 (`test_operator_bullets_inside_findings_survive`): Same setup. After 1st `append_finding(fid="F-01", status="OPEN", ...)`, inject operator bullet `- **Classification:** NO-FIX (rationale: scope)` and `- **Resolution:** acknowledged limitation` INSIDE the F-01 detail section (between `- **Diff:**` and the next finding header). Promote `F-01` status to `EXPECTED` in the Index table (simulates operator triage). Run `append_finding(fid="F-01", status="OPEN", ...)` again — the existing preservation guard (line 473 `existing[fid].status != "OPEN"`) kicks in. Assert the operator bullets are still present in the final file (HARN-09 contract).
    - Test 3 (`test_idempotent_by_title_does_not_duplicate`): Parametric × 4 packages (`["ambito-financiero-client", "iol-client", "higyrus-client", "matriz-client"]`). Call `append_finding(fid=f"F-{_:02d}", status="EXPECTED", title="prod-vs-remarkets divergence acknowledged", idempotent_by_title=True, ...)` 5× with DIFFERENT fids but SAME title. Read final findings file. Assert `text.count("prod-vs-remarkets divergence acknowledged") == 1` (NOT 5).
    - Test 4 (`test_idempotent_by_title_preserves_existing`): If a finding with the same title already exists, the new call no-ops (only ART block refresh). Assert original finding (fid, expected, actual, diff) is preserved verbatim.
  </behavior>
  <action>
    **Step 1 — Tests FIRST (RED, then GREEN). Create `verification/test_findings_append_only.py`** following the pattern at `11-PATTERNS.md:321-371`. Required test names:
    - `test_operator_prefix_survives_N_runs(tmp_path, monkeypatch)` — operator narrative ABOVE marker preserved
    - `test_operator_suffix_survives_N_runs(tmp_path, monkeypatch)` — operator narrative BELOW marker preserved
    - `test_operator_bullets_inside_findings_survive(tmp_path, monkeypatch)` — Classification/Rationale/Resolution bullets INSIDE finding sections survive when finding status != OPEN
    - `test_markers_present_in_freshly_written_file(tmp_path, monkeypatch)` — `<!-- BEGIN AUTO-GENERATED -->` and `<!-- END AUTO-GENERATED -->` strings appear in output of `write_findings("test-pkg")` exactly once each

    Use `monkeypatch.setattr("verification.findings._FINDINGS_DIR", tmp_path)` per analog at `verification/test_logging_root_unchanged.py:1-40`. Use `hashlib.sha256(...)` to assert byte-stability of operator prefix/suffix substrings (NOT full-file SHA256 — the ART block timestamp legitimately changes).

    **Step 2 — Create `verification/test_findings_dedupe_by_title.py`** following the pattern at `11-PATTERNS.md:374-391`. Parametric × 4 packages via `@pytest.mark.parametrize("pkg", ["ambito-financiero-client", "iol-client", "higyrus-client", "matriz-client"])`. Required test names:
    - `test_idempotent_by_title_does_not_duplicate(pkg, tmp_path, monkeypatch)` — 5× calls with different fids + same title write 1 occurrence of title
    - `test_idempotent_by_title_preserves_existing_finding(pkg, tmp_path, monkeypatch)` — second call with same title and `idempotent_by_title=True` is a no-op (does NOT update fields)
    - `test_idempotent_by_title_default_false_still_duplicates(pkg, tmp_path, monkeypatch)` — backwards-compat: omitting the kwarg preserves the legacy fid-based dedupe (5× calls with same fid update in place; 5× calls with different fids create 5 entries)

    Run both tests — they MUST fail RED (BEGIN/END markers + `idempotent_by_title` do not exist yet). Verify with `uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q` → ≥ 1 FAILED.

    **Step 3 — Extend `verification/findings.py::_ParsedFile`** (line 155-160). Add two new fields per `11-PATTERNS.md:99-108`:
    ```python
    operator_prefix: list[str] = field(default_factory=list)   # lines BEFORE <!-- BEGIN AUTO-GENERATED -->
    operator_suffix: list[str] = field(default_factory=list)   # lines AFTER <!-- END AUTO-GENERATED -->
    ```

    **Step 4 — Extend `_parse_findings`** (line 212-342) with BEGIN/END state machine per `11-PATTERNS.md:74-97`. After existing state vars (around line 240), add:
    ```python
    in_auto_zone = False
    seen_begin = False
    seen_end = False
    operator_prefix: list[str] = []
    operator_suffix: list[str] = []
    ```
    The line-scan loop (line 242) becomes 3-zone aware:
    - Before `<!-- BEGIN AUTO-GENERATED -->`: append `raw_line` to `operator_prefix`, skip existing parser logic.
    - On `<!-- BEGIN AUTO-GENERATED -->`: set `in_auto_zone = True`, `seen_begin = True`, `continue`.
    - On `<!-- END AUTO-GENERATED -->`: set `in_auto_zone = False`, `seen_end = True`, `continue`.
    - After `<!-- END AUTO-GENERATED -->`: append `raw_line` to `operator_suffix`, skip parser logic.
    - Inside auto-zone (the default for current behavior of files without markers): existing Index/Detalle parsing runs as today.

    **Back-compat:** files without ANY marker (the 4 baseline files PRE-migration) must parse as today — `operator_prefix` and `operator_suffix` stay empty, `in_auto_zone` defaults True so all content is treated as auto-zone (current behavior preserved). Verify by running `uv run pytest verification/test_findings_append_only.py::test_markers_present_in_freshly_written_file -q` against a marker-less file.

    Return `_ParsedFile(findings=..., art=..., operator_prefix=operator_prefix, operator_suffix=operator_suffix)`.

    **Step 5 — Extend `_serialize_findings`** (line 345-400) per `11-PATTERNS.md:111-128`. Signature gains kwargs:
    ```python
    def _serialize_findings(
        pkg: str,
        findings: list[_Finding],
        art: dict[str, str],
        *,
        prefix: list[str] | None = None,
        suffix: list[str] | None = None,
    ) -> str:
    ```
    Wrap the Index + Detalle blocks with `<!-- BEGIN AUTO-GENERATED -->` and `<!-- END AUTO-GENERATED -->` markers. Prepend `prefix` (if non-empty) BEFORE the package header `# Findings: ...`. Append `suffix` (if non-empty) AFTER the last finding section. Always emit markers even when `prefix`/`suffix` are empty (so new files get the contract).

    **Step 6 — Extend `append_finding`** (line 403-494) per `11-PATTERNS.md:131-156`. Add kwarg AFTER `regression=None` (around line 414):
    ```python
    idempotent_by_title: bool = False,
    ```
    When True, BEFORE the existing preservation guard at line 473 (`if fid in existing and existing[fid].status != "OPEN":`), check if any existing finding has the same title:
    ```python
    if idempotent_by_title:
        for existing_finding in findings_list:
            if existing_finding.title == title:
                # Title already present — refresh ART block only, return.
                path.write_text(_replace_art_block(text, art), encoding="utf-8")
                return path
    ```
    Update the call to `_serialize_findings` (line 493) to pass the new kwargs:
    ```python
    path.write_text(
        _serialize_findings(pkg, findings_list, art, prefix=parsed.operator_prefix, suffix=parsed.operator_suffix),
        encoding="utf-8",
    )
    ```
    Also update the preservation-path call at line 474 (`_replace_art_block`) to use `parsed.operator_prefix + text + parsed.operator_suffix` IF needed — actually the existing `_replace_art_block` operates on the raw text directly and preserves everything outside the 3 ART lines, so no change needed there. Verify by re-reading `_replace_art_block` (line 187-209) — it uses regex sub on ART block lines only, leaving operator narrative untouched.

    **Step 7 — Extend `new_findings`** (line 100-119) — append the BEGIN/END markers to the emitted skeleton so freshly-created files already have the contract:
    ```python
    return (
        f"# Findings: {pkg}-client\n"
        "\n"
        "## Run Context (ART)\n"
        "- Timestamp: <ISO-8601>\n"
        "- Resolved base URL / env: <url> (<remarkets|prod|public>)\n"
        "- Market hours note: <abierto|cerrado — afecta paths sesión-dependientes>\n"
        "\n"
        f"<!-- Clases (D-09): {classes} -->\n"
        f"<!-- Estados (D-08): {lifecycle}. Sin campo de severidad. -->\n"
        "\n"
        "<!-- BEGIN AUTO-GENERATED -->\n"
        "## Index\n"
        "| ID | Class | Surface | Status |\n"
        "|----|-------|---------|--------|\n"
        "<!-- END AUTO-GENERATED -->\n"
    )
    ```

    **Step 8 — `__all__` updated** (line 44-51): add `"_ParsedFile"` if needed for tests (the `_` prefix means it's intentionally private; the tests can import via `verification.findings._ParsedFile` without re-export — preferred).

    **Step 9 — Run tests, confirm GREEN:** `uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q` → all PASS. Also run full verification suite to confirm NO existing test breaks: `uv run pytest verification/ -q`.

    **Step 10 — Atomic commit:**
    ```
    feat(11-01): findings.py append-only with BEGIN/END zone parser + idempotent_by_title (HARN-07/08/09/10)

    - Test: verification/test_findings_append_only.py (HARN-07/09) RED → GREEN
    - Test: verification/test_findings_dedupe_by_title.py (HARN-08/10, parametric × 4 pkgs) RED → GREEN
    - Fix: verification/findings.py — _ParsedFile gains operator_prefix/operator_suffix; _parse_findings tracks BEGIN/END zone state; _serialize_findings emits markers + preserves prefix/suffix; append_finding accepts idempotent_by_title kwarg; new_findings emits markers in skeleton
    - Carry-forward: existing preservation path (status humano CONFIRMED/FIXED/EXPECTED/NO-FIX) untouched at line 473; existing _replace_art_block path untouched at line 187-209
    - Closes: HARN-07 (zone parser), HARN-08 (content-addressed dedupe), HARN-09 (operator field preservation), HARN-10 (matriz D-MATZ-27 EXPECTED dedupe — driver-side kwarg flip in Task 3)
    ```
  </action>
  <verify>
    <automated>uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q && uv run pytest verification/ -q && uv run ruff check verification/findings.py verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py && uv run mypy --strict verification/findings.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN AUTO-GENERATED" verification/findings.py` returns ≥ 3 (constant in `new_findings`, `_parse_findings` matcher, `_serialize_findings` emitter)
    - `grep -c "END AUTO-GENERATED" verification/findings.py` returns ≥ 3 (same: constant, matcher, emitter)
    - `grep -c "operator_prefix\|operator_suffix" verification/findings.py` returns ≥ 6 (dataclass fields + parse loop accumulation + serialize prefix/suffix emission)
    - `grep -c "idempotent_by_title" verification/findings.py` returns ≥ 2 (kwarg signature + dedupe guard branch)
    - `grep -c "idempotent_by_title" verification/test_findings_dedupe_by_title.py` returns ≥ 3 (3 tests reference it)
    - `grep -E "^def test_" verification/test_findings_append_only.py | wc -l` returns ≥ 4 (prefix-survives, suffix-survives, bullets-survive, markers-present)
    - `grep -E "^def test_" verification/test_findings_dedupe_by_title.py | wc -l` returns ≥ 3 (does_not_duplicate, preserves_existing, default_false_still_duplicates)
    - `uv run pytest verification/test_findings_append_only.py -q 2>&1 | grep -E "passed|failed" | grep -v "0 failed"` exits non-empty AND grep "failed" matches 0 (all green)
    - `uv run pytest verification/test_findings_dedupe_by_title.py -q 2>&1 | grep -E "passed|failed" | grep -v "0 failed"` exits non-empty AND grep "failed" matches 0 (all green; 3 tests × 4 packages parametric = 12 cases)
    - `uv run pytest verification/ -q 2>&1 | tail -3 | grep -E "passed|failed"` shows 0 failed (no existing test broken)
    - `uv run ruff check verification/findings.py verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py` exits 0
    - `uv run mypy --strict verification/findings.py` exits 0
    - `python -c "from verification.findings import _ParsedFile; p=_ParsedFile(); assert hasattr(p, 'operator_prefix') and hasattr(p, 'operator_suffix')"` exits 0
    - `python -c "from verification.findings import new_findings; t=new_findings('test-pkg'); assert '<!-- BEGIN AUTO-GENERATED -->' in t and '<!-- END AUTO-GENERATED -->' in t"` exits 0
  </acceptance_criteria>
  <done>
    `verification/findings.py` extended with BEGIN/END zone parser preserving operator content outside markers; `append_finding` accepts `idempotent_by_title=False` kwarg with no-op-on-existing-title behavior. Two new regression test files exist with ≥ 7 tests total (4 HARN-07/09 + 3 HARN-08/10 parametric × 4 pkgs = ≥ 12 test cases). All tests GREEN. Full `verification/` suite still GREEN. ruff + mypy strict clean. Atomic commit emitted per template.
  </done>
</task>

<task type="auto">
  <name>Task 2: One-time migration — inject BEGIN/END markers into 4 baseline findings.md files preserving operator content (HARN-07 migration path)</name>
  <files>.planning/verification/ambito-financiero-client-findings.md, .planning/verification/iol-client-findings.md, .planning/verification/higyrus-client-findings.md, .planning/verification/matriz-client-findings.md</files>
  <read_first>
    - All 4 `.planning/verification/<pkg>-findings.md` files in FULL — these are the canonical baseline from commit 4d48e07 ("docs(05): baseline DRIFT-02 cycle closure (verification-cycle-2026-Q2)")
    - `11-PATTERNS.md` §`.planning/verification/{ambito,iol,higyrus,matriz}-findings.md (MIGRATION ...)` (line 25) — one-time migration contract
    - `11-CONTEXT.md` §D-HARN-01 §Migration — locked decision: markers MUST be injected without losing operator content
    - The output of `uv run python -c "from verification.findings import new_findings; print(new_findings('matriz-client'))"` AFTER Task 1 — to confirm the exact marker format being injected
  </read_first>
  <behavior>
    - Each migrated file has `<!-- BEGIN AUTO-GENERATED -->` injected on a line BY ITSELF immediately BEFORE the `## Index` heading.
    - Each migrated file has `<!-- END AUTO-GENERATED -->` injected on a line BY ITSELF immediately AFTER the last finding section (or at EOF if no operator suffix exists today).
    - All existing operator content (`## Run Context (ART)`, the `<!-- Clases (D-09) -->` + `<!-- Estados (D-08) -->` comment lines, the package header `# Findings: <pkg>-client`, any operator narrative ABOVE `## Index` or BELOW the last finding) MUST remain byte-identical except for the 2 new marker lines.
    - Running `uv run python -c "from verification.findings import _parse_findings; from pathlib import Path; p = _parse_findings(Path('.planning/verification/<pkg>-findings.md').read_text()); print(f'prefix_lines={len(p.operator_prefix)} suffix_lines={len(p.operator_suffix)}')"` for each file returns nonzero `operator_prefix` length (the package header + ART block live above BEGIN now).
  </behavior>
  <action>
    **Step 1 — Read each of the 4 files and identify the injection points.** For each file (`ambito-financiero-client-findings.md`, `iol-client-findings.md`, `higyrus-client-findings.md`, `matriz-client-findings.md`):
    - Locate the first occurrence of `## Index` — the BEGIN marker goes on its own line immediately BEFORE this heading.
    - Locate the end of the last finding section (`### F-NN -- <title>` block — find the highest `F-NN` and walk to the end of its content; if there is operator content after the last `### F-NN` block, the END marker goes AFTER the auto-content but BEFORE the operator suffix).

    **Step 2 — For each file, use the `Edit` tool to inject the 2 marker lines.** Do NOT rewrite the file via `Write` — use surgical `Edit` to preserve any whitespace, trailing newlines, or quirks of the baseline. Specifically:
    - `Edit` the file with `old_string="## Index"` → `new_string="<!-- BEGIN AUTO-GENERATED -->\n## Index"` (or expand the match to include enough surrounding context to be unique within the file).
    - For END marker injection, determine the end of the auto-zone by inspecting the file content. If no operator suffix exists today (the file ends after the last finding section), append `\n<!-- END AUTO-GENERATED -->\n` to EOF. If operator suffix exists, inject BEFORE the suffix.

    **Step 3 — Round-trip verification per file.** For each migrated file:
    ```bash
    uv run python -c "
    from verification.findings import _parse_findings, _serialize_findings
    from pathlib import Path
    p = Path('.planning/verification/matriz-client-findings.md')
    text = p.read_text()
    parsed = _parse_findings(text)
    print(f'findings_count={len(parsed.findings)} prefix_lines={len(parsed.operator_prefix)} suffix_lines={len(parsed.operator_suffix)}')
    "
    ```
    Confirm: `findings_count` matches the count visible in the Index table (e.g., matriz has multiple F-NN entries). `prefix_lines` ≥ 5 (the file header + ART block + comment lines live above BEGIN).

    **Step 4 — Confirm no operator content lost.** Run `git diff` on each file and confirm the ONLY changes are the 2 marker lines added (no other line modifications, deletions, or additions). Operator-added classification rationale lines (e.g., Phase 5 `Classification rationale` lines in matriz-client-findings.md) MUST be unchanged.

    **Step 5 — Confirm Task 1's regression tests still pass after migration** (the test files use temp paths via monkeypatch, so the baseline files are untouched by tests — but we verify nothing regressed):
    ```bash
    uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q
    ```

    **Step 6 — Atomic commit:**
    ```
    docs(11-01): migrate 4 baseline findings.md files — inject BEGIN/END markers (HARN-07 migration)

    - One-time marker injection (D-HARN-01) — no operator content modified
    - Files: ambito-financiero-client, iol-client, higyrus-client, matriz-client
    - Baseline preserved at commit 4d48e07 (verification-cycle-2026-Q2)
    - Round-trip verified: _parse_findings emits same findings list pre/post migration
    - Closes: HARN-07 migration path
    ```
  </action>
  <verify>
    <automated>for pkg in ambito-financiero-client iol-client higyrus-client matriz-client; do grep -c "BEGIN AUTO-GENERATED" .planning/verification/$pkg-findings.md; grep -c "END AUTO-GENERATED" .planning/verification/$pkg-findings.md; done | awk 'BEGIN{ok=1}{if($1!="1"){ok=0; print "FAIL: count="$1}}END{exit ok?0:1}' && uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN AUTO-GENERATED" .planning/verification/ambito-financiero-client-findings.md` returns 1
    - `grep -c "BEGIN AUTO-GENERATED" .planning/verification/iol-client-findings.md` returns 1
    - `grep -c "BEGIN AUTO-GENERATED" .planning/verification/higyrus-client-findings.md` returns 1
    - `grep -c "BEGIN AUTO-GENERATED" .planning/verification/matriz-client-findings.md` returns 1
    - `grep -c "END AUTO-GENERATED" .planning/verification/ambito-financiero-client-findings.md` returns 1
    - `grep -c "END AUTO-GENERATED" .planning/verification/iol-client-findings.md` returns 1
    - `grep -c "END AUTO-GENERATED" .planning/verification/higyrus-client-findings.md` returns 1
    - `grep -c "END AUTO-GENERATED" .planning/verification/matriz-client-findings.md` returns 1
    - `git diff --stat 4d48e07 -- .planning/verification/matriz-client-findings.md | tail -1` reports very small change count (only 2 lines added — operator content preserved)
    - `uv run python -c "from verification.findings import _parse_findings; from pathlib import Path; p = _parse_findings(Path('.planning/verification/matriz-client-findings.md').read_text()); assert len(p.operator_prefix) > 0, 'prefix empty — migration broke parse'"` exits 0
    - `uv run python -c "from verification.findings import _parse_findings; from pathlib import Path; p = _parse_findings(Path('.planning/verification/matriz-client-findings.md').read_text()); assert len(p.findings) >= 1, 'no findings parsed — migration broke parse'"` exits 0
    - For each of the 4 packages, the same assertions pass (loop in the verify command).
    - `uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q` exits 0 (Task 1 tests still GREEN after migration)
  </acceptance_criteria>
  <done>
    4 baseline `<pkg>-findings.md` files have `<!-- BEGIN AUTO-GENERATED -->` and `<!-- END AUTO-GENERATED -->` markers injected exactly once each. Operator content (header, ART block, `<!-- Clases -->`/`<!-- Estados -->` comments, any operator narrative above Index or below last finding) is byte-preserved per `git diff` inspection. Round-trip via `_parse_findings` returns nonzero `operator_prefix` length AND the original findings list. Task 1 regression tests still GREEN. Atomic commit emitted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Driver-side adoption — flip identified EXPECTED/NO-FIX terminals to idempotent_by_title=True across the 4 drivers (HARN-08 cross-driver + HARN-10 matriz D-MATZ-27)</name>
  <files>main_matriz.py, main_iol.py, main_higyrus.py, main_ambito_financiero.py</files>
  <read_first>
    - `main_matriz.py` lines 2093-2114 — D-MATZ-27 EXPECTED terminal append_finding call (HARN-10 primary target)
    - `main_iol.py` lines 1430-1442 — EXPECTED terminal for auth_401 probe (`status="EXPECTED"` + title may repeat cross-run)
    - `main_higyrus.py` lines 1997-2008 — EXPECTED terminal for auth_401 probe (mirror iol pattern)
    - `main_ambito_financiero.py` lines 590-602 — EXPECTED terminal for antibot probe (`status="EXPECTED"` + title may repeat cross-run)
    - `11-PATTERNS.md` §`main_matriz.py` HARN-10 D-MATZ-27 dedupe (line 227-246) — exact fix sketch with `idempotent_by_title=True` kwarg flip
    - `11-PATTERNS.md` §`main_iol.py + main_ambito_financiero.py (driver smoke — HARN-08 adoption only)` (line 313-317) — surgical flip pattern
  </read_first>
  <behavior>
    - Each driver invocation that emits a terminal EXPECTED/NO-FIX finding with a title that repeats across runs MUST gain `idempotent_by_title=True` in the keyword arguments.
    - The fid argument is preserved as-is (still `_next_fid()` or equivalent), but the harness no-ops if title already present — driver-side is unchanged otherwise.
    - Probes that emit OPEN findings with UNIQUE titles per-run (e.g. `f"login() sync levantó {type(exc).__name__} no mapeado"`) do NOT get the flag (they should still create per-run entries; uniqueness comes from the exception type).
  </behavior>
  <action>
    **Step 1 — main_matriz.py D-MATZ-27 EXPECTED terminal flip (HARN-10):**

    Read `main_matriz.py` lines 2093-2114. The call is:
    ```python
    append_finding(
        _PKG,
        fid=fid,
        class_="SHAPE",
        surface="sync",
        status="EXPECTED",
        title="prod-vs-remarkets divergence acknowledged",
        expected=(...),
        actual=(...),
        diff="N/A (acknowledged limitation, not detected drift)",
        base_url=base,
    )
    ```
    Add `idempotent_by_title=True` as the last kwarg before the closing paren. Keep all other kwargs unchanged.

    **Step 2 — main_iol.py EXPECTED terminal flip (HARN-08):**

    Read `main_iol.py` lines 1430-1442 to confirm the exact `append_finding` call shape inside the auth_401 EXPECTED branch. The title pattern is the immutable string used to mark the EXPECTED auth_401 outcome cross-run. Add `idempotent_by_title=True` as the last kwarg. If the title is dynamic per-run (e.g., includes a timestamp), DO NOT add the kwarg — flag it in the SUMMARY for operator review.

    **Step 3 — main_higyrus.py EXPECTED terminal flip (HARN-08):**

    Read `main_higyrus.py` lines 1997-2008. Same pattern as iol (auth_401 EXPECTED). Add `idempotent_by_title=True`.

    **Step 4 — main_ambito_financiero.py EXPECTED terminal flip (HARN-08):**

    Read `main_ambito_financiero.py` lines 590-602. The antibot EXPECTED terminal. Add `idempotent_by_title=True`.

    **Step 5 — Audit any OTHER repeating-title append_finding calls across the 4 drivers.** Run:
    ```bash
    grep -nE "title=\"[^\"]+\"" main_*.py | sort -k 2 -t= | uniq -f1 -c | awk '$1 > 1'
    ```
    For any title appearing in 2+ append_finding calls within the SAME driver where the status is `EXPECTED` or `NO-FIX`, flip those too. Document each flip in the commit body.

    **Step 6 — Manual driver-level dry run (no live HTTP):**
    ```bash
    uv run python -c "
    import tempfile, pathlib
    from unittest.mock import patch
    # Use monkeypatched _FINDINGS_DIR via direct module mutation:
    import verification.findings
    tmp = pathlib.Path(tempfile.mkdtemp())
    verification.findings._FINDINGS_DIR = tmp
    from verification.findings import append_finding
    # Simulate the D-MATZ-27 call 3×:
    for i in range(3):
        append_finding('matriz-client', fid=f'F-{i:02d}', class_='SHAPE', surface='sync', status='EXPECTED', title='prod-vs-remarkets divergence acknowledged', expected='e', actual='a', diff='d', idempotent_by_title=True)
    text = (tmp / 'matriz-client-findings.md').read_text()
    assert text.count('prod-vs-remarkets divergence acknowledged') == 1, f'count={text.count(\"prod-vs-remarkets divergence acknowledged\")}'
    print('OK — 3 calls produce 1 occurrence')
    "
    ```

    **Step 7 — Re-run full verification suite to confirm nothing breaks:**
    ```bash
    uv run pytest -q
    ```

    **Step 8 — Atomic commit:**
    ```
    feat(11-01): content-addressed dedupe across 4 drivers (HARN-08 + HARN-10)

    - main_matriz.py:2097 — D-MATZ-27 EXPECTED terminal — idempotent_by_title=True (HARN-10)
    - main_iol.py:1435 — auth_401 EXPECTED terminal — idempotent_by_title=True (HARN-08)
    - main_higyrus.py:2001 — auth_401 EXPECTED terminal — idempotent_by_title=True (HARN-08)
    - main_ambito_financiero.py:595 — antibot EXPECTED terminal — idempotent_by_title=True (HARN-08)
    - Driver-side dry-run: 3× calls produce 1 finding occurrence (verified)
    - Closes: HARN-08 (cross-driver content-addressed dedupe), HARN-10 (matriz D-MATZ-27 EXPECTED terminal idempotent)
    ```
  </action>
  <verify>
    <automated>grep -c "idempotent_by_title=True" main_matriz.py main_iol.py main_higyrus.py main_ambito_financiero.py | awk -F: '{print $1": "$2}' && uv run pytest -q 2>&1 | tail -3 && uv run ruff check main_matriz.py main_iol.py main_higyrus.py main_ambito_financiero.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "idempotent_by_title=True" main_matriz.py` returns ≥ 1 (D-MATZ-27 terminal flipped)
    - `grep -c "idempotent_by_title=True" main_iol.py` returns ≥ 1 (auth_401 EXPECTED flipped)
    - `grep -c "idempotent_by_title=True" main_higyrus.py` returns ≥ 1 (auth_401 EXPECTED flipped)
    - `grep -c "idempotent_by_title=True" main_ambito_financiero.py` returns ≥ 1 (antibot EXPECTED flipped)
    - `awk '/prod-vs-remarkets divergence acknowledged/{n++} END{exit n==1?0:1}' main_matriz.py` exits 0 (title string appears exactly once in source — the single D-MATZ-27 call site)
    - `uv run pytest -q 2>&1 | tail -3 | grep -v "0 failed"` does NOT match "failed" with count > 0 (no test regression)
    - `uv run ruff check main_matriz.py main_iol.py main_higyrus.py main_ambito_financiero.py` exits 0
    - Driver-side dry-run from Step 6 exits 0 with message `OK — 3 calls produce 1 occurrence`
    - `grep -E "status=\"(EXPECTED|NO-FIX)\"" main_matriz.py main_iol.py main_higyrus.py main_ambito_financiero.py | wc -l` matches `grep "idempotent_by_title=True" main_matriz.py main_iol.py main_higyrus.py main_ambito_financiero.py | wc -l` (every EXPECTED/NO-FIX terminal in scope has the flag) — if mismatch, document the exemption (e.g., dynamic-title finding) in SUMMARY
  </acceptance_criteria>
  <done>
    The 4 drivers each have at least one `append_finding(..., idempotent_by_title=True)` call site at the EXPECTED/NO-FIX terminal positions identified by the read_first sources. Dry-run confirms 3× re-runs of the matriz D-MATZ-27 terminal produce exactly 1 finding occurrence. Full pytest suite still GREEN. ruff clean. Atomic commit emitted per template.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (operator-mediated) | `.planning/verification/<pkg>-findings.md` files are version-controlled artifacts edited by both drivers and operators; the BEGIN/END marker contract is the data-integrity boundary |
| Python `verification.findings` module API | Drivers call `append_finding(...)` from main_*.py; misuse (wrong fid, malformed title) is the input-validation boundary |
| temp filesystem (test isolation) | Tests use `monkeypatch.setattr("verification.findings._FINDINGS_DIR", tmp_path)` to avoid mutating baseline files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `verification/findings.py::_parse_findings` BEGIN/END state machine | mitigate | State machine MUST default `in_auto_zone=True` for files without markers (back-compat); test `test_markers_present_in_freshly_written_file` + 4 migrated baseline files exercise both paths; round-trip parse asserts findings list preserved pre/post migration. ASVS L1 V5 (output encoding/integrity). |
| T-11-02 | Tampering | Operator content destruction by driver re-run (HARN-09) | mitigate | Regression test `test_operator_bullets_inside_findings_survive` asserts operator-added `**Classification:**`/`**Resolution:**` bullets in finding sections WITH status != OPEN are preserved (existing line 473 guard path). Operator content OUTSIDE markers preserved by new `operator_prefix`/`operator_suffix` round-trip. ASVS L1 V5. |
| T-11-03 | Tampering | Title injection / newline injection in `idempotent_by_title` lookup | mitigate | Existing invariant at line 446-447 (`raise ValueError if "\n" in title or "\r" in title`) preserved — the equality check `existing_finding.title == title` is on validated single-line strings. ASVS L1 V5. |
| T-11-04 | Information Disclosure | Findings files may contain redacted credentials / PII | accept | Existing Phase 8 RedactingFilter discipline (LOG-02) covers logged content; findings files are operator-curated and contain only structured fields. No new exposure surface in Phase 11. |
| T-11-05 | Denial of Service | Pathological repeated title floods append_finding with O(N) title scan per call | accept | `idempotent_by_title` lookup is O(N) on existing findings list; N ≤ 100 per package in practice (Phase 5 baseline has < 20 findings per pkg). No throughput issue. |
| T-11-06 | Tampering | Migration task (Task 2) accidentally rewrites operator content | mitigate | Use `Edit` tool (surgical replace) NOT `Write` (full rewrite); verify via `git diff --stat 4d48e07 -- <file>` after each edit. Round-trip parse confirms findings list preserved. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new external dependencies added in this plan. All changes are internal Python source edits. Existing Phase 8 uv.lock unchanged. No Package Legitimacy Gate trigger. |
</threat_model>

<verification>
**Phase-level invariants to preserve (must stay GREEN):**
- Phase 6: fixture-reaches-production guard tests still PASS for 4 packages
- Phase 7: import-linter contracts (`_core.py` does NOT import client.py/aio.py) still PASS
- Phase 8: `verification/test_retry_mutation_gate.py` + `verification/test_logging_no_token_leak.py` + 6 cross-cutting guard tests still PASS
- Phase 9: BUG-01..04 regression tests still PASS
- Phase 10: matriz async cross-leak sentinel test still PASS
- ruff + mypy strict + lint-imports + lint-logging + pytest 3.12 all GREEN before merging

**Plan-specific gates:**
- `uv run pytest verification/test_findings_append_only.py verification/test_findings_dedupe_by_title.py -q` exits 0 with ≥ 12 test cases passing
- `uv run pytest verification/ -q` exits 0 (no existing verification test broken)
- `uv run pytest -q` exits 0 (full suite GREEN — no cross-package regression)
- 4 baseline `<pkg>-findings.md` files each contain exactly 1 BEGIN/END marker pair
- 4 drivers each contain ≥ 1 `idempotent_by_title=True` flag at the identified EXPECTED/NO-FIX terminal call sites
</verification>

<success_criteria>
**Goal-backward — what MUST be TRUE at end of plan:**
1. `verification/findings.py` extended with `_ParsedFile.operator_prefix`/`operator_suffix` fields + BEGIN/END zone state machine in `_parse_findings` + marker emission in `_serialize_findings` + `idempotent_by_title` kwarg in `append_finding` (HARN-07/08)
2. `verification/test_findings_append_only.py` exists with ≥ 4 tests (operator prefix/suffix/bullets survive, markers present); all GREEN (HARN-07/09)
3. `verification/test_findings_dedupe_by_title.py` exists with ≥ 3 tests parametric × 4 packages = ≥ 12 cases; all GREEN (HARN-08/10)
4. 4 baseline `.planning/verification/<pkg>-findings.md` files migrated with BEGIN/END markers injected — operator content byte-preserved per `git diff` (HARN-07 migration)
5. 4 drivers (main_matriz.py, main_iol.py, main_higyrus.py, main_ambito_financiero.py) flip identified EXPECTED/NO-FIX terminal `append_finding` calls to `idempotent_by_title=True` (HARN-08 + HARN-10)
6. Full pytest suite (Python 3.12) GREEN; ruff + mypy strict clean on all touched files
7. 3 atomic commits per template (Task 1 RED→GREEN, Task 2 migration, Task 3 driver adoption)
</success_criteria>

<output>
Create `.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md` when done. Include:
- Per-task atomic commit SHAs
- Test count delta (≥ +12 new test cases)
- File migration evidence (`git diff --stat 4d48e07 -- .planning/verification/<pkg>-findings.md` per file)
- Driver flip evidence (grep `idempotent_by_title=True` × 4 drivers count)
- Confirmation that Phase 6-10 invariants stay GREEN (cited tests still pass)
</output>
