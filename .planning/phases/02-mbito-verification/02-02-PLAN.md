---
phase: 02-mbito-verification
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - main_ambito_financiero.py
autonomous: true
requirements: [AMB-01, AMB-02, AMB-03, AMB-04, AMB-05, AMB-06, DRIFT-01]
tags: [verification, driver, probes, live-api, schema-snapshot]
must_haves:
  truths:
    - "main_ambito_financiero.py expone 7 sub-funciones probe_* al tope (D-01): probe_happy_sync, probe_happy_async, probe_parity_sync_async, probe_parse_decimal_adversarial, probe_no_data, probe_schema_snapshot, probe_antibot"
    - "main() invoca los probes en orden (D-13: probe_antibot último)"
    - "Cada probe maneja sus propias excepciones; el driver continúa todos los probes y exit 0 salvo crash inesperado (D-04)"
    - "Output a stdout = una línea verbatim por probe (D-02) + summary final con conteos por estado (PASS/FAIL/SKIPPED/FINDING)"
    - "Driver llama write_findings('ambito-financiero-client') al inicio (idempotente, D-03)"
    - "Cada hallazgo OPEN detectado durante el run llama append_finding(...) refrescando el ART block (D-10)"
    - "probe_antibot opt-in via VERIFY_ANTIBOT=1; sin la var imprime SKIPPED y sigue (D-12)"
    - "probe_antibot es one-shot, sin retry ni sleep (D-14); usa try/finally para restaurar GOOD_UA aun con excepción (D-15)"
    - "probe_antibot sólo sync (D-17); BAD_UA = f'python-httpx/{httpx.__version__}' (D-16)"
    - "Async lifecycle: un único asyncio.run(_async_main()) al final; _async_main termina con await aio.aclose() (D-11)"
    - "probe_schema_snapshot escribe .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json — ubicación lockeada (D-19: convención `.planning/verification/schemas/<pkg>/<slug>.json`, sub-dir por paquete que escala a Phases 3-5) y slug derivado del nombre de la función pública del cliente en kebab-case (D-20: `get_dollar_banco_nacion` → `get-dollar-banco-nacion.json`) — con el envelope D-21 si no existe; NO sobreescribe si existe y difiere (D-25); emite finding SHAPE OPEN en caso de drift"
    - "Todos los prints pasan por safe_print(text, secrets=[]) (D-26) — lista vacía OK para Ámbito sin credenciales"
    - "Helpers de fecha derivados de today: _last_business_day (existente) y _last_business_day_with_day_gt_12 (nuevo, D-24); probe_no_data usa today + 60d (D-24)"
  artifacts:
    - path: "main_ambito_financiero.py"
      provides: "Rewritten Ámbito live verification driver"
      contains: "def probe_happy_sync"
      contains_all: ["probe_happy_sync", "probe_happy_async", "probe_parity_sync_async", "probe_parse_decimal_adversarial", "probe_no_data", "probe_schema_snapshot", "probe_antibot"]
  key_links:
    - from: "main_ambito_financiero.py"
      to: "verification.findings.append_finding"
      via: "from verification.findings import append_finding"
      pattern: "from verification.findings import append_finding"
    - from: "main_ambito_financiero.py"
      to: "verification.write_findings + safe_print + schema_of"
      via: "from verification import ..."
      pattern: "from verification import .*write_findings"
    - from: "main_ambito_financiero.py"
      to: "ambito_financiero_client (sync surface + aio submódulo)"
      via: "import ambito_financiero_client as ambito; from ambito_financiero_client import aio"
      pattern: "import ambito_financiero_client"
---

<objective>
Re-escribir `main_ambito_financiero.py` (driver vivo del paquete) desde el smoke-test actual a 7 probes nombrados que cierran el loop completo de verificación viva sobre `mercados.ambito.com`: happy sync, happy async, parity sync↔async, parse_ar_decimal adversarial (D-23 doble check), no_data, schema_snapshot (DRIFT-01 — primer snapshot estructural committeable), y antibot (opt-in, one-shot). Todos los probes alimentan el findings file vía `append_finding` (del plan 02-01) y emiten líneas verbatim de status a stdout más un summary final. El driver es la columna vertebral de Phase 2 — los Slices 1 (append_finding) y 3 (tests mockeados) son DRY foundation + locking respectivamente; éste es el slice que **ejercita el cliente en vivo**.

Purpose: Cumplir AMB-01..AMB-06 + DRIFT-01 corriendo el cliente Ámbito en vivo y produciendo, de forma reproducible y no-destructiva, dos artefactos committeable (findings markdown + schema snapshot JSON) que se commitearán en el siguiente plan.

Output: `main_ambito_financiero.py` reescrito; al invocarlo a mano (`python main_ambito_financiero.py`), emite 7 líneas `PROBE <name>: <status>` + summary + exit 0; genera/actualiza `.planning/verification/ambito-financiero-client-findings.md` y `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` (DRIFT-01) en su primera corrida exitosa. NO se ejecuta dentro del suite pytest (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-mbito-verification/02-CONTEXT.md
@.planning/phases/02-mbito-verification/02-RESEARCH.md
@.planning/phases/02-mbito-verification/02-PATTERNS.md
@.planning/phases/02-mbito-verification/02-VALIDATION.md
@.planning/phases/02-mbito-verification/02-01-PLAN.md
@.planning/phases/01-safety-harness-verification-infrastructure/01-03-SUMMARY.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_ambito_financiero.py
@main_higyrus.py
@main_verify.py
@verification/findings.py
@verification/__init__.py
@verification/schema.py
@verification/capture.py
@verification/mutation_gate.py
@verification/redaction.py
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py
@packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py
@packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Rewrite main_ambito_financiero.py with 7 named probes + summary</name>
  <files>main_ambito_financiero.py</files>
  <read_first>
    - main_ambito_financiero.py (estado actual: smoke test mínimo con `_last_business_day` helper reusable en líneas 17-22 y main() en 25-34)
    - main_higyrus.py (analog principal: driver con `from verification import require_env, safe_print` + uso de `safe_print(text, secrets=[...])` líneas 17, 31-44)
    - main_verify.py (analog secundario: orchestrator con summary `RAN/SKIPPED/FAILED` por paquete líneas 83-97 — patrón del summary a adaptar)
    - .planning/phases/02-mbito-verification/02-PATTERNS.md (sección 3 "main_ambito_financiero.py — REWRITE COMPLETO" con código exacto del driver, ProbeResult, helpers de fecha, probe_antibot try/finally, probe_schema_snapshot envelope completo; sección "Shared Patterns" con safe_print + acceso a estado _module-private del cliente)
    - .planning/phases/02-mbito-verification/02-RESEARCH.md (sección "Pattern 2: Driver con sub-funciones nombradas + summary acumulado" con bosquejo completo y Pattern 3 anti-bot y Pattern 4 schema snapshot)
    - .planning/phases/02-mbito-verification/02-CONTEXT.md (D-01..D-26 lockeadas; específicamente D-01 nombres de probes, D-02 verbatim status strings, D-13 orden anti-bot último, D-21 envelope JSON, D-22 single file current, D-23 doble check parse_decimal, D-24 fechas derivadas, D-25 drift no sobreescribe)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (referencia: `_DEFAULT_USER_AGENT` línea 36-39, `configure` 47-54, `_request` 66-69, `get_dollar_banco_nacion` 72-88, `_base_url` 42 — el driver lee `ambito.client._DEFAULT_USER_AGENT` y `ambito.client._base_url`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (referencia: `aclose()` 69-74 — `_async_main` debe terminar con `await aio.aclose()`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py (parse_ar_decimal: `float(value.replace(".", "").replace(",", "."))` — necesario para verificar D-23 sanity de rango)
    - packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py (AmbitoFinancieroAuthError, AmbitoFinancieroNoDataError — `probe_no_data` espera AmbitoFinancieroNoDataError; `probe_antibot` espera AmbitoFinancieroAuthError con status_code=403)
    - verification/mutation_gate.py (precedente para acceso a `_base_url` privado del cliente vía `matriz_client.client._base_url` línea 55; lectura-only y comentario explícito)
    - verification/redaction.py (safe_print firma exacta `safe_print(text: str, secrets: list[str]) -> None` línea 43; comportamiento con secrets=[])
    - verification/schema.py (schema_of firma y comportamiento PII-free)
    - verification/findings.py + verification/__init__.py (después de Task 1.1 y 1.2: append_finding disponible vía barrel y directo)
    - .planning/verification/FINDINGS-TEMPLATE.md (clases fijas D-09 — el driver SOLO debe usar `class_` ∈ {SHAPE, AUTH, ERROR-MAP, PARAM, SYNC-ASYNC-DRIFT, NO-DATA, ANTI-BOT}; estados D-08 — OPEN para drift detectado, EXPECTED para anti-bot 403 esperado)
  </read_first>
  <action>
    Re-escribir completamente `main_ambito_financiero.py` siguiendo el bosquejo de 02-PATTERNS.md sección 3 y 02-RESEARCH.md Pattern 2/3/4. Mantener `_last_business_day` actual (líneas 17-22) tal cual.

    **Imports al tope** (orden: future → stdlib → third-party → local; alfabético dentro de grupo). Necesarios:
    - `from __future__ import annotations`
    - stdlib: `asyncio`, `datetime as dt`, `json`, `os`, `dataclasses.dataclass`, `pathlib.Path`
    - third-party: `httpx`
    - local: `ambito_financiero_client as ambito`, `from ambito_financiero_client import aio`, `from ambito_financiero_client._parsing import parse_ar_decimal`
    - verification: `from verification import safe_print, schema_of, write_findings` (barrel, sigue patrón main_higyrus.py:17), y `from verification.findings import append_finding` (directo — opcional via barrel también si Task 1.2 lo expuso, en cuyo caso ambos paths son válidos; preferir `from verification import append_finding` por uniformidad)

    **Module docstring** describiendo los 7 probes en orden (D-01), opt-in `VERIFY_ANTIBOT=1` (D-12), y los dos artefactos generados (`.planning/verification/ambito-financiero-client-findings.md` + `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json`).

    **Constantes module-level** (después de imports y docstring):
    - `_PKG = "ambito-financiero-client"`
    - `_REPO_ROOT = Path(__file__).resolve().parent`
    - `_SCHEMA_DIR = _REPO_ROOT / ".planning" / "verification" / "schemas" / _PKG`
    - `_SCHEMA_FILE = _SCHEMA_DIR / "get-dollar-banco-nacion.json"`
    - `_VENTA_MIN, _VENTA_MAX = 100.0, 100_000.0` (bounds D-23 draft; ajustables si causan falsos positivos durante implementación, comentario explícito)

    NOTA Discretion (Open Question 1 / 2 de 02-RESEARCH.md): para el campo `_GOOD_UA` no almacenarlo como constante module-level — leerlo dinámicamente dentro de `probe_antibot` con `good_ua = ambito.client._DEFAULT_USER_AGENT` para evitar Pitfall 8 (UA duplicado que queda desincronizado si client.py cambia).

    **Dataclass `ProbeResult`** (frozen, slots — analog `verification/anonymize.py:34-46`):
    ```
    @dataclass(frozen=True, slots=True)
    class ProbeResult:
        name: str
        status: str   # "PASS" | "FAIL" | "SKIPPED" | "FINDING"
        detail: str
    ```

    **Helpers de fecha**:
    - Mantener `_last_business_day(today: dt.date) -> dt.date` tal cual (líneas 17-22 actuales)
    - Agregar `_last_business_day_with_day_gt_12(today: dt.date) -> dt.date` (D-24 AMB-03): retrocede desde `_last_business_day(today)` día a día hasta que `date.day > 12`, saltando fines de semana

    **Los 7 probes** (firmas con tipo de retorno explícito por mypy strict; cada uno captura sus excepciones internamente para satisfacer D-04):

    1. `probe_happy_sync(today: dt.date) -> tuple[ProbeResult, list[list[str]] | None]` — usa `_last_business_day(today)`; llama `ambito.client._request("GET", f"/dolarnacion/historico-general/{fecha:%Y-%m-%d}/{fecha:%Y-%m-%d}")` para obtener el `httpx.Response` crudo y `rows = resp.json()`; valida shape `isinstance(rows, list)` y `len(rows) >= 2` y `rows[0] == ["Fecha", "Compra", "Venta"]`; también llama `ambito.get_dollar_banco_nacion(fecha)` para cross-check del API público. Retorna `(ProbeResult("happy_sync", "PASS", f"precio={precio}"), rows)` en happy path. Si shape falla: `append_finding(..., class_="SHAPE", surface="sync", status="OPEN", fid="F-NN", title="...", ...)` y retorna `(ProbeResult("happy_sync", "FINDING", "F-NN (OPEN)"), None)`. Si auth/network falla: clase apropiada (AUTH, ERROR-MAP, ANTI-BOT). El driver asigna `fid` deterministicamente (counter sequence o derivado del nombre del probe — Claude's Discretion; recomendado: contador module-level `_next_fid()` que va F-01, F-02, ... a través de probes).

    2. `probe_happy_async(today: dt.date) -> ProbeResult` — async function (será awaited en `_async_main`). Espejo de `probe_happy_sync` usando `await aio._request(...)` y `await aio.get_dollar_banco_nacion(...)`. NO retorna rows (sync ya los capturó). Misma estructura try/except + append_finding.

    3. `probe_parity_sync_async(today: dt.date, rows_sync: list[list[str]] | None, precio_async: float | None) -> ProbeResult` — compara el `precio` sync vs `precio_async` y el shape de `rows_sync` con la expectativa. Si difieren, emite finding clase `SYNC-ASYNC-DRIFT` OPEN. Si alguno de los inputs es None (probe anterior falló): `ProbeResult("parity_sync_async", "SKIPPED", "<reason from previous probe>")`. Test estructural: mismos floats al parsear, misma shape (`type` y header row idéntico).

    4. `probe_parse_decimal_adversarial(rows_sync: list[list[str]] | None) -> ProbeResult` — usa `rows_sync` capturado por `probe_happy_sync` (si None, `SKIPPED`). Extrae `venta_raw = rows_sync[1][2]` (string crudo de Venta). Doble check (D-23):
       - **Estructural:** if `"," not in venta_raw`: `append_finding(class_="PARAM", surface="both", status="OPEN", title="wire emite dot-decimal en lugar de AR-decimal", expected="separador decimal ','", actual=f"venta_raw={venta_raw!r}", ...)` → `ProbeResult("parse_decimal", "FINDING", "F-NN (OPEN)")`
       - **Rango plausible:** `venta_parseada = parse_ar_decimal(venta_raw)`; if `not (_VENTA_MIN <= venta_parseada <= _VENTA_MAX)`: `append_finding(class_="SHAPE", surface="both", status="OPEN", title="venta parseada fuera de rango plausible 100..100000", expected=f"{_VENTA_MIN}..{_VENTA_MAX}", actual=f"{venta_parseada}", ...)` → `FINDING`
       - Happy path: `ProbeResult("parse_decimal", "PASS", f"venta={venta_parseada}")`

    5. `probe_no_data(today: dt.date) -> ProbeResult` — usa `fecha_futura = today + dt.timedelta(days=60)` (D-24 draft); llama `ambito.get_dollar_banco_nacion(fecha_futura)`; espera que levante `ambito.AmbitoFinancieroNoDataError`. Si levanta: `ProbeResult("no_data", "PASS", f"NoDataError para {fecha_futura}")`. Si no levanta (retorna float): `append_finding(class_="NO-DATA", surface="sync", status="OPEN", title="fecha futura devolvió cotización en lugar de NoDataError", expected=f"AmbitoFinancieroNoDataError para {fecha_futura}", actual=f"precio={returned}", ...)` → `FINDING`. Si levanta otra excepción: finding clase ERROR-MAP OPEN.

    6. `probe_schema_snapshot(today: dt.date, rows_sync: list[list[str]] | None) -> ProbeResult` — Pattern 4 de 02-RESEARCH.md / 02-PATTERNS.md sección 6. Si `rows_sync is None` (probe_happy_sync falló): `SKIPPED`. Sino:
       - `actual_schema = schema_of(rows_sync)`
       - `envelope = {"endpoint": "/dolarnacion/historico-general/{from}/{to}", "client_function": "get_dollar_banco_nacion", "captured_at": dt.datetime.now(dt.UTC).isoformat(), "base_url": ambito.client._base_url, "sample_date": fecha.isoformat(), "schema": actual_schema}` (D-21 verbatim, orden insertion preserve per Open Question 2)
       - `_SCHEMA_DIR.mkdir(parents=True, exist_ok=True)`
       - if `not _SCHEMA_FILE.exists()`: write `json.dumps(envelope, indent=2, ensure_ascii=False) + "\n"` (con trailing newline UNIX) → `ProbeResult("schema_snapshot", "PASS", f"escrito {_SCHEMA_FILE.name}")`
       - else: `committed = json.loads(_SCHEMA_FILE.read_text(encoding="utf-8"))`; if `committed["schema"] == actual_schema`: `ProbeResult("schema_snapshot", "PASS", "schema sin drift")`
       - else (D-25 DRIFT — no sobreescribe): `append_finding(class_="SHAPE", surface="both", status="OPEN", title="Schema drift en get_dollar_banco_nacion", expected=json.dumps(committed["schema"]), actual=json.dumps(actual_schema), diff="ver expected vs actual", base_url=ambito.client._base_url)` → `ProbeResult("schema_snapshot", "FINDING", "F-NN (OPEN) — NO sobreescribe")`

    7. `probe_antibot(today: dt.date) -> ProbeResult` — Pattern 3 de 02-RESEARCH.md / 02-PATTERNS.md sección 3 "Anti-bot configure try/finally pattern" + D-12/D-13/D-14/D-15/D-16/D-17/D-18.
       - Gate: `if os.getenv("VERIFY_ANTIBOT") != "1": return ProbeResult("antibot", "SKIPPED", "(opt-in via VERIFY_ANTIBOT=1)")`
       - `fecha = _last_business_day(today)`; `bad_ua = f"python-httpx/{httpx.__version__}"` (D-16); `good_ua = ambito.client._DEFAULT_USER_AGENT` (lectura dinámica, Pitfall 8)
       - `try: ambito.configure(user_agent=bad_ua); try: ambito.get_dollar_banco_nacion(fecha); ...` (interno try/except cubre 3 ramas D-18)
       - Si **NO** levanta excepción (200 OK con BAD_UA) → D-18 OPEN: `append_finding(class_="ANTI-BOT", surface="sync", status="OPEN", title="UA inválido NO recibió 403 (defensa relajada)", expected="403 con UA=python-httpx/...", actual="200 OK (defensa relajada)", ...)` → FINDING (OPEN)
       - Si levanta `ambito.AmbitoFinancieroAuthError` con `exc.args[0] == 403` o `status_code` attribute == 403 (revisar la jerarquía: en exceptions.py el constructor es `AuthError(status_code, text)`, accesible vía `exc.args[0]` y/o el atributo guardado; usar `getattr(exc, "status_code", None)` con fallback a `exc.args[0]`) → D-14 EXPECTED terminal: `append_finding(class_="ANTI-BOT", surface="sync", status="EXPECTED", title="UA inválido recibe 403", expected="403", actual="403", diff="ninguno; comportamiento esperado", ...)` → FINDING (EXPECTED)
       - Si levanta cualquier otra cosa (429, timeout, network) → D-18 OPEN con detalle exacto: `append_finding(class_="ANTI-BOT", surface="sync", status="OPEN", title=f"UA inválido produjo {type(exc).__name__}", expected="403", actual=repr(exc), ...)` → FINDING (OPEN)
       - `finally: ambito.configure(user_agent=good_ua)` (D-15 — restaurar SIEMPRE)
       - **D-14: una sola llamada, sin retry ni sleep, no `time.sleep`, no `for _ in range(...)` loop.**

    **`_async_main(today: dt.date, results: list[ProbeResult]) -> None`** (D-11):
    - Único event loop
    - Try block:
      - `precio_async_result = await probe_happy_async(today)` (capturar el `ProbeResult`)
      - `results.append(precio_async_result)`
      - (Extraer el float async — si `probe_happy_async` retorna directamente el ProbeResult sin el float, una opción es que `probe_happy_async` retorne `tuple[ProbeResult, float | None]` paralelo al sync; otra es que `probe_parity_sync_async` lo llame nuevamente. Recomendación Discretion: `probe_happy_async` retorna `tuple[ProbeResult, float | None]` y el async_main pasa el float a `probe_parity_sync_async` después de awaited)
    - `finally: await aio.aclose()` (Pitfall 5: nunca olvidar; try/finally garantiza cierre aun con excepción)

    **`main() -> None`** orden de probes (D-13):
    1. `today = dt.date.today()`
    2. `write_findings(_PKG)` (idempotente, D-03)
    3. `results: list[ProbeResult] = []`
    4. `result_happy_sync, rows_sync = probe_happy_sync(today)` ; `results.append(result_happy_sync)`
    5. `result_happy_async, precio_async = asyncio.run(_async_main_wrapper(today))` — single event loop; el wrapper compone los probes async y retorna lo capturado (Discretion sobre la firma exacta, pero la regla es **un único `asyncio.run`** por D-11)
    6. `results.append(probe_parity_sync_async(today, rows_sync, precio_async))`
    7. `results.append(probe_parse_decimal_adversarial(rows_sync))`
    8. `results.append(probe_no_data(today))`
    9. `results.append(probe_schema_snapshot(today, rows_sync))`
    10. `results.append(probe_antibot(today))`  **← último, D-13**
    11. Para cada `r` en `results`: `safe_print(f"PROBE {r.name}: {r.status} {r.detail}".rstrip(), secrets=[])` (D-26 + D-02 verbatim)
    12. Summary final: contar estados; emitir `safe_print(f"SUMMARY: PASS={n_pass} FAIL={n_fail} SKIPPED={n_skip} FINDING={n_find}", secrets=[])` (D-02 verbatim conteos por estado)

    **Pre-condiciones / invariantes obligatorios:**
    - `if __name__ == "__main__": main()` al final (igual que el actual)
    - Cada `probe_*` con tipo de retorno explícito (mypy strict)
    - No usar `print(...)` directo; todo prints pasan por `safe_print(...)` con `secrets=[]` (D-26, Pattern de 02-PATTERNS.md "Shared Patterns")
    - El driver lee `ambito.client._base_url` y `ambito.client._DEFAULT_USER_AGENT` (estado privado del cliente, precedente harness vía `mutation_gate.py:55` — usar comentario "# estado resuelto en vivo; sólo lectura" para documentar)
    - No fabricar bug findings durante la implementación: SI los probes corren OK contra el server vivo y no detectan discrepancias, no se llama `append_finding`. El plan se valida en el siguiente plan con `python main_ambito_financiero.py` (live run).

    **Lo que NO va en este plan:**
    - NO correr el driver (se hace en 02-03 con checkpoint humano)
    - NO commitear `.planning/verification/schemas/...` ni `.planning/verification/ambito-financiero-client-findings.md` (se hace en 02-03 después de inspección humana de los artefactos generados)
    - NO escribir tests mockeados (se hace en 02-03 — D-08 invariantes)
    - NO aplicar fixes a `client.py` ni `aio.py` (depende de findings reales que solo aparecen al correr; opportunistic per nota MVP del contexto)

    Convenciones obligatorias (CONVENTIONS.md / CLAUDE.md):
    - `from __future__ import annotations` primera línea (mandatory)
    - ruff line-length=100, double quotes, 4 espacios
    - mypy --strict: todas las firmas completas con retorno explícito
    - No wildcard / no relative imports

    Commit sugerido: `feat(02-02): rewrite main_ambito_financiero.py with 7 named probes (D-01..D-26)`
  </action>
  <verify>
    <automated>uv run python -c "import main_ambito_financiero; assert hasattr(main_ambito_financiero, 'probe_happy_sync'); assert hasattr(main_ambito_financiero, 'probe_happy_async'); assert hasattr(main_ambito_financiero, 'probe_parity_sync_async'); assert hasattr(main_ambito_financiero, 'probe_parse_decimal_adversarial'); assert hasattr(main_ambito_financiero, 'probe_no_data'); assert hasattr(main_ambito_financiero, 'probe_schema_snapshot'); assert hasattr(main_ambito_financiero, 'probe_antibot'); assert hasattr(main_ambito_financiero, 'main'); assert hasattr(main_ambito_financiero, '_last_business_day_with_day_gt_12'); print('all 9 entries present')"</automated>
    <automated>uv run mypy main_ambito_financiero.py</automated>
    <automated>uv run ruff check main_ambito_financiero.py</automated>
    <automated>uv run ruff format --check main_ambito_financiero.py</automated>
    <automated>uv run python -c "import ast, sys; tree = ast.parse(open('main_ambito_financiero.py').read()); src = open('main_ambito_financiero.py').read(); assert 'time.sleep' not in src, 'D-14: no sleep in probe_antibot'; assert src.count('asyncio.run(') == 1, 'D-11: single event loop'; print('static checks OK')"</automated>
    <automated>grep -v '^#' main_ambito_financiero.py | grep -c 'safe_print('</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_ambito_financiero.py contains def probe_happy_sync(`
    - Source: `main_ambito_financiero.py contains async def probe_happy_async(` (or `def probe_happy_async` if returning Coroutine — Claude's choice; both acceptable)
    - Source: `main_ambito_financiero.py contains def probe_parity_sync_async(`
    - Source: `main_ambito_financiero.py contains def probe_parse_decimal_adversarial(`
    - Source: `main_ambito_financiero.py contains def probe_no_data(`
    - Source: `main_ambito_financiero.py contains def probe_schema_snapshot(`
    - Source: `main_ambito_financiero.py contains def probe_antibot(`
    - Source: `main_ambito_financiero.py contains def _last_business_day_with_day_gt_12(`
    - Source: `main_ambito_financiero.py contains def _last_business_day(` (existing helper preserved)
    - Source: `main_ambito_financiero.py contains write_findings(_PKG)` (or with the literal string)
    - Source: `main_ambito_financiero.py contains append_finding(`
    - Source: `main_ambito_financiero.py contains safe_print(`
    - Source: `main_ambito_financiero.py contains schema_of(`
    - Source: `main_ambito_financiero.py contains 'VERIFY_ANTIBOT'`
    - Source: `main_ambito_financiero.py contains f"python-httpx/{httpx.__version__}"` (o equivalent: `httpx.__version__` referenced + `python-httpx/` literal string)
    - Source: `main_ambito_financiero.py contains aclose()` (await aio.aclose())
    - Source: `main_ambito_financiero.py contains try:` AND `finally:` cercanos (try/finally para anti-bot configure restore)
    - Source: `main_ambito_financiero.py contains get-dollar-banco-nacion.json`
    - Source: `main_ambito_financiero.py contains "SUMMARY:"` (string verbatim del summary)
    - Source: `main_ambito_financiero.py contains "PROBE "` (string verbatim del per-probe line)
    - Source: `main_ambito_financiero.py contains from __future__ import annotations` (CONVENTIONS mandatory)
    - Behavior: `uv run python -c "import main_ambito_financiero"` exits 0 (no syntax/import errors)
    - Behavior: el import script de acceptance ("all 9 entries present") exits 0
    - Static: `uv run mypy main_ambito_financiero.py` exits 0 con "Success"
    - Static: `uv run ruff check main_ambito_financiero.py` exits 0
    - Static: `uv run ruff format --check main_ambito_financiero.py` exits 0
    - Source assertion: archivo NO contiene `time.sleep` (D-14: one-shot sin retry/sleep)
    - Source assertion: archivo contiene exactamente UN `asyncio.run(` (D-11: un solo event loop)
    - Behavior: `grep -v '^#' main_ambito_financiero.py | grep -c 'safe_print('` >= 2 (al menos para la línea per-probe + la línea SUMMARY)
    - Behavior: NO se ejecutan probes vivos en este task (solo se escribe el driver); el primer live run ocurre en plan 02-03
    - Non-regression: `uv run pytest -q` (suite completa) exits 0 (el driver NO entra al suite por D-05 — no tests file lo importan; sólo el smoke import del verify del propio acceptance criterion)
  </acceptance_criteria>
  <done>main_ambito_financiero.py reescrito con 7 probes nombrados, summary, asyncio.run único, try/finally para anti-bot, schema snapshot con drift detection D-25, append_finding usado para findings OPEN. mypy + ruff verdes. Suite pre-existente sin regresiones.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → mercados.ambito.com (live HTTP) | Llamadas read-only contra API pública; el cliente usa UA hardcodeado de browser; sin auth |
| driver → ambito.configure() (mutates module state) | probe_antibot temporariamente muta `_user_agent`; debe restaurar en `finally` |
| driver → .planning/verification/schemas/ (committeable JSON) | Snapshot PII-free por construcción (`schema_of` solo emite keys+types); NO sobreescribe en drift (D-25) |
| driver → .planning/verification/<pkg>-findings.md (committeable MD) | append_finding (del plan 02-01) garantiza preservación de status humano |
| driver stdout | safe_print(text, secrets=[]) — segunda capa Bearer regex aun sin secrets explícitos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Denial of Service (against self / IP-ban) | probe_antibot que hiciera múltiples llamadas con UA inválida | mitigate | D-12 opt-in `VERIFY_ANTIBOT=1` + D-14 one-shot (sin retry/sleep) + D-13 último en la secuencia. Acceptance criterion verifica que el archivo NO contiene `time.sleep`. |
| T-2-07 | Tampering | probe_antibot deja el cliente con BAD_UA si la excepción no se atrapa | mitigate | D-15 try/finally explícito con `ambito.configure(user_agent=good_ua)` en `finally`. Acceptance criterion verifica patrón try+finally. |
| T-2-08 | Tampering | schema snapshot sobreescribe baseline cuando hay drift sin gesto humano | mitigate | D-25: `if not _SCHEMA_FILE.exists(): write else compare`; en caso de drift NO write — append_finding SHAPE OPEN. La promoción a "drift aceptado" requiere edición manual del JSON (Open Question 3 resuelta: edición manual + re-run). |
| T-2-09 | Information Disclosure | Driver imprime payload crudo con campos inesperados | mitigate | D-26 `safe_print(text, secrets=[])` envuelve TODOS los prints; el regex `_BEARER` de defense-in-depth (verification/redaction.py línea 31) cubre tokens reflejados aun sin secrets enumerados. Ámbito sin auth = lista vacía OK. |
| T-2-10 | Repudiation | ART block del findings file no refleja el run actual | mitigate | append_finding refresca ART en cada call (T-2-03 del plan 02-01); el driver inicial llama write_findings(_PKG) que crea el esqueleto base. Open Question 1: discreción de Claude sobre refresh_art_block separado vs dentro de append_finding. |
| T-2-11 | Tampering | Driver hardcodea fechas y deja de funcionar cuando el server cambia disponibilidad | mitigate | D-24: helpers `_last_business_day(today)` (existente) y `_last_business_day_with_day_gt_12(today)` (nuevo). Cada run se mueve con el calendario. |
| T-2-12 | Information Disclosure | Schema snapshot committee valores reales en lugar de tipos | mitigate | `schema_of` de Phase 1 (verification/schema.py:36-40) emite ÚNICAMENTE keys+type-names, no valores — PII-free por construcción. Test unitario del helper en Phase 1 ya lo verifica. |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes externos en este plan. Sólo dependencias ya bloqueadas en `uv.lock`. |
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Sintaxis y firmas correctas
uv run python -c "import main_ambito_financiero; [getattr(main_ambito_financiero, p) for p in ('probe_happy_sync','probe_happy_async','probe_parity_sync_async','probe_parse_decimal_adversarial','probe_no_data','probe_schema_snapshot','probe_antibot','main','_last_business_day','_last_business_day_with_day_gt_12')]; print('OK')"

# 2. Static checks limpios
uv run mypy main_ambito_financiero.py
uv run ruff check main_ambito_financiero.py
uv run ruff format --check main_ambito_financiero.py

# 3. Source assertions D-11 + D-14
python -c "src = open('main_ambito_financiero.py').read(); assert 'time.sleep' not in src; assert src.count('asyncio.run(') == 1; print('OK')"

# 4. Sin regresiones del suite existente (el driver NO está en testpaths)
uv run pytest -q
```

NO se corre el driver en este plan (el primer live run con commit de artefactos pasa en el plan 02-03 que tiene el checkpoint humano para inspeccionar findings + schema antes de commitear).

Per Wave 2 merge:
```bash
uv run pytest -q && uv run mypy && uv run ruff check && uv run ruff format --check
```
</verification>

<success_criteria>
- `main_ambito_financiero.py` reescrito con 7 sub-funciones `probe_*` nombradas (D-01) en el orden D-13 (antibot último)
- `_last_business_day_with_day_gt_12` agregado adyacente al `_last_business_day` existente (D-24)
- Único `asyncio.run(...)` en `main()` envolviendo `_async_main` que termina con `await aio.aclose()` (D-11)
- `probe_antibot` con `try/finally` para restaurar `GOOD_UA`, opt-in vía `VERIFY_ANTIBOT=1`, one-shot sin retry/sleep (D-12/D-13/D-14/D-15/D-16/D-17/D-18)
- `probe_schema_snapshot` con drift detection D-25 (NO sobreescribe en drift; emite finding SHAPE OPEN)
- `probe_parse_decimal_adversarial` con doble check estructural + rango plausible (D-23)
- Output stdout: una línea verbatim por probe + `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` final (D-02)
- Todos los prints vía `safe_print(text, secrets=[])` (D-26)
- mypy --strict sin errores en `main_ambito_financiero.py`
- ruff check + format sin errores
- Suite completa sin regresiones (el driver NO está en `testpaths`)
- NO se commitea aún el findings markdown ni el schema JSON (eso ocurre en plan 02-03 después del live run + checkpoint humano)
</success_criteria>

<output>
Crear `.planning/phases/02-mbito-verification/02-02-SUMMARY.md` cuando done, registrando:
- Estructura final del driver (signatures de los 7 probes, tipos de retorno definitivos)
- Esquema interno de propagación de `rows_sync` entre `probe_happy_sync` → otros probes (tuple-return vs module-level cache vs parámetro explícito)
- Decisión sobre `_next_fid()` (counter sequence vs naming convention)
- Cualquier deviación de D-21 envelope (Discretion: Schema exacto)
- Bounds D-23 finales (`_VENTA_MIN`, `_VENTA_MAX`) — pueden haber sido ajustados de 100/100000
- Métrica de duración del plan
</output>
