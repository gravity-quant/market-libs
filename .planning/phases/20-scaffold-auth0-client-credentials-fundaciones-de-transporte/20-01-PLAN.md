---
phase: 20-scaffold-auth0-client-credentials-fundaciones-de-transporte
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/pyproject.toml
  - packages/market-data-client/README.md
  - packages/market-data-client/.env.example
  - packages/market-data-client/src/market_data_client/py.typed
  - packages/market-data-client/src/market_data_client/exceptions.py
  - packages/market-data-client/src/market_data_client/_state.py
  - packages/market-data-client/src/market_data_client/_transport.py
  - packages/market-data-client/src/market_data_client/_atransport.py
autonomous: true
requirements: [AUTH-MD-01, CORE-MD-01]
user_setup: []

must_haves:
  truths:
    - "The package mirrors the `iol-client` private-module layout (D-01 file set): this plan establishes `exceptions.py`, `_state.py`, `_transport.py`, `_atransport.py`, `pyproject.toml`, `py.typed`, `.env.example`, `README.md`; the remaining D-01 files (`_core.py`, `_logging.py`, `client.py`, `aio.py`, `__init__.py`, `tests/`) land in Waves 2-4"
    - "The package `market_data_client` is importable at the namespace level after `uv sync`"
    - "State holds Auth0 credentials, cached token, expiry, and lazy locks (AUTH-MD-01 foundation)"
    - "The retry transport foundation exists for both sync and async surfaces (CORE-MD-01 foundation)"
    - "The typed exception hierarchy exists with (status_code, message) attributes (CORE-MD-01)"
  artifacts:
    - path: "packages/market-data-client/pyproject.toml"
      provides: "Package metadata + hatchling build + deps (httpx, python-dotenv, tenacity); NO platformdirs"
      contains: "market-data-client"
    - path: "packages/market-data-client/src/market_data_client/py.typed"
      provides: "PEP 561 typed marker (empty)"
    - path: "packages/market-data-client/src/market_data_client/exceptions.py"
      provides: "MarketDataError / MarketDataAPIError / MarketDataAuthError / MarketDataRateLimitError"
      contains: "class MarketDataError"
    - path: "packages/market-data-client/src/market_data_client/_state.py"
      provides: "_ClientState dataclass + constants (_TOKEN_TTL_BUFFER_SECONDS=60, _TOKEN_TTL_FALLBACK_SECONDS=3600) + MARKET_DATA_* env factories"
      contains: "class _ClientState"
      min_lines: 60
    - path: "packages/market-data-client/src/market_data_client/_transport.py"
      provides: "RetryTransport (verbatim iol) with _LOGGER_NAME='market_data_client'"
      contains: "_LOGGER_NAME = \"market_data_client\""
      min_lines: 150
    - path: "packages/market-data-client/src/market_data_client/_atransport.py"
      provides: "AsyncRetryTransport (verbatim iol), imports from _transport"
      min_lines: 90
  key_links:
    - from: "packages/market-data-client/src/market_data_client/_atransport.py"
      to: "packages/market-data-client/src/market_data_client/_transport.py"
      via: "intra-package import of _LOGGER_NAME, _RETRY_AFTER_CAP_S, _RETRYABLE_EXC, _is_retryable_status, _parse_retry_after, _RetryableStatus"
      pattern: "from market_data_client._transport import"
    - from: "packages/market-data-client/src/market_data_client/_state.py"
      to: "os.environ"
      via: "env default factories for MARKET_DATA_CLIENT_ID/SECRET/AUDIENCE/AUTH0_TOKEN_URL/BASE_URL"
      pattern: "os.getenv\\(\"MARKET_DATA_"
---

<objective>
Stand up the `market-data-client` package skeleton and its dependency-free foundations by mirroring `iol-client`: package metadata (`pyproject.toml`, `py.typed`, `.env.example`, `README.md`), the typed exception hierarchy (`exceptions.py`, D-14), per-instance state (`_state.py`, D-06/D-07/D-13), and the retry transport pair (`_transport.py` / `_atransport.py`, D-10 verbatim). These files have no dependency on `_core`/`client`/`aio`, so they form Wave 1.

Purpose: Establish the package so downstream plans (`_core`, `_logging`, `client`, `aio`) can import `market_data_client.exceptions`, `market_data_client._state`, and `market_data_client._transport`. Subtract iol's deferred machinery (`platformdirs`, `_token_cache.py`, `refresh_token`, `username`/`password`).
Output: 8 files under `packages/market-data-client/`; the package resolves as a namespace package after `uv sync`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-CONTEXT.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-RESEARCH.md
@.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-PATTERNS.md
</context>

<artifacts_produced>
This plan creates (partial set for the phase):
- Package path `packages/market-data-client/` with `pyproject.toml` (name `market-data-client`, `version="0.1.0"`), `py.typed`, `.env.example`, `README.md`
- Modules: `exceptions.py`, `_state.py`, `_transport.py`, `_atransport.py`
- Exception classes: `MarketDataError`, `MarketDataAPIError`, `MarketDataAuthError`, `MarketDataRateLimitError`
- State: `_ClientState`, constants `_REQUEST_TIMEOUT`, `_TOKEN_TTL_BUFFER_SECONDS=60`, `_TOKEN_TTL_FALLBACK_SECONDS=3600`, `DEFAULT_BASE_URL="https://market-data-develop.bbsa.com.ar/api"`
- Transport: `RetryTransport`, `AsyncRetryTransport`, constant `_LOGGER_NAME="market_data_client"`
- Env var names: `MARKET_DATA_CLIENT_ID`, `MARKET_DATA_CLIENT_SECRET`, `MARKET_DATA_AUDIENCE`, `MARKET_DATA_AUTH0_TOKEN_URL`, `MARKET_DATA_BASE_URL`
(Downstream: `_core.py`/`_logging.py` in Wave 2; `client.py`/`aio.py` in Wave 3; `__init__.py`/tests in Wave 4.)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Package scaffold (pyproject, py.typed, .env.example, README) + register in workspace</name>
  <files>packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/py.typed, packages/market-data-client/.env.example, packages/market-data-client/README.md, pyproject.toml</files>
  <read_first>
    - packages/iol-client/pyproject.toml (analog to adapt; SUBTRACT the `platformdirs>=4.0,<5` line per D-02)
    - packages/iol-client/.env.example (analog for env var template shape)
    - packages/iol-client/README.md (analog for README shape)
    - pyproject.toml (root — `[tool.uv.workspace] members=["packages/*"]` auto-discovers the new package; `[tool.uv.sources]` block)
  </read_first>
  <action>
    Create `packages/market-data-client/pyproject.toml` mirroring iol's: `[project] name="market-data-client"`, `version="0.1.0"`, `requires-python=">=3.12"`, same authors/license/classifiers. Set `dependencies = ["httpx>=0.27", "python-dotenv>=1.0", "tenacity>=9.1.0,<10"]` — do NOT include `platformdirs` (D-02, deferred with `_token_cache.py`). Copy `[project.optional-dependencies].test` verbatim (pytest>=8.3, pytest-cov>=6.0, pytest-asyncio>=0.24, pytest-httpx>=0.34), `[build-system]` hatchling, `[tool.hatch.build.targets.wheel] packages=["src/market_data_client"]`, and `[tool.hatch.build.targets.sdist] include=["src","tests","README.md","pyproject.toml"]`. Update description/keywords to market-data terms.
    Create empty `packages/market-data-client/src/market_data_client/py.typed` (0 bytes, PEP 561).
    Create `packages/market-data-client/.env.example` listing the 5 vars per D-13: `MARKET_DATA_CLIENT_ID`, `MARKET_DATA_CLIENT_SECRET`, `MARKET_DATA_AUDIENCE`, `MARKET_DATA_AUTH0_TOKEN_URL` (placeholder `https://<tenant>.auth0.com/oauth/token`), `MARKET_DATA_BASE_URL` (default `https://market-data-develop.bbsa.com.ar/api`) — placeholders only, NEVER real credentials.
    Create `packages/market-data-client/README.md` (short usage note: sync `import market_data_client`, async `from market_data_client import aio`, Auth0 client_credentials, env vars).
    Add `market-data-client = { workspace = true }` to the root `pyproject.toml` `[tool.uv.sources]` block. Then run `uv sync --all-packages --all-extras --dev` to register the package in the venv (this updates `uv.lock` locally; the committed lockfile finalization + `ci.yml` matrix entry are Phase 24 / PUB-MD-01, NOT this phase — do not touch `ci.yml`).
  </action>
  <verify>
    <automated>test -f packages/market-data-client/pyproject.toml && test -f packages/market-data-client/src/market_data_client/py.typed && grep -q 'name = "market-data-client"' packages/market-data-client/pyproject.toml && ! grep -q 'platformdirs' packages/market-data-client/pyproject.toml && grep -q 'MARKET_DATA_AUTH0_TOKEN_URL' packages/market-data-client/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - `packages/market-data-client/pyproject.toml` contains `name = "market-data-client"` and `version = "0.1.0"`
    - `pyproject.toml` dependencies list httpx, python-dotenv, tenacity and do NOT contain `platformdirs`
    - `packages/market-data-client/src/market_data_client/py.typed` exists and is empty
    - `.env.example` contains all 5 `MARKET_DATA_*` var names with placeholder values (no real secrets)
    - Root `pyproject.toml` `[tool.uv.sources]` contains `market-data-client = { workspace = true }`
  </acceptance_criteria>
  <done>Package scaffold exists; `uv sync` registers `market-data-client` as a workspace member; deps match D-02 (no platformdirs).</done>
</task>

<task type="auto">
  <name>Task 2: exceptions.py (D-14) + _state.py (D-06/D-07/D-13)</name>
  <files>packages/market-data-client/src/market_data_client/exceptions.py, packages/market-data-client/src/market_data_client/_state.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/exceptions.py (analog — 4-class hierarchy to rename)
    - packages/iol-client/src/iol_client/_state.py (analog — reduce: drop username/password/refresh_token/token_cache_path/pathlib)
  </read_first>
  <action>
    Create `packages/market-data-client/src/market_data_client/exceptions.py` mirroring iol's structure verbatim, renamed (D-14): `MarketDataError(Exception)` base; `MarketDataAPIError(MarketDataError)` with `__init__(self, status_code: int, message: str)` setting `self.status_code`/`self.message` and `super().__init__(f"[{status_code}] {message}")`; `MarketDataAuthError(MarketDataAPIError)` (401/403); `MarketDataRateLimitError(MarketDataAPIError)` (429). Module starts with `from __future__ import annotations`.
    Create `packages/market-data-client/src/market_data_client/_state.py` reducing iol's `_state.py`: KEEP the `@dataclass(slots=True)` (non-frozen) `_ClientState`, constants `_REQUEST_TIMEOUT = 30.0` and `_TOKEN_TTL_BUFFER_SECONDS = 60`. CHANGE `DEFAULT_BASE_URL = "https://market-data-develop.bbsa.com.ar/api"`. ADD constant `_TOKEN_TTL_FALLBACK_SECONDS = 3600` (D-07 — used only when `expires_in` absent). Replace iol's `_env_user`/`_env_password` factories with `_env_client_id` (`MARKET_DATA_CLIENT_ID`), `_env_client_secret` (`MARKET_DATA_CLIENT_SECRET`), `_env_audience` (`MARKET_DATA_AUDIENCE`), `_env_auth0_token_url` (`MARKET_DATA_AUTH0_TOKEN_URL`); base-url factory reads `MARKET_DATA_BASE_URL`. `_ClientState` fields: `base_url`, `client_id`, `client_secret`, `audience`, `auth0_token_url` (all via `field(default_factory=...)`), `token: str | None = None`, `token_expires_at: float = 0.0`, `http_client: httpx.Client | httpx.AsyncClient | None = None`, `token_lock: asyncio.Lock | None = None`, `client_lock: asyncio.Lock | None = None`. REMOVE `username`, `password`, `refresh_token`, `token_cache_path`, and the `from pathlib import Path` import. Update `__all__` to add `_TOKEN_TTL_FALLBACK_SECONDS`. Naming of internal factories is Claude's discretion within monorepo conventions.
  </action>
  <verify>
    <automated>uv run ruff check packages/market-data-client/src/market_data_client/exceptions.py packages/market-data-client/src/market_data_client/_state.py && uv run python -c "from market_data_client._state import _ClientState, DEFAULT_BASE_URL, _TOKEN_TTL_BUFFER_SECONDS, _TOKEN_TTL_FALLBACK_SECONDS; from market_data_client.exceptions import MarketDataError, MarketDataAPIError, MarketDataAuthError, MarketDataRateLimitError; s=_ClientState(); assert DEFAULT_BASE_URL.endswith('/api'); assert _TOKEN_TTL_FALLBACK_SECONDS==3600; assert _TOKEN_TTL_BUFFER_SECONDS==60; assert hasattr(s,'auth0_token_url') and not hasattr(s,'refresh_token')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from market_data_client.exceptions import MarketDataError, MarketDataAPIError, MarketDataAuthError, MarketDataRateLimitError"` exits 0
    - `MarketDataAuthError` and `MarketDataRateLimitError` are subclasses of `MarketDataAPIError`, which subclasses `MarketDataError`
    - `_ClientState()` instance has attributes `client_id, client_secret, audience, auth0_token_url, token, token_expires_at, token_lock, client_lock` and does NOT have `username`, `password`, `refresh_token`, `token_cache_path`
    - `_state.py` defines `_TOKEN_TTL_FALLBACK_SECONDS = 3600` and `_TOKEN_TTL_BUFFER_SECONDS = 60`; `DEFAULT_BASE_URL == "https://market-data-develop.bbsa.com.ar/api"`
    - `ruff check` exits 0 on both files
  </acceptance_criteria>
  <done>Exception hierarchy and state dataclass exist per D-14/D-06/D-07/D-13; iol's deferred fields removed.</done>
</task>

<task type="auto">
  <name>Task 3: _transport.py + _atransport.py verbatim copy (D-10)</name>
  <files>packages/market-data-client/src/market_data_client/_transport.py, packages/market-data-client/src/market_data_client/_atransport.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/_transport.py (analog — copy verbatim; change ONLY `_LOGGER_NAME`)
    - packages/iol-client/src/iol_client/_atransport.py (analog — copy verbatim; change ONLY the import package path)
  </read_first>
  <action>
    Create `packages/market-data-client/src/market_data_client/_transport.py` as a byte-for-byte copy of iol's `_transport.py` with the SINGLE edit `_LOGGER_NAME = "market_data_client"` (D-10). Keep ALL constants verbatim: `_RETRYABLE_EXC = (httpx.ConnectError, httpx.ConnectTimeout, httpx.ReadTimeout)`, `_RETRYABLE_STATUS = frozenset({408, 409, 429, *range(500, 600)})`, `_RETRY_AFTER_CAP_S = 60.0`, `wait_exponential_jitter(initial=1.0, max=30.0, exp_base=2, jitter=1.0)`. Keep the mutation gate (`if not request.extensions.get("idempotent", False): return super().handle_request(request)`), the `max_attempts <= 1` bypass, the `_RetryableStatus` sentinel, and `_parse_retry_after` (RFC 9110 delta + HTTP-date) with the 60s cap. iol's reads of `request.extensions.get("account_id")` and the `max_attempts` extension are harmless no-ops (never populated this phase) — leave them. Update docstring references from iol to market-data (cosmetic only).
    Create `packages/market-data-client/src/market_data_client/_atransport.py` as a verbatim copy of iol's `_atransport.py` with the ONLY edit being the intra-package import path: `from market_data_client._transport import (_LOGGER_NAME, _RETRY_AFTER_CAP_S, _RETRYABLE_EXC, _is_retryable_status, _parse_retry_after, _RetryableStatus)`.
  </action>
  <verify>
    <automated>uv run ruff check packages/market-data-client/src/market_data_client/_transport.py packages/market-data-client/src/market_data_client/_atransport.py && uv run mypy packages/market-data-client/src/market_data_client/_transport.py packages/market-data-client/src/market_data_client/_atransport.py packages/market-data-client/src/market_data_client/_state.py packages/market-data-client/src/market_data_client/exceptions.py && grep -q '_LOGGER_NAME = "market_data_client"' packages/market-data-client/src/market_data_client/_transport.py && grep -q 'from market_data_client._transport import' packages/market-data-client/src/market_data_client/_atransport.py</automated>
  </verify>
  <acceptance_criteria>
    - `packages/market-data-client/src/market_data_client/_transport.py` contains `_LOGGER_NAME = "market_data_client"` and `frozenset({408, 409, 429, *range(500, 600)})`
    - `_atransport.py` imports from `market_data_client._transport` (not `iol_client._transport`)
    - `python -c "from market_data_client._atransport import AsyncRetryTransport; from market_data_client._transport import RetryTransport"` exits 0
    - `mypy` (strict) exits 0 over `_transport.py`, `_atransport.py`, `_state.py`, `exceptions.py`
  </acceptance_criteria>
  <done>Both retry transports mirror iol verbatim with only the logger name / import path changed; mypy strict passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package src → git | Source and `.env.example` are committed; real `.env` must never be |
| client → Auth0 / API | (foundation only this plan) transport dispatches HTTP; credentials live in `_state` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | `.env.example` / pyproject | mitigate | `.env.example` carries placeholders only; real `.env` gitignored (project constraint); no credentials in committed files |
| T-20-SC | Tampering | dependency installs (httpx, python-dotenv, tenacity) | accept | All three already pinned in repo `uv.lock` and used by 5 packages; RESEARCH Package Legitimacy Audit = all OK/Approved, zero `[ASSUMED]`/`[SUS]`/`[SLOP]` — no legitimacy checkpoint required |
| T-20-03 | Spoofing | `_ClientState` token/expiry fields | mitigate | State carries `token_expires_at` + `_TOKEN_TTL_BUFFER_SECONDS`=60 so downstream `token_is_fresh` can bound stale reuse |
</threat_model>

<verification>
- `uv run ruff check packages/market-data-client` and `uv run ruff format --check packages/market-data-client` exit 0
- `uv run mypy packages/market-data-client/src` exits 0 (no `_core`/`client`/`aio` yet — the four foundation modules type-check clean)
- Namespace import works: `uv run python -c "import market_data_client._state, market_data_client._transport, market_data_client._atransport, market_data_client.exceptions"`
</verification>

<success_criteria>
Package skeleton + 4 foundation modules exist and pass ruff + mypy strict; `platformdirs` and all `refresh_token`/`username`/`password`/`token_cache` machinery absent (contributes to SC1 + SC5).
</success_criteria>

<output>
Create `.planning/phases/20-scaffold-auth0-client-credentials-fundaciones-de-transporte/20-01-SUMMARY.md` when done.
</output>
