---
phase: 09-deferred-bug-fixes
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - packages/matriz-client/src/matriz_client/_core.py
  - packages/matriz-client/tests/test_core.py
  - .planning/verification/matriz-client-findings.md
autonomous: false
requirements: [BUG-01]
requirements_addressed: [BUG-01]
tags: [phase-09, matriz, bug-fixes, cfi-validation, single-site, wave-2]
user_setup:
  - service: matriz
    why: "Live re-run de main_matriz.py confirma probe_error_malformed_cfi → PASS + cycle_closure flip FAIL → PASS"
    env_vars:
      - name: MATRIZ_USER
        source: "matriz/Primary credentials (already in .env)"
      - name: MATRIZ_PASSWORD
        source: "matriz/Primary credentials (already in .env)"

must_haves:
  truths:
    - "BUG-01 (D-01 hybrid Literal+regex guard locked): en `build_get_instruments_by_cfi_request` (matriz `_core.py:423-441`), validar `cfi_code` pre-HTTP. Si `cfi_code in _CFI_LITERAL_VALUES` → pass (9 valores actuales). Si `_CFI_ISO_RE.match(cfi_code)` → pass (forward-compat ISO 10962:2021 `^[A-Z]{6}$`). Si ninguno → `raise PrimaryAPIError(status='ERROR', description=f'CFI inválido: {cfi_code!r} (no está en CFICode Literal ni matchea ^[A-Z]{{6}}$)', message=None)` pre-HTTP."
    - "BUG-01 (D-02 deviation lock): guard vive en el builder, NO en `raise_for_response`. `raise_for_response` solo recibe `httpx.Response` y no ve el `cfi_code` param. El contrato observable (`PrimaryAPIError(status='ERROR')`) se preserva — el probe driver `probe_error_malformed_cfi` (`main_matriz.py:1194`) captura el outcome esperado vía `except PrimaryAPIError as exc: if exc.status == 'ERROR': PASS`."
    - "Module-level constants definidos: `_CFI_ISO_RE = re.compile(r'^[A-Z]{6}$')` + `_CFI_LITERAL_VALUES = frozenset(get_args(CFICode))` (Pattern S5: compile-once, frozenset inmutable + hashable). Source of truth: `packages/matriz-client/src/matriz_client/types.py:50-61` declara `CFICode` Literal con 9 valores."
    - "Single-site fix (Phase 7 REFAC-03 idiom — Pattern S4): el cambio en `_core.py` builder propaga al transport shell `Client.get_instruments_by_cfi` automáticamente. matriz NO tiene `aio.py` REST aún (Phase 10 territory) → solo sync surface afectada en Phase 9."
    - "Mocked regression test `test_get_instruments_by_cfi_validates_cfi_code` (paramétrico 10 casos en `packages/matriz-client/tests/test_core.py`) cubre 3 buckets: Literal-known × 2 (`ESXXXX`, `DBXXXX`), regex forward-compat × 2 (`ABXXXX`, `ZQXXXX`), malformed × 6 (`INVALID-CFI`, `esxxxx`, `E2XXXX`, `ABCDE`, `ABCDEFG`, ``)."
    - "D-03 live re-verification: `uv run python main_matriz.py` (NO `--live` flag — el flag controla mutations). El probe `probe_error_malformed_cfi` (`main_matriz.py:1194`) reporta PASS post-fix. El probe 25 `cycle_closure_matriz_client` flipea FAIL → PASS (matriz no tiene Open ERROR-MAP findings sin Regression — F-09 ahora tiene Regression line)."
    - "Finding F-09 en `.planning/verification/matriz-client-findings.md` actualizado: Status CONFIRMED → FIXED + `Resolution: Phase 9 Plan 09-03 BUG-01 — hybrid Literal + ISO 10962 regex guard en build_get_instruments_by_cfi_request. Deviation D-02 vs ROADMAP literal. Live re-run confirma probe_error_malformed_cfi PASS; cycle_closure_matriz_client FAIL → PASS.` + `Regression: tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code` (10 casos paramétricos)."
    - "Snapshot público zero-diff (Phase 6 D-09): el cambio es interno al builder; no modifica signatures públicas. Plan 09-04 valida."
    - "Import-linter contract (Phase 7 D-09) preserved: `_core.py` sigue sin importar de `client.py`/`aio.py`. Cross-leak sentinel (Phase 7 D-10) GREEN."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/_core.py"
      provides: "Imports `import re` + `get_args` agregados; module-level `_CFI_ISO_RE` + `_CFI_LITERAL_VALUES`; `build_get_instruments_by_cfi_request` con guard pre-HTTP"
      contains: "_CFI_ISO_RE"
    - path: "packages/matriz-client/tests/test_core.py"
      provides: "Parametric test 10 casos cubriendo 3 buckets (literal-known / regex forward-compat / malformed)"
      contains: "test_get_instruments_by_cfi_validates_cfi_code"
    - path: ".planning/verification/matriz-client-findings.md"
      provides: "F-09 Status CONFIRMED → FIXED + Resolution + Regression line per Phase 5 Op A convention"
      contains: "F-09"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/_core.py::build_get_instruments_by_cfi_request"
      to: "packages/matriz-client/src/matriz_client/types.py::CFICode"
      via: "_CFI_LITERAL_VALUES = frozenset(get_args(CFICode))"
      pattern: "get_args\\(CFICode\\)"
    - from: "packages/matriz-client/tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code"
      to: "packages/matriz-client/src/matriz_client/_core.py::build_get_instruments_by_cfi_request"
      via: "10 parametric cases cover 3 buckets"
      pattern: "@pytest.mark.parametrize"
    - from: "main_matriz.py::probe_error_malformed_cfi"
      to: "packages/matriz-client/src/matriz_client/_core.py guard (raises PrimaryAPIError pre-HTTP)"
      via: "except PrimaryAPIError as exc: if exc.status == 'ERROR': return PASS"
      pattern: "PrimaryAPIError"
---

<objective>
Plan 09-03 cierra **matriz BUG-01 (F-09 ERROR-MAP CFI inválido)**: agrega un
guard hybrid Literal + ISO 10962 regex en
`build_get_instruments_by_cfi_request` (`_core.py:423-441`) que levanta
`PrimaryAPIError(status="ERROR")` pre-HTTP cuando el `cfi_code` es inválido.
Single-site fix (Phase 7 REFAC-03): cubre la superficie sync de matriz
(matriz NO tiene `aio.py` REST aún — Phase 10 territory).

Purpose:
1. Cierra el gap real en error mapping del cliente (F-09 CONFIRMED → FIXED).
2. Forward-compat ISO 10962:2021 sin lib bump (regex `^[A-Z]{6}$`).
3. Bloquea cast-bypass (`cast(CFICode, "INVALID-CFI")`) que pre-Phase 9
   permitía pasar input mal-formado al wire silenciosamente.
4. Flipea `cycle_closure_matriz_client` FAIL → PASS (matriz ya no tiene Open
   ERROR-MAP findings sin Regression line).
5. Deviation explícito D-02 vs literal ROADMAP `_core.raise_for_response()`
   — el guard vive en el builder porque `raise_for_response` solo ve
   `httpx.Response` y no ve el `cfi_code` param.

Output:
- `packages/matriz-client/src/matriz_client/_core.py` MODIFIED: imports + 2 module-level constants + builder body.
- `packages/matriz-client/tests/test_core.py` EXTENDED: new paramétrico (10 casos).
- `.planning/verification/matriz-client-findings.md` UPDATED: F-09 Status flip + Resolution + Regression line.
- 1 commit atómico per D-12: `fix(matriz): BUG-01 hybrid Literal+regex CFI validation + cycle_closure FAIL→PASS (BUG-01)`.
- Live re-run operator-driven con paste evidence (manual checkpoint Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-deferred-bug-fixes/09-CONTEXT.md
@.planning/phases/09-deferred-bug-fixes/09-RESEARCH.md
@.planning/phases/09-deferred-bug-fixes/09-PATTERNS.md
@.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
@.planning/verification/matriz-client-findings.md
@.planning/verification/FINDINGS-TEMPLATE.md
@packages/matriz-client/src/matriz_client/_core.py
@packages/matriz-client/src/matriz_client/types.py
@packages/matriz-client/src/matriz_client/exceptions.py
@packages/matriz-client/src/matriz_client/_state.py
@packages/matriz-client/tests/test_core.py
@main_matriz.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modificar `_core.py` con hybrid Literal+regex CFI guard + agregar test parametric en `test_core.py`</name>
  <files>
    packages/matriz-client/src/matriz_client/_core.py,
    packages/matriz-client/tests/test_core.py
  </files>
  <read_first>
    - packages/matriz-client/src/matriz_client/_core.py:1-90 (header + imports + `__all__` — para identificar dónde agregar `import re` + `get_args` + module-level constants)
    - packages/matriz-client/src/matriz_client/_core.py:423-441 (builder + parser actuales de `get_instruments_by_cfi` — sitio del fix)
    - packages/matriz-client/src/matriz_client/types.py:50-61 (`CFICode = Literal[...]` con 9 valores — source of truth para `_CFI_LITERAL_VALUES`)
    - packages/matriz-client/src/matriz_client/exceptions.py:10-28 (PrimaryAPIError signature: `__init__(self, status: str, description: str | None = None, message: str | None = None)`)
    - packages/matriz-client/tests/test_core.py (todo el archivo — confirmar imports actuales + idiom de tests paramétricos + pytest.raises + assertions sobre PrimaryAPIError.description)
    - packages/matriz-client/src/matriz_client/_state.py (para imports de `_ClientState` en el test)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-01, D-02 (decision spec + deviation rationale)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"_core.py (MODIFY: hybrid guard)" + §"test_core.py (EXTEND: parametric CFI test)" (deltas exactos)
    - .planning/phases/09-deferred-bug-fixes/09-RESEARCH.md §"Pattern 1 Single-Site Guard in `_core.py` Builder" + §"Pattern 5 Parametric Test for Bucket Coverage" + §"Common Pitfalls" 4 (typing.get_args order Python 3.10+)
  </read_first>
  <behavior>
    - Pre-fix: cualquier `cfi_code` string (incluso `cast(CFICode, "INVALID-CFI")`) pasa silenciosamente al wire request; el server aceptaba sin levantar. F-09 documentado.
    - Post-fix: el builder valida pre-HTTP. Si `cfi_code in {ESXXXX, DBXXXX, OCASPS, OPASPS, FXXXSX, OPAFXS, OCAFXS, EMXXXX, DBXXFR}` (literal-known) → pass. Si `re.match(r"^[A-Z]{6}$", cfi_code)` → pass (regex forward-compat ISO 10962:2021). Otherwise → `raise PrimaryAPIError(status="ERROR", description=f"CFI inválido: {cfi_code!r} (no está en CFICode Literal ni matchea ^[A-Z]{{6}}$)", message=None)`.
    - 10 parametric test cases cover 3 buckets:
      - Literal-known (2): `"ESXXXX"`, `"DBXXXX"` → pass; assert `spec.params == {"CFICode": cfi}`.
      - Regex forward-compat (2): `"ABXXXX"`, `"ZQXXXX"` → pass; assert spec.params correct (6 mayúsculas, NO en Literal pero matchean regex).
      - Malformed (6): `"INVALID-CFI"` (hyphen + len 11), `"esxxxx"` (lowercase), `"E2XXXX"` (digit), `"ABCDE"` (len 5), `"ABCDEFG"` (len 7), `""` (empty) → raise `PrimaryAPIError`; assert `exc_info.value.status == "ERROR"` + `"CFI inválido" in exc_info.value.description`.
    - Single-site fix propaga: matriz tiene `Client.get_instruments_by_cfi(cfi_code: CFICode)` que llama internamente al builder; el guard rechaza pre-HTTP sin que el shell tenga que cambiar. matriz `aio.py` REST surface no existe aún (D-25 Phase 8 / Phase 10 territory) — solo sync afectado en Phase 9.
  </behavior>
  <action>
    **PARTE A — `packages/matriz-client/src/matriz_client/_core.py` (MODIFY):**

    1. **Imports header (después de stdlib imports actuales `import time` línea 42 y `from typing import Any` línea 45):**
       - Agregar `import re` (después de `import time`, antes de `from collections.abc`).
       - Modificar `from typing import Any` → `from typing import Any, get_args`.
       NOTA: `Any` ya está importado; solo agregar `get_args` a la misma línea.

    2. **Module-level constants (insertar entre los imports y el primer `__all__` block, justo antes de la línea 74 `__all__ = [...]`):**
       - Block prose comment 1-2 lines explicando D-01 (hybrid guard) + source of truth `types.py:50-61`.
       - `_CFI_ISO_RE = re.compile(r"^[A-Z]{6}$")` (Pattern S5: compile-once).
       - `_CFI_LITERAL_VALUES = frozenset(get_args(CFICode))` (Pattern S5: frozenset inmutable + hashable + source-of-truth derivation).
       NOTA: `CFICode` ya está importado en línea 65 (`from matriz_client.types import ... CFICode`).

    3. **Modificar `build_get_instruments_by_cfi_request` (líneas 423-434):**
       - Mantener signature `def build_get_instruments_by_cfi_request(state: _ClientState, cfi_code: CFICode) -> RequestSpec:`.
       - Reemplazar docstring 1-liner con docstring expandida: 1-liner endpoint + párrafo D-01 hybrid guard description + párrafo D-02 deviation rationale referencing `raise_for_response` no-ver-cfi_code.
       - Body pre-guard: `if cfi_code not in _CFI_LITERAL_VALUES and not _CFI_ISO_RE.match(cfi_code): raise PrimaryAPIError(status="ERROR", description=f"CFI inválido: {cfi_code!r} (no está en CFICode Literal ni matchea ^[A-Z]{{6}}$)", message=None)`.
       - Body post-guard: mantener el `return RequestSpec(method="GET", path="/rest/instruments/byCFICode", params={"CFICode": cfi_code}, idempotent=True, endpoint_name="get_instruments_by_cfi")` original.
       NOTA: NO modificar `parse_get_instruments_by_cfi_response` (líneas 437-441) — el parser sigue intacto. NO modificar otros builders/parsers en el file.

    **PARTE B — `packages/matriz-client/tests/test_core.py` (EXTEND):**

    1. **Verificar imports actuales** (research: `_core`, `_ClientState`, `PrimaryAPIError` ya importados; no need to add).

    2. **Agregar nuevo test paramétrico** al final del archivo:
       ```text
       @pytest.mark.parametrize(
           "cfi,expect_raise",
           [
               # Literal-known bucket (2 valores del Literal CFICode)
               ("ESXXXX", False),
               ("DBXXXX", False),
               # Regex forward-compat bucket (6 mayúsculas, NO en Literal)
               ("ABXXXX", False),
               ("ZQXXXX", False),
               # Malformed bucket
               ("INVALID-CFI", True),  # hyphen + len 11
               ("esxxxx", True),       # lowercase
               ("E2XXXX", True),       # digit
               ("ABCDE", True),        # len 5
               ("ABCDEFG", True),      # len 7
               ("", True),             # empty
           ],
       )
       def test_get_instruments_by_cfi_validates_cfi_code(
           cfi: str, expect_raise: bool
       ) -> None:
       ```
       Body:
       - Crear state con `state = _ClientState(base_url="https://api.example.com")`.
       - If `expect_raise`: `with pytest.raises(PrimaryAPIError) as exc_info:` invocar builder con `# type: ignore[arg-type]` (porque cfi es `str` pero param declarado `CFICode`); asserts `exc_info.value.status == "ERROR"` + `"CFI inválido" in (exc_info.value.description or "")`.
       - Else: `spec = _core.build_get_instruments_by_cfi_request(state, cfi)` con `# type: ignore[arg-type]`; assert `spec.params == {"CFICode": cfi}`.
       NOTA: NO usar `cast(CFICode, ...)` — el `# type: ignore[arg-type]` es el idiom existente del archivo. NO agregar imports nuevos a `test_core.py`.

    **No tocar:**
    - Otros builders/parsers en `_core.py`.
    - El parser `parse_get_instruments_by_cfi_response`.
    - `client.py` / `aio.py` (single-site fix; Phase 7 REFAC-03 propaga automático).
    - Tests existentes en `test_core.py`.
    - `verification/snapshots/matriz-client-surface.txt` (zero diff esperado; Plan 09-04 valida).

    **Pitfall enforcement (RESEARCH.md):**
    - Pitfall 4 (`typing.get_args` orden): Python 3.12+ garantiza orden de declaración (PEP 586). Usar `frozenset(get_args(CFICode))` — order-independent. Verified at runtime en research § Standard Stack.
    - Pitfall 1 (no confundir con `_state.account_id`): este plan toca SOLO el builder CFI; NO toca campos de state ni `RequestSpec.account_id`.
    - El `description=f"CFI inválido: {cfi_code!r} (no está en CFICode Literal ni matchea ^[A-Z]{{6}}$)"` — usar `!r` para repr-quote del input (útil para el debug del operator); el `{{6}}` escape doble llaves en f-string para que aparezca como `{6}` literal en el output.
  </action>
  <verify>
    <automated>uv run pytest packages/matriz-client/tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/matriz-client/src/matriz_client/_core.py` contains `import re`. Verified via `grep -c "^import re$" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns 1.
    - `packages/matriz-client/src/matriz_client/_core.py` imports `get_args` from typing. Verified via `grep -c "from typing import.*get_args" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns 1.
    - Module-level constants exist: `grep -c "_CFI_ISO_RE = re.compile" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns 1 AND `grep -c "_CFI_LITERAL_VALUES = frozenset(get_args(CFICode))" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns 1.
    - Builder body contains the guard raise: `grep -c 'raise PrimaryAPIError' packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns ≥ 1 (puede haber otras raises pre-existentes; verificar al menos uno con "CFI inválido" en description: `grep -c "CFI inválido" packages/matriz-client/src/matriz_client/_core.py | grep -v '^#'` returns ≥ 1).
    - Parser preserved (no change): `grep -c "parse_get_instruments_by_cfi_response" packages/matriz-client/src/matriz_client/_core.py` returns ≥ 2 (definition + reference).
    - Test exists: `grep -c "def test_get_instruments_by_cfi_validates_cfi_code" packages/matriz-client/tests/test_core.py` returns 1.
    - Test command exit 0 (all 10 parametric cases pass): `uv run pytest packages/matriz-client/tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code -x` returns 0; output shows 10 passed.
    - Full matriz test_core.py GREEN: `uv run pytest packages/matriz-client/tests/test_core.py -x --no-header -q` returns 0.
    - Full matriz suite GREEN: `uv run pytest packages/matriz-client/tests/ -x --no-header -q` returns 0 (no regression).
    - Ruff + mypy strict clean: `uv run ruff check packages/matriz-client/src/matriz_client/_core.py packages/matriz-client/tests/test_core.py && uv run mypy --strict packages/matriz-client/` returns 0.
    - Import-linter contract (Phase 7 D-09) preserved: `uv run lint-imports` returns 0. Verifies `_core.py` no importa de `client.py`/`aio.py`.
    - Cross-leak sentinel (Phase 7 D-10) GREEN: `uv run pytest verification/test_sync_async_isolation.py -x` returns 0.
    - matriz `aio.py` LOC unchanged (Phase 8 D-25): `wc -l packages/matriz-client/src/matriz_client/aio.py` returns 103 ± minor whitespace.
    - matriz `_atransport.py` does NOT exist (Phase 8 D-25 preserved): `! test -e packages/matriz-client/src/matriz_client/_atransport.py` returns 0.
    - Public surface snapshot zero-diff: `uv run pytest verification/test_public_surface.py::test_matriz_client_surface_snapshot -x` returns 0.
  </acceptance_criteria>
  <done>
    `_core.py` modified con guard + constants + imports; `test_core.py`
    extended con 10 parametric cases; all 10 pass; full matriz suite GREEN;
    ruff + mypy + lint-imports + cross-leak sentinel + snapshot GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-driven live re-run de main_matriz.py + cycle_closure flip evidence</name>
  <files>(no file modification — operator-driven checkpoint)</files>
  <action>
    Pause for operator. Operator performs the steps described in `<how-to-verify>` and returns one of the signals in `<resume-signal>`. No file modification within this task — Plans 09-01..03 implementation tasks own the writes.
  </action>
  <verify>
    <automated>echo "checkpoint pending operator approval — no automated verify"</automated>
  </verify>
  <done>
    Operator returned an approval signal per `<resume-signal>`; the orchestrator records the response in chat / SUMMARY.
  </done>
  <what-built>
    Task 1 land: hybrid Literal+regex guard agregado en
    `_core.py::build_get_instruments_by_cfi_request`; test paramétrico 10
    casos pasa; full suite matriz GREEN; lint-imports + cross-leak +
    snapshot GREEN.
  </what-built>
  <how-to-verify>
    Este checkpoint es OPERATOR-DRIVEN (per CONTEXT.md Discretion +
    Pitfall 9: matriz live re-run timing — `cycle_closure` no es side-
    effect-free; risk de rate-limit lockout en automated re-runs).

    **Step 1 — Pre-flight:**
    1. `.env` raíz tiene `MATRIZ_USER` + `MATRIZ_PASSWORD` válidos para
       remarkets sandbox (baseline `verification-cycle-2026-Q2`).
    2. (Opcional pero recomendado) Confirmar que NO hay re-runs recientes
       del driver que puedan haber disparado rate-limit del sandbox.

    **Step 2 — Live re-run:**
    ```bash
    uv run --package matriz-client python main_matriz.py 2>&1 | tee /tmp/main_matriz_phase9_run.log
    ```
    NOTA: NO usar `--live` flag — ese flag controla mutations
    (new_order/cancel_order). Para BUG-01 (read-only CFI guard) el driver
    default (sin --live) es suficiente.

    **Step 3 — Inspeccionar output:**
    1. Verificar `probe_error_malformed_cfi` (`main_matriz.py:1194`):
       ```bash
       grep -A 3 "probe_error_malformed_cfi\|error_malformed_cfi" /tmp/main_matriz_phase9_run.log
       ```
       Expected: `error_malformed_cfi ... PASS` con detail referenciando
       que el cliente raised `PrimaryAPIError(status="ERROR")` pre-HTTP.

    2. Verificar probe 25 `cycle_closure_matriz_client`:
       ```bash
       grep -A 5 "cycle_closure" /tmp/main_matriz_phase9_run.log
       ```
       Expected: `cycle_closure_matriz_client ... PASS` con detail "matriz no tiene Open ERROR-MAP findings sin Regression" (después de Task 3 finding update).

       NOTA timing: el flip FAIL → PASS puede requerir que F-09 finding
       ya tenga la `Regression:` line escrita ANTES del re-run para que
       el cycle_closure detecte F-09 como "Open con Regression =
       satisfecho". Si el re-run aún reporta FAIL pero el resto de los
       indicadores son PASS, completar Task 3 (finding update) y re-run.

    **Step 4 — Paste evidence al SUMMARY:**
    Copiar las 2 secciones del output (probe_error_malformed_cfi +
    cycle_closure_matriz_client) al `09-03-SUMMARY.md` para forensic-
    trazable. Incluir timestamp del run.

    **Step 5 — Resume signal:**
    - `approved`: ambos probes reportan PASS; F-09 listo para finding flip.
    - `cycle_closure still FAIL`: ejecutar Task 3 (finding update) primero,
      luego re-run; reportar.
    - `probe_error_malformed_cfi UNEXPECTED outcome: <detail>`: investigation
      block; revisar si el guard se está disparando correctamente (puede
      indicar bug en la implementación del Task 1).
    - `rate-limit hit (HTTP 429)`: esperar cooldown documentado en Higyrus/
      Matriz support docs; retry.
  </how-to-verify>
  <resume-signal>
    Responder con `approved` (ambos probes PASS); OR `cycle_closure still
    FAIL` (do Task 3 first); OR `probe_error_malformed_cfi UNEXPECTED: <detail>`;
    OR `rate-limit hit`.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update F-09 finding en matriz-client-findings.md (Status CONFIRMED → FIXED + Resolution + Regression)</name>
  <files>.planning/verification/matriz-client-findings.md</files>
  <read_first>
    - .planning/verification/matriz-client-findings.md (sección F-09 + sección Cycle Closure — confirmar offsets actuales)
    - .planning/verification/FINDINGS-TEMPLATE.md (convention Phase 5 Op A: `Resolution:` + `Regression:` fields)
    - .planning/verification/higyrus-client-findings.md:20-27 (F-01 example del Resolution pattern)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-01..D-03 (full spec)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §".planning/verification/matriz-client-findings.md (UPDATE F-09)" (text exact)
    - El output del Task 2 live re-run (evidence pasted al summary o al chat)
  </read_first>
  <action>
    Editar `.planning/verification/matriz-client-findings.md`:

    1. **Sección F-09 (líneas 98-105 actuales):**
       - Cambiar `**Status:** \`CONFIRMED\`` → `**Status:** \`FIXED\``.
       - Modificar `- **Actual:** ninguna excepción; el cliente retornó normalmente` → `- **Actual:** ninguna excepción; el cliente retornó normalmente (pre-Phase 9)`.
       - Agregar al final de la lista de bullet points:
         - `- **Resolution:** Phase 9 Plan 09-03 BUG-01 — hybrid Literal + ISO 10962 regex guard agregado pre-HTTP en `build_get_instruments_by_cfi_request` (`packages/matriz-client/src/matriz_client/_core.py:423-441`). Deviation D-02 vs ROADMAP literal `_core.raise_for_response()`: el guard vive en el builder porque `raise_for_response` solo ve `httpx.Response` y no ve el `cfi_code` param; el contrato observable (`PrimaryAPIError(status="ERROR")`) se preserva. Live re-run de `main_matriz.py` confirma `probe_error_malformed_cfi` reporta PASS post-fix; `cycle_closure_matriz_client` flipea FAIL → PASS.`
         - `- **Regression:** \`tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code\` (10 casos paramétricos cubriendo 3 buckets: literal-known × 2, regex forward-compat × 2, malformed × 6).`

    2. **Sección Cycle Closure (líneas 115-135 aprox):**
       - Findings by status: decrement `CONFIRMED` count de 1 → 0; increment `FIXED` count de 0 → 1.
       - "Regression tests linked to FIXED/CONFIRMED findings" table: agregar row con F-09 → `tests/test_core.py::test_get_instruments_by_cfi_validates_cfi_code`.
       - `verify_cycle_closure("matriz-client") returned:` actualizar de **FAIL** a **PASS** (con caveat "post-Phase-9 next baseline run" si el re-run de Task 2 aún no había updateado el state — en la práctica el re-run de Task 2 (post Task 3 commit) debería reportar PASS directly).
       - Missing regressions: remover F-09 de la lista (queda empty para matriz).

    3. **Header / metadata** (si la convention lo requiere): actualizar `Last updated:` con fecha 2026-06-13.

    NO tocar otros findings (F-01..F-08) ni secciones no relacionadas. NO
    introducir nuevos findings. NO tocar `CYCLE-REPORT.md` (Plan 09-04
    green gate puede actualizar si necesario; pero F-09 close es suficiente
    para que el cycle_closure probe detecte el state correctamente).

    Si el evidence del Task 2 indica un wording diferente (e.g., el probe
    output trae detail específico que vale la pena referenciar), incorporar
    1-2 lines de quote al final del Resolution para forensic-localizable.
  </action>
  <verify>
    <automated>grep -A 1 "F-09" .planning/verification/matriz-client-findings.md | head -20 && grep -c "Status.*FIXED" .planning/verification/matriz-client-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - F-09 status flipped to FIXED: `grep -c "F-09" .planning/verification/matriz-client-findings.md` returns ≥ 1 AND `grep -A 2 "F-09" .planning/verification/matriz-client-findings.md | grep -c "FIXED"` returns ≥ 1 (status line marker presente).
    - Resolution line present: `grep -c "Resolution.*Phase 9 Plan 09-03 BUG-01" .planning/verification/matriz-client-findings.md | grep -v '^#'` returns ≥ 1.
    - Regression line present: `grep -c "Regression.*test_get_instruments_by_cfi_validates_cfi_code" .planning/verification/matriz-client-findings.md | grep -v '^#'` returns ≥ 1.
    - CONFIRMED count decrement reflected: el findings file ya no muestra F-09 en lista de CONFIRMED unfixed (verificar manualmente la sección Cycle Closure).
    - File parses como markdown OK: `python -c "import pathlib; pathlib.Path('.planning/verification/matriz-client-findings.md').read_text()"` returns 0 (no encoding errors).
  </acceptance_criteria>
  <done>
    F-09 status flipped, Resolution + Regression lines added per FINDINGS-
    TEMPLATE convention, Cycle Closure section updated. Confirmation del
    Task 2 cycle_closure PASS post-this-task (operator may re-run if needed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.get_instruments_by_cfi(cfi_code)` | El typed signature dice `CFICode` Literal, pero callers pueden bypass via `cast(CFICode, "INVALID-CFI")` (mypy strict NO captura cast bypass at runtime). El nuevo guard hybrid bloquea esto pre-HTTP. |
| `_core.py` builder → wire HTTP request | Pre-fix: input malformado pasaba al wire silenciosamente. Post-fix: validation pre-HTTP previene el wire request. |
| Live `main_matriz.py` → Primary API sandbox | Operator-controlled re-run; ya validated baseline `verification-cycle-2026-Q2`. Pitfall 9: evitar accidental re-runs por rate-limit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Input Validation Bypass | CFI cast-bypass (`cast(CFICode, "INVALID-CFI")`) llegando al wire | mitigate | Hybrid Literal+regex guard pre-HTTP en `build_get_instruments_by_cfi_request`. mypy strict NO captura cast bypass; el runtime guard lo hace. Tests cubren 6 malformed inputs incluyendo el cast-bypass scenario. |
| T-09-03-02 | Information Disclosure | Error description leakea internals del cliente | mitigate | `description=f"CFI inválido: {cfi_code!r} (no está en CFICode Literal ni matchea ^[A-Z]{{6}}$)"` — solo echo el input del caller + el regex constant. NO menciona state del server, paths internos, ni credenciales. RedactingFilter (Phase 8 D-10) activo. |
| T-09-03-03 | Spoofing | F-09 finding manual edit overwrite por re-runs futuros | accept | Plan 09-03 manual edit del finding; Phase 11 HARN-07/09 traerá BEGIN/END zone parser + operator-field preservation. Plan 09-03 confía en convention manual. |
| T-09-03-04 | Tampering | Single-site fix bypassed via direct httpx call | accept | Si un caller construye manualmente un `httpx.Request` y lo dispara via `state.http_client.request(...)` sin pasar por el builder, el guard no se aplica. Out of scope — el contract es que las funciones públicas (`Client.get_instruments_by_cfi`, `get_instruments_by_cfi` top-level) pasan por el builder. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | NO instala paquetes nuevos. Toda dependencia (`re` stdlib, `typing.get_args` stdlib, `pytest`) ya in `uv.lock`. |

Severity overall: **low** — input validation tightening; existing
PrimaryAPIError contract preserved; no new dependencies; live re-run
operator-controlled to avoid sandbox rate-limit (Pitfall 9).
</threat_model>

<verification>
## Phase-Level Checks (Plan 09-03)

- **Guard implementado:** `_core.py` contiene `_CFI_ISO_RE` + `_CFI_LITERAL_VALUES` + raise pre-HTTP en builder. Verified via grep + parametric test 10 cases pass.
- **Single-site preserved (Phase 7 REFAC-03):** sync transport shell sin modificar; `aio.py` matriz no existe (Phase 10 territory).
- **Import-linter contract (Phase 7 D-09):** `_core.py` sin importar de `client.py`/`aio.py`. Verified via `uv run lint-imports`.
- **Cross-leak sentinel (Phase 7 D-10):** GREEN post-change.
- **Snapshot público zero-diff (Phase 6 D-09):** Plan 09-04 valida en green gate; Task 1 acceptance criteria pre-validan localmente.
- **Live cycle_closure flip:** Task 2 operator confirma + Task 3 finding update consolida.
- **`aio.py` matriz LOC = 103 (Phase 8 D-25):** preservado, no modificado.
- **`_atransport.py` matriz absent (Phase 8 D-25):** preservado.

## Manual Checks

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| Live `main_matriz.py` re-run | Risk de rate-limit lockout en automated re-runs; cycle_closure probe vive en driver (no automatizable safely en CI) | Task 2 step-by-step |
| F-09 finding update | Plan 09-03 pre-dates HARN-07/09 (Phase 11 BEGIN/END zone parser) | Task 3 step-by-step |
</verification>

<success_criteria>
1. Task 1 verified: `_core.py` contains imports + module constants + guard; `test_core.py` extended with 10-case parametric test; all 10 pass; full matriz suite GREEN; lint-imports + cross-leak sentinel + snapshot GREEN; ruff + mypy clean; `aio.py` LOC 103 preserved; `_atransport.py` absent preserved.
2. Task 2 operator checkpoint closed: live re-run of `main_matriz.py` shows `probe_error_malformed_cfi` PASS + (post-Task-3) `cycle_closure_matriz_client` PASS; evidence pasted to SUMMARY.
3. Task 3 verified: F-09 Status flipped CONFIRMED → FIXED; Resolution line referencing Plan 09-03 + D-02 deviation + cycle_closure flip; Regression line linking to parametric test; Cycle Closure section updated.
4. Full pytest GREEN: `uv run pytest --no-header -q` returns 0 (all packages, including Plan 09-01 + Plan 09-02 deltas).
5. 1 atomic commit per D-12: `fix(matriz): BUG-01 hybrid Literal+regex CFI validation + cycle_closure FAIL→PASS (BUG-01)`.
</success_criteria>

<output>
Create `.planning/phases/09-deferred-bug-fixes/09-03-SUMMARY.md` when done.
Summary must document:
- Files modified (1 source + 1 test + 1 finding).
- Imports added to `_core.py`.
- Module-level constants added.
- Test count delta: +10 (parametric cases).
- Live re-run output evidence (probe_error_malformed_cfi + cycle_closure_matriz_client outputs).
- F-09 status diff (CONFIRMED → FIXED).
- lint-imports + cross-leak sentinel + snapshot zero-diff confirmation.
- `aio.py` LOC verification (103 unchanged).
</output>
</content>
</invoke>
