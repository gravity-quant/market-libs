---
phase: 35-fundaci-n-null-object-bool-pol-tica-del-walker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md
autonomous: true
requirements: [NOBJ-02]

must_haves:
  truths:
    - "Phase 39 can SUBTRACT rather than rediscover: a named, greppable ledger states exactly which census triples the new walker disposition removes, per package and per field (D-17)."
    - "The ledger's counting unit is the same 4-tuple (slug, model, field_path, kind) that 33-CENSUS.md uses, so Phase 39 performs a set difference and not a translation."
    - "Every row declares WHICH of the two 29-SIZING.md columns it counts against — records-summed-over-corpus-files or distinct triples — because the two differ by a factor of ~2 for market-data and matriz and conflating them fabricates a false 'below the floor'."
    - "All 35 non-optional model/list fields in the blast radius appear as rows: higyrus 11, iol 0, market-data 8, matriz 16."
    - "iol's zero is written as an explicit, reasoned row ('this package retires nothing in Phase 35; its puntas fields are Optional today and become links only in Phase 38'), never as an omitted package."
  artifacts:
    - path: ".planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md"
      provides: "The D-17 accounting ledger consumed by Phase 39 criterio 4"
      min_lines: 70
      contains: "field_path"
  key_links:
    - from: ".planning/phases/35-…/35-RETIRED-TRIPLES.md"
      to: ".planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md"
      via: "shared 4-tuple census unit — the ledger cites the census by name and states, per package, which of its two columns each number belongs to"
      pattern: "33-CENSUS"
    - from: ".planning/phases/35-…/35-RETIRED-TRIPLES.md"
      to: ".planning/milestones/v1.6-phases/29-decoder-observable/29-SIZING.md"
      via: "ratified floors per package, cited with file:line for the higyrus >=22 composition"
      pattern: "29-SIZING\\.md"
  prohibitions:
    - statement: "A per-package count is never reported as a bare 0 or a bare '>= 0'. A zero must carry its reason and its named future destination, because a zero that reads as clean is exactly the false-clean this milestone exists to eliminate."
      status: flagged-unverified
      verification: "Read every per-package row; each numeric zero must be accompanied by prose naming why it is zero and where the work lands instead."
    - statement: "No number in this ledger is estimated. Every count is either quoted from a measured artefact (35-RESEARCH F-10, 29-SIZING, 33-CENSUS) with its citation, or marked UNKNOWN with the reason it could not be measured."
      status: flagged-unverified
      verification: "Every table cell carrying a count must be traceable to a cited source line or explicitly marked UNKNOWN."
---

<objective>
Produce the D-17 accounting artefact: a named, greppable ledger of exactly which divergence triples the NOBJ-02 disposition removes from the census, so that Phase 39's live run can report "N divergences disappeared because the policy stopped recording them" separately from "M divergences disappeared because we fixed them".

Purpose: Phase 39 criterio 4 requires that separation. Without it, the number of divergences drops after this milestone and the drop reads as a clean bill of health — the precise false-clean that v1.6 was built to eliminate (STATE, v1.7 roadmap decisions). The ledger converts a discovery problem into a subtraction.

Output: `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`.

This plan writes no code and touches no package. It runs in Wave 1 in parallel with the 35-01 tracer because it shares no file with any other plan.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-CONTEXT.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md
</context>

<artifacts_this_phase_produces>
| Symbol / file | Owner plan |
|---|---|
| `.planning/phases/35-…/35-RETIRED-TRIPLES.md` (D-17 ledger) | **35-02 [this plan]** |
| `higyrus_client.models.SafeModel.empty()` + `.__bool__()`, higyrus `test_null_object.py`, higyrus wrong-type tests | 35-01 |
| `iol_client` / `market_data_client` bases + their `test_null_object.py` + wrong-type tests | 35-03 |
| `matriz_client._SafeModel.__bool__`, `UnknownFrame.__bool__`, matriz/ambito/wallets `test_null_object.py`, ambito wrong-type tests | 35-04 |
| `_decode.py` EDIT 1 + EDIT 2 ×5, the rewritten `# Phase 35, NOBJ-02:` comment ×5, the 11 inverted assertions, the recomputed `CANONICAL_DIGEST` | 35-05 |
</artifacts_this_phase_produces>

<flagged_assumptions>
| Requirement | Probe verdict | Resolved by | Residual risk |
|---|---|---|---|
| NOBJ-02 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterio 2 + CONTEXT D-17 + ROADMAP Phase 39 criterio 4. This plan resolves the *accounting* leg only. | The ledger predicts which triples stop being emitted from a static reading of the 52 shipped classes. If a field is reachable in live traffic in a shape the static reading did not anticipate, Phase 39's subtraction will not balance. Mitigation: the ledger states its own method and its own limits, so a non-balancing subtraction reads as a finding rather than as noise. |
| NOBJ-01 | `unclassified` / `unresolved` | Not in this plan's scope — resolved by 35-01, 35-03, 35-04. | none here |

RESEARCH assumption A1 ("the artefact should be a standalone `35-RETIRED-TRIPLES.md` file with the
4-tuple unit") is **adopted** by this plan. It was marked `[ASSUMED]` and is planner discretion per
CONTEXT; adopting it follows the `29-SIZING.md` / `33-CENSUS.md` / `33-LITERALS.md` precedent of
census artefacts as named files rather than VERIFICATION sections, and keeps the name greppable
from Phase 39.

RESEARCH Open Question 3 ("how many of the 35 fields correspond to already-measured census
triples?") is explicitly assigned to this plan as work, not left as research. Task 1 performs the
intersection; where a package's intersection cannot be resolved from the committed artefacts, the
row is marked UNKNOWN with the reason, never guessed.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Derive the 35-row retired-triples table and its intersection with the ratified floors</name>

  <files>.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</files>

  <read_first>
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Findings F-10 (the exact, measured 35-field roster: higyrus 11, iol 0, market-data 8, matriz 16, with the `S-3` / `S-5` structural-finding annotations) and §"D-17 — Forma recomendada del artefacto"
    - `.planning/milestones/v1.6-phases/29-decoder-observable/29-SIZING.md` — the ratified per-package floors; read `:290-320` for the composition of higyrus's `>= 22` and the surrounding prose stating that the floors are a FLOOR and not an estimate
    - `.planning/milestones/v1.6-phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md` — the census unit definition (the distinct 4-tuple `(slug, model, field_path, kind)` taken from `DivergenceHandler.seen`) and the section correcting the unit mismatch against `29-SIZING.md` (records summed over 43 corpus files vs. distinct triples: higyrus 22→22, matriz 24→14, market-data 50→22)
    - `.planning/ROADMAP.md` § Phase 39 criterio 4 — the consumer's exact requirement, so the ledger answers the question actually asked
  </read_first>

  <action>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`.

Open with a header block stating four things in prose, each on its own line: the counting unit (the distinct 4-tuple `(slug, model, field_path, kind)`, identical to `33-CENSUS.md`'s, cited by name); the disposition applied (NOBJ-02 — a null or absent value on a NON-OPTIONAL model-typed or list-typed field collapses to the empty instance or `[]` and stops being recorded); what the disposition does NOT retire (wrong-typed values, which keep emitting `type` or `non_dict`; `extra` keys; `missing` on scalar leaves; the top-level `non_dict` record for a `None`/204 body per D-03a; and the mapping axis in `models.py` per D-03c); and the date plus the commit SHA the roster was measured against.

Then the main table, one row per field, 35 rows, columns: `slug | model | field_path | kind retired | in the 29-SIZING floor? | measured in 33-CENSUS?`. Transcribe the roster from RESEARCH F-10 verbatim — those 35 entries were produced by introspecting `get_type_hints` over the 52 shipped classes and are measurement, not inference. Preserve F-10's structural-finding annotations as an extra note column or as footnotes: `matriz.Instrument.instrumentId` carries `S-3` (the highest-consequence structural finding from `29-SIZING.md`) and the four `matriz.MarketDataSnapshot` entry-value fields carry `S-5`.

For the two right-hand columns you must go and look, not assume. For each of the 35 rows, search `29-SIZING.md` and `33-CENSUS.md` for that `(model, field_path)` pair. Three outcomes are allowed per cell and only three: a citation (`yes — 29-SIZING.md:<line>`), an explicit `no` with the one-clause reason, or `UNKNOWN — <why it could not be resolved from the committed artefacts>`. Do not write a number you did not read. higyrus is already resolved by RESEARCH and must come out as exactly 2 of its 22 floor triples retired — `Movimiento.idMovimientos` and `Posicion.parking`; the other 20 (the 11 of `PosicionValuada`, `Posicion.disponibleAjustado`, and the remaining `Movimiento` fields) are scalars and keep emitting. If your reading of `29-SIZING.md:302-304` disagrees with that, report the discrepancy in the ledger instead of silently adopting either number.

Immediately after the table, add the "Unit warning" paragraph. State, in your own words and with the `33-CENSUS.md` citation, that `29-SIZING.md`'s floors are sums of RECORDS across 43 corpus files while `33-CENSUS.md`'s numbers are DISTINCT triples, that the two differ (higyrus 22/22, matriz 24/14, market-data 50/22), and that a subtraction performed across the two columns invents a shortfall that does not exist. Every number in the subtraction table below must therefore name its column.
  </action>

  <verify>
    <automated>test -f .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md && test "$(grep -c '^| ' .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md)" -ge 35</automated>
  </verify>

  <acceptance_criteria>
    - The file exists and the main table contains exactly 35 field rows (count them excluding header/separator rows and excluding the per-package subtraction table added in Task 2).
    - `grep -c "idMovimientos" <file>` returns at least `1` and `grep -c "parking" <file>` returns at least `1` — the two higyrus floor members RESEARCH resolved are present by name.
    - `grep -c "instrumentId" <file>` returns `5` — matriz's five `InstrumentId`-typed links (`Instrument`, `InstrumentDetail`, `MarketDataFrame`, `Order`, `OrderReport`) all appear.
    - `grep -c "33-CENSUS" <file>` is at least `2` and `grep -c "29-SIZING" <file>` is at least `2` — both source artefacts are cited, not merely alluded to.
    - Per-package row counts in the table are higyrus `11`, market-data `8`, matriz `16`, iol `0`; verify with `grep -c "^| higyrus-client "`, `"^| market-data-client "`, `"^| matriz-client "` on the main table.
    - No cell in the two right-hand columns is blank: `grep -c "| *| *|" <file>` returns `0` for the main table region (every cell carries a citation, a reasoned `no`, or `UNKNOWN`).
  </acceptance_criteria>

  <done>The ledger's main table lists all 35 retired fields with a resolved or explicitly-UNKNOWN provenance per cell, and the unit warning is stated with citations.</done>
</task>

<task type="auto">
  <name>Task 2: Add the per-package expected subtraction and the iol zero-with-a-reason row</name>

  <files>.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</files>

  <read_first>
    - The file produced by Task 1 (read it back before appending — the subtraction must agree with the table you just wrote, cell by cell)
    - `.planning/ROADMAP.md` § Phase 39 criterio 4 and § Phase 38 (which is where iol's `puntas` fields become non-optional links, and therefore where iol's zero stops being zero)
    - `.planning/STATE.md` § Accumulated Context — the `[Phase 33 / 33-07]` entries on non-vacuous per-package floors, and the entry recording that two packages were logged `SKIPPED` with measured cause and named destination rather than as zeros
  </read_first>

  <action>
Append two sections to the ledger.

Section "Expected subtraction per package": a table with columns `package | floor (records column) | floor (distinct-triples column) | triples retired by NOBJ-02 | expected post-35 baseline | column this row counts against`. Fill it from the Task 1 table by counting rows per package. The last column is not decorative — it is the mitigation for the unit hazard, and every row must name one column explicitly.

Two rows need prose, not just numbers, and the prose is the point:

`iol-client` — the retired count is `0`. Write the reason next to it: iol's `puntas` fields are declared `Optional` today, so they take the walker's early-return branch and were never emitting a link-level divergence in the first place; they become non-optional links only in Phase 38, at which point they start participating in this disposition. A bare `0` here, or omitting the package from the table, would read as "iol was already clean", which is a different and false claim.

`ambito-financiero-client` and `wallets-client` — these declare no response models at all (D-05), so they contribute no rows by enumeration rather than by cleanliness. Say so, and cite the two deliberately-empty `models.py` module docstrings as the evidence, so a future reader does not read the absence as an oversight.

Section "How Phase 39 should use this": three short paragraphs. First, the mechanics — Phase 39's live census is a set of distinct 4-tuples taken from `DivergenceHandler.seen`; the expected relationship is `census_39 ≈ census_33 − retired_here − fixed_in_36_37_38`, and the ledger supplies the middle term. Second, the failure mode this exists to prevent — if the middle term is not subtracted explicitly, the drop is attributed to quality and the milestone reports a clean it did not earn. Third, what a NON-balancing subtraction means: it is a finding to investigate, not a rounding error, and the two most likely causes are (a) a field reachable live in a shape the static roster did not anticipate and (b) a unit-column mix-up — check (b) first, because it is free to check.

Close the file with a "Method and limits" note: the roster came from introspecting `get_type_hints` over the shipped classes, so it is blind to anything that is not a declared annotation — value-level divergences (out-of-set values, NaN/Infinity, range and format problems, cross-field inconsistency) are outside it by construction, and the ledger therefore under-counts in exactly one direction. Say which direction and why that is the safe direction for this artefact to err in.
  </action>

  <verify>
    <automated>grep -q "iol-client" .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md && grep -q "wallets-client" .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md && grep -qi "phase 39" .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md</automated>
  </verify>

  <acceptance_criteria>
    - The subtraction table names all six packages: `grep -c "iol-client\|higyrus-client\|market-data-client\|matriz-client\|ambito-financiero-client\|wallets-client" <file>` is at least `6`, and each of the six appears at least once in the subtraction section specifically.
    - The higyrus subtraction row reads floor `22` (both columns), retired `2`, expected post-35 `20` — matching RESEARCH's derivation from `29-SIZING.md:302-304`.
    - The iol row's retired count is `0` AND the same table row or the line immediately following it names Phase 38 as where that changes: `grep -A1 "^| iol-client" <file> | grep -qi "phase 38"`.
    - Neither `ambito-financiero-client` nor `wallets-client` appears with a bare zero and no prose: each has an accompanying sentence citing the deliberately-empty `models.py` decision (D-05).
    - The "Method and limits" note states the direction of the undercount explicitly.
    - `grep -ci "estimat" <file>` returns `0` outside of any sentence explicitly denying that the numbers are estimates — every count is cited or marked UNKNOWN.
  </acceptance_criteria>

  <done>Phase 39 can open one file, read the middle term of its subtraction per package with the counting column named, and know both how to use it and where it is blind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes a single Markdown file under `.planning/`. It executes no code, imports no package, reads no wire payload, and touches no credential, transport, or dependency manifest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Repudiation | The ledger becoming the authority for a claim it cannot support | mitigate | Every count must be citation-backed or explicitly `UNKNOWN`; the "Method and limits" section states the introspection blind spot and the direction of the undercount. Enforced by the acceptance criteria on blank cells and on the absence of uncited numbers. |
| T-35-05 | Information disclosure | Leaking wire values or credentials into a committed planning document | accept | The ledger records declared type names and field paths only — no payload values, no hostnames, no tokens. Field paths derive from source annotations, not from live traffic. |
| T-35-SC | Tampering | pip / uv dependency installs | accept | No installs; no dependency manifest is touched. `git diff --exit-code pyproject.toml uv.lock` stays clean by construction. |
</threat_model>

<verification>
1. `test -f .planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RETIRED-TRIPLES.md`
2. `git diff --stat` shows exactly one file changed, under `.planning/` — no package, tool, or test file was touched by this plan.
3. `uv run pytest packages -q` — unchanged and green (sanity: this plan cannot have affected it).
4. The four v1.6 gates — unchanged and green.
</verification>

<success_criteria>
- `35-RETIRED-TRIPLES.md` exists with 35 field rows plus a six-package subtraction table.
- Every numeric cell is either citation-backed or explicitly `UNKNOWN` with a stated reason.
- The unit hazard (records-summed vs. distinct triples) is stated once with a `33-CENSUS.md` citation and every subtraction row names the column it counts against.
- iol's `0`, ambito's and wallets' absences all carry reasons and named destinations — no bare zero anywhere in the document.
- The document explains what a non-balancing Phase 39 subtraction means and which cause to check first.
</success_criteria>

<output>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-02-SUMMARY.md` when done.
Record the per-package retired counts and any cell that came out `UNKNOWN`, so 35-05's verification
and Phase 39's planner both see the gaps without reopening the ledger.
</output>
