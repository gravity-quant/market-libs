---
phase: 28-release-prep-publish-v0-3-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/pyproject.toml
  - packages/market-data-client/src/market_data_client/__init__.py
  - packages/market-data-client/README.md
  - uv.lock
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PUB-MUT-01]

must_haves:
  truths:
    - "The three version sites (packages/market-data-client/pyproject.toml, __version__, uv.lock workspace member) all read 0.4.0 and `uv lock --check` exits 0 — D-01, D-04, D-05, SC#1"
    - "packages/market-data-client/README.md carries a `### v0.4.0` changelog entry that explicitly names the CalendarDay field replacement (date/marketId/isBusinessDay removed, day/closed/description/open_time/close_time added) — D-03, non-negotiable"
    - "PUB-MUT-01 in REQUIREMENTS.md and the Phase 28 block in ROADMAP.md target v0.4.0 instead of the already-published v0.3.0, and the mypy/import-linter enrolment follow-up is archived in ROADMAP § Backlog for v1.6 — D-02, D-16"
    - "The local mirror of all four CI jobs is green and the full origin/main...HEAD diff carries no credential — D-15, T-28-01"
    - "origin/milestone/v1.5-mutations resolves to the same commit as local HEAD via a plain fast-forward push, never --force — C-1"
    - "No file under .github/workflows/ and no line of CLAUDE.md is touched by this phase — D-06, D-07"
  artifacts:
    - path: "packages/market-data-client/pyproject.toml"
      provides: "the version string release.yml validates against the tag"
      contains: "version = \"0.4.0\""
    - path: "packages/market-data-client/src/market_data_client/__init__.py"
      provides: "runtime-visible version mirror (NOT validated by the pipeline)"
      contains: "__version__ = \"0.4.0\""
    - path: "packages/market-data-client/README.md"
      provides: "consumer-facing changelog entry with the D-03 CalendarDay callout"
      contains: "### v0.4.0"
    - path: "uv.lock"
      provides: "workspace-member version registration, refreshed by `uv lock`"
      contains: "version = \"0.4.0\""
    - path: ".planning/ROADMAP.md"
      provides: "D-02 re-point + D-16 v1.6 backlog archive"
      contains: "### Deferred to v1.6"
  key_links:
    - from: "packages/market-data-client/pyproject.toml"
      to: ".github/workflows/release.yml"
      via: "release.yml:42-51 awk-reads the FIRST ^version line and fails the build unless it equals the tag version"
      pattern: "version = \"0.4.0\""
    - from: "packages/market-data-client/pyproject.toml"
      to: "uv.lock"
      via: "`uv lock` re-registers the workspace member version; `uv lock --check` is CI check 1 of 15 (ci.yml:32)"
      pattern: "name = \"market-data-client\""
---

<objective>
Perform every REVERSIBLE step of the `market-data-client` v0.4.0 release: bump the three version
sites, author the `### v0.4.0` changelog entry with the mandatory `CalendarDay` callout, refresh
`uv.lock`, re-point the `.planning/` artifacts from the already-published v0.3.0 to v0.4.0,
archive the D-16 follow-up in the v1.6 backlog, mirror the CI gate locally, re-run the
credential scan over the diff that is about to become public, and push the release branch.

Purpose: Everything in this plan can be amended or reverted with a commit. Isolating it from the
irreversible publish (plans 02 and 03) means a failed prep costs nothing (RESEARCH § Pattern 1).
Output: A pushed `milestone/v1.5-mutations` whose tip carries the 4-file bump commit and the
`.planning/` re-point commit, with all local CI gates green.

**Naming:** the phase directory says `v0-3-0`. The version this phase publishes is **`0.4.0`**
(D-01). `market-data-client-v0.3.0` and `-v0.3.1` are ALREADY published (PR #8 → `ea92dd8`,
PR #9 → `7b0e0b2`). Any assumption that v0.3.0 is unpublished is wrong.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md
@.planning/phases/28-release-prep-publish-v0-3-0/28-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Non-symbol artifacts produced across phase 28 (this plan produces the first group):

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| `### v0.4.0` README changelog section | `packages/market-data-client/README.md` | 28-01 Task 1 | `grep -qx '### v0.4.0'` + all 8 backticked `CalendarDay` field names asserted inside the sliced section |
| Three aligned version sites at `0.4.0` | `pyproject.toml`, `__init__.py`, `uv.lock` | 28-01 Task 1 | three-way `awk`/`grep` assertion |
| ROADMAP § Backlog `### Deferred to v1.6+` entry | `.planning/ROADMAP.md` | 28-01 Task 2 | `grep -q '^### Deferred to v1.6'` |
| Pushed release branch (fast-forward) | `git-ref:origin/milestone/v1.5-mutations` | 28-01 Task 3 | `git rev-parse HEAD` == `git rev-parse origin/...` |
| Merge commit on `main` (two parents) | `git-ref:origin/main` | 28-02 Task 3 | `git rev-list --parents -n1` → 3 fields |
| Release tag on the merge commit | `git-tag:market-data-client-v0.4.0` | 28-03 Task 2 | `git rev-list -n1 <tag>` == `git rev-parse origin/main` |
| Public GitHub Release + wheel + sdist | `gh-release:market-data-client-v0.4.0` | 28-03 Task 2 | `gh release view --json assets` contains `.whl` and `.tar.gz` |
</artifacts_this_phase_produces>

<scope_decisions>
Conscious deferrals, recorded so the executor and verifier see a decision rather than a gap.
Do NOT edit these unless the operator explicitly reopens scope.

- `.github/workflows/ci.yml` — already lists `market-data-client` in `matrix.package` (`:103`);
  the 15-check gate needs no edit. **D-06 — do not touch.**
- `.github/workflows/release.yml` — the tag regex (`:28`) already matches
  `market-data-client-v0.4.0`; the workflow is generic. **D-06 — do not touch.**
- `CLAUDE.md` (the `market-data-client v0.2.0` workspace bullet, ~line 74) — three releases
  stale, but it is a `/gsd-map-codebase` generated artifact and no prior release commit touched
  it (confirmed against `ea92dd8` and `7b0e0b2`). **D-07 — do not touch**; known doc debt.
- Root mypy `files` (`pyproject.toml:97`), import-linter `root_packages` (`:141-146`) and the
  per-package mypy-tests loop (`ci.yml:85`) still omit `market-data-client`. This is a typecheck
  COVERAGE gap, not a CI failure — every package-scoped check is green today. **D-16 — remains
  deferred; Task 2 archives it in ROADMAP § Backlog for v1.6** so it stops rolling silently
  release after release.
- The 19 pre-existing matriz failures under `verification/` stay unfixed. No CI job executes
  `verification/` (the only pytest invocation, `ci.yml:112-118`, passes an explicit
  `packages/<pkg>` path that overrides the root `testpaths`). **D-14 — out of scope.**
- No `CalendarDay` compat shim. D-03 rejected the deprecated-alias shim in favour of the
  changelog callout; do not re-open it.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Bump the three version sites, author the `### v0.4.0` changelog entry, refresh uv.lock (D-01, D-03, D-04 sites 1-4, D-05)</name>
  <files>packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/__init__.py, packages/market-data-client/README.md, uv.lock</files>
  <read_first>
    - `packages/market-data-client/pyproject.toml` (lines 1-10) — the `[project]` block; `version` is line 3
    - `packages/market-data-client/src/market_data_client/__init__.py` (lines 120-134) — `__all__` tail and `__version__` on the last line
    - `packages/market-data-client/README.md` (lines 55-105) — `## Changelog` H2 at line 60, `### v0.3.1` at 62, `### v0.3.0` at 72-88, `### v0.2.0` at 90-103. These three entries ARE the format contract.
    - `uv.lock` (lines 484-495) — the `[[package]] name = "market-data-client"` block; `version` is line 488
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md` § 4 (lines 121-218) — the verbatim `7f051ae` insertion diff plus Model A / Model B / Model C changelog voice and the locked conventions list
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Public Surface Delta (lines 530-604) — the 13 new public names, the `CalendarDay` field table, the non-breaking Phase-27 changes
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` § Decisions D-01 through D-05
  </read_first>
  <action>
    Perform the four edits in EXACTLY this order — the ordering is load-bearing (RESEARCH P-2).

    (a) `packages/market-data-client/pyproject.toml` line 3 — set the `[project]` `version` value
    to `0.4.0` (D-01, D-04 site 1). Nothing else in the file moves. `release.yml:42-51` awk-reads
    the FIRST `^version` line in this file and hard-fails the build with `Tag (X) ≠ pyproject.toml
    (Y)` on any mismatch with the tag, so this value and the tag `market-data-client-v0.4.0` must
    agree exactly. Do NOT choose 0.3.2 (the diff adds 13 new public names — a patch bump would
    violate semver for every `~=0.3.1` pin) and do NOT choose 1.0.0 (contradicts locked D-13/D-22
    and burns the major that `Symbol`'s docstring reserves for removing the `marketId` alias).

    (b) `packages/market-data-client/src/market_data_client/__init__.py` — set the final line to
    `__version__ = "0.4.0"` (D-04 site 2). The release pipeline does NOT validate this site
    (RESEARCH P-4): if it drifts, the Release ships green and every consumer reading
    `market_data_client.__version__` gets the wrong answer, undetectable at release time. The
    local assertion in this task's verify block is the only defense.

    (c) `packages/market-data-client/README.md` — insert a new `### v0.4.0` entry immediately
    ABOVE the existing `### v0.3.1` heading (currently line 62), under the `## Changelog` H2, with
    a blank line after `## Changelog`, a blank line after the new H3, and a blank line before
    `### v0.3.1` (D-04 site 3). Hold every convention the three existing entries establish:
    Spanish prose throughout; `### vX.Y.Z` H3; a bold lead line naming the bump class followed by
    a parenthetical semver justification; bullets shaped `- **<Área> (<REQ-ID>):**` with the
    requirement ID inline in the bold lead; every identifier and endpoint path in backticks;
    wrapped ~95-100 columns; continuation lines indented exactly 2 spaces. Model the new-surface
    bullets on the `### v0.3.0` entry (README.md:72-88) and the breaking-field callout on the
    `### v0.2.0` entry (README.md:90-103). The `trailing-whitespace` and `end-of-file-fixer`
    pre-commit hooks run over this file in CI — no trailing spaces.

    Required content, all three items, none optional:
      1. Calendar write, cited `(MUT-MD-02)` — the 8 new names in the flat `__all__`:
         `MarketHoursIn`, `HolidayIn`, `HolidaysIn`, `set_calendar_config`,
         `delete_calendar_config`, `preview_calendar_config`, `add_holidays`, `delete_holiday`;
         plus their 5 `aio` counterparts (`set_calendar_config`, `delete_calendar_config`,
         `preview_calendar_config`, `add_holidays`, `delete_holiday`, module-level shims, not
         re-exported into the flat namespace per monorepo convention); the endpoints
         `PUT /calendar/config`, `DELETE /calendar/config`, `POST /calendar/config/preview`,
         `POST /calendar/holidays`, `DELETE /calendar/holidays/{day}`; and the `confirm` guardrail
         defaulting to `False` so real market config is never persisted implicitly. All of it sits
         behind the existing opt-in mutating-gate.
      2. Live-verified fixes, cited `(LIVE-MUT-01)` — `update_symbol(symbol_id)` WIDENED from
         `str` to `int | str` at all four routes; `Symbol` gains five DEFAULTED fields `id`,
         `market_id`, `created_at`, `updated_at`, `received_at`; `Symbol.marketId` PRESERVED as a
         deprecated alias mirrored from the wire `market_id` via a `from_api` override; the
         symbols-write envelope is unwrapped while preserving `list[Symbol]`. All strictly
         additive or widening — describe them as non-breaking.
      3. NON-NEGOTIABLE (D-03) — an explicit `CalendarDay` field-replacement callout stating that
         `date`, `marketId` and `isBusinessDay` were REMOVED outright (no aliases) and replaced by
         `day`, `closed`, `description`, `open_time` and `close_time`; and that it ships inside a
         minor bump per D-13 because `parse_calendar_response` previously iterated the envelope's
         keys, so no released consumer could ever have held a populated instance. Those five new
         and three old field names must appear literally in the README text. Use the
         `### v0.2.0` voice ("Breaking changes (semver minor bump en línea 0.x)"). Do NOT add a
         compat shim — the callout IS the locked mitigation and the shim was explicitly rejected.

    (d) Run `uv lock` — never hand-edit `uv.lock` (D-05, D-04 site 4). It MUST run AFTER step (a):
    `uv lock` reads workspace-member versions from `pyproject.toml`, so running it first would
    regenerate the lock at 0.3.1 and desync it, failing CI check 1 of 15 (`ci.yml:32`). Then run
    `uv lock --check` (exit 0) and confirm the churn is exactly two lines — `git diff --stat
    uv.lock` must read `uv.lock | 2 +-`. A larger diff means uv re-resolved third-party
    dependencies and would smuggle an unreviewed dependency change into a release PR: STOP and
    surface it rather than committing (RESEARCH P-9, threat T-28-04).

    (e) Run the package suite scoped: `uv run pytest packages/market-data-client -q` — expect
    `387 passed`. NEVER run a bare `uv run pytest`: the root `testpaths` includes `verification/`,
    which carries 19 known pre-existing matriz failures that no CI job ever executes (D-14,
    RESEARCH P-5). A bare run would block this release indefinitely on out-of-scope failures.

    (f) Commit EXACTLY these four files and nothing else, matching the shape of the `7f051ae`
    precedent commit (README + pyproject + `__init__.py` + `uv.lock`). Subject:
    `chore(market-data-client): bump to v0.4.0 (calendar write + live-verified mutation fixes)`.
    Body: one short paragraph naming the bump class and headline, then a final line that
    inventories the four aligned sites (pyproject + `__version__` + README changelog + `uv.lock`
    aligned at 0.4.0). Do NOT include `.planning/` files (Task 2 owns those) and do NOT include
    the release-memory file (plan 28-03 owns it — it cites a `release.yml` run ID that does not
    exist yet, C-3).
  </action>
  <verify>
    <automated>V=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml); test "$V" = "0.4.0" && grep -qx '__version__ = "0.4.0"' packages/market-data-client/src/market_data_client/__init__.py && grep -A1 '^name = "market-data-client"$' uv.lock | grep -qx 'version = "0.4.0"' && grep -qx '### v0.4.0' packages/market-data-client/README.md && SEC=$(awk '/^### v0\.4\.0$/{f=1;next} /^### v/{f=0} f' packages/market-data-client/README.md) && test -n "$SEC" && for T in CalendarDay '`date`' '`marketId`' '`isBusinessDay`' '`day`' '`closed`' '`description`' '`open_time`' '`close_time`'; do printf '%s\n' "$SEC" | grep -qF -- "$T" || { echo "D-03 CALLOUT INCOMPLETE, missing: $T"; exit 1; }; done && grep -q 'MUT-MD-02' packages/market-data-client/README.md && grep -q 'LIVE-MUT-01' packages/market-data-client/README.md && uv lock --check && test "$(git show --numstat --format= HEAD -- uv.lock | awk '{print $1, $2}')" = "1 1" && uv run pytest packages/market-data-client -q && uv run python -c "import market_data_client as m; assert all(n in m.__all__ for n in ('get_market_data','get_latest','get_calendar','CalendarDay','Symbol','MarketDataSnapshot'))" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/market-data-client/pyproject.toml` outputs exactly `0.4.0`
    - `grep -qx '__version__ = "0.4.0"' packages/market-data-client/src/market_data_client/__init__.py` exits 0
    - The line following `^name = "market-data-client"$` in `uv.lock` is exactly `version = "0.4.0"`
    - `uv lock --check` exits 0
    - `git show --numstat --format= HEAD -- uv.lock` reports exactly 1 insertion and 1 deletion (the 2-line churn of the `7f051ae` precedent)
    - `packages/market-data-client/README.md` contains a line exactly equal to `### v0.4.0`, positioned above the `### v0.3.1` heading
    - That README section names all three removed `CalendarDay` fields (`date`, `marketId`, `isBusinessDay`) and all five replacements (`day`, `closed`, `description`, `open_time`, `close_time`). Each of the eight field names MUST be written in backticks — that is the established changelog convention (see the `v0.2.0` entry, `README.md:96-99`) and the automated gate slices the `### v0.4.0`..next-`### v` section and requires all eight backticked tokens plus `CalendarDay` inside it. An unbackticked field name fails the gate.
    - That README section cites both `(MUT-MD-02)` and `(LIVE-MUT-01)` and names all 8 new flat `__all__` entries
    - `uv run pytest packages/market-data-client -q` reports `387 passed` (no bare `uv run pytest` was executed at any point)
    - `uv run python -c "import market_data_client as m; assert all(n in m.__all__ for n in ('get_market_data','get_latest','get_calendar','CalendarDay','Symbol','MarketDataSnapshot'))"` exits 0 — the v0.2.0 read surface is intact (SC#4)
    - `git show --stat HEAD` lists exactly 4 files: README.md, pyproject.toml, `__init__.py`, uv.lock
  </acceptance_criteria>
  <done>The three version sites read 0.4.0 and agree, `uv.lock` churned by exactly two lines, the README carries a `### v0.4.0` entry with the mandatory `CalendarDay` callout, 387 package tests pass, and one 4-file `chore(market-data-client): bump to v0.4.0 (…)` commit exists.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point PUB-MUT-01 and the ROADMAP Phase 28 block to v0.4.0 and archive the D-16 follow-up in the v1.6 backlog (D-02, D-16)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` line 28 (the PUB-MUT-01 bullet) and line 65 (the traceability table row `| PUB-MUT-01 | Phase 28 | Pending |`); lines 20-24 show the `- [x] **REQ-ID**: …` completed form used by siblings
    - `.planning/ROADMAP.md` line 10 (v1.5 milestone bullet), line 19 (Phase 28 checklist bullet), lines 188-199 (the `### Phase 28:` detail block with Goal + 4 Success Criteria), line 233 (Progress table row), lines 235-246 (`## Backlog` and the existing `### Deferred to v1.5+` bullet formatting)
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md` § 7 and § 8 (lines 382-443) — the exact current text of every site plus the sibling bullet formats to match
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` D-02 and D-16
  </read_first>
  <action>
    Re-point the planning artifacts so PUB-MUT-01 is satisfied by publishing 0.4.0, and archive the
    D-16 follow-up. Wording is Claude's discretion; the target identifiers below are not.

    (a) `.planning/REQUIREMENTS.md` line 28 — the PUB-MUT-01 bullet currently names the 0.3.x
    release twice (the bare version and the tag). Set the target version to `v0.4.0` and the
    target tag to the exact literal `market-data-client-v0.4.0`, and append a short constancia
    (Spanish, matching the surrounding voice) recording that `v0.3.0` and `v0.3.1` were published
    mid-milestone via PR #8 (`ea92dd8`) and PR #9 (`7b0e0b2`), so the requirement is satisfied by
    the 0.4.0 publication rather than by the 0.3.0 one. Leave the checkbox unchecked — phase close
    flips it. Do NOT alter the `**PUB-MUT-01**` ID token.

    (b) `.planning/REQUIREMENTS.md` line 65 — leave `| PUB-MUT-01 | Phase 28 | Pending |` as is.
    Flipping it to `Complete` is the phase-close step, not a prep step; the sibling row
    `| LIVE-MUT-01 | Phase 27 | Complete |` shows the form it takes later.

    (c) `.planning/ROADMAP.md` line 19 — KEEP the phase title text `Phase 28: Release prep +
    publish v0.3.0` exactly as written; GSD tooling resolves the phase directory
    `28-release-prep-publish-v0-3-0` from it and renaming it would break `init.plan-phase` and
    every downstream command. Re-point only the release target inside the bullet body: the tag
    becomes the exact literal `market-data-client-v0.4.0`, plus a short parenthetical noting the
    0.3.x releases already shipped mid-milestone and that this phase publishes v0.4.0.

    (d) `.planning/ROADMAP.md` lines 188-199 — KEEP the `### Phase 28: Release prep + publish
    v0.3.0` heading verbatim for the same tooling reason. Inside the block: set the **Goal** to
    publishing `v0.4.0`; set SC#1's bumped-version token to `0.4.0` and extend it to say the
    changelog documents the calendar-write surface AND the `CalendarDay` field replacement; set
    SC#3's tag to the exact literal `market-data-client-v0.4.0`; and re-baseline SC#4 so the
    no-breaking claim is measured against **v0.3.1** (the actual predecessor, not v0.2.0) with an
    explicit acknowledgement of the D-03 `CalendarDay` carve-out — the read surface stays
    compatible, the `CalendarDay` field set is the documented exception.

    (e) `.planning/ROADMAP.md` line 10 — the v1.5 milestone bullet ends by naming the published
    version; update that trailing token to `v0.4.0` for consistency with (c) and (d). Leave the
    Progress-table row at line 233 unchanged (its title text is the same tooling-keyed string).

    (f) `.planning/ROADMAP.md` § Backlog — insert a new `### Deferred to v1.6+ (from v1.5)`
    subsection immediately after the `## Backlog` intro line and before the existing
    `### Deferred to v1.5+ (from v1.4 …)` subsection. Match the established bullet shape
    `- **<ID or short title>** — <description with backticked identifiers/paths> — <qualifier>`.
    The entry records the D-16 follow-up: enrol `market-data-client` in the root mypy `files` list
    (`pyproject.toml:97`, currently 5 packages), in the import-linter `root_packages` list
    (`pyproject.toml:141-146`, currently 4) and in the per-package mypy-tests loop (`ci.yml:85`,
    currently 5) — which requires authoring an import-linter contract for
    `market_data_client._core`. State plainly that it is a typecheck COVERAGE gap and **not** a CI
    failure (every package-scoped check is green; real mypy coverage for this package comes from
    the pre-commit hook scoped `files: ^packages/.*/src/`), deferred since Phase 24 and explicitly
    archived here per D-16 so it stops rolling silently release after release. The `REFAC-06`
    bullet later in the same Backlog is the model for a long-form deferral entry with rationale.

    (g) Commit these two files as a single separate commit — subject shape `docs(28): …` per the
    branch convention (`773e1ca`, `6bbf2ce`). Keeping it out of the Task 1 commit preserves that
    commit's exact 4-file release shape.
  </action>
  <verify>
    <automated>grep -n 'PUB-MUT-01' .planning/REQUIREMENTS.md | grep -q 'market-data-client-v0.4.0' && test "$(grep -c 'market-data-client-v0\.3\.0' .planning/REQUIREMENTS.md)" -eq 0 && test "$(grep -c 'market-data-client-v0\.3\.0' .planning/ROADMAP.md)" -eq 0 && test "$(grep -c 'market-data-client-v0\.4\.0' .planning/ROADMAP.md)" -ge 2 && grep -qx '### Phase 28: Release prep + publish v0.3.0' .planning/ROADMAP.md && grep -q '^### Deferred to v1.6' .planning/ROADMAP.md && grep -q 'root_packages' .planning/ROADMAP.md && grep -q 'import-linter' .planning/ROADMAP.md && grep -q '| PUB-MUT-01 | Phase 28 | Pending |' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The PUB-MUT-01 bullet in `.planning/REQUIREMENTS.md` names the tag `market-data-client-v0.4.0` and records that v0.3.0/v0.3.1 shipped mid-milestone
    - `grep -c 'market-data-client-v0\.3\.0' .planning/REQUIREMENTS.md` returns `0`
    - `grep -c 'market-data-client-v0\.3\.0' .planning/ROADMAP.md` returns `0`
    - `grep -c 'market-data-client-v0\.4\.0' .planning/ROADMAP.md` returns at least `2` (line 19 bullet + SC#3)
    - The line `### Phase 28: Release prep + publish v0.3.0` still exists verbatim in `.planning/ROADMAP.md` (phase-directory resolution depends on it)
    - `.planning/ROADMAP.md` contains a `### Deferred to v1.6` backlog subsection whose body mentions `root_packages`, `import-linter` and `ci.yml:85`
    - The traceability row `| PUB-MUT-01 | Phase 28 | Pending |` is unchanged
    - `git show --stat HEAD` for this commit lists exactly `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`
  </acceptance_criteria>
  <done>PUB-MUT-01 and the ROADMAP Phase 28 block target v0.4.0 with the mid-milestone constancia recorded, the phase title strings the tooling keys off are untouched, the D-16 follow-up is archived under a `### Deferred to v1.6+` backlog subsection, and one `docs(28): …` commit exists.</done>
</task>

<task type="auto">
  <name>Task 3: Mirror the CI gate locally, re-run the credential scan over the release diff, and fast-forward push the release branch (D-06, D-07, D-14, D-15, C-1, T-28-01)</name>
  <files>(no working-tree files modified — local verification + `git push`)</files>
  <read_first>
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Contradictions C-1 (lines 257-288) — the branch has never been pushed; the fast-forward is verified; `--force` is forbidden
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-RESEARCH.md` § Security Domain (lines 1280-1322) — the exact secret-scan commands and the clean result from 2026-08-01
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-PATTERNS.md` § S-3 and § S-4 (lines 725-746) — the scoped test invocation and the 6-command local CI mirror
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-VALIDATION.md` § Per-Task Verification Map — rows for the local mirror, the secret scan, the workflow-diff assertion and the push assertion
    - `.planning/phases/28-release-prep-publish-v0-3-0/28-CONTEXT.md` D-06, D-07, D-10, D-14, D-15
  </read_first>
  <action>
    Run every gate that CI will run, prove the diff about to become public carries no credential,
    then publish the branch. Nothing here writes to the working tree.

    (a) Mirror the four CI jobs locally, in this order, each must succeed:
    `uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`;
    `uv run mypy`; `uv run pytest packages/market-data-client -q`; `uv run pre-commit run
    --all-files`. Baselines measured on this branch on 2026-08-01 (D-15): ruff check "All checks
    passed!", ruff format "201 files already formatted" (expect 201 or more), lint-imports
    "Contracts: 4 kept, 0 broken.", mypy "Success: no issues found in 51 source files", pytest
    "387 passed", pre-commit all 9 hooks Passed with zero file rewrites. If `pre-commit` rewrites
    any file (trailing whitespace or EOF on the README entry authored in Task 1), amend Task 1's
    commit with the fix and re-run the whole mirror. NEVER run a bare `uv run pytest` (D-14).

    (b) Assert the two do-not-touch guarantees hold across the whole release diff, using the last
    published tag as the baseline: `git diff --name-only market-data-client-v0.3.1..HEAD --
    .github/workflows` must produce zero lines (D-06), and the same command scoped to `CLAUDE.md`
    must also produce zero lines (D-07).

    (c) Re-run the credential scan over the FULL diff that this PR will publish to a PUBLIC
    default branch. The scan RESEARCH ran on 2026-08-01 came back clean, but the diff grows with
    every commit added during this phase — including Tasks 1 and 2 — so it must be re-run here,
    immediately before the push (threat T-28-01). Run `git fetch origin main` first, then check:
    no JWT-shaped token in `git diff origin/main...HEAD` (pattern `eyJ[A-Za-z0-9_-]{20,}`); no
    `client_secret`-style assignment of a 20+ character value in the same diff; and no tracked
    `.env` file anywhere (`git ls-files` filtered to a path component equal to `.env` — only the
    six `.env.example` templates are legitimate). If ANY of these matches, STOP: do not push, do
    not proceed to plan 28-02, and surface the exact file and line. Never echo a token, a secret
    value or the `gh`/SSH credential itself into output — report file and line only.

    (d) Push the branch: `git push origin milestone/v1.5-mutations`. This step is missing from
    CONTEXT.md and is BLOCKING (C-1): `origin/milestone/v1.5-mutations` is stale at `ce77ed4` and
    every commit of Phases 25-27 plus this phase is local-only, so `gh pr create` in plan 28-02
    would fail or prompt interactively (RESEARCH P-1). The push is a verified plain fast-forward
    (`git merge-base --is-ancestor ce77ed4 HEAD` succeeds). `--force` and `--force-with-lease` are
    FORBIDDEN: a force-push would rewrite the ~99 commits whose SHAs the Phase 25-27 SUMMARYs
    cross-reference. If the push is rejected as non-fast-forward, STOP and surface it — do not
    escalate to a force flag, and do not rebase or merge `origin/main` into the branch (D-10: the
    "2 behind" state is cosmetic, `git diff --stat HEAD...origin/main` is empty, and a rebase
    would rewrite history reachable from the published v0.3.0/v0.3.1 tags).

    (e) Do NOT run `/gsd-pr-branch` or filter `.planning/` out of the branch (D-09) — the ~60 KB of
    live-verification evidence under `.planning/verification/` must reach `main`.

    Compute the ahead/behind counts at run time rather than trusting any literal in the planning
    artifacts; they drift with every commit.
  </action>
  <verify>
    <automated>uv lock --check && uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run mypy && uv run pytest packages/market-data-client -q && uv run pre-commit run --all-files && test "$(git diff --name-only market-data-client-v0.3.1..HEAD -- .github/workflows | wc -l | tr -d ' ')" = "0" && test "$(git diff --name-only market-data-client-v0.3.1..HEAD -- CLAUDE.md | wc -l | tr -d ' ')" = "0" && git fetch origin main --quiet && ! git diff origin/main...HEAD | grep -qE 'eyJ[A-Za-z0-9_-]{20,}' && ! git diff origin/main...HEAD | grep -qiE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}' && test -z "$(git ls-files | grep -E '(^|/)\.env$')" && git fetch origin milestone/v1.5-mutations --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/milestone/v1.5-mutations)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All seven local mirror commands exit 0; `uv run ruff check .` prints "All checks passed!"; `uv run lint-imports` prints "Contracts: 4 kept, 0 broken."; `uv run mypy` prints "Success: no issues found in 51 source files"; `uv run pytest packages/market-data-client -q` prints `387 passed`; `uv run pre-commit run --all-files` passes with zero file rewrites
    - `git diff --name-only market-data-client-v0.3.1..HEAD -- .github/workflows | wc -l` outputs `0` (D-06)
    - `git diff --name-only market-data-client-v0.3.1..HEAD -- CLAUDE.md | wc -l` outputs `0` (D-07)
    - `git diff origin/main...HEAD | grep -E 'eyJ[A-Za-z0-9_-]{20,}'` produces no match
    - `git diff origin/main...HEAD | grep -iE 'client_secret[[:space:]]*[=:][[:space:]]*.{0,2}[A-Za-z0-9_-]{20,}'` produces no match (verified false-positive-free against the current diff)
    - `git ls-files | grep -E '(^|/)\.env$'` produces empty output
    - `git rev-parse HEAD` equals `git rev-parse origin/milestone/v1.5-mutations`
    - The reflog / command history shows `git push origin milestone/v1.5-mutations` executed with no `--force` and no `--force-with-lease` flag
    - `git status --porcelain` is empty (clean tree — precondition for plan 28-02)
  </acceptance_criteria>
  <done>All seven local CI-mirror gates are green, the workflow and CLAUDE.md diffs are empty, the credential scan over `origin/main...HEAD` is clean, no `.env` is tracked, and `origin/milestone/v1.5-mutations` now resolves to local `HEAD` via a plain fast-forward push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local working tree → public GitHub repo | `gravity-quant/market-libs` visibility is `public`; this branch pushes ~90 changed files including ~60 KB of live-verification evidence and a 143 KB live driver. Everything crossing this boundary becomes permanently public. |
| `pyproject.toml` version string → `release.yml` build gate | The pipeline reads the version from the tagged tree and refuses to build on mismatch; a wrong value here produces a failed or mislabelled public artifact. |
| `uv lock` resolver → committed `uv.lock` | An unreviewed dependency re-resolution can enter the repo through a lockfile refresh nobody reads. |
| agent shell → GitHub credentials (`gh` OAuth token, SSH key) | Credentials are ambient in the environment; any echo of them lands in transcripts and logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | the `origin/main...HEAD` diff published to a PUBLIC default branch | mitigate | Task 3 (c) re-runs the credential scan immediately before the push — JWT pattern `eyJ[A-Za-z0-9_-]{20,}`, `client_secret`-style assignments, and any tracked `.env` — and STOPS the phase on any match. ASVS V14. Re-run is required because the diff grows with Tasks 1-2. |
| T-28-02 | Tampering | version strings vs. the release tag | mitigate | Task 1 asserts pyproject == `__version__` == `uv.lock` == `0.4.0` locally. The pipeline validates ONLY `pyproject.toml` (`release.yml:47`), so a drifted `__version__` would ship a silently mislabelled artifact — the local three-way assertion is the sole defense. ASVS V5. |
| T-28-04 | Supply Chain / Tampering | `uv.lock` refresh | mitigate | Task 1 (d) asserts the churn is exactly 1 insertion + 1 deletion (`uv.lock \| 2 +-`); any larger diff means a third-party re-resolution and halts the task before commit. |
| T-28-07 | Information Disclosure | `gh` OAuth token / SSH key in agent output | mitigate | Never echo a token or secret value; use `gh auth status` (which redacts). Scan findings report file and line only, never the matched value. ASVS V2. |
| T-28-08 | Tampering | published git history (Phases 25-27 SHAs) | mitigate | Task 3 (d) forbids `--force` / `--force-with-lease` and forbids rebasing or merging `origin/main` into the branch (D-10); the push is a verified plain fast-forward and a rejection halts the task instead of escalating. |
| T-28-SC | Tampering | uv/pip/cargo installs | accept | This phase installs zero external packages — `uv lock` re-resolves the existing dependency set with only the local workspace member's version string changed. No package legitimacy audit is applicable. |
</threat_model>

<verification>
1. Three version sites read `0.4.0` and agree; `uv lock --check` exits 0.
2. `uv.lock` churned by exactly two lines.
3. `packages/market-data-client/README.md` has a `### v0.4.0` entry naming all three removed and all five new `CalendarDay` fields, citing `(MUT-MD-02)` and `(LIVE-MUT-01)`.
4. `uv run pytest packages/market-data-client -q` → `387 passed` (never a bare `uv run pytest`).
5. The v0.2.0 read surface is intact — the `__all__` import assertion exits 0 (SC#4).
6. All seven local CI-mirror commands are green.
7. `.github/workflows/` and `CLAUDE.md` have a zero-line diff since `market-data-client-v0.3.1`.
8. The credential scan over `origin/main...HEAD` is clean; no `.env` is tracked.
9. `origin/milestone/v1.5-mutations` == local `HEAD`; the push used no force flag.
10. Exactly two commits were produced: the 4-file `chore(market-data-client): bump to v0.4.0 (…)` and the 2-file `docs(28): …`.
</verification>

<success_criteria>
- Every edit in this plan is reversible with a commit; nothing outward-facing and irreversible has happened.
- `market-data-client` is release-ready at 0.4.0 with a changelog entry that makes the `CalendarDay` field replacement discoverable rather than silent (D-03).
- `.planning/` no longer claims this phase publishes the already-published v0.3.0, and the D-16 follow-up is archived rather than silently rolling forward.
- The release branch exists on `origin` at local `HEAD`, unblocking `gh pr create` in plan 28-02 (C-1).
- The residual D-03 risk (D-13's `CalendarDay` non-breaking claim was never independently audited — `27-VERIFICATION.md` re-verified only D-22) is accepted with the changelog callout as the locked mitigation; record it in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/28-release-prep-publish-v0-3-0/28-01-SUMMARY.md` when done.

Record in the SUMMARY: the two commit SHAs, the measured `uv.lock` churn, the pytest count, the
seven local-mirror results, the credential-scan result, the computed ahead/behind counts at push
time, and an explicit note accepting the residual D-03 risk (D-13's `CalendarDay` non-breaking
claim was never independently audited; the changelog callout is the locked mitigation).
</output>
