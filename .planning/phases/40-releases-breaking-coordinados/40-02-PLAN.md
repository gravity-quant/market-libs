---
phase: 40-releases-breaking-coordinados
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified: []
autonomous: false
requirements: [PUB-NOBJ-01]
user_setup:
  - service: github
    why: "Opening a new PR against the public default branch and merging it are outward-facing GitHub operations; the merge is irreversible in practice"
    dashboard_config:
      - task: "Ensure the `gh` CLI is authenticated (asserted in Task 1 via `gh auth status`; never echo the token)"
        location: "local shell — `gh auth status`"
      - task: "Be available to answer the FIRST blocking go/no-go checkpoint before the merge (D-07a)"
        location: "the executing terminal session"

must_haves:
  truths:
    - "A NEW PR is created with `gh pr create --base main --head milestone/v1.7-nobj-null-objects` — no v1.7 PR existed to update, so unlike Phase 34 this is a create, not an edit — per D-05"
    - "The PR diff still carries the `.planning/` artifacts; they were not filtered out"
    - "The PR reports exactly 15 checks and all 15 have status `pass`, of which exactly 2 are `Tests · market-data-client · py3.1x`, exactly 2 are `Tests · iol-client · py3.1x` and exactly 2 are `Tests · matriz-client · py3.1x` — asserted by COUNT, never by absence of the word fail — per D-06"
    - "The PR is merged into `main` only after the operator has explicitly replied approved at a blocking checkpoint that ran before any merge command — per D-07(a)"
    - "The pre-merge briefing re-presents the D-02 and D-12 answers from plan 40-01 as RESOLVED read-back facts, not as open questions — per CONTEXT § Specifics and RESEARCH OQ-1"
    - "`origin/main` advances to a real merge commit with TWO parents, produced by `gh pr merge <n> --merge` — never squash, never rebase — per D-08"
    - "The merged tree reads the target version for every bumped package under `release.yml`'s own awk expression, so the version-match gate will pass for every tag — per D-09"
    - "No tag is created or pushed in this plan; the tag push is gated by a SECOND independent checkpoint in plan 40-03 — per D-07"
  prohibitions:
    - "Never rewrite history reachable from this branch or from `main`: `--squash` and `--rebase` are forbidden merge strategies, and no force flag is permitted on any push. Dozens of SUMMARY files across Phases 35-39 cite these SHAs by value, and `--rebase` would rewrite history already reachable from published tags."
    - "Never report a gate as green from anything except an exact positive count. `pending`, `skipping`, `cancelled` and a zero-check run all read as green under an absence-of-failure check, and `cancel-in-progress: true` (ci.yml:20) makes `cancelled` genuinely reachable."
    - "Neither of this phase's two blocking human checkpoints on irreversible operations may be satisfied by the agent itself, by `auto_advance`, by yolo mode, or by inferring approval from silence or from an ambiguous reply. Only a literal operator response counts, and it is recorded verbatim."
    - "Never re-open the D-02 or D-12 scope questions at this gate. They were answered in plan 40-01 before the bump; re-asking them here would force a second `uv lock` and contradict D-10 (RESEARCH P1)."
    - "Never patch `.github/workflows/ci.yml` or `release.yml` to make a check pass. Surface the failing job and step."
    - "Never push a commit to the branch while checks are in flight — `cancel-in-progress: true` cancels the run and `cancelled` is not `fail`."
  artifacts:
    - path: "gh-pr:<n>"
      provides: "the single release PR for v1.7, newly created against main"
      contains: "release:"
    - path: "git-ref:origin/main"
      provides: "the two-parent merge commit that becomes the shared tag anchor for EVERY tag in plan 40-03"
      contains: "Merge pull request"
  key_links:
    - from: "gh-pr:<n>"
      to: ".github/workflows/ci.yml"
      via: "the PR triggers ci.yml; the `test` job fans out over 6 packages (ci.yml:113-119) × 2 Python versions plus 3 non-matrix jobs, producing exactly 15 checks"
      pattern: "Tests · matriz-client · py3.1[23]"
    - from: "git-ref:origin/main"
      to: "git-tag:matriz-client-v0.3.0"
      via: "plan 40-03 creates every annotated tag ON this merge commit so release.yml's version-match gate runs against the merged tree"
      pattern: "matriz-client-v0.3.0"
    - from: "git-ref:origin/main"
      to: "git-tag:market-data-client-v0.6.0"
      via: "same merge commit, second tag; N tags on one commit, one approval"
      pattern: "market-data-client-v0.6.0"
    - from: "git-ref:origin/main"
      to: "git-tag:iol-client-v0.4.0"
      via: "same merge commit, third tag"
      pattern: "iol-client-v0.4.0"
---

<objective>
Open the v1.7 release PR from the branch plan 40-01 created, prove the CI gate is genuinely green by
counting passing checks, and — only after an explicit human go/no-go — merge it into `main` with a
real merge commit.

Purpose: `main` has NO branch protection in this repository (`gh api …/branches/main/protection`
returns 404) and all three merge methods are enabled. GitHub will merge this PR with red, pending or
cancelled checks and will not stop anything. The count-based assertion plus the D-07(a) checkpoint
are therefore the only enforcement standing between an unverified build and a public default branch.

Output: a merge commit on `origin/main` with two parents, carrying every version bump, ready to
anchor EVERY tag in plan 40-03.

This plan is `autonomous: false`. The merge is irreversible in practice and is gated by a blocking
`checkpoint:human-verify`. It requires a clean working tree and an authenticated `gh`. The tag
pushes and the public Releases are NOT part of this plan — they are gated behind a SECOND checkpoint
in plan 40-03. D-07 requires two separate gates on irreversible operations; do not collapse them and
do not add a third here.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md
@.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md
@.planning/phases/40-releases-breaking-coordinados/40-PATTERNS.md
@.planning/phases/40-releases-breaking-coordinados/40-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This phase creates **no new public symbols, classes or functions** — the surface it publishes was
built and live-verified by Phases 35-39. Stated explicitly rather than omitted. This plan produces
the middle group.

| Artifact | Pseudo-path | Produced by | Verified by |
|----------|-------------|-------------|-------------|
| Recorded D-02 + D-12 dispositions | `40-01-SUMMARY.md` § scope-gate dispositions | 40-01 Task 1 | verbatim operator reply + timestamp |
| Changelog entries + version sites for every bumped package | `packages/*/README.md`, `pyproject.toml`, `__init__.py` | 40-01 Tasks 2-3 | exact-line heading greps + `awk` / `grep -qx` |
| Single-refresh `uv.lock` | `uv.lock` | 40-01 Task 4 | one lock-touching commit, `N N` churn, `uv lock --check` |
| Pushed release branch | `git-ref:origin/milestone/v1.7-nobj-null-objects` | 40-01 Task 4 | `git rev-parse HEAD` == `git rev-parse origin/…` |
| **New release PR against `main`** | **`gh-pr:<n>`** | **40-02 Task 1** | `gh pr view <n> --json number,state,baseRefName,headRefName` |
| **Two-parent merge commit on `main`** | **`git-ref:origin/main`** | **40-02 Task 3** | `git rev-list --parents -n1 origin/main` → 3 fields |
| N annotated release tags on the merge SHA | `git-tag:<pkg>-client-v<X.Y.Z>` | 40-03 Task 2 | `git cat-file -t <tag>` == `tag`; `git rev-list -n1 <tag>` == merge SHA |
| N public GitHub Releases with wheel + sdist | `gh-release:<pkg>-client-v<X.Y.Z>` | 40-03 Task 2 | `gh release view --json assets` exact filenames |
| Post-publish install proof from the public wheels | throwaway venv (no repo path) | 40-03 Task 3 | deep-chain assertions against the installed distributions |
</artifacts_this_phase_produces>

<reversibility_gates>
| Task | Operation | Reversibility | Gate |
|---|---|---|---|
| Task 1 | `gh pr create` (title + body) | fully reversible — a PR can be edited or closed at any time before merge | none needed |
| Task 1 | `gh pr checks <n> --watch` | read-only | none needed |
| Task 2 | operator go/no-go | n/a — this IS the gate | **blocking human checkpoint, D-07(a)** |
| Task 3 | `gh pr merge <n> --merge` | **irreversible in practice** — publishes the whole Phase 35-40 diff to the public default branch; a revert is a forward commit, not an unpublish | gated by Task 2 |

This plan carries exactly ONE of the phase's two blocking gates on irreversible operations. Plan
40-03 carries the other. Adding a gate here, or collapsing this one into 40-03's, violates D-07. The
`checkpoint:decision` scope gate in 40-01 Task 1 is not one of the two — it merged nothing and
published nothing (CONTEXT D-07 states this explicitly).
</reversibility_gates>

<scope_decisions>
- **The PR is CREATED, never an edit of an existing one (D-05).** This is the structural delta from
  Phase 34, whose 34-02 Task 1 ran `gh pr edit 12`. Measured at plan time: `gh pr list` returns
  nothing, remote heads are only `main`, `milestone/v1.5-mutations` and `release/v0.2.0`, and no
  v1.7 branch or PR exists. Run `gh pr create`; do not look for a PR to reuse.
- **`.planning/` stays in the diff.** Do not run `/gsd-pr-branch` and do not filter the planning
  artifacts out — they have shipped with every prior release of this repository.
- **No rebase and no merge of `origin/main` into the branch.** The branch already contains
  `origin/main`'s history; any divergence is cosmetic and a rebase would rewrite published history.
- **`.github/workflows/` is read-only.** If a check fails, surface the failing job and step; never
  patch `ci.yml` or `release.yml` to make a gate pass. Phase 34 hit a red `Type check (mypy)` and
  fixed it by narrowing in the test, never by touching the workflow.
- **The check total is 15 regardless of how many packages bumped.** `matrix.package`
  (`ci.yml:113-119`) lists all six packages unconditionally, so 6 × 2 = 12 test checks plus the three
  non-matrix jobs (`Lint y formato (ruff)`, `pre-commit hooks`, `Type check (mypy)`) is invariant.
  Only the per-package row counts change with the bump set — and even those are 2 per package for all
  six packages, bumped or not; what the per-package assertions prove is that each bumped package's
  matrix cells actually ran and passed.
- **No tag in this plan.** Every tag belongs to 40-03, behind the second gate.
- **TDD framing does not apply.** `workflow.tdd_mode` is enabled project-wide; this plan performs
  only git/gh operations and produces no code, no API contract and no data transformation. There is
  nothing whose input/output could be pinned by a failing test first. Every task is `type: auto` with
  shell and gh-state assertions in `<verify><automated>`.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Create the v1.7 release PR and assert 15/15 checks pass BY COUNT, with 2 rows per bumped package (D-05, D-06)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/40-releases-breaking-coordinados/40-01-SUMMARY.md` — the recorded scope-gate
      dispositions (which decide the final package set), every commit SHA, the exact version
      transitions, `PHASE_BASE`, the measured `uv.lock` churn and the confirmation that
      `origin/milestone/v1.7-nobj-null-objects` equals local HEAD.
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` § Decisions D-05, D-06, D-07,
      D-08 and § Claude's Discretion (PR title and body wording).
    - `.github/workflows/ci.yml` — READ-ONLY. The trigger block and `paths-ignore` (`:6`, `:11`),
      `concurrency: cancel-in-progress: true` (`:20`), the three non-matrix job names
      (`Lint y formato (ruff)` `:24`, `pre-commit hooks` `:69`, `Type check (mypy)` `:87`), the test
      job name template (`:108`), `matrix.package` (`:113-119`) and `matrix.python-version` (`:120`).
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-02-PLAN.md` lines 153-241 (Task 1)
      — the three-assertion precondition block, the count-based CI gate verbatim, and the rationale
      for why an absence-of-failure check is inadequate. The Phase-40 delta: `gh pr edit 12` becomes
      `gh pr create`, and a third (or fourth) per-package row count is added.
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § Architecture Patterns →
      Pattern 1 (assert CI green by explicit count) and § Common Pitfalls P5.
  </read_first>
  <action>
    (a) Preconditions, all four must hold before anything else: `gh auth status` exits 0 (never echo
    the token — the command already redacts it); `git status --porcelain` is empty;
    `git rev-parse --abbrev-ref HEAD` is `milestone/v1.7-nobj-null-objects`; and `git rev-parse HEAD`
    equals `git rev-parse origin/milestone/v1.7-nobj-null-objects`. That last assertion is what plan
    40-01 Task 4's push makes true. If it is false, STOP: a PR whose head does not contain the
    release describes nothing. Recompute rather than trusting any literal.

    (b) Create a NEW PR with `gh pr create --base main --head milestone/v1.7-nobj-null-objects`. Do
    NOT look for an existing PR to edit — no v1.7 PR exists (D-05, verified: `gh pr list` empty, no
    v1.7 remote head). This is the structural delta from Phase 34, whose 34-02 Task 1 ran
    `gh pr edit 12`. Capture the returned PR number into a variable and use it everywhere below; do
    not hard-code a number from any document.

    Title, following the `release: <scope>` convention established by PRs #8-#14 and extended to the
    final package set — spell out every bumped package and version, e.g. for the three-package case:
    `release: market-data-client v0.6.0 + iol-client v0.4.0 + matriz-client v0.3.0 (Null Objects tipados, fases 35-40)`
    If the operator folded higyrus in at the 40-01 scope gate, add `+ higyrus-client v0.3.0`.

    Body wording is Claude's discretion; supply it with `--body-file` rather than an inline argument
    so multi-line Markdown survives shell quoting. It must cover, concretely:
      - every bumped package with its exact version transition;
      - that all of them are source-breaking, and that each carries a README changelog callout with a
        migration table — market-data's `### v0.6.0` (`market_data` → the typed `MarketDataEntries`
        Null Object), iol's `### v0.4.0` (`Cotizacion.puntas` / `Titulo.puntas` lose `| None`),
        matriz's `### v0.3.0` (four field retypes plus the envelope-unwrap behavior fix), and
        higyrus's `### v0.3.0` if it was folded in;
      - the phase span actually contained in the head: 35 through 40;
      - the two scope dispositions recorded at the 40-01 gate (D-02 and D-12) with their option ids,
        so a reviewer sees them decided rather than pending;
      - that `.planning/` artifacts are intentionally included;
      - that `ambito-financiero-client` and `wallets-client` are NOT bumped and NOT published —
        their surfaces moved additively only, which is measured and classified, not unchanged.
    The body must contain no credential and no secret value. The GitHub Releases themselves use
    `--generate-notes`, so the body does not need to be release-notes-shaped.

    (c) Wait for the checks to settle with `gh pr checks <n> --watch`, then take a final snapshot
    with `gh pr checks <n>`.

    (d) Assert the gate BY COUNT, never by the absence of the word "fail". Require all of: the total
    row count is exactly 15; the number of rows whose status field is `pass` is exactly 15; and
    exactly 2 rows match `Tests · <pkg> · py3.1[23]` for EACH bumped package —
    `market-data-client`, `iol-client`, `matriz-client`, plus `higyrus-client` if the scope gate
    folded it in.

    Statuses other than `pass` include `fail`, `pending`, `skipping` and `cancelled` — an
    absence-of-failure check reads all of those as green, and `cancelled` is genuinely reachable
    because `ci.yml:20` sets `concurrency: cancel-in-progress: true`. A Markdown-only diff would
    trigger ZERO checks (`paths-ignore: ["**.md", ".gitignore"]`), and "no checks" is not green —
    which is exactly why the total must equal 15 rather than merely being non-zero. For this
    particular PR the diff carries `uv.lock`, several `pyproject.toml` files and 186 commits of
    `.py`, so a zero-check run is not a live risk — assert `TOTAL == 15` anyway.

    The count is 15 whether three or four packages bumped: `matrix.package` (`ci.yml:113-119`) lists
    all six packages unconditionally, so 6 × 2 = 12 test checks plus three non-matrix jobs is
    invariant to how many packages a PR touches.

    (e) Do NOT push any further commit to the branch while checks are running — it cancels the
    in-flight run (RESEARCH P5). If a fix is genuinely required, land it, then re-run
    `gh pr checks <n> --watch` to completion and re-assert the full count from scratch. Never patch
    `ci.yml` to make a check pass.

    (f) Do NOT merge in this task. Do NOT create or push any tag in this task or anywhere in this
    plan. The merge is gated by Task 2; every tag is gated by a separate checkpoint in plan 40-03.
  </action>
  <verify>
    <automated>gh auth status >/dev/null 2>&1 && test -z "$(git status --porcelain)" && test "$(git rev-parse --abbrev-ref HEAD)" = "milestone/v1.7-nobj-null-objects" && git fetch origin milestone/v1.7-nobj-null-objects --quiet && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/milestone/v1.7-nobj-null-objects)" && PR=$(gh pr list --state open --base main --head milestone/v1.7-nobj-null-objects --json number --jq '.[0].number') && test -n "$PR" && test "$(gh pr list --state open --base main --json number --jq 'length')" = "1" && test "$(gh pr view "$PR" --json state --jq .state)" = "OPEN" && test "$(gh pr view "$PR" --json baseRefName --jq .baseRefName)" = "main" && test "$(gh pr view "$PR" --json headRefName --jq .headRefName)" = "milestone/v1.7-nobj-null-objects" && gh pr view "$PR" --json title --jq .title | grep -q '^release: ' && gh pr view "$PR" --json files --jq '.files[].path' | grep -q '^\.planning/' && TOTAL=$(gh pr checks "$PR" | wc -l | tr -d ' ') && PASSED=$(gh pr checks "$PR" | awk -F'\t' '$2=="pass"' | wc -l | tr -d ' ') && test "$TOTAL" = "15" && test "$PASSED" = "15" && for P in market-data-client iol-client matriz-client; do C=$(gh pr checks "$PR" | grep -c "Tests · $P · py3\.1[23]"); test "$C" = "2" || { echo "MATRIX ROWS MISSING FOR $P: $C"; exit 1; }; done && HV=$(awk -F'"' '/^version[[:space:]]*=/{print $2; exit}' packages/higyrus-client/pyproject.toml) && { test "$HV" = "0.2.0" || test "$(gh pr checks "$PR" | grep -c 'Tests · higyrus-client · py3\.1[23]')" = "2"; } && test -z "$(git tag -l 'matriz-client-v0.3.0')" && test -z "$(git tag -l 'iol-client-v0.4.0')" && test -z "$(git tag -l 'market-data-client-v0.6.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --state open --base main --json number --jq 'length'` outputs `1`, and that PR's head is `milestone/v1.7-nobj-null-objects` — a NEW PR was created with `gh pr create`; no `gh pr edit` on a pre-existing PR was attempted (D-05)
    - `gh pr view <n> --json title --jq .title` starts with `release: ` and names every bumped package with its exact version transition
    - `gh pr view <n> --json state --jq .state` is `OPEN`, `baseRefName` is `main`, `headRefName` is `milestone/v1.7-nobj-null-objects`
    - The PR body names every version transition, states that all bumped packages are source-breaking, names the phase span 35-40, records both scope-gate option ids from 40-01, and states that `ambito-financiero-client` and `wallets-client` are classified additive and not published
    - `gh pr view <n> --json files --jq '.files[].path'` includes at least one path under `.planning/` — the planning artifacts were not filtered out
    - `gh pr checks <n> | wc -l` outputs exactly `15`
    - `gh pr checks <n> | awk -F'\t' '$2=="pass"' | wc -l` outputs exactly `15` — no row is `fail`, `pending`, `skipping` or `cancelled`. The gate was asserted by positive count, never by grepping for the absence of `fail`
    - `gh pr checks <n> | grep -c 'Tests · <pkg> · py3\.1[23]'` outputs exactly `2` for each of `market-data-client`, `iol-client` and `matriz-client`, and also for `higyrus-client` if the 40-01 scope gate folded it in
    - `git rev-parse HEAD` still equals `git rev-parse origin/milestone/v1.7-nobj-null-objects` — no commit was pushed mid-run, so no in-flight check was cancelled
    - No tag exists for any bumped package
    - No `gh pr merge`, no `git tag` and no `git push` command was executed in this task, and no file under `.github/workflows/` was modified
  </acceptance_criteria>
  <done>A new PR exists against `main` from `milestone/v1.7-nobj-null-objects` with a `release: …` title naming every bumped package, it carries the `.planning/` artifacts, and `gh pr checks` reports 15 rows of which 15 are `pass`, including exactly 2 matrix rows for each bumped package.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FIRST blocking go/no-go gate — before the IRREVERSIBLE merge to the public default branch (D-07a)</name>
  <files>none — this task modifies no file</files>
  <read_first>
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` D-07 — exactly two blocking
      checkpoints on irreversible operations, both explicit and independent, never collapsed, never
      auto-approved despite `auto_advance: true` + `mode: yolo`. This is gate (a). Also read the
      correction stating that D-02 and D-12 do NOT live at this gate — they were resolved at the
      hoisted scope gate in 40-01 Task 1 and are re-presented here as read-back facts only.
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` D-08 — merge commit real,
      never squash, never rebase, and why.
    - `.planning/phases/40-releases-breaking-coordinados/40-01-SUMMARY.md` § `Scope-gate dispositions
      (D-02, D-12)` — the operator's own recorded answers, quoted back verbatim in the briefing.
    - The 40-02 Task 1 output already in context: the PR number and URL and the counted check totals.
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-02-PLAN.md` lines 243-320 (Task 2)
      — the literal element shape of this checkpoint in the direct precedent, including the
      five-item audit trail in `<acceptance_criteria>` and the "never auto-approvable" wording.
  </read_first>
  <acceptance_criteria>
    - The operator was shown: the PR URL and number, the literal `gh pr checks <n>` output with the counted totals (15 rows / 15 `pass` / 2 rows per bumped package), the diff stat, every exact version transition, and an explicit statement that `main` has no branch protection so this approval is the only gate
    - The operator was shown, as RESOLVED read-back facts and not as open questions, the two scope dispositions recorded at the 40-01 gate: the D-02 higyrus answer and the D-12 market_id/active answer, each with its option id and any named destination phase
    - No `gh pr merge`, no `git tag` and no `git push` command was executed before the operator's reply
    - The operator's reply is recorded verbatim in the SUMMARY together with its timestamp
    - The approval came from a literal operator response — not from `auto_advance`, not from yolo mode, not inferred from silence, and not self-issued by the agent
    - This approval is recorded separately from the tag-push approval in plan 40-03 — the two gates were not collapsed into one
    - Execution continues to Task 3 ONLY if the reply is an explicit "approved"; any other reply (including silence, ambiguity, or "abort") halts the plan with the PR still open and `origin/main` unchanged
  </acceptance_criteria>
  <action>PAUSE. This is the first of exactly two blocking human checkpoints on irreversible operations for this phase (`autonomous: false`). Do NOT merge the PR, do NOT create any tag, and do NOT push anything until the operator explicitly replies "approved". This gate is never auto-approvable: `auto_advance: true` and `mode: yolo` are both active in `.planning/config.json` and neither satisfies it, silence does not satisfy it, an ambiguous reply does not satisfy it, and the agent may not self-issue it. If the operator replies "abort", stop cleanly with no irreversible action taken — the PR stays open and everything so far is revertible with a commit. Present: the PR URL and number, the literal `gh pr checks <n>` output with the counted totals, the diff stat, every exact version transition being published, the two already-resolved scope dispositions from the 40-01 gate quoted back verbatim as read-back facts, and an explicit statement that `main` has NO branch protection so this approval is the only gate. Do NOT re-open D-02 or D-12 here — answering them at this point would force a second `uv lock` and contradict D-10.</action>
  <what-built>
    A NEW PR (`milestone/v1.7-nobj-null-objects` → `main`) has been created — no v1.7 PR existed
    before, so unlike the v1.6 release this is a create, not an edit. All 15 CI checks report `pass`,
    counted explicitly: 15 total rows, 15 with status `pass`, and 2 matrix rows for each bumped
    package. The working tree is clean and `gh` is authenticated.

    What this release publishes — every one of them source-breaking, every one with a README
    changelog callout FIRST and a consumer-followable migration table:
      - `market-data-client` 0.5.0 → **0.6.0** — `MarketDataSnapshot.market_data` went from
        `dict[str, Any] | None` to the typed Null Object `MarketDataEntries`, so
        `snapshot.market_data["LA"]["price"]` becomes `snapshot.market_data.last.price` and
        `if snapshot.market_data is None:` becomes `if not snapshot.market_data:`.
        `MarketDataSnapshot.entries` and `LatestRequest.entries` lost their `| None`.
      - `iol-client` 0.3.0 → **0.4.0** — `Cotizacion.puntas` (`list[Punta] | None` → `list[Punta]`)
        and `Titulo.puntas` (`Punta | None` → `Punta`) lost their `| None`. The `or []` fallback is
        no longer needed, and `is None` branches on `Titulo.puntas` go silently dead — the changelog
        flags that asymmetry explicitly because the typechecker cannot catch it.
      - `matriz-client` 0.2.0 → **0.3.0** — four model field retypes (`AccountReport.portfolio`,
        `InstrumentDetail.tickPriceRanges`, `DetailedPosition.report`,
        `AccountReport.detailedAccountReports`) PLUS an envelope-unwrap behavior fix:
        `get_detailed_positions` and `get_account_report` previously decoded from the wrong nesting
        level and returned all-default models. Consumers now see real data — or a `PrimaryAPIError`
        where they previously saw an empty model. This package's changelog section did not exist
        before this phase and was authored from scratch.
      - `higyrus-client` 0.2.0 → **0.3.0** — ONLY if the operator folded it in at the 40-01 scope
        gate. Quote the recorded disposition here; do not restate it as still open.

    Scope dispositions already decided by the operator at the 40-01 gate, quoted back verbatim so
    they are visible at this gate without being re-asked (CONTEXT § Specifics):
      - **D-02 (higyrus fold-in):** <recorded option id + verbatim reply + timestamp + any named
        destination phase>
      - **D-12 (`market_id` / `active` widening):** <recorded option id + verbatim reply + timestamp
        + any named destination phase>
    These are read-back facts. Re-opening either here would require a second `uv lock` run, which
    D-10 forbids, and would redden currently-green assertions with no clean way back — which is
    exactly why the gate was hoisted to the top of 40-01 (RESEARCH P1 / OQ-1).

    `ambito-financiero-client` and `wallets-client` are NOT bumped and NOT published. Their exported
    surfaces did move since their published tags — additively only (a keyword-only `strict_decode`
    parameter, a new exception class in ambito's case, new empty `models.py`/`types.py` in wallets').
    That is measured and classified, not unchanged; a non-empty surface diff for either is expected.

    The next task performs an operation that is irreversible in practice: merging this PR into
    `main`, the default branch of a PUBLIC repository. It will use `gh pr merge <n> --merge` — a real
    merge commit with two parents, never `--squash` and never `--rebase`. Squashing would orphan the
    SHAs that dozens of SUMMARY files across Phases 35-39 cross-reference by value; rebasing would
    rewrite history already reachable from published tags. The repository allows all three merge
    methods, so nothing but this instruction prevents the wrong one.

    CRITICAL CONTEXT: `main` has no branch protection — the GitHub API returns 404 for its protection
    endpoint. GitHub will merge this PR regardless of check status, with no required reviewers. The
    15/15 count above and YOUR approval are the only things preventing an unverified build from
    landing on a public default branch.

    A credential scan over the full `origin/main...HEAD` diff was re-run immediately before the push
    in plan 40-01 Task 4 and came back clean: no JWT-shaped token, no `client_secret` assignment, no
    tracked `.env`.

    The release TAGS and the public GitHub Releases are NOT part of this approval. Every tag is gated
    by a second, separate checkpoint in plan 40-03 (D-07 requires two independent gates, and that
    second gate covers all tags in one approval).
  </what-built>
  <how-to-verify>
    1. Open the PR on GitHub. Confirm the title starts with `release: ` and names every bumped
       package with its version transition, base `main`, head `milestone/v1.7-nobj-null-objects`, and
       that it is the ONLY open PR against `main`.
    2. Confirm the diff scope looks right: the package sources and tests from Phases 35-39, the
       version bumps in every bumped `pyproject.toml` and `__init__.py`, the single `uv.lock`
       refresh, the changelog entries, and the `.planning/` artifacts (intentionally kept).
    3. Confirm each bumped package's `## Changelog` section opens with the NEW version as its first
       `###` entry, above the previously published one, and that the breaking callout is the first
       content of that entry with a migration table under it. `matriz-client`'s section should exist
       for the first time.
    4. Confirm no README still says a future phase will do the bump, and that
       `packages/market-data-client/README.md`'s two install commands now pin `v0.6.0` and the
       `0.6.0` wheel filename — not the superseded ones.
    5. Confirm the `# NO BUMPEAR` comment block is gone from
       `packages/market-data-client/src/market_data_client/__init__.py` — it named this phase as the
       future executor and is false once merged.
    6. Confirm no file under `.github/workflows/` appears in the diff.
    7. Confirm the check table shows 15 rows and all 15 are `pass` — not pending, not cancelled, and
       not an empty table.
    8. Re-read the two scope dispositions quoted above and confirm they match what you decided at the
       40-01 gate. If either is wrong, reply "abort" — do not approve and correct it afterwards.
    9. Confirm you authorize the irreversible merge into public `main` NOW, and that you understand
       the tag push is a separate approval you will be asked for again.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `gh pr merge <n> --merge`, or "abort" (optionally describing blockers) to stop before any irreversible action. Anything other than an explicit "approved" means do not merge.</resume-signal>
  <verify>
    <automated>PR=$(gh pr list --state open --base main --head milestone/v1.7-nobj-null-objects --json number --jq '.[0].number') && test -n "$PR" && test "$(gh pr view "$PR" --json state --jq .state)" = "OPEN" && git fetch origin main --tags --quiet && test "$(git rev-parse origin/main)" = "$(git merge-base origin/main HEAD)" && test -z "$(git tag -l 'matriz-client-v0.3.0')" && test -z "$(git tag -l 'iol-client-v0.4.0')" && test -z "$(git tag -l 'market-data-client-v0.6.0')" && test -z "$(git tag -l 'higyrus-client-v0.3.0')" && test -z "$(git status --porcelain)" && echo "PASS — nothing irreversible happened before the operator reply"</automated>
  </verify>
  <done>The operator was shown the PR, the counted 15/15 gate, every version transition, the two already-resolved scope dispositions quoted verbatim, and the no-branch-protection warning; they replied with an explicit literal "approved" (or "abort"); the reply is recorded verbatim with its timestamp; and at the moment of the reply the PR was still OPEN, `origin/main` was still un-advanced, no tag existed and the tree was clean.</done>
</task>

<task type="auto">
  <name>Task 3: Merge the PR with a real merge commit and verify `origin/main` advanced to a two-parent commit carrying every bump (D-08, D-09)</name>
  <files>(git/gh operations — no working-tree files modified)</files>
  <read_first>
    - `.planning/phases/40-releases-breaking-coordinados/40-CONTEXT.md` D-08 (merge commit real,
      never squash, never rebase) and D-09 (tags go on the merge SHA, which is why the shape of this
      commit matters).
    - `.planning/phases/40-releases-breaking-coordinados/40-RESEARCH.md` § Don't Hand-Roll → "Proving
      the merge was a real merge" (parent count cannot lie) and § Architecture Patterns → the merge
      block of the release flow.
    - `.github/workflows/release.yml` lines 42-51 — READ-ONLY. The awk version-match gate that will
      run against the TAGGED tree in plan 40-03, which is why this task asserts the merged tree
      already carries every version.
    - `.planning/milestones/v1.6-phases/34-releases-por-paquete/34-02-PLAN.md` lines 322-381 (Task 3)
      — the merge command form, the two-parent assertion and the merged-tree version assertions,
      generalized here from two packages to three or four.
    - `.planning/phases/40-releases-breaking-coordinados/40-01-SUMMARY.md` — the final package set and
      exact target versions to assert against the merged tree.
  </read_first>
  <action>
    Execute ONLY after Task 2 returned an explicit "approved" from the operator. If it returned
    anything else — including silence, ambiguity, or an auto-advance signal — stop.

    (a) Merge with `gh pr merge <n> --merge`. The `--merge` flag is mandatory (D-08): `--squash`
    would collapse the branch into a single commit and orphan every SHA that the Phase 35-39
    SUMMARYs cross-reference by value; `--rebase` would rewrite published history reachable from the
    existing `iol-client-v0.3.0`, `market-data-client-v0.5.0` and `matriz-client-v0.2.0` tags. The
    repository allows all three merge methods (`allow_squash_merge: true`, `allow_rebase_merge:
    true`), so nothing but this instruction prevents the wrong one. `delete_branch_on_merge` is
    `false`, so `milestone/v1.7-nobj-null-objects` survives the merge. Do NOT pass a branch-delete
    flag.

    (b) Refresh local refs: `git fetch origin main --tags`. Then capture the merge commit as
    `MERGE_SHA=$(git rev-parse origin/main)` and inspect it with
    `git log -1 --format='%H %p %s' "$MERGE_SHA"` — the `%p` field MUST list two parent SHAs and the
    subject must be a `Merge pull request #<n> …` line. Assert it mechanically:
    `git rev-list --parents -n1 origin/main` must yield exactly three whitespace-separated fields. A
    single-parent commit means the merge was squashed or rebased: STOP and surface it rather than
    proceeding, because plan 40-03 tags exactly this commit and `release.yml` validates each version
    against the tagged tree. Reading the subject line is not sufficient — some UIs render a squash
    with a merge-looking subject; the parent count cannot lie.

    (c) Assert the merged tree actually carries EVERY bump, read with the same
    `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` expression `release.yml:47` uses — never a
    TOML parser, never `importlib.metadata`, never a bespoke regex, because any other reader can
    disagree with the gate that decides whether the release publishes:
    `git show origin/main:packages/market-data-client/pyproject.toml` must report `0.6.0`;
    `git show origin/main:packages/iol-client/pyproject.toml` must report `0.4.0`;
    `git show origin/main:packages/matriz-client/pyproject.toml` must report `0.3.0`; and
    `git show origin/main:packages/higyrus-client/pyproject.toml` must report `0.3.0` if and only if
    the 40-01 scope gate folded it in, otherwise `0.2.0`. If any does not match, the corresponding
    tag would be rejected by the pipeline's version-match gate — STOP.

    Also assert the merged tree still reads `0.2.0` for `ambito-financiero-client` and
    `wallets-client`, so no unchanged package can be accidentally tagged in plan 40-03.

    (d) Do NOT create or push any tag here. Do NOT push to `main` directly; the merge goes through
    the PR so attribution, PR closure and the merge-commit shape all match every prior release.
    Never echo the `gh` token, the SSH key or any credential.

    Record `MERGE_SHA` and both parent SHAs in the SUMMARY — plan 40-03 recomputes `MERGE_SHA` from
    `git rev-parse origin/main` rather than trusting a literal, but the recorded value is the
    cross-check.
  </action>
  <verify>
    <automated>git fetch origin main --tags --quiet && test "$(git rev-list --parents -n1 origin/main | wc -w | tr -d ' ')" -eq 3 && git log -1 --format=%s origin/main | grep -q '^Merge pull request' && MDV=$(git show origin/main:packages/market-data-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$MDV" = "0.6.0" && IV=$(git show origin/main:packages/iol-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$IV" = "0.4.0" && MZV=$(git show origin/main:packages/matriz-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$MZV" = "0.3.0" && AV=$(git show origin/main:packages/ambito-financiero-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$AV" = "0.2.0" && WV=$(git show origin/main:packages/wallets-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && test "$WV" = "0.2.0" && HV=$(git show origin/main:packages/higyrus-client/pyproject.toml | awk -F'"' '/^version[[:space:]]*=/{print $2; exit}') && { test "$HV" = "0.2.0" || test "$HV" = "0.3.0"; } && PR=$(gh pr list --state merged --base main --head milestone/v1.7-nobj-null-objects --json number --jq '.[0].number') && test -n "$PR" && test "$(gh pr view "$PR" --json state --jq .state)" = "MERGED" && git ls-remote --heads origin milestone/v1.7-nobj-null-objects | grep -q . && test -z "$(git tag -l 'matriz-client-v0.3.0')" && test -z "$(git tag -l 'iol-client-v0.4.0')" && test -z "$(git tag -l 'market-data-client-v0.6.0')" && test -z "$(git tag -l 'higyrus-client-v0.3.0')" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git rev-list --parents -n1 origin/main | wc -w` outputs `3` — the commit plus exactly two parents (D-08). This, not the subject line, is the proof the merge was real
    - `git log -1 --format=%s origin/main` begins with `Merge pull request`
    - `git show origin/main:packages/market-data-client/pyproject.toml` parsed with `awk -F'"' '/^version[[:space:]]*=/{print $2; exit}'` outputs `0.6.0`; the same reader outputs `0.4.0` for iol-client and `0.3.0` for matriz-client
    - The same reader outputs `0.2.0` for `ambito-financiero-client` and `wallets-client` on the merged tree — no unchanged package can be tagged in plan 40-03
    - `higyrus-client` on the merged tree reads `0.3.0` if the 40-01 scope gate returned `A-fold-higyrus`, and `0.2.0` otherwise
    - `gh pr view <n> --json state --jq .state` is `MERGED` (not `OPEN`, not `CLOSED`)
    - No tag exists for any bumped package at the end of this plan
    - `git ls-remote --heads origin milestone/v1.7-nobj-null-objects` still returns a ref — the branch survived the merge (`delete_branch_on_merge` is `false`)
    - The command history shows `gh pr merge <n> --merge` and contains no `--squash`, no `--rebase`, no branch-delete flag and no `git push origin main`
    - `MERGE_SHA` and both parent SHAs are recorded in the SUMMARY
    - No credential, token or SSH key was echoed at any point
  </acceptance_criteria>
  <done>The PR is merged; `origin/main` points at a real merge commit with two parents whose tree carries every bumped package's target version under `release.yml`'s own awk expression and leaves the unchanged packages at `0.2.0`; no tag exists yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release branch → public `main` | The merge publishes the whole Phase 35-40 diff to the default branch of a PUBLIC repository. This crossing is irreversible in practice. |
| CI check results → merge decision | `main` has no branch protection, so GitHub applies no gate of its own; the check status only influences the merge if the agent enforces it. |
| agent → irreversible GitHub operation | `gh pr merge` succeeds unconditionally; only the human checkpoint stands between an unverified build and `main`. |
| resolved scope dispositions → the pre-merge briefing | If the briefing misquotes or re-opens the 40-01 answers, the operator approves a package set they did not choose. |
| PR body / agent output → `gh` OAuth token | Ambient credential; any echo lands in transcripts and in a public PR body. |
| merge strategy → cross-referenced SHAs | Phases 35-39 SUMMARYs cite commit SHAs by value; squash or rebase orphans them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-10 | Tampering | merging red, pending, cancelled or zero-check code into an unprotected public `main` | mitigate | Task 1 (d) asserts the gate by COUNT — 15 total rows, 15 with status `pass`, 2 rows per bumped package — instead of an absence-of-failure check, which passes on pending, cancelled and empty output. `cancel-in-progress: true` (`ci.yml:20`) makes `cancelled` genuinely reachable, and `paths-ignore: ["**.md", ".gitignore"]` makes a zero-check run reachable. ASVS L1 V4. |
| T-40-11 | Elevation of Privilege / Repudiation | the irreversible merge itself | mitigate | Task 2 is a blocking `checkpoint:human-verify` in an `autonomous: false` plan; Task 3 executes only on an explicit literal "approved" and is not satisfiable by `auto_advance`, yolo mode, silence or agent self-issue; "abort" stops cleanly with the PR still open. With no branch protection on `main` (API 404) and no required reviewers, this checkpoint is the sole access control on the operation. ASVS L1 V4. |
| T-40-12 | Tampering | merge strategy — squash or rebase would rewrite history | mitigate | Task 3 (a) mandates `gh pr merge <n> --merge` and forbids `--squash` / `--rebase`; the acceptance criteria assert the resulting commit has exactly two parents rather than trusting the subject line, because a squash can render with a merge-looking subject. Preserves every SHA the Phase 35-39 SUMMARYs cite. |
| T-40-13 | Repudiation | the pre-merge briefing silently re-opening or misquoting D-02 / D-12 | mitigate | Task 2's `<action>` forbids re-opening either question and its `<acceptance_criteria>` require both dispositions to be quoted back verbatim from the 40-01 SUMMARY with their option ids. Re-answering here would force a second `uv lock` (contradicting D-10) and redden currently-green assertions. |
| T-40-14 | Tampering | tagging a commit that is not the merge commit, or one whose tree lacks a bump | mitigate | This plan creates no tag at all; Task 3 (c) verifies the merged tree already reads every target version under `release.yml`'s own awk expression so plan 40-03 can tag `origin/main` directly, and also verifies the unchanged packages still read `0.2.0` so neither can be tagged by mistake. A single-parent commit halts the phase. |
| T-40-15 | Information Disclosure | PR body / `gh` token | mitigate | The PR body carries no credential; the GitHub Releases use `--generate-notes`; `gh auth status` is used instead of ever echoing the token. ASVS L1 V2, V14. |
| T-40-16 | Tampering | patching `ci.yml` to make a check pass | mitigate | `.github/workflows/` is read-only for this phase; a failing check is surfaced by job and step, never patched. Phase 34 hit a red mypy check and fixed the test, never the workflow. ASVS L1 V14. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs occur in this plan. RESEARCH § Package Legitimacy Audit records no `[ASSUMED]`, `[SUS]` or `[SLOP]` packages for this phase, so no legitimacy checkpoint is applicable. |
</threat_model>

<verification>
1. Exactly one open PR against `main` before the merge, newly created from `milestone/v1.7-nobj-null-objects` (D-05) — not an edit of a pre-existing PR.
2. The PR title starts with `release: ` and names every bumped package and version; the body names the 35-40 phase span and both recorded scope dispositions.
3. `gh pr checks <n>` reports 15 rows; exactly 15 have status `pass`; exactly 2 rows per bumped package.
4. The PR diff contains `.planning/` paths and no `.github/workflows/` paths.
5. The operator explicitly replied "approved" before any merge command ran, and the reply is recorded verbatim with its timestamp (D-07a).
6. The pre-merge briefing quoted the D-02 and D-12 answers back as resolved facts and did not re-ask them.
7. `git rev-list --parents -n1 origin/main | wc -w` equals 3 (D-08).
8. The merged tree reads every bumped package's target version and leaves `ambito-financiero-client` and `wallets-client` at `0.2.0`, all read with `release.yml`'s own awk expression.
9. No tag for any bumped package exists at the end of this plan.
</verification>

<success_criteria>
- The 15-green gate was enforced by explicit counting, not by pattern-matching for failure, because nothing on GitHub enforces it.
- No irreversible operation ran without an explicit, literal human "approved" (D-07a) — and that approval was not manufactured by auto-advance or yolo mode.
- The operator saw the two scope dispositions they had already made, quoted verbatim, without being asked to re-decide anything.
- `main` carries a real merge commit with two parents whose tree satisfies the version-match gate for EVERY tag, ready to be tagged by plan 40-03.
- The release branch still exists on `origin`, and `main` was never pushed to directly.
</success_criteria>

<output>
Create `.planning/phases/40-releases-breaking-coordinados/40-02-SUMMARY.md` when done.

Record in the SUMMARY: the PR number and URL, the final title, the literal counted check totals
(total / pass / rows per bumped package), the operator's approval text and timestamp for the D-07(a)
gate recorded verbatim together with an explicit statement that it was not auto-issued, the captured
`MERGE_SHA` with its two parent SHAs, every merged-tree version as read by the `release.yml` awk
expression, and the merge method used. Record explicitly that no tag was created in this plan and
that the second gate remains outstanding.
</output>
