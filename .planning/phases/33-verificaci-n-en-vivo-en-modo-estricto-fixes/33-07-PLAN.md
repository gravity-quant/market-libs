---
phase: 33-verificaci-n-en-vivo-en-modo-estricto-fixes
plan: 07
type: execute
wave: 5
depends_on: ["33-05", "33-06"]
files_modified:
  - packages
  - verification/test_cycle_closure_phase33.py
  - .planning/verification/ambito-financiero-client-findings.md
  - .planning/verification/higyrus-client-findings.md
  - .planning/verification/iol-client-findings.md
  - .planning/verification/matriz-client-findings.md
  - .planning/verification/market-data-client-findings.md
  - .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md
autonomous: false
requirements: [LIVE-TYP-01]

must_haves:
  truths:
    - "Every divergence the operator triaged as a real defect is fixed inside its owning package, mirrored across `client.py` and `aio.py` (or the shared `_core.py` both dispatch through), with no cross-package dependency introduced (criterion 2, C-2, C-3)."
    - "Every fix carries a mocked regression test under `packages/<pkg>/tests/` — the only tree CI executes — and the finding's `Regression:` bullet resolves to `packages/<pkg>/tests/<file>.py::<test_name>` (criterion 2, P-8)."
    - "Each regression test exists in a sync form and an async form, and `test_surface_parity.py` stays green for the package, so no fix introduces sync/async drift (criterion 2, C-3)."
    - "`verify_cycle_closure(pkg)` returns `(True, [])` for all five packages AND the count of `CONFIRMED`/`FIXED` findings it actually inspected is strictly greater than the pre-phase baseline by at least the number of Phase 33 fixes — the PASS is non-vacuous (criterion 4, D-11, P-7)."
    - "Every confirmed divergence not fixed in this cycle appears in `33-CENSUS.md`'s re-scope table with a named destination, its package and its field — never silenced, never deferred to an unnamed later (criterion 5)."
  artifacts:
    - path: "verification/test_cycle_closure_phase33.py"
      provides: "per-package green + non-vacuity assertion with a numeric inspected-count lower bound"
      contains: "verify_cycle_closure"
    - path: "packages"
      provides: "in-cycle fixes mirrored sync/async plus one mocked regression test file per fix"
  key_links:
    - from: ".planning/verification/<pkg>-findings.md"
      to: "packages/<pkg>/tests/"
      via: "the Regression: bullet naming packages/<pkg>/tests/<file>.py::<test_name>, which verify_cycle_closure resolves"
      pattern: "Regression:.*packages/"
    - from: "packages/<pkg>/src/<pkg>/client.py"
      to: "packages/<pkg>/src/<pkg>/aio.py"
      via: "every logic fix mirrored on both surfaces, pinned by test_surface_parity.py"
      pattern: "test_surface_parity"
  prohibitions:
    - id: P-01
      category: privacy
      statement: "MUST NOT widen the frozen six-key type-not-value divergence record, and MUST NOT compose any finding field from anything other than those six keys plus the endpoint and surface the harness itself set — a git-committed findings file must never carry a live wire value."
      status: resolved
      verification: judgment
    - id: P-02
      category: transparency
      statement: "MUST NOT report a census number, a phase criterion, or a cycle-closure PASS produced by a signal that inspected nothing — a green arising from a dropped log level, a swallowed handler exception, an unseeded fid allocator, or a zero-applicable closure filter is a false clean."
      status: resolved
      verification: judgment
    - id: P-03
      category: transparency
      statement: "MUST NOT shrink the census to fit the ratified floor — no narrowing the walker, no dropping a divergence kind, and no deferring a confirmed finding to an unnamed later; every deferred finding carries a named destination with its package and field recorded."
      status: resolved
      verification: judgment
    - id: P-04
      category: transparency
      statement: "MUST NOT widen, close, or enforce matriz's four pre-existing RESPONSE Literal aliases, and MUST NOT promote iol's mercado/plazo to a Literal on RESPONSE-side evidence."
      status: resolved
      verification: judgment
    - id: P-05
      category: safety
      statement: "MUST NOT let a fix or its regression test fire a destructive or order-entry operation against a live vendor — every regression test is mocked with pytest-httpx and reaches no network."
      status: resolved
      verification: judgment
  flagged_assumptions:
    - "The deterministic edge-probe returned LIVE-TYP-01 as `unclassified` / `unresolved`; its acceptance surface is compound (five ROADMAP criteria), not a single classifiable edge. Carried as an explicit flagged assumption — the five ROADMAP Phase 33 success criteria are the real acceptance surface. Full statement in 33-01-PLAN.md."
    - "The size of the confirmed-defect set is unknowable before 33-05's live run completes — it is a runtime fact, not a planning fact. This plan therefore names its fix budget by CONSEQUENCE ORDER (S-3 first) rather than by count, and makes amending 33-CENSUS.md's re-scope table with named destinations a hard acceptance criterion for anything not fixed. Nothing may be dropped silently to fit the budget."
---

<objective>
Close criteria 2, 4 and 5: triage the divergences the live run surfaced, fix the confirmed defects
in-cycle inside their owning packages with sync/async parity and one mocked regression test each, and
prove `verify_cycle_closure` PASSes for all five packages **non-vacuously**.

Purpose: findings written by the handler land `OPEN`, and `verify_cycle_closure` filters to
`CONFIRMED`/`FIXED` only — so it currently returns `(True, [])` for all five packages while inspecting
zero findings for ámbito and higyrus. A criterion-4 PASS reported from that state is the false clean
this milestone exists to remove (D-11, P-7). Criterion 4 only becomes meaningful after triage promotes
real Phase 33 divergences and links each to a regression test that CI actually runs.

Output: promoted findings with resolvable regression links, package fixes mirrored across both
surfaces, one mocked regression test per fix under `packages/<pkg>/tests/`, and a non-vacuity gate
with numeric per-package lower bounds.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CONTEXT.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-RESEARCH.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-PATTERNS.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-LITERALS.md
@.planning/phases/29-decoder-observable/29-SIZING.md
@verification/cycle_report.py
@verification/findings.py
</context>

## Artifacts this phase produces

Created or modified by **this plan**: `verification/test_cycle_closure_phase33.py`; one
`packages/<pkg>/tests/test_<fix>.py` per confirmed fix; the fixed source files under
`packages/<pkg>/src/<pkg>/` (`client.py` + `aio.py`, or `_core.py` / `models.py` where both surfaces
dispatch through a shared module); the five `.planning/verification/<pkg>-findings.md` files (status
promotions and `Regression:` bullets); and an amendment to `33-CENSUS.md`'s `## Re-scope` table.

Everything else in the phase's artifact inventory is listed in `33-01-PLAN.md` under
"Artifacts this phase produces"; do not duplicate those symbols here.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the disposition for fixes that change a published model's public shape</name>
  <reversibility rating="one-way">Changing a declared field type or a model's shape in a package already published to a GitHub Release breaks the source contract every pinned consumer compiles against; undoing it after Phase 34 tags the release requires a second breaking release, not a revert.</reversibility>
  <decision>
    Some of the confirmed divergences cannot be fixed without changing a **published** model's public
    shape. The two named candidates from `29-SIZING.md`, both expected to be confirmed by 33-05:

    - **S-3 (matriz, highest consequence):** `Instrument.instrumentId: InstrumentId` is non-`Optional`,
      but `get_instruments_by_cfi` and `get_instruments_by_segment` return `marketId` / `symbol`
      flattened with no nested `instrumentId`. Today every row collapses to `InstrumentId.empty()` and
      the two fields carrying the real identity are reported as `extra` and discarded, so every
      consumer of `inst.instrumentId.symbol` reads empty on every row, silently.
    - **S-5 (matriz):** `MarketDataSnapshot.LA` / `.SE` / `.OI` / `.CL` are declared non-`Optional`
      nested models and arrive `null`. Whether that is the legitimate market-closed shape or a
      modelling error is decided by 33-05's trading-session result.

    Which disposition applies to each such fix?
  </decision>
  <context>
    `matriz-client` is published at v0.1.1 and `iol-client` at v0.2.0; Phase 34 is the per-package
    release phase and DT-08 already records iol 0.2.0 -> **0.3.0** as source-breaking with a callout.
    Adding a breaking model change here therefore has a real, already-established downstream cost: it
    changes which packages Phase 34 must bump and how. `29-SIZING.md` routes S-3 to "Phase 33 — confirm
    on the live wire, then Phase 30/31 model work", i.e. it does not itself assert that the model
    change lands in Phase 33. Read 33-05's confirmations for each candidate before choosing; the
    options may resolve differently per finding.
  </context>
  <options>
    <option id="fix-shape-now">
      <name>Fix the model shape in this phase and let Phase 34 carry the semver consequence</name>
      <pros>The defect is actually gone in the shipped wheel; the fix and the finding that motivated it live in the same cycle, which is this project's stated convention. Consumers stop silently reading empty identities.</pros>
      <cons>Adds at least one more source-breaking package to Phase 34's bump set, each needing a README changelog callout. Widens Phase 34's scope after its ROADMAP entry was written.</cons>
    </option>
    <option id="fix-parser-keep-shape">
      <name>Fix only the parser / decode side, keeping the declared model shape</name>
      <pros>No published contract changes, so Phase 34's bump set is unaffected — a patch or minor bump suffices. For S-3 this is genuinely available: the flattened `marketId` / `symbol` can be lifted into the existing nested `InstrumentId` at parse time, so `inst.instrumentId.symbol` starts returning the real value under the current declared type.</pros>
      <cons>Does not help where the wire genuinely sends `null` for a non-`Optional` field (S-5) — there the only honest fix is `Optional`, and a parser-side substitution would be re-introducing exactly the silent typed-zero this milestone removes.</cons>
    </option>
    <option id="defer-shape-to-named-phase">
      <name>Confirm and document now, route the shape change to a named destination phase</name>
      <pros>Keeps Phase 33's fix budget on defects that are fixable without a contract break, and gives the contract break its own cycle with its own release planning. Consistent with `29-SIZING.md`'s own routing for S-3.</pros>
      <cons>The defect ships one more release cycle. Requires a real named destination added to ROADMAP in this commit — "later" is not available — and the finding stays `CONFIRMED` rather than `FIXED`, which still counts toward criterion 4's non-vacuity.</cons>
    </option>
  </options>
  <resume-signal>Reply with a per-finding disposition, e.g. `S-3: fix-parser-keep-shape, S-5: defer-shape-to-named-phase`, covering every confirmed divergence 33-05 flagged as requiring a published-shape change. Task 2's fix list and Task 3's inspected-count lower bounds both read from this.</resume-signal>
  <files>.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-07-SUMMARY.md</files>
  <read_first>
    - `33-05-SUMMARY.md` and `33-CENSUS.md` `## Structural findings` — which of S-1..S-5 the live run CONFIRMED, and which confirmed defects require a published-shape change
    - `packages/matriz-client/src/matriz_client/models.py` — `Instrument.instrumentId` (`:264-268`) and the `MarketDataSnapshot` entry fields, the two shape candidates
    - `.planning/ROADMAP.md` § Phase 34 — the per-package release phase whose bump set this decision changes, and DT-08's already-recorded iol 0.2.0 -> 0.3.0 source-breaking callout
    - `.planning/phases/29-decoder-observable/29-SIZING.md` S-3 and S-5, including their `Route to:` lines
  </read_first>
  <action>
    Present the decision above to the operator, enumerating the actual confirmed shape-change
    candidates from `33-CENSUS.md` rather than only the two predicted ones, and wait for a per-finding
    verbatim selection. Do not pick a default and proceed: each of these changes the source contract of
    an already-published wheel, and undoing one after Phase 34 tags a release requires a second breaking
    release rather than a revert. Once the selection arrives, record it verbatim in `33-07-SUMMARY.md`
    under a `## Shape-change dispositions (locked)` heading, one line per finding with its option id and
    — for any `defer-shape-to-named-phase` selection — the named destination that Task 2 must add to
    `33-CENSUS.md`'s re-scope table. Write no code in this task.
  </action>
  <acceptance_criteria>
    - `33-07-SUMMARY.md` contains a `## Shape-change dispositions (locked)` section with one line per confirmed shape-change candidate.
    - Every line names one of `fix-shape-now`, `fix-parser-keep-shape` or `defer-shape-to-named-phase` verbatim.
    - Every `defer-shape-to-named-phase` line names a concrete destination — never `TBD` or `later`.
    - No source file was modified by this task: `git status --porcelain packages/` is empty at the end of it.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Shape-change dispositions (locked)' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-07-SUMMARY.md &amp;&amp; grep -qE 'fix-shape-now|fix-parser-keep-shape|defer-shape-to-named-phase' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-07-SUMMARY.md &amp;&amp; test -z "$(git status --porcelain packages/)"</automated>
  </verify>
  <done>Every confirmed defect requiring a published-model-shape change has an operator-selected disposition recorded verbatim, and each deferral names a concrete destination that Task 2 will add to the re-scope table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Triage, then fix each confirmed defect in-cycle with a mirrored mocked regression test</name>
  <reversibility rating="costly">Promoting a finding to CONFIRMED/FIXED and attaching a Regression link is recorded in a committed, append-only artifact whose non-OPEN statuses `append_finding` deliberately preserves; correcting a wrong promotion means an in-place edit of that file.</reversibility>
  <files>packages/&lt;pkg&gt;/src/&lt;pkg&gt;/client.py, packages/&lt;pkg&gt;/src/&lt;pkg&gt;/aio.py, packages/&lt;pkg&gt;/src/&lt;pkg&gt;/_core.py, packages/&lt;pkg&gt;/src/&lt;pkg&gt;/models.py, packages/&lt;pkg&gt;/tests/test_&lt;fix&gt;.py, .planning/verification/ambito-financiero-client-findings.md, .planning/verification/higyrus-client-findings.md, .planning/verification/iol-client-findings.md, .planning/verification/matriz-client-findings.md, .planning/verification/market-data-client-findings.md, .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md</files>
  <read_first>
    - `33-05-SUMMARY.md` — the shortlist of confirmed divergences ordered by consequence, and the per-package SUMMARY transcripts
    - `.planning/phases/33-.../33-CENSUS.md` — the S-1..S-5 dispositions and the existing `## Re-scope` table
    - `33-06-SUMMARY.md` / `33-LITERALS.md` — any observed value outside a declared member set, and where it was routed
    - `.planning/verification/<pkg>-findings.md` for each package with confirmed defects — the current `OPEN` blocks to promote
    - `verification/findings.py:583-706` — `append_finding`'s preservation short-circuit on non-`OPEN` status (`_replace_art_block`), and why re-serialising the file by hand destroys operator prose (`extra_bullets`, D-23)
    - `verification/cycle_report.py` — `verify_cycle_closure`, the `CONFIRMED`/`FIXED` filter, `_iter_findings`, and `_regression_is_resolvable`'s path-traversal defence and `def <test_name>(` substring match
    - `packages/higyrus-client/tests/test_decode.py:870-921` — the exact regression-test template: `httpx_mock` fixture, a context-restoring helper, an explicit `Client(...)` with a pre-seeded token so no auth round trip is mocked, and the async twin at `:907-921` written without `@pytest.mark.asyncio` because `asyncio_mode = "auto"`
    - `packages/<pkg>/tests/test_surface_parity.py` for each package being fixed — already exists ×5; run it after every fix, never rewrite it
    - The `client.py` and `aio.py` (and `_core.py` / `models.py`) of each package being fixed — C-3 requires the logic fix to be mirrored, and matriz DOES have an `aio.py` (CLAUDE.md's ARCHITECTURE section is stale on this point; `main_matriz.py:2101` constructs an `AsyncClient` and 20+ async probes exist)
    - `.planning/phases/29-decoder-observable/29-SIZING.md` `## Structural findings` S-1..S-5 and the "Also worth naming" table of dominant silent-substitution sites
  </read_first>
  <behavior>
    For each confirmed defect, RED then GREEN:

    - RED: a test in `packages/<pkg>/tests/test_<fix>.py` that mocks the exact divergent wire shape the
      live run observed (via `pytest-httpx`) and asserts the corrected behaviour. It MUST fail against
      the current source, and it must fail for the right reason — an assertion about the value or the
      raised error, not an `ImportError` or a mock-count mismatch. Written in both a sync form and an
      async form (the `aio` twin), following the `test_decode.py:870-921` template.
    - GREEN: the minimal fix inside the owning package that makes both forms pass, applied to
      `client.py` AND `aio.py`, or to the shared `_core.py` / `models.py` both surfaces dispatch
      through. No cross-package import is introduced; a fix that would need one is applied separately
      inside each package (C-2).
    - After each fix: `packages/<pkg>/tests/test_surface_parity.py` still passes, and the whole
      package suite passes.
    - The finding is then promoted from `OPEN` to `FIXED` with a `Regression:` bullet reading
      `packages/<pkg>/tests/<file>.py::<test_name>`, which `verify_cycle_closure` resolves.
  </behavior>
  <action>
    First triage. For every `OPEN` SHAPE finding the live run wrote, decide one of: a real defect to fix
    now; a real defect routed to a named destination (promote to `CONFIRMED` with the destination
    recorded); or expected vendor behaviour (`EXPECTED` / `NO-FIX` with the reason). Promote statuses
    by calling `append_finding` with the new status — never by re-serialising the findings markdown by
    hand: any non-`OPEN` status short-circuits into an in-place ART-block refresh precisely to avoid
    the lossy round trip that would destroy operator prose (CR-01/D-23). For matriz's six pre-existing
    `NO-FIX` records `F-03`..`F-08` and any near-duplicate the handler wrote alongside them, apply the
    disposition locked in 33-01 Task 1 — triage the new ones to `NO-FIX` referencing the originals.

    Then fix, in consequence order, starting with the item `33-05-SUMMARY.md` named first (expected to
    be S-3: matriz `Instrument.instrumentId` absent on the byCFICode and bySegment endpoints, where
    `marketId` / `symbol` arrive flattened and are currently discarded as `extra`, so every consumer of
    `inst.instrumentId.symbol` reads empty on every row). Apply each fix per the disposition selected
    in Task 1. Mirror every logic change across both surfaces; where both surfaces already dispatch
    through `_core.py`, fix it once there and prove the mirror with the async twin test rather than
    duplicating code.

    Write each regression test under `packages/<pkg>/tests/` and nowhere else. `verification/` has
    never executed in CI — the CI `test` job runs `pytest packages/${{ matrix.package }}`, an explicit
    path that overrides `testpaths` — and `verify_cycle_closure` validates a `Regression:` link by
    regex over file text, so a link pointing into `verification/` resolves structurally while the test
    never actually runs on a PR (P-8). Name each test descriptively after the behaviour it pins, not
    after the finding id, so it survives a renumbering.

    Do NOT edit any `_decode.py` copy: `tools/check_decode_intactness.py` pins all five to a
    `CANONICAL_DIGEST` and any edit fails the `lint` CI job until the digest is re-pinned, which is a
    deliberate reviewable act this phase has no mandate for. Do NOT widen, close or enforce any matriz
    `Literal` alias (D-09) and do NOT promote iol's `mercado` / `plazo` (D-10) — 33-06 already closed
    both with evidence.

    Finally, amend `33-CENSUS.md`'s `## Re-scope` table so it matches reality after triage: every
    confirmed defect NOT fixed in this cycle appears there with its package, model, field path, kind
    and a **named** destination that resolves to a ROADMAP phase heading or to a backlog entry added in
    this same commit. If the fix set grew or shrank relative to what 33-05 projected, amend the table
    rather than leaving it stale. Nothing may be dropped to fit a budget: if the confirmed set is
    larger than this plan can absorb, the overflow is re-scoped with named destinations, never
    silenced and never narrowed away.
  </action>
  <acceptance_criteria>
    - Every fix has a file `packages/<pkg>/tests/test_<fix>.py` containing both a sync test and an async test for the same behaviour, and `uv run pytest packages/<pkg>/tests/test_<fix>.py -q` passes.
    - Each new regression test was proven fail-first: `git stash` the source fix (or equivalent) reproduces a RED run, recorded in the SUMMARY per fix.
    - `uv run pytest packages -q` passes — the full CI-equivalent tree.
    - `uv run pytest packages/<pkg>/tests/test_surface_parity.py -q` passes for every package touched.
    - `grep -rc 'Regression:.*verification/' .planning/verification/*-findings.md` returns 0 for every file — no regression link points into the tree CI never runs.
    - Every `FIXED` or `CONFIRMED` finding written by this plan has a `Regression:` bullet, and `uv run python -c "import sys;sys.path.insert(0,'.');from verification.cycle_report import verify_cycle_closure;bad=[p for p in ['ambito-financiero-client','higyrus-client','iol-client','matriz-client','market-data-client'] if not verify_cycle_closure(p)[0]];print(bad);sys.exit(1 if bad else 0)"` exits 0.
    - `uv run python tools/check_decode_intactness.py` exits 0 — no `_decode.py` copy was edited.
    - `git diff --stat packages/matriz-client/src/matriz_client/types.py` is empty — no `Literal` alias was touched.
    - `uv run mypy && uv run ruff check . && uv run ruff format --check .` pass.
    - `33-CENSUS.md`'s `## Re-scope` table matches the post-triage reality and contains no `TBD`, `later` or empty Destination cell.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages -q &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run python tools/check_surface_types.py &amp;&amp; uv run python tools/check_uniform_structure.py &amp;&amp; test -z "$(git diff --stat packages/matriz-client/src/matriz_client/types.py)" &amp;&amp; ! grep -rq 'Regression:.*verification/' .planning/verification/ &amp;&amp; uv run mypy &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check .</automated>
  </verify>
  <done>Every confirmed defect is either fixed in its owning package with a mirrored, fail-first-proven mocked regression test under `packages/<pkg>/tests/`, or promoted to `CONFIRMED` and routed to a named destination in `33-CENSUS.md`'s re-scope table; no `_decode.py` copy and no `Literal` alias was touched; `pytest packages` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove criterion 4 non-vacuously — cycle closure with a numeric inspected-count floor</name>
  <files>verification/test_cycle_closure_phase33.py</files>
  <read_first>
    - `verification/test_cycle_closure_market_data.py` (all 54 lines) — the exact two-test structure to replicate: a green test asserting `(True, [])`, and a non-vacuity test asserting the findings file exists, that at least one finding is `CONFIRMED`/`FIXED`, and that the applicable count meets a numeric lower bound
    - `verification/cycle_report.py` — `verify_cycle_closure`, `_iter_findings` (note: a finding present only in the `## Index` with no detail block is not returned at all), and the `CONFIRMED`/`FIXED` filter
    - `verification/findings.py` — `findings_path`
    - `33-07-SUMMARY.md` in progress — the count of Phase 33 fixes and confirmations per package
    - `33-RESEARCH.md` Pitfall P-7 — the measured pre-phase inspected counts: ámbito **0**, higyrus **0**, iol **1**, matriz **1**, market-data **50**. Two of the five PASS entirely vacuously today.
  </read_first>
  <behavior>
    `verification/test_cycle_closure_phase33.py`, parametrized over all five packages:

    - `test_cycle_closure_is_green[<pkg>]`: `verify_cycle_closure(pkg)` returns `(True, [])`, with the
      assertion message listing any finding whose `Regression:` link did not resolve.
    - `test_cycle_closure_is_not_vacuous[<pkg>]`: the findings file exists (the function returns
      `(True, [])` for a nonexistent file, so a green result there would be meaningless); at least one
      finding is `CONFIRMED` or `FIXED`; and the applicable count is at least the per-package lower
      bound `pre_phase_baseline + phase_33_promotions`, where the baselines are the measured values
      ámbito 0, higyrus 0, iol 1, matriz 1, market-data 50, and the promotions come from Task 2. The
      assertion message states which number was the baseline and which was the phase contribution, so a
      later reader can tell a genuine regression from a stale bound.

    Ámbito's row is the sharpest case: its baseline is 0 and D-12 predicts zero divergences, so its
    non-vacuity bound must be justified explicitly rather than pencilled in.
  </behavior>
  <action>
    Create `verification/test_cycle_closure_phase33.py` by copying the structure of
    `verification/test_cycle_closure_market_data.py` (`:23-53`) and parametrizing it across the five
    package slugs. Replace that file's `>= 34` Phase-27 backfill literal with a per-package lower bound
    dict computed as `pre_phase_baseline + phase_33_promotions`, with the baselines hard-coded from the
    measured pre-phase values and a comment naming them as measurements, not estimates.

    For `ambito-financiero-client`, whose baseline is 0 and whose expected Phase 33 contribution is 0
    (D-12: zero model classes, therefore zero walker calls, therefore zero divergences), do NOT write a
    bound of `>= 0` — that is exactly the vacuous assertion this file exists to prevent. Instead assert
    the D-12 property directly and positively: the package declares zero model classes, its Phase 33
    strict run reported a non-zero probe count with `len(handler.seen) == 0` and zero handler errors,
    and its findings file is unchanged by this phase. Put that reasoning in the assertion message so
    the exemption is legible rather than looking like an oversight.

    Then run the closure check for real and record, per package, both numbers criterion 4 needs:
    `verify_cycle_closure(pkg)` and the count of `CONFIRMED`/`FIXED` findings it actually inspected. A
    PASS with an unchanged inspected count is the warning sign, not a result. Record the pre- and
    post-phase inspected counts side by side in `33-07-SUMMARY.md`.

    Finally, re-run the phase's full gate set and record the outcome: `uv run pytest packages -q`, the
    four `tools/check_*.py` scripts, `uv run mypy`, `uv run ruff check . && uv run ruff format --check .`,
    plus the targeted harness subset `verification/test_divergences.py
    verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py
    verification/test_cycle_closure_phase33.py verification/test_main_drivers_bare_except.py`. Do NOT
    gate on an unqualified `uv run pytest` or on a full `pytest verification` run: that directory is red
    today and takes roughly 14 minutes; compare it against `33-BASELINE.md`, not against zero, and
    report only the delta.
  </action>
  <acceptance_criteria>
    - `uv run pytest verification/test_cycle_closure_phase33.py -q` passes and reports at least 10 test cases (5 packages x 2 tests).
    - `grep -v '^#' verification/test_cycle_closure_phase33.py | grep -c 'verify_cycle_closure'` is at least 1, and the file contains a per-package numeric lower-bound mapping rather than a single shared literal.
    - `uv run python -c "import sys;sys.path.insert(0,'.');from verification.cycle_report import _iter_findings,verify_cycle_closure;from verification.findings import findings_path;\nfor p in ['ambito-financiero-client','higyrus-client','iol-client','matriz-client','market-data-client']:\n    ok,missing=verify_cycle_closure(p);n=len([s for _f,s,_r in _iter_findings(findings_path(p).read_text(encoding='utf-8')) if s in ('CONFIRMED','FIXED')]);print(p,ok,missing,n)"` prints `True []` for all five and an inspected count that is greater than the recorded pre-phase baseline for every package that received a Phase 33 promotion.
    - `uv run pytest packages -q` passes.
    - `uv run python tools/check_decode_intactness.py && uv run python tools/check_surface_types.py && uv run python tools/check_uniform_structure.py` exit 0.
    - `uv run pytest verification/test_divergences.py verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py verification/test_cycle_closure_phase33.py verification/test_main_drivers_bare_except.py -q` passes.
    - `uv run pytest verification -q --tb=no -rfE` produces a failure/error set that is a subset of the one recorded in `33-BASELINE.md`; any entry not in the baseline is a Phase 33 regression and blocks completion.
    - `uv run mypy && uv run ruff check . && uv run ruff format --check .` pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest verification/test_cycle_closure_phase33.py verification/test_divergences.py verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py verification/test_main_drivers_bare_except.py -q &amp;&amp; uv run pytest packages -q &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run python tools/check_surface_types.py &amp;&amp; uv run python tools/check_uniform_structure.py &amp;&amp; uv run mypy &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check .</automated>
    <human-check>Confirm criterion 4 is real, not vacuous: for each of the five packages, read the pre-phase and post-phase inspected-count pair recorded in 33-07-SUMMARY.md and confirm the gate actually inspected Phase 33 findings rather than passing on an empty filter. Confirm ámbito's exemption is argued from its zero-model-class structure plus its asserted smoke result, not pencilled in as `>= 0`. Confirm every `FIXED` finding's Regression link points into `packages/**/tests/` and that you accept the fix it names.</human-check>
  </verify>
  <done>`verification/test_cycle_closure_phase33.py` exists with per-package numeric lower bounds derived from the measured pre-phase baselines plus this phase's promotions; all five packages return `(True, [])` with an inspected count that moved; the full CI-equivalent gate set is green and `verification/` is no worse than `33-BASELINE.md`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/verification/<pkg>-findings.md` | Git-committed, append-only, and carrying operator triage prose that `append_finding`'s preservation path exists to protect. |
| `Regression:` bullet -> a filesystem path | `verify_cycle_closure` resolves it; a traversal or absolute path would reach outside the repo. |
| package source fix -> published wheel | Anything changed here ships in Phase 34 to every pinned consumer. |
| regression test -> network | A test that reaches a live vendor would fire real traffic on every CI run of every PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-38 | Tampering | re-serialising a findings file by hand and destroying operator triage prose | mitigate | Promote status only through `append_finding`; any non-`OPEN` status short-circuits into the in-place ART-block refresh (`_replace_art_block`) precisely to avoid the lossy round trip (CR-01/D-23). Never hand-edit the markdown. |
| T-33-39 | Tampering | path traversal or an absolute path in a `Regression:` bullet | mitigate | `verification/cycle_report.py::_regression_is_resolvable` already rejects `..` and absolute paths and confines resolution to the repo root (T-5-06). Use it; do not reimplement a "does this test exist" check. |
| T-33-40 | Repudiation | a criterion-4 PASS reported from a gate that inspected zero findings | mitigate | Task 3's non-vacuity test asserts the file exists, that at least one finding is `CONFIRMED`/`FIXED`, and a per-package numeric floor derived from the measured pre-phase baselines (ámbito 0, higyrus 0, iol 1, matriz 1, market-data 50). Ámbito's exemption is argued positively from its zero-model-class structure, never as `>= 0` (D-11, P-7). |
| T-33-41 | Repudiation | a regression test placed where CI never runs it, so the link resolves and the test never executes | mitigate | Every regression test goes under `packages/<pkg>/tests/`; an acceptance criterion greps every findings file for a `Regression:` link pointing into `verification/` and requires zero hits. The CI `test` job runs `pytest packages/${{ matrix.package }}`, an explicit path that overrides `testpaths` (P-8). |
| T-33-42 | Information Disclosure | a live wire value captured during 33-05 being pasted into a regression test fixture and committed | mitigate | Regression fixtures are synthesised to the SHAPE the live run observed, using placeholder identifiers — never a captured account number, CUIT or symbol tied to a real account. The six-key record the finding carries is type-not-value, so the finding itself supplies the shape without supplying a value. |
| T-33-43 | Tampering (live venue) | a regression test reaching a real vendor endpoint on every CI run | mitigate | Every regression test uses `pytest-httpx` with an explicit `Client(...)` carrying a pre-seeded token, so no auth round trip and no network call occurs — the `packages/higyrus-client/tests/test_decode.py:870-921` template. Prohibition P-05 records this. |
| T-33-44 | Elevation of Privilege | a fix silently changing a published model's public shape without the semver consequence being decided | mitigate | Task 1 is a blocking `checkpoint:decision` covering exactly that class of fix, with the Phase 34 bump-set consequence stated. The `<reversibility rating="one-way">` element keeps the record after the checkpoint resolves. |
| T-33-45 | Tampering | editing a `_decode.py` copy to make a divergence disappear | mitigate | `tools/check_decode_intactness.py` pins all five copies to a `CANONICAL_DIGEST` and is an acceptance criterion on both implementation tasks. Re-pinning is a deliberate reviewable act this phase has no mandate for. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages installed; `uv.lock` untouched (`33-RESEARCH.md` `## Package Legitimacy Audit` is empty by construction). No install task, therefore no legitimacy checkpoint. |
</threat_model>

<verification>
- `uv run pytest packages -q` — the full CI-equivalent tree, both surfaces
- `uv run pytest packages/<pkg>/tests/test_surface_parity.py -q` for every package touched
- `uv run pytest verification/test_cycle_closure_phase33.py -q` — 5 packages x 2 tests
- `uv run pytest verification/test_divergences.py verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py verification/test_main_drivers_bare_except.py -q`
- `verify_cycle_closure` returns `(True, [])` for all five packages with a moved inspected count
- `grep -r 'Regression:.*verification/' .planning/verification/` returns nothing
- `uv run python tools/check_decode_intactness.py && uv run python tools/check_surface_types.py && uv run python tools/check_uniform_structure.py`
- `uv run mypy && uv run ruff check . && uv run ruff format --check .`
- `uv run pytest verification -q --tb=no -rfE` is a subset of `33-BASELINE.md`
</verification>

<success_criteria>
- Every `OPEN` SHAPE finding the live run wrote has a triage disposition: fixed now, confirmed-and-routed, or expected
- Every fix is inside its owning package, mirrored across `client.py` and `aio.py` (or their shared `_core.py` / `models.py`), with no cross-package import introduced
- Every fix has a mocked, fail-first-proven regression test under `packages/<pkg>/tests/` in both a sync and an async form, and the finding's `Regression:` bullet resolves to it
- `test_surface_parity.py` is green for every package touched
- `verify_cycle_closure` returns `(True, [])` for all five packages, and the inspected `CONFIRMED`/`FIXED` count moved above the measured pre-phase baseline for every package that received a promotion
- Ámbito's non-vacuity is argued from its zero-model-class structure and its asserted smoke result, not from a `>= 0` bound
- `33-CENSUS.md`'s re-scope table matches post-triage reality with a named destination for every unfixed confirmed defect
- No `_decode.py` copy, no matriz `Literal` alias and no iol `Literal` promotion was touched
- `verification/` is no worse than the `33-BASELINE.md` red baseline
</success_criteria>

<output>
Create `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-07-SUMMARY.md` when done.
Record: the Task 1 per-finding disposition selection verbatim; one entry per fix with its package,
model, field path, the two surface files touched, the regression test path and the RED evidence that
proved it fail-first; the pre- and post-phase `CONFIRMED`/`FIXED` inspected counts side by side per
package; the final `## Re-scope` table as amended; and the `pytest verification` delta against
`33-BASELINE.md`.
</output>
