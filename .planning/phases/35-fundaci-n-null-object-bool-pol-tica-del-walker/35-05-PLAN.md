---
phase: 35-fundaci-n-null-object-bool-pol-tica-del-walker
plan: 05
type: execute
wave: 3
depends_on: ["35-01", "35-02", "35-03", "35-04"]
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/_decode.py
  - packages/higyrus-client/src/higyrus_client/_decode.py
  - packages/iol-client/src/iol_client/_decode.py
  - packages/market-data-client/src/market_data_client/_decode.py
  - packages/matriz-client/src/matriz_client/_decode.py
  - packages/ambito-financiero-client/tests/test_decode.py
  - packages/higyrus-client/tests/test_decode.py
  - packages/iol-client/tests/test_decode.py
  - packages/market-data-client/tests/test_decode.py
  - packages/matriz-client/tests/test_decode.py
  - packages/market-data-client/tests/test_core.py
  - tools/check_decode_intactness.py
autonomous: true
requirements: [NOBJ-02]

must_haves:
  truths:
    - "A null or absent value on a NON-OPTIONAL model-typed or list-typed field decodes to the empty instance or [] and emits NO divergence record — the collapse half of ROADMAP criterio 2."
    - "A wrong-typed value on the same field still emits the six-key record and still raises under strict_decode — the falsification half, already covered by the 10 tests waves 1-2 added, which must stay green through this change."
    - "Both halves are falsifiable: inverting the disposition reddens a named test. The collapse half is falsified by the 11 assertions this plan inverts; the wrong-type half by the 10 tests waves 1-2 wrote against the unedited walker."
    - "strict_decode needed no new code: the raise lives in the single DecodeScope.__call__ choke point, so not calling the sink is not raising, and the choke point is not edited (D-02)."
    - "check_decode_intactness reduces the five _decode.py copies to ONE new canonical hash — no copy left behind, none diverging — and the new CANONICAL_DIGEST value was READ FROM the gate's own failure message, never authored, predicted, or copied out of RESEARCH.md."
    - "The other three v1.6 gates pass with no rule loosened, no lower bound lowered, and no package excluded (D-11)."
    - "No public surface changed: the four verification/snapshots/ files are byte-identical after regeneration, and the only test edits in the whole phase are the 11 named inversions plus files created new (D-12, D-13)."
    - "Three adjacent dispositions are provably intact: the top-level non_dict record for a None/204 body, the missing record for scalar leaves including scalars inside list elements, and the mapping axis in models.py (D-03, D-14)."
  artifacts:
    - path: "packages/higyrus-client/src/higyrus_client/_decode.py"
      provides: "EDIT 1 (list site: silence gated to a null value) + EDIT 2 (model site: silent collapse) + the rewritten disposition comment"
      contains: "Phase 35, NOBJ-02"
    - path: "tools/check_decode_intactness.py"
      provides: "The new CANONICAL_DIGEST, recomputed from the gate's own output"
      contains: "CANONICAL_DIGEST"
    - path: "packages/market-data-client/tests/test_core.py"
      provides: "The eleventh inverted assertion — the one on a real shipped model rather than a walker fixture"
      contains: "test_health_from_api_missing_auth_yields_zero_valued_nested_model"
  key_links:
    - from: "packages/*/src/*/_decode.py (five copies)"
      to: "tools/check_decode_intactness.py"
      via: "the gate normalizes the five copies with 8 rules and asserts a single distinct hash equal to the pinned constant; function docstrings and inline comments ARE hashed, only the module docstring is stripped"
      pattern: "CANONICAL_DIGEST"
    - from: "packages/*/src/*/_decode.py list site"
      to: "packages/*/src/*/_decode.py DecodeScope.__call__"
      via: "the sink is the only path to the strict-mode raise; gating the sink call on a non-null value is what keeps wrong-type fatal while making the null collapse silent"
      pattern: "if value is not None:"
  prohibitions:
    - statement: "No gate is weakened to make this land. No normalization rule is added or relaxed, no lower bound is lowered, no package is excluded from any roster, and no strictness knob in pyproject.toml is touched. A red gate is a signal about the change, not about the gate."
      status: flagged-unverified
      verification: "git diff must show tools/check_decode_intactness.py changed on the CANONICAL_DIGEST line only, and pyproject.toml, the other three tools/ gates, and ci.yml byte-unchanged."
    - statement: "No test is edited beyond the 11 named assertions. Any other red is evidence the change left its scope, never evidence that the test is wrong."
      status: flagged-unverified
      verification: "git diff --numstat across the whole phase shows deletions in exactly six test files, all attributable to the 11 named assertions and their names/docstrings."
    - statement: "The silencing never extends beyond a null value. An empty string, a zero, an empty dict and an empty list are all falsy and are all legitimate wrong-types that must keep diverging — the gate is an identity check against null, never a truthiness check."
      status: flagged-unverified
      verification: "Read the list-site gate: it must compare against the null singleton, not test falsiness. The 10 wrong-type tests from waves 1-2 redden if this is violated."
    - statement: "The new CANONICAL_DIGEST is never predicted, authored, or copied from RESEARCH.md. RESEARCH F-6 printed a digest for a DIFFERENT variant of this edit (one without the comment rewrite) and pasting it would produce a gate that is green against a file nobody reviewed."
      status: flagged-unverified
      verification: "The SUMMARY must record the gate's own failure-message output as the provenance of the value, and the value must differ from the one quoted in RESEARCH F-6."
---

<objective>
Land the walker disposition that NOBJ-02 describes: a null or absent value on a non-optional model-typed or list-typed field stops being recorded as a divergence, while a wrong-typed value on the same field keeps diverging and keeps being fatal under `strict_decode`. Then re-pin the canonical hash and prove the whole phase changed no public surface.

Purpose: this is the only step in the phase that moves a byte inside a hashed, five-times-replicated file, and it is the step that cannot be partitioned. Any intermediate state — one copy edited, or five copies edited with a stale pin, or the pin bumped without the edit — leaves `check_decode_intactness` red, and that gate runs in the CI `lint` job on every push. The five edits, the comment rewrite, the eleven assertion inversions and the digest bump are ONE commit.

Output: EDIT 1 + EDIT 2 + the rewritten disposition comment in five `_decode.py` copies, eleven inverted assertions across six test files, a new `CANONICAL_DIGEST`, and a verified phase gate.

**Reversibility:** two-way door. This plan changes no public signature, no return value, and no wire behaviour — only what the divergence channel records. Reverting is a `git revert` of one commit; the digest bump follows the code back automatically. No human checkpoint is warranted and none is placed.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-CONTEXT.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-PATTERNS.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-01-SUMMARY.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-03-SUMMARY.md
@.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol / file | Owner plan |
|---|---|
| EDIT 1 — list site, silence gated to a null value — `_decode.py` ×5 | **35-05 [this plan]** |
| EDIT 2 — model site, silent collapse — `_decode.py` ×5 | **35-05 [this plan]** |
| Rewritten `# Phase 35, NOBJ-02:` disposition comment — `_decode.py` ×5 (hashed, byte-identical) | **35-05 [this plan]** |
| The 11 inverted assertions: `test_missing_list_field_returns_empty_list_and_reports` ×5, `test_absent_nested_model_key_is_missing_on_the_outer_model` ×5, `test_health_from_api_missing_auth_yields_zero_valued_nested_model` ×1 | **35-05 [this plan]** |
| New `CANONICAL_DIGEST` value at `tools/check_decode_intactness.py:222` — **recomputed, never copied** | **35-05 [this plan]** |
| `higyrus_client.models.SafeModel.empty()` + `.__bool__()` | 35-01 |
| `.planning/phases/35-…/35-RETIRED-TRIPLES.md` (D-17 ledger) | 35-02 |
| `iol_client` / `market_data_client` bases + suites | 35-03 |
| `matriz_client._SafeModel.__bool__`, `UnknownFrame.__bool__`, matriz/ambito/wallets suites | 35-04 |
| `test_null_object.py` ×6 | 35-01 / 35-03 / 35-04 |
| `test_wrong_typed_list_field_still_reports_type` ×5, `test_strict_mode_still_raises_on_a_wrong_typed_list` ×5 | 35-01 / 35-03 / 35-04 |
</artifacts_this_phase_produces>

<flagged_assumptions>
| Requirement | Probe verdict | Resolved by | Residual risk |
|---|---|---|---|
| NOBJ-02 | `unclassified` / `unresolved` | ROADMAP Phase 35 criterios 2, 3 and 4 + CONTEXT D-01..D-04, D-10..D-14 → the `must_haves.truths` rows above. | This is a change to what the observability channel records. RESEARCH assumption A4 flags it as MEDIUM: no consumer outside the monorepo is known to depend on the removed record, but that is not proven. Accepted because narrowing the channel exactly here is what NOBJ-02 asks for, the return values do not change, and Phase 39 reconciles the census against the 35-02 ledger so the reduction is accounted for rather than absorbed. |
| NOBJ-01 | `unclassified` / `unresolved` | Closed by plans 35-01, 35-03, 35-04. This plan must not redden it. | The final gate re-runs the full `packages` suite, so a regression against NOBJ-01 surfaces here. |

RESEARCH assumption A2 — that the exact source form of the list-site gate is what the plan fixes,
because the form determines the digest — is **closed by this plan**: the form is locked in Task 1
and the digest is recomputed against whatever that form plus the comment rewrite produces.

RESEARCH assumption A3 (the eleven-red set is identical on Python 3.13) stays open and is
discovered by CI's 6×2 matrix on first push. Everything measured in RESEARCH ran on CPython 3.12.11
and nothing in this change is version-specific.

RESEARCH assumption A5 (there is no twelfth hidden red) is backed by a full `packages` suite run
under the applied change, not by inference. The intermediate checkpoint in Task 1 re-measures it
rather than trusting it.
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ATOMIC — the two walker edits ×5, the disposition comment ×5, the 11 inversions, and the recomputed digest, in ONE commit</name>

  <files>packages/ambito-financiero-client/src/ambito_financiero_client/_decode.py, packages/higyrus-client/src/higyrus_client/_decode.py, packages/iol-client/src/iol_client/_decode.py, packages/market-data-client/src/market_data_client/_decode.py, packages/matriz-client/src/matriz_client/_decode.py, packages/ambito-financiero-client/tests/test_decode.py, packages/higyrus-client/tests/test_decode.py, packages/iol-client/tests/test_decode.py, packages/market-data-client/tests/test_decode.py, packages/matriz-client/tests/test_decode.py, packages/market-data-client/tests/test_core.py, tools/check_decode_intactness.py</files>

  <read_first>
    - `packages/higyrus-client/src/higyrus_client/_decode.py:425-495` — the whole `walk_field` region you are editing: the `Union`/`Optional` early return at `:432-440` (this is what makes everything below it non-optional by construction, and the rewritten comment must say so), the list site at `:442-452`, the `_is_model` site at `:454-487` with its 27-line comment block at `:455-481`, and the scalar branches at `:489+` which are all untouched
    - `packages/higyrus-client/src/higyrus_client/_decode.py:195-250` — `DecodeScope.__call__`, the single choke point where `strict_decode` raises, and `SILENT_SINK`. Read it to confirm you do NOT need to edit it (D-02) — the raise is downstream of the sink call, so not calling the sink is not raising.
    - `packages/higyrus-client/src/higyrus_client/_decode.py:360-370` — `_kind_of`, the missing-vs-wrong-type discriminator
    - `tools/check_decode_intactness.py:120-272` — the `CANONICAL_DIGEST` constant at `:222` and the comment block above it that prescribes the bump procedure literally; plus `:402-519` — the 8 normalization rules (rule 1 strips ONLY the module docstring; function and class docstrings and every inline comment ARE hashed; rule 6 replaces the package import name; rule 8 re-runs `ruff format` over the normalized text) and the failure message at `:506-516` that prints the computed digest
    - The five `test_decode.py` files and `packages/market-data-client/tests/test_core.py:1041-1082` — read each of the 11 assertion sites individually before editing any of them
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Pattern 1 (per-package line offsets — ambito is shifted +7), §Pitfall 1 (the eleventh assertion), §Pitfall 2 (the five copies of the second assertion are NOT verbatim siblings), §Pitfall 7 (why this is one commit), §Anti-Patterns, §Code Examples 4 and 5
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-PATTERNS.md` §B and §C — the exact current text at both sites and the per-package assertion shapes
  </read_first>

  <behavior>
    - After the edit and before the inversions, `uv run pytest packages -q` fails on EXACTLY the eleven named assertions and nothing else. That intermediate measurement is the scope proof.
    - After the inversions, `uv run pytest packages -q` reports zero failures.
    - The ten wrong-type tests waves 1-2 added stay green throughout — they are the tripwire proving the silencing did not over-reach past a null value.
    - `check_decode_intactness` reports one distinct hash across the five copies before the pin is bumped, and passes entirely after.
    - `check_uniform_structure`, `check_surface_types` and `surface_parity` stay green with no rule touched.
  </behavior>

  <action>
This is one commit. Do not stage or commit anything until every step below is done and every check in `<acceptance_criteria>` passes. A partial landing is red in CI on push no matter which half you land first: five edited copies with the old pin fails the pin check, and a bumped pin with unedited copies fails it from the other side.

**Step 1 — EDIT 1, the list site.** In each of the five `_decode.py` copies, find the `if origin is list:` branch (navigate by symbol, not by line number — ambito's copy is shifted about seven lines relative to the other four). Inside its `if not isinstance(value, list):` guard, the current single statement calls the sink with the discriminator function as the kind argument, then returns the empty list. Replace that with a nested guard: a new `if value is not None:` line, the sink call indented under it, and the return left where it is at the outer level. In the moved sink call, replace the discriminator-function kind argument with the string literal `"type"` — at that point in the code the value is provably not null, so the discriminator can only return that one answer, and the literal is what the record would have said anyway. The other four sink arguments do not change.

The gate is an identity test against the null singleton and nothing else. Do not write it as a falsiness test: an empty string, a zero, an empty dict and an empty list are all falsy and are all legitimate wrong-types that must keep diverging. The ten wrong-type tests from waves 1-2 exist to catch exactly this mistake.

This exact source form is locked by the plan — not because alternatives would misbehave, but because the digest is a function of the literal text, so the form has to be decided once and here rather than per copy.

**Step 2 — EDIT 2, the model site.** In the `if _is_model(hint):` branch, inside the `if value is None:` guard, delete the sink call on the line immediately preceding the `return hint(**walk_model(...))` line, so the guard's body becomes that single return. The returned value is unchanged — it was already the all-defaults instance built with the silent sink. Only the reporting goes away.

**Step 3 — the disposition comment, byte-identical ×5.** The comment block that precedes the model site currently ends with a paragraph explaining why a null on a declared nested-model field is recorded as a divergence of the outer model. That explanation is exactly the half that stops being true, so it must be rewritten rather than left to rot. Conserve the paragraphs above it — the ones about building nested models through the walker rather than through the tolerant constructor are still load-bearing and unrelated. Rewrite the final paragraph to state, in this order: that a null or absent value on a non-optional link is a legitimate shape and collapses to the empty instance without a record (prefix it `# Phase 35, NOBJ-02:` following this file's established `# Lock N:` / `# Phase NN code review, WR-0X:` convention); that the branch above returns early for optional annotations, which is what makes everything reaching this point non-optional by construction; that a genuinely non-dict, non-null payload keeps its record and its attribution; and that the returned value is unchanged, which is the whole point. Leave the historical citation to the earlier code-review item alongside the new one so a future reader sees the full history rather than a disposition with no provenance.

Two constraints on the comment text. It must be byte-identical across the five copies — comments are hashed, so the gate will enforce this for you and a divergence there is a hard red, not a nit. And it must not quote the deleted sink call verbatim, because an acceptance grep for that call is file-wide and a comment echoing it would keep the count above zero. Describe the old behaviour in words.

Apply the same replacement text mechanically to all five copies rather than retyping it five times; retyping is how a stray space enters copy four.

**Step 4 — format, type-check, and confirm mutual agreement BEFORE touching the pin.** Run `uv run ruff format packages/`, then `uv run ruff check packages/`, then `uv run mypy` and `uv run mypy packages/market-data-client/src`. Then run `uv run python tools/check_decode_intactness.py`. Read its failure output carefully: it must report a SINGLE distinct hash that disagrees with the pinned constant. If it reports two or more distinct hashes, the five copies disagree, the printed digest is meaningless, and you must fix the divergence before going further — do not bump the pin to silence a disagreement.

**Step 5 — the intermediate scope measurement.** Before inverting anything, run `uv run pytest packages -q`. It must fail on exactly eleven tests, and they must be exactly these, by name:
  - `test_missing_list_field_returns_empty_list_and_reports` in `tests/test_decode.py` of ambito-financiero, higyrus, iol, market-data and matriz (five)
  - `test_absent_nested_model_key_is_missing_on_the_outer_model` in the same five files (five)
  - `test_health_from_api_missing_auth_yields_zero_valued_nested_model` in `packages/market-data-client/tests/test_core.py` (one)
Record the exact failure list in the SUMMARY. A twelfth red means the change left its scope — stop and diagnose rather than editing the twelfth test. A count below eleven means an edit did not reach every copy. The eleventh is the one that lives outside `test_decode.py`, on a real shipped model rather than a walker fixture; it is the same logical species as the other ten and its inversion is authorized on the same grounds, but it is easy to meet unprepared and mistake for out-of-scope damage.

**Step 6 — invert the eleven, one at a time, by reading each.** These five copies are NOT verbatim siblings of each other the way the walker copies are; a scripted substitution will fail on at least matriz. For each site, keep every assertion about the returned VALUE byte-identical — the value not changing is the point of NOBJ-02 — and change only the assertion about the emitted records. Where the assertion is an equality against a one-element list, it becomes an equality against the empty list. Where it is a membership check (matriz's copy of the second assertion drives the tolerant constructor directly and asserts membership, and carries a second negative assertion after it), it becomes the negated membership; keep the trailing negative assertion intact. In `test_core.py` only the records line moves; the equality against the zero-valued nested model stays byte-identical.

Each of the ten in `test_decode.py` also has a NAME and a DOCSTRING asserting the old disposition in prose. Rewrite both alongside the assertion in all five copies — a test named for a report it no longer expects is a false document, and these names are how the next phase will find them. Add one sentence to each docstring recording that the inversion is deliberate, citing NOBJ-02 and D-13, so the change is not mistaken later for a weakened test.

**Step 7 — recompute and bump the pin.** Re-run `uv run python tools/check_decode_intactness.py`, read the digest it prints as computed, and paste that value into `tools/check_decode_intactness.py` at the `CANONICAL_DIGEST` assignment. Never author, predict, or copy a digest from any planning document: RESEARCH printed one for a variant of this edit that had no comment rewrite, and pasting it would produce a gate that is green against a file nobody reviewed. Change that one line and nothing else in this file — no normalization rule, no roster, no exemption. Re-run the gate until it exits clean.

**Step 8 — commit once**, with a message that states what changed and why the digest moved, per the procedure the constant's own comment block prescribes.
  </action>

  <verify>
    <automated>uv run python tools/check_decode_intactness.py && uv run pytest packages -q && uv run ruff check packages/ && uv run ruff format --check packages/ && uv run mypy && uv run mypy packages/market-data-client/src</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages -q` exits 0 with zero failures.
    - `uv run python tools/check_decode_intactness.py` exits 0 — one distinct hash across the five copies, equal to the new pin.
    - For each of the five `_decode.py` copies: `grep -c 'sink(model, path, "missing"' <file>` returns `0`. It returns `1` at `HEAD`, so this check is non-vacuous.
    - For each of the five copies: `grep -c 'if value is not None:' <file>` returns `1`. It returns `0` at `HEAD`.
    - For each of the five copies: `grep -c '_kind_of(value), _name_of(hint)' <file>` returns `1`. It returns `2` at `HEAD` — the list site lost its call and the `Literal` branch kept its own, which is the intended asymmetry.
    - For each of the five copies: `grep -c 'Phase 35, NOBJ-02' <file>` returns the same non-zero value, and that value is identical across all five.
    - `grep -c 'if value is None:' <file>` is unchanged from `HEAD` in all five copies — the `Optional` early return, the model-site guard and the other null checks all survive; only a sink call was removed.
    - `git diff --stat tools/check_decode_intactness.py` shows exactly 1 insertion and 1 deletion — the pin line and nothing else. No normalization rule, roster or exemption moved.
    - The new pin value differs from the digest quoted in `35-RESEARCH.md` §Findings F-6 as `computed:`. Equality would mean the comment rewrite did not happen or was copied from the document instead of produced.
    - `git diff --exit-code pyproject.toml .github/workflows/ci.yml tools/check_uniform_structure.py tools/check_surface_types.py tools/surface_parity.py` exits 0 — no gate other than the pin moved, no strictness knob loosened.
    - `uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py && uv run python tools/surface_parity.py` exits 0.
    - `uv run pytest packages -q -k "wrong_typed_list or still_raises_on_a_wrong_typed_list"` exits 0 and reports 10 passing — the wrong-type tripwire from waves 1-2 survived the silencing.
    - `uv run pytest packages -q -k "test_non_dict_returns_empty or test_none_payload_behaves_as_non_dict or test_strict_mode_raises_on_a_missing_mapping_field or test_strict_mode_does_not_make_empty_fatal or test_non_dict_nested_payload_keeps_the_nested_attribution"` exits 0 — the four adjacent dispositions D-14 names as untouchable are intact and were not edited.
    - `git diff --numstat` limited to test files shows deletions in exactly six files: the five `test_decode.py` and market-data's `test_core.py`. Any seventh test file with deletions means an unauthorized edit.
    - `git log --oneline -1 --name-only` lists all twelve files of this plan in a single commit.
  </acceptance_criteria>

  <done>The disposition is live in all five copies with one new canonical hash, the eleven authorized inversions are done and documented as deliberate, the ten wrong-type tests and the four adjacent dispositions are green, and it is all one commit.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — prove no public surface moved and record the evidence</name>

  <files>.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-05-SUMMARY.md</files>

  <read_first>
    - `verification/regen_snapshots.py` — the regenerator; note it has no `--check` flag, so the check is a regeneration followed by a git diff
    - `verification/test_public_surface.py:100-125` — the snapshot format (name, kind, and the constructor signature), which is why adding methods to a base does not move it (D-12)
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-RESEARCH.md` §Pitfall 5 and §Findings F-9 — why `verification/` must never be run wholesale as a verification step, and why a green there is neither achievable nor expected
    - `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-02-SUMMARY.md` — the retired-triples counts, so the phase SUMMARY can state the accounting handoff to Phase 39 with real numbers
    - `.planning/ROADMAP.md` § Phase 35 — the five success criteria this gate is answering, one by one
  </read_first>

  <action>
Run the phase gate and write the evidence down. Every command below uses an explicit path: never run bare `uv run pytest`, because `testpaths` includes `verification/`, which contains real-sleep retry tests that do not finish inside ten minutes and which is red at baseline under the known `HARN-VERIF-01` debt. A green there is neither achievable nor expected and is not part of this phase's contract.

Run, and capture the actual output of each rather than paraphrasing it:
  - the four v1.6 gates, in the order CI's `lint` job runs them: the decode-intactness gate, the uniform-structure gate, the surface-types gate, and the surface-parity gate;
  - `uv run pytest packages -q` and `uv run pytest tests -q` (the root `tests/` directory holds two fast tests and also never runs in CI);
  - `uv run ruff check packages/` and `uv run ruff format --check packages/`;
  - `uv run mypy` and `uv run mypy packages/market-data-client/src` — the second is not redundant, market-data sits outside the root mypy file list;
  - `uv run python verification/regen_snapshots.py` followed by `git diff --exit-code verification/snapshots/`, which must exit 0 across the four snapshot files. This is the mechanical proof of ROADMAP criterio 4's byte-identity half, and it is asserted here rather than inferred from a CI leg because `verification/` never runs in CI.
  - `git diff --exit-code pyproject.toml uv.lock` and the per-package `pyproject.toml` files, which must all be clean — this phase installs nothing and its dependency closure is unchanged.

Then write `35-05-SUMMARY.md` and make it answer ROADMAP's five criteria explicitly, one heading each, each with the command output that settles it:
  1. Truthiness — the per-package roster counts from the wave 1-2 summaries and the total, with the two zero-roster packages listed by name with their reason rather than as blanks.
  2. The disposition and its falsification — both halves, naming the eleven inverted assertions as the falsifier of the collapse half and the ten wrong-type tests as the falsifier of the report half, plus the intermediate eleven-red measurement from Task 1 as the scope proof.
  3. The four gates — with the OLD and NEW `CANONICAL_DIGEST` values side by side, and an explicit sentence recording that the new one was read from the gate's own failure message. State that no rule was loosened, no bound lowered and no package excluded, and cite the clean diff over the other three gate scripts and `pyproject.toml` as the evidence.
  4. No public surface change — the snapshot diff result, and the phase-wide test-edit accounting: which files gained new tests, which six files had assertions edited, and the statement that the eleven were the whole of it.
  5. Alias invisibility — where the test lives in each of the five packages that have a walker, and the explicit note that wallets has none and asserts that absence instead.

Add a short handoff section for Phase 39 pointing at `35-RETIRED-TRIPLES.md` by filename, restating the per-package retired counts, and repeating the unit warning: the ledger counts distinct 4-tuples, the ratified floors in the sizing document are sums of records over corpus files, and subtracting across the two columns invents a shortfall that is not there.

If any command in this gate comes back red, do not adjust the gate, the bound or the test. Record the red with its exact output, state which criterion it blocks, and stop — a red here is information about the change.
  </action>

  <verify>
    <automated>uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py && uv run python tools/surface_parity.py && uv run pytest packages -q && uv run pytest tests -q && uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/ && git diff --exit-code pyproject.toml uv.lock</automated>
  </verify>

  <acceptance_criteria>
    - The command in `<verify>` exits 0 end to end. Every leg is load-bearing; a partial pass is a red gate.
    - `git diff --exit-code verification/snapshots/` exits 0 after regeneration, across all four snapshot files (ambito, iol, higyrus, matriz). RESEARCH F-8 measured this as byte-identical under the full change, so a moved snapshot means the phase changed a public surface it promised not to.
    - `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml` exits 0 — no dependency, no version bump, no strictness knob.
    - `35-05-SUMMARY.md` exists and contains five headings, one per ROADMAP Phase 35 criterion, each carrying real captured command output rather than a restatement of the criterion.
    - The SUMMARY records BOTH the old and the new `CANONICAL_DIGEST` values verbatim, and states in one sentence that the new one came from the gate's own failure message. `grep -c "ac14868282ad0a5c6fb85ab7b7920068303a781835b9c76ca50f26283c1c3dc5" 35-05-SUMMARY.md` returns at least `1` (the old value is on record) and the new value appears alongside it and is different.
    - The SUMMARY's criterion-1 section names `ambito-financiero-client` and `wallets-client` with their zero-by-decision reason. No bare zero and no `>= 0` appears anywhere in the document.
    - The SUMMARY's handoff section names `35-RETIRED-TRIPLES.md` and repeats the unit warning (distinct 4-tuples vs. records summed over corpus files).
    - No file under `packages/`, `tools/` or `verification/` is modified by this task — `git status --porcelain` shows only the new SUMMARY under `.planning/`.
  </acceptance_criteria>

  <done>All four gates, both suites, both mypy invocations, ruff, and the snapshot byte-identity check are green with captured output, and the phase SUMMARY answers each of ROADMAP's five criteria with the evidence that settles it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party JSON / WebSocket frame → `walk_field` | The workspace's input-validation layer (ASVS V5). This plan edits it — narrowly, at two branches — and that is the whole of the security-relevant change in the phase. |
| divergence record → package logger | ASVS V7 log sink. This plan REDUCES what reaches it, for one specific input class. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-13 | Repudiation | Reduced observability — a malformed payload passing without a signal | accept | Bounded by construction and by test. The silence applies only to an identity match against the null singleton on a field the annotation declares non-optional — the input class the milestone declares legitimate. A wrong string, an empty dict, a zero, an empty list all still emit and all stay fatal under `strict_decode`, because the raise is downstream of the sink and the sink is still called for them. Proven in both directions: the eleven inversions falsify the collapse half, the ten wrong-type tests from waves 1-2 falsify the report half. |
| T-35-14 | Tampering | Silencing over-reaching from "is null" to "is falsy" | mitigate | The gate is an identity check against the null singleton, never a truthiness test — stated in the action and in a `must_haves.prohibitions` row, and detected by the 10 wrong-type tests, which use `""`-class and non-list wrong values. Enforced additionally by the acceptance grep requiring exactly one `if value is not None:` per copy. |
| T-35-15 | Tampering | Silently weakening a CI gate to make the change land | mitigate | `tools/check_decode_intactness.py` must show exactly one insertion and one deletion, on the pin line. The other three gate scripts, `pyproject.toml` and `ci.yml` must be byte-unchanged, asserted by `git diff --exit-code`. The pin must differ from the digest quoted in RESEARCH, which is what proves it was recomputed rather than pasted. |
| T-35-16 | Tampering | A partial landing leaving the intactness gate red on a pushed commit | mitigate | The five edits, the comment rewrite, the eleven inversions and the pin bump are one commit, asserted by `git log --oneline -1 --name-only` listing all twelve files. |
| T-35-17 | Information disclosure | Wire-derived strings reaching the log sink | accept | Unchanged. `_safe_key` neutralisation, the 64-character key cap, the per-triple dedupe and the never-raise guarantee of the emitter are all outside the two edited branches and are byte-untouched. |
| T-35-SC | Tampering | pip / uv dependency installs | accept | No installs anywhere in this phase. RESEARCH §Package Legitimacy Audit records "no aplica" against a measured diff. Enforced by `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml`. |

**ASVS L1 assessment:** no `high`-severity finding. The change narrows one observability path deliberately, measurably and falsifiably, and touches none of the four input-handling controls in the walker.
</threat_model>

<verification>
Run from the repo root. NEVER run bare `uv run pytest` — `testpaths` includes `verification/`, which
does not finish inside ten minutes and is red at baseline under `HARN-VERIF-01` (RESEARCH Pitfall 5 /
F-9). CI never runs it either: the test job passes an explicit per-package path that overrides
`testpaths`.

1. `uv run python tools/check_decode_intactness.py` — green, one distinct hash equal to the new pin.
2. `uv run python tools/check_uniform_structure.py` — green.
3. `uv run python tools/check_surface_types.py` — green, no rule relaxed.
4. `uv run python tools/surface_parity.py` — green (exit 0, no output).
5. `uv run pytest packages -q` — zero failures.
6. `uv run pytest tests -q` — green (two tests, sub-second).
7. `uv run ruff check packages/ && uv run ruff format --check packages/` — clean.
8. `uv run mypy` and `uv run mypy packages/market-data-client/src` — both `Success`.
9. `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` — byte-identical.
10. `git diff --exit-code pyproject.toml uv.lock packages/*/pyproject.toml .github/workflows/ci.yml tools/check_uniform_structure.py tools/check_surface_types.py tools/surface_parity.py` — all clean.
</verification>

<success_criteria>
- All five `_decode.py` copies carry EDIT 1 (the sink call at the list site gated on a non-null value, with the kind argument as a string literal) and EDIT 2 (the model-site null branch reduced to its return), plus a byte-identical rewritten disposition comment marked `# Phase 35, NOBJ-02:`.
- `check_decode_intactness` reduces the five copies to ONE new canonical hash and passes; the pin moved by exactly one line and its value came from the gate's own output, not from a document.
- The other three v1.6 gates pass with no rule loosened, no bound lowered and no package excluded.
- Exactly eleven assertions were inverted, the intermediate measurement confirmed those eleven were the entire red set, and every returned-VALUE assertion at those sites is byte-unchanged.
- The ten wrong-type tests from waves 1-2 are green, and the four dispositions D-14 protects (top-level non-dict, scalar leaves, the mapping axis, empty-is-not-fatal-under-strict) are green and unedited.
- `uv run pytest packages -q` reports zero failures; `verification/snapshots/` regenerates byte-identical; `pyproject.toml` and `uv.lock` are untouched.
- The whole of Task 1 landed as one commit containing all twelve files.
</success_criteria>

<output>
Create `.planning/phases/35-fundaci-n-null-object-bool-pol-tica-del-walker/35-05-SUMMARY.md` when done.
It is the phase's closing artefact: five sections answering ROADMAP's five criteria with captured
command output, the old and new `CANONICAL_DIGEST` side by side with the provenance of the new one,
the eleven-red intermediate measurement, and a Phase 39 handoff naming `35-RETIRED-TRIPLES.md` and
repeating its unit warning.
</output>
