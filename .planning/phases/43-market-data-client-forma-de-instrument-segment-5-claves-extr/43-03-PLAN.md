---
phase: 43-market-data-client-forma-de-instrument-segment-5-claves-extr
plan: 03
type: execute
wave: 3
depends_on: ["43-01", "43-02"]
files_modified:
  - packages/market-data-client/src/market_data_client/_core.py
  - .planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SHAPE-01, HARN-02]

must_haves:
  truths:
    - "El docstring de parse_segments_response deja de afirmar que el fix de forma esta deliberadamente diferido — esa prosa paso a ser falsa (D-14)"
    - "Existe una tabla de disposicion campo por campo con cero filas sin disponer para Instrument y Segment, con la evidencia citada (criterio 1)"
    - "El antes/despues de get_segments() esta demostrado con la medicion, no afirmado — contra el capture del 42 y el censo de divergencias, no contra el SHAPE-diff del driver, que esta inerte para estos dos modelos (criterio 2, D-07)"
    - "Que client.py y aio.py no necesitan cambio esta demostrado POR MEDICION, no asumido (criterio 5, D-14)"
    - "Los 4 jobs de CI quedan verdes localmente y la fase no bumpea version en ninguno de los tres sitios (criterio 5, D-16)"
    - "El sitio del driver que dereferencia el campo removido queda documentado como seguimiento con destino nombrado — ni corregido en silencio ni descartado en silencio"
  artifacts:
    - path: ".planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md"
      provides: "tabla de disposicion campo por campo, evidencia offline del criterio 2, prueba de medicion de D-14, y el item de seguimiento del driver"
      min_lines: 90
    - path: "packages/market-data-client/src/market_data_client/_core.py"
      provides: "docstring de parse_segments_response reconciliado con la realidad post-fix"
      contains: "parse_segments_response"
    - path: ".planning/ROADMAP.md"
      provides: "entrada de backlog con destino nombrado para el dereference del driver"
      contains: "main_market_data.py"
  key_links:
    - from: ".planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "cada fila de la tabla cita el campo declarado y su disposicion implementada"
      pattern: "live_instruments"
    - from: "packages/market-data-client/src/market_data_client/_core.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "el docstring del parser describe la forma que el modelo declara hoy"
      pattern: "segments"
---

<objective>
Cerrar la Phase 43: reconciliar la unica prosa de fuente que la correccion vuelve falsa (el docstring
de `parse_segments_response`, D-14), producir los tres artefactos de evidencia que los criterios 1, 2
y 5 exigen —tabla de disposicion campo por campo, antes/despues medido de `get_segments()`, y prueba
por medicion de que las dos superficies no necesitan cambio—, dejar registrado con destino nombrado
el dereference del driver que D-16 mantiene fuera de alcance, y verificar los 4 jobs de CI mas la
ausencia de bump de version.

Cubre las decisiones locked **D-07, D-14, D-15, D-16** y consolida la evidencia de D-01 a D-13.

Purpose: los criterios 1, 2 y 5 de la fase piden EVIDENCIA, no codigo. Sin este plan la correccion
existe pero nadie puede leer que se dispuso de cada campo, ni verificar que el antes/despues se midio
en vez de afirmarse, ni confirmar que la fase no publico nada.

Output: `_core.py` con el docstring corregido, `43-DISPOSITION.md` completo, una entrada de backlog
en `ROADMAP.md`, y los 4 gates verdes.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-CONTEXT.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-01-SUMMARY.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliar el docstring de parse_segments_response y medir D-14</name>

  <files>packages/market-data-client/src/market_data_client/_core.py</files>

  <read_first>
    - `packages/market-data-client/src/market_data_client/_core.py:975-1099` — los tres parsers: `parse_instruments_response`, `parse_segments_response` (cuyo docstring de :1030-1057 se reescribe) y `parse_symbols_response`
    - `packages/market-data-client/src/market_data_client/models.py:786-830` — la forma nueva de `Instrument` y `Segment` ya implementada en 43-01
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-01-SUMMARY.md` — la tabla de disposicion tal como quedo implementada
  </read_first>

  <action>
**(1) Reescribir el parrafo obsoleto del docstring de `parse_segments_response`.**

El parrafo que hoy empieza declarando que la otra mitad del blast radius de S-1 esta DELIBERADAMENTE
sin arreglar (`_core.py:1042-1051`) paso a ser FALSO: enumera los tres campos viejos como declarados
y los dos del wire como no declarados, y afirma que la correccion de forma esta ruteada al backlog
`SHAPE-MD-REF-33`. Reemplazarlo por un parrafo que diga:

- que la correccion de forma del modelo se hizo en la Phase 43 bajo el requisito SHAPE-01, contra la
  lectura fresca del wire del 2026-08-31;
- que las filas ahora decodifican POBLADAS con las dos claves reales del wire, en vez de decodificar
  como filas enteramente vacias con tres cadenas en blanco;
- que la ruta `SHAPE-MD-REF-33` del backlog queda CERRADA por esa fase.

Conservar SIN CAMBIO el resto del docstring: la descripcion del sobre y su clave de desenvolvimiento,
la referencia cruzada a `parse_instruments_response`, la mencion del bug S-1 de la Phase 33 con sus
findings, el parrafo de compatibilidad del body de lista pelada y sus guardas de colapso a lista
vacia, el orden body-consume-then-raise, y la nota de que no hay stamp `received_at` (D-05).

**(2) Revisar `parse_instruments_response`** (`:981-1028`) por la misma clase de prosa obsoleta. Si
su docstring afirma algo sobre la forma declarada de `Instrument` que la correccion de 43-01 volvio
falso, corregirlo con el mismo criterio. Si no dice nada sobre campos —es field-agnostic por
diseno—, dejarlo intacto y registrarlo asi en el SUMMARY.

**(3) Medir D-14 y registrar el comando y su salida** para el Task 2. La afirmacion a probar es que
`client.py` y `aio.py` requieren CERO cambios de fuente porque ambas superficies delegan al mismo
objeto funcion. Ejecutar y capturar la salida verbatim de las tres mediciones:
  a. que ninguna de las dos superficies referencia por nombre un campo removido ni uno agregado
     — un grep sobre los nombres de campo de la tabla de disposicion en los dos archivos;
  b. que las dos superficies llaman al MISMO objeto funcion del parser: comparar por identidad
     (`is`) el atributo `parse_segments_response` alcanzado desde cada superficie contra el de
     `_core`, y lo mismo para `parse_instruments_response`;
  c. que los gemelos async de los tests de 43-01 pasan sin ningun cambio de fuente en `aio.py`
     — el diff acumulado de la fase no lista ninguno de los dos archivos.

NO editar `client.py` ni `aio.py`. NO editar `main_market_data.py`.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client -q --no-cov; uv run python -c "from market_data_client import client as c, aio as a, _core; print(c.parse_segments_response is _core.parse_segments_response, a.parse_segments_response is _core.parse_segments_response, c.parse_instruments_response is _core.parse_instruments_response, a.parse_instruments_response is _core.parse_instruments_response)"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q --no-cov` termina con exit code 0.
    - `uv run python -c "from market_data_client import _core; d=_core.parse_segments_response.__doc__; print('DELIBERATELY' in d, 'SHAPE-MD-REF-33' in d)"` imprime `False False` — la prosa obsoleta y la ruta de backlog ya cerrada desaparecieron del docstring.
    - `uv run python -c "from market_data_client import _core; d=_core.parse_segments_response.__doc__; print(all(t in d for t in ('segments', 'received_at')))"` imprime `True` — el resto del docstring sobrevivio.
    - El comando de identidad del bloque `<verify>` imprime `True True True True` (las dos superficies llaman al mismo objeto funcion; D-14 medido, no asumido). Si alguna imprime `False`, D-14 es falso y el plan se detiene: `client.py` y `aio.py` necesitarian cambio y eso excede el alcance lockeado.
    - `git diff --name-only` lista exactamente `packages/market-data-client/src/market_data_client/_core.py`.
    - `git status --porcelain packages/market-data-client/src/market_data_client/client.py packages/market-data-client/src/market_data_client/aio.py main_market_data.py` no produce salida.
  </acceptance_criteria>

  <done>
    El docstring del parser describe la realidad post-fix, el resto de su contenido quedo intacto, y
    la afirmacion D-14 esta respaldada por una medicion de identidad de objeto ejecutada y capturada,
    no por una lectura.
  </done>
</task>

<task type="auto">
  <name>Task 2: Escribir 43-DISPOSITION.md con la tabla campo por campo, la evidencia offline y el item de seguimiento</name>

  <files>.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md, .planning/ROADMAP.md</files>

  <read_first>
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-01-SUMMARY.md` y `43-02-SUMMARY.md` — la disposicion tal como quedo implementada, mas los conteos de test
    - `.planning/ROADMAP.md` seccion "Phase 43" (los 5 Success Criteria) y seccion "## Backlog" (linea 196 en adelante; la entrada `SHAPE-MD-REF-33` y la nota de absorcion por v1.8 que hay que anotar con el resultado)
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md` secciones 2, 3, 4.1 y 5 — el schema medido, la marca NO-AUTORITATIVO de los baselines, la advertencia de que el SHAPE-diff del driver esta inerte para estos dos modelos, y la politica sobre el crudo
    - `.planning/verification/market-data-client-findings.md` — las entradas F-205 a F-242 (Instrument y Segment, sync y async), F-67 a F-71 y F-202 (health feed), F-109 y F-140 (`Symbol.note`)
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md` secciones "Wire medido — verificacion independiente del capture", "Pitfall 1" y "Pitfall 6"
    - `packages/market-data-client/src/market_data_client/models.py:786-830` y `:869-880` y `:1195-1350` — el estado final implementado, para que la tabla describa el codigo y no la intencion
  </read_first>

  <action>
**(1) Crear `43-DISPOSITION.md`** con estas seis secciones, en este orden.

**Seccion 1 — Tabla de disposicion campo por campo (criterio 1).**
Dos tablas, una por modelo, con estas columnas: `Campo`, `Origen` (declarado-hoy / clave-del-wire /
ambos), `Disposicion` (exactamente una de: `alias aditivo` / `remover` / `agregar` / `mantener`),
`Tipo final`, `Evidencia` (FID del ledger o seccion de `42-WIRE-READ.md`), `Decision` (D-XX).

Regla dura de completitud: **cero filas sin disponer**. La union de (campos declarados antes del
cambio) con (claves del wire fresco) tiene que aparecer entera. Para `Instrument` son 12 filas: los
5 declarados antes mas las 10 claves medidas, menos los 3 que estaban en ambos conjuntos.
Para `Segment` son 5 filas: los 3 declarados antes mas las 2 medidas, sin interseccion.
Verificar el conteo antes de dar la tabla por completa.

La fila del alias camelCase de `Instrument` debe decir `alias aditivo` explicitamente, con la nota de
que sigue el precedente D-22 de `Symbol` y que un rename directo esta prohibido en
`REQUIREMENTS.md` seccion Out of Scope.

Agregar una tercera tabla, mas chica, con las 5 claves de HARN-02: `Clave`, `Modelo`, `Tipo final`,
`Nullable si/no`, `Evidencia medida que respalda la nulabilidad o su ausencia`, `Decision`.

**Seccion 2 — Antes/despues medido de `get_segments()` (criterio 2, D-07).**
La demostracion es OFFLINE. Documentar:
  a. el estado ANTES, citado del ledger: las entradas del censo que reportan las claves del wire
     como `extra` sobre `Segment` y los campos declarados como `missing`, en las dos superficies;
  b. el key-set y el CONTEO de filas del capture del 42
     (`.planning/verification/captures/market-data-wire-segments-42.json`), obtenidos leyendo el
     archivo en tiempo de ejecucion de este plan;
  c. el estado DESPUES, con la salida verbatim de una decodificacion ejecutada sobre una fila
     sintetica del key-set medido, mostrando los dos campos poblados;
  d. la referencia al test reproducible que lo pinnea
     (`test_reference_envelope_unwrap.py`, las aserciones de valor agregadas en 43-01) — porque el
     capture es perecedero y el test no.

**RESTRICCION DE SEGURIDAD, ACTIVA (Pitfall 6 / T-42-05):** transcribir a este archivo el key-set y
los conteos, y NUNCA los valores del capture crudo. `.gitignore:53` excluye ese directorio con el
comentario de que son capturas crudas con PII y nunca committeables, y `42-WIRE-READ.md` seccion 5
prohibe transcribir la clave `payload` a git. `43-DISPOSITION.md` SI se commitea, asi que esta
restriccion aplica en su forma mas estricta: nombres de clave y numeros de fila, cero valores.

Registrar tambien, explicitamente, por que el SHAPE-diff del driver NO sirve como evidencia aca: su
sample se deriva tomando el primer elemento solo cuando el body es una lista, y el wire de estos dos
endpoints es un sobre de tipo dict, asi que el diff se saltea en silencio y un "cero findings"
post-fix seria un falso verde. La evidencia real es el censo de divergencias mas el capture.

**Seccion 3 — Criterio 3: ninguna `extra` se convirtio en `missing`.**
Consolidar del `43-02-SUMMARY.md`: la tabla de las 5 claves con el record que cada una produce contra
un payload sano (ninguno) y contra la fixture congelada del 2026-07-31 (solo el campo plano de D-11),
mas la explicacion mecanica de por que: la rama de union del walker retorna sin emitir, y un modelo
anidado no-opcional colapsa al Null Object con sink silencioso. Citar el test que lo prueba de forma
reproducible.

**Seccion 4 — Criterio 5: D-14 medido, no asumido.**
Pegar la salida verbatim de las tres mediciones del Task 1 (grep de nombres de campo sobre las dos
superficies, comparacion por identidad de los objetos funcion de parser, y el diff acumulado de la
fase que no lista ninguna de las dos). Y la confirmacion de que no hubo bump de version: version
actual en los tres sitios, sin cambio.

**Seccion 5 — Item de seguimiento: el dereference del driver.**
Documentar `main_market_data.py:1541-1542` con: que hace (dereferencia dentro de `probe_parity` un
campo de `Segment` que D-06 removio), por que ningun gate estatico lo detecta (`mypy` tiene
`files = ["packages/*/src"]`; el hook de pre-commit usa `files: ^packages/.*/src/`;
`verification/test_main_market_data_deep_chain.py` parsea el driver con `ast` sin importarlo y solo
audita cadenas de acceso profundo de market data), y cual es la consecuencia (el `try/except
Exception` de la linea siguiente lo degrada a un FINDING silencioso de handler en la proxima corrida
en vivo, no a un crash — exactamente el modo de falla que el code review CR-01 de la Phase 37
documento).

Y la DISPOSICION de esta fase, escrita: **no se corrige aca**, porque D-16 lockea el alcance a
`models.py` mas tests mas el docstring de D-14 y este sitio no es ninguno de los tres. Destino
nombrado: entrada de backlog, candidato para la Phase 44 (que ya toca el paquete para el release) o
para la Phase 45. La correccion es de dos lineas, sin logica, y no dispara la regla dual sync/async
de `CLAUDE.md` por ser un dereference.

**Seccion 6 — Items de seguimiento menores.**
El segundo item del `<flagged_followups>` del plan 43-02: `FeedSubscription` quedo en
`models.__all__` pero no en el `__all__` del paquete, con el efecto secundario de que el gate
`surface-types` no escanea sus 15 campos. Candidato para la Phase 44.

**(2) Agregar la entrada de backlog en `.planning/ROADMAP.md`.**
Usar `Edit` con reemplazo acotado, NUNCA `Write` sobre `ROADMAP.md`: un `Write` de archivo completo
destruye todas las entradas de fase fuera de la ventana del diff.

Insertar, dentro de la seccion `## Backlog` (linea 196 en adelante), una entrada nueva con un ID
propio siguiendo la convencion de nombrado de las entradas vecinas (dominio en mayusculas mas numero
de fase), que describa el dereference del driver, cite el archivo y las dos lineas, diga por que
ningun gate lo detecta, y nombre su destino. Y anotar la entrada `SHAPE-MD-REF-33` existente con el
resultado medido: **CERRADA por la Phase 43**, sin borrar su texto historico — el precedente del
propio archivo (la nota de la Q5 resuelta por el plan 42-06) es anotar, nunca reescribir prosa
historica.

**(3) NO tocar** ningun archivo bajo `packages/`, ni `main_market_data.py`, ni
`.planning/verification/`.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; test -f .planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md; wc -l .planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md; grep -c 'main_market_data.py' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `43-DISPOSITION.md` existe y tiene al menos 90 lineas.
    - El archivo contiene las seis cabeceras de seccion descritas en el `<action>`, en ese orden.
    - La tabla de `Instrument` tiene 12 filas de datos y la de `Segment` 5; cada fila tiene exactamente una de las cuatro disposiciones (`alias aditivo`, `remover`, `agregar`, `mantener`) y una celda de evidencia no vacia. Cero filas con la celda de disposicion vacia o con un guion.
    - `grep -c 'alias aditivo' .planning/phases/43-*/43-DISPOSITION.md` devuelve un valor mayor o igual a `1`.
    - La tabla de HARN-02 tiene 5 filas, una por clave.
    - `grep -c 'main_market_data.py' .planning/ROADMAP.md` devuelve un valor mayor o igual a `1` (la entrada de backlog existe).
    - `git diff --stat .planning/ROADMAP.md` muestra lineas agregadas y **cero** entradas de fase existentes borradas: verificar con `git diff .planning/ROADMAP.md | grep -c '^-.*### Phase'` igual a `0`.
    - `git status --porcelain packages/ main_market_data.py .planning/verification/` no produce salida.
    - Ningun valor del capture crudo aparece en `43-DISPOSITION.md`: verificar leyendo el capture y comprobando manualmente que ninguno de sus valores de `segment` ni de `live_instruments` fue transcrito al artefacto. Documentar la verificacion en el SUMMARY.
  </acceptance_criteria>

  <done>
    Existe un artefacto committeado donde un lector puede ver la disposicion de cada campo con su
    evidencia, el antes/despues medido de `get_segments()` sin un solo valor crudo transcrito, la
    prueba de medicion de D-14, y el destino nombrado de los dos items que la fase deja abiertos por
    alcance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate de fase — los 4 jobs de CI verdes y cero bump de version</name>

  <files>.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md</files>

  <read_first>
    - `.github/workflows/ci.yml` — los 4 jobs (`lint`, `pre-commit`, `typecheck`, `test`) y sus pasos exactos; en particular los 9 pasos de `lint`, incluido `uv lock --check`, el paso `surface-types` y la lista explicita de tests de `verification/`
    - `.pre-commit-config.yaml` — los hooks y su alcance
    - `packages/market-data-client/pyproject.toml` (linea 3) y `packages/market-data-client/src/market_data_client/__init__.py` (linea 163) — los dos sitios de version que NO se tocan
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md` — el archivo al que se le agrega la seccion de resultados de gate
  </read_first>

  <action>
Reproducir localmente los 4 jobs de `ci.yml` y registrar el resultado, sin editar ningun archivo de
fuente ni de CI.

**(1) Job `lint`** — ejecutar sus pasos en orden y capturar exit code y salida relevante de cada uno:
`uv lock --check`; `uv run ruff check .`; `uv run ruff format --check .`; `uv run lint-imports`;
el grep de LOG-01 sobre `packages/*/src/` (que debe NO encontrar coincidencias);
`uv run python tools/check_decode_intactness.py`;
`uv run python tools/check_uniform_structure.py`;
`uv run python tools/check_surface_types.py`;
y la lista explicita de tests de `verification/` que el job enumera —copiarla verbatim de `ci.yml`,
no reconstruirla de memoria ni sustituirla por `pytest verification/`, que arrastra fallas
pre-existentes ajenas a este paquete.

Registrar el delta del contador de `check_surface_types.py` respecto del baseline medido de 442
campos escaneados. El gate NO asserta un piso numerico —su unica anti-vacuidad es que definiciones y
campos no sean ambos cero—, asi que el delta es informativo. Confirmar que el numero de violaciones
es `0`.

**(2) Job `pre-commit`** — `uv run pre-commit run --all-files`.

**(3) Job `typecheck`** — `uv run mypy` (src global) y `uv run mypy packages/<pkg>/tests` para los
seis paquetes, en el mismo bucle que el job.

**(4) Job `test`** — `uv run pytest packages/market-data-client -q` con cobertura (sin `--no-cov`),
y ademas la suite completa `uv run pytest -q` para confirmar que ningun otro paquete regresiono.
Registrar el conteo de tests antes (711 medido en la investigacion) y despues.

**(5) Gate de no-publicacion (D-16).** Verificar los tres sitios de version:
`packages/market-data-client/pyproject.toml`, el `__version__` de
`packages/market-data-client/src/market_data_client/__init__.py`, y `uv.lock`. Los tres deben estar
en el valor de entrada de la fase, sin cambio. Verificar tambien que el diff acumulado de la fase no
lista `README.md` ni ningun archivo de release.

**(6) Gate de no-regresion del harness.** Confirmar que `verification/test_cycle_closure_phase33.py`
sigue verde: sus pisos son cotas INFERIORES sobre un ledger append-only, asi que un fix de forma no
puede reddenearlas — si se ponen rojas, alguien borro findings del ledger.

**(7) Escribir los resultados** en una seccion nueva al final de `43-DISPOSITION.md`, con una tabla
de `Job` / `Paso` / `Comando` / `Exit code` / `Nota`. Es la evidencia del criterio 5. Usar `Edit`
sobre el archivo existente, no `Write`.

**Si algun gate queda rojo:** NO relajar la asercion, NO renombrar el test, NO ensanchar la tabla de
exenciones de `check_surface_types.py` (su propio comentario de bloque advierte que la remedia a un
rojo es tipar el campo, nunca ensanchar la tabla). Detener el plan y reportar el fallo con su salida.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv lock --check; uv run ruff check .; uv run ruff format --check .; uv run lint-imports; uv run python tools/check_decode_intactness.py; uv run python tools/check_uniform_structure.py; uv run python tools/check_surface_types.py; uv run mypy; uv run mypy packages/market-data-client/tests; uv run pytest -q</automated>
  </verify>

  <acceptance_criteria>
    - Los nueve pasos del job `lint` terminan con exit code 0; `check_surface_types.py` reporta `0 violations`.
    - `uv run pre-commit run --all-files` termina con exit code 0.
    - `uv run mypy` y los seis `uv run mypy packages/<pkg>/tests` terminan con exit code 0.
    - `uv run pytest -q` (suite completa del monorepo) termina con exit code 0, y `uv run pytest packages/market-data-client -q --no-cov` reporta un conteo mayor o igual a 711.
    - `uv run python -c "import market_data_client; print(market_data_client.__version__)"` imprime `0.6.0`.
    - `grep -c '^version = "0.6.0"' packages/market-data-client/pyproject.toml` devuelve `1`.
    - `git status --porcelain uv.lock` no produce salida, y `uv lock --check` termina con exit code 0.
    - `git diff --name-only` acumulado de la fase NO lista: `README.md`, `packages/market-data-client/pyproject.toml`, `packages/market-data-client/src/market_data_client/__init__.py`, `uv.lock`, `packages/market-data-client/src/market_data_client/client.py`, `packages/market-data-client/src/market_data_client/aio.py`, `main_market_data.py`, `.github/workflows/release.yml`, ni ningun archivo bajo `packages/matriz-client/`.
    - `uv run pytest verification/test_cycle_closure_phase33.py -q --no-cov` termina con exit code 0.
    - `43-DISPOSITION.md` contiene la tabla de resultados de gate con una fila por paso y su exit code.
  </acceptance_criteria>

  <done>
    Los 4 jobs de CI estan verificados verdes localmente con su evidencia registrada, la fase no
    bumpeo version en ninguno de los tres sitios, ningun archivo de otro paquete fue tocado, y el
    piso del ledger de findings sigue en pie.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts que este plan produce

**Archivo nuevo:**
- `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md`
  — seis secciones: tabla de disposicion de `Instrument` (12 filas), tabla de `Segment` (5 filas),
  tabla de las 5 claves de HARN-02, antes/despues medido de `get_segments()`, evidencia del criterio
  3, medicion de D-14, item de seguimiento del driver, items menores, y la tabla de resultados de los
  4 jobs de CI.

**Prosa de fuente reconciliada:**
- docstring de `_core.py::parse_segments_response` — el parrafo que declaraba el fix diferido

**Entrada de backlog nueva en `.planning/ROADMAP.md`:** el dereference del driver, con ID propio y
destino nombrado. Mas la anotacion de resultado sobre la entrada `SHAPE-MD-REF-33` existente (texto
historico conservado verbatim).

**Ningun simbolo de codigo nuevo.** Ninguna dependencia externa. Ningun bump de version.
</artifacts_this_phase_produces>

<flagged_followups>
## Seguimiento marcado — NO se corrige en esta fase

1. **`main_market_data.py:1541-1542`** — dereferencia el campo de `Segment` que D-06 removio, fuera
   de mypy, de pre-commit y del lock AST del driver, envuelto en un `try/except Exception` que lo
   degrada a un FINDING silencioso de handler. **Disposicion: no se corrige aca** (D-16 lockea el
   alcance). Se documenta en `43-DISPOSITION.md` seccion 5 y se registra como entrada de backlog en
   `ROADMAP.md` con destino nombrado (candidato Phase 44 o 45). Correccion estimada: 2 lineas, sin
   logica, sin obligacion de espejo sync/async.

2. **`FeedSubscription` ausente del `__all__` del paquete** (`__init__.py`) — presente solo en
   `models.__all__`. Efecto: `check_surface_types.py` resuelve candidatos desde el `__all__` de cada
   `__init__.py`, asi que los 15 campos de la clase nueva no quedan escaneados por el gate.
   **Disposicion: no se corrige aca** (D-16). Candidato para la Phase 44, que ya toca `__init__.py`
   para el bump de version.
</flagged_followups>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture crudo gitignored -> artefacto committeado (`43-DISPOSITION.md`) | El capture puede contener PII; el artefacto SI se commitea. Es el unico cruce de frontera activo de este plan |
| ledger append-only de findings -> prosa del artefacto | El ledger es committeado y es la fuente de evidencia citable |
| `ROADMAP.md` existente -> edicion del plan | Un reemplazo de archivo completo destruiria entradas de fase fuera de la ventana del diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-11 | Information Disclosure | transcripcion del capture a `43-DISPOSITION.md` | mitigate | Solo key-set y conteos de fila cruzan a git; cero valores. `.gitignore:53` y `42-WIRE-READ.md` seccion 5 (T-42-05) lo prohiben. Verificado por el criterio de aceptacion de comparacion manual capture-vs-artefacto del Task 2 |
| T-43-12 | Repudiation | prosa historica de `ROADMAP.md` | mitigate | El texto historico de `SHAPE-MD-REF-33` se ANOTA con el resultado medido, nunca se reescribe — mismo precedente que la nota de Q5 del plan 42-06. Un texto historico reescrito borra la procedencia con la que se planifico la fase |
| T-43-13 | Tampering | edicion de `ROADMAP.md` con reemplazo de archivo completo | mitigate | Obligatorio usar `Edit` acotado. Verificado por el criterio `git diff .planning/ROADMAP.md \| grep -c '^-.*### Phase'` igual a `0` |
| T-43-14 | Elevation of Privilege | relajar un gate para pasarlo | mitigate | Prohibido explicitamente en el Task 3: no relajar aserciones, no renombrar tests, no ensanchar la tabla de exenciones de `check_surface_types.py`. Un rojo detiene el plan |
| T-43-15 | Tampering | publicacion accidental | mitigate | Gate de no-publicacion explicito: los tres sitios de version verificados sin cambio, `uv.lock` sin modificar, `release.yml` y `README.md` ausentes del diff acumulado |
| T-43-SC | Tampering | instalaciones npm/pip/cargo | accept | N/A verificado: cero paquetes externos instalados en toda la fase. `43-RESEARCH.md` seccion "Package Legitimacy Audit" lo registra; `uv lock --check` queda verde por no-op |
</threat_model>

<verification>
Cierre de fase. Los seis criterios de exito del ROADMAP quedan cubiertos asi:

1. **Tabla de disposicion campo por campo** — `43-DISPOSITION.md` seccion 1, con cero filas sin
   disponer y el alias camelCase de `Instrument` dispuesto explicitamente como `alias aditivo`.
2. **`get_segments()` devuelve filas pobladas, demostrado con la medicion** — `43-DISPOSITION.md`
   seccion 2 (offline, contra el capture del 42 y el censo) mas las aserciones de valor
   reproducibles de `test_reference_envelope_unwrap.py` (plan 43-01).
3. **Las 5 claves tipadas sin flip `extra` hacia `missing`** — `43-DISPOSITION.md` seccion 3, con el
   test de cero divergencias del plan 43-02 como prueba reproducible.
4. **Fixtures re-derivadas con asercion de subconjunto** — `test_every_fixture_key_is_a_measured_wire_key`
   (plan 43-02) mas la evidencia de que ningun test fue renombrado (planes 43-01 y 43-02).
5. **4 gates verdes, D-14 medido, sin bump ni publicacion** — `43-DISPOSITION.md` secciones 4 y 6,
   Task 3 de este plan.

Mas los dos items de seguimiento con destino nombrado.
</verification>

<success_criteria>
- El docstring de `parse_segments_response` describe la realidad post-fix y ya no rutea al backlog
  ya cerrado.
- D-14 esta respaldado por una comparacion de identidad de objeto ejecutada, no por una lectura.
- `43-DISPOSITION.md` existe, tiene las seis secciones, y sus tres tablas no tienen ninguna fila sin
  disponer.
- Ningun valor del capture crudo fue transcrito a git.
- `ROADMAP.md` gano una entrada de backlog con destino nombrado y no perdio ninguna entrada de fase.
- Los 4 jobs de CI estan verdes localmente con evidencia registrada.
- La version del paquete sigue en su valor de entrada en los tres sitios y `uv.lock` no fue tocado.
</success_criteria>

<output>
Create `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-03-SUMMARY.md` when done.

El SUMMARY debe incluir, ademas del formato estandar:
- La salida verbatim de la comparacion de identidad de D-14.
- La tabla de resultados de los 4 jobs con exit codes.
- La confirmacion explicita de la verificacion capture-vs-artefacto (ningun valor crudo transcrito).
- El conteo de tests antes y despues de la fase completa.
</output>
