---
phase: 45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - verification/test_main_matriz_skip_line_shape.py
  - verification/test_matriz_sweep_snapshot.py
  - verification/test_main_matriz_login_fail_uniformity.py
  - .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md
autonomous: true
requirements: [HARN-04]

must_haves:
  truths:
    - "Existe un documento FECHADO que decide el destino de los 2 archivos rotos de `verification/` de matriz, con los tres ítems que D-08 exige respondidos POR ARCHIVO."
    - "El rol de canario de `probe_context` queda transferido de verdad: el documento nombra el enrolamiento en CI de `verification/test_probe_context_coverage.py`, no sólo el archivo (D-08 ENMENDADA)."
    - "Los 3 tests que hoy pasan dentro de esos archivos están nombrados uno por uno con su destino; ninguno se pierde sin dar cuenta."
    - "La única aserción que ningún test enrolado cubría (`probe_login_sync` devuelve FINDING, no FAIL) queda cerrada dentro de un archivo YA enrolado, o descartada por escrito (Q4)."
    - "Los ~35 archivos `verification/` restantes quedan re-declarados por escrito como inertes y fuera de alcance de v1.8 (D-10), con el censo medido hoy."
  artifacts:
    - path: ".planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md"
      provides: "Decisión escrita y fechada de HARN-04 (criterio 3 del ROADMAP)"
      min_lines: 60
      contains: "test_probe_context_coverage.py"
    - path: "verification/test_main_matriz_skip_line_shape.py"
      provides: "Cierre de Q4: lock de taxonomía de retorno de `probe_login_sync` dentro de un archivo ya enrolado en CI"
    - path: "verification/test_matriz_sweep_snapshot.py"
      provides: "Puntero al documento de decisión (aceptar deuda no implica borrar)"
    - path: "verification/test_main_matriz_login_fail_uniformity.py"
      provides: "Puntero al documento de decisión"
  key_links:
    - from: "45-HARN-04-DECISION.md"
      to: ".github/workflows/ci.yml (edit consolidado del plan 45-05)"
      via: "la transferencia del canario se declara nombrando el enrolamiento que aterriza en 45-05"
      pattern: "45-05"
    - from: "verification/test_main_matriz_skip_line_shape.py"
      to: "main_matriz.py::probe_login_sync"
      via: "lock por AST sobre el handler de `AuthenticationError`"
      pattern: "login_sync"
---

<objective>
Cerrar **HARN-04** por escrito: aceptar formalmente como deuda documentada los dos archivos de `verification/` de matriz rotos desde la Phase 15 (D-08), con los tres ítems que la decisión exige respondidos por archivo y con la evidencia medida pegada; y cerrar la única fila de deuda que la investigación encontró REAL (Q4) dentro de un archivo que ya corre en CI.

Purpose: el criterio 3 del ROADMAP pide una decisión "escrita y fechada"; `PITFALLS.md` Pitfall 12 advierte que HARN-04 se lee como trabajo y crece. Es redacción sobre evidencia ya medida (`45-RESEARCH.md` Hallazgos 7-10), más un lock chico. No es una reparación: la reparación está explícitamente diferida.
Output: `45-HARN-04-DECISION.md` fechado, un test nuevo dentro de `verification/test_main_matriz_skip_line_shape.py`, y un puntero al documento dentro de los 2 archivos aceptados como deuda.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-CONTEXT.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-RESEARCH.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Q4): lock de taxonomía de `probe_login_sync` dentro de `verification/test_main_matriz_skip_line_shape.py` (archivo YA enrolado en CI)</name>
  <files>verification/test_main_matriz_skip_line_shape.py</files>
  <read_first>
    - `verification/test_main_matriz_skip_line_shape.py` (completo, 268 líneas) — docstring de dos capas, helpers `_driver_tree()` / `_module_str_constants()`, y el estilo de mensaje de assert
    - `main_matriz.py` líneas 775-830 (`probe_login_sync` completo: handler de `AuthenticationError` que devuelve `"FINDING"` con el comentario Phase 11 CR-02, handler residual, y el retorno `"PASS"` del camino feliz)
    - `.planning/phases/45-.../45-RESEARCH.md` Hallazgo 9 (la aserción única no cubierta) y Open Question Q4
  </read_first>
  <behavior>
    - Sobre `main_matriz.py`, hay exactamente 3 llamadas a `ProbeResult` cuyo primer argumento posicional es `"login_sync"`, y sus statuses son `"FINDING"`, `"FINDING"` y `"PASS"`.
    - El handler de `AuthenticationError` dentro de `probe_login_sync` devuelve un `ProbeResult` con status `"FINDING"` (nunca `"FAIL"`) — la uniformidad de taxonomía que fijó CR-02 de la Phase 11.
    - Si aparece una cuarta call site, el test se pone rojo: un status nuevo en ese probe es una decisión de taxonomía que merece triage, no un cambio silencioso.
  </behavior>
  <action>
    Agregar a `verification/test_main_matriz_skip_line_shape.py` una tercera capa: una función de test que parsee el driver con el `_driver_tree()` que el archivo ya tiene y assevere la taxonomía de retorno de `probe_login_sync`.

    Forma concreta: recorrer el AST buscando `ast.Call` cuyo callee sea el `Name` `ProbeResult` y cuyo primer argumento posicional sea una `ast.Constant` de string igual a `login_sync`; recolectar el segundo argumento posicional de cada uno. Assertar (i) que se encontraron exactamente 3 —piso de no-vacuidad y techo de triage—, (ii) que el conjunto de statuses recolectados es exactamente el de dos valores `FINDING` y `PASS`, y (iii) —la aserción específica de CR-02— que dentro de la `ast.FunctionDef` de `probe_login_sync`, el `ast.ExceptHandler` cuyo tipo es el `Name` `AuthenticationError` retorna un `ProbeResult` cuyo status es `FINDING`.

    **Hacerlo por AST, no por substring del fuente.** Un `assert "<literal>" not in source` es auto-invalidante: el propio docstring del test menciona el literal prohibido, queda escrito en el archivo, y el assert se cae solo. El repo ya documenta exactamente este razonamiento en `test_no_substring_membership_check_over_a_host_literal` de este mismo archivo ("el comentario que documenta POR QUÉ el chequeo viejo era inseguro cita su código, y un grep sobre el fuente no distingue la cita del código vivo").

    Extender también el docstring de módulo con un párrafo de "Capa 3" que declare el alcance nuevo y su procedencia: cierra la fila de deuda de `verification/test_main_matriz_login_fail_uniformity.py` identificada por `45-RESEARCH.md` Hallazgo 9 —la única aserción de los 2 archivos rotos que ningún test enrolado cubría— dentro de un archivo que YA corre en CI, sin enrolar ningún archivo nuevo ni reparar nada. Dejar el docstring diciendo "dos capas" mientras el archivo tiene tres sería la misma clase de mentira de alcance que esta fase existe para cerrar.

    Los mensajes de assert deben nombrar la consecuencia, siguiendo el estilo del archivo: si `probe_login_sync` volviera a `FAIL`, `main_verify.py` clasificaría ese probe distinto y la taxonomía uniforme que fijó CR-02 se rompería sin que ningún gate lo note.
  </action>
  <acceptance_criteria>
    - `uv run pytest -q verification/test_main_matriz_skip_line_shape.py` → todos los tests del archivo pasan y el conteo subió exactamente en 1 respecto de la línea base (registrar ambos conteos en el SUMMARY).
    - `grep -c 'login_sync' verification/test_main_matriz_skip_line_shape.py` ≥ 1.
    - El docstring de módulo describe tres capas (leerlo y confirmarlo en el SUMMARY).
    - No-vacuidad demostrada: cambiar temporalmente el status del handler de `AuthenticationError` en `main_matriz.py` por otro valor, correr el test nuevo, pegar la salida ROJA en el SUMMARY y restaurar; `git diff main_matriz.py` queda sin esa reversión.
    - `uv run ruff check verification/test_main_matriz_skip_line_shape.py && uv run ruff format --check verification/test_main_matriz_skip_line_shape.py` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q verification/test_main_matriz_skip_line_shape.py && uv run ruff check verification/test_main_matriz_skip_line_shape.py && uv run ruff format --check verification/test_main_matriz_skip_line_shape.py</automated>
  </verify>
  <done>Q4 queda cerrado por implementación: la taxonomía de `probe_login_sync` tiene guardián dentro de un archivo ya enrolado en CI, con su no-vacuidad demostrada y el docstring del archivo actualizado a tres capas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: escribir `45-HARN-04-DECISION.md` (fechado) y dejar un puntero dentro de los 2 archivos aceptados como deuda</name>
  <files>.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md, verification/test_matriz_sweep_snapshot.py, verification/test_main_matriz_login_fail_uniformity.py</files>
  <read_first>
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-DISPOSITION.md` (al menos su encabezado y las primeras 40 líneas) — el analog de formato: fecha + requisitos + planes en el header, y la declaración de que todo lo afirmado está medido en la corrida del plan
    - `.planning/phases/45-.../45-RESEARCH.md` Hallazgos 7, 8, 9, 10 y 11 (los 3 tests verdes por nombre, la supersession auto-declarada in-code, la aserción única del archivo de login, la ausencia de canario entre los enrolados, y el censo 53/13/40)
    - `.planning/phases/45-.../45-PATTERNS.md` sección `45-HARN-04-DECISION.md (nuevo, artefacto de decisión) — D-08`, que trae la tabla de contenido mínimo lockeado
    - `verification/test_matriz_sweep_snapshot.py` docstring del test verde `test_matriz_risk_probes_unwrap_their_envelope_key` (contiene la cita verbatim de supersession que el documento debe reproducir) y el docstring de módulo de `verification/test_main_matriz_login_fail_uniformity.py`
    - `verification/test_probe_context_coverage.py` líneas 1-35 (su docstring, incluida la frase de alcance que declara que `verification/` no corre en CI)
  </read_first>
  <action>
    Antes de escribir, MEDIR y pegar (todo lo que el documento afirme tiene que venir de una corrida de esta tarea, nunca heredado de un reporte):
    - `uv run pytest -q verification/test_matriz_sweep_snapshot.py verification/test_main_matriz_login_fail_uniformity.py` — el estado rojo pre-existente (research midió 19 failed / 3 passed / 19 errors).
    - `uv run pytest -q verification/test_probe_context_coverage.py` — el transferee del canario (research midió 6 passed; PATTERNS lo reconfirmó).
    - `ls verification/test_*.py | wc -l` y el conteo de líneas de allowlist del job `lint` de `.github/workflows/ci.yml` — el censo de HOY, antes de que el plan 45-05 lo mueva (research midió 53 en disco / 13 enrolados / 40 inertes, que corrige el 52/12/40 heredado de `41-ROLLUP.md`).

    Crear `.planning/phases/45-.../45-HARN-04-DECISION.md` con encabezado fechado al estilo de `43-DISPOSITION.md` (fecha, requisito `HARN-04`, planes de la fase) y estas secciones, todas obligatorias:

    **1. Decisión.** Aceptar como deuda formalmente documentada, NO reparar, `verification/test_matriz_sweep_snapshot.py` y `verification/test_main_matriz_login_fail_uniformity.py`; causa raíz única: llaman a los probes de `main_matriz.py` sin el argumento `client`, firma pre-migración REFAC-05 de la Phase 15. Decir explícitamente que aceptar la deuda **no implica borrar**: los 2 archivos quedan en disco con un puntero a este documento, y por eso los 3 tests verdes no se pierden.

    **2. Tabla por archivo con los TRES ítems de D-08.** Una fila por archivo, tres celdas cada una:
    - (1) *¿asevera algo que ningún test enrolado en CI asevera?* — para `test_matriz_sweep_snapshot.py`: **no**, y citar **verbatim** la frase del docstring del propio test verde que declara su supersession por `verification/test_main_matriz_risk_envelope_keys.py` ("que además corre en CI"), nombrando que ese archivo está enrolado en el job `lint`. Para `test_main_matriz_login_fail_uniformity.py`: **sí, una** —`probe_login_sync` devuelve `FINDING` y no `FAIL`—, con la aclaración de que la conducta está PRESENTE y verificada en HEAD (`main_matriz.py`, handler de `AuthenticationError` de `probe_login_sync`), así que lo que se acepta es la ausencia de guardián de regresión, no un defecto abierto; y decir que esa fila **quedó cerrada** por la Task 1 de este plan, nombrando el test que la cierra y el archivo ya enrolado donde vive (cierre de Q4 por escrito, tal como exige `45-CONTEXT.md`).
    - (2) *rol de canario de `probe_context`* — **TRANSFERIDO**, a `verification/test_probe_context_coverage.py`, **enrolado en el allowlist de CI en esta misma fase por el edit consolidado del plan 45-05 (D-11)**. Nombrar el enrolamiento explícitamente y pegar el `6 passed` medido arriba: `45-RESEARCH.md` Hallazgo 10 dice literalmente que transferir el rol a un archivo que también es inerte no es una transferencia sino renombrar el abandono, y D-08 ENMENDADA lo resuelve con enrolamiento real.
    - (3) *los 3 tests que hoy pasan* — nombrarlos uno por uno (los tres están en `test_matriz_sweep_snapshot.py`; el archivo de login tiene **cero** verdes) y dar el destino de cada uno: dos están subsumidos por capas estrictamente más fuertes del test enrolado `test_main_matriz_risk_envelope_keys.py`, y el tercero asevera la consistencia interna de una tabla de fixtures que vive dentro del propio archivo, así que su pérdida costaría cero cobertura de producción — y de todos modos no se pierde, porque el archivo queda en disco.

    **3. Alcance no reparado, con su razón.** Reparar significaría re-derivar expectativas mockeadas para conducta ya verificada EN VIVO contra el vendor real a lo largo de 4 milestones (Phases 33/35/37/39): evidencia más débil reemplazando evidencia más fuerte. Declarar que la rama "reparar" queda diferida y que, si algún día se toma, necesita presupuesto declarado por adelantado (la estimación heredada de "38 firmas de argumento" **no** fue re-medida en esta fase y debe re-medirse antes de planificarla).

    **4. Q5 — gap de gate de mypy sobre los drivers de la raíz.** Declarar por escrito, con la medición pegada: `uv run mypy` apuntado a mano a un driver de la raíz levanta errores reales (fue así como se detectó `DRV-MD-SEG-43`), pero el `files` de mypy del root lista seis rutas `packages/*/src`, el hook de pre-commit está scoped a `packages/*/src`, y el lock de deep-chain de market-data parsea el driver por AST sin importarlo: ningún gate de CI mira los 5 drivers de la raíz. Esta fase **arregla el sitio y no cierra el gate** —apuntar mypy a 5 archivos de miles de líneas dentro de una fase de limpieza es scope creep de tamaño no medido— y nombra el destino: entrada de backlog v1.9, que el plan 45-05 agrega al ROADMAP.

    **5. Censo de `verification/` y re-declaración de D-10.** Pegar el censo medido hoy (en disco / enrolados / inertes) y decir que corrige el 52/12/40 heredado de `41-ROLLUP.md`. Declarar por escrito que sólo se enrolan los archivos que HARN-01/03/04 tocan directamente —`test_public_surface.py`, `test_finding_count_consistency.py`, `test_findings_dedupe_by_title.py`, el archivo nuevo de falsificación `verification/test_drift_dedupe_falsification.py` y `test_probe_context_coverage.py`— y que los ~35 restantes **siguen inertes y fuera de alcance de v1.8**, con la razón: enrolar `verification/` en bloque convertiría una limpieza acotada en un yak-shave de alcance no medido, y la "declaración inerte" de la Phase 41 queda satisfecha por esta re-declaración explícita, no por enrolamiento total. Decir también que los 2 archivos rotos **no** se enrolan (D-10 lo condiciona a que D-08 se revirtiera a "reparar", y no se revirtió).

    **6. Dos límites de alcance que la fase declara en vez de silenciar.**
    - Enrolar `test_public_surface.py` cierra `IN-06` literalmente pero es una red **parcial**: sus snapshots cubren 4 de los 6 paquetes y no incluyen `market-data-client` ni `wallets-client`. Un lector futuro no debe leer "public surface enrolado en CI" como cobertura total.
    - Las **4 ramas hermanas** `missing assumed key …` de `main_iol.py` comparten el mismo hazard de título key-scoped que los 7 sitios corregidos, y D-02 las dejó fuera a propósito. Se declaran acá como el mismo hazard, fuera de alcance de esta fase.

    Después de escribir el documento, agregar en el docstring de módulo de `verification/test_matriz_sweep_snapshot.py` y de `verification/test_main_matriz_login_fail_uniformity.py` un párrafo corto que diga que el archivo está aceptado como deuda documentada, con la ruta de este documento y su fecha. Sólo el docstring: **no** tocar el código de esos archivos, no intentar arreglarlos y no borrarlos.
  </action>
  <acceptance_criteria>
    - El archivo `.planning/phases/45-.../45-HARN-04-DECISION.md` existe, su encabezado lleva fecha y las 6 secciones están presentes.
    - `grep -c 'test_probe_context_coverage.py' .../45-HARN-04-DECISION.md` ≥ 1 **y** el documento nombra el enrolamiento en `ci.yml` por el plan 45-05 (`grep -c '45-05' .../45-HARN-04-DECISION.md` ≥ 1).
    - Los 3 tests verdes están nombrados individualmente en el documento (contarlos al leer y confirmarlo en el SUMMARY).
    - Las tres mediciones (rojo pre-existente de los 2 archivos, `6 passed` del canario, censo en disco/enrolados/inertes) están pegadas con su comando y su salida.
    - `grep -c '45-HARN-04-DECISION' verification/test_matriz_sweep_snapshot.py` = 1 y lo mismo para `verification/test_main_matriz_login_fail_uniformity.py`.
    - `uv run pytest -q verification/test_matriz_sweep_snapshot.py verification/test_main_matriz_login_fail_uniformity.py` produce el MISMO conteo de failed/passed/errors que antes del edit (el puntero es sólo docstring; no repara ni empeora nada) — pegar ambos conteos.
    - `uv run ruff check . && uv run ruff format --check .` pasa.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md && grep -c 'test_probe_context_coverage.py' .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <done>La decisión de HARN-04 está escrita, fechada y sostenida por mediciones de esta corrida; los tres ítems de D-08 están respondidos por archivo; la transferencia del canario nombra su enrolamiento; Q4 y Q5 quedan cerrados por escrito; el censo y los dos límites de alcance están re-declarados; y los 2 archivos aceptados como deuda apuntan al documento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documento de decisión ↔ estado real del repo | Un documento que afirme una transferencia sin enrolamiento sería un renombre del abandono; la mitigación es que las afirmaciones se midan en la corrida |
| red / credenciales | Ninguna: este plan lee tests, no corre drivers en vivo, no toca ningún `.env` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-15 | Repudiation | afirmación "canario transferido" | mitigate | El documento debe nombrar el enrolamiento concreto (archivo + plan 45-05 + `ci.yml`), y el criterio de aceptación lo grepea |
| T-45-16 | Repudiation | los 3 tests verdes "desaparecidos" | mitigate | Nombrados uno por uno con destino; los archivos quedan en disco con puntero al documento — sin `git rm` |
| T-45-17 | Tampering | los 2 archivos rotos | mitigate | Sólo se edita su docstring; el criterio compara el conteo failed/passed/errors antes y después |
| T-45-18 | Information Disclosure | contenido del documento | accept | Sólo cita nombres de test, fids y rutas del repo; ninguna credencial ni base URL de vendor |
| T-45-SC | Tampering | instalación de paquetes | accept | N/A — sin dependencias nuevas |
</threat_model>

<verification>
- `uv run pytest -q verification/test_main_matriz_skip_line_shape.py` → verde, con un test más que la línea base.
- `uv run pytest -q verification/test_probe_context_coverage.py` → verde (precondición del enrolamiento que hará 45-05).
- `uv run ruff check . && uv run ruff format --check .` → limpio.
- El documento de decisión existe, tiene fecha y sus afirmaciones tienen comando + salida pegados.
</verification>

<success_criteria>
1. HARN-04 tiene decisión escrita y fechada, con los tres ítems de D-08 respondidos por archivo y evidencia medida en esta corrida.
2. La transferencia del canario está declarada nombrando el enrolamiento, no sólo el archivo.
3. Q4 queda cerrado por implementación dentro de un archivo ya enrolado, y Q5 declarado con destino v1.9 nombrado.
4. El censo de `verification/` y los dos límites de alcance (red parcial de public surface, 4 ramas hermanas de iol) quedan re-declarados por escrito.
</success_criteria>

<output>
Create `.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-04-SUMMARY.md` when done.
Debe incluir: los conteos de tests antes/después en `test_main_matriz_skip_line_shape.py`, la salida ROJA de la demostración de no-vacuidad de la Task 1, y las tres mediciones pegadas en el documento de decisión.
</output>
