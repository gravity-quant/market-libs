---
phase: 24-release-prep-publish-v0-1-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - CLAUDE.md
  - .claude/projects/-Users-admin-development-market-libs/memory/MEMORY.md
  - .claude/projects/-Users-admin-development-market-libs/memory/market-data-client-v0.1.0-published.md
autonomous: true
requirements: [PUB-MD-01]

must_haves:
  truths:
    - "The CI test matrix runs market-data-client (2 jobs: py3.12 + py3.13) — implements PUB-MD-01, D-01"
    - "CLAUDE.md documents market-data-client as the 6th monorepo package (list + count + component table) — D-04"
    - "MEMORY index references the published market-data-client v0.1.0 package — D-05"
    - "uv.lock has zero drift and the package version is aligned at 0.1.0 across pyproject + __version__ — D-03, D-11, SC-1"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "market-data-client entry in matrix.package"
      contains: "- market-data-client"
    - path: "CLAUDE.md"
      provides: "6th-package documentation (workspace bullet, count, component row)"
      contains: "market-data-client"
    - path: ".claude/projects/-Users-admin-development-market-libs/memory/MEMORY.md"
      provides: "published-package index pointer"
      contains: "market-data-client"
    - path: ".claude/projects/-Users-admin-development-market-libs/memory/market-data-client-v0.1.0-published.md"
      provides: "MEMORY pointer file for the published package"
      contains: "metadata"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/market-data-client"
      via: "matrix.package entry drives `pytest packages/market-data-client` with coverage in the test job"
      pattern: "market-data-client"
---

<objective>
Prepare the monorepo to publish `market-data-client v0.1.0` through the existing per-package
pipeline. This plan performs the autonomous, reversible pre-release edits and validations:
add the package to the CI test matrix (D-01), align the monorepo docs (CLAUDE.md + MEMORY,
D-04/D-05), and validate that the lockfile and version metadata are already correct
(D-03/D-11/SC-1). No functional package code changes.

Purpose: Land every edit/validation the release PR needs so that when the PR opens (Plan 02),
CI includes the new package and the docs reflect the 6th package.
Output: Edited ci.yml, CLAUDE.md, MEMORY index + new pointer file; validated uv.lock, root
pyproject workspace member, and package version alignment.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-release-prep-publish-v0-1-0/24-CONTEXT.md
@.planning/phases/24-release-prep-publish-v0-1-0/24-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This phase (Plan 01 portion) creates/emits — NO new code symbols:
- New `market-data-client` entry in `matrix.package` in `.github/workflows/ci.yml` (D-01).
- New CLAUDE.md Workspace Structure bullet for `market-data-client` v0.1.0 (D-04).
- Updated CLAUDE.md CI/CD count line: `5 packages` → `6 packages` (D-04).
- New CLAUDE.md Component Responsibilities table row `market_data_client` + updated `<pkg>.aio`
  and `<pkg>.models` meta-rows to acknowledge the 6th package (D-04).
- New MEMORY index bullet + companion pointer file
  `market-data-client-v0.1.0-published.md` (D-05).
(Plan 02 produces the git tag `market-data-client-v0.1.0` and the GitHub Release.)
</artifacts_this_phase_produces>

<scope_decisions>
Conscious deferrals surfaced from 24-PATTERNS.md "Watch-outs" — recorded here so the executor
and verifier see the decision rather than treating it as an accidental gap. D-01 states the CI
matrix edit is "el único cambio necesario en workflow"; D-11 limits the root pyproject change to
the workspace-member add. The following package-enumeration surfaces are therefore INTENTIONALLY
NOT edited in this phase:
- Global mypy `files` (`pyproject.toml:97`) — does not list `market-data-client/src`. Skipping it
  is a typecheck COVERAGE gap, not a CI failure (the job stays green). Out of D-01/D-11 scope.
- `importlinter` `root_packages` (`pyproject.toml:141-146`) — 4-package list, unchanged. Out of scope.
- CI `typecheck` per-package tests-mypy loop (`ci.yml:85`) — hardcoded 5-package list, unchanged.
  The new package's TESTS skip the CI mypy gate, but its `pytest`+coverage DOES run via the
  matrix edit (D-01), so SC-3 ("los checks incluyendo el nuevo paquete") is satisfied and CI
  stays green. Out of D-01 scope.
Rationale: none of these break the CI gate for the new package; full typecheck/import-linter
parity is a scope expansion beyond CONTEXT D-01/D-11. Do not edit unless the user reopens scope.
</scope_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Add market-data-client to the CI test matrix (D-01)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (lines 80-105 — the `typecheck` per-package loop and the
      `test:` job `matrix.package` list)
    - .planning/phases/24-release-prep-publish-v0-1-0/24-PATTERNS.md (section 1 — exact current
      content and required list-append form)
  </read_first>
  <action>
    Append a single list item `market-data-client` to the `matrix.package` list in the `test:`
    job of `.github/workflows/ci.yml` (currently the five entries higyrus-client, wallets-client,
    matriz-client, iol-client, ambito-financiero-client). Match the existing 2-space list indent
    and `- ` bullet exactly; append at the end of the list per D-01. Do NOT touch any other block
    in ci.yml — in particular leave the `typecheck` per-package mypy loop (`ci.yml:85`) and its
    5-package list unchanged, per D-01 and the `<scope_decisions>` above. This is the ONLY
    workflow-file edit in the entire phase (release.yml stays untouched per D-02).
  </action>
  <verify>
    <automated>grep -c '^          - market-data-client$' .github/workflows/ci.yml | grep -qx 1 && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); print('market-data-client' in d['jobs']['test']['strategy']['matrix']['package'])" | grep -qx True && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` `matrix.package` list contains an entry `- market-data-client`.
    - The YAML still parses (the grep+python check above prints `PASS`).
    - No other line in `ci.yml` is changed (git diff of ci.yml touches only the matrix list;
      the `typecheck` loop at line ~85 remains the original 5-package list).
  </acceptance_criteria>
  <done>market-data-client runs in the CI test matrix (py3.12 + py3.13); no other ci.yml block modified.</done>
</task>

<task type="auto">
  <name>Task 2: Document market-data-client as the 6th package in CLAUDE.md + MEMORY (D-04, D-05)</name>
  <files>CLAUDE.md, .claude/projects/-Users-admin-development-market-libs/memory/MEMORY.md, .claude/projects/-Users-admin-development-market-libs/memory/market-data-client-v0.1.0-published.md</files>
  <read_first>
    - CLAUDE.md (lines 68-73 Workspace Structure; line 83 CI/CD count line; lines 164-176
      Component Responsibilities table)
    - .claude/projects/-Users-admin-development-market-libs/memory/MEMORY.md (current single index bullet)
    - .claude/projects/-Users-admin-development-market-libs/memory/phase-23-wave2-pending-creds.md (frontmatter shape analog for the new pointer file)
    - .planning/phases/24-release-prep-publish-v0-1-0/24-PATTERNS.md (sections 2, 3, 4 — exact
      analog rows, public-surface facts, and MEMORY pointer format)
  </read_first>
  <action>
    Apply the "6th package" doc fan-out following the existing sibling entries verbatim in shape
    (per D-04/D-05 and PATTERNS sections 2-4):
    (a) CLAUDE.md Workspace Structure: add a bullet for `packages/market-data-client/` — reads
    `market-data-client` v0.1.0, domain "Market data / primary-extractor", transport "HTTP
    sync+async", auth "Auth0 client-credentials", matching the punctuation of the other five bullets.
    (b) CLAUDE.md CI/CD count line (currently "Test matrix: 5 packages × 2 Python versions"):
    change the package count to "6 packages" for consistency with the D-01 matrix edit.
    (c) CLAUDE.md Component Responsibilities table: insert a `market_data_client` row (underscore
    module name in backticks) before the `<pkg>.aio` meta-row, Responsibility describing the
    market-data / primary-extractor surface (quotes, instruments, calendar, health; Auth0
    client-credentials), Key Files `packages/market-data-client/src/market_data_client/client.py`.
    Also extend the `<pkg>.aio` meta-row enumeration to include market-data (it ships `aio.py`)
    and add market-data to the `<pkg>.models` example list (it ships `models.py`).
    (d) MEMORY: append one index bullet to MEMORY.md linking a new pointer file
    `market-data-client-v0.1.0-published.md`, and create that pointer file with the YAML
    frontmatter shape from the analog (`name`, `description`, `metadata.type: project`) plus a
    short prose body noting: 6th monorepo package published via per-package tag
    `market-data-client-v0.1.0`; CI matrix + CLAUDE.md updated in Phase 24. Leave
    `phase-23-wave2-pending-creds.md` in place (Phase 24 does not resolve the paused live
    verification). Put NO credentials, tokens, or `.env` values anywhere in these docs.
  </action>
  <verify>
    <automated>grep -q '`packages/market-data-client/` — `market-data-client` v0.1.0' CLAUDE.md && grep -q '6 packages × 2 Python versions' CLAUDE.md && grep -q '| `market_data_client` |' CLAUDE.md && grep -q 'market-data-client-v0.1.0-published.md' .claude/projects/-Users-admin-development-market-libs/memory/MEMORY.md && test -f .claude/projects/-Users-admin-development-market-libs/memory/market-data-client-v0.1.0-published.md && ! grep -RniE 'AUTH0.*(secret|client_secret)=|Bearer [A-Za-z0-9._-]{20,}' CLAUDE.md .claude/projects/-Users-admin-development-market-libs/memory/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains a `market-data-client` bullet under Workspace Structure with `v0.1.0` and the Auth0 client-credentials descriptor.
    - CLAUDE.md CI/CD line reads `6 packages × 2 Python versions (3.12, 3.13)`.
    - CLAUDE.md Component Responsibilities table contains a `| `market_data_client` |` row pointing at `packages/market-data-client/src/market_data_client/client.py`.
    - MEMORY.md contains a bullet linking `market-data-client-v0.1.0-published.md`, and that pointer file exists with YAML frontmatter (`name`, `description`, `metadata`).
    - No credential/secret pattern appears in the edited docs (the negated grep in verify passes).
  </acceptance_criteria>
  <done>CLAUDE.md and MEMORY reflect market-data-client as the published 6th package; no secrets committed.</done>
</task>

<task type="auto">
  <name>Task 3: Validate uv.lock, version alignment, and root workspace member (D-03, D-11, SC-1)</name>
  <files>uv.lock, pyproject.toml, packages/market-data-client/pyproject.toml, packages/market-data-client/src/market_data_client/__init__.py</files>
  <read_first>
    - .planning/phases/24-release-prep-publish-v0-1-0/24-PATTERNS.md (sections 5, 6, 7 — validation
      commands and already-present surfaces; do NOT regenerate the lockfile from scratch)
    - .github/workflows/ci.yml (line ~33 — the `uv lock --check` step CI runs)
  </read_first>
  <action>
    VALIDATE-only (no regeneration, per D-03). Confirm the lockfile and version metadata are
    already correct so the release passes `release.yml`'s version-match gate:
    (a) Run `uv sync --all-packages --all-extras --dev --frozen` — must exit 0 (no lock drift).
    (b) Run `uv lock --check` — must exit 0 (exactly what ci.yml runs).
    (c) Confirm `packages/market-data-client/pyproject.toml` version is `0.1.0` AND
    `packages/market-data-client/src/market_data_client/__init__.py` `__version__` is `0.1.0`
    (already aligned — do not edit).
    (d) Confirm the root `pyproject.toml` already registers the workspace member
    `market-data-client = { workspace = true }` (line ~20) and that `uv.lock` already lists
    `"market-data-client"` as a member (already present — do not edit). If (a) or (b) reports
    drift, STOP and surface it (do not silently regenerate) — the intent is validation, not
    recreation. The `M uv.lock` in git status is the intended staged workspace-member
    registration to be committed in this phase's PR.
  </action>
  <verify>
    <automated>uv sync --all-packages --all-extras --dev --frozen && uv lock --check && grep -qx 'version = "0.1.0"' <(sed -n '3p' packages/market-data-client/pyproject.toml) && grep -q '__version__ = "0.1.0"' packages/market-data-client/src/market_data_client/__init__.py && grep -q 'market-data-client = { workspace = true }' pyproject.toml && grep -q '"market-data-client"' uv.lock && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync --all-packages --all-extras --dev --frozen` exits 0.
    - `uv lock --check` exits 0.
    - `packages/market-data-client/pyproject.toml` version == `0.1.0` and `__version__` == `0.1.0`.
    - Root `pyproject.toml` contains `market-data-client = { workspace = true }`; `uv.lock` lists `"market-data-client"`.
  </acceptance_criteria>
  <done>Lockfile has no drift, package version is aligned at 0.1.0, and workspace member is registered — release.yml version-match gate will pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → committed docs | Edited CLAUDE.md / MEMORY / uv.lock become part of a public-facing PR to `main` |
| CI config → CI runner | ci.yml matrix entry controls what jobs execute against the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | CLAUDE.md / MEMORY docs | mitigate | Docs carry only public package metadata; Task 2 verify negative-greps for `AUTH0 *secret`/`client_secret=`/`Bearer <token>` patterns; CLAUDE.md constraint forbids committing `.env` or exposing credentials |
| T-24-02 | Tampering | .github/workflows/ci.yml | mitigate | Single list-append of the exact literal `market-data-client`; Task 1 verify asserts no other ci.yml block changed and YAML still parses (no injected/altered job) |
| T-24-SC | Tampering | uv/pip/cargo installs | accept | No new package installs in this phase — uv.lock is VALIDATED not regenerated (D-03); deps (httpx, python-dotenv, tenacity) are the repo's already-present audited dependencies. No RESEARCH.md legitimacy table required because no install task exists |
</threat_model>

<verification>
- `.github/workflows/ci.yml` matrix.package contains `- market-data-client` and YAML parses.
- CLAUDE.md shows market-data-client in the workspace list, the "6 packages" count, and the component table.
- MEMORY.md links a new pointer file that exists.
- `uv sync ... --frozen` and `uv lock --check` both exit 0; version aligned at 0.1.0.
- No credential/secret pattern present in the edited docs.
</verification>

<success_criteria>
Plan complete when: CI matrix includes market-data-client, CLAUDE.md + MEMORY document the 6th
package, and lockfile/version/workspace-member validations all pass — with the conscious
typecheck/import-linter deferrals recorded in `<scope_decisions>`. Addresses SC-1 (ci.yml +
uv.lock + version alignment) and SC-2 (CLAUDE.md + MEMORY) for PUB-MD-01.
</success_criteria>

<output>
Create `.planning/phases/24-release-prep-publish-v0-1-0/24-01-SUMMARY.md` when done.
</output>
