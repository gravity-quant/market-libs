---
phase: 30-iol-client-tipado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/iol-client/src/iol_client/models.py
  - packages/iol-client/src/iol_client/__init__.py
  - packages/iol-client/src/iol_client/_core.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_models.py
  - packages/iol-client/tests/test_typed_surface_red.py
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
  - packages/iol-client/tests/test_core.py
autonomous: true
requirements: [TYP-01]

must_haves:
  truths:
    - "El consumidor escribe `quote.ultimoPrecio` y obtiene un `float`; `quote[\"ultimoPrecio\"]` ya no existe (D-07, criterio 3)."
    - "Un typo de atributo sobre un modelo falla el typecheck y además levanta `AttributeError` en runtime (D-10, criterio 3)."
    - "`Cotizacion.from_api()` sobre el schema capturado `get-quote.json` produce cero divergencias (criterio 1)."
    - "`Cotizacion.from_api()` sobre el schema capturado `get-historical-quotes.json` produce cero divergencias — mismo modelo, nulabilidad diferencial (D-01/D-03)."
    - "`quote.to_dict()` re-proyecta el wire dict y `schema_of()` de ese dict reproduce el baseline committeado byte-idéntico (D-08, criterio 5)."
    - "Las 4 firmas de `get_quote` (método/shim × sync/async) devuelven `Cotizacion` y mypy strict pasa sobre src y tests (criterio 2)."
    - "`Cotizacion` y `Punta` son importables desde el paquete raíz y figuran en `__all__` (Pitfall 3)."
  artifacts:
    - path: "packages/iol-client/src/iol_client/models.py"
      provides: "SafeModel (from_api + to_dict), Punta, Cotizacion"
      contains: "class Cotizacion(SafeModel)"
      min_lines: 90
    - path: "packages/iol-client/tests/test_models.py"
      provides: "Construcción desde los 2 schemas capturados, polimorfismo de puntas, round-trip to_dict"
      min_lines: 60
    - path: "packages/iol-client/tests/test_typed_surface_red.py"
      provides: "Fixture RED de typecheck no-vacua en ambas direcciones (D-10)"
      min_lines: 15
  key_links:
    - from: "packages/iol-client/src/iol_client/models.py"
      to: "packages/iol-client/src/iol_client/_decode.py"
      via: "SafeModel.from_api delega a walk_model con POLICY y current_sink"
      pattern: "_decode\\.walk_model"
    - from: "packages/iol-client/src/iol_client/_core.py"
      to: "packages/iol-client/src/iol_client/models.py"
      via: "parse_get_quote_response construye Cotizacion.from_api"
      pattern: "Cotizacion\\.from_api"
    - from: "packages/iol-client/src/iol_client/client.py"
      to: "packages/iol-client/src/iol_client/_core.py"
      via: "el método y el shim de get_quote delegan al parser y re-anotan el retorno"
      pattern: "-> Cotizacion:"
  prohibitions:
    - statement: "Ningún campo de RESPONSE gana un tipo de conjunto cerrado en esta fase; `plazo` y `moneda` se declaran como texto libre y la promoción queda para F33 con censo vivo (D-09/F29, criterio 4)."
      status: unverified
      flagged: true
    - statement: "Ni `models.py` ni los parsers emiten un valor del wire por ningún canal de salida; los registros de divergencia siguen siendo tipo-y-ruta, nunca valor (Phase 29 T-29-36)."
      status: unverified
      flagged: true
    - statement: "La suite no se pone verde borrando o debilitando aserciones que tienen equivalente tipado; sólo se eliminan las que afirman una clave que el corpus vivo demuestra inexistente, y esa eliminación queda registrada en el SUMMARY."
      status: unverified
      flagged: true
    - statement: "`SafeModel` se copia; nunca se importa desde otro paquete del monorepo (constraint de cero código compartido, CLAUDE.md)."
      status: unverified
      flagged: true
---

<objective>
TRACER del Phase 30: llevar **una sola función** — `get_quote` — de dict opaco a atributo tipado, end-to-end y con calidad de producción, antes de expandir a las otras tres. La rebanada atraviesa toda la pila: modelo nuevo (`SafeModel` + `Punta` + `Cotizacion`) → parser de `_core` con `DecodeScope` propio → 4 firmas (método/shim × sync/async) → export público → tests de modelo → fixture RED de typecheck.

Purpose: `Cotizacion` es el modelo de mayor superficie (20 campos, compartido por `get_quote` y `get_historical_quotes`) y `get_quote` es el consumo real que `main_iol.py` ejercita. Si la rebanada cierra verde, el resto de la fase es replicación mecánica del mismo patrón. Si algo del walker de Phase 29 no encaja, se descubre acá con un blast radius de una función.

Output: `models.py` nuevo con la base y 2 de los 4 modelos; `parse_get_quote_response` devolviendo `Cotizacion`; 4 firmas migradas; `Cotizacion`/`Punta`/`SafeModel` exportados; `test_models.py` y `test_typed_surface_red.py` nuevos.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-iol-client-tipado/30-CONTEXT.md
@.planning/phases/30-iol-client-tipado/30-RESEARCH.md
@.planning/phases/30-iol-client-tipado/30-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos y archivos **creados** por Phase 30 (ninguno existe hoy — cualquier verificación de drift debe excluirlos):

| Artefacto | Tipo | Plan que lo crea |
|---|---|---|
| `packages/iol-client/src/iol_client/models.py` | archivo nuevo | 30-01 |
| `iol_client.models.SafeModel` | clase base | 30-01 |
| `iol_client.models.SafeModel.from_api` | classmethod | 30-01 |
| `iol_client.models.SafeModel.to_dict` | método | 30-01 |
| `iol_client.models.Punta` | dataclass frozen/slots | 30-01 |
| `iol_client.models.Cotizacion` | dataclass frozen/slots | 30-01 |
| `iol_client.models.Titulo` | dataclass frozen/slots | 30-02 |
| `iol_client.models.Instrumento` | dataclass frozen/slots | 30-03 |
| `iol_client._core._parse_list_or_raise` | helper privado con decorador | 30-02 |
| `packages/iol-client/tests/test_models.py` | archivo nuevo | 30-01 (ampliado en 30-02/30-03) |
| `packages/iol-client/tests/test_typed_surface_red.py` | archivo nuevo | 30-01 |
| `main_iol._as_wire` | helper del driver | 30-04 |
| Sección `## Changelog` + `### v0.3.0` en `packages/iol-client/README.md` | doc nueva | 30-04 |

## Flagged Assumptions (no resueltas — no borrar)

| # | Assumption | Estado | Por qué queda abierta |
|---|---|---|---|
| FA-01 | La fila de cobertura de edges para **TYP-01** llegó `unclassified` → **unresolved**. Los edges concretos se derivan de los 5 criterios de éxito del ROADMAP § Phase 30 y de D-01..D-12; ninguno se auto-resolvió. | unresolved | No hay SPEC file para esta fase; la clasificación automática no aplicó. Se declara explícitamente en vez de descartarse. |
| FA-02 (RESEARCH A1) | `Titulo.fechaVencimiento`, `precioEjercicio` y `tipoOpcion`: el corpus sólo registra `NoneType`; el tipo **no nulo** está inferido del nombre del campo, nunca observado. | abierta → F33 | Auto-correctiva: un tipo mal inferido produce un registro de divergencia con el tipo real nombrado. Se marca en el docstring del modelo (plan 30-02). |
| FA-03 (RESEARCH A2 / D-02) | El elemento de `Cotizacion.puntas` se asume con la misma forma de 4 floats que `Titulo.puntas`; la captura 2026-06-06 registró `[]`. | abierta → F33 | Propiedad de D-02 por decisión; la confirmación es la corrida estricta de Phase 33. |
| FA-04 (RESEARCH A3) | Se asume que nada fuera de este repo consume `iol-client` 0.2.0. | abierta → F34 | Requiere al operator. No bloquea: `to_dict()` + el callout del README son la mitigación correcta bajo cualquier resultado. |
| FA-05 | **Corrección de inventario:** D-01 y RESEARCH dicen "21 claves" para `Titulo`; el schema committeado `get-instruments-by-type.json` tiene **20**. El plan usa el schema (fuente de verdad declarada por CONTEXT). | resuelta a favor del schema | Se registra para que el checker no lea 20 como una omisión. |

## Reversibility Ratings (REVERSIBILITY_GATES)

| Decisión de este plan | Rating | Gate |
|---|---|---|
| Ruptura de superficie dict→modelo en `get_quote` | **two-way en este repo** — pre-autorizada por CONTEXT D-11/D-12 y ROADMAP DT-08; el bump de versión que la publica es de F34 | ninguno |
| `to_dict()` como escape hatch (`dataclasses.asdict`) | two-way — método aditivo | ninguno |
| Política `POLICY` typed-zeros re-ratificada (Pitfall 2) | two-way — es una confirmación por escrito, no un cambio de código | ninguno |
| Copiar `SafeModel` en vez de importarlo | one-way *conceptual* pero ya cerrada por CLAUDE.md (cero código compartido entre paquetes) y por el precedente de Phase 29 en 5 copias | ninguno — la decisión no se abre en esta fase |

Ninguna decisión de este plan es one-way sin autorización previa → **no se inserta ningún `checkpoint:decision`**.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `models.py` — SafeModel + Punta + Cotizacion, con tests derivados de los schemas capturados</name>

  <files>
    packages/iol-client/src/iol_client/models.py,
    packages/iol-client/src/iol_client/__init__.py,
    packages/iol-client/tests/test_models.py
  </files>

  <read_first>
    - `packages/higyrus-client/src/higyrus_client/models.py` líneas 1-116 — template a copiar: docstring de módulo, `SafeModel` (:41-54), y el patrón de declaración `PosicionValuada` (:74-116). NO copiar `_coerce` (:57-71).
    - `packages/iol-client/src/iol_client/_decode.py` — `POLICY` :140, `current_sink` :268, `hints_for` :400, `walk_field` :415 (rama Optional :433-442, rama float :517-522), `walk_model` :541. Sólo lectura: el archivo está congelado por hash.
    - `.planning/verification/schemas/iol-client/get-quote.json` y `get-historical-quotes.json` — las 20 claves y sus tipos observados (fuente de verdad de D-01/D-03/D-04).
    - `packages/iol-client/src/iol_client/__init__.py` líneas 25-75 — bloque de imports post-`_logging_attach.attach()` con `# noqa: E402` y el `__all__` ordenado.
    - `packages/higyrus-client/src/higyrus_client/__init__.py` líneas 56-72 — precedente de re-export de modelos.
    - `packages/market-data-client/tests/test_models.py` líneas 1-55 — forma del archivo de test de modelos.
    - `packages/iol-client/tests/test_decode.py` líneas 49-75 — fixture autouse `_pristine_decode_context`; cualquier aserción sobre registros de divergencia debe correr bajo scope limpio o un test previo la deduplica.
    - `.planning/phases/29-decoder-observable/29-SEMANTICS-MATRIX.md` líneas 104-148 — la obligación nominal que Phase 29 dejó a Phase 30 (re-ratificación de `DecodePolicy`).
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Model Field Tables, Pitfall 2, 3, 4, 8.
  </read_first>

  <behavior>
    Escribir estos tests PRIMERO en `packages/iol-client/tests/test_models.py` y verlos fallar (RED) antes de escribir `models.py`:
    - `Cotizacion.from_api(payload_de_get_quote)` construye los 20 campos con los valores del payload y **cero** registros de divergencia, usando un payload sintético cuyas claves y tipos salen de `get-quote.json`.
    - `Cotizacion.from_api(payload_de_serie_historica)` — mismo modelo, con `descripcionTitulo`, `plazo` y `puntas` en `None` — construye sin divergencias (D-03: la rama Optional del walker no emite registro).
    - `Cotizacion.from_api({})` devuelve typed zeros: `apertura == 0.0`, `cantidadOperaciones == 0`, `moneda == ""`, y `descripcionTitulo is None` / `plazo is None` / `puntas is None`.
    - `Cotizacion.from_api(None)` no levanta.
    - Polimorfismo de `puntas` en sus 3 formas observadas: `[]` → `[]`; `None` → `None`; `[{4 floats}]` → `[Punta(...)]` con los 4 floats.
    - `Punta.from_api({...4 floats...})` construye los 4 campos.
    - Asimetría D-04 (Pitfall 4): `Cotizacion.from_api({"cantidadOperaciones": 7})` → `7`; con `7.0` → `0` (typed zero) y **un** registro de divergencia de clase `type` en `cantidadOperaciones`. Este test documenta la rama estricta a propósito.
    - Round-trip: `schema_of(Cotizacion.from_api(payload).to_dict())` es igual al valor de la clave `schema` de `.planning/verification/schemas/iol-client/get-quote.json`; ídem para la fila 0 de `get-historical-quotes.json`. Importar `schema_of` desde `verification.schema`.
    - `to_dict()` devuelve un dict plano (el `Punta` anidado aplanado a dict) y es un `dict`, no una instancia de modelo.
  </behavior>

  <action>
    RED → GREEN → REFACTOR, en ese orden, con commit por etapa.

    **RED.** Crear `packages/iol-client/tests/test_models.py` con los casos de `<behavior>`. Empieza con `from __future__ import annotations` (obligatorio en todo módulo del repo). Importar `Cotizacion`, `Punta`, `SafeModel` desde `iol_client.models` y `schema_of` desde `verification.schema`. Los payloads sintéticos se derivan de los dos archivos JSON leídos en `read_first`: mismas claves, valores plausibles del tipo declarado. Correr `uv run pytest packages/iol-client/tests/test_models.py -q` y confirmar que falla por `ModuleNotFoundError`.

    **GREEN.** Crear `packages/iol-client/src/iol_client/models.py`:

    1. Docstring de módulo siguiendo `higyrus_client/models.py:1-38`, pero **omitiendo la frase sobre el per-file-ignore de nomenclatura camelCase**: esa afirmación es falsa para higyrus (`pyproject.toml:53-68` no incluye el ruleset `N` en `select`) y no debe propagarse a un tercer archivo (RESEARCH C-5). El docstring debe además contener el párrafo de **re-ratificación de `DecodePolicy`** que Phase 29 dejó pendiente (`29-SEMANTICS-MATRIX.md:126-131`): confirmar que los modelos de iol quieren sustitución por typed-zeros (`POLICY = DecodePolicy("", 0, 0.0, False, "from_api_none", False, False)`, `_decode.py:140`) y **no** la política estilo matriz de missing→`None`, con las tres razones: (a) D-03 ya rutea cada campo observado-nulable a un tipo opcional, que el walker devuelve como `None` sin emitir registro; (b) la política de matriz viaja con `scalar_passthrough=True`, que devolvería un entero del wire en un campo anotado como decimal y rompería la garantía de mypy que TYP-01 existe para dar; (c) el flag de conjunto cerrado está fijado por D-09 y no es un tunable en ninguna copia.
    2. Imports: `from __future__ import annotations`, luego `import dataclasses` (para `asdict`, D-08), `from dataclasses import dataclass`, `from typing import Any, Self`, `from iol_client import _decode`. **Ningún otro import.**
    3. `class SafeModel` — copia verbatim de `higyrus_client/models.py:41-54` (docstring + `from_api` clasificado que delega en `_decode.walk_model(cls, payload, policy=_decode.POLICY, sink=_decode.current_sink())` y retorna `cls(**kwargs)`), **más** `to_dict(self) -> dict[str, Any]` que retorna `dataclasses.asdict(self)` (D-08). `to_dict` vive en la base, no por modelo: `asdict` es genérico. La firma y el contrato de `from_api(payload)` se preservan exactamente (DT-05) — cero overrides en las subclases (Pitfall 7).
    4. `@dataclass(frozen=True, slots=True) class Punta(SafeModel)` — declarar **primera** (Pitfall 8; funciona en cualquier orden pero éste es el que se lee bien). Campos, todos decimales: `cantidadCompra`, `cantidadVenta`, `precioCompra`, `precioVenta`.
    5. `@dataclass(frozen=True, slots=True) class Cotizacion(SafeModel)` con exactamente estos 20 campos en este orden, tipos verbatim del corpus (D-01/D-02/D-03/D-04): `apertura: float`, `cantidadOperaciones: int`, `cierreAnterior: float`, `descripcionTitulo: str | None`, `fechaHora: str`, `interesesAbiertos: float`, `laminaMinima: int`, `lote: int`, `maximo: float`, `minimo: float`, `moneda: str`, `montoOperado: float`, `plazo: str | None`, `precioAjuste: float`, `precioPromedio: float`, `puntas: list[Punta] | None`, `tendencia: str`, `ultimoPrecio: float`, `variacion: float`, `volumenNominal: float`. Sin valores por defecto (el walker siempre pasa las 20 claves).
    6. Docstring de `Cotizacion` — siguiendo el patrón de `PosicionValuada` (notas verificadas-contra-wire con fecha). Debe registrar: que el mismo modelo sirve a `get_quote` y a cada fila de `get_historical_quotes` porque ambos endpoints traen el mismo set de 20 claves y difieren sólo en cuáles llegan nulas (captura 2026-06-06); que `descripcionTitulo`/`plazo`/`puntas` son opcionales por D-03; que el elemento de la lista `puntas` es **inobservado** (la captura registró `[]`) y `Punta` es la única forma de elemento vista en el corpus, a confirmar en Phase 33 (D-02); y la asimetría D-04 de `cantidadOperaciones` (entero acá, decimal en `Titulo`), señalando que la rama entera es estricta y sustituye typed-zero ante un decimal del wire mientras que la decimal ensancha en silencio — una divergencia de Phase 33 en este campo es esa asimetría disparando, no un defecto del modelo. `plazo` queda como texto libre: la promoción a conjunto cerrado es trabajo de F33 con censo vivo (DT-07, criterio 4).
    7. `models.py` no realiza **ninguna** emisión de diagnóstico ni escritura a stream: no importa nada fuera de la allowlist del punto 2 y no construye registros. Toda la observabilidad la posee el decoder congelado.

    Luego editar `packages/iol-client/src/iol_client/__init__.py` (Pitfall 3): agregar un bloque `from iol_client.models import (  # noqa: E402` con `Cotizacion`, `Punta`, `SafeModel` (orden alfabético, siguiendo `higyrus_client/__init__.py:56-64`) y añadir esos tres nombres al `__all__` ordenado. El `# noqa: E402` es obligatorio: los imports van después de `_logging_attach.attach()`. **No** tocar `__version__` — queda en `"0.2.0"` (D-11).

    **REFACTOR.** Correr `uv run ruff format packages/iol-client` y `uv run ruff check packages/iol-client --fix`; re-correr los tests.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_models.py -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_models.py -q` termina en 0 con ≥ 8 tests.
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `grep -c 'class Cotizacion(SafeModel)' packages/iol-client/src/iol_client/models.py` imprime `1`; ídem `class Punta(SafeModel)` y `class SafeModel`.
    - `grep -c 'frozen=True, slots=True' packages/iol-client/src/iol_client/models.py` imprime `2`.
    - `uv run python -c "import iol_client; assert {'Cotizacion','Punta','SafeModel'} <= set(iol_client.__all__), sorted(iol_client.__all__)"` sale 0.
    - `uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'"` sale 0 (D-11).
    - Allowlist de imports de `models.py`: `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('packages/iol-client/src/iol_client/models.py').read_text()); mods={n.module or '' for n in ast.walk(t) if isinstance(n,ast.ImportFrom)} | {a.name.split('.')[0] for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; assert mods <= {'__future__','dataclasses','typing','iol_client'}, mods"` sale 0. Esta es la verificación operativa de que `models.py` no abrió ningún canal de emisión propio.
    - `uv run python -c "from iol_client.models import Cotizacion; import dataclasses; f=[x.name for x in dataclasses.fields(Cotizacion)]; assert len(f)==20, f"` sale 0.
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py` sale 0 (D-09: archivo congelado).
    - `git diff --exit-code uv.lock` sale 0 (cero dependencias nuevas).
    - `uv run python tools/check_decode_intactness.py` sale 0.
  </acceptance_criteria>

  <done>
    `models.py` existe con `SafeModel` (incluido `to_dict`), `Punta` y `Cotizacion`; los tres están exportados en `__all__`; `test_models.py` cubre construcción desde los 2 schemas capturados, las 3 formas de `puntas`, la asimetría de `cantidadOperaciones` y el round-trip `to_dict()` → `schema_of` contra los baselines committeados; mypy/ruff/format verdes; `_decode.py` y `uv.lock` sin cambios; `__version__` sigue en `0.2.0`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: `get_quote` end-to-end — parser con DecodeScope propio + 4 firmas + migración de sus tests</name>

  <files>
    packages/iol-client/src/iol_client/_core.py,
    packages/iol-client/src/iol_client/client.py,
    packages/iol-client/src/iol_client/aio.py,
    packages/iol-client/tests/test_core.py,
    packages/iol-client/tests/test_client.py,
    packages/iol-client/tests/test_async_client.py
  </files>

  <read_first>
    - `packages/iol-client/src/iol_client/_core.py` líneas 320-360 — los 4 parsers actuales; sólo `parse_get_quote_response` (:325-331) cambia en este plan. Los builders :234-319 quedan intactos (`ajustada` es un conjunto cerrado de INPUT anterior a DT-07 y se conserva).
    - `packages/higyrus-client/src/higyrus_client/_core.py` líneas 457-500 — patrón de parser que devuelve modelos con el decorador de scope.
    - `packages/iol-client/src/iol_client/_decode.py` línea 312 — `_response_parser`, dueño del `DecodeScope` per-response (la deduplicación colapsa una lista de modelos a 1 registro por campo).
    - `packages/iol-client/src/iol_client/exceptions.py` línea 13 — `IOLAPIError.__init__(status_code: int, message: str)`: **dos posicionales**, no la firma de higyrus.
    - `packages/iol-client/src/iol_client/client.py` líneas 514-527 (método) y 673-680 (shim).
    - `packages/iol-client/src/iol_client/aio.py` líneas 536-546 (método) y 693-699 (shim).
    - `packages/iol-client/tests/test_core.py` líneas 330-345 — el test del parser de quote (hoy afirma igualdad contra un dict cuyas claves no son campos de `Cotizacion`).
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Pattern 3, § The 16 Signatures, Pitfall 6, Pitfall 9.
  </read_first>

  <behavior>
    Tests que deben existir y fallar antes del cambio de firma:
    - `parse_get_quote_response(resp)` devuelve una instancia de `Cotizacion`, no un dict, y `resultado.ultimoPrecio` tiene el valor del payload mockeado.
    - `iol_client.get_quote("GGAL")` (shim sync) devuelve `Cotizacion` y el acceso por atributo funciona.
    - `await aio.get_quote("GGAL")` devuelve `Cotizacion` y el atributo tiene el mismo valor que el shim sync (paridad de tipo sync/async).
    - `Client(...).get_quote(...)` y `AsyncClient(...).get_quote(...)` devuelven `Cotizacion`.
  </behavior>

  <action>
    RED → GREEN → REFACTOR.

    **RED.** Agregar/ajustar en `test_core.py`, `test_client.py` y `test_async_client.py` las aserciones de `<behavior>` (con `isinstance(..., Cotizacion)` explícito). Correr y verlas fallar.

    **GREEN.** Tres ediciones, todas en el mismo commit (Pitfall 6 — sync y async nunca se separan):

    1. `_core.py`: reescribir `parse_get_quote_response` **in place** (D-05). Retorno anotado `Cotizacion`. Cuerpo: `resp.read()`, `raise_for_response(resp)`, y `return Cotizacion.from_api(resp.json())`. Decorarla con `@_decode._response_parser` para que sea dueña de un `DecodeScope` por respuesta HTTP. **No** copiar el helper `_consume_and_check` de higyrus: iol no lo tiene y copiarlo agregaría tolerancia a 204/cuerpo vacío que iol no tiene hoy — cambio de comportamiento fuera de scope (adaptación 1 de RESEARCH § Pattern 3). Agregar `from iol_client.models import Cotizacion` al bloque de imports de `_core.py`. El sentido del import es `_core` → `models` → `_decode`; el único contrato de import-linter de iol prohíbe `_core` → `client`/`aio` (`pyproject.toml:163-167`), así que no hace falta contrato nuevo (D-05).
    2. `client.py`: cambiar el retorno anotado del método `get_quote` (:514-527) y del shim de módulo (:673-680) a `Cotizacion`, e importar el modelo. **Ninguna lógica se mueve a los shells**: siguen siendo delegaciones de 3 líneas. El parámetro `plazo: str = "t2"` **no cambia** — sigue siendo texto libre (criterio 4, DT-07).
    3. `aio.py`: idéntico en el método (:536-546) y el shim (:693-699).

    Luego migrar todas las aserciones que consumen un resultado de `get_quote`. Localizarlas con `grep -rn 'quote\[' packages/iol-client/tests/` y `grep -rn 'get_quote' packages/iol-client/tests/`. Reglas:
    - Subscript → atributo: una lectura por clave pasa a lectura por atributo del mismo nombre.
    - Igualdad contra dict (`test_client.py:556`, `test_async_client.py:543` afirman igualdad contra un dict de una sola clave) → aserción de atributo sobre el modelo.
    - **Eliminar, no reescribir**, las dos aserciones sobre la clave `simbolo` de un resultado de `get_quote` (`test_client.py:114`, `test_async_client.py:76`): `simbolo` **no** es campo de `Cotizacion` — no está entre las 20 claves de `get-quote.json`; post-migración esa clave produce un registro de divergencia de clase `extra`. Registrar las dos eliminaciones y su razón en el SUMMARY del plan.
    - `test_core.py:338` afirma igualdad contra `{"simbolo": ..., "precio": ...}` — **ninguna** de las dos claves es campo de `Cotizacion`. Reescritura completa: cambiar el payload mockeado por uno con claves reales del corpus (p.ej. sólo `ultimoPrecio`) y afirmar `isinstance(..., Cotizacion)` más el atributo.
    - Los mocks parciales de 1-2 claves ahora emiten ~19 registros de campo faltante por llamada (Pitfall 9). Es **inocuo y esperado**: `strict_decode` es `False` por defecto (`_state.py:101`) y ningún test de iol lo activa para la superficie del cliente. **No** activar modo estricto en esta fase; eso es entregable de Phase 33 y haría fallar la corrida viva en la primera clave faltante. Verificar que ningún test afirme sobre conteos de registros capturados del logger del paquete (hoy ninguno lo hace).

    **REFACTOR.** `uv run ruff format packages/iol-client` + `ruff check --fix`; suite completa del paquete verde.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client -q && uv run mypy packages/iol-client/src packages/iol-client/tests && uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client -q` sale 0.
    - `grep -c -- '-> Cotizacion:' packages/iol-client/src/iol_client/client.py` imprime `2` (método + shim).
    - `grep -c -- '-> Cotizacion:' packages/iol-client/src/iol_client/aio.py` imprime `2`.
    - `grep -c 'Cotizacion.from_api' packages/iol-client/src/iol_client/_core.py` imprime `1`.
    - `grep -B2 -n 'def parse_get_quote_response' packages/iol-client/src/iol_client/_core.py | grep -c '_response_parser'` imprime `1`.
    - `uv run python -c "import inspect, iol_client; from iol_client.models import Cotizacion; assert inspect.signature(iol_client.get_quote).return_annotation in ('Cotizacion', Cotizacion)"` sale 0.
    - `uv run mypy packages/iol-client/src packages/iol-client/tests` sale 0.
    - `uv run lint-imports` (import-linter) sale 0.
    - `git diff --exit-code packages/iol-client/src/iol_client/_decode.py` sale 0.
    - `grep -rc 'quote\["' packages/iol-client/tests/test_client.py packages/iol-client/tests/test_async_client.py` imprime `0` para ambos archivos.
  </acceptance_criteria>

  <done>
    Las 4 firmas de `get_quote` devuelven `Cotizacion`; el parser construye el modelo bajo un `DecodeScope` per-response; toda aserción de subscript sobre resultados de quote migró a atributo; las 3 aserciones sobre claves inexistentes en el corpus fueron eliminadas o reescritas con su razón registrada; suite del paquete, mypy, ruff, format e import-linter verdes.
  </done>
</task>

<task type="auto">
  <name>Task 3: fixture RED de typecheck — no-vacua en ambas direcciones (D-10)</name>

  <files>packages/iol-client/tests/test_typed_surface_red.py</files>

  <read_first>
    - `.planning/phases/30-iol-client-tipado/30-RESEARCH.md` § Pitfall 1 — la forma exacta validada empíricamente, y la trampa: la forma ingenua pone el CI en rojo **en runtime**, porque `packages/iol-client` está en la matriz de tests del CI y el typo deliberado se ejecuta de verdad.
    - `pyproject.toml` línea 86 — `warn_unused_ignores = true`: es lo único que hace la fixture no-vacua en la dirección inversa.
    - `.github/workflows/ci.yml` líneas 85-94 (loop de mypy sobre tests de cada paquete) y 100-107 (matriz de tests).
    - `packages/iol-client/src/iol_client/models.py` — la clase objetivo y sus campos reales.
  </read_first>

  <action>
    Crear `packages/iol-client/tests/test_typed_surface_red.py` con un único test que pinne **las dos piernas** de la garantía TYP-01, en la forma validada en RESEARCH § Pitfall 1:

    - Construir una `Cotizacion` desde un payload mínimo (`from_api` con una sola clave real).
    - Leer un atributo **deliberadamente mal escrito** (una variante de `ultimoPrecio` con una letra de más) dentro de un `pytest.raises(AttributeError)`, con un `# type: ignore[attr-defined]` en esa misma línea.
    - Afirmar después que el atributo bien escrito devuelve el valor del payload.

    Por qué esta forma y no la ingenua: la pierna **estática** es que mypy marca el atributo inexistente y el `ignore` lo absorbe; bajo `warn_unused_ignores = true`, el día que ese `ignore` deje de ser necesario (porque el atributo empezó a resolver) mypy lo reporta como ignore no usado y **falla** — la fixture no puede pudrirse en un no-op. La pierna **runtime** existe porque `slots=True` hace que el mismo typo levante `AttributeError`; sin el `pytest.raises` el archivo pone el CI en rojo al ejecutarse (medido en RESEARCH). El docstring del test debe explicar ambas piernas y por qué el `ignore` es la aserción, no una supresión.

    La fixture vive en `packages/iol-client/tests/` porque ese path lo typechequea el loop de mypy del CI. **Nunca** en `main_iol.py`: mypy no lo typechequea (`files` cubre `packages/*/src`). Sin invocación de mypy por subproceso — maquinaria nueva y ~10s de CI que D-10 descarta explícitamente.

    Comprobar la no-vacuidad **manualmente y en las dos direcciones** antes de cerrar: (a) con el typo, `uv run mypy packages/iol-client/tests` debe salir 0; (b) reemplazando temporalmente el typo por el atributo correcto **sin** tocar el comentario de ignore, mypy debe fallar con un error de ignore no usado. Restaurar el typo. Registrar ambas observaciones en el SUMMARY.
  </action>

  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_typed_surface_red.py -q && uv run mypy packages/iol-client/tests && uv run ruff check packages/iol-client</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/iol-client/tests/test_typed_surface_red.py -q` sale 0 con exactamente 1 test.
    - `grep -c 'attr-defined' packages/iol-client/tests/test_typed_surface_red.py` imprime `1`.
    - `grep -c 'pytest.raises(AttributeError)' packages/iol-client/tests/test_typed_surface_red.py` imprime `1`.
    - `uv run mypy packages/iol-client/tests` sale 0 con la fixture en su forma final.
    - Prueba de no-vacuidad inversa ejecutada y registrada: con el atributo correcto en lugar del typo, `uv run mypy packages/iol-client/tests` **falla**. Restaurado al estado final.
    - `uv run pytest packages/iol-client -q` sale 0 y reporta **más** tests que la línea base de 205.
  </acceptance_criteria>

  <done>
    Existe una fixture RED que falla el typecheck ante un typo de atributo y falla el typecheck **también** si el typo dejara de serlo; ambas direcciones verificadas a mano y registradas; la suite del paquete crece respecto de los 205 de línea base.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API IOL en vivo → parser de `_core` | Entra un payload JSON no confiable de un tercero; su forma puede cambiar sin aviso. |
| Modelo decodificado → salida de diagnóstico del paquete | Los registros de divergencia cruzan hacia handlers del consumidor; jamás deben llevar valores del wire. |
| Modelo decodificado → artefactos committeados (`verification/`) | Un dict proyectado desde el modelo puede terminar en un archivo versionado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure | `models.py` | mitigate | `models.py` no abre ningún canal de emisión: allowlist de imports verificada por AST en los acceptance criteria (`__future__`, `dataclasses`, `typing`, `iol_client`). Toda la observabilidad la posee `_decode.py`, congelado por hash, cuyos registros son tipo-y-ruta, nunca valor (`exceptions.py:41-43`, Phase 29 T-29-36). |
| T-30-02 | Tampering | inyección de log vía claves del wire | transfer | Ya mitigado por `_decode._safe_key` (Phase 29 lock 11 según enmienda CR-04): todo carácter fuera de un alfabeto conservador pasa a `?`, bloqueando el forjado de una línea de log desde una clave con salto de línea. Phase 30 no agrega ninguna línea de salida que haga eco de un valor del wire. |
| T-30-03 | Tampering | integridad del walker | mitigate | `_decode.py` no se toca. Verificado en cada tarea con `git diff --exit-code` sobre el archivo y con `uv run python tools/check_decode_intactness.py` (corre en el job de lint del CI). |
| T-30-04 | Repudiation | fixture RED | mitigate | La fixture se vuelve auto-invalidante bajo `warn_unused_ignores = true`: si el typo empezara a resolver, mypy falla por ignore no usado. La no-vacuidad se prueba en las dos direcciones antes de cerrar la tarea 3. |
| T-30-05 | Elevation of privilege | credenciales en fixtures de test | accept | Las migraciones tocan sólo payloads de respuesta. Los tokens centinela de `test_fixture_reaches_production.py` son literales de prueba y quedan intactos. Sin superficie de credenciales en este plan. |
| T-30-SC | Tampering | instalaciones vía gestor de paquetes | mitigate | Phase 30 instala **cero** paquetes externos (RESEARCH § Package Legitimacy Audit: no aplica). `git diff --exit-code uv.lock` es criterio de aceptación en la tarea 1; un diff del lockfile en un commit de Phase 30 es un defecto, no un entregable. Sin `[ASSUMED]`/`[SUS]`/`[SLOP]` que gatear. |
</threat_model>

<verification>
1. `uv run pytest packages/iol-client -q` → verde, > 205 tests.
2. `uv run mypy packages/iol-client/src packages/iol-client/tests` → 0.
3. `uv run ruff check packages/iol-client && uv run ruff format --check packages/iol-client` → 0.
4. `uv run lint-imports` → 0.
5. `uv run python tools/check_decode_intactness.py` → 0.
6. `git diff --exit-code uv.lock packages/iol-client/src/iol_client/_decode.py` → 0.
7. `uv run python -c "import iol_client; assert iol_client.__version__ == '0.2.0'"` → 0.
</verification>

<success_criteria>
- `Cotizacion` decodifica los dos schemas capturados que la usan con cero divergencias, y su `to_dict()` reproduce el `schema` committeado de `get-quote.json` byte-idéntico.
- Las 4 firmas de `get_quote` devuelven `Cotizacion` en sync y async, en el mismo commit.
- Un typo de atributo falla el typecheck, y la fixture que lo prueba no puede degradarse a no-op.
- `_decode.py`, `uv.lock` y `__version__` sin cambios.
</success_criteria>

<output>
Crear `.planning/phases/30-iol-client-tipado/30-01-SUMMARY.md` al terminar. Debe registrar: (a) la re-ratificación de `DecodePolicy` y dónde quedó escrita; (b) las aserciones eliminadas por afirmar claves inexistentes en el corpus, con su razón; (c) el resultado de la prueba de no-vacuidad inversa de la fixture RED; (d) el conteo de tests antes/después.
</output>
