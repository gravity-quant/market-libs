---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 06
type: execute
wave: 3
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md
autonomous: false
requirements: [REFAC-03]
requirements_addressed: [REFAC-03, CR-03, CR-05]
tags: [phase-07, green-gate, ci-consolidation]
user_setup: []

must_haves:
  truths:
    - "Phase 7 green gate consolidado — toda la phase corre verde sobre Python 3.12 + 3.13: full pytest matrix + ruff check + ruff format check + mypy strict + lint-imports + cross-leak sentinel + matriz sweep snapshot + Phase 6 public-surface snapshot (zero diff)"
    - "LOC drop ≥30% client+aio agregado per paquete (excepto matriz aio.py = stub Phase 6) — los 4 paquetes documentan su drop en SUMMARY.md respectivo (Plans 02-05)"
    - "CR-03 cerrado y verificado via `test_parse_envelope_consumes_body_before_raise` PASSING + source order verification"
    - "CR-05 cerrado y verificado via `verification/test_matriz_sweep_snapshot.py` 18 cases PASSING"
    - "REFAC-03 cerrado: `_core.py` por paquete + `client.py`/`aio.py` shells + import-linter 4 contracts verde + cross-leak sentinel verde"
    - "`.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` con `nyquist_compliant: true` (todas las gates pasan)"
    - "Operator checkpoint: human verificación final sobre que (a) toda CI matrix verde, (b) los SUMMARY.md de Plans 1-5 documentan drop LOC + B8 + CR-03/CR-05 closure, (c) ningún test fue weakeneado para hacer pasar el refactor (Pitfall 18)"
  artifacts:
    - path: ".planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md"
      provides: "Final validation report with nyquist_compliant: true + green-gate matrix evidence"
      contains: "nyquist_compliant"
  key_links:
    - from: "Plan 6 green gate"
      to: "Plans 02-05 deliverables"
      via: "depends_on enforces ordering; this plan re-verifies the combined result"
      pattern: "lint-imports|test_sync_async_isolation|test_matriz_sweep_snapshot|test_public_surface"
---

<objective>
Green-gate consolidation Phase 7 — verificar que el delivery agregado de Plans 1-5 satisface los 5 success criteria de ROADMAP Phase 7 sobre la matriz CI completa (Python 3.12 + 3.13) + ruff + mypy strict + pytest + lint-imports + cross-leak sentinel + matriz sweep snapshot + public surface snapshot zero-diff. Producir `07-VALIDATION.md` con `nyquist_compliant: true` si todas las gates pasan; checkpoint de human-verify final antes de cerrar la phase.

Purpose: Phase 7 success criteria #5 ("CI green final para Python 3.12/3.13") + cierre formal de la phase (D-12 Plan 6 — mirror Phase 6 Plan 7). Sin este plan, los Plans 2-5 podrían dejar un estado parcial en HEAD donde alguna gate aislada falla porque depende de un cambio cross-paquete que no fue testeado conjuntamente.

Output: 1 nuevo `07-VALIDATION.md` con evidencia de toda la matrix verde. Checkpoint final human-verify antes de cerrar phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-02-SUMMARY.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-03-SUMMARY.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-04-SUMMARY.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-05-SUMMARY.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-07-PLAN.md
@CLAUDE.md
@pyproject.toml
@.github/workflows/ci.yml
@verification/test_public_surface.py
@verification/test_sync_async_isolation.py
@verification/test_matriz_sweep_snapshot.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run full green-gate matrix locally and produce 07-VALIDATION.md with evidence</name>
  <files>.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-07-PLAN.md (Phase 6 plantilla green gate — Plan 6 mirror)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md (preexisting validation file — existe; revisar shape para EDITAR con add evidence, no overwrite arbitrario)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ "Validation Architecture" — automated commands por requirement; Sampling Rate; Wave 0 Gaps; § Sources sobre 393 baseline)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md (cuando exista — Plan 1 output)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-02-SUMMARY.md (Plan 2 LOC drop ámbito + B8)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-03-SUMMARY.md (Plan 3 LOC drop iol + CR-01)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-04-SUMMARY.md (Plan 4 LOC drop higyrus + URL-encoding)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-05-SUMMARY.md (Plan 5 LOC drop matriz + CR-03 + CR-05 + snapshot guard)
    - .github/workflows/ci.yml (verify lint-imports step exists; CI matrix Python 3.12 + 3.13)
  </read_first>
  <behavior>
    - Correr secuencialmente todas las gates locally + capturar output exacto + escribirlo a `07-VALIDATION.md` con marca `nyquist_compliant: true` si todas pasan.
    - El bloque `nyquist_compliant: true` es un signal binario para `/gsd-verify-work`: si ANY gate fails, marca `nyquist_compliant: false` y describe el blocker.
    - Documentar drop LOC agregado por paquete (consolidar Plans 02-05 SUMMARY.md drops):
      ```
      | Pkg | client.py (before→after) | aio.py (before→after) | Aggregate drop |
      |-----|--------------------------|-----------------------|----------------|
      | ámbito  | 270 → X  | 287 → X  | -Y%  PASS/FAIL  |
      | iol     | 522 → X  | 476 → X  | -Y%  PASS/FAIL  |
      | higyrus | 685 → X  | 669 → X  | -Y%  PASS/FAIL  |
      | matriz  | 754 → X  | 103 (UNCHANGED) | -Y% client.py only  PASS/FAIL  |
      ```
    - Documentar evidencia CR-03 + CR-05:
      - CR-03: `test_parse_envelope_consumes_body_before_raise` PASSED.
      - CR-05: `verification/test_matriz_sweep_snapshot.py` 18 cases PASSED.
    - Documentar 4 contracts import-linter: output literal de `uv run lint-imports`.
    - Documentar cross-leak sentinel: output literal de `uv run pytest verification/test_sync_async_isolation.py -v`.
    - Documentar test count baseline-vs-final: `pytest --collect-only -q | tail -1`.
  </behavior>
  <action>
    1. Correr secuencialmente y capturar output:
       ```
       uv lock --check
       uv sync --all-packages --all-extras --dev --frozen
       uv run ruff check .
       uv run ruff format --check .
       uv run mypy
       uv run lint-imports
       uv run pytest verification/test_public_surface.py -v
       uv run pytest verification/test_sync_async_isolation.py -v
       uv run pytest verification/test_matriz_sweep_snapshot.py -v
       uv run pytest -q 2>&1 | tail -10
       ```

    2. Capturar también:
       - `wc -l packages/ambito-financiero-client/src/ambito_financiero_client/client.py packages/ambito-financiero-client/src/ambito_financiero_client/aio.py packages/ambito-financiero-client/src/ambito_financiero_client/_core.py`
       - Idéntico para iol, higyrus, matriz.

    3. Leer `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` existente — el file existe (`-rw-r--r--@ 1 sebadlf staff 2205 Jun 12 12:51`). EDITAR (no overwrite) preservando cualquier metadata pre-existente; agregar las secciones nuevas con la evidencia.

    4. Actualizar `07-VALIDATION.md` con secciones:
       - **Phase 7 — Green Gate Final**: front-matter `nyquist_compliant: true` (o `false` si gate falla) + `phase_status: ready_for_verify` (o `blocked`).
       - **LOC drop summary** (matrix per package).
       - **CR-03 verification**: `test_parse_envelope_consumes_body_before_raise` output line.
       - **CR-05 verification**: `verification/test_matriz_sweep_snapshot.py` parametrize output line.
       - **REFAC-03 verification**: import-linter output + cross-leak sentinel output.
       - **Public surface snapshot**: `verification/test_public_surface.py` output.
       - **Test count**: baseline (393 from RESEARCH.md / Pitfall 4 / A5) → final (post-phase).
       - **Pitfall 18 statement**: explicit assertion "No tests were weakened during Phase 7 — all pre-existing tests in `packages/*/tests/` + `verification/` pass with their original assertions; only new tests were added."

    5. Si CUALQUIER gate falla:
       - Marcar `nyquist_compliant: false`.
       - Documentar el blocker exacto (test name + error output).
       - Tarea 2 checkpoint solicita decision del operator.
  </action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv sync --all-packages --all-extras --dev --frozen &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run mypy &amp;&amp; uv run lint-imports &amp;&amp; uv run pytest verification/test_public_surface.py -v &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -v &amp;&amp; uv run pytest verification/test_matriz_sweep_snapshot.py -v &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -10 &amp;&amp; grep -c 'nyquist_compliant: true' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `uv lock --check` exits 0
    - CLI: `uv sync --all-packages --all-extras --dev --frozen` exits 0
    - CLI: `uv run ruff check .` exits 0
    - CLI: `uv run ruff format --check .` exits 0
    - CLI: `uv run mypy` exits 0 (strict)
    - CLI: `uv run lint-imports` exits 0 — output text contains "4 kept, 0 broken" or equivalent success
    - Test command: `uv run pytest verification/test_public_surface.py -v` exits 0 — 4 packages snapshot zero diff
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -v` exits 0 — sync 4/4 PASS; async 3 PASS + 1 SKIPPED (matriz)
    - Test command: `uv run pytest verification/test_matriz_sweep_snapshot.py -v` exits 0 — ≥18 cases PASS
    - Test command: `uv run pytest packages/matriz-client/tests/test_core.py::test_parse_envelope_consumes_body_before_raise -v` PASS (CR-03)
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -10` shows total tests ≥ baseline + new tests (≥393 + ≥26 new); no failures
    - Source: `grep -c 'nyquist_compliant: true' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` == 1
    - Source: `grep -c 'CR-03' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` &gt;= 1
    - Source: `grep -c 'CR-05' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` &gt;= 1
    - Source: `grep -c 'REFAC-03' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` &gt;= 1
    - Source: `grep -ci 'pitfall 18\|no tests were weakened' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` &gt;= 1
    - Source: `grep -c 'LOC drop' .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` &gt;= 1
  </acceptance_criteria>
  <done>
    Todas las gates locally verde; `07-VALIDATION.md` actualizado con `nyquist_compliant: true` + evidencia consolidada.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator checkpoint — review CI matrix on PR + confirm SUMMARY.md drops + green-gate completeness</name>
  <files>.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md</files>
  <action>
    Pause for operator. Operator reviews PR CI matrix + each plan's SUMMARY.md + matriz aio.py preservation per the steps in `<how-to-verify>`, then types "approved" in chat. No file modification — this is the final close-out gate of Phase 7.
  </action>
  <what-built>
    Task 1 completó toda la matrix locally verde y produjo `07-VALIDATION.md` con `nyquist_compliant: true`. Antes de cerrar Phase 7, el operator humano confirma:
    1. CI matrix Python 3.12 + 3.13 corre verde sobre el merge commit (no solo local).
    2. Cada `07-0X-SUMMARY.md` (X=1..5) reporta LOC drop ≥30% para su paquete y B8 / CR-XX / Phase 7 D-XX closure según corresponda.
    3. Ningún test pre-existente fue weakeneado o skipeado para hacer pasar el refactor (Pitfall 18 statement).
    4. Public surface snapshot Phase 6 sigue idéntico (D-16).
    5. PEP 562 shim Phase 6 (deny-list `_user`/`_password`) sigue activa para los 3 paquetes con auth (ámbito no aplica).
    6. matriz `aio.py` sigue siendo el stub Phase 6 (103 LOC), no fue modificado por error.
  </what-built>
  <how-to-verify>
    1. Visit el PR Phase 7 en GitHub (URL provista al final del execute-phase output). Verificar CI matrix status: todos los jobs (lint, lint-imports, typecheck, tests Python 3.12, tests Python 3.13) muestran ✓.
    2. Abrir cada `07-0X-SUMMARY.md` y confirmar:
       - Plan 1: import-linter output + cross-leak guard + test count baseline.
       - Plan 2 (ámbito): LOC drop section con cifras + B8 identity confirmed + Public surface zero diff + Cross-leak guard verde.
       - Plan 3 (iol): LOC drop + B8 + CR-01 conditional refresh preservado.
       - Plan 4 (higyrus): LOC drop + B8 + URL-encoding quirk encapsulated.
       - Plan 5 (matriz): LOC drop client.py + aio.py UNCHANGED + CR-03 + CR-05 + snapshot guard.
    3. Correr `git log --oneline .planning/phases/07-core-py-extraction-sync-async-logic-dedup/` y confirmar 6 commits (uno por plan) con commit messages siguiendo el patrón RESEARCH §"Commit message pattern Phase 7".
    4. Run `wc -l packages/matriz-client/src/matriz_client/aio.py` — debe devolver exactamente `103` (Phase 6 stub unchanged).
    5. Mensaje de aprobación esperado: "approved — all 5 phase 7 success criteria met; CI matrix green; SUMMARYs consistent; Pitfall 18 honored; matriz aio.py preserved."
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all 6 close-out items in `<how-to-verify>` pass; CI matrix on PR is green; SUMMARYs are consistent; matriz aio.py preserved at 103 LOC.</human-check>
  </verify>
  <done>
    Operator types "approved" — Phase 7 ready_for_verify and the STATE.md update + final commit proceed.
  </done>
  <resume-signal>Type "approved" para cerrar la phase y avanzar a STATE.md update + final commit. Type "blocked: &lt;reason&gt;" si encuentra alguna gate inconsistente.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local execution → CI matrix (Python 3.12 + 3.13) | Task 1 corre local; Task 2 verifica CI |
| Plan 1-5 isolated outputs → consolidated state | Task 1 valida el delta combinado, no solo cada plan aislado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-CI-DRIFT | Tampering (CI integrity) | Local verde pero CI Python 3.13 rojo (e.g., httpx version diff resolve diff) | mitigate | Task 2 human-verify CHECKS CI matrix status on PR (not just local) |
| T-7-WEAK | Tampering (test weakening — Pitfall 18) | Algún Plan 2-5 podría haber removido o skipeado un test pre-existente para hacer pasar el refactor | mitigate | Task 1 incluye Pitfall 18 statement; Task 2 human-verify spotchecks SUMMARYs por mention de skipped/removed tests |
| T-7-D16-DRIFT | Tampering (API contract) | Public surface snapshot accidentally cambia (e.g., `_core` exposed in `__all__`) | mitigate | Task 1 corre `verification/test_public_surface.py` parametrizado; Task 2 human-verify confirma |
| T-7-MATRIZ-AIO | Tampering (Phase scope creep) | matriz `aio.py` accidentally modificado en Plan 5 (debería seguir stub Phase 6 → Phase 10) | mitigate | Task 1 captura `wc -l matriz/aio.py == 103` en 07-VALIDATION.md; Task 2 human-verify re-corre `wc -l` confirma |
</threat_model>

<verification>
- 07-VALIDATION.md con `nyquist_compliant: true`.
- CI matrix verde (operator confirms via PR status).
- Plans 1-5 SUMMARYs consistent.
- Pitfall 18 honored: no pre-existing test weakened.
- Public surface snapshot Phase 6 unchanged.
- matriz aio.py UNCHANGED (103 LOC stub).
</verification>

<success_criteria>
- `nyquist_compliant: true` en 07-VALIDATION.md.
- Toda CI matrix verde (Python 3.12 + 3.13).
- 5 Phase 7 ROADMAP success criteria cubiertos:
  1. `_core.py` por paquete con builders/parsers/auth-flow primitives — verified by Plans 2-5 SUMMARYs.
  2. CI rule (import-linter) + cross-leak sentinel — verified by Plan 1 + ongoing in Plans 2-5.
  3. LOC drop ≥30% per package — verified by 07-VALIDATION.md table.
  4. CR-03 cerrado — verified by `test_parse_envelope_consumes_body_before_raise` PASS.
  5. CR-05 cerrado — verified by `verification/test_matriz_sweep_snapshot.py` 18 cases PASS.
- Operator human-verify approval marca phase ready_for_verify.
</success_criteria>

<output>
Actualizar `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-VALIDATION.md` con `nyquist_compliant: true` + evidencia consolidada (Task 1).
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-06-SUMMARY.md` documentando:
- Green gate matrix verde.
- LOC drop table consolidado (4 paquetes).
- Test count baseline → final.
- CR-03 + CR-05 + REFAC-03 closure evidence.
- Operator approval marca + timestamp.
</output>
