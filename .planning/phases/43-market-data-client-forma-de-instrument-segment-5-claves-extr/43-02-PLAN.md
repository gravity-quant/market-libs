---
phase: 43-market-data-client-forma-de-instrument-segment-5-claves-extr
plan: 02
type: tdd
wave: 2
depends_on: ["43-01"]
files_modified:
  - packages/market-data-client/src/market_data_client/models.py
  - packages/market-data-client/tests/test_core.py
  - packages/market-data-client/tests/test_reference_models.py
autonomous: true
requirements: [HARN-02]

must_haves:
  truths:
    - "Las cinco claves extra medidas quedan declaradas y decodifican tipadas: symbols_never_delivered, last_error_age_seconds, last_error_at, subscription y note (D-08 a D-11)"
    - "Ninguna de las cinco aparece como missing sobre un payload sano — el flip extra hacia missing es una regresion disfrazada de fix (criterio 3)"
    - "subscription se lee como un modelo tipado de 15 campos, no como un mapping opaco que el walker no camina ni reporta (D-08)"
    - "Toda clave de fixture de health es subconjunto del key-set medido, verificado por un helper explicito y sin tocar ningun baseline write-once (D-13)"
    - "Symbol declara note nullable, asi que los cuatro endpoints que comparte un solo modelo decodifican sin divergencia (D-10)"
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/models.py"
      provides: "FeedSubscription nuevo, tres campos nuevos en FeedIngestor, uno en HealthFeed, Symbol.note, y models.__all__ actualizado"
      contains: "class FeedSubscription(SafeModel)"
    - path: "packages/market-data-client/tests/test_core.py"
      provides: "fixture _MEASURED_HEALTH_FEED_43, helper _keys_recursive, test de subconjunto de claves y test de cero divergencias sobre el payload medido"
      contains: "_MEASURED_HEALTH_FEED_43"
  key_links:
    - from: "packages/market-data-client/tests/test_core.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "HealthFeed.from_api sobre el payload medido produce cero records de divergencia"
      pattern: "_MEASURED_HEALTH_FEED_43"
    - from: "packages/market-data-client/src/market_data_client/models.py"
      to: "packages/market-data-client/src/market_data_client/_decode.py"
      via: "FeedSubscription no-opcional colapsa al Null Object con SILENT_SINK cuando la clave falta; los dos campos condicionales retornan por la rama Union sin emitir record"
      pattern: "subscription: FeedSubscription"
    - from: "packages/market-data-client/tests/test_public_surface_market_data.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "toda subclase de SafeModel del modulo debe estar en models.__all__ — se satisface sin editar ese test"
      pattern: "FeedSubscription"
---

<objective>
Tipar las cinco claves `extra` restantes que el censo de divergencias sigue reportando contra
`market-data-client`, en el mismo archivo `models.py` que la correccion de forma del plan 43-01:
`HealthFeed.symbols_never_delivered` plano (D-11), `FeedIngestor.last_error_age_seconds` y
`.last_error_at` nullables (D-09), `FeedIngestor.subscription` como una dataclass anidada nueva
`FeedSubscription` de 15 campos (D-08), y `Symbol.note` nullable (D-10). Mas el helper de subconjunto
de claves de D-13 que cierra el criterio 4 de la fase.

Cubre las decisiones locked **D-08, D-09, D-10, D-11, D-13** y la mitad HARN-02 de **D-12**.

Purpose: el criterio 3 de la fase exige que las cinco dejen de reportarse `extra` **sin** que ninguna
aparezca como `missing` en su lugar. La mecanica que lo permite ya esta medida: la rama `Union` del
walker retorna sin emitir record, y un modelo anidado no-opcional colapsa al Null Object con sink
silencioso. El unico campo declarado plano (D-11) SI emite `missing` sobre la fixture congelada del
2026-07-31, y eso rompe dos tests que D-12 no lista.

Output: `models.py` con los cuatro modelos de health y `Symbol` completos, `models.__all__`
actualizado, y `test_core.py` con la fixture medida nueva mas los tres tests de evidencia.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-CONTEXT.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-PATTERNS.md
@.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-01-SUMMARY.md
</context>

<interface_context>
## `FeedSubscription` — clase nueva, 15 campos, ningun default

Declarada `@dataclass(frozen=True, slots=True)` heredando `SafeModel`, **antes** de `FeedIngestor`
en el archivo (el bloque de comentario de `models.py:1137` fija el orden por dependencia).

`chunk_size: int`, `chunks: int`, `confirm_seconds: int`, `delivered_count: int`,
`forced_reconnects: int`, `last_reconnect_reason: str`, `quarantined_count: int`,
`quarantined_symbols: list[str]`, `requested: int`, `sent: int`, `smd_rejections: int`,
`smd_resends: int`, `smd_unattributed: int`, `unconfirmed_count: int`, `unconfirmed_symbols: list[str]`.

Sin override de `from_api`. Sin campo `received_at`. Sin ningun campo anotado como mapping.

## Cola nueva de `FeedIngestor` (orden load-bearing)

`... last_frame_at: str`, `started_at: str`, `market: FeedMarket`, `pipeline: FeedPipeline`,
`subscription: FeedSubscription`, `last_error: str | None = None`,
`last_error_age_seconds: int | None = None`, `last_error_at: str | None = None`.

`subscription` NO lleva default, asi que va antes del primer campo con default. Y NO puede
declararse `FeedSubscription | None`: la regla D-NO-01 de `tools/check_surface_types.py` reddenea
todo campo `Model | None` en una clase exportada.

## `HealthFeed` — un campo mas

`symbols_never_delivered: int` — **plano, no nullable** (D-11, doctrina option-b / restraint).

## `Symbol` — un campo mas

`note: str | None = None`, al final del bloque con default (despues de `received_at`).

## `models.__all__`

Gana la entrada `"FeedSubscription"`, en orden alfabetico estricto, entre `"FeedPipeline"` y
`"Health"` (`models.py:105`).

## Key-set medido de `GET /health/feed` (blob "Actual" de F-71 / F-202)

Nivel raiz: `active_symbols` int, `ingestor` dict, `newest_received_at` str,
`oldest_received_at` str, `staleness_seconds` float, `status` str, `symbols_never_delivered` int,
`symbols_with_data` int.

`ingestor`: `connected` bool, `frames_total` int, `heartbeat_age_seconds` float, `last_error` str,
`last_error_age_seconds` int, `last_error_at` str, `last_frame_age_seconds` float,
`last_frame_at` str, `market` dict, `pipeline` dict, `present` bool, `reason` str, `reconnects` int,
`rows_written` int, `started_at` str, `state` str, `subscription` dict, `symbols_subscribed` int,
`uptime_seconds` int.

`ingestor.subscription`: las 15 claves de `FeedSubscription`, con `quarantined_symbols` como lista
de str y `unconfirmed_symbols` como lista vacia (elemento nunca observado poblado).

`ingestor.market` e `ingestor.pipeline`: identicos a los de `_CAPTURED_HEALTH_FEED`
(`test_core.py:962-1004`), sin cambio.

## Simbolos existentes que se reutilizan sin re-implementar

- `_CAPTURED_HEALTH_FEED` y `_CAPTURED_HEALTH` — `test_core.py:958-1010`
- `_from_api(factory, caplog, payload)` — `test_core.py:1043-1051`
- fixture `pristine_decode_context` — `test_core.py:1019-1041`
- `_strip_optional` — `test_core.py:63`
- `_schema_of` — `test_core.py:1011-1017`
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — fixture medida, helper de subconjunto y los tres tests de evidencia de HARN-02</name>

  <files>packages/market-data-client/tests/test_core.py, packages/market-data-client/tests/test_reference_models.py</files>

  <read_first>
    - `packages/market-data-client/tests/test_core.py:930-1210` — el bloque completo de health: el comentario de provenance (:936-944), `_CAPTURED_HEALTH` y `_CAPTURED_HEALTH_FEED` (:957-1009), `_schema_of` (:1011-1017), la fixture `pristine_decode_context` (:1019-1041), el helper `_from_api` (:1043-1051), `test_captured_payloads_match_the_committed_live_schemas` (:1053-1062), T14 `test_health_feed_from_api_drops_an_undeclared_key_and_reports_it_once` (:1125-1137), T13 `test_health_feed_to_dict_round_trips_all_three_levels` (:1145-1150), los tres parametrizados sobre las seis clases de health (:1152-1181) y T11 `test_health_models_declare_exactly_the_two_locked_optionals` (:1183-1199)
    - `packages/market-data-client/tests/test_reference_models.py:211-228` — `test_symbol_field_set_matches_reconciled_wire`, el unico sitio de este archivo que este plan toca
    - `packages/market-data-client/src/market_data_client/models.py:1133-1343` — el bloque de health ACTUAL, incluido el veredicto de nulabilidad de :1146-1156
    - `packages/market-data-client/src/market_data_client/models.py:869-876` — la cola de campos de `Symbol`, donde entra `note`
    - `.planning/verification/market-data-client-findings.md` entradas F-70, F-71 y F-202 — el blob medido verbatim del que sale la fixture nueva
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md`, secciones "Mecanica del walker", "Pitfall 2", "Pitfall 3" y "Code Examples · El helper de subconjunto de D-13"
  </read_first>

  <behavior>
    - `test_every_fixture_key_is_a_measured_wire_key`: el conjunto de rutas de clave punteadas de `_CAPTURED_HEALTH_FEED` es subconjunto del de `_MEASURED_HEALTH_FEED_43`. Es la asercion del criterio 4 y es verde por construccion segun el diff de F-71.
    - `test_feed_subscription_decodes_the_measured_blob`: decodificar el payload medido pobla los 15 campos de `subscription` con sus valores, `quarantined_symbols` es una `list[str]` poblada y `unconfirmed_symbols` es la lista vacia.
    - `test_measured_health_feed_payload_produces_zero_divergence_records`: sobre el payload medido, `HealthFeed.from_api` bajo scope fresco emite **cero** records — ni `extra` ni `missing`. Es la prueba directa del criterio 3.
    - `test_healthy_feed_payload_emits_no_missing_for_the_conditional_error_fields`: sobre un payload SANO (el de `_CAPTURED_HEALTH_FEED`, que no trae ninguna de las dos claves condicionales), ninguna tupla de records nombra `last_error_age_seconds` ni `last_error_at` con `missing`. Si aparecen, alguien las declaro planas contra D-09.
    - T14 re-derivado: la lista exacta de records sobre la fixture congelada mas una clave desconocida pasa a tener **dos** elementos — la clave desconocida como `extra` y `symbols_never_delivered` como `missing`, con un comentario que explique por que ese `missing` es correcto y esperado bajo la doctrina option-b, y por que los otros tres campos nuevos NO contribuyen.
    - T13 re-derivado: el round-trip de `to_dict()` sobre la fixture congelada asserta el delta declarado explicitamente (los cuatro campos nuevos proyectados), no una igualdad que ya no puede valer.
    - T11 re-derivado: el conjunto exacto de optionals gana `FeedIngestor.last_error_age_seconds` y `FeedIngestor.last_error_at`.
    - T3 re-derivado en `test_reference_models.py`: el field-set exacto de `Symbol` gana `"note"`.
    - Los tres tests parametrizados sobre las seis clases de health (frozen, sin override de `from_api`, sin `received_at`) pasan a parametrizarse sobre **siete**, incluyendo `FeedSubscription`.
  </behavior>

  <action>
Escribir la especificacion ejecutable ANTES de tocar el modelo: al terminar, los tests nuevos y los
re-derivados deben FALLAR. No tocar `models.py` en este task.

**A. `test_core.py` — fixture medida nueva (D-13)**

Agregar `_MEASURED_HEALTH_FEED_43: dict[str, Any]` con el key-set completo de
`<interface_context>`, valores SINTETICOS de los tipos medidos. Colocarla inmediatamente despues de
`_CAPTURED_HEALTH_FEED` (`:1009`), con un comentario de provenance que diga: key-set tomado del blob
"Actual" de F-202 y F-71 en `.planning/verification/market-data-client-findings.md`, corrida del
2026-08-31; valores sinteticos; y —explicito— que **NO reemplaza ni refresca ningun baseline** de
`.planning/verification/schemas/market-data-client/`, que son write-once (D-25) y que
`42-WIRE-READ.md` seccion 3 marca NO-AUTORITATIVOS para esta fase.

`_CAPTURED_HEALTH_FEED` (`:962-1009`) queda **CONGELADA, sin una sola clave agregada**. Agregarle las
cinco claves nuevas es el arreglo "obvio" y esta PROHIBIDO por D-13: rompe
`test_captured_payloads_match_the_committed_live_schemas` (`:1053-1062`), que compara `_schema_of` de
esa fixture contra el baseline committeado por IGUALDAD EXACTA. Ese test no se toca y debe seguir
verde sin edicion.

Sub-diferencias a respetar respecto de `_CAPTURED_HEALTH_FEED`: en la fixture medida
`ingestor.last_error` es una cadena poblada (no `None`), y aparecen las tres claves nuevas de
`ingestor` mas `symbols_never_delivered` a nivel raiz. `ingestor.market` e `ingestor.pipeline` son
identicos, incluido `pipeline.last_write_error` en `None`.

**B. `test_core.py` — helper `_keys_recursive`**

Funcion de modulo que devuelve el conjunto de rutas de clave punteadas de un payload anidado,
recursando solo sobre dicts (un valor no-dict aporta la ruta pero no recursa). Docstring de una
linea. Tipada para mypy strict.

**C. `test_core.py` — los cuatro tests nuevos** descritos en `<behavior>`. Los tres que capturan
records usan `_from_api` (nunca `Model.from_api` directo) y llevan el marcador
`@pytest.mark.usefixtures("pristine_decode_context")`, como T14 (`:1112`, `:1126`): sin el scope
fresco, el dedupe por triple modelo/path/kind de un test previo apaga la asercion en silencio y la
flipea verde puramente por ORDEN de tests.

**D. `test_core.py` — re-derivar los tres tests que rompen**

- **T14** (`:1125-1137`): la lista de records pasa de un elemento a dos. Ordenar el `assert` segun el
  orden real en que el walker emite (las claves sobrantes se calculan primero, en `walk_model`, y
  luego se camina campo por campo). Escribir el comentario que explica los dos hechos que este test
  ahora pinnea: (i) el unico campo nuevo declarado PLANO emite `missing` sobre una fixture que no
  lleva la clave, y eso es correcto bajo la doctrina option-b; (ii) los otros tres campos nuevos NO
  contribuyen ningun record — los dos `| None` retornan por la rama `Union` sin emitir, y el modelo
  anidado no-opcional colapsa al Null Object con sink silencioso (NOBJ-02). Ese comentario es la
  evidencia mecanica de que el criterio 3 se esta evaluando bien.
- **T13** (`:1145-1150`): `SafeModel.to_dict()` es `dataclasses.asdict` puro y proyecta TODOS los
  campos declarados, poblados o no, asi que la igualdad contra la fixture congelada ya no puede
  valer. Re-derivarla a la igualdad contra el delta declarado explicitamente: la fixture congelada
  mas `symbols_never_delivered` en su cero tipado, mas —dentro de `ingestor`— la proyeccion de
  `FeedSubscription` vacia (`FeedSubscription.empty().to_dict()`) y los dos campos condicionales en
  `None`. Conservar las dos aserciones de que los agujeros `None` se preservan. El fallo de este test
  se ve como un diff gigante de dict en pytest, no como un `AttributeError`; es facil leerlo mal como
  "el walker se rompio".
- **T11** (`:1183-1199`): el conjunto exacto de optionals gana las dos entradas de D-09. Actualizar
  el docstring, que hoy dice que un septimo Optional no puede agregarse sin fallar aqui: pasa a
  nombrar los cuatro y a decir cual evidencia medida respalda cada uno.

**E. `test_core.py` — los tres parametrizados** (`:1152-1181`): agregar `FeedSubscription` a las tres
listas de `@pytest.mark.parametrize`. Los tres pasan con o sin el, pero agregarlo es lo consistente y
extiende la red a la clase nueva.

**F. `test_reference_models.py`** — unico sitio que este plan toca en ese archivo:
`test_symbol_field_set_matches_reconciled_wire` (`:211-228`) gana `"note"` en el conjunto exacto.
Actualizar su comentario para nombrar la sexta clave wire-only y su procedencia (presente en los acks
de escritura, ausente en las filas de `GET /symbols`; F-109 y F-140). Es una edicion de una linea mas
comentario, no un test nuevo. NO tocar nada mas de ese archivo — el resto quedo cerrado en 43-01.

**G. Regla transversal:** ningun test lee `.planning/verification/captures/`. Ninguna fixture nueva
transcribe un valor del capture crudo: claves y tipos reales, valores inventados.

**H. Prohibido:** editar cualquier `.json` bajo `.planning/verification/schemas/market-data-client/`.
Son write-once (D-25) y registran el WIRE, no el modelo; corregir el modelo no los invalida.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client/tests/test_core.py packages/market-data-client/tests/test_reference_models.py -q --no-cov; echo "exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_core.py -q --no-cov` termina con exit code distinto de 0 (RED) y el resumen nombra al menos `test_feed_subscription_decodes_the_measured_blob`, `test_measured_health_feed_payload_produces_zero_divergence_records`, `test_health_feed_from_api_drops_an_undeclared_key_and_reports_it_once`, `test_health_feed_to_dict_round_trips_all_three_levels` y `test_health_models_declare_exactly_the_two_locked_optionals`.
    - `uv run pytest packages/market-data-client/tests/test_core.py --collect-only -q --no-cov` termina con exit code 0. Un fallo de COLECCION es un error de sintaxis, no un RED valido.
    - `uv run pytest packages/market-data-client/tests/test_core.py::test_every_fixture_key_is_a_measured_wire_key -q --no-cov` termina con exit code 0 ya en RED: la asercion de subconjunto de D-13 es verde por construccion y no depende del cambio de modelo.
    - `uv run pytest "packages/market-data-client/tests/test_core.py::test_captured_payloads_match_the_committed_live_schemas" -q --no-cov` termina con exit code 0 (la fixture congelada no fue tocada).
    - `git diff --name-only` lista exactamente dos archivos: `packages/market-data-client/tests/test_core.py` y `packages/market-data-client/tests/test_reference_models.py`.
    - `git status --porcelain .planning/verification/schemas/` no produce salida (ningun baseline write-once fue tocado).
    - `grep -c 'verification/captures' packages/market-data-client/tests/test_core.py` devuelve `0`.
  </acceptance_criteria>

  <done>
    La fixture medida, el helper de subconjunto y los cuatro tests nuevos existen; los tres tests
    re-derivados fallan por la razon esperada (campo o clave inexistente, conjunto distinto); el test
    de igualdad contra el baseline write-once sigue verde; ningun baseline ni la fixture congelada
    fueron tocados.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — FeedSubscription, los tres campos condicionales, el campo plano y Symbol.note</name>

  <files>packages/market-data-client/src/market_data_client/models.py</files>

  <read_first>
    - `packages/market-data-client/src/market_data_client/models.py:1133-1343` — el bloque de health completo: el comentario de orden por dependencia y el veredicto de nulabilidad (:1133-1156), `FeedMarket` (:1195-1226), `FeedPipeline` (:1229-1260) —el analog mas cercano en forma para la clase nueva—, `FeedIngestor` (:1263-1303) y `HealthFeed` (:1306-1343)
    - `packages/market-data-client/src/market_data_client/models.py:816-876` — `Symbol`, con su bloque de campos con default donde entra `note`
    - `packages/market-data-client/src/market_data_client/models.py:95-123` — `models.__all__`, alfabetico estricto
    - `packages/market-data-client/tests/test_core.py` — los tests recien escritos que definen el contrato objetivo
    - `.planning/verification/market-data-client-findings.md` entrada F-71 — el blob de `subscription` de 15 claves verbatim
    - `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-RESEARCH.md`, secciones "Patron 2 — Modelo anidado tipado" y "El gate `surface-types`, exactamente"
  </read_first>

  <action>
Editar SOLO `packages/market-data-client/src/market_data_client/models.py`. NO tocar
`__init__.py`, `client.py`, `aio.py`, `pyproject.toml` ni `uv.lock`: D-16 lockea el alcance y el
release es la Phase 44.

**(1) `FeedSubscription` — clase nueva.** Insertarla ENTRE `FeedPipeline` y `FeedIngestor` (despues
de `:1260`, antes de `:1263`). El bloque de comentario de `:1137` fija que las clases se declaran en
orden de dependencia para que todo tipo anidado exista antes que su padre; `from __future__ import
annotations` haria funcionar la forward-ref, pero la convencion del archivo es explicita y es lo que
se sigue.

Los 15 campos verbatim de `<interface_context>`, en el orden ahi listado, ninguno con default —
las 15 volvieron pobladas en la captura medida, asi que la doctrina option-b (`models.py:1146-1156`)
no admite declarar ninguna nullable.

Cuatro invariantes que los tests del paquete ya pinnean y que esta clase debe respetar:
declararse antes de `FeedIngestor`; **sin** override de `from_api` (el walker construye un modelo
anidado desempaquetando el resultado de `walk_model`, nunca llamando a `from_api`, asi que un
override anidado seria silenciosamente salteado y pareceria un fix sin serlo); **ningun** campo
anotado como mapping; **ningun** `received_at`.

Docstring con cuatro parrafos:
  a. que sub-objeto de que endpoint es, y que fase/requisito lo agrega;
  b. procedencia: field set tomado verbatim del blob medido de F-71 y F-202 en
     `market-data-client-findings.md`, corrida del 2026-08-31; no de la OpenAPI y no de un mock;
  c. por que un mapping sin tipar NO era una opcion disponible: el paso `surface-types` del job
     `lint` (`tools/check_surface_types.py`) reddenea cualquier campo asi anotado en una clase
     exportada —su tabla de exenciones tiene una unica entrada, ajena a este paquete— y
     `_decode.walk_field` no tiene rama para mappings: cae al retorno final sin caminar ni reportar,
     asi que un mapping seria un punto ciego permanente en el censo de divergencias;
  d. la ASUNCION DECLARADA sobre `unconfirmed_symbols`: el wire mando la lista vacia y el tipo de
     elemento nunca se observo poblado; se tipa `list[str]` espejando a su hermana poblada
     `quarantined_symbols`, y una eleccion equivocada aflora RUIDOSAMENTE en el proximo censo de
     divergencias (un record `type`) en vez de silenciosamente. Mismo patron de asuncion
     semi-observada que `FeedIngestor.last_error` (`:1271-1275`).

**(2) `FeedIngestor` — tres campos nuevos** (`:1288-1303`). La cola queda exactamente como en
`<interface_context>`: `subscription: FeedSubscription` va junto a `market` y `pipeline`, **antes**
de `last_error`, porque no lleva default; los dos condicionales de D-09 van **despues** de
`last_error`. Un campo sin default detras de uno con default levanta un `TypeError` en tiempo de
import y revienta la coleccion entera de pytest.

`subscription` NO puede declararse como un modelo opcional: la regla D-NO-01 de
`tools/check_surface_types.py` reddenea todo campo de clase exportada anotado como modelo del
import-root con `| None`. D-08 ya lo pide no-opcional; esta es la razon mecanica y es un gate
independiente del de mapping.

Ampliar el docstring de `FeedIngestor` con la justificacion de `| None` de los dos condicionales:
ambas claves estan AUSENTES del baseline sano del 2026-07-31 (donde `last_error` es `null` y ninguna
de las dos companeras aparece) y PRESENTES en toda captura posterior junto a un `last_error`
poblado — son condicionales a que exista un error, no siempre presentes. Declararlas planas emitiria
un record `missing` en cada llamada sana al endpoint, que es exactamente lo que el criterio 3 de la
fase prohibe. Y agregar la nota de que `subscription` es un modelo anidado no-opcional, asi que una
clave ausente colapsa al Null Object sin emitir record.

**(3) `HealthFeed` — un campo nuevo** (`:1337-1343`): `symbols_never_delivered: int`, **plano**.
Ampliar el docstring con el parrafo inverso de "deliberadamente NO nullable": la clave esta ausente
solo del baseline stale del 2026-07-31 y presente en las tres capturas posteriores; un `Optional`
sobre-declarado absorberia en silencio un futuro `null` sin dejar record de divergencia. Este es el
UNICO campo nuevo que emite un `missing` contra la fixture congelada, y ese `missing` es la
evidencia —no la regresion— que el T14 re-derivado del Task 1 pinnea.

**(4) `Symbol.note`** (`:869-876`): agregar `note: str | None = None` al final del bloque con
default, despues de `received_at`. Ampliar el docstring de `Symbol` con la justificacion de `| None`:
la clave esta presente en los acks de escritura (`create-symbol-sync-response.json`,
`update-symbol-sync-response.json`) y ausente de las filas de `GET /symbols`
(`get-symbols-probe-prefix-sync.json`); un solo modelo sirve los cuatro endpoints
(`_core.py:1086-1094`), asi que es condicional por forma de respuesta — mismo argumento que los dos
campos de D-09. NO tocar el override `from_api` de `Symbol` ni ninguno de sus otros campos.

**(5) Actualizar el bloque de veredicto de nulabilidad** (`:1146-1149`). Hoy afirma que exactamente
DOS campos califican como nullables y los nombra; con D-09 pasan a ser cuatro. Reescribir esa
oracion para nombrar los cuatro y decir cual evidencia medida respalda cada par. El resto del bloque
(`:1150-1156`), que argumenta por que un `Optional` sobre-declarado esconde el campo del censo,
sigue siendo verdadero y **no se toca**: es exactamente la doctrina que D-09 y D-11 aplican.

**(6) `models.__all__`** (`:95-123`): agregar `"FeedSubscription"` en orden alfabetico estricto,
entre `"FeedPipeline"` y `"Health"`. Es OBLIGATORIO:
`test_public_surface_market_data.py::test_models_dunder_all_covers_every_safemodel_subclass`
(`:102-121`) deriva el conjunto esperado del propio modulo y exige que toda subclase de `SafeModel`
declarada ahi este en `models.__all__`. Ese test NO se edita — se satisface desde aca.

**(7) NO agregar `FeedSubscription` al `__all__` del paquete** (`__init__.py`). D-16 lockea el
alcance de la fase a `models.py` mas tests mas el docstring de D-14, y ningun test lo exige
(`test_models_dunder_all_covers_every_safemodel_subclass` solo mira `models.__all__`; los dos tests
que miran el `__all__` del paquete iteran una lista hand-maintained que no cambia). Se registra como
item de seguimiento para la Phase 44, junto con el release.
  </action>

  <verify>
    <automated>cd /Users/admin/development/market-libs; uv run pytest packages/market-data-client -q --no-cov; uv run python tools/check_surface_types.py; uv run mypy; uv run mypy packages/market-data-client/tests</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q --no-cov` termina con exit code 0, con al menos 711 tests pasados y sin fallos ni errores.
    - `uv run python -c "import dataclasses; from market_data_client.models import FeedSubscription as F; print(len(dataclasses.fields(F))); print(sorted(f.name for f in dataclasses.fields(F)))"` imprime `15` y luego exactamente `['chunk_size', 'chunks', 'confirm_seconds', 'delivered_count', 'forced_reconnects', 'last_reconnect_reason', 'quarantined_count', 'quarantined_symbols', 'requested', 'sent', 'smd_rejections', 'smd_resends', 'smd_unattributed', 'unconfirmed_count', 'unconfirmed_symbols']`.
    - `uv run python -c "from market_data_client.models import FeedSubscription as F; print(F.__dict__.get('from_api') is None); print('received_at' not in {f.name for f in __import__('dataclasses').fields(F)})"` imprime `True` y `True`.
    - `uv run python -c "import dataclasses; from market_data_client.models import FeedIngestor as F; print([f.name for f in dataclasses.fields(F)][-4:])"` imprime `['subscription', 'last_error', 'last_error_age_seconds', 'last_error_at']`.
    - `uv run python -c "import dataclasses; from market_data_client.models import HealthFeed as H, Symbol as S; print('symbols_never_delivered' in {f.name for f in dataclasses.fields(H)}); print('note' in {f.name for f in dataclasses.fields(S)})"` imprime `True` y `True`.
    - `uv run python -c "from market_data_client import models; print(models.__all__.index('FeedSubscription') == models.__all__.index('FeedPipeline') + 1); print(list(models.__all__) == sorted(models.__all__))"` imprime `True` y `True`.
    - `uv run python tools/check_surface_types.py` termina con exit code 0 y reporta `0 violations`.
    - `uv run mypy` y `uv run mypy packages/market-data-client/tests` terminan ambos con exit code 0.
    - `uv run ruff check .` y `uv run ruff format --check .` terminan con exit code 0.
    - `git diff --name-only` lista exactamente `packages/market-data-client/src/market_data_client/models.py`.
    - `git status --porcelain .planning/verification/schemas/ uv.lock packages/market-data-client/pyproject.toml packages/market-data-client/src/market_data_client/__init__.py` no produce salida.
  </acceptance_criteria>

  <done>
    Las cinco claves estan declaradas con el tipo que su evidencia medida respalda, el payload medido
    decodifica con cero records de divergencia, ninguna de las cinco emite `missing` sobre un payload
    sano, los dos gates estaticos de `surface-types` siguen en cero violaciones, y ni la version ni
    ningun baseline write-once fueron tocados.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts que este plan produce

**Clase nueva en `models.py`:** `FeedSubscription(SafeModel)` — `@dataclass(frozen=True, slots=True)`,
15 campos, sin override, sin `received_at`.

**Campos nuevos:**
- `FeedIngestor.subscription: FeedSubscription`
- `FeedIngestor.last_error_age_seconds: int | None = None`
- `FeedIngestor.last_error_at: str | None = None`
- `HealthFeed.symbols_never_delivered: int`
- `Symbol.note: str | None = None`

**Campos removidos:** ninguno en este plan.

**Export nuevo:** `"FeedSubscription"` en `models.__all__` (NO en el `__all__` del paquete — ver la
nota de seguimiento).

**Fixtures de test nuevas:** `_MEASURED_HEALTH_FEED_43` (`test_core.py`).

**Helpers de test nuevos:** `_keys_recursive` (`test_core.py`).

**Funciones de test nuevas (4):**
- `test_every_fixture_key_is_a_measured_wire_key`
- `test_feed_subscription_decodes_the_measured_blob`
- `test_measured_health_feed_payload_produces_zero_divergence_records`
- `test_healthy_feed_payload_emits_no_missing_for_the_conditional_error_fields`

**Funciones de test re-derivadas (4, ninguna renombrada):**
`test_health_feed_from_api_drops_an_undeclared_key_and_reports_it_once`,
`test_health_feed_to_dict_round_trips_all_three_levels`,
`test_health_models_declare_exactly_the_two_locked_optionals`,
`test_symbol_field_set_matches_reconciled_wire`.

**Listas de parametrizacion extendidas (3):** las tres de `test_core.py:1152-1181` pasan de seis a
siete clases de health.

**Ningun archivo nuevo.** Cero dependencias externas agregadas.
</artifacts_this_phase_produces>

<flagged_followups>
## Seguimiento marcado — NO se corrige en esta fase

1. **`FeedSubscription` no se exporta en el `__all__` del paquete** (`__init__.py`), solo en
   `models.__all__`. Es una inconsistencia con `FeedMarket` y `FeedPipeline`, que si estan en ambos.
   Ningun test lo exige y D-16 lockea el alcance. Efecto secundario real: `check_surface_types.py`
   resuelve candidatos desde el `__all__` de cada `__init__.py` hacia afuera, asi que los 15 campos
   de la clase nueva NO quedan escaneados por el gate. Candidato para la Phase 44, que ya toca
   `__init__.py` para el bump de version.

2. **`main_market_data.py:1541-1542`** — ver el bloque `<flagged_followups>` del plan 43-01. Se
   consolida en `43-DISPOSITION.md` (plan 43-03).
</flagged_followups>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta HTTP del vendor -> `SafeModel.from_api` | El payload de `/health/feed` y de los cuatro endpoints de symbols es entrada no confiable |
| blob medido del ledger de findings -> fixture de test | El ledger es committeado; el capture crudo no lo es. Solo el key-set y los tipos cruzan |
| baseline write-once (`.planning/verification/schemas/`) -> proceso de test | Lectura solamente; escribir ahi seria destruir la evidencia de la que depende el censo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Information Disclosure | `FeedSubscription` como campo tipado vs. mapping opaco | mitigate | Un mapping sin tipar seria un punto ciego permanente del censo: `walk_field` no tiene rama para mappings y cae al retorno final sin caminar ni reportar. Los 15 campos tipados restauran la cobertura del walker sobre el sub-objeto |
| T-43-07 | Tampering | over-declaracion de `Optional` | mitigate | La rama `Union` del walker retorna sin emitir record, asi que un `| None` de mas borra permanentemente la senal que el proyecto existe para producir. Cada `| None` nuevo lleva su parrafo de evidencia medida; `test_health_models_declare_exactly_the_two_locked_optionals` (re-derivado) pinnea el conjunto exacto en cuatro |
| T-43-08 | Information Disclosure | fixtures de test vs. capture gitignored | mitigate | `_MEASURED_HEALTH_FEED_43` lleva valores SINTETICOS; solo key-set y tipos vienen del blob committeado del ledger. Verificado por el criterio `grep -c 'verification/captures'` igual a `0` |
| T-43-09 | Tampering | baselines write-once de schema | mitigate | D-25: nunca se pisan on drift. Verificado por el criterio `git status --porcelain .planning/verification/schemas/` sin salida, en los dos tasks |
| T-43-10 | Denial of Service | payload parcial o nulo de `/health/feed` | mitigate | `FeedSubscription` hereda entera la tolerancia de `SafeModel` (Null Object mas ceros tipados) sin codigo nuevo; el test parametrizado de las siete clases pinnea que `from_api(None)` no levanta |
| T-43-SC | Tampering | instalaciones npm/pip/cargo | accept | N/A verificado: cero paquetes externos instalados. `43-RESEARCH.md` seccion "Package Legitimacy Audit" lo registra y D-16 prohibe tocar `uv.lock`; `uv lock --check` queda verde por no-op |
</threat_model>

<verification>
Al cerrar el plan, los cinco deben ser ciertos simultaneamente:

1. `uv run pytest packages/market-data-client -q --no-cov` — exit code 0.
2. `uv run python tools/check_surface_types.py` — exit code 0, `0 violations`.
3. `uv run mypy` y `uv run mypy packages/market-data-client/tests` — exit code 0.
4. `uv run ruff check .` y `uv run ruff format --check .` — exit code 0.
5. `git status --porcelain .planning/verification/schemas/ uv.lock` — sin salida.

Y el diff acumulado del plan contiene exactamente tres archivos: `models.py`, `test_core.py`,
`test_reference_models.py`.
</verification>

<success_criteria>
- Las cinco claves `extra` medidas estan declaradas con el tipo que su evidencia respalda.
- El payload medido decodifica con **cero** records de divergencia (criterio 3 probado, no afirmado).
- Ninguna de las cinco emite `missing` sobre un payload sano; el unico `missing` esperado es el del
  campo plano de D-11 contra la fixture congelada del 2026-07-31, documentado como tal.
- Toda clave de la fixture congelada es subconjunto del key-set medido (criterio 4).
- `models.__all__` cubre toda subclase de `SafeModel` del modulo, incluida la nueva.
- Ningun baseline write-once, ninguna version y ningun archivo fuera de los tres declarados fue
  tocado.
</success_criteria>

<output>
Create `.planning/phases/43-market-data-client-forma-de-instrument-segment-5-claves-extr/43-02-SUMMARY.md` when done.

El SUMMARY debe incluir, ademas del formato estandar:
- La tabla de las 5 claves con su tipo final y la evidencia medida que lo respalda (input para el
  criterio 3, que el plan 43-03 consolida).
- La lista exacta de records que T14 asserta ahora, con la explicacion de por que ese unico `missing`
  es correcto.
- Confirmacion explicita de que `__init__.py`, `client.py`, `aio.py`, `uv.lock` y los tres sitios de
  version quedaron sin tocar.
</output>
