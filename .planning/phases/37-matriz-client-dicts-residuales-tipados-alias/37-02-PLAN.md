---
phase: 37
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/matriz-client/src/matriz_client/models.py
  - packages/matriz-client/src/matriz_client/__init__.py
  - packages/matriz-client/tests/test_decode.py
  - packages/matriz-client/tests/test_models.py
  - packages/matriz-client/tests/test_logging.py
  - verification/snapshots/matriz-client-surface.txt
autonomous: true
requirements: [NOBJ-MTZ-01]

must_haves:
  truths:
    - "D-05: InstrumentDetail.tickPriceRanges is dict[str, TickPriceRange] — a string-keyed mapping, never list[TickPriceRange]; all three observed samples carry exactly one key and nothing observed proves contiguity or ordering."
    - "D-06: the mapping axis is UPDATED, not deleted — a dict[str, Model] hint still matches no walk_field branch, so without the axis the inner values would arrive as raw dicts."
    - "The axis routes each mapping value through _decode.walk_field with the SINK IT WAS HANDED, so dedupe and strict mode behave exactly as they do on every other field."
    - "The axis recurses when the element hint is itself a mapping, so a two-level dict[str, dict[str, Model]] decodes to models at depth 2 (the shape 37-03 needs for DetailedPosition.report)."
    - "D-04c: TickPriceRange is the ONLY field in this phase with real live-capture provenance, and its docstring cites the committed baseline file and its capture date — never the vendor doc dressed up as a capture."
    - "F-17: _convert keeps its reversed (tp, value) argument order and still answers {} for a bare dict[str, Any] hint against a None value."
    - "_decode.py is byte-unchanged — check_decode_intactness Check A hashes all five copies."
  artifacts:
    - path: "packages/matriz-client/src/matriz_client/models.py"
      provides: "TickPriceRange model plus the element-typed, self-recursing mapping axis"
      contains: "class TickPriceRange"
    - path: "packages/matriz-client/src/matriz_client/__init__.py"
      provides: "TickPriceRange on the exported surface"
      contains: "TickPriceRange"
    - path: "packages/matriz-client/tests/test_decode.py"
      provides: "Axis element-routing, recursion, sink-sharing and _convert-shim locks"
      contains: "TickPriceRange"
    - path: "verification/snapshots/matriz-client-surface.txt"
      provides: "Regenerated public-surface snapshot reflecting the new class and the retyped field"
      contains: "TickPriceRange"
  key_links:
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/_decode.py"
      via: "the axis decodes each mapping value by calling walk_field with the element hint and the caller's sink"
      pattern: "_decode\\.walk_field\\("
    - from: "packages/matriz-client/src/matriz_client/models.py"
      to: "packages/matriz-client/src/matriz_client/models.py"
      via: "_apply_mapping_policy derives the element hint from the declared dict annotation before delegating"
      pattern: "get_args\\("
  prohibitions:
    - statement: "Do not add a dict branch to _decode.py. check_decode_intactness.py Check A hashes all five copies of that file; a per-package branch there is the first crack in the invariant the gate exists to hold. The axis belongs in models.py, which RESEARCH F-18 measured as unconstrained by that gate."
    - statement: "Do not re-create the axis in market_data_client/models.py to restore symmetry. models.py:122-133 records that Phase 36 deleted it deliberately and instructs maintainers not to restore it."
    - statement: "Do not call Model.from_api from inside the axis. from_api resolves its own sink via current_sink(), leaving the surrounding scope and silently disabling dedupe lock 5 (_decode.py:459-506 documents the trap). Route through walk_field with the sink already in hand."
    - statement: "Do not flatten tickPriceRanges to list[TickPriceRange]. Every observed sample has exactly one key '0'; a list would assert a sequence property no evidence supports (D-05)."
    - statement: "Do not touch _core.py, client.py, aio.py, ws_client.py or _decode.py in this plan."
---

<objective>
The tracer slice: prove the whole "untyped mapping becomes a typed model, decoded through the
shared walker, with declared provenance" mechanism end-to-end on the ONE field in this phase that
has real live-capture evidence — `InstrumentDetail.tickPriceRanges`.

Purpose: `tickPriceRanges` is the only one of the four target fields backed by a committed live
capture (`get-instrument-detail.json`, 2026-06-10). Landing it first means the axis recursion, the
sink contract, the provenance-docstring form and the surface-snapshot workflow are all verified
against measured data BEFORE Plan 37-03 applies the same machinery to fields whose only evidence
is a vendor document.

Output: an element-typed, self-recursing mapping axis in `models.py`; the `TickPriceRange` model
with `baseline` provenance; `InstrumentDetail.tickPriceRanges` retyped; and a regenerated public
surface snapshot.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-CONTEXT.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-RESEARCH.md
@.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md
@CLAUDE.md
</context>

## Artifacts this phase produces

This plan creates the rows marked **37-02**. The full phase roster is reproduced so no plan
invents a second name for the same symbol.

| Symbol | Kind | Home | Created by |
|--------|------|------|------------|
| `parse_get_detailed_positions_response` / `parse_get_account_report_response` | behaviour change — envelope unwrap | `matriz_client._core` | 37-01 |
| `_parse_risk_response` | REMOVED | `matriz_client._core` | 37-01 |
| `TickPriceRange` | `_SafeModel` dataclass (`lowerLimit: float \| None`, `upperLimit: float \| None`, `tick: float \| None`) | `matriz_client.models` | **37-02** |
| `_mapping_value` | signature + control-flow change — takes an element hint, routes values through `walk_field`, self-recurses on a nested mapping hint | `matriz_client.models` | **37-02** |
| `_apply_mapping_policy` | derives the element hint from the declared annotation before delegating | `matriz_client.models` | **37-02** |
| `_convert` | derives the element hint from `get_args(tp)`, keeps its reversed `(tp, value)` order | `matriz_client.models` | **37-02** |
| `InstrumentDetail.tickPriceRanges` | annotation change → `dict[str, TickPriceRange]` | `matriz_client.models` | **37-02** |
| `InstrumentPositionReport` | `_SafeModel` dataclass (`instrumentInitialSize`, `instrumentFilledSize`, `instrumentCurrentSize` — all `float \| None`) | `matriz_client.models` | 37-03 |
| `DetailedAccountReport` | `_SafeModel` dataclass (`settlementDate: int \| None`) | `matriz_client.models` | 37-03 |
| `DetailedPosition.report` | annotation change → `dict[str, dict[str, InstrumentPositionReport]]` (TWO levels) | `matriz_client.models` | 37-03 |
| `AccountReport.detailedAccountReports` | annotation change → `dict[str, DetailedAccountReport]` (ONE level) | `matriz_client.models` | 37-03 |
| `AccountReport.portfolio` | annotation change → `float \| None` | `matriz_client.models` | 37-03 |
| `_field_candidates_for` / `_adjudicate_field` / `_field_annotation_is_untyped_mapping` / `_FIELD_EXEMPTIONS` / `ScanResult.fields` | new gate internals | `tools/check_surface_types.py` | 37-04 |
| `packages/matriz-client/tests/test_surface_types_red.py` | NEW test module | tests | 37-04 |
| `MarketDataSnapshot.bids` / `.offers` / `.last` / `.settlement` / `.close` / `.open_interest` | read-only `@property` aliases | `matriz_client.models` | 37-05 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give the mapping axis an element type and a recursion, without touching the walker</name>
  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/tests/test_decode.py</files>
  <read_first>
    - packages/matriz-client/src/matriz_client/models.py:75-197 (`_strip_optional`, `_is_model`, `_is_mapping`, the current `_mapping_value` with its full Phase 29 CR-03 / Phase 36 WR-03 history, `_apply_mapping_policy`, and the `_convert` shim — read all of it; the docstrings carry instructions that must survive the rewrite)
    - packages/matriz-client/src/matriz_client/models.py:205-263 (`_SafeModel.from_api` / `.empty` — the two call sites of `_apply_mapping_policy`, including the lock-8 `SILENT_SINK` swap under a non-dict payload)
    - packages/matriz-client/src/matriz_client/_decode.py:417-556 (every `walk_field` branch; note the model arm returns `hint(**walk_model(...))`, a constructed instance, and note the final statement is a bare `return value` — RESEARCH F-6 corrects CONTEXT's claim that it hands back `None`)
    - packages/matriz-client/tests/test_decode.py:427-520 (the existing mapping-axis tests and `test_no_mapping_carrying_model_is_ever_a_nested_field_type`, whose `__args__` walk is one level deep — F-11)
    - packages/matriz-client/tests/test_decode.py:915-935 (`test_convert_argument_order_is_unchanged` and `test_convert_shim_still_coerces` — the two pins on `_convert`, F-17)
    - .planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-PATTERNS.md § "mapping axis" (the patterns to preserve while rewriting)
  </read_first>
  <behavior>
    Element routing, one level:
    - A field declared `dict[str, TickPriceRange]` receiving `{"0": {"lowerLimit": 0, "tick": 0.1,
      "upperLimit": None}}` yields a dict whose value at `"0"` is a `TickPriceRange` instance, not
      a raw dict, with `lowerLimit == 0.0` (silently widened `int` → `float`), `tick == 0.1`,
      `upperLimit is None`.
    - Zero divergence records are emitted for that payload: every declared key is present and
      every type either matches or is silently widened by the `float` arm.

    Element routing, recursion:
    - A field declared `dict[str, dict[str, TickPriceRange]]` receiving
      `{"OUTER": {"0": {"tick": 0.05}}}` yields models at depth 2. Use a module-local synthetic
      carrier dataclass for this test; the real two-level field arrives in Plan 37-03.
    - The divergence path for a missing inner scalar reads through both keys, e.g. a record whose
      path contains `.field.OUTER.0`, so a report is locatable in the payload.

    Sink identity:
    - The axis emits through the sink it was HANDED. Decoding two models inside one
      `DecodeScope` dedupes repeated identical records exactly as it does for non-mapping fields;
      the axis never reaches for `current_sink()`.
    - Under a non-dict top-level payload the axis stays silent (lock 8): `from_api(None)` on a
      mapping-carrying model emits exactly one terminal `non_dict` record and no per-field record.

    Preserved reporting contract (assert unchanged, do not rewrite these tests):
    - A non-dict value on a mapping-declared field still emits one record with kind `missing` when
      the value was `None` and kind `type` otherwise, expected `dict`, actual the runtime type
      name — and still substitutes `{}`.
    - Strict mode is still fatal on that record, exactly as on every other axis.

    `_convert` shim (F-17 — these two must keep passing verbatim):
    - `_convert(dict[str, Any], None)` returns `{}`.
    - The parameter order is still type-hint first, value second.
    - `_convert(dict[str, TickPriceRange], {"0": {"tick": 0.1}})` returns a dict whose value is a
      `TickPriceRange`, proving the shim inherits the new routing rather than bypassing it.
  </behavior>
  <action>
    Rewrite `_mapping_value` in `models.py` to accept the declared ELEMENT hint alongside the
    existing keyword-only `path`, `model` and `sink` parameters. Keep the current non-dict branch
    byte-equivalent in behaviour: emit through `sink` with the same five-argument signature and the
    same `missing` / `type` discrimination, then return an empty dict. On the dict branch, build a
    new dict by iterating the incoming items and, for each value, either self-recursing when
    `_is_mapping(element_hint)` holds or delegating to `_decode.walk_field` with the element hint,
    the extended path, the same model name and the same sink. Compose the extended path by
    appending the key to the incoming path so a nested divergence stays locatable.

    When the element hint is `Any` or cannot be derived, walk_field's bare pass-through returns the
    value unchanged — that is the correct behaviour for a legacy bare `dict[str, Any]` hint and is
    what keeps the `_convert` pin green. Do not special-case it with a branch that skips the walker.

    In `_apply_mapping_policy`, derive the element hint from the declared annotation by taking the
    second argument of the `dict[...]` parameterisation after `_strip_optional`, defaulting to
    `Any` when the annotation is unparameterised. Reuse `_is_mapping` and `_strip_optional` rather
    than re-deriving Optional handling. Keep the `cast(Any, cls)` before `_decode.hints_for` — that
    is the file's documented mypy-strict discipline and no `type: ignore` is introduced.

    In `_convert`, derive the element hint the same way from `get_args(tp)` and pass it through.
    The `(tp, value)` parameter order is pinned by a committed test and must not change.

    Carry the existing docstring history FORWARD, do not drop it: the Phase 29 CR-03 paragraph, the
    Phase 36 WR-03 paragraph that ends the market-data synchronisation obligation and instructs
    maintainers not to re-create the copy, and the explanation of why the axis lives in `models.py`
    rather than in the byte-verbatim walker. Append a Phase 37 paragraph recording that the axis
    now also OWNS element decoding — the walker has no dict branch, so a `dict[str, Model]` hint
    would otherwise deliver raw dicts — and that the recursion exists for the two-level
    `report` field landing in Plan 37-03.

    Add the tests to `test_decode.py` alongside the existing mapping cases, using module-local
    synthetic dataclasses for the recursion and sink probes. Extend
    `test_no_mapping_carrying_model_is_ever_a_nested_field_type` only if it reddens; otherwise leave
    it and add a comment recording F-11's measured blind spot — the guard walks a single level of
    `__args__`, so a model nested at depth 2 is invisible to it, and the phase's answer is that
    every new inner model is kept mapping-free rather than deepening the walk.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests/test_decode.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_decode.py -k "mapping or convert" -q` exits 0 and collects strictly more tests than before this task.
    - `uv run mypy packages/matriz-client/src` reports success (no new `type: ignore` was added:
      `grep -c 'type: ignore' packages/matriz-client/src/matriz_client/models.py` returns the same
      value as before the task).
    - `uv run python tools/check_decode_intactness.py` exits 0.
    - `git diff --numstat packages/matriz-client/src/matriz_client/_decode.py` produces no output.
    - `grep -n 'current_sink' packages/matriz-client/src/matriz_client/models.py` shows the only
      occurrence is inside `_SafeModel.from_api`, never inside the axis helpers.
    - `grep -n 'Phase 36' packages/matriz-client/src/matriz_client/models.py` still matches — the
      WR-03 "do not re-create the copy in market-data" instruction survived the rewrite.
  </acceptance_criteria>
  <done>The axis decodes mapping values into models at one and two levels through the shared
  walker and the caller's sink, the two `_convert` pins are green, `_decode.py` is byte-unchanged,
  and the helper's accumulated history is intact plus one Phase 37 paragraph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type tickPriceRanges against the committed live baseline</name>
  <files>packages/matriz-client/src/matriz_client/models.py, packages/matriz-client/src/matriz_client/__init__.py, packages/matriz-client/tests/test_models.py, packages/matriz-client/tests/test_decode.py, packages/matriz-client/tests/test_logging.py, verification/snapshots/matriz-client-surface.txt</files>
  <read_first>
    - .planning/verification/schemas/matriz-client/get-instrument-detail.json (the ONLY live-capture evidence in this phase — read the `tickPriceRanges` entry and the capture timestamp at the top of the file; the three inner types recorded are `int`, `float`, `NoneType`)
    - packages/matriz-client/documentation/Primary-API.md:325-460 (the two vendor samples at :330 and :378/:454 — confirm they agree with the baseline on the three field names and on the single key)
    - packages/matriz-client/src/matriz_client/models.py:396-441 (matriz's own house style for a leaf model: `@dataclass(frozen=True)` with NO `slots=True`, base `_SafeModel`, `| None = None` leaves)
    - packages/market-data-client/src/market_data_client/models.py:272-300 (`BookLevel` — the Phase 36 provenance-docstring template AND the int→float widening paragraph; copy the FORM and the reasoning, never the content)
    - packages/matriz-client/src/matriz_client/__init__.py (the two lists a new model must join: the `from matriz_client.models import (...)` block and `__all__`, both alphabetical)
    - packages/matriz-client/tests/test_models.py:60-70, packages/matriz-client/tests/test_decode.py:460-475 and :548-558, packages/matriz-client/tests/test_logging.py:395-425 (every committed assertion on `tickPriceRanges` — the Pitfall 4 hit list; read them before editing so none is missed)
  </read_first>
  <behavior>
    The model:
    - `TickPriceRange` is a frozen `_SafeModel` dataclass with exactly three fields:
      `lowerLimit: float | None = None`, `upperLimit: float | None = None`,
      `tick: float | None = None`. Wire names verbatim, camelCase.
    - It inherits `from_api`, `empty` and `__bool__`; none of the three is hand-written.
    - `TickPriceRange.empty()` is falsy; an instance carrying any non-default value is truthy.

    Decoding from the committed baseline:
    - `InstrumentDetail.from_api(<the baseline instrument-detail payload>).tickPriceRanges` is a
      dict with one key `"0"` whose value is a `TickPriceRange`, not a raw dict.
    - `lowerLimit` decodes as a `float` from a wire `int` with NO divergence record (the `float`
      arm widens before consulting `scalar_passthrough`).
    - `upperLimit` is `None` and emits no record — `| None` leaves tolerate absence.

    Absence and malformation:
    - `InstrumentDetail.from_api({}).tickPriceRanges == {}` — the empty mapping default survives
      the retype. This is the assertion at `test_decode.py:467` and `test_models.py:65`; it stays
      TRUE, so those lines do not flip.
    - A non-dict value on the field still substitutes `{}` and emits one record, and is still fatal
      under strict mode.
    - An undeclared inner key inside a `tickPriceRanges` value emits exactly one non-fatal `extra`
      record attributed to `TickPriceRange`.
  </behavior>
  <action>
    Add `TickPriceRange` to `models.py` immediately before `InstrumentDetail` so the reader meets
    the element type before the field that uses it. Match matriz's local house style — decorated
    `@dataclass(frozen=True)` with NO `slots=True`, base `_SafeModel` (private, not market-data's
    public `SafeModel`).

    Write the provenance docstring using the `baseline` label, the first of the three provenance
    classes D-04a establishes. Cite the committed capture file path and its capture date, and state
    the environment it was captured against. Add a second paragraph explaining, in the same
    reasoning as Phase 36's `BookLevel`, why `lowerLimit` is declared `float | None` even though
    the capture records `int`: the walker's float arm widens before consulting the passthrough set,
    so the widening is silent and fabricates no divergence — a future reader must not "fix" it to
    `int`. Do NOT describe the vendor doc samples as a capture; they may be cited as corroboration,
    labelled as vendor-documented.

    Retype `InstrumentDetail.tickPriceRanges` (`models.py:344`) from the untyped mapping to
    `dict[str, TickPriceRange]`, keeping `field(default_factory=dict)`.

    Export `TickPriceRange` from `__init__.py`: add it to the models import block and to `__all__`,
    both in the existing alphabetical position. Exporting it is required — the gate resolves
    candidates from `__all__`, and an inner model reachable from an exported field but absent from
    the surface would be invisible to Plan 37-04's scan.

    Walk the committed assertion sites named in `read_first` and update only those whose truth
    changed. The empty-mapping assertions stay true and stay as they are; the ones that would now
    receive a model instead of a raw dict get repointed to attribute access. Add the
    baseline-driven decode test to `test_models.py` with a provenance comment naming the capture
    file, and add the `extra`-key case to `test_decode.py`.

    Regenerate the public surface snapshot with `uv run python verification/regen_snapshots.py` and
    commit the diff alongside the source change that justifies it — the file's own header forbids
    hand-editing. Confirm the regenerated `InstrumentDetail` line shows the new annotation and that
    a `TickPriceRange` row appeared.
  </action>
  <verify>
    <automated>uv run --package matriz-client pytest packages/matriz-client/tests -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0 with a count
      strictly greater than the 488 baseline.
    - `uv run --package matriz-client pytest packages/matriz-client/tests/test_models.py -k tickPriceRange -q` exits 0 and collects at least 1 test.
    - `uv run mypy packages/matriz-client/src` reports success.
    - `uv run ruff check packages/matriz-client` and `uv run ruff format --check packages/matriz-client` exit 0.
    - `uv run python -c "import matriz_client; print(matriz_client.TickPriceRange.empty())"` prints an
      all-`None` instance without raising.
    - `grep -c 'TickPriceRange' verification/snapshots/matriz-client-surface.txt` returns at least 2
      (its own row plus the `InstrumentDetail` signature).
    - `uv run python tools/check_decode_intactness.py`, `uv run python tools/check_uniform_structure.py`
      and `uv run python tools/surface_parity.py` all exit 0.
    - `uv run python tools/check_surface_types.py` still exits 0 (the gate is still vacuous on
      fields at this point — Plan 37-04 closes that, and it must not be reached before the retypes
      land, per RESEARCH Pitfall 3).
  </acceptance_criteria>
  <done>`InstrumentDetail.tickPriceRanges` returns a mapping of `TickPriceRange` models decoded from
  the committed live capture, its provenance is declared as `baseline` with the file and date, the
  class is on the exported surface, and the snapshot records the change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Primary API JSON → `_SafeModel.from_api` → the mapping axis | Untrusted vendor payload crosses into typed models here; keys and value types are attacker- or vendor-controlled |
| Model roster → the caller's attribute chain | A closed roster decides which wire keys survive into the public surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-06 | Denial of Service | Attribute chains over `tickPriceRanges` values | mitigate | Null Object: `TickPriceRange` inherits `empty()` / `__bool__` from `_SafeModel`, every field is `float \| None`, and a non-dict container still substitutes `{}` — no chain raises |
| T-37-07 | Tampering | An untyped mapping accepting arbitrary nested structure | mitigate | ASVS V5: values are now validated by `_decode.walk_field` against the declared element hint, with type mismatches reported through the sink instead of passed through unexamined |
| T-37-08 | Information disclosure (loss) | Closing the `TickPriceRange` roster to three keys | accept | Undeclared inner keys are DISCARDED but reported as non-fatal `extra` divergences by the existing walker mechanism; the roster is exactly the three keys the committed capture shows, so the closure is measured rather than guessed |
| T-37-09 | Repudiation | Provenance of a typed roster | mitigate | The `baseline` docstring cites the capture file path, capture date and environment, so a reader can re-derive the roster from committed evidence rather than trusting the annotation |
| T-37-10 | Tampering | Divergence sink capture by the axis | mitigate | The axis emits only through the sink it was handed; reaching for `current_sink()` would silently escape the surrounding `DecodeScope` and disable dedupe lock 5 — prohibited and grep-asserted |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed; `uv.lock` untouched. 37-RESEARCH's Package Legitimacy Audit records "not applicable" |
</threat_model>

<verification>
- `uv run --package matriz-client pytest packages/matriz-client/tests -q` exits 0.
- `uv run mypy packages/matriz-client/src` reports success.
- All four repo gates exit 0: `tools/check_surface_types.py`, `tools/check_decode_intactness.py`,
  `tools/check_uniform_structure.py`, `tools/surface_parity.py`.
- `uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py`
  exits 0 (the two `verification/` guards the `lint` job runs explicitly).
- `git diff --numstat packages/matriz-client/src/matriz_client/_decode.py` produces no output.
</verification>

<success_criteria>
- `InstrumentDetail.tickPriceRanges` is `dict[str, TickPriceRange]` and decodes the committed
  baseline into model instances, not raw dicts.
- The axis routes element values through `_decode.walk_field` with the caller's sink and recurses
  on a nested mapping hint.
- `_convert` still answers `{}` for a bare untyped-mapping hint against `None` and keeps its
  reversed argument order.
- `TickPriceRange` is on the exported surface and in the regenerated snapshot.
- `_decode.py` is byte-unchanged and all four gates are green.
</success_criteria>

<output>
Create `.planning/phases/37-matriz-client-dicts-residuales-tipados-alias/37-02-SUMMARY.md` when done.
Record the final axis signature verbatim — Plan 37-03 calls it with a two-level hint and must not
guess the parameter names.
</output>
