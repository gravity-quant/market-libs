---
phase: 12-codegen-spike
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt
  - .planning/spikes/SPIKE-005-codegen-tool-choice/README.md
  - .planning/spikes/MANIFEST.md
  - .planning/phases/12-codegen-spike/12-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/todos/pending/spike-codegen-libcst-v1.3.md
  - .claude/skills/spike-findings-codegen-market-libs/SKILL.md
  - CLAUDE.md
autonomous: false
requirements: [REFAC-06]
tags: [spike, codegen, decision, evidence-checklist, GO, NO-GO, wrap-up]
must_haves:
  truths:
    - "The 8 D-RIGOR-01 evidence items are collected into evidence-checklist.txt with per-item PASS/FAIL transcripts pointing at concrete artifacts from Plans 01+02"
    - "The 1-day timebox (D-SCOPE-03) has been checked: if cumulative wall-clock from Plan 01 Task 12-01-02 start to evidence-checklist completion exceeds 1 working day AND the spike has not converged to 8/8 PASS, NO-GO is triggered automatic"
    - "DECISION.md is signed by the operator with frontmatter `decision: GO | NO-GO` + signoff_date + signoff_by"
    - "If GO: per-package Rule config drafts captured in DECISION.md for ALL 4 packages (ambito, iol, higyrus, matriz); /gsd-spike --wrap-up has produced `.claude/skills/spike-findings-codegen-market-libs/SKILL.md` auto-loaded via CLAUDE.md"
    - "If NO-GO: Task 12-03-04a writes NO-GO.md root cause + pending todo `spike-codegen-libcst-v1.3.md` + NO-GO flavor SKILL.md + MANIFEST.md row flip; Task 12-03-04b (depends on 04a) writes REQUIREMENTS.md REFAC-06 → Future Requirements + ROADMAP.md Phase 16 marked DROPPED + CLAUDE.md auto-load update + 12-SUMMARY.md NO-GO close"
    - "12-SUMMARY.md exists with frontmatter `status: complete`, `decision: GO|NO-GO`, `next_phase: 13 (Phase 16 DROPPED if NO-GO)`, evidence checklist results, signoff date, spike directory + skill links"
    - "MANIFEST.md SPIKE-005 row flipped from `TBD` → `GO|NO-GO` and SPIKE-005 README.md frontmatter verdict flipped likewise"
  artifacts:
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt"
      provides: "Aggregated transcript of the 8 D-RIGOR-01 evidence items, each with PASS|FAIL + path to source artifact"
      contains: "D-RIGOR-01"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md"
      provides: "Operator-signed binary GO|NO-GO with 8-item evidence summary + per-package Rule config drafts (GO) or root cause pointer (NO-GO)"
      contains: "decision:"
    - path: ".planning/phases/12-codegen-spike/12-SUMMARY.md"
      provides: "Phase 12 close artifact with frontmatter signoff_date + next_phase routing"
      contains: "decision:"
    - path: ".claude/skills/spike-findings-codegen-market-libs/SKILL.md"
      provides: "Auto-loaded skill for Phase 16 (GO branch) or v1.3 libcst spike (NO-GO branch)"
      contains: "spike-findings-codegen-market-libs"
  key_links:
    - from: "DECISION.md"
      to: "evidence-checklist.txt"
      via: "frontmatter evidence_checklist field + body link"
      pattern: "evidence-checklist"
    - from: "12-SUMMARY.md"
      to: ".planning/spikes/SPIKE-005-codegen-tool-choice/"
      via: "spike_directory frontmatter field"
      pattern: "SPIKE-005"
    - from: "12-SUMMARY.md"
      to: ".claude/skills/spike-findings-codegen-market-libs/"
      via: "skill_produced frontmatter field"
      pattern: "spike-findings-codegen-market-libs"
    - from: "CLAUDE.md"
      to: ".claude/skills/spike-findings-codegen-market-libs/SKILL.md"
      via: "Auto-loaded Knowledge section"
      pattern: "Skill\\(.spike-findings-codegen-market-libs.\\)"
---

<objective>
Wave 5 + Wave 6 of the codegen spike: collect the 8 D-RIGOR-01 evidence items into a single auditable checklist, gate against the 1-day timebox (D-SCOPE-03), present the binary GO|NO-GO decision artifact to the operator for signoff, then execute the GO or NO-GO close-out branch end-to-end. This plan is the merge gate for Phase 12; its output is the artifact set that gates Phase 16 (REFAC-06) — either Phase 16 proceeds as scheduled (GO) or REFAC-06 defers to v1.3 and Phase 16 is dropped from the v1.2 schedule (NO-GO).

Purpose: convert the experimental artifacts produced by Plans 01 + 02 into the signed decision artifact + project-state updates that downstream phases (13, 14, 15, 16, 17) depend on. This is where Phase 12's output materializes as project-state changes.

Output: DECISION.md (operator-signed) + evidence-checklist.txt + GO branch: spike-findings-codegen-market-libs SKILL.md + CLAUDE.md update; OR NO-GO branch: NO-GO.md + REQUIREMENTS.md update + ROADMAP.md update + pending todo + spike-findings-codegen-market-libs SKILL.md (NO-GO flavor). Both branches close with 12-SUMMARY.md.

Honors locked decisions: D-RIGOR-01 (all 8 items), D-SCOPE-03 (timebox check), D-NOGO-01 (no libcst inline), D-NOGO-02 (NO-GO artifacts), D-ARTIFACT-02 (wrap-up Skill), D-ARTIFACT-03 (12-SUMMARY.md). Anti-Pitfall 1 (timebox slip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-codegen-spike/12-CONTEXT.md
@.planning/phases/12-codegen-spike/12-RESEARCH.md
@.planning/phases/12-codegen-spike/12-VALIDATION.md
@.planning/phases/12-codegen-spike/12-01-SUMMARY.md
@.planning/phases/12-codegen-spike/12-02-SUMMARY.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
@.planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
@.planning/spikes/MANIFEST.md
@.planning/spikes/WRAP-UP-SUMMARY.md
@.claude/skills/spike-findings-market-libs/SKILL.md
</context>

<tasks>

<task id="12-03-01" type="auto" tdd="false">
  <name>Task 12-03-01: D-RIGOR-01 8-item evidence collection + 1-day timebox check + draft DECISION.md</name>
  <read_first>
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" (the 8 items with their automated commands)
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §D-RIGOR-01 (the canonical 8-item list)
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §D-SCOPE-03 (1-day timebox + auto-NO-GO trigger)
    - .planning/phases/12-codegen-spike/12-01-SUMMARY.md (Plan 01 outcomes: 001a verdict + slopcheck + B8 identity)
    - .planning/phases/12-codegen-spike/12-02-SUMMARY.md (Plan 02 outcomes: 001b/c/d verdicts)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
    - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Pitfall 1 — Letting the spike drift past 1 day"
  </read_first>
  <action>
    Step 1 — Compute the 1-day timebox check. Read the timestamp of `.planning/spikes/SPIKE-005-codegen-tool-choice/README.md` (Plan 01 Task 12-01-02 created it) and the current time. If the elapsed wall-clock > 24 hours AND any of the 8 evidence items is FAIL (computed below), this triggers AUTO-NO-GO per D-SCOPE-03 + Anti-Pitfall 1. The timebox is measured as `time.time() - <README.md mtime>`. Use `stat -f %m` (macOS BSD) or `stat -c %Y` (Linux GNU) — be portable.

    Step 2 — Build `.planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt`. For each of the 8 items, write a clearly-labeled section with:
      - Item label (e.g., `=== D-RIGOR-01 item 1: Byte-identical round-trip ámbito ===`)
      - Automated command exactly as documented in 12-RESEARCH.md §"Phase Requirements → Test Map"
      - The actual command run + exit code (re-run the commands NOW against the current spike artifacts to get fresh transcripts)
      - PASS|FAIL verdict
      - Source artifact pointer (file path)

    The 8 items to collect (verbatim from D-RIGOR-01):

    | # | Item | Command | Source artifact |
    |---|------|---------|-----------------|
    | 1 | Byte-identical round-trip ámbito | `diff -u packages/ambito-financiero-client/src/ambito_financiero_client/client.py .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py` (must exit 0) | 001a/diff_vs_v1.1_client.txt |
    | 2 | B8 identity preserved on generated | Re-load via importlib + assert `mod._raise_for_response is aio._raise_for_response is _core.raise_for_response` (re-run the assertion from 001a/experiment.py Step 8) | 001a/run_log.txt |
    | 3 | `uv run ruff format --check` clean | `uv run ruff format --check .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py` (exit 0) | re-run captured here |
    | 4 | `uv run ruff check` clean (incl. ASYNC1xx) | `uv run ruff check .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py` (exit 0) | re-run captured here |
    | 5 | `uv run mypy --strict` clean | `uv run mypy --strict .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py` (exit 0) | re-run captured here |
    | 6 | ámbito mocked suite green vs generated | Temporarily symlink the generated as ambito client.py inside a sandbox copy of the package (DO NOT mutate packages/ in place — use `mktemp -d` + `cp -r` for a workspace copy, then run `uv run --package ambito-financiero-client pytest -q` against the copy). Per Anti-Pitfall 2, do NOT modify packages/ at any point. | re-run captured here |
    | 7 | `uv run lint-imports` 4 contracts intact | `uv run lint-imports` (exit 0; runs against production packages/, NOT spike artifacts — confirms the existing contracts still hold) | re-run captured here |
    | 8 | `@generated` marker compatible with `from __future__ import annotations` | Re-confirm from 001b/verification_transcripts.txt (all 4 sub-commands exit 0) | 001b/verification_transcripts.txt |

    For item 6 specifically: the sandbox-copy approach is per Anti-Pitfall 2. Concrete recipe — create `/tmp/spike-005-ambito-test-<timestamp>/`, `cp -r packages/ambito-financiero-client/` into it, overwrite `<sandbox>/src/ambito_financiero_client/client.py` with the generated file, then `cd <sandbox> && uv run --package ambito-financiero-client pytest -q`. Capture exit code + last 30 lines of output. Clean up the temp dir at end of task (`rm -rf /tmp/spike-005-ambito-test-*`).

    Step 3 — Append two SUMMARY blocks at end of evidence-checklist.txt:
      `=== TIMEBOX CHECK (D-SCOPE-03) ===\nElapsed: <hours>h <minutes>m\nCap: 24h\nStatus: WITHIN-CAP | OVER-CAP\n`
      `=== AGGREGATE VERDICT ===\nItems PASS: <N>/8\nItems FAIL: <N>/8\nMatriz audit unresolved rows: <0|N>  (D-SCOPE-02; must be 0)\nFinal: GO | NO-GO\n  - GO requires 8/8 PASS + matriz unresolved rows == 0 + WITHIN-CAP\n  - NO-GO triggered by ANY of: any item FAIL, matriz unresolved rows > 0, OVER-CAP\n`

    Step 4 — Draft `.planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md` with frontmatter:
      ```yaml
      ---
      spike: 005
      decision: <GO | NO-GO>           # filled by Task 12-03-02 operator signoff
      signoff_date: <YYYY-MM-DD>       # filled by Task 12-03-02
      signoff_by: <operator>           # filled by Task 12-03-02
      evidence_checklist:
        item_1_byte_identical_ambito: PASS|FAIL
        item_2_b8_identity: PASS|FAIL
        item_3_ruff_format_check: PASS|FAIL
        item_4_ruff_check: PASS|FAIL
        item_5_mypy_strict: PASS|FAIL
        item_6_ambito_pytest_green: PASS|FAIL
        item_7_lint_imports: PASS|FAIL
        item_8_marker_future_compat: PASS|FAIL
      matriz_audit_unresolved_rows: 0
      timebox_status: WITHIN-CAP|OVER-CAP
      next_phase: 13   # (always 13; Phase 16 conditional on this decision)
      phase_16_status: PROCEED|DROPPED
      ---
      ```
      Body sections:
        `# SPIKE-005 — Codegen Tool Choice Decision`
        `## Evidence Checklist Summary` — table summarizing the 8 items + matriz audit + timebox.
        `## Decision (TBD — operator signoff required in Task 12-03-02)` — placeholder.
        `## Per-Package Rule Config Drafts` — IF GO, 4 unasync.Rule blocks (one per package: ambito copies from 001a/FINDING.md; iol + higyrus + matriz inferred per D-SCOPE-01 from ámbito + per matriz audit deltas in 001c/FINDING.md). IF NO-GO (or if Aggregate Verdict is NO-GO), insert placeholder `(see NO-GO.md for root cause)`.
        `## Phase 16 Production Integration Recommendation` — IF GO, document: pre-commit hook syntax (regex matching `^# @generated by unasync`), `make codegen` Makefile target stub, CI `lint-codegen` job sketch, `pyproject.toml` dev-dep addition `unasync >=0.6.0,<0.7` to root `[dependency-groups] dev`. IF NO-GO, omit this section.
        `## Routing After Signoff` — explicit pointer to Task 12-03-02 (operator signoff) → Task 12-03-03 (GO branch) OR Task 12-03-04a + Task 12-03-04b (NO-GO branch — 04a writes spike-local artifacts, 04b depends on 04a and writes project-state governance files).

    Auto-fill the frontmatter evidence_checklist subkeys from the actual PASS|FAIL values determined by re-running the commands. Set `decision: TBD` for now; Task 12-03-02 flips it.

    Step 5 — Determine the AGGREGATE recommendation: based on the evidence checklist + matriz audit + timebox, write a "Recommended decision" line in DECISION.md body (NOT in frontmatter — operator signs the frontmatter):
      - If 8/8 PASS + matriz unresolved == 0 + WITHIN-CAP → "Recommended: GO"
      - Otherwise → "Recommended: NO-GO (root cause: <brief: first item failing OR audit gap OR timebox overrun>)"

    Anti-Pitfall 1 enforced via the timebox check + AGGREGATE VERDICT block.
    Anti-Pitfall 2 enforced via the sandbox-copy approach for item 6 + final `git status --porcelain packages/` check.
  </action>
  <verify>
    <automated>
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      grep -cE '^=== D-RIGOR-01 item [1-8]:' .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt | grep -q '^8$' && \
      grep -q '=== TIMEBOX CHECK (D-SCOPE-03) ===' .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt && \
      grep -q '=== AGGREGATE VERDICT ===' .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt && \
      grep -qE 'Final: (GO|NO-GO)$' .planning/spikes/SPIKE-005-codegen-tool-choice/evidence-checklist.txt && \
      grep -q '^spike: 005$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      grep -qE '^decision: (TBD|GO|NO-GO)$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      grep -qE '^timebox_status: (WITHIN-CAP|OVER-CAP)$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0" && \
      test "$(ls /tmp/spike-005-ambito-test-* 2>/dev/null | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/{evidence-checklist.txt,DECISION.md}</files>
  <done>evidence-checklist.txt has 8 D-RIGOR-01 sections + TIMEBOX CHECK + AGGREGATE VERDICT; DECISION.md draft has frontmatter (decision: TBD waiting for signoff) + body sections (evidence summary + per-package rule drafts if recommended-GO + routing); sandbox temp dirs cleaned; zero mutations under packages/.</done>
  <acceptance_criteria>
    - evidence-checklist.txt has 8 sections (one per D-RIGOR-01 item) each with a re-run transcript, exit code, and PASS|FAIL verdict + source artifact pointer.
    - TIMEBOX CHECK section is present with elapsed time + cap + status (WITHIN-CAP|OVER-CAP).
    - AGGREGATE VERDICT section is present with items PASS/FAIL counts + matriz unresolved rows + Final: GO|NO-GO line.
    - DECISION.md has the frontmatter shape above with `decision: TBD` (waiting for operator signoff in Task 12-03-02).
    - DECISION.md body has Evidence Checklist Summary + Decision placeholder + Per-Package Rule Config Drafts (if Recommended: GO) + Phase 16 Production Integration Recommendation (if Recommended: GO) + Routing After Signoff.
    - For item 6 sandbox-test: `/tmp/spike-005-ambito-test-*` temp dirs are cleaned up (zero matching dirs at task end).
    - `git status --porcelain packages/` is empty (Anti-Pitfall 2).
  </acceptance_criteria>
</task>

<task id="12-03-02" type="checkpoint:decision" gate="blocking">
  <name>Task 12-03-02: Operator signoff — binary GO|NO-GO on DECISION.md</name>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/{DECISION.md,README.md}, .planning/spikes/MANIFEST.md</files>
  <action>Pause and present the decision artifact (DECISION.md + evidence-checklist.txt + 4 FINDING.md files) to the operator. After response, edit DECISION.md frontmatter (decision + signoff_date + signoff_by + phase_16_status), flip SPIKE-005 README.md frontmatter verdict, flip MANIFEST.md SPIKE-005 row verdict column.</action>
  <verify><human-check>Operator response matches `(GO|NO-GO) YYYY-MM-DD <name>` AND DECISION.md frontmatter has been updated AND MANIFEST.md SPIKE-005 row no longer shows TBD.</human-check></verify>
  <done>DECISION.md `decision:` is no longer TBD; signoff_date + signoff_by populated; phase_16_status set to PROCEED (GO) or DROPPED (NO-GO); SPIKE-005 README + MANIFEST.md verdict flipped.</done>
  <decision>
    Whether to set `decision: GO` (Phase 16 proceeds in v1.2) or `decision: NO-GO` (REFAC-06 defers to v1.3 and Phase 16 is DROPPED) on `.planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md`.
  </decision>
  <context>
    Task 12-03-01 produced `evidence-checklist.txt` and a draft DECISION.md with a recommended decision. The operator MUST read evidence-checklist.txt + all 4 FINDING.md files + DECISION.md draft, then explicitly sign. Per D-RIGOR-01: GO requires ALL 8 items PASS + matriz audit zero unresolved rows + WITHIN 1-day timebox. ANY failure → NO-GO.

    Per D-NOGO-01: a NO-GO signoff defers REFAC-06 to v1.3; libcst is NOT evaluated inline in Phase 12.

    This is the merge gate. The decision is irrevocable for v1.2 (a flip from NO-GO → GO later would require reopening the spike, which violates the 1-day timebox).
  </context>
  <options>
    <option id="GO">
      <name>GO — Phase 16 proceeds in v1.2</name>
      <pros>
        - REFAC-06 ships in v1.2 as planned (the structural sync/async dedup goal of the milestone).
        - Phase 16 inherits the per-package Rule config drafts in DECISION.md (no re-research needed).
        - The auto-loaded skill `spike-findings-codegen-market-libs` accelerates Phase 16 planning.
      </pros>
      <cons>
        - Phase 16 inherits ANY edge case the spike did not surface (D-SCOPE-01 risk: ámbito-canary inference may not cover iol OAuth + higyrus event_hooks edge cases).
        - If GO is signed despite a borderline evidence item (e.g., 7/8 PASS), Phase 16 will encounter the failure mode in production scope.
      </cons>
    </option>
    <option id="NO-GO">
      <name>NO-GO — REFAC-06 defers to v1.3, Phase 16 DROPPED from v1.2</name>
      <pros>
        - Honors the strict D-RIGOR-01 gate (any failure → NO-GO; no soft-relax).
        - v1.2 still ships REFAC-05 + SEC-01 + ERG-01 + LIVE-03 (4 of 5 requirements; Phase 17 unblocks immediately after Phase 14 + 15).
        - libcst exploration gets a dedicated v1.3 spike with proper scope.
      </pros>
      <cons>
        - REFAC-06's LOC-reduction benefit defers to v1.3 (matriz client.py LOC drop residual stays open).
        - Carrier risk: operator may de-prioritize libcst spike in v1.3 backlog if codegen no longer feels urgent.
      </cons>
    </option>
  </options>
  <resume-signal>
    Reply with EXACTLY one of:
    - `GO 2026-MM-DD <operator-name>` to sign GO (commits to Phase 16 in v1.2)
    - `NO-GO 2026-MM-DD <operator-name>` to sign NO-GO (defers REFAC-06 to v1.3, drops Phase 16 from v1.2)
    The executor will edit DECISION.md frontmatter (decision + signoff_date + signoff_by + phase_16_status) accordingly and proceed to Task 12-03-03 (GO branch) or Tasks 12-03-04a + 12-03-04b (NO-GO branch — 04a runs first, 04b depends on 04a).
  </resume-signal>
  <acceptance_criteria>
    - Operator response matches the format `(GO|NO-GO) YYYY-MM-DD <name>`.
    - Executor updates DECISION.md frontmatter: `decision: <GO|NO-GO>`, `signoff_date: <YYYY-MM-DD>`, `signoff_by: <name>`, `phase_16_status: PROCEED|DROPPED`.
    - The Routing After Signoff section in DECISION.md body is updated to remove the TBD pointer and to state explicitly: "Decision signed <date> by <name>. Proceeding to <Task 12-03-03 GO branch | Tasks 12-03-04a + 12-03-04b NO-GO branch>."
    - SPIKE-005 README.md frontmatter `verdict:` is flipped from `TBD` to `GO` or `NO-GO`.
    - MANIFEST.md SPIKE-005 row's Verdict column is flipped from `TBD` to `GO` or `NO-GO`.
  </acceptance_criteria>
</task>

<task id="12-03-03" type="auto" tdd="false">
  <name>Task 12-03-03: GO branch close-out — wrap-up Skill + CLAUDE.md auto-load update + 12-SUMMARY.md (GO)</name>
  <read_first>
    - .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md (must have decision: GO frontmatter from Task 12-03-02)
    - .planning/spikes/WRAP-UP-SUMMARY.md (existing wrap-up pattern reference)
    - .claude/skills/spike-findings-market-libs/SKILL.md (template format model — frontmatter + body sections)
    - CLAUDE.md §"Auto-loaded Knowledge" (where the new skill reference will be inserted)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 9 — `/gsd-spike --wrap-up` Invocation + Resulting Skill Shape"
  </read_first>
  <action>
    PRECONDITION: this task only runs if Task 12-03-02 set `decision: GO`. Read DECISION.md frontmatter at task start; if `decision: NO-GO` is set, SKIP this task entirely and run Tasks 12-03-04a + 12-03-04b instead. If `decision: TBD`, ABORT and surface as a blocker (operator signoff missing).

    Step 1 — Invoke the project SDK wrap-up command. The exact slash-command form is `/gsd-spike --wrap-up`. If the slash-command is not available in the current execution shell (executors may not have slash-command capability), fall back to the manual wrap-up: copy the spike subdirectories into a new skill structure as follows:

      Create `.claude/skills/spike-findings-codegen-market-libs/` directory structure:
        `.claude/skills/spike-findings-codegen-market-libs/`
        `├── SKILL.md`
        `├── references/`
        `│   ├── unasync-rule-config-per-package.md`     # 4 Rule configs from DECISION.md
        `│   ├── matriz-construct-audit.md`              # copy of 001c/matriz-aio-constructs.md + triage notes from FINDING.md
        `│   └── codegen-pitfalls.md`                    # B8 identity + marker layout + format-stability + deny-list scope`
        `└── sources/`
        `    ├── 001a-ambito-round-trip/`                # full copy of spike sub-dir`
        `    ├── 001b-ambito-marker-future-compat/`
        `    ├── 001c-matriz-construct-audit/`
        `    └── 001d-matriz-deny-list-config/`

      Use `mkdir -p` and `cp -r` for the directory + sources copies. References are NEW files synthesized from FINDING.md content (do NOT just symlink — these are auto-loaded references that need to be self-contained).

    Step 2 — Write `.claude/skills/spike-findings-codegen-market-libs/SKILL.md` with frontmatter:
      ```yaml
      ---
      name: spike-findings-codegen-market-libs
      description: Implementation blueprint from Phase 12 codegen spike (SPIKE-005, GO 2026-MM-DD). Per-package unasync.Rule configs for ambito/iol/higyrus/matriz, @generated marker compatible with `from __future__ import annotations`, matriz deny-list (_token_store.py, _refresh_policy.py, _refresh.py, ws_client.py) achieved via fpath_list scope, B8 identity preservation pattern. Auto-loaded when Phase 16 plans codegen across 4 transport shells.
      ---
      ```
      Body sections (parallel to spike-findings-market-libs/SKILL.md):
        `<context>` — Phase 12 spike context: 1-day spike on unasync 0.6.0 for `aio.py` → `client.py` round-trip across 4 packages; GO outcome with 8/8 D-RIGOR-01 evidence.
        `<requirements>` — non-negotiable constraints distilled from spike: B8 identity preservation (mod._raise_for_response is aio._raise_for_response is _core.raise_for_response); marker line ordering (marker comment line 1 → blank → docstring → blank → `from __future__ import annotations`); deny-list scope via fpath_list (NOT directory scope); `ruff format` post-codegen mandatory; mypy strict on generated; lint-codegen verify-clean CI gate.
        `<findings_index>` — feature-area table linking 001a/b/c/d FINDING.md outcomes.
        `<integration_blueprint>` — Phase 16 production pattern: `scripts/codegen.py` standalone script invoking `unasync.unasync_files()` per package with the captured Rule configs; pre-commit hook detecting marker via `head -1 | grep '^# @generated by unasync'`; CI `lint-codegen` job running `make codegen && git diff --exit-code`; `pyproject.toml` root `[dependency-groups] dev` adds `unasync >=0.6.0,<0.7`.
        `<metadata>` — Processed spikes list (001a/b/c/d with verdicts).

    Step 3 — Update `CLAUDE.md` "Auto-loaded Knowledge" section. Read the current `## Auto-loaded Knowledge` block (around the line referencing `Skill("spike-findings-market-libs")`) and append a new bullet:
      `- **Spike findings for codegen (Phase 12 GO)** (per-package unasync.Rule configs, @generated marker compatible with future-import, matriz deny-list via fpath_list scope, B8 identity preservation) → `Skill("spike-findings-codegen-market-libs")``

    Step 4 — Update `.planning/spikes/MANIFEST.md` SPIKE-005 row: flip Verdict column from `TBD` to `GO`. Also append a "## SPIKE-005 Forward References" section noting: Phase 16 consumes this spike's Skill auto-load; the Rule configs in DECISION.md are the per-package recipe Phase 16 inherits.

    Step 5 — Write `.planning/phases/12-codegen-spike/12-SUMMARY.md` with frontmatter (use the exact template from 12-RESEARCH.md §"10.5 Phase 12 close artifact" adapted for GO):
      ```yaml
      ---
      gsd_state_version: 1.0
      phase: 12
      phase_name: Codegen Spike
      status: complete
      decision: GO
      spike_directory: .planning/spikes/SPIKE-005-codegen-tool-choice/
      skill_produced: .claude/skills/spike-findings-codegen-market-libs/
      signoff_date: <YYYY-MM-DD from DECISION.md frontmatter>
      signoff_by: <name from DECISION.md frontmatter>
      next_phase: 13
      phase_16_status: PROCEED
      evidence_checklist:
        item_1_byte_identical_ambito: PASS
        item_2_b8_identity: PASS
        item_3_ruff_format_check: PASS
        item_4_ruff_check: PASS
        item_5_mypy_strict: PASS
        item_6_ambito_pytest_green: PASS
        item_7_lint_imports: PASS
        item_8_marker_future_compat: PASS
      matriz_audit_unresolved_rows: 0
      timebox_status: WITHIN-CAP
      ---
      ```
      Body sections (per 12-RESEARCH.md §10.5 template adapted for GO):
        `# Phase 12: Codegen Spike — Summary (GO)`
        `## Decision` — `**GO** — Phase 16 (REFAC-06) proceeds in v1.2 as scheduled.`
        `## Evidence Checklist (D-RIGOR-01 — 8 items)` — table with all 8 PASS.
        `## Spike Directory` — link to `.planning/spikes/SPIKE-005-codegen-tool-choice/`
        `## Skill Produced` — link to `.claude/skills/spike-findings-codegen-market-libs/SKILL.md` + note on auto-load.
        `## Per-Package Rule Configs` — link to DECISION.md §Per-Package Rule Config Drafts.
        `## Anti-Pitfall Compliance` — sections for Pitfalls 1, 2, 4, 5, 6, 7.
        `## Operational Pre-Gate` — note from Plan 01 Task 12-01-01 outcome (approved | exception-documented).
        `## Next Steps`:
          1. `/gsd-plan-phase 13` (Cross-Package Ergonomics)
          2. Phase 16 inherits the spike-findings-codegen-market-libs skill auto-load.
          3. v1.2 milestone tracker stays on the 6-phase schedule (12 → 13 → 14 ∥ 15 → 16 → 17).

    Anti-Pitfall 2 final check: `git status --porcelain packages/` MUST return zero lines.
  </action>
  <verify>
    <automated>
      grep -q '^decision: GO$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      test -d .claude/skills/spike-findings-codegen-market-libs/ && \
      test -f .claude/skills/spike-findings-codegen-market-libs/SKILL.md && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001a-ambito-round-trip && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001b-ambito-marker-future-compat && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001c-matriz-construct-audit && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001d-matriz-deny-list-config && \
      grep -q '^name: spike-findings-codegen-market-libs$' .claude/skills/spike-findings-codegen-market-libs/SKILL.md && \
      grep -q 'spike-findings-codegen-market-libs' CLAUDE.md && \
      test -f .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^decision: GO$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^next_phase: 13$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^phase_16_status: PROCEED$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.claude/skills/spike-findings-codegen-market-libs/{SKILL.md,references/*.md,sources/001a*..001d*}, CLAUDE.md, .planning/spikes/MANIFEST.md, .planning/phases/12-codegen-spike/12-SUMMARY.md</files>
  <done>If decision=GO: SKILL.md created + 3 reference markdowns + 4 sources/ sub-dir copies; CLAUDE.md auto-load updated; MANIFEST.md SPIKE-005 verdict=GO; 12-SUMMARY.md written with decision: GO + phase_16_status: PROCEED + next_phase: 13 + all 8 evidence items PASS. If decision=NO-GO: task SKIPPED (precondition).</done>
  <acceptance_criteria>
    - Task only ran if DECISION.md frontmatter `decision: GO`.
    - `.claude/skills/spike-findings-codegen-market-libs/` exists with SKILL.md (frontmatter + body sections per template), references/ (3 markdown files), sources/ (4 sub-dirs copied from spike).
    - CLAUDE.md has a new bullet under `## Auto-loaded Knowledge` referencing `Skill("spike-findings-codegen-market-libs")`.
    - MANIFEST.md SPIKE-005 Verdict column shows `GO`.
    - 12-SUMMARY.md has frontmatter with all 8 evidence items PASS + decision: GO + next_phase: 13 + phase_16_status: PROCEED + signoff_date + signoff_by.
    - Zero mutations under packages/.
  </acceptance_criteria>
</task>

<task id="12-03-04a" type="auto" tdd="false">
  <name>Task 12-03-04a: NO-GO branch — spike artifacts + Skill wrap-up (local to .planning/spikes/ and .claude/skills/)</name>
  <read_first>
    - .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md (must have decision: NO-GO frontmatter from Task 12-03-02)
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §D-NOGO-02 (NO-GO close-out artifacts)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 10 — NO-GO Close-Out Artifacts" §10.1 + §10.4 (NO-GO.md + pending todo recipes)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/{001a,001b,001c,001d}/FINDING.md (root cause identification — which one FAILed)
    - .claude/skills/spike-findings-market-libs/SKILL.md (template format model)
    - .planning/spikes/WRAP-UP-SUMMARY.md (existing wrap-up pattern reference)
  </read_first>
  <action>
    PRECONDITION: this task only runs if Task 12-03-02 set `decision: NO-GO`. Read DECISION.md frontmatter at task start; if `decision: GO` is set, SKIP this task entirely. If `decision: TBD`, ABORT and surface as a blocker.

    Scope of this task (WARNING #5 from plan checker — Task 12-03-04 split into 04a + 04b):
    THIS task (04a) writes ONLY local-to-spike-and-skills artifacts. It does NOT touch project-state governance files (REQUIREMENTS.md, ROADMAP.md, CLAUDE.md, 12-SUMMARY.md) — those belong to 04b, which depends on this task completing.

    Step 1 — Identify the FIRST FAILing evidence item (or matriz audit failure or timebox overrun) by reading evidence-checklist.txt + the 4 FINDING.md files. The first failure is the canonical root cause.

    Step 2 — Write `.planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md` per Recipe 10.1:
      ```markdown
      # SPIKE-005 — NO-GO Decision

      **Decided:** <YYYY-MM-DD from DECISION.md signoff_date>
      **Signed off by:** <name from DECISION.md signoff_by>
      **Time spent:** <hh from timebox check>h <mm>m (D-SCOPE-03 cap: 24h)

      ## Root Cause Analysis

      <Specific failure: one of>
        - B8 identity break (item 2 FAIL): codegen emitted thunk wrapper instead of alias assignment.
        - ruff false-positives (item 3 or 4 FAIL): ASYNC1xx rules triggered on sync-generated output.
        - Marker syntax conflict (item 8 FAIL): @generated marker placement broke `from __future__ import annotations`.
        - Matriz construct without sync rewrite (D-SCOPE-02): line <N> `asyncio.<X>` has no `additional_replacements` match and is not in any deny-listed module.
        - Per-package additional_replacements explosion: > 10 entries accumulated, signaling unasync's tokenize approach is undersized for this codebase.
        - 1-day timebox overrun (D-SCOPE-03): cumulative wall-clock > 24h with non-converged diff.

      Concrete failure transcript:
      ```
      <copy-paste from evidence-checklist.txt OR the relevant FINDING.md ## Run Transcript section>
      ```

      ## Why Not Extend Phase 12?

      Per D-NOGO-01: libcst exploration requires AST-level reasoning (not token-replacement). It
      needs its own dedicated spike. Adding it inline to Phase 12 violates the 1-day timebox
      (D-SCOPE-03) and the v1.2 milestone scope.

      ## What's Deferred to v1.3?

      - libcst AST-level codemod evaluation for unasync alternatives
      - REFAC-06 entire scope (single-source sync/async transport shells)

      See `.planning/todos/pending/spike-codegen-libcst-v1.3.md` for v1.3 capture.

      ## Impact on v1.2 Roadmap

      - Phase 16 (Codegen Single-Source) — DROPPED from v1.2 schedule.
      - Phase 17 (LIVE-03) — UNBLOCKED; runs immediately after Phase 14 + 15.
      - REFAC-06 moved from v1.2 "Open" → v1.2 "Future Requirements (Defer to v1.3+)" in REQUIREMENTS.md.

      (The actual REQUIREMENTS.md + ROADMAP.md + CLAUDE.md + 12-SUMMARY.md edits land in
      Task 12-03-04b, which depends on this NO-GO.md being written first.)
      ```

    Step 3 — Write `.planning/todos/pending/spike-codegen-libcst-v1.3.md` per Recipe 10.4 (full content as documented in 12-RESEARCH.md §10.4, with `<YYYY-MM-DD>` and `<one-liner>` placeholders filled from NO-GO.md):
      Frontmatter:
        ```yaml
        ---
        category: spike
        captured_date: <YYYY-MM-DD>
        triggering_decision: SPIKE-005 NO-GO
        priority: medium
        target_milestone: v1.3
        related: [REFAC-06]
        ---
        ```
      Body sections: # Pending Todo: Codegen Single-Source Spike — libcst (v1.3) / ## Scope / ## Why libcst over unasync at v1.3? / ## Acceptance criteria for v1.3 libcst spike / ## Out of scope for v1.3 spike. Use the exact body content from 12-RESEARCH.md §10.4.

    Step 4 — Produce the NO-GO flavor of the wrap-up Skill at `.claude/skills/spike-findings-codegen-market-libs/`:
      Invoke `/gsd-spike --wrap-up` if available. If the slash-command is not available in the current execution shell, fall back to the manual wrap-up: copy the spike subdirectories into a new skill structure as follows:

        Create `.claude/skills/spike-findings-codegen-market-libs/` directory structure:
          `.claude/skills/spike-findings-codegen-market-libs/`
          `├── SKILL.md`
          `├── references/`
          `│   ├── unasync-failure-mode.md`              # what failed + transcripts
          `│   ├── matriz-construct-audit.md`            # copy of 001c/matriz-aio-constructs.md + triage notes
          `│   └── libcst-v1.3-exploration-path.md`      # AST-level codemod approach for v1.3`
          `└── sources/`
          `    ├── 001a-ambito-round-trip/`              # full copy of spike sub-dir`
          `    ├── 001b-ambito-marker-future-compat/`
          `    ├── 001c-matriz-construct-audit/`
          `    └── 001d-matriz-deny-list-config/`

        Use `mkdir -p` and `cp -r` for the directory + sources copies. References are NEW files synthesized from FINDING.md content (do NOT just symlink — these are auto-loaded references that need to be self-contained).

      Write `.claude/skills/spike-findings-codegen-market-libs/SKILL.md` with NO-GO flavor frontmatter:
      ```yaml
      ---
      name: spike-findings-codegen-market-libs
      description: Implementation blueprint from Phase 12 codegen spike (SPIKE-005, NO-GO <YYYY-MM-DD>). Documents the unasync failure mode + libcst exploration path for v1.3. Auto-loaded when v1.3 libcst spike or any future codegen revisit is planned.
      ---
      ```
      Body sections:
        `<context>` — Phase 12 NO-GO outcome, root cause, defer to v1.3.
        `<requirements>` — what v1.3 libcst spike MUST honor: same 8 D-RIGOR-01 items + new libcst-specific items.
        `<findings_index>` — failure mode table (which evidence item failed, with line numbers / transcripts).
        `<integration_blueprint>` — libcst v1.3 approach: AST-level CSTTransformer pattern; references libcst >= 1.8.0,<2 evaluated for AST-level codemod (preserves whitespace + comments).
        `<metadata>` — Processed spikes list (001a/b/c/d with verdicts).

    Step 5 — Update `.planning/spikes/MANIFEST.md` SPIKE-005 row: flip Verdict column from `TBD` to `NO-GO`. Append a "## SPIKE-005 Forward References" section noting: REFAC-06 deferred to v1.3; the v1.3 libcst spike captures the next iteration in `.planning/todos/pending/spike-codegen-libcst-v1.3.md`.

    Anti-Pitfall 2 final check: `git status --porcelain packages/` MUST return zero lines.
  </action>
  <verify>
    <automated>
      grep -q '^decision: NO-GO$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md && \
      grep -q '## Root Cause Analysis' .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md && \
      test -f .planning/todos/pending/spike-codegen-libcst-v1.3.md && \
      grep -q 'target_milestone: v1.3' .planning/todos/pending/spike-codegen-libcst-v1.3.md && \
      test -f .claude/skills/spike-findings-codegen-market-libs/SKILL.md && \
      grep -q 'NO-GO' .claude/skills/spike-findings-codegen-market-libs/SKILL.md && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001a-ambito-round-trip && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001b-ambito-marker-future-compat && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001c-matriz-construct-audit && \
      test -d .claude/skills/spike-findings-codegen-market-libs/sources/001d-matriz-deny-list-config && \
      grep -E '\| 005 \| codegen-tool-choice .* NO-GO' .planning/spikes/MANIFEST.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md, .planning/todos/pending/spike-codegen-libcst-v1.3.md, .claude/skills/spike-findings-codegen-market-libs/{SKILL.md,references/*.md,sources/001a*..001d*}, .planning/spikes/MANIFEST.md</files>
  <done>If decision=NO-GO: NO-GO.md written with root cause; spike-codegen-libcst-v1.3.md pending todo written; SKILL.md (NO-GO flavor) + sources/ + references/; MANIFEST.md verdict=NO-GO. If decision=GO: task SKIPPED (precondition). Project-state governance files (REQUIREMENTS.md, ROADMAP.md, CLAUDE.md, 12-SUMMARY.md) are NOT touched here — that is Task 12-03-04b.</done>
  <acceptance_criteria>
    - Task only ran if DECISION.md frontmatter `decision: NO-GO`.
    - `.planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md` exists with Root Cause Analysis section + concrete failure transcript.
    - `.planning/todos/pending/spike-codegen-libcst-v1.3.md` exists with target_milestone: v1.3 + content per Recipe 10.4.
    - `.claude/skills/spike-findings-codegen-market-libs/SKILL.md` exists with NO-GO flavor description + sources/ + references/.
    - MANIFEST.md SPIKE-005 Verdict = NO-GO.
    - Zero mutations under packages/.
    - Project-state governance files (REQUIREMENTS.md, ROADMAP.md, CLAUDE.md, 12-SUMMARY.md) are UNCHANGED by this task (they belong to 04b).
  </acceptance_criteria>
</task>

<task id="12-03-04b" type="auto" tdd="false">
  <name>Task 12-03-04b: NO-GO branch — project-state governance edits (REQUIREMENTS.md + ROADMAP.md + CLAUDE.md + 12-SUMMARY.md)</name>
  <read_first>
    - .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md (must have decision: NO-GO frontmatter)
    - .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md (precondition: must exist; written by 12-03-04a)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 10 — NO-GO Close-Out Artifacts" §10.2 + §10.3 + §10.5 (REQUIREMENTS.md + ROADMAP.md + 12-SUMMARY.md diff sketches)
    - .planning/REQUIREMENTS.md (current state — REFAC-06 in §"Arquitectura sync/async dedup" + Traceability table row)
    - .planning/ROADMAP.md (current state — Phase 16 entry + Progress table row 16)
    - CLAUDE.md §"Auto-loaded Knowledge" (where the new skill reference will be inserted)
  </read_first>
  <action>
    PRECONDITIONS (both must hold):
      (a) DECISION.md frontmatter `decision: NO-GO` (same precondition as 12-03-04a; if `decision: GO`, SKIP this task entirely).
      (b) NO-GO.md must already exist at `.planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md` (written by 12-03-04a). If it does not exist, ABORT — 12-03-04a must run first.

    Scope of this task (WARNING #5 from plan checker — second half of split): project-state governance edits only. Spike-local artifacts (NO-GO.md, pending todo, SKILL.md, MANIFEST.md row) are 12-03-04a's responsibility and are already done by the time this task runs.

    Step 1 — Update `.planning/REQUIREMENTS.md` per Recipe 10.2 (edit in place, do not create a new file):
      (a) Remove the `- [ ] **REFAC-06** ...` line from §"Arquitectura sync/async dedup (REFAC)".
      (b) Append the NO-GO entry to §"Future Requirements (Defer to v1.3+)" as the new first bullet:
          `- **REFAC-06** (deferred per Phase 12 NO-GO <YYYY-MM-DD>): unasync/codegen single-source for client.py/aio.py transport shells × 4 packages. NO-GO root cause: <one-liner from NO-GO.md>. Re-evaluation requires a v1.3 spike on libcst (AST-level codemod). See .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md and .planning/todos/pending/spike-codegen-libcst-v1.3.md.`
      (c) Update §Traceability table: change `| REFAC-06 | Phase 16 (conditional)      | Open   |` to `| REFAC-06 | Defer to v1.3                | Deferred (Phase 12 NO-GO <YYYY-MM-DD>) |`.
      (d) Update the "Coverage:" line at the bottom: change `5/5 requirements mapped` to `4/5 requirements mapped to v1.2 phases (REFAC-05, SEC-01, ERG-01, LIVE-03); REFAC-06 deferred to v1.3 per Phase 12 NO-GO.`.

    Step 2 — Update `.planning/ROADMAP.md` per Recipe 10.3:
      (a) In the §"Phase 16: Codegen Single-Source (REFAC-06) — CONDITIONAL" subsection, replace the `**Conditional**:` paragraph with:
          `**Status:** DROPPED. Phase 12 returned NO-GO on <YYYY-MM-DD>. REFAC-06 deferred to v1.3. See .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md.`
      (b) In the Progress table, change row 16 from:
          `| 16. Codegen Single-Source (CONDITIONAL on Phase 12)         | v1.2      | 0/?   | Not started | -          |`
          to:
          `| 16. Codegen Single-Source (DROPPED — Phase 12 NO-GO)         | v1.2      | -     | DROPPED     | <YYYY-MM-DD>|`
      (c) In the summary checklist at the top of v1.2 (the bulleted "Summary checklist (Phases 12-17)" list), change the `[ ] **Phase 16:** ...` line to `~~[ ] **Phase 16:** Codegen Single-Source — DROPPED per Phase 12 NO-GO; REFAC-06 deferred to v1.3.~~`.

    Step 3 — Update CLAUDE.md "Auto-loaded Knowledge" with the new skill (NO-GO flavor) bullet:
      `- **Spike findings for codegen (Phase 12 NO-GO)** (unasync failure mode + libcst v1.3 exploration path) → Skill("spike-findings-codegen-market-libs")`

    Step 4 — Write `.planning/phases/12-codegen-spike/12-SUMMARY.md` per Recipe 10.5 with frontmatter:
      ```yaml
      ---
      gsd_state_version: 1.0
      phase: 12
      phase_name: Codegen Spike
      status: complete
      decision: NO-GO
      spike_directory: .planning/spikes/SPIKE-005-codegen-tool-choice/
      skill_produced: .claude/skills/spike-findings-codegen-market-libs/
      signoff_date: <YYYY-MM-DD>
      signoff_by: <name>
      next_phase: 13
      phase_16_status: DROPPED
      defer_to_milestone: v1.3
      root_cause: <one-liner from NO-GO.md>
      requirements_updated: [REFAC-06]
      roadmap_updated: [Phase 16]
      pending_todos_created: [spike-codegen-libcst-v1.3.md]
      ---
      ```
      Body sections: # Phase 12: Codegen Spike — Summary (NO-GO) / ## Decision (NO-GO + root cause) / ## Evidence Checklist (D-RIGOR-01 — first failing item highlighted) / ## Spike Directory / ## Skill Produced (NO-GO flavor) / ## REQUIREMENTS.md / ROADMAP.md Updates (with diffs applied) / ## Anti-Pitfall Compliance / ## Next Steps:
        1. `/gsd-plan-phase 13` (Cross-Package Ergonomics).
        2. Phase 17 (LIVE-03) is UNBLOCKED to run immediately after Phase 14 + 15.
        3. v1.3 milestone tracker captures `spike-codegen-libcst-v1.3.md` as a first scheduled spike.

    Anti-Pitfall 2 final check: `git status --porcelain packages/` MUST return zero lines.
  </action>
  <verify>
    <automated>
      grep -q '^decision: NO-GO$' .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/NO-GO.md && \
      grep -q 'REFAC-06.*deferred per Phase 12 NO-GO' .planning/REQUIREMENTS.md && \
      grep -q 'Defer to v1.3' .planning/REQUIREMENTS.md && \
      grep -q '4/5 requirements mapped' .planning/REQUIREMENTS.md && \
      grep -q 'DROPPED.*Phase 12 NO-GO' .planning/ROADMAP.md && \
      grep -q 'spike-findings-codegen-market-libs' CLAUDE.md && \
      test -f .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^decision: NO-GO$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^phase_16_status: DROPPED$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      grep -q '^next_phase: 13$' .planning/phases/12-codegen-spike/12-SUMMARY.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, CLAUDE.md, .planning/phases/12-codegen-spike/12-SUMMARY.md</files>
  <done>If decision=NO-GO and NO-GO.md exists: REFAC-06 moved to REQUIREMENTS.md Future Requirements + Traceability row updated; ROADMAP.md Phase 16 marked DROPPED + Progress table updated; CLAUDE.md auto-load updated; 12-SUMMARY.md written with decision: NO-GO + phase_16_status: DROPPED + next_phase: 13 + defer_to_milestone: v1.3 + root_cause. If decision=GO OR NO-GO.md missing: task SKIPPED/ABORTED per PRECONDITION.</done>
  <acceptance_criteria>
    - Task only ran if DECISION.md `decision: NO-GO` AND NO-GO.md exists (12-03-04a completed first).
    - `.planning/REQUIREMENTS.md`: REFAC-06 moved from "Arquitectura sync/async dedup" to "Future Requirements (Defer to v1.3+)"; Traceability table row updated to "Defer to v1.3"; Coverage line updated to 4/5.
    - `.planning/ROADMAP.md`: Phase 16 subsection shows "Status: DROPPED"; Progress table row 16 status = DROPPED; summary checklist shows Phase 16 struck-through.
    - CLAUDE.md `## Auto-loaded Knowledge` has new bullet referencing the skill.
    - 12-SUMMARY.md frontmatter has decision: NO-GO + phase_16_status: DROPPED + next_phase: 13 + defer_to_milestone: v1.3 + root_cause + signoff_date + signoff_by.
    - Zero mutations under packages/.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DECISION.md frontmatter | Signoff is binary GO/NO-GO; immutable once committed (a flip requires reopening the spike, which violates the timebox) |
| Task 12-03-03/04 → REQUIREMENTS.md + ROADMAP.md (NO-GO only) | Mutates project-state artifacts; these affect downstream phases (13, 14, 15, 17) |
| Task 12-03-03/04 → CLAUDE.md "Auto-loaded Knowledge" | Mutates the auto-load list; new skill becomes globally available to all future agents in this repo |
| Task 12-03-01 sandbox-copy → /tmp/spike-005-ambito-test-<timestamp>/ | Temporary sandbox for item 6 (ámbito pytest against generated); cleaned up at end of task to avoid disk leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Tampering | NO-GO branch's REQUIREMENTS.md / ROADMAP.md edits being applied during a GO outcome (or vice versa) | mitigate | Tasks 12-03-03, 12-03-04a, and 12-03-04b each begin with a PRECONDITION check reading DECISION.md frontmatter; if decision doesn't match the task's intent, the task SKIPS. 12-03-04b adds a second precondition (NO-GO.md must exist) to enforce the 04a→04b order. Mutual exclusion is enforced. |
| T-12-03-02 | Repudiation | Operator signoff disputed later ("I never signed GO/NO-GO on that day") | mitigate | DECISION.md frontmatter captures signoff_date + signoff_by; the git commit at end of Plan 03 includes the signed DECISION.md and a co-authored-by commit message. The audit trail is the git history. |
| T-12-03-03 | Tampering | item 6 (ámbito pytest green vs generated) accidentally modifying packages/ambito-financiero-client/ in place | mitigate | Sandbox-copy approach per Anti-Pitfall 2: `cp -r packages/ambito-financiero-client/` to `/tmp/spike-005-ambito-test-<ts>/`; overwrite client.py inside the sandbox; pytest runs against the sandbox. Cleanup with `rm -rf` at end of task. Acceptance criteria checks both `git status --porcelain packages/` is empty AND the temp dir is removed. |
| T-12-03-04 | Tampering | Operator-supplied signoff_by string containing shell metacharacters that break downstream parsing | accept | The signoff_by is captured as YAML scalar; downstream consumers parse via YAML (PyYAML / etc.), not shell. Risk is low. |
| T-12-03-05 | Information Disclosure | DECISION.md or NO-GO.md transcripts embedding `.env` credentials accidentally | mitigate | Item 6's pytest run uses pytest-httpx (mocked); ambito has no auth so no .env to embed. Anti-Pitfall: transcripts are limited to last-30-lines of pytest output (no DEBUG-level logs). |
| T-12-03-SC | Tampering (Supply Chain) | `/gsd-spike --wrap-up` slash-command consuming spike content | accept | The slash-command is project-local SDK code; equivalent to manual `cp -r` operation. No external network call. |

## ASVS L1 Alignment

- V1.1: trust boundaries documented (table above).
- V14.2 (Dependency management): no new install (Plan 01 already validated unasync); only file copies + project-state edits.
- V8.3 (Data integrity): the binary GO/NO-GO decision is captured in DECISION.md frontmatter + committed to git; immutable history.
</threat_model>

<verification>
After all 5 tasks complete (only one of 12-03-03 OR the pair 12-03-04a+12-03-04b will have non-skip outcome):

1. `cat .planning/spikes/SPIKE-005-codegen-tool-choice/DECISION.md | head -20` shows frontmatter with decision (GO or NO-GO), signoff_date, signoff_by, evidence_checklist subkeys.
2. `cat .planning/phases/12-codegen-spike/12-SUMMARY.md | head -25` shows the frontmatter matching the decision branch.
3. `grep -c spike-findings-codegen-market-libs CLAUDE.md` returns > 0 (the new skill is auto-load registered).
4. `test -f .claude/skills/spike-findings-codegen-market-libs/SKILL.md` is true.
5. `grep "SPIKE-005" .planning/spikes/MANIFEST.md | grep -E "(GO|NO-GO)"` returns at least 1 match (no longer TBD).
6. NO-GO branch additional: `grep "REFAC-06" .planning/REQUIREMENTS.md` shows the row in "Future Requirements" section; `grep "DROPPED" .planning/ROADMAP.md` shows Phase 16 entry.
7. GO branch additional: `grep "PROCEED" .planning/phases/12-codegen-spike/12-SUMMARY.md` confirms phase_16_status: PROCEED.
8. `git status --porcelain packages/` is empty across all 4 tasks.
</verification>

<success_criteria>
- D-RIGOR-01 8-item evidence collection executed with fresh re-runs against spike artifacts.
- D-SCOPE-03 1-day timebox check performed; status WITHIN-CAP|OVER-CAP recorded.
- Operator signed DECISION.md with binary GO|NO-GO + date + name.
- Exactly ONE of branches runs: 12-03-03 (GO) OR 12-03-04a + 12-03-04b (NO-GO; 04a first, 04b after). The other branch SKIPS per PRECONDITION.
- `spike-findings-codegen-market-libs` SKILL.md exists and is registered in CLAUDE.md auto-load (both branches produce it; flavor differs).
- 12-SUMMARY.md exists with frontmatter matching the decision branch (next_phase: 13 in both; phase_16_status: PROCEED|DROPPED).
- If NO-GO: REQUIREMENTS.md + ROADMAP.md updated + pending todo created.
- MANIFEST.md SPIKE-005 verdict flipped from TBD.
- Zero mutations under `packages/` (Anti-Pitfall 2 enforced across all tasks).
</success_criteria>

<output>
After all tasks complete, create `.planning/phases/12-codegen-spike/12-03-SUMMARY.md` AND `.planning/phases/12-codegen-spike/12-SUMMARY.md` (the latter is the phase close artifact; the former is the plan close).

12-03-SUMMARY.md (plan close) frontmatter:
```yaml
---
phase: 12-codegen-spike
plan: 03
status: complete
wave: 3
tasks_completed: [12-03-01, 12-03-02, 12-03-03|(12-03-04a + 12-03-04b)]
files_modified: [list, including project-state edits if NO-GO]
decision: GO|NO-GO
signoff_date: <YYYY-MM-DD>
signoff_by: <name>
evidence_8_items_pass: <N>/8
matriz_audit_unresolved: 0
timebox_status: WITHIN-CAP|OVER-CAP
skill_produced: .claude/skills/spike-findings-codegen-market-libs/
phase_16_status: PROCEED|DROPPED
---
```

Body: ## Summary / ## Decision Outcome / ## Evidence Checklist Tally / ## Branch Executed (GO or NO-GO) / ## Project State Impact (REQUIREMENTS.md + ROADMAP.md edits if NO-GO) / ## Next Steps (Phase 13 next).

12-SUMMARY.md (phase close): see Task 12-03-03 (GO) or Task 12-03-04b (NO-GO; 04a writes spike-local artifacts, 04b writes 12-SUMMARY.md) — already drafted by the executed branch task.
</output>
