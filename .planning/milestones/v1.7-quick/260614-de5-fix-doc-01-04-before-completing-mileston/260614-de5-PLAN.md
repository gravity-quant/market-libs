---
phase: quick
plan: 260614-de5
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md
  - .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md
  - .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-02-SUMMARY.md
  - .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md
  - .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md
  - packages/matriz-client/src/matriz_client/_state.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 10 + 11 SUMMARY.md frontmatter `requirements_completed` lists every REQ-ID in the corresponding *-VALIDATION.md `requirements_closed`"
    - "REQUIREMENTS.md traceability table shows `Complete` for every REQ-ID whose body checkbox is `[x]` and whose phase VALIDATION/VERIFICATION reports `passed`/`approved`"
    - "ORP-01 dead field `account_id: str | None = None` is removed from matriz `_state.py`; matriz pytest suite stays green"
    - "Each closed phase has both a *-VALIDATION.md (operator-signed) and a *-VERIFICATION.md (3-source-matrix shim) under its phase directory"
  artifacts:
    - path: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md"
      provides: "Phase 10 plan-04 closure SUMMARY with `requirements_completed: [REFAC-04, LIVE-02]` in frontmatter"
      contains: "requirements_completed"
    - path: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md"
      provides: "Phase 11 plan-01 SUMMARY with `requirements_completed: [HARN-07, HARN-08, HARN-09, HARN-10]`"
      contains: "requirements_completed"
    - path: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-02-SUMMARY.md"
      provides: "Phase 11 plan-02 SUMMARY with `requirements_completed: [CR-01, CR-02, CR-04, CR-06, CR-07, CR-08]`"
      contains: "requirements_completed"
    - path: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md"
      provides: "Phase 11 plan-03 SUMMARY with `requirements_completed: [LIVE-01]`"
      contains: "requirements_completed"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table with 18 stale rows flipped from `Open` to `Complete`"
      contains: "Complete"
    - path: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md"
      provides: "5-line 3-source-matrix shim for Phase 10 referencing 10-VALIDATION.md + operator signoff"
      min_lines: 10
    - path: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md"
      provides: "5-line 3-source-matrix shim for Phase 11 referencing 11-VALIDATION.md + operator signoff"
      min_lines: 10
    - path: "packages/matriz-client/src/matriz_client/_state.py"
      provides: "`_ClientState` dataclass without the ORP-01 dead field `account_id`"
      contains: "@dataclass"
  key_links:
    - from: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md"
      to: ".planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md"
      via: "requirements_completed frontmatter mirrors requirements_closed from VALIDATION"
      pattern: "requirements_completed:.*REFAC-04.*LIVE-02"
    - from: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md"
      to: ".planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VALIDATION.md"
      via: "requirements_completed frontmatter draws from requirements_closed"
      pattern: "requirements_completed:.*LIVE-01"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "audit's `requirements: 29/29` is mirrored by traceability table showing zero `Open`"
      pattern: "\\| (REFAC-0[1-4]|CR-0[1-8]|HARN-(07|08|09|10)|LIVE-0[12]) +\\| Phase [0-9]+ +\\| Complete"
---

<objective>
Close the four documentation-hygiene follow-ups (DOC-01..04) flagged by the v1.1 milestone audit (`.planning/v1.1-MILESTONE-AUDIT.md` `status: passed`, `follow_ups`) so the persisted planning artifacts match the operator-signed reality, and `/gsd-complete-milestone v1.1` can run cleanly.

Purpose: The audit's verdict is `passed` (29/29 requirements, 9/9 flows, 6/6 phases). The only outstanding items are cosmetic doc drift:

- **DOC-01**: SUMMARY frontmatter `requirements_completed` empty for Phase 10 + 11 plans despite VALIDATION.md `requirements_closed` listing 13 IDs.
- **DOC-02**: REQUIREMENTS.md traceability table shows `Open` for 18 IDs whose body checkboxes are already `[x]` (REFAC-01..04, CR-01..08, HARN-07..10, LIVE-01..02 — that's 4+8+4+2=18; the audit said "17" but the listed enumeration totals 18; the executor must cross-check the literal table rows and flip exactly the rows that are stale).
- **DOC-03**: `packages/matriz-client/src/matriz_client/_state.py:59` carries the ORP-01 dead field `account_id: str | None = None`; integration checker confirmed 0 read sites; preflight `rg` in this plan re-confirmed 0 hits.
- **DOC-04**: Phase 10 + 11 lack `*-VERIFICATION.md` (the operator chose to close via `*-VALIDATION.md status: approved`); 3-source-matrix tooling expects a verification shim. Emit a 5-line shim per phase that references the VALIDATION.md + operator signoff.

Output: 4 SUMMARY frontmatter edits + 1 REQUIREMENTS.md edit + 1 source removal (3 lines) + 2 new VERIFICATION shims (≤ 15 lines each). No runtime behavior changes. Atomic commits: docs grouped, code (DOC-03) isolated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-VALIDATION.md
@.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VALIDATION.md
@.planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md
@.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md
@packages/matriz-client/src/matriz_client/_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill SUMMARY frontmatter + sync REQUIREMENTS.md traceability table + emit VERIFICATION shims (DOC-01, DOC-02, DOC-04)</name>
  <files>
    .planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md
    .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md
    .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-02-SUMMARY.md
    .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md
    .planning/REQUIREMENTS.md
    .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md
    .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md
  </files>
  <action>
    Three pure-doc fixes grouped into ONE atomic commit (no code touched):

    **DOC-01 — Backfill `requirements_completed` in SUMMARY frontmatter.** The authoritative source is each phase's VALIDATION.md `requirements_closed`. Read each SUMMARY first with the Read tool; locate the frontmatter line `requirements_completed: []` (or insert it if absent) and replace with a YAML list. Distribute REQ-IDs to the plan that delivered them — confirm by re-reading each SUMMARY's body before committing the assignment. Recommended distribution (matches plan body coverage already verified during audit):

    - `10-04-SUMMARY.md` → `requirements_completed: [REFAC-04, LIVE-02]` (Plan 10-04 is the phase-closure plan; this mirrors the Phase 6 pattern where 06-07 SUMMARY closes REFAC-01 + REFAC-02 across the phase).
    - `11-01-SUMMARY.md` → `requirements_completed: [HARN-07, HARN-08, HARN-09, HARN-10]` (HARN-07: BEGIN/END parser; HARN-08: idempotent_by_title kwarg; HARN-09: operator field preservation; HARN-10: D-MATZ-27 dedupe — all delivered in Plan 11-01 per 11-VALIDATION.md `HARN-07/08/09/10 Closure Evidence`).
    - `11-02-SUMMARY.md` → `requirements_completed: [CR-01, CR-02, CR-04, CR-06, CR-07, CR-08]` (delivered in Plan 11-02 per 11-VALIDATION.md `CR-01/02/04/06/07/08 Closure Evidence` table).
    - `11-03-SUMMARY.md` → `requirements_completed: [LIVE-01]` (delivered as the LIVE-01 final gate per 11-VALIDATION.md `LIVE-01 Evidence`).

    Insert the new key in alphabetical position within frontmatter (after `provides:`, `affects:` blocks where natural). Keep YAML format consistent with neighboring lists (single-line short-form `[A, B]` if the list is ≤ 4 items, multi-line block form if larger — match what each file already does).

    **DOC-02 — Flip stale rows in REQUIREMENTS.md traceability table.** The table is at lines 181–211 of REQUIREMENTS.md. Read the current state with Read first, then use Edit to flip rows from `| Open   |` to `| Complete |` for these REQ-IDs:

    REFAC-01, REFAC-02, REFAC-03, REFAC-04, CR-01, CR-02, CR-03, CR-04, CR-05, CR-06, CR-07, CR-08, HARN-07, HARN-08, HARN-09, HARN-10, LIVE-01, LIVE-02

    That's 18 IDs (4 REFAC + 8 CR + 4 HARN + 2 LIVE). The audit `follow_ups:` block in `v1.1-MILESTONE-AUDIT.md` called out "17 rows" but the enumeration in the same file (`tech_debt[3].items[3]`) lists 18 IDs; the table itself is the source of truth — flip exactly the rows whose `Status` column says `Open` AND whose body checkbox is `[x]`. Do NOT touch RELY/LOG/BUG rows (already `Complete`). Preserve column alignment (the table uses `|----------|-----------|--------|` separators with padding).

    After the flips, verify with `grep -c "| Open" .planning/REQUIREMENTS.md` — expected result `0` (all 29 rows should now be `Complete`).

    **DOC-04 — Emit Phase 10 + Phase 11 VERIFICATION.md shims.** Use the Write tool to create:

    `.planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md` with literal content:

    ```markdown
    ---
    phase: 10-matriz-aio-py-creation-tokenstore
    verified: 2026-06-14
    status: passed
    score: 2/2 requirements closed (per 10-VALIDATION.md)
    operator_signoff_date: 2026-06-14
    operator_signoff_by: sebadlf (Sebastián de la Fuente)
    verification_artifact: 10-VALIDATION.md
    ---

    # Phase 10 — Verification

    This phase was closed via `10-VALIDATION.md` `status: approved` + operator signoff (frontmatter `operator_signoff_date: 2026-06-14`, `live_paridad_sync_async: true`, `nyquist_compliant: true`, `wave_status: complete`). See that file for the full evidence trail, requirements_closed list (`REFAC-04`, `LIVE-02`), baseline/head commits, and operator dispositions per package. This VERIFICATION.md is a 3-source-matrix shim for milestone audit tooling.
    ```

    `.planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md` with literal content:

    ```markdown
    ---
    phase: 11-harness-hardening-code-review-close-out-live-re-verification
    verified: 2026-06-14
    status: passed
    score: 11/11 requirements closed (per 11-VALIDATION.md)
    operator_signoff_date: 2026-06-14
    operator_signoff_by: sebadlf (Sebastián de la Fuente)
    verification_artifact: 11-VALIDATION.md
    ---

    # Phase 11 — Verification

    This phase was closed via `11-VALIDATION.md` `status: approved` + operator signoff (frontmatter `operator_signoff_date: 2026-06-14`, `phase_status: ready_for_close`, `nyquist_compliant: true`). See that file for the full evidence trail, requirements_closed list (`HARN-07/08/09/10`, `CR-01/02/04/06/07/08`, `LIVE-01`), baseline/head commits (`4d48e07` → `71bf201`), and operator dispositions per package (ambito/iol/higyrus/matriz). This VERIFICATION.md is a 3-source-matrix shim for milestone audit tooling.
    ```

    Note: the executor should treat the markdown bodies above as literal file content. The ```markdown fences are presentational — the actual file content STARTS at the `---` frontmatter line and ends with the final paragraph (no closing fence in the file).

    **Atomic commit (per gsd-quick convention, single docs commit covers DOC-01/02/04):**

    ```
    docs(quick-260614-de5): sync v1.1 docs after audit — DOC-01/02/04

    - DOC-01: backfill requirements_completed in Phase 10 plan 04 + Phase 11 plans 01/02/03 SUMMARY frontmatter (from VALIDATION.md requirements_closed)
    - DOC-02: flip 18 traceability rows from Open to Complete in REQUIREMENTS.md
    - DOC-04: emit 10-VERIFICATION.md + 11-VERIFICATION.md (5-line 3-source-matrix shims)

    Closes audit follow-ups DOC-01/02/04 from .planning/v1.1-MILESTONE-AUDIT.md.
    No runtime impact; persisted docs now match operator-signed VALIDATION.md state.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>
      # DOC-01: every SUMMARY must list its REQ-IDs
      grep -E "requirements_completed:.*REFAC-04" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md && \
      grep -E "requirements_completed:.*LIVE-02" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md && \
      grep -E "requirements_completed:.*HARN-07" .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md && \
      grep -E "requirements_completed:.*CR-01" .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-02-SUMMARY.md && \
      grep -E "requirements_completed:.*LIVE-01" .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md && \
      # DOC-02: zero `Open` rows remain in the traceability table
      test $(grep -c "| Open" .planning/REQUIREMENTS.md) -eq 0 && \
      # DOC-04: VERIFICATION shims exist with proper frontmatter
      grep -q "verification_artifact: 10-VALIDATION.md" .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md && \
      grep -q "verification_artifact: 11-VALIDATION.md" .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md && \
      # Atomic commit landed
      git log -1 --format=%s | grep -q "DOC-01/02/04" && \
      echo "Task 1 verification PASS"
    </automated>
  </verify>
  <done>
    - 4 SUMMARY files have non-empty `requirements_completed:` matching the assignment table in `<action>` and reflecting the corresponding VALIDATION.md `requirements_closed`.
    - REQUIREMENTS.md traceability table has zero `Open` rows (all 29 say `Complete`).
    - Two new `*-VERIFICATION.md` files exist under Phase 10 and Phase 11 directories, each with frontmatter `status: passed`, `verification_artifact: <phase>-VALIDATION.md`, and operator signoff date.
    - Single atomic commit landed with `DOC-01/02/04` in the subject and Co-Authored-By trailer.
    - No source code touched in this task (DOC-03 is Task 2).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove ORP-01 dead field from matriz `_state.py` (DOC-03)</name>
  <files>packages/matriz-client/src/matriz_client/_state.py</files>
  <action>
    **DOC-03 — Remove the ORP-01 dead field.** The integration checker (see `.planning/v1.1-INTEGRATION-CHECK.md`) and the audit report confirmed zero read sites for `state.account_id` across `packages/matriz-client/`. This plan's preflight `rg "state\.account_id|\._state\.account_id" packages/matriz-client/` ALSO returned zero hits.

    **Pre-edit safety re-check (mandatory, do not skip):**

    ```bash
    rg -n "state\.account_id|\._state\.account_id|state\['account_id'\]|state\[\"account_id\"\]" packages/matriz-client/
    ```

    Expected: **zero matches**. If ANY match is found, STOP — do not edit — report to user and surface as a deviation (the audit's claim of "0 read sites" would be wrong). On a clean preflight, proceed.

    **The edit:** Use Edit to remove line 59 of `packages/matriz-client/src/matriz_client/_state.py`:

    ```
        account_id: str | None = None
    ```

    Surrounding context (read with Read first to anchor the Edit precisely):

    - Line 58: `    http_client: httpx.Client | httpx.AsyncClient | None = None`
    - Line 59: `    account_id: str | None = None`   ← REMOVE
    - Line 60: `    # Phase 10 Plan 10-03 REFAC-04: 3-way concurrent token primitive shared`

    Result: the `_ClientState` `@dataclass(slots=True)` retains `base_url`, `username`, `password`, `token`, `token_expires_at`, `http_client`, `token_store` (7 fields, was 8). Module docstring already does NOT mention `account_id` (it talks about token, http_client, refresh_token absence) so no docstring update needed. Module-level imports are unchanged.

    **Module docstring sanity check:** Read the file's docstring (lines 1–16) after the edit. If it references `account_id` (it does not in current state), update accordingly. The current docstring is account_id-free, so no further action expected.

    **Verification after the edit:**

    ```bash
    # mypy strict (matriz client surface)
    uv run mypy packages/matriz-client/src/matriz_client/_state.py

    # ruff (line-length + format)
    uv run ruff check packages/matriz-client/src/matriz_client/_state.py
    uv run ruff format --check packages/matriz-client/src/matriz_client/_state.py

    # matriz test suite (must stay green; integration checker said 0 reads, this is the proof)
    uv run --package matriz-client pytest -q
    ```

    Expected: mypy `Success: no issues found`; ruff `All checks passed`; pytest `passed` (count should match the post-Phase-11 baseline for the matriz package — roughly the matriz portion of the 907-test total).

    **Atomic commit:**

    ```
    refactor(matriz-client): remove ORP-01 dead field account_id from _ClientState — DOC-03

    Audit confirmed 0 read sites across packages/matriz-client/ src and tests.
    Frozen-ish slots=True dataclass — no mutation risk, no runtime impact.

    Closes audit follow-up DOC-03 from .planning/v1.1-MILESTONE-AUDIT.md.

    Verification:
    - rg "state\.account_id" packages/matriz-client/ → 0 matches
    - uv run mypy → Success (50 source files)
    - uv run --package matriz-client pytest -q → all green

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>
      # Dead field is gone
      ! grep -q "account_id: str | None" packages/matriz-client/src/matriz_client/_state.py && \
      # No latent read sites surfaced
      test $(rg -c "state\.account_id|\._state\.account_id" packages/matriz-client/ 2>/dev/null | wc -l) -eq 0 && \
      # mypy strict still passes for the edited file
      uv run mypy packages/matriz-client/src/matriz_client/_state.py 2>&1 | grep -q "Success" && \
      # ruff clean
      uv run ruff check packages/matriz-client/src/matriz_client/_state.py 2>&1 | grep -qE "All checks passed|no issues" && \
      # matriz test suite green (uses --package isolation per project convention)
      uv run --package matriz-client pytest -q 2>&1 | tail -5 | grep -qE "passed" && \
      # Atomic commit landed
      git log -1 --format=%s | grep -q "DOC-03" && \
      echo "Task 2 verification PASS"
    </automated>
  </verify>
  <done>
    - Line 59 (`account_id: str | None = None`) is removed from `packages/matriz-client/src/matriz_client/_state.py`.
    - `_ClientState` dataclass has 7 fields (was 8); `slots=True` preserved; `frozen=` flag unchanged (it is NOT frozen — `slots=True` only — confirmed by reading the file).
    - `uv run mypy` returns `Success`; `uv run ruff check` returns clean; `uv run --package matriz-client pytest -q` stays green.
    - Single atomic commit landed with `DOC-03` in the subject and Co-Authored-By trailer.
    - Working tree clean after this task.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these end-to-end sanity gates:

```bash
# 1. Audit follow-ups satisfied (all four):
# DOC-01
for f in \
  .planning/phases/10-matriz-aio-py-creation-tokenstore/10-04-SUMMARY.md \
  .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-01-SUMMARY.md \
  .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-02-SUMMARY.md \
  .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-03-SUMMARY.md; do
  grep -q "requirements_completed:" "$f" && \
  ! grep -qE "requirements_completed:\s*\[\s*\]" "$f" || { echo "FAIL: $f"; exit 1; }
done

# DOC-02
test $(grep -c "| Open" .planning/REQUIREMENTS.md) -eq 0

# DOC-03
! grep -q "account_id: str | None" packages/matriz-client/src/matriz_client/_state.py

# DOC-04
test -f .planning/phases/10-matriz-aio-py-creation-tokenstore/10-VERIFICATION.md
test -f .planning/phases/11-harness-hardening-code-review-close-out-live-re-verification/11-VERIFICATION.md

# 2. CI gates still green (smoke):
uv run ruff check . 2>&1 | tail -1 | grep -qE "All checks passed"
uv run mypy 2>&1 | tail -1 | grep -q "Success"

# 3. Two atomic commits landed (docs + refactor):
git log --oneline -5 | head -2 | grep -c "DOC-0[1234]" | grep -q "^2$"

echo "All verification gates PASS — milestone v1.1 ready for /gsd-complete-milestone"
```
</verification>

<success_criteria>
- All 4 audit follow-ups (DOC-01, DOC-02, DOC-03, DOC-04) are satisfied: SUMMARY frontmatter backfilled, REQUIREMENTS.md table fully `Complete`, ORP-01 dead field removed, VERIFICATION shims emitted.
- Two atomic commits land on `main`: one `docs(...)` covering DOC-01/02/04, one `refactor(matriz-client)...` covering DOC-03.
- `uv run mypy` strict still returns `Success`; `uv run ruff check .` is clean (any pre-existing out-of-scope findings already excluded via CR-08 `extend-exclude` remain excluded — no NEW ruff errors introduced).
- `uv run --package matriz-client pytest -q` is green after Task 2.
- ROADMAP.md is NOT modified (quick task — outside planned-phase scope per project constraint).
- Total blast radius: 8 files changed (4 SUMMARY + 1 REQUIREMENTS + 2 new VERIFICATION + 1 _state.py); net ~30 lines added (VERIFICATION shims) + ~25 lines edited (frontmatter + table) + 1 line removed (ORP-01).
- The user can immediately run `/gsd-complete-milestone v1.1` after this task lands.
</success_criteria>

<output>
Create `.planning/quick/260614-de5-fix-doc-01-04-before-completing-mileston/260614-de5-SUMMARY.md` when done, summarising:
- Which DOC-* items were closed (all 4)
- Commit SHAs (2 atomic commits)
- Files touched (8 total)
- Verification evidence (mypy, ruff, pytest, grep counts)
- Next step: `/gsd-complete-milestone v1.1`
</output>
