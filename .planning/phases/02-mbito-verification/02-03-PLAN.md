---
phase: 02-mbito-verification
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - packages/ambito-financiero-client/tests/test_client.py
  - packages/ambito-financiero-client/tests/test_async_client.py
  - .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json
  - .planning/verification/ambito-financiero-client-findings.md
autonomous: false
requirements: [AMB-01, AMB-02, AMB-03, AMB-04, AMB-05, DRIFT-01]
tags: [verification, tests, invariants, live-run, schema-commit]
must_haves:
  truths:
    - "tests/test_client.py contiene la sección `# ------ Verified live (Phase 2) ------` con los invariantes mockeados de D-08"
    - "tests/test_async_client.py contiene la sección `# ------ Verified live (Phase 2) ------` espejo async (D-06 + D-09)"
    - "Test mockeado verifica `parse_ar_decimal('1.415,00') == 1415.0` (AMB-02)"
    - "Test mockeado verifica la URL emitida `/dolarnacion/historico-general/YYYY-MM-DD/YYYY-MM-DD` con día > 12 (AMB-03)"
    - "Test mockeado verifica el shape `list[list[str]]` con header row 0 = `['Fecha','Compra','Venta']` (AMB-01)"
    - "Test mockeado verifica que `NoDataError` se levanta cuando `len(rows) < 2` (AMB-04, ya cubierto en suite existente — confirmar invariante sigue verde y agregar entrada en sección Verified live referenciando D-08)"
    - "Sección `# ------ Regressions ------` agregada en ambos test files (vacía por ahora; lista para FIXED findings opportunistic)"
    - "`.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` existe en disco y está committeado al repo (DRIFT-01)"
    - "`.planning/verification/ambito-financiero-client-findings.md` existe en disco y está committeado al repo (D-03 lifecycle)"
    - "El driver corrió exitosamente al menos una vez contra mercados.ambito.com (live verification de AMB-01..AMB-05)"
  artifacts:
    - path: "packages/ambito-financiero-client/tests/test_client.py"
      provides: "Sync verified-live invariants + Regressions section"
      contains: "# ------ Verified live (Phase 2) ------"
      contains_also: "# ------ Regressions ------"
    - path: "packages/ambito-financiero-client/tests/test_async_client.py"
      provides: "Async verified-live invariants (mirror) + Regressions section"
      contains: "# ------ Verified live (Phase 2) ------"
      contains_also: "# ------ Regressions ------"
    - path: ".planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json"
      provides: "DRIFT-01 baseline schema snapshot for get_dollar_banco_nacion"
      contains: "\"schema\":"
      contains_also: "\"endpoint\":"
    - path: ".planning/verification/ambito-financiero-client-findings.md"
      provides: "Phase 2 findings file (skeleton + any OPEN findings from first live run)"
      contains: "# Findings: ambito-financiero-client-client"
  key_links:
    - from: "packages/ambito-financiero-client/tests/test_client.py"
      to: "ambito_financiero_client._parsing.parse_ar_decimal"
      via: "from ambito_financiero_client._parsing import parse_ar_decimal"
      pattern: "from ambito_financiero_client._parsing import parse_ar_decimal"
    - from: ".planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json"
      to: "main_ambito_financiero.py::probe_schema_snapshot"
      via: "first live driver run generates this file"
      pattern: "get-dollar-banco-nacion.json"
---

<objective>
Cerrar el loop de Phase 2 ejecutando el driver vivo al menos una vez (genera el findings file inicial y el schema snapshot DRIFT-01), agregando los **invariantes verificados en vivo** como tests mockeados en `test_client.py` + `test_async_client.py` (sección `# ------ Verified live (Phase 2) ------`), agregando la sección vacía `# ------ Regressions ------` que recibirá tests opportunistic si aparecen bugs, y commiteando los dos artefactos (findings markdown + schema JSON). Este plan tiene un checkpoint humano para revisar los artefactos generados antes del commit (D-04 lifecycle + nota MVP del context: "los hallazgos OPEN y la promoción humana a CONFIRMED/FIXED no son parte del plan ejecutable").

Purpose: AMB-01..AMB-05 + DRIFT-01 quedan cumplidos. AMB-06 (anti-bot) ya queda implementado en el driver (Plan 02-02) y se ejercita manualmente con `VERIFY_ANTIBOT=1` — su verificación no produce artefactos committeables salvo un finding EXPECTED en el findings file si se corre. La regla MVP del context: "la fase pasa aun sin findings"; cualquier finding OPEN que aparezca y sea promovido a CONFIRMED/FIXED por el humano queda fuera del plan ejecutable (opportunistic per nota del contexto).

Output:
1. `test_client.py` + `test_async_client.py` con sección `Verified live (Phase 2)` (D-08 invariantes mockeados) y sección `Regressions` (D-09 vacía)
2. `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` committeado (DRIFT-01)
3. `.planning/verification/ambito-financiero-client-findings.md` committeado (esqueleto + cualquier finding OPEN/EXPECTED del primer live run)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-mbito-verification/02-CONTEXT.md
@.planning/phases/02-mbito-verification/02-RESEARCH.md
@.planning/phases/02-mbito-verification/02-PATTERNS.md
@.planning/phases/02-mbito-verification/02-VALIDATION.md
@.planning/phases/02-mbito-verification/02-01-PLAN.md
@.planning/phases/02-mbito-verification/02-02-PLAN.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_ambito_financiero.py
@verification/findings.py
@packages/ambito-financiero-client/tests/test_client.py
@packages/ambito-financiero-client/tests/test_async_client.py
@packages/ambito-financiero-client/tests/conftest.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Append Verified-live invariants + Regressions section to test_client.py + test_async_client.py (D-08, D-09)</name>
  <files>packages/ambito-financiero-client/tests/test_client.py, packages/ambito-financiero-client/tests/test_async_client.py</files>
  <read_first>
    - packages/ambito-financiero-client/tests/test_client.py (estado actual: 5 tests sync con `httpx_mock.add_response(url=...)` patrón completo en líneas 18-52)
    - packages/ambito-financiero-client/tests/test_async_client.py (estado actual: 3 tests async; convención `async def test_async_...` sin decorador líneas 17-37)
    - packages/ambito-financiero-client/tests/conftest.py (autouse fixtures `_configure_sync` / `_configure_async` líneas 15-27 — los tests nuevos heredan sin tocar)
    - .planning/phases/02-mbito-verification/02-PATTERNS.md (sección 4 "test_client.py — APPEND" y sección 5 "test_async_client.py — APPEND (espejo async)" con código de los tests verbatim de invariante)
    - .planning/phases/02-mbito-verification/02-RESEARCH.md (sección "Pattern 5: Tests mockeados de invariante (D-08) + regresión (D-06)"; Pitfall 4 "Tests mockeados que pasan pero el cliente fallaría en vivo")
    - .planning/phases/02-mbito-verification/02-CONTEXT.md (D-08 invariantes mínimos a codificar verbatim; D-09 secciones con dividers; D-06 dual surface; D-07 convención `Regression: ... (finding F-NN)`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py (parse_ar_decimal firma)
    - packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py (parse_ar_decimal NO está en __all__; importar desde `_parsing`)
    - .planning/codebase/TESTING.md (convención pytest-httpx con URL completa, async tests sin decorador por `asyncio_mode=auto`, convención `Regression: ... (issue #NNN)` adaptada por D-07)
  </read_first>
  <behavior>
    - Test 1 (AMB-03 sync): `test_get_dollar_banco_nacion_emite_url_dia_gt_12` mockea URL `/dolarnacion/historico-general/2026-04-21/2026-04-21` con día > 12 y verifica que `ambito.get_dollar_banco_nacion(dt.date(2026, 4, 21)) == 1415.0`. Mockear con header `["Fecha","Compra","Venta"]` y data `["21/04/2026","1.365,00","1.415,00"]`.
    - Test 2 (AMB-02 sync): `test_parse_ar_decimal_formato_real` — unit test puro de `parse_ar_decimal("1.415,00") == 1415.0` (sin httpx_mock — no hace request).
    - Test 3 (AMB-01/AMB-03 sync, shape): `test_get_dollar_banco_nacion_shape_list_of_list_str` mockea con shape `list[list[str]]` y header row exacto; el test pasa si `ambito.get_dollar_banco_nacion(dt.date(2026, 4, 21)) == 1415.0` (proxy de shape correcto).
    - Test 4 (AMB-04 sync) — YA EXISTENTE como `test_get_dollar_banco_nacion_sin_datos_levanta` (test_client.py:46-52). Confirmar que sigue verde; opcionalmente agregar un comment-divider de doc por encima referenciando D-08, pero el test mismo no se duplica.
    - Test 5 (AMB-03 async — espejo): `test_async_get_dollar_banco_nacion_emite_url_dia_gt_12` espejo de Test 1, usa `await aio.get_dollar_banco_nacion(...)`.
    - Test 6 (AMB-02 async — espejo): `test_async_parse_ar_decimal_formato_real` — duplicación literal de Test 2 (la función no es async pero D-09 manda "una sola convención"; recomendación 02-PATTERNS.md sección 5: duplicar para mantener simetría de la sección).
    - Test 7 (AMB-01/AMB-03 async, shape): `test_async_get_dollar_banco_nacion_shape_list_of_list_str` espejo de Test 3.
    - Test 8 (AMB-04 async) — YA EXISTENTE como `test_async_get_dollar_banco_nacion_sin_datos` (test_async_client.py:31-37). Igual que Test 4.
  </behavior>
  <action>
    Append a `packages/ambito-financiero-client/tests/test_client.py` la sección `# ------ Verified live (Phase 2) ------` (D-09 divisor) seguida de los tests 1, 2, 3 listados arriba. Después, la sección `# ------ Regressions ------` (D-09 divisor) que queda VACÍA por ahora (un comment explicativo `# (vacío hasta que un finding promovido a CONFIRMED se cierre como FIXED; convención: docstring `Regression: ... (finding F-NN)` per D-07)` está bien).

    Mismo append a `packages/ambito-financiero-client/tests/test_async_client.py`: sección `# ------ Verified live (Phase 2) ------` con los tests 5, 6, 7, luego `# ------ Regressions ------` con el mismo comment placeholder.

    **Convenciones obligatorias** (CONVENTIONS.md / TESTING.md):
    - Mantener `from __future__ import annotations` en línea 3 de cada archivo (ya presente)
    - Agregar import `from ambito_financiero_client._parsing import parse_ar_decimal` (al bloque de imports existente o al tope de los tests Verified live — recomendado al bloque de imports porque mypy strict lo prefiere). NO modificar el `__all__` del paquete (`_parsing` sigue siendo internal — comentario explícito en el test si querés clarificarlo).
    - pytest-httpx con URL completa (`url="https://mercados.ambito.com/...`) — sin query string para Ámbito.
    - Mock payload **derivado del payload real** observado en vivo (Pitfall 4 de 02-RESEARCH.md). Para AMB-02: emitir `"1.415,00"` (AR-decimal), no `"1.415"` ni `"1.41500"`. Para AMB-03: header exacto `["Fecha","Compra","Venta"]`. **Fecha fija en el mock** (`dt.date(2026, 4, 21)`) — los tests son determinísticos, no derivan de `today` (Pitfall del 02-PATTERNS.md sección 4 final).
    - Async tests sin decorador (asyncio_mode=auto) — el `async def test_async_...` ya está siendo manejado por `pytest-asyncio`.
    - Docstring corto en cada test referenciando el ID del requirement: `"""Phase 2: locking de URL emitida con día > 12 (AMB-03)."""`
    - ruff line-length=100, double quotes, 4 espacios
    - mypy --strict sigue verde

    **Lo que NO va en este task:**
    - No agregar tests de regression todavía (la sección queda vacía por nota MVP del contexto: opportunistic — solo si un finding aparece y se promueve a FIXED durante el live run)
    - No tocar tests existentes en posición/orden — todos los Verified live van AL FINAL del archivo, después del último test existente

    Sintaxis exacta del divider (verbatim D-09):
    ```
    # ------ Verified live (Phase 2) ------
    ```
    Y al final:
    ```
    # ------ Regressions ------
    ```

    Implementar siguiendo TDD: escribir primero los tests Verified live (deben pasar inmediatamente porque mockean el comportamiento esperado del cliente actual sin bugs detectados). Si alguno FALLA, eso indica un bug pre-existente del cliente — lo cual se documentaría como finding en el live run, NO se silencia. Si pasan: locking de invariantes completo.

    Commit sugerido (atomic):
    `test(02-03): add Verified live (Phase 2) invariants + Regressions placeholder (D-08, D-09)`
  </action>
  <verify>
    <automated>uv run pytest packages/ambito-financiero-client/tests/test_client.py -v</automated>
    <automated>uv run pytest packages/ambito-financiero-client/tests/test_async_client.py -v</automated>
    <automated>uv run pytest packages/ambito-financiero-client -q</automated>
    <automated>uv run mypy packages/ambito-financiero-client</automated>
    <automated>uv run ruff check packages/ambito-financiero-client/tests/test_client.py packages/ambito-financiero-client/tests/test_async_client.py</automated>
    <automated>uv run ruff format --check packages/ambito-financiero-client/tests/test_client.py packages/ambito-financiero-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 2) ------" packages/ambito-financiero-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Verified live (Phase 2) ------" packages/ambito-financiero-client/tests/test_async_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/ambito-financiero-client/tests/test_client.py</automated>
    <automated>grep -F "# ------ Regressions ------" packages/ambito-financiero-client/tests/test_async_client.py</automated>
  </verify>
  <acceptance_criteria>
    - Source: `packages/ambito-financiero-client/tests/test_client.py contains # ------ Verified live (Phase 2) ------`
    - Source: `packages/ambito-financiero-client/tests/test_client.py contains # ------ Regressions ------`
    - Source: `packages/ambito-financiero-client/tests/test_async_client.py contains # ------ Verified live (Phase 2) ------`
    - Source: `packages/ambito-financiero-client/tests/test_async_client.py contains # ------ Regressions ------`
    - Source: `packages/ambito-financiero-client/tests/test_client.py contains def test_get_dollar_banco_nacion_emite_url_dia_gt_12(`
    - Source: `packages/ambito-financiero-client/tests/test_client.py contains def test_parse_ar_decimal_formato_real(`
    - Source: `packages/ambito-financiero-client/tests/test_client.py contains def test_get_dollar_banco_nacion_shape_list_of_list_str(`
    - Source: `packages/ambito-financiero-client/tests/test_async_client.py contains async def test_async_get_dollar_banco_nacion_emite_url_dia_gt_12(`
    - Source: `packages/ambito-financiero-client/tests/test_async_client.py contains def test_async_parse_ar_decimal_formato_real(` (puede o NO ser async; D-09 manda duplicación literal — recomendación: NO async porque parse_ar_decimal no es async)
    - Source: `packages/ambito-financiero-client/tests/test_async_client.py contains async def test_async_get_dollar_banco_nacion_shape_list_of_list_str(`
    - Source: ambos archivos contienen `from ambito_financiero_client._parsing import parse_ar_decimal`
    - Source: ambos archivos contienen `"1.415,00"` literal en al menos un test (AMB-02 mock payload con AR-decimal real)
    - Source: ambos archivos contienen `"2026-04-21"` literal en al menos un test (AMB-03 fecha con día > 12)
    - Source: ambos archivos contienen `["Fecha", "Compra", "Venta"]` (header exacto del payload real)
    - Behavior: `uv run pytest packages/ambito-financiero-client -q` exits 0 con >= 12 PASSED (5 existentes sync + 3 nuevos sync + 3 existentes async + 3 nuevos async = 14 mínimo; mayor si se agregan más)
    - Static: `uv run mypy packages/ambito-financiero-client` exits 0
    - Static: `uv run ruff check packages/ambito-financiero-client/tests/test_client.py packages/ambito-financiero-client/tests/test_async_client.py` exits 0
    - Static: `uv run ruff format --check packages/ambito-financiero-client/tests/test_client.py packages/ambito-financiero-client/tests/test_async_client.py` exits 0
    - Non-regression: `uv run pytest -q` (suite completa) exits 0
    - Source: tests existentes (líneas 1-52 de test_client.py y 1-37 de test_async_client.py) NO modificados (solo APPEND al final)
  </acceptance_criteria>
  <done>Sección "Verified live (Phase 2)" agregada en ambos test files con los invariantes D-08 mockeados; sección "Regressions" agregada vacía. Suite completa verde, mypy strict + ruff limpios. Sin regresión de tests existentes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.2: Live driver run (one-shot, baseline schema + findings)</name>
  <files>main_ambito_financiero.py, .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json, .planning/verification/ambito-financiero-client-findings.md</files>
  <read_first>
    - main_ambito_financiero.py (reescrito en Plan 02-02; entry point del live run)
    - .planning/phases/02-mbito-verification/02-CONTEXT.md (D-04 — exit 0 siempre salvo crash inesperado; D-12/D-13/D-14 — anti-bot opt-in one-shot; D-21 — envelope JSON del schema)
    - .planning/verification/FINDINGS-TEMPLATE.md (formato esperado del archivo de findings generado por write_findings + append_finding)
  </read_first>
  <what-built>
    El driver `main_ambito_financiero.py` (reescrito en Plan 02-02) y los tests mockeados (Task 3.1) están listos. Ahora ejecutamos el driver una vez contra `mercados.ambito.com` para:

    1. **Generar el primer schema snapshot DRIFT-01:** `probe_schema_snapshot` escribirá `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` con el envelope D-21 (endpoint + client_function + captured_at + base_url + sample_date + schema).
    2. **Generar el findings file inicial:** `write_findings("ambito-financiero-client")` crea el esqueleto y cualquier finding OPEN/EXPECTED detectado por los 7 probes se append-ea vía `append_finding` (idempotente).
    3. **Verificar AMB-01..AMB-05 en vivo:** 7 líneas `PROBE <name>: <status>` + `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` en stdout; exit 0 (D-04: salvo crash inesperado).

    El comando exacto es:
    ```
    uv run --package ambito-financiero-client python main_ambito_financiero.py
    ```

    Para AMB-06 (anti-bot probe), el run adicional es:
    ```
    VERIFY_ANTIBOT=1 uv run --package ambito-financiero-client python main_ambito_financiero.py
    ```

    NOTA: ya el primer run (sin VERIFY_ANTIBOT) emite `PROBE antibot: SKIPPED (opt-in via VERIFY_ANTIBOT=1)` — verifica que el gate está activo. La corrida con `VERIFY_ANTIBOT=1` es opcional y opportunistic; agrega un finding EXPECTED (si el server retorna 403 como se espera) o OPEN (si retorna otra cosa, D-18). Para el commit de la baseline está OK correr **solo el run default** (sin VERIFY_ANTIBOT); el run con anti-bot puede correrse después como acto humano deliberado.

    **Importante (D-04):** los hallazgos OPEN son output esperado, NO errores. El driver exit 0 igual.
  </what-built>
  <action>
    Acción operativa (a ejecutarse en checkpoint humano antes del commit del Task 3.3):

    1. Asistente (o usuario) corre el driver default contra `mercados.ambito.com`:
       `uv run --package ambito-financiero-client python main_ambito_financiero.py`
       Verifica que stdout emite 7 líneas `PROBE <name>: <status>` (verbatim D-02) en el orden D-13 (antibot último) seguidas de una línea `SUMMARY: ...` con conteos por estado. Exit code = 0 (D-04).

    2. Se generan dos artefactos en disco (output del driver):
       - `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` con el envelope D-21 — primer schema snapshot DRIFT-01
       - `.planning/verification/ambito-financiero-client-findings.md` con el esqueleto generado por `write_findings` + cualquier finding OPEN/EXPECTED del run (vía `append_finding`)

    3. Usuario inspecciona los dos artefactos con `cat` antes del checkpoint approval:
       - JSON tiene keys `endpoint`, `client_function`, `captured_at`, `base_url`, `sample_date`, `schema`; campo `schema` es `[["str"]]` (payload Ámbito = list[list[str]])
       - Markdown tiene header `# Findings: ambito-financiero-client-client`, sección `## Run Context (ART)` con Timestamp ISO-8601 reciente y `base_url=https://mercados.ambito.com`, sección `## Index` (vacía o con rows según probes)

    4. (Opcional) Usuario corre AMB-06 anti-bot probe deliberadamente:
       `VERIFY_ANTIBOT=1 uv run --package ambito-financiero-client python main_ambito_financiero.py`
       UNA SOLA VEZ — D-13/D-14 prohíben loops por riesgo de IP-ban.

    5. Usuario clasifica findings OPEN encontrados (si los hay) según D-08 lifecycle:
       - CONFIRMED: bug real reproducido → editar archivo manualmente
       - EXPECTED: comportamiento del server esperado terminal
       - NO-FIX: dato puntual o ciclo no aplica
       - OPEN se mantiene si requiere más investigación

    6. Usuario emite resume signal:
       - `"approved"` para que Task 3.3 commitee los dos artefactos al repo
       - `"holds-finding-X"` + descripción para insertar Plan 02-04 opportunistic con fix dual sync/async + 2 regression tests ANTES del commit
       - `"reject"` + razón si el driver crashea o output no respeta D-02/D-04 (vuelve al Plan 02-02)

    No se escribe código en este task. No se edita ningún archivo (los artefactos son OUTPUT del driver, no INPUT humano). El task solo bloquea la ejecución hasta que el humano apruebe el commit.
  </action>
  <how-to-verify>
    1. **Corré el driver default** (sin VERIFY_ANTIBOT):
       ```
       uv run --package ambito-financiero-client python main_ambito_financiero.py
       ```

    2. **Verificá la salida stdout:** esperás ver 7 líneas `PROBE <name>: <status>` seguidas de `SUMMARY: ...`. Por ejemplo:
       ```
       PROBE happy_sync: PASS precio=1415.0
       PROBE happy_async: PASS precio=1415.0
       PROBE parity_sync_async: PASS
       PROBE parse_decimal: PASS venta=1415.0
       PROBE no_data: PASS NoDataError para 2026-07-30
       PROBE schema_snapshot: PASS escrito get-dollar-banco-nacion.json
       PROBE antibot: SKIPPED (opt-in via VERIFY_ANTIBOT=1)
       SUMMARY: PASS=6 SKIPPED=1 FAIL=0 FINDING=0
       ```
       (los nombres exactos de status y detalles pueden variar según Discretion del Claude que implementó el driver).

    3. **Verificá los artefactos en disco:**
       ```
       cat .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json
       cat .planning/verification/ambito-financiero-client-findings.md
       ```
       - El JSON debe contener un dict con keys `endpoint`, `client_function`, `captured_at`, `base_url`, `sample_date`, `schema` (D-21). El campo `schema` debe ser `[["str"]]` o equivalente (todos strings — list[list[str]] del payload Ámbito).
       - El markdown debe contener el header `# Findings: ambito-financiero-client-client`, el ART block actualizado con Timestamp ISO-8601 reciente + Resolved base URL = `https://mercados.ambito.com`, la tabla `## Index` (puede estar vacía si no aparecieron findings, o con rows OPEN/EXPECTED si los probes los detectaron).

    4. **Inspeccioná findings (si los hay) para clasificación humana:**
       - Cualquier finding OPEN: leer el campo `**Expected:**` vs `**Actual:**` y decidir si es un bug real del cliente (→ promover a CONFIRMED editando manualmente el archivo), un comportamiento esperado del server (→ promover a EXPECTED), o un dato puntual / no reproducible (→ dejar en OPEN o NO-FIX según corresponda — D-08 lifecycle).
       - NO se requiere fix en este plan (los fixes son opportunistic y van fuera del plan ejecutable según la nota MVP del contexto). Si un finding aparece y querés promoverlo a FIXED en este ciclo, eso requiere un Plan adicional (ver "Resume signal" abajo).

    5. **(OPCIONAL) Corré el anti-bot probe explícitamente** una sola vez:
       ```
       VERIFY_ANTIBOT=1 uv run --package ambito-financiero-client python main_ambito_financiero.py
       ```
       - Esperás ver `PROBE antibot: FINDING F-NN (EXPECTED)` y un nuevo finding en el archivo con clase `ANTI-BOT` status `EXPECTED` (D-14 — comportamiento esperado verificado terminal).
       - Si en cambio aparece `OPEN` con `actual=<otra cosa>` (D-18): registrar como hallazgo y NO re-correr el probe (D-13/D-14 — riesgo de IP-ban con loops).

    6. **Confirmá al asistente:**
       - **"approved"** si los artefactos lucen bien y querés que el siguiente task commitee findings + schema al repo
       - **"holds-finding-X"** + descripción si querés que se cree un Plan adicional 02-04 para promover un finding específico a CONFIRMED → fix dual sync+async → 2 regression tests (D-06, D-07) **antes** de commitear el archivo del findings. Es opportunistic per nota MVP del contexto.
       - **"reject"** + razón si el driver crashea o emite output que no respeta D-02 / D-04, para que se vuelva al Plan 02-02 a corregir.
  </how-to-verify>
  <verify>
    <human-check>Driver ejecutado al menos una vez con exit 0; output stdout respeta D-02 (7 líneas PROBE + SUMMARY); artefactos JSON + Markdown inspeccionados y aprobados por el humano</human-check>
    <automated>test -f .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json</automated>
    <automated>test -f .planning/verification/ambito-financiero-client-findings.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` (generado por el driver run)
    - File exists: `.planning/verification/ambito-financiero-client-findings.md` (generado por el driver run)
    - File content: el JSON contiene las 6 keys del envelope D-21 (endpoint, client_function, captured_at, base_url, sample_date, schema)
    - File content: el markdown contiene `# Findings: ambito-financiero-client-client` (header generado por new_findings)
    - File content: el markdown contiene una sección `## Run Context (ART)` con líneas `- Timestamp:` y `- Resolved base URL / env:`
    - Behavior: driver exit code = 0 (D-04)
    - Behavior: stdout contiene 7 líneas que matchean el regex `^PROBE [a-z_]+: (PASS|FAIL|SKIPPED|FINDING)` (D-02 verbatim)
    - Behavior: stdout contiene una línea que empieza con `SUMMARY: ` (D-02 summary final)
    - Human approval recibida (resume signal = "approved" o equivalente)
  </acceptance_criteria>
  <resume-signal>
    Type **"approved"** to proceed to Task 3.3 (commit baseline artifacts). Or describe issues / request opportunistic fix-plan.
  </resume-signal>
  <done>Driver ejecutado en vivo al menos una vez; baseline JSON + findings markdown generados en disco; humano inspeccionó y aprobó los artefactos para commit.</done>
</task>

<task type="auto">
  <name>Task 3.3: Commit baseline artifacts (DRIFT-01 schema + Phase 2 findings file)</name>
  <files>.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json, .planning/verification/ambito-financiero-client-findings.md</files>
  <read_first>
    - .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json (recién generado por Task 3.2)
    - .planning/verification/ambito-financiero-client-findings.md (recién generado por Task 3.2)
    - .gitignore (verificar que `.planning/verification/captures/` está ignored pero NO `.planning/verification/schemas/` ni `.planning/verification/ambito-financiero-client-findings.md`)
  </read_first>
  <action>
    Confirmar que los artefactos existen en disco y NO están gitignored:

    1. `test -f .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` exits 0
    2. `test -f .planning/verification/ambito-financiero-client-findings.md` exits 0
    3. `git check-ignore .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` exits 1 (NO ignored — committeable)
    4. `git check-ignore .planning/verification/ambito-financiero-client-findings.md` exits 1 (NO ignored — committeable)

    Si alguno de (3) o (4) reporta el archivo como ignored: STOP y arreglar `.gitignore` antes de continuar (debería ser un error en .gitignore — esperamos que `.planning/verification/captures/` esté ignored pero NO los demás).

    Stageá los dos archivos:
    ```bash
    git add .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json
    git add .planning/verification/ambito-financiero-client-findings.md
    ```

    Commit atómico (DRIFT-01 + Phase 2 lifecycle):
    ```bash
    git commit -m "feat(02-03): commit DRIFT-01 baseline schema + Phase 2 findings

    First live run of main_ambito_financiero.py against mercados.ambito.com:
    - Schema snapshot for get_dollar_banco_nacion (endpoint + envelope D-21).
    - Findings file skeleton populated by the 7 probes (D-01..D-26).

    Verifies AMB-01..AMB-05 in vivo; AMB-06 opt-in via VERIFY_ANTIBOT=1.
    Drift detection re-runs use schema snapshot (D-25) without overwriting.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
    ```

    Verificar que el commit aparece en git log:
    ```bash
    git log -1 --oneline
    ```

    NO ejecutar el driver de nuevo en este task. NO commitear nada más en este task (test files de Task 3.1 ya tienen su propio commit; no mezclar concerns).

    **Importante:** si Task 3.2 generó un findings file con findings en estado `OPEN` que el humano marcó como "holds-finding-X" en el checkpoint, este Task NO debe ejecutarse — el orquestador insertará un Plan opportunistic 02-04 antes de cerrar Phase 2. En el flujo normal "approved", este task corre directo.
  </action>
  <verify>
    <automated>test -f .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json</automated>
    <automated>test -f .planning/verification/ambito-financiero-client-findings.md</automated>
    <automated>! git check-ignore .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json 2>&1 | grep -q schemas</automated>
    <automated>! git check-ignore .planning/verification/ambito-financiero-client-findings.md 2>&1 | grep -q findings</automated>
    <automated>git log -1 --pretty=%s | grep -F "02-03"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "get-dollar-banco-nacion.json"</automated>
    <automated>git diff HEAD~1 HEAD --name-only | grep -F "ambito-financiero-client-findings.md"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json`
    - File exists: `.planning/verification/ambito-financiero-client-findings.md`
    - `.gitignore`: la ruta `.planning/verification/captures/` permanece ignored (T-2-CC del threat model preservado)
    - `.gitignore`: NO añade `.planning/verification/schemas/` ni `.planning/verification/<pkg>-findings.md` (los nuevos artefactos son committeables)
    - Behavior: `git check-ignore .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` exits 1 (NO ignored)
    - Behavior: `git check-ignore .planning/verification/ambito-financiero-client-findings.md` exits 1 (NO ignored)
    - Behavior: `git log -1` muestra un commit con prefijo `feat(02-03):` que incluye ambos archivos
    - Behavior: `git diff HEAD~1 HEAD --name-only` lista los dos archivos como agregados
    - File content: `get-dollar-banco-nacion.json` contiene un dict JSON con keys `endpoint`, `client_function`, `captured_at`, `base_url`, `sample_date`, `schema` (D-21)
    - File content: `ambito-financiero-client-findings.md` contiene el header `# Findings: ambito-financiero-client-client`
    - File content: `ambito-financiero-client-findings.md` contiene la sección `## Run Context (ART)` con líneas `- Timestamp:` y `- Resolved base URL / env:`
    - File content: `ambito-financiero-client-findings.md` contiene la sección `## Index` con la tabla `| ID | Class | Surface | Status |`
    - Source: ningún test file fue modificado en este task (sólo commit de artefactos)
  </acceptance_criteria>
  <done>Schema snapshot DRIFT-01 + findings file Phase 2 committeados al repo en un commit atómico `feat(02-03)`. .gitignore preserva captures/ ignored; schemas/ y findings.md committeable. Próxima corrida del driver compara contra esta baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live driver → mercados.ambito.com | Llamadas HTTP read-only durante el checkpoint Task 3.2 |
| driver → .planning/verification/ (committeable) | Artefactos que entran a git en Task 3.3 — sensibles a accidental PII leak |
| mocked tests → ambito_financiero_client | Locking de comportamiento esperado del cliente, derivado del payload real observado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-13 | Information Disclosure | findings file accidentalmente committeado con un valor crudo de la respuesta | mitigate | El findings file es producido por `append_finding` (Plan 02-01) que solo recibe `expected`/`actual`/`diff` que el driver le pasa explícitamente. El driver (Plan 02-02) usa `schema_of(rows)` en lugar de `rows` para schema-related findings (PII-free por construcción). Para finding `parse_decimal` D-23: el `actual` incluye `venta_raw` (string AR-decimal de FX pública — sin PII). Para finding `no_data`: incluye un float (precio FX — sin PII). Para finding `anti-bot`: incluye `repr(exc)` que puede tener bytes — verificar manualmente en checkpoint 3.2 que el contenido no tiene credenciales (Ámbito no tiene; check redundante). |
| T-2-14 | Tampering | Schema snapshot committeado con valores reales en lugar de tipos | mitigate | `schema_of` produce solo type-names (`"str"`, `"int"`, etc.) por construcción — verificado en Phase 1 con `test_harness_schema.py`. Acceptance criterion del Task 3.3 verifica que el contenido del JSON envelope es `[["str"]]` o equivalente structural. |
| T-2-15 | Repudiation | Schema snapshot sin metadata trazable | mitigate | Envelope D-21 incluye `captured_at` (ISO-8601 UTC), `base_url`, `sample_date` — la corrida que produjo la baseline queda registrada en el archivo y en git history. |
| T-2-16 | DoS-against-self (IP-ban) | Re-correr el anti-bot probe en loop durante el checkpoint | mitigate | Checkpoint 3.2 instruye explícitamente "una sola vez" y D-14 está en el código del driver (one-shot). La instrucción dice "NO re-correr el probe" si aparece OPEN. |
| T-2-17 | Tampering | Tests mockeados que pasan pero el cliente fallaría en vivo (mocks ficticios) | mitigate | D-08: invariantes derivados de payloads observados en vivo (Pitfall 4 de 02-RESEARCH.md). `"1.415,00"` y header exacto `["Fecha","Compra","Venta"]` son verbatim del docstring del cliente (`client.py:76`) que documenta el comportamiento real del API. |
| T-2-18 | Tampering | Drift silencioso si el schema snapshot se commitea con data ad-hoc o sin pasar por el driver | mitigate | El Task 3.3 commits SOLO los archivos generados por Task 3.2 (live driver run). No hay paso intermedio donde el humano edite el JSON antes de commitear (Open Question 3 / D-25: edición manual + re-run para drift aceptado, pero ese flujo es FUTURO, no la baseline inicial). |
| T-2-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes externos. |
</threat_model>

<verification>
Whole-plan integration (Task 3.1 + 3.2 + 3.3):

```bash
# Task 3.1 — Tests verdes después del append
uv run pytest packages/ambito-financiero-client -q
uv run mypy packages/ambito-financiero-client
uv run ruff check packages/ambito-financiero-client/tests
uv run ruff format --check packages/ambito-financiero-client/tests

# Task 3.2 — Driver corrido manualmente con checkpoint humano
# (no se automatiza; human verifies artifacts)

# Task 3.3 — Artefactos committeados
test -f .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json
test -f .planning/verification/ambito-financiero-client-findings.md
! git check-ignore .planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json 2>&1 | grep -q schemas
! git check-ignore .planning/verification/ambito-financiero-client-findings.md 2>&1 | grep -q findings

# Wave 3 full gate (todos los success criteria de Phase 2)
uv run --all-packages pytest && uv run mypy && uv run ruff check && uv run ruff format --check
```
</verification>

<success_criteria>
- `test_client.py` y `test_async_client.py` con sección `# ------ Verified live (Phase 2) ------` poblada con D-08 invariantes mockeados
- `test_client.py` y `test_async_client.py` con sección `# ------ Regressions ------` agregada (vacía por ahora; opportunistic per nota MVP)
- Driver `main_ambito_financiero.py` ejecutado al menos una vez contra `mercados.ambito.com` con exit 0 (D-04)
- `.planning/verification/schemas/ambito-financiero-client/get-dollar-banco-nacion.json` committeado al repo (DRIFT-01 baseline)
- `.planning/verification/ambito-financiero-client-findings.md` committeado al repo con el esqueleto + cualquier finding OPEN/EXPECTED del primer run
- Checkpoint humano completado: el humano revisó los artefactos y aprobó el commit (Task 3.2 resume signal = "approved")
- Suite completa verde después del commit (`uv run pytest -q && uv run mypy && uv run ruff check`)
- `.gitignore` preservado: `.planning/verification/captures/` sigue ignored; los nuevos artefactos son committeable
- AMB-01..AMB-05 verificados en vivo (driver run) + lockeados mockeados (tests Verified live)
- AMB-06 implementado y opt-in (Plan 02-02); su ejecución en vivo es opcional y opportunistic en este plan
- DRIFT-01 cumplido (schema snapshot committeado para `get_dollar_banco_nacion`)
- Re-corridas futuras del driver compararán contra esta baseline (drift detection D-25 lista para ejercitarse)
</success_criteria>

<output>
Crear `.planning/phases/02-mbito-verification/02-03-SUMMARY.md` cuando done, registrando:
- Output verbatim del primer live run del driver (las 7 líneas + summary) — sirve como ground-truth para futuras re-corridas
- Contenido del schema snapshot (`get-dollar-banco-nacion.json`) — registro de la baseline
- Findings detectados (si los hay) y su clasificación inicial (OPEN/EXPECTED) — registro del estado al cierre de Phase 2
- Decisión sobre AMB-06 anti-bot run: corrido o pospuesto (opt-in)
- Resultado del checkpoint humano (approved / opportunistic fix-plan inserted)
- Métrica de duración del plan
</output>
