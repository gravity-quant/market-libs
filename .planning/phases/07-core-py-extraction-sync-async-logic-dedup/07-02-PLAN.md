---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/client.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
  - packages/ambito-financiero-client/tests/test_core.py
autonomous: true
requirements: [REFAC-03]
requirements_addressed: [REFAC-03]
tags: [phase-07, ambito, canary, refac-03]
user_setup: []

must_haves:
  truths:
    - "`ambito_financiero_client._core` define `RequestSpec` frozen dataclass + builders puros `build_<endpoint>_request(state, ...) -> RequestSpec` + parsers puros `parse_<endpoint>_response(resp) -> typed result` por cada endpoint actual"
    - "`ambito_financiero_client.client` colapsa a transport shell con `_request(spec) -> httpx.Response` (D-03), endpoint methods de 2-4 líneas (D-05)"
    - "`ambito_financiero_client.aio` colapsa a transport shell async con shape espejo"
    - "`_raise_for_response` y otros helpers stateless se MUEVEN a `_core.py`; `client.py` y `aio.py` declaran aliases module-level `_raise_for_response = _core.raise_for_response` (D-04) — la identidad `aio._raise_for_response is client._raise_for_response` se preserva"
    - "B8 test `test_aio_imports_raise_for_response_from_client` sigue verde (`a_impl is c_impl`)"
    - "LOC drop ≥30% agregado client+aio vs Phase 6 baseline (557 LOC); planner reporta drop real en SUMMARY.md"
    - "Public surface snapshot (Phase 6) sin diff (D-16); `_core` NO aparece en `__all__` ni en `verification/snapshots/ambito_financiero_client-surface.txt`"
    - "`uv run lint-imports` exit 0; los 4 contracts (incluyendo `ambito_financiero_client._core` ⊬ {client, aio}) pasan en verde"
    - "`verification/test_sync_async_isolation.py` para ámbito (sync + async) sigue verde post-refactor"
    - "Test count baseline (393+) verde; nuevos `tests/test_core.py` agregan ≥4 tests unitarios"
  artifacts:
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/_core.py"
      provides: "RequestSpec + build_X_request + parse_X_response + raise_for_response (pure, no I/O, no transport import)"
      contains: "RequestSpec"
      min_lines: 50
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/client.py"
      provides: "Sync transport shell (Client class skeleton preserved from Phase 6; endpoint bodies collapsed)"
      contains: "_raise_for_response = _core.raise_for_response"
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/aio.py"
      provides: "Async transport shell (AsyncClient class skeleton preserved; endpoint bodies collapsed)"
      contains: "_raise_for_response = _core.raise_for_response"
    - path: "packages/ambito-financiero-client/tests/test_core.py"
      provides: "Unit tests for ámbito _core builders/parsers"
      min_lines: 40
  key_links:
    - from: "ambito_financiero_client.client._request"
      to: "ambito_financiero_client._core.RequestSpec"
      via: "spec parameter (D-03)"
      pattern: "spec:.*RequestSpec"
    - from: "ambito_financiero_client.client.get_<endpoint>"
      to: "ambito_financiero_client._core.build_<endpoint>_request + parse_<endpoint>_response"
      via: "3-liner shell call"
      pattern: "_core\\.build_.*_request|_core\\.parse_.*_response"
    - from: "ambito_financiero_client.aio._raise_for_response"
      to: "ambito_financiero_client._core.raise_for_response"
      via: "module-level alias (D-04)"
      pattern: "_raise_for_response = _core.raise_for_response"
---

<objective>
Canary del refactor `_core.py` Phase 7: extraer en `ambito_financiero_client/_core.py` los builders puros (`build_<endpoint>_request(state, ...) -> RequestSpec`) y parsers puros (`parse_<endpoint>_response(resp) -> typed result`) para cada endpoint actual; colapsar `client.py` y `aio.py` a transport shells; preservar B8 alias `_raise_for_response = _core.raise_for_response` (D-04); agregar `tests/test_core.py` con unit tests aislados de los builders/parsers. ámbito es el paquete más simple (sin auth, sin token refresh), por lo que sirve de canary del patrón antes de iol/higyrus/matriz (D-13, D-15).

Purpose: Validar mecánicamente el patrón `_core.py + transport shell` en el blast radius mínimo del repo (557 LOC client+aio agregado; sin OAuth, sin envelope, sin URL-encoding quirks). Cumple REFAC-03 success criterion #1 ("por cada paquete, `_core.py` contiene...") en su forma más simple.

Output: 1 nuevo módulo `_core.py` ámbito; 2 archivos colapsados (client/aio); 1 nuevo `tests/test_core.py`. Single commit atómico per D-05/D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md
@CLAUDE.md
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py
@packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py
@packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py
@packages/ambito-financiero-client/tests/conftest.py
@packages/ambito-financiero-client/tests/test_client_class.py
@verification/snapshots/ambito_financiero_client-surface.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build `ambito_financiero_client/_core.py` — RequestSpec + builders + parsers + raise_for_response (PURE; NO transport imports)</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/_core.py, packages/ambito-financiero-client/tests/test_core.py</files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (líneas 1-50 module shape + 64-70 `_raise_for_response` para MOVE + 78-186 Client class + endpoint bodies de `get_dollar_*` etc.)
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (líneas 1-60 imports + 71-190 AsyncClient class + endpoint bodies)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py (líneas 1-64 — analog módulo shape per PATTERNS.md §1 — copy header docstring shape, `from __future__ import annotations`, `__all__`, section dividers `# ---...---`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_parsing.py (parser helpers existentes para reusar — `parse_ar_decimal`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/exceptions.py (jerarquía `<Pkg>ClientError` para typed raises)
    - packages/ambito-financiero-client/tests/test_client_class.py (assert idiom — for shape de `tests/test_core.py`)
    - packages/ambito-financiero-client/tests/conftest.py (autouse `configure(base_url=...)` patrón — los tests de `_core` bypasen state usando `_ClientState` directamente)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 1 RequestSpec; Pattern 2 parser body-consume-then-raise; Pattern 5 auth-flow — ámbito no tiene auth, OMITIR; § Code Examples cualquier ejemplo de `_core.py` reduced a ámbito shape)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§1 module shape; §2 RequestSpec ámbito minimal: method + path + params + headers; §3 parser; §10 tests/test_core.py shape)
    - CLAUDE.md (mandatory `from __future__ import annotations`; no relative imports — usar `from ambito_financiero_client._core import RequestSpec`; no wildcard; module-level docstring obligatorio; section dividers `# ---...---`)
  </read_first>
  <behavior>
    - `_core.py` define `@dataclass(frozen=True, slots=True) RequestSpec` con fields ámbito-mínimos: `method: str`, `path: str`, `params: dict[str, Any] | None = None`, `headers: dict[str, str] | None = None`. NO incluye `auth_basic`/`data`/`json_body`/`url_pre_encoded` (no aplican a ámbito — D-01 per-package).
    - `_core.py` define `raise_for_response(resp: httpx.Response) -> None` que MUEVE la lógica actual de `client.py:64-70` (`_raise_for_response`) verbatim, conservando el manejo de exceptions ámbito (`AmbitoClientError`/etc.).
    - `_core.py` define un builder `build_<endpoint>_request(state: _ClientState, <args>) -> RequestSpec` y un parser `parse_<endpoint>_response(resp: httpx.Response) -> <typed_result>` por cada endpoint público actual del paquete ámbito.
    - Los parsers consumen body explícito vía `resp.read()` ANTES de `raise_for_response(resp)` (D-06 pattern aplicado preventivamente cross-package — coherente con CR-03 pattern; aunque CR-03 es matriz-specific, el patrón canon es "body-consume-then-raise").
    - `_core.py` NO importa `ambito_financiero_client.client` ni `ambito_financiero_client.aio` (D-09 import-linter enforça).
    - `_core.py` PUEDE importar `from ambito_financiero_client._state import _ClientState` (Phase 6, sin cambios).
    - `_core.py` PUEDE importar `from ambito_financiero_client.exceptions import ...` (sin cambios — el módulo `exceptions.py` no depende de `client.py`/`aio.py`).
    - `_core.py` PUEDE importar `from ambito_financiero_client._parsing import parse_ar_decimal` (reusar Phase 6 helpers).
    - `tests/test_core.py` NUEVO: unit tests directos sobre `build_<endpoint>_request(...)` (verificar shape de `RequestSpec` retornado) y `parse_<endpoint>_response(resp)` (verificar parsing contra `httpx.Response(status_code, content=...)` synthetic). Bypasan transport — sin httpx_mock necesario para los unit tests puros (httpx_mock se usa en `client.py` shell tests, no aquí).
    - mypy strict pasa contra `_core.py` y `tests/test_core.py`; ruff check + format pasan.
  </behavior>
  <action>
    1. Crear `packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` reemplazando el placeholder de Plan 1. Header:
       - Module docstring multi-linea (per CONVENTIONS.md):
         - "Pure builders and parsers for the ambito-financiero-client REST client."
         - "Phase 7 REFAC-03 + D-01 (per-package RequestSpec) + D-04 (raise_for_response moved here with alias re-export in client.py / aio.py)."
         - "All functions are pure: state in → RequestSpec or typed result out. No I/O. No imports from `ambito_financiero_client.client` or `ambito_financiero_client.aio` (enforced by import-linter contract in pyproject.toml)."
         - Sección `Example` con `::` code block mostrando `spec = _core.build_dollar_banco_nacion_request(state, date); resp = client._request(spec); result = _core.parse_dollar_banco_nacion_response(resp)`.
       - `from __future__ import annotations`.
       - Imports: `dataclasses (dataclass)`, `typing.Any`, `httpx`. Internos: `from ambito_financiero_client._state import _ClientState`, `from ambito_financiero_client.exceptions import AmbitoClientError` (o el subclass apropiado tras inspeccionar `exceptions.py`), `from ambito_financiero_client._parsing import parse_ar_decimal` (si los parsers lo usan).

    2. Definir `__all__` con todos los símbolos públicos del módulo:
       - `RequestSpec`
       - `raise_for_response`
       - cada `build_<endpoint>_request`
       - cada `parse_<endpoint>_response`

    3. Section dividers (PATTERNS.md §1):
       - `# --- RequestSpec ---`
       - `# --- Stateless helpers (D-04 — moved from client.py; alias preserved there) ---`
       - `# --- Endpoint builders ---`
       - `# --- Endpoint parsers ---`

    4. `RequestSpec` ámbito-mínimo (per CONTEXT D-01 + PATTERNS §2 ámbito row):
       ```
       @dataclass(frozen=True, slots=True)
       class RequestSpec:
           method: str
           path: str
           params: dict[str, Any] | None = None
           headers: dict[str, str] | None = None
       ```
       Docstring describiendo "Pure description of an HTTP request — no transport coupling. Per-package shape D-01: ámbito minimal (no auth, no body)."

    5. `raise_for_response(resp: httpx.Response) -> None`:
       - Copiar la lógica actual de `ambito_financiero_client/client.py:64-70` literalmente (status_code mapping a exceptions, parsing del body si aplica). Preservar firma y comportamiento existente — los tests del paquete dependen del comportamiento exacto.
       - Si la implementación actual consume body via `.text`/`.json()`, conservar; agregar `resp.read()` ANTES si no estaba.

    6. Para cada endpoint público actual del paquete (identificar inspeccionando `__init__.py` `__all__` + `client.py`/`aio.py` métodos públicos):
       - `build_<endpoint>_request(state: _ClientState, <args>) -> RequestSpec`:
         - Construye `path` con interpolación de fecha/símbolo según el endpoint (e.g. para `get_dollar_banco_nacion(fecha)`: path con la fecha formateada como hace el método actual).
         - Retorna `RequestSpec(method="GET", path=path, params=..., headers={...})`.
       - `parse_<endpoint>_response(resp: httpx.Response) -> <typed_result>`:
         - `resp.read()` PRIMERO (D-06 pattern).
         - `raise_for_response(resp)`.
         - Parse body (delegar a `parse_ar_decimal` o lógica actual del endpoint method).
         - Return typed result.

    7. Crear `packages/ambito-financiero-client/tests/test_core.py`:
       - Header: docstring "Unit tests for ambito_financiero_client._core builders/parsers (Phase 7 REFAC-03)." + `from __future__ import annotations`.
       - Imports: `import httpx`, `import pytest`, `from ambito_financiero_client import _core`, `from ambito_financiero_client._state import _ClientState`.
       - ≥4 tests cubriendo:
         - `test_build_<endpoint>_request_returns_correct_spec_shape`: instancia `state = _ClientState()` (con defaults o explicit base_url); call builder; assert `spec.method == "GET"`, `spec.path == "<expected literal>"`, `spec.params == {...}` o `is None`.
         - `test_parse_<endpoint>_response_returns_typed_result`: construir synthetic `resp = httpx.Response(200, content=b"<body>")`; call parser; assert result.
         - `test_raise_for_response_maps_status_correctly`: con `resp = httpx.Response(500, content=b"...")`, assert `_core.raise_for_response(resp)` raises `AmbitoClientError` (o el subclass relevante).
         - `test_parse_consumes_body_before_raise`: assert pattern D-06 funciona (body consumed antes de raise).
  </action>
  <verify>
    <automated>uv run pytest packages/ambito-financiero-client/tests/test_core.py -x -v &amp;&amp; uv run mypy packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/ambito-financiero-client/tests/test_core.py &amp;&amp; uv run ruff check packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/ambito-financiero-client/tests/test_core.py &amp;&amp; uv run ruff format --check packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/ambito-financiero-client/tests/test_core.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'from __future__ import annotations' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` == 1
    - Source: `grep -c '@dataclass(frozen=True, slots=True)' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` &gt;= 1
    - Source: `grep -c 'class RequestSpec' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` == 1
    - Source: `grep -c 'def build_' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` &gt;= 1
    - Source: `grep -c 'def parse_' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` &gt;= 1
    - Source: `grep -c 'def raise_for_response' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` == 1
    - Source: `grep -vc '^#' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py | head -1` confirma min_lines &gt;= 50
    - Source: `grep -E "from ambito_financiero_client\.(client|aio)" packages/ambito-financiero-client/src/ambito_financiero_client/_core.py | grep -v '^#'` returns NOTHING (no forbidden imports)
    - Source: `grep -c 'resp.read()' packages/ambito-financiero-client/src/ambito_financiero_client/_core.py` &gt;= 1 (D-06 pattern at least once)
    - Test command: `uv run pytest packages/ambito-financiero-client/tests/test_core.py -x` exits 0 with ≥4 tests passing
    - CLI: `uv run mypy packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/ambito-financiero-client/tests/test_core.py` exits 0 (strict)
    - CLI: `uv run ruff check packages/ambito-financiero-client/src/ambito_financiero_client/_core.py packages/ambito-financiero-client/tests/test_core.py` exits 0
    - CLI: `uv run lint-imports` exits 0 — 4 contracts still pass; specifically `ambito_financiero_client._core` doesn't depend on transport modules
  </acceptance_criteria>
  <done>
    `_core.py` ámbito completo con RequestSpec + builders + parsers + raise_for_response; tests unitarios verde; lint-imports verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse `ambito_financiero_client/client.py` + `aio.py` a transport shells consuming `_core` (D-04 alias preservada; B8 test verde; LOC drop ≥30%)</name>
  <files>packages/ambito-financiero-client/src/ambito_financiero_client/client.py, packages/ambito-financiero-client/src/ambito_financiero_client/aio.py</files>
  <read_first>
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (TODO — para refactorizar in-place; preserve Client class skeleton de Phase 6: `__slots__`, `__init__`, `_ensure_http_client`, `__enter__`/`__exit__`/`close`, PEP 562 shim al final; SOLO cambian los endpoint methods + `_request` + `_raise_for_response` alias)
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (TODO — análogo async; preserve AsyncClient skeleton; B8 import de `_raise_for_response` cambia de `client` a `_core`)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py (creado en Task 1 — usar RequestSpec, builders, parsers)
    - packages/ambito-financiero-client/tests/test_client_class.py (todos los tests existentes — verificar que NO se rompe ninguno; en particular el test B8 `test_aio_imports_raise_for_response_from_client` si existe en ámbito — searched en `grep -rn 'test_aio_imports_raise_for_response_from_client' packages/ambito-financiero-client/`)
    - packages/ambito-financiero-client/tests/test_fixture_reaches_production.py (verificar shape de `httpx_mock` test; tras refactor sigue verde)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§4 D-04 alias preservation con detalle ámbito; §5 transport shell `_request(spec) -> httpx.Response`; §6 endpoint method post-refactor 3-liner; §"B8 alias preservation (D-04 critical)" — incluye honesty flag sobre el `as _raise_for_response` mypy strict re-export pattern)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 3 transport shell sync, Pattern 4 transport shell async, Pattern 6 D-04 alias; § Common Pitfalls Pitfall 2 — alias falla por re-define accidental)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-03 `_request` retorna `httpx.Response`; D-04 alias; D-05 ≤30-50 LOC por endpoint group; D-14 LOC metric)
    - verification/snapshots/ambito_financiero_client-surface.txt (D-16 — public surface NO debe cambiar; el snapshot test va a verificar diff = 0)
    - CLAUDE.md (no wildcard imports; dual sync/async mirroring — el body de sync y async debe ser idéntico salvo `await`)
  </read_first>
  <behavior>
    - `client.py` post-refactor:
      - Conserva imports de Phase 6 + agrega `from ambito_financiero_client import _core` + `from ambito_financiero_client._core import RequestSpec`.
      - Conserva `__all__` IDÉNTICO al Phase 6 (D-16 snapshot preservation). NO agregar `_core` al `__all__`.
      - Conserva el header docstring + `load_dotenv()` + el module-level state (`_default_client` etc.) IDÉNTICO al Phase 6.
      - REEMPLAZA el `_raise_for_response` function body (líneas ~64-70) por la línea single `_raise_for_response = _core.raise_for_response  # D-04: preserves B8 identity (aio._raise_for_response is client._raise_for_response). Both aliases reference _core.raise_for_response.` (PATTERNS §4).
      - REEMPLAZA cada endpoint method del `Client` class (e.g. `get_dollar_banco_nacion`) por un 3-liner: `spec = _core.build_<endpoint>_request(self._state, ...)`; `resp = self._request(spec)`; `return _core.parse_<endpoint>_response(resp)`.
      - REEMPLAZA el `_request(self, method, path, ...)` por `_request(self, spec: RequestSpec) -> httpx.Response` (D-03):
        - `http = self._ensure_http_client()`
        - `url = f"{self._state.base_url}{spec.path}"`
        - `return http.request(spec.method, url, params=spec.params, headers=spec.headers)`
      - PEP 562 shim de Phase 6 (D-01..D-04) — `__getattr__` al final del módulo — NO TOCAR (Phase 7 expressly preserves; PATTERNS.md §"PEP 562 shim untouched").
    - `aio.py` post-refactor: mirror exacto sync con `await`:
      - Conservar imports de Phase 6 + agregar `from ambito_financiero_client import _core` + `from ambito_financiero_client._core import RequestSpec`.
      - CAMBIAR la línea de import B8 actual `from ambito_financiero_client.client import _raise_for_response as _raise_for_response` (PATTERNS §4 honesty flag — preserva el `as ALIAS` para mypy strict `implicit_reexport=False`) por `_raise_for_response = _core.raise_for_response  # D-04 mirror: identity with client.py preserved via shared _core source`. (No requiere `as` porque NO se está importando del módulo via `from ... import`; la asignación directa del atributo del módulo `_core` ya satisface el re-export pattern. SI mypy strict requiere `as ALIAS` para re-export, planner usa: `from ambito_financiero_client._core import raise_for_response as _raise_for_response`.)
      - REEMPLAZAR endpoint methods por `await` shells (3-liner mirror).
      - REEMPLAZAR `_request` async por shell.
      - PEP 562 shim NO TOCAR.
    - Identidad B8: tras refactor, `aio._raise_for_response is client._raise_for_response` debe seguir siendo `True` porque AMBOS apuntan a `_core.raise_for_response` (mismo objeto). El test (si existe — verify con grep) sigue verde.
    - LOC: tras refactor, `wc -l client.py + aio.py` agregado debe ser ≤ `557 * 0.70 = 390` (≥30% drop, success criterion D-14). El `_core.py` no entra en este conteo.
    - Public surface snapshot (`verification/test_public_surface.py`) sin diff — `_core` no debe aparecer en `__all__`.
    - Cross-leak sentinel test (`verification/test_sync_async_isolation.py`) para ámbito sync + ámbito async sigue verde — los sentinels base_url proxy aún detectan la wire request.
  </behavior>
  <action>
    1. Identificar todos los endpoint methods públicos en `client.py` y `aio.py` actuales (e.g. `get_dollar_banco_nacion`, etc. — usa `grep -nE '^    (async )?def (get_|configure)' packages/ambito-financiero-client/src/ambito_financiero_client/{client,aio}.py`).

    2. Refactor de `client.py`:
       - Top-of-file: agregar `from ambito_financiero_client import _core` después de los imports de Phase 6.
       - REEMPLAZAR la función `_raise_for_response(...)` (líneas ~64-70) por:
         ```
         # D-04: preserves B8 identity (aio._raise_for_response is client._raise_for_response).
         # Both aliases reference _core.raise_for_response.
         _raise_for_response = _core.raise_for_response
         ```
         (sin la firma `def _raise_for_response(resp)...` — es una asignación module-level).
       - DENTRO de la `Client` class:
         - REEMPLAZAR `_request` (la firma actual con `method, path, ...`) por `def _request(self, spec: _core.RequestSpec) -> httpx.Response:` con body que ejecuta `http.request(spec.method, f"{base_url}{spec.path}", params=spec.params, headers=spec.headers)`.
         - Por cada endpoint method, REEMPLAZAR el body por el 3-liner. Conservar la firma pública (args, defaults, kwargs) y el docstring exacto.
       - PEP 562 shim al final del módulo: NO TOCAR.
       - `__all__`: NO TOCAR.
       - `load_dotenv()`: NO TOCAR.

    3. Refactor de `aio.py`:
       - Top-of-file: REEMPLAZAR la línea B8 `from ambito_financiero_client.client import _raise_for_response as _raise_for_response` por:
         ```
         from ambito_financiero_client import _core
         # D-04 mirror — preserves B8 identity via shared _core source.
         from ambito_financiero_client._core import raise_for_response as _raise_for_response
         ```
         (usar `from _core import raise_for_response as _raise_for_response` para satisfacer mypy strict `implicit_reexport=False`).
       - DENTRO de la `AsyncClient` class:
         - REEMPLAZAR `_request` por `async def _request(self, spec: _core.RequestSpec) -> httpx.Response:` con `return await http.request(...)`.
         - Por cada endpoint async method, REEMPLAZAR el body por `spec = _core.build_<X>_request(...); resp = await self._request(spec); return _core.parse_<X>_response(resp)`.

    4. Validar identidad B8 manualmente con un repl rápido en el verify step:
       ```
       uv run python -c "from ambito_financiero_client.aio import _raise_for_response as a; from ambito_financiero_client.client import _raise_for_response as c; assert a is c, 'B8 identity broken'"
       ```

    5. Validar LOC drop:
       ```
       wc -l packages/ambito-financiero-client/src/ambito_financiero_client/{client,aio}.py
       ```
       Confirmar el sum ≤ 390. Si supera, escalate al usuario (D-14 acepta short-fall con documentation, pero PREFERENCIA es lograr ≥30%).

    6. Validar D-16 snapshot público sin diff:
       ```
       uv run pytest verification/test_public_surface.py::test_<...ambito case> -x -v
       ```
       (snapshot diff zero — `_core` NO debe aparecer; ningún atributo público nuevo).

    7. Correr full suite del paquete ámbito + verification:
       ```
       uv run pytest packages/ambito-financiero-client/ -x
       uv run pytest verification/test_sync_async_isolation.py -x -v
       uv run pytest verification/test_public_surface.py -x
       ```

    8. Validar lint-imports verde tras refactor real (no más placeholder ámbito).

    9. Confirmar cross-leak guard (`verification/test_sync_async_isolation.py`) sigue verde — sync y async sentinels alcanzan el wire request via base_url proxy.
  </action>
  <verify>
    <automated>uv run pytest packages/ambito-financiero-client/ -x &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -x -v &amp;&amp; uv run pytest verification/test_public_surface.py -x &amp;&amp; uv run python -c "from ambito_financiero_client.aio import _raise_for_response as a; from ambito_financiero_client.client import _raise_for_response as c; assert a is c, 'B8 identity broken'" &amp;&amp; uv run mypy packages/ambito-financiero-client/ &amp;&amp; uv run ruff check packages/ambito-financiero-client/ &amp;&amp; uv run ruff format --check packages/ambito-financiero-client/ &amp;&amp; uv run lint-imports &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run python -c "from ambito_financiero_client.aio import _raise_for_response as a; from ambito_financiero_client.client import _raise_for_response as c; assert a is c"` exits 0 (B8 identity preserved per D-04)
    - Source: `grep -c '_raise_for_response = _core.raise_for_response' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` == 1 (alias literal)
    - Source: `grep -c '_raise_for_response' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` &gt;= 1 (alias presente en aio)
    - Source: `grep -cE 'def _request\(self,\s*spec' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` == 1 (`_request` recibe `RequestSpec` per D-03)
    - Source: `grep -cE '(async )?def _request\(self,\s*spec' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` == 1
    - Source: `grep -c '_core.build_' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` &gt;= 1 (endpoint methods call `_core.build_*`)
    - Source: `grep -c '_core.parse_' packages/ambito-financiero-client/src/ambito_financiero_client/client.py` &gt;= 1 (endpoint methods call `_core.parse_*`)
    - Source: `grep -c '_core.build_' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` &gt;= 1
    - Source: `grep -c 'await' packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` &gt;= 1 (preserve async)
    - Source: aggregated `wc -l packages/ambito-financiero-client/src/ambito_financiero_client/client.py packages/ambito-financiero-client/src/ambito_financiero_client/aio.py` returns sum ≤ 390 (≥30% drop vs 557 baseline; if &gt;390, planner documents shortfall per D-14)
    - Source: `diff -u verification/snapshots/ambito_financiero_client-surface.txt &lt;(uv run python -c 'import ambito_financiero_client; ...regen logic...')` returns no diff (snapshot preserved per D-16). Alternatively: `uv run pytest verification/test_public_surface.py -k ambito -x` exits 0
    - Test command: `uv run pytest packages/ambito-financiero-client/ -x` exits 0 (all package tests pass)
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -x -v` exits 0 (sync ámbito + async ámbito both pass post-refactor)
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -3` shows total ≥ Plan 1 baseline (≥400) and NO failures
    - CLI: `uv run lint-imports` exits 0 — `ambito_financiero_client._core` contract still passes (no transport imports introduced)
    - CLI: `uv run mypy packages/ambito-financiero-client/` exits 0 (strict)
    - CLI: `uv run ruff check packages/ambito-financiero-client/` exits 0
    - CLI: `uv run ruff format --check packages/ambito-financiero-client/` exits 0
  </acceptance_criteria>
  <done>
    `client.py` y `aio.py` colapsados a transport shells; identidad B8 verde; LOC drop ≥30%; public surface snapshot zero-diff; lint-imports verde; tests baseline + nuevos verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_core.py` → `client.py`/`aio.py` | Boundary que import-linter enforça (T-7-01); este plan introduce el primer `_core.py` real (ámbito) y verifica que el contract sigue verde |
| sync surface → async surface (token isolation) | Cross-leak boundary verificado por `verification/test_sync_async_isolation.py` parametrizado (T-7-02); ámbito post-refactor debe seguir aislando |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | `_core.py` accidental import of transport state | mitigate | Task 1 source acceptance criterion `grep "from ambito_financiero_client\.(client\|aio)"` returns empty + Task 2 `uv run lint-imports` exit 0 |
| T-7-02 | Tampering | Sync/async token cross-contamination | mitigate | Task 2 verify step incluye `verification/test_sync_async_isolation.py` — sync sentinel y async sentinel siguen llegando al wire request separadamente |
| T-7-03 | Tampering | B8 alias rotura (Pitfall 2 — un dev redefine `_raise_for_response` localmente) | mitigate | Task 2 verify step incluye el repl assert `a is c`; código incluye comentario explícito D-04 sobre la asignación módulo-level |
| T-7-D16 | Tampering (test integrity / API contract) | `_core` accidentally added to public `__all__` (Phase 6 snapshot drift) | mitigate | Task 2 verify step corre `verification/test_public_surface.py` para ámbito; cualquier diff fallea |
</threat_model>

<verification>
- Identidad B8: `aio._raise_for_response is client._raise_for_response` == True.
- `uv run lint-imports` exit 0 — ámbito `_core` doesn't import transport.
- Public surface snapshot zero diff (D-16).
- `verification/test_sync_async_isolation.py` para ámbito sync + async verde.
- LOC drop ≥30% (≤390 LOC client+aio agregado).
- `uv run pytest -q` full suite verde; nuevos `tests/test_core.py` add ≥4 tests.
- ruff + mypy strict pasan.
</verification>

<success_criteria>
- `ambito_financiero_client/_core.py` existe con RequestSpec ámbito-minimal + builders + parsers + raise_for_response, PURO (no imports de transport).
- `client.py` y `aio.py` colapsados a transport shells consuming `_core`; LOC drop ≥30% agregado vs Phase 6 baseline 557.
- B8 alias D-04 preservado: `aio._raise_for_response is client._raise_for_response` (test verde si existe; verificación manual via repl en cualquier caso).
- Public surface snapshot Phase 6 unchanged (`_core` NO en `__all__`).
- Cross-leak guard sigue verde post-refactor.
- 4 contracts `lint-imports` pasan en verde (`ambito_financiero_client._core` boundary establecido).
- Test count ≥ Plan 1 baseline + ≥4 nuevos tests de `_core`.
</success_criteria>

<output>
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-02-SUMMARY.md` al finalizar, documentando:
- LOC drop reporting format (RESEARCH.md "Commit message pattern"):
  ```
  LOC drop vs Phase 6 baseline (commit 5db0a0d):
  - client.py: 270 → <X> (-<Y>%)
  - aio.py:    287 → <X> (-<Y>%)
  - _core.py:  0 → <N> (NEW)
  - Aggregate client+aio: 557 → <X> (-<Y>%)   <PASS/FAIL ≥30%>
  ```
- B8 identity confirmed: `aio._raise_for_response is client._raise_for_response` == True.
- Public surface snapshot: zero diff confirmed.
- Cross-leak guard: sync + async ámbito both pass.
- Nuevos tests de `_core` agregados: N.
</output>
