---
phase: 33-verificaci-n-en-vivo-en-modo-estricto-fixes
plan: 05
type: execute
wave: 3
depends_on: ["33-02", "33-03", "33-04"]
files_modified:
  - verification/test_probe_context_coverage.py
  - verification/test_finding_count_consistency.py
  - scripts/preflight_33.py
  - .planning/verification/ambito-financiero-client-findings.md
  - .planning/verification/higyrus-client-findings.md
  - .planning/verification/iol-client-findings.md
  - .planning/verification/matriz-client-findings.md
  - .planning/verification/market-data-client-findings.md
  - .planning/verification/schemas
  - .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md
autonomous: true
requirements: [LIVE-TYP-01]

must_haves:
  truths:
    - "All 130 probes across the five drivers carry the endpoint/surface binding — proven by an AST gate over the drivers, not by inspection (criterion 1)."
    - "An operator can prove every in-scope package authenticates against its live vendor BEFORE any census number is counted; a package that SKIPs is recorded as SKIPPED, never silently counted as clean (criterion 1, D-13)."
    - "Each of the five drivers has completed an observable pass (full census) and a strict pass (proves the raise fires), and both passes are recorded with their date, base URL and — for matriz — the market-hours state of the session (criterion 1, D-04, P-12)."
    - "The number of fids a run emitted equals the number of new `### F-` blocks in that package's findings file — so no finding was counted in the SUMMARY while being silently discarded (P-3)."
    - "`33-CENSUS.md` states, per package, the live distinct-`(model, field_path, kind)` count beside the ratified Phase 29 floor, and routes every deferred finding to a named destination with its package and field recorded (criterion 5)."
    - "Schema snapshots are reconciled against the baseline: every new or drifted file under `.planning/verification/schemas/` is accounted for in the census, including matriz's 9 declared-but-absent files taking their write-once branch (criterion 4)."
  artifacts:
    - path: "verification/test_probe_context_coverage.py"
      provides: "AST gate asserting every probe_* in all five drivers carries the context decorator"
      contains: "probe_context"
    - path: "verification/test_finding_count_consistency.py"
      provides: "P-3 regression class — emitted fid count equals new finding-block count"
    - path: "scripts/preflight_33.py"
      provides: "per-package live authentication proof with SKIP-not-raise semantics (D-13)"
    - path: ".planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md"
      provides: "live census vs the ratified >=96 floor, per-package, with the re-scope table"
      contains: "Re-scope"
  key_links:
    - from: "scripts/preflight_33.py"
      to: "packages/*/src/*/client.py"
      via: "Client().login() per package, printing only the exception class name on failure"
      pattern: "login\\(\\)"
    - from: "main_*.py driver SUMMARY"
      to: ".planning/phases/33-.../33-CENSUS.md"
      via: "len(handler.seen) per package is the census unit contrasted against 29-SIZING.md"
      pattern: "handler\\.seen|seen="
  prohibitions:
    - id: P-01
      category: privacy
      statement: "MUST NOT widen the frozen six-key type-not-value divergence record, and MUST NOT compose any finding field from anything other than those six keys plus the endpoint and surface the harness itself set — a git-committed findings file must never carry a live wire value."
      status: resolved
      verification: judgment
    - id: P-02
      category: transparency
      statement: "MUST NOT report a census number, a phase criterion, or a cycle-closure PASS produced by a signal that inspected nothing — a green arising from a dropped log level, a swallowed handler exception, an unseeded fid allocator, or a zero-applicable closure filter is a false clean."
      status: resolved
      verification: judgment
    - id: P-03
      category: transparency
      statement: "MUST NOT shrink the census to fit the ratified floor — no narrowing the walker, no dropping a divergence kind, and no deferring a confirmed finding to an unnamed later; every deferred finding carries a named destination with its package and field recorded."
      status: resolved
      verification: judgment
    - id: P-04
      category: transparency
      statement: "MUST NOT widen, close, or enforce matriz's four pre-existing RESPONSE Literal aliases, and MUST NOT promote iol's mercado/plazo to a Literal on RESPONSE-side evidence."
      status: resolved
      verification: judgment
    - id: P-05
      category: safety
      statement: "MUST NOT let the two-pass runner fire a destructive or order-entry operation against a live vendor — the strict pass runs with the market-data mutation gate off and no probe places a real order."
      status: resolved
      verification: judgment
  flagged_assumptions:
    - "The deterministic edge-probe returned LIVE-TYP-01 as `unclassified` / `unresolved`; its acceptance surface is compound (five ROADMAP criteria), not a single classifiable edge. Carried as an explicit flagged assumption — the five ROADMAP Phase 33 success criteria are the real acceptance surface. Full statement in 33-01-PLAN.md."
    - "Credential validity and vendor reachability are runtime facts no planning pass can settle (RESEARCH A1/A2). Task 1's pre-flight is the mechanism that converts them from assumption to measurement; until it prints AUTH OK for a package, no census number for that package is valid."
    - "RESEARCH A4 predicts the live census EXCEEDS the >=96 floor. An under-floor result is a signal of census loss (P-1/P-2/P-3), NOT a clean package — Task 3 investigates an under-floor result rather than accepting it."
---

<objective>
Run the phase: prove the mechanism covers all 130 probes, prove the credentials actually
authenticate, execute the two-pass observable-then-strict run against every live API, and write
`33-CENSUS.md` contrasting the real volume against the ratified `≥96` floor with an explicit re-scope
for anything deferred.

Purpose: criteria 1 and 5 are settled here, and this is the only point in the phase where the
distinction between "clean" and "silently losing records" is decidable. Three verified silent-loss
channels were closed in code (P-1/P-2/P-3); this plan installs the assertions that prove they stayed
closed, and gates every census number behind a pre-flight that measures authentication rather than
assuming it.

Output: two new harness gates, the pre-flight script, ten driver runs (five observable + five
strict), reconciled schema snapshots, and `33-CENSUS.md`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CONTEXT.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-RESEARCH.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-PATTERNS.md
@.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-BASELINE.md
@.planning/phases/29-decoder-observable/29-SIZING.md
@verification/divergences.py
@verification/env_gate.py
</context>

## Artifacts this phase produces

Created or modified by **this plan**: `verification/test_probe_context_coverage.py`,
`verification/test_finding_count_consistency.py`, `scripts/preflight_33.py` (may be uncommitted),
the five `.planning/verification/<pkg>-findings.md` files (appended by the live runs),
`.planning/verification/schemas/**` (write-once and drift branches), and `33-CENSUS.md`.

Everything else in the phase's artifact inventory is listed in `33-01-PLAN.md` under
"Artifacts this phase produces"; do not duplicate those symbols here.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-run gates — decorator coverage over all 130 probes, fid/finding consistency, and the live-auth pre-flight</name>
  <files>verification/test_probe_context_coverage.py, verification/test_finding_count_consistency.py, scripts/preflight_33.py</files>
  <read_first>
    - `verification/test_main_drivers_bare_except.py` (all 51 lines) — the parametrized AST-walk-over-drivers skeleton with repo root via `Path(__file__).resolve().parent.parent`, which the coverage gate copies
    - `verification/env_gate.py:32-41` — `require_env`'s SKIP-not-raise contract, which the pre-flight extends from presence to authentication
    - `main_iol.py:289-334` — `_redacted_exc`, the precedent for printing only a class name and never an exception body
    - `verification/findings.py` — `max_existing_fid`, `findings_path`, `_FINDINGS_DIR` (`:55-56`), and `append_finding`'s non-`OPEN` preservation short-circuit
    - `33-02-SUMMARY.md`, `33-03-SUMMARY.md`, `33-04-SUMMARY.md` — the exact SUMMARY line format each driver now prints and the decorator name as finalised in Plan 01
    - `33-RESEARCH.md` Pitfalls P-3, P-10, P-13 and `## Environment Availability`
  </read_first>
  <behavior>
    - `verification/test_probe_context_coverage.py::test_every_probe_carries_probe_context`
      parametrizes over all five drivers, parses each with `ast`, collects every `FunctionDef` /
      `AsyncFunctionDef` whose name starts with `probe_`, and asserts each has the context decorator
      in its `decorator_list`. It also asserts a lower bound per driver (ambito 7, higyrus 19, iol 15,
      market-data 43, matriz 46 — 130 total) so the gate cannot pass vacuously on a driver whose
      probes were renamed away, and it asserts the decorator's `surface` argument matches the
      function's `_sync` / `_async` suffix.
    - `verification/test_finding_count_consistency.py::test_emitted_fid_count_matches_new_finding_blocks`
      proves the P-3 regression class against a temp findings file: with
      `verification.findings._FINDINGS_DIR` monkeypatched to `tmp_path` and a findings file seeded
      with a non-`OPEN` `F-01`, an allocator seeded via `max_existing_fid` emits N fids and produces
      exactly N new `### F-` blocks; the same test with an UNSEEDED allocator produces fewer blocks
      than fids, so the assertion is proven fail-first rather than trivially true.
    - `scripts/preflight_33.py` prints one line per package, either `<pkg>: AUTH OK` or
      `<pkg>: AUTH FAIL <ExceptionClassName>`, and exits non-zero if any in-scope package failed. It
      never prints an exception body, never prints a credential, and never raises out.
  </behavior>
  <action>
    Create `verification/test_probe_context_coverage.py` by copying the skeleton of
    `verification/test_main_drivers_bare_except.py` (`:17-51`) — `from __future__ import annotations`,
    `import ast`, `_REPO_ROOT = Path(__file__).resolve().parent.parent`, a module-level `_DRIVERS`
    list, and `@pytest.mark.parametrize`. Extend `_DRIVERS` to all five drivers and add a
    `_EXPECTED_PROBE_COUNT` mapping with the five counts above. Walk for `probe_`-prefixed function
    definitions, assert every one carries the decorator (match on the decorator's callee name in
    `ast.dump`), assert the per-driver count equals the expected value, and assert the decorator's
    `surface` keyword literal matches the function name's suffix. Put the *why* in every assertion
    message, in the style of `verification/test_logging_root_unchanged.py:39-67`. Note in the module
    docstring that `verification/` never runs in CI, so this gate is a local phase gate and its value
    is that plans 33-02/33-03/33-04 cannot each believe they finished while one driver is unwired.

    Create `verification/test_finding_count_consistency.py` implementing the two-arm behaviour above.
    The unseeded arm is the fail-first control: without it, the seeded arm passes for a package whose
    findings file happens to be empty and proves nothing. Use `monkeypatch.setattr` on
    `verification.findings._FINDINGS_DIR` so no test writes to a committed findings file.

    Create `scripts/preflight_33.py` as a **real `.py` file**, never a `python -c` one-liner:
    `python-dotenv`'s `find_dotenv()` walks up from the calling frame's file and falls back to
    `os.getcwd()` when `__main__` has no `__file__`, and there is no `.env` at the repo root, so a
    `-c` invocation reports every credential missing (P-10, reproduced by execution). Model it on
    `verification/env_gate.py:32-41`'s SKIP-not-raise contract. For each of `higyrus-client`,
    `iol-client`, `matriz-client` and `market-data-client`, import the package, construct its default
    `Client()` and call `login()` inside a `try`, printing `AUTH OK` on success and
    `f"AUTH FAIL {type(exc).__name__}"` on failure — the class name only, never `str(exc)` or
    `repr(exc)`, because vendor error bodies plausibly carry account identifiers
    (`main_iol.py::_redacted_exc` documents this). `ambito-financiero-client` has no auth by design
    and is reported as `n/a`. Exit non-zero when any of the four printed `AUTH FAIL`. The script may
    stay uncommitted (Claude's Discretion), but if uncommitted its full text must be reproduced in
    `33-05-SUMMARY.md` so the method is reproducible.

    Run the pre-flight and record its output. Do not proceed to Task 2 for any package that printed
    `AUTH FAIL`: per D-13 that package falls back to the Phase 23 operator-runs-and-pastes path, and
    the fallback is recorded in `33-CENSUS.md` as `SKIPPED — credentials`, never as a zero.
  </action>
  <acceptance_criteria>
    - `uv run pytest verification/test_probe_context_coverage.py -q` passes and reports 5 parametrized cases.
    - `uv run pytest verification/test_finding_count_consistency.py -q` passes with at least 2 tests, one of which is the unseeded fail-first control.
    - `grep -v '^#' verification/test_probe_context_coverage.py | grep -c 130` is at least 1, or the five per-driver expected counts are individually present — the gate carries a numeric lower bound, not just a presence check.
    - `uv run python scripts/preflight_33.py` runs, prints exactly one status line per package, and its output contains no substring from any `.env` value.
    - `git status --porcelain .planning/verification/` is empty after the two new test files run — no test wrote to a committed findings file.
    - `uv run ruff check . && uv run ruff format --check .` pass; `uv run mypy verification` reports no error in the two new files.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py -q &amp;&amp; test -z "$(git status --porcelain .planning/verification/)" &amp;&amp; uv run ruff check . &amp;&amp; uv run mypy verification</automated>
  </verify>
  <done>The AST gate proves all 130 probes carry the decorator with a matching surface and a numeric lower bound per driver; the P-3 consistency assertion exists with a fail-first control; the pre-flight has printed an AUTH status for every in-scope package without leaking a credential or an exception body.</done>
</task>

<task type="auto">
  <name>Task 2: Two-pass live runs against all five real APIs</name>
  <reversibility rating="costly">The findings this run writes are permanent, cross-run-deduped by title; a wrong title convention or an unseeded allocator would have to be unwound by editing committed findings files. The convention was locked in 33-01 Task 1 and the allocator seeding is asserted by Task 1's consistency gate, so this run is safe to fire.</reversibility>
  <files>.planning/verification/ambito-financiero-client-findings.md, .planning/verification/higyrus-client-findings.md, .planning/verification/iol-client-findings.md, .planning/verification/matriz-client-findings.md, .planning/verification/market-data-client-findings.md, .planning/verification/schemas</files>
  <read_first>
    - `33-05-SUMMARY.md` in progress — the pre-flight output from Task 1 (which packages are AUTH OK)
    - `33-01-SUMMARY.md` — the locked title convention and the finalised `divergence_capture` signature
    - `33-02-SUMMARY.md`, `33-03-SUMMARY.md`, `33-04-SUMMARY.md` — the SUMMARY line format each driver prints and the probes that needed `endpoint_scope`
    - `main_market_data.py` around `:3354-3365` — the destructive-sweep ordering note explaining why an active test symbol can surface as `last_error` in `/health/feed`
    - `.planning/verification/schemas/matriz-client/` — 8 files on disk against 17 declared in `_SCHEMA_FILES`; the 9 absent ones take the write-once branch on a successful run
    - `.planning/phases/29-decoder-observable/29-SIZING.md` `## Structural findings` S-1..S-5 — what this run is expected to confirm or falsify
    - `33-RESEARCH.md` Pitfalls P-11 (double-fire), P-12 (market-closed captures), and `## Durable & Runtime State Inventory`
  </read_first>
  <action>
    For every package that printed `AUTH OK`, run two passes, sequentially and never in parallel (a
    parallel run competes for the same vendor rate budget and interleaves the schema write-once
    branch):

    Pass 1 — observable, full census: `uv run --package <pkg> python main_<x>.py` with
    `MARKET_LIBS_STRICT_DECODE` unset. This is the pass whose `len(handler.seen)` is the census number,
    because strict mode raises on the FIRST strict-fatal divergence per response and would under-count
    (S-2 predicts nine fabricated fields in one `CalendarConfig`; strict reports one).

    Pass 2 — strict, proves the raise fires: `MARKET_LIBS_STRICT_DECODE=1 uv run --package <pkg>
    python main_<x>.py`. For `main_market_data.py` this pass runs **without**
    `MARKET_DATA_VERIFY_MUTATING=1`: its purpose is proving the raise fires, not coverage, and firing
    the destructive symbol/holiday cycle twice in one session is the P-11 hazard the driver's own
    comment documents.

    Schedule the matriz pass-1 run **inside an ARG trading session** if at all possible, and populate
    the ART block's market-hours note (`append_finding(market_hours=...)`) for that run. S-5 (matriz
    `MarketDataSnapshot.LA/.SE/.OI/.CL` arriving `null`) is undecidable from a closed-market capture:
    a closed-market null cannot be distinguished from a modelling error. If a session run is not
    possible, record that explicitly and route S-5 to a named destination in Task 3 — never mark it
    resolved from a closed-market capture.

    After each pass, capture and record: the driver's full SUMMARY line (probe counts by status,
    `FINDING=N`, `len(handler.seen)`, the handler error count), the base URL, the run date, the
    market-hours state, and the count of new `### F-` blocks in that package's findings file. Assert
    per package that the emitted-fid count equals the new-block count — a mismatch is the P-3 failure
    mode and must be investigated before any number is recorded, not explained away. Assert per
    package that the handler error count is zero; a non-zero value means `append_finding` rejected a
    record inside the handler's self-guard and the census is incomplete.

    Check the `extra`-kind count for matriz and market-data specifically: both ratified floors are
    `extra`-dominant (18 of matriz's 24, 14 of market-data's 50), so an `extra` count of zero for
    either is the tell that the logger-level raise did not take effect (P-1). Investigate a zero
    there; do not record it.

    Then reconcile the schema snapshots: run `git status --porcelain .planning/verification/schemas/`
    and classify every new and every modified file. Note before starting that the iol
    snapshot-corruption risk carried forward from 30-CONTEXT D-07 is **already closed** per D-14:
    `to_dict()` is confined to the parity probe (`main_iol.py:1215-1216`) and the four iol snapshot
    sites consume raw wire through `_capture_raw_wire`, so no iol snapshot change is required and any
    iol snapshot drift observed here is a real wire change, not a model projection artifact. Do not
    re-open that risk or add a `to_dict()` guard; it is not a criterion-4 blocker. New files are expected — matriz declares 17
    `_SCHEMA_FILES` entries and only 8 exist on disk, so the nine absent ones (orders / positions /
    account-report) take the write-once branch on a successful live run. A **modified** file is drift
    and needs an explanation in Task 3's census, not a silent `git add`. Note that
    `_write_or_check_schema` calls `_next_fid()` on drift, so it shares the P-3 hazard the consistency
    assertion covers.

    Do not edit any package source in this plan; confirming a divergence is Task 3's and 33-07's job.
    Do not triage any finding to `CONFIRMED` / `FIXED` here — findings land `OPEN` and criterion 4 is
    only meaningful after 33-07's triage (D-11).
  </action>
  <acceptance_criteria>
    - For every `AUTH OK` package, two passes completed and each printed a SUMMARY line; the transcript of each is recorded in `33-05-SUMMARY.md`.
    - For every `AUTH OK` package, the handler error count is `0` in both passes.
    - For every `AUTH OK` package, the emitted-fid count equals the count of new `### F-` blocks in that package's findings file.
    - matriz's and market-data's pass-1 `extra`-kind counts are both non-zero, or a documented investigation explains why (P-1 tell).
    - `main_ambito_financiero.py` completed both passes with `len(handler.seen) == 0` and a non-zero probe count — the D-12 smoke result asserted positively.
    - `MARKET_DATA_VERIFY_MUTATING` was unset for the market-data strict pass; the invocation is recorded verbatim in the SUMMARY.
    - `git status --porcelain .planning/verification/schemas/` output is captured and every entry is classified as new (write-once) or modified (drift).
    - No package source file under `packages/` was modified: `git status --porcelain packages/` is empty.
    - `uv run pytest verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py -q` still passes after the runs.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain packages/)" &amp;&amp; uv run pytest verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py -q &amp;&amp; uv run python -c "import sys;sys.path.insert(0,'.');from verification.findings import findings_path;import re;ok=True;\nfor p in ['ambito-financiero-client','higyrus-client','iol-client','matriz-client','market-data-client']:\n    t=findings_path(p).read_text(encoding='utf-8');n=len(re.findall(r'^### F-',t,re.M));print(p,n)\nsys.exit(0)"</automated>
    <human-check>Confirm the two-pass runs are real and complete: for each package, the pass-1 and pass-2 SUMMARY transcripts in 33-05-SUMMARY.md show a live base URL, a non-SKIPPED probe majority, handler errors 0, and a fid/finding-block match. Confirm whether the matriz pass-1 ran inside an ARG trading session (this decides whether S-5 can be resolved or must be re-scoped). Confirm the market-data strict pass was invoked without MARKET_DATA_VERIFY_MUTATING.</human-check>
  </verify>
  <done>Ten driver invocations (or fewer, with every omission recorded as `SKIPPED — credentials` and routed to the Phase 23 operator-paste fallback) have completed; every package's fid count matches its new-finding-block count; handler errors are zero everywhere; the schema-snapshot delta is captured and classified.</done>
</task>

<task type="auto">
  <name>Task 3: Write 33-CENSUS.md — live volume against the ratified floor, with the explicit re-scope</name>
  <files>.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/29-decoder-observable/29-SIZING.md` — the floor table (`:159-171`), the breakdown by kind (`:175-205`), the five structural findings S-1..S-5 (`:214-297`), the blind-spot section, and the re-scope discipline in `## Consequence`
    - `33-05-SUMMARY.md` in progress — every SUMMARY transcript and schema delta from Task 2
    - `33-RESEARCH.md` Pattern 6 (`## Pattern 6: Census counting that is actually comparable to the ≥96 floor`) — the exact column set and the reason `handler.seen` is the only comparable unit
    - `33-RESEARCH.md` Open Questions 2 and 3 — iol has no ratified floor, and Phase 31's newly-typed endpoints inflate the number for non-defect reasons
    - `.planning/ROADMAP.md` — the phase list and the Backlog section, for naming a real destination
  </read_first>
  <action>
    Write `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md` with these
    sections:

    `## Method` — state that the census unit is the distinct `(package, model, field_path, kind)`
    tuple taken from `DivergenceHandler.seen`, which is the same unit `29-SIZING.md` counted, and
    state explicitly that neither the SHAPE-finding count nor the driver's `FINDING=N` is comparable:
    `idempotent_by_title` collapses records by title (and, under the convention locked in 33-01
    Task 1, may split one triple across two surfaces), while `FINDING=N` increments on every
    `_next_fid()` call including ones `append_finding` discards.

    `## Per-package table` — columns: Package | Floor (29-SIZING) | Live distinct triples | Live SHAPE
    findings written | `extra` count | Handler errors | Verdict. Fill every row from Task 2's
    transcripts. Mark `iol-client` as **first measurement, no prior budget**: `29-SIZING.md` reported
    it `N/A, not zero` because it had no `models.py` at sizing time, Phase 30 gave it one, and the
    re-scope rule is written against per-package floors iol does not have. Do NOT back-fill a floor
    for iol — `29-SIZING.md` is emphatic that these are floors, never estimates. Mark
    `ambito-financiero-client` as **smoke only, zero by construction** (D-12: zero model classes,
    therefore zero walker calls) and state that the zero is asserted from a non-zero probe count plus
    `len(handler.seen) == 0` plus zero handler errors, not inferred from an absence.

    `## TYP-02 attribution` — tag separately every divergence originating at an endpoint that was an
    `N/A — unmodelled dict` row in the sizing corpus and gained a model in Phase 31 (higyrus `Health`;
    market-data `Health` / `HealthFeed` / calendar-write). These inflate the live number above the
    floor for a non-defect reason — newly visible because newly typed — and conflating them with
    newly-discovered defects would corrupt the re-scope conversation.

    `## Structural findings S-1..S-5` — one subsection per finding recording whether the live run
    CONFIRMED, FALSIFIED or COULD-NOT-DECIDE it, with the evidence. S-3 (matriz
    `Instrument.instrumentId` absent on the byCFICode and bySegment endpoints, `marketId` / `symbol`
    arriving flattened and being discarded as `extra`) is the highest-consequence item in the set and
    is the primary candidate for 33-07's fix budget. S-5 is COULD-NOT-DECIDE unless the matriz pass-1
    ran inside an ARG trading session (P-12).

    `## Under-floor investigation` — if any package's live triple count came in BELOW its ratified
    floor, do not accept it. RESEARCH A4 argues the blind spot in the type-only corpus points only
    upward, so an under-floor result is evidence of census loss (P-1 logger level, P-2 swallowed
    handler exception, P-3 unseeded allocator), not of a clean package. Record the investigation and
    its outcome; a package that remains under-floor after investigation gets a named explanation, not
    a shrug. If every package met or exceeded its floor, state that and say the section is empty for
    that reason.

    `## Schema snapshot reconciliation` — the classified `git status` delta from Task 2. New files
    (expected: matriz's nine declared-but-absent orders / positions / account-report snapshots taking
    the write-once branch) listed separately from modified files. Every modified file gets a one-line
    explanation of the drift; an unexplained modification is a criterion-4 blocker, not a `git add`.

    `## Re-scope` — the table criterion 5 requires: Package | Model | Field path | Kind | Destination |
    Rationale. Every confirmed divergence that 33-07 will NOT fix in this cycle appears here with a
    **named** destination — an existing ROADMAP phase, or a concretely-named entry added to
    `.planning/ROADMAP.md` `## Backlog` in this same commit. "Later", "a future phase" and "TBD" are
    not destinations; `29-SIZING.md`'s rule is that deferring without a destination, or silencing by
    narrowing the walker, is not an available option. Note that Phase 34 is the per-package release
    phase and is not a valid destination for defect work. If the table is empty because 33-07 will fix
    everything confirmed, say so explicitly and state the count.

    Finally, add to the ROADMAP Backlog any named destination entries this table introduced, in the
    same commit as `33-CENSUS.md`, using a scoped edit to `.planning/ROADMAP.md` — never a whole-file
    rewrite.
  </action>
  <acceptance_criteria>
    - `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md` exists and contains all seven headings: `## Method`, `## Per-package table`, `## TYP-02 attribution`, `## Structural findings`, `## Under-floor investigation`, `## Schema snapshot reconciliation`, `## Re-scope`.
    - The per-package table has one row per package including `iol-client` (marked as a first measurement with no prior budget) and `ambito-financiero-client` (marked smoke-only), and every row carries a live distinct-triple number or an explicit `SKIPPED — credentials`.
    - `grep -c 'S-1\|S-2\|S-3\|S-4\|S-5' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md` is at least 5 — every structural finding has a disposition.
    - Every row of the `## Re-scope` table names a destination that resolves to an existing ROADMAP phase heading or to a heading added under `.planning/ROADMAP.md` `## Backlog` in the same commit; no row's Destination cell is `TBD`, `later`, or empty.
    - `33-CENSUS.md` contains no live wire value: no account number, CUIT, symbol identifier or token fragment. Model names, field paths, type names, endpoint templates and counts only.
    - `uv run ruff check . && uv run ruff format --check .` pass (no source changed, but the gate confirms the runs left nothing behind).
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md &amp;&amp; for h in "## Method" "## Per-package table" "## TYP-02 attribution" "## Structural findings" "## Under-floor investigation" "## Schema snapshot reconciliation" "## Re-scope"; do grep -qF "$h" .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md || exit 1; done &amp;&amp; ! grep -qiE '\| *(TBD|later|a futuro) *\|' .planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-CENSUS.md</automated>
    <human-check>Read 33-CENSUS.md and confirm: (1) the per-package numbers match the SUMMARY transcripts in 33-05-SUMMARY.md; (2) every deferred finding's Destination is a phase or backlog entry you accept, with the package and field recorded; (3) the S-1..S-5 dispositions are supported by the evidence cited, in particular whether S-5 was decided from a trading-session capture or correctly left COULD-NOT-DECIDE; (4) the document contains no live wire value.</human-check>
  </verify>
  <done>`33-CENSUS.md` states the live volume per package beside the ratified floor with the census unit named, disposes all five structural findings, explains every schema-snapshot change, and routes every deferred finding to a named destination that exists in ROADMAP after this commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live vendor APIs (IOL, Higyrus, MATBA ROFEX, market-data `develop`, Ámbito) -> driver process | Five untrusted third-party surfaces, four of them credentialed and account-scoped. |
| `.env` (higyrus 8, iol 3, matriz 8, market-data 4 keys) -> pre-flight and driver processes | Credentials in memory. The pre-flight deliberately triggers a real authentication round trip. |
| driver process -> `.planning/verification/**` | Git-committed and permanently published: findings files, schema snapshots, and `33-CENSUS.md`. |
| `.planning/verification/captures/` | Gitignored staging (`.gitignore:51`) — the only legal home for raw wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-24 | Information Disclosure | `scripts/preflight_33.py` printing a vendor error body containing an account identifier | mitigate | Print `type(exc).__name__` only; never `str(exc)`, `repr(exc)` or `exc.args`. Modelled on `main_iol.py::_redacted_exc` (AD-30-09-01). An acceptance criterion asserts the output contains no `.env` substring. |
| T-33-25 | Information Disclosure | a live wire value reaching `33-CENSUS.md`, a permanently committed artifact | mitigate | The census is composed only from model names, field paths, type names, uninterpolated endpoint templates and counts — all already type-not-value by the Lock 1 record contract. An explicit acceptance criterion re-checks the document. |
| T-33-26 | Information Disclosure | raw wire captured during the run being committed | mitigate | Raw payloads land only under `.planning/verification/captures/`, gitignored at `.gitignore:51`. The census report may be committed; the payloads may not. Verified by `git status` after each run. |
| T-33-27 | Tampering (data integrity on `develop`) | the two-pass runner double-firing the destructive symbol/holiday cycle | mitigate | The strict pass is invoked with `MARKET_DATA_VERIFY_MUTATING` unset, recorded verbatim in the SUMMARY, and asserted as an acceptance criterion (P-11). |
| T-33-28 | Repudiation | a census number recorded from a run whose credentials had silently expired, reading as a clean package | mitigate | Task 1's pre-flight measures authentication before any counting; a package that prints `AUTH FAIL` is recorded as `SKIPPED — credentials` and routed to the Phase 23 operator-paste fallback, never as a zero (D-13, RESEARCH A1). |
| T-33-29 | Repudiation | a false-clean census produced by any of the three silent-loss channels | mitigate | Three independent assertions: handler error count is zero (P-2), emitted-fid count equals new-finding-block count (P-3), and the `extra` count is non-zero for the two `extra`-dominant packages (P-1). Plus the under-floor investigation section, which treats an under-floor result as evidence of loss rather than of cleanliness. |
| T-33-30 | Tampering | an unexplained schema-snapshot modification being absorbed by a blanket `git add` | mitigate | Every entry in the `git status` delta is classified as new (write-once) or modified (drift), and every modified file needs a one-line explanation in the census. `_write_or_check_schema` also allocates a fid on drift, so it is covered by the P-3 consistency assertion. |
| T-33-31 | Tampering (data integrity on a live venue) | an order-entry probe firing against a real brokerage account during the strict pass | accept | Verified read-only across matriz (46 probes, all `get_*` / `error_*` / snapshot), iol, higyrus and ámbito. market-data is the only mutating surface and is behind the untouched double gate. No probe is added by this plan. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — zero external packages installed; `uv.lock` untouched (`33-RESEARCH.md` `## Package Legitimacy Audit` is empty by construction). No install task, therefore no legitimacy checkpoint. |
</threat_model>

<verification>
- `uv run pytest verification/test_probe_context_coverage.py verification/test_finding_count_consistency.py -q`
- `uv run python scripts/preflight_33.py` — one status line per package, no credential or exception body in the output
- Ten driver invocations (5 observable + 5 strict), each with a recorded SUMMARY transcript
- Per package: handler errors `0`, emitted-fid count equals new `### F-` block count
- matriz and market-data `extra` counts non-zero (the P-1 tell)
- `git status --porcelain packages/` empty — no package source touched
- `git status --porcelain .planning/verification/schemas/` classified entry by entry
- `33-CENSUS.md` present with all seven sections and no `TBD` destination
- `uv run ruff check . && uv run ruff format --check . && uv run mypy verification`
</verification>

<success_criteria>
- All 130 probes proven decorated by an AST gate with a numeric per-driver lower bound and a surface-suffix match
- The P-3 regression class is pinned by a test with a fail-first unseeded control
- Every in-scope package has a recorded live authentication result; no census number exists for a package that did not authenticate
- Both passes completed per authenticating package, with the market-data strict pass provably non-mutating
- `33-CENSUS.md` reports the distinct-triple count per package against the ratified floor, with iol marked as a first measurement and ámbito as an asserted smoke zero
- All five structural findings S-1..S-5 have a disposition backed by live evidence
- Every deferred finding has a named destination that exists in ROADMAP after this commit
- Schema snapshots reconciled: new files explained as write-once, modified files explained individually
</success_criteria>

<output>
Create `.planning/phases/33-verificaci-n-en-vivo-en-modo-estricto-fixes/33-05-SUMMARY.md` when done.
Record: the full pre-flight output; the ten SUMMARY transcripts verbatim; the per-package
distinct-triple, SHAPE-finding, `extra` and handler-error counts; whether the matriz pass-1 ran inside
a trading session; the classified schema-snapshot delta; the full text of `scripts/preflight_33.py` if
it was left uncommitted; and the shortlist of confirmed divergences 33-07 is expected to fix, ordered
by consequence with S-3 named first.
</output>
