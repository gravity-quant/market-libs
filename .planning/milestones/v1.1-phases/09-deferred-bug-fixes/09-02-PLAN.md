---
phase: 09-deferred-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/higyrus-client/src/higyrus_client/_state.py
  - packages/iol-client/src/iol_client/_state.py
  - packages/higyrus-client/tests/test_multi_account.py
  - main_higyrus.py
  - .planning/verification/higyrus-client-findings.md
autonomous: false
requirements: [BUG-02, BUG-04]
requirements_addressed: [BUG-02, BUG-04]
tags: [phase-09, higyrus, iol-cross-pkg, bug-fixes, multi-account, triage, wave-1]
user_setup:
  - service: higyrus
    why: "Live triage BUG-02 + live multi-account iteration BUG-04 requieren API live + ≥2 cuentas reales en HIGYRUS_USER"
    env_vars:
      - name: HIGYRUS_USER
        source: "Higyrus credentials (already in .env)"
      - name: HIGYRUS_PASSWORD
        source: "Higyrus credentials (already in .env)"
      - name: HIGYRUS_SAMPLE_CUENTAS
        source: "Operator-provided CSV `A,B` con ≥2 ids reales; opcional si get_listado_cuentas devuelve ≥2 cuentas live"

must_haves:
  truths:
    - "BUG-04 (D-08 per-call only): los 4 endpoints account-dependent (`get_movimientos`, `get_posiciones`, `get_posicion_valuada`, `get_listado_cuentas`) mantienen `id_cuenta` per-call (status quo Phase 6). NO se introduce `Client(account_id=X)` constructor en Phase 9."
    - "BUG-04 mocked regression: 2 cuentas mockeadas (`5208`, `9999`); loop sobre ambas con `higyrus_client.get_movimientos(id_cuenta=acct, ...)`; assert wire requests tienen `id_cuenta` correcto en URL path (`/api/cuentas/{acct}/movimientos`), exactly 2 outgoing requests, paths distintos."
    - "BUG-04 live regression: nuevo `probe_multi_account_iteration` en `main_higyrus.py` con source order — env var `HIGYRUS_SAMPLE_CUENTAS` (CSV) > live `get_listado_cuentas` primeras 2 > SKIPPED si <2 disponibles. Probe registrado en `_D_HIGY_10_ORDER` después de `get_listado_cuentas_async` para que `_resolved_cuenta` esté disponible cuando aplique."
    - "BUG-02 (D-04 quick triage time-boxed): re-correr `main_higyrus.py` con `logging.getLogger('higyrus_client').setLevel(DEBUG)` probe-scoped (Pattern S8: `try/finally` con `setLevel(original_level)` en finally — Pitfall 5). Outcome → 3 buckets (a/b/c) decididos por operator basado en output."
    - "BUG-02 contract guard mocked test (siempre, indep de bucket): el happy-path mocked test que verifica server-returns-N → client-returns-N como guard de regresiones futuras del happy path client-side. Extendido en `tests/test_client.py` (existing) o nuevo `tests/test_listado_cuentas_regression.py` (solo bucket c)."
    - "D-09 cross-package cleanup: `_state.account_id` field declaration + docstring entries REMOVED de `packages/higyrus-client/src/higyrus_client/_state.py:97-98` y `packages/iol-client/src/iol_client/_state.py:81-84`. Pitfall 1 mitigation: NO se toca `RequestSpec.account_id` (Phase 8 D-11 log correlation — feature distinto)."
    - "Finding `F-02` en `.planning/verification/higyrus-client-findings.md` actualizado con Status (FIXED|NO-FIX|FIXED-by-environment dependiendo del bucket) + `Resolution: <bucket marker (a)/(b)/(c)> + rationale 1-2 lines` + `Regression: <path>::<test>` per FINDINGS-TEMPLATE.md."
    - "Existing 768 tests baseline (760 Phase 8 + 8 Plan 09-01) + new tests Plan 09-02 stay GREEN; snapshot público zero-diff (Phase 6 D-09); cross-leak sentinel (Phase 7 D-10) sigue GREEN."
  artifacts:
    - path: "packages/higyrus-client/tests/test_multi_account.py"
      provides: "Mocked regression test BUG-04: loop sobre 2 cuentas + assert wire URLs distintas"
      contains: "for acct in"
      min_lines: 30
    - path: "main_higyrus.py"
      provides: "Nuevo `probe_multi_account_iteration` + `_SAMPLE_CUENTAS_CSV` env var read + register en `_D_HIGY_10_ORDER`"
      contains: "probe_multi_account_iteration"
    - path: "packages/higyrus-client/src/higyrus_client/_state.py"
      provides: "`account_id` field REMOVED + docstring entry removed"
      contains: "_ClientState"
    - path: "packages/iol-client/src/iol_client/_state.py"
      provides: "`account_id` field REMOVED + docstring entry removed (cross-package D-09)"
      contains: "_ClientState"
    - path: ".planning/verification/higyrus-client-findings.md"
      provides: "F-02 status flip + Resolution: + Regression: lines per Phase 5 Op A convention"
      contains: "F-02"
  key_links:
    - from: "packages/higyrus-client/tests/test_multi_account.py"
      to: "packages/higyrus-client/src/higyrus_client/client.py:347-422 (get_movimientos shell)"
      via: "higyrus_client.get_movimientos(id_cuenta=acct, ...) per-call kwarg"
      pattern: "get_movimientos\\(id_cuenta="
    - from: "main_higyrus.py::probe_multi_account_iteration"
      to: "higyrus_client.get_movimientos / get_listado_cuentas (account-dependent endpoints)"
      via: "for acct in cuentas[:2]: higyrus_client.get_movimientos(id_cuenta=acct, ...)"
      pattern: "probe_multi_account_iteration"
    - from: ".planning/verification/higyrus-client-findings.md F-02"
      to: "packages/higyrus-client/tests/test_multi_account.py + (cond) tests/test_listado_cuentas_regression.py"
      via: "`Regression:` line per FINDINGS-TEMPLATE.md"
      pattern: "Regression: "
---

<objective>
Plan 09-02 cierra **higyrus BUG-02 (quick triage time-boxed)** + **BUG-04
(per-call multi-account iteration)** en un solo commit atómico, con un
cleanup cross-package de `_state.account_id` que toca **iol** además de
higyrus (D-09).

Purpose:
1. BUG-04: validar que el patrón decidido (D-08 per-call only) funciona
   end-to-end con (i) mocked test 2 cuentas + (ii) live probe con override
   env var; remover el field muerto `_state.account_id` que era forward-
   declared para el constructor pattern descartado.
2. BUG-02: ejercer la API live con DEBUG logging probe-scoped para decidir
   bucket (a) transient / (b) FIXED-by-environment / (c) client-side fix.
   Actualizar finding `F-02` con Resolution + Regression según outcome.
3. Cross-package cleanup (D-09): borrar `account_id` de iol + higyrus
   `_state.py` (field + docstring entries) sin tocar `RequestSpec.account_id`
   (Phase 8 D-11) ni `request.extensions["account_id"]` (transport).

Output:
- `packages/higyrus-client/tests/test_multi_account.py` (NEW, mocked test).
- `main_higyrus.py` extendido con `_SAMPLE_CUENTAS_CSV` env var + `probe_multi_account_iteration` + register en `_D_HIGY_10_ORDER`.
- `packages/higyrus-client/src/higyrus_client/_state.py` modified (delete `account_id` field at line 98 + docstring entry at lines 36-37).
- `packages/iol-client/src/iol_client/_state.py` modified (delete `account_id` field at lines 81-84 + docstring updates at lines 25-29).
- `.planning/verification/higyrus-client-findings.md` actualizado con F-02 Resolution + Regression.
- (Conditional bucket c) `packages/higyrus-client/tests/test_listado_cuentas_regression.py`.
- 1 commit atómico per D-12: `fix(higyrus): BUG-02 triage + BUG-04 multi-account regression + remove _state.account_id (BUG-02, BUG-04)`.
- Live re-run operator-driven con paste evidence (manual checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-deferred-bug-fixes/09-CONTEXT.md
@.planning/phases/09-deferred-bug-fixes/09-RESEARCH.md
@.planning/phases/09-deferred-bug-fixes/09-PATTERNS.md
@.planning/phases/09-deferred-bug-fixes/09-VALIDATION.md
@.planning/verification/higyrus-client-findings.md
@.planning/verification/FINDINGS-TEMPLATE.md
@packages/higyrus-client/src/higyrus_client/_state.py
@packages/iol-client/src/iol_client/_state.py
@packages/higyrus-client/src/higyrus_client/_core.py
@packages/higyrus-client/src/higyrus_client/_params.py
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/tests/conftest.py
@packages/higyrus-client/tests/test_client.py
@main_higyrus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-package delete de `_state.account_id` en higyrus + iol (D-09)</name>
  <files>
    packages/higyrus-client/src/higyrus_client/_state.py,
    packages/iol-client/src/iol_client/_state.py
  </files>
  <read_first>
    - packages/higyrus-client/src/higyrus_client/_state.py (todo el archivo — confirmar offsets exactos 36-37 docstring, 97-98 field)
    - packages/iol-client/src/iol_client/_state.py (todo el archivo — confirmar offsets 25-29 docstring, 81-84 field declaration + comment block)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-08, D-09 (decision + scope cross-package + Pitfall 1 distinction `_state.account_id` vs `RequestSpec.account_id`)
    - .planning/phases/09-deferred-bug-fixes/09-RESEARCH.md §"Runtime State Inventory" (lista de líneas a tocar verificada por grep) + §"Common Pitfalls" 1 (cross-cutting field distinction)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"_state.py (MODIFY: remove account_id)" para iol Y higyrus (deltas exactos)
    - packages/higyrus-client/src/higyrus_client/_core.py (verificar que ninguna referencia a `state.account_id` exista — la mayoría son `RequestSpec.account_id` que NO se tocan)
    - packages/iol-client/src/iol_client/_core.py (mismo check)
  </read_first>
  <action>
    Para `packages/higyrus-client/src/higyrus_client/_state.py`:
    - DELETE las 2 líneas del docstring (líneas 36-37) que dicen `- ``account_id``: forward-declared for Phase 9 BUG-04 (multi-account iteration). Unused in Phase 6.`
    - DELETE las 2 líneas del field (líneas 97-98) que dicen `# Forward-declared for Phase 9 BUG-04 (multi-account iteration).` seguido de `account_id: str | None = None`
    - DEJAR intactos los campos restantes (`token`, `token_expires_at`, `http_client`, `token_lock`) y todas las referencias a otros campos.

    Para `packages/iol-client/src/iol_client/_state.py`:
    - DELETE/UPDATE el bloque del docstring (líneas 25-29) que menciona ambos `refresh_token` y `account_id`. Reemplazar por uno que mencione SOLO `refresh_token` (manteniendo la frase sobre Phase 6 `Client.__init__` NO accepting it as kwarg D-13). Texto resultante esperado: `The ``refresh_token`` field is forward-declared for schema consistency across packages (RESEARCH.md Per-Package Divergence Matrix). Phase 6 ``Client.__init__`` does NOT accept it as a kwarg (D-13). ``refresh_token`` is mutated by ``Client.login()`` / ``_refresh()`` internally.`
    - DELETE las 4 líneas del field block (líneas 81-84): comentario de 3 líneas (`# Forward-declared for Phase 9 BUG-04 ...` + `# populated in Phase 6 but kept in the schema for cross-package shape` + `# consistency with higyrus.`) + la línea `account_id: str | None = None`.
    - DEJAR intactos `refresh_token: str | None = None` (línea 80) y los campos restantes (`http_client`, `token_lock`).

    PITFALL 1 ENFORCEMENT (CRITICAL — leer §"Runtime State Inventory" del RESEARCH antes de tocar nada):
    - El grep `account_id` retorna ~30 ocurrencias entre `_state.py`, `_core.py`, `client.py`, `tests/`, `verification/snapshots/`, `main_higyrus.py`. La MAYORÍA son `RequestSpec.account_id` (Phase 8 D-11 log correlation) o `request.extensions["account_id"]` (transport).
    - Sites a tocar: SOLO los 6 sitios identificados — los 2 archivos `_state.py`.
    - NO tocar: `packages/higyrus-client/src/higyrus_client/_core.py:117, 132` (`RequestSpec.account_id`), `_core.py:310, 353, 408` (builders setean `account_id=id_cuenta`), `_transport.py:173-193` y `_atransport.py:102-121` (lee `request.extensions["account_id"]`), `tests/test_transport.py:33-258`, `tests/test_logging.py:166-177` (D-11 tests).
    - Cualquier diff fuera de los 2 archivos `_state.py` indica scope drift — abortar y re-verificar.

    Después del delete: verificar con `grep -n "account_id" packages/higyrus-client/src/higyrus_client/_state.py packages/iol-client/src/iol_client/_state.py` que devuelve 0 hits (excepto si por accidente queda algún whitespace). Luego correr la suite por paquete para confirmar que ningún test pre-existente leía `state.account_id` (research grep verified 0 lecturas runtime; debería pasar limpio).
  </action>
  <verify>
    <automated>grep -c "account_id" packages/higyrus-client/src/higyrus_client/_state.py packages/iol-client/src/iol_client/_state.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "account_id" packages/higyrus-client/src/higyrus_client/_state.py` returns 0.
    - `grep -c "account_id" packages/iol-client/src/iol_client/_state.py` returns 0.
    - `grep -c "refresh_token" packages/iol-client/src/iol_client/_state.py` returns ≥ 1 (el field `refresh_token: str | None = None` línea 80 sigue intacto).
    - `grep -c "token_expires_at" packages/higyrus-client/src/higyrus_client/_state.py` returns ≥ 1 (el field intacto).
    - higyrus suite GREEN: `uv run pytest packages/higyrus-client/tests/ -x --no-header -q` returns 0 (no regression — verifica research A5 "0 lecturas runtime").
    - iol suite GREEN: `uv run pytest packages/iol-client/tests/ -x --no-header -q` returns 0 (incluyendo Plan 09-01 nuevos tests si Task se ejecuta after Plan 09-01 — en paralelo Wave 1 ambos plans cierran independientes).
    - Cross-leak sentinel (Phase 7 D-10) GREEN: `uv run pytest verification/test_sync_async_isolation.py -x` returns 0.
    - Phase 8 logging tests GREEN: `uv run pytest packages/higyrus-client/tests/test_logging.py packages/higyrus-client/tests/test_transport.py -x` returns 0 (verifica que `RequestSpec.account_id` propagation intacta).
    - Ruff + mypy strict clean on both modified files: `uv run ruff check packages/{higyrus,iol}-client/src/*/_state.py && uv run mypy --strict packages/{higyrus,iol}-client/src/*/_state.py` returns 0.
    - Public surface snapshot zero-diff (Phase 6 D-09): `uv run pytest verification/test_public_surface.py -x` returns 0 — el field `_state.account_id` es privado, no debería aparecer en snapshots; si aparece, indica un leak vía reflection (Pitfall 10 investigation block).
  </acceptance_criteria>
  <done>
    Los 2 `_state.py` files no contienen `account_id`; las pruebas existentes
    de higyrus + iol siguen GREEN; cross-leak sentinel + Phase 8 logging
    tests GREEN; snapshot zero-diff confirmado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test_multi_account.py (mocked 2-cuenta loop) + extender main_higyrus.py con probe + env var (BUG-04)</name>
  <files>
    packages/higyrus-client/tests/test_multi_account.py,
    main_higyrus.py
  </files>
  <read_first>
    - packages/higyrus-client/tests/test_client.py:135-145 (`test_get_movimientos_serializa_fechas_dd_mm_yyyy` — analog idiomático: wire URL con `fechaDesde=DD%2FMM%2FYYYY` URL-encoded, `id_cuenta="123"` en path)
    - packages/higyrus-client/tests/conftest.py (autouse fixture configura token; NO modificar)
    - packages/higyrus-client/src/higyrus_client/_params.py:28-37 (`format_date`: `value.strftime("%d/%m/%Y")` → wire format `dd/mm/yyyy`, URL-encoded como `dd%2Fmm%2Fyyyy`)
    - packages/higyrus-client/src/higyrus_client/client.py:340-423 (get_movimientos / get_posiciones / get_posicion_valuada con `id_cuenta` per-call kwarg)
    - main_higyrus.py:1-200 (header + module-level globals: `_PKG`, `_SAMPLE_CUENTA`, `_SAMPLE_TIPO_CUENTA`, `_SAMPLE_NIVEL`, `_D_HIGY_10_ORDER`, `_auth_failed`, `_auth_failure_reason`, `ProbeResult` dataclass)
    - main_higyrus.py:657-820 (`probe_get_listado_cuentas_sync` skeleton — usar para shape del nuevo probe: SKIPPED si auth_failed, try/except, append_finding on FINDING)
    - main_higyrus.py:906-1100 (`probe_get_movimientos_sync` — patrón para invocar get_movimientos con id_cuenta + manejo errores)
    - main_higyrus.py:1300-1460 (lifecycle main() — donde el nuevo probe se registra en results.append() y aparece en summary)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-08, D-10 (per-call only + live regression spec)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"main_higyrus.py (EXTEND)" + §"test_multi_account.py" + §"Shared Pattern S6 ProbeResult shape" (idiom exacto del driver)
    - .planning/phases/09-deferred-bug-fixes/09-RESEARCH.md §"Pattern 4 Multi-Account Driver Probe" + §"Common Operation 3 Multi-account mocked iteration"
  </read_first>
  <behavior>
    - `test_multi_account_iteration_via_per_call_id_cuenta`: 2 cuentas mockeadas, loop, assert `len(httpx_mock.get_requests()) == 2` + `"/5208/" in str(requests[0].url)` + `"/9999/" in str(requests[1].url)`.
    - `probe_multi_account_iteration` en `main_higyrus.py`: SKIPPED si `_auth_failed`; source order — env var `HIGYRUS_SAMPLE_CUENTAS` (CSV) > live `get_listado_cuentas` primeras 2 > SKIPPED si <2 disponibles. Itera `for acct in cuentas[:2]: higyrus_client.get_movimientos(id_cuenta=acct, ...)`. PASS si ambas calls succeed; FINDING + append_finding si HigyrusAPIError; SKIPPED si <2 cuentas.
    - Module-level read del CSV env var: `_SAMPLE_CUENTAS_CSV: str = os.getenv("HIGYRUS_SAMPLE_CUENTAS", "")` después del `_SAMPLE_NIVEL` line (~línea 141).
    - Probe registrado en `_D_HIGY_10_ORDER` tuple después de `get_listado_cuentas_async` (después de la línea 150) y appended a `results` en `main()` después de los probes account-dependent (después de `probe_get_posiciones_async`).
  </behavior>
  <action>
    **PARTE A — `packages/higyrus-client/tests/test_multi_account.py` (NEW):**

    Header: `from __future__ import annotations` mandatory (Pattern S1). Imports: `import datetime as dt`, `import pytest`, `from pytest_httpx import HTTPXMock`, `import higyrus_client`.

    Test único `test_multi_account_iteration_via_per_call_id_cuenta(httpx_mock: HTTPXMock) -> None`. El autouse `_configure_sync` en `tests/conftest.py` ya precarga `token="test-token"`, `token_expires_at=9_999_999_999.0` — no setup adicional necesario.

    **CRÍTICO formato URL (Pitfall PATTERNS.md):**
    `_params.py::format_date` retorna `dd/mm/yyyy` (línea 37: `value.strftime("%d/%m/%Y")`). En URL queda URL-encoded como `dd%2Fmm%2Fyyyy`. El analog `test_client.py:135-145` lo usa así: `?fechaDesde=01%2F01%2F2026&fechaHasta=31%2F01%2F2026`. **Plan 09-02 debe usar exactamente este wire format** — NO `YYYY-MM-DD` (research template usaba ese formato por error; el código real serializa DD/MM/YYYY URL-encoded).

    Setup mocks loop:
    ```text
    for acct in ("5208", "9999"):
        httpx_mock.add_response(
            method="GET",
            url=(
                f"https://api.test/api/cuentas/{acct}/movimientos"
                f"?fechaDesde=13%2F06%2F2026&fechaHasta=13%2F06%2F2026"
            ),
            json=[],
        )
    ```

    Invoke loop:
    ```text
    today = dt.date(2026, 6, 13)
    for acct in ("5208", "9999"):
        higyrus_client.get_movimientos(
            id_cuenta=acct,
            fecha_desde=today,
            fecha_hasta=today,
        )
    ```

    Asserts:
    - `requests = httpx_mock.get_requests()`
    - `len(requests) == 2`
    - `"/5208/" in str(requests[0].url)`
    - `"/9999/" in str(requests[1].url)`

    **PARTE B — `main_higyrus.py` (EXTEND):**

    1. **Module-level env var read** (después de la línea `_SAMPLE_NIVEL` ~línea 141): agregar comentario block + line `_SAMPLE_CUENTAS_CSV: str = os.getenv("HIGYRUS_SAMPLE_CUENTAS", "")`. Comentario referencia D-10 Phase 9 BUG-04.

    2. **Nuevo `probe_multi_account_iteration() -> ProbeResult`** (insertar antes de `def main()`, después de `probe_auth_401`).

    Shape (Pattern S6 + RESEARCH §"Pattern 4"):
    - Si `_auth_failed`: return `ProbeResult("multi_account_iteration", "SKIPPED", f"auth failed: {_auth_failure_reason}")`.
    - Acceder `base_url = higyrus_client.client._base_url` (idiom existente del driver).
    - Source 1: si `_SAMPLE_CUENTAS_CSV.strip()`: parse `cuentas = [c.strip() for c in _SAMPLE_CUENTAS_CSV.split(",") if c.strip()]`.
    - Source 2: si vacío, intentar `live = higyrus_client.get_listado_cuentas(estado="alta")`; try/except `Exception as exc` → `ProbeResult(..., "SKIPPED", f"listado_cuentas failed: {exc!r}")`. Si éxito: `cuentas = [c.id for c in live[:2]] if len(live) >= 2 else []`.
    - Si `len(cuentas) < 2`: return `ProbeResult("multi_account_iteration", "SKIPPED", "need >=2 cuentas; set HIGYRUS_SAMPLE_CUENTAS=A,B")`.
    - Loop `for acct in cuentas[:2]: try: higyrus_client.get_movimientos(id_cuenta=acct, fecha_desde=dt.date.today(), fecha_hasta=dt.date.today())`. Except `HigyrusAPIError as exc`: `fid = _next_fid()` + `append_finding(_PKG, fid=fid, class_="ERROR-MAP", surface="sync", status="OPEN", title=f"multi_account: get_movimientos({acct})", expected="200 OK", actual=repr(exc), diff=f"status={exc.status_code!r}", base_url=base_url)` → `return ProbeResult("multi_account_iteration", "FINDING", f"{fid} (OPEN)")`.
    - Success path: `return ProbeResult("multi_account_iteration", "PASS", f"iterated {len(cuentas[:2])} cuentas successfully")`.

    3. **Register en `_D_HIGY_10_ORDER` tuple** (línea 144-163): agregar `"multi_account_iteration",` como último elemento del tuple. Mantener orden alfabético del tuple roto — `auth_401` queda al final por convención existente; nuevo probe inserted antes de `auth_401` para que la cascade SKIPPED por `_auth_failed` se cumpla antes del 401-probe.

    4. **Append a `results` en `main()`**: ubicar el bloque donde se appendean los probes (cerca de las llamadas a `probe_errors_envelope_sync`, `probe_auth_401` ~líneas 1430-1450). Agregar `results.append(probe_multi_account_iteration())` antes del bloque del cycle_closure (si existe; verificar `main_higyrus.py` lifecycle exact). NOTA: `main_higyrus.py` no tiene un cycle_closure como `main_matriz.py` per CONTEXT.md — registrar antes de la línea final del summary print.

    5. Si la implementación encuentra que `Cuenta.id` no existe (model field puede ser `idCuenta` o similar), grep en `packages/higyrus-client/src/higyrus_client/models.py` para `class Cuenta` y usar el nombre exacto del field (id field en el modelo).

    **No tocar:** otros probes existentes, ProbeResult dataclass, `_fid_counter`, `_resolved_cuenta` global, lifecycle order de probes preexistentes.
  </action>
  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_multi_account.py -x --no-header -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/higyrus-client/tests/test_multi_account.py` exists, starts with `from __future__ import annotations`.
    - 1 test function: `test_multi_account_iteration_via_per_call_id_cuenta`. `grep -c "^def test_" packages/higyrus-client/tests/test_multi_account.py | grep -v '^#'` returns 1.
    - Test uses wire URL format `fechaDesde=DD%2FMM%2FYYYY` (URL-encoded, matching `_params.py::format_date` actual idiom). Verified via `grep -c "fechaDesde=13%2F06%2F2026" packages/higyrus-client/tests/test_multi_account.py | grep -v '^#'` returns ≥ 2 (uno por cuenta).
    - Test asserts paths distintos: `grep -c '"/5208/"' packages/higyrus-client/tests/test_multi_account.py | grep -v '^#'` returns ≥ 1 AND `grep -c '"/9999/"' packages/higyrus-client/tests/test_multi_account.py | grep -v '^#'` returns ≥ 1.
    - Test asserts `len(requests) == 2` (exact, not >=).
    - Test command exit 0: `uv run pytest packages/higyrus-client/tests/test_multi_account.py -x` returns 0.
    - main_higyrus.py modified: `grep -c "_SAMPLE_CUENTAS_CSV" main_higyrus.py | grep -v '^#'` returns ≥ 2 (declaration + usage).
    - main_higyrus.py contains new probe: `grep -c "def probe_multi_account_iteration" main_higyrus.py` returns 1.
    - `_D_HIGY_10_ORDER` tuple updated: `grep -c "multi_account_iteration" main_higyrus.py | grep -v '^#'` returns ≥ 2 (tuple entry + probe function name).
    - Full higyrus suite GREEN: `uv run pytest packages/higyrus-client/tests/ -x --no-header -q` returns 0.
    - Ruff + mypy strict clean on test file + main_higyrus.py: `uv run ruff check packages/higyrus-client/tests/test_multi_account.py main_higyrus.py && uv run mypy --strict packages/higyrus-client/tests/test_multi_account.py` returns 0. Note: `mypy --strict main_higyrus.py` puede fallar por imports root-level — usar `mypy packages/higyrus-client/` para package-only check.
    - `main_higyrus.py` smoke import OK: `uv run python -c "import main_higyrus"` returns 0 (NO HTTPCalls; sin --live; sólo valida que el archivo se importa sin SyntaxError).
  </acceptance_criteria>
  <done>
    Mocked regression test pasa; `main_higyrus.py` extendido con probe + env
    var + register; full higyrus suite GREEN; ruff + mypy clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator-driven live re-run de main_higyrus.py + BUG-02 bucket classification + F-02 finding update</name>
  <files>(no file modification — operator-driven checkpoint)</files>
  <action>
    Pause for operator. Operator performs the steps described in `<how-to-verify>` and returns one of the signals in `<resume-signal>`. No file modification within this task — Plans 09-01..03 implementation tasks own the writes.
  </action>
  <verify>
    <automated>echo "checkpoint pending operator approval — no automated verify"</automated>
  </verify>
  <done>
    Operator returned an approval signal per `<resume-signal>`; the orchestrator records the response in chat / SUMMARY.
  </done>
  <what-built>
    Task 1 + Task 2 ya land: cross-package `_state.account_id` removed,
    `test_multi_account.py` mocked regression GREEN, `main_higyrus.py`
    extendido con `probe_multi_account_iteration` + `_SAMPLE_CUENTAS_CSV`
    env var override. Pre-flight: la suite higyrus + iol está GREEN.
  </what-built>
  <how-to-verify>
    Este checkpoint es OPERATOR-DRIVEN porque combina (a) live API access
    contra Higyrus + (b) decision criteria D-04/D-05 que requieren juicio
    humano basado en el output del live re-run.

    **Step 1 — Pre-flight (operator confirma):**
    1. `.env` raíz tiene `HIGYRUS_USER` + `HIGYRUS_PASSWORD` válidos.
    2. (Opcional) `HIGYRUS_SAMPLE_CUENTAS="A,B"` con ≥2 cuentas reales si
       el operator quiere forzar el override; de lo contrario, el probe
       intenta `live get_listado_cuentas` primero.

    **Step 2 — Live re-run + DEBUG triage BUG-02:**
    Activar DEBUG logging probe-scoped para BUG-02 quick triage. Como
    Plan 09-02 NO introduce un helper context manager
    (defer a v1.2 per CONTEXT.md Deferred Ideas), el operador usa el
    pattern manual:

    ```bash
    HIGYRUS_LOG_LEVEL=DEBUG uv run --package higyrus-client python main_higyrus.py 2>&1 | tee /tmp/main_higyrus_phase9_run1.log
    ```

    NOTA: si `main_higyrus.py` no respeta `HIGYRUS_LOG_LEVEL`, el operador
    puede ejecutar un script ad-hoc previo que active `logging.getLogger
    ("higyrus_client").setLevel(logging.DEBUG)` en Python REPL antes del
    `import main_higyrus`. Si NINGUNA opción está disponible, el operador
    corre el driver con su default (INFO/WARNING) y clasifica el outcome
    sin DEBUG — la bucket decision sigue válida basada en N=3 runs.

    Re-run 3 veces (N=3) para discriminar transient vs persistent:
    ```bash
    for i in 1 2 3; do
      uv run --package higyrus-client python main_higyrus.py > /tmp/main_higyrus_phase9_run${i}.log 2>&1
      echo "--- Run $i ---"
      grep -E "probe_get_listado_cuentas|probe_multi_account_iteration" /tmp/main_higyrus_phase9_run${i}.log
    done
    ```

    **Step 3 — Inspeccionar output, decidir BUG-02 bucket (D-05 criteria):**
    - Empty `[]` retornado 3/3 + Phase 8 logs muestran 200 OK + envelope
      vacío → **bucket (c) reproducible 100% client-side** → fix en
      `_core.py` requerido (escalar a sub-task — extend Plan 09-02 o
      defer); MUY improbable basado en investigación previa.
    - Empty `[]` retornado 0-2/3 (mezcla con non-empty) → **bucket (a)
      transient** → NO-FIX clasificación; Phase 8 retries amortiguan.
    - Non-empty list (`N >= 1`) retornado 3/3 → **bucket (b) FIXED-by-
      environment** → root cause exterior al cliente.
    - HTTP 4xx/5xx unexpected → fuera de scope; documentar como escalación
      v1.2.

    **Step 4 — Inspeccionar output BUG-04 multi-account probe:**
    - `probe_multi_account_iteration` reporta `PASS` para ambas cuentas →
      BUG-04 cierra como FIXED.
    - `SKIPPED need >=2 cuentas`: setear `HIGYRUS_SAMPLE_CUENTAS=A,B` con
      cuentas reales conocidas y re-correr el probe únicamente.
    - `FINDING` con `HigyrusAPIError`: investigar (auth, permisos, scopes).

    **Step 5 — Actualizar `.planning/verification/higyrus-client-findings.md`:**
    Editar F-02 según bucket:
    - **Bucket (a) NO-FIX:** cambiar `Status: OPEN` a `NO-FIX`; agregar
      `Resolution: Phase 9 Plan 09-02 BUG-02 quick triage (bucket a) — re-corrido `main_higyrus.py` N=3 con DEBUG logging; outcome [describir N runs / non-empty count]. Phase 8 D-01 RetryTransport amortigua transients. Contract guard preserva regresiones futuras del happy path.` + `Regression: tests/test_client.py::test_get_listado_cuentas_url_con_estado_alta` (existing).
    - **Bucket (b) FIXED-by-environment:** cambiar `Status` a `FIXED`;
      agregar `Resolution: Phase 9 Plan 09-02 BUG-02 quick triage (bucket b) — re-corrido devolvió N cuentas non-empty 3/3. Root cause exterior al cliente. Contract guard mocked.` + `Regression: tests/test_client.py::test_get_listado_cuentas_url_con_estado_alta`.
    - **Bucket (c) Client-side fix:** STOP — escalar como sub-task (extend
      Plan 09-02 con fix en `_core.py` + nuevo
      `tests/test_listado_cuentas_regression.py`). NO mergear sin el fix.

    Paste el output condensado de los 3 runs como evidence al final del
    finding (en una zona delimitada por `<!-- evidence -->...<!-- /evidence -->`).

    **Step 6 — Resume signal:** Una vez que F-02 está actualizado y el
    operator confirma que el multi-account probe pasa (PASS o SKIPPED con
    razón aceptable), responder con uno de:
    - `approved (bucket a)` — para NO-FIX path.
    - `approved (bucket b)` — para FIXED-by-environment path.
    - `bucket c → extend plan` — para escalar a sub-task adicional.
    - `multi-account FAILED: <reason>` — para investigation block.
  </how-to-verify>
  <resume-signal>
    Responder con `approved (bucket X)` donde X ∈ {a, b}; OR `bucket c →
    extend plan`; OR `multi-account FAILED: <reason>` para investigation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: (Conditional, ejecutar solo si Task 3 retornó bucket (c)) Crear test_listado_cuentas_regression.py + fix en `_core.py`</name>
  <files>
    packages/higyrus-client/tests/test_listado_cuentas_regression.py,
    packages/higyrus-client/src/higyrus_client/_core.py
  </files>
  <read_first>
    - packages/higyrus-client/src/higyrus_client/_core.py:357-384, 487-490 (builder + parser de `get_listado_cuentas`)
    - packages/higyrus-client/tests/test_client.py:130-175 (analog: existing happy-path tests para get_listado_cuentas)
    - .planning/phases/09-deferred-bug-fixes/09-CONTEXT.md §D-04 bucket (c) (client-side root cause path — el fix vive en `_core.py` single-site)
    - .planning/phases/09-deferred-bug-fixes/09-PATTERNS.md §"test_listado_cuentas_regression.py (CONDITIONAL — bucket (c) only)"
    - .planning/verification/higyrus-client-findings.md F-02 (post Task 3 update con bucket (c) outcome)
  </read_first>
  <action>
    **SKIP THIS TASK if Task 3 returned bucket (a) o bucket (b).** Solo
    ejecutar si Task 3 confirmó bucket (c) reproducible 100% con root cause
    en client-side.

    Si bucket (c):
    1. **Fix en `_core.py`:** basado en el output del triage de Task 3, el
       fix vivirá en `build_get_listado_cuentas_request` o
       `parse_get_listado_cuentas_response`. El delta exacto depende del
       output del triage — ejemplo hipotético: si el triage muestra que el
       envelope incluye N cuentas pero el parser las descarta cuando hay
       una key no esperada, fix el parser. Mantener el `idempotent=True`
       en el builder + el shape del parser current.
    2. **Crear `tests/test_listado_cuentas_regression.py`:** mocked test
       que reproduce el repro pre-fix (server returns N → client returns
       [] pre-fix) y assert que post-fix retorna N.
    3. **Actualizar F-02 en findings:** cambiar Resolution con
       `(bucket c)` marker + `Regression: tests/test_listado_cuentas_regression.py::test_<name>`.

    **Atomic commit policy (D-12):** este task se incorpora al commit
    atómico del Plan 09-02. NO commit separado. Si el operator prefiere
    separar (porque el fix es non-trivial), Plan 09-02 se split en 09-02 y
    09-02b — operator decision.
  </action>
  <verify>
    <automated>uv run pytest packages/higyrus-client/tests/test_listado_cuentas_regression.py -x --no-header -q || echo "SKIP: bucket a/b — task no aplica"</automated>
  </verify>
  <acceptance_criteria>
    - If Task 3 returned bucket (c):
      - File `packages/higyrus-client/tests/test_listado_cuentas_regression.py` exists with the regression test.
      - Fix in `packages/higyrus-client/src/higyrus_client/_core.py` is committed.
      - Test command exit 0: `uv run pytest packages/higyrus-client/tests/test_listado_cuentas_regression.py -x`.
      - Full higyrus suite GREEN incluyendo el nuevo regression.
      - F-02 finding includes `Regression: tests/test_listado_cuentas_regression.py::<test_name>`.
    - If Task 3 returned bucket (a) o (b):
      - Task skipped (no files modified, no test created).
      - Commit message reflects only `BUG-02 triage (bucket X)` sin `_core.py` delta.
  </acceptance_criteria>
  <done>
    Either bucket (c) fix landed con tests + finding update, OR task
    skipped por bucket (a)/(b) outcome.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live Higyrus API | DEBUG logging probe-scoped capturará request/response (incluyendo `cuit` query param que es PII). RedactingFilter (Phase 8 D-10) ya redacta `cuit=...` patterns en logs. |
| `HIGYRUS_SAMPLE_CUENTAS` env var → driver | CSV literal sin parsing-driven security; operator controla los valores. Cuentas IDs son non-secret pero potencialmente sensitive si leakean en logs públicos. |
| `_state.py` field cleanup | D-09 elimina field forward-declared sin uso — surface reduction. NO toca `RequestSpec.account_id` ni `request.extensions["account_id"]` (Pitfall 1 enforcement). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Information Disclosure | DEBUG logging captures wire body con cuit/account_id PII | mitigate | RedactingFilter (Phase 8 D-10) activo en `_logging.py`; redacta `cuit=`, `password=`, Bearer header. Probe-scoped DEBUG con `try/finally` (Pattern S8) evita leak cross-test. |
| T-09-02-02 | Authorization Bypass | Multi-account cross-account state leak | mitigate | Per-call `id_cuenta` (D-08): el caller siempre pasa la cuenta explícita. D-09 cleanup elimina field cache que pudo confundir caller futuro. Mocked test valida 2 cuentas distintas → 2 wire requests con paths distintos. |
| T-09-02-03 | Tampering | Cross-package `_state.py` delete confunde con `RequestSpec.account_id` | mitigate | Pitfall 1 explícitamente documented; delete scope limitado a 2 archivos `_state.py`; Phase 8 logging tests + transport tests permanecen GREEN como guard. Acceptance criteria verifican grep zero hits en `_state.py` files únicamente. |
| T-09-02-04 | Information Disclosure | `HIGYRUS_SAMPLE_CUENTAS` CSV en logs/error messages | accept | Cuentas IDs no son secrets (no PII directly), pero operator deberia evitar committear `.env` con CSV poblado. `.gitignore` ya excluye `.env`. |
| T-09-02-05 | Tampering | F-02 finding update overwrite por re-runs futuros del driver | accept | Plan 09-02 manual edit del finding; Phase 11 HARN-07/09 traerá BEGIN/END zone parser + operator-field preservation. Plan 09-02 confía en convention manual. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | NO instala paquetes nuevos. |

Severity overall: **low** — surface reduction (cleanup) + testing + driver
extension; live triage usa controls existentes (RedactingFilter + probe
scope).
</threat_model>

<verification>
## Phase-Level Checks (Plan 09-02)

- **Field removal verified cross-package:** `grep -c "account_id" packages/{higyrus,iol}-client/src/*/_state.py` returns 0 (both files).
- **Pitfall 1 enforcement:** `RequestSpec.account_id` and `request.extensions["account_id"]` references intact; Phase 8 logging + transport tests GREEN.
- **Mocked regression:** `test_multi_account_iteration_via_per_call_id_cuenta` passes with 2 wire requests, distinct paths.
- **Live regression (operator):** `probe_multi_account_iteration` reports PASS or SKIPPED (with valid reason — env var unset and live cuentas <2).
- **BUG-02 triage:** F-02 finding updated with Resolution + bucket marker + (if fix landed) Regression link.
- **Snapshot público zero-diff (Phase 6 D-09):** validated in Plan 09-04 green gate.
- **Existing 768 tests + new ones GREEN:** no regression en sync/async suites.

## Manual Checks

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| BUG-02 live triage bucket classification | Live API + decision criteria require human judgment | Task 3 step-by-step |
| BUG-04 live multi-account iteration | Live API + ≥2 cuentas confirmation | Task 3 Step 4 |
| F-02 finding manual edit | Plan 09-02 pre-dates HARN-07/09 (Phase 11) | Task 3 Step 5 |
</verification>

<success_criteria>
1. Task 1 verified: `account_id` field removed from both `_state.py` files; iol + higyrus existing test suites GREEN; cross-leak + Phase 8 logging tests GREEN.
2. Task 2 verified: `test_multi_account.py` passes (2 cuentas, 2 distinct wire requests); `main_higyrus.py` extended with `_SAMPLE_CUENTAS_CSV` env var + new probe + register in `_D_HIGY_10_ORDER`; ruff + mypy clean.
3. Task 3 operator checkpoint closed: F-02 finding updated with bucket marker (a/b/c) + Resolution + Regression line; multi-account probe live confirmation (PASS o SKIPPED con razón aceptable).
4. Task 4 conditionally complete: either bucket (c) fix + regression test landed, or task skipped per bucket (a)/(b).
5. Full pytest GREEN: `uv run pytest --no-header -q` returns 0 (all packages).
6. 1 atomic commit per D-12: `fix(higyrus): BUG-02 triage + BUG-04 multi-account regression + remove _state.account_id (BUG-02, BUG-04)`.
</success_criteria>

<output>
Create `.planning/phases/09-deferred-bug-fixes/09-02-SUMMARY.md` when done.
Summary must document:
- Files modified (3 source + 1 test + 1 driver + 1 finding) with line counts/deltas.
- Bucket outcome for BUG-02 (a/b/c) with rationale.
- Multi-account probe outcome (PASS/SKIPPED/FINDING).
- 3-run live evidence excerpt (probe_get_listado_cuentas + probe_multi_account_iteration outputs).
- F-02 finding update diff (before/after).
- Test count delta from Plan 09-02 (+N).
- Confirmation: `account_id` zero hits in `_state.py`; `RequestSpec.account_id` intact.
</output>
</content>
</invoke>