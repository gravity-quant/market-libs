---
phase: 26-calendar-write
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/src/market_data_client/_core.py
  - packages/market-data-client/tests/test_core.py
autonomous: true
requirements: [MUT-MD-02]
tags: [market-data-client, request-builders, parser, path-safety, calendar, python]

must_haves:
  truths:
    - "D-01: existen cinco builders puros nuevos en _core.py — build_set_calendar_config_request (PUT /calendar/config), build_delete_calendar_config_request (DELETE /calendar/config), build_preview_calendar_config_request (POST /calendar/config/preview), build_add_holidays_request (POST /calendar/holidays), build_delete_holiday_request (DELETE /calendar/holidays/{day}) — todos authenticated=True y con endpoint_name distinto."
    - "D-02: RequestSpec no cambia estructuralmente; los dos builders DELETE dejan json_body en None (nunca {}), de modo que httpx 0.28.1 emite content b'' y ningún header Content-Type."
    - "D-03: day se interpola RAW en /calendar/holidays/{day} — sin urllib.parse.quote(); el path param es format: date (ISO YYYY-MM-DD) en la OpenAPI en vivo."
    - "D-04 / ROADMAP SC#4: idempotent=True para PUT /calendar/config, DELETE /calendar/config, POST /calendar/config/preview y DELETE /calendar/holidays/{day}; idempotent=False EXPLÍCITO para POST /calendar/holidays (primer consumidor False del paquete)."
    - "D-18: build_delete_holiday_request levanta ValueError si day es vacío o contiene '/', '?', '#' o '..', ANTES de construir el RequestSpec."
    - "D-06/D-07: parse_calendar_write_response(resp) -> dict[str, Any] hace resp.read() → raise_for_response(resp) → decode tolerante: body vacío ⇒ {}, body no-dict (null/list/escalar) ⇒ {}, dict ⇒ el dict tal cual."
    - "D-16: el par de feriados NO reusa parse_calendar_response ni se tipa contra CalendarDay — ese par está roto contra el wire real."
    - "_core sigue IO-free: los cinco builders hacen `del state` y no leen política ni estado vivo."
  prohibitions:
    - "Los dos builders DELETE NO deben pasar json_body={} — verificado que httpx 0.28.1 emite b'{}' más un header Content-Type: application/json con {} y nada con None (26-RESEARCH.md Pitfall 1)."
    - "build_add_holidays_request NO debe omitir el kwarg idempotent aunque RequestSpec.idempotent ya defaultee a False — escribirlo explícito, es load-bearing (D-04) y el default implícito lo haría invisible en el diff."
    - "El parser nuevo NO debe reusar parse_health_response: sobre un body vacío levanta json.JSONDecodeError (error crudo de stdlib, fuera de la jerarquía MarketDataError) y su anotación miente para null/list."
    - "El código nuevo NO debe referenciar el modelo de día de calendario existente ni su parser de lectura (par roto, D-16); el retorno del par de feriados es dict[str, Any]."
    - "NO tocar el gate, _ClientState, exceptions.py ni el transporte — Phase 26 los consume sin cambios."
    - "NO agregar chequeos de política ni IO dentro de _core (boundary IO-free por convención; import-linter no cubre este paquete, X8)."
  artifacts:
    - path: "packages/market-data-client/src/market_data_client/_core.py"
      provides: "5 builders puros de calendar write + parse_calendar_write_response + 6 entradas nuevas en __all__"
      contains: "def build_set_calendar_config_request"
    - path: "packages/market-data-client/tests/test_core.py"
      provides: "5 specs de builder (method/path/json_body/idempotent/authenticated/endpoint_name), state-independence, guard D-18 y tolerancia del parser passthrough"
      contains: "build_delete_holiday_request"
  key_links:
    - from: "packages/market-data-client/src/market_data_client/_core.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "los builders con body reciben el dict ya serializado por el to_dict() del modelo (precedente Phase 25, NO el de Phase 20)"
      pattern: "json_body"
    - from: "packages/market-data-client/src/market_data_client/_core.py"
      to: "packages/market-data-client/src/market_data_client/_transport.py"
      via: "spec.idempotent llega a request.extensions['idempotent'] y corta el loop de retry en la primera línea de handle_request"
      pattern: "idempotent"
    - from: "packages/market-data-client/src/market_data_client/_core.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "el parser nuevo reusa raise_for_response para el mapeo 401/403/429/422"
      pattern: "raise_for_response\\(resp\\)"
---

<objective>
Construir la capa pura e IO-free de MUT-MD-02 en `_core.py`: los cinco builders que traducen
(estado, payload ya serializado) → `RequestSpec` con el método, path, idempotencia y
`endpoint_name` correctos por endpoint, el guard de path-safety D-18 sobre `day`, y un parser
passthrough tolerante nuevo para el par de feriados.

Purpose: son funciones puras con I/O totalmente definido — target TDD ideal — y no tocan el gate,
los shells ni el transporte. Al tocar sólo `_core.py`/`test_core.py` corren en Wave 1 en paralelo
con los request-models (Plan 01).

Output: cinco builders (cuatro `idempotent=True` y uno `idempotent=False`), dos de ellos sin
`json_body`, uno con guard de seguridad de path; un parser `dict[str, Any]` tolerante a body
vacío/`null`/no-dict; seis entradas nuevas en `_core.__all__`; y tests de spec, state-independence,
guard y tolerancia.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/26-calendar-write/26-CONTEXT.md
@.planning/phases/26-calendar-write/26-RESEARCH.md
@.planning/phases/26-calendar-write/26-PATTERNS.md
@.planning/phases/26-calendar-write/26-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces

> Roster completo de Phase 26 (los 4 planes) — ninguno de estos símbolos existe hoy en el repo
> (verificado por grep, 26-RESEARCH.md § Runtime State Inventory). El pass de source-grounding de
> `plan-review-convergence` debe excluirlos de la verificación de drift.

**Builders nuevos en `_core.py`** (este plan): `build_set_calendar_config_request`,
`build_delete_calendar_config_request`, `build_preview_calendar_config_request`,
`build_add_holidays_request`, `build_delete_holiday_request`.

**Parser nuevo en `_core.py`** (este plan): `parse_calendar_write_response`.

**Request models nuevos en `models.py`** (Plan 01): `MarketHoursIn`, `HolidayIn`, `HolidaysIn`.

**Métodos nuevos en `client.py`** (Plan 03): `Client.set_calendar_config`,
`Client.delete_calendar_config`, `Client.preview_calendar_config`, `Client.add_holidays`,
`Client.delete_holiday`.

**Métodos nuevos en `aio.py`** (Plan 03): `AsyncClient.set_calendar_config`,
`AsyncClient.delete_calendar_config`, `AsyncClient.preview_calendar_config`,
`AsyncClient.add_holidays`, `AsyncClient.delete_holiday`.

**Shims module-level nuevos** (Plan 03): los cinco nombres en `client.py` (sync) y los cinco en
`aio.py` (async) — 10 funciones.

**Entradas nuevas de `__all__`:** `_core.__all__` +6 (este plan); `models.__all__` +3 (Plan 01);
`__init__.__all__` +8 (Plan 04).

**Archivos de test nuevos** (Plan 03): `tests/test_calendar_write.py`,
`tests/test_calendar_write_async.py`.

**Archivos de test extendidos:** `tests/test_core.py` (este plan), `tests/test_models.py`
(Plan 01), `tests/test_public_surface_market_data.py` (Plan 04).

**Sin cambios (consumo puro):** `_state.py`, `exceptions.py`, `_transport.py`, `_atransport.py`,
`_params.py`, `_logging.py`, `tests/conftest.py`, `pyproject.toml`, `uv.lock`, `__version__`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cuatro builders de calendar write (config trio + add_holidays)</name>
  <files>packages/market-data-client/src/market_data_client/_core.py, packages/market-data-client/tests/test_core.py</files>
  <behavior>
    - Test: build_set_calendar_config_request(_ClientState(), body) → RequestSpec(method="PUT", path="/calendar/config", json_body=body, idempotent=True, authenticated=True, endpoint_name="set_calendar_config").
    - Test: build_preview_calendar_config_request(_ClientState(), body) → method="POST", path="/calendar/config/preview", json_body=body, idempotent=True, authenticated=True, endpoint_name="preview_calendar_config".
    - Test: build_add_holidays_request(_ClientState(), {"days": [...]}) → method="POST", path="/calendar/holidays", json_body=el dict, authenticated=True, endpoint_name="add_holidays" y `spec.idempotent is False` (la ÚNICA aserción de builder `False` del paquete).
    - Test: build_delete_calendar_config_request(_ClientState()) → method="DELETE", path="/calendar/config", idempotent=True, authenticated=True, endpoint_name="delete_calendar_config" y `spec.json_body is None` (D-02).
    - Test: los cuatro son state-independent — llamarlos con un _ClientState() fresco y con uno configurado (base_url/token distintos) produce specs idénticos para el mismo payload.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/_core.py:71-98 (`__all__` ordenado ASCII, 26 nombres), :104-130 (`RequestSpec` — ya carga method/path/params/headers/json_body/data/idempotent/endpoint_name/authenticated; `idempotent` defaultea a `False`; SIN cambio estructural per D-02), :394-409 (`build_create_symbol_request` — template EXACTO de builder con body: docstring, `del state`, kwargs), :412-426 (`build_create_symbols_request` — mismo shape, otro path), :502-515 (`build_segments_request` — template zero-kwarg: `params` y `json_body` quedan en su default), :567-580 (`build_calendar_config_request` — el vecino GET del mismo recurso; copiar su docstring/shape).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:41-115 (secciones «builders con body ya serializado» y «builder zero-kwarg sin body», con excerpts verbatim y la tabla de valores a instanciar), :235-241 (orden alfabético a respetar en `_core.__all__`).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:263-275 (tabla de los 5 endpoints verificados contra la OpenAPI en vivo), :617-635 (estado exacto de `RequestSpec` + la advertencia de NO omitir `idempotent=False`), :694-701 (Pitfall 1 — `json_body={}` emite `b"{}"` + `Content-Type`), :790 (X10 — seguir el precedente Phase 25 de serializar en el shell, NO el de Phase 20 que serializa dentro del builder).
    - packages/market-data-client/tests/test_core.py:323-346 (los dos tests de spec de builder de Phase 25 — template exacto a copiar, incluido el `state = _ClientState()`).
  </read_first>
  <action>
    Escribí PRIMERO los tests de spec que fallan en `tests/test_core.py` (los casos de `<behavior>`), después implementá.

    En `_core.py`, agregá una sección nueva de builders de calendar write con un divisor
    `# ---...---` al estilo del archivo, ubicada junto a `build_calendar_config_request`
    (`_core.py:567-580`). Los cuatro builders de esta task siguen el template de
    `build_create_symbol_request` (`_core.py:394-409`): docstring de una línea con el endpoint en
    formato rst-backtick, `del state  # state-independent (payload comes via json_body)` como
    primera sentencia del cuerpo, y un `return RequestSpec(...)` con kwargs explícitos.

    Firmas y valores concretos (paths e `idempotent` LOCKED por D-01/D-04):
    - `build_set_calendar_config_request(state: _ClientState, json_body: dict[str, Any]) -> RequestSpec` → `method="PUT"`, `path="/calendar/config"`, `json_body=json_body`, `idempotent=True`, `endpoint_name="set_calendar_config"`, `authenticated=True`.
    - `build_preview_calendar_config_request(state: _ClientState, json_body: dict[str, Any]) -> RequestSpec` → `method="POST"`, `path="/calendar/config/preview"`, `json_body=json_body`, `idempotent=True`, `endpoint_name="preview_calendar_config"`, `authenticated=True`.
    - `build_add_holidays_request(state: _ClientState, json_body: dict[str, Any]) -> RequestSpec` → `method="POST"`, `path="/calendar/holidays"`, `json_body=json_body`, `idempotent=False` ESCRITO EXPLÍCITO, `endpoint_name="add_holidays"`, `authenticated=True`. Es el primer consumidor `idempotent=False` del paquete: el docstring debe decir que un append de feriados no es idempotente (DM-03 / D-04) y que por eso `RetryTransport` no lo reintenta.
    - `build_delete_calendar_config_request(state: _ClientState) -> RequestSpec` → template zero-kwarg de `build_segments_request`: `del state  # state-independent`, `method="DELETE"`, `path="/calendar/config"`, `idempotent=True`, `endpoint_name="delete_calendar_config"`, `authenticated=True`. El kwarg `json_body` se OMITE por completo para que quede en su default `None`.

    El payload llega YA serializado desde el shell (precedente Phase 25); no llames `to_dict()`
    adentro del builder — el builder de Phase 20 `build_latest_batch_request` (`_core.py:364`) hace
    lo contrario y NO es el precedente a seguir (26-PATTERNS.md X10).

    No agregues todavía las entradas de `__all__` — se hacen todas juntas en la Task 3.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q` sale 0 con los 5 casos de `<behavior>` en verde.
    - `_core.py` contiene `def build_set_calendar_config_request(`, `def build_preview_calendar_config_request(`, `def build_add_holidays_request(` y `def build_delete_calendar_config_request(`.
    - `python -c "from market_data_client import _core; from market_data_client._state import _ClientState as S; s=_core.build_add_holidays_request(S(), {'days':[]}); assert s.idempotent is False and s.method=='POST' and s.path=='/calendar/holidays' and s.endpoint_name=='add_holidays' and s.authenticated is True"` sale 0.
    - `python -c "from market_data_client import _core; from market_data_client._state import _ClientState as S; s=_core.build_delete_calendar_config_request(S()); assert s.json_body is None and s.method=='DELETE' and s.path=='/calendar/config' and s.idempotent is True"` sale 0.
    - `python -c "from market_data_client import _core; from market_data_client._state import _ClientState as S; s=_core.build_set_calendar_config_request(S(), {'x':1}); assert s.method=='PUT' and s.path=='/calendar/config' and s.json_body=={'x':1}"` sale 0.
    - Los cuatro cuerpos contienen `del state` como primera sentencia después del docstring.
    - `uv run mypy packages/market-data-client/src` sale 0.
    - `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` salen 0.
  </acceptance_criteria>
  <done>Cuatro builders puros existen con method/path/endpoint_name correctos, la idempotencia asignada per DM-03 (incluido el único `False` del paquete, explícito) y el DELETE de config sin body ni `Content-Type`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_delete_holiday_request + guard de path-safety D-18</name>
  <files>packages/market-data-client/src/market_data_client/_core.py, packages/market-data-client/tests/test_core.py</files>
  <behavior>
    - Test: build_delete_holiday_request(_ClientState(), "2026-12-25") → RequestSpec(method="DELETE", path="/calendar/holidays/2026-12-25", idempotent=True, authenticated=True, endpoint_name="delete_holiday") y `spec.json_body is None`.
    - Test: la interpolación es RAW — un day ISO legítimo produce el path byte a byte igual, sin percent-encoding (D-03).
    - Test (D-18): build_delete_holiday_request(_ClientState(), "../config") levanta ValueError.
    - Test (D-18): build_delete_holiday_request(_ClientState(), "") levanta ValueError.
    - Test (D-18): build_delete_holiday_request(_ClientState(), "a/b") levanta ValueError.
    - Test (D-18): build_delete_holiday_request(_ClientState(), "2026-12-25?x=1") levanta ValueError.
    - Test (D-18): build_delete_holiday_request(_ClientState(), "2026-12-25#frag") levanta ValueError.
    - Test (D-13 sigue vigente): build_delete_holiday_request(_ClientState(), "2026-13-45") NO levanta — pasa el guard y produce path "/calendar/holidays/2026-13-45" (el formato lo valida el 422 del servidor, no el cliente).
    - Test: el error del guard es ValueError pelado, no una subclase de MarketDataError.
    - Test: el builder es state-independent.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/_core.py:429-447 (`build_update_symbol_request` — el ÚNICO template de builder con path-param del paquete; nótese que interpola `symbol_id` RAW y documenta la falta de encoding como deuda diferida: ese es exactamente el precedente que D-18 prohíbe repetir), :104-130 (`RequestSpec`).
    - packages/market-data-client/src/market_data_client/models.py:216-238 (`NewSymbols.__post_init__` — el único precedente de FORMA para un `ValueError` pelado de validación client-side; la jerarquía `MarketData*` queda reservada a errores de contrato del servidor).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:118-164 (sección «builder con path-param + guard D-18», con el excerpt del análogo y el bloque «SIN ANÁLOGO EN EL REPO» que enumera el guard línea por línea), :769-773 (tabla «No Analog Found»).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:1058-1080 (Open Question 1 — la verificación con httpx 0.28.1 de que `day="../config"` normaliza a `DELETE /api/calendar/config` y `day="X?a=1"` inyecta query string), :1036 (fila del threat register), :271 (el path param en la OpenAPI en vivo: `{"type":"string","format":"date"}`).
    - .planning/phases/26-calendar-write/26-CONTEXT.md:133-147 (D-18 completo — decisión de operator tomada en plan-phase, con la nota de que el guard es estrictamente más angosto que `urllib.parse.quote()` así que D-03 sigue valiendo, y que no es validación de formato así que D-13 sigue valiendo).
    - packages/market-data-client/tests/test_core.py:347-360 (test de spec del builder con path-param de Phase 25).
  </read_first>
  <action>
    Escribí PRIMERO los tests que fallan (los casos de `<behavior>`, incluidos los seis del guard),
    después implementá.

    En `_core.py`, junto a los builders de la Task 1, agregá
    `build_delete_holiday_request(state: _ClientState, day: str) -> RequestSpec`. Estructura del
    cuerpo, en este orden exacto:
    1. Docstring: endpoint `DELETE /calendar/holidays/{day}`, `idempotent=True` (DM-03),
       `authenticated=True`, retorno passthrough; y un párrafo que explique el guard D-18 y por qué
       el `422` del servidor no alcanza como mitigación (el request nunca llega al endpoint que lo
       validaría).
    2. El guard D-18: si `day` es falsy, o si contiene alguno de los cuatro tokens `/`, `?`, `#` o
       `..`, levantar un `ValueError` PELADO (no `MarketDataError` ni subclase). Implementalo como
       una comprobación explícita de esos cuatro tokens sobre el string — NO uses
       `urllib.parse.quote()` ni ningún escape: el guard debe RECHAZAR, no sanitizar, para que
       D-03 siga valiendo byte a byte para toda fecha ISO legítima. El mensaje debe nombrar el
       parámetro `day` y el valor rechazado, y NO debe incluir credenciales ni el `base_url`.
    3. `del state  # state-independent`.
    4. `return RequestSpec(method="DELETE", path=f"/calendar/holidays/{day}", idempotent=True, endpoint_name="delete_holiday", authenticated=True)` — el kwarg `json_body` se OMITE (default `None`, D-02).

    El guard NO valida formato: `"2026-13-45"` debe pasar y llegar al `422` del servidor (D-13). No
    agregues regex de fecha ni chequeo de longitud.

    No agregues todavía las entradas de `__all__` — Task 3.
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q` sale 0 con los 10 casos de `<behavior>` en verde.
    - `python -c "from market_data_client import _core; from market_data_client._state import _ClientState as S; s=_core.build_delete_holiday_request(S(), '2026-12-25'); assert s.method=='DELETE' and s.path=='/calendar/holidays/2026-12-25' and s.json_body is None and s.idempotent is True and s.endpoint_name=='delete_holiday'"` sale 0.
    - Para cada uno de los cinco inputs hostiles (`""`, `"../config"`, `"a/b"`, `"2026-12-25?x=1"`, `"2026-12-25#frag"`) el builder termina con `ValueError` — asertado por un test parametrizado o por cinco casos explícitos con `pytest.raises(ValueError)`.
    - `python -c "from market_data_client import _core; from market_data_client._state import _ClientState as S; s=_core.build_delete_holiday_request(S(), '2026-13-45'); assert s.path=='/calendar/holidays/2026-13-45'"` sale 0 (D-13 intacto).
    - Un test asserta que el error del guard cumple `type(exc) is ValueError` y `not isinstance(exc, MarketDataError)`.
    - `_core.py` NO contiene ninguna llamada a `urllib.parse.quote` (el guard rechaza, no sanitiza).
    - `uv run mypy packages/market-data-client/src` sale 0.
  </acceptance_criteria>
  <done>El builder de borrado de feriado existe con `json_body` ausente e `idempotent=True`, y ningún `day` capaz de escapar el segmento de path llega a construir un `RequestSpec`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: parse_calendar_write_response (passthrough tolerante) + _core.__all__</name>
  <files>packages/market-data-client/src/market_data_client/_core.py, packages/market-data-client/tests/test_core.py</files>
  <behavior>
    - Test: sobre un 200 con body b'{"deleted": true, "count": 1}' retorna {"deleted": True, "count": 1}.
    - Test: sobre un 200 con body vacío (b"") retorna {} — NO levanta json.JSONDecodeError.
    - Test: sobre un 200 con body b"null" retorna {}.
    - Test: sobre un 200 con body b"[]" (lista) retorna {}.
    - Test: sobre un 200 con body b'"texto"' (escalar) retorna {}.
    - Test: sobre un 422 levanta MarketDataAPIError (vía raise_for_response, sin manejo nuevo).
    - Test: sobre un 401 levanta MarketDataAuthError y sobre un 429 MarketDataRateLimitError (el orden body-consume-then-raise se preserva).
    - Test: los seis nombres nuevos están en _core.__all__ y __all__ sigue ordenado.
  </behavior>
  <read_first>
    - packages/market-data-client/src/market_data_client/_core.py:733-746 (`parse_calendar_config_response` — el análogo CORRECTO para la tolerancia: `resp.read()` → `raise_for_response(resp)` → guard `if not resp.content` → decode), :700-713 (`parse_symbols_response` — de acá sale el guard de colección/`isinstance`), :273-278 (`parse_health_response` — copiar SÓLO el orden body-consume-then-raise; NO es tolerante: sobre body vacío levanta `json.JSONDecodeError` y su anotación miente para `null`/lista), :138-150 (`raise_for_response` — mapeo 401/403 → Auth, 429 → RateLimit, resto is_error → APIError; sin cambios), :716-730 (`parse_calendar_response` — el parser ROTO contra el wire real; leerlo sólo para NO reusarlo, D-16), :71-98 (`__all__`).
    - .planning/phases/26-calendar-write/26-PATTERNS.md:168-231 (secciones «parser reusado verbatim» y «parser passthrough tolerante NUEVO», con el shape propuesto línea por línea y la advertencia sobre el análogo que CONTEXT.md nombra mal), :235-241 (orden alfabético de `__all__`).
    - .planning/phases/26-calendar-write/26-RESEARCH.md:549-575 (Pattern 6 con la tabla de comportamiento medido de `parse_health_response`), :703-712 (Pitfall 2), :714-723 (Pitfall 3 — por qué el par de feriados no se tipa contra el modelo de día de calendario), :765-771 (Pitfall 8 — `__all__` desordenado dispara `RUF022`).
    - packages/market-data-client/tests/test_core.py (buscar los tests existentes de parsers para copiar cómo se fabrica un `httpx.Response` sintético con `content=`/`json=` y `request=`).
  </read_first>
  <action>
    Escribí PRIMERO los tests de tolerancia que fallan, después implementá.

    En `_core.py` agregá `parse_calendar_write_response(resp: httpx.Response) -> dict[str, Any]`,
    ubicado junto a `parse_calendar_config_response` (`_core.py:733-746`). Cuerpo, en este orden
    exacto (D-07): `resp.read()` → `raise_for_response(resp)` → si `not resp.content` retornar un
    dict vacío → `raw = resp.json()` → si `raw` no es una instancia de `dict` retornar un dict
    vacío → retornar `raw`. Es una función NUEVA: no reuses `parse_health_response` (levanta sobre
    body vacío) ni `parse_calendar_response` (roto contra el wire real). Una sola función sirve para
    `add_holidays` y `delete_holiday` — mismo contrato, misma tolerancia, menos superficie
    (resuelve la discreción de CONTEXT.md sobre «uno o dos» a favor de uno).

    Docstring: citar D-06/D-07, aclarar que la OpenAPI en vivo declara los cinco `200` de calendar
    write como `object` sin schema, y que por eso el retorno es un passthrough tolerante hasta que
    Phase 27 (LIVE-MUT-01) capture la forma real. Documentá también que el trío de config NO usa
    este parser: reusa `parse_calendar_config_response` sin modificar (D-05).

    Extendé `_core.__all__` (`_core.py:71-98`) insertando en orden ASCII los SEIS nombres nuevos —
    no al final (ruff `RUF022`): `"build_add_holidays_request"` (primero de los `build_*`, antes de
    `build_calendar_config_request`), `"build_delete_calendar_config_request"` y
    `"build_delete_holiday_request"` (después de `build_create_symbols_request`, en ese orden entre
    sí), `"build_preview_calendar_config_request"` (entre `build_market_data_request` y
    `build_segments_request`), `"build_set_calendar_config_request"` (entre `build_segments_request`
    y `build_symbols_request`) y `"parse_calendar_write_response"` (después de
    `parse_calendar_response`, antes de `parse_health_response`).
  </action>
  <verify>
    <automated>uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --package market-data-client pytest packages/market-data-client/tests/test_core.py -q` sale 0 con los 8 casos de `<behavior>` en verde.
    - `_core.py` contiene `def parse_calendar_write_response(resp: httpx.Response) -> dict[str, Any]:`.
    - Un test construye un `httpx.Response(200, content=b"")` y asserta que el parser retorna `{}` sin levantar (el mismo input contra `parse_health_response` levantaría `json.JSONDecodeError`).
    - Un test construye un `httpx.Response(200, json=None)` y otro `httpx.Response(200, json=[])` y ambos assertan retorno `{}`.
    - `python -c "from market_data_client import _core; want={'build_add_holidays_request','build_delete_calendar_config_request','build_delete_holiday_request','build_preview_calendar_config_request','build_set_calendar_config_request','parse_calendar_write_response'}; assert want <= set(_core.__all__); assert list(_core.__all__) == sorted(_core.__all__)"` sale 0.
    - `uv run ruff check packages/market-data-client` sale 0 (sin `RUF022`).
    - `uv run mypy packages/market-data-client/src` sale 0.
    - `uv run --package market-data-client pytest packages/market-data-client/tests -q` sale 0 (suite completa del paquete, sin regresiones sobre el baseline de 191).
  </acceptance_criteria>
  <done>Existe un parser passthrough tolerante que degrada a un dict vacío ante cualquier body no-dict o ausente y sigue mapeando 401/403/429/422 por `raise_for_response`, y los seis símbolos nuevos están exportados en orden en `_core.__all__`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `day` provisto por el consumidor → path del request | Un string no confiable se interpola dentro de la URL que decide QUÉ endpoint se ejecuta. |
| builder → transporte | `spec.idempotent` viaja a `request.extensions["idempotent"]` y decide si `RetryTransport` puede repetir una operación mutante. |
| respuesta del servidor → dict del consumidor | Un body arbitrario (los cinco `200` son `object` sin schema en la OpenAPI) cruza al tipo de retorno público. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-26-01** | **Tampering** | **`build_delete_holiday_request` — interpolación RAW de `day` en `/calendar/holidays/{day}`** | **mitigate** | **Guard D-18: `ValueError` pelado si `day` es vacío o contiene `/`, `?`, `#` o `..`, ANTES de construir el `RequestSpec`. Verificado contra httpx 0.28.1 (26-RESEARCH.md Open Question 1): sin el guard, `day="../config"` normaliza a `DELETE /api/calendar/config` — un **reset de la configuración de mercado**, otro endpoint de esta misma fase — y `day="X?a=1"` inyecta query string; el `422` del servidor nunca se ejerce porque el request nunca llega al endpoint que lo validaría. Testeable acá: los cinco casos hostiles de la Task 2 con `pytest.raises(ValueError)`. La prueba end-to-end de CERO requests HTTP para `"../config"` vive en el Plan 04 Task 1 (requiere el método del shell, que existe recién en Wave 2).** |
| T-26-07 | Tampering | feriado duplicado por reintento de un POST no idempotente (`POST /calendar/holidays`) | mitigate | `idempotent=False` explícito en `build_add_holidays_request` ⇒ `RetryTransport.handle_request` corta en su primera línea y no reintenta. Testeable acá a nivel builder (`spec.idempotent is False`); la prueba dispatch-level de exactamente 1 request contra 503 repetido vive en el Plan 04 Task 2 (D-15). |
| T-26-08 | Tampering | un DELETE que emite un body JSON inesperado y el servidor lo interpreta | mitigate | Los dos builders DELETE omiten `json_body` (default `None`): verificado que httpx 0.28.1 emite `content == b""` y ningún `Content-Type`, mientras que `json={}` emitiría `b"{}"` + `Content-Type: application/json`. Testeable acá (`spec.json_body is None`) y en el wire en el Plan 03. |
| T-26-13 | Denial of Service | un `200` con body vacío o de forma inesperada aborta el flujo del consumidor con un error crudo de stdlib | mitigate | `parse_calendar_write_response` degrada a un dict vacío ante body ausente o no-dict en lugar de levantar `json.JSONDecodeError`; los errores de transporte siguen mapeados por `raise_for_response`. Testeable: los cinco casos de tolerancia de la Task 3. |
| T-26-14 | Information Disclosure | el mensaje del `ValueError` del guard filtra credenciales, token o `base_url` | mitigate | El mensaje nombra sólo el parámetro `day` y su valor; el builder hace `del state` y no tiene acceso a nada más. Testeable: aserción de que el mensaje no contiene el `base_url` ni el token del state configurado. |
| T-26-SC | Tampering | instalaciones npm/pip/cargo | accept | Phase 26 no instala ningún paquete — todo el código nuevo usa stdlib + deps ya en `uv.lock` (26-RESEARCH.md § Package Legitimacy Audit: no aplica; `uv lock --check` sigue verde sin cambios). |
</threat_model>

<verification>
- `uv run --package market-data-client pytest packages/market-data-client/tests -q` en verde (baseline 191 + los nuevos).
- `uv run ruff check packages/market-data-client && uv run ruff format --check packages/market-data-client` limpios (incluye `RUF022` sobre `_core.__all__`).
- `uv run mypy packages/market-data-client/src` sale 0 (el gate global `uv run mypy` NO cubre este paquete — 26-RESEARCH.md X6).
- Grep de disciplina IO-free: los cinco builders nuevos contienen `del state` y ninguna llamada a `httpx.`, `self.` ni `_ensure_`.
- Grep de disciplina D-18: `_core.py` no contiene `urllib` — el guard rechaza en lugar de sanitizar, de modo que D-03 sigue valiendo byte a byte.
</verification>

<success_criteria>
La capa pura de MUT-MD-02 está completa: cinco builders con method/path/`endpoint_name` correctos
y la idempotencia asignada per DM-03 (ROADMAP SC#4), los dos DELETE sin body ni `Content-Type`, el
guard de path-safety D-18 cerrando el retargeting de request verificado, y un parser passthrough
tolerante que nunca levanta un error crudo de stdlib; tests unitarios verdes y gates del paquete
limpios.
</success_criteria>

<output>
Create `.planning/phases/26-calendar-write/26-02-SUMMARY.md` when done.
</output>
