---
phase: 45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - main_iol.py
  - main_higyrus.py
  - main_matriz.py
  - main_ambito_financiero.py
  - verification/test_drift_dedupe_falsification.py
autonomous: true
requirements: [HARN-01]

must_haves:
  truths:
    - "Los 7 sitios de drift de D-02 consultan la guarda de dedupe ANTES de llamar a `_next_fid()` (D-03), en los 5 drivers."
    - "El no-op de cada sitio respeta el contrato de retorno de SU función: `return` desnudo, tupla `(\"PASS\", …)`, `ProbeResult(…, \"PASS\", …)` o `continue` — nunca un copy-paste uniforme."
    - "Un no-op en los 2 sitios de type drift de iol no acumula en `finding_fids`."
    - "Un lock por AST prueba las dos propiedades anteriores de forma permanente, no sólo en la corrida de esta fase."
  artifacts:
    - path: "main_iol.py"
      provides: "`_seen_drift_keys` + `_drift_digest` + guarda en 3 sitios (schema drift, type drift ×2)"
      contains: "_seen_drift_keys"
    - path: "main_higyrus.py"
      provides: "`_seen_drift_keys` + `_drift_digest` + guarda con no-op de tupla PASS"
      contains: "_seen_drift_keys"
    - path: "main_matriz.py"
      provides: "`_seen_drift_keys` + `_drift_digest` + guarda cuya identidad es el nombre del baseline segregado por venue"
      contains: "_seen_drift_keys"
    - path: "main_ambito_financiero.py"
      provides: "`_seen_drift_keys` + `_drift_digest` + guarda con no-op `ProbeResult` PASS"
      contains: "_seen_drift_keys"
    - path: "verification/test_drift_dedupe_falsification.py"
      provides: "Arms 4-6: lock AST de orden en 7 sitios, lock AST de forma de no-op por sitio, arm runtime del contrato de tupla"
      contains: "ast"
  key_links:
    - from: "verification/test_drift_dedupe_falsification.py"
      to: "los 5 drivers `main_*.py` de la raíz"
      via: "`ast.parse` sobre `Path(main_X.__file__)` — precedente `test_main_matriz_risk_envelope_keys.py`"
      pattern: "ast\\.parse"
    - from: "main_iol.py sitios de type drift"
      to: "`finding_fids` del `ProbeResult` final"
      via: "el no-op hace `continue` sin acumular el fid"
      pattern: "finding_fids"
---

<objective>
Extender la guarda de dedupe de `45-02` a los **6 sitios restantes** de D-02 —`main_iol.py` (schema drift + 2 type drift), `main_higyrus.py`, `main_matriz.py`, `main_ambito_financiero.py`— **con el no-op correcto para el contrato de retorno de cada uno**, y convertir esa corrección en un lock permanente por AST dentro del test de falsificación de D-04.

Purpose: los 7 sitios NO son homogéneos. Tres de ellos devuelven `tuple[str, str]`, uno devuelve `ProbeResult`, dos son ramas inline de un bucle que acumula `finding_fids`, y uno devuelve `None`. Un fix copy-paste rompe al menos tres. El lock por AST es lo que impide que el próximo editor los vuelva a uniformar.
Output: los 4 drivers restantes con guarda, y `verification/test_drift_dedupe_falsification.py` con 6 arms (3 de runtime de market-data + 1 de runtime de higyrus + 2 de AST sobre los 7 sitios).
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-CONTEXT.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-PATTERNS.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-02-SUMMARY.md
</context>

## Tabla de los 7 sitios (D-02) — identidad de clave y no-op por sitio

| # | Driver / función | Contrato de retorno | Componente `func` de la clave | Forma EXACTA del no-op |
|---|---|---|---|---|
| 1 | `main_market_data.py::_write_schema_snapshot` | `None` | `client_function` | `return` desnudo — **hecho en 45-02** |
| 2 | `main_iol.py::_write_or_check_schema` | `tuple[str, str]` | `func_name` | `return ("PASS", <detalle que NO empieza con "escrito">)` |
| 3 | `main_iol.py` type drift en `get_quote` (rama `elif observed[key] != expected_type`) | rama inline de un `for`, acumula `finding_fids` | `f"get_quote:{key}"` | `continue`, **sin** `finding_fids.append` |
| 4 | `main_iol.py` type drift en `get_historical_quotes[0]` | ídem | `f"get_historical_quotes[0]:{key}"` | `continue`, **sin** `finding_fids.append` |
| 5 | `main_higyrus.py::_write_or_check_schema` | `tuple[str, str]` | `func_name` | `return ("PASS", <detalle que NO empieza con "escrito">)` |
| 6 | `main_matriz.py::_write_or_check_schema` | `tuple[str, str]` | `file_path.name` (baseline segregado por venue) | `return ("PASS", <detalle que NO empieza con "escrito">)` |
| 7 | `main_ambito_financiero.py` probe de schema snapshot | `ProbeResult` | literal `"get_dollar_banco_nacion"` | `return ProbeResult("schema_snapshot", "PASS", <detalle>)` |

En los sitios 2, 5 y 6 el caller hace `if status == "FINDING": fid, fname = detail.split("|", 1)` / `elif detail.startswith("escrito"): …` / `else: matched.append(...)`. Por eso el detalle del no-op **no puede** empezar con `escrito` ni el status puede ser `FINDING` sin fid: caería en un `split("|")` sobre un string sin barra.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `main_iol.py` — guarda en los 3 sitios (schema drift + 2 type drift)</name>
  <files>main_iol.py</files>
  <read_first>
    - `main_iol.py` líneas 210-255 (bloque de estado module-level con `_fid_counter`, `_seed_fid_counter`, `_next_fid`)
    - `main_iol.py` líneas 1595-1705 (los dos bucles de type drift y el `ProbeResult` final que consume `finding_fids`)
    - `main_iol.py` líneas 1720-1770 (`_write_or_check_schema`) y 1820-1850 (su caller, con el `split("|", 1)` y el `startswith("escrito")`)
    - `main_market_data.py` (el diff que dejó 45-02: declaración de `_seen_drift_keys`, `_drift_digest` y forma de la guarda) — es el analog literal
    - `.planning/phases/45-.../45-PATTERNS.md` secciones "Forma actual — `main_iol.py:1753-1766` …" y "Forma actual — los 2 sitios de type drift"
  </read_first>
  <action>
    Copiar a `main_iol.py` los dos artefactos que 45-02 dejó en `main_market_data.py` —`_seen_drift_keys: set[tuple[str, str]] = set()` y `_drift_digest(payload: object) -> str`— ubicándolos junto a `_fid_counter` (~línea 212), más `import hashlib` en el bloque stdlib. **Copia local deliberada, no import cruzado**: `CLAUDE.md` y `45-RESEARCH.md § Project Constraints` prohíben código compartido entre unidades, y `verification/findings.py` está vedado como sitio de estado por ser append-only.

    **Sitio 2 — `_write_or_check_schema` (~línea 1751).** Entre `if committed.get("schema") == actual_schema: return ("PASS", f"{file_path.name} sin drift")` y `fid = _next_fid()`: ligar la clave a un local (por ejemplo `drift_key`) con el valor `(func_name, _drift_digest([committed.get("schema"), actual_schema]))`; si ese local ya está en `_seen_drift_keys`, devolver una tupla `("PASS", …)` cuyo detalle empiece por `file_path.name` y describa que el drift ya se reportó en esta corrida; si no, agregarlo al set y recién entonces llamar a `_next_fid()`, dejando el `append_finding(...)` y el `return ("FINDING", f"{fid}|{file_path.name}")` existentes intactos.

    **Sitios 3 y 4 — las dos ramas `elif observed[key] != expected_type:` / `elif observed_row[key] != expected_type:` (~líneas 1615 y 1683).** Dentro de cada rama, ANTES de `fid = _next_fid()`: ligar `drift_key` a `(f"get_quote:{key}", _drift_digest([expected_type, observed[key]]))` en el primero y a `(f"get_historical_quotes[0]:{key}", _drift_digest([expected_type, observed_row[key]]))` en el segundo; si ya está en el set, `continue` — **sin** `finding_fids.append(fid)`, que es justamente el conteo que el `ProbeResult` final reporta; si no, agregarlo y seguir con el cuerpo actual sin cambios.

    Dos restricciones de forma, ambas con consecuencias medibles:
    - La comparación de pertenencia tiene que hacerse sobre un **local ligado**, no sobre una tupla literal inline. `verification/test_main_matriz_skip_line_shape.py::test_no_substring_membership_check_over_a_host_literal` (enrolado en CI) marca como ofensa cualquier `ast.Compare` con `In`/`NotIn` cuyo lado izquierdo sea un literal string; ese lock corre sobre `main_matriz.py`, pero la misma forma es la que este plan replica en los 5 drivers y no conviene tener dos convenciones.
    - Las **4 ramas hermanas** `missing assumed key \`{key}\` in …` (2 en `get_quote`, 2 en `get_historical_quotes[0]`) **NO se tocan**. D-02 nombra 7 sitios y esas 4 quedan fuera a propósito; el límite se declara por escrito en el cierre de la fase (plan 45-05), no se expande acá.

    Los títulos de los tres findings no cambian.
  </action>
  <acceptance_criteria>
    - `grep -c '_seen_drift_keys' main_iol.py` ≥ 7 (1 declaración + 3 pertenencias + 3 altas).
    - `grep -c 'def _drift_digest' main_iol.py` = 1 y `grep -c '^import hashlib' main_iol.py` = 1.
    - `grep -c 'continue' main_iol.py` aumentó exactamente en 2 respecto de la línea base (`git diff -U0 main_iol.py` muestra 2 líneas `+` con `continue`).
    - `grep -c 'missing assumed key' main_iol.py` = 4 y ninguna de esas 4 ramas aparece modificada en `git diff main_iol.py`.
    - `uv run python -c "import main_iol"` sale 0.
    - `uv run pytest -q verification/test_main_iol_deep_chain.py` sigue verde.
    - `uv run ruff check main_iol.py && uv run ruff format --check main_iol.py` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import main_iol" && uv run pytest -q verification/test_main_iol_deep_chain.py && uv run ruff check main_iol.py && uv run ruff format --check main_iol.py</automated>
  </verify>
  <done>Los 3 sitios de iol tienen guarda antes de `_next_fid()`; el schema helper devuelve tupla PASS en el no-op y los 2 sitios de type drift hacen `continue` sin acumular fid; las 4 ramas hermanas quedaron intactas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `main_higyrus.py`, `main_matriz.py` y `main_ambito_financiero.py` — guarda con sus dos contratos de retorno restantes</name>
  <files>main_higyrus.py, main_matriz.py, main_ambito_financiero.py</files>
  <read_first>
    - `main_higyrus.py` líneas 218-230 (estado con `_fid_counter`), 556-603 (`_write_or_check_schema`) y 2300-2325 (su caller)
    - `main_matriz.py` líneas 350-395 (estado con `_fid_counter`), 505-545 (`_schema_path`, que segrega el baseline por venue) y 541-600 (`_write_or_check_schema`)
    - `main_ambito_financiero.py` líneas 95-135 (estado con `_fid_counter`) y 570-625 (el probe de schema snapshot completo, con sus tres `return ProbeResult(...)`)
    - El diff de `main_iol.py` que dejó la Task 1 de este plan — es el analog inmediato
    - `.planning/phases/45-.../45-PATTERNS.md`, párrafo "Contrato de retorno a preservar"
  </read_first>
  <action>
    En los tres drivers, replicar los dos artefactos (`_seen_drift_keys: set[tuple[str, str]] = set()` y `_drift_digest`) junto a su `_fid_counter`, más `import hashlib`. Una copia por driver; ningún helper compartido.

    **`main_higyrus.py` (~línea 587), sitio 5.** Idéntico al sitio 2 de iol: clave `(func_name, _drift_digest([committed.get("schema"), actual_schema]))` ligada a un local, no-op `return ("PASS", …)` con detalle que empieza por `file_path.name`, alta en el set y `_next_fid()` después.

    **`main_matriz.py` (~línea 581), sitio 6.** Misma forma, con **una diferencia deliberada**: el componente de identidad de la clave es `file_path.name`, no `func_name`. Motivo medido: desde la Phase 39 el baseline se elige por `(func_name, venue)` vía `_schema_path()`, así que dos venues distintos comparan contra baselines distintos. Con `func_name` a secas, dos drifts de venues distintos que casualmente compartieran `actual_schema` colapsarían pese a tener `expected` distintos — eso sería pérdida de censo. `file_path.name` ES la identidad del baseline y por lo tanto la lectura correcta del componente `func` de D-01 ENMENDADA para este driver. Dejar esa razón como comentario de una línea en el sitio. El `surface="sync"` del `append_finding` no se toca.

    **`main_ambito_financiero.py` (~línea 606), sitio 7.** Después de `if committed.get("schema") == actual_schema: return ProbeResult("schema_snapshot", "PASS", "schema sin drift")` y antes de `fid = _next_fid()`: clave `("get_dollar_banco_nacion", _drift_digest([committed.get("schema"), actual_schema]))` ligada a un local; el no-op devuelve un `ProbeResult("schema_snapshot", "PASS", …)` — la misma forma que el `return` de "sin drift" de dos líneas más arriba, con un detalle que diga que el drift ya se reportó en esta corrida. Un `return` desnudo acá sería un `None` donde el caller espera un `ProbeResult`.

    En los tres: los títulos no cambian, no se agrega ningún argumento nuevo a `append_finding`, la pertenencia se prueba sobre un local ligado (no sobre una tupla literal inline) y el `_next_fid()` queda estrictamente después del alta en el set.
  </action>
  <acceptance_criteria>
    - Para cada uno de los tres archivos: `grep -c '_seen_drift_keys' <archivo>` ≥ 3 y `grep -c 'def _drift_digest' <archivo>` = 1.
    - `grep -c 'file_path.name' main_matriz.py` incluye la línea de la clave (leer el sitio y confirmar en el SUMMARY que el componente de identidad es `file_path.name` y no `func_name`).
    - `uv run python -c "import main_higyrus, main_matriz, main_ambito_financiero"` sale 0.
    - `uv run pytest -q verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py verification/test_main_matriz_risk_envelope_keys.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py` → todo verde (en particular el lock de substring-membership de matriz, que la forma de la guarda podría haber activado).
    - `uv run ruff check . && uv run ruff format --check .` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import main_higyrus, main_matriz, main_ambito_financiero" && uv run pytest -q verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py verification/test_main_matriz_risk_envelope_keys.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <done>Los sitios 5, 6 y 7 tienen guarda antes del fid, cada uno con el no-op de su propio contrato de retorno; matriz usa la identidad del baseline segregado por venue; los locks de driver ya enrolados siguen verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: arms 4-6 del test de falsificación — orden por AST en los 7 sitios, forma del no-op por sitio, y contrato de tupla en runtime</name>
  <files>verification/test_drift_dedupe_falsification.py</files>
  <read_first>
    - `verification/test_drift_dedupe_falsification.py` (lo que dejó 45-02: docstring, fixture de aislamiento, contador de bloques, 3 arms de runtime)
    - `verification/test_main_matriz_risk_envelope_keys.py` líneas 60-110 — el precedente de walk por AST sobre un driver, con acumulación de `offenders` y mensaje de assert que explica la consecuencia
    - Los diffs de las Tasks 1 y 2 de este plan y el de 45-02, para conocer los 7 sitios ya corregidos
    - `main_higyrus.py` líneas 130-160 (`_SCHEMA_DIR` y el dict `_SCHEMA_FILES`, que se construye en tiempo de import con rutas absolutas)
  </read_first>
  <behavior>
    - Arm 4 (AST, orden): en los 5 drivers hay EXACTAMENTE 7 sitios de drift; en cada uno, la guarda de pertenencia sobre `_seen_drift_keys` aparece en una línea anterior a la del `_next_fid()` que alimenta ese `append_finding`.
    - Arm 5 (AST, forma del no-op): el cuerpo de cada guarda respeta el contrato de retorno de su función — `return` desnudo ×1, `return` de tupla con `"PASS"` ×3, `return ProbeResult(..., "PASS", ...)` ×1, `continue` ×2 — y ninguno de esos cuerpos llama a `_next_fid()` ni a `append_finding`.
    - Arm 6 (runtime, higyrus): dos llamadas a `main_higyrus._write_or_check_schema` con el mismo payload en drift devuelven `("FINDING", "<fid>|<archivo>")` y después `("PASS", …)`; se escribe 1 solo bloque; `_fid_counter` queda en 1; el detalle del segundo no empieza con `escrito`.
  </behavior>
  <action>
    Extender —no reescribir— `verification/test_drift_dedupe_falsification.py`. Actualizar su docstring para declarar que además de los arms de runtime el archivo lleva locks por AST sobre los 5 drivers, y por qué: el invariante P-3 de `verification/test_finding_count_consistency.py` es un property test con allocator local que **no importa ni parsea ningún driver**, así que es verde con el orden viejo y verde con el nuevo — incapaz de distinguirlos (`45-RESEARCH.md` Pitfall B). El peso de D-03 lo lleva este archivo.

    **Tabla declarativa en el módulo**: un mapping de `main_market_data`, `main_iol`, `main_higyrus`, `main_matriz`, `main_ambito_financiero` a la cantidad de sitios de drift esperada (1, 3, 1, 1, 1 — total 7) y a la forma de no-op esperada por archivo, expresada como multiconjunto para que el orden dentro del archivo no importe: market-data → un `Return` sin valor; iol → un `Return` de tupla con primer elemento `"PASS"` más dos `Continue`; higyrus y matriz → un `Return` de tupla con primer elemento `"PASS"` cada uno; ámbito → un `Return` cuyo valor es una llamada a `ProbeResult` con `"PASS"` como segundo argumento posicional.

    **Detección de sitios de drift (compartida por los arms 4 y 5):** `ast.parse` sobre `Path(main_X.__file__)`; recorrer las llamadas cuyo callee se llame `append_finding`; extraer del keyword `title` sus partes literales (una `ast.Constant` de string, o la concatenación de las partes `ast.Constant` de un `ast.JoinedStr`); considerar sitio de drift al que contenga `schema drift en` en minúsculas tras normalizar a lower-case, o `type drift on`. Normalizar es necesario: market-data escribe el título con `s` minúscula y los otros cuatro con `S` mayúscula. Assert de piso: el total tiene que ser 7 y el reparto por archivo el de la tabla — si algún día alguien agrega un sitio de drift sin guarda, este assert es lo que lo destapa (mensaje: un sitio nuevo sin guarda vuelve a inflar el ledger).

    **Arm 4:** para cada sitio, buscar dentro de la misma `ast.FunctionDef` que lo contiene la llamada a `_next_fid()` de mayor `lineno` estrictamente menor que la del `append_finding` — ése es el fid que ese sitio quema — y assertar que existe en la misma función un `ast.Compare` con operador `In` cuyo comparador sea el `Name` `_seen_drift_keys` y cuyo `lineno` sea menor. Además, por archivo, assertar que la cantidad de esos `ast.Compare` es igual a la cantidad de sitios de drift del archivo: sin esa igualdad, la guarda de un sitio "cubriría" a otro por ser simplemente anterior en el archivo (los 3 sitios de iol viven en el mismo módulo).

    **Arm 5:** para cada `ast.If` cuyo test sea uno de esos `ast.Compare`, tomar el cuerpo y assertar (i) que su forma coincide con el multiconjunto declarado para ese archivo y (ii) que dentro de ese cuerpo no hay ninguna llamada a `_next_fid` ni a `append_finding` — un no-op que emita algo no es un no-op. El mensaje de fallo tiene que nombrar el hazard concreto: un `return` desnudo en un helper que declara `tuple[str, str]` devuelve `None` y su caller hace `status, detail = ...`.

    **Arm 6 (runtime):** nueva fixture análoga a la de 45-02 pero para higyrus: `monkeypatch` de `verification.findings._FINDINGS_DIR` a `tmp_path`; `monkeypatch` de `main_higyrus._SCHEMA_DIR` a un subdirectorio de `tmp_path`; tomar una clave real del dict con `func = next(iter(main_higyrus._SCHEMA_FILES))` y `monkeypatch` de `main_higyrus._SCHEMA_FILES` a un dict que mapee esa clave a un archivo dentro del tmp; escribir ahí un baseline cuyo `"schema"` difiera del payload de prueba; `monkeypatch` de `main_higyrus._fid_counter` a 0 y de `main_higyrus._seen_drift_keys` a un `set()` nuevo. Llamar dos veces a `main_higyrus._write_or_check_schema(func, "<endpoint>", {}, <payload>, "https://example.invalid")` con el MISMO payload y assertar: primer retorno con status `"FINDING"` y detalle que contiene una barra; segundo retorno con status `"PASS"`; el detalle del segundo NO empieza con `escrito` (si empezara, el caller lo contaría como baseline recién escrito); exactamente 1 bloque `### F-` nuevo; y `main_higyrus._fid_counter == 1`.

    Correr el archivo completo: 6 arms verdes. Correr después el espejo parcial de CI para confirmar que nada más se movió.
  </action>
  <acceptance_criteria>
    - `uv run pytest -q verification/test_drift_dedupe_falsification.py` → 6 passed.
    - `uv run pytest --collect-only -q verification/test_drift_dedupe_falsification.py` lista 6 ítems.
    - El arm 4 falla si se invierte el orden: mover temporalmente la llamada a `_next_fid()` arriba de la guarda en `main_ambito_financiero.py`, correr el arm 4, pegar la salida ROJA en el SUMMARY y restaurar. `git diff main_ambito_financiero.py` queda sin esa reversión.
    - El arm 5 falla si se uniforma un no-op: cambiar temporalmente el no-op de `main_higyrus.py` a un `return` desnudo, correr el arm 5, pegar la salida ROJA y restaurar.
    - `uv run pytest -q verification/test_finding_count_consistency.py verification/test_findings_dedupe_by_title.py verification/test_findings_fid_seed.py` → verde, y `git diff --quiet HEAD -- verification/test_finding_count_consistency.py` sale 0.
    - `git status --porcelain .planning/verification/` vacío después de correr todo.
    - `uv run ruff check . && uv run ruff format --check .` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q verification/test_drift_dedupe_falsification.py verification/test_finding_count_consistency.py verification/test_findings_dedupe_by_title.py verification/test_findings_fid_seed.py && git status --porcelain .planning/verification/ && uv run ruff check . && uv run ruff format --check .</automated>
  </verify>
  <done>Los 6 arms pasan; los 7 sitios están pinneados por AST en orden y en forma de no-op; las dos demostraciones de no-vacuidad (orden invertido y no-op uniformado) están pegadas con su salida roja; P-3 sigue verde y sin editar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver ↔ ledger committeado | Seis sitios más donde una guarda mal puesta decidiría que un evento no llega al humano |
| test ↔ ledger committeado | El arm 6 escribe findings: sin `monkeypatch` de `_FINDINGS_DIR` tocaría `higyrus-client-findings.md` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-10 | Repudiation / Information Disclosure (por omisión) | los 6 sitios de drift restantes | mitigate | Digest sobre el par `[expected, actual]` en cada clave + arm 4/5 por AST sobre los 7 sitios; en matriz la identidad es el baseline segregado por venue, para que dos venues no se colapsen entre sí |
| T-45-11 | Repudiation | `finding_fids` de los 2 sitios de type drift de iol | mitigate | El no-op es `continue` sin `append`; el arm 5 asserta que el cuerpo de la guarda no llama a `_next_fid` ni a `append_finding` |
| T-45-12 | Denial of Service (del run completo) | no-op con forma equivocada (p. ej. `None` donde el caller desempaqueta una tupla) | mitigate | Arm 5 (forma del no-op por sitio) + arm 6 (contrato de tupla en runtime) + demostración de que el arm se pone rojo si se uniforma |
| T-45-13 | Tampering | `.planning/verification/*-findings.md` | mitigate | `monkeypatch` de `_FINDINGS_DIR` en toda fixture nueva + `git status --porcelain` vacío como criterio |
| T-45-14 | Tampering | lock enrolado `test_main_matriz_skip_line_shape.py` (substring-membership) | mitigate | La pertenencia se prueba sobre un local ligado, nunca sobre un literal string; el criterio de aceptación corre ese lock |
| T-45-SC | Tampering | instalación de paquetes | accept | N/A — sin dependencias nuevas |
</threat_model>

<verification>
- `uv run pytest -q verification/test_drift_dedupe_falsification.py` → 6 passed.
- `uv run python -c "import main_iol, main_higyrus, main_matriz, main_ambito_financiero, main_market_data"` → 0.
- Espejo parcial de CI: `uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py` → verde.
- `git status --porcelain .planning/verification/` → vacío.
</verification>

<success_criteria>
1. Los 7 sitios de D-02 tienen guarda antes de `_next_fid()`, cada uno con el no-op de su propio contrato de retorno.
2. Existe un lock permanente por AST que pinnea tanto el orden como la forma, y que se demostró capaz de ponerse rojo ante cada una de las dos violaciones.
3. Ningún test existente se relajó ni se editó; los locks de driver ya enrolados en CI siguen verdes.
</success_criteria>

<output>
Create `.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-03-SUMMARY.md` when done.
Debe incluir: la salida de los 6 arms, las dos salidas ROJAS de las demostraciones de no-vacuidad, y la tabla de los 7 sitios con el no-op efectivamente escrito en cada uno (archivo + línea).
</output>
