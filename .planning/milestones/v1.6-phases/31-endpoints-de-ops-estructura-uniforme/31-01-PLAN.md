---
phase: 31-endpoints-de-ops-estructura-uniforme
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/market-data-client/tests/test_mutation_gate_ast.py
  - packages/market-data-client/tests/test_v040_request_pin.py
autonomous: true
requirements: [TYP-02]

must_haves:
  truths:
    - "An AST guard proves `_ensure_mutation_allowed()` is the first literal statement of all 8 mutation methods in BOTH `client.py` and `aio.py`, and proves it non-vacuously by asserting the DISCOVERED method-name set EQUALS the 8-name roster (D-07, criterion 3)."
    - "A direct builder-flag assertion proves `build_add_holidays_request` and `build_delete_holiday_request` both emit `RequestSpec.idempotent is True` (D-20, criterion 3 second clause) — the stale `client.py:684` docstring claiming `False` is contradicted by the test, not by prose (G-6)."
    - "A byte-level pin proves the emitted `add_holidays` request equals the captured v0.4.0 tuple `(method, str(url), sorted headers, content bytes)` — raw bytes, never `json.loads` (D-08, criterion 2)."
    - "A byte-level pin proves the emitted `delete_holiday` request equals the captured v0.4.0 tuple, including `content == b\"\"` and the ABSENCE of `content-type` (D-08)."
    - "Both pins exist for the async surface too, asserting the same frozen literals — sync/async request parity is proven by construction (C-3)."
    - "`request.extensions` never enters any frozen tuple (Pitfall 2 — `request_id` is a fresh `uuid4().hex` per call)."
    - "All new tests are GREEN against UNCHANGED source at this wave — this plan builds the net before anything moves, so a later red is attributable to the change, not to the net."
    - statement: "The `user-agent` header stays inside the frozen header set but is derived as `f\"python-httpx/{httpx.__version__}\"` rather than hard-pinned to `0.28.1`, so a `uv.lock` bump does not redden criterion 2 for a reason unrelated to this phase (RESEARCH § Byte-Identical Request Test, Hazard 1). Confirm at execution that the derived form still reproduces the captured tuple under the installed httpx."
      verification: backstop
  prohibitions:
    - "MUST NOT relax, reorder, conditionally skip, or fixture-around `_ensure_mutation_allowed()` — including moving it below the builder call or stubbing it in a test — to make any response-shape assertion pass. Criterion 3 IS the ASVS V4 access-control regression gate for this package."
    - "MUST NOT alter any emitted request byte (method, URL, query string, header SET, body bytes, or body key ORDER) for `add_holidays`/`delete_holiday` while presenting the change as response-only. An assertion that parses the body before comparing hides key-order drift and counts as altering it undetected."
    - "MUST NOT ship a guard that can report green while finding zero subjects. A vacuous structural gate is a worse outcome than no gate — this repo has been bitten by it twice (Phase 15 WR-01/WR-02)."
  artifacts:
    - path: "packages/market-data-client/tests/test_mutation_gate_ast.py"
      provides: "Non-vacuous AST first-statement guard over both shells + both holiday builder idempotent-flag assertions"
      min_lines: 90
      contains: "_ensure_mutation_allowed"
    - path: "packages/market-data-client/tests/test_v040_request_pin.py"
      provides: "Raw-bytes v0.4.0 request pin for both holiday mutations, sync + async"
      min_lines: 80
      contains: "calendar/holidays"
  key_links:
    - from: "packages/market-data-client/tests/test_mutation_gate_ast.py"
      to: "packages/market-data-client/src/market_data_client/client.py"
      via: "ast.parse of the shell source read from disk"
      pattern: "ast\\.parse"
    - from: "packages/market-data-client/tests/test_mutation_gate_ast.py"
      to: "packages/market-data-client/src/market_data_client/aio.py"
      via: "same parametrized shell reader"
      pattern: "aio\\.py"
    - from: "packages/market-data-client/tests/test_v040_request_pin.py"
      to: "packages/market-data-client/src/market_data_client/_core.py"
      via: "builders reached through the real Client dispatch path, request captured from httpx_mock"
      pattern: "get_requests"
---

<objective>
Build the phase's safety net FIRST, against unchanged source, so every guard is provably
green before a single signature moves.

Three criterion-3 / criterion-2 gates that do not exist today:

1. The mutating-gate **AST guard** — ROADMAP criterion 3 says "guard AST existente verde",
   but no AST check anywhere in this repo targets `packages/*/src/*/client.py` or `aio.py`
   (D-07, RESEARCH Pitfall 3). It is built here, in-package, non-vacuously.
2. The **builder idempotent-flag assertion** — criterion 3's second clause, today covered
   only indirectly through `test_transport.py`'s retry short-circuit (G-6).
3. The **byte-identical v0.4.0 request pin** — criterion 2, with no reusable pattern in the
   repo (D-08); the exact frozen tuples were captured from the real code path by RESEARCH
   and proven identical to tag `market-data-client-v0.4.0` by `git diff`.

Purpose: this phase retypes the responses of two mutations that are already published in
`market-data-client` v0.4.0. Without these three gates in place and green FIRST, a later
red is ambiguous. With them, criterion 2 and criterion 3 become mechanically decidable.

Output: two new in-package test files that run in the existing 6×2 CI matrix.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-CONTEXT.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Location |
|---|---|---|
| `packages/market-data-client/tests/test_mutation_gate_ast.py` | new test module (AST guard) | market-data tests |
| `_MUTATION_METHODS` | frozenset roster (8 names) | in the new AST guard |
| `_GATE_CALL` | constant `"_ensure_mutation_allowed"` | in the new AST guard |
| `_shell_tree(name)` | AST reader over `client.py` / `aio.py` | in the new AST guard |
| `_called_name(node)` / `_functions(tree)` | AST helpers (transferred verbatim) | in the new AST guard |
| `_first_stmt_is_gate(fn)` | first-statement predicate (docstring-skipping) | in the new AST guard |
| `packages/market-data-client/tests/test_v040_request_pin.py` | new test module (wire pin) | market-data tests |
| `_ADD_HOLIDAYS_V040` / `_DELETE_HOLIDAY_V040` | frozen request tuples | in the new pin module |
| `_frozen(req)` | request → comparable tuple projection | in the new pin module |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-vacuous mutating-gate AST guard + holiday builder idempotent-flag assertions</name>

  <reversibility rating="reversible">A new in-package test file; deleting it restores the
  status quo ante with no migration and no contract change.</reversibility>

  <files>packages/market-data-client/tests/test_mutation_gate_ast.py</files>

  <read_first>
    - `verification/test_main_market_data_no_gate_bypass.py` lines 41-109 — the AST-parsing pattern to ADAPT (it parses the *driver*; this one parses the shells). `_called_name` and `_functions` transfer verbatim.
    - `packages/market-data-client/src/market_data_client/client.py` lines 260-275 (the `_ensure_mutation_allowed` definition) and lines 570-730 (the 8 gated methods) — read the actual current bodies, do not assume.
    - `packages/market-data-client/src/market_data_client/aio.py` lines 218-230 and lines 578-735 — the async mirror.
    - `packages/market-data-client/tests/test_mutation_gate.py` lines 1-60 — the existing BEHAVIORAL gate tests; this new file complements them, it does not replace or edit them.
    - `packages/market-data-client/src/market_data_client/_core.py` lines 1-60 (imports, `RequestSpec` dataclass) and the two holiday builders `build_add_holidays_request` / `build_delete_holiday_request`.
    - `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md` § "Mutating-Gate Invariant (criterion 3)" and § "Recommended Guard Placement" — the exact 8-name roster, the 16 confirmed call-site lines, and the `_first_stmt_is_gate` predicate spelled out.
  </read_first>

  <action>
Create `packages/market-data-client/tests/test_mutation_gate_ast.py` — a NEW file. Do not
edit the existing `packages/market-data-client/tests/test_mutation_gate.py`, which is
behavioral (helper-level refusal) and stays as-is.

Placement is in-package, NOT under `verification/`. Rationale to state in the module
docstring: `ci.yml`'s `test` job runs `pytest packages/${{ matrix.package }}`, an explicit
path that overrides `[tool.pytest.ini_options] testpaths`, so `verification/` has never
executed in CI (G-5, and `ci.yml`'s own inline comment on the `decode-intactness` step says
so verbatim). This guard must travel in the 6×2 matrix.

Module contents:

- Header per convention: module docstring → blank line → `from __future__ import annotations`
  → stdlib imports (`ast`, `pathlib`) → third-party (`pytest`) → first-party
  (`market_data_client`, `market_data_client._core as _core`).

- `_GATE_CALL = "_ensure_mutation_allowed"`.

- `_MUTATION_METHODS` as a `frozenset` of exactly these 8 names:
  `create_symbol`, `create_symbols`, `update_symbol`, `set_calendar_config`,
  `delete_calendar_config`, `preview_calendar_config`, `add_holidays`, `delete_holiday`.

- `_SHELLS = ("client.py", "aio.py")` and a `_shell_tree(name: str) -> ast.Module` that
  resolves the shell source via `pathlib.Path(market_data_client.__file__).parent / name`,
  reads it with `encoding="utf-8"`, and returns `ast.parse(...)`. Resolving through the
  imported package (rather than a hand-built repo-root path) keeps the guard working under
  `--import-mode=importlib` and under an installed wheel.

- `_called_name(node: ast.Call) -> str | None` — transfer VERBATIM from
  `verification/test_main_market_data_no_gate_bypass.py`: `ast.Name` → `.id`,
  `ast.Attribute` → `.attr`, else `None`.

- `_functions(tree: ast.Module) -> list[ast.FunctionDef | ast.AsyncFunctionDef]` — transfer
  verbatim: `ast.walk` filtered on the `ast.FunctionDef | ast.AsyncFunctionDef` union.

- `_first_stmt_is_gate(fn) -> bool` — take `fn.body`; if the first element is an `ast.Expr`
  wrapping an `ast.Constant`, drop it (that is the docstring); then return True only when the
  remaining body is non-empty AND `body[0]` is an `ast.Expr` wrapping an `ast.Call` whose
  `_called_name` equals `_GATE_CALL`. Note in a comment that the docstring skip is what makes
  "first LITERAL statement" mean "first EXECUTABLE statement".

- Test A, parametrized over `_SHELLS` — **non-vacuity by set equality** (Phase 15 WR-01/WR-02
  precedent, restated in Phase 30's decision log). Collect the names of every function in the
  shell whose name is in `_MUTATION_METHODS`, as a set, and assert that set `==`
  `_MUTATION_METHODS`. Never assert "is non-empty", never assert a `>=` count. A guard that
  finds zero methods and reports green is the exact failure this repo has already shipped twice.
  Include the shell name in the assertion message so a failure names which surface drifted.

- Test B, parametrized over `_SHELLS` — for every discovered mutation method, assert
  `_first_stmt_is_gate(fn) is True`, with a failure message naming the shell, the method, and
  the actual first statement's `ast.dump` truncated, so a regression is diagnosable without
  re-running under a debugger.

- Test C — the criterion-3 second clause, a DIRECT builder-flag assertion. Build a client
  state the same way the package's own tests do (construct a `market_data_client.Client` and
  read `client._state`; do NOT open the mutation gate — the builders are pure and gate-free).
  Call `_core.build_add_holidays_request(state, {"days": [{"day": "2099-12-29", "closed": True,
  "description": "probe"}]})` and `_core.build_delete_holiday_request(state, "2099-12-29")`,
  and assert on each returned `RequestSpec` that `.idempotent is True` — the identity form,
  not a truthiness check. Add an inline comment recording D-20: both flags were corrected to
  `True` in Phase 27 on live row-count measurement (two identical POSTs left exactly 1 row;
  the endpoint upserts by date), so criterion 3 means they must STAY `True`. Also record that
  `client.py`'s `add_holidays` docstring currently claims the opposite and is stale (G-6,
  fixed in plan 31-05) — this test is the authority, the prose is not.

Do not import anything from `verification/` — that package is not on the test path for the
per-package CI leg.
  </action>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_mutation_gate_ast.py -q` exits 0 with at least 5 tests collected (2 shells × 2 parametrized tests + the builder-flag test).
    - `grep -c 'ast.parse' packages/market-data-client/tests/test_mutation_gate_ast.py` returns at least 1.
    - `grep -c 'frozenset' packages/market-data-client/tests/test_mutation_gate_ast.py` returns at least 1.
    - The non-vacuity assertion is an equality against the roster, not a bound: `grep -cE 'discovered\s*==\s*_MUTATION_METHODS|_MUTATION_METHODS\s*==' packages/market-data-client/tests/test_mutation_gate_ast.py` returns at least 1.
    - Temporary RED proof (revert before committing): move `self._ensure_mutation_allowed()` in `client.py`'s `add_holidays` to AFTER the builder call, re-run the file, and observe Test B FAIL naming `client.py` / `add_holidays`. Then restore `client.py` byte-for-byte and confirm `git diff --stat packages/market-data-client/src/` is empty before the commit.
    - Temporary RED proof for non-vacuity (revert before committing): rename `add_holidays` to `add_holidays_x` in `aio.py`, re-run, observe Test A FAIL on the set inequality. Restore and confirm `git diff --stat packages/market-data-client/src/` is empty.
    - `uv run ruff check packages/market-data-client/tests/test_mutation_gate_ast.py` and `uv run ruff format --check packages/market-data-client/tests/test_mutation_gate_ast.py` both exit 0.
    - `git status --porcelain packages/market-data-client/src/` is empty at commit time — this plan adds tests only and must not move production source.
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_mutation_gate_ast.py -q</automated>
  </verify>

  <done>Both shells prove all 8 mutation methods exist by set equality and all 16 sites open with the gate call; both holiday builders prove `idempotent is True`; both RED proofs were observed and reverted; production source is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw-bytes v0.4.0 request pin for add_holidays and delete_holiday, sync + async</name>

  <reversibility rating="costly">The frozen literals become the definition of "the v0.4.0
  request" for every later phase; changing them later means re-deriving provenance against a
  tag whose source has since moved.</reversibility>

  <files>packages/market-data-client/tests/test_v040_request_pin.py</files>

  <read_first>
    - `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md` § "Byte-Identical Request Test (criterion 2) — ground truth" — the provenance proof, both captured tuples verbatim, the `_frozen()` helper shape, and the four documented hazards. This is the spec for this task.
    - `packages/market-data-client/tests/test_calendar_write.py` lines 34-100 — `_BASE`, `_TOKEN_URL`, `_CONFTEST_HOST`, and the `_open_gate()` helper; and lines 250-360 — the existing piecewise assertions, which are exactly what must NOT be copied here.
    - `packages/market-data-client/tests/conftest.py` — the seeded `test-token` and `base_url` fixture this pin depends on.
    - `packages/market-data-client/src/market_data_client/models.py` — `HolidayIn` and `HolidaysIn`, in particular their `to_dict()` key ORDER, which is part of the pinned contract.
    - `packages/market-data-client/src/market_data_client/_core.py` — `build_add_holidays_request` and `build_delete_holiday_request`, plus `_params.drop_none` (which is why `open_time`/`close_time` are absent from the pinned body).
  </read_first>

  <action>
Create `packages/market-data-client/tests/test_v040_request_pin.py` — a NEW dedicated file.
Do NOT add these tests to `test_calendar_write.py` / `test_calendar_write_async.py`: those
files are re-mocked in plan 31-05 and keeping the pin in its own module means the pin's diff
stays empty across that re-mock, which is itself evidence for criterion 2.

Module docstring must state the contract in one paragraph: `add_holidays` and
`delete_holiday` shipped in `market-data-client` v0.4.0; Phase 31 retypes their RESPONSES
only; this module pins their emitted REQUESTS as raw bytes so any request-side drift fails
loudly. Record the provenance: `git diff market-data-client-v0.4.0 -- packages/market-data-client/src/`
shows the only post-tag changes are Phase 29 decoder wiring (an import plus 7 parser
decorations, and two bind lines at the top of `_request`) — no builder body and no
request-construction line changed — therefore HEAD's emitted bytes ARE the v0.4.0 bytes.

Contents:

- Standard header, then `import httpx`, `pytest`, `from pytest_httpx import HTTPXMock`,
  `import market_data_client`, `from market_data_client import aio`, and
  `from market_data_client.models import HolidayIn, HolidaysIn`.

- Local copies of `_BASE = "https://market-data-develop.test/api"` and
  `_CONFTEST_HOST = "market-data-develop.test"`, plus a local `_open_gate()` calling
  `market_data_client.configure(mutating_allowed=True, expected_host=_CONFTEST_HOST)` and an
  async-side equivalent using `aio.configure(...)`. Copy, do not import across test modules.

- `_PROBE_DAY = "2099-12-29"` and a `_probe_holidays()` factory returning
  `HolidaysIn([HolidayIn(_PROBE_DAY, description="probe")])` — the exact payload RESEARCH
  executed to capture the tuples. Do not change the day or the description; the
  `content-length` literal is derived from them.

- `_UA = f"python-httpx/{httpx.__version__}"`. **Decision (Hazard 1, resolved here):** keep
  `user-agent` INSIDE the frozen header set but derive its value, rather than excluding it.
  Excluding it would stop the pin from noticing a header being dropped; hard-pinning
  `0.28.1` would redden criterion 2 on an unrelated `uv.lock` bump. Deriving keeps the header
  SET pinned while decoupling the version. State this rationale in an inline comment.

- `_ADD_HOLIDAYS_V040` — a 4-tuple: `"POST"`;
  `"https://market-data-develop.test/api/calendar/holidays"`; a tuple of `(name, value)`
  pairs in ascending key order — `accept` `*/*`, `accept-encoding` `gzip, deflate`,
  `authorization` `Bearer test-token`, `connection` `keep-alive`, `content-length` `67`,
  `content-type` `application/json`, `host` `market-data-develop.test`, `user-agent` `_UA`;
  and the body bytes `b'{"days":[{"day":"2099-12-29","closed":true,"description":"probe"}]}'`.
  Add an inline comment noting the body key order is `day, closed, description` — `description`
  last because `HolidayIn.to_dict()` lists it last, and `open_time`/`close_time` absent because
  `_params.drop_none` removed them. That ordering is the part a parsed comparison would lose.

- `_DELETE_HOLIDAY_V040` — `"DELETE"`;
  `"https://market-data-develop.test/api/calendar/holidays/2099-12-29"`; the same header pairs
  MINUS `content-length` and MINUS `content-type` (Hazard 3: a shared header list across both
  endpoints would be wrong); and `b""`. Freeze per-endpoint tuples, never a shared header list.

- `_frozen(req: httpx.Request) -> tuple[str, str, tuple[tuple[str, str], ...], bytes]`
  returning `(req.method, str(req.url), tuple(sorted(req.headers.items())), req.content)`.
  Add an inline comment: `req.extensions` is deliberately excluded because `request_id` is a
  fresh `uuid4().hex` per call (Hazard 2) — a 32-hex-char string in a failure diff is the
  warning sign that it leaked in.

- Four tests, all asserting a SINGLE equality of `_frozen(...)` against the corresponding
  literal — never `json.loads`, never a piecewise field-by-field comparison:
  `test_add_holidays_request_is_byte_identical_to_v040` (sync),
  `test_delete_holiday_request_is_byte_identical_to_v040` (sync), and the two `aio` mirrors.
  Each: call `_open_gate()`, register the response with `httpx_mock.add_response(...)` (any
  200 body — this test asserts nothing about the response), dispatch through the default
  singleton exactly as `test_calendar_write.py` does, then take the LAST captured request from
  `httpx_mock.get_requests()` matching the mutation path. Filter by path rather than indexing
  `[0]` blindly, so a token-grant request in the capture list cannot shift the index.

- Add one shared assertion helper or an inline comment stating that the four tests assert the
  SAME two literals across both surfaces — that identity is the sync/async request parity
  evidence (C-3), and diverging literals per surface would defeat it.
  </action>

  <acceptance_criteria>
    - `uv run pytest packages/market-data-client/tests/test_v040_request_pin.py -q` exits 0 with exactly 4 tests passing.
    - The comparison is raw-bytes equality, not a parsed comparison — no decode call appears in executable code: `grep -v '^\s*#' packages/market-data-client/tests/test_v040_request_pin.py | grep -c 'json.loads'` returns 0.
      <!-- planner-discipline-allow: json.loads -->
    - The per-call `extensions` mapping never enters a frozen tuple — no executable reference to it exists: `grep -v '^\s*#' packages/market-data-client/tests/test_v040_request_pin.py | grep -c 'req.extensions'` returns 0.
      <!-- planner-discipline-allow: req.extensions -->
      <!-- planner-discipline-allow: extensions -->
    - The `user-agent` value is derived, not hard-pinned: `grep -c 'httpx.__version__' packages/market-data-client/tests/test_v040_request_pin.py` returns at least 1, and `grep -v '^\s*#' packages/market-data-client/tests/test_v040_request_pin.py | grep -c '0\.28\.1'` returns 0.
      <!-- planner-discipline-allow: 0.28.1 -->
      <!-- planner-discipline-allow: 0\.28\.1 -->
    - Header sets differ per endpoint: `grep -c 'content-length' packages/market-data-client/tests/test_v040_request_pin.py` returns exactly 1 (the POST tuple only).
    - Temporary RED proof (revert before committing): in `market_data_client/models.py`, swap the emission order of `closed` and `description` inside `HolidayIn.to_dict()`, re-run the file, and observe the two `add_holidays` tests FAIL on the body bytes. Restore and confirm `git diff --stat packages/market-data-client/src/` is empty. This proves the pin catches the exact drift class a parsed comparison would miss.
    - `uv run ruff check packages/market-data-client/tests/test_v040_request_pin.py` and `uv run ruff format --check` on the same file both exit 0.
    - `uv run pytest packages/market-data-client -q` exits 0 with a count of at least the pre-change baseline plus the new tests (baseline for market-data + higyrus combined is 699).
  </acceptance_criteria>

  <verify>
    <automated>uv run pytest packages/market-data-client/tests/test_v040_request_pin.py -q</automated>
  </verify>

  <done>Four tests pin both mutations' emitted requests as raw bytes on both surfaces, green against unchanged source; the key-order RED proof was observed and reverted; no parsed comparison and no `extensions` reference exists in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `Client.add_holidays` / `delete_holiday` | A mutation crosses from in-process caller intent to a real write against a calendar. `_ensure_mutation_allowed()` is the sole authorization point and it is IO-free by design (an authorization read must precede any dispatch). |
| client → market-data API (develop/prod) | The `expected_host` exact-match leg decides which environment receives a write. A substring-tolerant match would let `…develop.bbsa.com.ar.attacker.example` pass. |
| `day` argument → URL path segment | A caller-supplied string is interpolated into a path. `_DAY_SEGMENT_RE` plus all-dots rejection is the allow-list. |

## STRIDE Threat Register (ASVS L1, block_on = high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering / EoP | `_ensure_mutation_allowed()` ordering in `client.py` + `aio.py` | mitigate | ASVS V4. Task 1's AST guard asserts the gate call is the first executable statement of all 8 methods in both shells, and proves non-vacuity by set equality against the 8-name roster. Both RED proofs (reordered gate, renamed method) must be observed. |
| T-31-02 | DoS / Tampering | `RetryTransport` idempotency gating on both holiday builders | mitigate | ASVS V4. Task 1 Test C asserts `RequestSpec.idempotent is True` directly on both builders. Ratified on live row-count measurement (D-20). Criterion 3 forbids changing either flag; the stale `client.py` docstring claiming `False` is contradicted by this test. |
| T-31-03 | Tampering | Request emission for two published mutations | mitigate | ASVS V13. Task 2's raw-bytes pin covers method, URL, query string, full header set, and body bytes INCLUDING key order. The Phase-27 `_DAY_SEGMENT_RE` path-safety allow-list and `HolidaysIn.__post_init__`'s 1-500 batch bound both live on this request path and are transitively pinned by the URL and body literals. |
| T-31-04 | Repudiation | A structural gate that reports green while finding nothing | mitigate | Non-vacuity is asserted as set EQUALITY, never as a non-empty check or a lower bound. Phase 15 WR-01/WR-02 precedent. |
| T-31-05 | Info Disclosure | Credentials in the pinned literals | accept | ASVS V7 / CLAUDE.md C-4. The pinned `authorization` value is the conftest-seeded `test-token`, never a real credential; `.env` files are untouched by this plan. |
| T-31-SC | Tampering | package-manager installs | mitigate | Not applicable — this plan installs zero packages and `uv.lock` must not change (`uv lock --check` is the first `lint` step). RESEARCH § Package Legitimacy Audit records the empty roster. |

**Load-bearing note:** the mutating-gate invariant (T-31-01) IS the ASVS V4 regression gate for
this package. It is never relaxed to make a response-shape test pass — the test is re-mocked
instead (Phase 30-03's ratified precedent).
</threat_model>

## Flagged Assumptions (probe-surfaced, unresolved — not dismissed)

The deterministic edge probe returned two rows for this phase's requirements, both
`unresolved`. Neither is auto-dismissed and neither is given a fabricated resolution:

- **TYP-02 / concurrency / unresolved** — probe: *"If interrupted or run in parallel, what is
  guaranteed?"* The stock concurrency probe does not map cleanly onto this shape: these are
  client libraries issuing sequential HTTP calls, and the phase changes only response
  deserialization, not any concurrency primitive. Interruption mid-`add_holidays` leaves
  server state governed by the endpoint's upsert-by-date semantics (D-20), which this phase
  does not touch. **Carried forward as an open assumption, not resolved.**
- **TYP-03 / unclassified / unresolved** — probe: *"unclassified — review manually."* Carried
  forward for manual review; TYP-03 is addressed by plan 31-02.

<verification>
Both files green against UNCHANGED production source:

```
uv run pytest packages/market-data-client/tests/test_mutation_gate_ast.py packages/market-data-client/tests/test_v040_request_pin.py -q
uv run pytest packages/market-data-client -q
uv run ruff check . && uv run ruff format --check .
git status --porcelain packages/market-data-client/src/    # must be empty
```

Both RED proofs (gate reorder, holiday-body key-order swap) observed and reverted before
commit, with `git diff --stat packages/market-data-client/src/` confirmed empty afterwards.
</verification>

<success_criteria>
- AST guard asserts set equality against the 8-name roster per shell, and first-executable-statement for every discovered method (2 shells × 8 methods = 16 sites).
- Both holiday builders assert `idempotent is True` by identity.
- Four raw-bytes request pins (2 endpoints × 2 surfaces) pass against unchanged source.
- No `json.loads`, no `extensions`, no hard-pinned httpx version anywhere in the pin module.
- `uv run pytest packages/market-data-client -q` green; ruff check + format clean.
- Production source untouched by this plan.
</success_criteria>

<output>
Create `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-01-SUMMARY.md` when done.
Record: the exact test counts added, the two RED-proof observations verbatim (what failed and
how), the resolved `user-agent` decision, and confirmation that `git diff` against
`packages/market-data-client/src/` is empty.
</output>
