---
phase: 45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/check_surface_types.py
  - main_market_data.py
  - .planning/ROADMAP.md
autonomous: true
requirements: [HARN-03]

must_haves:
  truths:
    - "El docstring de `tools/check_surface_types.py` cita cifras que un lector puede reproducir: el bloque histórico queda pinneado a su commit y el bloque vigente dice lo que el gate imprime hoy (D-05 ENMENDADA)."
    - "`probe_parity` de `main_market_data.py` vuelve a COMPARAR segments en vez de salir siempre por la rama de excepción (D-09 / DRV-MD-SEG-43)."
    - "`IN-05` deja de figurar como pendiente en el backlog del ROADMAP porque está resuelto en HEAD (D-07)."
    - "Ningún texto de planificación de la fase sigue afirmando el número stale `336` como si fuera el valor medido."
  artifacts:
    - path: "tools/check_surface_types.py"
      provides: "Docstring del gate con el bloque histórico pinneado a commit y el bloque vigente fechado"
      contains: "00ffb2f~1"
    - path: "main_market_data.py"
      provides: "probe_parity comparando `Segment.segment` (campo que existe en HEAD)"
      contains: "s.segment for s in seg_sync"
    - path: ".planning/ROADMAP.md"
      provides: "Backlog sin IN-05 pendiente + criterio 4 con la cifra medida"
  key_links:
    - from: "tools/check_surface_types.py"
      to: "salida real del propio gate"
      via: "las tres cifras del bloque vigente se copian de la corrida hecha en esta tarea"
      pattern: "definitions scanned"
    - from: "main_market_data.py:1541-1542"
      to: "packages/market-data-client/src/market_data_client/models.py (`Segment`)"
      via: "dereference del campo `segment` declarado por el modelo tras la Phase 43"
      pattern: "s\\.segment"
---

<objective>
Cerrar la mitad mecánica y verificable de **HARN-03** —el comentario stale del gate de superficie (D-05 ENMENDADA), el retiro de `IN-05` (D-07)— y aterrizar el fold-in **D-09** (`DRV-MD-SEG-43`), el dereference de 2 líneas contra un campo que la Phase 43 removió.

Purpose: son los tres ítems de la fase que no dependen de ninguna decisión abierta y que ningún otro plan necesita esperar. Además dejan `main_market_data.py` tocado y verde ANTES de que el plan 45-02 le agregue el dedupe, para que el diff del dedupe sea puro.
Output: `tools/check_surface_types.py` con el docstring reproducible, `main_market_data.py` con `probe_parity` comparando de verdad, y `.planning/ROADMAP.md` sin `IN-05` pendiente y con el criterio 4 diciendo la cifra medida.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-CONTEXT.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-PATTERNS.md
</context>

## Source Coverage Audit (conjunto de planes de la Phase 45)

| Fuente | Ítem | Plan que lo cubre | Estado |
|---|---|---|---|
| GOAL | "deja de inflar el ledger con bloques duplicados" | 45-02, 45-03 | COVERED |
| GOAL | "deja de arrastrar en silencio archivos que nunca corren" | 45-05 | COVERED |
| GOAL | "la deuda de `verification/` de matriz recibe una decisión escrita" | 45-04 | COVERED |
| REQ | HARN-01 | 45-02, 45-03 (+ enrolamiento en 45-05) | COVERED |
| REQ | HARN-03 | 45-01 (D-05/D-07) + 45-05 (D-06/IN-06) | COVERED |
| REQ | HARN-04 | 45-04 (+ enrolamiento del canario en 45-05) | COVERED |
| RESEARCH | Hallazgo 5 — el valor medido es 337, no 336 | 45-01 T1 | COVERED |
| RESEARCH | Hallazgo 4 — `:47` es cita histórica exacta, se pinnea y no se reescribe | 45-01 T1 | COVERED |
| RESEARCH | Hallazgo 12 / Pitfall D — `DRV-MD-SEG-43` + gap de gate de mypy | 45-01 T2 (fix) + 45-05 T3 (declaración Q5) | COVERED |
| RESEARCH | Hallazgo 2 — sólo market-data duplica in-process | 45-02 (arms runtime) + 45-03 (arm AST) | COVERED |
| RESEARCH | Hallazgo 3 / Pitfall 9 — no usar dedupe por título en la rama drift | 45-02 T2, 45-03 T1/T2 | COVERED |
| RESEARCH | Pitfall B — P-3 no detecta la violación; el peso lo lleva el test nuevo | 45-02 T1, 45-03 T3 | COVERED |
| RESEARCH | Hallazgos 7-10 — evidencia por archivo de HARN-04 | 45-04 T2 | COVERED |
| RESEARCH | Hallazgo 11 — censo 53/13/40 | 45-04 T2 (pre) + 45-05 T3 (post) | COVERED |
| RESEARCH | Hallazgo 6 — `test_public_surface.py` es red parcial (no cubre market-data) | 45-05 T1 | COVERED |
| CONTEXT | D-01 (ENMENDADA), D-02, D-03, D-04 | 45-02, 45-03 | COVERED |
| CONTEXT | D-05 (ENMENDADA), D-07 | 45-01 | COVERED |
| CONTEXT | D-06 | 45-05 | COVERED |
| CONTEXT | D-08 (ENMENDADA) | 45-04 | COVERED |
| CONTEXT | D-09 | 45-01 T2 | COVERED |
| CONTEXT | D-10 (ENMENDADA) | 45-04 T2 (declaración) + 45-05 T2 (enrolamiento) | COVERED |
| CONTEXT | D-11 | 45-05 T2 (edit único) + T3 (gate de un solo commit) | COVERED |

Sin ítems MISSING. Exclusiones declaradas (no son gaps): las Deferred Ideas de `45-CONTEXT.md` (reparación de los 2 archivos rotos, disposición individual de los ~35 `verification/` restantes, enrolamiento mypy de `verification/`) y los 4 sitios hermanos `missing assumed key` de `main_iol.py`, que D-02 excluye explícitamente.

## Artifacts this phase produces

Símbolos y archivos nuevos de toda la Phase 45 (referencia compartida por los 5 planes):

| Artefacto | Tipo | Plan | Dónde vive |
|---|---|---|---|
| `_seen_drift_keys: set[tuple[str, str]]` | estado module-level (una copia POR driver, sin helper compartido) | 45-02 (market-data), 45-03 (iol/higyrus/matriz/ámbito) | `main_market_data.py`, `main_iol.py`, `main_higyrus.py`, `main_matriz.py`, `main_ambito_financiero.py` |
| `_drift_digest(payload: object) -> str` | función privada (una copia POR driver) | 45-02, 45-03 | los mismos 5 drivers |
| `verification/test_drift_dedupe_falsification.py` | archivo de test nuevo (4 arms) | 45-02 (crea), 45-03 (extiende) | `verification/` |
| `45-HARN-04-DECISION.md` | artefacto de decisión fechado | 45-04 | `.planning/phases/45-.../` |
| Test `test_probe_login_sync_returns_finding_not_fail` (nombre a discreción) | test nuevo en archivo YA enrolado | 45-04 | `verification/test_main_matriz_skip_line_shape.py` |
| 5 líneas nuevas de allowlist en el job `lint` | config CI | 45-05 | `.github/workflows/ci.yml` |

No se crea ningún helper compartido nuevo en `verification/`: `findings.py` es append-only por contrato y `CLAUDE.md` prohíbe código compartido entre unidades (ver `45-RESEARCH.md § Project Constraints`).

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Docstring del gate de superficie — pin histórico en `:45-49` y re-medición en `:55-62` (D-05 ENMENDADA)</name>
  <files>tools/check_surface_types.py</files>
  <read_first>
    - `tools/check_surface_types.py` líneas 25-75 (los dos bloques del docstring y el párrafo "The same green, now earned")
    - `.planning/phases/45-.../45-PATTERNS.md` sección `tools/check_surface_types.py:45-62 (D-05 ENMENDADA)` — trae la forma verbatim actual y la acción exacta por línea
    - `.planning/phases/45-.../45-CONTEXT.md` § "Checkpoint de resolución post-research", bullet **D-05 ENMENDADA**
  </read_first>
  <action>
    Correr PRIMERO `uv run python tools/check_surface_types.py` y copiar su línea de salida completa; también capturar el sha corto de HEAD con `git rev-parse --short HEAD`. Las cifras que se escriban tienen que salir de ESA corrida, no de ningún documento de planificación (`45-RESEARCH.md` midió 187 / 337 / 467 el 2026-09-01 y `45-PATTERNS.md` lo reconfirmó, pero el árbol puede haberse movido — re-medir es la instrucción literal de D-05).

    Tres ediciones, ninguna más:

    (a) Bloque histórico "Before Phase 37" (hoy línea 45, cifras en 47-49): **NO tocar los números** `183` / `330` / `13` / `23` — `45-RESEARCH.md` Hallazgo 4 los reconstruyó con `git worktree` sobre `00ffb2f~1` y son byte-idénticos a lo que ese árbol imprimía. Cambiarlos introduce un error fáctico en un registro exacto. Editar SÓLO la frase que introduce el bloque para agregar el pin de commit, de modo que quede con la forma `**Before Phase 37** (measured at ``00ffb2f~1``) this gate printed::`.

    (b) Bloque vigente (hoy línea 55-56 la frase introductoria, 58-60 las cifras): reemplazar las tres cifras congeladas `186` / `330` / `442` por las tres medidas en la corrida de arriba, manteniendo el resto de la línea (packages, constant/alias exports, exempted con su desglose, `0 violations`) alineado con la salida real. Reescribir la frase introductoria para que deje de leerse como "lo que el gate imprime desde la Phase 37" y pase a llevar fecha y commit — forma sugerida: `**Measured YYYY-MM-DD** (commit ``<sha-corto>``), over a tree whose fields have since been typed::`. Ese defecto de framing es el `IN-01` genuino según Hallazgo 4: el dígito nunca estuvo mal, lo que faltaba era la fecha.

    (c) El párrafo inmediatamente posterior (hoy línea 62) dice "The same green, now earned: 442 fields inspected …". Ese `442` es la MISMA cifra stale en prosa: actualizarlo al valor de fields scanned recién medido. Corregir las tres cifras del bloque y dejar la prosa contradiciéndolas sería reproducir el defecto que la tarea cierra.

    No tocar ninguna otra parte del archivo: la lógica del gate, el bloque `Run it as::` y la nota sobre `test_surface_types_red.py` quedan intactas. El docstring está en inglés — mantener el idioma del archivo.
  </action>
  <acceptance_criteria>
    - `uv run python tools/check_surface_types.py` sigue imprimiendo `0 violations` y sale con código 0.
    - Las tres cifras (`__all__ names`, `definitions scanned`, `fields scanned`) del bloque vigente del docstring coinciden EXACTAMENTE con las de esa salida; comprobado leyendo ambas y pegando las dos en el SUMMARY.
    - `grep -c '00ffb2f~1' tools/check_surface_types.py` devuelve al menos 1 (el pin histórico existe).
    - `grep -c '330 definitions scanned' tools/check_surface_types.py` devuelve 1 y esa única ocurrencia es la del bloque histórico (la cita sigue byte-idéntica; el bloque vigente ya no dice 330).
    - El número de fields en la prosa "now earned" coincide con el del bloque vigente (leer las dos líneas y confirmarlo en el SUMMARY).
    - `uv run ruff check tools/check_surface_types.py && uv run ruff format --check tools/check_surface_types.py` pasa.
  </acceptance_criteria>
  <verify>
    <automated>uv run python tools/check_surface_types.py && uv run ruff check tools/check_surface_types.py && uv run ruff format --check tools/check_surface_types.py</automated>
  </verify>
  <done>El bloque histórico está pinneado a `00ffb2f~1` con sus cifras originales intactas; el bloque vigente y la prosa que lo sigue dicen las tres cifras medidas hoy, con fecha y commit; el gate sigue verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `DRV-MD-SEG-43` — `probe_parity` vuelve a comparar segments (D-09)</name>
  <files>main_market_data.py</files>
  <read_first>
    - `main_market_data.py` líneas 1526-1563 (`probe_parity` completo, incluido el `try/except Exception` de la ladder D-09)
    - `packages/market-data-client/src/market_data_client/models.py` líneas 870-896 (la declaración de `Segment` en HEAD: `segment: str`, `live_instruments: int`)
    - `.planning/phases/45-.../45-PATTERNS.md` sección `main_market_data.py:1541-1542 (driver, transform) — D-09`
  </read_first>
  <action>
    En `main_market_data.py`, dentro del `try` de `probe_parity` (hoy líneas 1541-1542), cambiar el atributo dereferenciado de `marketSegmentId` a `segment` en las dos comprehensions: `ids_sync` y `ids_async`. Dos líneas, sin lógica nueva, sin espejo sync/async (es un dereference de driver, no de `client.py`/`aio.py` — D-09 lo dice explícitamente).

    Nada más cambia: `segment` es `str` igual que el campo que se creía tener, así que `sorted()`, los `set()` de `only_sync`/`only_async` y los `len()` de los mensajes `expected`/`actual` siguen funcionando sin tocarse. El `except Exception` de la ladder D-09 se queda donde está — el contrato "una divergencia de forma degrada a finding, nunca a crash" no se toca.

    Contexto de por qué importa (para el SUMMARY): hoy el `AttributeError` se dispara SIEMPRE, así que el probe sale por la rama de excepción antes de comparar nada y reporta un `ERROR-MAP` espurio; el costo real medido en `45-RESEARCH.md` Hallazgo 12 no es el finding falso sino que el probe de paridad `SYNC-ASYNC-DRIFT` está ciego. Ningún gate de CI apunta a los drivers de la raíz (Pitfall D) — esta tarea arregla el sitio; declarar ese gap por escrito es trabajo del plan 45-05 (Q5), y NO se resuelve acá apuntando mypy a los drivers (scope creep de tamaño no medido).
  </action>
  <acceptance_criteria>
    - `uv run mypy main_market_data.py` sale con código 0 e imprime `Success: no issues found in 1 source file` (línea base medida antes del fix: exactamente 2 errores `attr-defined` en 1541 y 1542).
    - `grep -c 'for s in seg_sync' main_market_data.py` devuelve 1 y la línea que matchea dereferencia `s.segment`.
    - `uv run ruff check main_market_data.py && uv run ruff format --check main_market_data.py` pasa.
    - El `except Exception` de la ladder D-09 sigue presente en la misma función (leerlo y confirmarlo en el SUMMARY).
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy main_market_data.py && uv run ruff check main_market_data.py && uv run ruff format --check main_market_data.py</automated>
  </verify>
  <done>`uv run mypy main_market_data.py` está limpio; `probe_parity` compara ids derivados de `Segment.segment`; la ladder de excepción quedó intacta.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ROADMAP — retirar `IN-05` (D-07), corregir la cifra del criterio 4 y marcar `DRV-MD-SEG-43` como cerrado</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` línea 198 (criterio de éxito 4 de la Phase 45), línea 253 (entrada de backlog `DRV-MD-SEG-43`), línea 260 (entrada de backlog "Cosmético Phase 37", donde vive `IN-05`)
    - `.planning/phases/45-.../45-CONTEXT.md` D-07
  </read_first>
  <action>
    Verificar primero, no asumir: correr `uv run python -c "import matriz_client; print(matriz_client.__version__)"` y confirmar que imprime una versión (el research midió `0.3.0`, resuelto en la Phase 40). Sólo si imprime una versión se aplica el retiro; si fallara, PARAR y reportarlo — D-07 es un retiro de texto justificado por el código, no al revés.

    Tres ediciones de texto con `Edit` acotado (nunca reescribir el archivo entero):

    (a) En la entrada de backlog "Cosmético Phase 37" (hoy línea 260): quitar `IN-05` de la lista de ítems pendientes y dejar en su lugar una nota corta de que quedó resuelto en la Phase 40, citando el símbolo verificado (`matriz_client.__version__`) y la versión que imprimió el comando de arriba. `IN-01` e `IN-06` se quedan como están: `IN-01` lo cierra la Task 1 de este plan y `IN-06` el plan 45-05.

    (b) En el criterio de éxito 4 de la Phase 45 (hoy línea 198): el texto dice que el comentario debe decir "el número medido (336, no 330)". `336` se midió antes de las Phases 43/44 y hoy es incorrecto (`45-RESEARCH.md` Hallazgo 5). Reemplazar la cifra por la que la Task 1 midió, agregando entre paréntesis la fecha de medición y una nota de una línea explicando que el `336` del texto original quedó stale por el movimiento de superficie de las Phases 43/44. Dejar el criterio con un número que la propia fase midió como falso lo vuelve incumplible tal como está escrito.

    (c) En la entrada de backlog `DRV-MD-SEG-43` (hoy línea 253): agregar al final una nota de cierre indicando que se corrigió en la Phase 45 (plan 45-01) y que el gap de gate que lo dejó pasar queda declarado por separado (Q5, plan 45-05). No borrar la entrada: su medición de por qué ningún gate lo detectó es la evidencia que la declaración de Q5 cita.

    No tocar ninguna otra entrada del backlog: `HARN-VERIF-01` lo retira el plan 45-05, con puntero al documento de decisión de HARN-04.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import matriz_client; print(matriz_client.__version__)"` imprime una versión y su salida queda pegada en el SUMMARY.
    - `grep -n 'IN-05' .planning/ROADMAP.md` muestra únicamente líneas que lo describen como resuelto en la Phase 40 (ninguna que lo liste como pendiente).
    - El criterio 4 de la Phase 45 cita la misma cifra de `definitions scanned` que quedó escrita en `tools/check_surface_types.py` por la Task 1 (comparar las dos y pegarlas en el SUMMARY).
    - La entrada `DRV-MD-SEG-43` sigue existiendo y ahora incluye su nota de cierre con el plan que lo arregló.
    - `git diff --stat .planning/ROADMAP.md` muestra sólo líneas modificadas en esas tres zonas (sin reescritura masiva del archivo).
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'IN-05' .planning/ROADMAP.md; grep -c 'DRV-MD-SEG-43' .planning/ROADMAP.md</automated>
  </verify>
  <done>`IN-05` figura como resuelto y no como pendiente; el criterio 4 dice la cifra medida hoy con su fecha; `DRV-MD-SEG-43` queda marcado como cerrado en esta fase con el gap de gate ruteado a la declaración de Q5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver ↔ artefacto committeado (`.planning/verification/`) | Este plan NO escribe findings: la Task 2 sólo cambia un dereference y no corre ningún driver. |
| repo ↔ red / credenciales | Ninguna. Esta fase no corre drivers en vivo (`ROADMAP.md:53`); no se lee ningún `.env`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Repudiation | docstring de `tools/check_surface_types.py` | mitigate | Las cifras se copian de una corrida hecha en la tarea y el bloque histórico se pinnea a `00ffb2f~1`; el criterio de aceptación compara docstring contra salida real, así que una cifra inventada no pasa |
| T-45-02 | Tampering | cita histórica del bloque `:47-49` | mitigate | Prohibido reescribir sus dígitos; criterio de aceptación con grep byte-idéntico sobre `183 ... 330 definitions scanned` |
| T-45-03 | Denial of Service (del run) | ladder D-09 de `probe_parity` | mitigate | El fix es un cambio de nombre de atributo dentro del `try` existente; el criterio de aceptación exige que el `except Exception` siga presente |
| T-45-04 | Information Disclosure | PII en el ledger | accept | El plan no escribe findings ni cambia `expected`/`actual`; `schema_of()` sigue siendo keys+types por construcción |
| T-45-SC | Tampering | instalación de paquetes (npm/pip/cargo) | accept | **N/A** — la fase no instala nada (`45-RESEARCH.md § Package Legitimacy Audit`: tabla vacía, cero dependencias nuevas). Cualquier propuesta de instalar algo en esta fase es señal de scope creep |
</threat_model>

<verification>
- `uv run python tools/check_surface_types.py` → `0 violations`, y sus tres cifras están escritas verbatim en el docstring.
- `uv run mypy main_market_data.py` → `Success: no issues found in 1 source file`.
- `uv run ruff check . && uv run ruff format --check .` → limpio.
- `git status --porcelain .planning/verification/` → vacío (ningún artefacto de findings tocado por este plan).
</verification>

<success_criteria>
1. El docstring del gate es reproducible: bloque histórico pinneado a commit con cifras intactas, bloque vigente fechado con las cifras que el gate imprime hoy, prosa "now earned" alineada.
2. `uv run mypy main_market_data.py` pasa de 2 errores a 0.
3. `IN-05` deja de estar listado como pendiente, con la verificación de código pegada.
4. El criterio 4 del ROADMAP y el docstring citan la MISMA cifra medida.
</success_criteria>

<output>
Create `.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-01-SUMMARY.md` when done.
Debe incluir: la salida completa de `tools/check_surface_types.py` (antes y después no cambia — es el gate, no el docstring), la salida de `mypy main_market_data.py` antes y después, y la versión que imprimió `matriz_client.__version__`.
</output>
