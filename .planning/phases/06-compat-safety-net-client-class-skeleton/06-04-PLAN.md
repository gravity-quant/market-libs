---
phase: 06-compat-safety-net-client-class-skeleton
plan: 04
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - packages/iol-client/src/iol_client/_state.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/src/iol_client/__init__.py
  - packages/iol-client/tests/conftest.py
  - packages/iol-client/tests/test_client.py
  - packages/iol-client/tests/test_async_client.py
  - packages/iol-client/tests/test_client_class.py
  - packages/iol-client/tests/test_fixture_reaches_production.py
  - verification/snapshots/iol-client-surface.txt
autonomous: true
requirements: [REFAC-02]

user_setup: []

must_haves:
  truths:
    - "iol_client.Client (sync) and iol_client.AsyncClient (async) exist with __enter__/__exit__/close() and __aenter__/__aexit__/aclose() respectively."
    - "_ClientState holds: base_url, username, password, token, token_expires_at, refresh_token, http_client (sync or async), token_lock (None for sync; lazy asyncio.Lock for async). Mutable (NOT frozen); slots=True."
    - "iol_client.configure(token=..., token_expires_at=..., refresh_token=...) is accepted; carry-forward semantics for unset fields (RESEARCH.md Open Q #5)."
    - "PEP 562 shim forwards _token → state.token, _token_expires_at → state.token_expires_at, _refresh_token → state.refresh_token (Pitfall #3 addendum to D-02), _client → state.http_client. aio.py shim also forwards _token_lock → state.token_lock."
    - "OAuth refresh_token flow preserved: Client._ensure_token() checks token + token_expires_at; if expired and refresh_token present, attempt _refresh(); if refresh fails with IOLAuthError, fall back to login() password grant. Async mirror preserves the double-checked locking pattern."
    - "All 15+ inline monkeypatch sites in test_client.py / test_async_client.py migrated to pkg._get_default()._state.<field> = ... or pkg.configure(...) — the shim does NOT intercept setattr (Pitfall #1/#4 mitigation). Pre-edit grep enumerates every write site (W2 mitigation)."
    - "aio.py imports _raise_for_response from iol_client.client — does NOT duplicate the helper (resolves checker B8). Follows the same import pattern ARCHITECTURE.md confirms for aio.py importing InstrumentType from client.py."
    - "verification/snapshots/iol-client-surface.txt regenerated to ADD Client, AsyncClient entries; no baseline entries removed."
    - "Per-package guard file packages/iol-client/tests/test_fixture_reaches_production.py is MIGRATED from monkeypatch.setattr legacy pattern to iol_client.configure(token=..., token_expires_at=...) and aio.configure(...) and still passes. This plan owns this file exclusively in Wave 1 (resolves checker B3)."
  artifacts:
    - path: "packages/iol-client/src/iol_client/_state.py"
      provides: "@dataclass(slots=True) _ClientState with iol fields"
      min_lines: 35
    - path: "packages/iol-client/tests/test_client_class.py"
      provides: "Client/AsyncClient skeleton tests: lifecycle, repr-redaction, pickle/deepcopy, configure carry-forward, PEP 562 shim including _refresh_token, Pitfall #2 explicit-instance isolation"
      min_lines: 80
  key_links:
    - from: "iol_client.client._refresh_token (legacy read)"
      to: "_get_default()._state.refresh_token"
      via: "PEP 562 __getattr__ shim (Pitfall #3 addendum)"
      pattern: "_refresh_token.*refresh_token"
    - from: "iol_client.Client._ensure_token"
      to: "self._state.token + self._state.token_expires_at + self._state.refresh_token"
      via: "Instance method reading per-instance state"
      pattern: "self\\._state\\.token"
    - from: "iol_client.AsyncClient._ensure_token"
      to: "self._state.token_lock (lazy asyncio.Lock)"
      via: "Double-checked locking on lazy lock"
      pattern: "async with self\\._state\\.token_lock"
    - from: "iol_client.aio._raise_for_response (usage)"
      to: "iol_client.client._raise_for_response (definition)"
      via: "Direct import — no duplication (B8)"
      pattern: "from iol_client.client import _raise_for_response"
---

<objective>
Land the IOL skeleton: `_state.py` + `Client` (with OAuth refresh_token flow) + `AsyncClient` (with double-checked locking on a per-instance asyncio.Lock) + PEP 562 shim WITH the Pitfall #3 addendum (forward `_refresh_token`) + conftest migration + inline test-body monkeypatch migration (15+ sites — the biggest scope item in the phase) + snapshot regen + per-package guard migration to `configure(token=...)`.

Purpose: IOL has the OAuth refresh_token flow, which is the single most invasive per-instance change in the phase. The Pitfall #3 addendum to D-02 means the shim's `_FORWARDED_TO_STATE` MUST include `_refresh_token` for IOL — RESEARCH.md surfaces 13+ test sites that depend on this. The 15+ inline `monkeypatch.setattr(iol_client.client, "_token"/"_refresh_token", X, raising=False)` sites in `test_client.py`/`test_async_client.py` must ALL be migrated to write `iol_client._get_default()._state.<field> = X` (or `iol_client.configure(...)` where it fits the test semantic) because the PEP 562 shim does NOT intercept `__setattr__` (the monkeypatch lands on a dead address otherwise — RESEARCH.md Pitfall #4).

**B3 — Per-package guard ownership:** This plan owns `packages/iol-client/tests/test_fixture_reaches_production.py` exclusively in Wave 1. No other plan touches it. Plans 03/05/06 each own their own package's guard file. `depends_on` is therefore `[06-01, 06-02]` only — no inter-Plan-1 ordering needed.

**B8 — Shared helper import:** `aio.py` imports `_raise_for_response` from `iol_client.client` — does NOT duplicate. Justification: ARCHITECTURE.md confirms `aio.py` already imports shared types like `InstrumentType` from `client.py` without circular issues; `_raise_for_response` is stateless and follows the same pattern.

**W2 — IOL grep pre-flight:** Before editing any test file, run an enumeration grep listing every monkeypatch.setattr write site that targets module state. This catches missed migrations.

Output:
- 1 NEW source module: `_state.py`
- 4 MODIFIED source modules: `client.py`, `aio.py`, `__init__.py`
- 2 MODIFIED tests: `conftest.py`, `test_client.py`, `test_async_client.py` (inline monkeypatch migration)
- 1 NEW test file: `tests/test_client_class.py`
- 1 REGENERATED snapshot: `verification/snapshots/iol-client-surface.txt`
- 1 MODIFIED per-package guard file: `packages/iol-client/tests/test_fixture_reaches_production.py` (iol sync + async guards migrate from `monkeypatch.setattr(..., raising=False)` to `iol_client.configure(token=...)`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-03-PLAN.md
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/__init__.py
@packages/iol-client/tests/conftest.py
@packages/iol-client/tests/test_client.py
@packages/iol-client/tests/test_async_client.py
@packages/iol-client/tests/test_fixture_reaches_production.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_state.py
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-edit grep enumeration + _state.py + Client (sync, OAuth+refresh) + sync conftest + sync test migration</name>
  <files>
    packages/iol-client/src/iol_client/_state.py,
    packages/iol-client/src/iol_client/client.py,
    packages/iol-client/tests/conftest.py,
    packages/iol-client/tests/test_client.py,
    packages/iol-client/tests/test_client_class.py
  </files>
  <read_first>
    - packages/iol-client/src/iol_client/client.py (260 LOC — OAuth password grant + refresh_token flow; globals `_base_url`, `_user`, `_password`, `_token`, `_token_expires_at`, `_refresh_token`, `_client`; `_TOKEN_TTL_BUFFER_SECONDS=60`; `_ensure_token` checks `_token + time.time() < _token_expires_at` then tries `_refresh()` then `login()`)
    - packages/iol-client/tests/conftest.py (current sync autouse — sets test-token via monkeypatch)
    - packages/iol-client/tests/test_client.py (15+ inline `monkeypatch.setattr(iol_client.client, "_token"/"_refresh_token", X, raising=False)` sites — RESEARCH.md lists lines 147-149, 177-179, 215-217, 244-245, 275-277, 307, 314 + reads at 170, 208, 260, 290, 311, 316; `iol_client.client._password = "another"` at line 315)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_state.py (Plan 03 analog for slots dataclass shape)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (Plan 03 analog for Client class layout + PEP 562 shim + carry-forward configure)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 5 ClientState fields for iol; Section 6 Client class skeleton; Section 8 PEP 562 forwarded set for iol including `_refresh_token`)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md (Pattern 2; Pitfall #3 mitigation; Pitfall #4 inline-monkeypatch migration; Open Q #5 carry-forward)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-02 + Pitfall #3 addendum; D-13 iol kwargs; D-22 N/A here)
  </read_first>
  <behavior>
    - Pre-edit step: run an enumeration grep that lists ALL inline `monkeypatch.setattr(iol_client.client, "_<field>", ...)` write sites and ALL direct `iol_client.client._<field> = ...` write sites in `packages/iol-client/tests/*.py`. Output is captured in the task log so post-edit verification can confirm zero remaining hits (W2 mitigation).
    - `_state.py` defines `@dataclass(slots=True) _ClientState` with fields: `base_url: str` (default factory env), `username: str` (default factory env), `password: str` (default factory env), `token: str | None = None`, `token_expires_at: float = 0.0`, `refresh_token: str | None = None`, `account_id: str | None = None` (forward-declared for Phase 9), `http_client: httpx.Client | httpx.AsyncClient | None = None`, `token_lock: asyncio.Lock | None = None` (consumed by AsyncClient only in Task 2).
    - `iol_client.Client.__init__(*, username=None, password=None, base_url=None, token=None, token_expires_at=None)` per D-13. No `refresh_token=` kwarg in Phase 6 (D-13 defers to Phase 9 BUG-03).
    - `Client.login()` does the OAuth password grant against `f"{self._state.base_url}/token"` and stores `self._state.token`, `self._state.token_expires_at = time.time() + expires_in - _TOKEN_TTL_BUFFER_SECONDS`, `self._state.refresh_token = response_json.get("refresh_token")`.
    - `Client._refresh()` does the refresh-token grant.
    - `Client._ensure_token()`: if `self._state.token and time.time() < self._state.token_expires_at: return`; else if `self._state.refresh_token: try: self._refresh(); return; except IOLAuthError: pass`; else `self.login()`.
    - `Client._request(method, path, *, params=None, json_body=None)` calls `self._ensure_token()`, then asserts `self._state.token is not None`, then uses `self._ensure_http_client().request(method, f"{self._state.base_url}{path}", ..., headers={"Authorization": f"Bearer {self._state.token}"})`. Errors map via existing `_raise_for_response` (preserve as module-level helper). The helper is the single source of truth — `aio.py` will import it (B8 lock-in).
    - `Client.__repr__` redacts password and token: `IOLClient(base_url='...', username='alice', password='***', token='***')` when set; `None` for unset token.
    - `Client.__reduce__` / `__deepcopy__` raise `TypeError` per D-23.
    - `configure(*, base_url=None, username=None, password=None, token=None, token_expires_at=None, refresh_token=None)` carry-forward — note `refresh_token` is added as a kwarg here as Pitfall #3 mitigation extension to D-13 (also satisfies the test migration requirements for `_refresh_token` write sites).
    - Top-level `iol_client.login()` and `iol_client.get_*(...)` delegate to `_get_default().login()`/`.get_*(...)`.
    - PEP 562 shim: `_FORWARDED_TO_STATE = {"_token": "token", "_token_expires_at": "token_expires_at", "_refresh_token": "refresh_token"}` (Pitfall #3 addendum: `_refresh_token` is REQUIRED in this allowlist); `_FORWARDED_HTTP_CLIENT = "_client"`. Reads of `_user`, `_password`, `_base_url` raise `AttributeError`.
    - Conftest migrated to `iol_client.configure(base_url="https://api.test", username="u", password="p", token="test-token", token_expires_at=9_999_999_999.0)` — drop `monkeypatch` parameter from `_configure_sync`.
    - All 15+ inline monkeypatch sites in `test_client.py` migrated. The mechanical rewrites:
      - `monkeypatch.setattr(iol_client.client, "_token", X, raising=False)` → `iol_client._get_default()._state.token = X`
      - `monkeypatch.setattr(iol_client.client, "_token_expires_at", X, raising=False)` → `iol_client._get_default()._state.token_expires_at = X`
      - `monkeypatch.setattr(iol_client.client, "_refresh_token", X, raising=False)` → `iol_client._get_default()._state.refresh_token = X`
      - `iol_client.client._password = "another"` (line 315) → `iol_client._get_default()._state.password = "another"`
      - Read sites like `assert iol_client.client._refresh_token == X` — these go through the PEP 562 shim (now that `_refresh_token` is in the allowlist) and resolve to `_get_default()._state.refresh_token`. NO test code change required for reads — but verify each by running.
    - All existing iol-client tests pass after migration.
  </behavior>
  <action>
    **Step 0 (W2 pre-flight — MANDATORY before any test edit):** Run the following grep to enumerate ALL write sites that will need migration. The output is the canonical checklist used in the post-edit verify step:

        grep -nE "monkeypatch\.setattr.*_token|monkeypatch\.setattr.*_password|monkeypatch\.setattr.*_user|monkeypatch\.setattr.*_refresh_token|monkeypatch\.setattr.*_base_url|iol_client\.client\._password|iol_client\.client\._token|iol_client\.client\._refresh_token|iol_client\.client\._user|iol_client\.client\._base_url" packages/iol-client/tests/*.py

    Save the output. Each line in the output represents a write site that must be migrated in this task (sync sites) or Task 2 (async sites). After edits, re-running the SAME grep against the modified files must return zero hits (lines containing only assertions/reads through the shim are fine because they don't match the writes; assertions don't use `monkeypatch.setattr` or `=` assignment). If non-zero hits remain post-edit, a site was missed — fix before proceeding.

    Create `packages/iol-client/src/iol_client/_state.py` per PATTERNS.md Section 5 IOL row + RESEARCH.md Pattern 2. Module: `from __future__ import annotations`, imports `asyncio`, `os`, `dataclasses`, `httpx`. Private env helpers: `_env_base_url()` returning `os.getenv("IOL_BASE_URL", "https://api.invertironline.com").rstrip("/")`; `_env_user()` returning `os.getenv("IOL_USER", "")`; `_env_password()` returning `os.getenv("IOL_PASSWORD", "")`. Constants moved from `client.py`: `DEFAULT_BASE_URL`, `_REQUEST_TIMEOUT`, `_TOKEN_TTL_BUFFER_SECONDS`. Define `@dataclass(slots=True) class _ClientState` with the 9 fields enumerated in <behavior> above. Docstring documents NOT-frozen + per-instance ownership.

    Restructure `packages/iol-client/src/iol_client/client.py` per PATTERNS.md Section 6 IOL adaptation notes:
    1. Module top: `from __future__ import annotations`, stdlib imports, `httpx`, `from dotenv import load_dotenv`, `from iol_client._state import _ClientState, _REQUEST_TIMEOUT, _TOKEN_TTL_BUFFER_SECONDS`, exception imports. `load_dotenv()` at module top per D-19.
    2. Keep module-level `_raise_for_response(resp: httpx.Response) -> None` helper that maps 401→IOLAuthError, 429→IOLRateLimitError, else IOLAPIError (preserve existing logic verbatim; it's stateless). Add a module docstring note: "Shared with aio.py — imported there; not duplicated (B8)."
    3. Define `class Client` with `__slots__ = ("_state",)`. `__init__(*, base_url=None, username=None, password=None, token=None, token_expires_at=None)` copies non-None kwargs to `self._state.<field>` (rstrip base_url). `__enter__ -> Self`, `__exit__`, `close()` idempotent, `__repr__` redacted per D-18, `__reduce__`/`__deepcopy__` raise `TypeError`. `_ensure_http_client(self) -> httpx.Client` lazy-creates `httpx.Client(timeout=_REQUEST_TIMEOUT)`. Migrate the OAuth flow:
       - `def login(self) -> str`: POST `f"{self._state.base_url}/token"` with form-encoded `{"username": self._state.username, "password": self._state.password, "grant_type": "password"}`, parse `access_token`, `expires_in`, `refresh_token`, store on `self._state.*`. Return the access_token. Replicate the EXACT current logic from `client.py:87-120` (read the file).
       - `def _refresh(self) -> None`: POST `f"{self._state.base_url}/token"` with `{"refresh_token": self._state.refresh_token, "grant_type": "refresh_token"}`. Raise `IOLAuthError` on non-200. Store new `access_token` + `expires_in` (do NOT clobber `refresh_token` if API doesn't return a new one — preserve existing semantics; read current `_refresh()` for the exact rule).
       - `def _ensure_token(self) -> None`: identical logic to current `_ensure_token` lines 155-166 but using `self._state.*`.
       - `def _request(self, method, path, *, params=None, json_body=None) -> httpx.Response`: identical to current lines 168-189 but using `self._state.*` and `self._ensure_http_client()`.
       - Public methods (`get_quote`, `get_instruments`, every other top-level function — enumerate from current `client.py`): instance methods calling `self._request(...)`.
    4. Module-level `_default_client: Client | None = None`; `def _get_default() -> Client: global _default_client; if _default_client is None: _default_client = Client(); return _default_client`.
    5. Top-level `def configure(*, base_url=None, username=None, password=None, token=None, token_expires_at=None, refresh_token=None) -> None`: carry-forward semantics (RESEARCH.md Open Q #5). NOTE: D-13 lists kwargs without `refresh_token=`, but Pitfall #3 mitigation requires `refresh_token=` kwarg HERE so test bodies can call `iol_client.configure(refresh_token=X)` cleanly. Document this in the docstring as "Pitfall #3 addendum: `refresh_token=` accepted for parity with shim-forwarded read; full kwargs surface lands in Phase 9 BUG-03 per D-13." If user pushes back, fallback is `iol_client._get_default()._state.refresh_token = X` direct write.
    6. Top-level `def login() -> str: return _get_default().login()`.
    7. Top-level delegators for every existing top-level function (`def get_quote(...): return _get_default().get_quote(...)` etc.). Keep verbatim signatures.
    8. PEP 562 shim at module bottom (PATTERNS.md Section 8 IOL row):
        - `_FORWARDED_TO_STATE: dict[str, str] = {"_token": "token", "_token_expires_at": "token_expires_at", "_refresh_token": "refresh_token"}`
        - `_FORWARDED_HTTP_CLIENT = "_client"`
        - `def __getattr__(name: str) -> Any:` per PATTERNS.md Section 8. NO `DeprecationWarning` (D-03).
    9. REMOVE legacy module-level globals: `_base_url`, `_user`, `_password`, `_token`, `_token_expires_at`, `_refresh_token`, `_client`. Constants `DEFAULT_BASE_URL`, `_REQUEST_TIMEOUT`, `_TOKEN_TTL_BUFFER_SECONDS` moved to `_state.py` — re-export if useful or import where needed.

    Migrate `packages/iol-client/tests/conftest.py` `_configure_sync` fixture: drop `monkeypatch` param; replace 3-line monkeypatch+configure with a single `iol_client.configure(base_url="https://api.test", username="u", password="p", token="test-token", token_expires_at=9_999_999_999.0)`. Keep teardown as-is.

    Migrate `packages/iol-client/tests/test_client.py` inline monkeypatch sites enumerated in Step 0. Mechanical rewrites:
    - `monkeypatch.setattr(iol_client.client, "_token", X, raising=False)` → `iol_client._get_default()._state.token = X`
    - `monkeypatch.setattr(iol_client.client, "_token_expires_at", X, raising=False)` → `iol_client._get_default()._state.token_expires_at = X`
    - `monkeypatch.setattr(iol_client.client, "_refresh_token", X, raising=False)` → `iol_client._get_default()._state.refresh_token = X`
    - `iol_client.client._password = "another"` → `iol_client._get_default()._state.password = "another"`
    - Reads like `assert iol_client.client._token == "...":` LEAVE AS-IS — they go through the PEP 562 shim now. Same for `_refresh_token` reads (in the allowlist post Pitfall #3 fix).

    **Step N (post-edit grep verification — MANDATORY):** Re-run the Step 0 grep against the modified `packages/iol-client/tests/test_client.py`. Filter to write patterns only (the same regex). Output must be empty. If non-empty, identify the missed site and migrate it before continuing to Task 2.

    Create `packages/iol-client/tests/test_client_class.py` with these sync test cases:
    - `test_client_context_manager()`: `with Client() as c: pass`; `c._state.http_client is None`.
    - `test_close_idempotent()`.
    - `test_repr_redacts_password_and_token()`: build `Client(username="alice", password="secret", token="tok123")`; assert `"secret" not in repr(c)` AND `"tok123" not in repr(c)` AND `"alice" in repr(c)` AND `"***" in repr(c)`.
    - `test_pickle_raises()`, `test_deepcopy_raises()`.
    - `test_configure_carry_forward()`: `configure(base_url="https://a", username="u1")`; `configure(token="t1", token_expires_at=9.0e9)`; assert `_get_default()._state.base_url == "https://a"` AND `_get_default()._state.token == "t1"` AND `_get_default()._state.username == "u1"`.
    - `test_explicit_client_unaffected_by_top_level_configure()`: Pitfall #2.
    - `test_pep_562_shim_forwards_token()`: read `iol_client.client._token` returns `_get_default()._state.token`.
    - `test_pep_562_shim_forwards_refresh_token()` (Pitfall #3 enforcement): set `_get_default()._state.refresh_token = "rt-1"`; assert `iol_client.client._refresh_token == "rt-1"`.
    - `test_pep_562_shim_raises_for_user()`: `with pytest.raises(AttributeError): iol_client.client._user`.
    - `test_pep_562_shim_raises_for_base_url()`.
    - `test_pep_562_shim_forwards_http_client()`: post-call `mod._client is mod._get_default()._state.http_client`.
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_client.py packages/iol-client/tests/test_client_class.py -q &amp;&amp; ! grep -nE "monkeypatch\.setattr.*_token|monkeypatch\.setattr.*_password|monkeypatch\.setattr.*_refresh_token|iol_client\.client\._(token|password|refresh_token|user|base_url)\s*=" packages/iol-client/tests/test_client.py</automated>
  </verify>
  <done>
    Pytest passes. The post-edit grep returns ZERO hits in `test_client.py` (W2 pre-flight + post-edit closure). All existing test_client.py tests pass with migrated monkeypatch sites; new test_client_class.py tests pass. The OAuth refresh flow works through `Client._ensure_token` → `_refresh` → fallback to `login`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AsyncClient (async OAuth + double-checked locking) + async conftest + async test migration + __init__.py + snapshot + per-package guard migration</name>
  <files>
    packages/iol-client/src/iol_client/aio.py,
    packages/iol-client/src/iol_client/__init__.py,
    packages/iol-client/tests/conftest.py,
    packages/iol-client/tests/test_async_client.py,
    packages/iol-client/tests/test_client_class.py,
    verification/snapshots/iol-client-surface.txt,
    packages/iol-client/tests/test_fixture_reaches_production.py
  </files>
  <read_first>
    - packages/iol-client/src/iol_client/aio.py (264 LOC — async OAuth with `_token_lock`/`_client_lock` double-checked locking; `_login_unlocked`, `_refresh_unlocked`, `_ensure_token`, `_ensure_http_client`, `aclose`)
    - packages/iol-client/tests/conftest.py (Task 1 output sync; current async fixture `_configure_async`)
    - packages/iol-client/tests/test_async_client.py (async monkeypatch sites — mirror of test_client.py)
    - packages/iol-client/src/iol_client/_state.py (Task 1 output)
    - packages/iol-client/src/iol_client/client.py (Task 1 output for Client shape mirror; `_raise_for_response` helper to import)
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (Plan 03 AsyncClient analog)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-PATTERNS.md (Section 7 AsyncClient with double-checked locking on lazy lock; Section 8 aio.py shim row including `_token_lock`)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-RESEARCH.md (Pitfall #6 lazy lock creation; Pattern 4 AsyncClient skeleton)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-02 forwarded set for aio includes `_token_lock`; D-16 caller-responsible aclose)
    - packages/iol-client/src/iol_client/__init__.py (current `__all__`)
    - verification/snapshots/iol-client-surface.txt (Plan 01 baseline)
    - packages/iol-client/tests/test_fixture_reaches_production.py (Plan 02 output — iol sync and async guards to migrate; this plan owns the file exclusively per B3)
  </read_first>
  <behavior>
    - W2 pre-flight repeat for async file: grep enumerates async test write sites BEFORE editing.
    - `iol_client.AsyncClient` exists with `__slots__ = ("_state",)`, kwargs same as sync (no `refresh_token=`), `__aenter__/__aexit__/aclose()`, idempotent aclose, `__repr__` redacted, `__reduce__`/`__deepcopy__` raise. Includes lazy `_client_lock` as an instance attribute via slots — `__slots__ = ("_state", "_client_lock")` per PATTERNS.md Section 7 (iol/higyrus/matriz pattern; ambito diverges per B7).
    - `AsyncClient._ensure_token` uses `self._state.token_lock` (created lazily — if `None`, `self._state.token_lock = asyncio.Lock()`) for double-checked locking, mirroring current `aio.py:170-189`.
    - `AsyncClient._ensure_http_client` creates `self._state.http_client = httpx.AsyncClient(timeout=_REQUEST_TIMEOUT)` lazily under a (possibly separate) lock — replicate current double-checked pattern.
    - `aio.configure(...)` carry-forward semantics + new kwargs `token=`, `token_expires_at=`, `refresh_token=` (Pitfall #3 addendum).
    - `aio.py` imports `_raise_for_response` from `iol_client.client` (B8 — do NOT duplicate).
    - PEP 562 shim in aio.py: `_FORWARDED_TO_STATE = {"_token": "token", "_token_expires_at": "token_expires_at", "_refresh_token": "refresh_token", "_token_lock": "token_lock"}`; `_FORWARDED_HTTP_CLIENT = "_client"`.
    - `__init__.py` re-exports `Client`, `AsyncClient` (alphabetical insertion in `__all__`).
    - Conftest `_configure_async` migrated: drop monkeypatch, single `aio.configure(...)`.
    - All inline monkeypatch sites in `test_async_client.py` migrated to `aio._get_default()._state.<field> = ...` (mirror of Task 1 sync migration).
    - Snapshot file `verification/snapshots/iol-client-surface.txt` regenerated; `Client` and `AsyncClient` lines added; all baseline lines preserved (D-06).
    - Per-package guard `packages/iol-client/tests/test_fixture_reaches_production.py` MIGRATED (this plan owns the file exclusively in Wave 1): replace the legacy `monkeypatch.setattr(iol_client.client, "_token", "SYNC-sentinel-iol", raising=False)` with `iol_client.configure(base_url="https://api.test", username="u", password="p", token="SYNC-sentinel-iol", token_expires_at=9_999_999_999.0)`; same for async with `aio.configure(...)`. Both guards still pass — they prove the sentinel reaches the wire.
  </behavior>
  <action>
    Restructure `packages/iol-client/src/iol_client/aio.py`:
    1. Module top: `from __future__ import annotations`, `asyncio`, stdlib, `httpx`, `from iol_client._state import _ClientState, _REQUEST_TIMEOUT, _TOKEN_TTL_BUFFER_SECONDS`, exception imports, `from iol_client.client import InstrumentType, _raise_for_response` (B8 — import the shared helper, do NOT duplicate; the `_raise_for_response` helper is stateless and lives in `client.py` per ARCHITECTURE.md's "aio.py imports shared types from client.py" pattern). Do NOT call `load_dotenv()` (D-19).
    2. Use the imported `_raise_for_response` in `_request`. No local re-definition.
    3. Define `class AsyncClient` with `__slots__ = ("_state", "_client_lock")` per PATTERNS.md Section 7. `__init__(*, base_url=None, username=None, password=None, token=None, token_expires_at=None) -> None`: same body as sync. Note `_client_lock: asyncio.Lock | None = None` is an instance attribute (lazy-created inside `_ensure_http_client`), NOT in `_state` — it's about the http_client lifecycle, distinct from `_state.token_lock` which guards token refresh.
    4. `__aenter__ -> Self: return self`; `__aexit__(...) -> None: await self.aclose()`; `aclose(self)` idempotent (PATTERNS.md Section 7 excerpt).
    5. `_ensure_http_client(self) -> httpx.AsyncClient`: if `self._state.http_client is not None: return self._state.http_client`. Lazy-create `self._client_lock = asyncio.Lock() if self._client_lock is None else self._client_lock`; `async with self._client_lock:` check again, then `self._state.http_client = httpx.AsyncClient(timeout=_REQUEST_TIMEOUT)`; return it. Type-narrow assertion `assert isinstance(self._state.http_client, httpx.AsyncClient)`.
    6. `_ensure_token(self)`: if `self._state.token and time.time() < self._state.token_expires_at: return`. Lazy-create `self._state.token_lock = asyncio.Lock() if self._state.token_lock is None else self._state.token_lock`. `async with self._state.token_lock:` double-check; if `self._state.refresh_token: try: await self._refresh_unlocked(); return; except IOLAuthError: pass`. Else `await self._login_unlocked()`.
    7. `_login_unlocked(self)` / `_refresh_unlocked(self)`: identical logic to current `aio.py` `_login_unlocked`/`_refresh_unlocked` but using `self._state.*`.
    8. `_request(self, ...)`: await `self._ensure_token()`; `async with self._state.token_lock: token = self._state.token`; assert `token is not None`; `client = await self._ensure_http_client()`; await the request with `Authorization: Bearer {token}`; `_raise_for_response(resp)` on error (imported from `client.py`).
    9. Each public async method (`async def get_quote`, etc.) → instance method calling `self._request(...)`. Migrate from current top-level async functions (read the current file).
    10. Module-level `_default_async_client: AsyncClient | None = None`; `def _get_default() -> AsyncClient:` lazy singleton (sync function returning the instance; the instance's methods are async).
    11. Top-level `def configure(*, base_url=None, username=None, password=None, token=None, token_expires_at=None, refresh_token=None) -> None`: carry-forward (same as sync configure).
    12. Top-level `async def login() -> str: return await _get_default().login()` (use sync `def configure` and `async def login` mirroring current async surface).
    13. Top-level async delegators for every existing async function in current `aio.py`.
    14. PEP 562 shim: `_FORWARDED_TO_STATE = {"_token": "token", "_token_expires_at": "token_expires_at", "_refresh_token": "refresh_token", "_token_lock": "token_lock"}`. `_FORWARDED_HTTP_CLIENT = "_client"`. Same `__getattr__` shape as sync.
    15. REMOVE module-level globals: `_base_url`, `_user`, `_password`, `_token`, `_token_expires_at`, `_refresh_token`, `_client`, `_token_lock`, `_client_lock` (the module-level locks; instance locks live on the instance now).

    Edit `packages/iol-client/src/iol_client/__init__.py`: add `Client`, `AsyncClient` to `__all__` (alphabetical) and add `from iol_client.client import Client; from iol_client.aio import AsyncClient` re-exports.

    Migrate `packages/iol-client/tests/conftest.py` `_configure_async`: drop monkeypatch parameter; replace 3-line block with `aio.configure(base_url="https://api.test", username="u", password="p", token="test-token", token_expires_at=9_999_999_999.0)`. Teardown unchanged.

    **W2 async grep pre-flight (MANDATORY before editing test_async_client.py):**

        grep -nE "monkeypatch\.setattr.*aio.*_token|monkeypatch\.setattr.*aio.*_refresh_token|aio\._(token|password|refresh_token|user|base_url)\s*=" packages/iol-client/tests/test_async_client.py

    Save the output as the migration checklist. Mechanical rewrites mirror Task 1's sync pattern:
    - `monkeypatch.setattr(aio, "_token", X, raising=False)` → `aio._get_default()._state.token = X`
    - `monkeypatch.setattr(aio, "_token_expires_at", X, raising=False)` → `aio._get_default()._state.token_expires_at = X`
    - `monkeypatch.setattr(aio, "_refresh_token", X, raising=False)` → `aio._get_default()._state.refresh_token = X`
    - Reads like `assert aio._token == X` → LEAVE AS-IS (shim handles them).

    Append async mirror tests to `packages/iol-client/tests/test_client_class.py`:
    - `async def test_async_context_manager()`.
    - `async def test_aclose_idempotent()`.
    - `test_async_repr_redacts_password_and_token()`.
    - `test_async_pickle_raises()`, `test_async_deepcopy_raises()`.
    - `async def test_async_configure_carry_forward()`.
    - `async def test_async_explicit_unaffected_by_top_level_configure()`.
    - `async def test_async_pep_562_shim_forwards_token_lock()`: trigger lock creation by calling `_ensure_token()` indirectly, then assert `aio._token_lock is aio._get_default()._state.token_lock` (both `asyncio.Lock` instances).
    - `async def test_async_pep_562_shim_forwards_refresh_token()`.
    - `def test_aio_imports_raise_for_response_from_client()` (B8 enforcement): `from iol_client.client import _raise_for_response as s; from iol_client.aio import _raise_for_response as a; assert s is a`.

    Regenerate snapshot: `uv run python verification/regen_snapshots.py`. Verify D-06: every PRE-refactor entry in `iol-client-surface.txt` is preserved; `Client` and `AsyncClient` lines added.

    Migrate iol guards in `packages/iol-client/tests/test_fixture_reaches_production.py` (this plan exclusively owns this file in Wave 1):
    - `test_iol_sync_sentinel_token_reaches_authorization_header`: replace `monkeypatch.setattr(iol_client.client, "_token", "SYNC-sentinel-iol", raising=False)` + `monkeypatch.setattr(iol_client.client, "_token_expires_at", 9_999_999_999.0, raising=False)` with a single `iol_client.configure(base_url="https://api.test", username="u", password="p", token="SYNC-sentinel-iol", token_expires_at=9_999_999_999.0)`. Drop the `monkeypatch` parameter from the test signature.
    - `test_iol_async_sentinel_token_reaches_authorization_header`: same migration with `aio.configure(...)`.
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests verification/test_public_surface.py -q -k "iol or public_surface" &amp;&amp; ! grep -nE "monkeypatch\.setattr.*aio.*_token|aio\._(token|refresh_token|password|user|base_url)\s*=" packages/iol-client/tests/test_async_client.py</automated>
  </verify>
  <done>
    Pytest passes. The post-edit grep returns ZERO hits in `test_async_client.py` (W2 closure for async). All iol tests, public-surface snapshot, and iol guards (sync + async, now using `configure(token=...)`) pass. Per-package guard file `packages/iol-client/tests/test_fixture_reaches_production.py` is owned exclusively by this plan in Wave 1. `aio._raise_for_response is client._raise_for_response` (B8). Full sweep `uv run pytest -q` is green; 277 baseline maintained.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests → PEP 562 shim allowlist | The shim's `_FORWARDED_TO_STATE` allowlist for IOL is the authoritative contract. `_refresh_token` MUST be in the allowlist (Pitfall #3) or 13+ test sites break silently. |
| Inline `monkeypatch.setattr` writes | Writes hit the module dict, not `_state`. Production reads `self._state.*` — the mismatch is Pitfall #1/#4. Mitigated by migrating EVERY write site in this plan + the W2 pre/post-edit grep gate. |
| OAuth refresh_token persistence | Lives ONLY in `self._state.refresh_token` (in-memory per instance, no disk). Phase 9 BUG-03 will harden the lifecycle; Phase 6 only relocates the storage. |
| Per-package guard ownership (B3) | This plan exclusively owns `packages/iol-client/tests/test_fixture_reaches_production.py` in Wave 1. No write conflict with Plans 03/05/06. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | PEP 562 shim IOL allowlist | mitigate | Allowlist explicitly includes `_token`, `_token_expires_at`, `_refresh_token`, `_token_lock` (aio), `_client`. Tests `test_pep_562_shim_raises_for_user` / `_base_url` enforce the deny list. |
| T-06-02 | Information Disclosure | `Client.__repr__` token/password leak | mitigate | Task 1 implements `password='***'`, `token='***'` redaction. Test `test_repr_redacts_password_and_token` asserts the raw token/password are NOT in the repr. |
| T-06-03 | Tampering | conftest migration + 15+ inline sites | mitigate | Plan's entire purpose. The W2 pre/post-edit grep gate ensures zero missed sites. The fixture-reaches-production guard in `packages/iol-client/tests/test_fixture_reaches_production.py` (migrated in Task 2) PROVES the new path reaches the wire after migration. |
| T-06-04 | Denial of Service | `Client.close()`/`AsyncClient.aclose()` lifecycle | mitigate | Idempotent; tests `test_close_idempotent` and `test_aclose_idempotent` enforce. |
| T-06-05 | Information Disclosure | refresh_token in `__repr__` | mitigate | Add `refresh_token` to the redacted set in `__repr__`: `refresh_token={'***' if self._state.refresh_token else None!r}`. Task 1 `test_repr_redacts_password_and_token` extended to assert refresh_token is also masked. |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed. |
</threat_model>

<verification>
- `uv run pytest packages/iol-client/tests -q` → all iol tests green (existing + new `test_client_class.py`).
- `uv run pytest verification/ -q -k iol or public_surface` → iol public-surface snapshot test passes.
- `uv run pytest packages/iol-client/tests/test_fixture_reaches_production.py -q` → iol per-package guards pass.
- `uv run pytest -q` → full suite green; 277 baseline + new tests.
- `uv run ruff check packages/iol-client/` and `uv run mypy --strict packages/iol-client/src` → exit 0.
- W2 pre-flight + post-edit grep gates (sync + async) return zero hits.
- B8 spot-check: `python -c "from iol_client.client import _raise_for_response as s; from iol_client.aio import _raise_for_response as a; print(s is a)"` prints `True`.
- B3 ownership check: `packages/iol-client/tests/test_fixture_reaches_production.py` is touched by NO other plan in Wave 1 (cross-plan grep confirms exclusive ownership).
</verification>

<success_criteria>
- REFAC-02 (iol coverage) success criteria 3, 4 satisfied for iol-client.
- IOL OAuth refresh_token flow preserved through the per-instance refactor.
- Pitfall #3 (`_refresh_token` in allowlist) mitigated.
- All inline monkeypatch sites migrated; W2 pre/post-edit grep closure on both `test_client.py` and `test_async_client.py`.
- B8 lock-in: `aio.py` imports `_raise_for_response` from `client.py`.
- B3 per-package guard ownership: this plan exclusively owns `packages/iol-client/tests/test_fixture_reaches_production.py`.
- Public-surface snapshot updated.
- Fixture-reaches-production guards migrated to `configure(token=...)` pattern and green.
</success_criteria>

<output>
Create `.planning/phases/06-compat-safety-net-client-class-skeleton/06-04-SUMMARY.md` when done.
</output>
