---
phase: 08-retries-backoff-structured-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ambito-financiero-client/pyproject.toml
  - packages/iol-client/pyproject.toml
  - packages/higyrus-client/pyproject.toml
  - packages/matriz-client/pyproject.toml
  - uv.lock
  - verification/test_retry_mutation_gate.py
  - verification/test_retry_401_reauth.py
  - verification/test_retry_after_cap.py
  - verification/test_logging_root_unchanged.py
  - verification/test_logging_no_token_leak.py
  - verification/test_async_cancellation.py
  - pyproject.toml
  - .github/workflows/ci.yml
autonomous: true
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, infra, cross-cutting, wave-0, tests-first]
user_setup: []

must_haves:
  truths:
    - "tenacity>=9.1.0,<10 agregada como runtime dependency a los 4 packages' pyproject.toml [project] dependencies (per D-15 + RESEARCH §Package Legitimacy Audit `[OK]`); `uv.lock` actualizado vía `uv sync --all-packages --all-extras --dev --frozen`"
    - "6 cross-cutting guard tests creados en `verification/` (per D-26): `test_retry_mutation_gate.py`, `test_retry_401_reauth.py`, `test_retry_after_cap.py`, `test_logging_root_unchanged.py`, `test_logging_no_token_leak.py`, `test_async_cancellation.py`"
    - "Los 6 guard tests fallan RED en HEAD (no `_transport.py` / `_logging.py` aún) — falla esperada de Wave 1; Plans 2-5 los van turnando GREEN incrementalmente"
    - "CI grep step `lint-logging` agregado a `.github/workflows/ci.yml` lint job (per D-27 alt-b) — bloquea `logging.basicConfig` y `logging.root` en `packages/*/src/`"
    - "ruff `LOG` ruleset agregado a `pyproject.toml [tool.ruff.lint] select` (per D-27 alt-a — LOG015 cubre `logging.root.*` calls; complementario al grep para basicConfig)"
    - "Plan 1 NO toca ningún `packages/<pkg>/src/` — solo `pyproject.toml`, root config, y `verification/`; mantiene el patrón tests-first de Wave 0"
    - "Existing 527 tests baseline (Phase 7 final) sigue GREEN tras Plan 1 (no regression; solo adds)"
  artifacts:
    - path: "verification/test_retry_mutation_gate.py"
      provides: "Cross-cutting mutation gate guard — POST sin idempotent=True NO retry contra 503 (D-01, RELY-03)"
      contains: "@pytest.mark.parametrize"
      min_lines: 60
    - path: "verification/test_retry_401_reauth.py"
      provides: "Cross-cutting 401 re-auth-once guard parametrizado × auth packages (D-02, RELY-04)"
      contains: "@pytest.mark.parametrize"
      min_lines: 70
    - path: "verification/test_retry_after_cap.py"
      provides: "Cross-cutting Retry-After cap 60s guard — 429 + Retry-After:600 → cap (D-04, RELY-02)"
      contains: "Retry-After"
      min_lines: 30
    - path: "verification/test_logging_root_unchanged.py"
      provides: "logging.root.handlers/filters/level unchanged tras import × 4 paquetes (D-14, LOG-01)"
      contains: "logging.root.handlers"
      min_lines: 25
    - path: "verification/test_logging_no_token_leak.py"
      provides: "Cross-cutting caplog redaction guard parametrizado × 4 paquetes con SECRET-LITERAL-12345 (D-10, LOG-02)"
      contains: "SECRET-LITERAL"
      min_lines: 70
    - path: "verification/test_async_cancellation.py"
      provides: "Cross-cutting asyncio.CancelledError propagation × async packages; matriz skip (D-32, Pitfall 16)"
      contains: "asyncio.wait_for"
      min_lines: 40
    - path: "packages/ambito-financiero-client/pyproject.toml"
      provides: "tenacity dep added to ámbito package"
      contains: "tenacity>=9.1.0,<10"
    - path: "packages/iol-client/pyproject.toml"
      provides: "tenacity dep added to iol package"
      contains: "tenacity>=9.1.0,<10"
    - path: "packages/higyrus-client/pyproject.toml"
      provides: "tenacity dep added to higyrus package"
      contains: "tenacity>=9.1.0,<10"
    - path: "packages/matriz-client/pyproject.toml"
      provides: "tenacity dep added to matriz package"
      contains: "tenacity>=9.1.0,<10"
    - path: ".github/workflows/ci.yml"
      provides: "lint-logging step bloquea logging.basicConfig/logging.root in packages/*/src/ (D-27)"
      contains: "logging.basicConfig"
  key_links:
    - from: "verification/test_retry_mutation_gate.py"
      to: "packages/<pkg>/src/<pkg>/_transport.py (futuro Plans 2-5)"
      via: "parametrize × 4 paquetes + pytest-httpx mock 503 + assert len(httpx_mock.get_requests()) == 1"
      pattern: "@pytest.mark.parametrize.*pkg_name"
    - from: "verification/test_logging_no_token_leak.py"
      to: "packages/<pkg>/src/<pkg>/_logging.py (futuro Plans 2-5)"
      via: "caplog + RedactingFilter assertion path"
      pattern: "SECRET-LITERAL-12345"
    - from: ".github/workflows/ci.yml lint-logging step"
      to: "packages/*/src/**/*.py"
      via: "grep -rn 'logging\\.basicConfig\\|logging\\.root'"
      pattern: "grep -rn"
---

<objective>
Wave 1 cross-cutting infrastructure for Phase 8 — tests-first: agregar tenacity>=9.1.0,<10 a los 4 packages' runtime dependencies, crear los 6 cross-cutting guard tests en `verification/` (D-26), y wiring del CI grep + ruff LOG ruleset (D-27). Plan 1 NO toca ningún `packages/<pkg>/src/` — solo dependencies + tests + config + CI. Los 6 guard tests fallan RED en HEAD esperando la infra de Plans 2-5; Plans 2-5 los turnan GREEN incrementalmente per package.

Purpose: Per D-21 (LOCKED), Plan 1 entrega el scaffolding cross-cutting tests-first ANTES de que cualquier paquete reciba `_transport.py` / `_logging.py`. Esto satisface el principio "regression test asegura X" del roadmap (Success Criteria #1-5 Phase 8 explicitamente: "regression test mockea 429+Retry-After:30 y verifica delay aplicado", "regression test con POST mockeado contra 503 verifica EXACTAMENTE 1 outgoing request", etc.). Pitfall 4/5/6/7/13/15/16 todos tienen su guard test landed en Plan 1.

Output: 4 pyproject.toml updates + 1 uv.lock refresh + 6 nuevos archivos `verification/test_*.py` + 1 `.github/workflows/ci.yml` extension + 1 root `pyproject.toml` ruff LOG ruleset extension. Single commit atómico per D-21. NO `packages/<pkg>/src/` modifications.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md
@.planning/phases/08-retries-backoff-structured-logging/08-VALIDATION.md
@CLAUDE.md
@packages/ambito-financiero-client/pyproject.toml
@packages/iol-client/pyproject.toml
@packages/higyrus-client/pyproject.toml
@packages/matriz-client/pyproject.toml
@pyproject.toml
@.github/workflows/ci.yml
@verification/test_sync_async_isolation.py
@verification/test_public_surface.py
@packages/iol-client/tests/conftest.py
@packages/higyrus-client/tests/conftest.py
@packages/matriz-client/tests/conftest.py
@packages/ambito-financiero-client/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add tenacity>=9.1.0,<10 to 4 packages + refresh uv.lock + add ruff LOG ruleset + CI grep step</name>
  <files>packages/ambito-financiero-client/pyproject.toml, packages/iol-client/pyproject.toml, packages/higyrus-client/pyproject.toml, packages/matriz-client/pyproject.toml, uv.lock, pyproject.toml, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-15 — 2 nuevos kwargs; D-27 — CI grep rule; `<canonical_refs>` §Research — tenacity 9.1.4 verified)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Standard Stack" lines 156-208 — tenacity 9.1.4 + py.typed verified + installation block; §"Package Legitimacy Audit" lines 210-238 — `[OK]` slopcheck verdict; §"Code Examples" lines 1106-1128 — CI grep YAML excerpt + ruff LOG extension)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/pyproject.toml`" lines 551-570 — canonical dependencies shape; §"`pyproject.toml` root + `.github/workflows/ci.yml` (D-27 CI rule)" lines 732-746)
    - packages/ambito-financiero-client/pyproject.toml (canonical [project] dependencies shape — copy verbatim, only add tenacity entry; verify there is no existing tenacity entry)
    - packages/iol-client/pyproject.toml (idem)
    - packages/higyrus-client/pyproject.toml (idem)
    - packages/matriz-client/pyproject.toml (idem)
    - pyproject.toml (root — read [tool.ruff.lint] select block + import-linter config; locate insertion point for "LOG" ruleset)
    - .github/workflows/ci.yml (read `lint` job structure; locate insertion point AFTER `lint-imports` step and BEFORE typecheck step — keeps lint job cohesive)
  </read_first>
  <action>
    1. **Add tenacity to 4 packages** — for each of ámbito, iol, higyrus, matriz pyproject.toml:
       - Read the existing `[project] dependencies = [...]` array
       - Append `"tenacity>=9.1.0,<10",  # Phase 8 RELY-01..04 retries — per D-15` to the list
       - Keep alphabetical or chronological order (current convention is chronological per insertion — append at end)
       - Preserve trailing newlines, indentation (4 spaces), and quote style (double quotes)
       - DO NOT touch `[dependency-groups] dev` or `[build-system]` blocks

    2. **Refresh uv.lock** — run `uv sync --all-packages --all-extras --dev --frozen` will FAIL because lock is now stale; run `uv lock` first to refresh, then `uv sync --all-packages --all-extras --dev` (NOT `--frozen` — needs to write). This adds the tenacity entry to `uv.lock` for the 4 packages.

    3. **Add ruff LOG ruleset** — root `pyproject.toml` `[tool.ruff.lint] select = [...]`:
       - Current select array includes existing rules (E, W, F, I, B, UP, SIM, RUF, ASYNC, PIE, PT, RET, TID per CLAUDE.md)
       - Append `"LOG",  # Phase 8 D-27 — flake8-logging plugin (LOG001..LOG015 — root-logger-call enforcement)` to the select array
       - Preserve trailing comma + indentation
       - DO NOT modify `ignore` list or `[tool.ruff.lint.per-file-ignores]` block

    4. **Add CI grep step** — `.github/workflows/ci.yml` lint job:
       - Locate the existing `lint-imports` step
       - INSERT a new step AFTER `lint-imports` and BEFORE the next major step (typecheck or test):
         ```yaml
               - name: lint-logging (Phase 8 LOG-01 — no logging.basicConfig / logging.root in package src)
                 run: |
                   if grep -rn --include='*.py' 'logging\.basicConfig\|logging\.root' packages/*/src/; then
                     echo "::error::Phase 8 LOG-01 violated — package source must not call logging.basicConfig or logging.root.*"
                     exit 1
                   fi
         ```
       - The step uses bash + grep + exit 1 on match (any hit = lint failure). The `if` inverts: `grep` exit 0 → match found → echo + exit 1. Verified pattern per RESEARCH §"Code Examples" lines 1108-1116.

    5. **Verify tenacity importable post-sync:**
       ```
       uv run python -c "import tenacity; from tenacity import Retrying, AsyncRetrying, stop_after_attempt, wait_exponential_jitter, retry_if_exception_type, retry_if_result; print(tenacity.__version__)"
       ```
       MUST print `9.1.x` and complete sin errors. Per RESEARCH §"Standard Stack" line 204 verified.

    6. **Verify existing tests baseline preserved:**
       ```
       uv run pytest packages/ verification/ -q --no-header
       ```
       MUST report tests pass (527 baseline + zero regressions; no test file changes yet — only deps changed; if any test breaks, blocker — abort Plan 1).

    NOTE: Task 2 (creating the 6 guard tests) runs AFTER Task 1 commits the deps. The tests will fail RED on first run — that is INTENTIONAL per D-26 (tests-first; Plans 2-5 turn them GREEN).
  </action>
  <verify>
    <automated>uv lock &amp;&amp; uv sync --all-packages --all-extras --dev &amp;&amp; uv run python -c "from tenacity import Retrying, AsyncRetrying, stop_after_attempt, wait_exponential_jitter, retry_if_exception_type, retry_if_result; print('OK')" &amp;&amp; grep -c "tenacity&gt;=9.1.0,&lt;10" packages/ambito-financiero-client/pyproject.toml packages/iol-client/pyproject.toml packages/higyrus-client/pyproject.toml packages/matriz-client/pyproject.toml | tr -d '\n' &amp;&amp; grep -c "LOG" pyproject.toml &gt;= 1 &amp;&amp; grep -c "lint-logging" .github/workflows/ci.yml &amp;&amp; uv run pytest packages/ verification/ -q --no-header 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "tenacity>=9.1.0,<10" packages/ambito-financiero-client/pyproject.toml` == 1
    - Source: `grep -c "tenacity>=9.1.0,<10" packages/iol-client/pyproject.toml` == 1
    - Source: `grep -c "tenacity>=9.1.0,<10" packages/higyrus-client/pyproject.toml` == 1
    - Source: `grep -c "tenacity>=9.1.0,<10" packages/matriz-client/pyproject.toml` == 1
    - CLI: `uv run python -c "import tenacity; print(tenacity.__version__)"` prints a version `9.1.x` and exits 0
    - CLI: `uv run python -c "from tenacity import Retrying, AsyncRetrying"` exits 0
    - Source: `grep -c '"LOG"' pyproject.toml` >= 1 (ruff LOG ruleset added to `[tool.ruff.lint] select`)
    - Source: `grep -c "lint-logging" .github/workflows/ci.yml` == 1 (CI step added)
    - Source: `grep -c "logging.basicConfig" .github/workflows/ci.yml` >= 1 (grep pattern documented)
    - CLI: `uv run pytest packages/ verification/ -q --no-header 2>&1 | tail -5` shows existing 527 baseline tests pass with zero new failures
    - CLI: `uv lock --check` exits 0 (uv.lock is consistent with pyproject.toml deps)
  </acceptance_criteria>
  <done>
    tenacity dep added to 4 packages; uv.lock refreshed; ruff LOG ruleset + CI grep step wired; tenacity importable; existing 527 baseline preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the 6 cross-cutting guard tests in verification/ (RED in HEAD, GREEN per Plans 2-5)</name>
  <files>verification/test_retry_mutation_gate.py, verification/test_retry_401_reauth.py, verification/test_retry_after_cap.py, verification/test_logging_root_unchanged.py, verification/test_logging_no_token_leak.py, verification/test_async_cancellation.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-26 lines 120-127 — los 6 test files explícitos; D-22 — matriz auth_basic redaction; D-23 — matriz Risk skip; D-25 — matriz async skip; D-32 — async cancellation pattern; `<specifics>` lines 296-309 — RequestSpec extension example)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Code Examples" lines 843-903 mutation gate; 905-976 401 re-auth; 980-1050 caplog token leak; 1067-1104 logging.root unchanged; §"Common Pitfalls" lines 733-833 — Pitfalls 1/2/3/4/5/6/8/9 referenced; §"Validation Architecture" §"Wave 0 Gaps" lines 1256-1272 — 6 file checklist)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"Cross-cutting guard tests" lines 574-708 — exhaustive per-test patterns with idioms; §"Shared Patterns" lines 750-820 — `from __future__ import annotations`, `configure(token=..., token_expires_at=9_999_999_999.0)`, pytest-httpx idiom, matriz skip with literal reason)
    - verification/test_sync_async_isolation.py (Phase 7 — parametrize × 4 paquetes pattern; pytest-httpx idiom; configure(token=...) autouse pattern; matriz async branch skip; lines 41-46 _PACKAGES tuple; lines 112-208 sync vs async helpers; lines 175-176 matriz skip literal reason "matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")
    - verification/test_public_surface.py (Phase 6 — cross-package module import pattern; reusable for test_logging_root_unchanged.py shape)
    - packages/iol-client/tests/conftest.py (configure(token=..., token_expires_at=9_999_999_999.0) autouse fixture canonical shape)
    - packages/higyrus-client/tests/conftest.py (idem — higyrus configure with username/password/token)
    - packages/matriz-client/tests/conftest.py (idem — matriz configure with X-Auth-Token)
    - packages/ambito-financiero-client/tests/conftest.py (ámbito no auth — configure base_url only)
  </read_first>
  <behavior>
    Each of the 6 guard tests follows the SAME tests-first idiom:
    - Tests CALL into `pkg.<top-level fn>(...)` (NOT `pkg.Client()._request(...)`) — exercises the full shell path including the future `_transport.py` retry loop.
    - Tests use `pytest-httpx` for wire-request count + header inspection (`httpx_mock.add_response(...)` + `httpx_mock.get_requests()`).
    - Tests use `configure(token="SECRET-LITERAL-12345", token_expires_at=9_999_999_999.0)` to inject sentinels.
    - In HEAD (Phase 7 final state, no Phase 8 infra), tests fail RED:
      - `test_retry_mutation_gate.py` — no mutation gate exists; POST against 503 will retry per any future-added logic → assertion `len(requests) == 1` may PASS today (Phase 7 has no retries) — verify TEST FAILS by mocking with 2 responses and asserting only 1 is consumed. The assertion `assert len(httpx_mock.get_requests()) == N` works against any state. Tests are written so they assert the POST-Phase-8 invariants directly — they may PASS coincidentally in HEAD or FAIL — either is acceptable per D-26's "Tests fall fail RED in HEAD esperando la infra de Plans 2-5" (the principle is they GUARD against regressions once infra lands).
    - All 6 tests follow `from __future__ import annotations` + module docstring + parametrize idiom from `test_sync_async_isolation.py`.
    - matriz async skip uses literal verbatim reason: `pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")` (matches Phase 7 D-10 idiom).

    Test cases per file:

    **`verification/test_retry_mutation_gate.py`** — parametrize × 4 paquetes:
      - For each paquete, identify a MUTATING builder (POST or matriz HTTP GET con `idempotent=False`):
        - `ambito_financiero_client`: NO mutating endpoints (all GET, all idempotent). Test uses ámbito to exercise the OPPOSITE: GET against 503 SHOULD retry (after Plan 2 lands). For HEAD (Phase 7), one outgoing request only. Skip ámbito with pytest.skip("ámbito has no mutating endpoints — RELY-03 not applicable") OR test the GET retry path (recommended: latter — turns GREEN in Plan 2 when ámbito ships RetryTransport).
        - `iol_client`: planner picks `iol_client.login()` or the lowest-level mutating call available. Per RESEARCH note line 858-861, planner can use the Client._request path with a synthetic non-idempotent spec. Recommendation: use `_get_default()._request(_core.build_login_request(_get_default()._state))` with `idempotent` flipped to False via monkeypatch — TODO at execute time.
        - `higyrus_client`: `higyrus_client.login()` mutating path; OR mock a hypothetical POST (planner picks at execute time per discretion section line 858-866).
        - `matriz_client`: `matriz_client.new_order(symbol="GGAL", side="BUY", qty=1, account="test", price=100.0)` — Primary API HTTP GET con `idempotent=False` (CRITICAL: this is the duplicate-order risk per Pitfall 4 / D-01).
      - For each: mock 503 twice; call mutating fn; expect AuthError/APIError/RateLimitError; assert `len(httpx_mock.get_requests()) == 1` (no retry).
      - SECOND parametrize: GET path → mock 503 twice → assert `len(httpx_mock.get_requests()) == N` (where N = max_attempts; turns GREEN post-Plans 2-5).

    **`verification/test_retry_401_reauth.py`** — parametrize × auth paquetes (iol, higyrus, matriz Primary; ámbito skip; matriz Risk skip per D-23):
      - For each: configure with `token="STALE-TOKEN"`, `token_expires_at=9_999_999_999.0`; mock 401 (initial GET); mock 200 (login flow with FRESH-TOKEN); mock 200 (retry with fresh token). Fire `pkg.get_X(...)`. Assert `len(httpx_mock.get_requests()) == 3` (1 stale + 1 login + 1 fresh) AND last request's Authorization/X-Auth-Token header contains "FRESH-TOKEN" verbatim.
      - 401→401 chain: mock 401 twice + login 200 + 401 → assert exactly 3 wire requests + `<Pkg>AuthError` raised.
      - matriz Risk API branch: configure with `auth_basic=("u", "p")`; mock 401; assert `<Pkg>AuthError` raised AND `len(httpx_mock.get_requests()) == 1` (per D-23 — NO re-auth attempt).

    **`verification/test_retry_after_cap.py`** — cross-cutting (uses iol_client per RESEARCH §"`verification/test_retry_after_cap.py`" line 673):
      - Mock 429 with `headers={"Retry-After": "600"}` (10 minutes — would block indefinitely without cap); mock 200 success on retry.
      - `t0 = time.monotonic(); pkg.get_instruments("argentina"); elapsed = time.monotonic() - t0`
      - Assert `elapsed < 65.0` (cap 60s + tolerance — per D-04). MUST NOT block for the full 600s server-suggested delay.
      - Assert `len(httpx_mock.get_requests()) == 2` (retry happened).

    **`verification/test_logging_root_unchanged.py`** — cross-cutting (no parametrize):
      - `handlers_before = list(logging.root.handlers)`; `filters_before = list(logging.root.filters)`; `level_before = logging.root.level`
      - For each of 4 packages: `importlib.import_module(pkg_name)` (reimport-safe)
      - Assert `list(logging.root.handlers) == handlers_before` AND filters AND level
      - NOTE: pytest's caplog installs a root handler — use `monkeypatch.setattr(logging.root, "handlers", original_handlers, raising=False)` OR snapshot directly via `pytest -p no:logging` per RESEARCH §"D-26"; planner picks at execute time.

    **`verification/test_logging_no_token_leak.py`** — parametrize × 4 paquetes:
      - SECRET = `"SECRET-LITERAL-12345"`
      - For each paquete: configure with `token=SECRET` (3 auth pkgs) or `base_url=f"https://{SECRET}.test"` (ámbito sanity check inverse — ámbito has no token redaction surface; test asserts SECRET CAN appear in url-field since base_url is part of the public surface).
      - `caplog.set_level(logging.DEBUG, logger=pkg_name)`
      - Fire mocked request: `pkg.get_X(...)` with httpx_mock added_response of expected payload.
      - For each `record in caplog.records`:
        - `assert SECRET not in record.getMessage()`
        - `if record.args: assert SECRET not in str(record.args)`
        - `for k, v in record.__dict__.items(): if isinstance(v, str): assert SECRET not in v`
      - Special handling per D-22 matriz auth_basic: if pkg is matriz, also test with `auth_basic=("user", SECRET)` — assert password redacted in any auth_basic logging.

    **`verification/test_async_cancellation.py`** — parametrize × async paquetes (ámbito, iol, higyrus; matriz skip):
      - For each: `if pkg_name == "matriz_client": pytest.skip("matriz aio.py REST stub hasta Phase 10 REFAC-04 + TokenStore")` (verbatim from `test_sync_async_isolation.py:175-176`)
      - Configure async client; mock 503 twice (would normally trigger retry + backoff sleep ~1-2s).
      - `with pytest.raises(asyncio.TimeoutError): await asyncio.wait_for(pkg.aio.get_X(...), timeout=0.5)`
      - Assert elapsed time < 1.0s (cancellation interrupted backoff sleep — NOT waited full 30s retry budget).

    Module-level invariants for ALL 6 files:
    - `from __future__ import annotations` MANDATORY (project convention per CLAUDE.md).
    - Module docstring documenting purpose + decision refs (D-XX inline).
    - No relative imports (TID rule).
    - Use `pytest_httpx.HTTPXMock` fixture type hint.
    - Use `importlib.import_module(pkg_name)` for parametrize (matches `test_sync_async_isolation.py` idiom).
  </behavior>
  <action>
    1. Create `verification/test_retry_mutation_gate.py` (parametrize × 4 packages; mock 503 POST → 1 request; mock 503 GET → N requests). Use RESEARCH §"Code Examples" lines 843-903 + PATTERNS §lines 574-603 as templates. Per CONTEXT D-26 + D-01 + D-07.

    2. Create `verification/test_retry_401_reauth.py` (parametrize × iol/higyrus/matriz; ámbito skip; matriz Risk branch). Use RESEARCH §lines 905-976. Per D-02 + D-23.

    3. Create `verification/test_retry_after_cap.py` (cross-cutting; uses iol_client; mock 429 + Retry-After:600 → cap at 60s). Use PATTERNS §lines 671-688. Per D-04.

    4. Create `verification/test_logging_root_unchanged.py` (cross-cutting; no parametrize; snapshot root.handlers/filters/level + reimport 4 packages + assert unchanged). Use RESEARCH §lines 1067-1104. Per D-14 + LOG-01.

    5. Create `verification/test_logging_no_token_leak.py` (parametrize × 4 packages; caplog + SECRET-LITERAL-12345 + assertions on getMessage()/args/__dict__). Use RESEARCH §lines 980-1050. Per D-10 + D-22 + LOG-02.

    6. Create `verification/test_async_cancellation.py` (parametrize × ámbito/iol/higyrus; matriz skip with verbatim reason; asyncio.wait_for + 503→503 → TimeoutError sin full retry budget). Use PATTERNS §lines 690-708. Per D-32 + Pitfall 16.

    7. Run `uv run pytest verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_retry_after_cap.py verification/test_logging_root_unchanged.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py -v 2>&1 | tail -50` — capture output. Most will FAIL RED (no `_transport.py` yet) — record which fail and which pass coincidentally for forensic record in SUMMARY.md. Failure mode acceptable per Plan 1 Wave 0 design (D-26 — Plans 2-5 turn GREEN).

    8. Run `uv run pytest packages/ verification/ -q --no-header 2>&1 | tail -10` — capture full count. Verify baseline 527 still pass + 6 new test FILES collected (each adds ≥1 test case; total test count rises by ≥10-15 cases).

    9. Run `uv run ruff check verification/` — MUST exit 0 (ruff LOG ruleset now enabled in Task 1; verifies the 6 new files don't accidentally call `logging.basicConfig` themselves).
  </action>
  <verify>
    <automated>test -f verification/test_retry_mutation_gate.py &amp;&amp; test -f verification/test_retry_401_reauth.py &amp;&amp; test -f verification/test_retry_after_cap.py &amp;&amp; test -f verification/test_logging_root_unchanged.py &amp;&amp; test -f verification/test_logging_no_token_leak.py &amp;&amp; test -f verification/test_async_cancellation.py &amp;&amp; grep -l "@pytest.mark.parametrize" verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py &amp;&amp; grep -c "from __future__ import annotations" verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_retry_after_cap.py verification/test_logging_root_unchanged.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py | grep -v ':0' &amp;&amp; grep -c "SECRET-LITERAL-12345" verification/test_logging_no_token_leak.py &gt;= 1 &amp;&amp; grep -c "matriz aio.py REST stub hasta Phase 10" verification/test_async_cancellation.py &gt;= 1 &amp;&amp; uv run ruff check verification/ &amp;&amp; uv run pytest verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_retry_after_cap.py verification/test_logging_root_unchanged.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py --collect-only -q 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest packages/ -q --no-header 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: 6 test files exist in `verification/` (test_retry_mutation_gate.py, test_retry_401_reauth.py, test_retry_after_cap.py, test_logging_root_unchanged.py, test_logging_no_token_leak.py, test_async_cancellation.py)
    - Source: `grep -c "from __future__ import annotations" verification/test_retry_mutation_gate.py` == 1 (and idem for the other 5 files)
    - Source: `grep -c "@pytest.mark.parametrize" verification/test_retry_mutation_gate.py` >= 1
    - Source: `grep -c "@pytest.mark.parametrize" verification/test_retry_401_reauth.py` >= 1
    - Source: `grep -c "@pytest.mark.parametrize" verification/test_logging_no_token_leak.py` >= 1
    - Source: `grep -c "@pytest.mark.parametrize" verification/test_async_cancellation.py` >= 1
    - Source: `grep -c "SECRET-LITERAL-12345" verification/test_logging_no_token_leak.py` >= 1 (literal sentinel matches D-26 + RESEARCH §line 990)
    - Source: `grep -c "matriz aio.py REST stub hasta Phase 10" verification/test_async_cancellation.py` >= 1 (verbatim Phase 7 D-10 skip reason)
    - Source: `grep -c "Retry-After" verification/test_retry_after_cap.py` >= 1
    - Source: `grep -c "logging.root.handlers" verification/test_logging_root_unchanged.py` >= 1
    - Source: `grep -c "len(httpx_mock.get_requests())" verification/test_retry_mutation_gate.py` >= 1 (wire-request count assertion)
    - Source: `grep -c "auth_basic" verification/test_retry_401_reauth.py` >= 1 (D-23 matriz Risk branch covered)
    - CLI: `uv run ruff check verification/` exits 0 (new tests pass lint; ruff LOG rules don't fire false-positive)
    - CLI: `uv run pytest verification/test_retry_mutation_gate.py verification/test_retry_401_reauth.py verification/test_retry_after_cap.py verification/test_logging_root_unchanged.py verification/test_logging_no_token_leak.py verification/test_async_cancellation.py --collect-only -q 2>&1 | tail -5` shows `>= 10 tests collected` (parametrize × 4 packages over 4 files + 2 standalone tests = at minimum 10 test cases)
    - CLI: `uv run pytest packages/ -q --no-header 2>&1 | tail -5` shows packages baseline 527 tests still pass (Plan 1 added zero regressions to packages — only deps + verification + config)
  </acceptance_criteria>
  <done>
    6 cross-cutting guard tests exist + collect correctly + ruff-clean; baseline 527 packages tests still GREEN. Tests RED expected for invariants (mutation gate / 401 chain / Retry-After cap / no-token-leak / async cancellation) — they will turn GREEN as Plans 2-5 land per-package infra.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `packages/<pkg>/pyproject.toml [project] dependencies` ← tenacity 9.1.4 install | Supply chain — Phase 8 introduces 1 new runtime dep; legitimacy audit in 08-RESEARCH.md §"Package Legitimacy Audit" returns `[OK]` per slopcheck (CRITICAL: this is the only `[ASSUMED]` package in v1.1) |
| `.github/workflows/ci.yml lint-logging step` ← bash `grep` execution | Local lint gate — exit 1 on `logging.basicConfig`/`logging.root` match prevents accidental library logger hijack in future commits |
| `verification/test_*.py` × 6 new guard tests ← `pkg.configure(token="SECRET-LITERAL-12345")` | Test fixtures inject token sentinels — verifies the future `RedactingFilter` (Plans 2-5) actually scrubs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | Mutation gate (D-01) — POST 503 retry → duplicate matriz orders | mitigate | `verification/test_retry_mutation_gate.py` lands RED in HEAD, turns GREEN when Plans 2-5 implement `request.extensions["idempotent"]` gate in shell `_request()` (Pitfall 4 prevention; CRITICAL — duplicate-order risk on matriz Primary API) |
| T-8-02 | Denial-of-Service (self-induced) | 401 retry storm — expired token in retry_on= causes infinite loop | mitigate | `verification/test_retry_401_reauth.py` lands RED, GREEN post-Plans 3-5 (iol/higyrus/matriz auth shells implement D-02 exactly-once re-auth); transport `retry_on=` NEVER includes AuthError per D-07 |
| T-8-03 | Tampering | Library hijacks root logger (downstream apps lose log config) | mitigate | (a) `verification/test_logging_root_unchanged.py` snapshot guard; (b) ruff LOG015 enabled (`select=["LOG"]` in root pyproject); (c) CI grep step `lint-logging` blocks `logging.basicConfig` (which LOG015 does NOT cover); all 3 land in Plan 1 (D-14, D-27) |
| T-8-04 | Information Disclosure | Credentials leak in DEBUG log records (Bearer/X-Auth-Token/refresh_token/password) | mitigate | `verification/test_logging_no_token_leak.py` lands RED with SECRET-LITERAL-12345 sentinel; turns GREEN when Plans 2-5 implement `RedactingFilter` (D-10 + D-22) |
| T-8-05 | Availability | Async cancellation swallowed during retry (Ctrl-C delayed up to retry budget) | mitigate | `verification/test_async_cancellation.py` lands; asserts `asyncio.wait_for(..., timeout=0.5) → TimeoutError < 1.0s`; turns GREEN when Plans 2-4 implement `AsyncRetryTransport` using `asyncio.sleep` via tenacity AsyncRetrying (D-32; matriz skip per D-25) |
| T-8-06 | Tampering | tenacity supply chain — package legitimacy (9.1.4 from PyPI) | accept | `[OK]` per slopcheck scan; `py.typed` verified; mypy strict compatible; 12+ year-old library with millions of installs (anthropic/openai SDK dependencies); semver pinning `>=9.1.0,<10` prevents major-version drift |
| T-8-SC | Tampering (Supply Chain) | npm/pip/cargo installs | accept | Only 1 new package (tenacity); slopcheck verdict `[OK]`; per RESEARCH §"Package Legitimacy Audit". No `[ASSUMED]`/`[SUS]` blocking checkpoints required — `[OK]` is auto-approvable per `workflow.auto_advance` |
</threat_model>

<verification>
- `tenacity>=9.1.0,<10` added to 4 packages' pyproject.toml runtime deps
- `uv.lock` refreshed; `uv lock --check` exits 0
- Ruff LOG ruleset enabled in root pyproject.toml `[tool.ruff.lint] select`
- CI `.github/workflows/ci.yml` lint-logging grep step added (blocks `logging.basicConfig`/`logging.root` in `packages/*/src/`)
- 6 cross-cutting guard tests created in `verification/` (D-26): mutation gate, 401 re-auth, Retry-After cap, root logger unchanged, no-token-leak, async cancellation
- All 6 tests pass lint (ruff check); parametrize idioms match Phase 7 conventions
- Existing 527 baseline tests GREEN (zero regression — Plan 1 touches NO `packages/<pkg>/src/`)
- Tests fail RED for the invariants they guard (mutation gate, etc.) — expected per D-26; Plans 2-5 turn them GREEN incrementally
</verification>

<success_criteria>
- tenacity 9.1.x importable via `uv run python -c "from tenacity import Retrying, AsyncRetrying"` exit 0
- 4 packages' pyproject.toml each contain `tenacity>=9.1.0,<10` exactly once
- 6 `verification/test_*.py` files exist + collect correctly with pytest
- CI lint-logging step bash-valid (grep + exit 1 on match)
- ruff LOG015 active (catches `logging.root.*`)
- 527 baseline preserved (no `packages/<pkg>/src/` touched)
- Plan 1 SUMMARY.md documents which guard tests FAIL RED in HEAD (forensic record per D-26 design — those that fail are the gates Plans 2-5 turn GREEN)
- Single atomic commit per D-21
</success_criteria>

<output>
Create `.planning/phases/08-retries-backoff-structured-logging/08-01-SUMMARY.md` documenting:
- tenacity 9.1.4 verified import + py.typed status
- 4 pyproject.toml diffs (just the tenacity line added)
- 6 guard test files created + their initial RED/GREEN status (forensic record; expected most RED per D-26)
- ruff LOG ruleset addition + CI grep step location
- Test count delta: baseline 527 + ~10-15 new collected
- Atomic commit hash with message per RESEARCH §"Commit message patterns": `feat(verification): RetryTransport + _logging scaffolds + cross-cutting guard tests (RELY-01..04, LOG-01..03)`
</output>
