---
phase: 08-retries-backoff-structured-logging
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - packages/higyrus-client/src/higyrus_client/_transport.py
  - packages/higyrus-client/src/higyrus_client/_atransport.py
  - packages/higyrus-client/src/higyrus_client/_logging.py
  - packages/higyrus-client/src/higyrus_client/__init__.py
  - packages/higyrus-client/src/higyrus_client/_core.py
  - packages/higyrus-client/src/higyrus_client/client.py
  - packages/higyrus-client/src/higyrus_client/aio.py
  - packages/higyrus-client/tests/test_transport.py
  - packages/higyrus-client/tests/test_logging.py
  - verification/snapshots/higyrus_client-surface.txt
autonomous: true
requirements: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
requirements_addressed: [RELY-01, RELY-02, RELY-03, RELY-04, LOG-01, LOG-02, LOG-03]
tags: [phase-08, higyrus, atomic-commit, rely, log, account-id-propagation, json-password-redaction]
user_setup: []

must_haves:
  truths:
    - "`higyrus_client/_transport.py` define `RetryTransport(httpx.HTTPTransport)` mirror Plan 2 ámbito + Plan 3 iol (mechanical duplicate con `_LOGGER_NAME='higyrus_client'`)"
    - "`higyrus_client/_atransport.py` define `AsyncRetryTransport(httpx.AsyncHTTPTransport)` mirror (D-32)"
    - "`higyrus_client/_logging.py` define `RedactingFilter` con patterns adicionales per D-10: Bearer + X-Auth-Token + password baseline + Higyrus JSON `{\"password\":\"...\"}` (login body per `_core.py:189-197`) + Higyrus JSON `{\"token\":\"...\"}` (login response) + URL query `cuit=...` redaction (PII per PATTERNS line 259)"
    - "`higyrus_client/__init__.py` invoca `_logging.attach()` con idempotency + `del` cleanup (LOG-01)"
    - "`_core.py RequestSpec` extendido con `endpoint_name: str = ''` (D-09) + `account_id: str | None = None` (D-11 — higyrus-specific)"
    - "GET builders (movimientos, posicion_valuada, posiciones, etc.) flippean `idempotent=True` + setean `endpoint_name=<fn name>` + setean `account_id=id_cuenta` cuando el builder recibe `id_cuenta` (D-11)"
    - "`build_login_request` (`_core.py:189-197`) marcado `idempotent=True` (D-03)"
    - "`Client.__init__` + `AsyncClient.__init__` + `configure()` extendidos con 2 kwargs `max_retries=2` + `http_client=None` (D-15)"
    - "Shell `_request()` (sync + async) genera `request_id`, setea `extensions['idempotent'/'request_id'/'endpoint_name'/'account_id']` (account_id desde `spec.account_id` cuando non-None per D-11), implementa 401 re-auth-once vía pattern iol (D-02 — same shape, HigyrusAuthError → state.token=None → _ensure_token() → retry once)"
    - "`_ensure_token()` (login path) atraviesa el mismo `state.http_client` con RetryTransport (D-29) — login retry-eable en 5xx; password JSON redaction cubre el wire body"
    - "URL query string scrubbing en log records: la URL en el `extra['url']` del log preserva el path pero los query params sensibles (cuit) son redactados via RedactingFilter scan de record.__dict__ values"
    - "`verification/snapshots/higyrus_client-surface.txt` actualizado con 2 nuevos kwargs (D-28)"
    - "Cross-cutting guard tests: `test_retry_401_reauth.py[higyrus]` GREEN; `test_logging_no_token_leak.py[higyrus]` GREEN (incluye SECRET no aparece en JSON password ni en token)"
    - "higyrus existing tests (Phase 7 baseline) GREEN post-cambios; URL-encoding quirk (Phase 7 D-XX) preservado"
  artifacts:
    - path: "packages/higyrus-client/src/higyrus_client/_transport.py"
      provides: "RetryTransport sync mirror"
      contains: "class RetryTransport"
      min_lines: 90
    - path: "packages/higyrus-client/src/higyrus_client/_atransport.py"
      provides: "AsyncRetryTransport async mirror"
      contains: "class AsyncRetryTransport"
      min_lines: 90
    - path: "packages/higyrus-client/src/higyrus_client/_logging.py"
      provides: "RedactingFilter con Higyrus JSON password + token redaction + cuit query param scrubbing"
      contains: "password"
      min_lines: 75
    - path: "packages/higyrus-client/tests/test_transport.py"
      provides: "Unit tests for higyrus RetryTransport + account_id propagation in extensions"
      contains: "account_id"
      min_lines: 75
    - path: "packages/higyrus-client/tests/test_logging.py"
      provides: "Unit tests for higyrus RedactingFilter — JSON password, JSON token, cuit query scrubbing"
      contains: "password"
      min_lines: 60
    - path: "verification/snapshots/higyrus_client-surface.txt"
      provides: "higyrus snapshot con 2 nuevos kwargs"
      contains: "max_retries"
  key_links:
    - from: "higyrus_client._core.build_movimientos_request"
      to: "higyrus_client._transport.RetryTransport (via state.http_client.send) + log records[extra.account_id]"
      via: "RequestSpec.account_id=id_cuenta → request.extensions['account_id']"
      pattern: "account_id"
    - from: "higyrus_client.client.Client._request 401 path"
      to: "higyrus_client.client.Client._ensure_token (re-auth)"
      via: "except HigyrusAuthError → state.token=None → _ensure_token() → retry once"
      pattern: "HigyrusAuthError"
    - from: "higyrus_client._logging.RedactingFilter"
      to: "Higyrus login JSON body + response token redaction"
      via: "_PASSWORD_JSON_RE + _TOKEN_JSON_RE substitution"
      pattern: "password"
---

<objective>
higyrus Plan 8-04: replicar el patrón iol (Plan 3) en `higyrus-client` con los extras higyrus-specific: `RequestSpec.account_id: str | None = None` (D-11 — propagado a `request.extensions['account_id']` para structured log field per D-09 condicional), `_logging.py` con `RedactingFilter` cubriendo Higyrus JSON `{"password":"..."}` body (login per `_core.py:189-197`) Y JSON `{"token":"..."}` response, plus URL query `cuit=...` PII redaction. Plus el delivery sync+async cross-package estándar: `_transport.py`, `_atransport.py`, `_core.py` extensions, Client/AsyncClient/configure() 2 kwargs, snapshot update. Per-package atomic commit per D-21.

Purpose: higyrus es el segundo paquete con auth (después de iol) y el primero con `account_id` propagation (D-11). El pattern 401 re-auth-once del Plan 3 se duplica con HigyrusAuthError; el extra delta es account_id passing en `request.extensions` y RedactingFilter cubriendo JSON password (vs URL-encoded password de iol). higyrus URL-encoding quirk (Phase 7 D-XX) MUST be preserved — Plan 8-04 NO toca esa lógica, solo agrega retry/log wrapping. Plan 8-04 establece el "account_id propagation pattern" que Plan 8-05 (matriz) replica con `auth_basic` Risk API tweak.

Output: 1 commit atómico per D-21 con 2 nuevos módulos transport (`_transport.py`, `_atransport.py`) + 1 nuevo módulo logging (`_logging.py`) + 4 extends (`_core.py`, `client.py`, `aio.py`, `__init__.py`) + 2 nuevos test files + 1 snapshot update. Commit message: `feat(higyrus): retries + structured logging — RetryTransport, RedactingFilter, account_id propagation, JSON password redaction (RELY-01..04, LOG-01..03)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md
@.planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md
@.planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md
@.planning/phases/08-retries-backoff-structured-logging/08-03-SUMMARY.md
@CLAUDE.md
@packages/higyrus-client/src/higyrus_client/_state.py
@packages/higyrus-client/src/higyrus_client/_core.py
@packages/higyrus-client/src/higyrus_client/client.py
@packages/higyrus-client/src/higyrus_client/aio.py
@packages/higyrus-client/src/higyrus_client/__init__.py
@packages/higyrus-client/src/higyrus_client/_params.py
@packages/higyrus-client/src/higyrus_client/exceptions.py
@packages/higyrus-client/tests/conftest.py
@verification/snapshots/higyrus_client-surface.txt
@packages/iol-client/src/iol_client/_transport.py
@packages/iol-client/src/iol_client/_atransport.py
@packages/iol-client/src/iol_client/_logging.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@verification/test_retry_mutation_gate.py
@verification/test_retry_401_reauth.py
@verification/test_retry_after_cap.py
@verification/test_logging_root_unchanged.py
@verification/test_logging_no_token_leak.py
@verification/test_async_cancellation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create higyrus _transport.py + _atransport.py + _logging.py (mirror iol Plan 3 + Higyrus JSON password/token/cuit patterns)</name>
  <files>packages/higyrus-client/src/higyrus_client/_transport.py, packages/higyrus-client/src/higyrus_client/_atransport.py, packages/higyrus-client/src/higyrus_client/_logging.py, packages/higyrus-client/tests/test_transport.py, packages/higyrus-client/tests/test_logging.py</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-01, D-07, D-08 — RetryTransport; D-10 — RedactingFilter shape; D-11 — `account_id` propagation via RequestSpec; D-22 — auth_basic redaction NOT applicable to higyrus; D-29 — login through RetryTransport)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 4 lines 605-682 — RedactingFilter canonical; §"Code Examples" — token leak test; §"Common Pitfalls" — credential redaction)
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"Per-package redaction recipes" line 254-260 — higyrus: JSON password (login body, see `_core.py:189-197`); url query `cuit=...` redaction (Higyrus PII); JSON `{"token":"..."}` (login response))
    - packages/higyrus-client/src/higyrus_client/_core.py (locate `build_login_request` at lines ~189-197 — login body uses JSON `{"username":..., "password":...}` per Higyrus API; locate other GET builders that take `id_cuenta` to identify which need account_id propagation)
    - packages/iol-client/src/iol_client/_transport.py (Plan 3 baseline — verbatim duplicate template; higyrus _transport.py changes ONLY `_LOGGER_NAME = "higyrus_client"` + module docstring)
    - packages/iol-client/src/iol_client/_atransport.py (Plan 3 baseline — verbatim duplicate)
    - packages/iol-client/src/iol_client/_logging.py (Plan 3 baseline — extend with JSON token regex + cuit query regex; drop refresh_token regex which is iol-specific)
    - packages/higyrus-client/src/higyrus_client/exceptions.py (HigyrusAuthError class signature — used in 401 re-auth catch in Task 2)
  </read_first>
  <behavior>
    Mirror the iol Plan 3 modules with these higyrus-specific deltas:

    **`packages/higyrus-client/src/higyrus_client/_transport.py`** (~95 LOC — mechanical duplicate of iol _transport.py):
    - Identical body except `_LOGGER_NAME = "higyrus_client"`
    - Module docstring updated for higyrus

    **`packages/higyrus-client/src/higyrus_client/_atransport.py`** (~95 LOC):
    - Identical async mirror
    - Import shared constants from `higyrus_client._transport`

    **`packages/higyrus-client/src/higyrus_client/_logging.py`** (~80 LOC — extends with higyrus JSON token/password/cuit patterns):
    - Module docstring: `"""RedactingFilter + attach() — higyrus_client Phase 8 LOG-01/02/03 + Higyrus JSON password (login body) + JSON token (login response) + cuit URL query redaction (D-10 + PII)."""`
    - `from __future__ import annotations`
    - Imports: `logging`, `re`
    - Module-level regex (higyrus-specific set per PATTERNS line 259):
      ```python
      _BEARER_RE = re.compile(r"Bearer\s+[A-Za-z0-9._\-]+")
      _X_AUTH_TOKEN_RE = re.compile(r"(X-Auth-Token\s*:\s*)[A-Za-z0-9._\-]+", re.IGNORECASE)
      _PASSWORD_URLENC_RE = re.compile(r"(password=)[^&\s]+")
      _PASSWORD_JSON_RE = re.compile(r'("password"\s*:\s*")[^"]+(")')
      _TOKEN_JSON_RE = re.compile(r'("token"\s*:\s*")[^"]+(")')        # Higyrus login response: {"token":"..."}
      _CUIT_QUERY_RE = re.compile(r"(cuit=)\d+")                         # PII: higyrus URL query cuit=20XXXXX
      ```
      Notes:
      - NO `_REFRESH_TOKEN_*` regex (iol-specific, not higyrus)
      - `_TOKEN_JSON_RE` covers Higyrus login response payload `{"token": "..."}`
      - `_CUIT_QUERY_RE` covers Higyrus URL query params that include cuit (Argentine tax ID — PII)
    - `def _redact(text: str) -> str`: apply all 6 substitutions in order (Bearer, X-Auth-Token, password URL, password JSON, token JSON, cuit query)
    - `class RedactingFilter(logging.Filter)`: same generic filter body per RESEARCH §Pattern 4 lines 647-672
    - `def attach() -> None`: attach to `getLogger("higyrus_client")` with NullHandler + RedactingFilter idempotency
    - `__all__ = ["RedactingFilter", "attach"]`

    **`packages/higyrus-client/tests/test_transport.py`** (~80 LOC):
    - Same baseline tests as iol Plan 3
    - PLUS higyrus-specific:
      - `test_account_id_propagates_to_extensions`: build a RequestSpec with `account_id="ACC-123"` → call `_request()` → verify the wire request had `extensions["account_id"] == "ACC-123"` → verify the log record's `extra["account_id"] == "ACC-123"`

    **`packages/higyrus-client/tests/test_logging.py`** (~65 LOC):
    - Same baseline tests
    - PLUS higyrus-specific:
      - `test_redact_password_json_login_body`: `msg='{"username":"u","password":"secret123"}'` → after filter, password=*** in msg
      - `test_redact_token_json_login_response`: `msg='{"token":"abc.def.ghi","sessionId":"X"}'` → after filter, token=*** in msg
      - `test_redact_cuit_query`: `msg="GET /api/Cuentas?cuit=20123456789&page=1"` → after filter, `cuit=` value redacted
      - `test_account_id_NOT_redacted`: `extra={"account_id": "ACC-123"}` → NOT redacted (account_id is operational metadata, NOT PII per D-09)
  </behavior>
  <action>
    1. Create `packages/higyrus-client/src/higyrus_client/_transport.py` by reading Plan 3's iol `_transport.py` and substituting `iol_client` → `higyrus_client` for `_LOGGER_NAME` constant + module docstring. ALL other code identical.

    2. Create `packages/higyrus-client/src/higyrus_client/_atransport.py` by analogous substitution. Import shared constants from `higyrus_client._transport`.

    3. Create `packages/higyrus-client/src/higyrus_client/_logging.py` per RESEARCH §Pattern 4 lines 605-682 baseline + higyrus-specific 6 redaction regex (Bearer, X-Auth-Token, password URL, password JSON, token JSON, cuit query). Per D-10 + PATTERNS line 259.

    4. Create `packages/higyrus-client/tests/test_transport.py` — duplicate iol Plan 3's shape with higyrus imports; add `test_account_id_propagates_to_extensions`.

    5. Create `packages/higyrus-client/tests/test_logging.py` — duplicate iol Plan 3's shape; add 4 higyrus-specific tests (JSON password, JSON token, cuit query, account_id NOT redacted).

    6. Run `uv run pytest packages/higyrus-client/tests/test_transport.py packages/higyrus-client/tests/test_logging.py -v` — MUST GREEN.
  </action>
  <verify>
    <automated>test -f packages/higyrus-client/src/higyrus_client/_transport.py &amp;&amp; test -f packages/higyrus-client/src/higyrus_client/_atransport.py &amp;&amp; test -f packages/higyrus-client/src/higyrus_client/_logging.py &amp;&amp; test -f packages/higyrus-client/tests/test_transport.py &amp;&amp; test -f packages/higyrus-client/tests/test_logging.py &amp;&amp; grep -c "class RetryTransport" packages/higyrus-client/src/higyrus_client/_transport.py &gt;= 1 &amp;&amp; grep -c "class AsyncRetryTransport" packages/higyrus-client/src/higyrus_client/_atransport.py &gt;= 1 &amp;&amp; grep -c "class RedactingFilter" packages/higyrus-client/src/higyrus_client/_logging.py &gt;= 1 &amp;&amp; grep -c "higyrus_client" packages/higyrus-client/src/higyrus_client/_transport.py &gt;= 1 &amp;&amp; grep -c "_TOKEN_JSON_RE\|_PASSWORD_JSON_RE" packages/higyrus-client/src/higyrus_client/_logging.py &gt;= 2 &amp;&amp; grep -c "_CUIT_QUERY_RE\|cuit" packages/higyrus-client/src/higyrus_client/_logging.py &gt;= 1 &amp;&amp; uv run pytest packages/higyrus-client/tests/test_transport.py packages/higyrus-client/tests/test_logging.py -v 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_transport.py` exists with `RetryTransport` and `_LOGGER_NAME = "higyrus_client"`
    - Source: `_atransport.py` exists with `AsyncRetryTransport` using `AsyncRetrying`
    - Source: `_logging.py` exists with `RedactingFilter` AND `_PASSWORD_JSON_RE` AND `_TOKEN_JSON_RE` AND cuit query regex (per PATTERNS line 259 higyrus-specific)
    - Source: `_logging.py` does NOT contain `_REFRESH_TOKEN` (iol-specific, not higyrus) — verifies pattern isolation
    - Source: `_logging.py` attaches to `getLogger("higyrus_client")`
    - CLI: `uv run pytest packages/higyrus-client/tests/test_transport.py -v` exits 0 with ≥7 tests passing (includes account_id propagation)
    - CLI: `uv run pytest packages/higyrus-client/tests/test_logging.py -v` exits 0 with ≥8 tests passing (includes 4 higyrus-specific)
    - CLI: `uv run ruff check packages/higyrus-client/` exits 0
    - CLI: `uv run mypy --strict packages/higyrus-client/` exits 0
  </acceptance_criteria>
  <done>
    higyrus's 3 new private modules exist + pass unit tests; JSON password, JSON token, cuit query redaction work; account_id propagation tested; ruff + mypy strict GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend higyrus _core.py + client.py + aio.py + __init__.py shell with account_id propagation + 401 re-auth + 2 new kwargs; update snapshot (atomic commit per D-21)</name>
  <files>packages/higyrus-client/src/higyrus_client/_core.py, packages/higyrus-client/src/higyrus_client/client.py, packages/higyrus-client/src/higyrus_client/aio.py, packages/higyrus-client/src/higyrus_client/__init__.py, verification/snapshots/higyrus_client-surface.txt</files>
  <read_first>
    - .planning/phases/08-retries-backoff-structured-logging/08-CONTEXT.md (`<decisions>` D-02 — 401 re-auth-once; D-03 — login idempotent=True; D-09 — account_id is conditional structured field for higyrus + matriz; D-11 — `account_id: str | None = None` in RequestSpec; D-15 — 2 new kwargs)
    - .planning/phases/08-retries-backoff-structured-logging/08-RESEARCH.md (§"Architecture Patterns" Pattern 3 lines 549-602 — 401 re-auth pattern; §"Assumptions Log" A8 — account_id via getattr(spec, 'account_id', None))
    - .planning/phases/08-retries-backoff-structured-logging/08-PATTERNS.md (§"`packages/<pkg>/src/<pkg>/_core.py`" lines 310-321 — higyrus _core delta — RequestSpec adds `account_id: str | None = None`; builders with `id_cuenta` parameter set `account_id=id_cuenta` in returned spec; flip `idempotent=True` on all GET builders + `build_login_request`)
    - packages/higyrus-client/src/higyrus_client/_core.py (Phase 7 baseline — RequestSpec line ~89-113 already has `idempotent: bool = False` per D-13 Phase 7 + `url_pre_encoded: bool = False` (higyrus URL-encoding quirk); locate all builders that take `id_cuenta`: movimientos, posicion_valuada, posiciones, etc.)
    - packages/higyrus-client/src/higyrus_client/client.py (Phase 7 baseline — read FULL FILE; locate Client class, slots, __init__, _ensure_token, _ensure_http_client, _request, configure())
    - packages/higyrus-client/src/higyrus_client/aio.py (Phase 7 baseline — async lock pattern + mirror client.py changes)
    - packages/higyrus-client/src/higyrus_client/exceptions.py (HigyrusAuthError class — used in 401 re-auth catch)
    - packages/iol-client/src/iol_client/client.py (Plan 3 baseline — _request() with 401 re-auth pattern; higyrus _request() differs in adding account_id extension)
    - packages/higyrus-client/tests/conftest.py (existing configure(token=..., token_expires_at=9_999_999_999.0) autouse fixture)
  </read_first>
  <behavior>
    Extend higyrus shell with iol-Plan-3 pattern + account_id propagation:

    **`packages/higyrus-client/src/higyrus_client/_core.py`** (extend):
    - `RequestSpec` dataclass: add `endpoint_name: str = ""` (D-09) AND `account_id: str | None = None` (D-11)
    - Flip `idempotent=True` + set `endpoint_name=<fn>` on:
      - `build_login_request` — `endpoint_name="login"` (D-03)
      - All GET builders (movimientos, posicion_valuada, posiciones, etc.) — `endpoint_name="<name>"`, AND if the builder takes `id_cuenta` parameter: set `account_id=id_cuenta` in returned RequestSpec
      - Example: `build_movimientos_request(state, id_cuenta, fecha_desde, ...)` → returns `RequestSpec(method="GET", path=..., params=..., idempotent=True, endpoint_name="get_movimientos", account_id=id_cuenta)`
    - Preserve `url_pre_encoded: bool = False` (higyrus URL-encoding quirk per Phase 7 D-XX) — DO NOT touch this field

    **`packages/higyrus-client/src/higyrus_client/client.py`** (extend):
    - `Client.__init__` signature: add `max_retries: int = 2` + `http_client: httpx.Client | None = None` (D-15)
    - `Client.__slots__`: add `"_max_retries"`
    - `_ensure_http_client()`: wrap `httpx.Client(transport=_transport.RetryTransport(max_attempts=self._max_retries+1))`
    - `_request()` delta — extend iol Plan 3 pattern with account_id propagation:
      ```python
      def _request(self, spec: _core.RequestSpec) -> httpx.Response:
          self._ensure_token()
          assert self._state.token is not None
          request_id = uuid.uuid4().hex
          http = self._ensure_http_client()
          url = f"{self._state.base_url}{spec.path}"
          headers = {"Authorization": f"Bearer {self._state.token}", **(spec.headers or {})}
          req = http.build_request(
              spec.method, url,
              params=spec.params, json=spec.json_body, headers=headers,
          )
          req.extensions["idempotent"] = spec.idempotent
          req.extensions["request_id"] = request_id
          req.extensions["endpoint_name"] = spec.endpoint_name
          if spec.account_id is not None:
              req.extensions["account_id"] = spec.account_id   # D-11 conditional propagation
          resp = http.send(req)
          try:
              _raise_for_response(resp)
          except HigyrusAuthError:
              # D-02 exactly-one re-auth.
              self._state.token = None
              self._ensure_token()
              assert self._state.token is not None
              req.headers["Authorization"] = f"Bearer {self._state.token}"
              resp = http.send(req)
              _raise_for_response(resp)
          return resp
      ```
    - `configure()`: add `max_retries` + `http_client` carry-forward params

    **`packages/higyrus-client/src/higyrus_client/aio.py`** (extend — mirror sync + async lock preservation):
    - `AsyncClient.__init__`: add 2 kwargs (`http_client: httpx.AsyncClient | None = None`); slots `"_max_retries"`
    - `_ensure_http_client()` async: wrap `httpx.AsyncClient(transport=_atransport.AsyncRetryTransport(max_attempts=self._max_retries+1))` inside existing lock (preserve double-checked locking)
    - `_request()` async: mirror sync with `async`/`await` + `HigyrusAuthError` re-auth-once + account_id propagation
    - `configure()`: same 2 kwargs

    **`packages/higyrus-client/src/higyrus_client/__init__.py`** (extend):
    - Insert `_logging.attach()` block per Plan 2 pattern; `del _logging_attach`; DO NOT add to `__all__`

    **`verification/snapshots/higyrus_client-surface.txt`** (update per D-28):
    - Update Client.__init__, AsyncClient.__init__, configure() signatures with `max_retries` + `http_client` kwargs
    - DO NOT touch other entries

    **Atomic commit** per D-21:
    `feat(higyrus): retries + structured logging — RetryTransport, RedactingFilter, account_id propagation, JSON password redaction (RELY-01..04, LOG-01..03)`

    **URL-encoding quirk preservation:** Phase 7 D-XX `url_pre_encoded: bool = False` in RequestSpec stays UNTOUCHED. The new `account_id` field is additive only.
  </behavior>
  <action>
    1. Edit `packages/higyrus-client/src/higyrus_client/_core.py`:
       - Add `endpoint_name: str = ""` and `account_id: str | None = None` to RequestSpec
       - For `build_login_request`: set `idempotent=True` + `endpoint_name="login"`
       - For each GET builder with `id_cuenta` parameter: set `idempotent=True` + `endpoint_name="<name>"` + `account_id=id_cuenta`
       - For GET builders without `id_cuenta`: set `idempotent=True` + `endpoint_name="<name>"` (leave account_id=None default)

    2. Edit `packages/higyrus-client/src/higyrus_client/client.py`:
       - Add `import uuid` + `from higyrus_client import _transport`
       - Add 2 kwargs to `Client.__init__`; handle them in body; add `"_max_retries"` to slots
       - Modify `_ensure_http_client()` to wrap RetryTransport
       - REPLACE `_request()` body with the account_id-aware variant of RESEARCH §Pattern 3 (set `extensions["account_id"]` when `spec.account_id is not None`)
       - Modify `configure()` to accept + pass through the 2 kwargs

    3. Edit `packages/higyrus-client/src/higyrus_client/aio.py`:
       - Add `import uuid` + `from higyrus_client import _atransport`
       - Add 2 kwargs to `AsyncClient.__init__`; slots; `_ensure_http_client()` async wrap
       - REPLACE `_request()` async with account_id-aware async mirror
       - Preserve existing lock patterns

    4. Edit `packages/higyrus-client/src/higyrus_client/__init__.py`:
       - Insert `_logging.attach()` block; `del _logging_attach`

    5. Update `verification/snapshots/higyrus_client-surface.txt`:
       - Replace 3 lines for Client.__init__, AsyncClient.__init__, configure()
       - Add `max_retries: 'int' = 2` + `http_client: 'httpx.Client \| None' = None` (Client/configure) / `httpx.AsyncClient \| None` (AsyncClient)

    6. Run verification:
       ```
       uv run pytest packages/higyrus-client/ -v 2>&1 | tail -25
       uv run pytest verification/test_public_surface.py -k higyrus -v 2>&1 | tail -5
       uv run pytest verification/test_retry_mutation_gate.py -k higyrus -v 2>&1 | tail -10
       uv run pytest verification/test_retry_401_reauth.py -k higyrus -v 2>&1 | tail -10
       uv run pytest verification/test_logging_no_token_leak.py -k higyrus -v 2>&1 | tail -10
       uv run pytest verification/test_async_cancellation.py -k higyrus -v 2>&1 | tail -5
       uv run pytest verification/test_sync_async_isolation.py -v 2>&1 | tail -10   # URL-encoding quirk preservation
       uv run ruff check packages/higyrus-client/ verification/
       uv run mypy --strict packages/higyrus-client/
       uv run lint-imports
       ```

    7. Single atomic commit per D-21:
       ```
       git add packages/higyrus-client/src/ packages/higyrus-client/tests/ verification/snapshots/higyrus_client-surface.txt
       git commit -m "feat(higyrus): retries + structured logging — RetryTransport, RedactingFilter, account_id propagation, JSON password redaction (RELY-01..04, LOG-01..03)

       Phase 8 Plan 4 — higyrus. Adds RetryTransport+AsyncRetryTransport+RedactingFilter mirroring iol (Plan 3). Adds account_id propagation via RequestSpec.account_id → request.extensions['account_id'] → log record extra (D-11). Adds 401 re-auth-once flow via HigyrusAuthError pattern (D-02). RedactingFilter covers Higyrus JSON {\"password\":\"...\"} body + {\"token\":\"...\"} response + cuit URL query (PII). Extends Client/AsyncClient/configure() with max_retries=2 + http_client=None. URL-encoding quirk (Phase 7) preserved.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
       ```
  </action>
  <verify>
    <automated>grep -c "endpoint_name" packages/higyrus-client/src/higyrus_client/_core.py &gt;= 1 &amp;&amp; grep -c "account_id" packages/higyrus-client/src/higyrus_client/_core.py &gt;= 2 &amp;&amp; grep -c "idempotent=True" packages/higyrus-client/src/higyrus_client/_core.py &gt;= 3 &amp;&amp; grep -c "max_retries" packages/higyrus-client/src/higyrus_client/client.py &gt;= 2 &amp;&amp; grep -c "max_retries" packages/higyrus-client/src/higyrus_client/aio.py &gt;= 2 &amp;&amp; grep -c "RetryTransport" packages/higyrus-client/src/higyrus_client/client.py &gt;= 1 &amp;&amp; grep -c "AsyncRetryTransport" packages/higyrus-client/src/higyrus_client/aio.py &gt;= 1 &amp;&amp; grep -c "HigyrusAuthError" packages/higyrus-client/src/higyrus_client/client.py &gt;= 2 &amp;&amp; grep -c "HigyrusAuthError" packages/higyrus-client/src/higyrus_client/aio.py &gt;= 2 &amp;&amp; grep -c "uuid.uuid4" packages/higyrus-client/src/higyrus_client/client.py &gt;= 1 &amp;&amp; grep -c "spec.account_id\|account_id" packages/higyrus-client/src/higyrus_client/client.py &gt;= 1 &amp;&amp; grep -c "_logging_attach.attach\|_logging.attach" packages/higyrus-client/src/higyrus_client/__init__.py &gt;= 1 &amp;&amp; grep -c "max_retries" verification/snapshots/higyrus_client-surface.txt &gt;= 2 &amp;&amp; uv run pytest packages/higyrus-client/ -v 2&gt;&amp;1 | tail -15 &amp;&amp; uv run pytest verification/test_public_surface.py -k higyrus -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_retry_401_reauth.py -k higyrus -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_logging_no_token_leak.py -k higyrus -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run pytest verification/test_async_cancellation.py -k higyrus -v 2&gt;&amp;1 | tail -5 &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -v 2&gt;&amp;1 | tail -10 &amp;&amp; uv run ruff check packages/higyrus-client/ verification/ &amp;&amp; uv run mypy --strict packages/higyrus-client/ &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `_core.py` has `endpoint_name` field AND `account_id: str | None = None` AND `>= 3` instances of `idempotent=True` (login + GETs)
    - Source: `client.py` has `max_retries` (signature + body), `RetryTransport`, `HigyrusAuthError` (try + raise paths), `uuid.uuid4`, account_id propagation
    - Source: `aio.py` has same: `max_retries`, `AsyncRetryTransport`, `HigyrusAuthError` async catch, account_id async propagation
    - Source: `__init__.py` has `_logging.attach()` + `del _logging_attach`
    - Source: snapshot has `max_retries: 'int' = 2` (3 places) + `http_client` kwargs
    - CLI: `uv run pytest packages/higyrus-client/ -v` exits 0 (existing tests + new test_transport + new test_logging)
    - CLI: `uv run pytest verification/test_public_surface.py -k higyrus -v` exits 0 (snapshot zero diff)
    - CLI: `uv run pytest verification/test_retry_401_reauth.py -k higyrus -v` exits 0 (401 chain GREEN for higyrus)
    - CLI: `uv run pytest verification/test_logging_no_token_leak.py -k higyrus -v` exits 0 (SECRET literal scrubbed in JSON password + token)
    - CLI: `uv run pytest verification/test_async_cancellation.py -k higyrus -v` exits 0 (D-32)
    - CLI: `uv run pytest verification/test_sync_async_isolation.py -v` exits 0 (Phase 7 cross-leak GREEN — URL-encoding quirk preserved)
    - CLI: `uv run ruff check` + `uv run mypy --strict` + `uv run lint-imports` exit 0
    - CLI: `git log --oneline -1 | grep -c "feat(higyrus)"` >= 1 (atomic commit per D-21)
  </acceptance_criteria>
  <done>
    higyrus Plan 4 atomic commit landed: RetryTransport+AsyncRetryTransport+RedactingFilter+account_id propagation+401-re-auth-once flow wired. JSON password + JSON token + cuit query redacted. Login marked idempotent=True. 2 new kwargs in Client/AsyncClient/configure(). Snapshot updated. URL-encoding quirk (Phase 7) preserved. Cross-cutting guard tests GREEN for higyrus parametrize branches.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `higyrus_client.Client._request` → `request.extensions["account_id"] = spec.account_id` | Higyrus-specific tier: shell propagates account_id to log records via extensions; transport copies to `extra["account_id"]` when non-None (D-11) |
| `higyrus_client._logging.RedactingFilter` ← Higyrus login body `{"password": "..."}` | JSON-format password scrubbing — login wire request body redacted before emission to consumer's handlers |
| `higyrus_client._logging.RedactingFilter` ← URL query `cuit=20XXXXX` (PII) | Argentine tax ID redaction in URL query strings — beyond locked roadmap LOCK (research §Pattern 4 mention; defensive) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01-HIGY | Tampering | Mutation gate — POST against 503 retry (higyrus has POST login but it's marked idempotent=True per D-03; sanity: no business POST in current surface) | mitigate | `_core.py` builders default `idempotent=False`; GET builders explicit `True`; mutation gate via extensions; `test_retry_mutation_gate.py[higyrus]` verifies |
| T-8-02-HIGY | Denial-of-Service | 401 retry storm with stale Bearer | mitigate | `retry_on=` LOCKED excludes HigyrusAuthError per D-07; shell `_request()` D-02 pattern exactly-once re-auth; `test_retry_401_reauth.py[higyrus]` covers |
| T-8-03-HIGY | Tampering | Library hijacks root logger | mitigate | `_logging.attach()` to `getLogger("higyrus_client")`; LOG015 + CI grep; `test_logging_root_unchanged.py` |
| T-8-04-HIGY | Information Disclosure | Higyrus login JSON body `{"username":..., "password":"SECRET"}` leaks in DEBUG | mitigate | `_logging.py` `_PASSWORD_JSON_RE` regex scrubs JSON password; `test_logging_no_token_leak.py[higyrus]` covers |
| T-8-04b-HIGY | Information Disclosure | Higyrus login response `{"token": "FRESH-TOKEN", "sessionId": "X"}` leaks token in DEBUG | mitigate | `_logging.py` `_TOKEN_JSON_RE` regex scrubs JSON token; same test coverage |
| T-8-04c-HIGY | Information Disclosure | URL query `cuit=20XXXXX` (Argentine PII) leaks in log records | mitigate | `_logging.py` `_CUIT_QUERY_RE` regex scrubs cuit query values; per PATTERNS line 259 — beyond LOCK but defensive |
| T-8-05-HIGY | Availability | async cancellation during retry | mitigate | `AsyncRetryTransport` + `await asyncio.sleep`; `test_async_cancellation.py[higyrus]` |
| T-8-D11-HIGY | Information Disclosure (low severity) | account_id (operational metadata, NOT PII) accidentally redacted by overzealous filter | accept | account_id is operational metadata — NOT in redaction regex set; `test_account_id_NOT_redacted` (Task 1) verifies it survives |
| T-8-URL-ENC-PRESERVE | Tampering (regression) | Phase 7 URL-encoding quirk (`url_pre_encoded: bool = False`) accidentally lost during Plan 4 _core.py edits | mitigate | Plan 4 only ADDS fields to RequestSpec (endpoint_name + account_id); does NOT remove or modify `url_pre_encoded`. Existing higyrus tests + cross-leak sentinel `test_sync_async_isolation.py` verify post-Plan-4 |
</threat_model>

<verification>
- higyrus `_transport.py` + `_atransport.py` + `_logging.py` created (mirror iol Plan 3 with logger_name + higyrus-specific regex)
- higyrus `_core.py` RequestSpec extended with `endpoint_name` + `account_id` fields; builders flipped
- higyrus shell `_request()` (sync + async) implements 401 re-auth-once + account_id extensions propagation
- higyrus Client/AsyncClient/configure() extended with max_retries + http_client kwargs
- higyrus `__init__.py` invokes `_logging.attach()`
- higyrus snapshot updated atomically
- higyrus existing tests preserved; URL-encoding quirk (Phase 7) untouched
- Cross-cutting guard tests turn GREEN for higyrus parametrize branches
- ruff + mypy strict + lint-imports GREEN
- Single atomic commit per D-21
</verification>

<success_criteria>
- higyrus Phase 8 delivery establishes account_id propagation pattern (Plan 5 matriz replicates with `auth_basic` tweak)
- `test_retry_401_reauth.py[higyrus]` GREEN: 401 → re-login → 200 = 3 wire requests with FRESH-TOKEN
- `test_logging_no_token_leak.py[higyrus]` GREEN: SECRET literal scrubbed in JSON password + JSON token + cuit query
- higyrus existing tests + Phase 7 baselines preserved
- Single commit `feat(higyrus): retries + structured logging — RetryTransport, RedactingFilter, account_id propagation, JSON password redaction (RELY-01..04, LOG-01..03)` per D-21
</success_criteria>

<output>
Create `.planning/phases/08-retries-backoff-structured-logging/08-04-SUMMARY.md` documenting:
- LOC delta per Phase 8 specifics format: `_transport.py: 0 → ~95 (NEW)`, `_atransport.py: 0 → ~95 (NEW)`, `_logging.py: 0 → ~80 (NEW; JSON password + JSON token + cuit query regex vs Plan 3)`, `_core.py: +endpoint_name + account_id fields + builders flipped`, `client.py: +~45 LOC (401 re-auth pattern + account_id propagation + 2 kwargs)`, `aio.py: +~45 LOC`, `__init__.py: +3 lines`
- Snapshot diff: 2 new kwargs in 3 signatures
- Cross-cutting guard test status: test_retry_401_reauth.py[higyrus] GREEN; test_logging_no_token_leak.py[higyrus] GREEN; test_async_cancellation.py[higyrus] GREEN
- URL-encoding quirk preservation evidence
- Atomic commit hash
- "higyrus Plan 4 establishes account_id propagation pattern — Plan 5 (matriz) replicates with auth_basic Risk API + status=ERROR no-retry tweaks"
</output>
