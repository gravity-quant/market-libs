---
phase: 45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti
plan: 05
type: execute
wave: 4
depends_on: ["45-01", "45-03", "45-04"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/ROADMAP.md
  - .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md
autonomous: true
requirements: [HARN-01, HARN-03, HARN-04]

must_haves:
  truths:
    - "`verification/test_public_surface.py` corre en CI: `IN-06` queda cerrado (D-06)."
    - "El lock de falsificación de D-04 y el invariante P-3 corren en CI, así que el criterio 2 de HARN-01 significa algo fuera de local."
    - "El canario de `probe_context` está REALMENTE transferido: `verification/test_probe_context_coverage.py` corre en CI (D-08/D-10 ENMENDADAS)."
    - "Todos los edits de `ci.yml` de la fase llegaron en UN solo cambio consolidado (D-11, criterio 5 del ROADMAP)."
    - "Los ~35 archivos `verification/` restantes siguen inertes, re-declarados por escrito con el censo post-fase medido."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Allowlist explícito del job `lint` extendido de 13 a 18 archivos, sin convertirse en `pytest verification/`"
      contains: "verification/test_drift_dedupe_falsification.py"
    - path: ".planning/ROADMAP.md"
      provides: "`HARN-VERIF-01` retirado con puntero al documento de decisión + entrada de backlog v1.9 para el gap de gate de mypy sobre los drivers"
    - path: ".planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md"
      provides: "Sección de censo post-fase medida después del edit de CI"
  key_links:
    - from: ".github/workflows/ci.yml (job `lint`)"
      to: "verification/test_drift_dedupe_falsification.py"
      via: "línea nueva en la lista explícita de `uv run pytest -q`"
      pattern: "test_drift_dedupe_falsification"
    - from: ".github/workflows/ci.yml (job `lint`)"
      to: "verification/test_probe_context_coverage.py"
      via: "línea nueva que convierte la transferencia del canario en real"
      pattern: "test_probe_context_coverage"
---

<objective>
Aterrizar el **edit consolidado único** de `.github/workflows/ci.yml` (D-11) que cierra `IN-06` (D-06), hace que el lock de falsificación de HARN-01 y el invariante P-3 corran en CI, y convierte en real la transferencia del canario de `probe_context` (D-08/D-10 ENMENDADAS); y cerrar la fase con el censo post-fase medido y el backlog del ROADMAP actualizado.

Purpose: un lock que no corre no cuenta como cobertura — es la declaración inerte formal que la Phase 41 dejó ruteada a esta fase. Y la Phase 41 deliberadamente NO tocó `ci.yml` para dejar esta consolidación acá: dispersar las líneas entre planes incumpliría el criterio 5.
Output: `ci.yml` con 18 archivos en el allowlist, el ROADMAP sin `HARN-VERIF-01` pendiente y con el destino v1.9 del gap de mypy nombrado, y el documento de decisión con su censo post-fase.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-CONTEXT.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-01-SUMMARY.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-03-SUMMARY.md
@.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: pre-flight — los 5 archivos a enrolar pasan solos, medido y pegado</name>
  <files>(ninguno — tarea de medición)</files>
  <read_first>
    - `.github/workflows/ci.yml` líneas 60-95 (el step de driver locks, su comentario de `:69-78` y las 13 líneas del allowlist actual)
    - `.planning/phases/45-.../45-CONTEXT.md` D-06 ("verificar que el archivo pasa solo, antes de agregarlo") y D-10 ENMENDADA (la lista exacta de 5)
  </read_first>
  <action>
    Correr cada uno de los 5 archivos POR SEPARADO (no en un solo comando: si uno falla, hace falta saber cuál y por qué) y pegar cada salida en el SUMMARY:
    `verification/test_public_surface.py`, `verification/test_finding_count_consistency.py`, `verification/test_findings_dedupe_by_title.py`, `verification/test_drift_dedupe_falsification.py`, `verification/test_probe_context_coverage.py`.

    Línea base medida por research/pattern-map para contrastar: 4 passed, 2 passed, 12 passed, (el nuevo, 6 arms tras 45-03), 6 passed. Si alguno no pasa, PARAR y reportar: D-06 condiciona el enrolamiento a que el archivo pase solo, y enrolar un archivo rojo pone rojo el job `lint` de todo el repo.

    Registrar además, para la declaración del censo de la Task 3, el conteo previo: `ls verification/test_*.py | wc -l` y el número de líneas `verification/test_*.py` dentro del bloque de `uv run pytest -q` del job `lint`.
  </action>
  <acceptance_criteria>
    - Las 5 salidas están pegadas en el SUMMARY, cada una con su comando, y ninguna reporta `failed` ni `error`.
    - `verification/test_drift_dedupe_falsification.py` reporta 6 passed (los 3 arms de 45-02 más los 3 de 45-03).
    - El conteo previo de archivos en disco y de líneas enroladas está registrado.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q verification/test_public_surface.py && uv run pytest -q verification/test_finding_count_consistency.py && uv run pytest -q verification/test_findings_dedupe_by_title.py && uv run pytest -q verification/test_drift_dedupe_falsification.py && uv run pytest -q verification/test_probe_context_coverage.py</automated>
  </verify>
  <done>Los 5 archivos pasan solos, con salida pegada, y el censo previo quedó registrado.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: el edit consolidado ÚNICO de `.github/workflows/ci.yml` — 13 → 18 archivos en el allowlist (D-06 + D-10 ENMENDADA + D-11)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - `.github/workflows/ci.yml` líneas 40-95 (los 4 steps cross-package del job `lint`, el comentario de `:67-78` y la lista de 13)
    - `.planning/phases/45-.../45-PATTERNS.md` sección `.github/workflows/ci.yml:80-93 (D-06/D-10/D-11)` — la tabla de los 5 archivos con su origen y su precondición
    - `.planning/phases/45-.../45-HARN-04-DECISION.md` § censo / re-declaración de D-10 (la lista de 5 tiene que coincidir exactamente con la que el documento declaró)
  </read_first>
  <action>
    Un solo `Edit` acotado sobre el bloque `run: |` del step "driver locks" del job `lint`: agregar **exactamente cinco** líneas a la lista explícita, con la misma indentación y la misma continuación de línea con backslash que las 13 existentes:
    `verification/test_public_surface.py`, `verification/test_finding_count_consistency.py`, `verification/test_findings_dedupe_by_title.py`, `verification/test_drift_dedupe_falsification.py` y `verification/test_probe_context_coverage.py`.
    Total resultante: **18**. Ni una más: los otros ~35 archivos de `verification/` quedan fuera por D-10, y el documento de decisión del plan 45-04 ya lo declaró por escrito.

    **Prohibido** reemplazar la lista por `pytest verification/`: eso enrolaría de golpe ~35 archivos que nunca corrieron, entre ellos los 2 que D-08 acepta como deuda (19 failed / 19 errors), y pondría rojo el job `lint` del repo entero. **Preservar sin tocar** el comentario de `:67-78`, que es la razón escrita de por qué la lista es explícita y por la cual el próximo lector no la colapsa.

    Tampoco se crea un job nuevo ni se mueve el step: el precedente lockeado (Phase 32 D-05, Phase 42-01) es que los gates cross-package viven como *step* dentro del job `lint`, porque el job `test` pasa un path explícito por paquete que pisa `testpaths` y por eso `verification/` nunca corre ahí.

    Después del edit, correr el **espejo completo del comando de CI** en local: `uv run ruff check .`, `uv run ruff format --check .`, `uv run lint-imports`, `uv run python tools/check_decode_intactness.py`, `uv run python tools/check_uniform_structure.py`, `uv run python tools/check_surface_types.py`, y el `uv run pytest -q` con las 18 rutas exactamente como quedaron escritas en el YAML. **No** correr `uv run pytest -q` a secas como gate de fase: esa forma arrastra los 19 failed / 19 errors pre-existentes que D-08 decidió NO reparar, así que "suite completa verde" no es un gate alcanzable ni deseable en esta fase.

    Este es el ÚNICO plan de la fase autorizado a tocar `ci.yml`, y esta es la única tarea del plan que lo toca (D-11).
  </action>
  <acceptance_criteria>
    - `grep -c 'verification/test_.*\.py' .github/workflows/ci.yml` devuelve 18.
    - Las 5 rutas nuevas están presentes: `grep -c 'test_public_surface.py\|test_finding_count_consistency.py\|test_findings_dedupe_by_title.py\|test_drift_dedupe_falsification.py\|test_probe_context_coverage.py' .github/workflows/ci.yml` devuelve 5.
    - El comentario que explica por qué la lista es explícita sigue presente: `grep -c 'Es una lista EXPLÍCITA' .github/workflows/ci.yml` devuelve 1.
    - El job `lint` sigue teniendo un solo step de driver locks y no se agregó ningún job nuevo: `grep -c '^  [a-z-]*:$' .github/workflows/ci.yml` no cambió respecto de la línea base (registrar ambos números).
    - El comando de pytest con las 18 rutas del YAML corre verde en local, con la salida pegada.
    - El resto del espejo de CI (ruff check, ruff format --check, lint-imports, los 3 gates de `tools/`) corre verde, con salidas pegadas.
    - `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` (o `uv run python -c ...`) parsea sin error.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check . && uv run ruff format --check . && uv run lint-imports && uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py && uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_run_evidence.py verification/test_main_iol_deep_chain.py verification/test_main_higyrus_deep_chain.py verification/test_main_matriz_deep_chain.py verification/test_cycle_closure_phase33.py verification/test_literal_census_venue_gate.py verification/test_public_surface.py verification/test_finding_count_consistency.py verification/test_findings_dedupe_by_title.py verification/test_drift_dedupe_falsification.py verification/test_probe_context_coverage.py</automated>
  </verify>
  <done>El allowlist tiene 18 archivos, las 5 líneas nuevas son exactamente las de D-10 ENMENDADA, el comentario explicativo quedó intacto, no hay jobs nuevos, y el espejo de CI corre verde en local.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: cierre — censo post-fase, backlog del ROADMAP y gate de "un solo commit sobre `ci.yml`"</name>
  <files>.planning/ROADMAP.md, .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md</files>
  <read_first>
    - `.planning/phases/45-.../45-HARN-04-DECISION.md` (el documento que dejó 45-04, en particular su sección de censo y su sección Q5)
    - `.planning/ROADMAP.md` la entrada de backlog `HARN-VERIF-01` (hoy alrededor de la línea 265) y la sección de backlog donde viven los candidatos a milestone siguiente
    - `.planning/phases/45-.../45-01-SUMMARY.md` y `45-03-SUMMARY.md` (para citar los resultados que el cierre resume)
  </read_first>
  <action>
    (a) **Censo post-fase, medido.** Correr `ls verification/test_*.py | wc -l` y contar las rutas `verification/test_*.py` del allowlist del job `lint`; calcular los inertes por resta. Agregar al final de `45-HARN-04-DECISION.md` una sección corta "Censo post-fase" con el comando, la salida y la aritmética antes → después (la referencia esperada es 53/13/40 antes y 54/18/36 después, pero se escribe lo MEDIDO). Re-declarar en esa misma sección, por escrito, que los archivos inertes restantes siguen fuera de alcance de v1.8 y por qué.

    (b) **ROADMAP — `HARN-VERIF-01`.** Con `Edit` acotado (nunca reescribir el archivo), marcar la entrada de backlog `HARN-VERIF-01` como resuelta en la Phase 45 mediante decisión escrita, con la ruta del documento `45-HARN-04-DECISION.md` y su fecha. No borrar la entrada: su medición de causa raíz es la evidencia que el documento cita. Dejar explícito que la disposición fue "aceptar como deuda documentada", no "reparar", y que el gemelo de mypy sobre `verification/` sigue fuera de alcance por escrito (`REQUIREMENTS.md § Out of Scope`).

    (c) **ROADMAP — entrada de backlog v1.9 para el gap de gate de mypy sobre los drivers de la raíz (Q5).** Agregar una entrada nueva en la sección de backlog, con: el hecho medido (apuntar mypy a mano a un driver de la raíz levanta errores reales; así se detectó `DRV-MD-SEG-43`), la causa de alcance (el `files` de mypy del root lista seis rutas `packages/*/src`, el hook de pre-commit está scoped a `packages/*/src`, y el lock de deep-chain parsea el driver por AST sin importarlo), por qué NO se cerró en esta fase (apuntar mypy a 5 drivers de miles de líneas es alcance no medido dentro de una fase de limpieza) y el destino: v1.9. Es el destino nombrado que el documento de decisión declaró.

    (d) **Gate del criterio 5 (D-11).** Correr `git log --oneline 6b9b3b6..HEAD -- .github/workflows/ci.yml | wc -l` y confirmar que da **1**. Si diera más de 1, la fase incumplió D-11 y hay que reportarlo, no maquillarlo. Pegar comando y salida.

    (e) **Resumen de cierre para el SUMMARY** (no un archivo nuevo): los 5 criterios de éxito del ROADMAP con su evidencia — criterio 1 (arms de colapso y no-colapso verdes, con la aritmética esperada 22 → 11 declarada como lo que el mecanismo `(func, digest)` entrega sobre la corrida medida de 33-05, y la aclaración de que no es un colapso cross-proceso porque D-01 lo rechaza deliberadamente), criterio 2 (P-3 verde y sin editar + el arm de fid que sí detecta la violación), criterio 3 (documento de decisión fechado), criterio 4 (cifra medida en el docstring, `IN-06` cerrado por el enrolamiento, `IN-05` retirado) y criterio 5 (un solo commit sobre `ci.yml` + espejo de CI verde en local).
  </action>
  <acceptance_criteria>
    - `git log --oneline 6b9b3b6..HEAD -- .github/workflows/ci.yml | wc -l` devuelve exactamente 1, con la salida pegada.
    - `45-HARN-04-DECISION.md` tiene una sección "Censo post-fase" con comando, salida y aritmética antes → después.
    - `grep -c 'HARN-VERIF-01' .planning/ROADMAP.md` ≥ 1 y la entrada aparece marcada como resuelta con la ruta del documento de decisión.
    - El ROADMAP contiene la entrada de backlog v1.9 del gap de gate de mypy sobre los drivers de la raíz (leerla y citarla en el SUMMARY).
    - `git diff --stat .planning/ROADMAP.md` muestra sólo las zonas editadas (sin reescritura masiva).
    - El SUMMARY lista los 5 criterios de éxito con su evidencia y comandos.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git log --oneline 6b9b3b6..HEAD -- .github/workflows/ci.yml | wc -l | tr -d ' ')" = "1" && grep -c 'HARN-VERIF-01' .planning/ROADMAP.md && grep -c 'Censo post-fase' .planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-HARN-04-DECISION.md</automated>
    <human-check>Tras el push de la rama, confirmar en GitHub Actions que las 12 patas de la matriz (6 paquetes × py3.12/py3.13) más los jobs `lint`, `pre-commit` y `typecheck` quedan verdes con el allowlist de 18 archivos — criterio 5 del ROADMAP. El espejo local no ejercita las patas de la matriz.</human-check>
  </verify>
  <done>El censo post-fase está medido y escrito, `HARN-VERIF-01` figura resuelto con puntero al documento, el gap de gate de mypy tiene entrada de backlog v1.9, y el gate de un solo commit sobre `ci.yml` da 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo ↔ CI (GitHub Actions) | El único archivo de configuración de ejecución que esta fase toca; un enrolamiento erróneo pone rojo el job `lint` del repo entero |
| red / credenciales | Ninguna: el job `lint` no usa secretos y esta fase no corre drivers en vivo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-19 | Denial of Service (del pipeline) | job `lint` | mitigate | Task 1 verifica que los 5 archivos pasan SOLOS antes de enrolarlos (D-06); el espejo completo de CI corre en local antes de cerrar |
| T-45-20 | Tampering | allowlist explícito de `ci.yml` | mitigate | Prohibido colapsarlo a `pytest verification/` (enrolaría ~35 archivos nunca corridos, incluidos los 2 rotos); criterio de aceptación pinnea el conteo en 18 y la preservación del comentario que explica la política |
| T-45-21 | Repudiation | criterio 5 ("un cambio consolidado") | mitigate | Gate de git que exige exactamente 1 commit sobre `ci.yml` en toda la fase, con salida pegada |
| T-45-22 | Elevation of Privilege | workflow de CI | accept | El edit es una línea de lista dentro de un step existente: no agrega jobs, ni `permissions`, ni acciones de terceros, ni secretos |
| T-45-SC | Tampering | instalación de paquetes | accept | N/A — sin dependencias nuevas en toda la fase |
</threat_model>

<verification>
- Espejo completo de CI en local (ruff check, ruff format --check, lint-imports, los 3 gates de `tools/`, y el `pytest -q` con las 18 rutas) → verde.
- `git log --oneline 6b9b3b6..HEAD -- .github/workflows/ci.yml | wc -l` → 1.
- `git status --porcelain .planning/verification/` → vacío (ninguna corrida de test ensució el ledger).
- Human check al final: CI verde en las 12 patas de la matriz más `lint`, `pre-commit` y `typecheck`.
</verification>

<success_criteria>
1. El allowlist del job `lint` pasa de 13 a 18 archivos, exactamente los 5 de D-10 ENMENDADA, en UN solo commit.
2. `IN-06` queda cerrado y la transferencia del canario de `probe_context` es real (el archivo corre en CI).
3. El censo post-fase está medido y la re-declaración de inertes está escrita.
4. `HARN-VERIF-01` figura resuelto por decisión escrita y el gap de gate de mypy tiene destino v1.9 nombrado.
</success_criteria>

<output>
Create `.planning/phases/45-limpieza-del-harness-dedupe-de-drift-comentarios-stale-desti/45-05-SUMMARY.md` when done.
Debe incluir: las 5 salidas del pre-flight, la salida del espejo completo de CI, la salida del gate de un solo commit, el censo antes → después, y el cierre de los 5 criterios de éxito del ROADMAP con su evidencia.
</output>
