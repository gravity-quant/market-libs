---
milestone: v1.1 Tech Debt Cleanup
audit_date: 2026-06-14
phases_in_scope: [06, 07, 08, 09, 10, 11]
status: passed
previous_run: 2026-06-13T20:xx:xxZ (phases 06-09 only, conditional_pass)
int_01_status: RESOLVED (commit 3de1940, 2026-06-13T20:18Z)
test_suite_count: 907/908 collected (1 deselected) — delta +122 vs prior audit baseline 785
---

# v1.1 Integration Check — All 6 Phases (Full Milestone Close)

**Scope:** Cross-phase wiring audit for Phases 06, 07, 08, 09, 10, 11 — all COMPLETE.
**Audited:** 2026-06-14 (Phase 10 + 11 are new deliverables since the 2026-06-13 audit).

---

## Prior Run — Re-confirmed (Phase 6–9 Claims)

All 7 claims from the 2026-06-13 audit re-confirmed in this run. No regressions detected.

**Claim 1 — `_core.py` (Phase 7) consumed by `_transport.py` (Phase 8):** WIRED (unchanged).
Bridge remains `req.extensions["idempotent"]` — transport reads extensions dict, not `_core` import.
Import-linter: 4 contracts KEPT, 0 broken (confirmed live this run via `uv run lint-imports`).

**Claim 2 — Mutation gate `RequestSpec.idempotent` end-to-end:** WIRED (unchanged).
`_core.py` GET builders `idempotent=True`; `new_order`/`cancel_order`/`replace_order` `idempotent=False` (Pitfall 4 / D-01 / D-24 comment confirmed at `_core.py:560, 594, 622`).
Phase 10 async surface inherits same gate: `aio.py` `AsyncClient._request()` sets `req.extensions["idempotent"]` before passing to `AsyncRetryTransport.handle_async_request()`.

**Claim 3 — BUG-01 CFI guard sync/async reachability:** NOW FULLY WIRED (delta from prior run).
Prior run: sync WIRED, async NOT REACHABLE (D-25 stub). After Phase 10: `aio.py:447-450` calls `_core.build_get_instruments_by_cfi_request(self._state, cfi_code)` which contains the `_CFI_ISO_RE` / `_CFI_LITERAL_VALUES` guard — the guard is now reachable from both surfaces. See Phase 10 claim below.

**Claim 4 — BUG-03 `_state.refresh_token` lifecycle:** WIRED (unchanged).
`_state.refresh_token` → `_ensure_token()` → `_refresh()` rotation/preserve. 8 tests (4 sync + 4 async) confirmed in 907-test suite.

**Claim 5 — Fixture-reaches-production guards Phase 6 + transport stack:** WIRED (unchanged).
Previously: 7 active + 1 permanent skip (D-25). Now: **8 active** — the 3 forward-reference skips were FLIPPED to active tests in Phase 10 Plan 10-04 Task 1 (commit `85d68e7`). Confirmed: `grep -c "pytest.skip.*Phase 10\|pytest.skip.*REFAC-04"` returns 0 in all 3 target files.

**Claim 6 — `matriz aio.py` D-25 stub invariant:** NO LONGER APPLIES (by design).
Prior run: `wc -l aio.py` = 103 (lifecycle stub). After Phase 10: `wc -l aio.py` = **852** (full async REST surface). The D-25 invariant was a Phase 10 prerequisite placeholder — it is correctly retired. The 3 guard-test permanent skips are now active tests.

**Claim 7 — PEP 562 shim + `_get_default()` INT-01 fix:** WIRED (unchanged).
`grep -c "_base_url" main_iol.py` = 0. `grep -c "_get_default()._state.base_url" main_iol.py` = 15 (10 sync + 5 async). The INT-01 fix from quick-task 260613-nwb (commit `3de1940`) holds. An additional F-02 PROBE_STALE fix was applied in Phase 11 using the same INT-01 idiom pattern at `main_iol.py:1289` (see LIVE-01 / iol F-02 below).

---

## Phase 10 + 11 Wiring — New Since 2026-06-13T20:30Z

### Claim 8 — REFAC-04: `matriz_client/aio.py` full REST surface

**Result: WIRED**

`packages/matriz-client/src/matriz_client/aio.py` is **852 LOC** (was 103-LOC lifecycle stub).
The `AsyncClient` class exposes 22 REST methods on the class surface plus 22 matching module-level
async delegators:

Verified present via `grep -n "async def "`:
- Lifecycle: `__aenter__`, `__aexit__`, `aclose`, `_ensure_http_client`, `_aensure_token`, `login`
- Core transport: `_request`
- REST endpoints: `get_segments`, `get_all_instruments`, `get_instruments_details`,
  `get_instrument_detail`, `get_instruments_by_cfi`, `get_instruments_by_segment`,
  `new_order`, `replace_order`, `cancel_order`, `get_order_status`, `get_order_history`,
  `get_active_orders`, `get_filled_orders`, `get_all_orders`, `get_order_by_exec_id`,
  `get_market_data`, `get_trades`, `get_positions`, `get_detailed_positions`, `get_account_report`
- Module-level delegators: 22 corresponding `async def X()` functions calling `_get_default().X()`

B8 lock-in confirmed: `aio._raise_for_response is client._raise_for_response is _core.raise_for_response`
(10-VALIDATION.md spot check #2 + `_core.raise_for_response as _raise_for_response` at `aio.py:61`).

`_core` builders/parsers used throughout `AsyncClient` methods:
- `aio.py:447-450` calls `_core.build_get_instruments_by_cfi_request(self._state, cfi_code)` — BUG-01 CFI guard now reachable from async path.
- All other REST methods similarly delegate to `_core.build_*_request()` + `_core.parse_*_response()`.

`_atransport.py` confirmed present (173 LOC) with `AsyncRetryTransport(httpx.AsyncHTTPTransport)` at line 56 — the Phase 8 D-25 carve-out is resolved.

Import-linter: all 4 contracts KEPT (`_core` cannot import `client`/`aio`) confirmed this run.

### Claim 9 — TokenStore 3-way concurrency (REFAC-04)

**Result: WIRED**

`packages/matriz-client/src/matriz_client/_token_store.py` (177 LOC):
- `threading.Lock` guards token state at `_state_lock = threading.Lock()` (line 54) — callable
  from sync REST, asyncio context (via `asyncio.to_thread`), and ws_client daemon thread.
- `asyncio.Lock` instances lazy per event loop, keyed by `id(loop)`, via `_get_async_lock()` (lines 95-108).
- `get_sync()` (line 88): acquires `_state_lock`, calls `_do_refresh_if_needed()`.
- `get_async()` (line 110): double-checked locking — fast path reads state, slow path acquires
  async lock then offloads refresh to `asyncio.to_thread(self._refresh_under_state_lock)`.

Wiring confirmed end-to-end:
- `client.py:252-254`: `if self._state.token_store is None: self._state.token_store = build_token_store(...)` → `snap = self._state.token_store.get_sync()` (lazy-init on first `_ensure_token` call).
- `aio.py:279-309`: `if self._state.token_store is None:` lazy-init using the sync default's `http_client` (note: async lazy-init borrows sync `httpx.Client` for the TokenStore's sync refresh path — design decision documented in `aio.py:283-286`); `snap = await self._state.token_store.get_async()`.
- `ws_client.py:136-138`: daemon thread calls `default._ensure_token()` (lazy-inits token_store) then `state.token_store.get_sync()` directly — shares the SAME token_store instance as sync REST.

`_state.py` field: `token_store: TokenStore | None = None` at line 65 (added in Phase 10 Plan 10-03).

TokenStore 3-way regression test: `packages/matriz-client/tests/test_token_store_integration.py` — confirmed in 907-test suite (10-VALIDATION.md spot check #4 PASS).

ORP-01 status: `account_id: str | None = None` at `_state.py:59` is still present. Confirmed never read via `state.account_id` anywhere in `packages/matriz-client/src/` (only `spec.account_id` — a `RequestSpec` field — appears in client method bodies). This dead field was explicitly preserved in Phase 10 spot check #7 and Phase 11 CR-08 scope covered only spike-artifact ruff exclusion + `main_higyrus.py:767` line-length (which passes `uv run ruff check .` clean). The `_state.py:59` dead field remains as a WARNING; it was not in the REQUIREMENTS.md CR-08 spec text.

### Claim 10 — LIVE-02 (matriz async REST live paridad)

**Result: OPERATOR-CONFIRMED (file evidence, not re-runnable by auditor)**

`10-VALIDATION.md` frontmatter: `status: approved`, `live_paridad_sync_async: true`,
`nyquist_compliant: true`, `operator_signoff_date: 2026-06-14`.

Paridad run table in `10-VALIDATION.md:192-218`: 19 paired probes, 0 divergences.
Summary line confirmed: `PASS=31 FAIL=0 SKIPPED=18 FINDING=1 / Paridad PASS (probes_paired=19, divergences=0)`.

No in-repo test codifies the live-paridad assertion (the run log `/tmp/phase10-live-paridad.log` is
local-only per design). The codified paridad test is:
- `verification/test_sync_async_isolation.py::test_matriz_sync_async_state_and_token_store_instance_isolation` — confirms state isolation (in-suite).
- Mutation gate `packages/matriz-client/tests/test_async_mutations.py` (AM2/AM3/AM4) — confirms async Pitfall 4 (in-suite).
Live endpoint count parity (22 async methods vs 22 sync REST methods) is structurally confirmed by `grep -n "async def "` count above.

### Claim 11 — HARN-07/08/09/10: findings.py append-only + content-addressed dedupe + operator field preservation + D-MATZ-27 dedupe

**Result: WIRED**

`verification/findings.py` (640 LOC):
- `_AUTO_BEGIN_MARKER = "<!-- BEGIN AUTO-GENERATED -->"` (line 92) and `_AUTO_END_MARKER = "<!-- END AUTO-GENERATED -->"` (line 93) — 3-zone state machine in `_parse_findings()` (line 235). HARN-07 WIRED.
- `append_finding(..., idempotent_by_title: bool = False)` at line 531 — content-addressed dedupe by finding ID when kwarg is True (line 599+). HARN-08 WIRED.
- Operator prefix/suffix zones survive N re-runs (lines 248-260); `_replace_art_block()` (line 210) refreshes ART block in-place without touching operator content. HARN-09 WIRED.

4 findings files all have `<!-- BEGIN AUTO-GENERATED -->` / `<!-- END AUTO-GENERATED -->` markers confirmed:
- `ambito-financiero-client-findings.md:11,26`
- `higyrus-client-findings.md:12,59`
- `iol-client-findings.md:11,35`
- `matriz-client-findings.md:11,117`

Operator fields in `iol-client-findings.md` (F-02 FIXED entry, post-END marker):
`Classification: PROBE_STALE`, `Rationale:`, `Resolution:`, `Regression:` — HARN-09 preservation confirmed.

All 4 drivers use `append_finding` from `verification.findings` (via barrel `verification.__init__.py`):
- `main_ambito_financiero.py:49-50` — direct `from verification.findings import append_finding`
- `main_iol.py:74-75` — direct `from verification.findings import append_finding`
- `main_higyrus.py:99` — via barrel `from verification import append_finding`
- `main_matriz.py:69-77` — via barrel `from verification import ...`

HARN-10 — D-MATZ-27 EXPECTED dedupe: `main_matriz.py:2212` calls `append_finding(..., idempotent_by_title=True)` at the D-MATZ-27 EXPECTED terminal. `grep -n "idempotent_by_title=True" main_matriz.py` returns 2 occurrences. 10-VALIDATION.md HARN-10 clarification confirms pre/post count stable (2 findings, no 3rd duplicate added).

Regression test coverage: `verification/test_findings_append_only.py` (191 LOC, 4 tests including HARN-09 N=3 re-run) + `verification/test_findings_dedupe_by_title.py` (131 LOC, 3 tests) = 7 tests, all in 907-test suite.

### Claim 12 — CR-01/02/04/06/07/08 code review close-out

**Result: ALL WIRED**

**CR-01** (`probe_schema_snapshot` sample_params alignment): `main_matriz.py:1392+` `sample_params` dict uses `{name}` placeholder style everywhere (confirmed by test `verification/test_main_matriz_schema_snapshot_alignment.py` 3 cases GREEN).

**CR-02** (`probe_login_sync` FAIL→FINDING uniformity): Both sites in `main_matriz.py:484, 503` return `ProbeResult("login_sync", "FINDING", ...)`. `grep -c 'ProbeResult.*login_sync.*FAIL' main_matriz.py` = 0. 2 test cases in `verification/test_main_matriz_login_fail_uniformity.py` GREEN.

**CR-04** (`_first_dict` 3-branch): `main_matriz.py:205-253` — 3-branch implementation at line 205 (`def _first_dict(payload, *, fname=None)`): returns `dict | None` for ok/no_data cases and emits FINDING for wrong_type when `fname is not None`. 5 test cases in `verification/test_main_matriz_first_dict.py` GREEN.

**CR-06** (bare `except Exception` narrowing): `grep "except Exception"` returns 0 in `main_matriz.py` and 0 in `main_higyrus.py`. Both drivers define `_RESIDUAL_PROBE_EXCEPTIONS` tuple at module-level (`main_matriz.py:118`, `main_higyrus.py:132`). AST regression guard: `verification/test_main_drivers_bare_except.py` (2 parametric cases) confirms 0 bare sites. 27 sites narrowed total (10 matriz + 17 higyrus). Note: `PrimaryAPIError` is included in matriz's `_RESIDUAL_PROBE_EXCEPTIONS` tuple (Rule 1 deviation from CR-06 RED commit — correctly fixed in CR-02 `bc4acc1` commit).

**CR-07** (`main_higyrus.py` event_hooks thread-safety): `main_higyrus.py:217-226` — `_event_hooks_lock_sync = threading.Lock()` (sync) + lazy `_event_hooks_lock_async` (async). `_capture_sync_query_string()` at line 308 wraps mutation in `with _event_hooks_lock_sync:`. `_capture_async_query_string()` at line 363 wraps mutation in `async with _get_event_hooks_lock_async():`. 3 tests in `packages/higyrus-client/tests/test_event_hooks_thread_safety.py` GREEN.

**CR-08** (line-length + spike artifact ruff exclusion): `pyproject.toml:46-48` — `extend-exclude` for `.claude/skills/spike-findings-market-libs/sources/**` and `.planning/spikes/**`. `uv run ruff check .` → All checks passed (confirmed this run). `main_higyrus.py` passes ruff clean (line 767 area no longer a violation — refactored during CR-06/07 work). REQUIREMENTS.md CR-08 spec text covered these two items; ORP-01 `account_id` in `_state.py:59` is NOT in CR-08's REQUIREMENTS.md spec.

### Claim 13 — LIVE-01 E2E Flow (milestone-final gate)

**Result: COMPLETE — OPERATOR-CONFIRMED (with iol F-02 FIXED inline)**

`11-VALIDATION.md` frontmatter: `status: approved`, `nyquist_compliant: true`, `phase_status: ready_for_close`, `operator_signoff_date: 2026-06-14`.

Per-package dispositions:
- **ámbito**: `no_new_findings` — PASS=6, FAIL=0, SKIPPED=1, FINDING=0.
- **iol**: F-02 PROBE_STALE FIXED inline at `main_iol.py:1289` (INT-01 idiom). Post-fix: PASS=13, FAIL=0, SKIPPED=1, FINDING=1 (only pre-existing F-01 OPEN).
- **higyrus**: `no_new_findings` — PASS=16, FAIL=0, SKIPPED=2, FINDING=1 (pre-existing).
- **matriz**: `no_new_findings` — PASS=31, FAIL=0, SKIPPED=18, FINDING=1 (pre-existing EXPECTED).

Blocking regression triad: all ZERO (sync/async URL isolation GREEN via `verification/test_sync_async_isolation.py` 9 passed; credential leak GREEN; PASS→FAIL flips GREEN).

iol F-02 fix integrity verified: `main_iol.py:1289` now uses `iol_client.client._get_default()._state.token_expires_at = 0.0` (INT-01 idiom). Read at line 1329 (`iol_client.client._token_expires_at`) goes through `__getattr__` → `_FORWARDED_TO_STATE["_token_expires_at"]` → `_get_default()._state.token_expires_at` — read path was always correct; write path was the probe bug. Root cause: PROBE_STALE (not a client bug).

CI green final at head commit `71bf201`:
- ruff check: All checks passed (148 files formatted)
- mypy strict: no issues in 50 source files
- import-linter: 4 kept, 0 broken
- pytest Python 3.12: 907 passed, 1 deselected
- pytest Python 3.13: 907 passed, 1 deselected

---

## Integration Wiring Summary

**Connected:** 29/29 REQ-IDs wired end-to-end (all 6 phases)

**Orphaned:** 1 dead field — `packages/matriz-client/src/matriz_client/_state.py:59`
`account_id: str | None = None` — never read via `state.account_id` in matriz source. Not in
REQUIREMENTS.md CR-08 spec text. Severity: WARNING (cosmetic, no runtime impact, frozen slot).

**Missing/Broken:** 0 (INT-01 RESOLVED 2026-06-13T20:18Z; F-02 RESOLVED 2026-06-14 inline in Phase 11)

**Claims verified:** 13 cross-phase wiring claims; 12 WIRED, 1 operator-confirmed live run (LIVE-02, log local-only).

## API Coverage

All module-level delegator functions in all 4 packages called from `main_*.py` drivers and/or 907-test suite. No orphaned routes.

`_atransport.py` now present (173 LOC, `AsyncRetryTransport`) — Phase 8 D-25 carve-out resolved by Phase 10. `AsyncClient._request()` in `aio.py:340-420` uses `AsyncRetryTransport`.

## Auth Protection

**Protected:** All 4 packages — iol (Bearer), higyrus (Bearer), matriz Primary (X-Auth-Token), matriz Risk (auth_basic). Ambito public by design.

**TokenStore:** 3-way protection confirmed — `threading.Lock` at `_token_store.py:54` guards cross-thread (sync REST main thread + ws_client daemon thread + asyncio.to_thread offload).

**Unprotected:** None (ambito no-auth is intentional B7 divergence).

## E2E Flows

**Complete:** 9/9 flows verified

1. Auth token → wire header — `test_fixture_reaches_production.py` × 4 packages (now 8 active, 0 skips)
2. Retry on 408/409/429/5xx + ConnectError/Timeout — `test_transport.py` + `test_atransport.py` per package
3. Mutation gate (POST/PATCH not retried; Pitfall 4) — `test_retry_mutation_gate.py` (sync) + `test_async_mutations.py` (AM2/AM3/AM4)
4. CFI guard BUG-01 — now WIRED sync+async (16 parametric sync + async path via `aio.py:447-450`)
5. refresh_token lifecycle BUG-03 — 8 tests (4 sync + 4 async)
6. Multi-account iteration BUG-04 — `test_multi_account.py`
7. 401 re-auth-once — `test_retry_401_reauth.py` (7 cases)
8. Logging redaction LOG-02 — `test_logging_no_token_leak.py` (5 cases)
9. **LIVE-01 (`main_*.py` × 4 packages)** — COMPLETE (operator-confirmed 2026-06-14, F-02 iol FIXED inline)

**Broken:** 0

---

## Detailed Findings

### ORP-01 — WARNING (unchanged, scope clarified)

**Current status: WARNING — cosmetic dead field, v1.x backlog**

`packages/matriz-client/src/matriz_client/_state.py:59`: `account_id: str | None = None`
never read via `state.account_id` in `packages/matriz-client/src/` (confirmed by `grep -rn "state\.account_id\|_state\.account_id" packages/matriz-client/src/` = 0 matches).

Phase 10 spot check #7 explicitly preserved this field (confirming the `account_id` in `_FORWARDED_TO_STATE` discussion in 10-VALIDATION.md). Phase 11 CR-08 was scoped to: (a) spike-artifact ruff exclusion, (b) `main_higyrus.py:767` line-length — not to `_state.py` cleanup.

Severity: WARNING (not a BLOCKER — frozen `slots=True` dataclass, no mutation risk, no runtime
impact). The field may be intentionally preserved for future `AsyncClient.get_active_orders(account_id)` path where `account_id` is a per-call parameter, not a state-level field.

REQ-IDs affected: none in REQUIREMENTS.md. Informational carry-forward to v1.2 housekeeping.

### INT-01 — RESOLVED (confirmed carry-forward)

`main_iol.py` driver: 0 bare `_base_url` accesses; 15 correct `_get_default()._state.base_url` accesses (10 sync + 5 async). Shim `_DENIED_LEGACY` still active and correct in `iol_client/client.py:592, 608`.

### F-02 iol — RESOLVED (Phase 11 inline fix)

`main_iol.py:1289` probe-state mutation was writing to PEP 562 module attribute (shadowing `__getattr__`). Fixed using INT-01 idiom: `iol_client.client._get_default()._state.token_expires_at = 0.0`. Root cause: PROBE_STALE. Client `iol_client._refresh()` was correct all along. HARN-09 operator fields preserved in `iol-client-findings.md` below `<!-- END AUTO-GENERATED -->` marker.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Issue |
|-------------|-----------------|--------|-------|
| REFAC-01 (Phase 6) | `verification/test_public_surface.py` + 4 snapshot files + `verification/baselines/` + 4 `test_fixture_reaches_production.py` (now 8 active tests — 3 skips FLIPPED in Phase 10) | WIRED | — |
| REFAC-02 (Phase 6) | `Client`/`AsyncClient` per package → PEP 562 `__getattr__` shim → `_get_default()` → `pkg.get_X(...)` delegators → `main_iol.py` uses `_get_default()._state.base_url` (INT-01 RESOLVED) | WIRED | — |
| REFAC-03 (Phase 7) | `_core.py` (4 pkgs) → imported by `client.py` + `aio.py` shells → CI import-linter blocks reverse → `verification/test_sync_async_isolation.py` (9 passed) | WIRED | — |
| REFAC-04 (Phase 10) | `aio.py` 852 LOC with 22 REST methods → `AsyncClient._aensure_token()` → `state.token_store.get_async()` → `TokenStore` with `threading.Lock` + `asyncio.Lock` per loop → `ws_client.py` shares same `state.token_store.get_sync()` | WIRED | — |
| CR-03 (Phase 7) | `_core.parse_envelope_response`: `resp.read()` before `raise_for_response(resp)` in `matriz/_core.py` | WIRED | — |
| CR-05 (Phase 7) | `main_matriz.py::_envelope_probe` helper (1 def, 15 calls) → `verification/test_matriz_sweep_snapshot.py` | WIRED | — |
| RELY-01 (Phase 8) | `_core.RequestSpec` → `client._request()` → `RetryTransport` (tenacity `Retrying`) + `AsyncRetryTransport` (Phase 10) → `_RETRYABLE_STATUS = frozenset({408,409,429,*range(500,600)})` | WIRED | — |
| RELY-02 (Phase 8) | `wait_exponential_jitter(initial=1.0, max=30.0, exp_base=2)` + `_RETRY_AFTER_CAP_S = 60.0` in `_transport.py` + `_atransport.py` | WIRED | — |
| RELY-03 (Phase 8) | `RequestSpec.idempotent` → `req.extensions["idempotent"]` → `RetryTransport`/`AsyncRetryTransport` mutation gate | WIRED | — |
| RELY-04 (Phase 8) | Shell `_request()` 401-handler: `state.token = None` → `_ensure_token()` / `_aensure_token()` with lock → D-23 Risk no-reauth carve-out | WIRED | — |
| LOG-01 (Phase 8) | `__init__.py` → `_logging_attach.attach()` → `logging.getLogger("<pkg>").addHandler(NullHandler)` | WIRED | — |
| LOG-02 (Phase 8) | `_logging.RedactingFilter` per package → package-specific regex → `test_logging_no_token_leak.py` (5 cases) | WIRED | — |
| LOG-03 (Phase 8) | `_transport.py` + `_atransport.py` WARNING/ERROR log calls with structured extras | WIRED | — |
| BUG-01 (Phase 9) | `_core.build_get_instruments_by_cfi_request` guard (`_CFI_ISO_RE`, `_CFI_LITERAL_VALUES`) → raises `PrimaryAPIError` before HTTP call → `aio.py:447-450` wires async path (Phase 10) | WIRED (sync + async) | Prior audit: sync only. Phase 10 closes async gap. |
| BUG-02 (Phase 9) | NO-FIX bucket (a) — operator-authorized override; `test_get_listado_cuentas_url_con_estado_alta` regression guard | WIRED (guard only) | — |
| BUG-03 (Phase 9) | `_ClientState.refresh_token` → `_ensure_token()` → `_refresh()` rotation/preserve → 8 lifecycle tests | WIRED | — |
| BUG-04 (Phase 9) | `get_movimientos(id_cuenta=acct)` per-call → `build_get_movimientos_request(state, id_cuenta=acct)` → distinct wire URLs → `test_multi_account.py` | WIRED | — |
| HARN-07 (Phase 11) | `verification/findings.py:92-93` BEGIN/END markers + `_parse_findings()` 3-zone state machine + `_serialize_findings()` emit markers (HARN-07 comment at line 482) | WIRED | — |
| HARN-08 (Phase 11) | `append_finding(idempotent_by_title=True)` at 4 EXPECTED terminals (main_ambito:604, main_iol:1449, main_higyrus:2061, main_matriz:2212) → content-addressed dedupe in `findings.py:599+` | WIRED | — |
| HARN-09 (Phase 11) | Operator prefix/suffix zones in `_parse_findings()` lines 248-260; `_replace_art_block()` line 210 preserves operator content; `test_findings_append_only.py::test_operator_bullets_inside_findings_survive` (N=3 re-run) | WIRED | — |
| HARN-10 (Phase 11) | `main_matriz.py:2212` D-MATZ-27 EXPECTED terminal with `idempotent_by_title=True`; pre/post count stable = 2 (no 3rd duplicate on re-run) | WIRED | — |
| CR-01 (Phase 11) | `main_matriz.py:1392+` `sample_params` dict uses `{name}` placeholder-everywhere; `verification/test_main_matriz_schema_snapshot_alignment.py` (3 tests) | WIRED | — |
| CR-02 (Phase 11) | `main_matriz.py:484, 503` both return `ProbeResult("login_sync", "FINDING", ...)` — 0 FAIL sites; `verification/test_main_matriz_login_fail_uniformity.py` (2 tests) | WIRED | — |
| CR-04 (Phase 11) | `main_matriz.py:205-253` `_first_dict` 3-branch (ok/no_data/wrong_type); `fname=` opt-in kwarg at 5 call-sites; `verification/test_main_matriz_first_dict.py` (5 tests) | WIRED | — |
| CR-06 (Phase 11) | `main_matriz.py` + `main_higyrus.py`: 0 bare `except Exception` sites; `_RESIDUAL_PROBE_EXCEPTIONS` tuple at module-level; `verification/test_main_drivers_bare_except.py` (2 cases AST guard) | WIRED | — |
| CR-07 (Phase 11) | `main_higyrus.py:217-226` `threading.Lock` + lazy `asyncio.Lock`; `_capture_*_query_string` wrapped at lines 308, 363; `packages/higyrus-client/tests/test_event_hooks_thread_safety.py` (3 tests) | WIRED | — |
| CR-08 (Phase 11) | `pyproject.toml:46-48` `extend-exclude` for spike artifacts; `main_higyrus.py` ruff-clean; `uv run ruff check .` → 0 errors | WIRED | ORP-01 (`_state.py:59` `account_id`) not in CR-08 REQUIREMENTS.md spec — remains WARNING |
| LIVE-01 (Phase 11) | `main_*.py` × 4 packages live run → operator-gated dispositions; F-02 iol PROBE_STALE FIXED inline; 0 blocking regressions; CI 907 passed × 3.12+3.13 | WIRED | — |
| LIVE-02 (Phase 10) | `main_matriz.py` async probes (22 methods) + `_async_main()` + `asyncio.run()` at `main()` line 2216 → operator-confirmed paridad sync↔async (19 paired, 0 divergences) | WIRED (live log local-only) | — |

**Requirements with no cross-phase wiring:** None — all 29 REQ-IDs have been satisfied by their assigned phases and their cross-phase connections have been verified above.

---

## Test Suite Delta

| Run | Count | Date | Phases in scope |
|-----|-------|------|-----------------|
| Baseline (Phase 9 close) | 785 | 2026-06-13 | 06-09 |
| Phase 10 close | 876 | 2026-06-14 | 06-10 |
| Phase 11 close (current) | **907** | 2026-06-14 | 06-11 |

Delta from prior integration check baseline (785): **+122 tests** (+15.5%).
All tests confirmed collected: `907/908 tests collected (1 deselected) in 0.22s`.
1 deselected: live-only marker (as in all prior runs).
0 skips: the 3 D-25 permanent skips from prior audit are now active tests (Phase 10 Plan 10-04 Task 1 flip).

---

## Audit Notes

**Import-linter:** 4 contracts KEPT, 0 broken — confirmed live this run via `uv run lint-imports`.

**CI green final (head commit `71bf201`):**
- `uv run ruff check .` → All checks passed
- `uv run ruff format --check .` → 148 files already formatted
- `uv run mypy --strict` → Success: no issues in 50 source files
- `uv run pytest -q` → 907 passed, 1 deselected (Python 3.12 + 3.13)

**Nyquist compliance:** All 6 phases compliant or operator-accepted partial (Phase 7 SC#3 LOC drop deviation). Phase 10 `10-VALIDATION.md`: `nyquist_compliant: true`. Phase 11 `11-VALIDATION.md`: `nyquist_compliant: true`.

**Phase 10 skip note:** The Phase 10 Nyquist truth gate #5 (snapshot diff) materialized as a 0-line diff because `AsyncClient` was added to `matriz_client.__all__` by Plan 10-02 (the snapshot tool only walks `pkg.__all__`, not submodules). This is documented and accepted in `10-VALIDATION.md:106-110`.

---

_Prior audit: 2026-06-13T20:xxZ (Phases 06-09, INT-01 RESOLVED; Phases 10-11 not started)_
_This audit: 2026-06-14 (All 6 phases complete, LIVE-01 SATISFIED, F-02 iol FIXED, status: passed)_
_Auditor: Claude (gsd-integration-checker)_
