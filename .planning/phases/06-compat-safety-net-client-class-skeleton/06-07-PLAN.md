---
phase: 06-compat-safety-net-client-class-skeleton
plan: 07
type: execute
wave: 2
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified: []
autonomous: false
requirements: [REFAC-01, REFAC-02]

user_setup: []

must_haves:
  truths:
    - "Full test suite (277 baseline + Phase 6 new tests) passes via `uv run pytest -q` locally and in CI for Python 3.12 AND Python 3.13."
    - "ruff check + ruff format --check + mypy --strict packages/*/src pass for all 4 target packages."
    - "Public-surface snapshot test green: `uv run pytest verification/test_public_surface.py -q` produces no diff against committed snapshots; baseline entries preserved per D-06."
    - "All 8 fixture-reaches-production guards (4 sync + 3 async + 1 matriz async skip) pass."
    - "No regressions in main_*.py drivers — manual smoke that each driver imports and runs --help without error (driver code unchanged; the PEP 562 shim + _client forwarding preserves main_higyrus.py's event_hooks mutation pattern per D-21)."
    - "verification/mutation_gate.py works unchanged via the matriz `_base_url` shim extension; smoke test confirms the gate still returns False for non-sandbox hosts."
  artifacts: []
  key_links:
    - from: "CI workflow (.github/workflows/ci.yml)"
      to: "Python 3.12 + 3.13 matrix × 4 packages"
      via: "Matrix job already established; no workflow file edit needed"
      pattern: "matrix.python-version"
---

<objective>
Final phase gate: prove the 4-package refactor (Plans 03-06) plus the safety net (Plans 01-02) are green end-to-end. This plan does NOT modify code or tests — it RUNS the full verification stack: pytest sweep, ruff lint+format, mypy strict, all 8 guards, public-surface snapshot diff, and a manual checkpoint where the operator confirms `main_*.py` drivers still import cleanly. CI matrix on Python 3.12 and 3.13 is the binding gate.

Purpose: REFAC-01 and REFAC-02 success criteria 1-5 are all proven by THIS plan running green. Any failure here means a defect in Plans 03-06 (Wave 1) — go back and fix it before merging the phase.

Output: 0 files modified. 1 checkpoint task for operator approval after automated checks pass. The checkpoint message includes a copy-paste-ready CI status check and a structured driver smoke result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-01-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-02-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-03-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-04-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-05-PLAN.md
@.planning/phases/06-compat-safety-net-client-class-skeleton/06-06-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run automated phase verification — full pytest + ruff + mypy + snapshot diff + driver imports</name>
  <files></files>
  <read_first>
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-VALIDATION.md (Sampling Rate; Validation Sign-Off checklist)
    - .planning/phases/06-compat-safety-net-client-class-skeleton/06-CONTEXT.md (D-07 test cadence)
  </read_first>
  <behavior>
    - `uv run pytest -q` exits 0; collected test count = 277 baseline + Phase 6 new tests (estimate: 277 + ~50 new = ~327 tests + ~1 skipped for matriz async).
    - `uv run ruff check .` exits 0 (no lint errors in any modified file).
    - `uv run ruff format --check .` exits 0 (no formatting drift).
    - `uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src` exits 0.
    - `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/` exits 0 (snapshots match what's committed — proves the regen script is stable AND the live surface matches expectations).
    - Each `main_*.py` driver imports without raising. The drivers are NOT executed end-to-end against live APIs in CI (env_gate would block them); this task only confirms the import path is intact.
    - `verification/mutation_gate.py` smoke test: configure matriz to sandbox host → `mutating_allowed()` returns True; configure to prod host → returns False.
    - All checks above pass on Python 3.12. CI matrix repeats on 3.13.
  </behavior>
  <action>
    Execute the following commands in order, capturing exit codes. Stop on first failure and surface the failure to the checkpoint task (Task 2). NO source code edits in this task — diagnostic only.

    1. **Full pytest sweep:**
       `uv run pytest -q`
       Expected: 0 failures; ~327 tests passed + ~1 skipped (matriz async guard).

    2. **Ruff lint + format check:**
       `uv run ruff check .`
       `uv run ruff format --check .`
       Both exit 0.

    3. **Mypy strict:**
       `uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src`
       Exit 0.

    4. **Public-surface snapshot self-check:**
       `uv run python verification/regen_snapshots.py && git diff --exit-code verification/snapshots/`
       Exit 0 — proves snapshots are reproducible and match committed state.

    5. **Driver import smoke (no live calls):**
       For each of `main_iol.py`, `main_higyrus.py`, `main_matriz.py`, `main_ambito_financiero.py`, `main_wallets.py`:
       evaluate the module body without triggering the `if __name__ == '__main__'` block (e.g., `uv run python -c "exec(open('main_iol.py').read().split('if __name__')[0])"`).
       Each must succeed without raising — confirms the driver's `import iol_client` (etc.) plus any module-level setup still works through the new shim. The drivers that mutate `pkg.client._client.event_hooks` (main_higyrus.py per D-21) are EXPECTED to work because the shim forwards `_client` to `_default()._state.http_client` lazily — the first read triggers `_get_default()` which instantiates `Client()` which lazily creates `httpx.Client` on first request; if the driver mutates `event_hooks` BEFORE any request, it forces eager `http_client` creation via `_ensure_http_client()`. **CRITICAL:** the shim must return a NON-NONE `httpx.Client` even before any request — verify by reading the shim implementation and confirming `_get_default()._state.http_client` is materialized in `__getattr__` (it may not be, in which case mutating `event_hooks` on `None` would crash). If this is broken, surface to Task 2 checkpoint as a BLOCKING issue and recommend Plan 04/05 to add `self._ensure_http_client()` call inside the shim's `_client` forwarding branch.

    6. **mutation_gate smoke:** import matriz_client, `configure(base_url="https://api.remarkets.primary.com.ar")`, set `VERIFY_MUTATING=1`, assert `mutating_allowed() is True`. Then `configure(base_url="https://api.primary.com.ar")` and assert `mutating_allowed() is False`.

    7. **Compile a structured pass/fail report** for Task 2 to consume. Format:

        Phase 6 verification report
        ============================
        [PASS|FAIL] uv run pytest -q                          (<N> tests, <S> skipped)
        [PASS|FAIL] uv run ruff check .
        [PASS|FAIL] uv run ruff format --check .
        [PASS|FAIL] uv run mypy --strict packages/*/src       (per package)
        [PASS|FAIL] Snapshot regen idempotent                  (regen + git diff --exit-code)
        [PASS|FAIL] main_iol.py import smoke
        [PASS|FAIL] main_higyrus.py import smoke
        [PASS|FAIL] main_matriz.py import smoke
        [PASS|FAIL] main_ambito_financiero.py import smoke
        [PASS|FAIL] main_wallets.py import smoke              (wallets out of phase scope; should still import)
        [PASS|FAIL] mutation_gate sandbox=True / prod=False

  </action>
  <verify>
    <automated>uv run pytest -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run mypy --strict packages/ambito-financiero-client/src packages/iol-client/src packages/higyrus-client/src packages/matriz-client/src &amp;&amp; uv run python verification/regen_snapshots.py &amp;&amp; git diff --exit-code verification/snapshots/</automated>
  </verify>
  <done>
    All 11 checks in the structured pass/fail report (step 7) show PASS. Any FAIL surfaces a defect in a prior plan — STOP and fix that plan before proceeding to Task 2. The report is the input to the checkpoint.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator verifies CI green on push (Python 3.12 + 3.13 matrix) + reviews snapshot diff</name>
  <files></files>
  <what-built>
    Phase 6 ships: 2 verification harness additions (snapshot test + 8 fixture-reaches-production guards),
    4 per-package skeletons (`_state.py` + `Client` + `AsyncClient` per package; matriz async is stub-only),
    PEP 562 read-only shims preserving legacy `pkg.client._token` etc., conftest migrations away from
    `monkeypatch.setattr(... raising=False)` to `pkg.configure(token=...)`, all inline test-body
    monkeypatch sites migrated, ws_client.py + cross-package mutation gate test migrated.

    All automated checks in Task 1 must be PASS before this checkpoint fires. The pass/fail report
    from Task 1 step 7 is included verbatim.
  </what-built>
  <action>
    Operator action: review Task 1 pass/fail report, push the branch, watch the CI matrix on Python 3.12 + 3.13,
    review the snapshot diff for additive-only changes, and approve. See `<how-to-verify>` for the exact steps.
  </action>
  <how-to-verify>
    1. Confirm the Task 1 pass/fail report shows 11 PASS lines (no FAIL).

    2. Review the snapshot diff: `git diff verification/snapshots/`. Each of the 4
       `<pkg>-surface.txt` files should show ONLY additive lines (`Client : class : ...`,
       `AsyncClient : class : ...`, etc.) — NO line should be REMOVED from any baseline file
       (D-06 invariant).

    3. Push the branch and check the CI Actions tab:
       - lint job: green
       - typecheck job: green
       - tests job × Python 3.12 × 4 packages: green
       - tests job × Python 3.13 × 4 packages: green
       Expected total: ~10 green jobs depending on the matrix.

    4. Manual smoke (OPTIONAL — operator-driven, NOT required for phase close):
       For one of the 4 drivers (recommend `main_higyrus.py` because it mutates `_client.event_hooks`
       per D-21), run `uv run --package higyrus-client python main_higyrus.py --help` and confirm
       it does NOT raise an AttributeError on `pkg.client._client`. If a real live run is feasible
       (operator has `.env` populated), run a single probe and confirm the network call uses the
       refactored Client surface.

    5. Confirm with `uv run python -c "import iol_client; print(type(iol_client.Client).__name__, type(iol_client.AsyncClient).__name__); print(type(iol_client._get_default()).__name__)"` — should print without errors.

    Resume signal:
    - "approved" if all checks pass and CI is green.
    - Describe any issues otherwise (e.g., "ruff complains about UP032 in iol/client.py line 88" — Plan 04 needs a fix).
  </how-to-verify>
  <verify>
    <human-check>Operator approves once Task 1 report shows 11 PASS lines AND CI matrix on Python 3.12 + 3.13 is green AND snapshot diff is additive-only.</human-check>
  </verify>
  <done>
    Operator types "approved" after confirming: Task 1 report = 11 PASS, CI matrix green for 3.12 + 3.13, snapshot diff is additive-only (no baseline entries removed).
  </done>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local pre-CI checks → CI matrix | Local checks pass on the operator's Python 3.12; CI matrix repeats on 3.13. The gate is binary green/red on both. |
| Driver smoke → unchanged main_*.py code | The phase explicitly does NOT touch `main_*.py` per D-21. The drivers are expected to work transparently through the shim. The smoke check is a safety net for the shim contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | snapshot file regression unnoticed | mitigate | Task 2 explicitly requires operator review of `git diff verification/snapshots/` with the D-06 additive-only invariant. The PR template review step is the human gate. |
| T-06-11 | Denial of Service | CI matrix flaky failure | accept | If a single CI job is flaky and others green, the operator may re-run. Persistent failures are addressed by reverting the offending plan and re-planning. |
| T-06-SC | Tampering | npm/pip/cargo installs | N/A | No packages installed by this plan. |
</threat_model>

<verification>
- Task 1 automated check (long shell pipeline) exits 0.
- Task 2 operator approval ("approved" resume signal) after reviewing CI status + snapshot diff.
</verification>

<success_criteria>
- REFAC-01 success criteria 1, 2 fully met (snapshot test green; 8 fixture-reaches-production guards green).
- REFAC-02 success criteria 3, 4, 5 fully met (4 packages expose Client+AsyncClient with lifecycle, top-level API intact, 277 baseline tests green, CI matrix 3.12 + 3.13 green).
- Phase 6 deliverable complete: `_state.py` per package + Client/AsyncClient class skeletons + PEP 562 shim + conftest migration; refresh_token forward-declared (`None` for non-iol; populated for iol via shim and configure() kwarg).
- Ready to launch Phase 7 (`_core.py` extraction).
</success_criteria>

<output>
Create `.planning/phases/06-compat-safety-net-client-class-skeleton/06-07-SUMMARY.md` when done.
</output>
