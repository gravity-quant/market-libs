---
phase: 36
plan: 03
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - verification/test_main_market_data_deep_chain.py
  - main_market_data.py
  - packages/market-data-client/tests/test_null_object.py
autonomous: true
requirements: [NOBJ-MD-01, NOBJ-MD-02]

must_haves:
  truths:
    - "The four market-data read probes of main_market_data.py each dereference the full chain on the snapshots they just fetched — the driver CONSUMES the new shape at its real sites rather than merely counting rows (ROADMAP SC-5, D-09)."
    - "Every one of those dereferences sits inside the probe's own try block, so a broken link degrades to a FINDING and can never crash the driver or flip the package to FAILED."
    - "The four probe names market_data_sync, latest_sync, market_data_async and latest_async are unchanged — probe-name stability is a lock inherited from LIVE-01 / REFAC-05 and downstream findings are keyed on them."
    - "No second exception renderer appears: the driver still routes every caught exception through its single sanctioned renderer, and any helper introduced here receives already-rendered text rather than an exception object."
    - "_ENDPOINT_OPTIONAL still holds both note and entries, so the no-data row's model-only entries stays suppressed and no phantom SHAPE finding appears on the next live run (Pitfall 7 / F-6 — this is the measured answer to CONTEXT's open discretion item)."
    - "The committed live-capture baselines under .planning/verification/schemas/market-data-client/ are byte-identical to their pre-phase state — to_dict() became a lossy model-shaped projection in Plan 36-02 and must never reach a snapshot site (Pitfall 9)."
    - "market-data-client is still version 0.5.0 in both pyproject.toml and __version__, CHANGELOG and README are untouched, and uv.lock is unrefreshed — the bump, the changelog callout and the old-to-new migration table are Phase 40 work in full (D-09)."
    - "No prose anywhere in packages/market-data-client/ still claims this package takes form B of D-07 or runs a mapping pass — the form B to form A transition is a declared deliverable, not a side effect (Pitfall 4)."
    - "The SafeModel roster reachable by introspection is nineteen classes and each of the three new ones passes the four Null Object contract tests unedited — the roster bound in test_null_object.py absorbed the growth without being loosened."
  artifacts:
    - path: "verification/test_main_market_data_deep_chain.py"
      provides: "AST lock — each of the four read probes dereferences the chain inside its try body"
      min_lines: 60
    - path: "main_market_data.py"
      provides: "Deep-chain consumption at the four real probe sites (SC-5)"
      contains: "market_data.last"
    - path: "packages/market-data-client/tests/test_null_object.py"
      provides: "Form A prose and the retitled constructor-agreement test"
  key_links:
    - from: "verification/test_main_market_data_deep_chain.py"
      to: "main_market_data.py"
      via: "ast.parse over the driver source, mirroring the postprocess-guarded lock"
      pattern: "ast\\.parse"
    - from: "main_market_data.py"
      to: "packages/market-data-client/src/market_data_client/models.py"
      via: "the probes read the alias properties off the snapshots they fetched"
      pattern: "market_data\\.(last|bids|offers|settlement|close|open_interest)"
  prohibitions:
    - statement: "Do not rewrite or regenerate the committed live-capture baselines under .planning/verification/schemas/market-data-client/. They are the only record of what the vendor actually sends, and after Plan 36-02 the model projection no longer resembles the wire."
    - statement: "Do not bump the version, refresh uv.lock, or add a changelog entry to reach a sense of completeness. D-09 assigns the breaking bump, the callout and the migration table to Phase 40 in full, replicating the v1.6 pattern where one phase typed and a later one published."
    - statement: "Do not reach outside the three files this plan names. In particular _ENDPOINT_OPTIONAL is not an inconsistency to tidy up — removing entries from it manufactures a false SHAPE finding on every live run, which RESEARCH measured rather than argued."
---

<objective>
Close the phase: make the driver actually SPEND the new shape at its four real probe sites, lock
that consumption structurally so a future refactor cannot quietly revert it to a row count, and
finish the form B → form A prose transition the source change made true.

Purpose: ROADMAP SC-5 has two halves. Plan 36-02 delivered the first — the mapping machinery is
gone without moving the `_decode.py` digest. This plan delivers the second: `main_market_data.py`
consumes by deep chaining at its real sites (D-09), which is what turns the phase's claim from
"the chain type-checks" into "the chain is exercised by the code that will run against develop in
Phase 39". A probe that only counts rows would let a broken link ship.

Output: a new AST lock in `verification/`, four edited probe bodies, and a `models`/test prose
surface that no longer describes a pass that does not exist.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-CONTEXT.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-PATTERNS.md
@.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-02-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Created by THIS plan:

| Symbol | Kind | Home |
|--------|------|------|
| `verification/test_main_market_data_deep_chain.py` | NEW verification module — AST lock asserting each read probe dereferences the chain inside its `try` body | `verification/` |

Changed by THIS plan: the four probe bodies `probe_market_data_sync`, `probe_latest_sync`,
`probe_market_data_async`, `probe_latest_async` in `main_market_data.py`; the form-B prose and one
test title in `packages/market-data-client/tests/test_null_object.py`.

Created earlier in the phase and consumed here: `BookLevel`, `EntryValue`, `MarketDataEntries` and
the six aliases `bids`, `offers`, `last`, `settlement`, `close`, `open_interest` (Plan 36-02);
`packages/market-data-client/tests/test_market_data_chain.py` (Plan 36-02).

Explicitly NOT produced by this phase, by D-09: any version bump, any CHANGELOG entry, any
old-to-new migration table, any `uv.lock` refresh, and any live run against develop.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — lock the driver's deep-chain consumption by AST before writing it</name>
  <files>verification/test_main_market_data_deep_chain.py</files>
  <read_first>
    - verification/test_main_market_data_postprocess_guarded.py (whole file — the exact precedent: module docstring stating the contract, `_REPO_ROOT` / `_DRIVER` constants, `_protected_call_ids` walking only `try` BODIES, and a non-vacuity floor constant that reddens if the driver is gutted)
    - main_market_data.py:825-885 (`probe_market_data_sync` and `probe_latest_sync` — the two sync sites, their `try` ladders and their `ProbeResult` returns)
    - main_market_data.py:1136-1194 (`probe_market_data_async` and `probe_latest_async` — the async mirrors)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-PATTERNS.md § "main_market_data.py (driver, request-response) — SC-5" (the four site line numbers and the four driver locks the edit must respect)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md § "Wave 0 Requirements" (the AST-vs-inspection choice — AST is the recommendation, and this task takes it)
  </read_first>
  <behavior>
    - For each of the four probe function names, the AST contains at least one attribute access
      whose chain passes through `market_data` and then through one of the six alias names, and
      that access node is lexically inside the `body` of a `try` statement within that same
      function.
    - The count of such guarded accesses across the driver is at least the floor the module
      declares, so gutting the driver reddens the lock instead of passing it vacuously.
    - An access placed in an `except`, `else` or `finally` clause does NOT count as guarded — the
      D-09 ladder does not catch it.
    - The four probe names are asserted present by name, so a rename reddens here as well as at
      the existing probe-name locks.
  </behavior>
  <action>
    Create the module as a structural sibling of `verification/test_main_market_data_postprocess_guarded.py`.
    Copy its skeleton: the `from __future__ import annotations` header, `import ast`,
    `from pathlib import Path`, the `_REPO_ROOT` / `_DRIVER` constants, and the helper that
    collects the ids of every `Call` / expression node reachable from the `body` of a `try` inside
    a given function — deliberately excluding the handler, `else` and `finally` clauses, for the
    reason that module already states.

    Declare a frozenset of the six alias names — `bids`, `offers`, `last`, `settlement`, `close`,
    `open_interest` — and a frozenset of the four probe names. Walk the driver's AST; for each
    named probe, collect every `ast.Attribute` node whose attribute is one of the six aliases and
    whose value chain reaches an attribute named `market_data`. Assert each of the four probes
    contributes at least one such node and that every node found is inside a protected `try` body.
    Add a non-vacuity floor constant in the idiom of `_MIN_GUARDED_CALLS`, set to the number of
    accesses the driver will carry after Task 2, so a gutted driver fails RED rather than passing.

    Write a module docstring that states the contract in the same voice as its sibling: ROADMAP
    SC-5 requires the driver to consume by deep chaining at its real sites; a probe that only
    counts rows would pass while the chain was broken; and the D-09 never-FAILED contract requires
    that consumption sit inside the `try`.

    Do not import `main_market_data` at module scope — parse its source. The driver has import-time
    side effects and the sibling locks all parse rather than import for exactly that reason.

    This task ends RED: the driver does not chain yet.
  </action>
  <verify>
    <automated>uv run pytest verification/test_main_market_data_deep_chain.py -q 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest verification/test_main_market_data_deep_chain.py -q` FAILS, and the failure message names at least one of the four probes as carrying no chained access — not an import error and not a parse error.
    - `uv run ruff check verification/test_main_market_data_deep_chain.py` is clean.
    - `uv run mypy verification/test_main_market_data_deep_chain.py` prints `Success: no issues found`, if `verification/` is inside the mypy enrolment; otherwise the file carries full annotations regardless.
    - `grep -c 'import main_market_data' verification/test_main_market_data_deep_chain.py` reports `0` — the driver is parsed, never imported.
    - `uv run pytest verification/test_main_market_data_postprocess_guarded.py verification/test_main_market_data_no_gate_bypass.py verification/test_main_market_data_no_config_write.py verification/test_main_market_data_snapshot_identifiers.py verification/test_main_market_data_skip_line_shape.py verification/test_main_market_data_uses_single_client_instance.py verification/test_main_market_data_cleanup_emits_finding.py verification/test_cycle_closure_market_data.py verification/test_probe_context_coverage.py -q` still reports `29 passed` — the new lock disturbs no existing driver lock.
  </acceptance_criteria>
  <done>The SC-5 driver lock exists, parses the driver rather than importing it, carries a non-vacuity floor, and fails for exactly one reason: the driver does not chain yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — make the four read probes dereference the chain at their real sites</name>
  <files>main_market_data.py</files>
  <read_first>
    - main_market_data.py:825-885 (the two sync probes — note the `# D-09:` comment idiom already marking post-processing as inside the `try`)
    - main_market_data.py:1136-1194 (the two async mirrors)
    - main_market_data.py:100-131 (`_CLIENT_STAMPED`, `_ENDPOINT_OPTIONAL` and the comment that already warns against confusing it with `_ENDPOINT_TEMPLATES` — this constant is READ, not edited)
    - main_market_data.py:374-420 (`_finding_for_exc` and the single sanctioned exception renderer — nothing added here may become a second one)
    - main_market_data.py:678-720 (`_emit_shape` and the `skip_model_only` set that consumes `_ENDPOINT_OPTIONAL`)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Encadenamiento profundo en el driver (SC-5)" (the shape of the insertion and the four driver locks) and § "Pitfall 7" (the measured verdict on `_ENDPOINT_OPTIONAL`)
  </read_first>
  <behavior>
    - Each of the four probes, after fetching its snapshots, evaluates the full chain over every
      snapshot it received and reports how many it chained in its `ProbeResult` detail string.
    - The evaluation is inside the `try`, so if any link were `None` the probe returns a FINDING
      through the existing exception ladder instead of propagating.
    - With an empty result list the chain evaluation is a no-op and the probe still returns PASS —
      zero snapshots is not a failure.
    - Probe names, endpoint templates, decorators and the `_finding_for_exc` return path are
      untouched.
  </behavior>
  <action>
    Four edits, one per probe, all the same shape.

    In `probe_market_data_sync`, immediately after `snapshots = client.get_market_data(active=True)`
    and still inside the `try`, build a list comprehension over `snapshots` that dereferences the
    chain for each row: the symbol, `market_data.last.price`, the length of `market_data.bids`, the
    length of `market_data.offers`, `market_data.settlement.price`, `market_data.close.price` and
    `market_data.open_interest.price`. Bind it to a local. Then extend the existing `ProbeResult`
    detail string with a `chained=` term reporting the length of that local. Carry a `# D-09:`
    comment in the idiom already used at the site, stating that the chaining is deliberately inside
    the `try` so a broken link degrades to a finding rather than a crash, and that SC-5 requires the
    driver to EXERCISE the chain rather than count rows.

    Repeat verbatim in `probe_latest_sync` over the `latest` list, in `probe_market_data_async`
    over its awaited `snapshots`, and in `probe_latest_async` over its awaited `latest`. Keep the
    sync and async bodies structural mirrors of each other, which is how the rest of this driver is
    written (C-3).

    Respect the four driver locks without exception:
    - All post-processing inside the `try` (D-09). Nothing new goes after the `except`.
    - Do not rename a probe. `name = "market_data_sync"` and its three siblings are keyed by
      downstream findings (LIVE-01 / REFAC-05).
    - Do not introduce a second exception renderer. Nothing added here reads, formats or accepts an
      exception object; helpers in this driver take already-rendered text. The AST census that
      enforces this flags any function whose parameter is annotated with an exception type.
    - Do not touch `_ENDPOINT_OPTIONAL`. RESEARCH MEASURED the outcome: with the new shape, the
      bidirectional SHAPE diff over the no-data row returns exactly one model-only finding for
      `entries`, and that constant is what suppresses it; removing `entries` from the frozenset
      would manufacture a false SHAPE finding on every live run of Phase 39. `market_data` does not
      need adding, because the no-data row does carry that key, with a null. This is the measured
      answer to the open discretion item CONTEXT left for the planner — record it in the SUMMARY as
      resolved, not as still open.

    Do not touch `_write_schema_snapshot`, `_raw_via_request_sync` or `_raw_via_request_async`.
    Those sites feed RAW WIRE to the schema snapshots on purpose; after Plan 36-02, `to_dict()` is
    a lossy model-shaped projection and must not reach them (Pitfall 9).
  </action>
  <verify>
    <automated>uv run pytest verification/test_main_market_data_deep_chain.py verification/test_main_market_data_postprocess_guarded.py verification/test_main_market_data_no_gate_bypass.py verification/test_main_market_data_no_config_write.py verification/test_main_market_data_snapshot_identifiers.py verification/test_main_market_data_skip_line_shape.py verification/test_main_market_data_uses_single_client_instance.py verification/test_main_market_data_cleanup_emits_finding.py verification/test_cycle_closure_market_data.py verification/test_probe_context_coverage.py -q &amp;&amp; uv run ruff check main_market_data.py &amp;&amp; uv run mypy main_market_data.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest verification/test_main_market_data_deep_chain.py -q` PASSES — the lock from Task 1 turns green.
    - The nine pre-existing market-data driver locks still report `29 passed` in aggregate.
    - `uv run ruff check main_market_data.py` and `uv run ruff format --check main_market_data.py` are clean.
    - `uv run mypy main_market_data.py` prints `Success: no issues found` — the chain type-checks at the call site, not only in the library.
    - `grep -c 'name = "market_data_sync"\|name = "latest_sync"\|name = "market_data_async"\|name = "latest_async"' main_market_data.py` reports `4` — no probe was renamed.
    - `git rev-parse HEAD:main_market_data.py` differs from its pre-task value ONLY through this plan's commits, and `grep -n '_ENDPOINT_OPTIONAL = frozenset' main_market_data.py` still shows both `note` and `entries`.
    - `git diff --stat -- .planning/verification/schemas/market-data-client/` is empty.
  </acceptance_criteria>
  <done>All four read probes exercise the full chain inside their try ladders, the AST lock is green, every pre-existing driver lock still passes, and no probe was renamed.</done>
</task>

<task type="auto">
  <name>Task 3: Finish the form B to form A transition in prose and run the phase gate</name>
  <files>packages/market-data-client/tests/test_null_object.py</files>
  <read_first>
    - packages/market-data-client/tests/test_null_object.py:1-39 (the module docstring, whose fourth paragraph declares this package's membership in form B and describes the mapping pass)
    - packages/market-data-client/tests/test_null_object.py:254-282 (`test_empty_emits_nothing`'s docstring explaining why form B earns the test, and `test_empty_and_from_api_agree_on_every_mapping_declared_field`, whose subject no longer exists)
    - packages/market-data-client/tests/test_null_object.py:217-226 (`test_the_model_roster_is_not_vacuous` — the `>= 16` bound that RESEARCH F-7 verified absorbs the growth to 19 without an edit)
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-RESEARCH.md § "Pitfall 4" (the four prose sites; two were handled in Plan 36-02, these two are the rest) and § F-7
    - .planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-VALIDATION.md § "Sampling Rate" (the phase-gate command list)
  </read_first>
  <action>
    Two prose edits and one retitle in `test_null_object.py`, then the phase gate.

    Rewrite the module docstring's form-B paragraph: this package now takes form A of D-07 — its
    tolerant constructor and its `empty()` both reach the walker directly with nothing between —
    and the per-package differences that remain are still declared policy axes that must not be
    harmonised. Keep the surrounding paragraphs, including the deliberate-local-copies rationale,
    which is unchanged and is load-bearing for Plan 36-01's helper copies.

    Rewrite `test_empty_emits_nothing`'s docstring, which currently earns the test on form B
    grounds. Its property survives the transition and is if anything sharper now: `empty()` does
    not decode wire data, so it must report nothing, and with three levels of nested Null Objects
    a leaky sink would emit a record per field per level.

    Retitle `test_empty_and_from_api_agree_on_every_mapping_declared_field`. Its assertion still
    passes — RESEARCH verified both constructors converge on the same empty container — but its
    NAME and docstring describe a mapping field that no longer exists, so the green no longer means
    what it says. Rename it for the property it actually holds now: the two constructors agree on
    the nested container field, which is what makes a truthiness comparison against
    `snapshot.market_data` answer about the payload rather than about a constructor disagreement.
    Do not delete it — that property is exactly what a three-level Null Object chain depends on.

    Do NOT touch `test_the_model_roster_is_not_vacuous`. Its bound already accommodates the growth
    from sixteen classes to nineteen, and the growth is asserted individually by the parametrized
    tests. Loosening or tightening it here would be churn.

    Then run the phase gate in full and record the outcome in the SUMMARY. The gate is the four CI
    jobs, with one documented carve-out: the `verification/` directory as a whole is PRE-EXISTING
    RED and slow — measured at 21 failed, 19 errors, 385 passed in 13m49s, all failures in the
    matriz sweep, which is the standing item STATE records as open since Phase 32 and which
    `ci.yml` never exercises because it passes an explicit per-package path. Gate on the nine
    market-data driver locks plus the new one instead, and state in the SUMMARY that the matriz
    verification red is pre-existing and untouched by this phase, with the measured before-and-after
    counts to prove it.
  </action>
  <verify>
    <automated>uv run pytest packages/market-data-client -q 2>&amp;1 | tail -3 &amp;&amp; uv run pytest verification/test_main_market_data_deep_chain.py verification/test_main_market_data_postprocess_guarded.py verification/test_main_market_data_no_gate_bypass.py verification/test_main_market_data_no_config_write.py verification/test_main_market_data_snapshot_identifiers.py verification/test_main_market_data_skip_line_shape.py verification/test_main_market_data_uses_single_client_instance.py verification/test_main_market_data_cleanup_emits_finding.py verification/test_cycle_closure_market_data.py verification/test_probe_context_coverage.py -q &amp;&amp; uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run python tools/check_uniform_structure.py &amp;&amp; uv run python tools/check_surface_types.py &amp;&amp; uv run mypy</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest packages/market-data-client -q` is fully green at the count Plan 36-02 established.
    - `uv run pytest packages/market-data-client/tests/test_null_object.py -q` is green, with the three new classes each appearing in the four parametrized Null Object contract tests (roster 19).
    - `grep -ci 'form b' packages/market-data-client/src/market_data_client/models.py packages/market-data-client/tests/test_null_object.py` reports `0` for both files.
    - `uv run pytest verification/... -q` over the ten market-data driver locks reports `30 passed` (the 29 pre-existing plus the new deep-chain lock).
    - `uv run ruff check . && uv run ruff format --check .` are clean over the whole repo.
    - `uv run lint-imports` succeeds.
    - `uv run python tools/check_decode_intactness.py` succeeds with the digest equal to `CANONICAL_DIGEST`, `tools/check_uniform_structure.py` succeeds, and `tools/check_surface_types.py` reports `0 violations`.
    - `uv run mypy` prints `Success: no issues found` for every enrolled package, and `uv run pre-commit run --all-files` passes.
    - `git rev-parse HEAD:packages/market-data-client/pyproject.toml` is `cfb60655efb48907f417efd41b6b28d64499f99f`, `git rev-parse HEAD:uv.lock` is `5c8ea46c0be875d8634a573a3fb06dba78e8cb8e`, and `grep -c '__version__ = "0.5.0"' packages/market-data-client/src/market_data_client/__init__.py` reports `1` — D-09 held.
    - `git rev-parse HEAD:packages/market-data-client/src/market_data_client/_core.py` is `295ce13c609e6bec98b22b5ccb0ad6ff68ad4162`, `..._decode.py` is `5446832dea23a74cec5cb1e91173edcd42a377a5`, `...client.py` is `7724342d2e0ae886058b22b79fb88c18674d81e4`, `...aio.py` is `c4a75c1d77344cda086a6d7152b22ab854515de4`.
    - `git diff --stat -- .planning/verification/schemas/market-data-client/` is empty.
  </acceptance_criteria>
  <done>No prose in the package still describes a pass that does not exist, the retitled constructor-agreement test says what it means, and the phase gate is green with the pre-existing matriz verification red documented and quantified rather than silently inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| develop API response → driver probe → findings file | Untrusted vendor data reaches a file committed to git forever. This plan adds four dereferences on that path but emits no new value into a finding. |
| exception → findings / crash report | The driver has exactly one sanctioned renderer. This plan must not create a second. |
| model projection → committed schema baselines | After Plan 36-02, `to_dict()` is model-shaped rather than wire-shaped. The snapshot sites must keep feeding raw wire. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Information Disclosure | The `ProbeResult` detail string and any new finding text carrying a symbol or account identifier into a committed findings file | mitigate | The detail string gains a COUNT term only (`chained=<int>`), never a symbol, price or identifier; the chained tuple stays a local and is never rendered. Acceptance: `ruff` + the existing `verification/test_main_market_data_snapshot_identifiers.py` lock stay green. |
| T-36-03-02 | Repudiation | Overwriting the committed live-capture baselines, destroying the only evidence of vendor behaviour | mitigate | The snapshot sites are not touched; they keep feeding raw wire via `_raw_via_request_sync` / `_async`. Acceptance: `git diff --stat -- .planning/verification/schemas/market-data-client/` is empty in both Task 2 and Task 3. |
| T-36-03-03 | Tampering | A second exception renderer entering the driver and diverging from the sanctioned redaction | mitigate | Nothing added accepts or formats an exception; the chained tuple is built from model attributes only. The existing AST census that flags any function taking an exception-annotated parameter runs in the per-task gate. |
| T-36-03-04 | Denial of Service | The chain evaluation crashing the driver and flipping the package to FAILED | mitigate | Every dereference is placed inside the probe's `try`, structurally enforced by the new AST lock (Task 1) and by the pre-existing post-process guard. A broken link degrades to a FINDING by construction. |
| T-36-03-05 | Elevation of Privilege | Loosening the mutating gate while editing the driver | accept | Only read probes are edited. `verification/test_main_market_data_no_gate_bypass.py` and `test_main_market_data_no_config_write.py` run in every task gate of this plan. |
| T-36-03-SC | Tampering | npm / pip / cargo supply chain | accept | This plan installs nothing and refreshes nothing. Pinned by blob hash in the Task 3 acceptance criteria: `uv.lock` and `packages/market-data-client/pyproject.toml` must be byte-identical to their phase-start values (D-09). |
</threat_model>

<verification>
- `uv run pytest packages/market-data-client -q` — fully green.
- The ten market-data driver locks under `verification/` — `30 passed`.
- `uv run ruff check . && uv run ruff format --check . && uv run lint-imports` — clean.
- `uv run python tools/check_decode_intactness.py && uv run python tools/check_uniform_structure.py && uv run python tools/check_surface_types.py` — all clean, digest unmoved.
- `uv run mypy` and `uv run pre-commit run --all-files` — clean.
- Blob-hash pins for `_core.py`, `_decode.py`, `client.py`, `aio.py`, `pyproject.toml`, `uv.lock` — all unchanged.
- `git diff --stat -- .planning/verification/schemas/market-data-client/` — empty.

**Documented carve-out:** the full `verification/` directory is pre-existing RED (21 failed, 19
errors, 385 passed, 13m49s — measured at phase-planning time, all failures in the matriz sweep) and
is never exercised by `ci.yml`, which passes an explicit per-package path. This phase neither
causes nor repairs it; the SUMMARY must state the before/after counts so the inheritance is
explicit rather than silent.
</verification>

<success_criteria>
- ROADMAP SC-5 is complete on both halves: the mapping machinery is gone without moving the `_decode.py` digest (Plan 36-02), and `main_market_data.py` consumes by deep chaining at its four real sites (this plan), structurally locked.
- The `_ENDPOINT_OPTIONAL` discretion item CONTEXT left open is recorded as RESOLVED — unchanged, on measured evidence.
- No prose in `packages/market-data-client/` still claims form B or a mapping pass.
- D-09 held end to end: no version bump, no changelog, no migration table, no lock refresh, no live run.
- The whole phase is green on the four CI jobs, with the pre-existing matriz verification red quantified and attributed.
</success_criteria>

<output>
Create `.planning/phases/36-market-data-client-market-data-tipado-revocaci-n-de-la-fase-/36-03-SUMMARY.md` when done.
Record: the resolved `_ENDPOINT_OPTIONAL` verdict with its measured evidence; the before/after
`verification/` counts documenting the inherited matriz red; the confirmation that D-09 held on all
five artefacts (`pyproject.toml`, `__version__`, CHANGELOG, README, `uv.lock`); and the handoff
notes Phase 39 and Phase 40 will need — for 39, that the driver now exercises the chain so a
missing wire key surfaces as a non-fatal `extra` to be corrected in-cycle; for 40, the breaking
`market_data` shape change plus the three additive public names, both awaiting the bump and the
old-to-new migration table.
</output>
