---
phase: 07-core-py-extraction-sync-async-logic-dedup
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - packages/iol-client/src/iol_client/_core.py
  - packages/iol-client/src/iol_client/client.py
  - packages/iol-client/src/iol_client/aio.py
  - packages/iol-client/tests/test_core.py
autonomous: true
requirements: [REFAC-03]
requirements_addressed: [REFAC-03]
tags: [phase-07, iol, oauth, refresh-token, refac-03]
user_setup: []

must_haves:
  truths:
    - "`iol_client._core` define `RequestSpec` con field `data` para form-encoded OAuth + builders/parsers de endpoints + auth-flow primitives `build_login_request`/`parse_login_response`/`build_refresh_request`/`parse_refresh_response` (D-02 Pattern 5)"
    - "Semántica de `refresh_token` rotation CONDICIONAL preservada (CR-01 — solo escribe `state.refresh_token` si el response trae un valor truthy; ROADMAP Phase 6 D-05 documenta esta política)"
    - "`client.py` y `aio.py` colapsan a transport shells; `login()`/`_refresh()` orchestran HTTP via `_core` builders + parsers"
    - "Aliases D-04 `_raise_for_response = _core.raise_for_response` en client.py y aio.py — identidad B8 preservada"
    - "B8 test existente `test_aio_imports_raise_for_response_from_client` (iol — `packages/iol-client/tests/test_client_class.py:275-279`) sigue verde"
    - "LOC drop ≥30% agregado vs Phase 6 baseline 998 (client.py=522 + aio.py=476)"
    - "Public surface snapshot zero diff (D-16)"
    - "Cross-leak guard (`verification/test_sync_async_isolation.py`) para iol sync + iol async sigue verde — `SYNC-sentinel-iol_client` y `ASYNC-sentinel-iol_client` aislados"
    - "PEP 562 shim Phase 6 INTACTO; deny-list de `_user`/`_password` (Phase 6 D-Q) sigue activa — credentials NUNCA leakeen via shim"
    - "Suite baseline + nuevos tests de `_core` verde; `lint-imports` exit 0 con 4 contracts pasando"
  artifacts:
    - path: "packages/iol-client/src/iol_client/_core.py"
      provides: "RequestSpec (iol-shape with `data`) + auth-flow primitives + per-endpoint builders/parsers"
      contains: "build_login_request"
      min_lines: 100
    - path: "packages/iol-client/src/iol_client/client.py"
      provides: "Sync transport shell: Client class preserved; login()/_refresh() orchestran _core primitives"
      contains: "_raise_for_response = _core.raise_for_response"
    - path: "packages/iol-client/src/iol_client/aio.py"
      provides: "Async transport shell: AsyncClient class preserved; mirror sync surface con await"
      contains: "_raise_for_response"
    - path: "packages/iol-client/tests/test_core.py"
      provides: "Unit tests for iol _core: builders/parsers + auth-flow + CR-01 conditional refresh rotation"
      min_lines: 60
  key_links:
    - from: "iol_client.client.login"
      to: "iol_client._core.build_login_request + parse_login_response"
      via: "transport shell orchestrates HTTP between builder and parser"
      pattern: "_core\\.build_login_request|_core\\.parse_login_response"
    - from: "iol_client.client._refresh"
      to: "iol_client._core.build_refresh_request + parse_refresh_response"
      via: "transport shell orchestrates"
      pattern: "_core\\.build_refresh_request|_core\\.parse_refresh_response"
    - from: "iol_client.aio._raise_for_response"
      to: "iol_client._core.raise_for_response"
      via: "module-level alias D-04"
      pattern: "_raise_for_response = _core.raise_for_response|from iol_client\\._core import raise_for_response as _raise_for_response"
---

<objective>
Extraer `iol_client/_core.py` con auth-flow primitives (login + refresh-with-password-fallback) + per-endpoint builders/parsers + `raise_for_response`; colapsar `client.py` y `aio.py` a transport shells preservando CR-01 (conditional refresh_token rotation: only update state.refresh_token if response provides truthy value); preservar B8 alias D-04; PEP 562 shim Phase 6 intacto. iol valida el patrón auth-flow más complejo del milestone (OAuth password-grant + refresh-token chain).

Purpose: Phase 7 REFAC-03 success criterion #1 ("auth-flow helpers en `_core.py`") aplicado a iol — el paquete con la lógica auth más rica. D-02 Pattern 5 (auth-flow factorizado) + D-04 alias + D-03 `_request` retorna Response. iol corre en Wave 2 paralelo a Plans 4 (higyrus) y 5 (matriz) porque su file ownership es exclusivo.

Output: 1 nuevo `_core.py` iol; 2 archivos colapsados (client/aio); 1 nuevo `tests/test_core.py`. Single commit atómico per D-05/D-13.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md
@.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-01-SUMMARY.md
@CLAUDE.md
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/_state.py
@packages/iol-client/src/iol_client/__init__.py
@packages/iol-client/src/iol_client/exceptions.py
@packages/iol-client/tests/conftest.py
@packages/iol-client/tests/test_client_class.py
@packages/iol-client/tests/test_fixture_reaches_production.py
@verification/snapshots/iol_client-surface.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build `iol_client/_core.py` — RequestSpec (iol-shape with `data`) + auth-flow primitives + per-endpoint builders/parsers</name>
  <files>packages/iol-client/src/iol_client/_core.py, packages/iol-client/tests/test_core.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/client.py (líneas 1-50 module shape + 71-82 `_raise_for_response` (MOVE) + 189-220 `login()` source — PATTERNS.md §7 + 222-251 `_refresh()` source — CR-01 conditional rotation pattern + 254-288 `_ensure_token()` (sigue en shell) + 290-330 `_request()` (D-03 retorna Response) + endpoint methods `get_quote`/`get_instruments`/etc.)
    - packages/iol-client/src/iol_client/aio.py (líneas 1-60 imports + 56-end — AsyncClient con async lock pattern; auth-flow + endpoints)
    - packages/iol-client/src/iol_client/_state.py (Phase 6 — analog módulo shape; `_ClientState` con `username`, `password`, `token`, `token_expires_at`, `refresh_token`, `http_client`; constants `_TOKEN_TTL_BUFFER_SECONDS`, `DEFAULT_BASE_URL`)
    - packages/iol-client/src/iol_client/exceptions.py (jerarquía `IOLClientError → IOLAuthError`)
    - packages/iol-client/tests/test_client_class.py (B8 test ya existe — `test_aio_imports_raise_for_response_from_client` línea ~275; tests de CR-01 conditional refresh rotation — buscar `grep -n "refresh_token" packages/iol-client/tests/test_client_class.py`)
    - packages/iol-client/tests/conftest.py (autouse fixture pattern con `configure(token=...)`)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 1 RequestSpec iol-row con `data` field; Pattern 5 auth-flow `build_login_request` + `parse_login_response` + `build_refresh_request` + `parse_refresh_response` — la cita Code Examples shape verbatim; CR-01 honesty flag refresh rotation)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§7 auth-flow factoring iol example; §1 module shape; §2 iol RequestSpec con `data` para OAuth form-encoded; §10 tests/test_core.py shape iol — incluye CR-01 conditional test)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-01 RequestSpec per-package; D-02 auth-flow factorizado; D-04 alias; D-13 forward-decl `idempotent` discretional)
    - CLAUDE.md (`from __future__ import annotations`; no relative imports; double quotes; line-length 100; module docstring obligatorio)
  </read_first>
  <behavior>
    - `_core.py` define `@dataclass(frozen=True, slots=True) RequestSpec` con fields iol: `method: str`, `path: str`, `params: dict[str, Any] | None = None`, `headers: dict[str, str] | None = None`, `json_body: dict[str, Any] | None = None`, `data: dict[str, Any] | None = None` (`data` para OAuth form-encoded `POST /token` per CONTEXT D-01 iol row + PATTERNS §2 iol row).
    - `raise_for_response(resp: httpx.Response) -> None`: lógica actual de `client.py:71-82` MOVE verbatim (mapping HTTP errors a `IOLAuthError`/`IOLAPIError`/etc.).
    - `build_login_request(state: _ClientState) -> RequestSpec`:
      - Raise `IOLAuthError(0, "IOL_USER y IOL_PASSWORD son requeridos")` si `state.username` o `state.password` vacíos (preservar mensaje exacto Phase 6 — RESEARCH §Code Example 5 paso 1 + PATTERNS §7).
      - Retornar `RequestSpec(method="POST", path="/token", data={"username": ..., "password": ..., "grant_type": "password"}, headers={"Content-Type": "application/x-www-form-urlencoded"})`.
    - `parse_login_response(resp: httpx.Response) -> tuple[str, float, str | None]`:
      - `resp.read()` PRIMERO (D-06 pattern).
      - `raise_for_response(resp)`.
      - `data: dict[str, Any] = resp.json()`.
      - Extract `access_token` (raise `IOLAuthError(resp.status_code, "No access_token in response")` si falta o non-str).
      - Extract `expires_in` (default 900).
      - Extract `refresh_token`; CR-01 condicional: retornar `None` si vacío/no-str/missing — el transport shell solo escribe `state.refresh_token` si retorna non-None.
      - Calcular `expires_at = time.time() + expires_in - _TOKEN_TTL_BUFFER_SECONDS`.
      - Retornar tupla `(access_token, expires_at, refresh_token | None)`.
    - `build_refresh_request(state: _ClientState) -> RequestSpec`:
      - Raise `IOLAuthError(0, "No refresh_token cached")` si `state.refresh_token` falsy.
      - Retornar `RequestSpec(method="POST", path="/token", data={"refresh_token": ..., "grant_type": "refresh_token"}, headers={"Content-Type": "application/x-www-form-urlencoded"})`.
    - `parse_refresh_response(resp) -> tuple[str, float, str | None]`: misma shape que `parse_login_response`; PATTERNS §7 sugiere alias literal `parse_refresh_response = parse_login_response` (el server response shape es idéntico — verificar contra Phase 6 client.py `_refresh()`). Si la implementación actual difiere (e.g., diferentes campos), implementar separado fiel al actual.
    - `token_is_fresh(state: _ClientState) -> bool`: helper pure `return bool(state.token and time.time() < state.token_expires_at)`. Transport shell `_ensure_token()` usa esto.
    - Por cada endpoint público del paquete iol (inspeccionar via `grep -nE "^    def (get_|configure)" packages/iol-client/src/iol_client/client.py`): builder + parser pareja.
      - e.g. `build_get_quote_request(state, simbolo, *, mercado="bcba", plazo="t2") -> RequestSpec` retorna `RequestSpec(method="GET", path=f"/api/v2/{mercado}/Titulos/{simbolo}/Cotizacion", params={...})`.
      - `parse_get_quote_response(resp) -> dict[str, Any]`: `resp.read()` + `raise_for_response(resp)` + `return resp.json()`.
    - `_core.py` NO importa `iol_client.client` ni `iol_client.aio`. IMPORTA `iol_client._state._ClientState`, `iol_client._state._TOKEN_TTL_BUFFER_SECONDS`, `iol_client.exceptions.IOLAuthError`/etc.
    - `tests/test_core.py`: ≥6 tests:
      1. `test_build_login_request_raises_on_missing_credentials` — state vacío → IOLAuthError mensaje exacto.
      2. `test_build_login_request_returns_form_encoded_spec` — verifica `spec.data` shape + `Content-Type` header.
      3. `test_parse_login_response_extracts_token_and_expires` — synthetic Response con JSON `{"access_token": "T", "expires_in": 900, "refresh_token": "R"}` → tupla.
      4. `test_parse_login_response_refresh_token_none_when_missing` — JSON sin `refresh_token` → tupla retorna None en slot 3 (CR-01 prevention).
      5. `test_parse_login_response_raises_on_missing_access_token` — JSON sin `access_token` → IOLAuthError.
      6. `test_build_refresh_request_raises_without_cached_refresh` — state.refresh_token vacío → IOLAuthError.
      7. (opt) `test_build_get_quote_request_returns_correct_path` — verificación del path interpolation.
  </behavior>
  <action>
    1. Reemplazar el placeholder Plan 1 de `iol_client/_core.py` por el módulo completo siguiendo §1 + §7 PATTERNS:
       - Header docstring multi-linea: "Pure builders and parsers for the iol-client REST client. Phase 7 REFAC-03 + D-01 + D-02 (auth-flow factorized) + D-04 + D-06 (body-consume-then-raise)." + "All functions pure: state in → RequestSpec / typed result. No I/O. No imports from `iol_client.client` or `iol_client.aio` (enforced by import-linter)." + `Example` section con `::` code block mostrando `build_login_request` + `parse_login_response` usage + reference to CR-01 conditional refresh rotation.
       - `from __future__ import annotations`.
       - Imports: `time`, `dataclasses (dataclass)`, `typing.Any`, `httpx`; internos: `from iol_client._state import _ClientState, _TOKEN_TTL_BUFFER_SECONDS`, `from iol_client.exceptions import IOLAuthError` (+ otros subclass usados).

    2. Section dividers (§1 PATTERNS):
       - `# --- RequestSpec ---`
       - `# --- Stateless helpers (D-04 — moved from client.py; alias preserved there) ---`
       - `# --- Auth flow primitives (D-02) ---`
       - `# --- Endpoint builders ---`
       - `# --- Endpoint parsers ---`

    3. `__all__`: lista con todos los nombres públicos del módulo.

    4. Implementar `RequestSpec` con field `data` opcional (la única particularidad iol vs ámbito).

    5. Implementar `raise_for_response`, `token_is_fresh`, `build_login_request`, `parse_login_response`, `build_refresh_request`, `parse_refresh_response` siguiendo el shape de RESEARCH.md Code Example Pattern 5 + Code Example 5 (iol).

    6. Implementar builders/parsers para cada endpoint público del paquete. El executor identifica el set con `grep -nE "^    def (get_|fetch_)" packages/iol-client/src/iol_client/client.py` (endpoint methods sync).

    7. Crear `packages/iol-client/tests/test_core.py` con ≥6 tests cubriendo los casos en `<behavior>`. Cada test usa `_ClientState(...)` directo (bypass autouse conftest); `httpx.Response(200, content=b"...")` synthetic para los parsers.

    8. NO modificar aún `client.py` ni `aio.py` (eso es Task 2).
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/tests/test_core.py -x -v &amp;&amp; uv run mypy packages/iol-client/src/iol_client/_core.py packages/iol-client/tests/test_core.py &amp;&amp; uv run ruff check packages/iol-client/src/iol_client/_core.py packages/iol-client/tests/test_core.py &amp;&amp; uv run ruff format --check packages/iol-client/src/iol_client/_core.py packages/iol-client/tests/test_core.py &amp;&amp; uv run lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'from __future__ import annotations' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'class RequestSpec' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'data: dict\[str, Any\] | None' packages/iol-client/src/iol_client/_core.py` &gt;= 1 (iol-specific `data` field for OAuth form)
    - Source: `grep -c 'def build_login_request' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def parse_login_response' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def build_refresh_request' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def parse_refresh_response\|parse_refresh_response = parse_login_response' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def raise_for_response' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def token_is_fresh' packages/iol-client/src/iol_client/_core.py` == 1
    - Source: `grep -c 'def build_' packages/iol-client/src/iol_client/_core.py` &gt;= 5 (login + refresh + at least 3 endpoint builders)
    - Source: `grep -c 'def parse_' packages/iol-client/src/iol_client/_core.py` &gt;= 4
    - Source: `grep -c 'resp.read()' packages/iol-client/src/iol_client/_core.py` &gt;= 2 (D-06 in parse_login_response + parse_refresh_response at minimum)
    - Source: `grep -E 'from iol_client\.(client|aio)' packages/iol-client/src/iol_client/_core.py | grep -v '^#'` returns NOTHING
    - Source: `wc -l packages/iol-client/src/iol_client/_core.py | awk '{print $1}'` is ≥ 100 (min_lines)
    - Test command: `uv run pytest packages/iol-client/tests/test_core.py -x` exits 0 with ≥6 tests passing — including CR-01 conditional refresh tests
    - CLI: `uv run mypy packages/iol-client/src/iol_client/_core.py packages/iol-client/tests/test_core.py` exits 0
    - CLI: `uv run ruff check packages/iol-client/src/iol_client/_core.py packages/iol-client/tests/test_core.py` exits 0
    - CLI: `uv run lint-imports` exits 0 — iol _core contract still passes
  </acceptance_criteria>
  <done>
    `iol_client/_core.py` completo con auth-flow primitives + endpoint builders/parsers; ≥6 unit tests verde; lint-imports verde.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse `iol_client/client.py` + `aio.py` to transport shells (D-04 alias; CR-01 conditional rotation preserved; LOC drop ≥30%)</name>
  <files>packages/iol-client/src/iol_client/client.py, packages/iol-client/src/iol_client/aio.py</files>
  <read_first>
    - packages/iol-client/src/iol_client/client.py (current Client class skeleton + login + _refresh + _ensure_token + _request + endpoint methods + PEP 562 shim al final — TODO: collapse endpoint bodies + auth-flow body + _request signature; PEP 562 shim NO TOCAR)
    - packages/iol-client/src/iol_client/aio.py (current AsyncClient + async lock + same collapse async)
    - packages/iol-client/src/iol_client/_core.py (creado en Task 1 — usar)
    - packages/iol-client/tests/test_client_class.py (B8 test línea ~275 `test_aio_imports_raise_for_response_from_client` — VERIFICAR que sigue verde; tests CR-01 — `grep -n "refresh_token" packages/iol-client/tests/test_client_class.py` para identificar y NO romper)
    - packages/iol-client/tests/test_fixture_reaches_production.py (Phase 6 sync sentinel guard — debe seguir verde; el SYNC-sentinel-iol llega al header Authorization)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-PATTERNS.md (§4 D-04 alias preservation iol + honesty flag mypy strict `as ALIAS`; §5 transport shell sync iol; §6 3-liner endpoint method iol `get_quote`; §7 iol login + _refresh shell consumer)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-RESEARCH.md (§ Architecture Patterns Pattern 3 transport shell + Pattern 7 auth-flow consumer iol example; § Anti-Patterns Avoid — re-coupling sync/async)
    - .planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-CONTEXT.md (D-03 `_request` returns `httpx.Response`; D-05 ≤30-50 LOC por endpoint group; CR-01 conditional refresh rotation preserved)
    - verification/snapshots/iol_client-surface.txt (D-16 — public surface NO debe cambiar)
    - CLAUDE.md (no wildcard imports; dual sync/async mirroring; PEP 562 shim Phase 6 NO TOCAR)
  </read_first>
  <behavior>
    - `client.py` post-refactor:
      - Imports: agregar `from iol_client import _core` + `from iol_client._core import RequestSpec` (no relative — TID rule).
      - REEMPLAZAR la función `_raise_for_response(...)` actual (líneas ~71-82) por la asignación module-level `_raise_for_response = _core.raise_for_response  # D-04: preserves B8 identity (aio._raise_for_response is client._raise_for_response). Both aliases reference _core.raise_for_response.`.
      - Class `Client`:
        - REEMPLAZAR `login()` por: `spec = _core.build_login_request(self._state); http = self._ensure_http_client(); resp = http.request(spec.method, f"{self._state.base_url}{spec.path}", data=spec.data, headers=spec.headers); token, expires_at, refresh = _core.parse_login_response(resp); self._state.token = token; self._state.token_expires_at = expires_at; if refresh is not None: self._state.refresh_token = refresh; return token`. (CR-01 condicional preservada via `if refresh is not None`.)
        - REEMPLAZAR `_refresh()` por: `spec = _core.build_refresh_request(self._state); http = self._ensure_http_client(); resp = http.request(spec.method, f"{self._state.base_url}{spec.path}", data=spec.data, headers=spec.headers); token, expires_at, refresh = _core.parse_refresh_response(resp); self._state.token = token; self._state.token_expires_at = expires_at; if refresh is not None: self._state.refresh_token = refresh; return token`. La policy actual (fallback a `login()` si refresh falla — verificar en `client.py:222-251` el comportamiento) preservar literalmente: try/except con `except IOLAuthError: self.login()`.
        - `_ensure_token()`: el shell mantiene la decisión `if _core.token_is_fresh(self._state): return; if self._state.refresh_token: try: self._refresh() except IOLAuthError: self.login() else: self.login()` — preservar exact behavior actual.
        - REEMPLAZAR `_request(self, method, path, ...)` por `def _request(self, spec: RequestSpec) -> httpx.Response:` con body: `self._ensure_token(); assert self._state.token is not None; http = self._ensure_http_client(); url = f"{self._state.base_url}{spec.path}"; headers = {"Authorization": f"Bearer {self._state.token}", **(spec.headers or {})}; return http.request(spec.method, url, params=spec.params, json=spec.json_body, headers=headers)`.
        - Por cada endpoint method (`get_quote`, `get_instruments`, etc.): REEMPLAZAR body por `spec = _core.build_<endpoint>_request(self._state, ...); resp = self._request(spec); return _core.parse_<endpoint>_response(resp)`.
      - PEP 562 shim al final: NO TOCAR. La deny-list de `_user`/`_password` (Phase 6 D-Q) sigue activa.
      - `__all__` IDÉNTICO Phase 6 — `_core` NO entra.
    - `aio.py` post-refactor:
      - CAMBIAR import B8 actual de `from iol_client.client import _raise_for_response as ...` por `from iol_client._core import raise_for_response as _raise_for_response` (mypy strict `implicit_reexport=False` satisfied).
      - Agregar `from iol_client import _core` + `from iol_client._core import RequestSpec`.
      - Class `AsyncClient`:
        - REEMPLAZAR `login()` y `_refresh()` async por mirror de sync con `await` y respetando el async lock pattern actual.
        - REEMPLAZAR `_request` async por mirror del sync con `await http.request(...)`.
        - REEMPLAZAR endpoint methods por `await` mirror del 3-liner sync.
      - PEP 562 shim NO TOCAR.
    - Identidad B8: `aio._raise_for_response is client._raise_for_response` == True post-refactor.
    - LOC: agregado client+aio ≤ 998 * 0.70 = 699 LOC (≥30% drop). El `_core.py` no entra al conteo.
    - Public surface snapshot zero diff.
    - Cross-leak guard verde — `SYNC-sentinel-iol_client` llega solo al wire sync; `ASYNC-sentinel-iol_client` llega solo al wire async.
  </behavior>
  <action>
    1. Identificar todos los endpoint methods públicos en `client.py` y `aio.py` actuales (`grep -nE '^    (async )?def (get_|fetch_|configure|login|_refresh|_ensure_token|_request)' packages/iol-client/src/iol_client/{client,aio}.py`).

    2. Refactor de `client.py`:
       - Top: agregar `from iol_client import _core` + `from iol_client._core import RequestSpec` (después de los imports de Phase 6).
       - REEMPLAZAR `_raise_for_response(...)` por `_raise_for_response = _core.raise_for_response  # D-04` (con comentario).
       - `Client.login()`: body 3-liner según `<behavior>`.
       - `Client._refresh()`: body 3-liner según `<behavior>` + try/except IOLAuthError preservar fallback a login().
       - `Client._ensure_token()`: usar `_core.token_is_fresh(self._state)`; resto de la lógica (refresh → login fallback) preservar.
       - `Client._request(spec)`: body según `<behavior>`.
       - Endpoint methods: 3-liner body.
       - PEP 562 shim: NO TOCAR.

    3. Refactor de `aio.py`: mirror sync con `await`. Conservar async locks/`_ensure_token_lock()` actuales.

    4. Validar identidad B8 con repl:
       ```
       uv run python -c "from iol_client.aio import _raise_for_response as a; from iol_client.client import _raise_for_response as c; assert a is c, 'B8 identity broken'"
       ```

    5. Validar LOC drop:
       ```
       wc -l packages/iol-client/src/iol_client/{client,aio}.py
       ```
       Confirmar sum ≤ 699.

    6. Validar D-16 snapshot zero diff:
       ```
       uv run pytest verification/test_public_surface.py -k iol -x
       ```

    7. Validar tests existentes — B8 + CR-01 + sync sentinel fixture-reaches-production todos verde:
       ```
       uv run pytest packages/iol-client/ -x -v
       ```

    8. Validar lint-imports verde:
       ```
       uv run lint-imports
       ```

    9. Validar cross-leak guard:
       ```
       uv run pytest verification/test_sync_async_isolation.py -k iol -x -v
       ```
  </action>
  <verify>
    <automated>uv run pytest packages/iol-client/ -x &amp;&amp; uv run pytest verification/test_sync_async_isolation.py -k iol -x -v &amp;&amp; uv run pytest verification/test_public_surface.py -k iol -x &amp;&amp; uv run python -c "from iol_client.aio import _raise_for_response as a; from iol_client.client import _raise_for_response as c; assert a is c" &amp;&amp; uv run mypy packages/iol-client/ &amp;&amp; uv run ruff check packages/iol-client/ &amp;&amp; uv run ruff format --check packages/iol-client/ &amp;&amp; uv run lint-imports &amp;&amp; uv run pytest -q 2&gt;&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv run python -c "from iol_client.aio import _raise_for_response as a; from iol_client.client import _raise_for_response as c; assert a is c"` exits 0 (B8 D-04 identity)
    - Source: `grep -c '_raise_for_response = _core.raise_for_response' packages/iol-client/src/iol_client/client.py` == 1 (alias literal D-04)
    - Source: `grep -cE 'from iol_client._core import raise_for_response as _raise_for_response' packages/iol-client/src/iol_client/aio.py` == 1 (mypy strict `as ALIAS` re-export)
    - Source: `grep -cE 'def _request\(self,\s*spec' packages/iol-client/src/iol_client/client.py` == 1 (D-03 signature)
    - Source: `grep -cE 'async def _request\(self,\s*spec' packages/iol-client/src/iol_client/aio.py` == 1
    - Source: `grep -c '_core.build_login_request' packages/iol-client/src/iol_client/client.py` == 1 (login orchestrates via _core)
    - Source: `grep -c '_core.parse_login_response' packages/iol-client/src/iol_client/client.py` == 1
    - Source: `grep -c '_core.build_refresh_request' packages/iol-client/src/iol_client/client.py` == 1
    - Source: `grep -c 'if refresh is not None' packages/iol-client/src/iol_client/client.py` &gt;= 2 (CR-01 conditional rotation preserved in login + _refresh)
    - Source: `grep -c 'await' packages/iol-client/src/iol_client/aio.py` &gt;= 1
    - Source: aggregated `wc -l packages/iol-client/src/iol_client/{client,aio}.py` returns sum ≤ 699 (≥30% drop vs 998 baseline)
    - Test command: `uv run pytest packages/iol-client/ -x` exits 0 — including B8 test_aio_imports_raise_for_response_from_client + CR-01 conditional refresh tests + Phase 6 fixture-reaches-production
    - Test command: `uv run pytest verification/test_sync_async_isolation.py -k iol -x -v` exits 0 (sync iol + async iol)
    - Test command: `uv run pytest verification/test_public_surface.py -k iol -x` exits 0 — D-16 snapshot zero diff
    - Test command: `uv run pytest -q 2&gt;&amp;1 | tail -3` shows no failures; total ≥ previous baseline
    - CLI: `uv run lint-imports` exits 0 — `iol_client._core` boundary respected
    - CLI: `uv run mypy packages/iol-client/` exits 0 (strict)
    - CLI: `uv run ruff check packages/iol-client/` exits 0
    - CLI: `uv run ruff format --check packages/iol-client/` exits 0
  </acceptance_criteria>
  <done>
    `client.py` y `aio.py` colapsados; B8 identidad verde; CR-01 conditional refresh rotation preservada; LOC drop ≥30%; public surface snapshot zero diff; cross-leak guard verde; full suite verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `_core.py` → `client.py`/`aio.py` | T-7-01 — import-linter enforça |
| sync surface → async surface (token isolation) | T-7-02 — `verification/test_sync_async_isolation.py` cubre |
| Auth credentials (`_user`/`_password`) → PEP 562 shim | T-7-AUTH-LEAK — Phase 6 D-Q deny-list preserva: shim NO forward `_user`/`_password` |
| `refresh_token` rotation (CR-01) | T-7-04 — semantic drift si plan accidentally rewrites refresh_token siempre (not condicional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | `_core.py` accidental import of transport state | mitigate | Task 1 source assertion `grep "from iol_client\.(client\|aio)"` empty + Task 2 `uv run lint-imports` exit 0 |
| T-7-02 | Tampering | Sync/async token cross-contamination | mitigate | Task 2 verify includes `verification/test_sync_async_isolation.py -k iol` — sync sentinel SYNC-sentinel-iol_client reaches sync Authorization header; ASYNC-sentinel-iol_client reaches async Authorization header |
| T-7-03 | Tampering | B8 alias break (Pitfall 2) | mitigate | Task 2 verify includes B8 repl assert + `test_aio_imports_raise_for_response_from_client` (`packages/iol-client/tests/test_client_class.py:275`) sigue verde |
| T-7-04 | Tampering (auth-flow semantic drift) | CR-01 conditional refresh_token rotation accidentally rewritten as unconditional | mitigate | Task 1 `tests/test_core.py` incluye `test_parse_login_response_refresh_token_none_when_missing`; Task 2 source assertion `grep "if refresh is not None"` &gt;= 2 |
| T-7-AUTH-LEAK | Information Disclosure | PEP 562 shim accidentally forwards `_user`/`_password` | mitigate | Phase 7 NO TOCA PEP 562 shim; existing Phase 6 D-Q deny-list test sigue verde como parte de `uv run pytest packages/iol-client/` |
</threat_model>

<verification>
- B8 identity: `aio._raise_for_response is client._raise_for_response` == True.
- `uv run lint-imports` exit 0 — iol `_core` boundary clean.
- Public surface snapshot zero diff (D-16).
- Cross-leak guard verde — iol sync/async sentinels aislados.
- LOC drop ≥30% (≤699 LOC client+aio agregado vs 998 baseline).
- CR-01 conditional refresh_token rotation preserved (tests verde + source assertion).
- Phase 6 PEP 562 shim deny-list `_user`/`_password` sigue verde.
- mypy strict + ruff check + ruff format pasan.
- Full suite verde.
</verification>

<success_criteria>
- `iol_client/_core.py` con RequestSpec (con `data`), auth-flow primitives, builders/parsers per endpoint, raise_for_response; PURO.
- `client.py` y `aio.py` colapsados; LOC drop ≥30% agregado vs Phase 6 baseline 998.
- B8 alias D-04 preservado; tests verde.
- CR-01 conditional refresh_token rotation preservada en login + _refresh; tests dedicados verde.
- Public surface snapshot Phase 6 unchanged.
- Cross-leak guard verde.
- lint-imports verde con 4 contracts; PEP 562 shim Phase 6 intacto.
- ≥6 nuevos tests de `_core` agregados.
</success_criteria>

<output>
Crear `.planning/phases/07-core-py-extraction-sync-async-logic-dedup/07-03-SUMMARY.md` documentando:
- LOC drop:
  ```
  LOC drop vs Phase 6 baseline:
  - client.py: 522 → <X> (-<Y>%)
  - aio.py:    476 → <X> (-<Y>%)
  - _core.py:  0 → <N> (NEW)
  - Aggregate client+aio: 998 → <X> (-<Y>%)   <PASS/FAIL ≥30%>
  ```
- B8 identity confirmed.
- CR-01 conditional refresh: tests pasan + source grep count.
- Public surface snapshot: zero diff.
- Cross-leak guard verde.
- N nuevos tests `_core` agregados.
</output>
