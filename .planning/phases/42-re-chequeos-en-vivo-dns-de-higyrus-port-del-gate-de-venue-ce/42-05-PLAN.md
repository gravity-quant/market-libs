---
phase: 42
plan: 05
type: execute
wave: 3
depends_on: ["42-03"]
files_modified:
  - main_higyrus.py
  - main_matriz.py
  - verification/test_main_higyrus_skip_line_shape.py
  - verification/test_main_verify_classification.py
  - verification/test_run_evidence.py
  - verification/test_cycle_closure_phase33.py
  - verification/test_main_higyrus_deep_chain.py
  - .planning/verification/run-evidence/higyrus-client.json
autonomous: true
requirements: [LIVE-01]

must_haves:
  truths:
    - "Si el veredicto del plan 42-03 fue SKIPPED, el destino nombrado del bloqueo de higyrus queda renombrado a LIVE-HIGY-42 en todos los sitios VIVOS (criterio 2)"
    - "La historia congelada de v1.6 no se toca: el guard que asevera contra 33-CENSUS.md sigue verde"
    - "El sobre de evidencia lleva el destino nuevo porque una corrida real lo reescribió, no porque alguien editó el JSON"
    - "Las reglas de forma de las constantes de veredicto se preservan: literal de módulo, sin interpolación, prefijo con dos puntos load-bearing, destino al final"
  artifacts:
    - path: "main_higyrus.py"
      provides: "Constantes de veredicto con el destino renombrado, sin interpolación y con el prefijo clasificable intacto"
      contains: "_VENDOR_UNREACHABLE_SKIP_LINE"
    - path: ".planning/verification/run-evidence/higyrus-client.json"
      provides: "Sobre regenerado por una corrida real, con el destino nuevo y captured_at fresco"
      contains: "captured_at"
  key_links:
    - from: "main_matriz.py"
      to: "main_higyrus.py"
      via: "_CYCLE_CLOSURE_DESTINATION['higyrus-client'] tiene que nombrar el mismo destino que _VENDOR_UNREACHABLE_EVIDENCE, o _cycle_closure_verdict concatenaría el destino dos veces"
      pattern: "_CYCLE_CLOSURE_DESTINATION"
    - from: "main_higyrus.py"
      to: ".planning/verification/run-evidence/higyrus-client.json"
      via: "write_run_evidence(skipped=_VENDOR_UNREACHABLE_EVIDENCE) reescribe el sobre entero en la corrida de la Task 2"
      pattern: "_VENDOR_UNREACHABLE_EVIDENCE"
---

<!-- planner-discipline-allow: LIVE-HIGY-33 -->

<objective>
Ejecutar el rename D-06 —el destino nombrado del bloqueo de higyrus pasa de llevar la fase de origen
`33` a llevar `42`— en los **sitios vivos** y sólo ahí, y regenerar el sobre de evidencia con una
corrida real para que el destino nuevo llegue al JSON committeado sin que nadie lo edite a mano.

Purpose: el criterio 2 del ROADMAP lo exige literalmente ("con el destino `LIVE-HIGY-33`
renombrado") y la convención de nombres embebe la fase de origen (`LIVE-MATZ-33` sigue el mismo
patrón). El plan 42-03 mide el veredicto; **este plan actúa sobre esa medición**.

Riesgo de mayor consecuencia del rename, medido por `42-RESEARCH.md § Runtime State Inventory`:
CONTEXT.md D-06 estima "~8 archivos de test"; el conteo real es **11 ocurrencias en 6 archivos
vivos**, y **una de ellas NO debe renombrarse** porque asevera sobre un artefacto histórico
congelado de v1.6. Renombrarla rompe el guard y viola la premisa entera de la Phase 41.

Output: el rename aplicado a los 7 archivos vivos enumerados abajo, el sobre regenerado por corrida,
y los 4 gates de CI verdes.
</objective>

## Condicionalidad: este plan sólo dispara si el veredicto fue SKIPPED

Leer el veredicto explícito que `42-03-SUMMARY.md` registra al final ("¿el rename D-06 dispara?").

- **Veredicto `SKIPPED` (camino esperado; el DNS medido en research no resuelve):** ejecutar las dos
  tasks tal como están escritas.
- **Veredicto `RAN` (el vendor resolvió y el driver corrió sus probes):** el rename es **moot** — la
  rama `_vendor_unreachable` nunca disparó y el bloqueo cuya fase de origen se renombraría acaba de
  dejar de estar vigente. En ese caso: NO renombrar nada, dejar las dos tasks como no-op explícito, y
  registrar en el SUMMARY (a) que D-06 no aplicó, (b) por qué, y (c) que el ítem de backlog conserva
  su nombre y su estado. El plan cierra igual, con `git status --porcelain -- '*.py'` vacío.
- **Veredicto inesperado (`FINDING`/`FAILED` con otra clase de excepción):** NO renombrar. Escalar,
  registrar el resultado medido y dejar el rename para una disposición posterior. Renombrar sobre un
  veredicto que no es el que la convención describe sería estampar un nombre sobre evidencia que no
  lo sostiene.

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CONTEXT.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-PATTERNS.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-03-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos y archivos creados por la Phase 42 (no existen en HEAD). Excluir de cualquier verificación
de drift: son salida, no deriva.

| Artefacto | Tipo | Creado en |
|-----------|------|-----------|
| `verification/test_literal_census_venue_gate.py` (11 tests + `_census_matriz_node`, `_membership_offenders`, `_TARGET`) | archivo nuevo | 42-01 |
| `_census_header_lines()`, `_census_header()`, local `venue`, flag `--matriz-only`, prefijos `CENSUS-HEADER`/`CENSUS-DLOCK` | símbolos nuevos en `scripts/literal_census_33.py` | 42-01 |
| entrada `verification/test_literal_census_venue_gate.py` en `ci.yml` | línea de allowlist | 42-01 |
| `.planning/phases/42-…/42-CENSUS.md` | artefacto committeado | 42-02 |
| `capture` importado en `main_market_data.py`; labels `wire-instruments-42` / `wire-segments-42`; capturas gitignored | símbolos y artefactos nuevos | 42-04 |
| `.planning/phases/42-…/42-WIRE-READ.md` | artefacto committeado | 42-04 |
| **`LIVE-HIGY-42`** | **identificador de destino nuevo — lo crea ESTE plan** | 42-05 |

<tasks>

<task type="auto">
  <name>Task 1: Rename atómico del destino en los 7 archivos VIVOS (D-06)</name>
  <files>main_higyrus.py, main_matriz.py, verification/test_main_higyrus_skip_line_shape.py, verification/test_main_verify_classification.py, verification/test_run_evidence.py, verification/test_cycle_closure_phase33.py, verification/test_main_higyrus_deep_chain.py</files>
  <read_first>
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-03-SUMMARY.md`
      — el veredicto medido. **Si no es `SKIPPED`, esta task es un no-op explícito** (ver
      § Condicionalidad).
    - `main_higyrus.py:239-254` — el bloque de las tres constantes con sus comentarios. Es a la vez
      el sitio y el patrón: `_VENDOR_UNREACHABLE_SKIP_LINE` (`:244-246`),
      `_VENDOR_UNREACHABLE_DETAIL` (`:249`, **sin** destino → no se renombra) y
      `_VENDOR_UNREACHABLE_EVIDENCE` (`:254`).
    - `main_higyrus.py:1886` y `:2052` — dos comentarios en prosa que citan el bloqueo **vigente**.
    - `main_matriz.py:158-171` — `_CYCLE_CLOSURE_DESTINATION` (`:162-165`) y
      `_cycle_closure_destination`.
    - `main_matriz.py:206-218` — la rama `probes <= 0` de `_cycle_closure_verdict`: concatena el
      destino sólo si no está ya en la causa (`detail = cause if destination in cause else …`). Por
      eso el destino del mapa de `main_matriz.py` y el de `_VENDOR_UNREACHABLE_EVIDENCE` tienen que
      quedar **iguales**: si divergen, el detalle lo lleva dos veces y el pin `detail.count(...) == 1`
      se pone rojo.
    - `verification/test_cycle_closure_phase33.py:243-260` — ⚠️ **LEER ANTES DE EDITAR ESTE ARCHIVO.**
      Las líneas `250-252` aseveran contra `_CENSUS.read_text()`, donde `_CENSUS` (`:91-98`) apunta a
      `.planning/milestones/v1.6-phases/33-…/33-CENSUS.md`, un artefacto **histórico congelado**.
      **Esas líneas NO se tocan.**
    - `verification/test_cycle_closure_phase33.py:380-395` y `:475-490` — los tres sitios VIVOS de
      este archivo (envelope sintético `:385`, `detail.count(...) == 1` `:392`, y el pin de
      `_cycle_closure_destination` `:482`).
    - `verification/test_run_evidence.py:205-225` — el envelope sintético (`:211`) y los dos asserts
      (`:217`, `:218`).
    - `verification/test_main_higyrus_skip_line_shape.py:165-175` — el assert `line.endswith(...)`
      (`:170`).
    - `verification/test_main_verify_classification.py:75-85` — el assert `line.endswith(...)`
      (`:79`).
    - `verification/test_main_higyrus_deep_chain.py:30-40` — el docstring (`:36`) que afirma que el
      bloqueo heredado "sigue en pie": afirma un **estado presente** que esta fase cambia.
    - `.planning/phases/42-…/42-PATTERNS.md` § `main_higyrus.py` / `main_matriz.py` — las cuatro
      reglas del patrón que el rename no debe romper.
    - `.planning/phases/42-…/42-RESEARCH.md` § "Mapa de sitios … RENOMBRAR vs. NO TOCAR" — el
      inventario exhaustivo.
  </read_first>
  <action>
    Aplicar el rename del identificador de destino de higyrus: la parte numérica pasa de la fase de
    origen `33` a la fase de origen `42`, quedando `LIVE-HIGY-42`. **Un solo commit**, porque el
    rename es atómico por construcción: renombrar las constantes sin renombrar los pins deja la
    suite roja a mitad de camino, y renombrar los pins sin las constantes también. Ésa es la razón
    por la que esta task toca 7 archivos en vez de dividirse.

    **RENOMBRAR — 11 ocurrencias en 6 archivos (todas de estado vivo, forward-looking):**

    | Archivo | Línea | Qué es |
    |---------|-------|--------|
    | `main_higyrus.py` | `245` | valor de `_VENDOR_UNREACHABLE_SKIP_LINE` |
    | `main_higyrus.py` | `254` | valor de `_VENDOR_UNREACHABLE_EVIDENCE` |
    | `main_matriz.py` | `163` | valor de `_CYCLE_CLOSURE_DESTINATION["higyrus-client"]` |
    | `verification/test_main_higyrus_skip_line_shape.py` | `170` | argumento de `line.endswith(...)` |
    | `verification/test_main_verify_classification.py` | `79` | argumento de `line.endswith(...)` |
    | `verification/test_run_evidence.py` | `211`, `217`, `218` | envelope sintético + 2 asserts |
    | `verification/test_cycle_closure_phase33.py` | `385`, `392`, `482` | envelope sintético, `detail.count(...) == 1`, pin de `_cycle_closure_destination` |

    **Más los tres sitios de prosa que describen el presente (A5, decidido: sí se renombran):**
    `main_higyrus.py:1886` y `:2052` (comentarios que citan el bloqueo vigente), y el docstring de
    `verification/test_main_higyrus_deep_chain.py:36`, que afirma que el bloqueo heredado sigue en
    pie — reescribirlo para que refleje el estado que esta corrida midió, nombrando el destino nuevo.

    **NO TOCAR (historia congelada — renombrar acá falsifica historia y rompe el guard):**

    | Archivo | Línea | Por qué |
    |---------|-------|---------|
    | `verification/test_cycle_closure_phase33.py` | `250-252` | Asevera contra `33-CENSUS.md` en `.planning/milestones/v1.6-phases/`, congelado. El string describe lo que era verdad el 2026-08-27 |
    | `.planning/milestones/**` (todo) | — | Árbol congelado de v1.6/v1.7; la Phase 41 auditó contra él |
    | `.planning/PROJECT.md` | `29`, `78`, `278`, `417` | Párrafos históricos de fases completadas |
    | `.planning/STATE.md` | `171`, `241`, `405`, `412`, `541`, `542` | Log de decisiones, inmutable por convención |
    | `packages/higyrus-client/tests/test_deep_chain_edges.py` | `5` | Docstring que cita un plan histórico por nombre |

    Los sitios forward-looking de `.planning/ROADMAP.md`, `.planning/PROJECT.md` y
    `.planning/research/ARCHITECTURE.md` **no** se tocan acá: son el plan 42-06.

    **Cuatro reglas del patrón que el rename NO debe romper** (`42-PATTERNS.md`):
    1. Las dos constantes de `main_higyrus.py` siguen siendo **literales de módulo**, con cero
       interpolación — es lo que garantiza que el veredicto no filtre el hostname (T-39-04).
    2. `_VENDOR_UNREACHABLE_SKIP_LINE` conserva el prefijo con el slug del paquete seguido de dos
       puntos: `main_verify.py` clasifica por `^SKIPPED \S.*:` y los dos puntos son load-bearing.
    3. El destino va al final, después del guion largo, con la misma separación.
    4. `_VENDOR_UNREACHABLE_DETAIL` (`:249`) **no lleva destino y por lo tanto no se renombra**.

    **Usar un rename dirigido sitio por sitio, no un `sed -i` global.** Un reemplazo ciego sobre el
    árbol tocaría las líneas `250-252` de `test_cycle_closure_phase33.py` y todo
    `.planning/milestones/`.
  </action>
  <verify>
    <automated>uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; test "$(grep -c 'LIVE-HIGY-33' main_higyrus.py main_matriz.py | awk -F: '{s+=$2} END {print s}')" = "0" &amp;&amp; test "$(grep -c 'LIVE-HIGY-33' verification/test_cycle_closure_phase33.py)" = "2" &amp;&amp; test -z "$(git status --porcelain -- .planning/milestones/)" &amp;&amp; uv run pytest -q verification/test_main_higyrus_skip_line_shape.py verification/test_main_verify_classification.py verification/test_run_evidence.py verification/test_cycle_closure_phase33.py verification/test_main_higyrus_deep_chain.py</automated>
  </verify>
  <acceptance_criteria>
    - `main_higyrus.py` y `main_matriz.py` no contienen ninguna ocurrencia del identificador viejo:
      la suma de `grep -c` sobre los dos archivos es `0`.
    - `verification/test_cycle_closure_phase33.py` conserva **exactamente 2** ocurrencias del
      identificador viejo, y son las de las líneas `250-252` (la aserción contra la historia
      congelada de `33-CENSUS.md`). Ni más ni menos: `3` significaría un sitio vivo sin renombrar,
      `0` significaría que se rompió el guard de historia congelada.
    - `.planning/milestones/` no tiene ningún cambio: `git status --porcelain -- .planning/milestones/`
      vacío.
    - `packages/higyrus-client/tests/test_deep_chain_edges.py` no cambió.
    - `_VENDOR_UNREACHABLE_DETAIL` (`main_higyrus.py:249`) conserva su valor exacto sin destino.
    - `main_matriz._CYCLE_CLOSURE_DESTINATION["higyrus-client"]` y el sufijo de
      `main_higyrus._VENDOR_UNREACHABLE_EVIDENCE` nombran el **mismo** destino — de lo contrario
      `_cycle_closure_verdict` lo concatenaría dos veces y el pin `detail.count(...) == 1` quedaría
      rojo.
    - `uv run pytest -q` sobre los 5 archivos de pin → `0 failed`.
    - `uv run pytest -q` sobre las 13 rutas de la allowlist de `.github/workflows/ci.yml` →
      `0 failed` y `passed` estrictamente mayor que `129`.
    - `uv run --frozen ruff check .`, `ruff format --check .` y `uv run --frozen mypy` verdes.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
  </acceptance_criteria>
  <done>El destino nombrado del bloqueo de higyrus lleva la fase de origen correcta en todos los
  sitios vivos, la historia congelada quedó intacta, y la suite está verde en un solo commit.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerar el sobre de evidencia corriendo el driver, nunca editando el JSON</name>
  <files>.planning/verification/run-evidence/higyrus-client.json, .planning/verification/higyrus-client-findings.md</files>
  <read_first>
    - `main_higyrus.py:2895-2930` — el corte temprano de `main()`, que imprime la línea SKIPPED (ya
      renombrada por la Task 1) y llama `write_run_evidence(..., skipped=_VENDOR_UNREACHABLE_EVIDENCE)`.
    - `verification/run_evidence.py:97-134` — `write_run_evidence` **reemplaza** el archivo entero y
      estampa `captured_at` UTC fresco.
    - `.planning/verification/run-evidence/higyrus-client.json` — el sobre que dejó el plan 42-03,
      todavía con el destino viejo en su campo `skipped`.
    - `.planning/phases/42-…/42-RESEARCH.md` § Runtime State Inventory, fila "Stored data" — el orden
      de operaciones obligatorio: **renombrar la constante → correr el driver → commitear el JSON
      regenerado**. Editar el JSON a mano fabricaría evidencia.
    - `verification/test_cycle_closure_phase33.py:243-260` — el guard de historia congelada que debe
      seguir verde después de todo esto.
  </read_first>
  <action>
    Volver a correr el driver completo, ahora con la constante renombrada, para que el sobre lleve
    el destino nuevo **porque una corrida real lo escribió**:

    `uv run --package higyrus-client python main_higyrus.py 2>&1 | tee /tmp/42-higyrus-run2.log`

    y registrar el exit code real del driver (no el de `tee`).

    La corrida es barata: falla en la resolución del host antes de cualquier round trip contra el
    vendor, e imprime la línea SKIPPED renombrada. Además vuelve a confirmar la medición del plan
    42-03 — dos mediciones independientes del mismo veredicto en la misma sesión.

    **Prohibido editar a mano `.planning/verification/run-evidence/higyrus-client.json`.** El sobre
    tiene que salir de `write_run_evidence`; un JSON editado a mano es exactamente el modo de
    repudio que la costura de no-vacuidad de la Phase 39 (T-39-12) existe para cerrar.

    Si esta segunda corrida diverge de la primera (p.ej. ahora el host resuelve), NO renombrar de
    vuelta ni forzar el resultado: registrar las dos mediciones y su divergencia en el SUMMARY y
    escalar. Una divergencia dentro de la misma sesión es información, no un problema a esconder.

    Commitear el sobre regenerado y, si el driver apendeó al ledger
    `.planning/verification/higyrus-client-findings.md`, commitearlo tal cual salió.
  </action>
  <verify>
    <automated>test -s /tmp/42-higyrus-run2.log &amp;&amp; grep -q "\"captured_at\": \"$(date -u +%Y-%m-%d)" .planning/verification/run-evidence/higyrus-client.json &amp;&amp; test "$(grep -c 'LIVE-HIGY-33' .planning/verification/run-evidence/higyrus-client.json)" = "0" &amp;&amp; uv run pytest -q verification/test_cycle_closure_phase33.py verification/test_run_evidence.py &amp;&amp; test -z "$(git status --porcelain -- .planning/milestones/)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/verification/run-evidence/higyrus-client.json` tiene `captured_at` con la fecha UTC
      de HOY y su campo `skipped` nombra el destino **nuevo**; `grep -c` del identificador viejo
      sobre ese archivo devuelve `0`.
    - El sobre fue escrito por `write_run_evidence` durante la corrida, no editado a mano: el
      envelope conserva las claves `slug`, `driver`, `captured_at`, `counts`, `probes_executed`,
      `n_triples`, `triples`, `skipped`, y `driver` es `main_higyrus.py`.
    - El SUMMARY registra las **dos** mediciones de la sesión (plan 42-03 y esta re-corrida) con su
      veredicto y exit code, y declara si coincidieron.
    - `uv run pytest -q verification/test_cycle_closure_phase33.py verification/test_run_evidence.py`
      → `0 failed`: el guard de historia congelada sigue verde y los pins del sobre reconocen el
      destino nuevo.
    - `git status --porcelain -- .planning/milestones/` vacío.
    - `uv run pytest -q` sobre las 13 rutas de la allowlist de `.github/workflows/ci.yml` →
      `0 failed` y `passed` estrictamente mayor que `129`.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
  </acceptance_criteria>
  <done>El sobre committeado lleva el destino nuevo porque una corrida real lo escribió, la
  medición quedó doblemente confirmada en la sesión, y la historia congelada sigue intacta.</done>
</task>

</tasks>

<threat_model>
> `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constantes de veredicto → stdout → clasificador de `main_verify.py` | La forma de la línea es load-bearing; romperla cambia el veredicto de la corrida |
| constantes de veredicto → sobre de evidencia committeado | Frontera de redacción: cruza la causa y el destino, nunca el hostname |
| estado vivo (drivers, tests de pin) ↔ historia congelada (`.planning/milestones/`, `33-CENSUS.md`) | Frontera de integridad: el rename cruza sólo en un sentido |
| driver → sobre de evidencia | El sobre sólo puede escribirlo una corrida real; una edición a mano es evidencia fabricada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-17 | Tampering | Historia congelada (`33-CENSUS.md`, `.planning/milestones/`, `STATE.md`, sitios históricos de `PROJECT.md`) | mitigate | Rename dirigido sitio por sitio con inventario enumerado (prohibido `sed -i` global). `<verify>` asevera **exactamente 2** ocurrencias remanentes en `verification/test_cycle_closure_phase33.py` (las de las líneas 250-252) y `git status --porcelain -- .planning/milestones/` vacío |
| T-42-10 | Repudiation | Sobre de evidencia editado a mano para que muestre el destino nuevo | mitigate | Prohibición escrita en el `<action>` de la Task 2 + orden de operaciones obligatorio (renombrar → correr → commitear). `<verify>` asevera `captured_at` de hoy, que sólo la corrida real produce |
| T-42-04 | Information Disclosure | Constantes de veredicto renombradas | mitigate | Regla 1 del patrón: siguen siendo literales de módulo sin interpolación; el rename no introduce ninguna f-string. Pinneado por `verification/test_main_higyrus_skip_line_shape.py` |
| T-42-18 | Denial of Service | Romper el prefijo clasificable (`^SKIPPED \S.*:`) al renombrar | mitigate | Regla 2 del patrón + `verification/test_main_verify_classification.py` verde en el `<verify>` de la Task 1 |
| T-42-19 | Repudiation | Destinos divergentes entre `main_matriz._CYCLE_CLOSURE_DESTINATION` y `_VENDOR_UNREACHABLE_EVIDENCE` | mitigate | `<acceptance_criteria>` exige que nombren el mismo destino; el pin `detail.count(...) == 1` de `test_cycle_closure_phase33.py:392` lo detecta |
| T-42-20 | Tampering | Renombrar sobre un veredicto que no lo sostiene | mitigate | § Condicionalidad: el rename sólo dispara con veredicto `SKIPPED`; con `RAN` o con un resultado inesperado, las dos tasks son no-op explícito y se escala |
| T-42-02 | Tampering | `verification/mutation_gate.py` | mitigate | Byte-identidad verificada en el `<verify>` de las dos tasks (`6bdaec00…`) |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala ningún paquete (`42-RESEARCH.md § Package Legitimacy Audit`: N/A); verificado en el gate de cierre (plan 42-06) |
</threat_model>

<verification>
1. `42-03-SUMMARY.md` declara el veredicto y si el rename dispara.
2. `grep -c` del identificador viejo sobre `main_higyrus.py` + `main_matriz.py` suma `0`.
3. `grep -c` del identificador viejo sobre `verification/test_cycle_closure_phase33.py` es
   **exactamente 2** (las líneas 250-252 de historia congelada).
4. `git status --porcelain -- .planning/milestones/` vacío y
   `packages/higyrus-client/tests/test_deep_chain_edges.py` sin cambios.
5. `.planning/verification/run-evidence/higyrus-client.json` con `captured_at` de hoy, destino nuevo
   y escrito por `write_run_evidence`.
6. `uv run pytest -q` sobre las 13 rutas de la allowlist → `0 failed`, `passed` > 129.
7. `uv run --frozen ruff check .`, `ruff format --check .`, `uv run --frozen mypy` verdes.
8. `git hash-object verification/mutation_gate.py` = `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
</verification>

<success_criteria>
- El destino nombrado del bloqueo de higyrus lleva la fase de origen `42` en los 11 sitios vivos.
- La historia congelada de v1.6/v1.7 quedó intacta y su guard sigue verde.
- El sobre de evidencia committeado lleva el destino nuevo porque una corrida real lo escribió.
- Las cuatro reglas de forma de las constantes de veredicto se preservaron.
- Los 4 gates de CI quedaron verdes en un solo commit atómico.
</success_criteria>

<output>
Create `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-05-SUMMARY.md` when done.

El SUMMARY debe incluir, además del formato estándar:
- Si D-06 disparó o no, y por qué (el veredicto del plan 42-03 que lo decidió).
- El inventario de sitios renombrados con `archivo:línea`, y el inventario de sitios deliberadamente
  NO tocados con su razón.
- El conteo `grep -c` remanente en `verification/test_cycle_closure_phase33.py` (esperado: `2`).
- Las dos mediciones del veredicto de la sesión y si coincidieron.
- El `captured_at` y el campo `skipped` del sobre regenerado.
</output>