---
phase: 03-iol-verification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - main_iol.py
autonomous: true
requirements: [IOL-01, IOL-02, IOL-03, IOL-04, IOL-06, IOL-07]
tags: [verification, driver, probes, live-api, schema-snapshot, oauth, dual-sync-async]
must_haves:
  truths:
    - "main_iol.py expone 15 probes nombrados en orden D-IOL-5: probe_login_sync, probe_login_async, probe_get_quote_sync, probe_get_quote_async, probe_get_historical_quotes_sync, probe_get_historical_quotes_async, probe_get_instruments_sync, probe_get_instruments_async, probe_get_instruments_by_type_sync, probe_get_instruments_by_type_async, probe_parity_sync_async, probe_field_type_map, probe_schema_snapshot, probe_refresh_token, probe_auth_401"
    - "main() invoca los probes en orden D-IOL-5 (login first, antibot last → en IOL es probe_auth_401 último, D-IOL-4)"
    - "Cada probe maneja excepciones y el driver continúa todos los probes con exit 0 salvo crash inesperado (D-04 Phase 2 mirror)"
    - "stdout = una línea verbatim por probe `PROBE <name>: <status> <detail>` + summary final `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` (D-02 Phase 2 mirror)"
    - "Driver llama write_findings('iol-client') al inicio (idempotente, D-03 Phase 2 mirror)"
    - "Cualquier discrepancia detectada en vivo llama append_finding(...) con clases SHAPE/AUTH/ERROR-MAP/PARAM/SYNC-ASYNC-DRIFT (D-IOL-15)"
    - "probe_auth_401 opt-in via VERIFY_IOL_BAD_CREDS=1 (D-IOL-1); sin la var imprime SKIPPED y sigue"
    - "probe_auth_401 es one-shot, sin retry ni sleep ni loop (D-IOL-1, mirror D-14 Phase 2)"
    - "probe_auth_401 usa try/finally para restaurar IOL_PASSWORD original tras inyección de bad_password (D-IOL-2)"
    - "Cascade SKIPPED: si probe_login_sync o probe_login_async fallan, los probes downstream emiten SKIPPED con razón 'auth failed' (D-IOL-3) — implementado vía flag module-level `_auth_failed: bool`"
    - "Async lifecycle: un único asyncio.run(_async_main(...)) y await aio.aclose() dentro de contextlib.suppress(Exception) (D-IOL-6, mirror IN-03 Phase 2)"
    - "probe_schema_snapshot escribe 4 JSON files en .planning/verification/schemas/iol-client/ con envelope D-21 y D-25 no-overwrite-on-drift (D-IOL-16)"
    - "Para get_instruments_by_type: snapshot solo con 'acciones' + sanity check de los 6 InstrumentType con type-only assertion (D-IOL-17)"
    - "probe_field_type_map llama _request directo (NO el wrapper get_instruments_by_type) para evitar envelope swallow en la detección de drift IOL-04 (Pitfall 2)"
    - "probe_refresh_token verifica el fix IOL-07 in-vivo: fuerza _token_expires_at = 0.0 y observa _token + _refresh_token + expires_at cambian sin re-disparar password grant (D-IOL-11)"
    - "Todos los prints pasan por safe_print(text, secrets=[IOL_USER, IOL_PASSWORD, _refresh_token]) (D-IOL-7, D-IOL-22) — lista de secrets NO vacía"
    - "Helpers de fecha derivados: _last_business_day(today) reutilizable y _ALL_INSTRUMENT_TYPES tupla con los 6 types"
    - "El driver lee state privado iol_client.client._base_url / _token / _refresh_token / _token_expires_at vía atributo directo (precedente Phase 2: verification/mutation_gate.py:55)"
  artifacts:
    - path: "main_iol.py"
      provides: "Rewritten IOL live verification driver with 15 probes (was smoke test)"
      contains: "def probe_login_sync"
      contains_all: ["probe_login_sync", "probe_login_async", "probe_get_quote_sync", "probe_get_quote_async", "probe_get_historical_quotes_sync", "probe_get_historical_quotes_async", "probe_get_instruments_sync", "probe_get_instruments_async", "probe_get_instruments_by_type_sync", "probe_get_instruments_by_type_async", "probe_parity_sync_async", "probe_field_type_map", "probe_schema_snapshot", "probe_refresh_token", "probe_auth_401"]
  key_links:
    - from: "main_iol.py"
      to: "verification.findings.append_finding"
      via: "from verification.findings import append_finding"
      pattern: "from verification.findings import append_finding"
    - from: "main_iol.py"
      to: "verification (safe_print, schema_of, write_findings, require_env)"
      via: "from verification import ..."
      pattern: "from verification import .*require_env"
    - from: "main_iol.py"
      to: "iol_client (sync surface) + aio submódulo + IOLAuthError + IOLAPIError"
      via: "import iol_client; from iol_client import aio, IOLAPIError, IOLAuthError"
      pattern: "from iol_client import"
    - from: "main_iol.py::probe_refresh_token"
      to: "iol_client.client._refresh_token + _token + _token_expires_at"
      via: "direct attribute reads + write _token_expires_at = 0.0"
      pattern: "iol_client.client._refresh_token"
    - from: "main_iol.py::probe_auth_401"
      to: "iol_client.configure(password=IOL_PASSWORD + '_INVALID')"
      via: "try/finally injection (D-IOL-2)"
      pattern: "_INVALID"
---

<objective>
Re-escribir `main_iol.py` desde su estado actual (smoke test mínimo: `require_env` + `login()` + `redact(token)`, 33 líneas) a un driver completo con 15 probes nombrados en orden D-IOL-5 que cierra el loop end-to-end de Phase 3 sobre `api.invertironline.com`: auth (login sync + async, IOL-01), happy-path sweep dual (4 endpoints × 2 surfaces = 8 probes, IOL-02), parity estructural sync↔async (IOL-06), field→type map vs `_ASSUMED_*` (IOL-03 + IOL-04 detail), 4 schema snapshots (DRIFT-01 mirror, D-IOL-16), refresh_token in-vivo verification (IOL-07 + verifica el fix de Plan 03-01), y opt-in 401 last-in-sequence (IOL-05). Todos los probes alimentan `.planning/verification/iol-client-findings.md` vía `append_finding` y emiten líneas verbatim a stdout más un summary final, con secrets dinámicamente extendidos para incluir `_refresh_token` capturado por `login()`.

Es el **slice central de Phase 3**: el fix del Plan 03-01 está implementado pero no verificado in-vivo; este plan lo verifica via `probe_refresh_token` (D-IOL-11) y deja al Plan 03-03 el cierre con tests Verified-live + human checkpoint + commit baseline.

Purpose: Cumplir IOL-01..04 + IOL-06 + IOL-07 (verificación in-vivo del fix) corriendo el cliente IOL completo en vivo y produciendo, de forma reproducible y no-destructiva (auth-once discipline + opt-in 401 + single-shot + safe_print con secrets), dos clases de artefactos: el findings markdown + 4 schema snapshots JSON que se commitearán en el Plan 03-03.

Output: `main_iol.py` reescrito; al invocarlo a mano (`uv run --package iol-client python main_iol.py`), emite 15 líneas `PROBE <name>: <status>` + `SUMMARY: ...` y exit 0; en su primera corrida exitosa genera/actualiza `.planning/verification/iol-client-findings.md` (esqueleto + cualquier finding OPEN/EXPECTED) y los 4 schema snapshots en `.planning/verification/schemas/iol-client/`. NO se ejecuta dentro del suite pytest (D-05 Phase 2 mirror).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-iol-verification/03-CONTEXT.md
@.planning/phases/03-iol-verification/03-RESEARCH.md
@.planning/phases/03-iol-verification/03-PATTERNS.md
@.planning/phases/03-iol-verification/03-VALIDATION.md
@.planning/phases/03-iol-verification/03-01-PLAN.md
@.planning/phases/02-mbito-verification/02-CONTEXT.md
@.planning/phases/02-mbito-verification/02-02-PLAN.md
@.planning/phases/02-mbito-verification/02-REVIEW.md
@.planning/verification/FINDINGS-TEMPLATE.md
@main_iol.py
@main_ambito_financiero.py
@main_higyrus.py
@main_verify.py
@verification/findings.py
@verification/__init__.py
@verification/schema.py
@verification/redaction.py
@verification/env_gate.py
@verification/mutation_gate.py
@packages/iol-client/src/iol_client/client.py
@packages/iol-client/src/iol_client/aio.py
@packages/iol-client/src/iol_client/exceptions.py
@packages/iol-client/src/iol_client/__init__.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Rewrite main_iol.py with 15 named probes + summary + safe_print dynamic secrets</name>
  <files>main_iol.py</files>
  <read_first>
    - main_iol.py (estado actual: 33 líneas, smoke test mínimo con `require_env` + `redact` + `iol_client.login()`; se reescribe completo)
    - main_ambito_financiero.py (ANALOG DIRECTO Phase 2, 728 líneas, 7 probes; copiar estructura — ProbeResult, _fid_counter/_next_fid, _last_business_day, probe_* shape, schema_snapshot envelope D-21, probe_antibot try/finally, _async_main con contextlib.suppress, main() summary)
    - main_higyrus.py (analog secundario: import `from verification import require_env, safe_print` líneas 17, uso de `safe_print(text, secrets=[...])` 31-44)
    - .planning/phases/03-iol-verification/03-PATTERNS.md (sección 4 "main_iol.py — REWRITE COMPLETO (smoke-test → 15 probes)" con código verbatim: module docstring listando los 15 probes, imports, constantes module-level incluyendo _SAMPLE_SYMBOL/_SAMPLE_INSTRUMENT_TYPE/_ALL_INSTRUMENT_TYPES/_ASSUMED_*, ProbeResult, _next_fid, _last_business_day, cascade SKIPPED pattern, probe_get_quote_sync con try/except + status_code typed WR-01, probe_field_type_map_envelope_check con `_request` directo Pitfall 2, probe_refresh_token con state observation, probe_auth_401 try/finally con configure password injection, _write_or_check_schema helper, _sanity_check_six_instrument_types, probe_field_type_map orquestador, probe_parity_sync_async 4-endpoint diff, _async_main con contextlib.suppress, main() orchestrator con safe_print dynamic secrets incluyendo _refresh_token)
    - .planning/phases/03-iol-verification/03-RESEARCH.md (Pattern 1 "Auth-once with cascade SKIPPED" con `_auth_failed: bool` flag + `_auth_failure_reason: str`; Pattern 4 "Phase 2 driver patterns inheritance map" con WR-01/WR-03/IN-03 fixes; Anti-Patterns "Calling get_instruments_by_type then comparing schema" / "Doubling HTTP calls" / "time.sleep" / "Multiple asyncio.run" / "Resetting _refresh_token outside configure/_refresh success"; Pitfall 2 "envelope key swallowed by get_instruments_by_type" con código del envelope check; Pitfall 9 "IOL lockout policy unknown — opt-in single-shot")
    - .planning/phases/03-iol-verification/03-CONTEXT.md (D-IOL-1..22 lockedas; específicamente D-IOL-5 orden completo de probes, D-IOL-7 safe_print con secrets dinámicos, D-IOL-11 probe_refresh_token observación de state, D-IOL-14 _ASSUMED_* constants module-level, D-IOL-15 tres sub-clases SHAPE finding, D-IOL-17 baseline + sanity check 6 types, D-IOL-18 _SAMPLE_SYMBOL=GGAL, D-IOL-19 rango histórico 5 días hábiles, D-IOL-20 parity estructural NO valores)
    - packages/iol-client/src/iol_client/client.py (DESPUÉS de Plan 03-01: contiene `_refresh_token`, `_refresh()`, `_ensure_token` con fallback; el driver lee `iol_client.client._refresh_token` directamente para probe 14)
    - packages/iol-client/src/iol_client/aio.py (DESPUÉS de Plan 03-01: mismo state)
    - packages/iol-client/src/iol_client/exceptions.py (jerarquía: IOLAuthError(status_code, text) hereda de IOLAPIError; `exc.status_code` es typed, WR-01 mirror — NUNCA usar `exc.args[0]` fallback)
    - packages/iol-client/src/iol_client/__init__.py (re-exports públicos — el driver importa `IOLAPIError, IOLAuthError` desde el barrel; `aio` se importa con `from iol_client import aio`)
    - verification/findings.py (append_finding firma hardened post Phase 2; class_ in FINDING_CLASSES; status in STATUS_LIFECYCLE; preserva status humano)
    - verification/__init__.py (barrel — `from verification import safe_print, schema_of, write_findings, require_env, append_finding` posible; del directo `from verification.findings import append_finding` también válido)
    - verification/redaction.py (safe_print firma `safe_print(text: str, secrets: list[str]) -> None` línea 43; los secrets de IOL son IOL_USER, IOL_PASSWORD, y _refresh_token dinámico)
    - verification/env_gate.py (require_env firma; SKIPPED format `SKIPPED <pkg>: missing X, Y`)
    - verification/schema.py (schema_of firma; PII-free por construcción; recursivo)
    - verification/mutation_gate.py:55 (precedente para acceso a state privado: `matriz_client.client._base_url` — pattern de lectura sin tocar el client)
    - .planning/verification/FINDINGS-TEMPLATE.md (clases fijas D-09 Phase 1: SHAPE, AUTH, ERROR-MAP, PARAM, SYNC-ASYNC-DRIFT, NO-DATA, ANTI-BOT; estados D-08 Phase 1: OPEN, CONFIRMED, FIXED, EXPECTED, NO-FIX)
    - CLAUDE.md (Python 3.12+, ruff line-length=100, double quotes, 4 espacios, mypy --strict, `from __future__ import annotations` mandatory, no wildcard/relative imports)
  </read_first>
  <action>
    Re-escribir COMPLETAMENTE `main_iol.py` siguiendo el bosquejo de `03-PATTERNS.md` sección 4 (~600 líneas estimadas, refleja la estructura Phase 2 con 8 probes más). El smoke test actual (`require_env` + `login` + `redact`) se reemplaza enteramente.

    **Imports al tope (orden: future → stdlib → third-party → local; alfabético dentro de grupo):**
    - `from __future__ import annotations`
    - stdlib: `asyncio`, `contextlib`, `datetime as dt`, `json`, `os`, `dataclasses.dataclass`, `pathlib.Path`, `typing.Any`
    - local verification: `from verification import require_env, safe_print, schema_of, write_findings` y `from verification.findings import append_finding`
    - local iol_client: `import iol_client`, `from iol_client import IOLAPIError, IOLAuthError, aio`

    NOTA: NO importar `IOLRateLimitError` salvo que se use explícitamente — el `_raise_for_response` ya lo levanta y se atrapa como `IOLAPIError` su padre. NO importar `httpx` (a diferencia de Phase 2 que necesitaba `httpx.__version__` para BAD_UA — IOL no tiene anti-bot UA).

    **Module docstring** (verbatim según 03-PATTERNS.md sección 4):
    Describir los 15 probes en orden D-IOL-5, env vars requeridas (`IOL_USER`, `IOL_PASSWORD`, opcional `IOL_BASE_URL`), opt-in `VERIFY_IOL_BAD_CREDS=1` para probe 15, artefactos generados (findings markdown + 4 schemas), reglas de seguridad (auth-once, single-shot 401, cascade SKIPPED).

    **Constantes module-level (después de imports + docstring):**
    - `_PKG = "iol-client"`
    - `_REPO_ROOT = Path(__file__).resolve().parent`
    - `_SCHEMA_DIR = _REPO_ROOT / ".planning" / "verification" / "schemas" / _PKG`
    - `_SCHEMA_FILES = {"get_quote": _SCHEMA_DIR / "get-quote.json", "get_historical_quotes": _SCHEMA_DIR / "get-historical-quotes.json", "get_instruments": _SCHEMA_DIR / "get-instruments.json", "get_instruments_by_type": _SCHEMA_DIR / "get-instruments-by-type.json"}` (D-IOL-16)
    - `_SAMPLE_SYMBOL = "GGAL"` (D-IOL-18)
    - `_SAMPLE_INSTRUMENT_TYPE: iol_client.InstrumentType = "acciones"` (D-IOL-17, D-IOL-18)
    - `_ALL_INSTRUMENT_TYPES: tuple[iol_client.InstrumentType, ...] = ("obligacionesNegociables", "titulosPublicos", "cedears", "acciones", "letras", "cauciones")` (D-IOL-17)
    - `_ASSUMED_QUOTE_FIELDS: dict[str, str] = {"ultimoPrecio": "float", "simbolo": "str"}` (D-IOL-14 — campos mínimos que los callers asumen; ampliar a discreción tras observar el primer payload real)
    - `_ASSUMED_HISTORICAL_FIELDS: dict[str, str] = {"fechaHora": "str", "ultimoPrecio": "float"}` (D-IOL-14)
    - `_ASSUMED_INSTRUMENTS_BY_TYPE_ENVELOPE: dict[str, str] = {"titulos": "list"}` (D-IOL-14 — la clave del envelope IOL-04)
    - `_fid_counter: int = 0` + `_next_fid()` helper (mirror Phase 2)
    - `_auth_failed: bool = False` + `_auth_failure_reason: str = ""` (D-IOL-3 cascade SKIPPED flag)

    **`ProbeResult` dataclass:** frozen+slots, fields `name: str`, `status: str` ("PASS"|"FAIL"|"SKIPPED"|"FINDING"), `detail: str` (mirror Phase 2 verbatim).

    **`_next_fid() -> str`:** mismo helper Phase 2 (`F-{counter:02d}` con `global _fid_counter`).

    **`_last_business_day(today: dt.date) -> dt.date`:** copia verbatim del helper Phase 2 (retrocede lunes-viernes).

    **Los 15 probes:**

    **1. `probe_login_sync() -> ProbeResult`** (IOL-01):
    - `global _auth_failed, _auth_failure_reason`
    - try: `iol_client.login()` (call único, sin retry — D-IOL-1)
    - except `IOLAuthError as exc`: setear `_auth_failed = True`, `_auth_failure_reason = f"sync login: {exc}"`; emitir `append_finding(_PKG, fid=_next_fid(), class_="AUTH", surface="sync", status="OPEN", title="login() sync failed", expected="login succeeds + cached token", actual=repr(exc), diff=f"status_code={exc.status_code!r}", base_url=iol_client.client._base_url)`; return `ProbeResult("login_sync", "FINDING", f"{fid} (OPEN)")`
    - except cualquier otra excepción: propagar (D-IOL-3 último párrafo — error inesperado crash)
    - return `ProbeResult("login_sync", "PASS", f"_token cached, _refresh_token={'<cached>' if iol_client.client._refresh_token else 'NONE'}")`

    **2. `async def probe_login_async() -> ProbeResult`** (IOL-01):
    - Mismo patrón con `await aio.login()`; setear el mismo `_auth_failed` (compartido entre surfaces — si sync funcionó y async falla, probes async downstream skip; los sync ya pasaron así que NO los marca SKIPPED retroactivamente). Discretion: usar UN solo flag global `_auth_failed` que afecta a TODOS los downstream (el más estricto); alternativa con flags separados `_auth_failed_sync` / `_auth_failed_async` también es válida — recomendado el flag único por simplicidad.
    - Si falla: append_finding clase AUTH surface="async" status=OPEN; mismo pattern detail
    - return `ProbeResult("login_async", "PASS", "ok")` si éxito

    **3. `probe_get_quote_sync() -> tuple[ProbeResult, dict[str, Any] | None]`** (IOL-02):
    - Cascade check: `if _auth_failed: return ProbeResult("get_quote_sync", "SKIPPED", f"auth failed: {_auth_failure_reason}"), None`
    - `base_url = iol_client.client._base_url`
    - try: `quote = iol_client.get_quote(_SAMPLE_SYMBOL)` (un solo call — WR-03)
    - except `IOLAuthError as exc`: append_finding AUTH surface=sync status=OPEN con `status_code={exc.status_code!r}` (WR-01 typed); return `ProbeResult("get_quote_sync", "FINDING", f"{fid} (OPEN)"), None`
    - except `IOLAPIError as exc`: append_finding ERROR-MAP surface=sync status=OPEN con `status_code={exc.status_code!r}`; return FINDING
    - except `Exception as exc`: append_finding ERROR-MAP surface=sync status=OPEN con `title=f"get_quote_sync unexpected {type(exc).__name__}"`, `actual=repr(exc)`; return FINDING
    - return `ProbeResult("get_quote_sync", "PASS", f"ultimoPrecio={quote.get('ultimoPrecio')!r}"), quote`

    **4. `async def probe_get_quote_async() -> tuple[ProbeResult, dict[str, Any] | None]`** (IOL-02):
    - Mirror del 3 con `await aio.get_quote(_SAMPLE_SYMBOL)`; surface="async" en los findings.

    **5. `probe_get_historical_quotes_sync(today: dt.date) -> tuple[ProbeResult, list[dict[str, Any]] | None]`** (IOL-02):
    - Cascade check.
    - `hasta = _last_business_day(today)`; `desde = hasta - dt.timedelta(days=7)` (D-IOL-19: 5 días hábiles; 7 calendario = ~5 hábiles). Comentario inline: "D-IOL-19: ~5 días hábiles back desde el último hábil."
    - try: `serie = iol_client.get_historical_quotes(_SAMPLE_SYMBOL, desde, hasta)`
    - except mismo pattern (auth/api/exception → finding)
    - return `(ProbeResult("get_historical_quotes_sync", "PASS", f"len={len(serie)}"), serie)`

    **6. `async def probe_get_historical_quotes_async(today: dt.date) -> tuple[ProbeResult, list[dict[str, Any]] | None]`** (IOL-02):
    - Mirror

    **7. `probe_get_instruments_sync() -> tuple[ProbeResult, Any]`** (IOL-02):
    - Cascade check.
    - try: `data = iol_client.get_instruments("argentina")`
    - return `(ProbeResult("get_instruments_sync", "PASS", f"type={type(data).__name__}"), data)`

    **8. `async def probe_get_instruments_async() -> tuple[ProbeResult, Any]`** (IOL-02): mirror.

    **9. `probe_get_instruments_by_type_sync() -> tuple[ProbeResult, list[dict[str, Any]] | None, dict[str, Any] | None]`** (IOL-02 + IOL-04 envelope):
    - Cascade check.
    - **Diferencia clave (Pitfall 2):** NECESITAMOS el envelope crudo para detectar drift de `"titulos"`, pero también el resultado unwrapped del wrapper para el sweep happy-path. Solución: el probe captura AMBOS — un call al wrapper (`iol_client.get_instruments_by_type(_SAMPLE_INSTRUMENT_TYPE)`) y un call separado al `_request` directo para el envelope. **PERO WR-03 dice "single HTTP call per probe."** Resolución: en este probe NO duplicar — el envelope check va EN `probe_field_type_map` (probe 12), que llama `_request` directo. Este probe solo hace `wrapper_result = iol_client.get_instruments_by_type(_SAMPLE_INSTRUMENT_TYPE)` (un solo HTTP call vía wrapper) y devuelve `(ProbeResult, wrapper_result, None)` — el tercer elemento `envelope` se popula en el probe 12. Alternativa Discretion: hacer un único `_request` directo acá y unwrap manual; la elección es del implementador siempre que la verificación del envelope quede cubierta por probe 12 + Pitfall 2 con `_request` directo.
    - **Sanity check de los 6 types (D-IOL-17):** después del call principal con `_SAMPLE_INSTRUMENT_TYPE`, iterar `_ALL_INSTRUMENT_TYPES` (los 6) y para cada uno hacer `titulos = iol_client.get_instruments_by_type(itype)` y assert type-only `isinstance(titulos, list) and titulos and isinstance(titulos[0], dict)`; cualquier discrepancia → append_finding SHAPE OPEN con detalle de qué types fallaron. **CUIDADO con el conteo de HTTP calls — el sanity check hace 6 HTTP calls adicionales. Discretion del implementador: poner el sanity check EN ESTE probe (puro happy-path queda con `acciones` solamente y el sanity es parte del mismo probe) o como sub-probe separado de probe 12. Recomendación: incluir en este probe con comment explícito explicando que el WR-03 cap de "single HTTP call per probe" aplica al concepto-probe; los sanity HTTP calls están agrupados conceptualmente con el sample principal y NO son "verificación duplicada del mismo endpoint."**
    - return tupla con el wrapper_result y el `envelope` (que será populado en probe 12 vía `_request` directo)

    **10. `async def probe_get_instruments_by_type_async() -> tuple[ProbeResult, list[dict[str, Any]] | None, dict[str, Any] | None]`** (IOL-02):
    - Mirror; el sanity check de 6 types corre solo en sync (probe 9) — async hace solo el sample principal y la verificación de parity (probe 11). Discretion del implementador si replicar el sanity check en async; recomendado NO replicar (los 6 endpoints son idénticos sync vs async modulo lock pattern; parity se verifica estructuralmente en probe 11).

    **11. `probe_parity_sync_async(quote_sync, quote_async, historical_sync, historical_async, instruments_sync, instruments_async, by_type_sync, by_type_async) -> ProbeResult`** (IOL-06, D-IOL-20):
    - Cascade check.
    - Para cada uno de los 4 pares (quote, historical, instruments, instruments_by_type):
      - Si alguno es None (probe anterior falló): saltar
      - `schema_of(sync_data) == schema_of(async_data)` → match; sino: append_finding SYNC-ASYNC-DRIFT surface=both status=OPEN con detalle de qué endpoint difiere
    - NO comparar valores numéricos (precio cambia entre dos calls).
    - return PASS si todos match; FINDING con lista de fids si discrepancia

    **12. `probe_field_type_map(quote, historical, instruments, instruments_by_type_envelope) -> ProbeResult`** (IOL-03 + IOL-04, D-IOL-13: reusa `verification.schema.schema_of` como único primitivo para el mapa observado, PII-free por construcción):
    - Cascade check.
    - **Importante (Pitfall 2):** este probe captura el envelope crudo de `get_instruments_by_type` llamando `iol_client.client._request("GET", f"/api/v2/Cotizaciones/{_SAMPLE_INSTRUMENT_TYPE}/argentina/Todos")` DIRECTO y leyendo `resp.json()` (NO el wrapper, que silenciosamente devuelve [] si falta la clave `titulos`). Asignar a `instruments_by_type_envelope` y luego validar `"titulos" in instruments_by_type_envelope`; si missing → append_finding SHAPE surface=both status=OPEN con `title="missing envelope key 'titulos' in get_instruments_by_type"`, `expected="dict with key 'titulos' (list[dict])"`, `actual=f"keys={sorted(instruments_by_type_envelope.keys())}"`, `diff="client.py:206 does data.get('titulos', []) silently returns [] when missing"`, `base_url=iol_client.client._base_url`. (Este DOBLE HTTP call al endpoint by_type es el ÚNICO caso permitido de duplicación por Pitfall 2 — comentar explícito en el código.)
    - Para `quote` (si no es None): iterar `_ASSUMED_QUOTE_FIELDS`:
      - `observed = schema_of(quote)`
      - `if key not in observed`: append_finding SHAPE OPEN "missing assumed key `<key>` in get_quote"
      - `elif observed[key] != expected_type`: append_finding SHAPE OPEN "type drift on `<key>` in get_quote: assumed `<expected_type>`, observed `<observed[key]>`"
    - Mismo para `historical[0]` (si len(historical) >= 1, iterar `_ASSUMED_HISTORICAL_FIELDS` sobre el primer row)
    - return PASS si todos los assumed keys están presentes y typed; FINDING con lista de fids si drift

    **13. `probe_schema_snapshot(quote, historical, instruments, by_type_envelope) -> ProbeResult`** (D-IOL-16, DRIFT-01 mirror):
    - Cascade check.
    - Para cada uno de los 4 endpoints, llamar helper `_write_or_check_schema(func_name, endpoint_template, sample_params, raw_payload, base_url)`:
      - `actual_schema = schema_of(raw_payload)`
      - `envelope = {"endpoint": endpoint_template, "client_function": func_name, "captured_at": dt.datetime.now(dt.UTC).isoformat(), "base_url": base_url, "sample_params": sample_params, "schema": actual_schema}` (D-21 verbatim)
      - `_SCHEMA_DIR.mkdir(parents=True, exist_ok=True)`
      - if not `_SCHEMA_FILES[func_name].exists()`: write JSON; return PASS f"escrito {file.name}"
      - else: leer committed; if `committed["schema"] == actual_schema`: PASS "schema sin drift"
      - else (D-25 no-overwrite): append_finding SHAPE surface=both status=OPEN "Schema drift en `<func_name>`" con expected=committed schema, actual=schema_of(raw), diff="comparar expected vs actual; NO sobreescribe baseline (D-25)"; return FINDING "<fid> (OPEN) — NO sobreescribe"
    - Para `get_instruments_by_type`: el snapshot usa el envelope CRUDO (con `"titulos"`), NO el unwrapped list (D-IOL-17 + Pitfall 2)
    - Resultado del probe: PASS si los 4 escriben (primera corrida) o si los 4 match committed; sino FINDING con lista de fids por drift

    **14. `probe_refresh_token() -> ProbeResult`** (IOL-07 in-vivo, D-IOL-11):
    - Cascade check.
    - `base_url = iol_client.client._base_url`
    - `refresh_before = iol_client.client._refresh_token`
    - Si `refresh_before is None`: append_finding AUTH surface=sync status=OPEN "login() no capturó refresh_token del payload" expected=`_refresh_token != None tras login() exitoso` actual=`_refresh_token=None` diff=`el server no devolvió refresh_token o el cliente lo descartó` (D-IOL-11); return FINDING (no proceder porque sin refresh_token el branch refresh no se ejercita)
    - `token_before = iol_client.client._token`
    - Forzar expiry: `iol_client.client._token_expires_at = 0.0` (mutar directo el module global — precedente: monkeypatch en tests; en driver es read-after-write directo, comentar "# Simulación in-vivo de expiry para forzar el branch refresh")
    - Disparar call autenticado para gatillar `_ensure_token`: `iol_client.get_instruments("argentina")` (un endpoint barato; cambiar a otro si causa cargo no deseable; opcional usar el quote para mantener mínimo footprint)
    - except `IOLAuthError` o `IOLAPIError`: append_finding AUTH OPEN "refresh path no funciona en vivo" con detail repr(exc); return FINDING
    - `token_after = iol_client.client._token`; `refresh_after = iol_client.client._refresh_token`; `expires_at_after = iol_client.client._token_expires_at`
    - Heurística D-IOL-11: éxito si `token_before != token_after` AND `expires_at_after > time.time()` (renovado) AND `refresh_after is not None` (no se perdió). Si `refresh_after is None` → finding AUTH OPEN "refresh path borró _refresh_token (Pitfall 3 violation)".
    - Si `token_before == token_after`: posiblemente el ensure encontró expires_at ok (race?) o el refresh no funcionó. Discretion: append_finding AUTH OPEN "token no cambió después de forzar expiry" o aceptar como OK si fue race condition. Recomendación: emitir finding OPEN para que el humano investigue.
    - return `ProbeResult("refresh_token", "PASS", f"refresh path verified — token rotated, _refresh_token={'rotated' if refresh_after != refresh_before else 'preserved'}")`
    - **NOTA importante:** este probe NO ejercita la rama "refresh falla → fallback a password" en vivo (D-IOL-11 explícito — esa cobertura es mocked-only en Plan 03-01).

    **15. `probe_auth_401() -> ProbeResult`** (IOL-05, D-IOL-1/2/4):
    - Gate opt-in: `if os.getenv("VERIFY_IOL_BAD_CREDS") != "1": return ProbeResult("auth_401", "SKIPPED", "(opt-in via VERIFY_IOL_BAD_CREDS=1)")`
    - Cascade check (si auth ya falló, no tiene sentido el probe).
    - `base_url = iol_client.client._base_url`
    - `original_password = os.getenv("IOL_PASSWORD", "")` (D-IOL-2: solo lee env, NO el state cacheado del cliente que pudo haber sido sobreescrito por algún probe)
    - `bad_password = original_password + "_INVALID"` (D-IOL-2)
    - Estructura try/finally OBLIGATORIA (D-IOL-2):
      - try:
        - `iol_client.configure(password=bad_password)` (configure resetea `_token` y `_refresh_token` automáticamente per Plan 03-01)
        - try:
          - `iol_client.login()` (call único, sin retry — D-IOL-1)
          - Si NO levanta: defense relajada — append_finding AUTH OPEN "credenciales inválidas NO recibieron 401" expected="401 con password=IOL_PASSWORD+_INVALID" actual="200 OK (defensa relajada)"; return FINDING
        - except `IOLAuthError as exc`:
          - `status_code = exc.status_code` (typed WR-01 — NUNCA `exc.args[0]`)
          - if `status_code == 401`: append_finding AUTH surface=sync status=EXPECTED "credenciales inválidas reciben 401" expected="401" actual="401" diff="ninguno; comportamiento esperado"; return FINDING f"{fid} (EXPECTED)"
          - else (otro status): append_finding AUTH OPEN "credenciales inválidas recibieron status inesperado" expected="401" actual=f"status={status_code}"; return FINDING OPEN
        - except `Exception as exc`: append_finding AUTH OPEN "credenciales inválidas produjeron error inesperado" actual=repr(exc); return FINDING OPEN
      - finally:
        - `iol_client.configure(password=original_password)` (D-IOL-2: SIEMPRE restaura, incluso si try interno levantó)
    - **D-IOL-1 hardcoded:** ÚNICA llamada a `login()`, sin retry, sin sleep, sin loop. Acceptance criterion verifica que `time.sleep` no aparece en el archivo.

    **`_async_main(today: dt.date) -> tuple[...]`** (D-IOL-6, IN-03):
    - Tipo de retorno explícito como tupla con los ProbeResult + payloads necesarios para parity/field_type_map/schema_snapshot del lado async
    - try:
      - `result_login_async = await probe_login_async()`
      - `result_quote_async, quote_async = await probe_get_quote_async()`
      - `result_historical_async, historical_async = await probe_get_historical_quotes_async(today)`
      - `result_instruments_async, instruments_async = await probe_get_instruments_async()`
      - `result_by_type_async, by_type_async, _ = await probe_get_instruments_by_type_async()`
    - finally:
      - `with contextlib.suppress(Exception): await aio.aclose()` (IN-03 mirror)
    - return tupla

    **`main() -> None`** (orden D-IOL-5):
    - `if not require_env(_PKG, ["IOL_USER", "IOL_PASSWORD"]): return` (HARN-01 — exit 0 sin bloquear)
    - `today = dt.date.today()`
    - `write_findings(_PKG)` (idempotente — D-03 mirror)
    - `results: list[ProbeResult] = []`
    - **Probe 1 sync:** `results.append(probe_login_sync())`
    - **Probes 2 + 4 + 6 + 8 + 10 (async batch) en single asyncio.run:**
      - `(result_login_async, result_quote_async, quote_async, result_historical_async, historical_async, result_instruments_async, instruments_async, result_by_type_async, by_type_async) = asyncio.run(_async_main(today))`
      - Append results en orden
    - **Probe 3 sync:** `result_quote_sync, quote_sync = probe_get_quote_sync()`; append; append result_quote_async (intercalado)
    - **Probe 5 sync:** historical
    - **Probe 7 sync:** instruments
    - **Probe 9 sync:** instruments_by_type (con sanity check 6 types)
    - **Probe 11:** parity_sync_async con los 8 payloads capturados
    - **Probe 12:** field_type_map (este probe internamente hace el `_request` directo para el envelope by_type — captura `by_type_envelope` adentro)
    - **Probe 13:** schema_snapshot (4 endpoints; usa el envelope crudo de by_type capturado en probe 12 si fuera necesario, o vuelve a hacer `_request` directo — Discretion del implementador, comentar)
    - **Probe 14:** refresh_token
    - **Probe 15:** auth_401 (ÚLTIMO — D-IOL-4)
    - **safe_print con secrets dinámicos (D-IOL-7, D-IOL-22):**
      - `secrets = [v for v in (os.getenv("IOL_USER"), os.getenv("IOL_PASSWORD"), iol_client.client._refresh_token) if v and len(v) >= 4]`
      - Por cada `r in results`: `safe_print(f"PROBE {r.name}: {r.status} {r.detail}".rstrip(), secrets=secrets)`
    - **Summary final (D-02 mirror Phase 2):**
      - `n_pass = sum(1 for r in results if r.status == "PASS")`; idem FAIL, SKIPPED, FINDING
      - `safe_print(f"SUMMARY: PASS={n_pass} FAIL={n_fail} SKIPPED={n_skip} FINDING={n_find}", secrets=secrets)`
    - `if __name__ == "__main__": main()`

    **Pre-condiciones / invariantes obligatorios:**
    - Cada `probe_*` con tipo de retorno explícito (mypy strict)
    - NO `print(...)` directo en ningún lugar; todos los prints pasan por `safe_print(...)` con `secrets=[...]` (D-IOL-7)
    - El driver lee `iol_client.client._base_url`, `iol_client.client._token`, `iol_client.client._refresh_token`, `iol_client.client._token_expires_at` como atributos directos (precedente Phase 2 `verification/mutation_gate.py:55`)
    - `IOLAPIError.status_code` es typed — usar `exc.status_code` directo en WR-01 mirror (NO `exc.args[0]`)
    - UN único `asyncio.run(...)` (D-IOL-6, anti-pattern del RESEARCH)
    - NO `time.sleep` en ningún lugar (D-IOL-1, anti-pattern del RESEARCH)
    - NO se fabrican findings: si los probes corren OK contra el server vivo y no detectan discrepancias, no se llama `append_finding`. El primer live run ocurre en Plan 03-03 con checkpoint humano.
    - NO se commitea `.planning/verification/iol-client-findings.md` ni `.planning/verification/schemas/iol-client/*` en este plan — Task 3.3 los commitea.
    - NO se modifica `client.py` ni `aio.py` — esos cambios son del Plan 03-01.

    **Lo que NO va en este plan:**
    - NO correr el driver en vivo (eso es Plan 03-03 con checkpoint humano)
    - NO escribir tests Verified-live (eso es Plan 03-03 Task 3.1)
    - NO fixes opportunistic ad-hoc sobre el cliente — Plan 03-01 cubrió IOL-07; otros findings que aparezcan son OPEN y se manejan humanos ex-post

    Convenciones obligatorias (CONVENTIONS.md / CLAUDE.md):
    - `from __future__ import annotations` primera línea (mandatory)
    - ruff line-length=100, double quotes, 4 espacios
    - mypy --strict: todas las firmas completas con retorno explícito
    - No wildcard / no relative imports

    Commit sugerido: `feat(03-02): rewrite main_iol.py with 15 named probes (D-IOL-1..22)`
  </action>
  <verify>
    <automated>uv run python -c "import main_iol; probes = ['probe_login_sync', 'probe_login_async', 'probe_get_quote_sync', 'probe_get_quote_async', 'probe_get_historical_quotes_sync', 'probe_get_historical_quotes_async', 'probe_get_instruments_sync', 'probe_get_instruments_async', 'probe_get_instruments_by_type_sync', 'probe_get_instruments_by_type_async', 'probe_parity_sync_async', 'probe_field_type_map', 'probe_schema_snapshot', 'probe_refresh_token', 'probe_auth_401', 'main', '_last_business_day', '_next_fid']; [getattr(main_iol, p) for p in probes]; print('all 18 entries present')"</automated>
    <automated>uv run mypy main_iol.py</automated>
    <automated>uv run ruff check main_iol.py</automated>
    <automated>uv run ruff format --check main_iol.py</automated>
    <automated>uv run python -c "src = open('main_iol.py').read(); assert 'time.sleep' not in src, 'D-IOL-1: no sleep'; assert src.count('asyncio.run(') == 1, 'D-IOL-6: single event loop'; assert 'VERIFY_IOL_BAD_CREDS' in src, 'D-IOL-1 opt-in env var'; assert '_INVALID' in src, 'D-IOL-2 password injection'; assert 'contextlib.suppress' in src, 'IN-03 aclose suppress'; print('static checks OK')"</automated>
    <automated>grep -v '^#' main_iol.py | grep -c 'safe_print('</automated>
    <automated>grep -c 'append_finding(' main_iol.py</automated>
    <automated>grep -v '^#' main_iol.py | grep -c 'iol_client.client._refresh_token'</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Source: `main_iol.py contains def probe_login_sync(`
    - Source: `main_iol.py contains async def probe_login_async(`
    - Source: `main_iol.py contains def probe_get_quote_sync(`
    - Source: `main_iol.py contains async def probe_get_quote_async(`
    - Source: `main_iol.py contains def probe_get_historical_quotes_sync(`
    - Source: `main_iol.py contains async def probe_get_historical_quotes_async(`
    - Source: `main_iol.py contains def probe_get_instruments_sync(`
    - Source: `main_iol.py contains async def probe_get_instruments_async(`
    - Source: `main_iol.py contains def probe_get_instruments_by_type_sync(`
    - Source: `main_iol.py contains async def probe_get_instruments_by_type_async(`
    - Source: `main_iol.py contains def probe_parity_sync_async(`
    - Source: `main_iol.py contains def probe_field_type_map(`
    - Source: `main_iol.py contains def probe_schema_snapshot(`
    - Source: `main_iol.py contains def probe_refresh_token(`
    - Source: `main_iol.py contains def probe_auth_401(`
    - Source: `main_iol.py contains def _last_business_day(`
    - Source: `main_iol.py contains def _next_fid(`
    - Source: `main_iol.py contains def main(`
    - Source: `main_iol.py contains async def _async_main(`
    - Source: `main_iol.py contains @dataclass(frozen=True, slots=True)` (ProbeResult)
    - Source: `main_iol.py contains write_findings(_PKG)` o `write_findings("iol-client")`
    - Source: `main_iol.py contains append_finding(`
    - Source: `main_iol.py contains safe_print(`
    - Source: `main_iol.py contains schema_of(`
    - Source: `main_iol.py contains require_env(`
    - Source: `main_iol.py contains 'VERIFY_IOL_BAD_CREDS'`
    - Source: `main_iol.py contains '_INVALID'` (password injection D-IOL-2)
    - Source: `main_iol.py contains contextlib.suppress(Exception)` (IN-03 mirror)
    - Source: `main_iol.py contains await aio.aclose()`
    - Source: `main_iol.py contains iol_client.client._refresh_token` (probe 14 reads + safe_print secrets list)
    - Source: `main_iol.py contains iol_client.client._token_expires_at = 0.0` (probe 14 forced expiry)
    - Source: `main_iol.py contains "SUMMARY:"` (string verbatim)
    - Source: `main_iol.py contains "PROBE "` (string verbatim)
    - Source: `main_iol.py contains _SAMPLE_SYMBOL = "GGAL"`
    - Source: `main_iol.py contains _ALL_INSTRUMENT_TYPES`
    - Source: `main_iol.py contains _ASSUMED_QUOTE_FIELDS`
    - Source: `main_iol.py contains _ASSUMED_HISTORICAL_FIELDS`
    - Source: `main_iol.py contains _ASSUMED_INSTRUMENTS_BY_TYPE_ENVELOPE`
    - Source: `main_iol.py contains get-quote.json` AND `get-historical-quotes.json` AND `get-instruments.json` AND `get-instruments-by-type.json` (los 4 schema files)
    - Source: `main_iol.py contains from __future__ import annotations` (CONVENTIONS mandatory)
    - Source: `main_iol.py contains _auth_failed` (D-IOL-3 cascade flag)
    - Source assertion: archivo NO contiene `time.sleep` (D-IOL-1 single-shot)
    - Source assertion: archivo contiene exactamente UN `asyncio.run(` (D-IOL-6 single event loop)
    - Source assertion: archivo NO contiene `exc.args[0]` (WR-01 typed status_code)
    - Behavior: `uv run python -c "import main_iol"` exits 0 (no syntax/import errors)
    - Behavior: el script de presence ("all 18 entries present") exits 0
    - Static: `uv run mypy main_iol.py` exits 0 con "Success"
    - Static: `uv run ruff check main_iol.py` exits 0
    - Static: `uv run ruff format --check main_iol.py` exits 0
    - Behavior: `grep -v '^#' main_iol.py | grep -c 'safe_print('` >= 16 (>= 1 por línea per probe + summary; estructura discrecional permite menos si se usa un loop, mínimo 2 para la línea per-probe y la línea SUMMARY)
    - Behavior: `grep -c 'append_finding(' main_iol.py` >= 6 (los probes login_sync/async, field_type_map múltiples sub-classes, schema_snapshot drift, refresh_token finding, auth_401 finding — al menos 6 call sites)
    - Behavior: `grep -v '^#' main_iol.py | grep -c 'iol_client.client._refresh_token'` >= 2 (al menos probe_refresh_token + safe_print secrets list)
    - Non-regression: `uv run pytest -q` exits 0 (driver no entra al testpaths por D-05 mirror; el suite preexistente de Plan 03-01 sigue verde)
    - Behavior: NO se ejecutan probes vivos en esta task (solo se escribe el driver); primer live run en Plan 03-03 Task 3.2
    - Behavior: NO se commiteo `.planning/verification/iol-client-findings.md` ni `.planning/verification/schemas/iol-client/*` (Plan 03-03 Task 3.3)
  </acceptance_criteria>
  <done>main_iol.py reescrito con 15 probes nombrados en orden D-IOL-5, ProbeResult dataclass, _next_fid, _last_business_day, cascade SKIPPED via `_auth_failed` flag, single asyncio.run + contextlib.suppress aclose, probe_field_type_map con `_request` directo para envelope (Pitfall 2), probe_refresh_token con observation de state (D-IOL-11), probe_auth_401 try/finally con password injection D-IOL-2 + opt-in D-IOL-1 + last D-IOL-4, schema snapshot D-25 no-overwrite, safe_print con secrets dinámicos incluyendo _refresh_token (D-IOL-7). mypy --strict + ruff verdes. Suite pre-existente sin regresiones. NO se corre el driver ni se commitean artefactos generados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver → api.invertironline.com (live HTTP) | Llamadas autenticadas (Bearer); auth-once discipline + opt-in 401 + single-shot |
| driver → iol_client.configure() (mutates module state — password) | probe_auth_401 temporariamente muta `_password` global; try/finally garantiza restore |
| driver → iol_client.client._token_expires_at (force expiry) | probe_refresh_token mutea directamente el module global para gatillar refresh path; lectura post para verificar (no se restaura — el branch refresh refresca naturalmente) |
| driver → .planning/verification/schemas/iol-client/ (committeable JSONs) | 4 snapshots PII-free por construcción (`schema_of` solo emite keys+types); D-25 no sobreescribe en drift |
| driver → .planning/verification/iol-client-findings.md (committeable MD) | `append_finding` (Plan 02-01 hardened) preserva status humano + valida class_/status |
| driver stdout → user terminal | `safe_print` con secrets dinámicos `[IOL_USER, IOL_PASSWORD, _refresh_token]` (D-IOL-7) + regex `_BEARER` defense-in-depth |
| driver → iol_client.client._refresh_token (state read for probe 14 + secrets list) | Lectura del module global; el driver NO escribe `_refresh_token` directamente; el helper `_refresh()` del cliente lo administra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-09 | Denial of Service (against self / account lockout) | probe_auth_401 disparando múltiples bad-credentials attempts | mitigate | D-IOL-1 opt-in via `VERIFY_IOL_BAD_CREDS=1` (default off) + D-IOL-1 single-shot (no retry, no sleep, no loop) + D-IOL-4 último en la secuencia. Acceptance criterion verifica que el archivo NO contiene `time.sleep`. Pitfall 9: documentar en docstring que cada run consume 1 attempt; máximo 1 corrida por hora discrecional. |
| T-3-10 | Tampering | probe_auth_401 deja `_password` global con valor `_INVALID` si excepción no se atrapa | mitigate | D-IOL-2 try/finally explícito con `iol_client.configure(password=original_password)` en `finally`. Acceptance criterion verifica patrón try+finally en el archivo. |
| T-3-11 | Tampering | probe_refresh_token mutea `_token_expires_at = 0.0` y el state queda inconsistente | mitigate | El call subsiguiente a `get_instruments` dispara `_ensure_token` que naturalmente refresca el token (via refresh path o password fallback), restaurando el state. No hay teardown explícito porque el branch refresh es el comportamiento esperado a verificar. |
| T-3-12 | Tampering | Schema snapshot sobreescribe baseline cuando hay drift sin gesto humano | mitigate | D-25 mirror Phase 2: `if not _SCHEMA_FILE.exists(): write else compare`; en drift NO write, emite finding SHAPE OPEN. La promoción a "drift aceptado" requiere edición manual del JSON. |
| T-3-13 | Information Disclosure | Driver imprime un payload crudo que incluye access_token reflejado | mitigate | safe_print regex `_BEARER` (verification/redaction.py:31) cubre tokens reflejados aun sin enumerar en `secrets`. Adicionalmente, lista `secrets=[IOL_USER, IOL_PASSWORD, _refresh_token]` enumera los valores conocidos. El access_token NO se enumera porque cambia entre runs y entre rama refresh; el regex es defensa primaria. |
| T-3-14 | Information Disclosure | `_refresh_token` aparece en findings markdown vía `actual=` o `diff=` | mitigate | El driver NO pasa `_refresh_token` a `append_finding` en ningún call site. Probe 14 lo lee para verificación pero solo reporta booleanos (rotated vs preserved) en el `detail` del ProbeResult — sin valor crudo. Acceptance criterion implícito: revisión del código durante Plan 03-03 checkpoint. |
| T-3-15 | Repudiation | Cascade SKIPPED enmascara la causa raíz del fallo de auth | mitigate | D-IOL-3: si login falla, el primer probe emite finding AUTH OPEN con `actual=repr(exc)` + `status_code` typed; los downstream marcan SKIPPED con `reason=<auth failure reason>` capturado en el flag módulo. El humano ve exactamente qué pasó. |
| T-3-16 | Tampering | Driver pasa raw bytes de exception en `actual=` que podrían tener credenciales | mitigate | `_raise_for_response` levanta `IOLAuthError(status_code, text)` con `text=resp.text`; el text del 401 de IOL típicamente NO contiene credenciales (es invalid_grant o similar). Defensa en profundidad: el `safe_print` con secrets enumerados redacta IOL_USER e IOL_PASSWORD si aparecen reflejados en cualquier print del driver (no en el findings file, pero el findings file no contiene `IOL_USER`/`IOL_PASSWORD` por construcción del driver — solo pasa lo que probe captura). Discrecional: revisar manualmente en checkpoint del Plan 03-03 antes de commit. |
| T-3-17 | Tampering | Pitfall 2: probe_field_type_map detecta drift via wrapper que silenciosa lo devuelve [] | mitigate | Probe 12 llama `iol_client.client._request("GET", path)` DIRECTO (NO el wrapper) para capturar el envelope crudo y verificar `"titulos" in data`. Acceptance criterion verifica que `_request(` aparece en el código del field_type_map. |
| T-3-18 | Denial of Service | Multiple asyncio.run() reabre el client después de aclose | mitigate | D-IOL-6 + Anti-Pattern del RESEARCH: UN único asyncio.run. Acceptance criterion verifica que `asyncio.run(` aparece exactamente 1 vez. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes externos en este plan. Solo se usan paquetes ya pinneados en `uv.lock`. Package Legitimacy Audit de 03-RESEARCH.md sección "Package Legitimacy Audit" — vacía intencional. |
</threat_model>

<verification>
Whole-plan integration:

```bash
# 1. Sintaxis y firmas correctas (los 15 probes + helpers presentes)
uv run python -c "import main_iol; probes = ['probe_login_sync','probe_login_async','probe_get_quote_sync','probe_get_quote_async','probe_get_historical_quotes_sync','probe_get_historical_quotes_async','probe_get_instruments_sync','probe_get_instruments_async','probe_get_instruments_by_type_sync','probe_get_instruments_by_type_async','probe_parity_sync_async','probe_field_type_map','probe_schema_snapshot','probe_refresh_token','probe_auth_401','main','_last_business_day','_next_fid','_async_main']; [getattr(main_iol, p) for p in probes]; print('OK')"

# 2. Static checks
uv run mypy main_iol.py
uv run ruff check main_iol.py
uv run ruff format --check main_iol.py

# 3. Source assertions de invariantes seguros
python -c "src = open('main_iol.py').read(); assert 'time.sleep' not in src; assert src.count('asyncio.run(') == 1; assert 'exc.args[0]' not in src; assert 'VERIFY_IOL_BAD_CREDS' in src; assert '_INVALID' in src; assert 'contextlib.suppress' in src; print('OK')"

# 4. Sin regresiones (driver no está en testpaths)
uv run pytest -q
```

NO se corre el driver en este plan (primer live run con commit baseline pasa en Plan 03-03 Task 3.2 con checkpoint humano).

Per Wave 2 merge:
```bash
uv run pytest -q && uv run mypy . && uv run ruff check . && uv run ruff format --check .
```
</verification>

<success_criteria>
- `main_iol.py` reescrito de smoke test a 15 probes nombrados en orden D-IOL-5 (D-IOL-4: probe_auth_401 último)
- Los 15 probes manejan excepciones internamente y producen `ProbeResult(name, status, detail)`; el driver siempre exit 0 (D-04 Phase 2 mirror)
- Cascade SKIPPED implementado vía `_auth_failed: bool` flag (D-IOL-3 Pattern 1 RESEARCH)
- `probe_auth_401` con try/finally para restaurar `IOL_PASSWORD` original (D-IOL-2), opt-in via `VERIFY_IOL_BAD_CREDS=1` (D-IOL-1), single-shot sin retry/sleep/loop, ÚLTIMO en la secuencia (D-IOL-4)
- `probe_refresh_token` verifica el fix IOL-07 in-vivo observando `iol_client.client._refresh_token`, `_token`, `_token_expires_at` antes/después de forzar expiry (D-IOL-11)
- `probe_field_type_map` llama `iol_client.client._request("GET", ...)` directo para el envelope check de `get_instruments_by_type` (Pitfall 2 — evita el envelope swallow del wrapper)
- 4 schema snapshots cubren los 4 endpoints (`get-quote.json`, `get-historical-quotes.json`, `get-instruments.json`, `get-instruments-by-type.json`) con envelope D-21 y D-25 no-overwrite-on-drift (D-IOL-16)
- Sanity check de los 6 InstrumentType con type-only assertion para `get_instruments_by_type` (D-IOL-17)
- Único `asyncio.run(...)` en `main()` envolviendo `_async_main` que termina con `await aio.aclose()` dentro de `contextlib.suppress(Exception)` (D-IOL-6, IN-03 mirror)
- Output stdout: 15 líneas verbatim `PROBE <name>: <status> <detail>` + 1 línea `SUMMARY: PASS=N FAIL=N SKIPPED=N FINDING=N` (D-02 Phase 2 mirror)
- Todos los prints vía `safe_print(text, secrets=[IOL_USER, IOL_PASSWORD, _refresh_token])` con secrets dinámicamente extendidos (D-IOL-7, D-IOL-22 — diferencia clave vs Phase 2: secrets NO vacíos)
- WR-01 mirror: `exc.status_code` typed direct, NUNCA `exc.args[0]` fallback (verificable por `grep "exc.args" main_iol.py` == 0)
- WR-03 mirror: single HTTP call per probe (probes 3-10 una sola llamada al endpoint, excepción documentada: probe 12 hace `_request` directo + probe 9 hace 1 + sanity 6 con comment explícito)
- IN-03 mirror: `contextlib.suppress(Exception)` envuelve `await aio.aclose()`
- mypy --strict sin errores en `main_iol.py`
- ruff check + format sin errores
- Suite completa sin regresiones — el driver NO está en `testpaths`, solo se chequea import OK
- NO se commitea findings markdown ni schemas/ — esos commits son del Plan 03-03
- NO se corre el driver — primera ejecución en vivo es Task 3.2 del Plan 03-03 con checkpoint humano
</success_criteria>

<output>
Crear `.planning/phases/03-iol-verification/03-02-SUMMARY.md` cuando done, registrando:
- Estructura final del driver (signatures definitivas de los 15 probes; tipos de retorno exactos)
- Esquema de propagación de payloads sync entre probes (tuple-return vs cache module-level — Discretion del implementador)
- Decisión sobre `_next_fid()` (counter sequence global persistente o reset al inicio de `main()`)
- Decisión sobre cascade SKIPPED (`_auth_failed` flag único vs flags separados sync/async)
- Cómo `probe_get_instruments_by_type_sync` resolvió la tensión entre WR-03 (single HTTP call) y D-IOL-17 (sanity 6 types) y Pitfall 2 (envelope check)
- Decisión sobre dónde se hace el `_request` directo para el envelope (en probe 12 o en probe 9)
- Discretion del `probe_refresh_token` para detectar éxito del refresh (token change + expires_at + refresh_token preserved)
- Cantidad y clases de findings que `append_finding` puede emitir (lista de call sites en el driver)
- Confirmación de safe_print con secrets dinámicos (orden de evaluación de `_refresh_token` AFTER probes para captura post-login)
- Métrica de duración del plan
</output>
</content>
</invoke>
