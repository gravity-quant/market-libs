---
phase: 14-iol-disk-persistence-sec-01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/iol-client/pyproject.toml
  - verification/test_iol_disk_persistence.py
autonomous: true
requirements: [SEC-01]
requirements_addressed: [SEC-01]

must_haves:
  truths:
    - "platformdirs >=4.0,<5 is declared as a runtime dependency in packages/iol-client/pyproject.toml ONLY"
    - "Negative scope D-T4: NO other package pyproject.toml (ambito, higyrus, matriz, wallets, root) mentions platformdirs"
    - "verification/test_iol_disk_persistence.py exists with 3 CRITICAL merge gates + 8 regression tests (11 total)"
    - "All 11 tests are RED in HEAD per D-P1 idiom — collected by pytest, but fail with AttributeError/ImportError because iol_client._token_cache and Client(token_cache_path=...) do not exist yet"
    - "SENTINEL constant 'REFRESH-TOKEN-SENTINEL-DO-NOT-LEAK-9876543210' present verbatim per SC #1"
    - "Concurrency test uses ThreadPoolExecutor(max_workers=20) per SC #2 verbatim"
    - "Failed-refresh test uses STALE-REFRESH-TOKEN → 401 → FRESH-REFRESH-TOKEN flow per SC #3"
  artifacts:
    - path: "packages/iol-client/pyproject.toml"
      provides: "platformdirs >=4.0,<5 runtime dependency on iol-client ONLY"
      contains: "platformdirs>=4.0,<5"
    - path: "verification/test_iol_disk_persistence.py"
      provides: "3 CRITICAL merge gate tests + 8 BUG-03 × {sync, async} regression tests"
      contains: "REFRESH-TOKEN-SENTINEL-DO-NOT-LEAK-9876543210"
      min_lines: 250
  key_links:
    - from: "verification/test_iol_disk_persistence.py imports"
      to: "iol_client._token_cache module (to be created in Plan 2)"
      via: "from iol_client import _token_cache"
      pattern: "from iol_client import _token_cache"
    - from: "verification/test_iol_disk_persistence.py Client construction sites"
      to: "Client(token_cache_path=...) kwarg (to be added in Plan 2)"
      via: "explicit kwarg in fixture/test body"
      pattern: "token_cache_path="
---

<objective>
Land the Phase 14 tests-first cross-cutting RED set + declare `platformdirs >=4.0,<5` runtime dep on iol-client only. Implements D-P1 idiom (tests-first, RED in HEAD, GREEN by Plans 2-3) and D-T4 single-package scope per CONTEXT.md.

Purpose: Anchor SEC-01 verification surface BEFORE any source modification. The 3 CRITICAL merge gate tests (anti-Pitfalls 7, 8, 9) and 8 regression tests (4 BUG-03 lifecycle paths × {sync, async}) define the contract that Plans 2-3 must satisfy. Per Phase 8 D-21 / Phase 13 D-P1 tests-first carry-forward: tests committed RED give bisect/forensics clarity if a later plan breaks the contract; tests committed GREEN as a side-effect of impl hides regression risk.

Output:
- `packages/iol-client/pyproject.toml`: 1 line added (`"platformdirs>=4.0,<5"`) inside `[project] dependencies`.
- `verification/test_iol_disk_persistence.py`: NEW file, ~300 LOC, 11 tests (3 CRITICAL + 8 regression). All 11 collected and RED in HEAD (collection errors are NOT acceptable — must be runtime failures from missing module/kwarg, not import errors that abort collection of the whole file).
- D-T4 negative grep clean: no other package's pyproject.toml or the root pyproject.toml gains `platformdirs`.
- Commit message per CONTEXT.md `<specifics>`: `feat(verification): cross-cutting iol disk persistence tests + platformdirs dep (SEC-01)`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/REQUIREMENTS.md
@packages/iol-client/pyproject.toml
@packages/iol-client/tests/conftest.py
@verification/test_logging_no_token_leak.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add platformdirs runtime dep to iol-client ONLY (D-T4 single-package scope)</name>
  <files>packages/iol-client/pyproject.toml</files>
  <read_first>
    - packages/iol-client/pyproject.toml (full file — confirm the existing `dependencies` array shape with comma + comment trailing on `tenacity>=9.1.0,<10`)
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<code_context>` "packages/iol-client/pyproject.toml — agrega dep" sub-section (the exact line to add)
    - .planning/research/STACK.md (platformdirs >=4.0,<5 rationale)
  </read_first>
  <action>
    Edit `packages/iol-client/pyproject.toml`. Inside the existing `[project] dependencies = [ ... ]` array, append a new last entry `"platformdirs>=4.0,<5",` (note: closing comma, version pin matches REQUIREMENTS §SEC-01 spec). Place it AFTER the `"tenacity>=9.1.0,<10",  # Phase 8 RELY-01..04 retries — per D-15` line. Do NOT modify any other `[project]` field, do NOT touch `[project.optional-dependencies]`, do NOT modify `[build-system]` / `[tool.hatch.*]`. Add a comment after the new entry: `# Phase 14 SEC-01 disk persistence (XDG/macOS resolution)`.

    Do NOT modify ANY other pyproject.toml file (root, ambito, higyrus, matriz, wallets). Per CONTEXT.md D-T4 negative scope.

    Implements D-T4 per CONTEXT.md `<decisions>` "Plan 1 — Cross-cutting tests + dep" + REQUIREMENTS §SEC-01.
  </action>
  <verify>
    <automated>grep -c 'platformdirs&gt;=4.0,&lt;5' packages/iol-client/pyproject.toml | grep -q '^1$' && for f in pyproject.toml packages/ambito-financiero-client/pyproject.toml packages/higyrus-client/pyproject.toml packages/matriz-client/pyproject.toml packages/wallets-client/pyproject.toml; do test "$(grep -c platformdirs $f)" = "0"; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'platformdirs>=4.0,<5' packages/iol-client/pyproject.toml` returns exactly `1`.
    - Negative (D-T4 iol-only): `grep -c platformdirs pyproject.toml` returns `0`; same for `packages/ambito-financiero-client/pyproject.toml`, `packages/higyrus-client/pyproject.toml`, `packages/matriz-client/pyproject.toml`, `packages/wallets-client/pyproject.toml`.
    - Behavior: `uv sync --all-packages --all-extras --dev --frozen` MAY fail because the lockfile is stale — that is expected; refresh handled at end of Task 3. NOT a failure of this task per se.
    - Quality: `pyproject.toml` is still valid TOML — `python -c "import tomllib; tomllib.loads(open('packages/iol-client/pyproject.toml').read())"` exits 0.
  </acceptance_criteria>
  <done>
    `packages/iol-client/pyproject.toml` declares `platformdirs>=4.0,<5` in `[project] dependencies`; all other pyproject.toml files unchanged on `platformdirs`. Lockfile refresh deferred to Task 3 to keep this commit minimal and reversible.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create verification/test_iol_disk_persistence.py with 11 tests (3 CRITICAL + 8 regression), RED in HEAD</name>
  <files>verification/test_iol_disk_persistence.py</files>
  <read_first>
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<specifics>` (the 3 CRITICAL test shapes verbatim — `test_disk_persistence_never_logs_token`, `test_disk_token_write_under_concurrent_processes`, `test_disk_token_deleted_on_refresh_401`)
    - .planning/research/PITFALLS.md §Pitfall 7, §Pitfall 8, §Pitfall 9, §Pitfall 10 (source of the test design)
    - verification/test_logging_no_token_leak.py (v1.1 LOG-02 existing test — DO NOT modify; mirror its `caplog.set_level(logging.DEBUG, logger="iol_client")` idiom)
    - packages/iol-client/tests/conftest.py (autouse `configure(token=..., token_expires_at=NEVER_EXPIRES)` fixture — Phase 14 tests use isolated `tmp_path` + `monkeypatch.setenv` instead, NOT this conftest, because they live in `verification/` not `packages/iol-client/tests/`)
    - packages/iol-client/src/iol_client/client.py:127-160 (current `Client.__init__` signature — confirms `token_cache_path` kwarg does NOT exist yet, hence RED in HEAD)
    - .planning/REQUIREMENTS.md §SEC-01 (full spec)
  </read_first>
  <behavior>
    - The file must be `pytest` collectable (imports succeed; collection errors abort the whole file). Use `try/except ImportError` around any reference to `iol_client._token_cache` so the module-import succeeds even if `_token_cache` does not exist; tests inside individual functions reference `iol_client._token_cache` directly and FAIL at call time with `AttributeError`/`ModuleNotFoundError`.
    - Test 1 (anti-Pitfall 7 CRITICAL): `test_disk_persistence_never_logs_token` — exercises write-on-rotate → read-on-init → write-fail OSError (parent dir chmod 0o500) → corrupt-file read (`{"version": 1, "refresh_token":` truncated JSON), asserts SENTINEL never appears in any `record.getMessage()`, `repr(record.args or ())`, or any string-valued `record.__dict__` field.
    - Test 2 (anti-Pitfall 9 CRITICAL): `test_disk_token_write_under_concurrent_processes` — 20 distinct tokens, 20 `submit()` to `ThreadPoolExecutor(max_workers=20)`, after `f.result()` propagation reads the file, asserts JSON parses, `version == 1`, `refresh_token in tokens`, and `path.read_bytes() == json.dumps(loaded).encode("utf-8")` (file integrity: no extra bytes, no truncation, no interleaving).
    - Test 3 (anti-Pitfall 8 CRITICAL): `test_disk_token_deleted_on_refresh_401` — seeds disk with `STALE-REFRESH-TOKEN` via `_token_cache.save(path, "STALE-REFRESH-TOKEN", acquired_at=1.0)`; `httpx_mock` rejects `grant_type=refresh_token` with 401 and accepts `grant_type=password` with `{"refresh_token": "FRESH-REFRESH-TOKEN", ...}`; constructs `iol_client.Client(username="u", password="p", token_cache_path=path)`, calls `client._ensure_token()`, asserts the disk file now contains `FRESH-REFRESH-TOKEN`, NOT `STALE-REFRESH-TOKEN`.
    - 8 regression tests (4 BUG-03 paths × {sync, async}):
      - `test_disk_token_loaded_on_init_skips_password_sync` + `_async`: seed disk with valid refresh_token, construct Client, mock IOL to return 200 on `grant_type=refresh_token`, assert refresh path taken (no password grant in `httpx_mock.get_requests()`).
      - `test_disk_token_written_after_successful_refresh_sync` + `_async`: construct Client without seeding disk but with `refresh_token=...` kwarg in `configure()` carrying the seed; mock IOL refresh returning new rotated token; assert disk file contains the FRESH token + valid JSON shape + acquired_at recent.
      - `test_disk_token_preserved_when_no_kwarg_sync` + `_async`: construct Client WITHOUT `token_cache_path` kwarg and WITHOUT `IOL_TOKEN_CACHE_PATH` env var AND with `CI=true` (refuses default-path); seed disk anyway at a separate path; assert the disk file is UNTOUCHED after `_ensure_token()` runs (no read, no write).
      - `test_disk_token_rotated_on_explicit_path_sync` + `_async`: seed disk with old token, construct Client with explicit `token_cache_path=path`, mock IOL refresh returning new token, assert disk file now contains new token (NOT old).
    - All 11 tests are RED in HEAD per D-P1. Document the expected failure mode in a module-level docstring: "Tests are RED in HEAD pending Plans 2-3 implementation; `iol_client._token_cache` module + `Client(token_cache_path=...)` kwarg do not exist yet."
  </behavior>
  <action>
    Create `verification/test_iol_disk_persistence.py`. File MUST start with `from __future__ import annotations` (project convention, mandatory per CLAUDE.md). Module-level docstring explains RED-in-HEAD intent per D-P1.

    Imports (top of file): stdlib `import asyncio`, `import json`, `import logging`, `import os`, `import secrets`, `from concurrent.futures import ThreadPoolExecutor`, `from pathlib import Path`; third-party `import pytest`; project `import iol_client` (only the top-level; `_token_cache` referenced inside test bodies as `iol_client._token_cache` so that ModuleNotFoundError surfaces at TEST execution, not at collection).

    Module-level constant: `SENTINEL = "REFRESH-TOKEN-SENTINEL-DO-NOT-LEAK-9876543210"` (verbatim per CONTEXT.md `<specifics>` SC #1).

    Implement Test 1 verbatim per CONTEXT.md `<specifics>` "D-P2 CRITICAL caplog test shape" — including the 4-phase lifecycle (write-on-rotate, read-on-init, write-fail OSError, corrupt-file read) and the multi-channel sentinel assertion (`record.getMessage()`, `repr(record.args or ())`, all string-valued `record.__dict__` entries).

    Implement Test 2 verbatim per CONTEXT.md `<specifics>` "D-P2 CRITICAL multi-process race test shape" — including the 20-token generation, `ThreadPoolExecutor(max_workers=20)`, `f.result()` propagation, file integrity check `path.read_bytes() == json.dumps(loaded).encode("utf-8")`.

    Implement Test 3 verbatim per CONTEXT.md `<specifics>` "D-P2 CRITICAL failed-refresh cleanup test shape" — using `httpx_mock.add_response(..., match_content=b"grant_type=refresh_token", status_code=401)` BEFORE the `grant_type=password` 200 response. After client._ensure_token() runs, read the disk file via `_token_cache.load(path)` and assert `loaded["refresh_token"] == "FRESH-REFRESH-TOKEN"` AND `loaded["refresh_token"] != "STALE-REFRESH-TOKEN"`.

    Implement the 8 regression tests as described in `<behavior>`. Each uses `tmp_path` for isolation and `monkeypatch.setenv("CI", "false")` to enable default-path resolution where relevant (or `"true"` for the `_preserved_when_no_kwarg` tests). Async tests are `async def` decorated by `@pytest.mark.asyncio` (project pytest-asyncio config has `asyncio_mode = "auto"` per CLAUDE.md so explicit marker is NOT required, but include it defensively for grep-ability). Use `aio` via `from iol_client import aio` and `aio.AsyncClient(token_cache_path=path)`.

    Logger namespace: `caplog.set_level(logging.DEBUG, logger="iol_client")` — covers `iol_client._token_cache` by descendant inheritance (anti-Pitfall 7 mitigation precondition that Plan 2 must satisfy via `logger = logging.getLogger(__name__)`).

    Do NOT modify `verification/test_logging_no_token_leak.py` (v1.1 LOG-02 existing) — Phase 14 tests live in a SEPARATE file per D-P2.

    Implements D-P1 + D-P2 per CONTEXT.md `<decisions>` "Plan 1 — Cross-cutting tests + dep".
  </action>
  <verify>
    <automated>uv run pytest verification/test_iol_disk_persistence.py --collect-only -q 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `verification/test_iol_disk_persistence.py` exists.
    - Source: `grep -c 'from __future__ import annotations' verification/test_iol_disk_persistence.py` returns `1`.
    - Source: `grep -c 'REFRESH-TOKEN-SENTINEL-DO-NOT-LEAK-9876543210' verification/test_iol_disk_persistence.py` returns `>=1` (sentinel must be present verbatim per SC #1).
    - Source: `grep -c 'ThreadPoolExecutor(max_workers=20)' verification/test_iol_disk_persistence.py` returns `>=1` (per SC #2).
    - Source: `grep -c 'STALE-REFRESH-TOKEN' verification/test_iol_disk_persistence.py` returns `>=2` (seed + non-equal assertion); `grep -c 'FRESH-REFRESH-TOKEN' verification/test_iol_disk_persistence.py` returns `>=2`.
    - Test inventory: `uv run pytest verification/test_iol_disk_persistence.py --collect-only -q | grep -c '::test_'` returns exactly `11` (3 CRITICAL + 8 regression).
    - Specific test names present: `grep -c 'def test_disk_persistence_never_logs_token' verification/test_iol_disk_persistence.py` returns `1`; same for `test_disk_token_write_under_concurrent_processes`, `test_disk_token_deleted_on_refresh_401`, `test_disk_token_loaded_on_init_skips_password_sync`, `test_disk_token_loaded_on_init_skips_password_async`, `test_disk_token_written_after_successful_refresh_sync`, `test_disk_token_written_after_successful_refresh_async`, `test_disk_token_preserved_when_no_kwarg_sync`, `test_disk_token_preserved_when_no_kwarg_async`, `test_disk_token_rotated_on_explicit_path_sync`, `test_disk_token_rotated_on_explicit_path_async`.
    - Quality: `uv run ruff check verification/test_iol_disk_persistence.py` exits 0 (S101 ignored in tests per ruff config).
    - Quality: `uv run ruff format --check verification/test_iol_disk_persistence.py` exits 0.
  </acceptance_criteria>
  <done>
    11 tests collected, file lints clean. All tests will FAIL when run (Plan 2-3 turn them GREEN), but collection succeeds (no ImportError at module level).
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm RED-in-HEAD state, refresh uv.lock, commit Plan 1</name>
  <files>uv.lock</files>
  <read_first>
    - uv.lock (top — confirm it is the existing lockfile; will be updated by `uv sync` after platformdirs dep landed)
    - .planning/phases/14-iol-disk-persistence-sec-01/14-CONTEXT.md `<specifics>` commit message pattern: `feat(verification): cross-cutting iol disk persistence tests + platformdirs dep (SEC-01)`
  </read_first>
  <action>
    Step 1: Refresh `uv.lock` so the new `platformdirs` dep resolves. Run `uv lock`. This updates `uv.lock` with the resolved `platformdirs` version and any transitive deps. Do NOT run `uv sync` (which would mutate `.venv/` — the next CI run does that idempotently).

    Step 2: Confirm the RED state by running `uv run pytest verification/test_iol_disk_persistence.py -x --tb=line 2>&1 | tail -30`. Expected output: 11 tests collected, first one fails with `AttributeError: module 'iol_client' has no attribute '_token_cache'` OR `TypeError: Client.__init__() got an unexpected keyword argument 'token_cache_path'` OR `ModuleNotFoundError: No module named 'iol_client._token_cache'`. Document the actual error class observed in the task completion note. Do NOT try to make the tests pass.

    Step 3: Stage `packages/iol-client/pyproject.toml`, `verification/test_iol_disk_persistence.py`, `uv.lock`. Commit with message: `feat(verification): cross-cutting iol disk persistence tests + platformdirs dep (SEC-01)`. Body lines: 3 CRITICAL merge gate tests + 8 regression tests; tests-first RED-in-HEAD per D-P1; platformdirs >=4.0,<5 iol-only per D-T4 (negative grep clean on other 4 pyproject.toml files + root).

    Step 4: Run the D-T4 negative grep verification once more after commit: `for f in pyproject.toml packages/ambito-financiero-client/pyproject.toml packages/higyrus-client/pyproject.toml packages/matriz-client/pyproject.toml packages/wallets-client/pyproject.toml; do echo "$f: $(grep -c platformdirs $f)"; done` — every line MUST show `: 0`.

    Implements D-P1 RED-in-HEAD idiom + D-T4 negative-scope verification.
  </action>
  <verify>
    <automated>uv run pytest verification/test_iol_disk_persistence.py --tb=line 2>&amp;1 | tail -5 | grep -E "(11 failed|failed.*passed)" &amp;&amp; for f in pyproject.toml packages/ambito-financiero-client/pyproject.toml packages/higyrus-client/pyproject.toml packages/matriz-client/pyproject.toml packages/wallets-client/pyproject.toml; do test "$(grep -c platformdirs $f)" = "0" || exit 1; done &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `uv lock` exits 0 (lockfile refresh succeeds).
    - Behavior: `uv run pytest verification/test_iol_disk_persistence.py --tb=line 2>&1 | tail -5` shows 11 failures (or 11 errors), 0 passing. NO collection errors.
    - Negative D-T4: every one of the 4 other-package + root pyproject.toml files has `grep -c platformdirs` returning `0`.
    - Git: HEAD commit message starts with `feat(verification): cross-cutting iol disk persistence tests + platformdirs dep (SEC-01)`.
    - Git: HEAD commit touches exactly 3 files: `packages/iol-client/pyproject.toml`, `verification/test_iol_disk_persistence.py`, `uv.lock` (verify via `git show --stat HEAD | head -20`).
    - Baseline preservation: `uv run pytest --ignore=verification/test_iol_disk_persistence.py 2>&1 | tail -3` shows >=973 passing (Phase 13 baseline; the new file contributes only failures, the rest must remain intact). This is a sanity check that platformdirs dep didn't break import chain.
  </acceptance_criteria>
  <done>
    Plan 1 committed atomically. 11 RED tests in HEAD; D-T4 single-package scope verified; uv.lock refreshed; pre-existing 973 tests still passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → iol_client public API | Tests construct `Client(token_cache_path=...)` and `_token_cache.save/load/delete` directly — the API surface that Plans 2-3 implement is the boundary |
| logging records → operator observability | Anti-Pitfall 7 boundary: any token-bearing data crossing into a `LogRecord` is exfiltration |
| concurrent threads → single disk file | Anti-Pitfall 9 boundary: 20 threads compete for the same path; `fcntl.flock` is the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 (T1) | Information Disclosure | `_token_cache` log sites (NOT YET IMPLEMENTED — Plan 2) | mitigate | `test_disk_persistence_never_logs_token` is RED in HEAD; turns GREEN in Plan 2 when `logger = logging.getLogger(__name__)` (= `iol_client._token_cache`) inherits the v1.1 LOG-02 `RedactingFilter`. Test guards against future log-site regression (any new logger outside `iol_client.*` namespace fails the test). Anti-Pitfall 7. |
| T-14-02 (T2) | Tampering (stale state) | `_refresh()` cleanup-on-401 path (NOT YET IMPLEMENTED — Plan 2) | mitigate | `test_disk_token_deleted_on_refresh_401` is RED in HEAD; turns GREEN in Plan 2 when `Client._refresh` calls `_token_cache.delete(path)` before password fallback. Test asserts disk file contains FRESH-REFRESH-TOKEN, NOT STALE-REFRESH-TOKEN, after the recovery. Anti-Pitfall 8. |
| T-14-03 (T3) | Denial of Service (data corruption) | concurrent disk writes from 20 processes (NOT YET IMPLEMENTED — Plan 2) | mitigate | `test_disk_token_write_under_concurrent_processes` is RED in HEAD; turns GREEN in Plan 2 when `_token_cache.save()` uses `fcntl.flock(LOCK_EX) + os.replace` atomic write-rename. Test asserts `path.read_bytes() == json.dumps(loaded).encode("utf-8")` (byte-equality with parsed roundtrip — guards interleaving, truncation, double-write). Anti-Pitfall 9. |
| T-14-04 (T4) | Information Disclosure (filesystem) | default-path resolution + chmod 0o600 + parent 0o700 + CI refusal (NOT YET IMPLEMENTED — Plan 2) | mitigate | Regression tests `test_disk_token_preserved_when_no_kwarg_sync/async` are RED in HEAD; turn GREEN in Plan 2 when `_resolve_default_path()` returns `None` under `CI=true`. Test asserts disk file at default path is UNTOUCHED. Anti-Pitfall 10. |
| T-14-SC | Tampering (supply chain) | `platformdirs>=4.0,<5` new runtime dep on iol-client | mitigate | Version pin `>=4.0,<5` per RESEARCH.md STACK; widely-used (pip, virtualenv, black, ipython all consume it); transient surface verified via `uv lock` resolution. Not flagged as ASSUMED/SUS/SLOP — platformdirs is in the top 1k PyPI by downloads. |
</threat_model>

<verification>
- All 11 tests collected by pytest; 11 fail at HEAD (RED-in-HEAD per D-P1).
- `packages/iol-client/pyproject.toml` declares `platformdirs>=4.0,<5`; no other pyproject.toml does (D-T4).
- `uv.lock` updated; pre-existing 973-test baseline still passes (`uv run pytest --ignore=verification/test_iol_disk_persistence.py` >=973 passing).
- Ruff + ruff-format clean on the new test file.
- `verification/test_logging_no_token_leak.py` (v1.1 LOG-02) is UNCHANGED — scope separation per D-P2.

**Nyquist skip rationale:** No VALIDATION.md exists for Phase 14 — the user opted out of research in this phase (no Validation Architecture artifact required per the planning context note). Coverage of SEC-01 success criteria #1-5 is delivered via the 3 CRITICAL cross-cutting tests + 8 BUG-03 regression tests in `verification/test_iol_disk_persistence.py` (Plan 1) + per-package mocked iol-client test suite (Plans 2-3 acceptance gates). This carries forward the Phase 13 pattern. (Plan 1 is the RED side of the gate; Plan 3 confirms GREEN.)
</verification>

<success_criteria>
- `packages/iol-client/pyproject.toml` declares `platformdirs>=4.0,<5` ONLY on iol-client (D-T4 negative grep clean × 4 other pyproject.toml + root).
- `verification/test_iol_disk_persistence.py` exists with 11 tests (3 CRITICAL + 8 regression).
- All 11 tests RED in HEAD; collection succeeds.
- `uv.lock` refreshed.
- Plan 1 committed atomically per CONTEXT.md commit message.
- Pre-existing 973-test baseline preserved (the new RED tests are NET-ADDITIVE failures only).
</success_criteria>

<output>
Create `.planning/phases/14-iol-disk-persistence-sec-01/14-01-SUMMARY.md` when done.
</output>
