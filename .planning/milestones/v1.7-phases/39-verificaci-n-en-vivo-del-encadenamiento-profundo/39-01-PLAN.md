---
phase: 39-verificaci-n-en-vivo-del-encadenamiento-profundo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main_matriz.py
  - main_higyrus.py
  - verification/test_main_verify_classification.py
  - verification/test_main_matriz_skip_line_shape.py
  - verification/test_main_higyrus_skip_line_shape.py
  - .github/workflows/ci.yml
autonomous: false
requirements: [LIVE-NOBJ-01]

must_haves:
  truths:
    - "Con PRIMARY_BASE_URL apuntando al sandbox bbsa confirmado por el operador, main_matriz.py deja pasar la corrida (no aborta) — D-02"
    - "Con un host fuera del allowlist (incluido un sufijo hostil tipo <host-conocido>.attacker.example y la variante userinfo), main_matriz.py imprime en stdout una línea SKIPPED con causa medida y destino LIVE-MATZ-33, y sale con código 0 — D-01/D-02"
    - "main_verify.py clasifica esa corrida como SKIPPED (no FAILED) sin que main_verify.py cambie una sola línea"
    - "Cuando el host vendor de higyrus no resuelve por DNS, main_higyrus.py imprime SKIPPED con destino LIVE-HIGY-33, sale 0 y NO escribe ningún finding nuevo — D-01"
    - "La línea colon-free del mutation gate sigue sin ser clasificada como SKIPPED"
  artifacts:
    - path: "verification/test_main_verify_classification.py"
      provides: "Contrato de clasificación de _run_driver pinneado (stdout-only, colon load-bearing, 6 drivers)"
      contains: "from main_verify import _ENV_SKIP"
    - path: "verification/test_main_matriz_skip_line_shape.py"
      provides: "Lock de forma de la línea SKIPPED de matriz + allowlist de host exacto"
      contains: "import main_matriz"
    - path: "verification/test_main_higyrus_skip_line_shape.py"
      provides: "Lock de forma de la línea SKIPPED de higyrus"
      contains: "from main_verify import _ENV_SKIP"
    - path: ".github/workflows/ci.yml"
      provides: "Allowlist explícita de verification/ ampliada con los 3 archivos nuevos"
      contains: "test_main_verify_classification.py"
  key_links:
    - from: "main_matriz.py"
      to: "main_verify.py"
      via: "línea SKIPPED emitida en stdout que _ENV_SKIP matchea"
      pattern: "SKIPPED \\{_PKG\\}:"
    - from: "main_higyrus.py"
      to: "main_verify.py"
      via: "línea SKIPPED emitida en stdout que _ENV_SKIP matchea"
      pattern: "SKIPPED \\{_PKG\\}:"
    - from: "verification/test_main_matriz_skip_line_shape.py"
      to: ".github/workflows/ci.yml"
      via: "nombre de archivo agregado a la lista explícita del job test"
      pattern: "test_main_matriz_skip_line_shape"
---

<objective>
Corregir la clasificación PASS/SKIPPED/FAILED de los dos bloqueos heredados (D-01) y
ampliar el gate anti-prod D-MATZ-33 a un allowlist explícito de hosts conocidos-seguros
que admite el sandbox `bbsa.matrizoms.com.ar` (D-02).

Este plan es Wave 0 en el sentido de RESEARCH: **debe aterrizar antes de cualquier corrida
en vivo**. Correr antes escribe basura en ledgers versionados y puede quemar baselines
write-once.

Purpose: SC-1 exige "PASS o SKIPPED con causa medida y destino nombrado, nunca como cero".
Hoy matriz sale `FAILED` (ABORT a stderr + exit 1, que `_ENV_SKIP` no matchea) y higyrus
sale `RAN` (falso limpio: el `ConnectError` de DNS se absorbe como finding `AUTH OPEN`).
Además D-02 desbloquea la corrida en vivo de matriz, que es la precondición de D-05.

Output: gate de host por igualdad exacta de hostname en `main_matriz.py`, rama de
inalcanzabilidad en `main_higyrus.py`, y tres locks nuevos bajo `verification/` cableados
a la allowlist de `ci.yml`.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-RESEARCH.md
@.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Checkpoint de seguridad — ampliación del allowlist D-MATZ-33 (D-02)</name>
  <files>(ninguno — checkpoint humano bloqueante, no escribe código)</files>
  <read_first>
    - `.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md` sección
      D-02 — la decisión firmada del operador y su alcance exacto.
    - `.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-RESEARCH.md` sección
      "Security Domain" — la tabla de patrones de amenaza conocidos y su estado, y la nota de que
      `mode: yolo` + `auto_advance: true` no colapsan este checkpoint.
    - `verification/mutation_gate.py:85-131` — el segundo gate independiente que mantiene el order
      entry fail-closed bajo bbsa **sin cambio de código**, y los comentarios que documentan la
      clase de ataque de sufijo hostil y la variante userinfo.
    - `.planning/ROADMAP.md:38` — la nota del milestone sobre la política que "no se rodea", de la
      que D-02 es la resolución parcial explícitamente autorizada para el caso bbsa.
  </read_first>
  <action>
    Presentar al operador el alcance exacto del cambio antes de escribir una línea de código:
    qué host se agrega, por qué forma (igualdad exacta de hostname), qué **no** cambia
    (`verification/mutation_gate.py`, la prohibición general de rodear la política para hosts no
    confirmados) y cuál es el riesgo residual declarado. Esperar respuesta explícita. No derivar la
    aprobación de la configuración de avance automático. Transcribir la respuesta verbatim en el
    SUMMARY del plan y detener el plan si la respuesta no es una aprobación.
  </action>
  <what-built>
    Nada todavía. Este checkpoint precede al cambio de código y es **bloqueante e
    incolapsable**: `mode: yolo` y `auto_advance: true` están activos en
    `.planning/config.json` y NO aplican acá (precedente D-08 / D-18 de fases anteriores;
    RESEARCH "Security Domain" lo declara explícitamente).
  </what-built>
  <how-to-verify>
    1. El operador ya firmó esta decisión el 2026-08-29 (registrada en `39-CONTEXT.md` D-02
       y en la memoria de sesión `project_matriz_bbsa_sandbox.md`): `api.bbsa.matrizoms.com.ar`
       es un sandbox real, seguro, no-producción, distinto de remarkets, con `login()` y
       `get_segments()` ya verificados ahí.
    2. Confirmar que la firma sigue vigente y que el cambio que se va a aplicar es el que
       se firmó: allowlist por **igualdad exacta de hostname**, no substring, no `endswith`,
       no debilitamiento del check.
    3. Confirmar que `verification/mutation_gate.py` **no se toca** — su `_SANDBOX_HOST`
       remarkets-only es lo que mantiene el order entry fail-closed automáticamente bajo bbsa.
    4. Confirmar que la ampliación es sólo para bbsa: la prohibición P-05 de rodear la
       política para hosts no confirmados sigue vigente sin cambios.
    5. Riesgo residual declarado (A1 del Assumptions Log de RESEARCH): la seguridad del
       sandbox bbsa es una aserción del operador, no verificable por máquina. Es la mayor
       dependencia de confianza de esta fase.
  </how-to-verify>
  <resume-signal>Escribir "approved" para habilitar el cambio de allowlist, o describir objeciones</resume-signal>
  <verify>
    <automated>test -f .planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md &amp;&amp; grep -q "bbsa.matrizoms.com.ar" .planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - El operador responde explícitamente en esta sesión; la aprobación NO se deriva de
      `auto_advance` ni del `mode: yolo`.
    - La respuesta queda transcrita verbatim en el SUMMARY del plan.
  </acceptance_criteria>
  <done>Aprobación humana explícita registrada, o el plan se detiene acá.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: matriz — allowlist por hostname exacto + línea SKIPPED en stdout (D-02 + D-01 mitad matriz)</name>
  <files>main_matriz.py, verification/test_main_matriz_skip_line_shape.py, verification/test_main_verify_classification.py, .github/workflows/ci.yml</files>
  <read_first>
    - `main_matriz.py:2540-2600` — `require_env`, construcción del `Client`, y el gate actual
      D-MATZ-33 (`base = client._state.base_url`, chequeo por pertenencia de substring,
      `print(..., file=sys.stderr)`, `sys.exit(1)`), más el orden canónico
      `write_findings` &lt; `_seed_fid_counter` &lt; primer probe que debe preservarse.
    - `main_matriz.py:2686-2712` — el finding terminal `EXPECTED` con
      `title="prod-vs-remarkets divergence acknowledged"` e `idempotent_by_title=True`
      (Pitfall 5: su texto y su cita a `REQUIREMENTS.md` quedan falsos con D-02).
    - `main_matriz.py:107,133-150` — `_SCHEMA_DIR` y `_SCHEMA_FILES` (sólo para ubicar dónde
      vivirá el token de venue en el plan 39-06; este plan NO los toca).
    - `main_verify.py` completo (102 líneas) — `_DRIVERS` (seis entradas), `_ENV_SKIP`
      (`^SKIPPED \S.*:`), y `_run_driver` que escanea **solo** `result.stdout`.
    - `verification/env_gate.py:32-41` — la forma verbatim `SKIPPED &lt;pkg&gt;: missing ...`
      emitida con `print`.
    - `verification/mutation_gate.py:85-131` — `_SANDBOX_HOST`, `mutating_allowed()`, y los
      comentarios que documentan la clase de ataque de sufijo hostil y la variante userinfo.
      **Archivo de sólo lectura en esta fase.**
    - `verification/test_main_market_data_skip_line_shape.py` completo — analog exacto:
      `from main_verify import _ENV_SKIP`, `_module_str_constants`, `_render`,
      `_print_payloads`, `_PRINTERS`, `_HOSTILE`, `_MIN_PRINT_SITES`, y el control positivo.
    - `verification/test_main_matriz_risk_envelope_keys.py:1-45` — precedente de test bajo
      `verification/` que hace `import main_matriz` y ya está en la allowlist de CI.
    - `.github/workflows/ci.yml:70-85` — la lista explícita de 4 archivos y el comentario que
      explica por qué `verification/` nunca corre como directorio.
  </read_first>
  <behavior>
    Predicado de venue (nueva función a nivel de módulo en `main_matriz.py`, anotada para
    mypy strict, testeable por import):
    - `api.remarkets.primary.com.ar` → token `remarkets`
    - `api.bbsa.matrizoms.com.ar` → token `bbsa`
    - `api.bbsa.matrizoms.com.ar.attacker.example` → `None` (rechazado)
    - `https://api.bbsa.matrizoms.com.ar@attacker.example` → `None` (rechazado: el hostname
      real es `attacker.example`)
    - `api.primary.com.ar` (producción) → `None` (rechazado)
    - `https://api.bbsa.matrizoms.com.ar/` con barra final → token `bbsa` (el estado del
      cliente ya viene `rstrip("/")`-normalizado, pero el predicado no debe depender de eso)
    Clasificación:
    - un stdout que contiene la línea SKIPPED nueva ⇒ `_run_driver` devuelve `"SKIPPED"`
    - `SKIPPED (mutating, guard off)` (sin dos puntos) ⇒ NO clasifica como SKIPPED
    - `returncode != 0` sin línea SKIPPED ⇒ `"FAILED"`; en cualquier otro caso ⇒ `"RAN"`
    - `_DRIVERS` sigue teniendo seis entradas y market-data / wallets no cambian de clase
  </behavior>
  <action>
    RED primero: escribir `verification/test_main_matriz_skip_line_shape.py` y
    `verification/test_main_verify_classification.py` antes de tocar `main_matriz.py`, correrlos
    y confirmar que fallan por la razón esperada.

    `verification/test_main_matriz_skip_line_shape.py`: copiar la estructura de
    `verification/test_main_market_data_skip_line_shape.py` — importar `_ENV_SKIP` desde
    `main_verify` (nunca re-declarar el regex), y reutilizar verbatim los helpers
    `_module_str_constants`, `_render` y `_print_payloads` con `_PRINTERS = frozenset({"safe_print", "print"})`
    y un valor hostil interpolado en cada hueco de f-string. **Invertir la aserción final
    respecto del analog**: el analog exige que ninguna línea del driver matchee `_ENV_SKIP`;
    acá se exige que matchee **exactamente una** línea renderizada, que sea la línea D-01 de
    matriz, y que la línea colon-free del mutation gate siga sin matchear. Conservar el
    control positivo (`_ENV_SKIP` sí matchea la forma del env-gate) para que el guard no sea
    vacuo. Agregar además tests de la tabla de `<behavior>` sobre el predicado de venue:
    `import main_matriz`, llamar al predicado con cada base URL de la tabla y assertear el
    token o `None`. Sobre `main_matriz` importado, assertear también que la constante del
    allowlist tiene exactamente dos entradas y que sus claves son los dos hostnames conocidos.

    `verification/test_main_verify_classification.py`: `from main_verify import _ENV_SKIP, _DRIVERS`
    y `_run_driver` no se invoca (lanza subprocesos); en su lugar pinear el contrato con
    aserciones sobre `_ENV_SKIP` y sobre `_DRIVERS`: (a) la forma del env-gate matchea;
    (b) la forma D-01 de matriz matchea; (c) la forma D-01 de higyrus matchea; (d) la línea
    colon-free del mutation gate no matchea; (e) una línea `SUMMARY:` que contenga el texto
    hostil no matchea; (f) `len(_DRIVERS) == 6` y los slugs incluyen `market-data-client` y
    `wallets-client` (el docstring del módulo dice "cinco" y está desactualizado — assertear
    el hecho, no el docstring). Documentar en el docstring del test que sólo se escanea
    `stdout`, que los dos puntos son load-bearing y que por eso la línea de matriz se mueve
    de stderr a stdout.

    GREEN: en `main_matriz.py`, agregar a nivel de módulo una constante de allowlist que mapea
    hostname exacto → token de venue con exactamente dos entradas
    (`api.remarkets.primary.com.ar` → `remarkets`, `api.bbsa.matrizoms.com.ar` → `bbsa`) y una
    función anotada que recibe la base URL, extrae el hostname con `urllib.parse.urlsplit(...).hostname`
    y devuelve el token del allowlist o `None`. Un comentario sobre la constante debe registrar:
    fuente de la autorización (decisión del operador, 2026-08-29, D-02), que la comparación es
    por igualdad exacta de hostname y no por pertenencia de substring ni por sufijo, y que
    `verification/mutation_gate.py` mantiene el order entry fail-closed bajo bbsa sin cambios.
    Reemplazar el gate D-MATZ-33 de `main()` por: resolver el token de venue; si es `None`,
    emitir con `print` a **stdout** la línea `SKIPPED {_PKG}: base URL fuera del allowlist D-MATZ-33 — LIVE-MATZ-33`
    y `sys.exit(0)`. La línea **no** debe interpolar la base URL ni el hostname (Open Question 5
    de RESEARCH: emitir el veredicto de política y el destino, no el dato). El gate sigue
    ubicado donde está hoy: después de construir el `Client` y **antes** de `write_findings(_PKG)`,
    de modo que la rama de skip no escribe ningún finding.

    Pitfall 5, en el mismo commit: el finding terminal `EXPECTED` cuyo `expected` afirma que la
    verificación está limitada al sandbox remarkets por política y cita una fila de
    `REQUIREMENTS.md` que ya no existe queda falso bajo D-02. Reescribir su `title`, `expected`
    y `actual` para que digan lo que ahora es cierto: la verificación corre contra el venue
    resuelto por el allowlist D-MATZ-33 (nombrar el token de venue, no la URL), la forma de
    producción sigue sin verificarse, y la autorización es la decisión de operador de D-02.
    Como usa `idempotent_by_title=True`, un título nuevo crea un finding **nuevo**: dejar
    explicitado en el `<action>` del SUMMARY que el finding superseded queda en el ledger y
    debe recibir disposición explícita en el plan 39-07 (no borrarlo).

    Cerrar el commit agregando `verification/test_main_verify_classification.py` y
    `verification/test_main_matriz_skip_line_shape.py` a la lista explícita del job `test` de
    `.github/workflows/ci.yml` (el bloque `uv run pytest -q \` con los 4 archivos actuales).
    Un lock bajo `verification/` que no está en esa lista es inerte — ya pasó una vez en esta
    repo (Phase 36 WR-01).
  </action>
  <verify>
    <automated>uv run --frozen python -m pytest -q verification/test_main_matriz_skip_line_shape.py verification/test_main_verify_classification.py &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; uv run --frozen mypy &amp;&amp; grep -c test_main_matriz_skip_line_shape.py .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen python -m pytest -q verification/test_main_matriz_skip_line_shape.py verification/test_main_verify_classification.py` sale 0.
    - `grep -c 'test_main_verify_classification.py' .github/workflows/ci.yml` devuelve al menos 1
      y `grep -c 'test_main_matriz_skip_line_shape.py' .github/workflows/ci.yml` devuelve al menos 1.
    - `uv run --frozen python -m pytest -q verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py` (la allowlist previa) sigue saliendo 0.
    - `git diff --name-only` para este commit NO incluye `verification/mutation_gate.py` ni `main_verify.py`.
    - `uv run --frozen ruff check .`, `ruff format --check .` y `mypy` salen 0.
  </acceptance_criteria>
  <done>
    matriz clasifica SKIPPED (no FAILED) cuando el host queda fuera del allowlist, corre normal
    bajo bbsa y bajo remarkets, y los dos locks nuevos corren en CI.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: higyrus — vendor inalcanzable se reporta SKIPPED, no como finding (D-01 mitad higyrus)</name>
  <files>main_higyrus.py, verification/test_main_higyrus_skip_line_shape.py, .github/workflows/ci.yml</files>
  <read_first>
    - `main_higyrus.py:137-152` — la tupla `_RESIDUAL_PROBE_EXCEPTIONS` (incluye
      `httpx.HTTPError`, del que `httpx.ConnectError` es subclase) y el comentario que explica
      que el catch-all fue escrito para residuales **inesperados**.
    - `main_higyrus.py:587-663` — `probe_login_sync`: la escalera `HigyrusAPIError` →
      `_RESIDUAL_PROBE_EXCEPTIONS`, ambas ramas llamando `append_finding(class_="AUTH", status="OPEN")`
      y seteando `_auth_failed`.
    - `main_higyrus.py:665-720` — `probe_login_async`, espejo byte-paralelo del anterior.
    - `main_higyrus.py:2720-2760` — el bloque `with divergence_capture(("higyrus_client",), ...)`
      de `main()` y el orden `write_findings` / `_seed_fid_counter` / primer probe.
    - `main_higyrus.py:2840-2855` — la línea `SUMMARY: ... DIVERGENCES=... HANDLER_ERRORS=...`.
    - `verification/test_cycle_closure_phase33.py:226-250` — la rama de exención de higyrus, que
      assertea `"OPEN" not in statuses`: un finding `AUTH OPEN` nuevo la enrojece.
    - `verification/env_gate.py:32-41` — forma verbatim de la línea SKIPPED, emitida con `print`.
    - `verification/test_main_market_data_skip_line_shape.py` completo — analog del lock de forma.
  </read_first>
  <behavior>
    - `httpx.ConnectError` levantado por `client.login()` (el caso DNS `gaierror` medido en esta
      sesión de research) ⇒ el probe devuelve `ProbeResult("login_sync", "SKIPPED", ...)` con
      causa medida, **sin** llamar `append_finding`, y marca un flag de módulo de
      inalcanzabilidad del vendor.
    - Mismo comportamiento en el espejo async (`probe_login_async`), con `surface="async"`.
    - Un `HigyrusAPIError` (rechazo real del vendor) sigue produciendo el finding `AUTH OPEN`
      de siempre: la rama nueva **no** debe tragarse rechazos.
    - Con el flag de inalcanzabilidad activo, `main()` imprime a stdout
      `SKIPPED higyrus-client: vendor host unreachable (DNS) — LIVE-HIGY-33` y sale con código 0.
    - `httpx.ConnectTimeout` **no** entra por la rama nueva (no es subclase de `ConnectError`):
      sigue cayendo en el bracket residual.
  </behavior>
  <action>
    RED primero: escribir `verification/test_main_higyrus_skip_line_shape.py` copiando la
    estructura de `verification/test_main_market_data_skip_line_shape.py` (importar `_ENV_SKIP`
    desde `main_verify`, reutilizar `_module_str_constants` / `_render` / `_print_payloads` y el
    valor hostil), con la aserción invertida igual que en la Task 2: exactamente una línea
    renderizada de `main_higyrus.py` matchea `_ENV_SKIP`, esa línea es la línea D-01 de higyrus,
    y el control positivo del env-gate se conserva. Agregar además, en el mismo archivo, una
    aserción estructural por `ast` sobre `main_higyrus.py`: dentro de `probe_login_sync` y de
    `probe_login_async` existe un handler `except` cuyo tipo capturado nombra `ConnectError`, y
    ese handler aparece **antes** (índice menor en `node.handlers`) que el handler que captura
    `_RESIDUAL_PROBE_EXCEPTIONS`, y su cuerpo no contiene ninguna llamada a `append_finding`.

    GREEN: en `main_higyrus.py`, agregar una global de inalcanzabilidad del vendor (booleano,
    anotada, inicializada en `False`, con el mismo estilo que `_auth_failed`) y una global de
    causa medida. En `probe_login_sync`, insertar un handler `except httpx.ConnectError as exc:`
    **antes** del handler de `_RESIDUAL_PROBE_EXCEPTIONS` (y después del de `HigyrusAPIError`)
    que: setea las dos globales nuevas más `_auth_failed` con su razón, **no** llama
    `append_finding`, y devuelve `ProbeResult("login_sync", "SKIPPED", "vendor host unreachable (DNS)")`.
    Espejar exactamente en `probe_login_async` con `ProbeResult("login_async", ...)` — el espejo
    sync/async es obligatorio por CLAUDE.md y por D-08.

    En `main()`, inmediatamente después de que el resultado del login sync se agrega a `results`
    y antes de que corran los probes siguientes, chequear el flag: si el vendor es inalcanzable,
    emitir con `print` a stdout la línea `SKIPPED {_PKG}: vendor host unreachable (DNS) — LIVE-HIGY-33`
    y `sys.exit(0)`. La salida debe ocurrir dentro del bloque `with divergence_capture(...)` sólo
    si eso deja el `finally` del context manager restaurando los loggers; si no, romper el bloque
    y salir después. No emitir el hostname ni la base URL en la línea. Ningún `append_finding`
    puede correr en este camino: un finding `AUTH OPEN` nuevo enrojece
    `verification/test_cycle_closure_phase33.py` (rama de exención de higyrus).

    Cerrar el commit agregando `verification/test_main_higyrus_skip_line_shape.py` a la lista
    explícita del job `test` de `.github/workflows/ci.yml`.
  </action>
  <verify>
    <automated>uv run --frozen python -m pytest -q verification/test_main_higyrus_skip_line_shape.py verification/test_main_verify_classification.py &amp;&amp; uv run --frozen python -m pytest -q packages/higyrus-client &amp;&amp; uv run --frozen ruff check . &amp;&amp; uv run --frozen mypy</automated>
  </verify>
  <acceptance_criteria>
    - `uv run --frozen python -m pytest -q verification/test_main_higyrus_skip_line_shape.py` sale 0.
    - `uv run --frozen python -m pytest -q packages/higyrus-client` sale 0 (sin regresión de paquete).
    - `grep -c 'test_main_higyrus_skip_line_shape.py' .github/workflows/ci.yml` devuelve al menos 1.
    - `git diff .planning/verification/higyrus-client-findings.md` está vacío tras este commit
      (la rama nueva no escribe findings).
    - `uv run --frozen ruff check .` y `uv run --frozen mypy` salen 0.
  </acceptance_criteria>
  <done>
    Un fallo de DNS del vendor higyrus se reporta como SKIPPED con causa medida y destino
    LIVE-HIGY-33, y un rechazo real del vendor sigue produciendo su finding AUTH.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` (`PRIMARY_BASE_URL`) → `main_matriz.py` gate | Un valor de entorno controla contra qué venue corre el sweep; es el único input que decide si la corrida toca un sandbox o algo que no lo es |
| `main_matriz.py` / `main_higyrus.py` stdout → `main_verify.py` clasificador | El hijo emite una línea que el runner interpreta como veredicto de política |
| driver → `.planning/verification/*-findings.md` | Artefactos versionados públicos y permanentes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Spoofing / Elevation | gate D-MATZ-33 en `main_matriz.py` | mitigate | Allowlist por **igualdad exacta de hostname** vía `urlsplit(...).hostname`, con exactamente dos entradas. Rechaza el sufijo hostil (`<host-conocido>.attacker.example`) y la variante userinfo (`https://<host-conocido>@attacker.example`). Pinneado por los casos de `<behavior>` de la Task 2. Nunca pertenencia de substring ni `endswith` |
| T-39-02 | Elevation of Privilege | orden entry bajo un venue nuevo | mitigate (verificado, sin cambio de código) | `verification/mutation_gate.py` compara `_SANDBOX_HOST` por hostname exacto remarkets-only ⇒ bajo bbsa las mutaciones quedan fail-closed automáticamente. **Archivo intocable en esta fase.** Además, la lista `sweep_probes` de matriz no contiene `new_order` / `replace_order` / `cancel_order` |
| T-39-03 | Elevation of Privilege | host desconocido | mitigate | Fail-closed por diseño: hostname que no está en el allowlist ⇒ SKIPPED + `sys.exit(0)` **antes** de `write_findings` y antes del primer probe. La ampliación es sólo para bbsa; la prohibición P-05 sigue vigente |
| T-39-04 | Information Disclosure | línea SKIPPED promovida de stderr a stdout | mitigate | La línea nueva emite veredicto de política + destino nombrado, **sin** interpolar base URL ni hostname (Open Question 5 de RESEARCH). El texto es una constante renderizable, verificada por el lock de forma |
| T-39-05 | Repudiation | rama de inalcanzabilidad de higyrus que traga un rechazo real | mitigate | El handler nuevo captura sólo `httpx.ConnectError`, se ubica **después** del handler de `HigyrusAPIError` y **antes** del bracket residual; el orden y la ausencia de `append_finding` en su cuerpo se assertean por `ast` en el lock de forma |
| T-39-06 | Tampering | guard nuevo bajo `verification/` que nunca corre | mitigate | Los tres archivos nuevos se agregan a la allowlist explícita de `.github/workflows/ci.yml` en la misma tarea que los crea; acceptance criteria lo verifica por `grep -c` |
| T-39-SC | Tampering | instalaciones npm/pip/cargo | accept | Esta fase instala **cero** paquetes externos (RESEARCH "Package Legitimacy Audit"). Si un plan propone una dependencia, es señal de drift, no de necesidad |
</threat_model>

<verification>
- La corrida de matriz con `PRIMARY_BASE_URL` apuntando a bbsa no aborta y llega a los probes.
- Una corrida de matriz con un host fuera del allowlist emite la línea SKIPPED en stdout, sale 0,
  y `main_verify.py` la clasifica `SKIPPED` sin haber cambiado.
- `verification/mutation_gate.py` y `main_verify.py` quedan byte-idénticos.
- Los 4 archivos previos de la allowlist de CI más los 3 nuevos corren verdes juntos.
</verification>

<success_criteria>
- `uv run --frozen python -m pytest -q verification/test_main_verify_classification.py verification/test_main_matriz_skip_line_shape.py verification/test_main_higyrus_skip_line_shape.py verification/test_main_market_data_deep_chain.py verification/test_safemodel_diff_null_object_links.py verification/test_main_matriz_risk_envelope_keys.py verification/test_safemodel_diff_mapping_recursion.py` sale 0.
- `uv run --frozen ruff check . && uv run --frozen ruff format --check . && uv run --frozen mypy` sale 0.
- El checkpoint humano de D-02 está aprobado y transcrito.
</success_criteria>

## Artifacts this phase produces

Creados por **este plan**:

| Símbolo / artefacto | Tipo | Ubicación |
|---|---|---|
| constante de allowlist hostname→venue (2 entradas) | constante de módulo | `main_matriz.py` |
| función de resolución de token de venue desde base URL | función | `main_matriz.py` |
| línea `SKIPPED matriz-client: ... — LIVE-MATZ-33` | salida stdout | `main_matriz.py` |
| global de inalcanzabilidad de vendor + causa medida | globales de módulo | `main_higyrus.py` |
| handler `except httpx.ConnectError` en ambos probes de login | rama de código | `main_higyrus.py` |
| línea `SKIPPED higyrus-client: ... — LIVE-HIGY-33` | salida stdout | `main_higyrus.py` |
| `test_main_verify_classification.py` | archivo de test nuevo | `verification/` |
| `test_main_matriz_skip_line_shape.py` | archivo de test nuevo | `verification/` |
| `test_main_higyrus_skip_line_shape.py` | archivo de test nuevo | `verification/` |
| 3 entradas nuevas en la allowlist explícita del job `test` | config | `.github/workflows/ci.yml` |
| finding terminal `EXPECTED` de matriz reescrito (Pitfall 5) | entrada de ledger | `.planning/verification/matriz-client-findings.md` |

Artefactos de la fase completa (para contexto): ver el mismo encabezado en `39-08-PLAN.md`.

<output>
Crear `.planning/phases/39-verificaci-n-en-vivo-del-encadenamiento-profundo/39-01-SUMMARY.md` al terminar.
</output>
