---
phase: 31-endpoints-de-ops-estructura-uniforme
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tools/check_uniform_structure.py
  - packages/iol-client/src/iol_client/types.py
  - packages/higyrus-client/src/higyrus_client/types.py
  - packages/market-data-client/src/market_data_client/types.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/types.py
  - packages/ambito-financiero-client/src/ambito_financiero_client/models.py
  - packages/wallets-client/src/wallets_client/types.py
  - packages/wallets-client/src/wallets_client/models.py
  - .github/workflows/ci.yml
  - .planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md
autonomous: true
requirements: [TYP-03]

must_haves:
  truths:
    - "All 6 packages carry both `models.py` and `types.py` under `src/<import_name>/` — 7 files are new (D-09: `types.py` in iol, higyrus, market-data, ambito, wallets; `models.py` in ambito and wallets)."
    - "`tools/check_uniform_structure.py` enumerates `packages/*/` FROM DISK, resolves each package's `src/<import_name>/` directory, and fails with a per-package problem list naming every missing file — so a seventh package entering the workspace is caught automatically (criterion 4)."
    - "The check runs in CI as a new step of the EXISTING `lint` job, mirroring the `decode-intactness` step verbatim including its two-line rationale comment (D-12). It is never a pytest file under `verification/` — that directory has never executed in CI."
    - "The check was observed RED before the 7 files existed, listing all 7 by path — non-vacuity proved by observation, not by assertion."
    - "`wallets-client`'s two new modules import NOTHING beyond `from __future__ import annotations` — no `SafeModel`, no `_decode` (that module does not exist there; the import would `ImportError` at package import and redden all 12 wallets CI matrix legs)."
    - "`ambito-financiero-client` gets the same docstring-only treatment (D-11) and neither new module is re-exported into `__init__.__all__`, so `verification/snapshots/ambito-financiero-client-surface.txt` does not move."
    - "`tools/check_decode_intactness.py` stays green and UNEDITED — Check D inspects only `_decode.py` presence/absence, never `models.py`/`types.py` (D-10, verified against `check_d_roster()`)."
    - "All 7 new modules pass `ruff check`, `ruff format --check`, and (for the 5 packages enrolled in mypy `files`) `mypy --strict` — which is why `__all__` carries the explicit `list[str]` annotation rather than a bare empty literal."
    - "The 5 new `types.py` are docstring placeholders with no `Literal` content, because Phase 29's signed D-lock forbids closing RESPONSE fields as `Literal` in this milestone and iol's `mercado`/`plazo` promotion is deferred to Phase 33 (D-09)."
  prohibitions:
    - "MUST NOT ship the uniform-structure check in a form that can report green while a package is missing a file — no hardcoded 6-name package list that goes stale, no silent `continue` on an unresolvable `src/` directory, no bare `except`. A structural gate that cannot fail is worse than no gate."
    - "MUST NOT give `wallets-client` a real `SafeModel` or any import of `_decode` to make the layout look more uniform than it is. The exemption is load-bearing; a cosmetically-uniform wallets that fails at import trades a documented gap for a hidden outage."
    - "MUST NOT edit `tools/check_decode_intactness.py` or its roster constants as part of this plan. The Phase 29 exemption remains valid unedited; touching it here would couple two independent gates."
  artifacts:
    - path: "tools/check_uniform_structure.py"
      provides: "Stdlib-only cross-package existence gate for models.py + types.py"
      min_lines: 60
      contains: "packages"
    - path: "packages/wallets-client/src/wallets_client/models.py"
      provides: "Docstring-only placeholder, zero imports beyond the future flag"
      min_lines: 8
    - path: "packages/wallets-client/src/wallets_client/types.py"
      provides: "Docstring-only placeholder, zero imports beyond the future flag"
      min_lines: 8
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/models.py"
      provides: "Docstring-only placeholder (ambito parses via _parsing.parse_ar_decimal, no models)"
      min_lines: 8
    - path: "packages/ambito-financiero-client/src/ambito_financiero_client/types.py"
      provides: "Docstring-only placeholder"
      min_lines: 8
    - path: "packages/iol-client/src/iol_client/types.py"
      provides: "Docstring-only placeholder; Literal content deferred to Phase 33"
      min_lines: 8
    - path: "packages/higyrus-client/src/higyrus_client/types.py"
      provides: "Docstring-only placeholder"
      min_lines: 8
    - path: "packages/market-data-client/src/market_data_client/types.py"
      provides: "Docstring-only placeholder"
      min_lines: 8
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tools/check_uniform_structure.py"
      via: "new `uniform-structure` step in the existing `lint` job"
      pattern: "check_uniform_structure"
    - from: "tools/check_uniform_structure.py"
      to: "packages/"
      via: "on-disk enumeration of package directories, not a hardcoded roster"
      pattern: "iterdir|glob"
---

<objective>
Give the 6 packages the same file layout so the next endpoint is born with somewhere to
live, and make that layout a CI-enforced invariant rather than a convention.

TYP-03 in full: 7 new module files (5 × `types.py`, 2 × `models.py`), one new stdlib-only
`tools/` gate, one new `lint`-job step. Wholly orthogonal to TYP-02 — this plan shares no
file with any other plan in the phase and runs in parallel with them.

Purpose: without shared code between packages (a deliberate architecture constraint), a
committed gate is the only thing that stops six surfaces diverging over three releases.
The existence check is the cheap half of that; Phase 32 adds the AST surface gate.

Output: a uniform 6-package layout, verifiable by `uv run python tools/check_uniform_structure.py`
in the `lint` job.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-CONTEXT.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md
@.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-PATTERNS.md
@.planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md
</context>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Location |
|---|---|---|
| `tools/check_uniform_structure.py` | new stdlib-only CI gate script | repo `tools/` |
| `CheckFailure` (or equivalent) + `main()` + `raise SystemExit(main())` | script skeleton mirroring `check_decode_intactness.py` | in the new script |
| `packages/iol-client/src/iol_client/types.py` | new docstring-only module | iol |
| `packages/higyrus-client/src/higyrus_client/types.py` | new docstring-only module | higyrus |
| `packages/market-data-client/src/market_data_client/types.py` | new docstring-only module | market-data |
| `packages/ambito-financiero-client/src/ambito_financiero_client/types.py` | new docstring-only module | ambito |
| `packages/ambito-financiero-client/src/ambito_financiero_client/models.py` | new docstring-only module | ambito |
| `packages/wallets-client/src/wallets_client/types.py` | new docstring-only module | wallets |
| `packages/wallets-client/src/wallets_client/models.py` | new docstring-only module | wallets |
| `uniform-structure` step | new step in the existing `lint` job | `.github/workflows/ci.yml` |
| amendment note | 3-line addendum recording that ambito + wallets now carry empty `models.py`/`types.py` | `29-WALLETS-EXEMPTION.md` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — stdlib-only uniform-structure gate that fails on the 7 missing files</name>

  <reversibility rating="reversible">A standalone `tools/` script plus one CI step; removing
  both restores the prior CI surface exactly.</reversibility>

  <files>tools/check_uniform_structure.py, .github/workflows/ci.yml</files>

  <behavior>
    - Run against HEAD (before Task 2): exits 1 and prints a problem list naming all 7 missing files by repo-relative path — `iol_client/types.py`, `higyrus_client/types.py`, `market_data_client/types.py`, `ambito_financiero_client/types.py`, `ambito_financiero_client/models.py`, `wallets_client/types.py`, `wallets_client/models.py`.
    - Run after Task 2: exits 0 and prints one success SENTENCE naming the number of packages checked and the two required module names.
    - A package directory under `packages/` whose `src/<import_name>/` cannot be resolved is reported as a problem, never skipped.
    - A hypothetical seventh package appearing under `packages/` is checked automatically, because the roster comes from disk.
  </behavior>

  <read_first>
    - `tools/check_decode_intactness.py` — read the whole file, but especially `check_d_roster()` (lines ~625-662) for the `problems: list[str]` accumulation with four-space-indented problem lines and the single terminal raise, and `main()` (lines ~668-693) for the checks tuple, the `::error::` stderr prefix, the failure-count summary, and `raise SystemExit(main())`. Also read how `REPO_ROOT` and the `_fail` / `CheckFailure` helpers are defined at the top of the file.
    - `.github/workflows/ci.yml` lines 21-53 — the entire `lint` job, and in particular the `decode-intactness` step at the end, whose name format and three-line rationale comment are the pattern to mirror.
    - `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-RESEARCH.md` § "File Layout Census (criterion 4)" (the exact 6×10 module matrix and the constraints table) and § "Existence-check wiring (D-12)".
    - `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-PATTERNS.md` § "`tools/check_uniform_structure.py` (utility, batch — NEW)" — the code excerpts to copy.
    - `pyproject.toml` line ~97 — the mypy `files` list, to confirm which of the 5 new modules are strict-typechecked.
  </read_first>

  <action>
Create `tools/check_uniform_structure.py`. **Stdlib-only** — `pathlib` and `sys` are the only
imports permitted (D-12; `uv lock --check` is the first `lint` step and must not see a lock
change). No `httpx`, no `tomllib`-driven metadata parsing, no third-party helper.

Structure, copied from `tools/check_decode_intactness.py`:

- Module docstring naming the phase and requirement (`Phase 31, TYP-03`), stating what is
  checked (`models.py` and `types.py` present in every package's import root) and WHY it is a
  `tools/` script in the `lint` job rather than a pytest file: the `test` job passes an
  explicit `packages/${{ matrix.package }}` path that overrides `[tool.pytest.ini_options]
  testpaths`, so `verification/` has never executed in CI; and this check is cross-package by
  nature so it cannot live in the per-package `test` job either.
- `from __future__ import annotations`, then `import sys`, then `from pathlib import Path`.
- `REPO_ROOT = Path(__file__).resolve().parent.parent`.
- `REQUIRED_MODULES = ("models.py", "types.py")`.
- A `CheckFailure(Exception)` class and a `_fail(message)` helper, mirroring the sibling
  script's shapes.
- `_import_root(package_dir: Path) -> Path | None` — resolve `package_dir / "src"`, then take
  the single child directory of `src/` that is a directory and does not end in `.egg-info`. If
  `src/` is absent, or there is not exactly one such child, return `None` so the caller reports
  it as a problem. Do not guess an import name by string-munging the directory name — resolving
  from disk is what makes `ambito-financiero-client` → `ambito_financiero_client` correct
  without a mapping table.
- `check_uniform_structure() -> str` — build `problems: list[str]`; enumerate
  `sorted((REPO_ROOT / "packages").iterdir())` filtered to directories (this on-disk
  enumeration is load-bearing: it is what catches a seventh package automatically); for each,
  resolve the import root and, when it resolves, append a four-space-indented problem line for
  every missing required module, formatted with the path `relative_to(REPO_ROOT)` so the
  message is copy-pasteable. When the import root does not resolve, append a distinct problem
  line saying so — never skip. Also append a problem if `packages/` itself is missing or holds
  zero package directories, so an empty scan can never read as clean. If `problems` is
  non-empty, `raise _fail("uniform structure is incomplete:\n" + "\n".join(problems))`.
  Otherwise return a success SENTENCE naming the package count and the two module names, in
  the same voice as Check D's summary line.
- `main() -> int` — a `checks` tuple containing the one check, the same try/except printing
  `check()` on success and `::error::Phase 31 TYP-03 uniform structure -- {exc}` to stderr on
  `CheckFailure`, a `failures` counter, a terminal
  `::error::uniform-structure gate FAILED (...)` line, and `return 1` / `return 0`.
- `if __name__ == "__main__": raise SystemExit(main())`.

Then edit `.github/workflows/ci.yml`: append ONE new step to the EXISTING `lint` job,
immediately after the `decode-intactness` step (do not create a new job, do not touch the
`test`, `typecheck`, or `pre-commit` jobs). Name it
`uniform-structure (Phase 31 TYP-03 — models.py + types.py en los 6 paquetes)` and carry the
same two-line rationale comment the sibling step carries, in the same Spanish voice: that it
is cross-package by nature so it does not go in the per-package `test` job, and that it cannot
live under `verification/` because the `test` job passes an explicit path that overrides
`testpaths` so that directory never ran in CI. `run: uv run python tools/check_uniform_structure.py`.

**Observe the RED before writing Task 2's files.** Run the script now; it must exit 1 and name
all 7 missing paths. Capture that output verbatim for the SUMMARY — that observation IS the
non-vacuity proof, and it is only available in this window.

Commit this task on its own with a `test(31-02):` prefix so the RED gate is a distinct commit.
  </action>

  <acceptance_criteria>
    - `uv run python tools/check_uniform_structure.py; echo "exit=$?"` prints `exit=1` at this point in the plan, and its stderr/stdout together name all 7 missing paths. Capture the output verbatim.
    - `uv run python tools/check_uniform_structure.py 2>&1 | grep -c 'types.py'` returns at least 5 and `... | grep -c 'models.py'` returns at least 2.
    - The roster is read from disk, not hardcoded: `grep -cE 'iterdir|glob' tools/check_uniform_structure.py` returns at least 1. Additionally, inspect the module's executable lines and confirm no list/tuple/set literal enumerates package directory names — the only string literals present should be `"src"`, the two required module names, and message text.
    - Stdlib-only: `grep -E '^(import|from) ' tools/check_uniform_structure.py` lists only `__future__`, `sys`, and `pathlib`.
    - No silent-skip paths: inspect every `except` clause in the module and confirm each names `CheckFailure` explicitly — no bare handler and no broad `Exception` handler exists in executable code.
    - `grep -c 'check_uniform_structure.py' .github/workflows/ci.yml` returns exactly 1, and it sits inside the `lint` job — confirm by `awk '/^  lint:/,/^  pre-commit:/' .github/workflows/ci.yml | grep -c 'check_uniform_structure.py'` returning 1.
    - `tools/check_decode_intactness.py` is unmodified: `git diff --stat tools/check_decode_intactness.py` is empty.
    - `uv run ruff check tools/ && uv run ruff format --check tools/` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>uv run python tools/check_uniform_structure.py; test $? -eq 1 &amp;&amp; echo "RED confirmed"</automated>
  </verify>

  <done>The gate exists, is wired into the `lint` job, enumerates packages from disk, and was OBSERVED failing with all 7 missing paths listed. Its RED output is captured verbatim for the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — the 7 docstring-only modules that turn the gate green</name>

  <reversibility rating="reversible">Seven near-empty modules, none re-exported into any
  package's public surface; deleting them restores the prior layout with no consumer impact.</reversibility>

  <files>packages/iol-client/src/iol_client/types.py, packages/higyrus-client/src/higyrus_client/types.py, packages/market-data-client/src/market_data_client/types.py, packages/ambito-financiero-client/src/ambito_financiero_client/types.py, packages/ambito-financiero-client/src/ambito_financiero_client/models.py, packages/wallets-client/src/wallets_client/types.py, packages/wallets-client/src/wallets_client/models.py, .planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md</files>

  <read_first>
    - `packages/matriz-client/src/matriz_client/types.py` lines 1-20 — the ONLY existing `types.py`, and the header form (module docstring → blank line → `from __future__ import annotations` → imports → `__all__`) all 7 new modules must follow.
    - `packages/wallets-client/src/wallets_client/` — list it. Confirm there is no `_decode.py`, no `_core.py`, no `_state.py`, no `_logging.py`. This is why the wallets modules import nothing.
    - `packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py` — confirm its `__all__` and that neither new module gets added to it.
    - `verification/snapshots/ambito-financiero-client-surface.txt` — the golden that must not move.
    - `.planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md` — the whole file, especially "What happens when the exemption ends", which names Phase 31 by name.
    - `.planning/phases/29-decoder-observable/29-DLOCK-RESPONSE-LITERAL.md` — the signed D-lock the `types.py` docstrings must cite as the reason they carry no `Literal` content.
    - `pyproject.toml` line ~97 — mypy `files`; higyrus, wallets, matriz, iol and ambito are enrolled, market-data is not (D-13).
  </read_first>

  <action>
Create the 7 modules. Every one of them, without exception:

- opens with a real module docstring (CLAUDE.md C-10),
- carries `from __future__ import annotations` (CLAUDE.md C-5 — mandatory and applied
  uniformly across the monorepo, docstring-only modules included),
- declares `__all__: list[str] = []` with the explicit annotation. The annotation is
  load-bearing, not decoration: an unannotated empty list literal fails `mypy --strict` with
  `var-annotated` in the 5 enrolled packages.

**The 5 `types.py`** (`iol_client`, `higyrus_client`, `market_data_client`,
`ambito_financiero_client`, `wallets_client`) are placeholders by decision, not by oversight.
Their docstrings must say so and must give the reason, citing by path:
`.planning/phases/29-decoder-observable/29-DLOCK-RESPONSE-LITERAL.md` — the signed Phase 29
lock under which RESPONSE fields are never closed as `Literal` in this milestone (they decode
as `str`; out-of-set values are reported, not enforced) — and record that the promotion of
iol's `mercado`/`plazo` to `Literal` is deferred to Phase 33 pending a live census. Name
`matriz_client.types` as the shape these will grow into. Do NOT import `typing.Literal`; an
unused import fails `ruff check`.

**`ambito_financiero_client/models.py`** — docstring states that ámbito parses through
`ambito_financiero_client._parsing.parse_ar_decimal` and declares no response models today;
the module exists so the 6 packages present the same layout and the next endpoint has
somewhere to be born (D-11). No `SafeModel`, no dataclass, no import of `_decode`.

**`wallets_client/models.py` and `wallets_client/types.py`** — the strictest case (D-10,
Pitfall 8). Docstrings state that `wallets-client` is still a stub with no verifiable
endpoints, cite `.planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md`, and record
explicitly that these modules import nothing: the package has no `_decode.py` to import from,
so a real `SafeModel` here would raise `ImportError` at package import and redden all 12
wallets CI matrix legs. Beyond `from __future__ import annotations`, these two files contain
zero import statements.

**None of the 7 modules is added to its package's `__init__.py` or `__all__`.** For ámbito
that is a hard requirement — a re-export would add rows to
`verification/snapshots/ambito-financiero-client-surface.txt`. For the others it keeps this
plan's diff disjoint from the TYP-02 plans, which edit `__init__.py` in higyrus and
market-data.

Now run `uv run python tools/check_uniform_structure.py` — it must exit 0 and print the
success sentence. Then run `uv run python tools/check_decode_intactness.py` and confirm it is
still green WITHOUT editing it: Check D reads only `pkg.decode_path.is_file()` and a
set-difference of `packages/` directory names against its roster, and never opens
`models.py`/`types.py` (D-10).

Finally, append a short amendment note (3 lines, under a `## Amendment` heading) at the end of
`.planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md`, dated, recording that Phase
31 gave `wallets-client` and `ambito-financiero-client` docstring-only `models.py` and
`types.py`, that the exemption is UNCHANGED (it is scoped to `_decode.py`, which neither
package received), and that the document's module table is stale in one further respect
independent of this phase — iol gained a `models.py` in Phase 30. This is the cheap half of
RESEARCH Open Question 4; the formal supersession stays deferred. Do not edit
`tools/check_decode_intactness.py` and do not touch its `EXEMPTION_DOC` constant.

Commit with a `feat(31-02):` prefix so the GREEN gate is a distinct commit from Task 1's RED.
  </action>

  <acceptance_criteria>
    - `uv run python tools/check_uniform_structure.py; echo "exit=$?"` prints `exit=0` and emits a one-line success sentence naming 6 packages.
    - Every package has both modules: `for d in packages/*/src/*/; do test -f "$d/models.py" && test -f "$d/types.py" || echo "MISSING $d"; done` prints nothing.
    - All 7 carry the future import: `grep -l 'from __future__ import annotations' packages/iol-client/src/iol_client/types.py packages/higyrus-client/src/higyrus_client/types.py packages/market-data-client/src/market_data_client/types.py packages/ambito-financiero-client/src/ambito_financiero_client/types.py packages/ambito-financiero-client/src/ambito_financiero_client/models.py packages/wallets-client/src/wallets_client/types.py packages/wallets-client/src/wallets_client/models.py | wc -l` returns 7.
    - All 7 annotate `__all__`: `grep -c '__all__: list\[str\] = \[\]' <each of the 7>` returns 1 for each.
    - wallets' two modules import nothing else: `grep -cE '^(import |from )' packages/wallets-client/src/wallets_client/models.py` returns exactly 1, and the same for `packages/wallets-client/src/wallets_client/types.py`.
    - No placeholder is re-exported: `grep -cE '\b(types|models)\b' packages/ambito-financiero-client/src/ambito_financiero_client/__init__.py` returns 0.
    - The ámbito golden did not move: `git diff --stat verification/snapshots/ambito-financiero-client-surface.txt` is empty.
    - `uv run python tools/check_decode_intactness.py` exits 0 and `git diff --stat tools/check_decode_intactness.py` is empty.
    - `uv run mypy` exits 0 (covers the 4 new modules in the 5 enrolled packages) and `uv run mypy packages/market-data-client/src` exits 0 locally (D-13 acceptance step; CI does not run it).
    - `uv run ruff check . && uv run ruff format --check .` exits 0.
    - `uv run pytest -q` (full local suite INCLUDING `verification/`) exits 0 — this is the only run that exercises the surface goldens, since CI never runs that directory.
    - `.planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md` gained an `## Amendment` section: `grep -c '## Amendment' .planning/phases/29-decoder-observable/29-WALLETS-EXEMPTION.md` returns 1.
  </acceptance_criteria>

  <verify>
    <automated>uv run python tools/check_uniform_structure.py &amp;&amp; uv run python tools/check_decode_intactness.py &amp;&amp; uv run mypy &amp;&amp; uv run pytest -q</automated>
  </verify>

  <done>All 6 packages carry both modules; the gate is green; the decode-intactness gate is green and unedited; ámbito's surface golden is unchanged; mypy strict and ruff are clean; the exemption doc carries a dated amendment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → CI verdict | The `lint` job's exit status is the only mechanical statement that the 6 surfaces have not diverged. A gate that cannot fail converts that statement into noise. |
| new module → package import graph | Every file added under `src/<import_name>/` is imported (directly or by discovery) on the package's CI legs. An import of a module that does not exist in that package is an outage, not a lint error. |
| this plan's diff → Phase 29's committed decode gate | `tools/check_decode_intactness.py` and its exemption roster are an independent, byte-hash-pinned gate. Coupling the two would let one plan's convenience relax the other's guarantee. |

## STRIDE Threat Register (ASVS L1, block_on = high)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Repudiation | `tools/check_uniform_structure.py` reporting green vacuously | mitigate | The roster is enumerated from disk (`packages/` `iterdir`), never hardcoded; an unresolvable `src/` root is a PROBLEM, never a skip; an empty package scan is itself a problem; and the RED state was OBSERVED before the 7 files existed, with the output captured. Acceptance greps forbid a hardcoded package-name literal and forbid bare/`Exception` handlers. |
| T-31-07 | DoS (self-inflicted) | `wallets_client` package import | mitigate | ASVS V7 adjacent. The two wallets modules import nothing beyond the future flag — `wallets-client` has no `_decode.py`, and an import of it would `ImportError` at package import and redden all 12 wallets matrix legs. Asserted by an import-count grep of exactly 1 per file. |
| T-31-08 | Tampering | `verification/snapshots/ambito-financiero-client-surface.txt` | mitigate | ASVS V8. The new ámbito modules are not re-exported into `__init__.__all__`, so the golden cannot move. Asserted by an empty `git diff --stat` on the snapshot, and by the full local `pytest -q` run that includes `verification/` (which CI never runs). |
| T-31-09 | Tampering | Phase 29 decode-intactness gate and its exemption roster | mitigate | This plan is forbidden from editing `tools/check_decode_intactness.py`; verified by an empty `git diff --stat` on it plus a green run. The exemption doc gains a note only, never a roster change. |
| T-31-10 | Info Disclosure | new module docstrings | accept | ASVS V7. The docstrings reference planning-document paths and package names only — no credentials, no hosts, no wire values. `.env` files are untouched. |
| T-31-SC | Tampering | package-manager installs | mitigate | Not applicable — zero installs. D-12 requires the gate be stdlib-only precisely so `uv.lock` does not move; `uv lock --check` is the first `lint` step and would fail if it did. Enforced by an acceptance criterion listing the script's only three imports. |
</threat_model>

## Flagged Assumptions (probe-surfaced, unresolved — not dismissed)

- **TYP-03 / unclassified / unresolved** — the deterministic edge probe classified this
  requirement as `unclassified` and emitted the generic *"review manually"* probe. It is not
  auto-dismissed and no resolution is fabricated here. The manual reading applied while
  planning: the requirement is a file-layout invariant with no runtime, data, or concurrency
  dimension, and its one genuine failure mode — a gate that reports green while a file is
  missing — is addressed above as T-31-06 with an observed RED. **The probe row itself remains
  open.**
- **TYP-02 / concurrency / unresolved** — carried forward; addressed (also unresolved) in plan
  31-01, which owns TYP-02's gates.

<verification>
```
uv run python tools/check_uniform_structure.py     # exit 0, success sentence
uv run python tools/check_decode_intactness.py     # still green, file unedited
uv run mypy                                        # 5 enrolled packages, strict
uv run mypy packages/market-data-client/src        # local-only, D-13
uv run ruff check . && uv run ruff format --check .
uv run pytest -q                                   # FULL suite incl. verification/
git diff --stat tools/check_decode_intactness.py verification/snapshots/   # both empty
```
</verification>

<success_criteria>
- 7 new modules exist, one pair per package across all 6 packages.
- The gate was seen RED with all 7 paths listed, then GREEN.
- The gate is wired into the existing `lint` job, mirroring `decode-intactness`.
- wallets' two modules contain exactly one import statement each.
- No placeholder is re-exported anywhere; ámbito's surface golden is byte-unchanged.
- `check_decode_intactness.py` green and unedited; exemption doc carries a dated amendment.
- mypy strict, ruff check, ruff format, and the full local suite (including `verification/`) all clean.
</success_criteria>

<output>
Create `.planning/phases/31-endpoints-de-ops-estructura-uniforme/31-02-SUMMARY.md` when done.
Record: the verbatim RED output of the gate (all 7 paths), the final success sentence, the
decision made on the exemption-doc amendment, and confirmation that
`tools/check_decode_intactness.py` and the ámbito surface golden are byte-unchanged.
</output>
