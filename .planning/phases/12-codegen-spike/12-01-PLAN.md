---
phase: 12-codegen-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/SPIKE-005-codegen-tool-choice/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/diff_vs_v1.1_client.txt
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/experiment.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/audit.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/FINDING.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/README.md
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/experiment.py
  - .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/FINDING.md
  - .planning/spikes/MANIFEST.md
  - .planning/phases/12-codegen-spike/12-VALIDATION.md
autonomous: false
requirements: [REFAC-06]
tags: [spike, codegen, unasync, ambito, round-trip]
must_haves:
  truths:
    - "v1.1 head 71bf201 confirmed CI-green on Python 3.13 BEFORE any spike experiment runs (operational pre-gate; anti-Pitfall 17)"
    - "SPIKE-005 spike directory exists at .planning/spikes/SPIKE-005-codegen-tool-choice/ with 4 sub-directories (001a/b/c/d) following CONVENTIONS.md naming"
    - ".planning/spikes/MANIFEST.md has a row for SPIKE-005 (id, slug, status=in-progress, created date)"
    - "Ámbito round-trip experiment runs: unasync.unasync_files() invoked against ambito-financiero-client aio.py, output written to spike work dir as client_generated.py"
    - "Generated ambito client_generated.py is byte-identical to packages/ambito-financiero-client/src/ambito_financiero_client/client.py modulo `uv run ruff format` normalization (diff empty)"
    - "B8 identity preserved on generated output: client_generated._raise_for_response is ambito_financiero_client.aio._raise_for_response is ambito_financiero_client._core.raise_for_response"
  artifacts:
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/README.md"
      provides: "Spike entry with frontmatter (spike: 005, type: standard, validates: ...) and verdict link"
      contains: "spike: 005"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py"
      provides: "Standalone Python script invoking unasync.unasync_files() on ámbito aio.py + ruff format + diff + B8 identity check"
      min_lines: 60
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py"
      provides: "Codegen output (unasync token-replacement + ruff format)"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/diff_vs_v1.1_client.txt"
      provides: "diff -u transcript vs v1.1 hand-written client.py"
    - path: ".planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md"
      provides: "Verdict (PASS|FAIL) + hunks classification + draft per-package Rule config for ámbito"
    - path: ".planning/spikes/MANIFEST.md"
      provides: "Registry row for SPIKE-005"
      contains: "SPIKE-005"
  key_links:
    - from: "001a-ambito-round-trip/experiment.py"
      to: "packages/ambito-financiero-client/src/ambito_financiero_client/aio.py"
      via: "shutil.copy → unasync.unasync_files(fpath_list=[work/aio.py])"
      pattern: "unasync\\.unasync_files"
    - from: "001a-ambito-round-trip/experiment.py"
      to: "packages/ambito-financiero-client/src/ambito_financiero_client/client.py"
      via: "subprocess diff -u"
      pattern: "diff.*client\\.py"
    - from: "001a-ambito-round-trip/experiment.py"
      to: "ambito_financiero_client._core.raise_for_response"
      via: "importlib.util.spec_from_file_location + assert is"
      pattern: "is.*raise_for_response"
---

<objective>
Wave 0 + Wave 1 of the codegen spike: bootstrap the spike directory structure, register SPIKE-005 in MANIFEST.md, confirm the v1.1 operational pre-gate, fill in 12-VALIDATION.md, then run the ámbito round-trip experiment (001a) — the canary that decides whether unasync can produce a byte-identical `client.py` from `aio.py` while preserving B8 identity. Also creates README + experiment.py + FINDING.md skeletons for sub-experiments 001b/c/d (so Plan 02 can fill the experiments without re-creating skeletons).

Purpose: SPIKE-005 is the gate for Phase 16 (REFAC-06). Wave 1's 001a experiment is the central piece of evidence — if it fails decisively (B8 break, semantic-non-cosmetic diff hunk unfixable via `additional_replacements`), the spike is heading NO-GO and the remaining waves still execute to document the failure mode for the v1.3 libcst pending todo.

Output: SPIKE-005 directory skeleton + 4 sub-directory skeletons + ámbito round-trip 001a fully executed with `output/client_generated.py`, `diff_vs_v1.1_client.txt`, and `FINDING.md` (verdict + Rule config draft for ámbito). 12-VALIDATION.md frontmatter flipped to `nyquist_compliant: true` with per-task verification map populated.

Honors locked decisions: D-SCOPE-01 (ámbito = canary), D-SCOPE-03 (1-day timebox starts here), D-RIGOR-01 items 1+2 (byte-identical + B8 identity), D-ARTIFACT-01 (SPIKE-005 numbering), D-NOGO-01 (no libcst inline). Anti-Pitfalls 4 (B8 break) and 17 (CI 3.13 attribution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-codegen-spike/12-CONTEXT.md
@.planning/phases/12-codegen-spike/12-RESEARCH.md
@.planning/phases/12-codegen-spike/12-VALIDATION.md
@.planning/spikes/CONVENTIONS.md
@.planning/spikes/MANIFEST.md
@.claude/skills/spike-findings-market-libs/SKILL.md
@packages/ambito-financiero-client/src/ambito_financiero_client/client.py
@packages/ambito-financiero-client/src/ambito_financiero_client/aio.py
@packages/ambito-financiero-client/src/ambito_financiero_client/_core.py
</context>

<tasks>

<task id="12-01-01" type="checkpoint:human-verify" gate="blocking">
  <name>Task 12-01-01: Pre-Phase-12 operational gate — v1.1 head 71bf201 CI-green on Python 3.13</name>
  <files>(none — checkpoint, no file mutation)</files>
  <read_first>
    - .planning/STATE.md §"Operational gate before Phase 12 starts" + §"Acknowledged at v1.1 close on 2026-06-14" (3 deferred CI 3.13 human-verification items)
    - .planning/ROADMAP.md §"v1.2 ... Operational gate (pre-Phase-12)"
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §TL;DR bullet 2 ("Operational gate (PRE-Phase-12)")
  </read_first>
  <action>Pause execution and present the how-to-verify steps to the operator. Wait for an explicit "approved" or a documented exception. Do not proceed to Task 12-01-02 until the resume-signal is received. Anti-Pitfall 17.</action>
  <verify><human-check>Operator types "approved" OR provides a documented exception (e.g., "3.13 matrix not in 71bf201 baseline; proceed against 3.12-only").</human-check></verify>
  <done>Operator response captured in the orchestrator's checkpoint log; if "approved", proceed to 12-01-02; if exception, note it in 12-SUMMARY.md when written.</done>
  <what-built>
    Pre-gate verification (no code automation possible — this requires a human looking at GitHub Actions in a browser).
  </what-built>
  <how-to-verify>
    1. Open https://github.com/<owner>/market-libs/actions (or run `gh run list --branch main --limit 5 --json conclusion,headSha,createdAt`).
    2. Locate the workflow run for commit `71bf201` (v1.1 head, shipped 2026-06-14).
    3. Confirm the run conclusion is `success` AND the matrix includes BOTH Python 3.12 AND Python 3.13.
    4. If both passed → reply `approved` to unblock Phase 12.
    5. If Python 3.13 red OR the run is missing → reply with the failure transcript or a description of the missing run. The orchestrator will STOP Phase 12 and recommend filing a quick-task (`/gsd-quick "fix CI 3.13 on v1.1 head 71bf201"`) before resuming Phase 12.
    6. If the run exists but did not include 3.13 in the matrix (because the matrix was added later than 71bf201) → confirm explicitly that Phase 12 is acceptable to proceed against the 3.12-only baseline; document the gap in your reply.
    Anti-Pitfall 17: this gate exists so that any 3.13-specific break surfaced during v1.2 is unambiguously a v1.2 regression, not a pre-existing v1.1 issue.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or paste the failure transcript / describe the missing-matrix situation.</resume-signal>
  <acceptance_criteria>
    - Operator explicitly types "approved" OR provides a documented exception (e.g., 3.13 matrix added post-71bf201; explicit go-ahead vs 3.12-only baseline).
    - If "approved" includes "3.12 only", the executor MUST add a one-line note to .planning/phases/12-codegen-spike/12-SUMMARY.md (when created in Plan 03) recording the exception.
  </acceptance_criteria>
</task>

<task id="12-01-02" type="auto">
  <name>Task 12-01-02: Bootstrap SPIKE-005 directory + 4 sub-directory skeletons + MANIFEST registration + fill 12-VALIDATION.md</name>
  <read_first>
    - .planning/spikes/CONVENTIONS.md (naming + per-spike layout + Verdict Standard)
    - .planning/spikes/MANIFEST.md (existing table shape, last spike row to mirror format)
    - .planning/spikes/001a-tokenstore-threading-lock-to_thread/README.md (existing frontmatter shape for sub-experiment README)
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recommended Spike Directory Structure" + §"Recipe 8 — Concrete Spike Directory Layout"
    - .planning/phases/12-codegen-spike/12-VALIDATION.md (current draft to flip into populated)
    - .planning/phases/12-codegen-spike/12-CONTEXT.md §D-ARTIFACT-01 (SPIKE-005 numbering rationale)
  </read_first>
  <action>
    Run `mkdir -p .planning/spikes/SPIKE-005-codegen-tool-choice/{001a-ambito-round-trip/output,001b-ambito-marker-future-compat,001c-matriz-construct-audit,001d-matriz-deny-list-config}` from the repo root. Confirm cwd is /Users/sebadlf/development/becerra/market-libs.

    Re-run `slopcheck install unasync` and capture the output. If it returns anything other than `[OK] unasync (pypi)`, STOP and surface as a blocker (D-NOGO trigger — supply chain risk per T-12-SC). Embed the verified version + slopcheck transcript inline in 001a/FINDING.md draft created below.

    Write the following files using the Write tool (per project skill rules — no heredoc). All Python files MUST start with `from __future__ import annotations` per CLAUDE.md mandatory directive.

    (a) `.planning/spikes/SPIKE-005-codegen-tool-choice/README.md`:
        - Frontmatter: `spike: 005`, `name: codegen-tool-choice`, `type: standard`, `validates: "Given the v1.1 hand-written packages/ambito-financiero-client/.../client.py + matriz aio.py 852 LOC, when unasync 0.6.0 runs against aio.py with prescribed additional_replacements AND ruff format normalizes, then byte-identical diff + B8 identity preserved + matriz construct audit zero TBD + deny-list intact"`, `verdict: TBD`, `related: [001a, 001b, 001c, 001d]`, `tags: [codegen, unasync, ambito, matriz, B8-identity, phase-12]`.
        - Body sections per CONVENTIONS.md (## What This Validates / ## Research / ## How to Run / ## What to Expect / ## Sub-Experiments).
        - Sub-Experiments lists 001a/b/c/d with one-line descriptions.

    (b) `.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/README.md`:
        - Frontmatter: `spike: 005`, `sub: 001a`, `name: ambito-round-trip`, `type: comparison`, `validates: "Given v1.1 hand-written ambito client.py, when unasync.unasync_files() runs on aio.py with prescribed additional_replacements AND ruff format normalizes, then diff is empty AND B8 identity preserved (mod._raise_for_response is aio._raise_for_response is _core.raise_for_response)"`, `verdict: TBD`, `related: [001b, 001c, 001d]`, `tags: [codegen, unasync, ambito, byte-identical, B8-identity]`.
        - Sections: What This Validates / Given / When / Then / How to Run / What to Expect / Investigation Trail (initially empty).

    (c) `.planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/README.md`:
        - Frontmatter: `spike: 005`, `sub: 001b`, `name: ambito-marker-future-compat`, `type: standard`, `validates: "Given the @generated marker comment prepended to client_generated.py, when ruff check + ruff format --check + mypy --strict + ast.parse run, then all 4 commands exit 0 (marker compatible with from __future__ import annotations per PEP 236)"`, `verdict: TBD`, `related: [001a]`, `tags: [codegen, marker, future-import, ruff, mypy]`.
        - Body sections same as above.

    (d) `.planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/README.md`:
        - Frontmatter: `spike: 005`, `sub: 001c`, `name: matriz-construct-audit`, `type: enumeration`, `validates: "Given matriz aio.py 852 LOC, when ast.walk enumerates async-only constructs (AsyncFunctionDef, AsyncWith, AsyncFor, Await, asyncio.*) AND operator triages REVIEW rows, then zero TBD/REVIEW/DENY-LIST-VIOLATION rows remain (D-SCOPE-02 merge gate)"`, `verdict: TBD`, `related: [001a, 001d]`, `tags: [codegen, matriz, ast, audit, deny-list]`.

    (e) `.planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/README.md`:
        - Frontmatter: `spike: 005`, `sub: 001d`, `name: matriz-deny-list-config`, `type: standard`, `validates: "Given the matriz codegen deny-list (_token_store.py, _refresh_policy.py, ws_client.py), when a per-file Rule(fromdir=aio.py-only) configuration is simulated AND sha256 of deny-listed files is computed before+after, then deny-listed files are byte-identical (zero mutation)"`, `verdict: TBD`, `related: [001c]`, `tags: [codegen, matriz, deny-list, sha256]`.

    (f) `.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py`, `.../001b.../experiment.py`, `.../001c.../audit.py`, `.../001d.../experiment.py`: write empty placeholders containing only `from __future__ import annotations\n\n# Filled in subsequent task / Plan 02 wave\n` (the 001a script is filled in this plan's Task 12-01-03; b/c/d are filled in Plan 02). 001a script content is implemented in Task 12-01-03 — for now this placeholder line is enough so the file exists.

    (g) `.../001a.../FINDING.md`, `.../001b.../FINDING.md`, `.../001c.../FINDING.md`, `.../001d.../FINDING.md`: placeholder bodies containing only `<!-- Filled after experiment runs -->\n# FINDING: [TBD]\n\n**Verdict:** TBD\n\n**Slopcheck transcript (2026-06-14):**\n<paste the slopcheck install unasync output captured at task start>\n`.

    (h) Update `.planning/spikes/MANIFEST.md`: append a row to the Spikes table (after the row for spike 003) with columns `| 005 | codegen-tool-choice | standard | unasync 0.6.0 round-trip + B8 identity + matriz construct audit + deny-list intact | TBD | codegen, unasync, phase-12 |`. Also append a "## SPIKE-005 sub-experiments" section listing 001a/b/c/d with one-line summaries.

    (i) Fill in `.planning/phases/12-codegen-spike/12-VALIDATION.md` (overwrite, the current contents are a template). Use the Per-Task Verification Map from 12-RESEARCH.md §"Validation Architecture" §"Phase Requirements → Test Map" and adapt to the 3-plan task layout (Tasks 12-01-01 through 12-03-NN). Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`. The table MUST include rows for every task in all 3 plans referencing REFAC-06. Test Infrastructure section: Framework = "shell + uv run + pytest + stdlib ast"; Quick run command = `uv run --with unasync python <experiment>.py`; Full suite command = the 8-item evidence checklist composed in Plan 03 Task 12-03-01. Wave 0 Requirements section lists ONLY the 4 sub-directory skeletons + MANIFEST row (all delivered by Task 12-01-02). Sign-Off section ticked except the final "approved" line which stays "approved 2026-06-XX" placeholder.

    Per CLAUDE.md GSD enforcement: do NOT touch any file under `packages/`. Anti-Pitfall 2 (spike code creeping into packages/) — verify with `git status --porcelain packages/ | wc -l` after task; must return 0.
  </action>
  <verify>
    <automated>
      test -d .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output && \
      test -d .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat && \
      test -d .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit && \
      test -d .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config && \
      grep -q '^spike: 005$' .planning/spikes/SPIKE-005-codegen-tool-choice/README.md && \
      grep -q '^sub: 001a$' .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/README.md && \
      grep -q '^sub: 001b$' .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/README.md && \
      grep -q '^sub: 001c$' .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/README.md && \
      grep -q '^sub: 001d$' .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/README.md && \
      grep -q '| 005 | codegen-tool-choice |' .planning/spikes/MANIFEST.md && \
      grep -q '^nyquist_compliant: true$' .planning/phases/12-codegen-spike/12-VALIDATION.md && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/README.md, .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/{README.md,experiment.py,FINDING.md}, .planning/spikes/SPIKE-005-codegen-tool-choice/001b-ambito-marker-future-compat/{README.md,experiment.py,FINDING.md}, .planning/spikes/SPIKE-005-codegen-tool-choice/001c-matriz-construct-audit/{README.md,audit.py,FINDING.md}, .planning/spikes/SPIKE-005-codegen-tool-choice/001d-matriz-deny-list-config/{README.md,experiment.py,FINDING.md}, .planning/spikes/MANIFEST.md, .planning/phases/12-codegen-spike/12-VALIDATION.md</files>
  <done>SPIKE-005 skeleton + 4 sub-experiment skeletons exist on disk; MANIFEST.md has the SPIKE-005 row; 12-VALIDATION.md is no longer a draft (nyquist_compliant: true) with a populated Per-Task Verification Map covering all 3 plans; slopcheck transcript captured in 001a/FINDING.md; zero mutations under packages/.</done>
  <acceptance_criteria>
    - All 4 sub-directories exist with README.md (correctly-shaped frontmatter), experiment.py / audit.py placeholder, FINDING.md placeholder.
    - MANIFEST.md contains a row whose first cells match `| 005 | codegen-tool-choice |`.
    - 12-VALIDATION.md frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`.
    - 12-VALIDATION.md Per-Task Verification Map table has rows for at least 9 tasks (3 plans × ~3 tasks each) and every row references REFAC-06.
    - `git status --porcelain packages/` returns no output (zero mutations under packages/).
    - slopcheck transcript for `unasync` is captured in 001a/FINDING.md as `[OK] unasync (pypi)` (or task escalates if not OK).
  </acceptance_criteria>
</task>

<task id="12-01-03" type="auto" tdd="false">
  <name>Task 12-01-03: 001a Ámbito round-trip experiment — run unasync, ruff format, diff, assert B8 identity</name>
  <read_first>
    - .planning/phases/12-codegen-spike/12-RESEARCH.md §"Recipe 1 — unasync 0.6.0 Invocation Recipe", §"Recipe 2 — Ámbito Round-Trip Experiment Recipe", §"Recipe 3 — B8 Identity Preservation Test Recipe", §"Recipe 6 — ruff format/check + mypy --strict on Generated Output", §"Pitfall 1-5", §"Pitfall 7 (additional_replacements docstring matching)"
    - packages/ambito-financiero-client/src/ambito_financiero_client/aio.py (entire file — codegen source; identify every `Async*` / `aio` / `a_*` token that needs replacement)
    - packages/ambito-financiero-client/src/ambito_financiero_client/client.py (entire file — round-trip target; identify the additional_replacements needed: AsyncClient→Client, AsyncRetryTransport→RetryTransport, _atransport→_transport, AmbitoFinancieroAsyncClient→AmbitoFinancieroClient, _default_async_client→_default_client, aclose→close, plus the docstring fix-ups identified in Recipe 1)
    - packages/ambito-financiero-client/src/ambito_financiero_client/_core.py (B8 identity center — `raise_for_response` is the exported name aio/client both alias to `_raise_for_response`)
    - CLAUDE.md §"Code Style" (every Python file MUST start with `from __future__ import annotations`)
    - .planning/spikes/CONVENTIONS.md §"To Avoid" (spike must NOT import from packages/*/src/; use subprocess + pathlib + importlib.util.spec_from_file_location instead)
  </read_first>
  <action>
    Replace the placeholder `.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py` with a runnable script that follows Recipe 2 + 3 + 6 exactly. The script MUST:

    Step 1 — Module header: `from __future__ import annotations`, module docstring `"""Spike 001a: ámbito round-trip experiment — unasync 0.6.0 + ruff format + diff + B8 identity."""`, imports (`shutil`, `subprocess`, `sys`, `importlib.util`, `pathlib.Path`, `unasync`).

    Step 2 — Compute paths using `pathlib.Path(__file__).resolve().parents[4]` as REPO_ROOT (the file lives 4 levels deep under repo root: `.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py`). Set `SRC = REPO_ROOT / "packages/ambito-financiero-client/src/ambito_financiero_client"` and `WORK = Path(__file__).resolve().parent / "output"`. Verify WORK.exists() (Task 12-01-02 created it).

    Step 3 — Copy `SRC / "aio.py"` to `WORK / "aio.py"` via `shutil.copy`. This isolates the spike from the source (Anti-Pitfall 2: spike code creeping into packages/).

    Step 4 — Invoke `unasync.unasync_files(fpath_list=[str(WORK / "aio.py")], rules=[unasync.Rule(fromdir=str(WORK) + "/", todir=str(WORK) + "/", additional_replacements={...})])`. The additional_replacements dict MUST contain (read both ámbito files to validate the exact token list):
      - "AsyncClient": "Client"
      - "AsyncRetryTransport": "RetryTransport"
      - "_atransport": "_transport"
      - "AmbitoFinancieroAsyncClient": "AmbitoFinancieroClient"
      - "_default_async_client": "_default_client"
      - "aclose": "close"
      - Plus any token discovered during the read of aio.py vs client.py that differs by an async prefix or `a*` shorthand. If a discrepancy is found that cannot be expressed as a single-token replacement (per the unasync issue #74 single-token limitation documented in Recipe 1), STOP and record the limitation in FINDING.md as a NO-GO root cause.

    Step 5 — Rename `WORK / "aio.py"` to `WORK / "client_generated.py"` via `Path.rename`. (Unasync writes to the input filename; the rename happens AFTER unasync runs.)

    Step 6 — Run `uv run ruff format` on the generated file via `subprocess.run(["uv", "run", "ruff", "format", str(generated)], check=True, cwd=REPO_ROOT)`. This is industry-standard codegen post-processing (Recipe 6).

    Step 7 — Run `diff -u <v1.1 client.py> <generated>` via `subprocess.run(["diff", "-u", str(SRC / "client.py"), str(generated)], capture_output=True, text=True, cwd=REPO_ROOT)`. Write `result.stdout + result.stderr` to `.../001a-ambito-round-trip/diff_vs_v1.1_client.txt`. Print the exit code.

    Step 8 — B8 identity check (Recipe 3): load `generated` as a module via `importlib.util.spec_from_file_location("ambito_financiero_client.client_generated", str(generated))` + `module_from_spec` + `exec_module`. Import `ambito_financiero_client._core` and `ambito_financiero_client.aio` using normal Python imports (these are workspace packages, available via `uv run`). Assert `mod._raise_for_response is aio._raise_for_response is _core.raise_for_response`. If assertion fails, print `B8 IDENTITY FAILED — codegen emitted a thunk wrapper`. If passes, print `B8 IDENTITY: PASS`.

    Step 9 — Exit codes: the script MUST exit 0 if BOTH diff was empty (exit 0) AND B8 assertion passed; otherwise exit 1 with a clear summary line `SPIKE 001a VERDICT: <PASS|FAIL diff=<empty|N hunks> b8=<PASS|FAIL>>`.

    Per D-RIGOR-01 item 3: also run `subprocess.run(["uv", "run", "ruff", "format", "--check", str(generated)])` AFTER step 6 to confirm idempotency (formatting is stable — running again is a no-op). This is the "format-stable" check from Pitfall 3.

    Then RUN the experiment:
      `cd /Users/sebadlf/development/becerra/market-libs && uv run --with unasync python .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/experiment.py`

    Capture the stdout/stderr in `.../001a-ambito-round-trip/run_log.txt`.

    Then update `.../001a-ambito-round-trip/FINDING.md`:
      - Frontmatter-style header: `# FINDING: Ámbito Round-Trip`
      - `**Verdict:** PASS` (if exit 0) OR `**Verdict:** FAIL — see root cause below` (if exit 1)
      - `## Slopcheck Verification` section: paste the `slopcheck install unasync` transcript captured in Task 12-01-02
      - `## Run Transcript` section: paste contents of `run_log.txt`
      - `## Diff Analysis` section: if diff empty, write `Empty diff after ruff format. Confirms SC#1 (byte-identical round-trip)`. If diff non-empty, classify each hunk per Recipe 2 triage protocol (cosmetic / fixable-via-additional-replacements / unfixable-structural / inherent-asymmetry like the `_validate_max_retries` import direction). Inherent-asymmetry hunks DO NOT trigger NO-GO; document them as Phase 16 setup steps.
      - `## B8 Identity Check` section: PASS / FAIL with the assert statement evaluated.
      - `## Ámbito Rule Config Draft` section: emit the exact `unasync.Rule(fromdir=..., todir=..., additional_replacements={...})` block that produced the result, suitable for inclusion in Phase 16 / DECISION.md.
      - `## Open Questions for Operator` section: list any inherent-asymmetry hunks AND any docstring-mutation surprises (anti-Pitfall 7) that need operator confirmation before signoff.

    Update `.../001a-ambito-round-trip/README.md` frontmatter: flip `verdict: TBD` to `verdict: PASS` or `verdict: FAIL` based on the script's exit code. If FAIL, also append an `## Investigation Trail` section listing what was tried.

    Anti-Pitfall 4 (B8 skip under time pressure): the experiment.py MUST embed the B8 check INSIDE the script. The check failing → FINDING.md verdict FAIL → spike is heading NO-GO. Plan 03 Task 12-03-01 evidence collection will see this.

    Anti-Pitfall 2 (spike creeping into packages/): the script reads `SRC / "aio.py"` via shutil.copy (never writes there). Verify: `git status --porcelain packages/` after running must return zero lines.
  </action>
  <verify>
    <automated>
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/output/client_generated.py && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/diff_vs_v1.1_client.txt && \
      test -f .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/run_log.txt && \
      grep -qE '^\*\*Verdict:\*\* (PASS|FAIL)' .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md && \
      grep -qE '^verdict: (PASS|FAIL)$' .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/README.md && \
      grep -q 'B8 IDENTITY' .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/run_log.txt && \
      test "$(git status --porcelain packages/ | wc -l | tr -d ' ')" = "0"
    </automated>
  </verify>
  <files>.planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/{experiment.py,output/client_generated.py,diff_vs_v1.1_client.txt,run_log.txt,FINDING.md,README.md}</files>
  <done>experiment.py runs to completion; client_generated.py produced under output/; diff transcript + run log + FINDING.md (verdict PASS|FAIL) + README.md (verdict no longer TBD) all written; zero mutations under packages/.</done>
  <acceptance_criteria>
    - `experiment.py` is a runnable Python script starting with `from __future__ import annotations` (CLAUDE.md mandatory).
    - The script ran end-to-end and produced `output/client_generated.py`, `diff_vs_v1.1_client.txt`, `run_log.txt`.
    - FINDING.md contains: Verdict (PASS|FAIL) + Slopcheck transcript + Run Transcript + Diff Analysis (each hunk classified, if any) + B8 Identity Check result + Ámbito Rule Config Draft + Open Questions.
    - README.md frontmatter `verdict:` is no longer `TBD` (it is `PASS` or `FAIL`).
    - `git status --porcelain packages/` is empty (Anti-Pitfall 2 enforced).
    - If FAIL: the FINDING.md ## Diff Analysis identifies the root cause class (cosmetic, fixable, unfixable-structural, inherent-asymmetry) AND README.md has an `## Investigation Trail` section listing the iterations attempted.
    - The script's exit code (recorded in run_log.txt) MUST match the verdict in FINDING.md (exit 0 → PASS; exit 1 → FAIL).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → spike directory | operator-driven file writes; throwaway code, no production reach |
| spike experiment → packages/ source files (READ-ONLY) | `shutil.copy` reads packages/.../aio.py into spike work dir; spike NEVER writes to packages/ |
| PyPI → spike environment | `uv run --with unasync` resolves unasync 0.6.0 transiently from pypi.org (supply chain) |
| spike `importlib` → workspace packages | B8 identity test loads spike output as a module AND imports real `ambito_financiero_client._core` / `.aio` from the workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-SC | Tampering (Supply Chain) | `uv run --with unasync` transient install of `unasync 0.6.0` from PyPI | mitigate | Task 12-01-02 re-runs `slopcheck install unasync` and embeds the `[OK]` transcript in 001a/FINDING.md before any experiment runs. Mandatory checkpoint (Task 12-01-01 + slopcheck rerun); per D-SCOPE-03 a failed slopcheck triggers immediate NO-GO escalation. Package legitimacy audit already complete (12-RESEARCH.md §Package Legitimacy Audit, both packages `[OK]` 2026-06-14). |
| T-12-01-02 | Tampering | spike experiment accidentally mutating packages/*/src/ | mitigate | Anti-Pitfall 2 enforcement: every task ends with `git status --porcelain packages/` returning zero lines. CONVENTIONS.md §"To Avoid" forbids importing FROM packages/*/src — spike uses `shutil.copy` + `subprocess` + `importlib.util.spec_from_file_location`. Verified in Task 12-01-02 and 12-01-03 acceptance criteria. |
| T-12-01-03 | Information Disclosure | spike output/transcripts accidentally embedding credentials | accept | Ámbito has no auth (no `.env` secrets); the spike never touches `packages/iol-client/.env` or `packages/higyrus-client/.env` or `packages/matriz-client/.env`. Transcripts capture stdout/stderr of `unasync` + `ruff` + `mypy` + `diff` — none of which read `.env`. No PII / no creds in scope. |
| T-12-01-04 | Tampering | `experiment.py` running arbitrary subprocess commands (`uv`, `ruff`, `mypy`, `diff`) | accept | All commands invoked are part of the existing dev toolchain. `subprocess.run([...], check=True)` uses explicit arg list (no `shell=True`); no string injection vectors. |
| T-12-01-05 | Repudiation | `verdict: PASS|FAIL` flipped without supporting transcripts | mitigate | FINDING.md MUST contain Run Transcript + Diff Analysis + B8 Identity Check sections (all are evidence) before verdict can be set non-TBD. Acceptance criteria gate this. |

## ASVS L1 Alignment

- V1.1: trust boundaries documented (table above). 
- V14.2 (Dependency management): unasync legitimacy verified via slopcheck; transient install only.
- No new attack surface introduced — spike artifacts are NOT shipped, NOT imported by the runtime, NOT in any wheel.
</threat_model>

<verification>
After all 3 tasks complete:

1. `ls -la .planning/spikes/SPIKE-005-codegen-tool-choice/` shows the 4 sub-directories.
2. `head -20 .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/FINDING.md` shows the Verdict line + Slopcheck transcript.
3. `cat .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/run_log.txt` shows the experiment.py stdout including the B8 identity result line.
4. `grep "verdict:" .planning/spikes/SPIKE-005-codegen-tool-choice/001a-ambito-round-trip/README.md` is no longer `TBD`.
5. `grep "SPIKE-005" .planning/spikes/MANIFEST.md` returns at least 1 line.
6. `git status --porcelain packages/` returns empty (NO mutation to production code, Anti-Pitfall 2 enforced).
7. `grep -c "REFAC-06" .planning/phases/12-codegen-spike/12-VALIDATION.md` returns > 0 (validation map references the phase requirement).
</verification>

<success_criteria>
- Wave 0 + Wave 1 deliverables complete: SPIKE-005 directory + 4 sub-directory skeletons + MANIFEST registration + 12-VALIDATION.md populated + 001a Ámbito round-trip experiment executed end-to-end.
- 001a FINDING.md has a verdict (PASS or FAIL — either is a valid outcome at this wave; FAIL means heading-toward-NO-GO, but the spike continues to Plans 02+03 to document the failure mode).
- Operational pre-gate (CI 3.13 confirmation) is either approved or documented as an explicit exception by the operator.
- Zero mutations under `packages/` (Anti-Pitfall 2).
- All Python files start with `from __future__ import annotations` (CLAUDE.md mandatory).
</success_criteria>

<output>
Create `.planning/phases/12-codegen-spike/12-01-SUMMARY.md` with frontmatter:

```yaml
---
phase: 12-codegen-spike
plan: 01
status: complete
wave: 1
tasks_completed: [12-01-01, 12-01-02, 12-01-03]
files_created: [list of files actually created]
files_modified: [list of files actually modified]
ambito_round_trip_verdict: PASS|FAIL
b8_identity_check: PASS|FAIL
diff_hunk_count: <N>
slopcheck_unasync: OK|NOT-OK
operational_pregate: approved|exception-documented
---
```

Body sections: ## Summary / ## Ámbito Round-Trip Outcome / ## B8 Identity Outcome / ## Slopcheck Outcome / ## Anti-Pitfall Compliance (2, 4, 17) / ## Next Steps (→ Plan 02 covers Waves 2-4: marker, matriz audit, deny-list; or → if 001a FAIL, Plan 02 still runs to complete the failure-mode documentation for the NO-GO close-out in Plan 03).
</output>
