---
phase: 42
plan: 04
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - main_market_data.py
  - .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md
  - .planning/verification/market-data-client-findings.md
autonomous: true
requirements: [LIVE-02]

must_haves:
  truths:
    - "La corrida deja en disco una lectura fresca del wire de /instruments y /segments, fechada en esta sesión (criterio 5)"
    - "La evidencia fresca es citable por la Phase 43 sin depender del working tree del ejecutor: hay un artefacto COMMITTEADO con captured_at adentro"
    - "El baseline committeado del 2026-07-31 queda explícitamente marcado como NO AUTORITATIVO para SHAPE-01 (criterio 5)"
    - "El payload crudo no entra a git: vive exclusivamente en el staging gitignored captures/"
    - "_write_schema_snapshot sigue siendo write-once y no sobrescribe el baseline en drift (D-25)"
  artifacts:
    - path: "main_market_data.py"
      provides: "capture() con envelope timestampeado en los dos probe sites de instruments y segments (D-07 / D-08)"
      contains: "from verification.capture import capture"
    - path: ".planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md"
      provides: "Lectura fresca committeada y PII-free (schema_of) de /instruments y /segments, con captured_at y la marca de baseline no-autoritativo"
      min_lines: 50
      contains: "NO AUTORITATIVO"
  key_links:
    - from: "main_market_data.py"
      to: ".planning/verification/captures/market-data-wire-instruments-42.json"
      via: "capture('market-data', 'wire-instruments-42', envelope) dentro del try de probe_instruments_sync"
      pattern: "wire-instruments-42"
    - from: ".planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md"
      to: ".planning/verification/schemas/market-data-client/get-instruments.json"
      via: "marca explícita de no-autoritatividad del baseline 2026-07-31 que la Phase 43 debe NO usar como referencia de SHAPE-01"
      pattern: "get-instruments.json"
---

<objective>
Instrumentar `main_market_data.py` para que su corrida deje una **lectura fresca y fechada** del
wire de `/instruments` y `/segments` (D-07), correrla en vivo, y producir un artefacto
**committeado y PII-free** (`42-WIRE-READ.md`) que la Phase 43 pueda citar y que marque
explícitamente el baseline del 2026-07-31 como no-autoritativo para SHAPE-01.

Purpose: criterio 5 del ROADMAP y la dependencia cross-fase declarada en
`REQUIREMENTS.md § Dependencias cross-fase` ("Lectura fresca del wire de `/instruments` +
`/segments`" producida en Phase 42, consumida por Phase 43). `42-RESEARCH.md § Pitfall 6` midió que,
sin una decisión explícita, ese criterio produce evidencia que existe **sólo** en el working tree
del ejecutor: `capture()` escribe a `.planning/verification/captures/`, que está en `.gitignore:53`
y no lleva timestamp adentro del archivo; y `_write_schema_snapshot` es write-once por diseño (D-25)
así que los baselines seguirían fechados 2026-07-31 aunque el driver corra hoy.

**Decisión D-08 (Open Question Q1, cerrada acá con la recomendación (b) de research):** las dos
mitades. (1) El payload de `capture()` se envuelve en un envelope con `captured_at`, espejando la
forma que `_write_schema_snapshot` ya usa (`main_market_data.py:474-480`). (2) Se produce
`42-WIRE-READ.md`, committeado, que lleva `captured_at`, conteos y el `schema_of(raw)` —keys+types,
PII-free por construcción, exactamente lo mismo que el baseline guarda— más la marca de
no-autoritatividad. Sólo la mitad (2) sobrevive a otro clone, a otro worktree o a un `git clean -xdf`.

Output: `main_market_data.py` instrumentado, dos capturas fechadas en el staging gitignored, y
`42-WIRE-READ.md` committeado.
</objective>

<execution_context>
@/Users/admin/development/market-libs/.claude/gsd-core/workflows/execute-plan.md
@/Users/admin/development/market-libs/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-CONTEXT.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-RESEARCH.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-PATTERNS.md
@.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md
@CLAUDE.md
</context>

## Artifacts this phase produces

Símbolos y archivos creados por la Phase 42 (no existen en HEAD). Excluir de cualquier verificación
de drift: son salida, no deriva.

| Artefacto | Tipo | Creado en |
|-----------|------|-----------|
| `verification/test_literal_census_venue_gate.py` (11 tests + `_census_matriz_node`, `_membership_offenders`, `_TARGET`) | archivo nuevo | 42-01 |
| `_census_header_lines()`, `_census_header()`, local `venue`, flag `--matriz-only`, prefijos `CENSUS-HEADER`/`CENSUS-DLOCK` | símbolos nuevos en `scripts/literal_census_33.py` | 42-01 |
| entrada `verification/test_literal_census_venue_gate.py` en `ci.yml` | línea de allowlist | 42-01 |
| `.planning/phases/42-…/42-CENSUS.md` | artefacto committeado | 42-02 |
| `capture` importado en `main_market_data.py` | import nuevo | 42-04 |
| labels de captura `wire-instruments-42` y `wire-segments-42` | identificadores nuevos | 42-04 |
| `.planning/verification/captures/market-data-wire-instruments-42.json`, `…-segments-42.json` | artefactos gitignored | 42-04 |
| `.planning/phases/42-…/42-WIRE-READ.md` | artefacto committeado | 42-04 |
| `LIVE-HIGY-42` | identificador de destino nuevo | 42-05 |

<tasks>

<task type="auto">
  <name>Task 1: Instrumentar main_market_data.py con capture() y envelope timestampeado (D-07 / D-08)</name>
  <files>main_market_data.py</files>
  <read_first>
    - `main_market_data.py:50-96` — el bloque de imports. `capture` **NO** está importado hoy;
      `dt` ya está como `import datetime as dt` (`:50`) y `schema_of` ya viene del barrel
      `from verification import (...)` (`:60-68`).
    - `main_market_data.py:457-522` — `_write_schema_snapshot`: la forma exacta del envelope
      (`:472-480`, con `endpoint`, `client_function`, `captured_at`, `base_url`, `schema`), el
      write-once (`:482-487`) y la rama de drift que **nunca** sobrescribe (`:508-522`, D-25).
    - `main_market_data.py:968-994` — `probe_instruments_sync`: `raw` disponible en `:975-980`,
      `_write_schema_snapshot` en `:985-991`, todo dentro del `try` y con
      `except Exception` → `_finding_for_exc` (D-09).
    - `main_market_data.py:997-1022` — `probe_segments_sync`: `raw` en `:1004`,
      `_write_schema_snapshot` en `:1010-1016`.
    - `main_market_data.py:132-145` — `_ENDPOINT_TEMPLATES`, con `get_instruments` → `/instruments`
      y `get_segments` → `/instruments/segments`. Los valores son la ÚNICA copia de cada URL del
      driver: consumirlos desde ahí, nunca repetir el literal.
    - `verification/capture.py:42-51` — la firma `capture(pkg, endpoint, payload) -> Path`, que
      escribe a `.planning/verification/captures/{pkg}-{endpoint}.json`, sobrescribe, y **no**
      agrega envelope ni timestamp.
    - `verification/schema.py:27-40` — `schema_of(payload)`: reduce a claves+tipos, nunca valores.
      Es lo único derivado del wire que puede llegar a un artefacto committeado.
    - `scripts/literal_census_33.py:212-214` — el patrón de uso de `capture()` que ya existe en el
      repo y que este driver debe copiar.
    - `verification/test_main_market_data_postprocess_guarded.py:34-40` — el invariante D-09: todo
      post-procesado va **dentro** del `try`.
    - `.planning/phases/42-…/42-RESEARCH.md` § Pitfall 6 y § Code Examples ("Envelope timestampeado
      para `capture()`").
  </read_first>
  <action>
    Tres cambios en `main_market_data.py`, todos en el mismo archivo:

    1. **Import.** Agregar `from verification.capture import capture` al bloque de imports de
       `verification`, inmediatamente antes de la línea 69
       (`from verification.cycle_report import verify_cycle_closure`), de modo que quede en orden
       alfabético con los otros `from verification.*`. Son imports top-level sin hack de `sys.path`,
       así que **no** lleva `# noqa` (a diferencia del script de censo). Dejar que
       `ruff format` / isort confirmen la posición.

    2. **`probe_instruments_sync`.** Dentro del `try`, inmediatamente después del bloque que liga
       `raw` (`:975-980`) y **antes** de la llamada a `_write_schema_snapshot` (`:985-991`), agregar
       una llamada a `capture` con `pkg="market-data"`, `endpoint="wire-instruments-42"` y un
       payload que sea un `dict[str, Any]` con exactamente estas claves, espejando la forma de
       `_write_schema_snapshot:474-480`:
       - `captured_at` — `dt.datetime.now(dt.UTC).isoformat()`
       - `endpoint` — `_ENDPOINT_TEMPLATES["get_instruments"]` (nunca el literal repetido)
       - `client_function` — `"get_instruments"`
       - `base_url` — el local `base_url` que el probe ya tiene
       - `n_rows` — `len(raw)` si `raw` es una lista, si no `None`
       - `schema` — `schema_of(raw)`
       - `payload` — `raw` (el crudo; sólo aterriza en el staging gitignored)

    3. **`probe_segments_sync`.** El espejo exacto del punto 2, dentro del `try`, después de ligar
       `raw` (`:1004`) y antes de `_write_schema_snapshot` (`:1010-1016`), con
       `endpoint="wire-segments-42"`, `_ENDPOINT_TEMPLATES["get_segments"]` y
       `client_function="get_segments"`.

    **Invariante D-09 a respetar:** las dos llamadas van **dentro** del `try`, al lado del
    `_write_schema_snapshot`, nunca después del `except`. Si la captura fallara, el
    `except Exception` del probe degrada a finding vía `_finding_for_exc` y no crashea el driver.

    **Lo que NO se toca:** `_write_schema_snapshot` no se modifica ni se le agrega un modo
    overwrite. Es write-once / no-overwrite-on-drift por diseño (D-25) y ésa es exactamente la razón
    por la que no puede producir la evidencia fresca de esta fase. Los baselines
    `get-instruments.json` / `get-segments.json` deben quedar con su `captured_at` del 2026-07-31
    después de la corrida.

    **Los espejos async (`probe_instruments_async:1332`, `probe_segments_async:1363`) NO se
    instrumentan:** el criterio 5 pide una lectura fresca del wire, no dos, y duplicarla en la
    superficie async sólo sobrescribiría las mismas dos rutas de `captures/` con el mismo contenido.
    Decisión tomada acá, no en ejecución.
  </action>
  <verify>
    <automated>uv run --frozen ruff check . &amp;&amp; uv run --frozen ruff format --check . &amp;&amp; test "$(grep -c 'wire-instruments-42' main_market_data.py)" = "1" &amp;&amp; test "$(grep -c 'wire-segments-42' main_market_data.py)" = "1" &amp;&amp; uv run pytest -q verification/test_main_market_data_deep_chain.py verification/test_main_market_data_postprocess_guarded.py verification/test_main_market_data_skip_line_shape.py verification/test_main_market_data_snapshot_identifiers.py verification/test_main_market_data_no_config_write.py verification/test_main_market_data_no_gate_bypass.py verification/test_main_market_data_uses_single_client_instance.py verification/test_main_market_data_cleanup_emits_finding.py</automated>
  </verify>
  <acceptance_criteria>
    - `main_market_data.py` contiene exactamente una línea `from verification.capture import capture`.
    - `grep -c "wire-instruments-42" main_market_data.py` y `grep -c "wire-segments-42"` devuelven `1`
      cada uno (una sola llamada por endpoint, sólo en la superficie sync).
    - Las dos llamadas a `capture` están dentro del `try` de su probe: verificable leyendo el fuente
      y confirmado por
      `uv run pytest -q verification/test_main_market_data_postprocess_guarded.py` verde.
    - Los 8 locks de `verification/test_main_market_data_*.py` +
      `verification/test_cycle_closure_market_data.py` siguen verdes (baseline medido en HEAD:
      27 passed en el subconjunto de 8 archivos).
    - `uv run --frozen ruff check .` y `ruff format --check .` verdes.
    - `git diff --stat` de esta task toca **un solo archivo**: `main_market_data.py`. En particular,
      `_write_schema_snapshot` no cambió y los dos baselines de
      `.planning/verification/schemas/market-data-client/` no cambiaron.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
  </acceptance_criteria>
  <done>El driver escribe, en los dos puntos donde ya tiene el JSON crudo en mano, una captura con
  envelope timestampeado y `schema_of` PII-free, sin tocar el mecanismo write-once del baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Corrida en vivo de main_market_data.py — lectura fresca del wire</name>
  <files>.planning/verification/captures/market-data-wire-instruments-42.json (gitignored), .planning/verification/captures/market-data-wire-segments-42.json (gitignored), .planning/verification/market-data-client-findings.md</files>
  <read_first>
    - `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-01-SUMMARY.md`
      — **verificar que el checkpoint bloqueante del plan 42-01 fue aprobado y transcrito verbatim**.
      Si no lo está, DETENER: esta task emite tráfico en vivo.
    - `main_market_data.py:1-45` — el docstring de uso; la invocación canónica es
      `uv run --package market-data-client python main_market_data.py`.
    - `main_market_data.py:3648` — `main()` no toma `argv` y no tiene flags: no hay forma de correr
      un subconjunto de probes, así que la corrida es del driver completo (Q3, decidido: se corre
      entero respetando D-07).
    - `.planning/verification/schemas/market-data-client/get-instruments.json` y `get-segments.json`
      — los baselines committeados, `captured_at: 2026-07-31`. Después de la corrida deben seguir
      con esa misma fecha (write-once, D-25).
    - `.planning/phases/42-…/42-RESEARCH.md` § Assumptions Log A4 — el churn conocido y aceptado del
      ledger de findings (ROADMAP:55: el harness de hoy es "ruidoso pero no lossy", y por eso lo
      vivo va antes que la limpieza de la Phase 45).
  </read_first>
  <action>
    Ejecutar el driver completo:

    `uv run --package market-data-client python main_market_data.py 2>&1 | tee /tmp/42-market-data-run.log`

    y registrar el exit code real del driver (no el de `tee`).

    **No setear `MARKET_DATA_VERIFY_MUTATING`** ni ninguna variable que habilite mutaciones: esta
    corrida es de lectura, y el criterio 5 sólo pide la lectura fresca del wire.

    Tras la corrida, confirmar los tres hechos y registrarlos en el SUMMARY:
    - Los dos archivos de captura existen en `.planning/verification/captures/` y su campo
      `captured_at` es de HOY.
    - Los dos baselines de `.planning/verification/schemas/market-data-client/` siguen con
      `captured_at: 2026-07-31…` — write-once respetado. Si alguno cambió, es una regresión de D-25
      y hay que escalarla, no aceptarla.
    - El diff del ledger `.planning/verification/market-data-client-findings.md`: cuántos bloques
      `### F-` nuevos, cuántos de ellos son duplicados cosméticos de `schema drift`. Ese churn es
      ruido conocido y aceptado (A4 / ROADMAP:55) y se commitea tal cual salió, sin editar a mano.

    Si algún probe devolvió `FINDING` en `/instruments` o `/segments`, la lectura fresca puede no
    haberse escrito: en ese caso registrar el hecho y escalar; **no** fabricar la captura a mano.
  </action>
  <verify>
    <automated>test -s /tmp/42-market-data-run.log &amp;&amp; grep -q "\"captured_at\": \"$(date -u +%Y-%m-%d)" .planning/verification/captures/market-data-wire-instruments-42.json &amp;&amp; grep -q "\"captured_at\": \"$(date -u +%Y-%m-%d)" .planning/verification/captures/market-data-wire-segments-42.json &amp;&amp; grep -q "\"captured_at\": \"2026-07-31" .planning/verification/schemas/market-data-client/get-instruments.json &amp;&amp; grep -q "\"captured_at\": \"2026-07-31" .planning/verification/schemas/market-data-client/get-segments.json &amp;&amp; test -z "$(git status --porcelain -- .planning/verification/captures/)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/verification/captures/market-data-wire-instruments-42.json` y
      `market-data-wire-segments-42.json` existen y su `captured_at` es la fecha UTC de hoy.
    - Cada envelope contiene las siete claves `captured_at`, `endpoint`, `client_function`,
      `base_url`, `n_rows`, `schema`, `payload`.
    - `.planning/verification/schemas/market-data-client/get-instruments.json` y `get-segments.json`
      siguen con `captured_at` del `2026-07-31` — write-once respetado (D-25), y ninguno de los dos
      aparece en `git status --porcelain`.
    - `git status --porcelain -- .planning/verification/captures/` está vacío: el directorio es
      gitignored (`.gitignore:53`) y el payload crudo **no** entra a git.
    - El SUMMARY registra el exit code del driver, el `n_rows` de cada endpoint y el conteo de
      bloques `### F-` nuevos del ledger.
    - `git hash-object verification/mutation_gate.py` sigue siendo
      `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
  </acceptance_criteria>
  <done>Existe en disco una lectura fresca y fechada del wire de `/instruments` y `/segments`, sin
  que el baseline write-once se haya movido.</done>
</task>

<task type="auto">
  <name>Task 3: 42-WIRE-READ.md — evidencia committeada, PII-free, con el baseline marcado no-autoritativo</name>
  <files>.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md</files>
  <read_first>
    - `.planning/verification/captures/market-data-wire-instruments-42.json` y
      `market-data-wire-segments-42.json` — la fuente. Leer **sólo** las claves `captured_at`,
      `endpoint`, `client_function`, `base_url`, `n_rows` y `schema`. La clave `payload` **no se
      transcribe nunca**.
    - `.planning/verification/schemas/market-data-client/get-instruments.json` y `get-segments.json`
      — los baselines del 2026-07-31, cuyo `schema` es la referencia contra la que se compara.
      Nótese que ya llevan `base_url` en el envelope committeado (`get-segments.json:5`): es el
      precedente que hace aceptable incluirlo también acá.
    - `verification/schema.py:27-40` — `schema_of` es PII-free por construcción (claves + nombres de
      tipo, nunca valores). Es lo único derivado del wire que puede entrar a git.
    - `.planning/ROADMAP.md` § Phase 42 criterio 5 y § Phase 43 criterios 1 y 2 — este documento es
      la base de evidencia que la Phase 43 consume; escribirlo pensando en ese consumidor.
    - `.planning/ROADMAP.md` § Backlog, entrada `SHAPE-MD-REF-33` — el detalle de qué campos declara
      `Instrument`/`Segment` y qué manda el wire, que esta lectura fresca viene a re-medir.
    - `.planning/REQUIREMENTS.md:79-84` — la tabla de dependencias cross-fase que nombra esta
      precondición.
    - `.planning/phases/42-…/42-PATTERNS.md` § "No Analog Found" — no existe hoy ningún markdown
      committeado con esta forma; el esqueleto de secciones sale de los campos del envelope de
      `_write_schema_snapshot`.
  </read_first>
  <action>
    Escribir `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md`
    con estas secciones, en este orden:

    1. **Sobre de la lectura.** Una tabla con una fila por endpoint (`/instruments`,
       `/instruments/segments`) y las columnas `client_function`, `captured_at` (UTC ISO verbatim del
       envelope), `base_url`, `n_rows`, y la ruta del archivo de captura en el staging gitignored.
       Más el comando exacto que produjo la corrida y su exit code.
    2. **Schema medido.** El `schema_of(raw)` de cada endpoint, transcrito verbatim del envelope, en
       un bloque de código JSON. Es claves + nombres de tipo, sin un solo valor del wire.
    3. **Marca de no-autoritatividad — sección obligatoria del criterio 5.** Declarar con esas
       palabras que `.planning/verification/schemas/market-data-client/get-instruments.json` y
       `get-segments.json` (`captured_at` del **2026-07-31**) quedan **NO AUTORITATIVOS** para
       SHAPE-01, y que la referencia de la Phase 43 es esta lectura fresca. Explicar por qué no
       se refrescaron: `_write_schema_snapshot` es write-once / no-overwrite-on-drift por diseño
       (D-25), así que en drift emite un finding `SHAPE` y **nunca** pisa el baseline — no es un
       olvido, es el contrato.
    4. **Delta contra el baseline.** Para `Instrument` y para `Segment`, dos listas: claves presentes
       en la lectura fresca y ausentes en el baseline, y claves presentes en el baseline y ausentes
       en la lectura fresca. Si el delta es vacío, decirlo explícitamente (que el schema no se movió
       desde el 2026-07-31 es un hecho, y es un hecho útil para la Phase 43). Esta sección es lo que
       la Phase 43 consume directamente para su tabla de disposición campo por campo.
    5. **Dónde vive el crudo y por qué no está acá.** Nombrar las dos rutas de
       `.planning/verification/captures/`, decir que están gitignored (`.gitignore:53`), que existen
       sólo en el working tree del ejecutor, y que ése es el único hogar legal del payload crudo con
       PII (C-4 / D-11 / T-33-32). La consecuencia práctica para la Phase 43: si corre en otro clone
       o después de un `git clean -xdf`, este documento es la evidencia disponible, y por eso lleva
       el schema adentro.
    6. **Qué NO decide este documento.** La lectura fresca es una **medición**. La disposición campo
       por campo de `Instrument`/`Segment`, el alias aditivo de `Instrument.marketId` (precedente
       D-22) y el tipado de las 5 claves `extra` son trabajo de la Phase 43 (SHAPE-01 / HARN-02); la
       Phase 42 no los adelanta ni los prejuzga.

    **Restricción de contenido (C-4 / D-11 / T-33-32):** este archivo se commitea. Sólo pueden
    entrar `captured_at`, `endpoint`, `client_function`, `base_url`, conteos de filas, rutas de
    archivo y la salida de `schema_of` (claves + nombres de tipo). **Ningún valor del wire, ningún
    identificador de instrumento, ninguna credencial, ningún token.**
  </action>
  <verify>
    <automated>test -f .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md &amp;&amp; grep -q "NO AUTORITATIVO" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md &amp;&amp; grep -q "2026-07-31" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md &amp;&amp; grep -q "$(date -u +%Y-%m-%d)" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md &amp;&amp; grep -q "get-instruments.json" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md &amp;&amp; grep -q "get-segments.json" .planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-WIRE-READ.md</automated>
  </verify>
  <acceptance_criteria>
    - `42-WIRE-READ.md` existe, lleva el `captured_at` UTC de hoy transcrito verbatim de los dos
      envelopes, y nombra las dos rutas de captura del staging gitignored.
    - Contiene el `schema_of` de los dos endpoints en bloques JSON, y **ningún** valor del wire:
      cada hoja del JSON transcrito es un nombre de tipo (`"str"`, `"int"`, `"float"`, `"bool"`,
      `"NoneType"`, …), no un dato.
    - Contiene una sección que marca explícitamente `get-instruments.json` y `get-segments.json`
      (`2026-07-31`) como **NO AUTORITATIVOS** para SHAPE-01, con la razón (write-once, D-25).
    - Contiene el delta de claves contra el baseline para `Instrument` y para `Segment`, o la
      declaración explícita de que el delta es vacío.
    - Contiene la sección que declara que este documento **no** decide la disposición campo por
      campo (eso es la Phase 43).
    - `git status --porcelain` muestra `42-WIRE-READ.md` como archivo nuevo y **no** muestra ningún
      archivo bajo `.planning/verification/captures/`.
  </acceptance_criteria>
  <done>La Phase 43 puede citar una lectura fresca fechada de `/instruments` y `/segments` desde un
  artefacto committeado, y el baseline del 2026-07-31 quedó marcado como no-autoritativo.</done>
</task>

</tasks>

<threat_model>
> `security_enforcement: true`, `security_asvs_level: 1`, `security_block_on: "high"`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API de market-data (Auth0) → payload crudo en memoria del driver | El wire trae datos reales, potencialmente con PII |
| payload crudo → `captures/` (gitignored) vs. `42-WIRE-READ.md` (committeado) | Frontera de datos: sólo cruza a git el `schema_of` (claves + tipos) |
| `.env` de `market-data-client` → credenciales de Auth0 | Nunca se transcriben en logs, reportes ni tests (C-4) |
| baseline write-once (`schemas/`) → corrida de hoy | El baseline es un compromiso histórico; la corrida no puede reescribirlo (D-25) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-05 | Information Disclosure | `42-WIRE-READ.md` committeado | mitigate | Sólo entra `schema_of(raw)` (claves + nombres de tipo, PII-free por construcción), conteos y rutas. El `payload` crudo del envelope se lee pero **nunca** se transcribe; `<acceptance_criteria>` exige que cada hoja del JSON transcrito sea un nombre de tipo |
| T-42-14 | Information Disclosure | Payload crudo escrito por `capture()` | mitigate | `capture()` escribe exclusivamente bajo `.planning/verification/captures/`, que está en `.gitignore:53`. `<verify>` asevera `git status --porcelain -- .planning/verification/captures/` vacío |
| T-42-15 | Tampering | Baseline write-once reescrito por la corrida | mitigate | `_write_schema_snapshot` no se modifica; `<verify>` asevera que los dos baselines siguen con `captured_at` del `2026-07-31` tras la corrida (D-25) |
| T-42-10 | Repudiation | Evidencia fresca sin fecha adentro del archivo, o inexistente en otro clone | mitigate | Envelope con `captured_at` UTC ISO (D-08 mitad 1) + artefacto committeado `42-WIRE-READ.md` que sobrevive a otro worktree y a `git clean -xdf` (D-08 mitad 2) |
| T-42-16 | Tampering | Fabricar la captura a mano si el probe falló | mitigate | `<action>` de la Task 2 lo prohíbe explícitamente y manda escalar; `<verify>` asevera `captured_at` de hoy, que sólo la corrida real produce |
| T-42-02 | Tampering | `verification/mutation_gate.py` / mutaciones de market-data | mitigate | `MARKET_DATA_VERIFY_MUTATING` no se setea en ninguna invocación; byte-identidad del gate verificada en el `<verify>` de las Tasks 1 y 2 |
| T-42-13 | Elevation of Privilege | Tráfico en vivo antes de la autorización humana | mitigate | La Task 2 lee `42-01-SUMMARY.md` primero y DETIENE si el checkpoint `gate="blocking-human"` no está aprobado y transcrito |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | Esta fase no instala ningún paquete (`42-RESEARCH.md § Package Legitimacy Audit`: N/A); verificado en el gate de cierre (plan 42-06) |
</threat_model>

<verification>
1. El checkpoint del plan 42-01 está aprobado y transcrito verbatim en `42-01-SUMMARY.md`.
2. `uv run --frozen ruff check .` y `ruff format --check .` verdes; `uv run --frozen mypy` `Success`.
3. Los 8 locks de `verification/test_main_market_data_*.py` verdes (baseline en HEAD: 27 passed).
4. Las dos capturas existen con `captured_at` de hoy; los dos baselines siguen en `2026-07-31`.
5. `git status --porcelain -- .planning/verification/captures/` vacío.
6. `42-WIRE-READ.md` existe con la marca `NO AUTORITATIVO` y el schema de los dos endpoints.
7. `git hash-object verification/mutation_gate.py` = `6bdaec006cc16f7c8dbfac41701712a9085c691b`.
</verification>

<success_criteria>
- La corrida dejó en disco una lectura fresca del wire de `/instruments` y `/segments`, fechada en
  esta sesión.
- Existe un artefacto **committeado** y PII-free que la Phase 43 puede citar sin depender del
  working tree del ejecutor.
- El baseline del 2026-07-31 queda explícitamente marcado como no-autoritativo para SHAPE-01.
- El payload crudo no entró a git y el mecanismo write-once del baseline quedó intacto.
</success_criteria>

<output>
Create `.planning/phases/42-re-chequeos-en-vivo-dns-de-higyrus-port-del-gate-de-venue-ce/42-04-SUMMARY.md` when done.

El SUMMARY debe incluir, además del formato estándar:
- El `captured_at` verbatim de los dos envelopes y el `n_rows` de cada endpoint.
- La confirmación de que los dos baselines siguen en `2026-07-31` (write-once respetado).
- El conteo de bloques `### F-` nuevos apendeados al ledger de market-data.
- El delta de claves contra el baseline, o la declaración de que es vacío.
</output>
